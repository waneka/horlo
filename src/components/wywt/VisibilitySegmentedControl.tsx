'use client'

// Placeholder — implementation lands in Phase 15 Plan 03b Task 3.
import type { JSX } from 'react'
import type { WearVisibility } from '@/lib/wearVisibility'

export function VisibilitySegmentedControl(_props: {
  value: WearVisibility
  onChange: (v: WearVisibility) => void
  disabled?: boolean
}): JSX.Element {
  return null as unknown as JSX.Element
}
