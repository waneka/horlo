'use client'

// Placeholder — full implementation lands in Phase 15 Plan 03b Task 2.
import type { JSX } from 'react'
import type { Watch } from '@/lib/types'

export function WywtPostDialog(_props: {
  open: boolean
  onOpenChange: (v: boolean) => void
  ownedWatches: Watch[]
  viewerId: string
}): JSX.Element {
  return null as unknown as JSX.Element
}
