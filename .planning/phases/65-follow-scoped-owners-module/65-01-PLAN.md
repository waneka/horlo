---
phase: 65-follow-scoped-owners-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/follows.ts
  - tests/data/getFollowedOwnersForCatalog.test.ts
autonomous: true
requirements:
  - FOLL-02
  - FOLL-04
tags:
  - dal
  - follows
  - privacy
  - sql

must_haves:
  truths:
    - "D-02 / D-11: Module exports a typed FollowedOwner row { userId, username, displayName, avatarUrl } from src/data/follows.ts — identical projection to CatalogCollector, but a distinct symbol (not re-exported)."
    - "D-05 / FOLL-04: getFollowedOwnersForCatalog applies BOTH profileSettings.profilePublic = true AND profileSettings.collectionPublic = true in the SQL WHERE; a follow does NOT override either gate."
    - "D-05a: getFollowedOwnersForCatalog excludes the viewer from their own result (sql`${profiles.id} != ${viewerId}`)."
    - "D-05b: getFollowedOwnersForCatalog restricts watches.status to ('owned','wishlist','grail') — 'sold' rows are NOT counted."
    - "D-06: A dedicated function getFollowedOwnersForCatalog exists; getCollectorsForCatalog is unchanged (no viewerFollowingOnly flag added to discovery.ts)."
    - "D-07 / FOLL-02 / Pitfall 1: The DAL uses exactly one INNER JOIN on follows with (followerId = viewerId, followingId = profiles.id) — the viewer-as-follower → owner-as-followee direction. NOT mutual; NOT reversed."
    - "D-08: ORDER BY desc(watches.createdAt), asc(profiles.username) (recency of ownership; username as deterministic tie-breaker)."
    - "FOLL-04 / Pitfall 3: Single SELECT joining follows ⋈ watches ⋈ profiles ⋈ profileSettings; rows are overfetched to LIMIT 50 and JS-deduped by userId (no N+1; multi-row-per-user collapses to one chip)."
    - "FOLL-04 / Pitfall 4: A separate count(DISTINCT profiles.id)::int query with the IDENTICAL WHERE + JOIN set returns totalCount for the overflow caption."
    - "D-12: tests/data/getFollowedOwnersForCatalog.test.ts mirrors the 6 privacy/sort/dedup tests in tests/data/getCollectorsForCatalog.test.ts AND adds Test 7 (viewer does NOT follow → owner excluded) and Test 8 (one-way follow → owner included, proves NOT mutual-only)."
  artifacts:
    - path: "src/data/follows.ts"
      provides: "FollowedOwner type + getFollowedOwnersForCatalog DAL function (extends the existing follows DAL module)"
      contains: "export interface FollowedOwner"
    - path: "src/data/follows.ts"
      provides: "getFollowedOwnersForCatalog implementation with the follows-direction join + count(DISTINCT) + JS dedup"
      contains: "export async function getFollowedOwnersForCatalog"
    - path: "tests/data/getFollowedOwnersForCatalog.test.ts"
      provides: "DAL integration tests (8 cases) covering FOLL-02 + FOLL-04 contract"
      contains: "maybe('getFollowedOwnersForCatalog"
  key_links:
    - from: "src/data/follows.ts"
      to: "@/db/schema (follows, profiles, profileSettings, watches)"
      via: "Drizzle innerJoin chain with the new follows-direction join"
      pattern: "innerJoin\\(\\s*follows"
    - from: "tests/data/getFollowedOwnersForCatalog.test.ts"
      to: "@/data/follows.getFollowedOwnersForCatalog"
      via: "dynamic import in beforeAll (matches getCollectorsForCatalog.test.ts pattern)"
      pattern: "import\\('@/data/follows'\\)"
---

<objective>
Ship the new DAL function `getFollowedOwnersForCatalog(catalogId, viewerId, { limit })` in `src/data/follows.ts` and its full integration-test mirror in `tests/data/getFollowedOwnersForCatalog.test.ts`. The function returns the top-5 (by `watches.createdAt DESC`) public collectors who own/wishlist/grail this catalog ref AND whom the viewer follows, plus a true `totalCount` for the "+N more" caption.

Purpose: Phase 65 FOLL-02 + FOLL-04 are realized by this DAL. Component-only plans (02) and integration plans (03) consume the typed contract; without this plan no caller has anything to import.

Output: A standalone, RSC-safe (`server-only`-gated by the module's existing top-of-file import) DAL function plus a passing test file that proves the 4 privacy edges + follow-direction gate + dedup + ordering contract — establishing the single-query, no-N+1, privacy-respecting read path mandated by FOLL-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-follow-scoped-owners-module/65-CONTEXT.md
@.planning/phases/65-follow-scoped-owners-module/65-RESEARCH.md
@.planning/phases/65-follow-scoped-owners-module/65-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from the codebase. -->
<!-- The DAL is structurally a near-clone of getCollectorsForCatalog with ONE added INNER JOIN. -->
<!-- Use these signatures directly — no codebase exploration needed. -->

From src/data/discovery.ts (the STRUCTURAL TEMPLATE — DO NOT modify this file in Plan 01):

```typescript
export interface CatalogCollector {
  userId: string
  username: string
  displayName: string | null
  avatarUrl: string | null
}

export async function getCollectorsForCatalog(
  catalogId: string,
  viewerId: string,
  opts: { limit?: number } = {},
): Promise<{ collectors: CatalogCollector[]; totalCount: number }>
```

From src/db/schema.ts (existing tables — Plan 01 introduces NO schema change):
- follows: { id, followerId, followingId, createdAt } with index follows_follower_idx on followerId and unique constraint follows_unique_pair on (followerId, followingId)
- profiles: { id, username, displayName, avatarUrl, bio, ... }
- profileSettings: { userId, profilePublic, collectionPublic, wishlistPublic, ... }
- watches: { id, userId, catalogId (nullable), status, brand, model, createdAt, ... }

From src/data/follows.ts (top of file — imports already present):
- `import 'server-only'`
- `import { and, desc, eq, inArray, or, sql } from 'drizzle-orm'`
- `import { db } from '@/db'`
- `import { follows, profiles, profileSettings, watches } from '@/db/schema'`

The new DAL needs the `asc` helper added to the drizzle-orm import (used for the username tie-breaker sort).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FollowedOwner type + getFollowedOwnersForCatalog DAL function to src/data/follows.ts</name>
  <files>src/data/follows.ts</files>
  <read_first>
    - src/data/follows.ts (current state — confirm `server-only` import on line 1 and that follows/profiles/profileSettings/watches are already imported on line 7; identify a natural insertion point AFTER the existing follow-list helpers and BEFORE the taste-overlap section so the new function sits in a coherent section per D-06)
    - src/data/discovery.ts lines 35-136 (the EXACT structural template — getCollectorsForCatalog body: select shape, three innerJoin chain, where clause with two privacy gates + self-exclusion + status filter, orderBy(desc(watches.createdAt), asc(profiles.username)), .limit(50), the second count(DISTINCT profiles.id)::int query with the IDENTICAL where clause, and the Set-based dedup loop)
    - src/db/schema.ts lines 238-251 (confirm follows columns: followerId, followingId; confirm follows_follower_idx index makes the new join cheap)
    - .planning/phases/65-follow-scoped-owners-module/65-RESEARCH.md (Pattern 1 lines 222-315 — the exact reference implementation including the ONE added INNER JOIN; Pitfalls 1-4 for failure modes)
    - .planning/phases/65-follow-scoped-owners-module/65-PATTERNS.md (Pattern Assignments section for src/data/follows.ts — confirms `asc` must be added to drizzle-orm import; confirms WHERE clause must apply to BOTH the main query AND the count query)
    - .planning/phases/65-follow-scoped-owners-module/65-CONTEXT.md (D-05, D-05a, D-05b, D-06, D-07, D-08, D-11)
  </read_first>
  <behavior>
    - Test 1 (FOLL-02): Given (viewer, alice) both seeded as public users with collectionPublic=true AND a watches row alice→catalogX (status='owned') AND a follows row (followerId=viewer.id, followingId=alice.id), the DAL returns owners containing alice and totalCount=1.
    - Test 2 (FOLL-02 Pitfall 1): Given (viewer, bob) seeded but NO follows row from viewer to bob, even when bob owns catalogX the DAL excludes bob and totalCount=0.
    - Test 3 (FOLL-02 NOT mutual): Given (viewer → alice) follow but NOT (alice → viewer), the DAL still includes alice (proves the join is one-way, not mutual-only).
    - Test 4 (D-05 layer 1): A user with profilePublic=false that the viewer follows AND who owns catalogX is excluded; totalCount excludes them.
    - Test 5 (D-05 layer 2 — load-bearing): A user with profilePublic=true but collectionPublic=false that the viewer follows AND who owns catalogX is excluded; totalCount excludes them. (Proves follow does NOT override either privacy gate.)
    - Test 6 (D-05a): Even if the viewer follows themselves (or seeds a self-row for symmetry), the DAL never returns the viewer in owners; totalCount excludes them.
    - Test 7 (D-05b): A user the viewer follows whose only watches row for catalogX has status='sold' is excluded; totalCount excludes them.
    - Test 8 (D-08 ordering): Two followed owners with different watches.createdAt values appear in DESC order (most recent first).
    - Test 9 (Pitfall 3 dedup): A followed user with BOTH an 'owned' and a 'wishlist' row for the same catalog appears exactly once in owners; totalCount = 1 (count(DISTINCT)).
  </behavior>
  <action>
    Extend `src/data/follows.ts` (do NOT rewrite the file — add to it) with one new exported interface `FollowedOwner` and one new exported async function `getFollowedOwnersForCatalog`, per D-06 (dedicated new function — getCollectorsForCatalog stays untouched), D-11 (type lives in follows.ts, NOT re-exported from discovery.ts), and the Pattern Assignments in 65-PATTERNS.md.

    Concrete identifiers to introduce:
    - `export interface FollowedOwner { userId: string; username: string; displayName: string | null; avatarUrl: string | null }` — projection shape matches CatalogCollector but is a distinct symbol (D-11).
    - `export async function getFollowedOwnersForCatalog(catalogId: string, viewerId: string, opts: { limit?: number } = {}): Promise<{ owners: FollowedOwner[]; totalCount: number }>` — strict (NON-null) catalogId per D-01a / RESEARCH Open Q4; null-handling is the call site's responsibility (Plan 03).
    - Default `limit = opts.limit ?? 5` (matches D-04b and getCollectorsForCatalog default).

    Query shape — clone getCollectorsForCatalog's main select EXACTLY (select fields {userId, username, displayName, avatarUrl, addedAt: watches.createdAt}; .from(watches); .innerJoin(profiles, eq(profiles.id, watches.userId)); .innerJoin(profileSettings, eq(profileSettings.userId, profiles.id))), then add ONE additional innerJoin clause AFTER the profileSettings join:
    - `.innerJoin(follows, and(eq(follows.followerId, viewerId), eq(follows.followingId, profiles.id)))` — viewer-as-follower → owner-as-followee (FOLL-02 / Pitfall 1 mnemonic).

    WHERE clause must include ALL FIVE conjuncts (D-05, D-05a, D-05b):
    1. eq(watches.catalogId, catalogId)
    2. eq(profileSettings.profilePublic, true)
    3. eq(profileSettings.collectionPublic, true)
    4. sql`${profiles.id} != ${viewerId}`
    5. inArray(watches.status, ['owned', 'wishlist', 'grail'])

    ORDER BY: desc(watches.createdAt), asc(profiles.username) — username as a deterministic tie-breaker (D-08). Note: this requires adding `asc` to the existing drizzle-orm import on line 4 (currently `import { and, desc, eq, inArray, or, sql } from 'drizzle-orm'` — extend to include `asc`).

    LIMIT 50 (Pitfall 3 overfetch).

    Count query: issue a separate `db.select({ count: sql<number>\`count(DISTINCT ${profiles.id})::int\` }).from(watches)…` chaining the SAME three innerJoins (profiles, profileSettings, follows) AND the SAME WHERE clause as the main query. Read `totalRows[0]?.count ?? 0` into `totalCount`. The follows innerJoin MUST appear in the count query too — RESEARCH.md and 65-PATTERNS.md both require IDENTICAL JOIN+WHERE for privacy consistency.

    JS dedup: clone the existing `seen = new Set<string>()` loop from discovery.ts lines 120-134 — iterate `rows`, skip if seen, push `{ userId, username, displayName, avatarUrl }` into `owners`, break when `owners.length >= limit`. Return `{ owners, totalCount }`.

    Add a JSDoc block above the function that cites: D-05/05a/05b (privacy + self-exclusion + status), D-06 (dedicated DAL — NOT a flag on getCollectorsForCatalog), D-07 / Pitfall 1 (viewer-as-follower → owner-as-followee direction; rejects mutual + reverse), D-08 (sort key), Pitfalls 3 + 4 (dedup + count(DISTINCT)). Use the comment style of discovery.ts:42-71 as the model.

    DO NOT modify `src/data/discovery.ts`. DO NOT add a `viewerFollowingOnly` flag to `getCollectorsForCatalog`. DO NOT add any new external import — only extend the drizzle-orm import on line 4 with `asc`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "src/data/follows\.ts" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "^export interface FollowedOwner" src/data/follows.ts` returns 1.
    - Source assertion: `grep -c "^export async function getFollowedOwnersForCatalog" src/data/follows.ts` returns 1.
    - Source assertion: The function signature on src/data/follows.ts (the line starting with `export async function getFollowedOwnersForCatalog`) reads exactly `export async function getFollowedOwnersForCatalog(catalogId: string, viewerId: string, opts: { limit?: number } = {}): Promise<{ owners: FollowedOwner[]; totalCount: number }> {`.
    - Source assertion: The drizzle-orm import line on src/data/follows.ts includes the symbol `asc` (e.g. `grep -E "^import \{[^}]*\\basc\\b[^}]*\} from 'drizzle-orm'" src/data/follows.ts` returns 1 match).
    - Source assertion: Inside the new function body, the follows innerJoin uses `eq(follows.followerId, viewerId)` AND `eq(follows.followingId, profiles.id)` — `grep -E "eq\\(follows\\.followerId,\\s*viewerId\\)" src/data/follows.ts` returns ≥ 1 AND `grep -E "eq\\(follows\\.followingId,\\s*profiles\\.id\\)" src/data/follows.ts` returns ≥ 1.
    - Source assertion: BOTH `eq(profileSettings.profilePublic, true)` and `eq(profileSettings.collectionPublic, true)` appear inside the new function (counted with `awk` between the function open brace and its matching close, OR by counting that these patterns each appear at least TWICE in the file — once in main query, once in count query).
    - Source assertion: `grep -c "sql\`count(DISTINCT \${profiles.id})::int\`" src/data/follows.ts` returns 1 (the new count query) — proves Pitfall 4 was honored, not skipped via `rows.length`.
    - Source assertion: `grep -c "inArray(watches.status, \\['owned', 'wishlist', 'grail'\\])" src/data/follows.ts` returns 2 (main query + count query).
    - Source assertion: The function returns the literal property name `owners` (not `collectors`): `grep -E "return \{ owners," src/data/follows.ts` returns 1.
    - Source assertion: `src/data/discovery.ts` is UNCHANGED — `git diff --stat src/data/discovery.ts` shows no change.
    - Behavior assertion (via Task 2 test file): all 9 behaviors in `<behavior>` pass when `DATABASE_URL` + Supabase env are present (locally: `set -a; source .env.local; set +a; npx vitest run tests/data/getFollowedOwnersForCatalog.test.ts`).
    - TypeScript: `npx tsc --noEmit` produces no NEW errors mentioning `src/data/follows.ts` (baseline noise from other files is permissible per the project_baseline_not_green_build_is_gate memory).
  </acceptance_criteria>
  <done>
    `src/data/follows.ts` exports `FollowedOwner` and `getFollowedOwnersForCatalog` with the contract above, both privacy gates + self-exclusion + status filter + follow-direction join + ORDER BY + LIMIT 50 + count(DISTINCT) + JS dedup all present, JSDoc cites the relevant D-XX decisions and Pitfalls, drizzle-orm import extended with `asc`, and `src/data/discovery.ts` is untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/data/getFollowedOwnersForCatalog.test.ts (8 integration tests, full FOLL-02 + FOLL-04 mirror)</name>
  <files>tests/data/getFollowedOwnersForCatalog.test.ts</files>
  <read_first>
    - tests/data/getCollectorsForCatalog.test.ts (ENTIRE file, lines 1-312 — the EXACT mirror template per D-12; copy seed helpers verbatim per RESEARCH Open Q1: copy-paste, do NOT extract)
    - tests/fixtures/users.ts (the seedTwoUsers helper used by getCollectorsForCatalog.test.ts beforeAll — confirm it returns { userA, userB, cleanup })
    - src/data/follows.ts (the new function from Task 1 — confirm signature is `(catalogId, viewerId, opts)` and return shape is `{ owners, totalCount }`)
    - .planning/phases/65-follow-scoped-owners-module/65-RESEARCH.md (lines 547-583 — the EXACT Test 7 / Test 8 implementations for the FOLL-02 follow-direction gate)
    - .planning/phases/65-follow-scoped-owners-module/65-PATTERNS.md (DAL test mirror section — confirms Tests 1-6 copy verbatim with substitutions; Tests 7-8 are additions; type-import line swaps `@/data/discovery` → `@/data/follows`)
  </read_first>
  <behavior>
    - Test file is gated with `maybe = hasDrizzle && hasSupabaseAdmin ? describe : describe.skip` so it skips silently in environments without DATABASE_URL + Supabase admin keys (matches the canonical pattern; allows CI to skip cleanly).
    - All 8 named tests (the 6 mirrored from getCollectorsForCatalog.test.ts plus the 2 new follow-direction tests from RESEARCH lines 547-583) execute the SAME high-level shape: seed catalog + watches rows + follows rows where needed, call `dal.getFollowedOwnersForCatalog(catalogId, viewer.id)`, assert presence/absence in `owners` and the expected `totalCount`.
    - Tests 1 (privacy layer 1) / 2 (privacy layer 2) / 3 (self-exclusion) / 4 (sold filter) / 5 (sort order) / 6 (dedup) mirror getCollectorsForCatalog.test.ts Tests 1-6 EXACTLY, with one structural addition per test: every user that is expected to APPEAR in the result must ALSO be seeded into `follows` as `(followerId: viewer.id, followingId: thatUser.id)` BEFORE the DAL call, OR the new follow-direction filter will exclude them and the test will fail for the wrong reason.
    - Test 7 (FOLL-02 viewer-does-NOT-follow → excluded): seeds bob owning catalogX BUT does NOT seed any (viewer → bob) follow row; asserts `owners.find(o => o.userId === bob.id) === undefined` AND `totalCount === 0`.
    - Test 8 (FOLL-02 one-way follow → included, proves NOT mutual-only): seeds alice owning catalogX AND seeds (viewer → alice) follow row but NOT (alice → viewer); asserts `owners.find(o => o.userId === alice.id)` is defined AND `totalCount === 1`.
  </behavior>
  <action>
    Create `tests/data/getFollowedOwnersForCatalog.test.ts` by copying the full structure of `tests/data/getCollectorsForCatalog.test.ts` and applying the substitutions below. The new file is ~320 lines and substantially clones the source; this is intentional (RESEARCH Open Q1 — copy > extract for two test files).

    Per-section substitutions:

    1. Header JSDoc (lines 1-21 of source): rewrite to reference Phase 65 + FOLL-02 + FOLL-04 + D-12. Cite the 8 tests by name (Tests 1-6 mirrored from Phase 39b NSV-18; Tests 7-8 NEW for the FOLL-02 follow-direction gate). Replace the DISC-11/NSV-18 references with the Phase 65 D-XX decision IDs (D-05, D-05a, D-05b, D-06, D-07, D-08, D-12) and Pitfall numbers from RESEARCH.md.

    2. Gating boilerplate (source lines 22-29): copy verbatim — `hasDrizzle` + `hasSupabaseAdmin` + `maybe = hasDrizzle && hasSupabaseAdmin ? describe : describe.skip`.

    3. describe block (source line 31): change suite name to `'getFollowedOwnersForCatalog — DAL integration'`.

    4. DalT type-import (source line 32): swap `typeof import('@/data/discovery')` → `typeof import('@/data/follows')`. Keep SchemaT + DbT unchanged.

    5. User fixtures + seedProfile / seedTestCatalogRow / seedWatchForCatalog helpers (source lines 51-119): copy VERBATIM. RESEARCH Open Q1 resolves to copy-paste, not extract.

    6. beforeAll (source lines 121-203): copy VERBATIM but ALSO seed the (viewer → alice) and (viewer → carol) follows rows during beforeAll for the tests that need them (or alternatively, seed follows inside each individual test next to the watches insertion — preferred for test isolation; mirrors how each existing test seeds its own catalog row).

    7. afterAll (source lines 205-208): copy VERBATIM (the cleanup function in beforeAll already wipes by user id; follows rows cascade-delete via the existing FK to users).

    8. Tests 1-6 (source lines 210-311): copy with the following substitutions per test:
       - Replace every `dal.getCollectorsForCatalog` → `dal.getFollowedOwnersForCatalog`.
       - Replace `{ collectors, totalCount }` destructure → `{ owners, totalCount }`.
       - Replace `collectors.find` → `owners.find`.
       - Replace `collectors.filter` → `owners.filter`.
       - Replace `collectors.length` → `owners.length`.
       - Replace `collectors[0].userId` / `collectors[1].userId` → `owners[0].userId` / `owners[1].userId`.
       - For Tests 1, 2, 3, 4: the EXPECTED-EXCLUDED user is the test's seeded private/sold/self user → the test must NOT seed a (viewer → that user) follow row (the follow-direction join would otherwise exclude them BEFORE the privacy gate, weakening the test's privacy-gate assertion). KEY: for these tests, do NOT add `await dbModule.db.insert(schema.follows)` — the absence of the follow row is incidental; the privacy/status/self gate is what the test proves.
       - For Test 5 (sort ordering) + Test 6 (dedup): the expected-INCLUDED users (bob+carol for Test 5, alice for Test 6) MUST be followed by the viewer — add `await dbModule.db.insert(schema.follows).values({ followerId: viewer.id, followingId: bob.id }).onConflictDoNothing()` etc. INSIDE the test body, BEFORE the DAL call. Without these inserts the new follow-direction join excludes them and the test fails.
       - Update each `it(...)` description to cite the Phase 65 D-XX decision (e.g. Test 1 → "Test 1: excludes private-profile users (D-05 layer 1 / T-39b-01 layer 1)").

    9. Add Test 7 + Test 8 from RESEARCH.md lines 549-583 VERBATIM (already in the FOLL-02 idiom; references `bob` + `alice` which are seeded in beforeAll; uses `dbModule.db.insert(schema.follows).values({...}).onConflictDoNothing()` for the follow insertion).

    All assertions must be exact: `expect(owners.find(...)).toBeUndefined()` for excluded users; `expect(owners.find(...)).toBeDefined()` and `expect(totalCount).toBe(N)` for included users.

    Do NOT extract seed helpers into a shared fixture file (RESEARCH Open Q1 explicitly resolves to copy-paste).
  </action>
  <verify>
    <automated>set -a; source .env.local 2>/dev/null; set +a; npx vitest run tests/data/getFollowedOwnersForCatalog.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `tests/data/getFollowedOwnersForCatalog.test.ts`.
    - Source assertion: `grep -c "^import { describe, it, expect, beforeAll, afterAll } from 'vitest'" tests/data/getFollowedOwnersForCatalog.test.ts` returns 1.
    - Source assertion: `grep -c "const maybe = hasDrizzle && hasSupabaseAdmin ? describe : describe.skip" tests/data/getFollowedOwnersForCatalog.test.ts` returns 1.
    - Source assertion: `grep -c "typeof import('@/data/follows')" tests/data/getFollowedOwnersForCatalog.test.ts` returns 1 (and `grep -c "typeof import('@/data/discovery')" tests/data/getFollowedOwnersForCatalog.test.ts` returns 0).
    - Source assertion: the file references `dal.getFollowedOwnersForCatalog` at least 8 times (one per test): `grep -c "dal.getFollowedOwnersForCatalog" tests/data/getFollowedOwnersForCatalog.test.ts` ≥ 8.
    - Source assertion: 8 `it(` invocations: `grep -cE "^\s+it\(" tests/data/getFollowedOwnersForCatalog.test.ts` ≥ 8.
    - Source assertion: Tests 7 and 8 are present and reference FOLL-02 in their it-descriptions: `grep -c "FOLL-02" tests/data/getFollowedOwnersForCatalog.test.ts` ≥ 2.
    - Behavior assertion: With env loaded (`set -a; source .env.local; set +a`), `npx vitest run tests/data/getFollowedOwnersForCatalog.test.ts` reports 8 passing tests (no skips beyond the env-gated suite-level skip).
    - Behavior assertion: Without env, the same command reports 0 failures (the `maybe = describe.skip` short-circuit fires).
    - Behavior assertion: Test 6 (dedup) returns `owners.filter(o => o.userId === alice.id).length === 1` (proves Pitfall 3 dedup is honored even with the new follow join).
    - Behavior assertion: Test 8 (one-way follow) seeds only the (viewer → alice) row and asserts alice IS included, proving the join is one-way not mutual-only.
    - Behavior assertion: Test 7 (no-follow) does NOT seed any follows row for (viewer → bob) and asserts bob is excluded with totalCount=0, proving the join filters correctly.
  </acceptance_criteria>
  <done>
    `tests/data/getFollowedOwnersForCatalog.test.ts` exists, contains the 6 mirrored tests + Tests 7 and 8, uses the env-gated `maybe` pattern, dynamically imports from `@/data/follows`, and passes 8/8 locally when `.env.local` is sourced. Without env it skips cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer → RSC | Untrusted catalogId (UUID-validated at the route in page.tsx line 149 before reaching the DAL); viewerId is session-derived via getCurrentUser() and is trusted. |
| RSC → DAL | viewerId + catalogId pass to the DAL as bound parameters via Drizzle (`eq`, `sql\`\``). No untrusted string concatenation. |
| DAL → DB | Service-role pooler bypasses RLS; the DAL WHERE clause is the privacy gate (per discovery.ts "Threat surface" comment). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Information Disclosure | getFollowedOwnersForCatalog SQL WHERE | mitigate | TWO privacy gates (`profilePublic = true` AND `collectionPublic = true`) MUST appear in BOTH the main query and the count query (D-05). A follow does NOT override either gate. Source assertion in Task 1 acceptance_criteria proves both patterns appear twice. |
| T-65-02 | Information Disclosure | Self-exclusion bypass via crafted viewerId | mitigate | `sql\`${profiles.id} != ${viewerId}\`` self-exclusion (D-05a) appears in BOTH main and count queries; viewerId is session-derived (trusted source) so a crafted-from-client value is not in scope. |
| T-65-03 | Tampering / SQL injection | catalogId / viewerId parameters | mitigate | Every value flows through Drizzle's `eq()` / `sql\`\`` template-literal interpolation → bound parameter, never raw SQL. No string concatenation. Mirrors discovery.ts:88-117 hardened pattern. |
| T-65-04 | Information Disclosure | Wrong follow direction (renders "your followers who own this") | mitigate | INNER JOIN clause MUST be `eq(follows.followerId, viewerId) AND eq(follows.followingId, profiles.id)` — viewer-as-follower → owner-as-followee (FOLL-02 / Pitfall 1). Test 8 in Task 2 seeds (viewer → alice) WITHOUT (alice → viewer) and asserts alice appears; Test 7 seeds bob with NO follow and asserts bob is excluded. Both tests fail loudly if the join is reversed. |
| T-65-05 | DoS / Information Disclosure | Unbounded result set on a popular catalog | accept | Main query has `.limit(50)` overfetch (Pitfall 3) and slice to top 5 (D-04b). Even a very popular catalog with thousands of public owners is bounded at 50 rows fetched + 5 returned. No DoS surface. |
| T-65-06 | Information Disclosure | Anonymous viewer (no auth) reaching the DAL | accept | The page route (Plan 03's integration point) is auth-gated via getCurrentUser() which throws UnauthorizedError for anon viewers (RESEARCH Assumption A2 — verified at src/lib/auth.ts:25-37). The DAL signature requires a non-null viewerId; if a future caller passes a null/empty viewerId the Drizzle binding would still execute safely (returning empty results because `profiles.id != ''` is true and the follows join would not match), but the code path is gated upstream. Out of scope at the DAL layer. |
</threat_model>

<verification>
- All FOLL-02 + FOLL-04 contract bullets in `must_haves.truths` are observable in the source via `grep` and in behavior via `npx vitest run tests/data/getFollowedOwnersForCatalog.test.ts`.
- `src/data/discovery.ts` is unchanged (git diff clean) — D-06 honored (dedicated function, not a flag).
- `npx tsc --noEmit` shows no NEW errors in `src/data/follows.ts` beyond pre-existing baseline noise.
- `npm run build` exits 0 (the authoritative gate — pre-existing baseline must remain green; new DAL is server-only and not bundled to client).
</verification>

<success_criteria>
- `src/data/follows.ts` exports `FollowedOwner` and `getFollowedOwnersForCatalog`.
- Both privacy gates (profilePublic + collectionPublic) AND self-exclusion AND status filter AND follow-direction join AND ORDER BY (desc createdAt + asc username) AND LIMIT 50 AND count(DISTINCT) query AND JS-Set dedup all present and tested.
- 8 integration tests pass locally with `.env.local` sourced; skip cleanly without env.
- `src/data/discovery.ts` untouched (git diff confirms).
- No new external dependencies added (only `asc` added to the existing drizzle-orm import).
</success_criteria>

<output>
After completion, create `.planning/phases/65-follow-scoped-owners-module/65-01-SUMMARY.md` documenting the new DAL function signature, the 5-conjunct WHERE clause + the new follow join, the count-query mirror, and a per-test pass/skip record from the `npx vitest run` output.
</output>
