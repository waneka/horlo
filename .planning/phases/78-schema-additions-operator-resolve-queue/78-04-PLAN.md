---
phase: 78
plan: 04
type: execute
wave: 3
depends_on: ["78-02", "78-03"]
files_modified:
  - .planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md
autonomous: false
requirements: [CANON-03, CANON-04]
tags: [prod-push, operator-action, migration-handoff]
must_haves:
  truths:
    - "`supabase db push --linked` succeeds against prod on the first attempt with no extension-schema portability surprises (CANON-03, CANON-04, ROADMAP success criterion #4)"
    - "After prod push, `brands.needs_review`, `watch_families.needs_review`, and `watch_families.aliases` columns exist on PROD Postgres with correct defaults"
    - "After prod push, `watch_families_aliases_gin_idx` exists on PROD"
    - "After prod push, all existing prod brand and watch_families rows backfill to `needs_review = false`"
    - "After prod push, all existing prod watch_families rows have `aliases = '{}'` (D-78-08 — empty everywhere)"
    - "Optional prod dry-run of `scripts/v8.4-brand-canonicalization.ts` against prod DATABASE_URL produces a comparable artifact shape (no Postgres extension errors)"
  artifacts:
    - path: ".planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md"
      provides: "Operator handoff document with prod-push checklist, verification SQL, rollback note"
      contains: "supabase db push --linked"
  key_links:
    - from: "supabase/migrations/20260624000000_phase78_aliases_needs_review.sql"
      to: "prod Postgres (Supabase pooler)"
      via: "supabase db push --linked"
      pattern: "supabase db push --linked"
---

<objective>
Hand the Plan 02 migration over to the operator for the prod push. This plan is `autonomous: false` — Claude prepares the post-deploy checklist; the operator runs `supabase db push --linked` against prod when ready. Once the operator confirms the push succeeded, Claude (in a follow-up session) re-runs prod introspection to confirm CANON-03 + CANON-04 + MIG-05 portability foundation are live on prod.

Purpose: ship the schema additions to prod. The migration file from Plan 02 has been verified locally; the prod push is the gate per CLAUDE.md "Local-First Development" and `[[drizzle-supabase-db-mismatch]]`. No data migration runs in this plan — Phase 79's `--apply` is the data work. This plan is the last Phase 78 task before phase close.

Output: a checked, operator-completed prod push of the Phase 78 columns and index; verification logs captured; Phase 78 ready to close.

Per the user's note in STATE.md: "260623-uua + Phase 76 still CODE-COMPLETE on `main` awaiting operator prod migration push per 76-POST-DEPLOY.md — these can be batched with the first v8.4 migration push." Plan 04 supports batching the three migrations (Phase 76 video schema + Phase 77 storage RLS + 260623-uua search + Phase 78 aliases/needs_review) in a single `supabase db push --linked` run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-CONTEXT.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-RESEARCH.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-02-SUMMARY.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-03-SUMMARY.md

<interfaces>
<!-- Push-target inputs -->

Plan 02 deliverables:
- `supabase/migrations/20260624000000_phase78_aliases_needs_review.sql` — hand-written portable SQL
- `drizzle/0014_phase78_aliases_needs_review.sql` — local mirror (NOT pushed to prod)

Plan 03 deliverables:
- `scripts/v8.4-brand-canonicalization.ts` — read-only; optionally dry-run against prod
- `.planning/v8.4-brand-merge-decisions.md` — committed artifact

Existing precedent: `76-POST-DEPLOY.md` (Phase 76 video schema operator handoff) — same pattern.
</interfaces>

<read_first>
- .planning/phases/78-schema-additions-operator-resolve-queue/78-02-SUMMARY.md — local push outcomes baseline
- .planning/phases/78-schema-additions-operator-resolve-queue/78-03-SUMMARY.md — script smoke-run outcomes
- supabase/migrations/20260624000000_phase78_aliases_needs_review.sql — the file being pushed
- CLAUDE.md `## Local-First Development` — verify-then-push gate
- STATE.md "Phase 76 P04" entry — precedent for the `76-POST-DEPLOY.md` pattern
- .planning/phases/78-schema-additions-operator-resolve-queue/78-RESEARCH.md (lines 757-820 Phase 78 Local Verification Sequence steps 11-12 — the prod-side verification; lines 698-705 — R-FIND-01..04 cross-reference map per W-78-04)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate 78-POST-DEPLOY.md operator checklist</name>
  <files>.planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md</files>
  <read_first>
    - .planning/phases/78-schema-additions-operator-resolve-queue/78-02-SUMMARY.md (psql introspection baselines)
    - .planning/phases/78-schema-additions-operator-resolve-queue/78-03-SUMMARY.md (script smoke outcomes)
    - supabase/migrations/20260624000000_phase78_aliases_needs_review.sql (the file being pushed)
  </read_first>
  <action>
    Create `.planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md` as the operator handoff document.

    **Required sections:**

    **# Phase 78 Prod Push — Operator Checklist**

    **## Pre-push state**
    - Migration files awaiting push:
      - `supabase/migrations/20260624000000_phase78_aliases_needs_review.sql` (Phase 78)
      - Plus any older un-pushed migrations: `20260622000000_phase76_video_schema.sql`, `20260623000000_phase77_storage_rls_poster_filename.sql`, `20260623200000_quick_260623_uua_search_unaccent_trgm.sql` (per STATE.md "operator prod migration push" note — confirm with `supabase migration list --linked` before pushing)
    - Local verification: ✅ (per 78-02-SUMMARY.md)
    - Local dry-run script smoke: ✅ (per 78-03-SUMMARY.md)

    **## Push command**
    ```bash
    supabase db push --linked
    ```

    Per `[[drizzle-supabase-db-mismatch]]`, this command pushes ALL migrations newer than the highest applied prod migration. If multiple migrations are queued (e.g. 76 + 77 + 260623-uua + 78), they apply in timestamp order: 20260622 → 20260623000000 → 20260623200000 → 20260624000000.

    Expected outcome: exit 0, with `supabase` logging "Applying migration 20260624000000_phase78_aliases_needs_review.sql" (along with any other queued migrations).

    **## Failure recovery**
    - If push fails on Phase 78 specifically:
      - Most likely cause: another developer concurrently pushed a competing 20260624000000_*.sql file. Run `supabase migration list --linked` to confirm. If so: rename the Phase 78 file to the next available timestamp (e.g. `20260624010000_phase78_aliases_needs_review.sql`) and retry.
      - Less likely cause: prod row count larger than expected causes the `ADD COLUMN ... DEFAULT '{}'` to time out. Per Pitfall 4 (Postgres 15+ metadata-only optimization), this should NOT happen on row counts <100k. If it does: investigate before retry.
    - If push fails on an earlier queued migration (76 / 77 / 260623-uua): see the respective phase's POST-DEPLOY.md (76-POST-DEPLOY.md is the precedent).
    - **DO NOT** modify `supabase/migrations/20260624000000_phase78_aliases_needs_review.sql` after the push starts. If retry is needed, use a NEW filename per gotcha #1 of `[[drizzle-supabase-db-mismatch]]`.

    **## Post-push verification (RUN ALL — capture output)**

    Connect to prod via the Supabase Dashboard SQL editor OR via `psql $PROD_DATABASE_URL`.

    1. **Column existence:**
       ```sql
       SELECT column_name, data_type, is_nullable, column_default
       FROM information_schema.columns
       WHERE table_schema = 'public'
         AND ((table_name = 'brands' AND column_name = 'needs_review')
           OR (table_name = 'watch_families' AND column_name IN ('aliases', 'needs_review')))
       ORDER BY table_name, column_name;
       ```
       Expected: 3 rows. `brands.needs_review` boolean NO `false`. `watch_families.aliases` ARRAY NO `'{}'::text[]`. `watch_families.needs_review` boolean NO `false`.

    2. **GIN index existence:**
       ```sql
       SELECT indexname, indexdef
       FROM pg_indexes
       WHERE schemaname = 'public'
         AND indexname = 'watch_families_aliases_gin_idx';
       ```
       Expected: 1 row; `indexdef` contains `USING gin (aliases)`.

    3. **Backfill defaults:**
       ```sql
       SELECT
         (SELECT count(*) FROM public.brands WHERE needs_review IS NOT FALSE) AS brands_bad,
         (SELECT count(*) FROM public.watch_families WHERE needs_review IS NOT FALSE) AS families_bad,
         (SELECT count(*) FROM public.watch_families WHERE aliases IS DISTINCT FROM '{}') AS aliases_bad;
       ```
       Expected: `brands_bad = 0`, `families_bad = 0`, `aliases_bad = 0`.

    4. **Natural-key constraint intact:**
       ```sql
       SELECT conname FROM pg_constraint WHERE conname = 'watches_catalog_natural_key';
       ```
       Expected: 1 row.

    5. **Optional — prod dry-run smoke:**
       ```bash
       PROD_DATABASE_URL=<prod-pooler-url> DATABASE_URL=$PROD_DATABASE_URL tsx scripts/v8.4-brand-canonicalization.ts --force
       ```
       Compare the generated `.planning/v8.4-brand-merge-decisions.md` against the local-generated one. Differences are expected (prod has more brands), but BOTH must:
       - Use the same column headers (D-78-01)
       - Have ZERO `auto-resolved` rows whose `proposed_target_id` is `null` (logic error)
       - Have Hamilton/Hamilton Watch, Héron/Héron Watches, and Brut Date/Brut Datejust cases (when present in prod catalog) land in `needs-review` (D-78-04)
       - Have any `Omega` + `OMEGA` case-pair (when present in prod catalog) BOTH land in `auto-resolved` sharing the same `proposed_target_id` — case-collapse per B-78-01 (D-78-04 exact-match on `name_normalized='omega'`)

    **## Sign-off**
    Operator: when all 5 verification steps pass, mark complete by checking the box below and committing:
    - [ ] Phase 78 prod push completed: YYYY-MM-DD
    - [ ] All 4+1 verification steps green
    - [ ] No data migrations triggered (Phase 79 owns data writes)

    **## What this push does NOT do** (forward-armor against scope creep):
    - Does NOT backfill brand_id / family_id (Phase 79 MIG-02 / MIG-03)
    - Does NOT flip NOT NULL on brand_id / family_id (Phase 80 CANON-01 / CANON-02)
    - Does NOT change `/api/extract-watch` behavior (Phase 80 INGEST-01..04)
    - Does NOT change recommender or display Server Actions (Phase 81)
    - Does NOT add UI surfaces (Phase 82)

    **## Roll-forward note** (if a rollback IS ever needed — extremely unlikely for additive ADD COLUMN):
    - The columns are additive; no DROP COLUMN script is provided. If a hard rollback becomes necessary, write a new migration `2026MMDDHHMMSS_phase78_rollback.sql` doing `ALTER TABLE ... DROP COLUMN IF EXISTS ... CASCADE`. Phase 79+ depends on these columns existing, so rollback also requires reverting Phase 79+ work. Do NOT roll back without a full forward-recovery plan.
  </action>
  <verify>
    <automated>test -f .planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md && grep -c "supabase db push --linked" .planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md` exists
    - Contains `supabase db push --linked` (the canonical push command)
    - Contains all 4 verification SQL snippets (column existence, GIN index existence, backfill defaults, natural-key constraint)
    - Contains the 5 verification steps in numbered form
    - Contains the explicit "what this push does NOT do" forward-armor list referencing Phase 79/80/81/82
    - Contains a roll-forward note (no rollback script provided; explicit rationale)
    - File is ≤300 lines (operators read these end-to-end; long checklists get skipped)
  </acceptance_criteria>
  <done>
    78-POST-DEPLOY.md committed; operator has a single document to follow for the prod push.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [CHECKPOINT — OPERATOR ACTION]: Run `supabase db push --linked` against prod</name>
  <what-built>
    Plan 02 hand-wrote `supabase/migrations/20260624000000_phase78_aliases_needs_review.sql` and verified it locally. Plan 03 verified the dry-run script runs against the new schema. Plan 04 Task 1 wrote the operator checklist at `.planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md`.

    This is the ONLY task in Phase 78 that requires the operator to take a manual action. `supabase db push --linked` is an operator-credential-gated CLI invocation that Claude cannot automate (per the GSD doctrine: actions requiring credentials Claude does not hold).
  </what-built>
  <how-to-verify>
    1. **Open `78-POST-DEPLOY.md`** and follow the checklist.
    2. **Confirm Supabase CLI is linked to prod:** `supabase status` should show the linked project. If not linked, run `supabase link --project-ref <ref>`.
    3. **Run the push:**
       ```bash
       supabase db push --linked
       ```
       Expected: exit 0 with one or more "Applying migration ..." log lines. The Phase 78 file (`20260624000000_phase78_aliases_needs_review.sql`) should be listed.
    4. **Run all 4 verification SQL snippets** from `78-POST-DEPLOY.md` against prod (via Dashboard SQL editor or `psql $PROD_DATABASE_URL`). Capture outputs.
    5. **(Optional) Run the prod dry-run script** per step 5 of the checklist to confirm `extensions.word_similarity` resolves correctly on prod (per R-FIND-02 Pitfall 1).
    6. **Report back:** paste the verification outputs in this session OR commit them to `78-POST-DEPLOY.md` under the "Sign-off" section.

    Expected outcomes:
    - Column existence query → 3 rows
    - GIN index query → 1 row
    - Backfill defaults query → all 3 counters = 0
    - Natural-key constraint query → 1 row
    - Optional prod dry-run → `.planning/v8.4-brand-merge-decisions.md` regenerates with similar shape (likely more `auto-resolved` rows than local since prod catalog is order-of-magnitude similar but different content)

    **If ANY verification fails:** STOP. Do NOT proceed to Phase 79. Capture the failure mode and surface it as a Phase 78 gap-closure plan candidate.
  </how-to-verify>
  <resume-signal>Operator pastes verification output here OR types `pushed and verified` after committing the sign-off to 78-POST-DEPLOY.md. Operator types `push-failed: <details>` to trigger gap-closure planning.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Capture operator verification output + update 78-POST-DEPLOY.md sign-off</name>
  <files>.planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md</files>
  <read_first>
    - .planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md (post-Task-1 state)
    - Operator's resume-signal text from Task 2
  </read_first>
  <action>
    After the operator's resume signal in Task 2:

    1. Append a new section `## Verification Output (captured YYYY-MM-DD)` to `78-POST-DEPLOY.md` containing the operator's raw verification SQL output (paste verbatim — do not re-format; readability matters less than reproducibility).
    2. Check the sign-off checkboxes:
       - [x] Phase 78 prod push completed: YYYY-MM-DD
       - [x] All 4+1 verification steps green
       - [x] No data migrations triggered (Phase 79 owns data writes)
    3. If the operator's optional prod dry-run produced a different `.planning/v8.4-brand-merge-decisions.md`, document the diff summary (`auto-resolved` count delta, `needs-review` count delta, any new SEED-021-class strings appearing in prod that didn't show locally). The committed `.md` on `main` is the local-generated one per Plan 03 Task 3; if the operator wants to commit the prod-generated artifact, that's their call documented inline (treat as advisory; Phase 79 input is whichever artifact the operator opens for editing).
  </action>
  <verify>
    <automated>grep -c "Verification Output" .planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `78-POST-DEPLOY.md` contains a `## Verification Output` section with raw psql outputs
    - All 3 sign-off checkboxes are checked
    - If push failed: a `## Push Failure` section captures the failure mode + recommended gap-closure approach
  </acceptance_criteria>
  <done>
    78-POST-DEPLOY.md is operator-completed; Phase 78 ready to close.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude→operator | Push command requires operator credentials (Supabase linked project). Claude cannot automate. |
| local→prod | First time the Phase 78 SQL touches prod; local-first gate (Plans 02 + 03) has already passed. |
| timestamp ordering→migration application | `supabase db push --linked` applies in timestamp order; Phase 78 file timestamp is 20260624000000. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04-01 | Tampering | Migration applied out of order (race with another contributor's push) | mitigate | `78-POST-DEPLOY.md` step 2 confirms `supabase migration list --linked` before push to catch competing 20260624 files |
| T-78-04-02 | Information Disclosure | Operator running prod dry-run with PROD_DATABASE_URL in shell history | accept | Operator vigilance per script header; D-78-06 read-only construction makes accidental capture low-impact |
| T-78-04-03 | Denial of Service | Long lock during ADD COLUMN against prod | accept | Per Pitfall 4: Postgres 15 + bare `'{}'` constant → metadata-only; expected <1s lock on ~205 row prod catalog |
| T-78-04-04 | Tampering | Prod missing `extensions.unaccent` or `extensions.pg_trgm` (script step 5) | mitigate | Already installed in prod via 260623-uua migration (verified during that quick task's prod push). 78-POST-DEPLOY.md step 5 is the canary if 260623-uua was somehow skipped |
| T-78-04-05 | Tampering | Plan 02 migration file modified after push starts | mitigate | 78-POST-DEPLOY.md explicit warning + gotcha #1 of `[[drizzle-supabase-db-mismatch]]` |
</threat_model>

<verification>
Phase 78 close gate:
- `78-POST-DEPLOY.md` exists with operator sign-off and verification SQL output
- `brands.needs_review`, `watch_families.needs_review`, `watch_families.aliases` columns live on PROD
- `watch_families_aliases_gin_idx` index live on PROD
- All prod backfill defaults verified (`needs_review = false`, `aliases = '{}'`)
- `watches_catalog_natural_key` UNIQUE constraint intact on PROD
- Optional prod dry-run script smoke confirms `extensions.word_similarity` resolves (validates R-FIND-02 / Pitfall 1 in prod)
</verification>

<success_criteria>
- ROADMAP success criterion #4 (MIG-05 portability foundation — first prod push succeeds; ordering + filename + additive shape validated end-to-end)
- Phase 78 closed; ROADMAP table updated; STATE.md "next phase" pointer flipped to Phase 79
</success_criteria>

<output>
After Task 3 completes (operator sign-off captured), create `.planning/phases/78-schema-additions-operator-resolve-queue/78-04-SUMMARY.md` confirming:
- Prod push date + applied migration list
- Verification outputs (or link to 78-POST-DEPLOY.md)
- Phase 78 ready for `/gsd-verify-work` then `/gsd-complete-phase`
</output>
