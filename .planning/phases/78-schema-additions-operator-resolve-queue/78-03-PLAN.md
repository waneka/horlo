---
phase: 78
plan: 03
type: execute
wave: 2
depends_on: ["78-01", "78-02"]
files_modified:
  - scripts/v8.4-brand-canonicalization.ts
  - package.json
  - .planning/v8.4-brand-merge-decisions.md
  - tests/integration/scripts/v8.4-brand-canonicalization.test.ts
  - tests/unit/scripts/v8.4-md-artifact-schema.test.ts
  - tests/unit/scripts/v8.4-seed021-golden.test.ts
  - tests/unit/scripts/v8.4-regenerate-merge.test.ts
  - tests/integration/scripts/v8.4-readonly.test.ts
autonomous: true
requirements: [MIG-01]
tags: [dry-run-script, postgres-lib, gfm-table, operator-queue]
must_haves:
  truths:
    - "Running `npm run db:v8.4-brand-canon` against local Supabase writes `.planning/v8.4-brand-merge-decisions.md` with one row per distinct `lower(trim(watches_catalog.brand))` value (D-78-01)"
    - "Three SEED-021 cases (Hamilton/Hamilton Watch, Héron/Héron Watches, Brut Date/Brut Datejust) land with `status: needs-review` (D-78-04). The Omega/OMEGA case-collapses to `auto-resolved` with both raw strings sharing the same `proposed_target_id` per B-78-01 — D-78-04's exact-match on `name_normalized='omega'` triggers auto-resolve; case-drift is surfaced via `/admin/brands` not the resolve queue."
    - "Running the script twice in a row WITHOUT `--regenerate` exits non-zero on the second run with an error message instructing the operator to use `--regenerate` or `--force` (D-78-07)"
    - "Running with `--regenerate` after operator edits rows to `merge:<uuid>` / `new` / `skip` preserves those rows verbatim and appends only NEW brand_raw strings as `needs-review` (D-78-07)"
    - "The script issues only SELECT statements — `count(*)` on brands, watch_families, watches_catalog AND `max(updated_at)` on brands are identical before/after a run (D-78-05)"
    - "Output rows include top 3 fuzzy candidates ≥0.5 in the `candidates / notes` cell formatted as `name (0.XX), name2 (0.XX)` per D-78-03"
    - "The status grammar in the emitted file matches D-78-02 exactly: `auto-resolved` | `merge:<uuid>` | `new` | `skip` | `needs-review` — no other values emitted by the script"
  artifacts:
    - path: "scripts/v8.4-brand-canonicalization.ts"
      provides: "Dry-run TS script — distinct catalog brands, exact-only auto-resolve, fuzzy candidates, GFM emission"
      contains: "extensions.word_similarity"
    - path: "package.json"
      provides: "npm script entry `db:v8.4-brand-canon` per Q2 recommendation"
      contains: "db:v8.4-brand-canon"
    - path: ".planning/v8.4-brand-merge-decisions.md"
      provides: "Generated GFM artifact (committed per Q1 recommendation)"
      contains: "| brand_raw | normalized | proposed_target_id | status | candidates / notes |"
  key_links:
    - from: "scripts/v8.4-brand-canonicalization.ts"
      to: "local Postgres via process.env.DATABASE_URL"
      via: "postgres({ max: 1, prepare: false }) — `scripts/inventory-explore-catalog.ts:36` pattern"
      pattern: "postgres\\(connStr"
    - from: "scripts/v8.4-brand-canonicalization.ts"
      to: "Phase 79's --apply parser"
      via: "GFM table grammar contract — D-78-01 columns + D-78-02 status grammar"
      pattern: "auto-resolved|merge:|needs-review"
---

<objective>
Build `scripts/v8.4-brand-canonicalization.ts` as a read-only dry-run that connects via service-role `DATABASE_URL`, scans `watches_catalog` for distinct brand strings, proposes exact-match auto-resolutions, pre-computes top 3 fuzzy candidates ≥0.5 for unresolved rows, and emits `.planning/v8.4-brand-merge-decisions.md` in the GFM-table format Phase 79's `--apply` will parse. Wire `--regenerate` merge-forward + refuse-to-overwrite guard. Add `npm run db:v8.4-brand-canon` entry per Q2.

Purpose: ship MIG-01 — the operator-resolve queue. The artifact this plan generates is the human-edit-then-Phase-79-consume contract. Every Phase 78 success criterion #3 condition is satisfied by this plan; the three distinct-normalization SEED-021 cases (Hamilton Watch / Héron Watches / Brut Datejust) landing in `needs-review` AND the Omega/OMEGA case-collapse landing in `auto-resolved` (per B-78-01) is the smoke test that proves D-78-04 exact-only auto-resolve is correctly implemented.

Output: working script + npm script entry + first generated `.md` artifact committed (per Q1 recommendation) so Phase 79 has a reproducible starting point.

Per R-FIND-02, the script's `word_similarity` calls MUST be schema-qualified as `extensions.word_similarity(...)` because the fresh `postgres`-lib connection does not inherit migration-time `SET search_path`. Per R-FIND-04, expected ~46 distinct brand strings → ~3-6 `needs-review` rows on local catalog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-CONTEXT.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-RESEARCH.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-01-PLAN.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-02-SUMMARY.md

<interfaces>
<!-- Reusable building blocks from existing scripts -->

From `scripts/inventory-explore-catalog.ts` (lines 14-37, 125-198):
- `import postgres from 'postgres'` — direct lib, NOT `src/db`
- Connection: `postgres(connStr, { max: 1, prepare: false })` — `prepare:false` MANDATORY for Supabase pooler
- Markdown emission: line-array pattern with `lines.push(...)` then `lines.join('\n')` — NO template engine
- Connection cleanup: `try { ... } finally { await sql.end() }`

From `scripts/backfill-catalog-brands.ts` (lines 25-156):
- `DISTINCT ON (brand_normalized) ORDER BY brand_normalized, brand ASC` pattern — collapses multiple raw spellings to one normalized row (per Q10 recommendation)

From `scripts/factual-apply.ts` (lines 200-208):
- `existsSync` gate for refuse-to-overwrite (Phase 78 D-78-07 inverts this — exit if EXISTS without --regenerate)

From `src/data/catalog.ts` (lines 511-566):
- DAL precedent for `word_similarity` + `f_unaccent` invocation — confirms `extensions.word_similarity` + `public.f_unaccent` schemas

From `package.json` lines 13-32 — existing `db:*` script convention:
- `"db:backfill-catalog-brands": "tsx --env-file=.env.local scripts/backfill-catalog-brands.ts"`
- New entry should follow this exact form

Existing Postgres types (already available via the schema, no new types needed):
- `brands.id` is UUID
- `brands.name_normalized` is GENERATED `lower(trim(name))`
- `watches_catalog.brand_normalized` is GENERATED `lower(trim(brand))`
- `extensions.word_similarity(text, text) → real` (installed by 260623-uua migration)
- `public.f_unaccent(text) → text` IMMUTABLE wrapper (installed by 260623-uua migration)
</interfaces>

<read_first>
- scripts/inventory-explore-catalog.ts (full file) — closest analog; copy env-bootstrap + connection + markdown emission patterns
- scripts/backfill-catalog-brands.ts (lines 1-160) — DISTINCT ON pattern + final assertion idiom; `db.execute(sql\`...\`)` vs `postgres` lib (this plan uses `postgres` lib per the inventory-explore precedent and D-78-06)
- scripts/factual-apply.ts (lines 200-220) — `existsSync` review-file gate pattern
- .planning/phases/78-schema-additions-operator-resolve-queue/78-RESEARCH.md (lines 366-500 Pattern 3 — skeleton script; lines 539-545 Pitfall 1 — extensions.word_similarity prefix; lines 575-672 Code Examples for all 4 stages; lines 698-705 — R-FIND-01..04 cross-reference map per W-78-04)
- .planning/phases/78-schema-additions-operator-resolve-queue/78-CONTEXT.md (D-78-01 GFM columns; D-78-02 status grammar; D-78-03 candidates format; D-78-04 exact-only; D-78-05 read-only; D-78-06 service-role DATABASE_URL; D-78-07 refuse-overwrite + --regenerate)
- supabase/migrations/20260623200000_quick_260623_uua_search_unaccent_trgm.sql (lines 80-108) — `public.f_unaccent` definition; confirms function lives in `public` schema, not `extensions`
- tests/integration/scripts/v8.4-brand-canonicalization.test.ts (from Plan 01) — stubs this plan must green
- tests/unit/scripts/v8.4-md-artifact-schema.test.ts (from Plan 01)
- tests/unit/scripts/v8.4-seed021-golden.test.ts (from Plan 01) — SEED-021 fixture cases
- tests/unit/scripts/v8.4-regenerate-merge.test.ts (from Plan 01)
- tests/integration/scripts/v8.4-readonly.test.ts (from Plan 01)
- package.json (lines 13-32) — npm script convention
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the dry-run script (4 stages + refuse-to-overwrite + --regenerate)</name>
  <files>scripts/v8.4-brand-canonicalization.ts, package.json</files>
  <behavior>
    Tests already exist as Wave 0 stubs (Plan 01). This task implements the script body so the stubs can be greened in Task 2.

    Required behavioral contract (drives implementation):
    - On invocation with no flags + no existing artifact: read distinct brands, exact-match auto-resolve, fuzzy candidates ≥0.5 for unresolved, write .md, exit 0.
    - On invocation with no flags + existing artifact: exit non-zero with error referencing `--regenerate` or `--force`.
    - On `--regenerate` + existing artifact: parse existing, preserve rows whose status is not `needs-review` verbatim, append new brand_raws as `needs-review`, exit 0.
    - On `--force`: overwrite unconditionally, exit 0.
    - Zero INSERT/UPDATE/DELETE in any code path.
    - Zero `extensions.` prefix omissions on `word_similarity` calls.
  </behavior>
  <read_first>
    - scripts/inventory-explore-catalog.ts (full) — env bootstrap + postgres lib + markdown emission
    - scripts/backfill-catalog-brands.ts (lines 50-160) — DISTINCT ON brand_normalized + name_normalized JOIN
    - scripts/factual-apply.ts (lines 200-220) — existsSync gate
    - .planning/phases/78-schema-additions-operator-resolve-queue/78-RESEARCH.md (lines 366-672 Pattern 3 + Code Examples)
  </read_first>
  <action>
    Create `scripts/v8.4-brand-canonicalization.ts`.

    **Header block (comment):**
    - Phase 78 MIG-01 (v8.4 Catalog Brand+Model Canonicalization)
    - "Usage (local default):" — `npm run db:v8.4-brand-canon` (uses `--env-file=.env.local` per npm script convention; honors `.env.development.local` override via shell since `DATABASE_URL` from `.env.development.local` is the local Supabase target per CLAUDE.md "Local-First Development")
    - "Usage (prod dry-run):" — `DATABASE_URL=<prod-pooler-url> tsx scripts/v8.4-brand-canonicalization.ts`
    - "Flags: --regenerate (merge operator decisions forward) | --force (overwrite unconditionally) | (none = refuse-to-overwrite per D-78-07)"
    - "WRITES: only `.planning/v8.4-brand-merge-decisions.md`. NEVER writes to the DB (D-78-05)."

    **Imports:**
    ```
    import { existsSync } from 'node:fs'
    import { readFile, writeFile, mkdir } from 'node:fs/promises'
    import * as path from 'node:path'
    import postgres from 'postgres'
    ```

    **Constants:**
    - `OUTPUT_FILE = path.join(process.cwd(), '.planning/v8.4-brand-merge-decisions.md')`
    - `FUZZY_THRESHOLD = 0.5` (per D-78-03)
    - `TOP_K = 3` (per D-78-03)

    **parseArgs():** flag set from `process.argv.slice(2)`. Return `{ regenerate: boolean, force: boolean }`.

    **main() flow:**
    1. **Refuse-to-overwrite gate (D-78-07):** if `existsSync(OUTPUT_FILE) && !args.regenerate && !args.force`, log error including the path and exit code 1. Error text MUST include both `--regenerate` and `--force` hints + tell the operator NOT to delete the file (per Pitfall 5).
    2. **Connection bootstrap:** read `process.env.DATABASE_URL`. If unset: error + exit 1. Connect via `postgres(connStr, { max: 1, prepare: false })`. Wrap remainder in `try { ... } finally { await sql.end() }`.
    3. **Stage 1+2 — Distinct catalog brands LEFT JOIN exact:**
       ```
       SELECT DISTINCT ON (wc.brand_normalized)
         wc.brand AS brand_raw,
         wc.brand_normalized,
         b.id AS proposed_target_id
       FROM public.watches_catalog wc
       LEFT JOIN public.brands b ON b.name_normalized = wc.brand_normalized
       ORDER BY wc.brand_normalized, wc.brand ASC
       ```
       Use the postgres lib tagged-template form `await sql<RowType[]>\`...\`` for type safety (per inventory-explore-catalog.ts:42 pattern).
    4. **Stage 3 — Fuzzy candidates ≥0.5 for unresolved rows:** for each row where `proposed_target_id IS NULL`, run a SECOND query (per-row loop, not IN-list — avoids `[[drizzle-sql-any-array-pitfall]]`):
       ```
       SELECT
         name_normalized,
         extensions.word_similarity(
           lower(public.f_unaccent(${r.brand_normalized})),
           lower(public.f_unaccent(name_normalized))
         ) AS score
       FROM public.brands
       WHERE extensions.word_similarity(
               lower(public.f_unaccent(${r.brand_normalized})),
               lower(public.f_unaccent(name_normalized))
             ) > 0.5
       ORDER BY score DESC
       LIMIT 3
       ```
       **CRITICAL (per R-FIND-02):** `extensions.word_similarity` MUST be schema-qualified; unqualified `word_similarity` fails with `42883 function does not exist` because the fresh postgres connection does not inherit migration-time `SET search_path`. Same applies to `extensions` in the `f_unaccent` invocation — but `f_unaccent` lives in `public` schema (per `20260623200000` migration line 99), so use `public.f_unaccent`. Store results in `candidatesByNormalized: Map<string, Array<{ name: string; score: number }>>`.
    5. **Stage 4 — classify status (D-78-04 exact-only):** for each row: `status = proposed_target_id ? 'auto-resolved' : 'needs-review'`. Compose `notes` cell: `candidates.map(c => \`${c.name} (${c.score.toFixed(2)})\`).join(', ')` — empty string if no candidates above threshold.
    6. **Cell sanitization:** before emitting any cell, run `.replace(/\|/g, '\\|')` defensively per RESEARCH Q6 — guards against `|` characters appearing in brand_raw breaking the GFM table parser.
    7. **Emit GFM table:**
       - Title: `# v8.4 Brand Merge Decisions`
       - Blank line
       - Subtitle/intro lines per RESEARCH Q4 example, including the generated date `new Date().toISOString().slice(0, 10)` and the line `Edit \`status\` cells to lock decisions: auto-resolved | merge:<uuid> | new | skip | needs-review` so the operator sees D-78-02 grammar inline
       - Blank line
       - Header row: `| brand_raw | normalized | proposed_target_id | status | candidates / notes |`
       - Separator row: `| --------- | ---------- | ------------------ | ------ | ------------------ |`
       - Data rows in `brand_normalized` ASC order (matching the SELECT)
    8. **--regenerate merge-forward (D-78-07):** if `args.regenerate && existsSync(OUTPUT_FILE)`, before emitting the fresh table, parse the existing artifact:
       - Read with `readFile(OUTPUT_FILE, 'utf8')`. Split lines. For each line starting with `|` and not the header (`| brand_raw`) and not the separator (`| ---`), split on `|`, trim cells. Index by `cells[1]` (brand_raw). If `cells[4]` (status) is NOT `needs-review`, store the entire raw line in `existingByBrandRaw: Map<string, string>`.
       - When emitting fresh rows: if `brand_raw` is in the preserved map, use the preserved raw line verbatim (preserves any operator edits in the notes column too); otherwise use the fresh-generated row.
       - This implements: non-`needs-review` rows preserved verbatim; `needs-review` rows overwritten with fresh state; new brand_raws (not in prior file) appended at the bottom of the table since they aren't in the preserved map.
    9. **Ensure `.planning/` exists:** `await mkdir(path.dirname(OUTPUT_FILE), { recursive: true })`.
    10. **Write artifact:** `await writeFile(OUTPUT_FILE, header + tableLines.join('\n') + '\n', 'utf8')`.
    11. **Log:** `console.log(\`[v8.4-brand-canon] wrote ${OUTPUT_FILE} (${rows.length} brand rows, ${needsReviewCount} needs-review, ${autoResolvedCount} auto-resolved)\`)`.

    **main().catch:** log fatal error + exit 1 (per inventory-explore-catalog.ts:200 pattern).

    **Export hook for tests:** extract pure-data functions (`buildTableRows(rows, candidatesByNormalized)`, `mergeForward(existing, fresh)`, `formatCell(value)`) as named exports so unit tests can call them without invoking `main()` / `process.exit`. Keep `main()` as the default-export wrapper that wires DB → pure functions → writeFile.

    **package.json edit:** add an entry to `scripts` (between existing `db:*` entries, alphabetical order):
    ```
    "db:v8.4-brand-canon": "tsx --env-file=.env.local scripts/v8.4-brand-canonicalization.ts",
    ```
    No trailing comma violations; JSON must remain valid.

    **Smoke test the script:** run `npm run db:v8.4-brand-canon` against local. Confirm exit 0 and `.planning/v8.4-brand-merge-decisions.md` exists. Then run again WITHOUT flags — confirm exit 1 with the expected error message. Run with `--regenerate` — confirm exit 0. Inspect the artifact: 3 SEED-021 cases (Hamilton Watch, Héron Watches, Brut Datejust) MUST land with `status: needs-review` (D-78-04); the Omega/OMEGA pair (if present) MUST land with `status: auto-resolved` sharing the same `proposed_target_id` (B-78-01 case-collapse).

    Local catalog SEED-021 verification SQL (run before script smoke test to confirm A3 from RESEARCH):
    ```
    docker exec supabase_db_horlo psql -U postgres -d postgres -c \
      "SELECT DISTINCT brand FROM watches_catalog WHERE lower(brand) IN ('omega','brut date','brut datejust','hamilton','hamilton watch','héron','héron watches') ORDER BY brand"
    ```
    Document which of the 4 SEED-021 strings actually appear in local seed in the SUMMARY (per Assumption A3 from RESEARCH).
  </action>
  <verify>
    <automated>npm run db:v8.4-brand-canon && test -s .planning/v8.4-brand-merge-decisions.md && grep -cE "needs-review" .planning/v8.4-brand-merge-decisions.md</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/v8.4-brand-canonicalization.ts` exists; contains `import postgres from 'postgres'` and `postgres(connStr, { max: 1, prepare: false })`
    - File exports named functions `buildTableRows`, `mergeForward`, `formatCell` (for unit testing)
    - File contains the literal string `extensions.word_similarity` (R-FIND-02 schema-qualification)
    - File contains the literal string `public.f_unaccent`
    - File does NOT contain `'\\<>INSERT' || '\\<>UPDATE' || '\\<>DELETE'` SQL keywords (D-78-05 enforcement) — verify via: `grep -ciE "(^|[^a-z])(INSERT|UPDATE|DELETE)[[:space:]]" scripts/v8.4-brand-canonicalization.ts | grep -v '^[[:space:]]*//'` returns 0 (regex skips comment lines per W-78-06; header comments mentioning these SQL keywords for documentation are not actual code paths)
    - `package.json` contains entry `"db:v8.4-brand-canon"` with value matching the `tsx --env-file=.env.local scripts/v8.4-brand-canonicalization.ts` form
    - `node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8'))"` exits 0 (valid JSON)
    - First run: `npm run db:v8.4-brand-canon` exits 0; `.planning/v8.4-brand-merge-decisions.md` exists and is non-empty
    - First-emitted file's header row is exactly `| brand_raw | normalized | proposed_target_id | status | candidates / notes |`
    - Second run (no flags): `npm run db:v8.4-brand-canon` exits non-zero; stderr contains both `--regenerate` and `--force`
    - Third run with `--regenerate`: exits 0
    - SEED-021 needs-review smoke test: `grep -E "(Hamilton Watch|Héron Watches|Brut Datejust)" .planning/v8.4-brand-merge-decisions.md | grep needs-review | wc -l` returns ≥1 hit per present case (D-78-04).
    - SEED-021 case-collapse smoke test (B-78-01 sanity gate): `grep -E "(Omega|OMEGA)" .planning/v8.4-brand-merge-decisions.md | grep auto-resolved | wc -l` returns ≥2 (one per case-variant; both share the same proposed_target_id per B-78-01).
    - Pre/post run row counts identical: `docker exec supabase_db_horlo psql -U postgres -d postgres -c "SELECT count(*) FROM brands"` returns the same number before and after a script run (D-78-05)
  </acceptance_criteria>
  <done>
    Script implemented; npm entry committed; first run writes artifact; refuse-to-overwrite + --regenerate both verified; 3 SEED-021 cases (Hamilton Watch / Héron Watches / Brut Datejust) land in needs-review per D-78-04, Omega/OMEGA case-collapses to auto-resolved per B-78-01; no DB mutation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Green the Plan 01 script Wave 0 stubs (5 files)</name>
  <files>tests/integration/scripts/v8.4-brand-canonicalization.test.ts, tests/unit/scripts/v8.4-md-artifact-schema.test.ts, tests/unit/scripts/v8.4-seed021-golden.test.ts, tests/unit/scripts/v8.4-regenerate-merge.test.ts, tests/integration/scripts/v8.4-readonly.test.ts</files>
  <read_first>
    - All 5 Plan 01 stub files (current state with `it.todo()`)
    - scripts/v8.4-brand-canonicalization.ts (from Task 1) — the exported pure functions for unit tests
    - .planning/v8.4-brand-merge-decisions.md (from Task 1 smoke run) — actual artifact content to assert against
    - tests/integration/migration-drop-archetype.test.ts (lines 1-100) — DATABASE_URL-gated integration test idiom
  </read_first>
  <action>
    Replace `it.todo()` callsites with real `it()` assertions across the 5 files:

    **`tests/integration/scripts/v8.4-brand-canonicalization.test.ts`** (DATABASE_URL-gated):
    1. Use Node `child_process` `execSync` (or `spawnSync`) to run `npx tsx scripts/v8.4-brand-canonicalization.ts` with env `DATABASE_URL` passed through. Use a temp artifact path override if the script accepts one; otherwise write to a temp dir via `process.cwd()` symlink approach is risky — instead, accept that the test will write to `.planning/v8.4-brand-merge-decisions.md` and use `beforeEach`/`afterEach` to back up + restore any pre-existing file (per Pitfall 5).
    2. Test 1: First run with `--force` (to bypass any pre-existing file) exits 0 and `.planning/v8.4-brand-merge-decisions.md` exists and contains the expected header row.
    3. Test 2: Second run WITHOUT flags exits non-zero; stderr contains `--regenerate`.
    4. Test 3: `--regenerate` run exits 0.

    **`tests/unit/scripts/v8.4-md-artifact-schema.test.ts`** (no DB gate; parses the actual emitted .md file or a fixture):
    1. Read `.planning/v8.4-brand-merge-decisions.md` if it exists (this test runs AFTER Task 1's smoke run in CI order; the file is committed) OR use the exported `buildTableRows` directly with a fixture.
    2. Test 1: First non-blank `|`-line is exactly `| brand_raw | normalized | proposed_target_id | status | candidates / notes |`.
    3. Test 2: Second `|`-line starts with `| ---`.
    4. Test 3: Every data row has exactly 6 pipe characters (5 cells + 2 boundaries) — `(line.match(/\|/g) || []).length === 6`.
    5. Test 4: Every status cell matches one of `auto-resolved | needs-review` (script never emits `merge:<uuid>`, `new`, or `skip` — those are operator-edited values).

    **`tests/unit/scripts/v8.4-seed021-golden.test.ts`** (no DB gate; uses imported `buildTableRows` with a fixture):

    Per D-78-04 + B-78-01 (checker feedback): the Omega/OMEGA case is asserted as a **case-collapse / auto-resolve** scenario — both raw catalog strings normalize to `omega` via `lower(trim())` and exact-match a single canonical brands row, so both legitimately land in `auto-resolved` per D-78-04 (exact match on `name_normalized` IS the auto-resolve condition; the operator queue surfaces it via the catalog view if drift is suspected). The remaining three SEED-021 cases (Hamilton, Héron, Brut Date) carry distinct `name_normalized` values, do NOT exact-match any brands row, and land in `needs-review`.

    1. Build a fixture array of distinct catalog brands containing the 4 SEED-021 strings + 1 known-good clean string. The fixture brands table contains ONLY a single canonical `Omega` row with `name_normalized = 'omega'`. Both `Omega` and `OMEGA` raw catalog strings normalize to `omega` and exact-match that row, so both rows are emitted from the SELECT DISTINCT ON pipeline with `proposed_target_id = 'uuid-omega'`:
       ```
       const fixtureRows = [
         { brand_raw: 'Hamilton', brand_normalized: 'hamilton', proposed_target_id: 'uuid-hamilton' },
         { brand_raw: 'Hamilton Watch', brand_normalized: 'hamilton watch', proposed_target_id: null },
         { brand_raw: 'Omega', brand_normalized: 'omega', proposed_target_id: 'uuid-omega' },
         { brand_raw: 'OMEGA', brand_normalized: 'omega', proposed_target_id: 'uuid-omega' },  // case-collapses to same canonical brands row
         { brand_raw: 'Héron Watches', brand_normalized: 'héron watches', proposed_target_id: null },
         { brand_raw: 'Brut Date', brand_normalized: 'brut date', proposed_target_id: null },
         { brand_raw: 'Brut Datejust', brand_normalized: 'brut datejust', proposed_target_id: 'uuid-brut' },
       ]
       ```
       Add an inline comment in the test file naming D-78-04 + B-78-01: "Omega/OMEGA exact-match the SAME `name_normalized='omega'` canonical row; D-78-04 (exact-only auto-resolve) applies. The case-mismatch is intentionally surfaced via Test 5 to verify the script does not split them across statuses."
    2. Call exported `buildTableRows(fixtureRows, new Map())` (empty candidates map for unit test).
    3. **Test 1 (Hamilton Watch — D-78-04):** row for `Hamilton Watch` has `status: needs-review`. Cite D-78-04 in the `it()` label.
    4. **Test 2 (Héron Watches — D-78-04):** row for `Héron Watches` has `status: needs-review`. Cite D-78-04 in the `it()` label.
    5. **Test 3 (Brut Date — D-78-04):** row for `Brut Date` has `status: needs-review`. Cite D-78-04 in the `it()` label.
    6. **Test 4 (candidates format — D-78-03):** build a candidates map with `{ 'hamilton watch': [{ name: 'hamilton', score: 0.85 }, { name: 'hamilton-khaki', score: 0.62 }] }`. Re-call `buildTableRows`. Assert the row's notes cell equals exactly `hamilton (0.85), hamilton-khaki (0.62)`.
    7. **Test 5 (Omega/OMEGA case-collapse — D-78-04 + B-78-01):** assert BOTH `Omega` and `OMEGA` rows from the fixture above land with `status: auto-resolved` AND with the SAME `proposed_target_id: 'uuid-omega'`. This explicitly documents the script's behavior: when two raw catalog strings normalize to the same `name_normalized` and exact-match a single canonical brands row, both auto-resolve to that row — the case-mismatch is a catalog-drift signal surfaced via the `/admin/brands` view (Phase 82), NOT via the operator-resolve queue. The `it()` label MUST cite both D-78-04 and B-78-01. Cover via two `expect()` calls on the same row pair: one for status equality across both rows, one for `proposed_target_id` equality across both rows.

    **`tests/unit/scripts/v8.4-regenerate-merge.test.ts`** (no DB gate; uses exported `mergeForward`):
    1. Create a fixture "existing" .md string with 3 operator-decided rows: `Hamilton | hamilton | uuid-h | merge:00000000-0000-0000-0000-000000000001 | ...`, `Acme | acme | | new | ...`, `Defunct | defunct | | skip | ...`, plus 2 needs-review rows.
    2. Create a fresh-generated rows array with all 5 of the above PLUS one new row `Tudor | tudor | uuid-t | auto-resolved | ...`.
    3. Call exported `mergeForward(existingMd, freshRows)`.
    4. Test 1: result includes the `merge:00000000-...` row verbatim.
    5. Test 2: result includes the `new` row verbatim.
    6. Test 3: result includes the `skip` row verbatim.
    7. Test 4: result OVERWRITES the 2 `needs-review` rows with the fresh-generated form.
    8. Test 5: result includes the new `Tudor` row at the bottom.

    **`tests/integration/scripts/v8.4-readonly.test.ts`** (DATABASE_URL-gated):
    1. `beforeAll`: connect via `postgres(connStr, { max: 1, prepare: false })`. Capture `SELECT count(*) FROM brands`, `count(*) FROM watch_families`, `count(*) FROM watches_catalog`, `max(updated_at) FROM brands`, `max(updated_at) FROM watch_families`.
    2. Run the script via `execSync('npx tsx scripts/v8.4-brand-canonicalization.ts --force')`.
    3. Capture the same 5 metrics again.
    4. Test 1: brands count unchanged.
    5. Test 2: watch_families count unchanged.
    6. Test 3: watches_catalog count unchanged.
    7. Test 4: brands max(updated_at) unchanged.
    8. Test 5: watch_families max(updated_at) unchanged.

    Remove the sanity `it()` from each stub once the real assertions are in place.

    Run `DATABASE_URL=$(grep -E '^DATABASE_URL=' .env.development.local | cut -d= -f2- | tr -d '"') npm run test -- tests/integration/scripts/v8.4 tests/unit/scripts/v8.4` and confirm all pass.
  </action>
  <verify>
    <automated>DATABASE_URL=$(grep -E '^DATABASE_URL=' .env.development.local | cut -d= -f2- | tr -d '"') npm run test -- tests/integration/scripts/v8.4-brand-canonicalization.test.ts tests/unit/scripts/v8.4-md-artifact-schema.test.ts tests/unit/scripts/v8.4-seed021-golden.test.ts tests/unit/scripts/v8.4-regenerate-merge.test.ts tests/integration/scripts/v8.4-readonly.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files report 0 todo, 0 failed; ≥3 passed each
    - `tests/unit/scripts/v8.4-seed021-golden.test.ts` reports 4+ passed including explicitly-named tests for Hamilton Watch, Héron Watches, Brut Date
    - `tests/integration/scripts/v8.4-readonly.test.ts` reports 5 passed (3 counts + 2 max(updated_at))
    - `tests/unit/scripts/v8.4-regenerate-merge.test.ts` reports 5 passed (merge/new/skip preservation, needs-review overwrite, new row append)
    - Re-running the full set is idempotent — the script can be smoke-run by tests without leaving state behind (artifact remains in place but tests handle backup/restore)
  </acceptance_criteria>
  <done>
    All 5 Plan 01 stub files greened; 0 failed; SEED-021 + readonly + merge-forward assertions all real and passing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit the generated artifact + run full Phase 78 local verification sequence</name>
  <files>.planning/v8.4-brand-merge-decisions.md</files>
  <read_first>
    - .planning/v8.4-brand-merge-decisions.md (current state from Task 1 smoke run)
    - .planning/phases/78-schema-additions-operator-resolve-queue/78-RESEARCH.md (lines 757-820 Phase 78 Local Verification Sequence — the 12-step gate)
  </read_first>
  <action>
    Per RESEARCH Q1 recommendation, commit `.planning/v8.4-brand-merge-decisions.md` to git so:
    - Phase 79's `--apply` has a reproducible starting input
    - The diff-history surfaces "why did the operator pick `merge:<uuid>` for Brand X"
    - The first artifact serves as a sanity baseline for future re-runs

    Confirm the file is NOT in `.gitignore` (open `.gitignore` and verify no rule excludes `.planning/v8.4-*.md`). If excluded, add an inverse rule `!.planning/v8.4-brand-merge-decisions.md` or remove the conflicting rule.

    Execute the 12-step Phase 78 Local Verification Sequence (per RESEARCH lines 757-820):
    1. `supabase db push` (local) — should be idempotent since Plan 02 already applied; expect "Already at the latest version"
    2. `npx drizzle-kit push` — idempotent; expect no plan
    3. psql `\d brands | grep needs_review` — expect 1 row
    4. psql `\d watch_families | grep -E "aliases|needs_review"` — expect 2 rows
    5. psql `pg_indexes WHERE indexname = 'watch_families_aliases_gin_idx'` — expect 1 row
    6. psql `count(*) WHERE needs_review IS NOT FALSE` on both tables — expect 0 + 0
    7. psql `pg_constraint WHERE conname = 'watches_catalog_natural_key'` — expect 1 row
    8. `npm run db:v8.4-brand-canon --force` (force because file now committed) — expect exit 0
    9. `test -s .planning/v8.4-brand-merge-decisions.md` — expect 0 exit
    10. `grep -E "(Hamilton Watch|Héron Watches)" .planning/v8.4-brand-merge-decisions.md | grep needs-review` — expect ≥1 hit per case present in prod catalog (D-78-04). Followed by case-collapse sanity (B-78-01): `grep -E "(Omega|OMEGA)" .planning/v8.4-brand-merge-decisions.md | grep auto-resolved | wc -l` returns ≥2 if both Omega-case variants are present in prod catalog (B-78-01).
    11. Pre/post snapshot of brands count + max(updated_at): identical
    12. `npm run test -- tests/static/phase78 tests/integration/migrations/78 tests/integration/scripts/v8.4 tests/unit/scripts/v8.4` — 0 failed
    13. `npm run build` — exit 0

    Capture each step's output in the SUMMARY.md.

    Do NOT execute step 12 of the original RESEARCH sequence (`supabase db push --linked` against prod) — that is Plan 04's job (operator-gated).
  </action>
  <verify>
    <automated>git status .planning/v8.4-brand-merge-decisions.md 2>&1 | grep -E "(new file|modified)" && npm run test -- tests/static/phase78 tests/integration/migrations/78 tests/integration/scripts/v8.4 tests/unit/scripts/v8.4</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v8.4-brand-merge-decisions.md` is tracked by git (`git ls-files .planning/v8.4-brand-merge-decisions.md` returns the path) OR is staged for the next commit
    - File is NOT excluded by `.gitignore`
    - All 12 verification steps complete with expected outcomes (documented in SUMMARY)
    - `npm run build` exits 0
    - Full Phase 78 test set (`tests/static/phase78` + `tests/integration/migrations/78` + `tests/integration/scripts/v8.4` + `tests/unit/scripts/v8.4`) reports 0 failed
    - Artifact has ≥1 `auto-resolved` row AND ≥1 `needs-review` row (sanity: local catalog has both clean and dirty brand strings per RESEARCH Q9)
  </acceptance_criteria>
  <done>
    Artifact committed; full Phase 78 local verification sequence green; ready for Plan 04 operator prod push.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script→Postgres | Service-role connection via DATABASE_URL; bypasses RLS so reads see every row. No writes by D-78-05. |
| operator-edited `.md`→Phase 79 parser | Phase 78 emits strict GFM; Phase 79 enforces D-78-02 status grammar. Cross-phase contract documented in artifact header. |
| dev→prod DATABASE_URL accident | Operator could export prod URL and run the script. Read-only by construction, so no data corruption, but artifact would reflect prod data not local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-03-01 | Tampering | Pipe character `\|` in brand_raw breaking GFM table | mitigate | Task 1 step 6: defensive `.replace(/\|/g, '\\|')` on every cell value before emission |
| T-78-03-02 | Information Disclosure | DATABASE_URL leaking into logs | mitigate | Script never logs `connStr`; error paths use `process.exit(1)` with generic message per inventory-explore-catalog.ts pattern |
| T-78-03-03 | Tampering | Accidental DB mutation in a future edit | mitigate | acceptance_criteria includes `grep -ciE "(INSERT|UPDATE|DELETE)"` returning 0 — gates any future re-introduction of writes |
| T-78-03-04 | Tampering | Operator accidentally deletes `.md` between dry-run and operator edit | mitigate | Pitfall 5 — error message tells operator NOT to delete the file; --regenerate is the safe recovery path |
| T-78-03-05 | Spoofing | Phase 79 parser accepting a malformed `merge:<not-a-uuid>` | accept | Phase 79's parser is the gate per D-78-02; Phase 78 only commits to NOT emit malformed values itself. Forward-armor: Plan 01's stub `v4.regenerate-merge.test.ts` includes a regex guard for the UUID shape on `merge:` rows |
| T-78-03-06 | Information Disclosure | Cross-env URL confusion (running against prod when intending local) | accept | D-78-06 — read-only by construction makes accidental prod-target a no-op on data; artifact reflects prod data but is dev-time only (not deployed). Operator vigilance per script header |
</threat_model>

<verification>
After all 3 tasks:
- `scripts/v8.4-brand-canonicalization.ts` exists; runs against local Supabase
- `npm run db:v8.4-brand-canon` returns exit 0 on first run, exit non-zero on second (refuse-to-overwrite)
- `.planning/v8.4-brand-merge-decisions.md` committed to git
- SEED-021 cases present in local catalog all land in `needs-review` (D-78-04 verified)
- All 7 Plan 78 test files green: 0 failed, 0 todo
- Pre/post run row counts + max(updated_at) on brands, watch_families, watches_catalog identical (D-78-05 verified)
- `npm run build` exits 0
- Phase 78 ready for Plan 04 prod push handoff
</verification>

<success_criteria>
- ROADMAP success criterion #3 (MIG-01 dry-run script writes `.planning/v8.4-brand-merge-decisions.md` listing every distinct brand value with auto-resolved or needs-review markers — without touching any DB row)
- D-78-01 through D-78-07 all implemented (D-78-08 honored in Plan 02)
- Phase 79's `--apply` has a reproducible input contract
</success_criteria>

<output>
After completion, create `.planning/phases/78-schema-additions-operator-resolve-queue/78-03-SUMMARY.md` including:
- The actual number of distinct brand strings in local catalog (per RESEARCH A1 verification)
- Which of the 4 SEED-021 strings actually appear in local seed (per A3 verification)
- The count of `auto-resolved` vs `needs-review` rows in the first-generated artifact
- Confirmation that all 5 Plan 01 stub files are now passing
- Confirmation that Plan 04 has a ready-to-push migration file
</output>
