---
phase: 68
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/watch/ConfirmStep.tsx
  - src/components/watch/ConfirmStep.test.tsx
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, CONF-05, CONF-06, CONF-07, CONF-08, CONF-09, CONF-10]
tags: [react, presenter-component, aria-radiogroup, tdd]

must_haves:
  truths:
    - "D-01: New file src/components/watch/ConfirmStep.tsx ships as a sibling of VerdictStep.tsx; VerdictStep.tsx is NOT modified or deleted in Phase 68 (Phase 70 owns the cutover); co-located test file src/components/watch/ConfirmStep.test.tsx ships alongside"
    - "D-02: ConfirmStep is a named export (no default export); top-of-file 'use client' directive is present"
    - "D-03: Pure presenter — ConfirmStep has zero useState, zero fetch, zero Server Action import, zero router.push; the locked ConfirmStepProps interface is implemented verbatim (props in, callbacks out)"
    - "D-04: Status picker uses <div role='radiogroup'> with three <button role='radio' aria-checked={status === value}> children; the codebase shadcn Tabs primitive is NOT used here"
    - "D-05: Inline lucide-react Star icon (size-4, aria-hidden) renders INSIDE the Grail option button immediately before the 'Grail' text; Owned and Wishlist options are text-only"
    - "D-06: ConfirmStep accepts separate catalogImageUrl and extractedImageUrl optional props; the fallback chain priority is catalog → extracted → lucide WatchIcon placeholder"
    - "D-07: Reference renders as a plain <Input>; productionYear renders as <Input type='number'> with blank-string-to-undefined onChange parsing; inputs sit in a 2-col grid (grid-cols-1 gap-3 sm:grid-cols-2)"
    - "D-08: Reference input is ENABLED on catalog-bound rows; the Phase 67 D-10 server-override invariant is documented in the JSDoc on the reference prop (year is NOT overridden by Phase 67 D-10)"
    - "D-09: 'Edit details' button is variant='ghost' and emits onEditDetails callback only; ConfirmStep does NOT decide inline-expand vs route-change (Phase 70 owns that)"
    - "D-10: CTA_LABELS = { owned: 'Add to Collection', wishlist: 'Add to Wishlist', grail: 'Save as Grail' } is declared at module scope; the primary CTA renders CTA_LABELS[status]; the 'Start over' button is variant='ghost' disabled when pending"
    - "D-11: status is a strict required prop with no internal default — initialStatus resolution is Phase 70's concern"
    - "The confirm screen shows the catalog cover photo, then extracted imageUrl, then a watch-icon placeholder — in that fallback order (CONF-01)"
    - "Choosing 'Owned' in the picker shows a 'Price paid' field; choosing 'Wishlist' or 'Grail' shows a 'Target price' field; 'Sold' does not appear as an option (CONF-03, CONF-06)"
    - "The 'Grail' picker option renders a lucide Star icon inline; its visual weight matches the other options (CONF-04)"
    - "The primary CTA label updates dynamically: 'Add to Collection', 'Add to Wishlist', or 'Save as Grail' per the selected status (CONF-08)"
    - "Clicking 'Start over' emits onStartOver; clicking 'Edit details' emits onEditDetails — parent owns destination (CONF-07, CONF-09)"
    - "Status is a required controlled prop with no internal default — ConfirmStep does not own initialStatus resolution (CONF-10, D-11)"
    - "Brand and model render read-only by default; reference and year render as inline-editable inputs (CONF-02, CONF-05)"
  artifacts:
    - path: "src/components/watch/ConfirmStep.tsx"
      provides: "Pure-presenter ConfirmStep component"
      exports: ["ConfirmStep"]
      contains: "function ConfirmStep"
      min_lines: 180
    - path: "src/components/watch/ConfirmStep.test.tsx"
      provides: "15 unit test cases (a)-(o) covering all CONF requirements"
      contains: "describe('ConfirmStep"
      min_lines: 250
  key_links:
    - from: "src/components/watch/ConfirmStep.tsx"
      to: "lucide-react"
      via: "named imports"
      pattern: "import.*\\{[^}]*Star[^}]*\\}.*from 'lucide-react'"
    - from: "src/components/watch/ConfirmStep.tsx"
      to: "next/image"
      via: "default import"
      pattern: "import Image from 'next/image'"
    - from: "src/components/watch/ConfirmStep.tsx"
      to: "@/components/ui/button"
      via: "named import"
      pattern: "import \\{ Button \\} from '@/components/ui/button'"
    - from: "src/components/watch/ConfirmStep.test.tsx"
      to: "@/components/watch/ConfirmStep"
      via: "named import"
      pattern: "import \\{ ConfirmStep \\} from '@/components/watch/ConfirmStep'"
---

<objective>
Ship `src/components/watch/ConfirmStep.tsx` — a pure-presenter confirm-screen component — plus its co-located test file `src/components/watch/ConfirmStep.test.tsx` covering 15 cases (a)-(o) per 68-CONTEXT D-Discretion.

Purpose: Phase 70's `AddWatchFlow` rewrite will mount ConfirmStep in place of today's `VerdictStep` to deliver the lighter, status-picker-bearing confirm screen the v8.0 add-flow redesign requires (CONF-01..CONF-10). ConfirmStep ships dormant in Phase 68 — no route mounts it; Phase 70 is the consumer.

Output: Two new files, both green under `npm run test -- src/components/watch/ConfirmStep.test.tsx`. `VerdictStep.tsx`, `AddWatchFlow.tsx`, and `WatchForm.tsx` are NOT modified (Phase 70 owns those).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/68-confirmstep-component/68-CONTEXT.md
@.planning/phases/68-confirmstep-component/68-RESEARCH.md
@.planning/phases/68-confirmstep-component/68-PATTERNS.md
@.planning/phases/68-confirmstep-component/68-UI-SPEC.md
@.planning/phases/68-confirmstep-component/68-VALIDATION.md
@src/components/watch/VerdictStep.tsx
@src/components/watch/VerdictStep.test.tsx
@src/components/watch/WatchForm.tsx
@src/components/settings/PrivacyToggleRow.tsx
@src/components/watch/WatchPhotoSection.tsx

<interfaces>
<!-- Key contracts the executor needs. Sourced from the codebase + 68-CONTEXT D-03 locked contract. -->
<!-- Executor uses these directly — no codebase exploration needed for these symbols. -->

From src/lib/types.ts:
```typescript
export type WatchStatus = 'owned' | 'wishlist' | 'sold' | 'grail'
```
ConfirmStep's `status` prop is the 3-element subset `'owned' | 'wishlist' | 'grail'` — `'sold'` is intentionally excluded (CONF-03).

From src/components/ui/button.tsx:
```typescript
export const buttonVariants  // CVA with variants: default | outline | ghost | secondary | destructive | link
export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>, VariantProps<typeof buttonVariants> { asChild?: boolean }
export function Button({ className, variant, size, asChild, ...props }): JSX.Element
```
Defaults: `variant="default"` produces the primary CTA appearance; `variant="outline"` is the radiogroup base; `variant="ghost"` is "Edit details" / "Start over".

From src/components/ui/input.tsx:
```typescript
export function Input({ className, type, ...props }: React.ComponentProps<'input'>): JSX.Element
```
Pass-through of native input props. Use `value`, `onChange`, `id`, `placeholder`, `type` directly.

From src/components/ui/label.tsx:
```typescript
export function Label({ className, ...props }: React.ComponentProps<typeof LabelPrimitive.Root>): JSX.Element
```
Pair with `htmlFor` matching the Input `id`.

From src/components/ui/card.tsx:
```typescript
export function Card({ className, ...props }): JSX.Element
export function CardContent({ className, ...props }): JSX.Element
```

From lucide-react (verified present in package.json):
- `Star` (outline star icon — no `StarFilled` variant exists; see 68-RESEARCH Pitfall 5)
- `Loader2` (spinner used in pending CTA)
- `Watch` (aliased to `WatchIcon` per WatchPhotoSection precedent)

From @/lib/utils:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From @/lib/constants:
```typescript
export const MOVEMENT_LABELS: Record<MovementType, string>
```
Used only inside the optional `SpecHeadline` private helper.

LOCKED PROP CONTRACT (68-CONTEXT D-03 — DO NOT modify shape, names, or types):
```typescript
interface ConfirmStepProps {
  catalogImageUrl?: string | null
  extractedImageUrl?: string | null
  brand: string
  model: string
  reference: string | null | undefined
  onReferenceChange: (value: string) => void
  productionYear: number | undefined
  onProductionYearChange: (value: number | undefined) => void
  status: 'owned' | 'wishlist' | 'grail'
  onStatusChange: (next: 'owned' | 'wishlist' | 'grail') => void
  price: number | undefined
  onPriceChange: (value: number | undefined) => void
  onPrimary: () => void
  onEditDetails: () => void
  onStartOver: () => void
  pending?: boolean
  // Optional spec props per 68-UI-SPEC §SpecHeadline Helper (recommendation (a))
  movement?: string | null
  caseSizeMm?: number | null
  dialColor?: string | null
}
```

MODULE-SCOPE CONSTANTS (68-CONTEXT D-10 + 68-UI-SPEC §Module-Scope Constant):
```typescript
const CTA_LABELS = {
  owned: 'Add to Collection',
  wishlist: 'Add to Wishlist',
  grail: 'Save as Grail',
} as const

const OPTIONS: Array<{ value: 'owned' | 'wishlist' | 'grail'; label: string }> = [
  { value: 'owned',    label: 'Owned' },
  { value: 'wishlist', label: 'Wishlist' },
  { value: 'grail',    label: 'Grail' },
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): RED — write failing test scaffold ConfirmStep.test.tsx with all 15 cases (a)-(o)</name>
  <files>src/components/watch/ConfirmStep.test.tsx</files>
  <read_first>
    - src/components/watch/VerdictStep.test.tsx (test file template — copy mock pattern, beforeEach, describe/it grouping, fireEvent + screen.getByRole shape)
    - .planning/phases/68-confirmstep-component/68-CONTEXT.md §Claude's Discretion (full case list (a)-(o) — DO NOT abbreviate, all 15 ship now)
    - .planning/phases/68-confirmstep-component/68-RESEARCH.md §"Test File Structure" + §"Code Examples → Full Prop Interface" (locked BASE_PROPS shape)
    - .planning/phases/68-confirmstep-component/68-PATTERNS.md §5 "ConfirmStep.test.tsx — adaptations from VerdictStep.test.tsx"
    - .planning/phases/68-confirmstep-component/68-UI-SPEC.md §Copywriting Contract (exact button names, label strings) + §Accessibility Contract (aria attribute assertions)
  </read_first>
  <behavior>
    Test file MUST contain exactly 15 `it(...)` blocks covering cases (a)–(o):
    - (a) `catalogImageUrl` set + `extractedImageUrl` also set → catalog wins (CONF-01 priority)
    - (b) only `extractedImageUrl` set → extracted renders (CONF-01)
    - (c) neither image set → WatchIcon placeholder renders, queryable via accessible role or test marker (CONF-01)
    - (d) picker shows exactly 3 elements with `role="radio"`, names 'Owned' / 'Wishlist' / 'Grail'; NO element with name 'Sold' (CONF-03)
    - (e) the `role="radio"` button named "Grail" contains a Star icon as its child; "Owned" and "Wishlist" do NOT (CONF-04)
    - (f) clicking the "Owned" radio fires `onStatusChange('owned')` exactly once; with `status='owned'` the CTA text is "Add to Collection" (CONF-08)
    - (g) with `status='wishlist'` the CTA text is "Add to Wishlist" (CONF-08)
    - (h) with `status='grail'` the CTA text is "Save as Grail" (CONF-08)
    - (i) `status='owned'` → label text "Price paid" present, "Target price" absent; `status='wishlist'` → "Target price" present; `status='grail'` → "Target price" present (CONF-06)
    - (j) typing into the reference input fires `onReferenceChange` with the typed string (CONF-05)
    - (k) typing "2020" into the year input fires `onProductionYearChange(2020)` (number); clearing it fires `onProductionYearChange(undefined)` (CONF-05)
    - (l) clicking the "Edit details" button fires `onEditDetails` exactly once (CONF-07)
    - (m) clicking the "Start over" button fires `onStartOver` exactly once (CONF-09)
    - (n) `pending=true` → primary CTA is disabled; the visible text includes "Saving..."; "Edit details" + "Start over" are also disabled (mirrors VerdictStep)
    - (o) status='owned' → the "Owned" radio has `aria-checked="true"` and "Wishlist" / "Grail" radios have `aria-checked="false"`; rerender with status='grail' → "Grail" radio has `aria-checked="true"` and the others "false" (CONF-03 a11y)
  </behavior>
  <action>
    Create `src/components/watch/ConfirmStep.test.tsx`. Use `import { describe, it, expect, vi, beforeEach } from 'vitest'` and `import { render, screen, fireEvent } from '@testing-library/react'`. NO `@vitest-environment node` directive — jsdom default is correct (this is a DOM presenter, not a filesystem walker).

    Add the `next/image` mock BEFORE the component import (per 68-PATTERNS §5a + Pitfall 7): `vi.mock('next/image', () => ({ default: (p: { src: string; alt: string }) => <img src={p.src} alt={p.alt} /> }))`.

    Import the component under test: `import { ConfirmStep } from '@/components/watch/ConfirmStep'`. This import is intentionally RED at this task — Wave 1 ships the component.

    Declare a `BASE_PROPS` constant with all 13 required props at safe defaults (brand='Omega', model='Speedmaster', reference='311.30.42.30.01.005', productionYear=undefined, status='wishlist' as const, price=undefined) and all 7 callbacks as `vi.fn()` (onReferenceChange, onProductionYearChange, onStatusChange, onPriceChange, onPrimary, onEditDetails, onStartOver). Add `beforeEach(() => { vi.clearAllMocks() })` in each describe block (mirrors VerdictStep.test.tsx:44).

    Group cases into 6 describe blocks per 68-PATTERNS §5c grouping:
    1. `describe('ConfirmStep — cover photo (CONF-01)')` → cases (a)(b)(c)
    2. `describe('ConfirmStep — status picker (CONF-03/04/08)')` → cases (d)(e)(f)(g)(h)(o)
    3. `describe('ConfirmStep — price field (CONF-06)')` → case (i)
    4. `describe('ConfirmStep — inline inputs (CONF-05)')` → cases (j)(k)
    5. `describe('ConfirmStep — action affordances (CONF-07/09)')` → cases (l)(m)
    6. `describe('ConfirmStep — pending state')` → case (n)

    Query rules:
    - Radiogroup options: `screen.getByRole('radio', { name: 'Owned' })` etc. (the radio role MUST be queryable — assert via getByRole, not getByText, since the picker is a radiogroup per D-04).
    - "Sold" absence: `expect(screen.queryByRole('radio', { name: /sold/i })).not.toBeInTheDocument()`.
    - Primary CTA: `screen.getByRole('button', { name: /Add to Collection|Add to Wishlist|Save as Grail|Saving/ })` — use the specific exact string per status.
    - Ghost buttons: `screen.getByRole('button', { name: 'Edit details' })` / `'Start over'`.
    - aria-checked: `expect(btn).toHaveAttribute('aria-checked', 'true')` (string, not boolean).
    - Price label: `screen.getByText('Price paid')` / `screen.getByText('Target price')`.
    - Reference + year inputs: query by accessible name via `getByLabelText('Reference')` / `getByLabelText('Year')`.
    - Image cover branches: use the mocked `<img src={...} alt={...}>` — assert `screen.getByRole('img', { name: 'Omega Speedmaster' })` has the catalog URL when both are set; assert it has the extracted URL when only extracted is set; assert `screen.queryByRole('img')` is null when neither is set.
    - WatchIcon placeholder: the icon carries `aria-hidden`, so it is NOT queryable by role. Either render with a data-testid wrapper assertion (`screen.getByTestId('confirm-cover-placeholder')`) OR assert that no `<img>` element exists AND assert the placeholder container is present via a stable class assertion. Prefer giving the placeholder wrapper div a `data-testid="confirm-cover-placeholder"` and using `getByTestId` for case (c).

    Use literal strings — DO NOT use regex for label assertions. Copy is locked per 68-UI-SPEC §Copywriting Contract.

    No additional mocks beyond `next/image`. Do NOT mock `next/navigation`, `next/cache`, `CollectionFitCard`, or Server Actions — ConfirmStep imports none of them (D-03 pure-presenter contract).
  </action>
  <acceptance_criteria>
    - File exists: `test -f src/components/watch/ConfirmStep.test.tsx`
    - File contains EXACTLY 15 `it(` blocks: `grep -c '^\s*it(' src/components/watch/ConfirmStep.test.tsx` returns 15
    - File contains all 6 describe blocks: `grep -c "^describe('ConfirmStep" src/components/watch/ConfirmStep.test.tsx` returns 6
    - File has the next/image mock: `grep -c "vi.mock\\('next/image'" src/components/watch/ConfirmStep.test.tsx` returns 1
    - File imports from the under-test path: `grep -c "from '@/components/watch/ConfirmStep'" src/components/watch/ConfirmStep.test.tsx` returns 1
    - File does NOT mock CollectionFitCard: `grep -c "CollectionFitCard" src/components/watch/ConfirmStep.test.tsx` returns 0
    - File does NOT mock Server Actions: `grep -c "@/app/actions" src/components/watch/ConfirmStep.test.tsx` returns 0
    - File does NOT mock next/navigation: `grep -c "next/navigation" src/components/watch/ConfirmStep.test.tsx` returns 0
    - Test RED (expected at this task): `npm run test -- src/components/watch/ConfirmStep.test.tsx` exits non-zero with import-resolution failure on `@/components/watch/ConfirmStep` (this is the intended RED gate — Wave 1 turns it green)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^\s*it(" src/components/watch/ConfirmStep.test.tsx | grep -qx 15 && grep -q "vi.mock('next/image'" src/components/watch/ConfirmStep.test.tsx && grep -q "from '@/components/watch/ConfirmStep'" src/components/watch/ConfirmStep.test.tsx && echo "RED scaffold structure OK"</automated>
  </verify>
  <done>
    File `src/components/watch/ConfirmStep.test.tsx` exists; contains 15 `it(` blocks across 6 `describe` blocks; imports the not-yet-existing `ConfirmStep` from `@/components/watch/ConfirmStep` (RED on test run); mocks only `next/image`; declares BASE_PROPS fixture with 13 props + 7 vi.fn() callbacks; case-list matches the 15 cases (a)-(o) above; ready for Wave 1 to ship the component that turns these tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): GREEN — implement ConfirmStep.tsx so all 15 test cases pass</name>
  <files>src/components/watch/ConfirmStep.tsx</files>
  <read_first>
    - src/components/watch/ConfirmStep.test.tsx (the RED tests from Task 1 — implement to satisfy these exact assertions)
    - src/components/watch/VerdictStep.tsx (primary structural template — copy aria-live root wrapper, Card cover-photo block tokens, Loader2 pending CTA pattern, mobile-first flex tokens, SpecHeadline helper signature)
    - src/components/watch/WatchForm.tsx:304-451 (price-gating + numeric-input blank-to-undefined idiom — copy verbatim, adapt to controlled `price`/`onPriceChange` props)
    - src/components/settings/PrivacyToggleRow.tsx:49-67 (aria-checked on raw button precedent — extend from 2-state role="switch" to 3-state role="radio" within role="radiogroup")
    - src/components/watch/WatchPhotoSection.tsx (WatchIcon empty-state precedent — `import { Watch as WatchIcon } from 'lucide-react'`; placeholder JSX `<div className="flex h-full w-full items-center justify-center"><WatchIcon className="h-16 w-16 text-muted-foreground/40" aria-hidden /></div>`)
    - .planning/phases/68-confirmstep-component/68-UI-SPEC.md §Component Tree + Container Hierarchy (authoritative section ordering — Cover → Inputs → Picker → Price → Ghost buttons → Primary CTA; ghost buttons ABOVE primary CTA per UI-SPEC clarification)
    - .planning/phases/68-confirmstep-component/68-UI-SPEC.md §Interaction Spec §Keyboard navigation — radiogroup (the WAI-ARIA 1.2 arrow-key/Home/End handler SHIPS in this phase per UI-SPEC, not deferred)
    - .planning/phases/68-confirmstep-component/68-CONTEXT.md §D-03 (locked prop contract — DO NOT modify shape) §D-08 (reference input stays enabled invariant — document in JSDoc) §D-10 (CTA_LABELS module-scope constant) §D-11 (no internal status default)
    - .planning/phases/68-confirmstep-component/68-RESEARCH.md §"Pattern 2: WAI-ARIA Radiogroup" (full handleKeyDown implementation) §Pitfall 1 (roving tabindex) §Pitfall 2 (aria-checked explicit boolean) §Pitfall 4 (alt fallback) §Pitfall 6 (confirm- prefixed IDs) §Pitfall 8 (aria-live="polite" on root)
    - src/lib/types.ts (WatchStatus union — confirm 'sold' is the type element ConfirmStep excludes from its restricted union)
  </read_first>
  <behavior>
    Component must satisfy ALL 15 RED assertions from Task 1:
    - (a)(b)(c) Cover photo fallback chain: catalogImageUrl → extractedImageUrl → WatchIcon placeholder (CONF-01)
    - (d) Status picker contains exactly 3 elements with `role="radio"` (Owned / Wishlist / Grail); no Sold (CONF-03)
    - (e) Grail option contains a Star icon child; Owned/Wishlist do not (CONF-04)
    - (f)(g)(h) Primary CTA label maps to status via CTA_LABELS (CONF-08)
    - (i) Price label is "Price paid" when isOwned, else "Target price" (CONF-06)
    - (j) reference input onChange fires onReferenceChange(value) (CONF-05)
    - (k) year input onChange fires onProductionYearChange(Number | undefined) (CONF-05)
    - (l) Edit details button onClick fires onEditDetails (CONF-07)
    - (m) Start over button onClick fires onStartOver (CONF-09)
    - (n) pending=true disables primary + both ghost buttons; primary shows Loader2 + "Saving..." text
    - (o) aria-checked on each radio is the string "true"/"false" (not undefined) matching status===value
  </behavior>
  <action>
    Create `src/components/watch/ConfirmStep.tsx` with `'use client'` directive at line 1 (D-02 — onClick handlers + controlled inputs + keyboard handler all require client surface).

    Imports (single import block; named imports only; absolute `@/` paths per CLAUDE.md CONVENTIONS):
    - `Image` from `next/image`
    - `{ Loader2, Star, Watch as WatchIcon }` from `lucide-react` (the `Watch as WatchIcon` alias matches WatchPhotoSection precedent — case (c) placeholder)
    - `{ Card, CardContent }` from `@/components/ui/card` (cover-photo block only — Sections 2–6 are siblings of the Card per 68-UI-SPEC §Container rationale)
    - `{ Button }` from `@/components/ui/button`
    - `{ Input }` from `@/components/ui/input`
    - `{ Label }` from `@/components/ui/label`
    - `{ MOVEMENT_LABELS }` from `@/lib/constants` (only used inside the optional SpecHeadline private helper)
    - `{ cn }` from `@/lib/utils` (selected-state class composition)

    DO NOT import: `CollectionFitCard`, `addWatch`, `useRouter`, `next/navigation`, `next/cache`, or anything from `@/app/actions/*`. Pure presenter (D-03). DO NOT use `useState`.

    Write the file-header JSDoc per 68-RESEARCH §Pattern 8 — name Phase 68 + decisions D-01..D-11 + the 6 rendered sections + the D-08 reference-input-stays-enabled invariant + the D-03 no-state/no-Server-Action/no-router.push invariant + the "CollectionFitCard NOT mounted here — verdict deliberately out of scope per PROJECT.md" invariant + the pending behavior summary.

    Declare module-scope constants BEFORE the component:
    - `const CTA_LABELS = { owned: 'Add to Collection', wishlist: 'Add to Wishlist', grail: 'Save as Grail' } as const` (D-10)
    - `const OPTIONS: Array<{ value: 'owned' | 'wishlist' | 'grail'; label: string }> = [ { value: 'owned', label: 'Owned' }, { value: 'wishlist', label: 'Wishlist' }, { value: 'grail', label: 'Grail' } ]` (the Star icon for Grail is rendered via JSX inside the .map, not stored in OPTIONS — per 68-UI-SPEC §Module-Scope Constant final paragraph)

    Declare the locked `ConfirmStepProps` interface (D-03) verbatim — see <interfaces> block above. The three optional spec props (movement?, caseSizeMm?, dialColor?) are added per 68-UI-SPEC §SpecHeadline Helper (recommendation a) — keep visual continuity with VerdictStep.

    Export the named function `function ConfirmStep(...)` with destructured props and `pending = false` default. Inside the function body, derive: `const brandModel = [brand, model].filter(Boolean).join(' ') || 'Watch'` (Pitfall 4 — `|| 'Watch'` guards against empty-alt a11y failure); `const isOwned = status === 'owned'`; `const coverUrl = catalogImageUrl ?? extractedImageUrl ?? null`.

    Implement the arrow-key roving-tabindex handler (per 68-UI-SPEC §Interaction Spec §Keyboard navigation — locked IN, not deferred). Inline `function handleKeyDown(e: React.KeyboardEvent<HTMLDivElement>)` inside the component body. Logic: declare `const values = ['owned', 'wishlist', 'grail'] as const`; find `const idx = values.indexOf(status)`; on `ArrowRight`/`ArrowDown` → `e.preventDefault(); onStatusChange(values[(idx + 1) % values.length])`; on `ArrowLeft`/`ArrowUp` → `e.preventDefault(); onStatusChange(values[(idx - 1 + values.length) % values.length])`; on `Home` → `e.preventDefault(); onStatusChange(values[0])`; on `End` → `e.preventDefault(); onStatusChange(values[values.length - 1])`. Space/Enter are handled by native `<button>` semantics — no extra branch needed.

    Render the 6 sections in order per 68-UI-SPEC §Component Tree:

    Section root: `<div className="space-y-6" aria-live="polite">` (Pitfall 8 — polite announcement of label changes).

    Section 1 — Cover photo + identity Card:
    - Outer: `<Card><CardContent className="flex items-start gap-4 p-6">`
    - Cover slot: ALWAYS render the `size-20 rounded-md bg-muted overflow-hidden flex-shrink-0` container (this differs from VerdictStep which conditionally skips the container).
    - Inside slot: ternary on `coverUrl`. If truthy → `<Image src={coverUrl} alt={brandModel} width={80} height={80} className="object-cover w-full h-full" unoptimized />`. If falsy → `<div data-testid="confirm-cover-placeholder" className="flex h-full w-full items-center justify-center"><WatchIcon className="h-16 w-16 text-muted-foreground/40" aria-hidden /></div>` (the `data-testid` is required so test case (c) can assert presence — the icon is `aria-hidden` and therefore not role-queryable).
    - Identity block: `<div className="space-y-1 min-w-0"><h2 className="text-base font-semibold text-foreground truncate">{brandModel}</h2><SpecHeadline movement={movement} caseSizeMm={caseSizeMm} dialColor={dialColor} /></div>`. NOTE: brand+model is the H2; reference is NOT rendered here as a `<p>` (it appears as an `<Input>` in Section 2).

    Section 2 — Inline-editable reference + year (outside the Card):
    - Wrapper: `<div className="space-y-3"><div className="grid grid-cols-1 gap-3 sm:grid-cols-2">`
    - Reference field: `<div className="space-y-2"><Label htmlFor="confirm-reference">Reference</Label><Input id="confirm-reference" value={reference ?? ''} onChange={(e) => onReferenceChange(e.target.value)} /></div>`
    - Year field: `<div className="space-y-2"><Label htmlFor="confirm-year">Year</Label><Input id="confirm-year" type="number" value={productionYear ?? ''} onChange={(e) => onProductionYearChange(e.target.value ? Number(e.target.value) : undefined)} /></div>`

    Section 3 — Status radiogroup picker:
    - Wrapper: `<div className="space-y-2"><Label htmlFor="confirm-status-group">Status</Label><div role="radiogroup" aria-label="Watch status" id="confirm-status-group" className="flex gap-2" onKeyDown={handleKeyDown}>`
    - Map over OPTIONS. For each `{ value, label }` render `<Button key={value} type="button" role="radio" aria-checked={status === value} tabIndex={status === value ? 0 : -1} variant="outline" className={cn('min-h-[44px]', status === value && 'border-primary bg-primary/10')} onClick={() => onStatusChange(value)}>`. Inside the Button content: ternary — when `value === 'grail'` render `<span className="inline-flex items-center gap-1.5"><Star className="size-4" aria-hidden /> Grail</span>`; otherwise render `{label}`.
    - Pitfall 2: `aria-checked={status === value}` — always boolean true/false (React serializes to "true"/"false" strings).
    - Pitfall 1: `tabIndex={status === value ? 0 : -1}` — only the selected radio is in the Tab order (roving tabindex).
    - Pitfall 6: ID is `confirm-status-group` (collision-safe with WatchForm).

    Section 4 — Status-gated price field:
    - `<div className="space-y-2"><Label htmlFor="confirm-price">{isOwned ? 'Price paid' : 'Target price'}</Label><Input id="confirm-price" type="number" value={price ?? ''} onChange={(e) => onPriceChange(e.target.value ? Number(e.target.value) : undefined)} placeholder="$" /></div>`
    - Single Input, label flips with `isOwned` (single condition, no two-branch — per D-03 single `price` prop).

    Section 5 — Ghost escape affordances (ABOVE primary CTA per 68-UI-SPEC §Container rationale):
    - Row: `<div className="flex flex-col gap-2 sm:flex-row sm:gap-3">`
    - Edit details: `<Button type="button" variant="ghost" onClick={onEditDetails} disabled={pending} className="w-full sm:flex-1">Edit details</Button>`
    - Start over: `<Button type="button" variant="ghost" onClick={onStartOver} disabled={pending} className="w-full sm:flex-1">Start over</Button>`

    Section 6 — Primary CTA (full-width at all breakpoints):
    - `<Button type="button" onClick={onPrimary} disabled={pending} className="w-full">`
    - Ternary on `pending`: when true render `<><Loader2 className="size-4 mr-2 animate-spin" aria-hidden="true" />Saving...</>`; when false render `{CTA_LABELS[status]}`.

    AFTER the export — write the private SpecHeadline helper (NOT exported). Per 68-PATTERNS §1f — signature is `function SpecHeadline({ movement, caseSizeMm, dialColor }: { movement?: string | null; caseSizeMm?: number | null; dialColor?: string | null })`. Body builds `const parts = [movement ? MOVEMENT_LABELS[movement as keyof typeof MOVEMENT_LABELS] : null, caseSizeMm ? \`${caseSizeMm}mm\` : null, dialColor].filter((p): p is string => Boolean(p))`. If `parts.length === 0` return `null`. Otherwise `return <p className="text-sm text-muted-foreground">{parts.join(' • ')}</p>`.

    Honor 68-CONTEXT D-08 invariant in the JSDoc above the `reference` prop in the interface — copy verbatim from 68-RESEARCH §"Full Prop Interface" the comment block explaining "When catalogId is bound, Phase 67 D-10 server-side OVERRIDES this value with catalogRow.reference … User edits to this field on catalog-bound rows are silently superseded. The input stays ENABLED — this is correct behavior; the catalog row is canonical for identity." And on the `productionYear` prop document "productionYear is NOT overridden by Phase 67 D-10 (year is not part of the catalog identity tuple)."

    DO NOT add CollectionFitCard. DO NOT add `<form>` wrapper (D-Discretion-5 — use `<div>`; primary CTA fires via onClick, no native form submit semantics). DO NOT change the file from a named export to a default export.
  </action>
  <acceptance_criteria>
    - File exists: `test -f src/components/watch/ConfirmStep.tsx`
    - File starts with `'use client'` on line 1: `head -1 src/components/watch/ConfirmStep.tsx | grep -q "^'use client'"`
    - Named export of ConfirmStep present (NOT default): `grep -c "^export function ConfirmStep" src/components/watch/ConfirmStep.tsx` returns 1 AND `grep -c "^export default" src/components/watch/ConfirmStep.tsx` returns 0
    - No forbidden imports — CollectionFitCard absent: `grep -c "CollectionFitCard" src/components/watch/ConfirmStep.tsx` returns 0
    - No forbidden imports — Server Actions absent: `grep -c "from '@/app/actions" src/components/watch/ConfirmStep.tsx` returns 0
    - No forbidden imports — next/navigation absent: `grep -c "from 'next/navigation'" src/components/watch/ConfirmStep.tsx` returns 0
    - No useState (pure presenter, D-03): `grep -c "useState" src/components/watch/ConfirmStep.tsx` returns 0
    - CTA_LABELS module-scope constant present (D-10): `grep -c "^const CTA_LABELS" src/components/watch/ConfirmStep.tsx` returns 1
    - OPTIONS module-scope constant present: `grep -c "^const OPTIONS" src/components/watch/ConfirmStep.tsx` returns 1
    - aria-live="polite" present on root: `grep -c 'aria-live="polite"' src/components/watch/ConfirmStep.tsx` returns 1
    - role="radiogroup" wrapper present: `grep -c 'role="radiogroup"' src/components/watch/ConfirmStep.tsx` returns 1
    - role="radio" present (mapped 3x via OPTIONS): `grep -c 'role="radio"' src/components/watch/ConfirmStep.tsx` returns at least 1 (single string in source even if mapped)
    - Star icon imported: `grep -E "import.*\\{[^}]*Star[^}]*\\}.*from 'lucide-react'" src/components/watch/ConfirmStep.tsx | grep -q Star`
    - WatchIcon alias imported: `grep -c "Watch as WatchIcon" src/components/watch/ConfirmStep.tsx` returns 1
    - All 15 test cases pass: `npm run test -- src/components/watch/ConfirmStep.test.tsx` exits 0 with 15 tests passing
    - Lint clean for the new files: `npx eslint src/components/watch/ConfirmStep.tsx src/components/watch/ConfirmStep.test.tsx` exits 0
    - VerdictStep.tsx unchanged: `git diff --exit-code src/components/watch/VerdictStep.tsx` exits 0
    - AddWatchFlow.tsx unchanged: `git diff --exit-code src/components/watch/AddWatchFlow.tsx` exits 0
    - WatchForm.tsx unchanged: `git diff --exit-code src/components/watch/WatchForm.tsx` exits 0
    - Build exits 0 (project memory: build is the authoritative gate): `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/components/watch/ConfirmStep.test.tsx --run 2>&1 | tail -20 && git diff --exit-code src/components/watch/VerdictStep.tsx src/components/watch/AddWatchFlow.tsx src/components/watch/WatchForm.tsx</automated>
  </verify>
  <done>
    `src/components/watch/ConfirmStep.tsx` exists with pure-presenter shape (no useState, no Server Action import, no next/navigation, no CollectionFitCard); exports `ConfirmStep` as named function; mounts 6 sections in 68-UI-SPEC order (Cover Card → Inputs grid → Radiogroup picker → Price field → Ghost buttons row → Primary CTA full-width); status picker uses `role="radiogroup"` + `role="radio"` + `aria-checked` + roving tabindex + arrow-key handleKeyDown; Grail option contains inline Star icon; CTA label resolves via `CTA_LABELS[status]` module-scope lookup; price label flips per `isOwned`; pending state disables ghost + primary, swaps CTA to Loader2 + "Saving..."; all 15 test cases green; lint clean; build exits 0; VerdictStep/AddWatchFlow/WatchForm git-diff-empty.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parent (Phase 70 AddWatchFlow) → ConfirmStep props | ConfirmStep receives `catalogImageUrl` / `extractedImageUrl` URL strings, brand/model/reference text, and numeric inputs from the parent. The parent is trusted; ConfirmStep performs no validation. |
| ConfirmStep → next/image renderer | ConfirmStep passes the resolved `coverUrl` directly to `<Image src={...} unoptimized>`. The `unoptimized` flag bypasses next.config.ts `remotePatterns` allow-listing — the URL is fetched as-is by the browser. |
| User keyboard input → controlled callbacks | reference / year / price inputs fire `onChange` callbacks on every keystroke. The parent (Phase 70) owns validation and downstream persistence (Server Action). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Information Disclosure | `<Image src={coverUrl} unoptimized>` (Section 1 cover slot) | accept | `unoptimized` does not parse arbitrary HTML — the URL is rendered as a browser-loaded image, not a script context. The URL is supplied by Phase 70's trusted props (catalog row imageUrl or extracted imageUrl from the LLM/scraper). No DOM injection beyond `<img src>`. Same posture as VerdictStep.tsx:65-73 which has been in production since v4.0 Phase 20.1 with no security incident. ASVS V5: not applicable (this is a pure presenter — input validation happens server-side in Phase 70's Server Action). |
| T-68-02 | Tampering (XSS) | Reference / year / price inputs (controlled by React) | mitigate | React's JSX escaping handles all string interpolation automatically. No `dangerouslySetInnerHTML`. No HTML concatenation. ASVS V5 Output Encoding: satisfied by React's default behavior. |
| T-68-03 | Spoofing | Status picker (`role="radio"`) | accept | A malicious parent could pass an unexpected status value, but the TypeScript union `'owned' | 'wishlist' | 'grail'` enforces the contract at compile time. At runtime, an out-of-band value would render an unselected picker (no option matches) — visually degraded but safe. Not a security threat; safe default behavior. |
| T-68-04 | Denial of Service | Keyboard arrow-key handler | accept | The `handleKeyDown` handler is bounded — O(1) per keystroke (single `indexOf` lookup, single modulo arithmetic). No recursion, no async, no fetch. Cannot be DoS'd via key spamming. |

**Net security posture:** ConfirmStep is a pure presenter with no auth, no fetch, no Server Action call, no `dangerouslySetInnerHTML`, no `eval`, no router navigation. The security surface is bounded by parent-supplied props. All threats are either mitigated by React's default behavior or accepted as low-severity (no HIGH-severity threats — phase gate `block_on=high` is satisfied).

ASVS L1 categories applicable: V5 Input Validation (mitigated by React JSX escaping). V2/V3/V4/V6 do not apply (no auth, no session, no access control, no crypto).
</threat_model>

<verification>
## Phase 68 Verification Gate

Run from project root (`/Users/tylerwaneka/Documents/horlo`):

1. **Component tests green:**
   ```
   npm run test -- src/components/watch/ConfirmStep.test.tsx --run
   ```
   Expected: exit 0, 15 tests passing across 6 describe blocks.

2. **Full suite green (no regressions):**
   ```
   npm run test
   ```
   Expected: exit 0. Pre-existing CommentGateLocked font-medium failure is baseline noise per project memory `baseline_not_green_build_is_gate` — not attributable to this phase.

3. **Lint clean:**
   ```
   npm run lint
   ```
   Expected: exit 0.

4. **Build gate (authoritative):**
   ```
   npm run build
   ```
   Expected: exit 0. ConfirmStep is a new file not yet imported by any route or component, so build should be clean. The pre-existing tsc full-suite ~77 test-file errors are baseline noise; the `next build` gate is what matters.

5. **Phase boundary preservation:**
   ```
   git diff --exit-code src/components/watch/VerdictStep.tsx \
                       src/components/watch/AddWatchFlow.tsx \
                       src/components/watch/WatchForm.tsx
   ```
   Expected: exit 0 (no diff). These are Phase 70's territory; Phase 68 must leave them intact.

6. **Pure-presenter discipline (forbidden imports absent):**
   ```
   grep -E "from '(@/app/actions|next/navigation|@/components/insights/CollectionFitCard)'" \
        src/components/watch/ConfirmStep.tsx || echo "PURE_PRESENTER_OK"
   ```
   Expected: `PURE_PRESENTER_OK` (grep finds no forbidden imports).

7. **CollectionFitCard guard (verdict-out-of-scope per PROJECT.md):**
   ```
   grep -c "CollectionFitCard" src/components/watch/ConfirmStep.tsx
   ```
   Expected: `0`.
</verification>

<success_criteria>
- `src/components/watch/ConfirmStep.tsx` exists, exports `ConfirmStep` as a named function, starts with `'use client'`
- `src/components/watch/ConfirmStep.test.tsx` exists with 15 `it(` blocks across 6 `describe` blocks, all green
- `npm run test -- src/components/watch/ConfirmStep.test.tsx` exits 0
- `npm run build` exits 0
- `npm run lint` exits 0
- ConfirmStep contains zero forbidden imports (CollectionFitCard, addWatch, next/navigation)
- ConfirmStep has zero `useState` calls (pure presenter, D-03)
- ConfirmStep mounts the 6 sections in 68-UI-SPEC order with the exact tokens cited there
- Status picker uses `role="radiogroup"` + `role="radio"` + `aria-checked` (D-04) — NOT shadcn Tabs
- Grail option contains a Star icon child (D-05); other options do not
- CTA label resolves via module-scope `CTA_LABELS` lookup (D-10)
- Price label flips per `isOwned = status === 'owned'` (CONF-06, WatchForm.tsx:304 idiom)
- Pending state disables ghost buttons + primary CTA; primary swaps to Loader2 + "Saving..." (CONF-08 visual)
- Arrow-key roving-tabindex handler ships in this phase (per 68-UI-SPEC §Interaction Spec)
- VerdictStep.tsx, AddWatchFlow.tsx, WatchForm.tsx untouched (`git diff --exit-code` clean for all three)
- Every CONF-01..CONF-10 requirement has at least one passing test case from cases (a)-(o)
</success_criteria>

<output>
After completion, create `.planning/phases/68-confirmstep-component/68-01-SUMMARY.md` documenting:
- Files created (paths + line counts)
- Test cases shipped (the 15 cases (a)-(o) by ID)
- Requirements satisfied (CONF-01..CONF-10 — all 10)
- Decisions honored (D-01..D-11 + 68-UI-SPEC arrow-key keyboard handler locked IN)
- Key invariants preserved (no useState, no forbidden imports, sibling files unchanged)
- Lessons learned / friction encountered (if any)
- Next plan: Phase 70 wires this component into the AddWatchFlow rewrite
</output>
