---
phase: 61-photo-upload-carousel-ui
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/watch/WatchPhotoSection.tsx
  - src/components/watch/SortablePhotoThumb.tsx
  - src/components/watch/PhotoDropzone.tsx
autonomous: true
gap_closure: true
requirements: [PHOTO-02, PHOTO-03, PHOTO-05, PHOTO-06]
user_setup: []

must_haves:
  truths:
    - "In edit mode the upload affordance renders its full 'Drop photos here or tap to choose' text legibly (gap #2)"
    - "On mobile the filmstrip drag handle is large enough to reliably grab for reorder (gap #3)"
    - "The carousel position indicator is horizontally centered on the photo, not offset right (gap #4)"
    - "The 'Cover' badge shows ONLY in edit mode, per the revised D-07 (gap #5)"
    - "Tapping the × hides the photo immediately on click (optimistic), keeping undo restore and server-failure revert (gap #6)"
    - "The × delete badge glyph has legible light contrast against the destructive background, not black (gap #7)"
    - "The desktop filmstrip scrolls internally within its column instead of expanding and collapsing the right rail at higher photo counts (gap #8)"
  artifacts:
    - path: "src/components/watch/WatchPhotoSection.tsx"
      provides: "Carousel + filmstrip with centered indicator, edit-mode-only Cover badge, immediate optimistic delete, contained filmstrip overflow, full-size edit-mode dropzone"
      contains: "WatchPhotoSection"
    - path: "src/components/watch/SortablePhotoThumb.tsx"
      provides: "Thumbnail with enlarged drag-handle hit area, edit-mode-gated Cover badge, legible × glyph"
      contains: "SortablePhotoThumb"
    - path: "src/components/watch/PhotoDropzone.tsx"
      provides: "Dropzone able to render its full affordance text in edit-mode contexts"
      contains: "PhotoDropzone"
  key_links:
    - from: "WatchPhotoSection editMode"
      to: "SortablePhotoThumb Cover badge"
      via: "isCover gated on editMode (D-07)"
      pattern: "editMode"
    - from: "SortablePhotoThumb × button onClick"
      to: "WatchPhotoSection handleDelete"
      via: "immediate optimistic setDeletedIds on click"
      pattern: "setDeletedIds"
---

<objective>
Close the seven carousel/filmstrip/dropzone UAT gaps (#2–#8) reported during Phase 61 prod verification. All seven are cosmetic/minor UI fixes concentrated in the same three components — WatchPhotoSection, SortablePhotoThumb, PhotoDropzone — so they are grouped here to avoid file-ownership conflicts.

Purpose: The carousel and filmstrip functionally pass UAT (tests 2–7 passed), but a cluster of legibility, centering, hit-area, contrast, overflow, and behavior-regression issues degrade the owner experience. One of them (#5) is a confirmed decision change to D-07.

Output: WatchPhotoSection, SortablePhotoThumb, and PhotoDropzone updated to satisfy the seven gap truths. No new components, no schema changes, no new server actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative gap source
@.planning/phases/61-photo-upload-carousel-ui/61-UAT.md

# Decision source — D-07 was REVISED to edit-mode-only
@.planning/phases/61-photo-upload-carousel-ui/61-CONTEXT.md

# UI design contract (honor it)
@.planning/phases/61-photo-upload-carousel-ui/61-UI-SPEC.md

# What was already built (reuse, don't duplicate)
@.planning/phases/61-photo-upload-carousel-ui/61-02-SUMMARY.md

<interfaces>
<!-- Key facts the executor needs without re-exploring. -->

SortablePhotoThumb props (src/components/watch/SortablePhotoThumb.tsx):
  id: string; index: number; signedUrl: string | null; isCover: boolean;
  editMode: boolean; onDelete: () => void; onClick: () => void

WatchPhotoSection renders the indicator as a sibling <p> BELOW the carousel
viewport. The carousel viewport div is `relative aspect-square w-full max-w-md`;
the indicator <p> is `text-center` but lives in the full-width parent region —
so `text-center` centers on the region width, not the max-w-md photo (gap #4).

The edit-mode +Add tile wraps the full PhotoDropzone inside a `w-16 h-16`
(64×64px) box, which clips PhotoDropzone's `p-4` body text (gap #2).

Delete behavior (gap #6): handleDelete currently fires the undo toast
immediately but moves BOTH setDeletedIds (optimistic hide) AND the server call
into the 5s setTimeout — so the photo only hides when the timer fires.

Token fact (gap #7): globals.css defines `--destructive` (dark red, oklch 0.55)
and `--foreground` but does NOT define `--destructive-foreground`. So
`text-destructive-foreground` resolves to an undefined custom property and the
× glyph falls back to inherited near-black `--foreground`. Use an explicit light
class (e.g., `text-white`) on the × glyph, OR define `--destructive-foreground`
as a light value in globals.css if you prefer the token route — but if you touch
globals.css, add it to files_modified in the SUMMARY and keep it minimal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SortablePhotoThumb — drag-handle hit area (#3), Cover badge edit-mode gate (#5), × glyph contrast (#7)</name>
  <files>src/components/watch/SortablePhotoThumb.tsx, tests/components/watch-photo-section.test.tsx</files>
  <read_first>
    - src/components/watch/SortablePhotoThumb.tsx (the file being modified)
    - .planning/phases/61-photo-upload-carousel-ui/61-UAT.md (gaps #3, #5, #7)
    - .planning/phases/61-photo-upload-carousel-ui/61-CONTEXT.md (D-07 revised text — edit-mode-only Cover badge)
    - .planning/phases/61-photo-upload-carousel-ui/61-UI-SPEC.md §"Touch targets" (44px min) + §"Cover badge" + §"Edit mode" delete × contract
  </read_first>
  <action>
    Gap #3 — Enlarge the GripVertical drag handle hit area. The current handle is a `size-3` icon in an `absolute bottom-1 left-1` div with no padding, far below the 44px touch-target the UI-SPEC requires. Increase the tappable area: add padding to the handle wrapper (e.g., a larger padded box around GripVertical) so the grab target is comfortably finger-sized while staying anchored in the thumbnail corner; keep `{...listeners}` and the `touch-manipulation` / `touchAction` on the handle ONLY (Pitfall 3 — do not move listeners to the outer thumbnail). Keep the icon visually small if desired but expand the clickable/draggable zone. Per MEMORY feedback_mobile_ui_verify_on_prod the final touch confirmation is on prod — assert structurally here.

    Gap #5 (D-07 REVISED) — Gate the "Cover" badge on editMode. Currently the badge renders whenever `isCover` is true. Change the badge render condition to `isCover && editMode` so the badge appears ONLY in edit mode, per the revised D-07 ("Cover badge edit-mode only", confirmed 2026-05-25, commit 38b8e1c). Keep the badge classes and `aria-label="Cover photo"` unchanged. The badge must still move to the new first position during drag (driven by the parent's `isCover={visibleIds[0] === id}`).

    Gap #7 — Fix the × glyph contrast. The button uses `text-destructive-foreground`, but `--destructive-foreground` is undefined in globals.css so the glyph inherits near-black `--foreground` over the dark-red `bg-destructive`. Replace `text-destructive-foreground` with an explicit light color (`text-white`) on the × button so the glyph is legible. Leave `bg-destructive` and the `hover:bg-destructive/80` as-is.
  </action>
  <acceptance_criteria>
    - source: SortablePhotoThumb's Cover badge JSX is gated on `editMode` — `grep -v '^[[:space:]]*//' src/components/watch/SortablePhotoThumb.tsx | grep -c 'isCover && editMode'` returns ≥ 1 (or the equivalent guard renders the badge only when editMode is true).
    - source: the × delete button className contains `text-white` and NO LONGER contains `text-destructive-foreground` — `grep -c 'text-destructive-foreground' src/components/watch/SortablePhotoThumb.tsx` returns 0.
    - source: the drag-handle wrapper around GripVertical has added padding (a padding utility class such as `p-1.5`/`p-2`) enlarging the hit area beyond the bare icon; `{...listeners}` remains on the handle wrapper only (NOT on the outer setNodeRef div).
    - behavior: in view mode (editMode=false) no Cover badge is rendered for any thumbnail; in edit mode the first thumbnail shows the Cover badge.
    - test: `npm test -- watch-photo-section` runs; update any existing Cover-badge-in-view-mode assertion in tests/components/watch-photo-section.test.tsx to expect edit-mode-only behavior (the old test asserted the badge in view mode and must flip to match D-07).
    - build: `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The Cover badge appears only in edit mode (D-07 revised), the × glyph is white over the destructive background, and the drag handle has an enlarged padded hit area.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WatchPhotoSection — indicator centering (#4), edit-mode-only Cover in view filmstrip (#5), immediate optimistic delete (#6), filmstrip overflow containment (#8), full-size edit-mode dropzone (#2)</name>
  <files>src/components/watch/WatchPhotoSection.tsx, src/components/watch/PhotoDropzone.tsx, tests/components/watch-photo-section.test.tsx</files>
  <read_first>
    - src/components/watch/WatchPhotoSection.tsx (the file being modified)
    - src/components/watch/PhotoDropzone.tsx (the +Add tile content — gap #2)
    - .planning/phases/61-photo-upload-carousel-ui/61-UAT.md (gaps #2, #4, #5, #6, #8)
    - .planning/phases/61-photo-upload-carousel-ui/61-UI-SPEC.md §"Position indicator" (centered below carousel) + §"Cover badge" + §"Delete" + §"Filmstrip" + §"Add-Watch dropzone Entry"
    - .planning/phases/61-photo-upload-carousel-ui/61-02-SUMMARY.md (CR-01/WR-02 delete history — do not undo the unmount-cleanup or the failure auto-revert)
  </read_first>
  <action>
    Gap #4 — Center the position indicator on the photo. The indicator `<p>` is `text-center` but sits in the full-width parent region while the carousel viewport is constrained to `max-w-md`, so it centers on the wrong width and drifts right. Constrain the indicator so its centering box matches the carousel width: wrap or class it so it occupies the same `w-full max-w-md` block as the carousel viewport (e.g., place the indicator inside a `w-full max-w-md` wrapper, or move it under the carousel block that already has `max-w-md`). It must read centered relative to the photo.

    Gap #5 (D-07 REVISED) — In the VIEW-MODE filmstrip branch (the non-DndContext branch), the Cover badge currently always renders on `visibleIds[0]`. Remove that view-mode Cover badge so the badge shows ONLY in edit mode. The edit-mode branch already passes `isCover={visibleIds[0] === id}` to SortablePhotoThumb (Task 1 gates the actual render on editMode there); the view-mode plain-thumbnail branch must NOT render a Cover span. Keep the `aria-label` on view-mode thumbnails free of the ", Cover" suffix (or remove that conditional) so the clean view carries no cover labeling. Cite D-07 revised.

    Gap #6 — Hide the photo IMMEDIATELY on × click (optimistic), not after the 5s timer. Restructure handleDelete: call `setDeletedIds(photoId)` synchronously inside a startTransition AT CLICK TIME so the thumbnail/slide disappears at once; show the undo toast immediately as today; schedule ONLY the actual server `deleteWatchPhotoAction` (the permanent commit) inside the 5s setTimeout. On Undo: clear the timer AND restore the photo (remove it from deletedIds) so the immediate-hide is reverted. On server-delete failure after the window lapses: keep the existing WR-02 auto-revert/error behavior so a failed delete restores the photo and shows an error. Preserve the CR-01 unmount cleanup (clear pending timer on unmount). The net behavior: click → instant hide + undo toast; Undo within 5s → photo returns; no Undo → server delete commits at 5s; server failure → photo returns + error toast.

    Gap #8 — Contain the filmstrip horizontal scroll within its column. The `flex overflow-x-auto` filmstrip row expands its parent instead of scrolling internally (flexbox min-width:auto gotcha). Add `min-w-0` (and `max-w-full` as needed) to the filmstrip container AND to any flex ancestor column it lives in within WatchPhotoSection so `overflow-x-auto` actually scrolls internally rather than pushing the right-rail layout. Apply to BOTH the edit-mode filmstrip row and the view-mode filmstrip row.

    Gap #2 — Make the edit-mode upload affordance render its full text. The +Add tile currently wraps the full PhotoDropzone inside a `w-16 h-16` (64×64) box, clipping the "Drop photos here or tap to choose" copy so only the camera icon and "Drop" show. Fix by giving the upload affordance enough room for its text: render the PhotoDropzone (with full affordance text) at an adequate size in the edit-mode context — either move the dropzone out of the 64×64 tile to a full-width affordance below the filmstrip in edit mode, or size the tile/dropzone so the `text-sm` body copy fits legibly. Keep the cap behavior (hidden/disabled at 10 photos) and the `currentPhotoCount={visibleIds.length + localUploadCount}` math intact. Do not regress the upload pipeline wiring.
  </action>
  <acceptance_criteria>
    - source (gap #4): the position-indicator element is contained within a `max-w-md` box matching the carousel viewport (so `text-center` centers on the photo width) — `grep -c 'max-w-md' src/components/watch/WatchPhotoSection.tsx` returns ≥ 2 (carousel viewport + indicator wrapper).
    - source (gap #5): the VIEW-MODE filmstrip branch no longer renders a "Cover" badge span; `grep -v '^[[:space:]]*//' src/components/watch/WatchPhotoSection.tsx` shows the only Cover-badge render path is the edit-mode SortablePhotoThumb (no `>Cover<` literal in the view-mode plain-thumbnail branch).
    - behavior (gap #6): tapping × calls `setDeletedIds` immediately (within a click-time startTransition) so the photo hides at once; the 5s setTimeout contains ONLY the `deleteWatchPhotoAction` server call (not the optimistic hide). Undo clears the timer and restores the hidden id. Server-delete failure still reverts + toasts an error.
    - source (gap #6): `setDeletedIds(` appears at click time (outside the 5s setTimeout body), and `deleteWatchPhotoAction(` appears inside the setTimeout body.
    - source (gap #8): the filmstrip container(s) and their enclosing flex column carry `min-w-0` so `overflow-x-auto` scrolls internally — `grep -c 'min-w-0' src/components/watch/WatchPhotoSection.tsx` returns ≥ 1, applied to the overflow-x-auto filmstrip ancestry.
    - source (gap #2): the edit-mode upload affordance is no longer constrained to a `w-16 h-16` box that clips PhotoDropzone's body text — the PhotoDropzone with full "Drop photos here or tap to choose" text renders at a legible size in edit mode.
    - test: `npm test -- watch-photo-section` runs; existing delete/undo and Cover-badge tests updated to the new behavior (immediate hide; edit-mode-only Cover).
    - build: `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The position indicator reads centered on the photo, the Cover badge is edit-mode-only (view filmstrip carries none), × hides the photo immediately with working undo and failure-revert, the filmstrip scrolls internally without collapsing the right rail, and the edit-mode dropzone shows its full affordance text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client filmstrip → server actions | Owner-only delete/reorder/upload actions invoked from the carousel UI |

No NEW trust boundary is introduced by this plan. All seven gaps are cosmetic/minor CSS and client-state changes (indicator centering, badge gating, drag-handle padding, glyph color, overflow containment, optimistic-hide timing). The delete path (gap #6) re-uses the existing `deleteWatchPhotoAction` server action, which already double-verifies ownership via the DAL `watches.user_id` gate (Plan 01). No new network endpoints, no new data exposure, no change to the signed-URL boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05-01 | Tampering | gap #6 immediate-optimistic delete | mitigate | Optimistic hide is client-only; the authoritative delete remains `deleteWatchPhotoAction` (server-side `watches.user_id` ownership check unchanged); failure auto-reverts the client state. No new tampering surface. |
| T-61-05-02 | Elevation of Privilege | edit-mode-gated controls | accept | Cover badge gating and drag-handle padding are presentation-only; the existing `viewerCanEdit` gate + DAL ownership re-check (Plan 01/02) remain the security boundary. No privilege change. |
| T-61-05-03 | Information Disclosure | × glyph color / indicator centering / overflow CSS | accept | Pure CSS/styling; no data crosses any boundary. |
</threat_model>

<verification>
- `npm run build` exits 0 (authoritative gate per MEMORY project_baseline_not_green_build_is_gate).
- `npm test -- watch-photo-section` runs; updated assertions reflect edit-mode-only Cover badge and immediate-hide delete.
- Static source greps in the acceptance criteria confirm each concrete class/behavior change.
- Device-only confirmations (mobile drag-handle grab #3; visual centering #4; filmstrip touch-scroll #8; × tap-hide on iOS #6) are classified human_needed and verified by the user on PROD after the bundled deploy (MEMORY feedback_mobile_ui_verify_on_prod). Do NOT plan local e2e for these (local e2e skips on the empty test DB).
</verification>

<success_criteria>
- Gap #2: edit-mode dropzone affordance text fully legible.
- Gap #3: drag-handle hit area enlarged (structural; prod-confirmed).
- Gap #4: position indicator centered on the photo.
- Gap #5: Cover badge edit-mode-only (D-07 revised), both components.
- Gap #6: × hides photo immediately on click, undo restores, server failure reverts.
- Gap #7: × glyph legible (white) over destructive background.
- Gap #8: filmstrip scrolls internally, right rail no longer collapses.
- Build exits 0; watch-photo-section tests pass with updated expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/61-photo-upload-carousel-ui/61-05-SUMMARY.md`
</output>
