---
phase: 61-photo-upload-carousel-ui
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/watch/AddWatchFlow.tsx
  - src/components/watch/WatchForm.tsx
  - src/components/watch/flowTypes.ts
  - tests/components/add-watch-flow-photos.test.tsx
  - tests/static/ppr-dynamic-before-use-cache.test.ts
autonomous: true
gap_closure: true
requirements: [PHOTO-09]
user_setup: []

must_haves:
  truths:
    - "On the extract → fit-verdict → 'Add to collection' → save path, the prominent 'Add your photos' step (WatchPhotoStep) appears before navigation — it no longer falls through to router.push (gap #9 / PHOTO-09 / SC5)"
    - "The same photos step appears on the manual-entry create path (already wired; must remain working)"
    - "A static guard asserts the P61-BUG-01 ordering rule (no dynamic API call after a 'use cache' marker in the affected PPR routes) so the resolved #419 regression cannot silently return (gap #1 guard)"
  artifacts:
    - path: "src/components/watch/AddWatchFlow.tsx"
      provides: "photos-pending transition reliably reached from the extract→collection commit path"
      contains: "photos-pending"
    - path: "src/components/watch/WatchForm.tsx"
      provides: "create-success handoff to onWatchCreated firing before any navigation on every create-mode path"
      contains: "onWatchCreated"
    - path: "tests/static/ppr-dynamic-before-use-cache.test.ts"
      provides: "P61-BUG-01 ordering regression guard"
      contains: "use cache"
  key_links:
    - from: "WatchForm create-success"
      to: "AddWatchFlow handleWatchCreated"
      via: "onWatchCreated(watchId, dest) invoked BEFORE router.push on all create-mode paths"
      pattern: "onWatchCreated"
    - from: "AddWatchFlow handleWatchCreated"
      to: "WatchPhotoStep render"
      via: "setState({ kind: 'photos-pending' })"
      pattern: "photos-pending"
---

<objective>
Close gap #9 (MAJOR): the prominent "Add your photos" step never appears on the live extract → fit-verdict → "Add to collection" → save path. Structural verification confirmed `onWatchCreated` is wired on BOTH WatchForm instances, yet the live path bypasses it and falls through to navigation. This plan is an INVESTIGATION-then-FIX: trace exactly which create handler/WatchForm path the live "Add to collection" CTA fires, confirm whether `onWatchCreated` is invoked before navigation, and ensure the photos-pending transition reliably fires on the URL-extract path (the primary path users hit).

This plan also adds the OPTIONAL lightweight regression guard for the already-resolved gap #1 (404 + React #419 on soft-nav, fixed in commit 98e7289): a static test asserting the P61-BUG-01 ordering rule so this project's chronic #419 family cannot silently recur via these PPR routes.

Purpose: PHOTO-09 / SC5 ("the add-watch flow presents a prominent, not easily skipped photo upload affordance") is failing in production. The step exists and is wired, but does not render on the path users actually take.

Output: AddWatchFlow / WatchForm / flowTypes adjusted so the photos step fires on the extract→collection path; an updated test proving it; a minimal static PPR-ordering guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative gap source
@.planning/phases/61-photo-upload-carousel-ui/61-UAT.md

# Decision source — D-15/D-16 (photos step placement + friction-to-skip)
@.planning/phases/61-photo-upload-carousel-ui/61-CONTEXT.md

# What was already built for PHOTO-09 (reuse, don't duplicate)
@.planning/phases/61-photo-upload-carousel-ui/61-03-SUMMARY.md

# Gap #1 resolution — the fix is DONE; this plan only adds a guard
@.planning/debug/phase61-404-react-419-soft-nav.md

<interfaces>
<!-- Trace map the executor needs. The wiring already exists; the bug is runtime. -->

Extract → collection path (src/components/watch/AddWatchFlow.tsx):
  handleExtract → setState({ kind: 'verdict-ready', ... })
  VerdictStep "Add to Collection" button → onCollection → handleCollection
  handleCollection → setState({ kind: 'form-prefill', catalogId, extracted })
  form-prefill render → <WatchForm mode="create" lockedStatus="owned"
    onWatchCreated={handleWatchCreated} ... />
  handleWatchCreated(watchId, dest) → setState({ kind: 'photos-pending', watchId, destination })
  photos-pending render → <WatchPhotoStep ... onDone/onSkip → router.push(destination) />

WatchForm create-success (src/components/watch/WatchForm.tsx ~line 259-278):
  result.success && mode === 'create':
    dest = returnTo ?? defaultDestinationForStatus(finalStatus, viewerUsername)
    if (onWatchCreated && result.data && 'id' in result.data) {
      onWatchCreated(result.data.id, dest); return result
    }
    router.push(dest)
  — wrapped in run(...) from useFormFeedback. run() ALSO fires a success toast and,
    when successAction is provided via buildSuccessOpts, wires its OWN
    onClick: () => router.push(href) into the Sonner toast action slot. The
    success-toast lifecycle and the buildSuccessOpts opts are independent of the
    onWatchCreated early-return inside the action body.

DIAGNOSTIC CLUE (hand to investigation, do not pre-conclude): the user reported
the button shows "Saving..." then redirects. WatchForm's create-mode submit
button label is "Adding..." (mode==='create'), and "Saving..." is the EDIT-mode
label (WatchForm) and the VerdictStep button pending label. "Saving..." also
appears on the VerdictStep "Add to Collection"/"Add to Wishlist" buttons. This
suggests the live create may be routing through a code path whose pending label
is "Saving..." — investigate whether a verdict-step-level submit, an edit-mode
form, or a non-onWatchCreated WatchForm instance is firing, rather than the
form-prefill create handler. flowTypes.ts also declares a 'submitting-collection'
state that is NOT rendered anywhere in AddWatchFlow — check whether any path sets
it. Confirm the actual runtime path with logging/inspection before changing code.

P61-BUG-01 (gap #1 guard, src/app — DO NOT re-implement the fix): in PPR routes
that contain both 'use cache' functions and dynamic API access (cookies/headers
via createSupabaseServerClient or signCoverUrls) in the same async function body,
ALL dynamic API access must come BEFORE all 'use cache' calls. Affected files
already fixed in 98e7289:
  - src/app/u/[username]/[tab]/page.tsx
  - src/app/w/[ref]/page.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate and fix the extract→collection photos-step bypass (gap #9 / PHOTO-09)</name>
  <files>src/components/watch/AddWatchFlow.tsx, src/components/watch/WatchForm.tsx, src/components/watch/flowTypes.ts, tests/components/add-watch-flow-photos.test.tsx</files>
  <read_first>
    - src/components/watch/AddWatchFlow.tsx (handleCollection, handleWatchCreated, form-prefill + manual-entry render branches, photos-pending render branch)
    - src/components/watch/WatchForm.tsx (handleSubmit, the run()/useFormFeedback wrapper, the create-success onWatchCreated handoff ~line 259-278, buildSuccessOpts)
    - src/components/watch/VerdictStep.tsx (the "Add to Collection" button + its "Saving..." pending label — diagnostic clue)
    - src/components/watch/flowTypes.ts (FlowState union, incl. the unrendered 'submitting-collection' variant)
    - src/lib/hooks/useFormFeedback.ts (run() — fires success toast + its own router.push via successAction; confirm it does not pre-empt the onWatchCreated handoff)
    - src/components/watch/WatchPhotoStep.tsx (the step that must appear)
    - .planning/phases/61-photo-upload-carousel-ui/61-03-SUMMARY.md (the original PHOTO-09 wiring)
    - .planning/phases/61-photo-upload-carousel-ui/61-UAT.md (gap #9 reason + hypothesis)
  </read_first>
  <action>
    STEP 1 — Trace the live path (investigation, no code change yet). Confirm by inspection (add temporary console logging if needed, then remove) exactly what fires when the user clicks "Add to Collection" on the verdict screen and then submits the prefilled form:
      (a) Does VerdictStep's onCollection → handleCollection → form-prefill actually mount the WatchForm with `onWatchCreated={handleWatchCreated}`?
      (b) On form submit, does WatchForm reach the create-success branch with `onWatchCreated` truthy AND `result.data` containing `id`, so it calls `onWatchCreated(...)` and returns BEFORE `router.push(dest)`?
      (c) Does useFormFeedback's run() independently navigate first — e.g. via the `successAction` Sonner toast `onClick: () => router.push(href)` from buildSuccessOpts, or via any other navigation — such that the photos-pending state is set but immediately superseded by a route change?
      (d) Is `result.data` actually shaped as `{ id }` at runtime (addWatch returns `{ success: true, data: watch }` where watch has `id`), or is the `'id' in result.data` guard failing so it falls through to `router.push`?
      (e) Account for the "Saving..." clue: determine whether the live submit is the create-mode WatchForm (button label "Adding...") or some other surface, to localize the bug.

    STEP 2 — Apply the minimal fix that makes the photos step appear on the extract→collection path. The fix MUST be the smallest change that guarantees, on a successful create-mode commit with `onWatchCreated` present, the flow transitions to `photos-pending` (rendering WatchPhotoStep) and does NOT navigate away until the user presses Done or Skip. Likely candidates (pick based on STEP 1 findings — do not apply blindly):
      - Ensure the create-success onWatchCreated handoff cannot be raced by run()'s success-toast navigation: when `onWatchCreated` is provided, suppress the auto-navigating `successAction` (the toast's router.push) so navigation is owned solely by the photos step's Done/Skip. The toast body may still show, but it must not push the route out from under the photos step.
      - Harden the create-success guard so it fires `onWatchCreated` whenever a watch id is present, even if `result.data` shape is wider than `{ id }`.
      - If a stale/duplicate WatchForm instance or an unrendered 'submitting-collection' branch is implicated, correct the branch so the form-prefill instance with `onWatchCreated` is the one that commits.

    Keep the manual-entry create path working (it shares the same handoff). Do NOT change edit-mode behavior. Do NOT alter the WatchPhotoStep component itself unless STEP 1 proves the bug lives there. Honor D-15 (step after verdict, before commit-nav) and D-16 (skippable with friction; never blocks save).
  </action>
  <acceptance_criteria>
    - source: WatchForm's create-success branch invokes `onWatchCreated(...)` and returns BEFORE any `router.push` on the create-mode path — `grep -n 'onWatchCreated' src/components/watch/WatchForm.tsx` shows the call precedes the create-mode `router.push(dest)`.
    - source: when `onWatchCreated` is present on a create-mode commit, no independent navigation (toast `successAction` router.push or otherwise) fires that would supersede the photos-pending step — verify the run() opts passed in this case omit/neutralize the auto-navigating `successAction`.
    - behavior: on the extract→"Add to Collection"→submit path, AddWatchFlow reaches `setState({ kind: 'photos-pending', ... })` and WatchPhotoStep renders ("Add your photos" heading) BEFORE any navigation; navigation occurs only on WatchPhotoStep onDone/onSkip.
    - test: tests/components/add-watch-flow-photos.test.tsx is extended with a case that drives the FULL extract→verdict-ready→onCollection→form-prefill→submit path (mock addWatch to resolve `{ success: true, data: { id: ... } }`) and asserts the "Add your photos" step renders and NO navigation occurred until Done/Skip. The existing manual-entry case must still pass.
    - build: `npm run build` exits 0.
    - test: `npm test -- add-watch-flow-photos` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- add-watch-flow-photos</automated>
  </verify>
  <done>Submitting the prefilled form after "Add to Collection" on the URL-extract path shows the prominent "Add your photos" step before navigating; the step still appears on the manual-entry path; PHOTO-09 / SC5 is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Add minimal P61-BUG-01 static regression guard (gap #1)</name>
  <files>tests/static/ppr-dynamic-before-use-cache.test.ts</files>
  <read_first>
    - .planning/debug/phase61-404-react-419-soft-nav.md (the resolved fix + the durable P61-BUG-01 rule + the SPECIFIC VIOLATIONS list — the test must encode this ordering rule)
    - src/app/u/[username]/[tab]/page.tsx (a fixed PPR route to assert against)
    - src/app/w/[ref]/page.tsx (a fixed PPR route to assert against)
    - tests/static/ (existing static-guard patterns in this directory — mirror their structure)
    - MEMORY project_vitest_static_node_env (fs-walking static guards MUST use `// @vitest-environment node`, else they pass locally on jsdom but fail the Vercel prebuild)
  </read_first>
  <action>
    Create a minimal static guard `tests/static/ppr-dynamic-before-use-cache.test.ts` that asserts the P61-BUG-01 ordering rule for the two affected PPR route files (`src/app/u/[username]/[tab]/page.tsx` and `src/app/w/[ref]/page.tsx`): within each route module, in any async function body that contains BOTH a `'use cache'` marker (or a call to a known cached helper like `getBatchedWatchCountsCached` / `getLikesForTargetCached` / `ProfileShellResolver`) AND a dynamic-API call (`createSupabaseServerClient(` or `signCoverUrls(` / `cookies(`), no dynamic-API call appears AFTER a 'use cache' / cached-call marker. Keep the heuristic simple and robust: read each file with readFileSync, locate the first cached-marker occurrence and assert no `createSupabaseServerClient(` / `signCoverUrls(` / `cookies(` occurrence comes after it within the same module (or implement a per-branch scan if a whole-file scan is too coarse — but prefer the simplest assertion that would have caught the 98e7289 regression). The test MUST begin with the `// @vitest-environment node` pragma at the top of the file (this guard walks/reads the filesystem — without node env it passes on jsdom locally but the externalized `node:fs` breaks the Vercel prebuild; cost a failed Phase 59 prod deploy). Use `grep -v '^#'`-style comment filtering if you count occurrences so header prose cannot self-invalidate the gate. Keep it minimal — this is a recurrence tripwire, not exhaustive PPR analysis.
  </action>
  <acceptance_criteria>
    - source: tests/static/ppr-dynamic-before-use-cache.test.ts exists and its FIRST line is `// @vitest-environment node`.
    - source: the test asserts the ordering rule against BOTH `src/app/u/[username]/[tab]/page.tsx` and `src/app/w/[ref]/page.tsx`.
    - test: `npm test -- ppr-dynamic-before-use-cache` passes against the current (fixed) source (the guard is GREEN now that 98e7289 is applied).
    - build: `npm run build` exits 0 (the guard does not affect the build; this confirms no import/type breakage from the test file).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- ppr-dynamic-before-use-cache</automated>
  </verify>
  <done>A minimal node-env static guard encodes the P61-BUG-01 ordering rule for the two fixed PPR routes and passes against the current source, preventing silent recurrence of the #419/404 soft-nav regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client add-watch flow → addWatch / addWatchPhotoAction | Authenticated owner creates a watch then uploads photos |

No NEW trust boundary is introduced. Gap #9 is a client-side routing/state-machine fix in AddWatchFlow/WatchForm — it changes WHEN navigation happens, not what data crosses any boundary. The watch is still created via the existing `addWatch` server action (ownership-bound to the authenticated user); photos still upload via `addWatchPhotoAction` (DAL `watches.user_id` ownership check, Plan 01). The `photos-pending` `watchId` is the session's own `addWatch` result. The gap #1 guard is a test-only file. No new endpoints, no new auth paths, no new attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-06-01 | Tampering | photos-pending watchId | accept | watchId originates from the caller's own `addWatch` success result; `addWatchPhotoAction` re-verifies ownership server-side (DAL `watches.user_id`). Client cannot upload to a watch it does not own regardless of flow state. |
| T-61-06-02 | Spoofing | create-mode commit path | accept | `addWatch` binds the new row to `getCurrentUser()` server-side; the routing change does not touch authentication. |
| T-61-06-03 | Denial of Service | static guard fs walk | mitigate | Guard reads only the two named route files (bounded), runs in node env (no jsdom fs externalization break); no unbounded directory walk. |
</threat_model>

<verification>
- `npm run build` exits 0 (authoritative gate per MEMORY project_baseline_not_green_build_is_gate).
- `npm test -- add-watch-flow-photos` passes, including the new extract→collection happy-path case proving WatchPhotoStep renders before navigation.
- `npm test -- ppr-dynamic-before-use-cache` passes against the current fixed source.
- Final functional confirmation of gap #9 on the live extract path is verified by the user on PROD after deploy (MEMORY feedback_mobile_ui_verify_on_prod); the new component test is the structural proof.
- Gap #1 is resolved-pending-prod-verification per debug session 98e7289; this plan adds only the recurrence tripwire and does NOT re-implement the fix.
</verification>

<success_criteria>
- Gap #9: the prominent "Add your photos" step appears on the extract→fit-verdict→"Add to collection"→save path before navigation; manual-entry path still works; PHOTO-09 / SC5 satisfied.
- Gap #1: minimal node-env static guard encodes P61-BUG-01 for the two fixed PPR routes and is GREEN against current source.
- Build exits 0; both test suites pass.
</success_criteria>

<output>
After completion, create `.planning/phases/61-photo-upload-carousel-ui/61-06-SUMMARY.md`
</output>
