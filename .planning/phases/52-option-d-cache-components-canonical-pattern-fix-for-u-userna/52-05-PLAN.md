---
phase: 52
plan: 05
type: execute
wave: 2
depends_on: [01, 03, 04]
files_modified:
  - src/app/u/[username]/[tab]/page.tsx
autonomous: true
requirements: [REQ-52-04]

must_haves:
  truths:
    - "src/app/u/[username]/[tab]/page.tsx exports a SYNC `ProfileTabPage` component as default (no `async` keyword)"
    - "The sync outer ProfileTabPage returns `<Suspense fallback={<ProfileTabContentSkeleton/>}><ProfileTabContent paramsPromise={params} /></Suspense>` and nothing else"
    - "An async inner `ProfileTabContent` function exists in the same file; receives `paramsPromise: Promise<{ username: string; tab: string }>`; body is the original page body verbatim (368 lines minus the outer wrapper)"
    - "All tab-specific branching (collection / wishlist / worn / notes / stats / common-ground / insights) is preserved verbatim inside ProfileTabContent"
    - "Phase 39c Pitfall 5 / D-52-CF-03 preserved: `notFound()` calls on lines 58 and 79 (current) remain in their existing relative positions inside ProfileTabContent (tab validation first, then resolver, then notFound on missing profile)"
    - "Test 5 in tests/profile-route-51.test.ts (REQ-52-04) PASSES after this plan"
    - "Vitest full suite green (5/5 in tests/profile-route-51.test.ts; no regressions in tests/app/profile-tab-*.test.tsx)"
  artifacts:
    - path: "src/app/u/[username]/[tab]/page.tsx"
      provides: "Sync outer ProfileTabPage wrapping async ProfileTabContent in Suspense (canonical Cache Components page pattern)"
      contains: "async function ProfileTabContent"
  key_links:
    - from: "src/app/u/[username]/[tab]/page.tsx (outer ProfileTabPage)"
      to: "src/app/u/[username]/[tab]/page.tsx (inner ProfileTabContent)"
      via: "<ProfileTabContent paramsPromise={params} /> inside <Suspense>"
      pattern: "ProfileTabContent\\s+paramsPromise"
    - from: "src/app/u/[username]/[tab]/page.tsx (inner ProfileTabContent)"
      to: "src/app/u/[username]/profile-shell-resolver.tsx"
      via: "await ProfileShellResolver({ username }) — UNCHANGED from current behavior"
      pattern: "await\\s+ProfileShellResolver"
---

<objective>
Restructure `src/app/u/[username]/[tab]/page.tsx` per RESEARCH.md Pattern 2 / instant-navigation.md ProductPage canonical example: outer `ProfileTabPage` becomes SYNC and returns ONLY `<Suspense fallback={<ProfileTabContentSkeleton/>}><ProfileTabContent paramsPromise={params}/></Suspense>`. The original body (368 lines including imports, VALID_TABS list, viewer resolution, ProfileShellResolver call, all tab branching, notFound calls, locked-tab cards, etc.) moves VERBATIM into a new inner async function `ProfileTabContent({ paramsPromise })`. The only mechanical changes are the function declaration keyword, the function name, and the parameter name (`params` → `paramsPromise`, then `await paramsPromise` inside).

Purpose: Eliminate top-level awaits in the page (the second half of the recurrence-4 root cause) by deferring runtime API resolution into a Suspense-wrapped inner component. This satisfies REQ-52-04 (the second half — Plan 04 covered the layout's ProfileChrome). The page's Suspense uses `ProfileTabContentSkeleton` (the narrow content-area skeleton) per D-52-15 — distinct from the layout's `ProfileShellSkeleton` (the full chrome skeleton).

This is the LAST structural change Wave 2 makes. After this plan ships, the canonical Next 16 Cache Components pattern is implemented end-to-end on `/u/[username]/[tab]`: sync layout → Suspense → async ProfileChrome → ProfileGate → sync page → Suspense → async ProfileTabContent → resolver + tab content.

Output: 1 file refactored in place. Test 5 in `tests/profile-route-51.test.ts` flips from FAIL to PASS. The build-time validator (added in Plan 03) now produces zero structural errors on `/u/[username]/[tab]` — the recurrence-5 prevention contract is fully live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-CONTEXT.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-RESEARCH.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-PATTERNS.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-03-VALIDATOR-OUTPUT.md

@src/app/u/[username]/[tab]/page.tsx
@src/app/u/[username]/profile-shell-skeleton.tsx
@src/app/u/[username]/profile-shell-resolver.tsx

<interfaces>
Target shape (from RESEARCH.md Pattern 2 lines 252-279 + audit followup Step 2 lines 289-319):

Outer sync component:
- `export default function ProfileTabPage({ params }: { params: Promise<{ username: string; tab: string }> })` (NOT async)
- returns ONLY `<Suspense fallback={<ProfileTabContentSkeleton />}><ProfileTabContent paramsPromise={params} /></Suspense>`
- no other logic in the outer function

Inner async component (NOT exported, file-local):
- `async function ProfileTabContent({ paramsPromise }: { paramsPromise: Promise<{ username: string; tab: string }> })`
- body line 1: `const { username, tab } = await paramsPromise`
- remainder: the original 311 lines of page body, verbatim, no logic changes

Imports added:
- `import { Suspense } from 'react'`
- `import { ProfileTabContentSkeleton } from '../profile-shell-skeleton'` (sibling-directory import — confirm exact path from existing imports list)

`unstable_instant` export (added in Plan 03) stays at the top of the file unchanged.

Phase 39c Pitfall 5 / D-52-CF-03 preservation:
- `notFound()` on the invalid-tab check (line 58 currently): stays at the START of ProfileTabContent body, immediately after `await paramsPromise` destructuring + before any other awaits
- `notFound()` on the missing-profile check (line 79 currently): stays AFTER `await ProfileShellResolver({ username })` and BEFORE any subsequent awaits (`isFollowing`, `resolveCommonGround`, per-tab data fetches)
- Ordering invariant: validate tab → resolve viewer (via getCurrentUser) → resolve profile (via ProfileShellResolver) → notFound if missing → continue with viewer-dependent fetches
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist [tab]/page.tsx body into inner async ProfileTabContent; outer becomes sync</name>
  <files>src/app/u/[username]/[tab]/page.tsx</files>
  <read_first>
    - src/app/u/[username]/[tab]/page.tsx (current 368-line file in full — every line gets either preserved verbatim inside ProfileTabContent OR removed from the outer wrapper. There are NO other places where the file is touched outside the function-signature transform.)
    - src/app/u/[username]/profile-shell-skeleton.tsx — confirm `ProfileTabContentSkeleton` is a named export
    - .planning/phases/52-.../52-RESEARCH.md § Pattern 2 (lines 241-279) — verbatim target shape
    - .planning/phases/52-.../52-PATTERNS.md § [tab]/page.tsx (lines 150-210) — verbatim refactor instructions; explicitly notes "body moves verbatim"
    - .planning/audits/cache-components-2026-05-21-followup.md § Step 2 page.tsx subsection (lines 286-319) — the audit followup's canonical shape with explicit "all the existing page logic ... moves here verbatim" instruction
    - .planning/phases/52-.../52-03-VALIDATOR-OUTPUT.md — confirm the validator flagged the page's top-level awaits as expected and that no inner-component-shape surprises surfaced
    - tests/profile-route-51.test.ts Test 5 (added in Plan 01) — the assertions this refactor must satisfy
  </read_first>
  <action>
Restructure `src/app/u/[username]/[tab]/page.tsx` end-to-end in a single atomic edit. The change is best described as a "function-signature transform": the existing body becomes the body of a new inner function; the outer wrapper becomes a 1-line Suspense-around-inner-component pattern.

Five surgical changes; apply in this exact order to keep the diff reviewable:

1. **Add `import { Suspense } from 'react'`** — insert at the top of the import block, BEFORE the `import { notFound } from 'next/navigation'` line. New first import line.

2. **Add `ProfileTabContentSkeleton` to the existing `'../profile-shell-skeleton'` import** — the file currently does NOT import from `profile-shell-skeleton.tsx` directly. Add a new import line: `import { ProfileTabContentSkeleton } from '../profile-shell-skeleton'`. Place it after the existing `from '../profile-shell-resolver'` import line for grouping.

3. **Leave the `unstable_instant` export (added in Plan 03) and the stale comment block at lines 33-39 UNCHANGED** — Plan 09 rewrites the stale comment. Plan 05 does NOT touch it. The `unstable_instant` line stays where Plan 03 put it.

4. **Leave `VALID_TABS` const + `Tab` type UNCHANGED** — these stay at module scope between the comment block and the outer function (lines 41-50 currently).

5. **Transform the outer `ProfileTabPage` function** — this is the core refactor:

   a. Change the function declaration from `export default async function ProfileTabPage(...)` to `export default function ProfileTabPage(...)` — remove `async`.

   b. Replace the entire body of `ProfileTabPage` (currently lines 57-368 — everything from `const { username, tab } = await params` through the final closing brace of the function) with EXACTLY:
   ```
   return (
     <Suspense fallback={<ProfileTabContentSkeleton />}>
       <ProfileTabContent paramsPromise={params} />
     </Suspense>
   )
   ```
   The outer function body is now exactly 5 lines (return + Suspense open + ProfileTabContent + Suspense close + close paren) plus surrounding braces.

   c. The outer function's signature stays exactly as-is — `({ params }: { params: Promise<{ username: string; tab: string }> })`. The `params` Promise is passed unchanged into `ProfileTabContent` as `paramsPromise={params}` — DO NOT destructure or await it in the outer function.

6. **Add a new inner async function `ProfileTabContent`** — paste the entire ORIGINAL body of `ProfileTabPage` (the 311 lines that lived between `const { username, tab } = await params` and the function's final closing brace) into a new file-local async function:

   ```
   async function ProfileTabContent({ paramsPromise }: { paramsPromise: Promise<{ username: string; tab: string }> }) {
     const { username, tab } = await paramsPromise
     if (!VALID_TABS.includes(tab as Tab)) notFound()
     ... (everything from current line 59 onwards, verbatim) ...
   }
   ```

   Critical preservation rules (D-52-CF-03 / Phase 39c Pitfall 5):
   - The FIRST line of the function body is `const { username, tab } = await paramsPromise` — the ONLY mechanical change from the original (was `await params`)
   - The SECOND line is `if (!VALID_TABS.includes(tab as Tab)) notFound()` — UNCHANGED from current line 58; this is the first notFound call and it MUST stay before any post-suspending await
   - The viewer resolution try/catch (current lines 64-69) stays UNCHANGED
   - The `await ProfileShellResolver({ username })` call (current line 78) stays UNCHANGED
   - The `if (!resolved.profile) notFound()` (current line 79) stays UNCHANGED in its exact position — AFTER the resolver, BEFORE subsequent fetches
   - All tab-specific branching (current lines 117-368: common-ground, insights, collection lock, wishlist lock, worn lock, notes lock, stats lock, plus the JSX for each public branch) stays VERBATIM
   - All imports referenced inside the body stay (notFound, Link, Card components, getCurrentUser, UnauthorizedError, ProfileShellResolver, tab content components, lib helpers — every existing import is still needed by ProfileTabContent)
   - Do NOT extract any helpers, refactor any branches, or "improve" any logic. The diff is purely mechanical.

   Inner function placement: insert IMMEDIATELY AFTER the outer `ProfileTabPage` function's closing brace, at module scope (NOT nested inside the outer function — file-local functions, both at the top level).

7. **Top-of-file comment refresh** — REMOVE the stale paragraph at current lines 98-104 (the "Phase 51 post-merge tab-UX refinement" block that talks about "chrome composition lives at the layout level via `<ProfileGate>` in `../layout.tsx`"). Reason: it's now accurate but misplaced — that paragraph was an implementation note for the old outer-function body and no longer makes sense at the new outer function. Move its substantive content into the inner `ProfileTabContent`'s top-of-body comment block, citing Phase 52 + D-52-16 / D-52-CF-03 (Pitfall 5 preservation) + RESEARCH.md Pattern 2.

   Add a new top-of-file comment block (BEFORE the `unstable_instant` export, AFTER the imports) explaining:
   - Phase 52 D-52-16 structural lock applied: outer is sync (returns Suspense around inner); inner is async (does runtime API access + the full tab body)
   - The `unstable_instant` export is the validator gate; the Suspense + inner-async structure is what produces a valid instant-nav contract
   - D-52-CF-03 / Phase 39c Pitfall 5 explicitly preserved: notFound() calls remain in their original ordering relative to suspending awaits
   - Citation: RESEARCH.md Pattern 2, audit followup Step 2, instant-navigation.md "ProductPage" canonical example

8. **Do NOT delete the stale Phase 39c comment at current lines 33-39** ("Phase 39c D-39c-07 unstable_instant export was REMOVED 2026-05-14") — Plan 09 owns rewriting it. Leaving it in place here keeps the diff narrowly mechanical.

After this task:
- File grows by roughly 4-6 lines (new imports + new comment + new outer return + inner-function signature) but the line count is dominated by the inner ProfileTabContent body which is the original page body verbatim
- The outer ProfileTabPage is ~7 lines total (signature + return-Suspense block)
- Test 5 in `tests/profile-route-51.test.ts` flips from FAIL to PASS — `ProfileTabContent` matches; `<Suspense` matches; `ProfileTabContent paramsPromise=` matches
- All 5 tests in tests/profile-route-51.test.ts now PASS (Tests 1+4 from Plan 04 still pass; Tests 2+3 unchanged; Test 5 newly passes)
- The build-time validator (Plan 03's `unstable_instant` export) now produces zero structural errors on this route — Wave 1's hypothesis is confirmed (or, if cross-route violations surfaced, Plan 07 handles those separately)
- All existing tab-specific tests under tests/app/profile-tab-*.test.tsx + tests/app/profile-layout.test.tsx still pass — verify in the verify step below
  </action>
  <verify>
    <automated>npx vitest run tests/profile-route-51.test.ts tests/app/profile-tab-insights.test.tsx tests/app/profile-layout.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: file does NOT contain `export default async function ProfileTabPage` — `grep -c "export default async function ProfileTabPage" src/app/u/[username]/[tab]/page.tsx` returns 0
    - Source: file contains `export default function ProfileTabPage` (sync) — `grep -c "export default function ProfileTabPage" src/app/u/[username]/[tab]/page.tsx` returns 1
    - Source: file contains exactly one `async function ProfileTabContent` declaration — `grep -c "async function ProfileTabContent" src/app/u/[username]/[tab]/page.tsx` returns 1
    - Source: file contains `<Suspense fallback={<ProfileTabContentSkeleton` — verifies the outer JSX shape
    - Source: file contains `ProfileTabContent paramsPromise={params}` — outer passes the Promise unchanged to the inner
    - Source: inner function body contains `await paramsPromise` (NOT `await params`) — verifies the parameter rename happened
    - Source: file imports `Suspense` from `react` and `ProfileTabContentSkeleton` from `../profile-shell-skeleton`
    - Source: `unstable_instant` export (from Plan 03) is still present unchanged
    - Source: file contains `if (!VALID_TABS.includes(tab as Tab)) notFound()` — invalid-tab check survives verbatim
    - Source: file contains `if (!resolved.profile) notFound()` — missing-profile check survives verbatim, in the position AFTER `await ProfileShellResolver({ username })`
    - Source: file line count is in the range [365, 380] — original 368 + ~5 lines (new imports + Suspense scaffold)
    - Behavior: `npx vitest run tests/profile-route-51.test.ts` exits 0 — all 5 tests PASS (Test 5 flipped from FAIL to PASS)
    - Behavior: `npx vitest run tests/app/profile-tab-insights.test.tsx tests/app/profile-layout.test.tsx` exits 0 — no regressions in tab-content tests
    - Behavior: `npx tsc --noEmit` exits 0 — TypeScript type checks pass (inner function's params/return type aligned)
    - Behavior: `npm run build` exits 0 on the `/u/[username]/[tab]` route — the `unstable_instant` validator no longer surfaces in-route structural errors (cross-route findings are Plan 07's scope). If `npm run build` still fails on cross-route violations from Wave 1's VALIDATOR-OUTPUT.md, that's expected; Plan 07 closes those.
  </acceptance_criteria>
  <done>
`src/app/u/[username]/[tab]/page.tsx` outer sync wrapping inner async; full vitest suite green for profile-route tests; type-check passes; in-route validator errors resolved. Plan 06 then closes any cross-route findings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request→ProfileTabPage outer | Sync; no runtime API access. Pure JSX scaffold. |
| ProfileTabPage outer→ProfileTabContent inner | params Promise passed as prop; inner resolves inside Suspense. |
| ProfileTabContent→ProfileShellResolver | Cached, viewer-independent. Receives only `{ username }`. |
| ProfileTabContent→Server Actions (revalidation chain) | revalidatePath('/u/[username]', 'layout') still works post-refactor — path-based invalidation is topology-agnostic (per RESEARCH.md A2 assumption). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05-01 | Information Disclosure | Viewer-dependent data leaking via cached resolver (Phase 39c Pitfall 1) | mitigate | ProfileShellResolver still receives ONLY `{ username }` (unchanged from current behavior). viewerId is resolved inside ProfileTabContent (uncached async, inside Suspense) and used for isOwner / privacy gates / common-ground resolution. Acceptance criterion verifies the inner function structure preserves this. |
| T-52-05-02 | Information Disclosure | `notFound()` called AFTER a suspending await (Phase 39c Pitfall 5 / loading.md status code rule) | mitigate | The invalid-tab notFound() stays at the first line of ProfileTabContent body (before viewer resolution); the missing-profile notFound() stays immediately after the resolver and before subsequent awaits. Verbatim ordering preservation enforced by the "body moves verbatim" rule and the source-grep acceptance criteria. (D-52-CF-03) |
| T-52-05-03 | Tampering | Future regression — someone re-adds `async` to ProfileTabPage outer | mitigate | tests/profile-route-51.test.ts Test 5 asserts `async\s+function\s+ProfileTabContent` (inner present) AND the page's outer Suspense JSX. A regression that moves the body back to the outer function would fail Test 5. |
| T-52-05-04 | Information Disclosure | LockedTabCard branches leak existence of private tabs (PRIV-02/03/04, T-39c-04) | accept | Existing per-tab privacy gates inside ProfileTabContent are preserved verbatim (collection / wishlist / worn / notes / stats locked-state cards + insights owner-only notFound). No change to gate logic. |
</threat_model>

<verification>
1. `npx vitest run tests/profile-route-51.test.ts` — all 5 tests PASS
2. `npx vitest run tests/app/profile-tab-insights.test.tsx tests/app/profile-layout.test.tsx tests/app/profile-tab-stats.test.tsx 2>/dev/null || true` — no regressions
3. `npx tsc --noEmit` — exits 0
4. `grep -c "async function ProfileTabContent" src/app/u/[username]/[tab]/page.tsx` — returns 1
5. `grep -c "export default async function ProfileTabPage" src/app/u/[username]/[tab]/page.tsx` — returns 0
6. `grep -c "<Suspense fallback={<ProfileTabContentSkeleton" src/app/u/[username]/[tab]/page.tsx` — returns 1
</verification>

<success_criteria>
- [ ] Outer ProfileTabPage is sync (no `async`)
- [ ] Outer ProfileTabPage body is ONLY the Suspense + ProfileTabContent JSX
- [ ] Inner async ProfileTabContent exists at module scope (NOT nested)
- [ ] Inner ProfileTabContent body is the original page body verbatim (only `params` → `paramsPromise` rename + `await paramsPromise` line 1)
- [ ] All notFound() calls preserved in their existing relative positions (D-52-CF-03)
- [ ] All tab-specific branching preserved verbatim
- [ ] Suspense + ProfileTabContentSkeleton imports added
- [ ] `unstable_instant` export from Plan 03 unchanged
- [ ] All 5 vitest tests in tests/profile-route-51.test.ts PASS
- [ ] tests/app/profile-tab-*.test.tsx + profile-layout.test.tsx all green
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npm run build` exits 0 on the `/u/[username]/[tab]` route (cross-route findings, if any, addressed by Plan 06)
</success_criteria>

<output>
After completion, create `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-05-SUMMARY.md` per the standard summary template. Include:
  - Vitest output showing 5/5 tests pass in tests/profile-route-51.test.ts
  - `npm run build` output for the `/u/[username]/[tab]` route — confirm no validator errors (cross-route findings, if any, deferred to Plan 06)
  - Line-count before/after for `src/app/u/[username]/[tab]/page.tsx`
  - Confirmation that Phase 39c Pitfall 5 ordering is preserved (notFound calls in same relative positions)

Update `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-VALIDATION.md` Per-Task Verification Map with row:
| 52-05-01 | 05 | 2 | REQ-52-04 (part 2 — ProfileTabContent inner) | T-52-05-01, T-52-05-02, T-52-05-03 | Outer sync + inner async with verbatim body; notFound ordering preserved | unit (source-grep) + build | `npx vitest run tests/profile-route-51.test.ts` (5/5 pass) + `npm run build` (exit 0) | ✅ | ⬜ pending |
</output>
