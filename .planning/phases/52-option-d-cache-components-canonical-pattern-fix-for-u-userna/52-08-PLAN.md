---
phase: 52
plan: 08
type: execute
wave: 3
depends_on: [04, 05]
files_modified:
  - src/app/u/[username]/[tab]/page.tsx
  - src/app/u/[username]/loading.tsx
  - src/app/u/[username]/profile-gate.tsx
  - .planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md
autonomous: true
requirements: [REQ-52-09]

must_haves:
  truths:
    - "src/app/u/[username]/[tab]/page.tsx comment block at lines 33-39 is REPLACED with a Phase 52 D-52-11 diagnosis-reversal note + citation of the audit followup + canonical reference to RESEARCH.md"
    - "src/app/u/[username]/loading.tsx comment block at lines 3-13 is REWRITTEN to describe Phase 52's THREE-boundary structure as the single source of truth (D-52-14)"
    - "src/app/u/[username]/profile-gate.tsx top-of-file JSDoc block updates the 'caller' attribution from 'the page' (line 14) to 'ProfileChrome' — the new runtime-API consumer"
    - "Phase 51 51-CONTEXT.md gains a <decisions> annotation flagging the `unstable_instant`-on-blocklist entry as REVERSED by Phase 52 D-52-11 (preserves historical record; corrects future readers)"
    - "If any `.continue-here.md` file under .planning/ references `unstable_instant`, it is revised per D-52-12 (preemptive grep — likely no-op)"
  artifacts:
    - path: "src/app/u/[username]/[tab]/page.tsx"
      provides: "Rewritten comment block at lines 33-39 — reflects Phase 52 diagnosis reversal"
      contains: "D-52-11"
    - path: "src/app/u/[username]/loading.tsx"
      provides: "Rewritten comment block — three-boundary structure described (layout Suspense + page Suspense + loading.tsx implicit Suspense)"
      contains: "three-boundary"
    - path: "src/app/u/[username]/profile-gate.tsx"
      provides: "Caller attribution updated to ProfileChrome"
      contains: "ProfileChrome"
    - path: ".planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md"
      provides: "<decisions> annotation citing D-52-11 reversal"
      contains: "REVERSED by Phase 52"
  key_links:
    - from: "src/app/u/[username]/[tab]/page.tsx (rewritten comment)"
      to: ".planning/phases/52-.../52-CONTEXT.md + .planning/audits/cache-components-2026-05-21-followup.md"
      via: "inline cite block"
      pattern: "52-CONTEXT|cache-components-2026-05-21-followup"
    - from: ".planning/phases/51-.../51-CONTEXT.md (new annotation)"
      to: ".planning/phases/52-.../52-CONTEXT.md D-52-11"
      via: "REVERSED note in <decisions> block"
      pattern: "REVERSED.*Phase 52"
---

<objective>
Apply Phase 52's documentation-reversal updates (D-52-11, D-52-12, D-52-14) across four files. The diagnosis inversion (RESEARCH.md § Summary lines 80-88 + audit followup § "What changed since the original audit" lines 47-60) is load-bearing: Phase 39c and Phase 51 placed `unstable_instant` on a "failed-attempt blocklist" believing it CAUSED the recurrence-2 prod 404s. Phase 52 reversed that — the instant-navigation doc reveals `unstable_instant` is a VALIDATOR, not a runtime feature. Future sessions reading any of these comments / decisions must see the corrected story, not the misdiagnosis.

Purpose: Phase 52 ships clean documentation alongside the structural fix. Without these doc updates, the next session reading `src/app/u/[username]/[tab]/page.tsx:33-39` (the "REMOVED 2026-05-14" comment) would re-introduce the misdiagnosis. Same for `loading.tsx`'s reference to a no-longer-existent layout Suspense. Same for `profile-gate.tsx`'s reference to "the page" as the caller (now ProfileChrome). Same for Phase 51's `51-CONTEXT.md` blocklist entry.

This plan runs in Wave 3 alongside Plan 07 (cleanups), but is file-disjoint — they can land in parallel. Order doesn't matter; both consume Wave 2's outputs.

Output: 4 files modified in place; no source-file deletions; comment-block-only edits (no behavioral changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-CONTEXT.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-RESEARCH.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-PATTERNS.md
@.planning/audits/cache-components-2026-05-21-followup.md

@src/app/u/[username]/[tab]/page.tsx
@src/app/u/[username]/loading.tsx
@src/app/u/[username]/profile-gate.tsx
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md

<interfaces>
The three Phase 52 Suspense boundaries (D-52-13 + RESEARCH.md Architecture Patterns):
  1. layout.tsx `<Suspense fallback={<ProfileShellSkeleton/>}>` around ProfileChrome — cold-load chrome skeleton
  2. [tab]/page.tsx `<Suspense fallback={<ProfileTabContentSkeleton/>}>` around ProfileTabContent — tab-nav content skeleton
  3. loading.tsx implicit Suspense — implicit-prefetch client-navigation case
  All three coexist; audit followup says "having all is harmless."

Diagnosis reversal cite chain:
  - 52-CONTEXT.md D-52-11 (the reversal record)
  - 52-RESEARCH.md § State of the Art > Deprecated/outdated (lines 638-641) (the corrected lesson)
  - .planning/audits/cache-components-2026-05-21-followup.md § "What changed since the original audit" lines 47-60 (the root-cause analysis)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stale comment block at src/app/u/[username]/[tab]/page.tsx:33-39</name>
  <files>src/app/u/[username]/[tab]/page.tsx</files>
  <read_first>
    - src/app/u/[username]/[tab]/page.tsx (current state post-Plan-05 — file has the `unstable_instant` export from Plan 03 + the new sync-outer/async-inner structure from Plan 05; the stale comment block at lines 33-39 is the ONLY remaining inaccurate doc element on this file)
    - .planning/phases/52-.../52-CONTEXT.md D-52-11 (the reversal record)
    - .planning/audits/cache-components-2026-05-21-followup.md § "What changed since the original audit" (lines 47-60) — the canonical narrative for the reversal
  </read_first>
  <action>
Locate the comment block currently at `src/app/u/[username]/[tab]/page.tsx:33-39` (after the imports, before `VALID_TABS`). Current text starts with "// Phase 39c D-39c-07 unstable_instant export was REMOVED 2026-05-14 ..." (the misdiagnosis record).

REPLACE this block (do not delete and re-insert at a different line — replace in place) with a corrected comment block describing the Phase 52 reversal. The new block sits BETWEEN the imports (ending at line 31 currently) and the `unstable_instant` export added by Plan 03.

Required content of the new comment block:

1. Reference Phase 52 D-52-11 (the reversal decision ID)
2. State the corrected diagnosis verbatim: `unstable_instant` is a VALIDATOR, not a runtime feature. The Phase 39c-era removal of the export was based on a misreading of the symptom — it removed the validation, not the bug
3. Cite the audit followup: `.planning/audits/cache-components-2026-05-21-followup.md` § "What changed since the original audit"
4. Cite the canonical doc page: `node_modules/next/dist/docs/01-app/02-guides/instant-navigation.md` ("How validation works" section)
5. Briefly note: the `unstable_instant = { prefetch: 'static' }` export below this comment IS the recurrence-5 prevention contract (D-52-03 hard CI gate); the sync-outer / async-inner ProfileTabContent structure is what produces a valid instant-nav contract
6. End with a pointer to `.planning/phases/52-.../52-CONTEXT.md` for the full decision record

The block should be 10-15 lines. Replace the existing 7 lines (lines 33-39). Result: file grows by 5-8 lines in that region.

Do NOT modify any other line in this file. The `unstable_instant` export (Plan 03), the new top-of-file Phase 52 comment that Plan 05 added BEFORE the export, the outer ProfileTabPage (Plan 05), and the inner ProfileTabContent (Plan 05) are all unchanged by this task.

Note on ordering with Plan 05's top-of-file comment: Plan 05 added a new top-of-file comment block BEFORE the `unstable_instant` export explaining the D-52-16 structural lock. Plan 08's reversal comment sits BETWEEN that Plan 05 comment block and the `unstable_instant` export — replacing the old "REMOVED 2026-05-14" block. Confirm placement by reading the file in its post-Plan-05 state before editing.
  </action>
  <verify>
    <automated>grep -c "REMOVED 2026-05-14" src/app/u/[username]/[tab]/page.tsx | xargs -I {} test {} -eq 0 && grep -c "D-52-11\|cache-components-2026-05-21-followup\|VALIDATOR" src/app/u/[username]/[tab]/page.tsx | xargs -I {} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Source: file does NOT contain the stale phrase `REMOVED 2026-05-14` — `grep -c "REMOVED 2026-05-14" src/app/u/[username]/[tab]/page.tsx` returns 0
    - Source: file contains AT LEAST ONE of `D-52-11`, `cache-components-2026-05-21-followup`, or `VALIDATOR` (uppercase, in a comment) — verifies the reversal cite is present
    - Source: file contains `Phase 52` (or `52-CONTEXT`) at least once in a comment line — verifies the new attribution
    - Source: the `unstable_instant` export line is preserved unchanged — `grep -c "export const unstable_instant = { prefetch: 'static' }" src/app/u/[username]/[tab]/page.tsx` returns 1
    - Source: file line count within [370, 390] (Plan 05 brought us to ~373; replacement adds 5-8 lines)
    - Behavior: `npx vitest run tests/profile-route-51.test.ts` still exits 0 (all 5 tests pass — Test 4 still finds the `unstable_instant` export)
  </acceptance_criteria>
  <done>
src/app/u/[username]/[tab]/page.tsx comment block at the validator-export region is rewritten. Next session reading this file sees the Phase 52 reversal, not the Phase 39c misdiagnosis. Tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite src/app/u/[username]/loading.tsx comment block (D-52-14)</name>
  <files>src/app/u/[username]/loading.tsx</files>
  <read_first>
    - src/app/u/[username]/loading.tsx (current 17-line file — comment block at lines 3-13 is the rewrite target; the executable code at lines 14-16 is preserved unchanged)
    - .planning/phases/52-.../52-CONTEXT.md D-52-13 + D-52-14 (the three-boundary description + comment rewrite mandate)
    - .planning/phases/52-.../52-PATTERNS.md § loading.tsx (lines 213-231) — exact target shape
  </read_first>
  <action>
Rewrite the comment block at `src/app/u/[username]/loading.tsx:3-13` to describe Phase 52's three-boundary Suspense topology as the single source of truth for future debugging. Per D-52-14, the current comment references "the layout's own `<Suspense fallback={<ProfileShellSkeleton/>}>`" — that Suspense now EXISTS (Plan 04 added it), but the comment was written before Plan 04 and is structurally confusing.

Required content of the new comment block (10-15 lines):

1. State the three Phase 52 Suspense boundaries on the profile route (D-52-13):
   - **Layout-level Suspense:** `src/app/u/[username]/layout.tsx` `<Suspense fallback={<ProfileShellSkeleton/>}>` around `ProfileChrome` — cold-load case (full chrome skeleton renders while ProfileChrome resolves params + getCurrentUser inside Suspense)
   - **Page-level Suspense:** `src/app/u/[username]/[tab]/page.tsx` `<Suspense fallback={<ProfileTabContentSkeleton/>}>` around `ProfileTabContent` — tab-navigation case (chrome stays mounted from the layout subtree; only the per-tab content area swaps)
   - **`loading.tsx` implicit Suspense (THIS FILE):** Next 16's segment-loading boundary per `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md`. Used during the implicit-prefetch client-navigation case described in the linking-and-navigating docs. With Cache Components, this is treated as a regular Suspense boundary per layout.md "Interaction with loading.js."

2. Explain WHY all three boundaries coexist (audit followup verbatim: "having all is harmless"):
   - Cold load: layout Suspense fires first → ProfileShellSkeleton renders → ProfileChrome resolves → ProfileGate renders chrome → page's Suspense fires → ProfileTabContentSkeleton → ProfileTabContent resolves → tab content streams
   - Tab navigation: layout subtree already resolved (chrome mounted) → only the page Suspense fires → ProfileTabContentSkeleton briefly visible → tab content streams. `loading.tsx` is the implicit-prefetch fallback during this navigation.

3. Why we render `<ProfileTabContentSkeleton />` here (not `<ProfileShellSkeleton />`) — chrome stays mounted on client navigation; the loading boundary should match the visual state expected during a tab swap, which is "chrome stays, content updates."

4. Cite Phase 52 D-52-13 + D-52-14 + D-52-15 (skeletons intentionally distinct).

5. Cite the audit followup § Step 3 (lines 322-334) — explicit "keep all three" decision.

REPLACE the existing 11-line comment block (lines 3-13). The executable code (`export default function Loading() { return <ProfileTabContentSkeleton /> }` at lines 14-16) is preserved EXACTLY byte-for-byte.

Do NOT change the import statement at line 1 (`import { ProfileTabContentSkeleton } from './profile-shell-skeleton'`). Do NOT change the executable code. Comment-only edit at lines 3-13.

After this task: future readers see Phase 52's full topology described in one place; the previous stale claim (layout had no Suspense) is corrected.
  </action>
  <verify>
    <automated>grep -c "three-boundary\|three Phase 52\|three boundaries" src/app/u/[username]/loading.tsx | xargs -I {} test {} -ge 1 && grep -c "export default function Loading" src/app/u/[username]/loading.tsx | xargs -I {} test {} -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Source: file contains a phrase signaling the three-boundary topology — `grep -c "three-boundary\|three Phase 52\|three boundaries\|D-52-13" src/app/u/[username]/loading.tsx` returns at least 1
    - Source: file cites Phase 52 — `grep -c "Phase 52" src/app/u/[username]/loading.tsx` returns at least 1
    - Source: executable code at the bottom is unchanged — `grep -c "export default function Loading" src/app/u/[username]/loading.tsx` returns 1; `grep -c "return <ProfileTabContentSkeleton />" src/app/u/[username]/loading.tsx` returns 1
    - Source: import statement at line 1 unchanged — `grep -c "import { ProfileTabContentSkeleton } from './profile-shell-skeleton'" src/app/u/[username]/loading.tsx` returns 1
    - Source: file does NOT contain the stale claim about a non-existent layout Suspense (the original comment's "layout's own <Suspense ..." phrasing) — `grep -c "layout's own <Suspense" src/app/u/[username]/loading.tsx` returns 0
    - Source: file line count within [17, 28] (original 17 + up to ~10 added in the rewritten comment block)
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
loading.tsx comment block now describes Phase 52's three-boundary structure as the canonical source of truth. Executable code untouched. Type-check passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update profile-gate.tsx top-of-file JSDoc — caller attribution to ProfileChrome</name>
  <files>src/app/u/[username]/profile-gate.tsx</files>
  <read_first>
    - src/app/u/[username]/profile-gate.tsx (current 144-line file — JSDoc block at lines 11-35 currently says "Receives `viewerId` from the page (the runtime-API consumer)" on line 14; this attribution becomes inaccurate after Plan 04 — ProfileChrome is now the caller for the layout-level composition)
    - src/app/u/[username]/profile-chrome.tsx (created in Plan 04) — confirm ProfileChrome's role
    - .planning/phases/52-.../52-CONTEXT.md D-52-CF-02 + the profile-gate.tsx invariants
  </read_first>
  <action>
Minor JSDoc update to `src/app/u/[username]/profile-gate.tsx`. The function signature, props interface, PROHIBITED block, and all logic are UNCHANGED (D-52-CF-02 structural lock).

The ONLY change is updating the caller attribution in the JSDoc comment block at lines 11-35:

1. Find the phrase on line 14 (current text): `Receives `viewerId` from the page (the runtime-API consumer)`
   - Update to: `Receives `viewerId` from ProfileChrome (the runtime-API consumer — see ./profile-chrome.tsx)` OR an equivalent phrasing that:
     - Names ProfileChrome explicitly as the caller for the layout-level composition path
     - Notes that the page (`[tab]/page.tsx` ProfileTabContent inner) is also a caller via its own resolveCommonGround + tab-specific data fetches
     - Cites Phase 52 D-52-CF-02 (Pitfall 1 — viewer identity outside cached scope)

2. Add ONE sentence to the JSDoc body (anywhere appropriate within lines 11-35) noting Phase 52 D-52-CF-02 preservation: the gate physically cannot read cookies; the cookie read lives in ProfileChrome (uncached, async, inside Suspense per the canonical Cache Components pattern).

3. The PROHIBITED block at lines 29-35 is UNCHANGED — those invariants still hold (no `'use cache'`, no `next/cache` imports, no `@/lib/auth` import, no cookie reads via any means).

4. The function signature at lines 36-55 is UNCHANGED.

5. All logic at lines 56-143 is UNCHANGED.

After this task: profile-gate.tsx's JSDoc accurately describes the new caller chain (layout → Suspense → ProfileChrome → ProfileGate). The Phase 51 51-02 contract (viewerId-as-prop) is preserved verbatim.
  </action>
  <verify>
    <automated>grep -c "ProfileChrome" src/app/u/[username]/profile-gate.tsx | xargs -I {} test {} -ge 1 && npx vitest run tests/profile-route-51.test.ts -t "ProfileGate" --reporter=verbose 2>&1 | grep -E "(PASS|✓)" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Source: file contains `ProfileChrome` (verified via `grep -c "ProfileChrome" src/app/u/[username]/profile-gate.tsx` returning at least 1)
    - Source: function signature unchanged — `grep -c "export async function ProfileGate" src/app/u/[username]/profile-gate.tsx` returns 1
    - Source: PROHIBITED block at the JSDoc unchanged — `grep -c "PROHIBITED inside this file" src/app/u/[username]/profile-gate.tsx` returns 1; `grep -c "Importing from \`@/lib/auth\`" src/app/u/[username]/profile-gate.tsx` returns 1
    - Source: no `getCurrentUser` import or call introduced — `grep -c "getCurrentUser" src/app/u/[username]/profile-gate.tsx` returns 0 (Test 2 of tests/profile-route-51.test.ts asserts this)
    - Behavior: `npx vitest run tests/profile-route-51.test.ts -t "ProfileGate"` exits 0 (Test 2 still passes)
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
profile-gate.tsx JSDoc attribution updated; function body and PROHIBITED invariants preserved. Test 2 passes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Annotate Phase 51 51-CONTEXT.md with D-52-11 reversal note + preemptive .continue-here.md grep</name>
  <files>.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md</files>
  <read_first>
    - .planning/phases/51-.../51-CONTEXT.md (the full file; locate the `<decisions>` block and any reference to `unstable_instant`)
    - .planning/phases/52-.../52-CONTEXT.md D-52-11 + D-52-12 (the reversal + the preemptive grep)
  </read_first>
  <action>
Two sub-changes; both are doc-only edits in planning files.

Sub-change A — Annotate Phase 51 CONTEXT.md `<decisions>` block:
1. Open `.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md`
2. Find the `<decisions>` block (it should contain a Phase 51 decision that placed `unstable_instant` on a "failed-attempt blocklist" — typically a bullet or paragraph mentioning "blocklist" or "REMOVED 2026-05-14" or the F3-Composite framing)
3. Add a NEW annotation BLOCK (do NOT delete the original entry — preserve the historical record) immediately following the affected decision entry. Format:
   ```
   > **⚠ REVERSED by Phase 52 (D-52-11):** This blocklist entry was based on a
   > misdiagnosis. Per the deep-docs research captured in
   > `.planning/audits/cache-components-2026-05-21-followup.md` and the
   > Phase 52 `52-RESEARCH.md`, `unstable_instant` is a VALIDATOR (not a
   > runtime feature). Phase 39c's removal of the export removed the
   > validation, not the bug. Phase 52 re-introduced
   > `unstable_instant = { prefetch: 'static' }` as the recurrence-5
   > prevention contract. See
   > `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-CONTEXT.md`
   > for the full decision record.
   ```
4. Use blockquote (`>`) prefix per the convention shown above so the annotation is visually distinct from the original decision text.
5. Place the annotation IMMEDIATELY AFTER the original decision (do not insert at the top of the `<decisions>` block — keep it associated with the entry it corrects).

Sub-change B — Preemptive `.continue-here.md` grep (D-52-12):
1. Run `grep -rn "unstable_instant" .planning/ --include="*.continue-here.md" 2>/dev/null` to find any session-continuity files referencing the export
2. Expected outcome: NO matches (Phase 51's directory has no `.continue-here.md`; this is documented in 52-CONTEXT.md as preemptive). If indeed no matches, no further action — Task 4 records this as "no .continue-here.md anti-pattern found; D-52-12 preemptive grep clean."
3. If matches ARE found: read each file and revise to reflect the corrected diagnosis (apply the same annotation format as Sub-change A). Each revision is a separate inline edit. Do NOT delete the file; preserve the historical context with the correction blockquote.

After this task: future sessions reading Phase 51 CONTEXT.md see both the original blocklist decision AND the Phase 52 reversal — the audit trail is preserved, the corrected diagnosis is visible.
  </action>
  <verify>
    <automated>grep -c "REVERSED by Phase 52" .planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md | xargs -I {} test {} -ge 1 && grep -rn "unstable_instant" .planning/ --include='*.continue-here.md' 2>/dev/null | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/phases/51-.../51-CONTEXT.md` contains the literal phrase `REVERSED by Phase 52` — `grep -c "REVERSED by Phase 52" .planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md` returns at least 1
    - Source: the annotation is in blockquote format — `grep -c "^>.*REVERSED" .planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md` returns at least 1
    - Source: the original blocklist entry is PRESERVED (not deleted) — verify by reading the file and confirming the original text + the new annotation both exist
    - Source: no `.continue-here.md` files under `.planning/` reference `unstable_instant` (verified via `grep -rn "unstable_instant" .planning/ --include='*.continue-here.md' 2>/dev/null | wc -l` returning 0) — if any DO reference it, they are revised with the same annotation
  </acceptance_criteria>
  <done>
Phase 51 CONTEXT.md gains the reversal annotation; historical record preserved; future sessions see the correction inline. Preemptive .continue-here.md grep complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comment block→reader | Documentation-only edits. No runtime data crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-08-01 | Repudiation | Historical Phase 51 decision deleted instead of annotated | mitigate | Task 4 explicitly preserves the original entry and ADDS an annotation block; acceptance criterion enforces both elements exist. Reviewers can verify via `git diff` showing addition-only on the original entry's lines. |
| T-52-08-02 | Tampering | Comment-only edit accidentally touches executable code | mitigate | Tasks 1, 2, 3 each have an acceptance criterion that verifies the executable code is byte-for-byte unchanged (Task 1: `unstable_instant` export line; Task 2: `export default function Loading` + return; Task 3: `export async function ProfileGate` signature). |
</threat_model>

<verification>
1. `npx vitest run tests/profile-route-51.test.ts` — all 5 tests pass (no regressions from comment edits)
2. `grep -c "REMOVED 2026-05-14" src/app/u/[username]/[tab]/page.tsx` — returns 0
3. `grep -c "three-boundary\|three Phase 52\|three boundaries\|D-52-13" src/app/u/[username]/loading.tsx` — returns at least 1
4. `grep -c "ProfileChrome" src/app/u/[username]/profile-gate.tsx` — returns at least 1
5. `grep -c "REVERSED by Phase 52" .planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md` — returns at least 1
6. `npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- [ ] `[tab]/page.tsx:33-39` comment block REPLACED with Phase 52 D-52-11 reversal note
- [ ] `loading.tsx` comment block REWRITTEN to describe three-boundary structure (D-52-14)
- [ ] `profile-gate.tsx` JSDoc updated to attribute ProfileChrome as the layout-level caller
- [ ] Phase 51 `51-CONTEXT.md` annotated with REVERSED-by-Phase-52 blockquote (D-52-11)
- [ ] D-52-12 preemptive `.continue-here.md` grep complete (likely no-op, confirmed)
- [ ] All vitest tests pass (no regressions from doc edits)
- [ ] Type-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-08-SUMMARY.md` per the standard summary template. Include:
  - Diff snippet for each of the four files (just the changed comment ranges; not the full file)
  - Confirmation that all 5 vitest tests still pass
  - Result of the D-52-12 .continue-here.md grep (no-op or list of revised files)

Update `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-VALIDATION.md` Per-Task Verification Map with rows:
| 52-08-01 | 08 | 3 | REQ-52-09 (D-52-11 reversal in [tab]/page.tsx) | T-52-08-02 | Comment block reflects validator-not-runtime-feature framing | unit (source-grep) | `grep -c "REMOVED 2026-05-14" src/app/u/[username]/[tab]/page.tsx` returns 0 | ✅ | ⬜ pending |
| 52-08-02 | 08 | 3 | REQ-52-09 (D-52-14 loading.tsx rewrite) | T-52-08-02 | Three-boundary description present | unit (source-grep) | `grep -c "three-boundary\|three boundaries" src/app/u/[username]/loading.tsx` >= 1 | ✅ | ⬜ pending |
| 52-08-03 | 08 | 3 | REQ-52-09 (profile-gate.tsx attribution) | T-52-08-02 | ProfileChrome named as caller in JSDoc | unit (source-grep) | `grep -c "ProfileChrome" src/app/u/[username]/profile-gate.tsx` >= 1 | ✅ | ⬜ pending |
| 52-08-04 | 08 | 3 | REQ-52-09 (D-52-11 annotation in 51-CONTEXT.md + D-52-12 grep) | T-52-08-01 | Phase 51 CONTEXT preserved + annotated | grep | `grep -c "REVERSED by Phase 52" .planning/phases/51-.../51-CONTEXT.md` >= 1 | ✅ | ⬜ pending |
</output>
