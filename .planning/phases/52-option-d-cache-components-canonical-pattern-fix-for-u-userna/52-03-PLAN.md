---
phase: 52
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/app/u/[username]/[tab]/page.tsx
  - .planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-03-VALIDATOR-OUTPUT.md
autonomous: false
requirements: [REQ-52-01, REQ-52-02]

must_haves:
  truths:
    - "src/app/u/[username]/[tab]/page.tsx exports `unstable_instant = { prefetch: 'static' }` at the top of the file"
    - "Running `npm run dev` (with .next/ cleared) surfaces overlay errors on /u/[username]/[tab] — the validator output is captured verbatim"
    - "Running `npm run build` exits NON-ZERO with structural errors identifying the offending components — the build output is captured verbatim"
    - "52-03-VALIDATOR-OUTPUT.md exists in the phase directory with the full dev + build error transcripts, parsed by component name, and a numbered list of refactor sites for Wave 2 to consume"
    - "tests/profile-route-51.test.ts Test 4 (REQ-52-01 — unstable_instant export present) now PASSES (was failing in Plan 01)"
  artifacts:
    - path: "src/app/u/[username]/[tab]/page.tsx"
      provides: "Single-line `unstable_instant` export added at the top of the file"
      contains: "export const unstable_instant = { prefetch: 'static' }"
    - path: ".planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-03-VALIDATOR-OUTPUT.md"
      provides: "Verbatim dev + build error transcripts + parsed refactor site list for Wave 2"
      contains: "validator output"
  key_links:
    - from: "src/app/u/[username]/[tab]/page.tsx"
      to: "Next 16 build/dev validator (instant.md prefetch:'static' validator)"
      via: "route-segment config export"
      pattern: "export\\s+const\\s+unstable_instant"
    - from: "52-03-VALIDATOR-OUTPUT.md"
      to: "Wave 2 Plans 04/05/06"
      via: "executor reads this artifact to drive refactor scope"
      pattern: "VALIDATOR-OUTPUT"
---

<objective>
Execute Step 1 of the audit followup (one-line change probe) to surface the canonical Next 16 Cache Components validator's verdict on `/u/[username]/[tab]`. Add ONLY `export const unstable_instant = { prefetch: 'static' }` to `src/app/u/[username]/[tab]/page.tsx`. Then run `npm run dev` and `npm run build` with `.next/` cleared. Capture both error transcripts verbatim in a new artifact `52-03-VALIDATOR-OUTPUT.md` that Wave 2 plans will consume.

Purpose: The validator output is the GROUND TRUTH for the actual scope of the structural fix (audit followup Step 1 verbatim: "Do not guess — let the validator tell us"). The working hypothesis is that the validator surfaces 5 errors (top-level awaits in layout + page + ProfileShellResolver), but possible surprises include cross-route violations in `/u/[username]/followers`, `/u/[username]/following`, or other PPR-classified routes. Wave 2's refactor scope (Plans 04/05/06) depends on what the validator actually says — not on what the planner guessed.

Output: One source file modified (one line added); one artifact file created with the verbatim validator transcript + a parsed refactor site list. NON-AUTONOMOUS: operator must run `npm run dev` (foreground) and copy the overlay output, and run `npm run build` (foreground) and capture stderr. This is Claude-driven on the source-modify side and operator-driven on the capture side.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-CONTEXT.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-RESEARCH.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-PATTERNS.md
@.planning/audits/cache-components-2026-05-21-followup.md

@src/app/u/[username]/[tab]/page.tsx

<interfaces>
<!-- unstable_instant route-segment config export contract (from instant.md):
       export const unstable_instant = {
         prefetch: 'static',
       }
     Works ONLY with cacheComponents: true (already true in next.config.ts). Validates at dev + build time. -->
<!-- Per RESEARCH.md Pitfall 5: `.next/` cache may serve stale validator output. Clear it before measuring. -->
<!-- Per audit followup Step 1: Do NOT pre-implement Step 2 (the refactor). This plan is ONLY the validator probe. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `unstable_instant` export to [tab]/page.tsx</name>
  <files>src/app/u/[username]/[tab]/page.tsx</files>
  <read_first>
    - src/app/u/[username]/[tab]/page.tsx (current 368-line file — the stale comment block at lines 33–39 stays in place for this plan; Plan 09 in Wave 3 rewrites the comment block. The `export const unstable_instant` line goes BEFORE that comment block.)
    - .planning/audits/cache-components-2026-05-21-followup.md § Step 1 (lines 188–217) — verbatim instruction
    - .planning/phases/52-.../52-RESEARCH.md § Code Examples > `unstable_instant` Export (lines 505–512)
  </read_first>
  <action>
Modify `src/app/u/[username]/[tab]/page.tsx` to add ONE line — the `unstable_instant` route-segment config export.

Insertion location: BETWEEN the import block (which ends at line 31, `import type { WatchWithWear } from '@/lib/types'`) and the stale comment block at lines 33–39 (the "Phase 39c D-39c-07 unstable_instant export was REMOVED 2026-05-14" block). Insert with one blank line above and one blank line below.

The exact text to insert (no surrounding comment for now — Plan 09 will rewrite the stale block and add a docs reference comment above the export at that time):

```
export const unstable_instant = { prefetch: 'static' }
```

Do NOT touch any other line in this file. Do NOT delete the stale comment at lines 33–39 — that's Plan 09's job. Do NOT refactor anything else. The whole point of Step 1 is to add ONE line and observe.

Per D-52-01 / D-52-03 + audit followup Step 1: this is a probe. The validator output is ground truth for Wave 2.

After modification: Test 4 in `tests/profile-route-51.test.ts` (added in Plan 01) flips from FAIL to PASS — that's the first proof point.
  </action>
  <verify>
    <automated>grep -c "export const unstable_instant" src/app/u/[username]/[tab]/page.tsx && npx vitest run tests/profile-route-51.test.ts -t "unstable_instant" 2>&1 | grep -E "(✓|PASS)" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "^export const unstable_instant" src/app/u/[username]/[tab]/page.tsx` returns 1
    - Source: the line matches the literal pattern `export const unstable_instant = { prefetch: 'static' }` (no extra whitespace; double-quote vs single-quote: single per the RESEARCH.md template)
    - Source: file length grew by exactly 2 lines (1 export + 1 blank line) — verify via `wc -l` before/after
    - Behavior: `npx vitest run tests/profile-route-51.test.ts -t "unstable_instant"` reports Test 4 as PASS (was FAIL in Plan 01 baseline)
    - Behavior: no other test that was passing on the Plan 01 baseline now fails (regression-free addition)
  </acceptance_criteria>
  <done>
Single-line export added at the top of `src/app/u/[username]/[tab]/page.tsx`. Test 4 in tests/profile-route-51.test.ts passes. File diff is exactly one new export line + blank line padding.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture validator output via `npm run dev` + `npm run build`</name>
  <action>Human-action checkpoint — Claude orchestrates context capture but the operator performs the action and responds via the resume-signal. The substantive work, verification, and resume protocol are documented in the &lt;what-needed&gt; and &lt;how-to-verify&gt; blocks below (intentionally kept in the checkpoint-template idiom for executor readability).</action>
  <verify>
    <automated>echo "checkpoint — awaiting operator response per resume-signal"</automated>
  </verify>
  <done>Operator responds via the resume-signal documented below; the orchestrator captures the response and proceeds to the next task.</done>
  <what-needed>
The validator runs at dev time (in the Next overlay) and at build time (stdout/stderr). Both must be captured verbatim into a new artifact so Wave 2 plans have ground truth for scope.

This task is human-action because:
  (a) `npm run dev` is a long-running foreground command — Claude cannot read the dev overlay (it lives in the browser/devtools, not the terminal)
  (b) the operator must visit `/u/twwaneka/collection` (or another `/u/[username]/[tab]` URL) in a browser to surface the dev overlay errors
  (c) `npm run build` output is multi-thousand-line; the operator copies the relevant validator-output section into the artifact
  </what-needed>
  <how-to-verify>
1. Clear the Next cache to ensure no stale validator output (per RESEARCH.md Pitfall 5):
   ```
   rm -rf .next
   ```
2. **Dev-mode validator capture:**
   - Run `npm run dev` in one terminal
   - Wait for "Ready in ..."
   - In a browser, navigate to `http://localhost:3000/u/twwaneka/collection` (substitute the username confirmed in Plan 02 Task 0)
   - Click each tab once (collection → wishlist → worn → notes → stats → insights → common-ground) to surface per-tab validator errors
   - Open browser DevTools → Console + the Next dev error overlay
   - Copy ALL validator-related errors verbatim
   - Stop the dev server with Ctrl+C
3. **Build-mode validator capture:**
   - Run `npm run build 2>&1 | tee /tmp/phase-52-build.log`
   - Wait for completion (will exit non-zero with validator errors on the unfixed structure)
   - Copy the relevant validator-output section (search the log for "instant", "unstable_instant", "Suspense", "Push dynamic access down", or "could not be prerendered" — these are the validator's signal phrases per instant.md)
4. **Write the artifact** at `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-03-VALIDATOR-OUTPUT.md` using this structure (Claude generates the file scaffold; operator fills in the verbatim transcripts):

   ```markdown
   # Phase 52 — Step 1 Validator Output (Wave 1 probe)

   **Captured:** YYYY-MM-DD
   **Source state:** main + Plan 03 Task 1 only (single `unstable_instant` export added)
   **Cache state:** `.next/` cleared before measurement (per RESEARCH.md Pitfall 5)

   ## Dev-mode output

   <!-- Verbatim copy of overlay errors from each tab navigation -->
   <!-- One subsection per surfaced error site -->

   ### Error 1 — {component_name}
   ```
   <verbatim text>
   ```

   ### Error 2 — {component_name}
   ```
   <verbatim text>
   ```

   ## Build-mode output

   <!-- Relevant section of `npm run build` output — typically a "Compilation" or "Validation" block -->

   ```
   <verbatim text>
   ```

   ## Parsed refactor sites (consumed by Wave 2)

   | # | Site (file:component) | Error category | Wave 2 plan that fixes it |
   |---|------------------------|----------------|----------------------------|
   | 1 | src/app/u/[username]/layout.tsx → ProfileLayout top-level `await params` | top-level await in layout | Plan 05 |
   | 2 | src/app/u/[username]/layout.tsx → ProfileLayout top-level `await getCurrentUser()` | top-level await in layout | Plan 05 |
   | 3 | src/app/u/[username]/[tab]/page.tsx → ProfileTabPage top-level `await params` | top-level await in page | Plan 06 |
   | 4 | src/app/u/[username]/[tab]/page.tsx → ProfileTabPage top-level `await getCurrentUser()` | top-level await in page | Plan 06 |
   | 5 | src/app/u/[username]/[tab]/page.tsx → ProfileTabPage top-level `await ProfileShellResolver()` | top-level await in page | Plan 06 |
   <!-- Additional rows for any cross-route violations the validator surfaced -->
   <!-- If cross-route violations exist → Plan 07 handles them (REQ-52-05 / D-52-02) -->

   ## Cross-route findings (if any)

   <!-- List each non-/[tab] route that produced validator errors -->
   <!-- Per D-52-02: each gets `unstable_instant = false` in Plan 07 -->
   <!-- These will also be recorded in SEED-014 (Plan 08) -->

   ## Decisions for Wave 2

   - [ ] If parsed sites match the working hypothesis (5 in-route errors, 0 cross-route): Wave 2 proceeds as planned (Plans 04 + 05 + 06)
   - [ ] If cross-route sites present: Plan 07 expands to cover them (apply `unstable_instant = false`)
   - [ ] If unexpected error categories surface (e.g., "cached component received Promise"): planner is re-spawned with the surprise context
   ```

5. Commit the artifact to git in the same commit as Task 1's source change.

Operator confirms: artifact exists at the canonical path; parsed refactor sites table is populated; dev + build transcripts are verbatim.
  </how-to-verify>
  <resume-signal>Reply with `captured` once `.planning/phases/52-.../52-03-VALIDATOR-OUTPUT.md` is committed; OR reply with `cross-route-surfaced sites=<list>` to signal that Wave 2 Plan 07 needs to expand; OR reply with `unexpected-errors: <description>` to trigger re-planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server overlay→operator clipboard | Operator copies validator output. Validator output is build/dev tooling, no user data. |
| `.next/` cache→validator | Stale cache can mask validator errors. Mitigation: clear `.next/` before measurement (Pitfall 5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03-01 | Repudiation | 52-03-VALIDATOR-OUTPUT.md verbatim transcripts | accept | Operator captures and commits the transcripts; the document is the canonical record. Git history is the audit trail. No external system trust assumed. |
| T-52-03-02 | Tampering | `.next/` cache staleness obscuring real validator errors | mitigate | Task 2 step 1 explicitly clears `.next/` before measurement. Acceptance criterion enforces this. |
</threat_model>

<verification>
1. `grep -c "^export const unstable_instant" src/app/u/[username]/[tab]/page.tsx` returns 1
2. `test -f .planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-03-VALIDATOR-OUTPUT.md` exits 0
3. `npx vitest run tests/profile-route-51.test.ts -t "unstable_instant"` reports PASS
4. The VALIDATOR-OUTPUT.md `## Parsed refactor sites` table is non-empty
</verification>

<success_criteria>
- [ ] `src/app/u/[username]/[tab]/page.tsx` has the `unstable_instant` export at the top (line ~31–33 area)
- [ ] Test 4 (REQ-52-01) in `tests/profile-route-51.test.ts` now PASSES
- [ ] `.next/` was cleared before measurement
- [ ] Dev-mode overlay errors captured verbatim
- [ ] Build-mode output captured verbatim
- [ ] Parsed refactor sites table populated (rows for each validator error)
- [ ] Cross-route findings section either empty or populated with Plan 07 follow-ups
- [ ] Artifact committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-03-SUMMARY.md` per the standard summary template, including:
  - The one-line diff to `src/app/u/[username]/[tab]/page.tsx`
  - A reference to `52-03-VALIDATOR-OUTPUT.md` (not duplicating its contents)
  - The Wave 2 scope decision (working hypothesis confirmed vs. cross-route expansion needed)
  - Test 4 transition: was FAIL → now PASS

Update `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-VALIDATION.md` Per-Task Verification Map with rows:
| 52-03-01 | 03 | 1 | REQ-52-01 | — | unstable_instant export present | unit (source-grep) | `npx vitest run tests/profile-route-51.test.ts -t unstable_instant` | ✅ | ⬜ pending |
| 52-03-02 | 03 | 1 | REQ-52-02 (probe) | — | Validator output captured verbatim — ground truth for Wave 2 | manual (operator) | inspect 52-03-VALIDATOR-OUTPUT.md | ❌ (probe; gate green only after Wave 2) | ⬜ pending |
</output>
