---
phase: 52
plan: 09
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified: []
autonomous: false
requirements: [REQ-52-02, REQ-52-06, REQ-52-08]

must_haves:
  truths:
    - "Pre-deploy gate: `npm run test` exits 0 (full vitest suite green)"
    - "Pre-deploy gate: `npm run build` exits 0 (Plan 03's unstable_instant validator green globally)"
    - "Pre-deploy gate: `npx playwright test tests/e2e/profile-tab-instant.test.ts` exits 0 (chrome-mounted invariant holds against local dev server)"
    - "Vercel preview deploy succeeds; preview URL is reachable"
    - "Post-deploy curl: anon /u/twwaneka/collection → 307 + Cache-Control: no-store (Branch B contract preserved, D-52-CF-01)"
    - "Operator UAT: signed-in tab navigation through 2 full cycles (6 tabs × 2) — zero 404s, zero React #419 errors in console"
    - "15-min post-deploy revalidation wait: a second UAT click cycle after the 300s cacheLife window expires — zero 404s, zero React #419 (recurrence-4 prevention confirmed in prod)"
    - "Phase 52 close: project memory file updated; ROADMAP.md marked complete"
  artifacts:
    - path: ".planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-09-UAT-LOG.md"
      provides: "Operator UAT transcript — pre-deploy gates, deploy timestamp, curl outputs, 2x UAT click cycles, 15-min wait result"
      contains: "UAT"
  key_links:
    - from: "operator UAT"
      to: "Phase 52 verification + close"
      via: "52-09-UAT-LOG.md captures the deploy → UAT → 15-min → second-UAT chain"
      pattern: "UAT"
---

<objective>
Final gate for Phase 52: pre-deploy verification (full vitest green + build green + Playwright e2e green against local dev), Vercel preview deploy, post-deploy curl verification of the Branch B contract (D-52-CF-01), operator UAT through 2 full tab-navigation cycles, then a 15-minute wait + second UAT cycle to confirm the 300s cacheLife revalidation window does NOT trigger React #419 (the recurrence-4 prevention contract — the entire reason Phase 52 exists).

Purpose: Phase 52's whole rationale is "structural fix that the build process enforces" (operator preference per 52-CONTEXT.md § Specifics). Plans 01-08 land the structure. This plan proves the structure works in prod. The 15-minute revalidation wait is the single most important UAT step — recurrence-4 hit precisely at this point on 2026-05-20, and Phase 52 must pass it.

This plan is NON-AUTONOMOUS. Claude orchestrates the gates and the deploy push; the operator performs the UAT (signing in, clicking tabs, observing console). Phase 52 is closed only after the 15-min second-cycle UAT passes.

Output: 1 artifact created (`52-09-UAT-LOG.md`); 0 source files modified. Phase 52 ROADMAP entry marked complete; project memory file updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-CONTEXT.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-RESEARCH.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-VALIDATION.md
@.planning/audits/cache-components-2026-05-21-followup.md
@scripts/verify-phase-51-prod.sh

<interfaces>
Pre-deploy gates (from 52-VALIDATION.md § Sampling Rate "Before /gsd-verify-work"):
  - `npm run test` exits 0 (full vitest)
  - `npm run build` exits 0 (validator gate; REQ-52-02 final)
  - `npm run test:e2e` exits 0 (Playwright; REQ-52-06)

Branch B contract verification (Phase 51 protocol — reusable):
  curl -s -o /dev/null --cookie-jar /dev/null --cookie /dev/null \
    -w "STATUS=%{http_code}\n" -D /tmp/h \
    "https://www.horlo.app/u/twwaneka/collection"
  grep -i cache-control /tmp/h
  # Expect: STATUS=307 + cache-control: no-store

Recurrence-4 prevention contract (Phase 52's reason for being):
  After deploy → wait 15+ minutes (>= 2 cacheLife windows of 300s) → click tabs → no React #419 + no 404
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy automated gates (vitest + build + Playwright e2e)</name>
  <files>(none — runs CI commands)</files>
  <read_first>
    - 52-VALIDATION.md § Sampling Rate
    - scripts/verify-phase-51-prod.sh — for the post-deploy curl reference
    - Test files: tests/profile-route-51.test.ts, tests/proxy.test.ts, tests/e2e/profile-tab-instant.test.ts
  </read_first>
  <action>
Execute the three gates in this exact order. ANY non-zero exit halts the plan (no deploy until all green).

1. **Vitest full suite:**
   ```
   rm -rf .next  # Pitfall 5: clear cache before tests that read manifest, if any
   npm run test
   ```
   Expected: exits 0; the previous baseline (5233 tests) ± any Phase 52 additions all pass. Failing tests block deploy. Capture the test count + pass/fail summary in the UAT log.

2. **Build-time validator gate (REQ-52-02 final):**
   ```
   npm run build 2>&1 | tee /tmp/phase-52-build-pre-deploy.log
   ```
   Expected: exits 0; no validator errors in the build log. Specifically grep:
   - `grep -c "instant\\|unstable_instant" /tmp/phase-52-build-pre-deploy.log | grep -v "0$"` should produce no "error" or "fail" lines (compile-time matches in the static analyser output are OK; runtime errors are not)
   - Build output should show the `/u/[username]/[tab]` route as `◐ Partial Prerender` (correct — PPR with the Suspense fallbacks in the static shell) or whatever the post-Plan-05 classification is
   Capture relevant build output lines in the UAT log.

3. **Playwright e2e gate (REQ-52-06):**
   ```
   npm run test:e2e
   ```
   This runs the auth-setup project (creates storageState.json) followed by the chromium project (executes `tests/e2e/profile-tab-instant.test.ts`). The `instant()` test asserts chrome (heading + tablist) stays visible across the tab navigation.

   Expected: exits 0 against local dev server. If it fails on the assertion, that's a regression — the structural fix isn't fully landed. If it fails on a flake (slow dev server start, etc.), retry once; if still failing, block deploy and re-spawn the debug session.

   Capture: pass/fail counts, total runtime, the `instant()` callback's success path in the UAT log.

If ALL three gates pass: proceed to Task 2 (deploy). If ANY fails: block, write up the failure in 52-09-UAT-LOG.md § Pre-deploy gates, do NOT deploy.
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -3 && npm run build 2>&1 | tail -3 && npm run test:e2e 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npm run test` exits 0
    - Behavior: `npm run build` exits 0
    - Behavior: `npm run test:e2e` exits 0
    - Source: `/tmp/phase-52-build-pre-deploy.log` exists and is captured in the UAT log
    - The UAT log artifact `.planning/phases/52-.../52-09-UAT-LOG.md` is initialized with the pre-deploy gate results (Task 4 will append the post-deploy UAT)
  </acceptance_criteria>
  <done>
All three pre-deploy gates green. UAT log initialized. Ready for Task 2 (deploy).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push to main; Vercel deploys to prod</name>
  <action>Human-action checkpoint — Claude orchestrates context capture but the operator performs the action and responds via the resume-signal. The substantive work, verification, and resume protocol are documented in the &lt;what-needed&gt; and &lt;how-to-verify&gt; blocks below (intentionally kept in the checkpoint-template idiom for executor readability).</action>
  <verify>
    <automated>echo "checkpoint — awaiting operator response per resume-signal"</automated>
  </verify>
  <done>Operator responds via the resume-signal documented below; the orchestrator captures the response and proceeds to the next task.</done>
  <what-needed>
Phase 52's branch (or whichever working branch the operator has been on) needs to land on `main`. Vercel auto-deploys `main` to `https://www.horlo.app`.

This task is human-action because the deploy involves a git push to a remote (the merge / push approval is operator-controlled) and the Vercel deploy is observed (typically 2-3 minutes) before Task 3 runs.
  </what-needed>
  <how-to-verify>
1. Ensure working tree is clean: `git status` returns "nothing to commit, working tree clean" (or the operator has staged + committed all Phase 52 changes).
2. If on a feature branch (e.g., `gsd/phase-52-...`), merge to main:
   - `git checkout main`
   - `git merge --no-ff <branch-name>` (use --no-ff to preserve the phase boundary in history)
   - Resolve any conflicts (unlikely if Phase 52 was the only branch).
3. Push to remote: `git push origin main`
4. Open the Vercel dashboard or `vercel ls --prod` (if vercel CLI is configured) to watch the deploy.
5. Wait until status is "Ready" (typically 2-3 minutes). Capture the deploy commit SHA + the Vercel deploy URL in the UAT log.
6. Confirm by running: `curl -s -I https://www.horlo.app | grep -i 'x-vercel-id\\|x-matched-path'` — the response headers should include a `x-vercel-id` reflecting a recent edge POP routing.

Operator: reply once the deploy is "Ready" on Vercel with the deploy commit SHA.
  </how-to-verify>
  <resume-signal>Reply with `deployed sha=<short-sha>` (e.g. `deployed sha=abc123d`) once the Vercel deploy is "Ready".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-deploy Branch B contract verification (curl)</name>
  <files>(none — runs curl commands)</files>
  <read_first>
    - scripts/verify-phase-51-prod.sh (the Phase 51 contract curl script — reusable)
    - .planning/phases/52-.../52-CONTEXT.md D-52-CF-01 (Branch B contract preservation)
  </read_first>
  <action>
Run the Phase 51 Branch B contract verification curl against the now-deployed prod URL. This proves Phase 52 did NOT regress the anon-redirect contract (D-52-CF-01).

```
# Anon request to /u/* MUST return 307 + Cache-Control: no-store
curl -s -o /dev/null --cookie-jar /dev/null --cookie /dev/null \
  -w "STATUS=%{http_code}\n" -D /tmp/phase-52-curl-headers.txt \
  "https://www.horlo.app/u/twwaneka/collection"

# Inspect headers
echo "=== Status + Cache-Control ==="
cat /tmp/phase-52-curl-headers.txt | grep -iE "HTTP/|location|cache-control"
```

Expected:
- `STATUS=307` printed
- `location: /login?next=/u/twwaneka/collection` in the headers
- `cache-control: no-store` in the headers

If ANY of these fail, the Branch B contract is broken — IMMEDIATELY block, surface to operator. Phase 52 should preserve Phase 51's contract; failure here is a regression.

ALSO run the `scripts/verify-phase-51-prod.sh` script with Branch B mode if it's still available (Plan 07 may have left it intact — different from the deleted `assert-phase-51-build.mjs`):
```
PHASE51_BRANCH_B=1 bash scripts/verify-phase-51-prod.sh https://www.horlo.app
```
Expected: exits 0 + prints "OK Phase 51 prod contract verified".

Capture all curl outputs + script outputs in the UAT log under § "Post-deploy automated verification."
  </action>
  <verify>
    <automated>curl -s -o /dev/null --cookie-jar /dev/null --cookie /dev/null -w "STATUS=%{http_code}\n" -D /tmp/phase-52-curl-headers.txt "https://www.horlo.app/u/twwaneka/collection" && grep -iE "HTTP/|location|cache-control" /tmp/phase-52-curl-headers.txt</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `curl -s ... -w "STATUS=%{http_code}\n" https://www.horlo.app/u/twwaneka/collection` prints `STATUS=307`
    - CLI: `/tmp/phase-52-curl-headers.txt` contains `cache-control: no-store` (case-insensitive grep)
    - CLI: `/tmp/phase-52-curl-headers.txt` contains a `location:` header pointing to `/login?next=/u/twwaneka/collection` or similar
    - Behavior: `PHASE51_BRANCH_B=1 bash scripts/verify-phase-51-prod.sh https://www.horlo.app` exits 0 (if the script is still present after Plan 07 — it should be; Plan 07 deleted `assert-phase-51-build.mjs`, NOT `verify-phase-51-prod.sh`)
    - The curl outputs are recorded in the UAT log (Task 4)
  </acceptance_criteria>
  <done>
Branch B contract preserved post-deploy. No regression on the 307 + no-store invariant. Ready for operator UAT.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Operator UAT — first click cycle (2x through all 6 tabs) + 15-min wait + second click cycle</name>
  <action>Human-action checkpoint — Claude orchestrates context capture but the operator performs the action and responds via the resume-signal. The substantive work, verification, and resume protocol are documented in the &lt;what-needed&gt; and &lt;how-to-verify&gt; blocks below (intentionally kept in the checkpoint-template idiom for executor readability).</action>
  <verify>
    <automated>echo "checkpoint — awaiting operator response per resume-signal"</automated>
  </verify>
  <done>Operator responds via the resume-signal documented below; the orchestrator captures the response and proceeds to the next task.</done>
  <what-needed>
This is THE critical test of Phase 52. Recurrence-4 hit precisely at this point on 2026-05-20 — ~10 minutes after deploy, after the `cacheLife(300s)` window expired and PPR re-evaluated the static/dynamic partition. Phase 52's structural fix is judged by whether this UAT passes.

Operator: sign in to https://www.horlo.app and do TWO full UAT cycles separated by a 15-minute wait. Capture observations in the UAT log.
  </what-needed>
  <how-to-verify>
**First UAT cycle (immediately after Task 3):**

1. Open https://www.horlo.app in a fresh browser tab (or incognito if cookies are stale)
2. Sign in via the normal /login flow with your account credentials
3. Open browser DevTools → Console tab; ALSO open the Network tab and filter for `RSC` (Next.js RSC payload requests)
4. Navigate to `/u/twwaneka` (substitute your username); you should land on `/u/twwaneka/collection` (or wherever the default tab is)
5. Click through each tab in order — at least twice through the full cycle. Tab order:
   - collection → wishlist → worn → notes → stats → insights → common-ground → collection (back to start)
   - second pass: same order
6. For each click, observe:
   - URL updates correctly (no `/404` or unexpected redirect)
   - Tab content renders (no infinite skeleton spinner)
   - Browser console is clean — NO `React #419` ("The server could not finish this Suspense boundary"), NO `Cannot read property 'X' of undefined`, NO uncaught promise rejections
   - Chrome (avatar / name / tablist) stays VISIBLE throughout each navigation — no full-page flash
   - RSC payload responses (in Network tab) show 200 status + non-zero body bytes

Record observations in the UAT log under § "First UAT cycle":
- Timestamps for start + end
- All 14 tab clicks (7 tabs × 2 cycles) — pass/fail per click
- Console error count
- Visual chrome-mounted observations

**15-minute wait:**

7. After the first UAT cycle, do NOT close the browser. Note the time.
8. Wait at least 15 minutes (recommended 20 minutes for safety — the `cacheLife` is 300s = 5 min, two windows = 10 min; the recurrence-4 trigger was at ~10 min; a 15-20 min wait covers the most volatile re-evaluation window with margin).
9. During the wait, optionally tail Vercel logs: `vercel logs https://www.horlo.app --follow` (if CLI configured) — look for any 5xx or React error markers.

Record the wait start/end timestamps in the UAT log under § "15-minute revalidation wait."

**Second UAT cycle (recurrence-4 prevention contract):**

10. After the wait, return to the still-open browser tab. The page may have remained on the last-visited tab; you're now past the cacheLife window.
11. Repeat steps 5-6 — click through all 7 tabs at least twice.
12. CRITICALLY observe for React #419 errors, 404s, or chrome flashing. THIS is the recurrence-4 reproduction window — Phase 52's structural fix passes only if zero errors surface here.
13. Record observations in the UAT log under § "Second UAT cycle (post-15-min)."

**Pass criteria:**
- Both UAT cycles: zero 404s, zero React #419, zero infinite skeletons, chrome stays mounted
- Branch B contract still holds (Task 3 verified — anon redirects with no-store)
- No 5xx errors in Vercel logs during the wait

**If any UAT step fails:**
- Document the failure mode + reproduction steps in the UAT log
- Reply with `fail: <description>` — this triggers debug + re-planning, NOT a rollback (operator's call on whether to leave Phase 52 deployed or revert)

**If UAT passes:**
- Reply with `pass cycle1=<timestamp> cycle2=<timestamp> wait=<minutes>` — Phase 52 is verified
  </how-to-verify>
  <resume-signal>Reply with `pass cycle1=<ts> cycle2=<ts> wait=<min>` OR `fail: <description>` once both UAT cycles + 15-min wait are complete.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Phase 52 close — update STATE.md, ROADMAP.md, project memory file</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, ~/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/project_cc_audit_2026_05_21.md (memory note)</files>
  <read_first>
    - .planning/STATE.md (current — update progress, last_activity, stopped_at, Phase 52 entry)
    - .planning/ROADMAP.md (current — mark Phase 52's plan checklist items as complete)
    - ~/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/project_cc_audit_2026_05_21.md (current memory note — to be updated per 52-CONTEXT.md note)
    - 52-09-UAT-LOG.md (the just-completed UAT log) — extract the "passed" verdict
  </read_first>
  <action>
Three close-out file updates:

1. **`.planning/STATE.md`:**
   - Update `last_activity` to: `2026-05-21 -- Phase 52 complete — Option D Cache Components canonical pattern fix; 5-wave plan landed; recurrence-4 prevention verified in prod via 15-min UAT`
   - Update `stopped_at` to: `Phase 52 closed — awaiting next milestone or quick task`
   - Update `progress` block: bump `completed_phases` by 1; bump `total_phases` accordingly if Phase 52 was already counted in totals; bump `completed_plans` by 9 (Plan 01 through Plan 09 of Phase 52)
   - Add a Phase 52 entry to the "Current Position" section: brief description + UAT pass timestamp + the 15-min wait result
   - Add `## Phase 52 close` entry under `## Accumulated Context > Key Decisions` summarizing:
     - The diagnosis reversal (D-52-11) — `unstable_instant` is a validator, not a runtime feature
     - The canonical Next 16 Cache Components pattern (sync layout + Suspense + async ProfileChrome; sync page + Suspense + async ProfileTabContent)
     - The recurrence-4 prevention contract — passed via 15-min UAT
     - Carry-forwards: SEED-014 (broader CC sweep) + the `'use cache: remote'` migration deferred (D-52-CF-04)

2. **`.planning/ROADMAP.md`:**
   - Find the Phase 52 entry (under `### Phase 52: Option D — Cache Components canonical pattern fix ...`)
   - Update the `Plans:` checklist: mark Plans 01-09 as `[x]` complete (use the plan titles from each PLAN.md file)
   - Update `Requirements`: confirm REQ-52-01 through REQ-52-09 are all listed (post-discovery; the planner may have refined these during Plan 03's validator output)
   - Add a phase-close note: "Closed 2026-05-21 — recurrence-4 prevention contract verified in prod via 15-min cache-revalidation UAT."

3. **`~/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/project_cc_audit_2026_05_21.md`** (the user memory file auto-loaded into context at session start — per 52-CONTEXT.md note "Will need an update at Phase 52 close"):
   - Read the current content
   - Append a `## 2026-05-21 — Phase 52 Outcome` section at the end:
     - Phase 52 landed: Option D selected → executed in 5 waves over [N hours/days]
     - Diagnosis reversal: `unstable_instant` is validator-not-runtime-feature; previous Phase 39c/51 blocklist entry annotated as REVERSED
     - Canonical pattern: sync layout + Suspense + async ProfileChrome; sync page + Suspense + async ProfileTabContent
     - Recurrence-4 prevention contract: PASSED in prod via 15-min UAT
     - Carry-forwards: SEED-014 (`.planning/seeds/SEED-014-cache-components-canonical-sweep.md`) tracks the broader CC sweep; `'use cache: remote'` migration deferred (RESEARCH.md A1)
     - Branch B contract (D-52-CF-01) preserved
   - The format must match the existing memory file's style (typically dated subsections, brief observations).

4. Commit all four files (the three above + the UAT log from Task 4) in a single `docs(52): phase close + memory update` commit.

After this task: STATE.md reflects Phase 52 complete; ROADMAP.md shows the phase shipped; the memory file carries the outcome into future sessions. Phase 52 is officially closed.
  </action>
  <verify>
    <automated>grep -c "Phase 52" .planning/STATE.md | xargs -I {} test {} -ge 2 && grep -c "Phase 52.*complete\\|Phase 52.*closed" .planning/STATE.md | xargs -I {} test {} -ge 1 && grep -c "\\[x\\] 52-01\\|\\[x\\] 52-09" .planning/ROADMAP.md | xargs -I {} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/STATE.md` `last_activity` field reflects Phase 52 close — `grep -c "Phase 52" .planning/STATE.md` returns at least 2
    - Source: `.planning/STATE.md` `progress.completed_phases` incremented to reflect Phase 52 done
    - Source: `.planning/ROADMAP.md` Phase 52 plan checklist entries marked `[x]` for Plans 01-09 — `grep -c "\\[x\\] 52-" .planning/ROADMAP.md` returns 9
    - Source: project memory file at `~/.claude/projects/.../memory/project_cc_audit_2026_05_21.md` contains a `2026-05-21 — Phase 52 Outcome` section — `grep -c "Phase 52 Outcome\\|Phase 52 complete" <memory file>` returns at least 1
    - Filesystem: all four files committed in one git commit — verified via `git log --oneline -1` containing the close message
  </acceptance_criteria>
  <done>
STATE.md updated; ROADMAP.md plans checklist marked complete; memory file appended with Phase 52 outcome; close commit landed. Phase 52 is officially closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser→prod Vercel deploy | UAT cycles cross the public internet. Auth flow uses the operator's real session cookies. No new attack surface vs. baseline. |
| curl → prod | Anon request crosses the public internet. Branch B contract verified per the Phase 51 protocol — no behavioral change in Phase 52. |
| operator clipboard → UAT log artifact | Operator captures observations. The artifact is internal documentation; no user data outside the operator's own test session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-09-01 | Spoofing | Cached prod CDN edge serving stale anon-redirect responses | mitigate | Branch B contract verified via Cache-Control: no-store on the 307 (Task 3 acceptance criterion). D-52-CF-01 invariant. |
| T-52-09-02 | Denial of Service | Vercel deploy fails / rollback needed mid-UAT | accept | Operator-controlled rollback path: `git revert` on the merge commit + push. Zero users at risk. Operator can take Phase 52 down with one command. |
| T-52-09-03 | Information Disclosure | UAT log captures sensitive session cookies | mitigate | UAT log captures observations + timestamps, NOT raw cookie values. Curl outputs include only response headers (Cache-Control, Location), not session JWTs. |
</threat_model>

<verification>
1. Pre-deploy: `npm run test` + `npm run build` + `npm run test:e2e` all exit 0
2. Curl: anon `/u/twwaneka/collection` → STATUS=307 + cache-control: no-store
3. Operator UAT: 2 click cycles × 7 tabs = 14 navigations, zero 404s + zero React #419
4. 15-min wait + second UAT cycle: zero 404s + zero React #419 (recurrence-4 prevention contract)
5. STATE.md + ROADMAP.md + memory file all updated; close commit landed
</verification>

<success_criteria>
- [ ] Pre-deploy: vitest + build + e2e all green
- [ ] Vercel preview deploy reaches "Ready" status
- [ ] Branch B curl: 307 + Cache-Control: no-store confirmed
- [ ] First UAT cycle: 2x through 7 tabs, zero errors
- [ ] 15-min wait: no Vercel 5xx; cache revalidation completes
- [ ] Second UAT cycle: 2x through 7 tabs, zero errors (recurrence-4 prevention contract PASSED)
- [ ] UAT log artifact created and committed
- [ ] STATE.md, ROADMAP.md, project memory file updated with phase close
- [ ] Final commit on main reflects phase close
</success_criteria>

<output>
After completion, create `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-09-SUMMARY.md` per the standard summary template. Include:
  - Pre-deploy gate results (vitest count, build exit code, e2e pass/fail)
  - Deploy SHA + Vercel deploy URL
  - Curl output snippets (Status + Cache-Control + Location headers)
  - UAT log link (.planning/phases/52-.../52-09-UAT-LOG.md)
  - 15-min wait timestamps + outcome
  - Confirmation of recurrence-4 prevention contract pass
  - Pointers to STATE.md / ROADMAP.md / memory file changes

Update `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-VALIDATION.md` Per-Task Verification Map with rows:
| 52-09-01 | 09 | 4 | REQ-52-02 (pre-deploy build) | — | npm run build exits 0 pre-deploy | build | `npm run build && echo OK` | ✅ | ⬜ pending |
| 52-09-02 | 09 | 4 | REQ-52-06 (pre-deploy e2e) | — | Playwright chrome-mounted invariant green | e2e | `npm run test:e2e` | ✅ | ⬜ pending |
| 52-09-03 | 09 | 4 | REQ-52-08 (Branch B contract preserved) | T-52-09-01 | Anon → 307 + no-store | manual (curl) | curl on prod | ✅ | ⬜ pending |
| 52-09-04 | 09 | 4 | REQ-52-08 (recurrence-4 prevention — 15-min UAT) | T-52-09-02 | Second UAT cycle post-15-min: zero React #419 | manual (operator) | inspect 52-09-UAT-LOG.md | ✅ | ⬜ pending |

Set `nyquist_compliant: true` in 52-VALIDATION.md frontmatter after all rows are filled and verified.
</output>
