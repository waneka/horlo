---
phase: 52
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/profile-route-51.test.ts
autonomous: true
requirements: [REQ-52-01, REQ-52-03a, REQ-52-03b, REQ-52-04]

must_haves:
  truths:
    - "tests/profile-route-51.test.ts contains Test 1 inverted (no top-level await getCurrentUser in layout; layout HAS Suspense)"
    - "tests/profile-route-51.test.ts contains a new Test 4 asserting page exports unstable_instant"
    - "tests/profile-route-51.test.ts contains a new Test 5 asserting page has inner async ProfileTabContent inside Suspense"
    - "Running `npx vitest run tests/profile-route-51.test.ts` on current main (commit 2f22003) FAILS Tests 1, 4, 5 — the regression contract is live"
  artifacts:
    - path: "tests/profile-route-51.test.ts"
      provides: "Source-grep regression contract for Phase 52 structural invariants — Test 1 inverted, Tests 4 & 5 added"
      contains: "await\\s+getCurrentUser"
    - path: "tests/profile-route-51.test.ts"
      provides: "Source-grep assertion for unstable_instant export presence"
      contains: "unstable_instant"
    - path: "tests/profile-route-51.test.ts"
      provides: "Source-grep assertion for inner async ProfileTabContent inside page-level Suspense"
      contains: "ProfileTabContent"
  key_links:
    - from: "tests/profile-route-51.test.ts"
      to: "src/app/u/[username]/layout.tsx"
      via: "fs.readFileSync source inspection"
      pattern: "readFileSync.*layout\\.tsx"
    - from: "tests/profile-route-51.test.ts"
      to: "src/app/u/[username]/[tab]/page.tsx"
      via: "fs.readFileSync source inspection"
      pattern: "readFileSync.*\\[tab\\].*page\\.tsx"
---

<objective>
Update the Phase 51 source-grep regression test (`tests/profile-route-51.test.ts`) in place to assert the post-Phase-52 structural contract. Test 1 inverts (layout MUST NOT directly await getCurrentUser; layout MUST contain Suspense). Test 4 (new) asserts page exports `unstable_instant`. Test 5 (new) asserts page has an inner async `ProfileTabContent` invoked inside `<Suspense>`. Tests 2 and 3 (Phase 51 ProfileGate + ProfileShellResolver invariants) remain unchanged. All three new/inverted assertions MUST FAIL on current `main` (`2f22003`) before any structural code changes land — this is the regression contract that prevents recurrence-5.

Purpose: Lock the post-refactor invariants into vitest before Wave 1's probe and Wave 2's refactor. The file name stays `profile-route-51.test.ts` (per RESEARCH.md Open Question 2 recommendation) — it pins the joint Phase 51+52 structural contract in a single artifact.

Output: One file modified in-place (`tests/profile-route-51.test.ts`); test count grows from 3 to 5 `it()` blocks; vitest run reports the 3 newly-asserted invariants as FAILING on current `main`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-CONTEXT.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-RESEARCH.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-PATTERNS.md

@vitest.config.ts
@tests/profile-route-51.test.ts
@tests/no-evaluate-route.test.ts

<interfaces>
<!-- Test framework: Vitest 2.1.9; `npm test` resolves to `vitest run`. -->
<!-- vitest.config.ts include pattern: tests/**/*.test.ts — file already included. -->
<!-- `server-only` aliased to shim; this file only does fs.readFileSync — no component imports. -->
<!-- Established source-grep pattern: import { describe, it, expect } from 'vitest'; readFileSync(resolve(process.cwd(), 'src/...'), 'utf8'); expect(regex.test(source)).toBe(true|false). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invert Test 1 + add Tests 4 and 5 to tests/profile-route-51.test.ts</name>
  <files>tests/profile-route-51.test.ts</files>
  <read_first>
    - tests/profile-route-51.test.ts (full current file — 87 lines; preserves Tests 2 and 3 verbatim)
    - src/app/u/[username]/layout.tsx (current 60-line file — top-level `await params` + `await getCurrentUser()` will be removed by Wave 2 Plan 05; Test 1 inversion must FAIL on this source)
    - src/app/u/[username]/[tab]/page.tsx (current 368-line file — no `unstable_instant` export, no `ProfileTabContent` inner function exists; Tests 4 and 5 must FAIL on this source)
    - .planning/phases/52-.../52-PATTERNS.md § `tests/profile-route-51.test.ts` (lines 270–334 — verbatim assertion templates for the three changes)
    - .planning/phases/52-.../52-RESEARCH.md § Code Examples > "Inverted Test 1 Assertion" (lines 545–555) and § "Test 4 — unstable_instant Export Present" (lines 558–567) for the canonical regex shapes
  </read_first>
  <action>
Modify `tests/profile-route-51.test.ts` to assert the post-Phase-52 contract per D-52-CF-CONTEXT D-52-11 / REQ-52-01 / REQ-52-03a / REQ-52-03b / REQ-52-04.

Top-of-file: extend the JSDoc block comment to add a "Phase 52 inversion" note before the `describe` block. Document explicitly: (a) Test 1 was inverted on 2026-05-21 to reflect the canonical Next 16 Cache Components pattern (layout sync + Suspense around async runtime-API consumer); (b) the old "no Suspense in layout" assertion was based on a misdiagnosis (Phase 39c blocklist) corrected by Phase 52 D-52-11; (c) Tests 4 and 5 added to lock the page-level structural contract.

Test 1 — rewrite assertions inside the existing `it('layout has no Suspense over an awaited shell (REQ-51-04)', ...)` block:
- Update the it() title to: `'layout must be sync — no top-level await getCurrentUser; Suspense MUST wrap ProfileChrome (REQ-52-03a, REQ-52-03b — Phase 52 inversion of Phase 51 REQ-51-04)'`
- REMOVE the two existing assertions at lines 36–37 (`expect(/<Suspense[^>]*fallback={<ProfileShellSkeleton/s.test(source)).toBe(false)` and `expect(/<Suspense\b/.test(source)).toBe(false)`)
- REPLACE with these three assertions in this exact order:
  - `expect(/await\s+getCurrentUser/s.test(source)).toBe(false)` — layout MUST NOT directly await `getCurrentUser` (REQ-52-03a)
  - `expect(/<Suspense\b/.test(source)).toBe(true)` — layout MUST have `<Suspense>` wrapping ProfileChrome (REQ-52-03b)
  - `expect(/ProfileChrome/.test(source)).toBe(true)` — layout MUST reference the new ProfileChrome component
- KEEP `expect(source.includes('<main')).toBe(true)` at line 44 unchanged — the `<main>` chrome wrapper invariant survives the inversion
- Update the inline comment block within Test 1 (lines 29–35) to explain the inversion: replace the existing `cache-components.md` reference with a Phase 52 reference plus a citation to the audit followup; explicitly note that layout MAY (and SHOULD) have Suspense around `ProfileChrome` per the canonical Next 16 pattern

Test 2 — UNCHANGED. Preserve verbatim (lines 47–67 in the current file). This is the Phase 51 51-02 contract (ProfileGate accepts `viewerId` as prop; no `getCurrentUser` call in gate).

Test 3 — UNCHANGED. Preserve verbatim (lines 69–86 in the current file). This is the Phase 39c / Phase 51 resolver contract (`'use cache'` + `cacheTag('profile:${username}')` + `cacheLife({ revalidate: 300 })`).

Test 4 — NEW `it()` block after Test 3 (append before the closing `})` of the describe):
- Title: `'page exports unstable_instant for build-time validator gate (REQ-52-01)'`
- Read `src/app/u/[username]/[tab]/page.tsx` via the same `readFileSync(resolve(process.cwd(), ...), 'utf8')` pattern as Tests 1–3
- Assert: `expect(/export\s+const\s+unstable_instant\s*=/.test(source)).toBe(true)`
- Add an inline comment citing D-52-01 + D-52-03 + the instant-navigation doc — explain the validator-not-runtime-feature framing and that the export IS the recurrence-5-prevention CI gate

Test 5 — NEW `it()` block after Test 4 (append before the closing `})` of the describe):
- Title: `'page has inner async ProfileTabContent component inside Suspense (REQ-52-04)'`
- Read `src/app/u/[username]/[tab]/page.tsx` via the same pattern
- Three positive assertions in this exact order:
  - `expect(/async\s+function\s+ProfileTabContent/.test(source)).toBe(true)` — inner async function exists
  - `expect(/<Suspense\b/.test(source)).toBe(true)` — page MUST contain a `<Suspense>` JSX open tag (the outer page-level Suspense)
  - `expect(/ProfileTabContent\s+paramsPromise=/.test(source)).toBe(true)` — outer component passes `paramsPromise` prop into ProfileTabContent
- Add an inline comment citing REQ-52-04 and the instant-navigation.md ProductPage canonical example

All three changes (Test 1 inversion + Tests 4 + 5) MUST FAIL on current `main` (commit `2f22003`). Verification step below confirms this.

Do NOT touch any other file. Do NOT add new imports beyond what already exists at the top (`describe, it, expect` from vitest; `readFileSync` from `node:fs`; `resolve` from `node:path`). Do NOT remove the existing JSDoc comment — extend it.
  </action>
  <verify>
    <automated>npx vitest run tests/profile-route-51.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/profile-route-51.test.ts` exists and is syntactically valid TypeScript
    - Source assertion: `grep -c '^it\\|^  it(' tests/profile-route-51.test.ts` returns at least 5 (or `grep -v '^[[:space:]]*//' tests/profile-route-51.test.ts | grep -c "it(\\'"` returns 5)
    - Source assertion: file contains literal `await\\s+getCurrentUser` (the Test 1 inversion regex) — verified via `grep -c 'await\\\\s+getCurrentUser' tests/profile-route-51.test.ts` returns at least 1
    - Source assertion: file contains literal `unstable_instant` — verified via `grep -c 'unstable_instant' tests/profile-route-51.test.ts` returns at least 1 (the Test 4 assertion)
    - Source assertion: file contains literal `ProfileTabContent` — verified via `grep -c 'ProfileTabContent' tests/profile-route-51.test.ts` returns at least 1 (the Test 5 assertion)
    - Source assertion: file does NOT contain the old removed regex `<Suspense[^>]*fallback={<ProfileShellSkeleton` (the Phase 51 Test 1 assertion that is now reversed) — verified via `grep -c 'fallback={<ProfileShellSkeleton' tests/profile-route-51.test.ts` returns 0
    - Behavior: `npx vitest run tests/profile-route-51.test.ts` exits NON-ZERO on current main (commit `2f22003`); at least 3 tests are reported FAILED (Test 1 inversion + Test 4 + Test 5)
    - Behavior: Tests 2 and 3 still PASS — preserved unchanged from Phase 51
  </acceptance_criteria>
  <done>
File `tests/profile-route-51.test.ts` modified in place; 5 `it()` blocks total; Tests 1/4/5 are the regression contract and FAIL on current main; Tests 2/3 pass unchanged. Failure output captured in the SUMMARY as evidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→test runner | `readFileSync` reads source files; no user input crosses this boundary. Test runner reads from controlled repo paths only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01-01 | Tampering | tests/profile-route-51.test.ts | accept | Test file is committed source; tamper-resistance is the same as the rest of the codebase (review + git history). No runtime data crosses this boundary. |
</threat_model>

<verification>
1. `npx vitest run tests/profile-route-51.test.ts --reporter=verbose` — must report exactly 5 `it()` blocks, 3 FAILED (Tests 1, 4, 5) and 2 PASSED (Tests 2, 3) on current `main`
2. `grep -c "it(\\'" tests/profile-route-51.test.ts` — must return 5
3. `grep 'unstable_instant\\|ProfileTabContent\\|await.*getCurrentUser' tests/profile-route-51.test.ts | wc -l` — must return at least 3 distinct matches
</verification>

<success_criteria>
- [ ] File `tests/profile-route-51.test.ts` modified in place (not replaced; Tests 2 and 3 preserved verbatim)
- [ ] Test count: 5 `it()` blocks (was 3)
- [ ] Test 1 inverted per the new regex assertions; old `<Suspense[^>]*fallback={<ProfileShellSkeleton` regex removed
- [ ] Test 4 added (REQ-52-01 — `unstable_instant` export)
- [ ] Test 5 added (REQ-52-04 — inner `ProfileTabContent` inside Suspense)
- [ ] On current main: 3 tests FAIL (regression contract); 2 tests PASS
- [ ] No new imports added; no other files touched
- [ ] JSDoc comment block extended to document the Phase 52 inversion (D-52-11)
</success_criteria>

<output>
After completion, create `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-01-SUMMARY.md` per the standard summary template, including the `npx vitest run tests/profile-route-51.test.ts` output as evidence that Tests 1, 4, 5 fail on current main (regression contract live).

Update `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-VALIDATION.md` Per-Task Verification Map with this task's row:
| 52-01-01 | 01 | 0 | REQ-52-01, REQ-52-03a, REQ-52-03b, REQ-52-04 | — | Test scaffolds asserting post-refactor invariants fail on current main | unit (source-grep) | `npx vitest run tests/profile-route-51.test.ts` | ✅ | ⬜ pending |
</output>
