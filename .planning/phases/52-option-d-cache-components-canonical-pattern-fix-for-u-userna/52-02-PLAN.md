---
phase: 52
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - playwright.config.ts
  - tests/e2e/auth-setup.ts
  - tests/e2e/profile-tab-instant.test.ts
  - vitest.config.ts
  - .gitignore
autonomous: false
requirements: [REQ-52-06, REQ-52-07]

must_haves:
  truths:
    - "@playwright/test and @next/playwright are installed in devDependencies at compatible versions"
    - "playwright.config.ts exists with webServer spawning `npm run dev` and baseURL http://localhost:3000"
    - "tests/e2e/auth-setup.ts produces a tests/e2e/storageState.json via Supabase email/password sign-in against a seeded local user"
    - "tests/e2e/profile-tab-instant.test.ts asserts chrome (heading + tablist) stays visible inside instant() callback during /collection → /wishlist navigation"
    - "vitest is configured to EXCLUDE tests/e2e/** so the new e2e files do not break `npm run test`"
    - "npm run test:e2e fails on current main because the structural fix is not yet shipped (chrome flashes or RSC 404; the failure mode is the regression contract)"
  artifacts:
    - path: "package.json"
      provides: "@playwright/test ^1.60.0, @next/playwright 16.2.6 devDependencies + `test:e2e` script"
      contains: "@playwright/test"
    - path: "playwright.config.ts"
      provides: "Playwright config with local-dev-server webServer, storageState, setup→chromium project chain"
      contains: "webServer"
    - path: "tests/e2e/auth-setup.ts"
      provides: "globalSetup that signs in seeded user and writes tests/e2e/storageState.json"
      contains: "storageState"
    - path: "tests/e2e/profile-tab-instant.test.ts"
      provides: "Playwright instant() chrome-mounted invariant test for REQ-52-06"
      contains: "instant("
    - path: "vitest.config.ts"
      provides: "exclude pattern for tests/e2e/** so Playwright tests are not picked up by vitest"
      contains: "tests/e2e"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/e2e/auth-setup.ts"
      via: "projects[].testMatch /auth-setup\\.ts/ + chromium project dependencies: ['setup']"
      pattern: "auth-setup"
    - from: "tests/e2e/profile-tab-instant.test.ts"
      to: "@next/playwright instant() helper"
      via: "import { instant } from '@next/playwright'"
      pattern: "from '@next/playwright'"
    - from: "tests/e2e/auth-setup.ts"
      to: "local Supabase auth endpoint"
      via: "page.goto('/login') + email/password form fill"
      pattern: "page\\.fill"
---

<objective>
Establish Playwright e2e test infrastructure as a Wave 0 TDD scaffold. Install `@playwright/test` + `@next/playwright`, create `playwright.config.ts`, `tests/e2e/auth-setup.ts` (storageState pattern, signed-in test user), and `tests/e2e/profile-tab-instant.test.ts` (chrome-mounted invariant via `@next/playwright` `instant()` helper). Exclude `tests/e2e/**` from vitest's include glob so the new files do not contaminate `npm run test`. The e2e test MUST FAIL on current `main` (`2f22003`) — the structural fix lives in Wave 2 Plans 04/05/06, so chrome will flash or React #419 will surface during the `instant()` callback. That failure IS the regression contract.

Purpose: Lock the runtime chrome-mounted invariant (REQ-52-06) in CI before the refactor lands. The build-time validator (Wave 1's `unstable_instant` export) is the structural CI gate; this e2e test is the runtime complement for the cases the validator cannot statically prove.

Output: Six files modified or created; one human checkpoint (seeded test user confirmation per RESEARCH.md Open Question A4); local `npm run test:e2e` run executable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-CONTEXT.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-RESEARCH.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-PATTERNS.md

@package.json
@vitest.config.ts
@src/components/profile/WishlistTabContent.tsx
@src/components/profile/ProfileTabs.tsx

<interfaces>
<!-- @playwright/test version target: ^1.60.0 (matches existing `playwright` 1.60.0 in devDependencies; same major). -->
<!-- @next/playwright version target: 16.2.6 (pin exact; peer deps require @playwright/test >= 1.0.0; verified via `npm view @next/playwright` per RESEARCH.md Standard Stack table). -->
<!-- instant() signature (from @next/playwright@16.2.6 type declarations, RESEARCH.md Pattern 5):
       export declare function instant<T>(
         page: PlaywrightPage,
         fn: () => Promise<T>,
         options?: { baseURL?: string }
       ): Promise<T>
     The helper sets cookie `next-instant-navigation-testing=1; path=/`; while active, dynamic content is held back. After callback, cookie clears and dynamic streams in. Assumes warm cache — caller MUST page.goto(...) before instant() to fill the cache. -->
<!-- vitest.config.ts current include: ['tests/**/*.test.ts', ...] — must add `exclude: ['tests/e2e/**', 'node_modules/**']` to prevent vitest from running Playwright tests. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Confirm seeded test user availability (RESEARCH.md A4)</name>
  <action>Human-action checkpoint — Claude orchestrates context capture but the operator performs the action and responds via the resume-signal. The substantive work, verification, and resume protocol are documented in the &lt;what-needed&gt; and &lt;how-to-verify&gt; blocks below (intentionally kept in the checkpoint-template idiom for executor readability).</action>
  <verify>
    <automated>echo "checkpoint — awaiting operator response per resume-signal"</automated>
  </verify>
  <done>Operator responds via the resume-signal documented below; the orchestrator captures the response and proceeds to the next task.</done>
  <what-needed>
The Playwright e2e auth-setup needs a local Supabase user with known email + password to sign in once and save storageState. RESEARCH.md Open Question A4 flags this as a planner-confirm item before writing auth-setup.ts.

Operator: verify whether a seeded test user with a known password exists in your local Supabase. Two acceptable answers:
  (a) Yes — provide TEST_USER_EMAIL + TEST_USER_PASSWORD (will be exported into a new `.env.local` block; the auth-setup script reads them via process.env)
  (b) No — confirm you want auth-setup to CREATE the user via Supabase admin API on first run using `SUPABASE_SERVICE_ROLE_KEY` from `.env.local`

The username `twwaneka` is referenced in RESEARCH.md / audit followup as the canonical test profile; confirm that username exists in local Supabase and has at least one watch in collection + one watch in wishlist (needed for the chrome-flash assertion to be meaningful).
  </what-needed>
  <how-to-verify>
1. Run `npx supabase status` to confirm local Supabase is up; if not, run `npx supabase start` first.
2. Check whether `auth.users` contains an entry for the test username. Suggested query (substitute your DB connection):
   `docker exec -it supabase_db_horlo psql -U postgres -d postgres -c "select id, email from auth.users where email like '%twwaneka%' or email like '%test%';"`
3. Confirm a profile row exists: `select username from public.profiles where username = 'twwaneka';`
4. Confirm at least one watch exists for that user: `select count(*) from public.watches where user_id = '<id from step 2>';`
5. Reply to Claude with either:
   - Option A: `email=<...> password=<...>` (script will read from process.env at runtime; do NOT commit credentials)
   - Option B: `create-via-admin` (script will mint the user via Supabase admin API)
  </how-to-verify>
  <resume-signal>Reply with `option-a email=<email> password=<password>` OR `option-b create-via-admin` OR `option-b username=<existing username> create-credentials` to proceed.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install @playwright/test + @next/playwright + add test:e2e script + exclude tests/e2e from vitest</name>
  <files>package.json, vitest.config.ts, .gitignore</files>
  <read_first>
    - package.json (current devDependencies block — `playwright: ^1.60.0` is already at line 74; `@playwright/test` is NOT installed)
    - vitest.config.ts (current 22-line file — include pattern is `tests/**/*.test.ts, tests/**/*.test.tsx, src/**/*.test.ts, src/**/*.test.tsx`)
    - .gitignore (must add `tests/e2e/storageState.json` and `playwright-report/` and `test-results/`)
    - .planning/phases/52-.../52-RESEARCH.md § Standard Stack > Core (lines 109–129) for verbatim install command + version pins
  </read_first>
  <action>
Three-file change, single commit. Apply in this exact order:

1. **`package.json`**:
   - Add to `devDependencies` (alphabetical position; insert AFTER `@base-ui/react` would be in dependencies; for devDeps insert AFTER `@react-email/components` line 58, but the exact ordering must follow npm conventions — let `npm install --save-dev` handle the ordering automatically):
     - `@playwright/test`: `^1.60.0`
     - `@next/playwright`: `16.2.6` (no caret — pin exact per D-52-05)
   - Add to `scripts` (insert AFTER the existing `"test:watch": "vitest"` line at line 11):
     - `"test:e2e": "playwright test"`
   - Use the actual command `npm install --save-dev @playwright/test@^1.60.0 @next/playwright@16.2.6` to install — this also generates the lockfile updates. Do NOT hand-edit package.json. The script entry must be hand-added via Edit since `npm install` does not add scripts.
   - After install, run `npx playwright install chromium` to download the Chromium browser binary (Playwright tests cannot run without it). This may take 1–2 minutes on first run.

2. **`vitest.config.ts`**:
   - Modify the `test` config object to add an `exclude` field. Vitest's default exclude already covers `node_modules` and `dist`; the explicit addition needed is `tests/e2e/**`.
   - Insert immediately AFTER the `include` array (line 11):
     - `exclude: ['tests/e2e/**', 'node_modules/**', 'dist/**', '.next/**'],`
   - Do not touch other fields (`environment`, `setupFiles`, `globals`, `alias`).
   - Inline comment above the `exclude` line: `// Phase 52: Playwright e2e tests live under tests/e2e/ and are run by 'npm run test:e2e' — explicitly excluded from vitest's pattern.`

3. **`.gitignore`**:
   - Append at the end of the file (if `.gitignore` exists; if not, this is unexpected — abort and ask):
     - `# Phase 52: Playwright artifacts`
     - `tests/e2e/storageState.json`
     - `playwright-report/`
     - `test-results/`
   - Do NOT remove existing `.gitignore` entries.

Per D-52-05 these three packages + script + exclude form the Wave 0 infra footprint. After this task: `npm install` resolves cleanly; `npx playwright --version` prints `1.60.x`; `npm run test:e2e` exits with "no tests found" or similar (tests are added in Tasks 2 and 3 below).
  </action>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && cat package.json | grep -E '"@playwright/test"|"@next/playwright"|"test:e2e"' | wc -l | tr -d ' ' | xargs -I {} test {} -ge 3 && grep -c "tests/e2e" vitest.config.ts && grep -c "tests/e2e/storageState.json" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - Source: `package.json` contains `"@playwright/test"` and `"@next/playwright"` in devDependencies (verified via `node -e 'const p=require("./package.json");console.log(p.devDependencies["@playwright/test"]&&p.devDependencies["@next/playwright"]?"OK":"FAIL")'`)
    - Source: `package.json` `scripts.test:e2e` equals `playwright test` (verified via `node -e 'const p=require("./package.json");process.exit(p.scripts["test:e2e"]==="playwright test"?0:1)'`)
    - Source: `vitest.config.ts` includes an `exclude` array containing the string `tests/e2e/**`
    - Source: `.gitignore` contains the literal line `tests/e2e/storageState.json`
    - Behavior: `npx playwright --version` exits 0 and reports `Version 1.60.x` (or later 1.x)
    - Behavior: `npm run test:e2e` exits without an "unknown package" or "module not found" error (it may exit non-zero with "no tests found" because Tasks 2/3 have not run yet — that's acceptable)
    - Behavior: `npm run test` (vitest) does NOT attempt to load files under `tests/e2e/` (verified by running `npm run test 2>&1 | grep -c "tests/e2e"` returning 0)
  </acceptance_criteria>
  <done>
Three packages installed, `test:e2e` script wired, vitest excludes `tests/e2e/**`, gitignore excludes the generated storageState + Playwright reports. `npx playwright --version` works.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create playwright.config.ts and tests/e2e/auth-setup.ts</name>
  <files>playwright.config.ts, tests/e2e/auth-setup.ts</files>
  <read_first>
    - .planning/phases/52-.../52-RESEARCH.md § Pattern 6: Playwright Config with webServer (lines 393–426) — verbatim config template
    - .planning/phases/52-.../52-PATTERNS.md § `tests/e2e/auth-setup.ts` (lines 396–423) — globalSetup template
    - .planning/phases/52-.../52-RESEARCH.md § Pitfall 6 (lines 489–493) — Supabase auth strategy (Option A: signInWithPassword vs Option B: admin API)
    - src/app/login/page.tsx (or whatever the login route is) — confirm form field selectors (`name="email"`, `name="password"`, `type="submit"`); if selectors differ, the auth-setup script must use the actual selectors
    - Task 0 resolution (operator's choice of seeded credentials vs admin-API user creation; encode the chosen path)
  </read_first>
  <action>
Create two new files. They form the e2e infrastructure foundation (`playwright.config.ts` is the runner config; `auth-setup.ts` produces the `storageState.json` consumed by all subsequent e2e tests).

1. **`playwright.config.ts`** (new file at repo root):
   - Use the RESEARCH.md § Pattern 6 template verbatim (lines 396–425). Specifically:
     - `import { defineConfig, devices } from '@playwright/test'` at the top
     - `export default defineConfig({ ... })`
     - `testDir: './tests/e2e'`
     - `fullyParallel: false` (per D-52-07 — single dev server, sequential)
     - `retries: 0`
     - `use.baseURL: 'http://localhost:3000'`
     - `use.storageState: 'tests/e2e/storageState.json'`
     - `projects`: two-element array — `{ name: 'setup', testMatch: /auth-setup\.ts/ }` and `{ name: 'chromium', use: { ...devices['Desktop Chrome'] }, dependencies: ['setup'] }`
     - `webServer: { command: 'npm run dev', url: 'http://localhost:3000', reuseExistingServer: !process.env.CI, timeout: 120_000 }`
   - Add a top-of-file comment block citing D-52-05 + D-52-07 + RESEARCH.md Pattern 6.
   - Do NOT add `forbidOnly`, `reporter`, `globalTeardown`, or other fields. Keep config minimal.

2. **`tests/e2e/auth-setup.ts`** (new file):
   - Branch on Task 0 resolution:
     - **Option A (seeded credentials)** — implement per PATTERNS.md lines 405–421:
       - Use `import { test as setup } from '@playwright/test'` (NOT `test`; the auth-setup uses Playwright's test runner with a single `setup()` block, NOT the older `globalSetup` pattern — this is cleaner with the `projects[0].testMatch` config in playwright.config.ts)
       - Inside `setup('authenticate as seeded user', async ({ page }) => { ... })`:
         - `await page.goto('/login')`
         - Fill the email/password inputs (confirm selectors against `src/app/login/page.tsx` — the audit followup mentions email/password fields exist; common selectors are `[name="email"]` and `[name="password"]` per PATTERNS.md, but use whatever the actual login form renders)
         - `await page.click('[type="submit"]')` or the actual submit selector
         - `await page.waitForURL('**/u/**', { timeout: 30_000 })` — wait for redirect to profile or `/network` or wherever the post-login default is. Read the login page source to confirm the redirect target; adjust the URL pattern accordingly.
         - `await page.context().storageState({ path: 'tests/e2e/storageState.json' })`
       - Read `TEST_USER_EMAIL` and `TEST_USER_PASSWORD` from `process.env` with a clear failure mode if unset: throw with message "Set TEST_USER_EMAIL + TEST_USER_PASSWORD in .env.local for Phase 52 e2e tests"
     - **Option B (admin-API user creation)** — adapt the same shape but call `supabaseAdmin.auth.admin.createUser({ email, password, email_confirm: true })` first using `SUPABASE_SERVICE_ROLE_KEY` from `.env.local`, then sign in via UI. Document this branch with an inline comment referencing the operator's choice.
   - Top-of-file comment block citing D-52-06 + RESEARCH.md Pitfall 6 + the operator's Task 0 decision.
   - The file ends with `setup.use({ storageState: { cookies: [], origins: [] } })` ABOVE the setup block (to ensure the setup itself runs without inheriting storageState — otherwise it would loop).

After this task: `npx playwright test tests/e2e/auth-setup.ts` produces `tests/e2e/storageState.json` (assuming Task 0 credentials are valid and local Supabase + dev server are up). The file is gitignored per Task 1.
  </action>
  <verify>
    <automated>test -f playwright.config.ts && test -f tests/e2e/auth-setup.ts && grep -c "webServer" playwright.config.ts && grep -c "storageState" tests/e2e/auth-setup.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `playwright.config.ts` exists at repo root; `cat playwright.config.ts | grep -c "webServer\\|defineConfig\\|storageState"` returns at least 3
    - Source: `playwright.config.ts` contains the literal `'http://localhost:3000'` (verifies baseURL/url) — `grep -c "localhost:3000" playwright.config.ts` returns at least 2
    - Source: `playwright.config.ts` contains `testDir: './tests/e2e'`
    - Source: `playwright.config.ts` contains `projects:` with `setup` and `chromium` named projects
    - Source: `tests/e2e/auth-setup.ts` exists; `grep -c "storageState" tests/e2e/auth-setup.ts` returns at least 1
    - Source: `tests/e2e/auth-setup.ts` contains either `signInWithPassword` (Option A) or `auth.admin.createUser` (Option B) depending on Task 0 resolution
    - Behavior: with local Supabase + dev server up and TEST_USER_* env vars set (or Option B credentials in `.env.local`): `npx playwright test --project=setup` exits 0 and creates `tests/e2e/storageState.json` with non-zero size
    - Behavior: `npx playwright test --project=setup` is idempotent (re-runs do not fail; storageState is overwritten cleanly)
  </acceptance_criteria>
  <done>
Both files exist; storageState round-trips through Playwright's setup project; the file is created on first run and is correctly gitignored.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tests/e2e/profile-tab-instant.test.ts</name>
  <files>tests/e2e/profile-tab-instant.test.ts</files>
  <read_first>
    - .planning/phases/52-.../52-RESEARCH.md § Pattern 5: `instant()` Playwright Helper (lines 349–392) — verbatim test template
    - .planning/phases/52-.../52-PATTERNS.md § `tests/e2e/profile-tab-instant.test.ts` (lines 427–451) — analog template
    - src/components/profile/WishlistTabContent.tsx — INSPECT for `data-testid="wishlist-content"` (RESEARCH.md A5 open question). If absent, use a stable text selector (e.g., the tab heading or a known string like the test user's wishlist count). The plan-context note flagged this as a Wave 0 sub-task.
    - src/components/profile/ProfileTabs.tsx — confirm the tablist `role` attribute is present (the `instant()` callback asserts `getByRole('tablist')`)
    - .planning/audits/cache-components-2026-05-21-followup.md § Step 7 (lines 387–412) — canonical test shape from the audit followup
  </read_first>
  <action>
Create `tests/e2e/profile-tab-instant.test.ts`. This is the runtime contract for REQ-52-06 (chrome stays mounted across tab navigation).

1. Imports:
   - `import { test, expect } from '@playwright/test'`
   - `import { instant } from '@next/playwright'`

2. Top-of-file comment block citing D-52-08, REQ-52-06, RESEARCH.md Pattern 5, and the audit followup Step 7. Explain the warm-cache requirement (page.goto BEFORE instant()).

3. Single `test()` block:
   - Title: `'profile chrome stays mounted on tab navigation (recurrence-5 guard, REQ-52-06)'`
   - Signature: `async ({ page }) => { ... }`
   - Body steps in order:
     a. `await page.goto('/u/twwaneka/collection')` — warm cache; the test user is whatever was confirmed in Plan 02 Task 0 (substitute `twwaneka` if the operator's confirmed username differs)
     b. `await expect(page.getByRole('heading', { name: /@twwaneka/ })).toBeVisible()` — sanity: the page rendered successfully BEFORE instant() (if this fails, the test isn't measuring chrome-mounted; it's measuring "did anything render at all")
     c. `await instant(page, async () => { ... })` block:
        - `await page.click('a[href="/u/twwaneka/wishlist"]')` — click the wishlist tab link
        - `await expect(page.getByRole('heading', { name: /@twwaneka/ })).toBeVisible()` — heading MUST still be visible (chrome stays mounted)
        - `await expect(page.getByRole('tablist')).toBeVisible()` — tablist MUST still be visible
     d. After the `instant()` callback returns (dynamic content streams in):
        - Post-stream assertion — use whatever selector the actual `WishlistTabContent` exposes (from Wave 0 read_first inspection):
          - If `data-testid="wishlist-content"` exists: `await expect(page.getByTestId('wishlist-content')).toBeVisible({ timeout: 10_000 })`
          - If NOT, use a stable text selector — e.g., `await expect(page.locator('text=Wishlist').first()).toBeVisible({ timeout: 10_000 })` or assert on the tab strip aria-current state: `await expect(page.locator('[aria-current="page"]', { hasText: 'Wishlist' })).toBeVisible({ timeout: 10_000 })`. Pick the selector that's most stable against UI copy changes.

4. Inline comments inside the `instant()` callback explaining the cookie protocol (next-instant-navigation-testing=1) and the warm-cache requirement.

5. Do NOT add `test.beforeAll`, `test.beforeEach`, `test.afterEach`, or auth-setup invocation inline — the auth is provided by `playwright.config.ts`'s `projects[1].dependencies: ['setup']` chain. This test inherits storageState from the setup project.

The test MUST FAIL on current main because the structural fix hasn't shipped (Wave 2 Plans 04/05/06). The failure mode is either:
  - `instant()` callback assertion fails because chrome flashes (heading disappears during the click) — the recurrence-4 symptom; OR
  - `instant()` throws because RSC payload is 0 bytes / React #419 surfaces

That failure IS the regression contract. Capture the failure output as evidence in the SUMMARY.
  </action>
  <verify>
    <automated>test -f tests/e2e/profile-tab-instant.test.ts && grep -c "instant(" tests/e2e/profile-tab-instant.test.ts && grep -c "getByRole.*tablist" tests/e2e/profile-tab-instant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: file `tests/e2e/profile-tab-instant.test.ts` exists
    - Source: contains import of `instant` from `@next/playwright` — verified via `grep -c "from '@next/playwright'" tests/e2e/profile-tab-instant.test.ts` returns 1
    - Source: contains exactly one call to `instant(page, async ...)` — verified via `grep -c "instant(page," tests/e2e/profile-tab-instant.test.ts` returns 1
    - Source: contains assertion `getByRole('tablist')` inside the instant() callback (chrome-mounted invariant)
    - Source: contains a `page.goto('/u/` BEFORE the `instant(` call (warm-cache step)
    - Behavior: `npx playwright test tests/e2e/profile-tab-instant.test.ts` exits non-zero on current main (the structural fix hasn't shipped; chrome flashes during tab nav). The failure output is captured in the SUMMARY as evidence.
    - Behavior: after Wave 2 Plans 04/05/06 ship, this same command will exit 0 — but that's NOT this plan's gate
  </acceptance_criteria>
  <done>
File exists; `instant()` test scaffold complete; running on current main reproduces the chrome-flash regression (test fails). Failure output recorded in SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem→`storageState.json` | Auth-setup writes session cookies to disk. The file is gitignored. Contains a JWT with profile-read access to the test user only. |
| Playwright dev server→Supabase auth | auth-setup signs in to local Supabase via password; credentials come from `.env.local` (never committed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02-01 | Information Disclosure | tests/e2e/storageState.json | mitigate | Add to .gitignore (Task 1); confirm via `grep -c "storageState.json" .gitignore`. The file contains a test-user JWT only; no prod credentials. |
| T-52-02-02 | Tampering | tests/e2e/auth-setup.ts credentials read from env | accept | Credentials must come from `process.env.TEST_USER_*` (or `SUPABASE_SERVICE_ROLE_KEY` in option B) — both already conventionally read from `.env.local`; no hard-coding allowed. Task 2 acceptance criterion enforces this. |
| T-52-02-03 | Repudiation | Playwright tests can run against prod (operator misconfig) | mitigate | playwright.config.ts `use.baseURL` is hard-coded to `http://localhost:3000` and `webServer.command` is `npm run dev` — there is no `BASE_URL` env injection path. Operator would have to edit the config to point at prod. |
</threat_model>

<verification>
1. `npm install` runs cleanly; `npx playwright --version` reports 1.60.x
2. `npm run test:e2e --project=setup` (or `npx playwright test --project=setup`) creates `tests/e2e/storageState.json`
3. `npx playwright test tests/e2e/profile-tab-instant.test.ts` exits NON-ZERO on current main — failure output captured
4. `npm run test` (vitest) does NOT load `tests/e2e/**` files (test count unchanged from pre-Phase-52 baseline)
5. `cat .gitignore | grep -c "tests/e2e/storageState.json"` returns 1
</verification>

<success_criteria>
- [ ] Task 0 resolved (operator confirmed test user strategy + provided credentials path)
- [ ] `@playwright/test ^1.60.0` and `@next/playwright 16.2.6` installed
- [ ] Chromium browser binary installed via `npx playwright install chromium`
- [ ] `playwright.config.ts` exists with webServer + setup→chromium project chain
- [ ] `tests/e2e/auth-setup.ts` exists; produces `tests/e2e/storageState.json` on first run
- [ ] `tests/e2e/profile-tab-instant.test.ts` exists; uses `instant()` helper; asserts chrome-mounted invariant
- [ ] `vitest.config.ts` excludes `tests/e2e/**`
- [ ] `.gitignore` excludes `storageState.json` + `playwright-report/` + `test-results/`
- [ ] `npx playwright test tests/e2e/profile-tab-instant.test.ts` FAILS on current main (regression contract)
- [ ] `npm run test` (vitest) test count unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-02-SUMMARY.md` per the standard summary template, including:
  - Resolved Task 0 choice (Option A vs B; redacted credentials)
  - Output of `npx playwright --version`
  - Output of `npx playwright test tests/e2e/profile-tab-instant.test.ts --reporter=list` showing the failure on current main (regression contract evidence)
  - Confirmation that `npm run test` count unchanged

Update `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-VALIDATION.md` Per-Task Verification Map with rows:
| 52-02-01 | 02 | 0 | REQ-52-07 | T-52-02-01, T-52-02-02 | Playwright auth setup writes storageState.json; credentials from env only | e2e (setup) | `npx playwright test --project=setup` | ❌ → ✅ after install | ⬜ pending |
| 52-02-02 | 02 | 0 | REQ-52-06 | T-52-02-03 | Profile chrome stays mounted during instant() tab navigation | e2e (instant) | `npx playwright test tests/e2e/profile-tab-instant.test.ts` | ❌ on current main (contract) | ⬜ pending |
</output>
