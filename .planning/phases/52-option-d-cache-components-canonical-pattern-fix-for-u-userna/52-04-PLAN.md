---
phase: 52
plan: 04
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - src/app/u/[username]/profile-chrome.tsx
  - src/app/u/[username]/layout.tsx
autonomous: true
requirements: [REQ-52-03a, REQ-52-03b, REQ-52-04]

must_haves:
  truths:
    - "New file src/app/u/[username]/profile-chrome.tsx exists; async server component that awaits paramsPromise and getCurrentUser inside its body"
    - "ProfileChrome imports 'server-only' and the existing ProfileGate; does NOT import or call ProfileShellResolver, '@/data/*', or 'next/cache'"
    - "src/app/u/[username]/layout.tsx is SYNCHRONOUS (no `async` keyword on the exported function)"
    - "Layout wraps `<ProfileChrome paramsPromise={params}>{children}</ProfileChrome>` in a `<Suspense fallback={<ProfileShellSkeleton/>}>` block"
    - "Layout no longer imports getCurrentUser, UnauthorizedError, or ProfileGate; new imports are Suspense (react), ProfileChrome, ProfileShellSkeleton"
    - "Test 1 in tests/profile-route-51.test.ts (REQ-52-03a + REQ-52-03b) PASSES after this plan"
    - "Phase 39c Pitfall 1 preserved: viewerId NEVER enters ProfileShellResolver scope — ProfileChrome resolves it and passes to ProfileGate as prop (D-52-CF-02)"
  artifacts:
    - path: "src/app/u/[username]/profile-chrome.tsx"
      provides: "Async runtime-API consumer for the layout — awaits paramsPromise + getCurrentUser; wraps ProfileGate"
      contains: "export async function ProfileChrome"
    - path: "src/app/u/[username]/layout.tsx"
      provides: "Sync layout with Suspense around ProfileChrome (canonical Cache Components pattern)"
      contains: "Suspense fallback"
  key_links:
    - from: "src/app/u/[username]/layout.tsx"
      to: "src/app/u/[username]/profile-chrome.tsx"
      via: "ProfileChrome paramsPromise={params} child inside Suspense"
      pattern: "ProfileChrome\\s+paramsPromise"
    - from: "src/app/u/[username]/profile-chrome.tsx"
      to: "src/app/u/[username]/profile-gate.tsx"
      via: "ProfileGate username viewerId child with children"
      pattern: "ProfileGate"
    - from: "src/app/u/[username]/profile-chrome.tsx"
      to: "src/lib/auth getCurrentUser"
      via: "await getCurrentUser() inside try/catch for UnauthorizedError"
      pattern: "await\\s+getCurrentUser"
---

<objective>
Implement the canonical Next 16 Cache Components pattern at the layout level: create `src/app/u/[username]/profile-chrome.tsx` (new async server component that owns runtime API access — `paramsPromise` + `getCurrentUser`), then refactor `src/app/u/[username]/layout.tsx` to be SYNC with a `<Suspense fallback={<ProfileShellSkeleton/>}>` boundary wrapping `<ProfileChrome>`. This is the structural fix for REQ-52-03a, REQ-52-03b, and the first half of REQ-52-04 (the ProfileChrome async component). Plan 06 in this wave does the matching page-level refactor.

Purpose: Eliminate top-level awaits in the layout (the recurrence-4 root cause). All runtime API access (params, cookies) moves into ProfileChrome, which is wrapped in `<Suspense>`. This is the "Push dynamic access down" pattern (RESEARCH.md Pattern 2 / streaming.md verbatim) and the "Interaction with loading.js" rule (RESEARCH.md Pattern 1 / layout.md verbatim).

D-52-16 structural lock: this is the ONLY canonical shape — sync layout + always-Suspense + always-async-ProfileChrome. The refactor must not introduce conditional Suspense or perf-driven shortcuts.

Output: 1 new file (`profile-chrome.tsx`); 1 file refactored in place (`layout.tsx`). Test 1 in `tests/profile-route-51.test.ts` flips from FAIL to PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-CONTEXT.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-RESEARCH.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-PATTERNS.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-03-VALIDATOR-OUTPUT.md

@src/app/u/[username]/layout.tsx
@src/app/u/[username]/profile-gate.tsx
@src/app/u/[username]/profile-shell-skeleton.tsx
@src/lib/auth.ts

<interfaces>
ProfileChrome target signature (RESEARCH.md Pattern 3, lines 286-311 — copy verbatim):
- import 'server-only' on line 1
- imports: getCurrentUser, UnauthorizedError from @/lib/auth; ProfileGate from ./profile-gate
- interface Props with paramsPromise: Promise<{ username: string }> and children: React.ReactNode
- export async function ProfileChrome destructures { paramsPromise, children } from Props
- body: await paramsPromise; try/catch around getCurrentUser; returns ProfileGate JSX with username + viewerId + children props

Layout target signature (RESEARCH.md Pattern 1 / PATTERNS.md lines 124-142):
- imports: Suspense from react; ProfileChrome from ./profile-chrome; ProfileShellSkeleton from ./profile-shell-skeleton
- export default function ProfileLayout (NOT async)
- props destructure: children, params from LayoutProps<'/u/[username]'>
- returns main className "mx-auto max-w-5xl px-4 py-8 lg:px-8 lg:py-12" wrapping Suspense (with ProfileShellSkeleton fallback) wrapping ProfileChrome with paramsPromise={params} and children

ProfileGate already accepts viewerId as a prop (Phase 51 51-02 contract; tests/profile-route-51.test.ts Test 2 asserts this). No change to profile-gate.tsx in this plan — Plan 09 may tweak its top-of-file comment.

ProfileShellSkeleton is exported alongside ProfileTabContentSkeleton from src/app/u/[username]/profile-shell-skeleton.tsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/app/u/[username]/profile-chrome.tsx</name>
  <files>src/app/u/[username]/profile-chrome.tsx</files>
  <read_first>
    - src/app/u/[username]/profile-gate.tsx (analog file — lines 1-55 show the import 'server-only' + async function signature pattern; lines 26-35 contain a PROHIBITED block that ProfileChrome must match for consistency)
    - src/lib/auth.ts — confirm getCurrentUser + UnauthorizedError exports; confirm getCurrentUser is React-cache()-memoized (per recent commit 172f211)
    - .planning/phases/52-.../52-RESEARCH.md § Pattern 3: ProfileChrome (lines 282-311) — verbatim template
    - .planning/phases/52-.../52-PATTERNS.md § profile-chrome.tsx (lines 31-92) — analog mapping + prohibited patterns
    - .planning/phases/52-.../52-03-VALIDATOR-OUTPUT.md — confirm the validator surfaced the layout's top-level awaits as expected (drives this plan's exact change); cross-route violations are handled by Plan 07
  </read_first>
  <action>
Create a new file at `src/app/u/[username]/profile-chrome.tsx`. This is the async runtime-API consumer that the new sync layout wraps in `<Suspense>` (Task 2 of this plan).

Structure (mirror RESEARCH.md Pattern 3 verbatim, with a top-of-file comment block):

1. `import 'server-only'` on line 1 (per PATTERNS.md analog mapping — ProfileGate establishes this convention)
2. Imports:
   - `import { getCurrentUser, UnauthorizedError } from '@/lib/auth'`
   - `import { ProfileGate } from './profile-gate'`
3. Top-of-file JSDoc-style comment block explaining:
   - What ProfileChrome is — the async runtime-API consumer for the layout scope; the uncached layer that owns cookie reads
   - Why it exists — Phase 52 D-52-CF-02 / Pitfall 1 — viewer identity MUST live outside ProfileShellResolver's cached scope; ProfileChrome is the structural boundary that enforces this
   - The contract — receives `paramsPromise: Promise<{ username: string }>` (NOT a resolved string — awaiting at parent re-introduces the bug); resolves viewerId via getCurrentUser; passes both to ProfileGate
   - The PROHIBITED block (matching ProfileGate's lines 29-35):
     - NO `'use cache'` directive — this is the uncached layer
     - NO `next/cache` tag/life primitives
     - NO `ProfileShellResolver` call — the resolver is invoked inside ProfileGate, NOT here
     - NO `@/data/*` imports beyond what's strictly needed (this file imports only `@/lib/auth` and `./profile-gate`)
   - Citations: RESEARCH.md Pattern 3, audit followup Step 2, Phase 39c Pitfall 1, D-52-CF-02, D-52-16
4. `interface Props { paramsPromise: Promise<{ username: string }>; children: React.ReactNode }` — use `interface`, not `type`, for the public component signature
5. `export async function ProfileChrome({ paramsPromise, children }: Props) { ... }`:
   - `const { username } = await paramsPromise` — resolve the params Promise INSIDE the function body
   - Initialize `let viewerId: string | null = null`
   - `try { viewerId = (await getCurrentUser()).id } catch (err) { if (!(err instanceof UnauthorizedError)) throw err }` — mirror the existing layout's pattern verbatim (the try/catch shape is preserved from current layout.tsx lines 45-50; see PATTERNS.md § "Error try/catch for UnauthorizedError")
   - Return JSX: `<ProfileGate username={username} viewerId={viewerId}>{children}</ProfileGate>` — pass `viewerId` as prop (D-52-CF-02 / Phase 51 51-02 contract)
6. No `'use cache'`, no resolver call, no data imports. The function body is exactly the 4 statements above + the return.

Do NOT add an `initialIsFollowing` prop or any other prop — ProfileGate's signature already handles `initialIsFollowing` optionally (line 53 of profile-gate.tsx) and falls back to its own fetch when omitted. ProfileChrome does not pre-fetch follow state.

After this task: the new file is in place but the layout still references the old ProfileGate composition directly (Task 2 wires them together). The file is unused on its own.
  </action>
  <verify>
    <automated>test -f src/app/u/[username]/profile-chrome.tsx && grep -c "import 'server-only'" src/app/u/[username]/profile-chrome.tsx && grep -c "export async function ProfileChrome" src/app/u/[username]/profile-chrome.tsx && grep -c "await paramsPromise" src/app/u/[username]/profile-chrome.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: file `src/app/u/[username]/profile-chrome.tsx` exists
    - Source: line 1 is `import 'server-only'` (exact, no leading whitespace)
    - Source: contains exactly one `export async function ProfileChrome` declaration
    - Source: function signature accepts `paramsPromise: Promise<{ username: string }>` (verified via grep matching `paramsPromise:\s*Promise<\{\s*username:\s*string\s*\}>` or equivalent)
    - Source: contains `await paramsPromise` (runtime API resolution INSIDE the function, not at module level)
    - Source: contains `(await getCurrentUser()).id` and `UnauthorizedError` (try/catch for anon viewers)
    - Source: JSX return contains `ProfileGate` with `username` and `viewerId` props (verified via grep for `ProfileGate[^>]*username` and `viewerId`)
    - Source: does NOT contain `'use cache'` — `grep -c "'use cache'" src/app/u/[username]/profile-chrome.tsx` returns 0
    - Source: does NOT contain `ProfileShellResolver` — `grep -c "ProfileShellResolver" src/app/u/[username]/profile-chrome.tsx` returns 0
    - Source: does NOT import from `@/data/` — `grep -c "from '@/data" src/app/u/[username]/profile-chrome.tsx` returns 0
    - Source: does NOT import from `next/cache` — `grep -c "from 'next/cache'" src/app/u/[username]/profile-chrome.tsx` returns 0
    - Behavior: `npx tsc --noEmit` exits 0 (TypeScript compilation succeeds)
  </acceptance_criteria>
  <done>
File `src/app/u/[username]/profile-chrome.tsx` exists with the exact shape from RESEARCH.md Pattern 3. PROHIBITED invariants verified by grep. Type-check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor src/app/u/[username]/layout.tsx to sync + Suspense around ProfileChrome</name>
  <files>src/app/u/[username]/layout.tsx</files>
  <read_first>
    - src/app/u/[username]/layout.tsx (current 60-line file — lines 35-59 are the function body that gets restructured; lines 1-34 are imports + the stale post-Phase-51 comment block, both of which get rewritten in this task)
    - src/app/u/[username]/profile-chrome.tsx (just created by Task 1 of this plan)
    - src/app/u/[username]/profile-shell-skeleton.tsx — confirm `ProfileShellSkeleton` is a named export
    - .planning/phases/52-.../52-RESEARCH.md § Pattern 1 (lines 207-239) + § Code Examples > Sync Layout (lines 523-543) — verbatim target shape
    - .planning/phases/52-.../52-PATTERNS.md § layout.tsx (lines 95-148) — current-vs-target diff
    - tests/profile-route-51.test.ts Test 1 (after Plan 01 inversion) — the assertions this refactor must satisfy
  </read_first>
  <action>
Rewrite `src/app/u/[username]/layout.tsx` to be SYNC + wrap `<ProfileChrome>` in `<Suspense fallback={<ProfileShellSkeleton/>}>`. Per D-52-16, this is the structural lock for the layout — no `async`, no top-level awaits, always Suspense around ProfileChrome.

1. **Imports** — full replacement:
   - REMOVE: `import { getCurrentUser, UnauthorizedError } from '@/lib/auth'`
   - REMOVE: `import { ProfileGate } from './profile-gate'`
   - ADD: `import { Suspense } from 'react'`
   - ADD: `import { ProfileChrome } from './profile-chrome'`
   - ADD: `import { ProfileShellSkeleton } from './profile-shell-skeleton'`

2. **Top-of-file comment block** (currently lines 4-34) — replace with a Phase 52 / D-52-16 / D-52-11 comment block that explains:
   - This layout is SYNC by design (Phase 52 D-52-16 structural lock — always-sync-layout, always-Suspense, always-async-ProfileChrome)
   - The Suspense wrap + ProfileChrome split is the canonical Next 16 Cache Components pattern (cite RESEARCH.md Pattern 1, layout.md "Interaction with loading.js", streaming.md "Push dynamic access down")
   - Phase 39c Pitfall 1 / D-52-CF-02 still holds — viewerId is resolved by ProfileChrome (uncached) and passed as a prop to ProfileGate; the resolver remains viewer-independent
   - The persistent-chrome UX (mounted across sibling tab navs) is preserved — layout owns the main wrapper; ProfileChrome owns the composition
   - Phase 51's "recurrence-2 / 3 prevention" framing is REVERSED per D-52-11 — `unstable_instant` is the validator, not the cause; see Phase 52 CONTEXT.md
   - The previous "layout reads cookies inline" claim (current line 21) is now FALSE — cookies are read inside ProfileChrome inside Suspense
   - Citations: 52-CONTEXT.md, audit followup, RESEARCH.md Pattern 1
   - REMOVE the historical paragraph about "the angle bracket is escaped in the prior paragraph so this comment doesn't trip the source-grep assertion" — that defensive workaround is no longer needed because Test 1 now asserts Suspense PRESENCE, not absence

3. **Function body** — replace the existing `async function ProfileLayout` with a sync function returning the canonical shape. The function signature is:
   - `export default function ProfileLayout` (NOT async)
   - destructures `{ children, params }` from `LayoutProps<'/u/[username]'>`
   - returns a single `<main>` element (preserved className verbatim) wrapping `<Suspense fallback={<ProfileShellSkeleton />}>` wrapping `<ProfileChrome paramsPromise={params}>{children}</ProfileChrome>`

   The exact JSX shape is documented in RESEARCH.md Pattern 1 (lines 217-238) and PATTERNS.md lines 124-142 — copy that shape verbatim.

   Constraints:
   - The `async` keyword is REMOVED from the function declaration (D-52-16 structural lock)
   - `await params` and `await getCurrentUser()` are GONE (moved to ProfileChrome)
   - `params` is passed as-is (a Promise) into ProfileChrome via `paramsPromise={params}` — DO NOT destructure or await `params` in the layout body
   - The `<main>` wrapper className must match the existing one byte-for-byte: `"mx-auto max-w-5xl px-4 py-8 lg:px-8 lg:py-12"` (this is the persistent-chrome wrapper that survives across tab navs)

4. **No other changes.** The `LayoutProps<'/u/[username]'>` type from Next 16 is preserved; the file ends at the closing brace of the function (no extra exports, no helper functions).

After this task:
- File has roughly 35-45 lines (was 60); the size reduction is from removing the awaited body
- Test 1 in `tests/profile-route-51.test.ts` (inverted in Plan 01) flips from FAIL to PASS — `await getCurrentUser` no longer in the source; `<Suspense>` is in the source; `ProfileChrome` is in the source
- The Phase 51 51-03 / 51-05 "layout owns persistent chrome" property is PRESERVED (the `<main>` wrapper survives in the layout)
- D-52-CF-02 is structurally enforced — ProfileChrome owns the cookie read, ProfileGate stays cookie-free, ProfileShellResolver stays viewer-independent
  </action>
  <verify>
    <automated>npx vitest run tests/profile-route-51.test.ts --reporter=verbose 2>&1 | grep -cE "PASS|✓|passed" | xargs -I {} test {} -ge 5 && (grep -c "async function ProfileLayout" src/app/u/[username]/layout.tsx || echo 0) | xargs -I {} test {} -eq 0 && grep -c "Suspense" src/app/u/[username]/layout.tsx | xargs -I {} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Source: file does NOT contain `async function ProfileLayout` — `grep -c "async function ProfileLayout" src/app/u/[username]/layout.tsx` returns 0
    - Source: file does NOT contain `await getCurrentUser` — `grep -c "await\s\+getCurrentUser" src/app/u/[username]/layout.tsx` returns 0
    - Source: file does NOT contain `await params` — `grep -c "await\s\+params" src/app/u/[username]/layout.tsx` returns 0
    - Source: file DOES contain a `<Suspense` tag — `grep -c "<Suspense" src/app/u/[username]/layout.tsx` returns at least 1
    - Source: file contains `ProfileChrome paramsPromise=` — passes the params Promise (NOT a resolved value) into ProfileChrome
    - Source: file contains `ProfileShellSkeleton` reference in the Suspense fallback
    - Source: file contains the literal main className `"mx-auto max-w-5xl px-4 py-8 lg:px-8 lg:py-12"` — the persistent chrome wrapper survives
    - Source: imports include exactly: `Suspense` from `react`, `ProfileChrome` from `./profile-chrome`, `ProfileShellSkeleton` from `./profile-shell-skeleton`
    - Source: imports do NOT include `getCurrentUser`, `UnauthorizedError`, or `ProfileGate` directly
    - Behavior: `npx vitest run tests/profile-route-51.test.ts` exits 0 — all 5 tests pass (Tests 1+4 newly pass; Tests 2+3 unchanged; Test 5 still fails because Plan 06 hasn't shipped)
    - Behavior: more precisely, Test 5 (`page has inner async ProfileTabContent inside Suspense`) STILL FAILS because Plan 06 has not refactored the page yet — vitest output should show 4 passes, 1 fail (Test 5). Status flips fully green after Plan 06.
    - Behavior: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
`src/app/u/[username]/layout.tsx` is sync, has Suspense around ProfileChrome, no top-level awaits. Test 1 in `tests/profile-route-51.test.ts` passes. Test 5 still fails (waits for Plan 06). Type-check passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request→layout | params is a runtime API; access deferred into ProfileChrome inside Suspense (no longer at layout top level). |
| client cookie→ProfileChrome | getCurrentUser reads HTTP-only session cookie; cookie never enters ProfileShellResolver's cached scope. |
| ProfileChrome→ProfileGate | viewerId passes as a typed prop (string | null); no implicit cookie access in the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04-01 | Information Disclosure | Session data leaking into ProfileShellResolver's `'use cache'` scope (Phase 39c Pitfall 1) | mitigate | ProfileChrome owns getCurrentUser; viewerId passed to ProfileGate as prop; ProfileShellResolver is viewer-independent and receives only `{ username }`. Acceptance criterion verifies ProfileChrome contains NO `ProfileShellResolver` call. (D-52-CF-02) |
| T-52-04-02 | Elevation of Privilege | Stale auth token served from prerender cache | mitigate | getCurrentUser called INSIDE ProfileChrome (uncached async server component inside Suspense); not at layout top level (which would make it static-shell-resolved). The Suspense boundary forces this resolution to happen at runtime per request. |
| T-52-04-03 | Information Disclosure | LockedProfileState bypass via tampering with viewerId prop | accept | viewerId is server-resolved from the session cookie; cannot be tampered by the client. ProfileGate's locked-profile branch reads settings.profilePublic from the cached resolver after notFound() short-circuit (Pitfall 5 ordering). Existing Phase 39c mitigation preserved. |
| T-52-04-04 | Tampering | Layout async regression (someone re-adds `async` to ProfileLayout) | mitigate | tests/profile-route-51.test.ts Test 1 asserts `expect(/await\s+getCurrentUser/s.test(source)).toBe(false)` — source-grep CI gate catches this. D-52-16 structural lock documented in the layout's top-of-file comment block. |
</threat_model>

<verification>
1. `npx vitest run tests/profile-route-51.test.ts` — Tests 1, 2, 3, 4 PASS; Test 5 FAILS (regression contract still live for Plan 06)
2. `npx tsc --noEmit` — exits 0
3. `grep -c "async function ProfileLayout" src/app/u/[username]/layout.tsx` — returns 0
4. `grep -c "<Suspense" src/app/u/[username]/layout.tsx` — returns at least 1
5. `grep -c "ProfileShellResolver\|'use cache'" src/app/u/[username]/profile-chrome.tsx` — returns 0 (both forbidden)
</verification>

<success_criteria>
- [ ] `src/app/u/[username]/profile-chrome.tsx` exists with the canonical shape (server-only, async, runs paramsPromise + getCurrentUser, wraps ProfileGate)
- [ ] `src/app/u/[username]/layout.tsx` is sync (no `async` keyword on ProfileLayout)
- [ ] Layout wraps ProfileChrome in `<Suspense fallback={<ProfileShellSkeleton />}>`
- [ ] Layout passes `paramsPromise={params}` (NOT a resolved value)
- [ ] Layout no longer imports `getCurrentUser`, `UnauthorizedError`, or `ProfileGate`
- [ ] D-52-CF-02 invariant: ProfileChrome does NOT call ProfileShellResolver
- [ ] D-52-16 structural lock documented in layout's top-of-file comment block
- [ ] Tests 1, 2, 3, 4 pass; Test 5 still fails (waits for Plan 06)
- [ ] `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-04-SUMMARY.md` per the standard summary template. Include the `npx vitest run tests/profile-route-51.test.ts` output showing Tests 1+2+3+4 PASS and Test 5 FAIL (regression contract live for Plan 06).

Update `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-VALIDATION.md` Per-Task Verification Map with rows:
| 52-04-01 | 04 | 2 | REQ-52-04 (part 1 — ProfileChrome) | T-52-04-01 | ProfileChrome is uncached async runtime-API consumer; no ProfileShellResolver | unit (source-grep) + tsc | `grep -c "ProfileShellResolver" src/app/u/[username]/profile-chrome.tsx` returns 0; `npx tsc --noEmit` exits 0 | ✅ | ⬜ pending |
| 52-04-02 | 04 | 2 | REQ-52-03a + REQ-52-03b | T-52-04-02, T-52-04-04 | Layout sync + Suspense around ProfileChrome | unit (source-grep) | `npx vitest run tests/profile-route-51.test.ts -t "layout must be sync"` | ✅ | ⬜ pending |
</output>
