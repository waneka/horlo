---
phase: 42-nyquist-hardening-sweep-uat-triage-parallel-track
plan: 03
type: execute
wave: 3
depends_on: ['02']
files_modified:
  - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/25-VALIDATION.md
  - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/26-VALIDATION.md
  - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/27-VALIDATION.md
  - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/28-VALIDATION.md
  - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/30-VALIDATION.md
  - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/31-VALIDATION.md
autonomous: true
requirements: [DEBT-10]
must_haves:
  truths:
    - "VALIDATION.md files exist for Phases 25 and 26 (previously missing)"
    - "Phases 27, 28, 30 VALIDATION.md are nyquist_compliant: true + wave_0_complete: true"
    - "Phase 31 VALIDATION.md documents the docs-only scope exception (nyquist_compliant: false, status: scope_exception)"
    - "Each VALIDATION.md cites the real browser test file authored in Plan 02"
  artifacts:
    - path: ".planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/25-VALIDATION.md"
      provides: "Phase 25 Nyquist validation artifact (authored, targeted depth)"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/30-VALIDATION.md"
      provides: "Phase 30 upgraded validation artifact with CSS-chain assertion citation"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/31-VALIDATION.md"
      provides: "Phase 31 docs-only scope-exception artifact"
      contains: "scope_exception"
  key_links:
    - from: "42-validation-backfill/30-VALIDATION.md"
      to: "tests/browser/phase30-css-chain.browser.test.tsx"
      via: "cited automated command in per-task map"
      pattern: "phase30-css-chain"
    - from: "42-validation-backfill/27-VALIDATION.md"
      to: "tests/integration/phase27-*.test.ts"
      via: "cited existing Wave 0 tests"
      pattern: "phase27-"
---

<objective>
Author or recover the six VALIDATION.md files for Phases 25-31 and consolidate them under `42-validation-backfill/` (D-10). Phases 25/26 are authored from scratch at targeted depth (D-09). Phases 27/28/30 are recovered from git history, root-caused, and upgraded to `nyquist_compliant: true` + `wave_0_complete: true`. Phase 31 is the documented exception — a docs-only phase that stays `nyquist_compliant: false` with `status: scope_exception`.

Purpose: DEBT-10 / ROADMAP SC#1 require the validation-artifact gap closed for these six phases in one traceable place. Source phase directories were deleted by `dd58ba4` and stay deleted; Phase 42 owns the debt closure end-to-end.
Output: six files in `42-validation-backfill/` keeping the original phase-numbered names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-CONTEXT.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-02-SUMMARY.md

<interfaces>
Phase slugs (for the `slug:` frontmatter field):
- 25 → `profile-nav-prominence-empty-states-form-polish`
- 26 → `wywt-auto-nav`
- 27 → `watch-card-collection-render-polish`
- 28 → `add-watch-flow-verdict-copy-polish`
- 30 → `wywt-capture-alignment-fix`
- 31 → `v4-0-verification-backfill`

Git recovery commands (all five paths VERIFIED working in 42-RESEARCH.md §Pattern 3):
- `git show dd58ba4^:.planning/phases/27-watch-card-collection-render-polish/27-VALIDATION.md`
- `git show dd58ba4^:.planning/phases/28-add-watch-flow-verdict-copy-polish/28-VALIDATION.md`
- `git show dd58ba4^:.planning/phases/29-nav-profile-chrome-cleanup/29-VALIDATION.md` (the COMPLIANT exemplar — structure reference, not backfilled)
- `git show dd58ba4^:.planning/phases/30-wywt-capture-alignment-fix/30-VALIDATION.md`
- `git show dd58ba4^:.planning/phases/31-v4-0-verification-backfill/31-VALIDATION.md`

Browser test files authored in Plan 02 (cite these in the per-task maps):
- tests/browser/phase25-css-chain.browser.test.tsx … phase30-css-chain.browser.test.tsx

Phase 27 Wave 0 tests ALREADY EXIST (42-PATTERNS.md §Pre-existing Test Files; A1 in 42-RESEARCH.md):
- tests/integration/phase27-schema.test.ts
- tests/integration/phase27-bulk-reorder.test.ts
- tests/integration/phase27-backfill.test.ts
- tests/integration/phase27-getwatchesbyuser-order.test.ts
Do NOT create duplicates — cite these existing files as the closed Wave 0 gap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover and upgrade Phases 27, 28, 30, 31 VALIDATION.md</name>
  <files>.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/27-VALIDATION.md, .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/28-VALIDATION.md, .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/30-VALIDATION.md, .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/31-VALIDATION.md</files>
  <read_first>
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md (§Root-Cause Analysis — the specific `partial` cause per phase; §Common Pitfalls Pitfall 3 — Phase 31 cannot reach nyquist_compliant: true; Pitfall 4)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md (§27/28/30/31 VALIDATION.md — target frontmatter + root-cause closure body patterns)
    - 42-02-SUMMARY.md (confirm the six browser test file paths to cite)
  </read_first>
  <action>
    Create the `42-validation-backfill/` subfolder inside the phase directory. Recover each source file with `git show dd58ba4^:<path>` (paths in the interfaces block) and write the recovered content as the starting point — DO NOT recreate the deleted source phase directories.
    27-VALIDATION.md: upgrade frontmatter to `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, add `upgraded: 2026-05-15` and `upgrade_ref: Phase 42 DEBT-10`. Add a `## Phase 42 Upgrade Notes` section: root cause of `partial` was Wave 0 test files marked `❌ W0`; resolution — those Wave 0 tests now exist as `tests/integration/phase27-schema.test.ts`, `phase27-bulk-reorder.test.ts`, `phase27-backfill.test.ts`, `phase27-getwatchesbyuser-order.test.ts` (created after the original VALIDATION.md was authored — the `❌ W0` markers were stale); browser CSS-chain coverage added in `tests/browser/phase27-css-chain.browser.test.tsx`. Update the per-task verification map rows to reference the real existing test files. Do NOT create duplicate test files.
    28-VALIDATION.md: upgrade frontmatter identically. `## Phase 42 Upgrade Notes`: root cause was a per-task map with `TBD` in all columns. Fill the per-task map (Phase 28 was copy/logic — minimal visual surface) and cite `tests/browser/phase28-css-chain.browser.test.tsx` for the prose-layout assertion.
    30-VALIDATION.md: upgrade frontmatter identically. `## Phase 42 Upgrade Notes`: root cause was the missing Wave 0 test file `tests/components/wywt/CameraCaptureView.test.tsx` — that file now exists (4 math tests for `computeObjectCoverSourceRect`); the remaining gap (the D-08 computed-style assertion the v4.1 feedback memory called out) is closed by `tests/browser/phase30-css-chain.browser.test.tsx`. State explicitly that the Phase 30 browser assertion would have caught the `h-full` hotfix regression.
    31-VALIDATION.md: this is the documented exception (RESEARCH.md Pitfall 3). Set frontmatter `status: scope_exception`, KEEP `nyquist_compliant: false` and `wave_0_complete: false`, add `upgraded: 2026-05-15`, `upgrade_ref: Phase 42 DEBT-10`, and an `exception_reason` field stating Phase 31 is a docs-only phase that does not modify production code, add tests, or produce executable artifacts — `nyquist_compliant: false` is the correct terminal state. Add a `## Phase 42 Scope Exception` body section with the same rationale. Flag in the SUMMARY that SC#1 names Phase 31 but it is an intentional exception, not an upgrade target — user can override if they disagree.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d='.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/';for(const n of ['27','28','30']){const s=fs.readFileSync(d+n+'-VALIDATION.md','utf8');if(!/nyquist_compliant:\s*true/.test(s))throw new Error(n+' must be nyquist_compliant: true');if(!/wave_0_complete:\s*true/.test(s))throw new Error(n+' must be wave_0_complete: true');if(!s.includes('phase'+n+'-css-chain'))throw new Error(n+' must cite its browser test');}const s31=fs.readFileSync(d+'31-VALIDATION.md','utf8');if(!/status:\s*scope_exception/.test(s31))throw new Error('31 must be scope_exception');if(!/nyquist_compliant:\s*false/.test(s31))throw new Error('31 must stay nyquist_compliant: false');console.log('27/28/30/31 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `42-validation-backfill/` directory exists; 27/28/30/31-VALIDATION.md present, keeping original phase-numbered names.
    - 27, 28, 30 frontmatter: `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, plus `upgraded` + `upgrade_ref`.
    - 27-VALIDATION.md cites the four existing `tests/integration/phase27-*.test.ts` files and does NOT introduce duplicate test files.
    - 30-VALIDATION.md `## Phase 42 Upgrade Notes` states the browser assertion would have caught the `h-full` regression and cites `tests/browser/phase30-css-chain.browser.test.tsx`.
    - 31-VALIDATION.md keeps `nyquist_compliant: false`, sets `status: scope_exception`, carries an `exception_reason` field, and the SUMMARY flags it as an intentional deviation from SC#1 wording.
  </acceptance_criteria>
  <done>Four phases' validation artifacts recovered, root-caused, and upgraded (or exception-documented) under the backfill folder.</done>
</task>

<task type="auto">
  <name>Task 2: Author Phases 25 and 26 VALIDATION.md at targeted depth</name>
  <files>.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/25-VALIDATION.md, .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/26-VALIDATION.md</files>
  <read_first>
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-CONTEXT.md (D-09 — targeted depth; do not re-litigate phases that shipped to prod with full UAT sign-off)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md (§25/26-VALIDATION.md — frontmatter + per-task map + Wave 0 citation patterns)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md (§Visual Surfaces by Phase — Phase 25/26 surfaces)
  </read_first>
  <action>
    Author `42-validation-backfill/25-VALIDATION.md` and `26-VALIDATION.md` from scratch at TARGETED depth (D-09 — Phases 25/26 shipped to prod with full UAT sign-off at commit `7132ac0`, 2026-05-02; close the artifact gap, do not reconstruct full coverage as if unverified).
    Frontmatter for both: `phase: 25` / `26`, `slug:` from the interfaces block, `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, `created: 2026-05-15`, plus `backfill_location: .planning/phases/42-.../42-validation-backfill/` and `backfill_reason: source phase directory deleted by commit dd58ba4`.
    Body — per-task verification map with columns `Req ID | Behavior | Test Type | Automated Command | File Exists | Status`. For each requirement:
    - Visual surfaces: cite the D-07 browser test (`tests/browser/phase25-css-chain.browser.test.tsx` / `phase26-css-chain.browser.test.tsx`) with status `approved`.
    - Non-visual requirements (Phase 25: form polish, empty states, nav prominence; Phase 26: WYWT auto-nav) WITH existing test coverage: cite the existing test file, `File Exists: ✅ existing`, status `approved`.
    - Non-visual requirements with NO existing test and NO genuine coverage gap: cite prod UAT sign-off (commit `7132ac0`, 2026-05-02) as wave-0 evidence, status `approved`. Author a NEW behavioral test ONLY where there is a genuine coverage gap — document any such gap explicitly; if none, state "no coverage gap found — prod UAT sign-off is the wave-0 evidence."
    Add a short `## Backfill Rationale` section explaining D-09 targeted depth and the deleted-directory consolidation (D-10).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d='.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-validation-backfill/';for(const n of ['25','26']){const s=fs.readFileSync(d+n+'-VALIDATION.md','utf8');if(!/nyquist_compliant:\s*true/.test(s))throw new Error(n+' must be nyquist_compliant: true');if(!/wave_0_complete:\s*true/.test(s))throw new Error(n+' must be wave_0_complete: true');if(!s.includes('phase'+n+'-css-chain'))throw new Error(n+' must cite its browser test');if(!s.includes('7132ac0'))throw new Error(n+' must cite prod UAT commit 7132ac0');}console.log('25/26 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 25-VALIDATION.md and 26-VALIDATION.md exist in `42-validation-backfill/` with the original phase-numbered names.
    - Both have frontmatter `nyquist_compliant: true`, `wave_0_complete: true`, `status: approved`, plus `backfill_location` + `backfill_reason`.
    - Both cite their D-07 browser test (`phase25-css-chain` / `phase26-css-chain`).
    - Both cite prod UAT sign-off commit `7132ac0` as wave-0 evidence for non-visual requirements per D-09.
    - Each per-task map row has a concrete `Automated Command` or an explicit prod-UAT citation — no `TBD` rows.
  </acceptance_criteria>
  <done>Phases 25 and 26 have authored VALIDATION.md artifacts at targeted depth; all six backfill files are in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — planning artifacts only) | This plan writes Markdown VALIDATION.md files. No code, no API, no data handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Repudiation | Backfilled VALIDATION.md artifacts | accept | Each file records `backfill_reason`, `upgraded`/`upgrade_ref`, and cites the originating commit (`dd58ba4` deletion, `7132ac0` prod UAT) — provenance is traceable. Documentation-only; no runtime threat surface. |

This plan produces planning documentation only. No production threat surface exists; documented honestly per the security-enforcement note for this phase.
</threat_model>

<verification>
- All six files exist under `42-validation-backfill/` with original phase-numbered names.
- 25/26/27/28/30 are `nyquist_compliant: true` + `wave_0_complete: true`; 31 is `status: scope_exception` with `nyquist_compliant: false`.
- Each non-exception file cites a real browser test file from Plan 02; 27 cites the existing `tests/integration/phase27-*.test.ts` files (no duplicates created).
</verification>

<success_criteria>
- ROADMAP SC#1 satisfied for Phases 25/26/27/28/30; Phase 31 documented as the intentional exception.
- ROADMAP SC#2 satisfied — Phase 30's VALIDATION.md cites the computed-style CSS-chain assertion.
- DEBT-10 validation-artifact gap closed in one traceable location.
</success_criteria>

<output>
After completion, create `.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-03-SUMMARY.md`
</output>
