---
phase: 42-nyquist-hardening-sweep-uat-triage-parallel-track
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - tests/browser/phase25-css-chain.browser.test.tsx
  - tests/browser/phase26-css-chain.browser.test.tsx
  - tests/browser/phase27-css-chain.browser.test.tsx
  - tests/browser/phase28-css-chain.browser.test.tsx
  - tests/browser/phase29-css-chain.browser.test.tsx
  - tests/browser/phase30-css-chain.browser.test.tsx
autonomous: true
requirements: [DEBT-10]
must_haves:
  truths:
    - "Phase 30 video element computed height is asserted > 0 and objectFit = cover (would have caught the h-full hotfix regression)"
    - "Every visual surface touched by Phases 25-30 has a computed-style assertion in a browser test"
    - "No browser-test assertion checks a class name"
  artifacts:
    - path: "tests/browser/phase30-css-chain.browser.test.tsx"
      provides: "CameraCaptureView aspect-square + video h-full/object-cover computed-style assertions"
      contains: "objectFit"
    - path: "tests/browser/phase26-css-chain.browser.test.tsx"
      provides: "WearDetailHero aspect-[4/5] + h-full object-cover computed-style assertions"
    - path: "tests/browser/phase27-css-chain.browser.test.tsx"
      provides: "ProfileWatchCard aspect-[4/5] + grid-cols-2 computed-style assertions"
    - path: "tests/browser/phase25-css-chain.browser.test.tsx"
      provides: "UserMenu gap-1 + size-11 computed-style assertions"
    - path: "tests/browser/phase29-css-chain.browser.test.tsx"
      provides: "ProfileTabs overflow-x/overflow-y computed-style assertions"
    - path: "tests/browser/phase28-css-chain.browser.test.tsx"
      provides: "WishlistRationalePanel prose layout computed-style assertion (low priority)"
  key_links:
    - from: "tests/browser/phase30-css-chain.browser.test.tsx"
      to: "real Chromium getComputedStyle"
      via: "window.getComputedStyle on video element"
      pattern: "getComputedStyle"
---

<objective>
Author the six browser-mode CSS-chain test files that cover every visual surface touched by Phases 25-30. Each file constructs raw DOM elements with the exact Tailwind classes from the component source and asserts real-browser computed styles — never class names. The Phase 30 file is the acceptance bar: its assertions must fail against the pre-hotfix `<video>` (no `h-full`) and pass after it.

Purpose: DEBT-10 D-07 mandates computed-style assertions for all Phase 25-31 visual surfaces; D-08 forbids class-name assertions. The v4.1 feedback memory (CSS-chain blind spot) is the direct origin — the Phase 30 black bar shipped through 6/6 token-validating PASS because nothing asserted the resolved chain.
Output: six `tests/browser/phaseNN-css-chain.browser.test.tsx` files, all green under the browser project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-01-SUMMARY.md

<interfaces>
Exact Tailwind class chains under test (from 42-PATTERNS.md component inspections):
- Phase 25 — UserMenu: `flex items-center gap-1` container; avatar `<a>` `size-11 rounded-full`.
- Phase 26 — WearDetailHero (src/components/wear/WearDetailHero.tsx:33-40): wrapper `w-full aspect-[4/5] overflow-hidden bg-muted`; `<img>` `w-full h-full object-cover`.
- Phase 27 — ProfileWatchCard (src/components/profile/ProfileWatchCard.tsx:61): wrapper `relative aspect-[4/5] bg-muted`; CollectionTabContent grid `grid grid-cols-2 gap-4`.
- Phase 28 — WishlistRationalePanel: prose container `space-y-2 text-sm text-muted-foreground`.
- Phase 29 — ProfileTabs (src/components/profile/ProfileTabs.tsx:65): `overflow-x-auto overflow-y-hidden pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden`.
- Phase 30 — CameraCaptureView (src/components/wywt/CameraCaptureView.tsx:120-137): wrapper `relative w-full aspect-square overflow-hidden rounded-md bg-black`; `<video>` `block h-full w-full object-cover`.

DOM-only test pattern (42-PATTERNS.md §Browser Test DOM-Only Pattern): construct raw elements, assign className, set an explicit `style.width` to anchor aspect-ratio math, append to body, read getComputedStyle, assert, remove.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author Phase 30 + Phase 26 CSS-chain browser tests (the regression-catching pair)</name>
  <files>tests/browser/phase30-css-chain.browser.test.tsx, tests/browser/phase26-css-chain.browser.test.tsx</files>
  <behavior>
    - phase30: aspect-square wrapper computes equal width and height (within 1px) and height > 0.
    - phase30: a `<video>` with `block h-full w-full object-cover` inside the wrapper computes height > 0 (fails if `h-full` absent — the regression) and `objectFit === 'cover'`.
    - phase26: `w-full aspect-[4/5] overflow-hidden bg-muted` wrapper computes height ≈ width × 1.25 and height > 0.
    - phase26: an `<img>` with `w-full h-full object-cover` computes height > 0 and `objectFit === 'cover'`.
  </behavior>
  <read_first>
    - src/components/wywt/CameraCaptureView.tsx (lines 120-137 — the exact wrapper + video class strings)
    - src/components/wear/WearDetailHero.tsx (lines 33-40 — the exact wrapper + img class strings)
    - tests/components/wywt/CameraCaptureView.test.tsx (existing unit test — header comment + import style)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md (§phase30 + §phase26 — full test bodies)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md (§Code Examples, §Common Pitfalls Pitfall 5)
  </read_first>
  <action>
    Create `tests/browser/phase30-css-chain.browser.test.tsx` and `tests/browser/phase26-css-chain.browser.test.tsx` using the DOM-only pattern from 42-PATTERNS.md (NO React, NO component imports — raw `document.createElement`, assign exact Tailwind classes from the component source, anchor with an explicit `style.width`, read `window.getComputedStyle`, assert, then remove the element).
    Phase 30 file: include the standard header comment block referencing DEBT-10 D-07/D-08 and `42-validation-backfill/30-VALIDATION.md`. In `beforeEach`, stub `navigator.mediaDevices.getUserMedia` via `vi.stubGlobal` to avoid the Chromium getUserMedia permission prompt (RESEARCH.md Pitfall 5) — even though the DOM-only pattern does not mount the component, keep the stub for safety. Two `it()` blocks: (1) `aspect-square` wrapper width≈height within 1px and >0; (2) `<video class="block h-full w-full object-cover">` inside the wrapper has computed `height` parsing > 0 AND `objectFit === 'cover'`. Add an inline comment on block (2) stating it would have caught the `h-full` hotfix regression — pre-hotfix the video lacked `h-full` so computed height was 0px / objectFit had nothing to cover.
    Phase 26 file: header comment block referencing DEBT-10 + `42-validation-backfill/26-VALIDATION.md`. One `it()` block asserting the `aspect-[4/5]` wrapper computes `height ≈ width × 1.25` (within 1px, >0) and the inner `<img class="w-full h-full object-cover">` computes `height > 0` and `objectFit === 'cover'`.
    EVERY assertion reads `window.getComputedStyle(el).<property>`. NO `classList.contains(...)` and NO `className` substring checks anywhere (D-08 hard ban).
  </action>
  <verify>
    <automated>npx vitest run --project browser tests/browser/phase30-css-chain.browser.test.tsx tests/browser/phase26-css-chain.browser.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist in `tests/browser/` and run under the `browser` project.
    - phase30 block (2) reads `getComputedStyle(video).height` and asserts `> 0`, and reads `objectFit` and asserts `'cover'` — an assertion that "would have caught the `h-full` hotfix regression" (computed height would be 0px / objectFit empty before the hotfix).
    - phase26 asserts the `aspect-[4/5]` wrapper computed `height` is within 1px of `width × 1.25`.
    - Zero occurrences of `classList` or `.className` substring assertions in either file (`grep -E "classList|className" tests/browser/phase30-css-chain.browser.test.tsx tests/browser/phase26-css-chain.browser.test.tsx | grep -v '^#'` returns no assertion lines).
    - `npx vitest run --project browser tests/browser/phase30-css-chain.browser.test.tsx tests/browser/phase26-css-chain.browser.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The two highest-stakes CSS-chain test files exist and pass; the Phase 30 video assertion is the concrete DEBT-10 acceptance bar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author Phase 27 + Phase 25 CSS-chain browser tests</name>
  <files>tests/browser/phase27-css-chain.browser.test.tsx, tests/browser/phase25-css-chain.browser.test.tsx</files>
  <behavior>
    - phase27: `relative aspect-[4/5] bg-muted` card wrapper computes height ≈ width × 1.25 and height > 0.
    - phase27: `grid grid-cols-2 gap-4` container renders two equal-width columns, each > 100px at a 360px container.
    - phase25: `flex items-center gap-1` container computes `display: flex`, `align-items: center`, `column-gap` ≈ 4px.
    - phase25: `size-11 rounded-full` avatar link computes width ≈ 44px and height ≈ 44px.
  </behavior>
  <read_first>
    - src/components/profile/ProfileWatchCard.tsx (line 61 — exact wrapper class)
    - src/components/profile/CollectionTabContent.tsx (line ~170 — exact grid class)
    - src/components/layout/UserMenu.tsx (the gap-1 container + size-11 avatar link classes)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md (§phase27 + §phase25 — full test bodies)
  </read_first>
  <action>
    Create `tests/browser/phase27-css-chain.browser.test.tsx` and `tests/browser/phase25-css-chain.browser.test.tsx` with the DOM-only pattern and the standard header comment block (each referencing DEBT-10 D-07/D-08 and its backfilled `42-validation-backfill/NN-VALIDATION.md`).
    Phase 27 file: two `it()` blocks — (1) `relative aspect-[4/5] bg-muted` wrapper anchored at `width = '180px'` computes `height` within 1px of `width × 1.25` and `> 0`; (2) `grid grid-cols-2 gap-4` container anchored at `width = '360px'` with two child divs computes each child width `> 100` and the two child widths within 1px of each other. Note in a comment that the Next.js `<Image fill>` internals are out of scope — the assertion targets the Tailwind CSS chain on a plain element.
    Phase 25 file: two `it()` blocks — (1) `flex items-center gap-1` container computes `display === 'flex'`, `alignItems === 'center'`, and `columnGap` parsing close to 4px; (2) an `<a class="size-11 rounded-full">` computes `width` and `height` close to 44px (Tailwind `size-11` = 11 × 4px).
    EVERY assertion reads `window.getComputedStyle`. NO class-name assertions (D-08).
  </action>
  <verify>
    <automated>npx vitest run --project browser tests/browser/phase27-css-chain.browser.test.tsx tests/browser/phase25-css-chain.browser.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist in `tests/browser/` and run under the `browser` project.
    - phase27 asserts the `aspect-[4/5]` wrapper computed `height` ≈ `width × 1.25` and the `grid-cols-2` container yields two equal-width columns via computed `width`.
    - phase25 asserts computed `columnGap` ≈ 4px and computed `width`/`height` ≈ 44px for `size-11`.
    - Zero `classList` / `.className` substring assertions in either file.
    - `npx vitest run --project browser tests/browser/phase27-css-chain.browser.test.tsx tests/browser/phase25-css-chain.browser.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Phase 27 and Phase 25 visual surfaces have computed-style coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author Phase 29 + Phase 28 CSS-chain browser tests</name>
  <files>tests/browser/phase29-css-chain.browser.test.tsx, tests/browser/phase28-css-chain.browser.test.tsx</files>
  <behavior>
    - phase29: ProfileTabs list class computes `overflowX` ∈ {auto, scroll}, `overflowY === 'hidden'`, `paddingBottom > 0`.
    - phase28: WishlistRationalePanel prose container computes `display: block` and `fontSize > 0`.
  </behavior>
  <read_first>
    - src/components/profile/ProfileTabs.tsx (line 65 — exact overflow class string)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md (§phase29 + §phase28 — full test bodies)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md (§Visual Surfaces by Phase — Phase 28 is LOW priority, minimal surface)
  </read_first>
  <action>
    Create `tests/browser/phase29-css-chain.browser.test.tsx` and `tests/browser/phase28-css-chain.browser.test.tsx` with the DOM-only pattern and the standard header comment block.
    Phase 29 file: one `it()` block — a `div` with the exact ProfileTabs list class `w-full justify-start gap-2 overflow-x-auto overflow-y-hidden pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden` anchored at `width = '320px'` computes `overflowX` in `['auto','scroll']`, `overflowY === 'hidden'`, and `paddingBottom` parsing `> 0`. Note in a comment that jsdom cannot resolve overflow — browser mode is required (this is why Phase 29 needs a browser test even though it was already `nyquist_compliant`).
    Phase 28 file: header comment notes Phase 28 was primarily copy/logic — minimal visual surface. One `it()` block — a `div` with `space-y-2 text-sm text-muted-foreground` computes `display === 'block'` and `fontSize` parsing `> 0`.
    EVERY assertion reads `window.getComputedStyle`. NO class-name assertions (D-08).
  </action>
  <verify>
    <automated>npx vitest run --project browser tests/browser/phase29-css-chain.browser.test.tsx tests/browser/phase28-css-chain.browser.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist in `tests/browser/` and run under the `browser` project.
    - phase29 asserts computed `overflowX` ∈ {auto, scroll} and `overflowY === 'hidden'` — values jsdom cannot produce.
    - phase28 asserts computed `display` and `fontSize` on the prose container.
    - Zero `classList` / `.className` substring assertions in either file.
    - `npx vitest run --project browser tests/browser/phase29-css-chain.browser.test.tsx tests/browser/phase28-css-chain.browser.test.tsx` exits 0.
    - Full browser project green: `npx vitest run --project browser` exits 0 (all six phaseNN files + the Wave 0 smoke test).
  </acceptance_criteria>
  <done>All six Phase 25-30 CSS-chain browser test files exist and pass; the browser project is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — test code only) | This plan adds `tests/browser/*.browser.test.tsx` files. No production code, no API, no user input, no data handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Tampering | Test files (`tests/browser/`) | accept | Test-only code; cannot affect production behavior. The `vi.stubGlobal('navigator', ...)` in the Phase 30 file is scoped to the test process and torn down by Vitest between files. |

This plan authors test assertions only. There is no production threat surface; documenting that honestly per the security-enforcement note for this phase.
</threat_model>

<verification>
- `npx vitest run --project browser` exits 0 — all six phaseNN files plus the Wave 0 smoke test pass.
- `grep -rE "classList|\.className" tests/browser/phase2*.browser.test.tsx tests/browser/phase3*.browser.test.tsx | grep -v '^[^:]*:#'` finds no class-name assertions (D-08 enforcement).
- `npm test` introduces no NEW failures in the jsdom unit project vs. the ~51-failing baseline.
</verification>

<success_criteria>
- Every visual surface touched by Phases 25-30 has a computed-style assertion.
- The Phase 30 video assertion concretely encodes the `h-full` regression-catching bar.
- Zero class-name assertions across all browser test files.
</success_criteria>

<output>
After completion, create `.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-02-SUMMARY.md`
</output>
