---
phase: 42-nyquist-hardening-sweep-uat-triage-parallel-track
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.workspace.ts
  - tests/browser/css-chain-smoke.browser.test.tsx
autonomous: true
requirements: [DEBT-10]
must_haves:
  truths:
    - "npm test runs both a jsdom unit project and a browser/chromium project under one runner"
    - "A browser-mode test can read a real Tailwind-resolved computed style (not an empty string)"
    - "All existing jsdom tests still resolve @/* and server-only aliases"
  artifacts:
    - path: "vitest.workspace.ts"
      provides: "Two-project workspace (unit jsdom + browser chromium)"
      contains: "defineWorkspace"
    - path: "tests/browser/css-chain-smoke.browser.test.tsx"
      provides: "Wave 0 smoke test proving Tailwind CSS reaches the browser iframe"
    - path: "package.json"
      provides: "@vitest/browser@2.1.9 + playwright devDependencies"
      contains: "@vitest/browser"
  key_links:
    - from: "vitest.workspace.ts"
      to: "vitest.config.ts"
      via: "extends for the unit project"
      pattern: "extends.*vitest.config"
    - from: "vitest.workspace.ts browser project"
      to: "playwright chromium"
      via: "provider string"
      pattern: "provider.*playwright"
---

<objective>
Establish the browser-test infrastructure DEBT-10 depends on: install `@vitest/browser@2.1.9` + `playwright`, add a two-project `vitest.workspace.ts` (unit/jsdom + browser/chromium), and prove with a smoke test that real Tailwind-resolved computed styles are reachable from the browser project. This is the Wave 0 of DEBT-10 — no CSS-chain assertion or VALIDATION.md can be authored until this lands.

Purpose: D-06 mandates real-browser computed-style assertions; jsdom's `getComputedStyle` returns empty strings for Tailwind utility classes. A single runner (D-09 discretion: prefer Vitest browser mode over standalone `@playwright/test`) keeps `npm test` as the one entry point.
Output: `vitest.workspace.ts`, browser deps in `package.json`, a passing smoke test in `tests/browser/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md

<interfaces>
Existing vitest.config.ts (repo root) — the unit project extends this:
- `environment: 'jsdom'`, `setupFiles: ['./tests/setup.tsx']`, `globals: true`
- `include`: tests/**/*.test.{ts,tsx} + src/**/*.test.{ts,tsx}
- `resolve.alias`: `@` → ./src, `server-only` → ./tests/shims/server-only.ts

The new workspace MUST NOT break either alias for the existing jsdom suite.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install browser-mode dependencies (correct v2.x packages)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (confirm vitest is `^2.1.9`; confirm @vitest/browser and playwright are absent)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md (§Standard Stack, §Common Pitfalls — Pitfall 1)
  </read_first>
  <action>
    Run `npm install --save-dev @vitest/browser@2.1.9 playwright` then `npx playwright install chromium`.
    Pin `@vitest/browser` to EXACTLY `2.1.9` — its peerDependency is `{ vitest: '2.1.9' }` and a mismatch fails install. `playwright` takes the current published version (`^1.x`).
    DO NOT install `@vitest/browser-playwright` — that is a v3/v4-only package and does not exist for the v2.x line; installing it causes version conflicts (RESEARCH.md Pitfall 1). DO NOT upgrade `vitest` itself off `^2.1.9`.
    After install, confirm `package.json` devDependencies contains `@vitest/browser` at `2.1.9` and `playwright`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d=p.devDependencies||{};if(!d['@vitest/browser']||!d['@vitest/browser'].includes('2.1.9'))throw new Error('@vitest/browser must be 2.1.9');if(!d['playwright'])throw new Error('playwright missing');if(d['@vitest/browser-playwright'])throw new Error('forbidden @vitest/browser-playwright present');console.log('deps OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies has `@vitest/browser` resolving to `2.1.9` and `playwright` present.
    - `@vitest/browser-playwright` is NOT in `package.json` (forbidden v3/v4 package).
    - `vitest` devDependency remains `^2.1.9` (unchanged).
    - `npx playwright install chromium` completed without error (Chromium binary present).
  </acceptance_criteria>
  <done>Browser-mode dependencies installed at the correct v2.x versions; Chromium binary available.</done>
</task>

<task type="auto">
  <name>Task 2: Author vitest.workspace.ts (unit jsdom + browser chromium)</name>
  <files>vitest.workspace.ts</files>
  <read_first>
    - vitest.config.ts (the unit project extends this; do not modify it)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md (§`vitest.workspace.ts` config — VERIFIED v2.1.9 shape)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md (§Pattern 1, §Code Examples — vitest.workspace.ts)
  </read_first>
  <action>
    Create `vitest.workspace.ts` at the repo root using `defineWorkspace` imported from `vitest/config`. Define exactly two projects:
    - Project `unit`: `extends: './vitest.config.ts'`; `name: 'unit'`; `include` covers `tests/**/*.test.{ts,tsx}` + `src/**/*.test.{ts,tsx}`; `exclude: ['tests/browser/**']` so browser files do not run under jsdom.
    - Project `browser`: `name: 'browser'`; `include: ['tests/browser/**/*.browser.test.{ts,tsx}']`; `browser` block with `enabled: true`, `provider: 'playwright'` (a STRING literal — v2.x API, NOT a `playwright()` function import), `name: 'chromium'` (v2.x uses `name`, NOT `instances`), `headless: true`. Add `resolve.alias` mapping `@` to `./src` (the browser project does not inherit `vitest.config.ts`).
    Keep `package.json`'s `"test": "vitest run"` script unchanged — `vitest run` auto-detects `vitest.workspace.ts` and runs both projects.
    Do NOT use the v3/v4 `provider: playwright()` function-import pattern; it does not apply to Vitest 2.1.9.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('vitest.workspace.ts','utf8');if(!s.includes('defineWorkspace'))throw new Error('no defineWorkspace');if(!/provider:\s*'playwright'/.test(s))throw new Error('provider must be string literal playwright');if(/playwright\(\)/.test(s))throw new Error('forbidden v3/v4 playwright() function call');if(!s.includes(\"extends: './vitest.config.ts'\"))throw new Error('unit project must extend vitest.config.ts');if(!/exclude:.*tests\/browser/.test(s))throw new Error('unit project must exclude tests/browser');console.log('workspace OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.workspace.ts` exists at repo root and exports `defineWorkspace([...])` with exactly two project objects.
    - The `unit` project has `extends: './vitest.config.ts'` and `exclude: ['tests/browser/**']`.
    - The `browser` project sets `provider: 'playwright'` as a string literal and `name: 'chromium'`.
    - No `playwright()` function-call form appears anywhere in the file (v3/v4 API forbidden).
    - `package.json` `"test"` script is still `"vitest run"` (unchanged).
  </acceptance_criteria>
  <done>Two-project workspace config committed; `npm test` will route jsdom tests to the unit project and `.browser.test.tsx` files to the chromium project.</done>
</task>

<task type="auto">
  <name>Task 3: Author the Tailwind-served smoke test (browser project)</name>
  <files>tests/browser/css-chain-smoke.browser.test.tsx</files>
  <read_first>
    - vitest.workspace.ts (created in Task 2 — confirm the browser project include glob matches `*.browser.test.tsx`)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md (§Open Questions item 3 — A3 assumption; §Common Pitfalls Pitfall 2)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md (§Browser Test DOM-Only Pattern, §Vitest Test File Header Comment Block)
  </read_first>
  <action>
    Create `tests/browser/css-chain-smoke.browser.test.tsx` in the repo's `tests/browser/` directory (create the directory). Use the DOM-only pattern from 42-PATTERNS.md §Browser Test DOM-Only Pattern — `import { describe, it, expect } from 'vitest'`, NO React, NO component imports. Include the standard header comment block (file path + DEBT-10 D-07/D-08 reference + "Wave 0 smoke test: confirms Tailwind CSS reaches the browser iframe").
    Author two assertions:
    1. `bg-black` resolves to a non-empty computed `background-color` equal to `rgb(0, 0, 0)` — create a `div` with `className = 'bg-black'`, append to `document.body`, read `window.getComputedStyle(el).backgroundColor`, assert it equals `'rgb(0, 0, 0)'`, then remove the element.
    2. `aspect-square` resolves a real ratio — create a `div` with `className = 'aspect-square'`, set `el.style.width = '200px'`, append, assert computed `height` parses to a number greater than 0 and within 1px of the width.
    The purpose is to prove RESEARCH.md assumption A3 (Tailwind PostCSS output is served to the browser iframe). If either computed value comes back as an empty string, the smoke test fails — that is the intended signal that the browser project needs a CSS import in setup. If it fails for that reason, add a CSS import for `src/app/globals.css` to a browser-project `setupFiles` entry in `vitest.workspace.ts` and document the addition in the SUMMARY; otherwise no config change is needed.
  </action>
  <verify>
    <automated>npx vitest run --project browser tests/browser/css-chain-smoke.browser.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `tests/browser/css-chain-smoke.browser.test.tsx` exists and runs under the `browser` project.
    - The `bg-black` assertion reads `window.getComputedStyle(el).backgroundColor` and asserts `'rgb(0, 0, 0)'` — a real computed value, NOT a class-name check, NOT an empty string.
    - The `aspect-square` assertion confirms computed `height > 0` and within 1px of an explicit `width`.
    - `npx vitest run --project browser tests/browser/css-chain-smoke.browser.test.tsx` exits 0 (both assertions pass in real Chromium).
    - No `classList.contains(...)` or `className` substring assertion appears in the file (D-08 forbids class-name assertions).
  </acceptance_criteria>
  <done>Smoke test green — Tailwind-resolved computed styles confirmed reachable from the browser project; Wave 0 infrastructure proven before any CSS-chain assertion is authored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | New dev-only packages (`@vitest/browser`, `playwright`) are pulled in; they never ship in production bundles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | New devDependencies (`@vitest/browser`, `playwright`) | accept | Dev-tooling only; not shipped to production. `package-lock.json` pins resolved versions and is committed. No runtime/user-facing surface. |
| T-42-02 | Elevation of Privilege | Playwright Chromium browser process spawned by tests | accept | Runs locally/headless in the dev test runner only; no network-facing exposure; no CI in this phase (CI explicitly deferred per CONTEXT.md Deferred Ideas). |

This plan adds test infrastructure only — no production code, no API surface, no data handling. Threat surface is minimal and documented honestly.
</threat_model>

<verification>
- `npm test` runs (both projects discovered; existing jsdom suite still resolves `@/*` and `server-only` aliases — no NEW regressions vs. the ~51-failing baseline noted in 42-RESEARCH.md §Sampling Rate).
- `npx vitest run --project browser tests/browser/css-chain-smoke.browser.test.tsx` exits 0.
- `package.json` carries `@vitest/browser@2.1.9` + `playwright`; does NOT carry `@vitest/browser-playwright`.
</verification>

<success_criteria>
- Two-project Vitest workspace operational under a single `npm test`.
- Real-browser computed styles proven reachable (smoke test green).
- No Vitest upgrade; v2.x string-provider API used.
- No new failures in the existing jsdom suite.
</success_criteria>

<output>
After completion, create `.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-01-SUMMARY.md`
</output>
