---
phase: 42-nyquist-hardening-sweep-uat-triage-parallel-track
plan: 05
type: execute
wave: 2
depends_on: ['04']
files_modified:
  - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-HUMAN-UAT.md
  - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-CONTEXT.md
autonomous: false
requirements: [DEBT-11]
must_haves:
  truths:
    - "The user has run every CLOSED-candidate UAT item and recorded a pass/fail verdict"
    - "42-CONTEXT.md carries a <triage> section with the final closure table for all ~33 items"
    - "Every closure row has a disposition (CLOSED / SUPERSEDED / DEFERRED) and a resolution note"
    - "D-02: the CLOSED-candidate set runs as a blocking 42-HUMAN-UAT.md checkpoint — the phase cannot close until the user signs off; no provisional rows ship"
    - "D-05: the final closure table is appended to 42-CONTEXT.md as a <triage> section — the canonical DEBT-11 artifact"
  artifacts:
    - path: ".planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-CONTEXT.md"
      provides: "Final DEBT-11 closure table appended as a <triage> section"
      contains: "<triage>"
  key_links:
    - from: "42-HUMAN-UAT.md results"
      to: "42-CONTEXT.md <triage> closure table CLOSED rows"
      via: "human-recorded pass/fail verdicts feed the closure note"
      pattern: "CLOSED"
---

<objective>
Run the blocking human-UAT checklist for the CLOSED-candidate set (D-02), then append the final DEBT-11 closure table to `42-CONTEXT.md` as a `<triage>` section (D-05) — the canonical DEBT-11 artifact. The phase cannot close until every CLOSED-candidate has a non-`pending` verdict and the closure table covers all ~33 items.

Purpose: D-02 mandates a blocking step — no provisional rows ship; the closure table is finalized with real CLOSED / FAIL results before the phase closes. D-05 mandates the table live in `42-CONTEXT.md` per ROADMAP SC#4.
Output: completed `42-HUMAN-UAT.md` and a `<triage>` section in `42-CONTEXT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-CONTEXT.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PRE-TRIAGE.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-HUMAN-UAT.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run the blocking CLOSED-candidate UAT checklist</name>
  <what-built>
    Plan 04 produced `42-PRE-TRIAGE.md` (all ~33 UAT items classified with cited evidence) and `42-HUMAN-UAT.md` (a numbered checklist of the surviving CLOSED-candidate items, each with `result: [pending]`). SUPERSEDED and DEFERRED items already closed on evidence and are NOT in this checklist.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-HUMAN-UAT.md`.
    2. For each `### N.` entry: perform the steps in `expected:` against the running app (run `npm run dev` and use the URLs/preconditions noted in each entry; some Phase 22 items need live email/network — test on prod where the entry says so).
    3. After each item, edit that entry's `result:` line to `pass` or `fail` (replace `[pending]`). If an item turns out to be impractical to run, change `result:` to `deferred` and add a one-line reason — it will be reclassified DEFERRED in the closure table rather than left pending.
    4. Update the `## Summary` block counts (`passed`, `issues`, `pending`, `blocked`) to match.
    5. Confirm zero `result: [pending]` lines remain.
  </how-to-verify>
  <resume-signal>Type "approved" once every checklist item has a pass/fail/deferred verdict and no `[pending]` remains, or describe any blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Append the final DEBT-11 closure table to 42-CONTEXT.md</name>
  <files>.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-CONTEXT.md</files>
  <read_first>
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-HUMAN-UAT.md (the user's completed pass/fail/deferred verdicts from Task 1)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PRE-TRIAGE.md (SUPERSEDED/DEFERRED rows with cited evidence)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-CONTEXT.md (D-05 — the closure table appends here as a <triage> section)
  </read_first>
  <action>
    Append a new `<triage>` section to the END of `42-CONTEXT.md` (do NOT modify the existing `<domain>` / `<decisions>` / other sections — append-only).
    The `<triage>` section contains a single closure table covering ALL ~33 items, columns: `# | Item Description | Original Phase | Disposition | Resolution Note`.
    Build it by merging two sources:
    - SUPERSEDED + DEFERRED rows: carry directly from `42-PRE-TRIAGE.md` with their cited evidence as the resolution note.
    - CLOSED-candidate rows: read each item's verdict from the completed `42-HUMAN-UAT.md`. A `pass` verdict → `Disposition: CLOSED`, resolution note = the UAT result (e.g. "UAT 2026-05-1X: passed"). A `fail` verdict → keep `Disposition: CLOSED` is WRONG — instead record `Disposition: CLOSED` only on pass; on `fail` record the item under a `## Gaps` note in 42-HUMAN-UAT.md and mark the closure row `Disposition: DEFERRED` with reason "UAT failed — carry to v5.x gap-closure" (a failed item is a real gap, not a closure). A `deferred` verdict → `Disposition: DEFERRED` with the user's reason.
    Add a final row noting D-04: "5 stale Phase 20.1 debug entries — already resolved prior to Phase 42 (present in `.planning/debug/resolved/`); no action needed." Mark its disposition `SUPERSEDED` (resolved by gap-closure plans 20.1-06/07/08).
    Add a one-line summary above the table: total items + per-disposition counts.
    Confirm every row has a non-empty Disposition and Resolution Note.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-CONTEXT.md','utf8');if(!/<triage>/.test(s)||!/<\/triage>/.test(s))throw new Error('missing <triage> section');const seg=s.split('<triage>')[1].split('</triage>')[0];const rows=seg.split('\n').filter(l=>/^\|\s*[\dD]/.test(l));if(rows.length<30)throw new Error('closure table needs ~33+ rows, found '+rows.length);for(const r of rows){const c=r.split('|').map(x=>x.trim());if(!/^(CLOSED|SUPERSEDED|DEFERRED)$/.test(c[4]||''))throw new Error('bad disposition: '+r);if(!(c[5]||'').length)throw new Error('empty resolution note: '+r);}console.log('closure table OK: '+rows.length+' rows')"</automated>
  </verify>
  <acceptance_criteria>
    - `42-CONTEXT.md` has a new `<triage>` section appended at the end; pre-existing sections are unchanged.
    - The closure table covers all ~33 items plus the D-04 note row; columns are `# | Item Description | Original Phase | Disposition | Resolution Note`.
    - Every row's Disposition is exactly `CLOSED`, `SUPERSEDED`, or `DEFERRED`; every row has a non-empty Resolution Note.
    - CLOSED rows reflect a `pass` verdict from `42-HUMAN-UAT.md`; any `fail` verdict is recorded as `DEFERRED` (gap carried to v5.x), not CLOSED.
    - `42-HUMAN-UAT.md` has zero `result: [pending]` lines (verified at Task 1 checkpoint).
  </acceptance_criteria>
  <done>The canonical DEBT-11 closure table is in `42-CONTEXT.md`; every UAT item has a final, evidence-or-verdict-backed disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — planning artifacts + human verification) | This plan runs a manual UAT checklist and appends a Markdown closure table. No code, no API, no data handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06 | Repudiation | DEBT-11 closure table | accept | The blocking checkpoint (D-02) requires real human pass/fail verdicts before any CLOSED row ships; failed items become DEFERRED gaps rather than being silently closed. The closure table records evidence per row. Documentation + human-verification only; no runtime threat surface. |

This plan produces a closure table and runs a manual checklist. No production threat surface; documented honestly per the security-enforcement note for this phase.
</threat_model>

<verification>
- `42-HUMAN-UAT.md` has no `result: [pending]` lines.
- `42-CONTEXT.md` `<triage>` section closure table covers all ~33 items + D-04; every row has a disposition and resolution note.
</verification>

<success_criteria>
- ROADMAP SC#3 satisfied — all ~33 UAT items triaged to CLOSED / SUPERSEDED / DEFERRED.
- ROADMAP SC#4 satisfied — closure table exists in phase CONTEXT.md.
- No provisional rows: every CLOSED row is backed by a real UAT pass verdict.
</success_criteria>

<output>
After completion, create `.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-05-SUMMARY.md`
</output>
