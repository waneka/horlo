---
phase: 69
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/watch/useCatalogSearchCache.ts
  - src/components/watch/useCatalogSearchCache.test.ts
  - src/components/watch/useStructuredExtractCache.ts
  - src/components/watch/useStructuredExtractCache.test.ts
autonomous: true
requirements: [CLNP-07]
tags: [module-scope-cache, viewerUserId-reset, cache-hygiene]

must_haves:
  truths:
    - "D-06: Each cache hook owns its own lastUserId check inline in render — mirrors useWatchSearchVerdictCache.ts:42-45 pattern with viewerUserId as discriminant"
    - "D-06: The reset block uses intentional sync mutation in render (NOT setState) — module state has no React-tracked subscribers"
    - "D-07: useCatalogSearchCache + useStructuredExtractCache accept viewerUserId as a REQUIRED positional arg (no getUser() inside hooks — proxy_router_cache_poisoning memory)"
    - "D-09: Per-hook unit tests written behavioral (no static fs guards — vitest_static_node_env memory)"
    - "D-18: useStructuredExtractCache key is JSON.stringify({brand: brand.trim().toLowerCase(), model: model.trim().toLowerCase(), reference: (reference ?? '').trim().toLowerCase(), year: year ?? null})"
    - "Discretion: useCatalogSearchCache key is query.trim().toLowerCase() (symmetric normalization with the catalog DAL natural-key normalization)"
    - "Both new hooks export __reset___ForTests() that resets BOTH moduleCache AND moduleUserId — used by AddWatchFlow.test.tsx integration test in Plan 06"
    - "Both hooks have a stale-write guard inside set(): if (moduleUserId !== viewerUserId) return; — mirrors useWatchSearchVerdictCache.ts:52-54"
    - "CLNP-07: Switching viewerUserId across renders clears the cache (unit-tested) — the per-hook behavior that composes into the four-cache integration test in Plan 06"
    - "Memory project_baseline_not_green_build_is_gate: npm run build exit 0 is the authoritative phase gate; ~77 pre-existing tsc errors are baseline noise — don't chase"
  artifacts:
    - path: "src/components/watch/useCatalogSearchCache.ts"
      provides: "Module-scope Map<string, SearchCatalogWatchResult[]> cache with viewerUserId-keyed reset"
      exports: ["useCatalogSearchCache", "__resetCatalogSearchCacheForTests"]
      min_lines: 45
    - path: "src/components/watch/useCatalogSearchCache.test.ts"
      provides: "Unit tests — set/get round-trip + user-switch invalidation"
      contains: "describe.*useCatalogSearchCache"
    - path: "src/components/watch/useStructuredExtractCache.ts"
      provides: "Module-scope Map<string, ExtractCacheEntry> cache with JSON-tuple key (D-18)"
      exports: ["useStructuredExtractCache", "__resetStructuredExtractCacheForTests"]
      min_lines: 45
    - path: "src/components/watch/useStructuredExtractCache.test.ts"
      provides: "Unit tests — set/get round-trip + user-switch invalidation + D-18 key format"
      contains: "describe.*useStructuredExtractCache"
  key_links:
    - from: "src/components/watch/useCatalogSearchCache.ts"
      to: "module-scope state (let moduleCache, let moduleUserId)"
      via: "in-render reset mutation"
      pattern: "moduleUserId !== viewerUserId"
    - from: "src/components/watch/useStructuredExtractCache.ts"
      to: "ExtractCacheEntry type (imported from useUrlExtractCache.ts OR re-declared)"
      via: "type import"
      pattern: "ExtractCacheEntry"
---

<objective>
Ship the two NEW module-scope caches the Wave 2 components consume:

1. **`useCatalogSearchCache`** — caches SearchEntry debounced typeahead results; keyed by lowercased trimmed query (Discretion); value = `SearchCatalogWatchResult[]`.
2. **`useStructuredExtractCache`** — caches StructuredEntryPanel structured-extract results; keyed by JSON-stringified `{brand, model, reference, year}` tuple with per-field `.trim().toLowerCase()` (D-18); value = `ExtractCacheEntry` (same shape as the existing URL cache).

Both implement the D-06 user-switch reset: at the top of the hook body, if `moduleUserId !== viewerUserId`, reset both `moduleCache` and `moduleUserId`. Stale-write guard inside `set()`. Both export `__reset___ForTests()` for the Plan 06 integration test.

This plan is independent of Plans 01/03 — runs parallel in Wave 1.

Purpose: Establish the two new cache primitives before SearchEntry (Plan 05) and StructuredEntryPanel (Plan 04) wire them.
Output: 4 new files (2 hooks + 2 colocated tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-CONTEXT.md
@.planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-RESEARCH.md
@.planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-PATTERNS.md
@.planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-VALIDATION.md

<interfaces>
<!-- Closest analog: src/components/watch/useUrlExtractCache.ts (entire file, 51 lines). -->
<!-- Reset-block analog: src/components/search/useWatchSearchVerdictCache.ts lines 38-45 + 51-54 stale-write guard. -->

From `src/components/watch/useUrlExtractCache.ts`:
```typescript
'use client'
import type { ExtractedWatchData } from '@/lib/extractors'

export type ExtractCacheEntry = {
  catalogId: string
  extracted: ExtractedWatchData
  catalogIdError: string | null
}

let moduleCache: Map<string, ExtractCacheEntry> = new Map()

export function __resetUrlExtractCacheForTests(): void {
  moduleCache = new Map()
}

export function useUrlExtractCache() {
  return {
    get: (url: string): ExtractCacheEntry | undefined => moduleCache.get(url),
    set: (url: string, entry: ExtractCacheEntry): void => {
      moduleCache.set(url, entry)
    },
  }
}
```

From `src/components/search/useWatchSearchVerdictCache.ts` lines 38-45 (reset pattern) and 51-54 (stale-write guard) — Phase 69 mirrors these BYTE-FOR-BYTE in both new hooks.

From `src/lib/searchTypes.ts`:
- `SearchCatalogWatchResult` shape (verified in RESEARCH.md `ownersCount` is present)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useCatalogSearchCache module-scope cache + test (D-06, D-07, CLNP-07)</name>
  <files>src/components/watch/useCatalogSearchCache.ts, src/components/watch/useCatalogSearchCache.test.ts</files>
  <read_first>
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-CONTEXT.md (D-06 reset mechanism, D-07 prop-drill viewerUserId, Discretion §"Cache key for useCatalogSearchCache" — `query.trim().toLowerCase()`)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-RESEARCH.md (lines 587–615 — full code sketch of useCatalogSearchCache including stale-write guard)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-PATTERNS.md (§useCatalogSearchCache — mirror useUrlExtractCache.ts entire file shape; reset block from useWatchSearchVerdictCache.ts:38-45)
    - src/components/watch/useUrlExtractCache.ts (full 51-line analog to mirror)
    - src/components/search/useWatchSearchVerdictCache.ts (lines 38-45 reset block + lines 51-54 stale-write guard — both copy verbatim)
    - src/lib/searchTypes.ts (SearchCatalogWatchResult type)
  </read_first>
  <behavior>
    - Test 1: set("omega") then get("omega") returns the stored array for the same viewerUserId
    - Test 2: switching viewerUserId across renders clears the cache — get(key) returns undefined after rerender with new uid
    - Test 3: stale-write guard — call set() with the old user's hook return AFTER user-switch; the new user's cache stays empty (the stale closure cannot pollute)
    - Test 4: __resetCatalogSearchCacheForTests() resets BOTH moduleCache AND moduleUserId (subsequent get returns undefined AND a fresh viewerUserId triggers the reset-block as if first render)
  </behavior>
  <action>
    Create `src/components/watch/useCatalogSearchCache.ts`. Top-of-file directive: `'use client'`. Mirror the file shape of `src/components/watch/useUrlExtractCache.ts` (51 lines analog) with these specific deltas:

    1. Imports: replace `ExtractedWatchData` import with `import type { SearchCatalogWatchResult } from '@/lib/searchTypes'`.

    2. Module-level state — TWO let bindings:
       - `let moduleCache: Map<string, SearchCatalogWatchResult[]> = new Map()`
       - `let moduleUserId = ''` (NEW — Phase 69 D-06)

    3. Export `__resetCatalogSearchCacheForTests()` that resets BOTH `moduleCache = new Map()` AND `moduleUserId = ''`. Use this exact export name (Plan 06 integration test imports it).

    4. Export the hook with the signature `export function useCatalogSearchCache(viewerUserId: string)`. The viewerUserId arg is REQUIRED — not optional — per D-08 design rule (TypeScript surfaces every caller).

    5. At the TOP of the hook body, BEFORE the return statement, copy the D-06 reset block verbatim from `useWatchSearchVerdictCache.ts:42-45`:
       ```
       if (moduleUserId !== viewerUserId) {
         moduleCache = new Map()
         moduleUserId = viewerUserId
       }
       ```
       Mark this with a JSDoc-style comment matching the existing `useWatchSearchVerdictCache.ts` rationale ("Intentional sync mutation in render; module state has no React-tracked subscribers").

    6. Return shape (mirrors useUrlExtractCache):
       - `get: (key: string) => moduleCache.get(key)` — typed return `SearchCatalogWatchResult[] | undefined`
       - `set: (key: string, results: SearchCatalogWatchResult[]) => void` — INSIDE the body, add a stale-write guard (mirrors useWatchSearchVerdictCache.ts:52-54): `if (moduleUserId !== viewerUserId) return;` BEFORE `moduleCache.set(key, results)`.

    7. Cache key: callers pass `query.trim().toLowerCase()` themselves (the hook stays key-shape-agnostic — same as useUrlExtractCache treats `url` as opaque); document the expected key shape in JSDoc but do NOT normalize inside the hook (consistent with useUrlExtractCache discipline).

    Create co-located `src/components/watch/useCatalogSearchCache.test.ts` (NOT `.tsx`; the test is hook-only, no React render). Use `renderHook` from `@testing-library/react`. Pattern from PATTERNS.md §`useCatalogSearchCache.test.ts`:
    - Import { useCatalogSearchCache, __resetCatalogSearchCacheForTests } from the canonical path
    - `beforeEach(() => __resetCatalogSearchCacheForTests())`
    - 4 tests per Behavior block above

    Test 2 pattern:
    ```
    const { result, rerender } = renderHook(({ uid }) => useCatalogSearchCache(uid), { initialProps: { uid: 'user-a' } })
    result.current.set('omega', [/* fixture */])
    rerender({ uid: 'user-b' })
    expect(result.current.get('omega')).toBeUndefined()
    ```

    Test 3 (stale-write guard) — capture the hook return object BEFORE rerender, then call .set() AFTER rerender; assert the new-user cache stays empty.
  </action>
  <verify>
    <automated>npm run test -- --run src/components/watch/useCatalogSearchCache.test.ts</automated>
  </verify>
  <done>
    All 4 tests pass exit 0. Hook signature is `useCatalogSearchCache(viewerUserId: string)` (required positional arg). Module-level `let moduleUserId = ''` present. `__resetCatalogSearchCacheForTests` resets both module vars. Stale-write guard present inside `set()`.
  </done>
  <acceptance_criteria>
    - File: `src/components/watch/useCatalogSearchCache.ts` exists with `'use client'` directive
    - Source: `grep -c "^export function useCatalogSearchCache(viewerUserId: string)" src/components/watch/useCatalogSearchCache.ts` returns `1`
    - Source: `grep -c "let moduleUserId" src/components/watch/useCatalogSearchCache.ts` returns `1`
    - Source: `grep -c "^export function __resetCatalogSearchCacheForTests" src/components/watch/useCatalogSearchCache.ts` returns `1`
    - Source: stale-write guard present — `grep -c "if (moduleUserId !== viewerUserId) return" src/components/watch/useCatalogSearchCache.ts` returns `1`
    - Behavior: `npm run test -- --run src/components/watch/useCatalogSearchCache.test.ts` exits 0
    - Counter-assertion: hook does NOT call getUser() — `grep -c "getUser\|createSupabaseBrowserClient" src/components/watch/useCatalogSearchCache.ts` returns `0`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useStructuredExtractCache module-scope cache + test (D-06, D-18, CLNP-07)</name>
  <files>src/components/watch/useStructuredExtractCache.ts, src/components/watch/useStructuredExtractCache.test.ts</files>
  <read_first>
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-CONTEXT.md (D-18 — JSON-tuple key shape with per-field trim+lower normalization; D-06 reset; D-07 prop-drill)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-RESEARCH.md (lines 618–625 — useStructuredExtractCache identical shape to useCatalogSearchCache with D-18 key)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-PATTERNS.md (§useStructuredExtractCache — identical shape; D-18 key derivation example)
    - src/components/watch/useUrlExtractCache.ts (analog file; the new hook can RE-USE the `ExtractCacheEntry` type via import OR re-declare — Plan 04/Plan 06 import sites will surface the choice)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-CONTEXT.md (Memory project_local_catalog_natural_key_drift — D-18 normalization aligns with catalog natural-key normalization)
  </read_first>
  <behavior>
    - Test 1: set with a D-18 JSON-tuple key then get with the same key returns the stored ExtractCacheEntry for the same viewerUserId
    - Test 2: switching viewerUserId across renders clears the cache — get(key) returns undefined after rerender with new uid
    - Test 3: D-18 key format — JSON.stringify({brand, model, reference, year}) where: brand="omega", model="speedmaster", reference="3135", year=null is the canonical fixture; both writer and reader use the same JSON.stringify call shape
    - Test 4: __resetStructuredExtractCacheForTests() resets BOTH moduleCache AND moduleUserId
    - Test 5: stale-write guard — same as useCatalogSearchCache Task 1 Test 3 pattern
  </behavior>
  <action>
    Create `src/components/watch/useStructuredExtractCache.ts`. Top-of-file directive: `'use client'`. Mirror the file shape of `useCatalogSearchCache.ts` (Task 1) with these specific deltas:

    1. Imports: re-use the `ExtractCacheEntry` type via `import type { ExtractCacheEntry } from '@/components/watch/useUrlExtractCache'` (canonical re-export; avoids type-duplication drift if `ExtractedWatchData` shape changes).

    2. Module-level state:
       - `let moduleCache: Map<string, ExtractCacheEntry> = new Map()`
       - `let moduleUserId = ''`

    3. Export `__resetStructuredExtractCacheForTests()` that resets both module vars.

    4. Hook signature: `export function useStructuredExtractCache(viewerUserId: string)`. Required positional arg per D-08.

    5. Same D-06 reset block at top of hook body verbatim (`if (moduleUserId !== viewerUserId) { moduleCache = new Map(); moduleUserId = viewerUserId; }`).

    6. Return shape identical to useCatalogSearchCache:
       - `get: (key: string) => moduleCache.get(key)` — typed return `ExtractCacheEntry | undefined`
       - `set: (key: string, entry: ExtractCacheEntry) => void` — INSIDE body, stale-write guard `if (moduleUserId !== viewerUserId) return;` BEFORE `moduleCache.set(key, entry)`.

    7. Cache key (D-18 — callers responsibility, document in JSDoc): callers pass `JSON.stringify({ brand: brand.trim().toLowerCase(), model: model.trim().toLowerCase(), reference: (reference ?? '').trim().toLowerCase(), year: year ?? null })`. The hook stays key-shape-agnostic (treats `key: string` as opaque) — same discipline as useUrlExtractCache. JSDoc must spell out the D-18 key shape explicitly so SearchEntry/StructuredEntryPanel build deterministically against the same normalization.

    Create co-located `src/components/watch/useStructuredExtractCache.test.ts`. Same `renderHook` pattern as Task 1. Use the D-18 key fixture:
    ```
    const key = JSON.stringify({ brand: 'omega', model: 'speedmaster', reference: '3135', year: null })
    const entry: ExtractCacheEntry = { catalogId: 'cat-uuid', extracted: {/* fixture from ExtractedWatchData */}, catalogIdError: null }
    ```
    Implement all 5 tests in Behavior block above verbatim. Type the `ExtractCacheEntry` fixture imports from the canonical `useUrlExtractCache` path (NOT inline).

    Note on test environment: `vitest.config.ts` defaults to jsdom; the `renderHook` test runs fine in jsdom (no node-only fs walks). Do NOT add `@vitest-environment node` (D-09 + vitest_static_node_env memory — behavioral tests only).
  </action>
  <verify>
    <automated>npm run test -- --run src/components/watch/useStructuredExtractCache.test.ts</automated>
  </verify>
  <done>
    All 5 tests pass exit 0. Hook signature is `useStructuredExtractCache(viewerUserId: string)`. ExtractCacheEntry type imported from useUrlExtractCache (no inline re-declaration). D-18 key shape documented in JSDoc and used in tests.
  </done>
  <acceptance_criteria>
    - File: `src/components/watch/useStructuredExtractCache.ts` exists with `'use client'` directive
    - Source: `grep -c "^export function useStructuredExtractCache(viewerUserId: string)" src/components/watch/useStructuredExtractCache.ts` returns `1`
    - Source: `grep -c "^export function __resetStructuredExtractCacheForTests" src/components/watch/useStructuredExtractCache.ts` returns `1`
    - Source: `grep -c "import type.*ExtractCacheEntry.*useUrlExtractCache" src/components/watch/useStructuredExtractCache.ts` returns `1`
    - Source: stale-write guard present — `grep -c "if (moduleUserId !== viewerUserId) return" src/components/watch/useStructuredExtractCache.ts` returns `1`
    - Source: JSDoc mentions the D-18 JSON-tuple key — `grep -c "JSON.stringify" src/components/watch/useStructuredExtractCache.ts` returns ≥ `1` (in JSDoc comment example)
    - Behavior: `npm run test -- --run src/components/watch/useStructuredExtractCache.test.ts` exits 0
    - Counter-assertion: no `@vitest-environment node` pragma in either test file
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Both new test files pass exit 0
- `grep -rn "useCatalogSearchCache\|useStructuredExtractCache" src/ | wc -l` returns ONLY the new files + their tests (no callers yet — Plan 04/05 wire)
- Counter-assertion: no `getUser` / `createSupabaseBrowserClient` calls in either new hook (proxy_router_cache_poisoning memory)
- Counter-assertion: no `@vitest-environment node` pragmas (vitest_static_node_env memory — behavioral tests only)
</verification>

<success_criteria>
- CLNP-07 mechanism in place for the two NEW caches; per-hook reset verified
- D-18 JSON-tuple key shape documented and exercised in test
- Module-level `moduleUserId` discipline established (Plan 03 retrofits the existing two to match)
- `__reset___ForTests` exports usable by the Plan 06 integration test (`AddWatchFlow.test.tsx`)
</success_criteria>

<output>
After completion, create `.planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-02-SUMMARY.md`
</output>
