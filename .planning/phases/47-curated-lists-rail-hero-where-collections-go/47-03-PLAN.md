---
phase: 47-curated-lists-rail-hero-where-collections-go
plan: 03
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - src/components/explore/HeroModule.tsx
  - src/components/explore/WhereCollectionsGo.tsx
  - src/components/explore/PathCard.tsx
  - src/app/explore/paths/page.tsx
  - src/app/explore/page.tsx
  - src/lib/heroTypes.ts
  - src/components/explore/__tests__/HeroModule.test.tsx
  - src/components/explore/__tests__/WhereCollectionsGo.test.tsx
autonomous: true
requirements: [EXPL-08, EXPL-09]

must_haves:
  truths:
    - "The Hero shows one quality-gated featured list (full-bleed image, title, curator) per page load"
    - "A manual pin overrides auto-selection when the pin is set and not expired"
    - "When no pin is active the Hero auto-selects a list via weekly rotation"
    - "The Hero hides itself entirely when no eligible content exists"
    - "Where Collections Go shows 3 rotating published paths with seed + follow-on watches, rationale, and a path-type chip"
    - "Where Collections Go renders a numbered vertical stack at 360px mobile and a horizontal sequence on desktop"
    - "/explore/paths shows every published path grouped into sections by path-type label"
    - "D-05: /explore/paths renders every published path grouped into sections by path-type label, reusing the WhereCollectionsGo path renderer (Task 3)"
    - "D-07: when no pin is active the Hero auto-selects via weekly rotation through the quality-gated pool using a deterministic week index (Task 1)"
    - "D-08: the Hero quality gate makes a list eligible iff published AND >= 3 items AND non-empty cover image AND non-empty intro copy (Task 1)"
    - "D-09: a non-expired manual pin (cms_settings.pinned_list_id) overrides auto-selection, and the Hero read is wrapped in a 'use cache' scope tagged explore:hero (Task 1)"
    - "D-10: the Hero hides entirely (return null) when the pool is empty and no valid pin exists; the HeroFeature shape is a discriminated union on format accepting featured_collector (Task 1)"
    - "D-11: on mobile each path renders as a numbered vertical stack with number badges and a vertical connector line (Task 2)"
    - "D-12: on desktop each path renders as a horizontal sequence with connectors and rationale under each watch (Task 2)"
    - "D-13: WhereCollectionsGo shows 3 paths chosen by weekly rotation via the same week-index mechanism as the Hero (Task 2)"
    - "D-14: each path shows its path-type label as a chip and tapping any watch opens /catalog/[catalogId] (Task 2)"
  artifacts:
    - path: "src/components/explore/HeroModule.tsx"
      provides: "quality-gated featured-list hero with pin override + weekly rotation"
      min_lines: 40
    - path: "src/components/explore/WhereCollectionsGo.tsx"
      provides: "rotating 3-path collection-paths module"
    - path: "src/components/explore/PathCard.tsx"
      provides: "single path renderer — mobile vertical stack + desktop horizontal sequence"
    - path: "src/app/explore/paths/page.tsx"
      provides: "/explore/paths see-all route grouped by path-type"
    - path: "src/lib/heroTypes.ts"
      provides: "HeroFeature discriminated union (forward-compat for featured_collector)"
      exports: ["HeroFeature"]
  key_links:
    - from: "src/components/explore/HeroModule.tsx"
      to: "explore:hero cache tag"
      via: "cacheTag('explore:hero') in 'use cache' scope"
      pattern: "explore:hero"
    - from: "src/components/explore/HeroModule.tsx"
      to: "getCmsSettings + getPublishedLists"
      via: "pin read + quality-gated pool"
      pattern: "getCmsSettings"
    - from: "src/components/explore/WhereCollectionsGo.tsx"
      to: "getWeekIndex"
      via: "weekly rotation slice"
      pattern: "getWeekIndex"
---

<objective>
Wire the Hero (EXPL-08) and Where Collections Go (EXPL-09) modules, ship the
`/explore/paths` see-all route, and give the Hero its full-width grid slot in
the explore shell. Replaces the `HeroModule` and `WhereCollectionsGo`
`return null` stubs.

Purpose: Users see an editorial featured-list hero and discover curated
collection paths; manual pin changes propagate immediately via the
`explore:hero` tag wired in Phase 45.
Output: Three new components (`HeroModule`, `WhereCollectionsGo`, `PathCard`),
the `HeroFeature` type, one new route, an explore-shell tweak, and live tests
converted from the Plan 01 scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-CONTEXT.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-UI-SPEC.md

<interfaces>
<!-- Contracts finished by Plan 01 — consume directly, no exploration. -->

From src/lib/weekIndex.ts:
  getWeekIndex(now: Date): number   — epoch-days ÷ 7; shared by Hero + WhereCollectionsGo

From src/lib/pathTypes.ts:
  PATH_TYPES   — ['Going Deeper','Branching Out','Trading Up','Filling a Gap'] as const
  PathType     — (typeof PATH_TYPES)[number]

From src/data/curatedLists.ts:
  getPublishedLists(limit = 12)  → curated_lists rows incl. publishedAt (Date|null)
  getListItemCount(listId)       → Promise<number>

From src/data/collectionPaths.ts:
  getPublishedPaths(limit = 50)  → collection_paths rows { id, seedCatalogId,
                                    status, pathType, rationale, source, sortOrder, ... }
  getPathWithNodes(pathId)       → { ...path, nodes: [...], seedWatch }
                                    nodes + seedWatch now include imageUrl
  getPathNodes(pathId)           → nodes joined w/ watches_catalog incl. imageUrl

From src/data/cmsSettings.ts:
  getCmsSettings()  → { id, pinnedListId: string|null, pinExpiresAt: Date|null,
                        heroFormat: 'featured_list'|'featured_collector', updatedAt }

revalidateTag('explore:hero', 'max') is ALREADY wired in all four Phase 45
write paths (setPinnedHero, clearPinnedHero, publishCuratedList,
unpublishCuratedList) — this plan only needs cacheTag('explore:hero').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HeroModule + HeroFeature type + explore-shell slot</name>
  <files>src/components/explore/HeroModule.tsx, src/lib/heroTypes.ts, src/app/explore/page.tsx, src/components/explore/__tests__/HeroModule.test.tsx</files>
  <read_first>
    - src/components/explore/HeroModule.tsx (current return-null stub)
    - src/components/explore/CollectorArchetypes.tsx (the 'use cache' + cacheTag + cacheLife module pattern)
    - src/app/explore/page.tsx (the 5-module shell; Hero needs md:col-span-2)
    - src/data/cmsSettings.ts (getCmsSettings return shape — pinnedListId, pinExpiresAt)
    - src/data/curatedLists.ts (getPublishedLists, getListItemCount)
    - src/lib/weekIndex.ts (getWeekIndex — created in Plan 01)
    - src/components/explore/__tests__/HeroModule.test.tsx (the Plan 01 scaffold to convert)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-UI-SPEC.md § "2. HeroModule (EXPL-08)" and § "CSS Chain Assertions" (Hero full-bleed image chain)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md § "src/components/explore/HeroModule.tsx"
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md § "Pattern 2" and § "Hero Quality Gate" / "HeroFeature Discriminated Union"
  </read_first>
  <behavior>
    - HeroModule returns null when the quality-gated eligible pool is empty
    - HeroModule renders the manually pinned list when pinnedListId is set, the pin is not expired, and the pinned list is eligible
    - HeroModule falls back to weekly rotation (getWeekIndex) when no valid pin exists
    - HeroModule falls back to weekly rotation when the pinned list is no longer eligible/published
  </behavior>
  <action>
    Create `src/lib/heroTypes.ts` exporting `HeroFeature` as a discriminated
    union on a `format` field: `{ format: 'featured_list'; list: <curated list
    shape> } | { format: 'featured_collector' }` (D-10 — only `featured_list`
    is wired this phase, but the union shape must accept `featured_collector`
    for SEED-008 forward-compat; do NOT collapse it to a single variant).

    Replace the `src/components/explore/HeroModule.tsx` stub. Async Server
    Component: `'use cache'` first, then `cacheTag('explore:hero')` — the tag
    is `explore:hero` ONLY, NOT `cacheTag('explore', 'explore:hero')` (D-09;
    the Hero revalidates on editorial pin/publish changes, not generic explore
    invalidation). Then `cacheLife('hours')`.

    Selection logic: `Promise.all` of `getCmsSettings()` and
    `getPublishedLists(50)`. Build per-list item counts via
    `getListItemCount`. Apply the D-08 quality gate — a list is eligible iff
    `itemCount >= 3` AND `coverUrl` is non-empty AND `introMarkdown` is
    non-empty. If the eligible pool is empty, `return null` (EXPL-02). Pin
    override (D-09): if `settings.pinnedListId` is set AND
    (`pinExpiresAt` is null OR `pinExpiresAt > new Date()`), pick the eligible
    list whose id matches `pinnedListId`. If no valid pin OR the pinned list
    is not in the eligible pool, fall back to weekly rotation: sort the
    eligible pool by `publishedAt` ascending then `id` (stable order), compute
    `getWeekIndex(new Date())`, select `sorted[week % sorted.length]` (D-07).
    Do NOT call `getCurrentUser()` in this file.

    Build a `HeroFeature` value `{ format: 'featured_list', list: featured }`
    and render it. The render is a `<Link href={`/explore/lists/${list.id}`}>`
    wrapping the full-bleed container. CSS chain (assert explicitly per
    `feedback_ui_spec_css_chain_blind_spot` memory): container
    `relative w-full aspect-video overflow-hidden rounded-xl bg-muted
    min-h-[200px]`; image `<img className="absolute inset-0 w-full h-full
    object-cover">`; a gradient overlay `absolute inset-0 bg-gradient-to-t
    from-black/60 via-black/20 to-transparent`; a bottom-anchored text block
    `absolute bottom-0 left-0 right-0 p-4 md:p-6` with curator name
    (`text-xs font-semibold text-white/70 uppercase tracking-wide`), the list
    title `<h2 className="text-2xl font-semibold text-white leading-tight">`,
    and a `{watchCount} watches` line (`text-sm text-white/80`).

    In `src/app/explore/page.tsx`, give the Hero its full-width slot: wrap the
    `<HeroModule />` render so it occupies `md:col-span-2` of the existing
    `md:grid-cols-2` shell (UI-SPEC Discretion: "the explore page shell must
    give it md:col-span-2"). Do not change any other module's placement and do
    not move `getCurrentUser()`.

    Convert the `HeroModule.test.tsx` scaffold: turn the `it.todo`/`it.skip`
    cases into live `it(...)` tests for the four behaviors above (mock
    `getCmsSettings`, `getPublishedLists`, `getListItemCount`).
  </action>
  <verify>
    <automated>npm test -- --run src/components/explore/__tests__/HeroModule.test.tsx && grep -c "cacheTag('explore:hero')" src/components/explore/HeroModule.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/heroTypes.ts` exports `HeroFeature` as a union with both `format: 'featured_list'` and `format: 'featured_collector'` variants
    - `HeroModule.tsx` first function-body statement is `'use cache'`, followed by `cacheTag('explore:hero')` (exact — NOT `cacheTag('explore', ...)`) and `cacheLife('hours')`
    - `HeroModule.tsx` applies a quality gate requiring `>= 3` items, non-empty `coverUrl`, and non-empty `introMarkdown`, and has a `return null` on empty eligible pool
    - `HeroModule.tsx` contains pin-override logic checking `pinnedListId` and `pinExpiresAt`, plus a `getWeekIndex` fallback
    - `HeroModule.tsx` does NOT contain `getCurrentUser`
    - The Hero full-bleed container has `aspect-video`, `overflow-hidden`, `min-h-[200px]`, and the image has `absolute inset-0 w-full h-full object-cover`
    - `src/app/explore/page.tsx` gives the `HeroModule` slot `md:col-span-2`
    - `npm test -- --run src/components/explore/__tests__/HeroModule.test.tsx` passes with at least 4 live `it(...)` cases (no `it.todo`/`it.skip` remaining)
  </acceptance_criteria>
  <done>HeroModule renders a quality-gated featured list with pin override and weekly-rotation fallback, hides when empty, uses the explore:hero tag and the verified full-bleed CSS chain; HeroFeature union defined; explore shell gives Hero a full-width slot; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WhereCollectionsGo + PathCard components</name>
  <files>src/components/explore/WhereCollectionsGo.tsx, src/components/explore/PathCard.tsx, src/components/explore/__tests__/WhereCollectionsGo.test.tsx</files>
  <read_first>
    - src/components/explore/WhereCollectionsGo.tsx (current return-null stub)
    - src/components/explore/CollectorArchetypes.tsx (the 'use cache' + section/h2 + "see all" link pattern)
    - src/components/explore/DiscoveryWatchCard.tsx (the watch-image CSS chain reused in PathCard desktop nodes)
    - src/data/collectionPaths.ts (getPublishedPaths, getPathWithNodes return shapes — nodes/seedWatch include imageUrl from Plan 01)
    - src/lib/weekIndex.ts (getWeekIndex)
    - src/components/ui/badge.tsx (Badge component for the path-type chip)
    - src/components/explore/__tests__/WhereCollectionsGo.test.tsx (the Plan 01 scaffold to convert)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-UI-SPEC.md § "3. WhereCollectionsGo (EXPL-09)" and § "CSS Chain Assertions" (Connector line, Path node watch image)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md § "src/components/explore/WhereCollectionsGo.tsx" and § "src/components/explore/PathCard.tsx"
  </read_first>
  <behavior>
    - WhereCollectionsGo returns null when getPublishedPaths() resolves to an empty array
    - WhereCollectionsGo renders at most 3 paths (the weekly-rotation slice)
    - When fewer than 3 paths are published, all of them render (no crash, no duplicates beyond available)
    - PathCard renders a numbered node for the seed watch plus each follow-on, in sortOrder
  </behavior>
  <action>
    Replace the `src/components/explore/WhereCollectionsGo.tsx` stub. Async
    Server Component: `'use cache'` first, then
    `cacheTag('explore', 'explore:paths')`, then `cacheLife('hours')`
    (UI-SPEC Discretion). Call `getPublishedPaths()`; if empty, `return null`
    (EXPL-02). Weekly rotation (D-13): compute `getWeekIndex(new Date())` and
    select 3 paths starting at `week % allPaths.length`, wrapping around to the
    start of the array if the slice runs short (so fewer-than-3 published
    paths all render and a 3-slice near the end wraps cleanly). For each
    selected path, fetch `getPathWithNodes(path.id)` to get the seed watch +
    follow-on nodes. Render a `<section className="space-y-4">` with a
    `flex items-center justify-between` header: `<h2>` "Where Collections Go"
    (`text-xl font-semibold leading-tight text-foreground`) and an "Explore all
    paths" `<Link href="/explore/paths">`. Below, a `flex flex-col gap-8` of
    `<PathCard>` elements. Do NOT call `getCurrentUser()`.

    Create `src/components/explore/PathCard.tsx` — a Server sub-component
    taking the path plus its ordered nodes (seed watch is node 1, follow-ons
    2..N). Render the path-type chip with the existing `Badge` component:
    `<Badge variant="secondary" className="text-xs">{path.pathType}</Badge>`
    above the path sequence (D-14). Two layouts:

    Mobile (D-11, `flex flex-col gap-3 md:hidden`): each node is a
    `flex gap-3 items-start` row. Left column `flex flex-col items-center
    gap-1` holds a numbered badge — `flex items-center justify-center size-6
    rounded-full bg-accent text-accent-foreground text-xs font-semibold
    shrink-0` containing the 1-based index — and, for every node except the
    last, a connector line `<div className="w-px flex-1 bg-border
    min-h-[24px]" />`. Right column `flex-1 pb-2`: the watch brand+model in a
    `<Link href={`/catalog/${node.catalogId}`}>` and the node rationale `<p>`
    below (omitted when rationale is null).

    Desktop (D-12, `hidden md:flex gap-4 items-start`): each node is a
    `w-44 space-y-2` block — a watch image block reusing the DiscoveryWatchCard
    CSS chain (`aspect-square rounded-md bg-muted overflow-hidden` with
    `<img className="w-full h-full object-cover">` when imageUrl present), the
    brand+model in a `<Link href={`/catalog/${node.catalogId}`}>`, and the
    rationale `<p>`. Between consecutive nodes (not after the last) render a
    `<ChevronRight className="size-5 text-muted-foreground mt-10 shrink-0" />`
    from lucide-react, using a `Fragment` keyed wrapper.

    Both layouts must render correctly down to 360px width — the mobile stack
    is the explicitly-recommended 360px layout (STATE.md Phase 47 research
    flag; criterion #5).

    Convert the `WhereCollectionsGo.test.tsx` scaffold: turn `it.todo`/`it.skip`
    into live `it(...)` tests for the four behaviors above (mock
    `getPublishedPaths` and `getPathWithNodes`).
  </action>
  <verify>
    <automated>npm test -- --run src/components/explore/__tests__/WhereCollectionsGo.test.tsx && npx tsc --noEmit 2>&1 | grep -E "WhereCollectionsGo|PathCard" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `WhereCollectionsGo.tsx` first function-body statement is `'use cache'`, followed by `cacheTag('explore', 'explore:paths')` and `cacheLife('hours')`
    - `WhereCollectionsGo.tsx` calls `getPublishedPaths`, has a `return null` empty guard, and uses `getWeekIndex` for the 3-path slice with wrap-around
    - `WhereCollectionsGo.tsx` does NOT contain `getCurrentUser`
    - `PathCard.tsx` has a mobile layout (`md:hidden`) with `bg-accent text-accent-foreground` numbered badges and a `w-px ... bg-border` connector omitted on the last node
    - `PathCard.tsx` has a desktop layout (`hidden md:flex`) with `ChevronRight` connectors between nodes and the DiscoveryWatchCard image CSS chain
    - `PathCard.tsx` renders the path-type chip via `<Badge`
    - Every watch node links to `/catalog/${node.catalogId}`
    - `npm test -- --run src/components/explore/__tests__/WhereCollectionsGo.test.tsx` passes with at least 4 live `it(...)` cases (no `it.todo`/`it.skip` remaining)
  </acceptance_criteria>
  <done>WhereCollectionsGo renders 3 weekly-rotated paths and hides when empty; PathCard renders a numbered vertical stack on mobile and a horizontal chevron sequence on desktop with a path-type chip; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: /explore/paths see-all route</name>
  <files>src/app/explore/paths/page.tsx</files>
  <read_first>
    - src/app/explore/brands/page.tsx (new-route structure: metadata, getCurrentUser-outside-cache, container, back nav)
    - src/components/explore/PathCard.tsx (created in Task 2 — reused per section)
    - src/data/collectionPaths.ts (getPublishedPaths, getPathWithNodes)
    - src/lib/pathTypes.ts (PATH_TYPES — created in Plan 01; import from here, NOT from the 'use server' action file)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-UI-SPEC.md § "/explore/paths (D-05)"
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md § "src/app/explore/paths/page.tsx"
  </read_first>
  <action>
    Create `src/app/explore/paths/page.tsx` — a Server Component. Export
    `metadata = { title: 'Collection Paths — Horlo' }`. In the body,
    `await getCurrentUser()` FIRST (outside any cache scope). Fetch every
    published path via `getPublishedPaths(100)` and each path's
    `getPathWithNodes`. Group the paths by `pathType` into buckets.

    Render a `<main className="container mx-auto px-4 md:px-8 py-8 max-w-6xl">`
    with a `← Explore` back `<Link href="/explore">`, an `<h1>` "Collection
    Paths" (`text-2xl font-semibold text-foreground`). Iterate the imported
    `PATH_TYPES` constant IN ITS DECLARED ORDER (Going Deeper → Branching Out →
    Trading Up → Filling a Gap — D-05); for each type that has at least one
    path, render a `<section className="mb-10">` with an `<h2>` of the type
    label (`text-xl font-semibold text-foreground mb-4`) and a
    `flex flex-col gap-8` of `<PathCard>` elements. Sections with zero paths
    are omitted entirely (EXPL-02 absent-not-empty extended to sections — do
    not render an empty `<section>`). Import `PATH_TYPES` from
    `src/lib/pathTypes.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "explore/paths/page" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/explore/paths/page.tsx` exists; first page-body statement is `await getCurrentUser()`
    - The file imports `PATH_TYPES` from `@/lib/pathTypes` (NOT from `@/app/actions/cms/collectionPaths`)
    - The file contains `getPublishedPaths` and groups paths by `pathType`
    - Sections are iterated in `PATH_TYPES` declared order and a section is only rendered when its bucket has ≥1 path
    - `<h1>` text is "Collection Paths" at `text-2xl`; back link points to `/explore`
    - `npx tsc --noEmit` reports no new errors in the file
  </acceptance_criteria>
  <done>/explore/paths renders every published path grouped into path-type sections in canonical order; empty sections are omitted; reuses PathCard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated visitor → /explore/paths route | proxy.ts auth gate; getCurrentUser() page-body assertion |
| cms_settings pin → Hero render | pinnedListId / pinExpiresAt drive which list is featured |
| Hero `'use cache'` scope → all viewers | globally-shared cache entry tagged explore:hero |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-10 | Information Disclosure | Draft/unpublished list surfacing in the Hero | mitigate | `getPublishedLists` filters `status = 'published'` (Phase 45 DAL); the D-08 quality gate further requires cover + intro + ≥3 items — drafts never reach the eligible pool |
| T-47-11 | Information Disclosure | Stale pin showing an unpublished list after unpublish | mitigate | `cacheTag('explore:hero')` + Phase 45 `revalidateTag('explore:hero','max')` in `unpublishCuratedList`/`clearPinnedHero` invalidate immediately; the pin-fallback logic also drops a no-longer-eligible pinned list |
| T-47-12 | Information Disclosure | Per-viewer data poisoning the shared Hero/paths cache | mitigate | No `getCurrentUser()` in any of the three modules — verified by acceptance criteria; auth asserted once in `page.tsx` outside cache scope (RESEARCH Pitfall 1) |
| T-47-13 | Information Disclosure | Draft path leaking via /explore/paths or the module | mitigate | `getPublishedPaths` filters `status = 'published'` (Phase 45 DAL); RLS `USING (status='published')` is the second layer |
| T-47-14 | Tampering | Stale cached Hero/paths after editorial change | accept | `cacheLife('hours')` bounds staleness; pin/publish events invalidate via tags; weekly rotation advances naturally — acceptable for evergreen editorial content |
| T-47-15 | Denial of Service | N+1 `getListItemCount` / `getPathWithNodes` calls | accept | Pools are tens of rows (single-user, <500 watches, personal-first); all calls run inside one `'use cache'` scope and are amortized by the hours-long cache window (RESEARCH Assumption A3) |
</threat_model>

<verification>
- `npm test -- --run` for both HeroModule and WhereCollectionsGo test files is green with live tests
- `npx tsc --noEmit` reports no new type errors across the plan's files
- `/explore` renders Hero full-width without console errors; Hero hides when no eligible list
- `/explore/paths` renders without runtime errors against the local DB
- `HeroModule` uses `cacheTag('explore:hero')` exactly (not `cacheTag('explore', 'explore:hero')`)
</verification>

<success_criteria>
- HeroModule shows a quality-gated featured list with pin override + weekly rotation, hides when empty
- WhereCollectionsGo shows 3 rotating paths with mobile vertical stack + desktop horizontal sequence
- /explore/paths shows every published path grouped by path-type
- Watch taps in paths navigate to /catalog/[catalogId]
- All HeroModule and WhereCollectionsGo tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-03-SUMMARY.md`
</output>
</content>
</invoke>
