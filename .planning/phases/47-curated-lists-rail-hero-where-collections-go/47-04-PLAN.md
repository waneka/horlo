---
phase: 47-curated-lists-rail-hero-where-collections-go
plan: 04
type: execute
wave: 3
depends_on: [47-01, 47-02, 47-03]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [EXPL-06, EXPL-07, EXPL-08, EXPL-09]

must_haves:
  truths:
    - "The full /explore page renders all five modules without runtime or console errors"
    - "The Hero updates immediately when a list is pinned, unpinned, or unpublished (no TTL wait)"
    - "Where Collections Go is legible at 360px mobile width with numbered progression"
    - "The full test suite passes and the production build succeeds"
    - "All four EXPL-06..09 requirements are verified end-to-end"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "EXPL-06..09 traceability marked Complete"
      contains: "EXPL-06"
  key_links:
    - from: "/explore page"
      to: "Hero + Rail + WhereCollectionsGo modules"
      via: "all three stubs replaced and rendering"
      pattern: "explore"
---

<objective>
Verify Phase 47 end-to-end: the full `/explore` page renders all five modules,
the editorial routes work, the Hero cache-invalidation propagates, the 360px
mobile layout is legible, the test suite and production build pass. Then mark
EXPL-06..09 complete in the requirements traceability.

Purpose: Confirm the editorial half of `/explore` is live and correct before
the phase closes — including the cross-AI-blind-spot visual checks the
6-pillar checker cannot catch (CSS chains, 360px layout, immediate Hero
propagation).
Output: A green full test suite, a clean build, an operator-verified mobile
layout + Hero propagation check, and updated requirements traceability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-CONTEXT.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-01-SUMMARY.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-02-SUMMARY.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + build + integration verification</name>
  <files>(no source files — verification only)</files>
  <read_first>
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-01-SUMMARY.md
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-02-SUMMARY.md
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-03-SUMMARY.md
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md § "Validation Architecture"
  </read_first>
  <action>
    Run the full validation gate for Phase 47:
    1. `npm test -- --run` — the entire Vitest suite must be green, including
       the five Phase 47 test files (weekIndex, curatedLists, CuratedListsRail,
       HeroModule, WhereCollectionsGo) now carrying live tests.
    2. `npx tsc --noEmit` — zero type errors across the project.
    3. `npm run build` — the production build must succeed; confirm the three
       new routes (`/explore/lists`, `/explore/lists/[id]`, `/explore/paths`)
       appear in the build route manifest output.
    4. Integration seam check — confirm `src/app/explore/page.tsx` renders all
       five modules (`HeroModule`, `CollectorArchetypes`, `BrowseModule`,
       `CuratedListsRail`, `WhereCollectionsGo`) and that none of the three
       Phase 47 components is still a `return null`-only stub (each has a real
       implementation body).
    5. Confirm no Phase 47 module calls `getCurrentUser()` inside a
       `'use cache'` scope (grep the three component files — RESEARCH Pitfall 1).

    If any step fails, do not mark the phase complete — report the failure for
    a gap-closure plan.
  </action>
  <verify>
    <automated>npm test -- --run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run` exits 0 with the full suite green
    - `npx tsc --noEmit` reports zero errors
    - `npm run build` succeeds and the route manifest includes `/explore/lists`, `/explore/lists/[id]`, and `/explore/paths`
    - `grep -L "return null$" src/components/explore/{HeroModule,CuratedListsRail,WhereCollectionsGo}.tsx` confirms none is a bare stub (each file has a real render body)
    - No Phase 47 component file contains `getCurrentUser` (`grep -c getCurrentUser` on each of the three returns 0)
  </acceptance_criteria>
  <done>Full test suite green, type-clean, production build succeeds with all three new routes; no stub or cache-poisoning regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual + mobile + Hero-propagation verification</name>
  <what-built>
    Phase 47 wired the three editorial /explore modules (Curated Lists Rail,
    Hero, Where Collections Go) and three new routes (/explore/lists,
    /explore/lists/[id], /explore/paths). The automated suite, type check, and
    production build all pass. This checkpoint covers the three things
    automated checks cannot verify: the Hero full-bleed CSS chain, the 360px
    mobile path layout, and immediate Hero cache propagation.
  </what-built>
  <read_first>
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-UI-SPEC.md § "CSS Chain Assertions"
    - $HOME/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/MEMORY.md (feedback_ui_spec_css_chain_blind_spot — assert CSS chains visually; project_turbopack_next_cache_stale_css — clear .next before concluding a CSS fix failed)
  </read_first>
  <files>(no source files — operator-driven dev-server verification)</files>
  <action>
    Operator-driven verification. Follow the steps in how-to-verify below to
    visually confirm the Hero full-bleed CSS chain, the 360px Where Collections
    Go layout, the editorial routes, and immediate Hero cache propagation —
    items the automated suite cannot assert.
  </action>
  <how-to-verify>
    Start the dev server (`npm run dev`). If a CSS change looks wrong, run
    `rm -rf .next` and restart before concluding it failed (memory note
    `project_turbopack_next_cache_stale_css`).

    1. Visit `/explore` on a desktop viewport. Confirm: the Hero is a
       full-bleed 16:9 image filling the top of the page (NOT a black bar, NOT
       letterboxed, NOT height-collapsed) with the curator name + list title
       legible over a gradient. The Curated Lists Rail scrolls horizontally;
       at least one card shows a "New" badge if a list was published in the
       last 7 days; every card shows a relative timestamp. Where Collections
       Go shows up to 3 paths with path-type chips.
    2. Resize the browser to 360px width (or use device emulation). Confirm
       Where Collections Go renders each path as a numbered vertical stack
       (1 → 2 → 3 → 4) with a visible connector line between nodes and
       readable rationale text — nothing clipped or overlapping.
    3. Visit `/explore/lists` — confirm the grid renders and the
       Newest/Most-watches sort re-orders cards instantly. Open a list detail
       page — confirm the markdown intro renders with proper heading/list
       styling (prose) and editorial rows show watch image + commentary;
       tapping a watch opens its `/catalog/[id]` page.
    4. Visit `/explore/paths` — confirm paths are grouped into path-type
       sections.
    5. Hero propagation: in `/admin/lists`, pin a different eligible list as
       the hero (or unpublish the currently-featured list). Reload `/explore`
       — confirm the Hero updates IMMEDIATELY (no need to wait for a cache
       TTL). This verifies the `explore:hero` tag wiring (roadmap criterion #3).
  </how-to-verify>
  <acceptance_criteria>
    - Hero renders full-bleed with no black bar / letterbox / collapsed height
    - Where Collections Go at 360px is a legible numbered vertical stack with connectors
    - /explore/lists sort works; list detail renders styled markdown + editorial rows; watch taps reach /catalog
    - /explore/paths groups paths by path-type section
    - Pinning/unpublishing a list updates the Hero immediately on reload
  </acceptance_criteria>
  <verify>
    <automated>MISSING — visual/interaction verification only; operator confirms via the dev server per how-to-verify</automated>
  </verify>
  <done>Operator has confirmed the Hero full-bleed render, the 360px mobile path layout, the editorial routes, and immediate Hero pin propagation.</done>
  <resume-signal>Type "approved" if all five checks pass, or describe the issues observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update requirements traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the EXPL section and the Traceability table)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, mark EXPL-06, EXPL-07, EXPL-08, and EXPL-09
    as complete: change their `- [ ]` checkboxes to `- [x]` in the
    "Explore Page (EXPL)" section, and update their rows in the Traceability
    table from `Pending` to `Complete`. Do this ONLY after Task 1 (full
    verification) passes and Task 2 (human-verify) is approved. Make no other
    edits to the file.
  </action>
  <verify>
    <automated>grep -E "EXPL-0[6789]" .planning/REQUIREMENTS.md | grep -c "Complete"</automated>
  </verify>
  <acceptance_criteria>
    - EXPL-06, EXPL-07, EXPL-08, EXPL-09 checkboxes are `- [x]` in the EXPL section
    - All four EXPL-06..09 rows in the Traceability table show `Complete`
    - `grep "EXPL-0[6789]" .planning/REQUIREMENTS.md | grep -c "Complete"` returns 4
    - No other requirement rows were modified
  </acceptance_criteria>
  <done>EXPL-06..09 marked complete in both the requirements list and the traceability table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification plan — no new code) | This plan only runs checks and updates a planning doc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-16 | Tampering | False-positive verification (build passes, runtime broken) | mitigate | The `published_at` push was a [BLOCKING] task in Plan 01; this plan additionally runs a live dev-server visual check (Task 2) — build-only passing cannot mask a missing column or a broken render |
| T-47-17 | Information Disclosure | Hero showing stale unpublished content post-verification | mitigate | Task 2 explicitly verifies pin/unpublish propagation via the `explore:hero` tag — confirms the draft-leak control holds in the live app |
</threat_model>

<verification>
- `npm test -- --run` green; `npx tsc --noEmit` clean; `npm run build` succeeds
- All three new routes present in the build manifest
- Human-verify checkpoint approved (Hero CSS chain, 360px layout, Hero propagation)
- EXPL-06..09 marked Complete in REQUIREMENTS.md
</verification>

<success_criteria>
- Full /explore page renders all five modules without errors
- Editorial routes (/explore/lists, /explore/lists/[id], /explore/paths) work end-to-end
- Hero updates immediately on pin/unpublish
- 360px mobile path layout is legible
- Test suite + build pass; EXPL-06..09 traceability updated
</success_criteria>

<output>
After completion, create `.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-04-SUMMARY.md`
</output>
