---
phase: 47-curated-lists-rail-hero-where-collections-go
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - supabase/migrations/20260519000000_phase47_published_at.sql
  - src/data/curatedLists.ts
  - src/data/collectionPaths.ts
  - src/lib/weekIndex.ts
  - src/lib/pathTypes.ts
  - src/lib/__tests__/weekIndex.test.ts
  - src/data/__tests__/curatedLists.test.ts
  - src/components/explore/__tests__/CuratedListsRail.test.tsx
  - src/components/explore/__tests__/HeroModule.test.tsx
  - src/components/explore/__tests__/WhereCollectionsGo.test.tsx
autonomous: true
requirements: [EXPL-06, EXPL-07, EXPL-08, EXPL-09]

must_haves:
  truths:
    - "curated_lists has a nullable published_at column in both local and prod databases"
    - "Existing published lists have published_at backfilled to created_at"
    - "Re-publishing a list after an unpublish does not reset published_at"
    - "getListItems and getPathNodes return imageUrl for each catalog watch"
    - "getWeekIndex returns a stable integer that advances every 7 days"
    - "D-02: a nullable published_at timestamp column is added to curated_lists via a Drizzle schema edit plus a new supabase migration (Task 1)"
    - "D-03: setListStatus stamps published_at on the first draft→published transition only, and the migration backfills published_at = created_at for already-published lists (Tasks 1 and 2)"
  artifacts:
    - path: "supabase/migrations/20260519000000_phase47_published_at.sql"
      provides: "published_at column add + backfill migration"
      contains: "ADD COLUMN IF NOT EXISTS published_at"
    - path: "src/lib/weekIndex.ts"
      provides: "shared deterministic week-index utility"
      exports: ["getWeekIndex"]
    - path: "src/lib/pathTypes.ts"
      provides: "shared PATH_TYPES vocab constant"
      exports: ["PATH_TYPES", "PathType"]
    - path: "src/db/schema.ts"
      provides: "publishedAt column on curatedLists"
      contains: "publishedAt"
  key_links:
    - from: "src/data/curatedLists.ts setListStatus"
      to: "curated_lists.published_at"
      via: "COALESCE(published_at, NOW()) on publish"
      pattern: "COALESCE.*publishedAt"
    - from: "src/data/curatedLists.ts getListItems"
      to: "watches_catalog.image_url"
      via: "select imageUrl from innerJoin"
      pattern: "imageUrl.*watchesCatalog"
---

<objective>
Lay the schema, DAL, and shared-utility foundation that all Phase 47 render
plans depend on. Adds the `published_at` column to `curated_lists` (D-02),
extends `setListStatus` to stamp it on first publish (D-03), extends two DAL
joins to return `imageUrl` (RESEARCH Pitfall 2), creates the shared
`getWeekIndex` and `PATH_TYPES` utilities, and scaffolds the Wave 0 test files
the render plans will fill in.

Purpose: Render plans (02, 03) must build against finished contracts — column,
DAL signatures, utilities — with zero codebase exploration.
Output: One schema column + migration (pushed local), two extended DAL
functions, two new `src/lib/` utilities, five test files (utility tests
passing; component tests as scaffolds).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-CONTEXT.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md

<interfaces>
<!-- Current DAL signatures the render plans consume — extracted from source. -->

From src/data/curatedLists.ts (existing):
  getPublishedLists(limit = 12)        → curated_lists rows (all columns)
  getListWithItems(id)                 → { ...list, items: [...] }
  getListItemCount(listId)             → Promise<number>
  getListItems(listId)                 → items joined with watches_catalog
  setListStatus(id, status)            → void  [EXTENDED by Task 2]

From src/data/collectionPaths.ts (existing):
  getPublishedPaths(limit = 50)        → collection_paths rows
  getPathWithNodes(pathId)             → { ...path, nodes: [...], seedWatch }
  getPathNodes(pathId)                 → nodes joined with watches_catalog

From src/data/cmsSettings.ts (existing):
  getCmsSettings()                     → { id, pinnedListId, pinExpiresAt, heroFormat, updatedAt }

Migration filename floor: must sort after 20260518210000_phase45_cms_covers_bucket.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add published_at column + migration + shared utilities</name>
  <files>src/db/schema.ts, supabase/migrations/20260519000000_phase47_published_at.sql, src/lib/weekIndex.ts, src/lib/pathTypes.ts</files>
  <read_first>
    - src/db/schema.ts (lines 570-590 — the curatedLists table definition and its existing timestamp columns)
    - supabase/migrations/20260518200000_phase45_cms_tables.sql (migration SQL style and structure)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md § "Schema Change: published_at"
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md § "src/lib/weekIndex.ts" and § "src/lib/pathTypes.ts"
    - src/app/actions/cms/collectionPaths.ts (line 33 — the existing PATH_TYPES const being extracted)
    - src/lib/archetype-config.ts (shared-constant file style)
  </read_first>
  <action>
    In `src/db/schema.ts`, add a nullable `publishedAt` column to the
    `curatedLists` pgTable definition: `timestamp('published_at', { withTimezone: true })`
    with no `.notNull()` and no default. Place it immediately after `sortOrder`
    and before `createdAt` to match the column order the migration assumes (D-02).

    Create `supabase/migrations/20260519000000_phase47_published_at.sql`. The
    filename MUST sort lexically after `20260518210000_phase45_cms_covers_bucket.sql`
    (memory note `project_drizzle_supabase_db_mismatch` — filename ordering
    gotcha). Migration contents: an `ALTER TABLE public.curated_lists ADD COLUMN
    IF NOT EXISTS published_at timestamptz;` statement, followed by a backfill
    `UPDATE public.curated_lists SET published_at = created_at WHERE status =
    'published' AND published_at IS NULL;` (D-03). No enum operations, no
    SECURITY DEFINER functions — plain DDL only.

    Create `src/lib/weekIndex.ts` exporting `getWeekIndex(now: Date): number`
    that returns `Math.floor(now.getTime() / (7 * 24 * 60 * 60 * 1000))` —
    epoch-days ÷ 7 (Claude's discretion resolved in UI-SPEC; deterministic,
    no ISO-week edge cases). Pure function, no imports. This is the single
    shared rotation source for Hero (D-07) and WhereCollectionsGo (D-13).

    Create `src/lib/pathTypes.ts` exporting `PATH_TYPES` (the four-value
    `as const` tuple: `'Going Deeper'`, `'Branching Out'`, `'Trading Up'`,
    `'Filling a Gap'`) and a `PathType` type alias `(typeof PATH_TYPES)[number]`.
    Extracted from `src/app/actions/cms/collectionPaths.ts:33` so the
    `/explore/paths` Server Component page can import the vocab without
    importing a `'use server'` module.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "schema.ts|weekIndex.ts|pathTypes.ts" || echo "no type errors in new/modified files"</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `publishedAt: timestamp('published_at'` inside the `curatedLists` table block, with no `.notNull()` modifier on that line
    - `supabase/migrations/20260519000000_phase47_published_at.sql` exists and contains both `ADD COLUMN IF NOT EXISTS published_at` and `SET published_at = created_at`
    - `grep -c "ADD COLUMN" supabase/migrations/20260519000000_phase47_published_at.sql` returns at least 1
    - `src/lib/weekIndex.ts` exports `getWeekIndex` and contains `7 * 24 * 60 * 60 * 1000`
    - `src/lib/pathTypes.ts` exports `PATH_TYPES` as a 4-element `as const` tuple and a `PathType` type
    - `npx tsc --noEmit` reports no new errors in the three modified/created TS files
  </acceptance_criteria>
  <done>published_at column declared in Drizzle schema; migration file created with add + backfill; weekIndex and pathTypes utilities created and type-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend setListStatus + imageUrl DAL joins</name>
  <files>src/data/curatedLists.ts, src/data/collectionPaths.ts</files>
  <read_first>
    - src/data/curatedLists.ts (full file — especially lines 102-107 setListStatus and lines 149-166 getListItems)
    - src/data/collectionPaths.ts (full file — especially lines 51-70 getPathWithNodes seed query and lines 83-100 getPathNodes)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md § "src/data/curatedLists.ts" and § "src/data/collectionPaths.ts"
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md § "Pattern 5: setListStatus Extension" and § "Pattern 4" (CRITICAL GAP / SAME GAP FOR PATHS notes)
  </read_first>
  <action>
    Extend `setListStatus(id, status)` in `src/data/curatedLists.ts` (D-03):
    when `status === 'published'`, the update must set `publishedAt` to
    `sql\`COALESCE(${curatedLists.publishedAt}, NOW())\`` so the first
    draft→published transition stamps the timestamp and any subsequent
    re-publish (after an unpublish) leaves the original value intact. When
    `status === 'draft'`, do NOT touch `publishedAt`. Keep the existing
    `updatedAt: new Date()` write. Import `sql` from `drizzle-orm`.

    Extend `getListItems(listId)` in `src/data/curatedLists.ts`: add
    `imageUrl: watchesCatalog.imageUrl` to the `.select({...})` object. The
    `innerJoin` on `watchesCatalog` already exists — this is a one-line
    addition. Required so the list-detail editorial rows (Plan 02, D-06) can
    render watch images.

    Extend `getPathNodes(pathId)` in `src/data/collectionPaths.ts`: add
    `imageUrl: watchesCatalog.imageUrl` to its `.select({...})` object.

    Extend the seed-watch query inside `getPathWithNodes(pathId)` in
    `src/data/collectionPaths.ts`: add `imageUrl: watchesCatalog.imageUrl` to
    its `.select({...})` object so the path seed watch (node 1) renders an
    image (Plan 03, D-11/D-12).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "curatedLists.ts|collectionPaths.ts" || echo "no type errors in DAL files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "imageUrl: watchesCatalog.imageUrl" src/data/curatedLists.ts` returns at least 1
    - `grep -c "imageUrl: watchesCatalog.imageUrl" src/data/collectionPaths.ts` returns at least 2 (getPathNodes + seed query)
    - `setListStatus` in `src/data/curatedLists.ts` contains `COALESCE` and references `curatedLists.publishedAt`
    - `setListStatus` does NOT write `publishedAt` when `status === 'draft'` (verifiable by source inspection — the publishedAt assignment is inside a `status === 'published'` branch)
    - `src/data/curatedLists.ts` imports `sql` from `drizzle-orm`
    - `npx tsc --noEmit` reports no new errors in either DAL file
  </acceptance_criteria>
  <done>setListStatus conditionally stamps published_at via COALESCE; getListItems, getPathNodes, and the getPathWithNodes seed query all select imageUrl.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Push schema to local DB</name>
  <files>(no source files — database state change)</files>
  <read_first>
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md § "Schema Change: published_at" (Push commands subsection)
    - $HOME/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/MEMORY.md (the project_drizzle_supabase_db_mismatch note — drizzle-kit push is LOCAL ONLY)
  </read_first>
  <action>
    Run `npx drizzle-kit push` to apply the `published_at` column to the LOCAL
    database. This is mandatory — without it, the local DB lacks the column and
    every Hero/Rail render that orders by `published_at` will fail at runtime
    even though `tsc` and `next build` pass (types come from `schema.ts`, not
    the live DB). Per memory note `project_drizzle_supabase_db_mismatch`,
    `drizzle-kit push` targets LOCAL ONLY; the prod push
    (`supabase db push --linked`) is an operator step performed at deploy time
    and is NOT run here. If `drizzle-kit push` presents an interactive
    confirmation prompt, accept the additive column change (it is non-destructive
    — a nullable column add). Do NOT accept any prompt that proposes dropping or
    renaming an existing column.
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -tAc "SELECT column_name FROM information_schema.columns WHERE table_name='curated_lists' AND column_name='published_at';" 2>/dev/null | grep -q published_at && echo "published_at column present in local DB" || echo "FAIL: column missing — re-run drizzle-kit push"</automated>
  </verify>
  <acceptance_criteria>
    - The local database `curated_lists` table has a `published_at` column (verified by the information_schema query returning the column name)
    - `npx drizzle-kit push` completed without dropping or renaming any existing column
  </acceptance_criteria>
  <done>Local database curated_lists table has the published_at column; render plans can query it without runtime errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wave 0 test scaffolds + weekIndex/setListStatus tests</name>
  <files>src/lib/__tests__/weekIndex.test.ts, src/data/__tests__/curatedLists.test.ts, src/components/explore/__tests__/CuratedListsRail.test.tsx, src/components/explore/__tests__/HeroModule.test.tsx, src/components/explore/__tests__/WhereCollectionsGo.test.tsx</files>
  <read_first>
    - src/components/explore/__tests__/CollectorArchetypes.test.tsx (existing explore component test — mock + render pattern)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md § "Validation Architecture" (Phase Requirements → Test Map, Wave 0 Gaps)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md § "DAL Test Mock Pattern"
    - src/lib/weekIndex.ts (created in Task 1)
    - src/data/curatedLists.ts (the extended setListStatus from Task 2)
  </read_first>
  <behavior>
    - weekIndex: getWeekIndex returns the same integer for two Dates in the same 7-day window
    - weekIndex: getWeekIndex(dateA) + 1 equals getWeekIndex(dateA plus exactly 7 days)
    - weekIndex: getWeekIndex is monotonically non-decreasing as the Date advances
    - curatedLists: setListStatus with status 'published' issues an update whose set object contains a publishedAt COALESCE expression
    - curatedLists: setListStatus with status 'draft' issues an update whose set object does NOT contain publishedAt
  </behavior>
  <action>
    Create `src/lib/__tests__/weekIndex.test.ts` — full passing unit tests for
    `getWeekIndex` covering the five behaviors above (same-week stability,
    +7-day increment, monotonicity).

    Create `src/data/__tests__/curatedLists.test.ts` — using the `vi.hoisted` +
    `vi.mock('@/db')` mock pattern from `47-PATTERNS.md § DAL Test Mock Pattern`,
    assert that `setListStatus(id, 'published')` produces a `.set()` call whose
    argument includes a `publishedAt` key, and that `setListStatus(id, 'draft')`
    produces a `.set()` call whose argument has NO `publishedAt` key. These
    tests must PASS against the Task 2 implementation.

    Create three component test SCAFFOLDS — `CuratedListsRail.test.tsx`,
    `HeroModule.test.tsx`, `WhereCollectionsGo.test.tsx` — under
    `src/components/explore/__tests__/`. Each file sets up the DAL mocks and
    contains the test cases from the RESEARCH Test Map for that component
    (CuratedListsRail: null-when-empty + renders-cards; HeroModule:
    null-when-pool-empty + pin-override + weekly-rotation-fallback;
    WhereCollectionsGo: null-when-empty + rotation). Since the components are
    still `return null` stubs at this point, mark every component-test body
    with `it.todo(...)` OR a skipped `it.skip(...)` carrying the assertion
    description — Plans 02 and 03 convert these to live `it(...)` tests when
    they implement the components. The scaffolds must NOT fail the suite.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/__tests__/weekIndex.test.ts src/data/__tests__/curatedLists.test.ts src/components/explore/__tests__/CuratedListsRail.test.tsx src/components/explore/__tests__/HeroModule.test.tsx src/components/explore/__tests__/WhereCollectionsGo.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run` for the five named files exits 0 (no failures)
    - `src/lib/__tests__/weekIndex.test.ts` has at least 3 passing `it(...)` cases asserting same-week stability, +7-day increment, and monotonicity
    - `src/data/__tests__/curatedLists.test.ts` has a passing test asserting `setListStatus('published')` sets `publishedAt` and `setListStatus('draft')` does not
    - The three component test files exist with `it.todo`/`it.skip` cases for every Test-Map row for that component — `grep -c "it.todo\|it.skip\|it(" src/components/explore/__tests__/HeroModule.test.tsx` returns at least 3
    - No test in any of the five files is left in a failing state
  </acceptance_criteria>
  <done>weekIndex and setListStatus tests pass; three component test scaffolds exist with todo/skip cases covering every Test-Map row; full named-file run is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → local database | Drizzle DAL writes; migration DDL applied via drizzle-kit push |
| (no client boundary in this plan) | This plan is schema + DAL + utilities only — no rendered routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | `published_at` backfill UPDATE | accept | Backfill scoped with `WHERE status = 'published' AND published_at IS NULL` — idempotent, touches no draft rows; re-running is a no-op |
| T-47-02 | Information Disclosure | `getListItems` / `getPathNodes` imageUrl exposure | accept | `imageUrl` is `watches_catalog` public catalog data — not user-private; catalog rows have no owner/status gating by design (Phase 17) |
| T-47-03 | Denial of Service | `setListStatus` COALESCE write | accept | Owner-only Server Action path (Phase 45 `assertOwner()`); single-row update by primary key, no unbounded scan |
| T-47-04 | Elevation of Privilege | Migration adds no SECURITY DEFINER functions | mitigate | Plain `ALTER TABLE ADD COLUMN` only — no function grants; `project_supabase_secdef_grants` gotcha does not apply (verified in RESEARCH § Schema Change gotcha 3) |
</threat_model>

<verification>
- `npx tsc --noEmit` reports no new type errors across schema, DAL, and lib files
- `npm test -- --run` for the five Wave 0 files is green
- Local DB `curated_lists` has the `published_at` column (information_schema query)
- Migration filename sorts after `20260518210000_phase45_cms_covers_bucket.sql`
</verification>

<success_criteria>
- `published_at` column exists in `schema.ts` and the local database
- Migration file created with add + backfill, correctly named
- `setListStatus` stamps `published_at` via COALESCE on publish only
- `getListItems`, `getPathNodes`, and the seed query select `imageUrl`
- `getWeekIndex` and `PATH_TYPES` shared utilities exist and are type-clean
- weekIndex + setListStatus tests pass; three component scaffolds present
</success_criteria>

<output>
After completion, create `.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-01-SUMMARY.md`
</output>
</content>
</invoke>
