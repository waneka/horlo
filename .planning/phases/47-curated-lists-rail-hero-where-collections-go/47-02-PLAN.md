---
phase: 47-curated-lists-rail-hero-where-collections-go
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - src/components/explore/CuratedListsRail.tsx
  - src/components/explore/RailListCard.tsx
  - src/components/explore/ListSortFilterControls.tsx
  - src/app/explore/lists/page.tsx
  - src/app/explore/lists/[id]/page.tsx
  - src/components/explore/__tests__/CuratedListsRail.test.tsx
autonomous: true
requirements: [EXPL-06, EXPL-07]

must_haves:
  truths:
    - "Curated Lists Rail shows up to 12 published lists with cover, title, curator, watch count, and a freshness indicator"
    - "Lists published within 7 days show a 'New' badge; every card shows a relative timestamp"
    - "The rail hides itself entirely (returns null) when there are no published lists"
    - "View all links to /explore/lists, which shows every published list in a sortable grid"
    - "A list detail page renders the markdown intro copy and per-item editorial commentary"
    - "Tapping a watch on the detail page opens /catalog/[catalogId]"
  artifacts:
    - path: "src/components/explore/CuratedListsRail.tsx"
      provides: "horizontally-scrollable rail of published lists"
      min_lines: 25
    - path: "src/components/explore/RailListCard.tsx"
      provides: "single rail card with cover, freshness, metadata"
    - path: "src/app/explore/lists/page.tsx"
      provides: "/explore/lists see-all route"
    - path: "src/app/explore/lists/[id]/page.tsx"
      provides: "/explore/lists/[id] markdown list-detail route"
    - path: "src/components/explore/ListSortFilterControls.tsx"
      provides: "client-side sort controls for the see-all grid"
  key_links:
    - from: "src/components/explore/CuratedListsRail.tsx"
      to: "getPublishedLists"
      via: "DAL call inside 'use cache' scope"
      pattern: "getPublishedLists"
    - from: "src/app/explore/lists/[id]/page.tsx"
      to: "react-markdown + rehypeSanitize"
      via: "ReactMarkdown with rehypePlugins"
      pattern: "rehypeSanitize"
    - from: "src/app/explore/lists/[id]/page.tsx"
      to: "/catalog/[catalogId]"
      via: "Link href per editorial row"
      pattern: "catalog/"
---

<objective>
Wire the Curated Lists Rail (EXPL-06) and ship the two list routes:
`/explore/lists` (see-all sortable grid) and `/explore/lists/[id]`
(magazine-style markdown detail page, EXPL-07). Replaces the
`CuratedListsRail` `return null` stub with a real `'use cache'` module.

Purpose: Users discover curated lists from the explore rail, browse all
published lists, and read the curator's editorial intro + per-item commentary.
Output: Two new components (`CuratedListsRail`, `RailListCard`), one client
component (`ListSortFilterControls`), two new routes, and live tests
converted from the Plan 01 scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-CONTEXT.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md
@.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-UI-SPEC.md

<interfaces>
<!-- Contracts finished by Plan 01 — consume directly, no exploration. -->

From src/data/curatedLists.ts:
  getPublishedLists(limit = 12)  → curated_lists rows incl. publishedAt (nullable Date)
  getListItemCount(listId)       → Promise<number>
  getListWithItems(id)           → { ...list, items: [...] } where items now include imageUrl
  getListItems(listId)           → items joined w/ watches_catalog incl. imageUrl

curated_lists row shape: { id, title, curatorName, coverUrl, introMarkdown,
  status, sortOrder, publishedAt: Date|null, createdAt, updatedAt }
curated_list_items row shape (from getListItems / getListWithItems):
  { id, listId, catalogId, commentary, sortOrder, createdAt, brand, model,
    reference, imageUrl }

Auth: getCurrentUser() from '@/lib/auth' — page-body only, OUTSIDE 'use cache'.
react-markdown + rehype-sanitize: already in package.json; prose wrapper required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CuratedListsRail + RailListCard components</name>
  <files>src/components/explore/CuratedListsRail.tsx, src/components/explore/RailListCard.tsx, src/components/explore/__tests__/CuratedListsRail.test.tsx</files>
  <read_first>
    - src/components/explore/CuratedListsRail.tsx (current return-null stub)
    - src/components/explore/CollectorArchetypes.tsx (the 'use cache' + cacheTag + cacheLife + section/h2 + null-hide pattern to mirror)
    - src/components/explore/DiscoveryWatchCard.tsx (the card + cover-image CSS chain to mirror for RailListCard)
    - src/components/explore/BrowseModule.tsx (scrollable rail / "View all" link pattern)
    - src/components/explore/__tests__/CuratedListsRail.test.tsx (the Plan 01 scaffold to convert to live tests)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-UI-SPEC.md § "1. CuratedListsRail (EXPL-06)" and § "CSS Chain Assertions" (Rail card cover image chain)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md § "Code Examples" (Freshness Indicator)
  </read_first>
  <behavior>
    - CuratedListsRail returns null when getPublishedLists() resolves to an empty array
    - CuratedListsRail renders one RailListCard per published list, capped at 12
    - RailListCard renders a "New" badge when publishedAt is within 7 days of now
    - RailListCard renders no "New" badge when publishedAt is older than 7 days or null
    - RailListCard always renders a relative timestamp string (e.g. "3 days ago", "today")
  </behavior>
  <action>
    Replace the `src/components/explore/CuratedListsRail.tsx` stub. Make it an
    async Server Component: `'use cache'` as the first statement, then
    `cacheTag('explore', 'explore:lists')`, then `cacheLife('hours')` (UI-SPEC
    Discretion Decisions). Call `getPublishedLists(12)`; if the result is empty,
    `return null` (EXPL-02 — not an empty container). For each list, also fetch
    its `getListItemCount(list.id)` so the card can show the watch count. Render
    a `<section className="space-y-4">` with a `flex items-center justify-between`
    header: `<h2>` "Curated Lists" (`text-xl font-semibold leading-tight
    text-foreground`) and a "View all" `<Link href="/explore/lists">`
    (`text-sm font-semibold text-muted-foreground hover:text-foreground`).
    Below it a `flex gap-4 overflow-x-auto pb-2 scroll-smooth` container of
    `RailListCard` elements. Do NOT call `getCurrentUser()` anywhere in this
    file — the module is viewer-independent (RESEARCH Pitfall 1).

    Create `src/components/explore/RailListCard.tsx` — a Server sub-component
    taking `{ list, watchCount }`. Render a `<Link href={`/explore/lists/${list.id}`}>`
    with `block w-44 md:w-52 shrink-0 space-y-2`. Cover image block: a `div`
    with `aspect-square rounded-md bg-muted overflow-hidden relative`, an
    `<img src={list.coverUrl ?? ''} alt="" className="w-full h-full object-cover" />`
    (this exact CSS chain is verified working — UI-SPEC CSS Chain Assertions).
    When the list is "new" (publishedAt within 7 days of `Date.now()`), render
    an absolutely-positioned "New" badge: `absolute top-2 left-2 rounded-full
    bg-accent text-accent-foreground text-xs font-semibold px-2 py-0.5`.
    Metadata block: title (`text-sm font-semibold text-foreground truncate`),
    curator name and `{watchCount} watches` and the relative timestamp (each
    `text-sm text-muted-foreground`). Compute the relative timestamp with
    `Intl.RelativeTimeFormat('en', { numeric: 'auto' })` — "today" for <1 day,
    days for <7, weeks for <30, months otherwise (RESEARCH Code Examples). A
    null `publishedAt` yields no "New" badge and an empty/omitted timestamp.

    Convert the `CuratedListsRail.test.tsx` scaffold: turn the `it.todo`/`it.skip`
    cases into live `it(...)` tests asserting the five behaviors above (mock
    `getPublishedLists` and `getListItemCount` from `@/data/curatedLists`).
  </action>
  <verify>
    <automated>npm test -- --run src/components/explore/__tests__/CuratedListsRail.test.tsx && npx tsc --noEmit 2>&1 | grep -E "CuratedListsRail|RailListCard" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/explore/CuratedListsRail.tsx` first function-body statement is `'use cache'`, followed by `cacheTag('explore', 'explore:lists')` and `cacheLife('hours')`
    - `CuratedListsRail.tsx` contains `getPublishedLists(12)` and a `return null` guarded by an empty-array check
    - `CuratedListsRail.tsx` does NOT contain `getCurrentUser`
    - `RailListCard.tsx` cover image block contains `aspect-square`, `overflow-hidden`, and an `<img>` with `w-full h-full object-cover`
    - `RailListCard.tsx` "New" badge uses `bg-accent text-accent-foreground` and is rendered conditionally on a 7-day publishedAt window
    - `RailListCard.tsx` uses `Intl.RelativeTimeFormat`
    - `npm test -- --run src/components/explore/__tests__/CuratedListsRail.test.tsx` passes with at least 5 live `it(...)` cases (no `it.todo`/`it.skip` remaining)
  </acceptance_criteria>
  <done>CuratedListsRail renders a cached scrollable rail of up to 12 lists with freshness indicators, hides when empty; RailListCard implements the verified cover-image CSS chain; component tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: /explore/lists see-all route + ListSortFilterControls</name>
  <files>src/app/explore/lists/page.tsx, src/components/explore/ListSortFilterControls.tsx</files>
  <read_first>
    - src/app/explore/brands/page.tsx (the new-route structure: metadata, getCurrentUser-outside-cache, container, back nav)
    - src/components/explore/RailListCard.tsx (created in Task 1 — reused in the grid)
    - src/components/filters/StatusToggle.tsx ('use client' component shell pattern)
    - src/data/curatedLists.ts (getPublishedLists, getListItemCount signatures)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-UI-SPEC.md § "/explore/lists (D-04)"
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md § "src/app/explore/lists/page.tsx" and § "src/components/explore/ListSortFilterControls.tsx"
  </read_first>
  <action>
    Create `src/app/explore/lists/page.tsx` — a Server Component. Export
    `metadata = { title: 'Curated Lists — Horlo' }`. In the page body,
    `await getCurrentUser()` as the FIRST statement (outside any cache scope —
    RESEARCH Pitfall 1). Fetch every published list via `getPublishedLists(100)`
    and each list's `getListItemCount`. Render a `<main className="container
    mx-auto px-4 md:px-8 py-8 max-w-6xl">` with a `← Explore` back
    `<Link href="/explore">`, an `<h1>` "Curated Lists" (`text-2xl font-semibold
    text-foreground`), and the `<ListSortFilterControls lists={...} />` client
    component receiving the list+count array.

    Create `src/components/explore/ListSortFilterControls.tsx` — a `'use client'`
    component. Local `useState<'newest' | 'most-watches'>('newest')` and a
    `useMemo` that sorts the lists array: `'newest'` by `publishedAt` descending
    (nulls last), `'most-watches'` by `itemCount` descending (D-04, Claude's
    discretion: local state, not URL params — RESEARCH Pitfall 7). Render a
    `Sort by:` label + a plain Tailwind-styled `<select>` (`text-sm rounded-md
    border border-border bg-background px-3 py-1.5`) with options "Newest" and
    "Most watches", then a `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4
    gap-4 mt-4` of `RailListCard` elements over the sorted array. Since
    `RailListCard` is a Server Component, ListSortFilterControls receives
    pre-rendered card elements as a prop OR `RailListCard` must be confirmed
    importable into a client component (it has no server-only imports — it is
    a pure presentational `<Link>` card). Pass the list+count data and render
    `RailListCard` inside the client component.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "explore/lists/page|ListSortFilterControls" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/explore/lists/page.tsx` exists; its first page-body statement is `await getCurrentUser()`
    - `page.tsx` contains `getPublishedLists` and renders `<ListSortFilterControls`
    - `page.tsx` back link points to `/explore` and `<h1>` text is "Curated Lists" at `text-2xl`
    - `ListSortFilterControls.tsx` first line is `'use client'`
    - `ListSortFilterControls.tsx` uses `useState` and `useMemo`, has exactly two sort options ("Newest", "Most watches"), and contains no `useRouter`/`searchParams` (local state only)
    - The grid uses `grid-cols-2 md:grid-cols-3 lg:grid-cols-4`
    - `npx tsc --noEmit` reports no new errors in the two files
  </acceptance_criteria>
  <done>/explore/lists renders every published list in a responsive grid with a working client-side Newest/Most-watches sort; auth asserted outside any cache scope.</done>
</task>

<task type="auto">
  <name>Task 3: /explore/lists/[id] markdown detail route</name>
  <files>src/app/explore/lists/[id]/page.tsx</files>
  <read_first>
    - src/app/explore/genres/page.tsx (auth + data fetch + render structure for a new route)
    - src/components/admin/MarkdownEditor.tsx (the react-markdown + rehypeSanitize + prose pattern; CR-02 security note)
    - src/data/curatedLists.ts (getListWithItems return shape — items now include imageUrl from Plan 01)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-UI-SPEC.md § "/explore/lists/[id] (D-06)" and § "CSS Chain Assertions" (Editorial row image chain)
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-PATTERNS.md § "src/app/explore/lists/[id]/page.tsx"
    - .planning/phases/47-curated-lists-rail-hero-where-collections-go/47-RESEARCH.md § "Pattern 6: react-markdown + rehype-sanitize"
  </read_first>
  <action>
    Create `src/app/explore/lists/[id]/page.tsx` — a Server Component. Export
    `metadata` (static title or a `generateMetadata` deriving from the list
    title is acceptable). In the body, `await getCurrentUser()` FIRST (outside
    any cache scope). Read the route param `id`, call
    `getListWithItems(id)`; if it returns null/undefined, call `notFound()`
    from `next/navigation`.

    Render a `<main className="container mx-auto px-4 md:px-8 py-8 max-w-3xl">`
    (note `max-w-3xl` — narrower editorial measure, NOT `max-w-6xl`). Back nav
    `<Link href="/explore/lists">` labelled "← All lists". Header block: curator
    name (`text-sm font-semibold text-muted-foreground uppercase tracking-wide`),
    `<h1>` list title (`text-2xl font-semibold text-foreground leading-tight`),
    a `{watchCount} watches · {relativeTimestamp}` line.

    Intro copy: when `introMarkdown` is non-empty, render it inside
    `<div className="prose prose-sm dark:prose-invert max-w-none mb-10">` with
    `<ReactMarkdown rehypePlugins={[rehypeSanitize]}>`. BOTH the `prose`
    wrapper and `rehypeSanitize` are mandatory — `rehypeSanitize` is the XSS
    control for curator-authored markdown (CR-02, security_requirement);
    omitting `prose` strips heading/list styles via Tailwind Preflight.

    Editorial rows: a `flex flex-col divide-y divide-border` container. Each
    item renders as a `flex flex-col md:flex-row gap-4 py-6` row — image on one
    side (a `<Link href={`/catalog/${item.catalogId}`}>` wrapping a
    `w-full md:w-40 aspect-square rounded-md bg-muted overflow-hidden` block
    with an `<img className="w-full h-full object-cover">` when `imageUrl` is
    present, else a "No image" placeholder), commentary prose on the other
    (brand/model/reference + the `item.commentary` `<p>` when present; omit the
    commentary `<p>` entirely when commentary is null). When the list has zero
    items render the copy "This list has no watches yet." per the UI-SPEC
    Copywriting Contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "explore/lists/\[id\]" || echo "no type errors" && grep -c "rehypeSanitize" "src/app/explore/lists/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/explore/lists/[id]/page.tsx` exists; first page-body statement is `await getCurrentUser()`
    - The file contains `getListWithItems` and a `notFound()` call guarded by a null-list check
    - The intro markdown render uses `<ReactMarkdown` with `rehypePlugins={[rehypeSanitize]}` AND is wrapped in a `prose` div — `grep -c "rehypeSanitize" path` returns at least 1
    - The `<main>` container uses `max-w-3xl` (not `max-w-6xl`)
    - Editorial rows use `flex flex-col md:flex-row` and each watch image links to `/catalog/${item.catalogId}`
    - Editorial row image block contains `aspect-square`, `overflow-hidden`, and `w-full h-full object-cover`
    - `npx tsc --noEmit` reports no new errors in the file
  </acceptance_criteria>
  <done>/explore/lists/[id] renders the markdown intro (sanitized + prose-wrapped) and magazine-style editorial rows; each watch links to its catalog page; missing list 404s.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated visitor → /explore/lists* routes | proxy.ts auth gate; getCurrentUser() page-body assertion |
| Curator-authored markdown → public render | introMarkdown rendered via ReactMarkdown on a public page |
| Route param `[id]` → DAL lookup | arbitrary id supplied in URL path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-05 | Tampering | XSS via `introMarkdown` on /explore/lists/[id] | mitigate | `ReactMarkdown` with `rehypePlugins={[rehypeSanitize]}` (GitHub-schema allow-list) — mandatory on the public render path; CMS does not sanitize on write |
| T-47-06 | Information Disclosure | Draft list leak via rail or see-all grid | mitigate | `getPublishedLists` filters `WHERE status = 'published'` (Phase 45 DAL); RLS `USING (status = 'published')` is the second layer — drafts never enter the result set |
| T-47-07 | Information Disclosure | IDOR — non-owner reads a draft list via `/explore/lists/[id]` | mitigate | `getListWithItems` is a published-only public-read DAL function (Phase 45 two-layer pattern); a draft id resolves to null → `notFound()`. Confirm `getListWithItems` filters `status = 'published'`; if it does not, the executor MUST add the filter |
| T-47-08 | Information Disclosure | Unauthenticated access to /explore/lists* | mitigate | `proxy.ts` redirects unauthenticated viewers; `getCurrentUser()` is the page-body backstop |
| T-47-09 | Denial of Service | Unbounded list fetch on /explore/lists | accept | `getPublishedLists(100)` is capped; catalog scale is <500 watches / tens of lists (single-user, personal-first) |
</threat_model>

<verification>
- `npm test -- --run src/components/explore/__tests__/CuratedListsRail.test.tsx` is green with live tests
- `npx tsc --noEmit` reports no new type errors across the plan's files
- `/explore/lists` and `/explore/lists/[id]` render without runtime errors against the local DB (which has `published_at` from Plan 01)
- Confirm `getListWithItems` enforces `status = 'published'`; add the filter if absent (T-47-07)
</verification>

<success_criteria>
- CuratedListsRail shows up to 12 published lists with freshness, hides when empty
- /explore/lists shows every published list with working client-side sort
- /explore/lists/[id] renders sanitized markdown intro + editorial commentary rows
- Watch taps on the detail page navigate to /catalog/[catalogId]
- All CuratedListsRail component tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-curated-lists-rail-hero-where-collections-go/47-02-SUMMARY.md`
</output>
