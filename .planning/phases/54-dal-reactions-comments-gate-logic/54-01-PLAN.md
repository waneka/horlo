---
phase: 54-dal-reactions-comments-gate-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/phase54-dal-gate.test.ts
  - src/data/__tests__/reactions-comments-gate.test.ts
autonomous: true
requirements: [GATE-01, GATE-04, GATE-05, SEC-02]
must_haves:
  truths:
    - "Both Phase 54 test files exist and are picked up by the vitest runner"
    - "The integration suite is localhost-gated and skips cleanly when DATABASE_URL is not local"
    - "The unit suite imports the (not-yet-implemented) DAL exports so it fails RED until Wave 1/2 land"
  artifacts:
    - path: "tests/integration/phase54-dal-gate.test.ts"
      provides: "SEC-02 + GATE-04 + GATE-05 direct-DAL integration suite (localhost-gated)"
      contains: "CommentGateError"
    - path: "src/data/__tests__/reactions-comments-gate.test.ts"
      provides: "mocked-db unit suite for isMutualFollow + canViewerCommentOnTarget gate branches"
      contains: "vi.mock('@/db'"
  key_links:
    - from: "tests/integration/phase54-dal-gate.test.ts"
      to: "src/data/comments.ts"
      via: "import { createComment, CommentGateError, getCommentsForTarget }"
      pattern: "from '@/data/comments'"
    - from: "tests/integration/phase54-dal-gate.test.ts"
      to: "src/data/follows.ts"
      via: "import { isMutualFollow }"
      pattern: "from '@/data/follows'"
---

<objective>
Wave 0 — create the two automated test scaffolds that encode the Phase 54 verification contract (SEC-02, GATE-01, GATE-04, GATE-05). These tests are written FIRST and are expected to fail RED until the DAL implementations land in Wave 1 (`follows.ts`/`reactions.ts`) and Wave 2 (`comments.ts`).

Purpose: Nyquist compliance — every Phase 54 requirement gets an automated verify, and the SEC-02 direct-DAL-bypass threat (T-SEC-02) gets its load-bearing test before any gate code exists.
Output: `tests/integration/phase54-dal-gate.test.ts` (localhost-gated integration suite) and `src/data/__tests__/reactions-comments-gate.test.ts` (mocked-db unit suite).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-dal-reactions-comments-gate-logic/54-CONTEXT.md
@.planning/phases/54-dal-reactions-comments-gate-logic/54-RESEARCH.md
@.planning/phases/54-dal-reactions-comments-gate-logic/54-PATTERNS.md
@.planning/phases/54-dal-reactions-comments-gate-logic/54-VALIDATION.md

<interfaces>
<!-- Contracts these tests are written AGAINST. These functions do NOT exist yet (Wave 1/2 create them). -->
<!-- The tests import them so the suite fails RED until the implementation lands. -->

From src/data/follows.ts (Wave 1 adds):
```typescript
export async function isMutualFollow(userA: string, userB: string): Promise<boolean>
```

From src/data/reactions.ts (Wave 1 creates):
```typescript
export interface LikesResult { count: number; viewerHasLiked: boolean }
export async function getLikesForTarget(viewerId: string, target: { type: 'watch' | 'wear'; id: string }): Promise<LikesResult>
export async function createLike(userId: string, target: { type: 'watch' | 'wear'; id: string }): Promise<void>
export async function deleteLike(userId: string, target: { type: 'watch' | 'wear'; id: string }): Promise<void>
```

From src/data/comments.ts (Wave 2 creates):
```typescript
export class CommentGateError extends Error {}
export async function canViewerCommentOnTarget(viewerId: string, target: { type: 'watch' | 'wear'; id: string }): Promise<boolean>
export async function getCommentsForTarget(viewerId: string, target: { type: 'watch' | 'wear'; id: string }): Promise<Comment[]>
export async function createComment(input: { authorId: string; target: { type: 'watch' | 'wear'; id: string }; body: string }): Promise<Comment>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the localhost-gated SEC-02 / GATE-04 / GATE-05 integration suite</name>
  <files>tests/integration/phase54-dal-gate.test.ts</files>
  <read_first>
    - tests/integration/phase34-rls.test.ts (strict `dbUrlIsLocal` localhost guard, lines 19-25)
    - tests/integration/phase15-wear-detail-gating.test.ts (fixed-UUID seeding + beforeAll/afterAll cleanup in FK dependency order; negative-cells-first ordering)
    - tests/fixtures/users.ts (seedTwoUsers shape — reference only; this suite seeds via db.insert directly)
    - src/db/schema.ts:295-380 (wearEvents, watchLikes, wearLikes, comments column identifiers; watches.userId/status)
    - .planning/phases/54-dal-reactions-comments-gate-logic/54-RESEARCH.md (§Code Examples — SEC-02 integration test structure)
  </read_first>
  <action>
    Create the integration suite. Use the STRICT localhost guard from phase34-rls.test.ts: a `dbUrlIsLocal` check that `process.env.DATABASE_URL` includes `localhost` or `127.0.0.1`, then `const maybe = process.env.DATABASE_URL && process.env.NEXT_PUBLIC_SUPABASE_URL && process.env.SUPABASE_SERVICE_ROLE_KEY && dbUrlIsLocal ? describe : describe.skip` (Pitfall 5 — never run against prod). Import from `@/data/comments` (`createComment`, `CommentGateError`, `getCommentsForTarget`) and `@/data/follows` (`isMutualFollow`) — these do not exist yet, so the suite fails to resolve/RED until Wave 1/2; that is intended.

    Define four fixed UUIDs in the `00000000-0000-0000-0000-0000000054XX` namespace: `owner` (..54a0), `mutual` (..54a1, follows owner AND owner follows back), `oneWay` (..54a2, follows owner but owner does NOT follow back), `stranger` (..54a3, no follow relationship). In `beforeAll`: run cleanup first, then `db.insert(users)` for all four, update `profiles`/`profileSettings`, insert `follows` rows (mutual↔owner bidirectional; oneWay→owner one-directional only), insert one `wishlist`-status watch owned by `owner` (capture its id), and one `owned`-status watch owned by `owner` (control for the non-gated path). In `afterAll`: cleanup. The `cleanup()` helper deletes in FK dependency order: comments → watch_likes → wear_likes → watches → follows (both followerId and followingId) → profileSettings → profiles → users, all scoped via `inArray(..., allIds)`.

    Write these test cases (NEGATIVE CELLS FIRST per the phase15 convention):
    - SEC-02: `createComment({ authorId: oneWay, target: { type: 'watch', id: wishlistWatchId }, body: 'x' })` rejects with `CommentGateError` (instanceof).
    - SEC-02: `createComment` as `stranger` on the wishlist watch rejects with `CommentGateError`.
    - GATE-01 read-gate: `getCommentsForTarget(oneWay, { type: 'watch', id: wishlistWatchId })` resolves to `[]` (no content/count leaked).
    - GATE-04: `createComment` as `owner` on own wishlist watch resolves (no throw); `getCommentsForTarget(owner, …)` returns that comment.
    - GATE-01 open path: `createComment` as `stranger` on the `owned` watch resolves (non-wishlist open to all authenticated).
    - mutual path: `createComment` as `mutual` on the wishlist watch resolves.
    - GATE-05: `isMutualFollow(oneWay, owner)` resolves to `false` (only A→B exists); `isMutualFollow(mutual, owner)` resolves to `true`.

    Reference 54-RESEARCH.md §Code Examples for the exact structure. Do NOT implement the DAL functions here — only the test.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "Cannot find module '@/data/comments'\|Cannot find module '@/data/reactions'\|isMutualFollow" ; npm run test -- tests/integration/phase54-dal-gate.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/phase54-dal-gate.test.ts exists and contains 'CommentGateError'
    - tests/integration/phase54-dal-gate.test.ts contains 'dbUrlIsLocal' and 'describe.skip'
    - tests/integration/phase54-dal-gate.test.ts contains 'isMutualFollow' and imports from '@/data/comments' and '@/data/follows'
    - The suite SKIPS cleanly (describe.skip) when DATABASE_URL is unset/non-local — `npm run test -- tests/integration/phase54-dal-gate.test.ts` does not error on guard logic
    - When run against local Docker before Wave 1/2 land, the suite is RED (imports unresolved) — this is the expected initial state
  </acceptance_criteria>
  <done>Integration suite committed; localhost-gated; encodes SEC-02 (×2), GATE-01 read-gate + open-path, GATE-04 owner-always, mutual path, and GATE-05 one-way-false; RED until implementation lands.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the mocked-db unit suite for isMutualFollow + canViewerCommentOnTarget</name>
  <files>src/data/__tests__/reactions-comments-gate.test.ts</files>
  <read_first>
    - src/data/__tests__/watches-leftjoin.test.ts (the `vi.mock('@/db')` chained-mock scaffold + beforeEach reset, lines 6-31)
    - vitest.config.ts (include patterns cover `src/**/*.test.ts`; `server-only` aliased to a no-op shim — Pitfall 7, no action needed)
    - .planning/phases/54-dal-reactions-comments-gate-logic/54-PATTERNS.md (§reactions-comments-gate.test.ts — what to cover + mock chain shape)
    - .planning/phases/54-dal-reactions-comments-gate-logic/54-RESEARCH.md (§Code Examples — canViewerCommentOnTarget + isMutualFollow shapes the mocks must satisfy)
  </read_first>
  <action>
    Create the mocked-db unit suite. Mirror the `vi.mock('@/db', () => ({ db: { select: vi.fn(() => ({ from, where, limit/... })) } }))` scaffold from watches-leftjoin.test.ts; import the DAL functions AFTER the mock; reset `mockRows`/`vi.clearAllMocks()` in `beforeEach`. The mock chain must match the call shapes the implementations use: `isMutualFollow` ends in `.from().where()` returning the two-FILTER-aggregate row; `canViewerCommentOnTarget` uses `.select().from().where().limit(1)` to fetch the watch row, then calls `isMutualFollow`; `getLikesForTarget` ends in `.from().where()` returning the count/bool_or row.

    Import `isMutualFollow` from `@/data/follows`, and `canViewerCommentOnTarget` from `@/data/comments` (do not exist yet → RED until Wave 1/2). Optionally import `getLikesForTarget` from `@/data/reactions` for the null-coalesce branch.

    Cover these cases:
    - `isMutualFollow`: returns `false` when the mocked aggregate row has `aToB >= 1` but `bToA = 0` (one-way follow); returns `true` when both directions present (GATE-05).
    - `canViewerCommentOnTarget`: wear-target short-circuit returns `true` WITHOUT touching the db mock (assert the select mock was not called); owner bypass (viewerId === mocked watch.userId) returns `true`; non-wishlist status (e.g. `'owned'`) returns `true`; wishlist + mutual returns `true`; wishlist + non-mutual returns `false`; watch-not-found (empty mockRows) returns `false` (fail closed).
    - `getLikesForTarget`: `viewerHasLiked` defaults to `false` and `count` to `0` when the aggregate row is empty/null (Pitfall 1 coalesce).

    For the `canViewerCommentOnTarget` branches that call `isMutualFollow`, either mock the follows query rows or spy/mock the `isMutualFollow` export — whichever keeps the test deterministic. Reference 54-PATTERNS.md for the exact mock-chain shape.
  </action>
  <verify>
    <automated>npm run test -- src/data/__tests__/reactions-comments-gate.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/data/__tests__/reactions-comments-gate.test.ts exists and contains "vi.mock('@/db'"
    - File imports isMutualFollow from '@/data/follows' and canViewerCommentOnTarget from '@/data/comments'
    - Covers: isMutualFollow one-way→false + both→true; wear short-circuit (no db call); owner bypass; non-wishlist open; wishlist+mutual; wishlist+non-mutual→false; not-found→false
    - Suite is RED before Wave 1/2 land (functions unresolved) — expected initial state; no `--watch` flag used
  </acceptance_criteria>
  <done>Unit suite committed; covers every gate branch of `canViewerCommentOnTarget` and both `isMutualFollow` directions on mocked db; RED until implementation lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → local DB | The integration suite seeds and mutates a local Supabase Docker DB via Drizzle `db` (RLS-bypass). Accidental prod execution would write test rows into prod. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | phase54-dal-gate.test.ts seeding | mitigate | Strict `dbUrlIsLocal` guard (must include `localhost`/`127.0.0.1`) gates the suite to local Docker only; `describe.skip` otherwise (Pitfall 5). |
| T-SEC-02 | Tampering | createComment gate (encoded as failing test) | mitigate | This plan writes the SEC-02 RED test (non-mutual-follower direct DAL call expects `CommentGateError`); the gate itself is implemented in Plan 03. |
| T-54-02 | Information disclosure | getCommentsForTarget read-gate test | mitigate | GATE-01 read-gate test asserts `[]` (no content/count) for a non-mutual viewer on a wishlist watch. |
</threat_model>

<verification>
- `npm run test -- src/data/__tests__/reactions-comments-gate.test.ts` runs (RED until Wave 1/2).
- `npm run test -- tests/integration/phase54-dal-gate.test.ts` skips cleanly off-localhost; RED on local Docker until implementation lands.
- No `--watch` flag anywhere.
</verification>

<success_criteria>
Both test files exist, are discovered by vitest, encode all four requirement contracts (GATE-01, GATE-04, GATE-05, SEC-02), and fail RED in the absence of the Wave 1/2 implementations.
</success_criteria>

<output>
After completion, create `.planning/phases/54-dal-reactions-comments-gate-logic/54-01-SUMMARY.md`
</output>
