---
phase: 51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta
plan: 08
type: execute
wave: 6
depends_on: [51-06]
files_modified:
  - .planning/debug/profile-page-404-top-nav.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The debug file frontmatter status is updated from `reverted_pending_f3` to `resolved`"
    - "A new frontmatter field `recurrence_3_fix_commit` records the merge SHA from plan 51-06"
    - "A new frontmatter field `prod_verified` records the ISO date prod verification passed"
    - "A new frontmatter field `phase` records `51`"
    - "A new section `## RECURRENCE 3 — 2026-05-20 → RESOLVED 2026-05-NN` is prepended with a short resolution summary citing F3-Composite and (Branch B) the re-gate"
    - "The existing investigation history (TL;DR, recurrence sections, etc.) is PRESERVED verbatim — only the frontmatter and one new top-section heading are added"
  artifacts:
    - path: ".planning/debug/profile-page-404-top-nav.md"
      provides: "Resolved debug session record"
      contains: "status: resolved"
  key_links:
    - from: ".planning/debug/profile-page-404-top-nav.md"
      to: "Phase 51 directory + merge commit"
      via: "frontmatter recurrence_3_fix_commit field"
      pattern: "recurrence_3_fix_commit"
---

<objective>
Close the loop on the debug session file. Update `.planning/debug/profile-page-404-top-nav.md` to record:
1. `status: resolved` (was `reverted_pending_f3`)
2. The merge SHA from plan 51-06 in `recurrence_3_fix_commit`
3. The ISO date prod verification passed in `prod_verified`
4. A new top section summarizing the resolution

The existing investigation history is preserved verbatim — three recurrence narratives, hypothesis space, evidence dumps — all kept as the project's authoritative reference for any future investigation.

Purpose: Future debug sessions (and the `gsd-state-management` tooling) discover resolved debug files by frontmatter. Without this update, the file remains marked as open and the next operator may waste time re-investigating a closed bug.

Output: Single-file edit (the debug markdown file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-PLAN.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-06-PLAN.md

@.planning/debug/profile-page-404-top-nav.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update debug file frontmatter and prepend resolution summary</name>
  <files>.planning/debug/profile-page-404-top-nav.md</files>
  <action>
Edit `.planning/debug/profile-page-404-top-nav.md`. The file has frontmatter and then narrative content; only the frontmatter and the immediate post-frontmatter section change.

**Step 1 — Update frontmatter.** The current frontmatter looks like:

```yaml
---
slug: profile-page-404-top-nav
status: reverted_pending_f3
trigger: profile page 404 on top-nav click, refresh works
created: 2026-05-13T23:00:00Z
updated: 2026-05-20
reopened_at: 2026-05-20
prior_resolutions:
  - cf250b1 (removed unstable_instant — wrong mechanism, recurrence 1)
  - 5def872 (isProfilePath ungates /u/* in proxy — held until 2026-05-20, recurrence 2)
recurrence_count: 3
recurrence_3_attempts:
  - { id: F2, commit: b963e6a, approach: "...", result: FAILED_PROD_VERIFICATION, reason: "..." }
  - { id: F1, commit: a6f1016, approach: "...", result: WOULD_NOT_HAVE_WORKED, reason: "..." }
revert_action: hard reset to 61cd924 + force-push (operator-approved 2026-05-20)
next_path: F3 — structural layout/page boundary restructure (defer to gsd-debug-fix-phase or analogous planning workflow)
---
```

Change to:

```yaml
---
slug: profile-page-404-top-nav
status: resolved
trigger: profile page 404 on top-nav click, refresh works
created: 2026-05-13T23:00:00Z
updated: <today's ISO date — date of plan 51-08 execution>
reopened_at: 2026-05-20
resolved_at: <today's ISO date — same as updated>
phase: 51
recurrence_3_fix_commit: <merge SHA from plan 51-06's main-merge>
prod_verified: <ISO date prod verification passed in plan 51-06>
prior_resolutions:
  - cf250b1 (removed unstable_instant — wrong mechanism, recurrence 1)
  - 5def872 (isProfilePath ungates /u/* in proxy — held until 2026-05-20, recurrence 2)
recurrence_count: 3
recurrence_3_attempts:
  - { id: F2, commit: b963e6a, approach: "await connection() in [tab]/page.tsx + bare /u/[username]/page.tsx", result: FAILED_PROD_VERIFICATION, reason: "Vercel edge still returned x-vercel-cache: HIT + x-nextjs-prerender: 1 + 0-byte RSC for both prefetch and soft-nav (state-tree-keyed) requests. The Cache Components opt-out was not honored at the prod edge." }
  - { id: F1, commit: a6f1016, approach: "prefetch={false} on ProfileTabs / UserMenu / BottomNav Links", result: WOULD_NOT_HAVE_WORKED, reason: "Soft-nav click responses (with Next-Router-State-Tree header) also return 0 bytes server-side — F1 only stops prefetch-poisoning, not the click-time 0-byte response that would still render as 404." }
  - { id: F3, commit: <merge SHA>, approach: "Composite — layout collapsed to static chrome; gate moved into [tab]/page.tsx so page is the runtime-API consumer; (Branch B) proxy re-gated /u/* with cookie-only auth + Cache-Control: no-store on 307", result: PROD_VERIFIED, reason: "Structural opt-out via build-time PPR classification — route is no longer prerender-eligible; verified via scripts/verify-phase-51-prod.sh against prod" }
revert_action: hard reset to 61cd924 + force-push (operator-approved 2026-05-20)
next_path: RESOLVED via Phase 51 F3-Composite (+ Branch B re-gate, if applicable). See .planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/.
---
```

The executor must look up the actual merge SHA from `git log --oneline -10 origin/main` and the prod-verified date from plan 51-06's SUMMARY. Replace the `<>` placeholders with real values.

**Step 2 — Prepend a resolution summary section at the top of the body (immediately after `---` end of frontmatter, before the existing `## RECURRENCE 3 — 2026-05-20 (this session — REVERTED, NEEDS F3)` heading).**

Insert the following block:

```markdown
## RECURRENCE 3 — RESOLVED <today's ISO date> via Phase 51 F3-Composite

**Status:** Resolved. Prod verified. No further action required.

**Fix:** Phase 51 implemented F3-Composite (RESEARCH-ranked #1 variant):
- `src/app/u/[username]/layout.tsx` collapsed to a pure static chrome shell — no Suspense, no ProfileGate composition, no async data. The layout's contribution to the static shell is just the `<main>` wrapper.
- `src/app/u/[username]/[tab]/page.tsx` became the route's runtime-API consumer (`getCurrentUser()` reads cookies) and the dynamic stream target. The page wraps each tab's JSX return in `<Suspense fallback={<ProfileShellSkeleton/>}><ProfileGate username viewerId>...</ProfileGate></Suspense>`.
- `src/app/u/[username]/profile-gate.tsx` refactored to receive `viewerId` as a prop instead of calling `getCurrentUser()` internally — reinforces Phase 39c Pitfall 1 structurally.
- `src/app/u/[username]/profile-shell-resolver.tsx` UNCHANGED — still `'use cache' + cacheTag('profile:${username}') + cacheLife({revalidate: 300})`. The cache infrastructure and the Server-Action revalidate-path chain stay effective.

**Branch B (if applicable):** Proxy re-gated `/u/*` to authenticated viewers with two safety changes:
- `src/lib/supabase/proxy.ts:updateSession` switched from `getUser()` (network call) to `getSession()` (cookie-only). Per `node_modules/next/dist/docs/01-app/02-guides/authentication.md:1031`, proxy MUST be cookie-only on prefetch routes.
- The `NextResponse.redirect()` to `/login` carries `Cache-Control: no-store` to prevent Next 16's Router Cache from storing the 307 — defense in depth against recurrence-2 root cause.

**Verification gate that proves the fix:**
- `scripts/verify-phase-51-prod.sh https://www.horlo.app` exits 0 (REQ-51-01, REQ-51-02)
- (Branch B) `PHASE51_BRANCH_B=1 bash scripts/verify-phase-51-prod.sh https://www.horlo.app` exits 0 (REQ-51-07)
- `scripts/assert-phase-51-build.mjs` exits 0 — build output no longer classifies `/u/[username]/[tab]` as PARTIALLY_STATIC (REQ-51-03)
- `tests/profile-route-51.test.ts` all 3 specs PASS (REQ-51-04, -05, -06) — structural regression contract locks the topology
- Operator UAT: incognito + tab click-through reproduces ZERO 404s

**Recurrence-4 gate:** the regression test + build-artifact assertion + prod curl run pre-merge for any future change to `/u/[username]/**`. The next operator who touches the layout or page MUST run `npm test` + `npm run build && node scripts/assert-phase-51-build.mjs` before merging.

**Original session diagnostic notes follow below — preserved as authoritative reference for future investigations.**

---
```

After this block, the existing `## RECURRENCE 3 — original session diagnostic notes (before revert)` heading and all subsequent content stays verbatim.

**Important:** Do NOT delete or reword the historical narrative below the new resolution block. The hypothesis space, evidence captures, and prior-recurrence sections are project memory.
  </action>
  <verify>
    <automated>head -30 .planning/debug/profile-page-404-top-nav.md ; grep -c "status: resolved" .planning/debug/profile-page-404-top-nav.md ; grep -c "recurrence_3_fix_commit" .planning/debug/profile-page-404-top-nav.md</automated>
  </verify>
  <done>
    - Frontmatter `status` is `resolved`
    - `recurrence_3_fix_commit` field is populated with the real merge SHA
    - `prod_verified` field is populated with the real date
    - `phase: 51` field is added
    - A new top-section summary is prepended; the existing investigation history below is unchanged
    - File remains valid markdown (no broken frontmatter delimiters)
  </done>
</task>

</tasks>

<verification>
- `head -25 .planning/debug/profile-page-404-top-nav.md` shows new frontmatter with `status: resolved` + `recurrence_3_fix_commit` + `prod_verified`
- `grep -c "RECURRENCE 3 — RESOLVED" .planning/debug/profile-page-404-top-nav.md` ≥ 1
- The historical sections (TL;DR, evidence, recurrence narratives) still exist below
</verification>

<success_criteria>
- [ ] Debug file frontmatter updated to `status: resolved` with `phase: 51`, `recurrence_3_fix_commit`, `prod_verified` fields populated with real values from plan 51-06
- [ ] Resolution summary section prepended above the existing recurrence-3 narrative
- [ ] No existing content deleted — only frontmatter fields added/changed and a new top section added
- [ ] File still parses as valid markdown (frontmatter delimiters intact)
- [ ] Commit message references the debug file closure and the Phase 51 directory
</success_criteria>

<output>
After completion, create `.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-08-SUMMARY.md` per the standard summary template, noting the debug file is now closed.

Phase 51 is COMPLETE after this plan. The full phase verification gate (per 51-PLAN.md):
- All 3 unit tests pass ✓ (from plan 51-03)
- Build passes + structural assertion passes ✓ (from plan 51-03)
- Vercel preview deploy passes ✓ (from plan 51-06)
- Prod-contract curl exits 0 ✓ (from plan 51-06)
- Operator UAT passes ✓ (from plan 51-06)
- Debug file closed ✓ (this plan)
</output>
