---
phase: 51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta
plan: 06
type: execute
wave: 5
depends_on: [51-03, 51-05]
files_modified: []
autonomous: false
requirements: [REQ-51-01, REQ-51-02, REQ-51-07]

must_haves:
  truths:
    - "A Vercel preview deploy of the Phase 51 branch is reachable at a known URL"
    - "scripts/verify-phase-51-prod.sh against the preview URL exits 0 (REQ-51-01, REQ-51-02)"
    - "Under Branch B: PHASE51_BRANCH_B=1 verify run against preview also passes (REQ-51-07)"
    - "Operator UAT: incognito URL paste + tab clicks → zero 404s on any tab transition"
  artifacts: []
  key_links:
    - from: "Phase 51 branch in git"
      to: "Vercel preview deployment"
      via: "git push triggering Vercel's preview build"
      pattern: "vercel\\.app|horlo"
    - from: "scripts/verify-phase-51-prod.sh"
      to: "Vercel preview URL"
      via: "curl -s with RSC headers"
      pattern: "curl.*RSC"
---

<objective>
Hard gate the merge to main on a real Vercel preview deploy + prod-contract curl verification. This is the lesson of recurrences 1, 2, 3: `next start` / `next dev` and Vercel's prod edge DIVERGE. F3-Composite is structurally sound per the docs, but the only way to be sure is to deploy to a preview and run the contract check against it.

This plan is **NOT autonomous** — it has two checkpoints:
1. Operator confirms the preview URL has been deployed.
2. Operator runs `scripts/verify-phase-51-prod.sh <preview-url>` and reports the exit code.
3. Operator performs the UAT (incognito + tab click-through) and reports pass/fail.

If any of the three checkpoints fails, the plan returns to planning — F3-Composite may need refinement or an additional structural lever.

Output: A confirmation in the plan SUMMARY that the preview deploy passed REQ-51-01, REQ-51-02 (and REQ-51-07 under Branch B), and that the operator UAT passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-PLAN.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-RESEARCH.md
@scripts/verify-phase-51-prod.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push the Phase 51 branch to trigger Vercel preview</name>
  <files></files>
  <action>
Push the Phase 51 working branch to the remote so Vercel auto-creates a preview deployment.

Steps:
1. Confirm current branch is the Phase 51 branch (NOT main): `git rev-parse --abbrev-ref HEAD`
2. Confirm working tree is clean: `git status --short`
3. Push: `git push -u origin <branch-name>`
4. Wait for Vercel to start the deployment (look for the auto-comment on the GitHub PR if one exists, or check `vercel ls --scope <scope>` if Vercel CLI is installed and authenticated)
5. Record the preview URL output by Vercel

DO NOT merge to main in this task. Merge only happens after all checkpoints pass.
  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD ; git status --short ; git log -1 --oneline</automated>
  </verify>
  <done>Branch pushed; Vercel preview deploy started (operator confirms URL in the next checkpoint task).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 51 working branch pushed; Vercel preview deployment is building or has built.</what-built>
  <how-to-verify>
1. Wait for Vercel to finish the preview deploy (1-3 minutes typically).
2. Copy the preview URL (something like `https://horlo-<hash>-tyler-wanekas-projects.vercel.app`).
3. Run the prod-contract verifier WITHOUT Branch B flag first:
   ```
   bash scripts/verify-phase-51-prod.sh <preview-url>
   ```
   Expected: exit 0; printed message "OK Phase 51 prod contract verified: <preview-url>".

4. If Branch B was selected, ALSO run:
   ```
   PHASE51_BRANCH_B=1 bash scripts/verify-phase-51-prod.sh <preview-url>
   ```
   Expected: exit 0 (anon curl to /u/twwaneka/collection returns 307 + Cache-Control: no-store).

5. Manual operator UAT (the burnt-out user's repro):
   a. Open an incognito/private browser window.
   b. Paste the preview URL + `/u/twwaneka/collection` (or any known public profile path under Branch A; for Branch B, log in first OR use an authenticated context).
   c. Wait for the first load to complete (skeleton → content).
   d. Click each profile tab in sequence: collection → wishlist → worn → notes → stats → common-ground (if visible) → insights (if you're the owner).
   e. **For each tab transition:** confirm zero 404s. The page should render content, not a Next.js 404 page.
   f. Repeat the click cycle twice (the bug surfaces consistently after Router Cache poisoning — a single click-through may not be enough).
   g. Optional but recommended: use Chrome DevTools Network tab to confirm RSC requests are returning non-empty bodies.

If ALL of the above pass, type "approved" with the preview URL.
If ANY fail, paste the failing curl output, the failing UAT screenshot/description, and the preview URL.
  </how-to-verify>
  <resume-signal>Type "approved <preview-url>" (e.g. "approved https://horlo-abc123-tyler-wanekas-projects.vercel.app") or describe failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Merge to main (after checkpoint passes)</name>
  <files></files>
  <action>
After the checkpoint above is approved:

1. Switch to main: `git checkout main`
2. Pull latest: `git pull --ff-only`
3. Merge the Phase 51 branch (preserving history): `git merge --no-ff <branch-name>` — use a descriptive merge commit message: `fix(profile): Phase 51 — F3-Composite structural opt-out from PPR (+ Branch B re-gate if applicable)`
4. Push: `git push origin main`
5. Wait for Vercel prod deploy to complete (3-5 minutes).
6. Run the prod-contract verifier against PROD:
   ```
   bash scripts/verify-phase-51-prod.sh https://www.horlo.app
   PHASE51_BRANCH_B=1 bash scripts/verify-phase-51-prod.sh https://www.horlo.app   # Branch B only
   ```
   Both should exit 0. Capture the output for the SUMMARY.

If prod verification fails after merge: this is a CRITICAL escalation — open a debug session, do NOT attempt a hot-revert (the entire phase is a structural change; revert would re-introduce the bug). Investigate divergence between preview and prod.
  </action>
  <verify>
    <automated>git log --oneline -3 origin/main ; bash scripts/verify-phase-51-prod.sh https://www.horlo.app 2>&1 | tail -5</automated>
  </verify>
  <done>Phase 51 branch merged to main; prod deploy succeeded; prod-contract curl exits 0.</done>
</task>

</tasks>

<verification>
- Preview URL deployed and reachable
- `bash scripts/verify-phase-51-prod.sh <preview-url>` exits 0
- (Branch B) `PHASE51_BRANCH_B=1 bash scripts/verify-phase-51-prod.sh <preview-url>` exits 0
- Operator UAT (incognito + tab clicks) passes
- Main merged; prod deploy succeeded; prod-contract curl exits 0
</verification>

<success_criteria>
- [ ] Preview deploy URL captured and verifier passes against it
- [ ] (Branch B) Branch B verifier flag passes against preview
- [ ] Operator UAT passes (zero 404s on tab clicks; the recurrence-3 symptom does not reproduce)
- [ ] Merge to main complete; prod deploy succeeded
- [ ] Prod-contract curl against `https://www.horlo.app` exits 0
- [ ] Both runs (with and without `PHASE51_BRANCH_B=1`) captured for the SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-06-SUMMARY.md` containing:
- Preview URL
- Verifier output (preview + prod)
- Merge commit SHA
- UAT result

Next: plan 51-07 (optional bare-username redirect via next.config.ts) and plan 51-08 (close the debug file).
</output>
