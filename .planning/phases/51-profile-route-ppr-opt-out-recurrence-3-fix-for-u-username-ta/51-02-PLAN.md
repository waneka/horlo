---
phase: 51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta
plan: 02
type: execute
wave: 1
depends_on: [51-01]
files_modified:
  - src/app/u/[username]/profile-gate.tsx
autonomous: true
requirements: [REQ-51-05, REQ-51-06]

must_haves:
  truths:
    - "ProfileGate accepts viewerId as a typed prop"
    - "ProfileGate does NOT call getCurrentUser() internally"
    - "ProfileGate does NOT import getCurrentUser or UnauthorizedError"
    - "All consumers of ProfileGate (currently only the layout) are KEPT WORKING by deferring the caller signature change to plan 51-03"
    - "Test 2 (REQ-51-05) in tests/profile-route-51.test.ts now PASSES"
    - "Test 3 (REQ-51-06) in tests/profile-route-51.test.ts continues to PASS (resolver unchanged)"
    - "Phase 39c Pitfall 1 invariant — viewerId outside cache — is REINFORCED, not violated"
  artifacts:
    - path: "src/app/u/[username]/profile-gate.tsx"
      provides: "Uncached gate that branches on viewerId received as a prop"
      contains: "viewerId: string | null"
  key_links:
    - from: "src/app/u/[username]/profile-gate.tsx"
      to: "src/app/u/[username]/profile-shell-resolver.tsx"
      via: "await ProfileShellResolver({ username })"
      pattern: "ProfileShellResolver\\(\\{\\s*username\\s*\\}\\)"
---

<objective>
Refactor `<ProfileGate>` to receive `viewerId: string | null` as a typed prop instead of calling `getCurrentUser()` internally. This is a pure signature change — gate body logic, locked-branch rendering, ProfileShellResolver invocation, notFound() ordering, and common-ground composition all stay identical.

Purpose: Make `ProfileGate` a pure async function of `(username, viewerId, children)`. This is the prerequisite for plan 51-03's structural change that moves cookie-reading + gate-composition into `[tab]/page.tsx`, which is the actual F3-Composite topology lever.

Phase 39c Pitfall 1 is REINFORCED by this change: the gate now physically cannot read cookies, so the documented invariant becomes structural rather than convention.

Output: Single-file edit to `src/app/u/[username]/profile-gate.tsx`. After this plan, plan 51-03 will update the layout to PASS `viewerId` to the gate (temporarily — the layout still calls the gate until 51-03's larger restructure).

**CRITICAL — atomic-commit contract (recurrence-3-baseline avoidance):**

Plan 51-02 + Plan 51-03 are a **single atomic unit**. Between 51-02's commit and 51-03's commit, `src/app/u/[username]/layout.tsx` will fail TypeScript compilation because the layout still calls `<ProfileGate username={username}>{children}</ProfileGate>` without the new required `viewerId` prop.

**This unit MUST NOT be pushed to `origin/main` partially.** Two acceptable execution paths:

1. **Single PR (recommended):** Execute 51-02 and 51-03 sequentially on a feature branch (`fix/phase-51-profile-route-ppr-opt-out`), then open ONE PR that contains both commits. Squash on merge is fine; preserve-history is also fine. Never push 51-02 alone to a deploy-tracking branch.

2. **Squash locally before push:** Execute 51-02 and 51-03 on a local branch, then `git rebase -i HEAD~2` to squash into one commit before push. The squashed commit's working tree includes both file changes; the broken-build window never reaches origin.

The plan author considered bundling 51-02 + 51-03 into a single plan file but kept them split for plan-size hygiene (single-file edits are easier to review and revert independently). The hard contract above replaces that hygiene cost with execution discipline.

**Executor gate:** before pushing 51-02's commit to origin, verify the branch ALSO contains 51-03's commit. If not, do not push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-RESEARCH.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-PLAN.md

@src/app/u/[username]/profile-gate.tsx
@src/app/u/[username]/profile-shell-resolver.tsx
@src/app/u/[username]/layout.tsx
@src/app/u/[username]/[tab]/page.tsx

<interfaces>
<!-- Current signature (to be replaced): -->
<!-- export async function ProfileGate({ username, children }: { username: string; children: React.ReactNode }) -->

<!-- New signature: -->
<!-- export async function ProfileGate({ username, viewerId, children }: { username: string; viewerId: string | null; children: React.ReactNode }) -->

<!-- Current internal call (to be removed): -->
<!-- let viewerId: string | null = null -->
<!-- try { viewerId = (await getCurrentUser()).id } catch (err) { if (!(err instanceof UnauthorizedError)) throw err } -->

<!-- Imports to remove: -->
<!-- import { getCurrentUser, UnauthorizedError } from '@/lib/auth' -->

<!-- ALL other code in the gate stays IDENTICAL — the rest of the function body references `viewerId` which is now sourced from props instead of the local try/catch. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor ProfileGate to accept viewerId as a prop</name>
  <files>src/app/u/[username]/profile-gate.tsx</files>
  <behavior>
    - The exported function `ProfileGate` accepts `{ username, viewerId, children }` where `viewerId: string | null`
    - The function body does NOT contain any call to `getCurrentUser()` or reference to `UnauthorizedError`
    - The import statement for `getCurrentUser`/`UnauthorizedError` from `@/lib/auth` is REMOVED
    - The existing JSDoc comment block referencing "Pitfall 1" / "viewer resolution OUTSIDE the cached scope" is UPDATED to reflect that viewerId now comes from the caller (the page), reinforcing Pitfall 1 structurally
    - All downstream uses of `viewerId` within the function body remain identical (isOwner check, isFollowing call, LockedProfileState props, ProfileHeader props, etc.)
    - The `notFound()` call ordering is preserved (called immediately after `!resolved.profile` check, BEFORE any post-suspending await)
    - The call `await ProfileShellResolver({ username })` is unchanged — resolver still receives only `username`
    - `tests/profile-route-51.test.ts` Test 2 (REQ-51-05) PASSES after this change
  </behavior>
  <action>
Edit `src/app/u/[username]/profile-gate.tsx`:

1. Remove the import: `import { getCurrentUser, UnauthorizedError } from '@/lib/auth'` (line 3)

2. Change the function signature from:
   ```
   export async function ProfileGate({
     username,
     children,
   }: {
     username: string
     children: React.ReactNode
   }) {
   ```
   to:
   ```
   export async function ProfileGate({
     username,
     viewerId,
     children,
   }: {
     username: string
     viewerId: string | null
     children: React.ReactNode
   }) {
   ```

3. DELETE the try/catch block that resolved viewerId locally (currently lines 40-45):
   ```
   let viewerId: string | null = null
   try {
     viewerId = (await getCurrentUser()).id
   } catch (err) {
     if (!(err instanceof UnauthorizedError)) throw err
   }
   ```
   (viewerId now arrives via props)

4. Update the JSDoc header comment block (lines 12-31): replace the existing description so it documents the new contract:
   - State: "Viewer-dependent branching outside the cached scope (D-39c-05, T-39c-01 / T-39c-04 mitigations). Receives `viewerId` from the page (the runtime-API consumer) so this gate can be a pure async function of (username, viewerId)."
   - Preserve the three load-bearing invariants list, updating (b) to read: "`viewerId` is received as a prop and lives OUTSIDE the `<ProfileShellResolver/>` cached scope (Pitfall 1). The resolver MUST NOT receive viewerId."
   - Preserve invariant (c) verbatim
   - PROHIBITED list at the bottom: keep all four items; do NOT add `getCurrentUser` to the prohibited list (the page calls it now — that's the whole point)

5. The rest of the function body (lines 48 onwards in the current file: `const resolved = await ProfileShellResolver(...)` through the final return statement) is UNCHANGED.

DO NOT touch `src/app/u/[username]/layout.tsx` in this plan. The layout will fail TypeScript compilation after this edit because it calls `<ProfileGate username={username}>{children}</ProfileGate>` without `viewerId`. Plan 51-03 fixes this in the immediately-following plan. The commit message MUST call out this temporary broken state with the literal string `[DO NOT PUSH ALONE]` so reviewers and pre-push hooks can detect it.
  </action>
  <verify>
    <automated>npm test -- tests/profile-route-51.test.ts --reporter=verbose 2>&1 | grep -E "(ProfileGate|viewerId|REQ-51-05|✓|✗)" | head -10</automated>
  </verify>
  <done>
    - Test 2 (REQ-51-05) "gate accepts viewerId prop" PASSES
    - Test 3 (REQ-51-06) "resolver remains cached" continues to PASS (resolver unchanged)
    - `grep -c "getCurrentUser" src/app/u/[username]/profile-gate.tsx` returns 0
    - `grep -c "UnauthorizedError" src/app/u/[username]/profile-gate.tsx` returns 0
    - TypeScript compilation of layout.tsx FAILS (expected — fixed by 51-03)
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/profile-route-51.test.ts -t "gate accepts viewerId prop"` → PASS
- `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' src/app/u/[username]/profile-gate.tsx | grep -c "getCurrentUser"` → 0 (filtering out comment lines)
- `grep -E '^export async function ProfileGate' src/app/u/[username]/profile-gate.tsx` matches the new 3-prop signature

DO NOT run `npm run build` after this plan — layout.tsx is in a temporarily broken state. Plan 51-03 restores buildability.
</verification>

<success_criteria>
- [ ] `src/app/u/[username]/profile-gate.tsx` exports `ProfileGate({ username, viewerId, children })` with `viewerId: string | null`
- [ ] No imports from `@/lib/auth` remain in the file
- [ ] No calls to `getCurrentUser()` or references to `UnauthorizedError` in the file body
- [ ] JSDoc updated to reflect the new contract (Pitfall 1 stated as structural, not just conventional)
- [ ] Test 2 of `tests/profile-route-51.test.ts` PASSES
- [ ] ProfileShellResolver invocation unchanged: still receives only `{ username }`
- [ ] Commit message flags layout.tsx as temporarily broken; plan 51-03 fixes it
</success_criteria>

<output>
After completion, create `.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-02-SUMMARY.md` per the standard summary template, noting that layout.tsx is in a temporary broken state pending plan 51-03.
</output>
