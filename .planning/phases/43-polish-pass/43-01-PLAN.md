---
phase: 43-polish-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/extractors/llm.ts
  - src/components/search/FilterDrawer.tsx
  - src/components/search/SearchPageClient.tsx
  - tests/extractors/llm.test.ts
  - tests/components/search/FilterDrawer.test.tsx
autonomous: true
requirements: [PLSH-01, PLSH-02, PLSH-07]
must_haves:
  truths:
    - "User can dismiss the /search filter sheet by swiping down"
    - "User can dismiss the /search filter sheet by tapping the backdrop"
    - "Filter sheet dismiss is never blocked while a filtered query is loading"
    - "Watch extraction uses the non-deprecated claude-sonnet-4-6 model ID"
  artifacts:
    - path: "src/components/search/FilterDrawer.tsx"
      provides: "Base UI Drawer-based filter sheet with swipe-to-dismiss"
      contains: "swipeDirection"
    - path: "src/lib/extractors/llm.ts"
      provides: "LLM extraction call with current model ID"
      contains: "claude-sonnet-4-6"
  key_links:
    - from: "src/components/search/SearchPageClient.tsx"
      to: "src/components/search/FilterDrawer.tsx"
      via: "import + render with open/onOpenChange props"
      pattern: "FilterDrawer"
    - from: "src/components/search/FilterDrawer.tsx"
      to: "Drawer.Root onOpenChange"
      via: "onOpenChange passed straight through, no async guard"
      pattern: "onOpenChange=\\{onOpenChange\\}"
---

<objective>
Migrate the `/search` filter bottom-sheet from the shared `Sheet` primitive to a
Base UI Drawer with native swipe-to-dismiss, and update the deprecated Claude
model ID in the watch extractor.

Purpose: PLSH-01/02 — the filter sheet must be dismissable by swipe-down and
backdrop tap, and dismiss must never be blocked while a filtered query is in
flight. PLSH-07 — `claude-sonnet-4-20250514` is deprecated June 15, 2026.
Output: New `FilterDrawer.tsx` component wired into `SearchPageClient`, the old
`FilterSheet.tsx` left in place but no longer rendered, and a one-line model ID
patch in `llm.ts`. Two new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-polish-pass/43-CONTEXT.md
@.planning/phases/43-polish-pass/43-RESEARCH.md
@.planning/phases/43-polish-pass/43-PATTERNS.md
@.planning/phases/43-polish-pass/43-UI-SPEC.md

# AGENTS.md: this is a non-standard Next.js 16. Read the relevant guide in
# node_modules/next/dist/docs/ before writing any Next.js-specific code.

<interfaces>
<!-- Drawer sub-components confirmed present in @base-ui/react v1.3.0 (installed).
     Do NOT bump @base-ui/react — RESEARCH.md verified Drawer is stable at 1.3.0. -->
import { Drawer } from '@base-ui/react/drawer'
  Drawer.Root  — props: open, onOpenChange(open: boolean), swipeDirection
  Drawer.Portal, Drawer.Backdrop, Drawer.Viewport, Drawer.Popup
  Drawer.Content, Drawer.Title, Drawer.Close (supports `render` prop)

<!-- FilterDrawerProps — identical shape to existing WatchFacetSheetProps in FilterSheet.tsx -->
interface FilterDrawerProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  movement: string | null
  size: string | null
  styleArr: string[]
  onMovementChange: (v: string | null) => void
  onSizeChange: (v: string | null) => void
  onStyleChange: (v: string[]) => void
  styleVocab: string[]
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update deprecated Claude model ID (PLSH-07)</name>
  <files>src/lib/extractors/llm.ts, tests/extractors/llm.test.ts</files>
  <read_first>
    - src/lib/extractors/llm.ts (the file being modified — confirm the model string is on line 78 region)
    - src/lib/taste/enricher.ts (analog — already uses `claude-sonnet-4-6`, match it exactly)
    - tests/extractors/ (directory — check whether a test file for llm.ts already exists)
  </read_first>
  <action>
    In `src/lib/extractors/llm.ts`, change the `model:` string passed to the
    Anthropic SDK from `claude-sonnet-4-20250514` to `claude-sonnet-4-6` (per
    D-13). Change ONLY that string literal — no other lines. Then create or
    extend `tests/extractors/llm.test.ts` with a test asserting the extractor
    issues its Anthropic call with `model: 'claude-sonnet-4-6'`. If a test file
    already exists in `tests/extractors/`, add the assertion as a new `it()`
    block rather than overwriting. Mock `@anthropic-ai/sdk` so no network call
    is made; assert the `model` field on the messages-create call arguments.
  </action>
  <verify>
    <automated>grep -n "claude-sonnet-4-6" src/lib/extractors/llm.ts && ! grep -q "claude-sonnet-4-20250514" src/lib/extractors/llm.ts && npm test -- llm</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/extractors/llm.ts` contains the exact string `claude-sonnet-4-6`
    - `src/lib/extractors/llm.ts` no longer contains `claude-sonnet-4-20250514`
    - `npm test -- llm` passes and includes an assertion on the `model` argument
  </acceptance_criteria>
  <done>The watch extractor calls Claude with `claude-sonnet-4-6`; a test pins the model ID.</done>
</task>

<task type="auto">
  <name>Task 2: Create FilterDrawer component (PLSH-01, PLSH-02)</name>
  <files>src/components/search/FilterDrawer.tsx, tests/components/search/FilterDrawer.test.tsx</files>
  <read_first>
    - src/components/search/FilterSheet.tsx (exact analog — copy the props interface, the `handleClearAll` function, and the chip render order verbatim; this is a drop-in replacement)
    - src/components/ui/sheet.tsx (transition pattern — `data-starting-style`/`data-ending-style` on the popup; lines ~56-57)
    - src/components/search/MovementChips.tsx, CaseSizeChips.tsx, StyleChips.tsx (chip component prop signatures — `selected`/`onSelect`/`vocab`)
    - .planning/phases/43-polish-pass/43-PATTERNS.md § "src/components/search/FilterDrawer.tsx" (full Drawer component tree)
    - .planning/phases/43-polish-pass/43-UI-SPEC.md § "1. FilterDrawer" (component tree, classNames, interaction contract)
    - node_modules/@base-ui/react/drawer/index.d.ts (Drawer sub-component types)
  </read_first>
  <action>
    Create `src/components/search/FilterDrawer.tsx` as a `'use client'` component
    that replaces the render of `FilterSheet.tsx`. Use `FilterDrawerProps` with
    the exact shape of `WatchFacetSheetProps` (open, onOpenChange, movement,
    size, styleArr, onMovementChange, onSizeChange, onStyleChange, styleVocab).
    Build the Drawer tree: `Drawer.Root` with `open={open}`,
    `onOpenChange={onOpenChange}`, `swipeDirection="down"` → `Drawer.Portal` →
    `Drawer.Backdrop` (`className="fixed inset-0 z-50 bg-black/10"`) →
    `Drawer.Viewport` (`className="fixed inset-0 z-50 flex flex-col justify-end pointer-events-none"`)
    → `Drawer.Popup` (`className="max-h-[80vh] overflow-y-auto bg-popover rounded-t-xl border-t border-border pb-[env(safe-area-inset-bottom)] pointer-events-auto"`).
    Inside the Popup: a drag-handle `<div className="mx-auto mt-2 h-2 w-10 rounded-full bg-muted-foreground/30 shrink-0" />`
    (8px tall per UI-SPEC, NOT `h-1.5`); a `Drawer.Content`
    (`className="flex flex-col gap-6 px-4 pb-2 pt-2"`) holding a `Drawer.Title`
    (`className="font-heading text-base font-semibold text-foreground"`, text
    "Filters") then `MovementChips`, `CaseSizeChips`, `StyleChips` in that order
    with the same props the analog passes; and a footer `<div className="mt-auto flex flex-col gap-2 p-4 border-t border-border pt-3">`
    containing a `Drawer.Close` rendered as a `Button variant="ghost" size="sm"`
    with `className="text-destructive hover:text-destructive w-full"` and
    `onClick={handleClearAll}`, text "Clear all". Copy `handleClearAll` verbatim
    from the analog (sets movement/size to null, styleArr to []). CRITICAL per
    D-03: `onOpenChange` MUST be passed straight to `Drawer.Root` with NO
    `if (!loading)` guard, NO wrapping — dismiss must never be gated on async
    state. For the Popup enter/exit transition, only use
    `data-starting-style`/`data-ending-style` selectors (mirror `sheet.tsx`);
    do NOT add a blanket `transition` utility class — it conflicts with the
    `--drawer-swipe-movement-y` swipe animation (RESEARCH.md Pitfall 1).
    Then create `tests/components/search/FilterDrawer.test.tsx`: render the
    component with `open={true}`, assert the "Filters" title is in the document,
    assert the `swipeDirection="down"` prop reaches `Drawer.Root` (assert via
    rendered DOM data attribute or by spying on the component), and assert that
    calling the rendered `onOpenChange` is not wrapped in any pending guard —
    i.e. a backdrop/close interaction invokes the passed `onOpenChange` callback
    directly.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- FilterDrawer</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/search/FilterDrawer.tsx` exists, first line is `'use client'`
    - File imports `{ Drawer } from '@base-ui/react/drawer'`
    - File contains the literal `swipeDirection="down"`
    - `onOpenChange` is passed directly to `Drawer.Root` — `grep` shows no `if`/`!loading`/`pending` token wrapping the `onOpenChange` prop value
    - Drag handle div uses class `h-2 w-10` (NOT `h-1.5`)
    - `Drawer.Popup` has NO standalone `transition` utility class (only `data-starting-style`/`data-ending-style` based transitions)
    - `npm test -- FilterDrawer` passes; test asserts the "Filters" title renders and `onOpenChange` fires unguarded
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>FilterDrawer renders the filter chips in a swipe-dismissible Base UI Drawer; dismiss is never gated on query state.</done>
</task>

<task type="auto">
  <name>Task 3: Wire FilterDrawer into SearchPageClient</name>
  <files>src/components/search/SearchPageClient.tsx</files>
  <read_first>
    - src/components/search/SearchPageClient.tsx (the file being modified — find where `WatchFacetSheet`/`FilterSheet` is imported and rendered, and where `setSheetOpen`/sheet-open state lives)
    - src/components/search/useSearchState.ts (confirm whether any loading/pending flag is currently passed into the sheet — D-03 requires NONE)
    - src/components/search/FilterDrawer.tsx (the new component from Task 2 — its props)
  </read_first>
  <action>
    In `src/components/search/SearchPageClient.tsx`, replace the import and JSX
    render of the filter `Sheet`/`WatchFacetSheet` (`FilterSheet.tsx`) with the
    new `FilterDrawer` component from `@/components/search/FilterDrawer`. Pass
    the identical props the old component received — `open`, `onOpenChange`
    (the existing `setSheetOpen` setter or equivalent), `movement`, `size`,
    `styleArr`, `onMovementChange`, `onSizeChange`, `onStyleChange`,
    `styleVocab`. Per D-02, do NOT touch `src/components/ui/sheet.tsx` or any
    other Dialog/Sheet usage. Per D-03, verify the migration does NOT pass any
    loading/pending flag into `onOpenChange` and does not introduce an async
    guard around closing the drawer. Leave `FilterSheet.tsx` on disk untouched
    (it is simply no longer rendered) — do not delete it in this plan.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/search/SearchPageClient.tsx` imports `FilterDrawer` from `@/components/search/FilterDrawer`
    - `SearchPageClient.tsx` no longer renders `WatchFacetSheet`/`FilterSheet`
    - `grep` on `SearchPageClient.tsx` shows no `if`/`loading`/`pending` token gating the value passed to the drawer's `onOpenChange`
    - `src/components/ui/sheet.tsx` is unchanged (no diff)
    - `npx tsc --noEmit` and `npm run build` both succeed
  </acceptance_criteria>
  <done>The /search page renders the FilterDrawer; the filter sheet is swipe- and backdrop-dismissible with no async guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Anthropic API | model ID string sent server-side in the extractor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | `FilterDrawer.tsx` | accept | Pure client-side UI component; filter values are non-privileged display state. No new attack surface — the Drawer replaces an equivalent Sheet. |
| T-43-02 | Denial of Service | `llm.ts` model ID | accept | Model ID is a hardcoded string constant, not user input. Changing it cannot be exploited. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run build` succeeds.
- `npm test -- FilterDrawer` and `npm test -- llm` pass.
- Manual (UAT, see 43-VALIDATION.md): on `/search`, apply a filter, and while
  results load swipe the sheet down AND tap the backdrop — sheet closes both
  ways with no JS error.
</verification>

<success_criteria>
- `claude-sonnet-4-20250514` no longer appears in `src/lib/extractors/llm.ts`.
- The `/search` filter sheet is a Base UI Drawer with `swipeDirection="down"`.
- Drawer dismiss is never gated on pending query state (D-03).
- `src/components/ui/sheet.tsx` untouched (D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/43-polish-pass/43-01-SUMMARY.md`
</output>
