---
phase: 43-polish-pass
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/profile/ProfileWatchCard.tsx
  - src/components/profile/CollectionTabContent.tsx
  - src/components/profile/WishlistTabContent.tsx
autonomous: true
gap_closure: true
requirements: [PLSH-04, PLSH-05]
user_setup: []

must_haves:
  truths:
    - "Watch cards in the collection and wishlist grids are visibly shorter — tighter vertical spacing around the brand/model header and the watch image"
    - "Cards in a given grid still all reach the same total height regardless of metadata or photo (equal-height behavior preserved)"
    - "The 'Add to Collection' and 'Add to Wishlist' buttons render with the outline (secondary) style, not the filled primary style"
  artifacts:
    - path: "src/components/profile/ProfileWatchCard.tsx"
      provides: "Equal-height watch card with tightened vertical spacing"
      contains: "h-full flex flex-col"
    - path: "src/components/profile/CollectionTabContent.tsx"
      provides: "Add to Collection button with outline variant"
      contains: "variant=\"outline\""
    - path: "src/components/profile/WishlistTabContent.tsx"
      provides: "Add to Wishlist button with outline variant"
      contains: "variant=\"outline\""
  key_links:
    - from: "src/components/profile/ProfileWatchCard.tsx"
      to: "CSS grid equal-height row"
      via: "h-full flex flex-col on Card + flex-1 on CardContent"
      pattern: "flex-1"
---

<objective>
Close two UAT gaps in the Phase 43 polish pass, both pure-frontend tweaks on already-shipped profile components.

- **GAP-43-01 (PLSH-04):** `ProfileWatchCard` cards render correctly with equal height, but run taller than the user wants. Tighten the vertical spacing — reduce padding above/below the brand-model header and around the watch image — WITHOUT breaking the equal-height CSS chain.
- **GAP-43-02 (PLSH-05):** The "Add to Collection" / "Add to Wishlist" buttons above the populated grids use the filled primary (`variant="default"`) style. Change both to `variant="outline"` (user-selected). Nothing else about the buttons changes.

Purpose: The watch grids are the primary collection-browsing surface; the polish pass is meant to make them feel finished before v5.1's `/explore` surfaces are built. The user reviewed the shipped cards and asked for tighter spacing and a less-loud add button.

Output: Two visual tweaks across three already-shipped components. No new files, no new dependencies, no DB changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-polish-pass/43-HUMAN-UAT.md
@.planning/phases/43-polish-pass/43-CONTEXT.md

# Project instructions — NON-STANDARD Next.js 16
This project runs Next.js 16 with breaking changes vs. training data. Per AGENTS.md,
read the relevant guide in `node_modules/next/dist/docs/` before writing any Next.js
code. These two tasks are pure Tailwind class / prop changes inside existing
`'use client'` components — no Next.js API surface changes — but verify if anything
looks unfamiliar.

<interfaces>
<!-- Current ProfileWatchCard structure (src/components/profile/ProfileWatchCard.tsx). -->
<!-- The equal-height CSS chain MUST be preserved by GAP-43-01: -->
<!--   - <Card> has: "...overflow-hidden... h-full flex flex-col"     ← grows to grid row height -->
<!--   - header <div> has: "px-3 pt-3 pb-1"                           ← TIGHTEN this -->
<!--   - image <div> has: "relative aspect-[3/4] bg-muted"            ← keep aspect-[3/4]; tighten spacing AROUND it -->
<!--   - <CardContent> has: "p-3 flex flex-col gap-1 flex-1"          ← flex-1 absorbs extra height; TIGHTEN the padding -->
<!-- The card already uses aspect-[3/4] (already shorter than 4/5) — do NOT change the aspect ratio. -->

<!-- Add-button call sites (variant is the ONLY thing that changes): -->
<!-- CollectionTabContent.tsx ~line 169-178: <Button variant="default" size="sm" className="shrink-0 min-h-[44px]" render={<Link .../>}>Add to Collection</Button> -->
<!-- WishlistTabContent.tsx ~line 111-122:  <Button variant="default" size="sm" className="shrink-0 min-h-[44px]" render={<Link .../>}>Add to Wishlist</Button> -->
<!-- The Button `outline` variant is a standard shadcn variant already used elsewhere in the codebase — no new variant definition needed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten ProfileWatchCard vertical spacing (GAP-43-01)</name>
  <files>src/components/profile/ProfileWatchCard.tsx</files>
  <read_first>
    - src/components/profile/ProfileWatchCard.tsx — the component being modified; note the equal-height CSS chain on lines 61 (Card), 63 (header div), 68 (image div), 97 (CardContent).
    - .planning/phases/43-polish-pass/43-HUMAN-UAT.md — GAP-43-01 detail block.
    - .planning/phases/43-polish-pass/43-02-PLAN.md — the original plan that established the equal-height layout, for the rationale behind each class.
  </read_first>
  <action>
    Per GAP-43-01 (PLSH-04, decisions D-04/D-05): the card height is too tall. Reduce vertical
    spacing ONLY — the equal-height behavior is correct and must be preserved.

    Make these spacing reductions:
    - Brand/model header `<div>` (currently `px-3 pt-3 pb-1`): reduce the top padding from
      `pt-3` to `pt-2` and keep `pb-1` (or reduce to `pb-0.5`). Keep `px-3` for horizontal
      alignment.
    - `<CardContent>` (currently `p-3 flex flex-col gap-1 flex-1`): reduce the vertical
      padding so the block above and below the image text is tighter — change `p-3` to
      `px-3 py-2` (keeps horizontal padding, shrinks top/bottom). Keep `flex flex-col gap-1
      flex-1` exactly.
    - The image `<div>` keeps `relative aspect-[3/4] bg-muted` UNCHANGED — the aspect ratio
      is already shorter than 4/5 and is not the cause of excess height. The "padding above
      and below the watch image" is the header div's bottom padding and the CardContent's
      top padding, both addressed above.

    DO NOT change:
    - `h-full flex flex-col` on the `<Card>` — this is the equal-height key.
    - `flex-1` on `<CardContent>` — this absorbs extra height for sparse cards.
    - `aspect-[3/4]` on the image div.
    - Any conditional rendering (wear badge, wear line, tag, price, notes) — wear-UI gating
      from PLSH-03 stays exactly as-is.

    Net effect: the card is shorter; sparse cards still equalize to the tallest card in the
    grid row.
  </action>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && npx tsc --noEmit -p tsconfig.json && npm test -- ProfileWatchCard --run 2>&1 | tail -20 && grep -qF 'h-full flex flex-col' src/components/profile/ProfileWatchCard.tsx && grep -qF 'flex-1' src/components/profile/ProfileWatchCard.tsx && grep -qF 'aspect-[3/4]' src/components/profile/ProfileWatchCard.tsx && echo CHAIN_INTACT</automated>
  </verify>
  <acceptance_criteria>
    - The `<Card>` element still has the literal classes `h-full flex flex-col` (equal-height
      chain intact) — `grep -F 'h-full flex flex-col'` returns a match.
    - The `<CardContent>` element still has `flex-1` (sparse-card height absorption intact) —
      `grep -F 'flex-1'` returns a match.
    - The image `<div>` still has `aspect-[3/4]` — `grep -F 'aspect-[3/4]'` returns a match.
    - The header div no longer uses `pt-3`; it uses `pt-2` (tighter top padding).
    - `<CardContent>` no longer uses bare `p-3`; it uses split horizontal/vertical padding
      with reduced vertical padding (`px-3 py-2`).
    - `npx tsc --noEmit` passes with no new errors.
    - `npm test -- ProfileWatchCard` passes (existing equal-height / wear-UI tests stay green).
    - The verify command prints `CHAIN_INTACT`.
  </acceptance_criteria>
  <done>ProfileWatchCard renders shorter cards (tighter header + CardContent vertical padding) while the `h-full flex flex-col` / `flex-1` / `aspect-[3/4]` equal-height CSS chain remains fully intact.</done>
</task>

<task type="auto">
  <name>Task 2: Change add-watch buttons to outline variant (GAP-43-02)</name>
  <files>src/components/profile/CollectionTabContent.tsx, src/components/profile/WishlistTabContent.tsx</files>
  <read_first>
    - src/components/profile/CollectionTabContent.tsx — the "Add to Collection" button is at ~line 169-178 inside the populated-grid filter row.
    - src/components/profile/WishlistTabContent.tsx — the "Add to Wishlist" button is at ~line 111-122 inside the owner populated branch.
    - .planning/phases/43-polish-pass/43-HUMAN-UAT.md — GAP-43-02 detail block.
    - .planning/phases/43-polish-pass/43-03-PLAN.md — the original add-button relocation plan, for the placement/copy/returnTo contract that must NOT change.
  </read_first>
  <action>
    Per GAP-43-02 (PLSH-05): both populated-grid add buttons currently use `variant="default"`
    (filled primary). The user reviewed the placement and approved it, then asked for the
    button to NOT be primary-weighted.

    Change ONLY the `variant` prop on these two buttons:
    - `CollectionTabContent.tsx` — the "Add to Collection" `<Button>` inside the populated
      filter-chips + search row (the one guarded by `{isOwner && (...)}`, with
      `render={<Link href=.../>}`): change `variant="default"` to `variant="outline"`.
    - `WishlistTabContent.tsx` — the "Add to Wishlist" `<Button>` inside the owner populated
      branch (`{isOwner && (<div className="mb-4 flex justify-end">...)`): change
      `variant="default"` to `variant="outline"`.

    DO NOT change anything else on either button:
    - Keep `size="sm"`.
    - Keep `className="shrink-0 min-h-[44px]"` (the 44px touch target stays).
    - Keep the `render={<Link href={...} />}` pattern and the exact `href` expressions.
    - Keep the button copy ("Add to Collection" / "Add to Wishlist").
    - Keep the `{isOwner && (...)}` guards and the surrounding row markup.

    SCOPE GUARD — do NOT touch the empty-state CTAs. Per D-08, the centered empty-state
    cards (the "Nothing here yet." / "No wishlist watches yet." blocks, including
    `CollectionTabContent`'s two-button `!hasUrlExtract` fallback that uses
    `variant="default"`, and `WishlistTabContent`'s empty-state "Add a wishlist watch"
    button) are explicitly OUT of scope. Only the populated-grid add buttons change. Leave
    every other `variant="default"` in both files untouched.
  </action>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && npx tsc --noEmit -p tsconfig.json && npm test -- CollectionTabContent WishlistTabContent --run 2>&1 | tail -20 && grep -qF 'variant="outline"' src/components/profile/CollectionTabContent.tsx && grep -qF 'variant="outline"' src/components/profile/WishlistTabContent.tsx && echo OUTLINE_APPLIED</automated>
  </verify>
  <acceptance_criteria>
    - `CollectionTabContent.tsx`: the populated-grid "Add to Collection" `<Button>` has
      `variant="outline"`.
    - `WishlistTabContent.tsx`: the populated owner "Add to Wishlist" `<Button>` has
      `variant="outline"`.
    - Both buttons still have `size="sm"` and a `className` containing `min-h-[44px]`.
    - Both buttons still use `render={<Link .../>}` with their original `href` expressions.
    - The empty-state `!hasUrlExtract` fallback buttons in `CollectionTabContent.tsx` still
      use `variant="default"` (untouched) — `grep -c 'variant="default"' src/components/profile/CollectionTabContent.tsx`
      returns at least 2.
    - `WishlistTabContent.tsx`'s empty-state "Add a wishlist watch" button still uses
      `variant="default"` (untouched).
    - `npx tsc --noEmit` passes with no new errors.
    - `npm test -- CollectionTabContent WishlistTabContent` passes.
    - The verify command prints `OUTLINE_APPLIED`.
  </acceptance_criteria>
  <done>Both populated-grid add-watch buttons render with the outline (secondary) style; placement, copy, link targets, and the 44px touch target are unchanged; empty-state CTAs are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes only Tailwind class strings and one component prop on already-shipped client components. No new input crosses any trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-G05-01 | Tampering | ProfileWatchCard / TabContent CSS | accept | Pure presentational class/prop changes; no data flow, no input handling, no auth surface. No new attack surface introduced — confirmed by the phase `<security_threat_model>` scope note ("The frontend gap (43-05) has no new attack surface"). |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no new errors across all three modified files.
- `npm test -- ProfileWatchCard CollectionTabContent WishlistTabContent` is fully green.
- Equal-height CSS chain assertion: `grep -F 'h-full flex flex-col'`, `grep -F 'flex-1'`, and `grep -F 'aspect-[3/4]'` on `ProfileWatchCard.tsx` all return matches (Task 1 verify prints `CHAIN_INTACT`).
- Manual visual smoke check (mobile 375px viewport): four cards in a collection/wishlist row with mixed metadata still reach the same total height within ~2px, and the overall card is visibly shorter than before.
- Manual visual check: the "Add to Collection" / "Add to Wishlist" buttons render with the outline style (border + transparent fill), not the filled primary style.
</verification>

<success_criteria>
- GAP-43-01 closed: collection and wishlist watch cards are visibly shorter (tighter header and CardContent vertical padding) and still equal-height within a grid row.
- GAP-43-02 closed: both populated-grid add-watch buttons use `variant="outline"`.
- No regression: PLSH-03 wear-UI gating, PLSH-04 equal-height, PLSH-05 button placement/copy/links, and all empty-state CTAs are unchanged.
- Full test suite for the three components stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/43-polish-pass/43-05-SUMMARY.md`
</output>
