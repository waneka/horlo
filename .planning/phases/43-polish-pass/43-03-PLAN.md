---
phase: 43-polish-pass
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/profile/CollectionTabContent.tsx
  - src/components/profile/WishlistTabContent.tsx
  - tests/components/profile/CollectionTabContent.test.tsx
  - tests/components/profile/WishlistTabContent.test.tsx
autonomous: true
requirements: [PLSH-05]
must_haves:
  truths:
    - "The profile owner sees an 'Add to Collection' button above the populated collection grid"
    - "The profile owner sees an 'Add to Wishlist' button above the populated wishlist grid"
    - "Non-owner viewers see no add-watch button on either populated grid"
    - "The end-of-grid AddWatchCard tile is gone from both populated grids"
    - "Empty-state CTAs are unchanged"
    - "D-06: the end-of-grid AddWatchCard is replaced with a per-tab button ('Add to Collection' / 'Add to Wishlist')"
    - "D-07: the add-watch button is right-aligned within the existing filter-chips + search row above the grid"
    - "D-08: the empty-state CTA in CollectionTabContent / WishlistTabContent is left unchanged"
  artifacts:
    - path: "src/components/profile/CollectionTabContent.tsx"
      provides: "Right-aligned add-to-collection button in the filter row"
      contains: "Add to Collection"
    - path: "src/components/profile/WishlistTabContent.tsx"
      provides: "Right-aligned add-to-wishlist button above the grid"
      contains: "Add to Wishlist"
  key_links:
    - from: "CollectionTabContent filter row"
      to: "/watch/new"
      via: "Button asChild wrapping Link"
      pattern: "watch/new"
    - from: "WishlistTabContent header row"
      to: "/watch/new?status=wishlist"
      via: "Button asChild wrapping Link"
      pattern: "status=wishlist"
---

<objective>
Relocate the add-watch CTA from an end-of-grid `AddWatchCard` tile to a
right-aligned button above the grid in both the collection and wishlist tabs.

Purpose: PLSH-05 (D-06, D-07, D-08) — the add action should be a prominent
button above the grid, not a card lost at the end. Collection tab places it in
the existing filter-chips + search row; the wishlist tab gets a minimal
header row. Empty-state CTAs stay unchanged.
Output: Modified `CollectionTabContent.tsx` and `WishlistTabContent.tsx`,
plus tests asserting the populated grids show the button and no trailing
`AddWatchCard` tile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-polish-pass/43-CONTEXT.md
@.planning/phases/43-polish-pass/43-RESEARCH.md
@.planning/phases/43-polish-pass/43-PATTERNS.md
@.planning/phases/43-polish-pass/43-UI-SPEC.md

# AGENTS.md: this is a non-standard Next.js 16. Read the relevant Next.js
# guide in node_modules/next/dist/docs/ before writing routing-specific code.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "Add to Collection" button to CollectionTabContent (PLSH-05)</name>
  <files>src/components/profile/CollectionTabContent.tsx, tests/components/profile/CollectionTabContent.test.tsx</files>
  <read_first>
    - src/components/profile/CollectionTabContent.tsx (the file being modified — read the populated-state filter row, the grid block, the empty-state branches, and the current `AddWatchCard` usage and `isOwner`/`returnTo`/`pathname` variables)
    - src/components/profile/AddWatchCard.tsx (the component being relocated — confirm its collection-variant href and label so the new button matches)
    - .planning/phases/43-polish-pass/43-PATTERNS.md § "src/components/profile/CollectionTabContent.tsx" (exact target filter-row structure)
    - .planning/phases/43-polish-pass/43-UI-SPEC.md § "3. Add-Watch Button" (collection tab spec, button variant)
    - src/components/ui/button.tsx (confirm `asChild` is supported)
    - tests/components/profile/ (directory — check for an existing test file)
  </read_first>
  <action>
    In `src/components/profile/CollectionTabContent.tsx`, in the POPULATED-state
    filter row (`<div className="mb-4 flex items-center gap-2">` holding
    `FilterChips` and the search input), add an add-watch button AFTER the
    search-input `<div>`, gated by `isOwner` (per D-06/D-07). The button is
    `<Button asChild variant="default" size="sm" className="shrink-0 min-h-[44px]">`
    wrapping a `next/link` `<Link>` to `/watch/new` (append
    `?returnTo=${encodeURIComponent(pathname)}` when `pathname` is set, matching
    the existing `AddWatchCard` returnTo behavior), with label text
    "Add to Collection". The `min-h-[44px]` enforces the 44px touch target per
    UI-SPEC. Then REMOVE the `<AddWatchCard returnTo={...} />` element from the
    end of the populated grid `<div>` (the `{isOwner && <AddWatchCard ... />}`
    line inside the grid). Per D-08, DO NOT touch the empty-state branches —
    the `AddWatchCard` import stays and the empty-state CTA is unchanged.
    Confirm `Link` and `Button` are already imported (PATTERNS.md notes both
    are); add the import only if missing. Then create `tests/components/profile/CollectionTabContent.test.tsx`
    as a new file (no test file currently exists for this component): render the
    populated owner state (watches length > 0, `isOwner` true) and assert the
    "Add to Collection" button is present and links to `/watch/new`; assert no
    `AddWatchCard` tile renders at the end of the populated grid; and add a
    non-owner case asserting the button is absent.
  </action>
  <verify>
    <automated>npm test -- CollectionTabContent && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `CollectionTabContent.tsx` contains a `Button asChild` with label text `Add to Collection` inside the populated-state filter row, gated by `isOwner`
    - The button's `<Link>` href targets `/watch/new`
    - The button element carries class `min-h-[44px]`
    - The populated grid no longer renders `<AddWatchCard` (grep within the grid block shows it removed from the populated path)
    - The `AddWatchCard` import is still present (used by the empty state) and the empty-state branch is byte-unchanged
    - `tests/components/profile/CollectionTabContent.test.tsx` exists as a new file
    - `npm test -- CollectionTabContent` passes
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>The populated collection tab shows a right-aligned "Add to Collection" button visible only to the owner; the end-of-grid AddWatchCard tile is gone; empty state untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the "Add to Wishlist" button to WishlistTabContent (PLSH-05)</name>
  <files>src/components/profile/WishlistTabContent.tsx, tests/components/profile/WishlistTabContent.test.tsx</files>
  <read_first>
    - src/components/profile/WishlistTabContent.tsx (the file being modified — read the `OwnerWishlistGrid` sub-component, the `isOwner`/`returnTo`/`wishlistHref` variables, the `return <OwnerWishlistGrid ...>` branch, and the `AddWatchCard variant="wishlist"` placement inside the grid at the SortableContext end)
    - .planning/phases/43-polish-pass/43-PATTERNS.md § "src/components/profile/WishlistTabContent.tsx" (target header-row structure, AddWatchCard removal)
    - .planning/phases/43-polish-pass/43-UI-SPEC.md § "3. Add-Watch Button" (wishlist tab spec)
    - .planning/phases/43-polish-pass/43-RESEARCH.md § "Pitfall 5" (wishlist tab has no existing filter row — add a minimal header row)
  </read_first>
  <action>
    In `src/components/profile/WishlistTabContent.tsx`, the populated owner view
    (`OwnerWishlistGrid`) has no filter row. Add a minimal header row above the
    grid, gated by `isOwner`: a `<div className="mb-4 flex justify-end">`
    containing `<Button asChild variant="default" size="sm" className="shrink-0 min-h-[44px]">`
    wrapping a `next/link` `<Link>` to `/watch/new?status=wishlist` (append
    `&returnTo=${encodeURIComponent(...)}` when a returnTo path is available —
    reuse the already-derived `wishlistHref`/`returnTo` values), label
    "Add to Wishlist". Per PATTERNS.md, place this header row in
    `WishlistTabContent` immediately before the `return <OwnerWishlistGrid .../>`
    so `isOwner` is in scope without threading a new prop (wrap the existing
    return in a fragment with the header row above it). Then REMOVE the
    `<AddWatchCard variant="wishlist" returnTo={returnTo} />` element from inside
    `OwnerWishlistGrid` (the element after the `SortableContext`, currently the
    last child of the grid div) — per D-06 it leaves the populated grid. Per
    D-08, leave the `AddWatchCard` import and its empty-state usage unchanged.
    Do not change the non-owner populated branch. Then CREATE
    `tests/components/profile/WishlistTabContent.test.tsx` as a new file — no
    test file currently exists for this component; the component test directory
    mirrors `src/` under `tests/components/`, so component tests for
    `src/components/profile/*` live under `tests/components/profile/` (matching
    the `CollectionTabContent.test.tsx` sibling created in Task 1). In the new
    file: render the populated owner state and assert the "Add to Wishlist"
    button renders and links to a `/watch/new?status=wishlist` URL; assert no
    `AddWatchCard` tile renders at the end of the populated grid; and add a
    non-owner case asserting the button is absent.
  </action>
  <verify>
    <automated>npm test -- WishlistTabContent && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `WishlistTabContent.tsx` renders a `<div className="mb-4 flex justify-end">` header row containing a `Button asChild` with label text `Add to Wishlist`, gated by `isOwner`
    - The button's `<Link>` href contains `status=wishlist`
    - The button element carries class `min-h-[44px]`
    - `<AddWatchCard variant="wishlist"` no longer renders inside `OwnerWishlistGrid`'s populated grid
    - The `AddWatchCard` import remains and the owner empty-state usage is unchanged
    - `tests/components/profile/WishlistTabContent.test.tsx` exists as a new file with an owner case (button present) and a non-owner case (button absent)
    - `npm test -- WishlistTabContent` passes
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>The populated wishlist tab shows a right-aligned "Add to Wishlist" button above the grid visible only to the owner; the end-of-grid AddWatchCard tile is gone; empty state untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. Both components are client-side; the add-watch button is
a navigation link to an existing route.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Tampering | add-watch `<Link>` href | accept | `pathname`/`returnTo` is passed through `encodeURIComponent` before being placed in the query string — same handling as the existing `AddWatchCard`. The `/watch/new` route already validates `returnTo` server-side. No new attack surface. |
</threat_model>

<verification>
- `npm test -- CollectionTabContent` and `npm test -- WishlistTabContent` pass.
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Manual (43-VALIDATION.md): populated collection and wishlist tabs show the
  add button right-aligned above the grid; no trailing AddWatchCard tile;
  empty-state CTAs unchanged.
</verification>

<success_criteria>
- Both populated tabs show a right-aligned add-watch button above the grid, visible only to the owner (PLSH-05).
- The end-of-grid `AddWatchCard` tile is removed from both populated grids.
- Empty-state CTAs are byte-unchanged (D-08).
</success_criteria>

<output>
After completion, create `.planning/phases/43-polish-pass/43-03-SUMMARY.md`
</output>
