---
phase: 43-polish-pass
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/profile/ProfileWatchCard.tsx
  - tests/components/profile/ProfileWatchCard.test.tsx
  - src/components/settings/ProfileSection.tsx
  - src/app/settings/page.tsx
  - src/components/settings/SettingsTabsShell.tsx
  - tests/components/settings/ProfileSection.test.tsx
autonomous: true
gap_closure: true
requirements: [PLSH-04]
must_haves:
  truths:
    - "Each profile watch card is roughly 60px shorter because the image is 1:1, not portrait"
    - "Every card in a grid still reaches identical outer height regardless of metadata completeness"
    - "The Settings > Profile tab renders a working profile-edit form (display name, bio, avatar upload)"
    - "Saving the Settings profile form persists changes via the existing updateProfile Server Action"
    - "The 'Profile editing coming in the next update.' footer note is gone"
    - "The 'View public profile' link to /u/{username} is still present in Settings > Profile"
  artifacts:
    - path: "src/components/profile/ProfileWatchCard.tsx"
      provides: "Watch card with aspect-square image container"
      contains: "aspect-square"
    - path: "src/components/settings/ProfileSection.tsx"
      provides: "Functional profile-edit section reusing ProfileEditForm"
      contains: "ProfileEditForm"
    - path: "src/app/settings/page.tsx"
      provides: "Settings page threading bio + userId to the settings shell"
  key_links:
    - from: "src/components/settings/ProfileSection.tsx"
      to: "src/components/profile/ProfileEditForm.tsx"
      via: "component composition"
      pattern: "ProfileEditForm"
    - from: "src/app/settings/page.tsx"
      to: "src/components/settings/SettingsTabsShell.tsx"
      via: "bio + userId props"
      pattern: "userId="
    - from: "src/components/profile/ProfileEditForm.tsx"
      to: "src/app/actions/profile.ts"
      via: "updateProfile Server Action (reused unchanged)"
      pattern: "updateProfile"
---

<objective>
Close the two remaining Phase 43 UAT gaps from round 2 (GAP-43-04 and GAP-43-05).

Purpose: Finish the polish pass so the app feels consistent before v5.1's `/explore`
work. GAP-43-04 makes profile watch cards roughly 60px shorter by switching the
image to a 1:1 aspect ratio (the dominant remaining height driver after 43-05 trimmed
padding). GAP-43-05 turns the Settings > Profile tab from a non-functional read-only
stub into a working profile-edit surface by reusing the existing `ProfileEditForm`.

Output: A shorter `ProfileWatchCard`, an editable Settings Profile section, and
updated tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

This is a NON-STANDARD Next.js 16 codebase. Before writing any Next.js-specific
code (Server Components, data fetching, routing), read the relevant guide in
`node_modules/next/dist/docs/` — APIs differ from training data. See AGENTS.md.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-polish-pass/43-HUMAN-UAT.md
@.planning/phases/43-polish-pass/43-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs — extracted from the codebase, use directly. -->

ProfileEditForm (src/components/profile/ProfileEditForm.tsx) — REUSE UNCHANGED:
```typescript
interface ProfileEditFormProps {
  initial: {
    displayName: string | null
    avatarUrl: string | null
    bio: string | null
  }
  onDone: () => void
  userId: string
}
```
- ProfileEditForm is a `'use client'` component.
- It already wires the `updateProfile` Server Action and `AvatarUploader`
  (avatar upload to Supabase Storage, scoped to `userId`).
- `AvatarUploader` receives `userId` and uses it as the Storage folder — this
  MUST be the current authenticated user's id.

ProfileHeader (src/components/profile/ProfileHeader.tsx) — REFERENCE for wiring:
- Constructs `initial` from `{ displayName, avatarUrl, bio }`.
- Passes `userId={props.targetUserId}` (the owner's id).
- `onDone={() => setEditing(false)}` — closes the inline edit panel. In the
  Settings context there is no panel to close (the form is always visible).

getProfileById (src/data/profiles.ts) — returns the full `profiles` row
(`db.select().from(profiles)`), which includes `id`, `username`, `displayName`,
`avatarUrl`, and `bio`. The settings page already calls this; it currently only
reads `username`, `displayName`, `avatarUrl` off the result.

SettingsTabsShellProps (src/components/settings/SettingsTabsShell.tsx) — currently
has `username`, `displayName`, `avatarUrl`, `profilePublic`, `currentEmail`,
`pendingNewEmail`, `lastSignInAt`, `settings`, `preferences`. NEEDS two new
fields threaded through to ProfileSection: `bio` and `userId`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch ProfileWatchCard image to a square aspect ratio (GAP-43-04)</name>
  <files>src/components/profile/ProfileWatchCard.tsx, tests/components/profile/ProfileWatchCard.test.tsx</files>
  <read_first>
    - src/components/profile/ProfileWatchCard.tsx — the component being modified
      (the image `<div>` at line 68 currently uses `aspect-[3/4]`).
    - tests/components/profile/ProfileWatchCard.test.tsx — the test file; note the
      image-container probe at lines 122-125 finds the div by `className.includes('aspect-')`.
  </read_first>
  <action>
    In `src/components/profile/ProfileWatchCard.tsx`, change the image container
    `<div>` className from `relative aspect-[3/4] bg-muted` to
    `relative aspect-square bg-muted`. This is the only line that changes — the
    aspect class stays on the IMAGE `<div>` only and MUST NEVER move to the
    `<Card>`. Do NOT touch the equal-height CSS chain: the `<Card>` keeps
    `h-full flex flex-col`, `<CardContent>` keeps `flex-1`. Per GAP-43-04 / D-04
    this 1:1 ratio shortens each card by roughly 60px and is the user-decided fix.

    In `tests/components/profile/ProfileWatchCard.test.tsx`, update the stale
    comment at line 122 that says `aspect-[3/4]` to say `aspect-square`. Then add
    one new test that explicitly asserts the image container has the
    `aspect-square` class and does NOT have `aspect-[3/4]`, and asserts the
    equal-height chain is intact: the `<Card>` element carries `h-full`,
    `flex`, and `flex-col`, and the `<CardContent>` carries `flex-1`. Locate the
    image container the same way the existing layout test does (the only `<div>`
    whose className includes `aspect-`).
  </action>
  <verify>
    <automated>npx vitest run tests/components/profile/ProfileWatchCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/profile/ProfileWatchCard.tsx` image container className
      contains `aspect-square` and does NOT contain `aspect-[3/4]`.
    - The `<Card>` still has `h-full flex flex-col`; `<CardContent>` still has `flex-1`.
    - No `aspect-*` class appears on the `<Card>` element.
    - A test asserts `aspect-square` present, `aspect-[3/4]` absent, and the
      equal-height chain intact; the full ProfileWatchCard test suite passes.
  </acceptance_criteria>
  <done>Profile watch cards render with a 1:1 image, are ~60px shorter, equal-height behavior preserved, tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the Settings ProfileSection stub with the real ProfileEditForm (GAP-43-05)</name>
  <files>src/components/settings/ProfileSection.tsx, src/app/settings/page.tsx, src/components/settings/SettingsTabsShell.tsx, tests/components/settings/ProfileSection.test.tsx</files>
  <read_first>
    - src/components/settings/ProfileSection.tsx — the read-only stub being replaced.
    - src/components/profile/ProfileEditForm.tsx — the existing edit form to reuse;
      note its required props `initial` (displayName, avatarUrl, bio), `onDone`, `userId`.
    - src/components/profile/ProfileHeader.tsx — the analog showing how
      `ProfileEditForm` is currently wired (lines 33-48): how `initial` is
      constructed, that `userId` is the owner's id, and what `onDone` does.
    - src/components/settings/SettingsTabsShell.tsx — passes props to ProfileSection
      (lines 142-148); `SettingsTabsShellProps` interface at lines 40-53.
    - src/app/settings/page.tsx — server component fetching `getProfileById(userId)`
      and rendering `SettingsTabsShell`.
    - tests/components/settings/ProfileSection.test.tsx — the test asserting the
      old stub markup.
  </read_first>
  <action>
    Rewrite `src/components/settings/ProfileSection.tsx` so the section body renders
    the existing `ProfileEditForm` instead of the read-only stub. Add a `'use client'`
    directive (the form is interactive). Extend `ProfileSectionProps` with `bio:
    string | null` and `userId: string` (keep `username`, `displayName`, `avatarUrl`).
    Keep the `SettingsSection` wrapper with `title="Profile"`. Inside it: keep the
    "View public profile" `Button` linking to `/u/{username}` (the existing
    `variant="outline"` `size="sm"` button with `render={<Link href={...} />}`).
    Render `ProfileEditForm` with `initial={{ displayName, avatarUrl, bio }}`,
    `userId={userId}`, and an `onDone` handler. For `onDone`: there is no edit modal
    to close in the always-visible settings form — call `useRouter().refresh()` from
    `next/navigation` so server-fetched profile data re-renders after a save (this is
    the simplest correct behavior; a bare no-op `() => {}` is also acceptable, but
    prefer the refresh so a fresh page load shows the saved values). REMOVE the
    "Profile editing coming in the next update." footer `<p>` entirely. The old
    inline avatar `<img>` / `bg-muted` placeholder block is no longer needed —
    `ProfileEditForm`'s `AvatarUploader` owns the avatar UI now; drop it. Update the
    Phase 22 D-19 docblock comment to reflect the new behavior (no longer a stub).

    In `src/components/settings/SettingsTabsShell.tsx`, add `bio: string | null` and
    `userId: string` to `SettingsTabsShellProps`, and pass `bio={props.bio}` and
    `userId={props.userId}` into the `<ProfileSection>` element in the `profile`
    `TabsContent`.

    In `src/app/settings/page.tsx`, the page already has `userId` (from
    `fullUser.id`) and already calls `getProfileById(userId)`. Pass two new props to
    `<SettingsTabsShell>`: `userId={userId}` and `bio={profile?.bio ?? null}`. The
    `profiles` row returned by `getProfileById` already includes `bio` — no new
    query needed. SECURITY: `userId` here is `fullUser.id` from
    `getCurrentUserFull()` — the authenticated current user. Settings is always a
    self-edit surface, so this id (which `AvatarUploader` uses as its Storage folder)
    is correctly the current user's id; do NOT derive it from any client-supplied
    value.

    Rewrite `tests/components/settings/ProfileSection.test.tsx` to match the new
    behavior: pass the new `bio` and `userId` props in renders. Assert that the
    "Profile editing coming in the next update." string is NO LONGER present
    (`queryByText` returns null). Assert the "View public profile" link to
    `/u/{username}` is still rendered. Assert `ProfileEditForm`'s fields are present
    — e.g. the "Display name" label/input and the "Bio" label/textarea render (these
    are stable, user-facing labels from ProfileEditForm). Remove the old stub-only
    assertions (avatar `<img>` / `bg-muted` placeholder, footer-note test). If
    `ProfileEditForm` pulls in modules that need mocking under jsdom (e.g.
    `AvatarUploader` / Server Action import), mock them at the test boundary the same
    way other settings-suite tests mock interactive children — keep the test focused
    on ProfileSection's own markup.
  </action>
  <verify>
    <automated>npx vitest run tests/components/settings/ProfileSection.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/settings/ProfileSection.tsx` renders `ProfileEditForm` and has
      a `'use client'` directive.
    - The string "Profile editing coming in the next update." appears nowhere in
      `ProfileSection.tsx`.
    - The "View public profile" link to `/u/{username}` is still rendered.
    - `ProfileSectionProps` includes `bio` and `userId`; `SettingsTabsShellProps`
      includes `bio` and `userId`; `settings/page.tsx` passes both (with `userId`
      sourced from `fullUser.id`).
    - No new mutation path is introduced — `updateProfile` is reached only via the
      unchanged `ProfileEditForm`.
    - `tests/components/settings/ProfileSection.test.tsx` asserts the footer note is
      gone and `ProfileEditForm` fields render; the test passes.
    - `npx tsc --noEmit` reports no type errors.
  </acceptance_criteria>
  <done>Settings > Profile tab shows a working edit form (display name, bio, avatar upload); footer stub note removed; "View public profile" link kept; saving persists via the existing updateProfile action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action (`updateProfile`) | Settings Profile form now submits display name + bio mutations |
| client → Supabase Storage (`avatars` bucket) | `AvatarUploader` uploads an image scoped to `{userId}/...` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-07-01 | Elevation of Privilege | Settings ProfileSection → ProfileEditForm `userId` prop | mitigate | `userId` is sourced from `fullUser.id` (`getCurrentUserFull()`) in `settings/page.tsx`, never from client input. Settings is a self-edit-only surface; the id passed to `AvatarUploader` for the Storage folder is the authenticated current user. Asserted in Task 2 acceptance criteria. |
| T-43-07-02 | Tampering | `updateProfile` Server Action | accept | Mitigation already exists and is unchanged — `ProfileEditForm` is reused as-is; `updateProfile` enforces auth + ownership server-side with a strict input schema. No new mutation path is introduced by this plan. |
| T-43-07-03 | Tampering | Supabase Storage `avatars` bucket upload | accept | Folder-scoped RLS policies (`avatars_insert_own_folder`, `avatars_select_own_folder` — the latter added by 43-06) enforce `{userId}` ownership server-side. `AvatarUploader` is reused unchanged. |
| T-43-07-04 | (none) | ProfileWatchCard aspect change | accept | GAP-43-04 is a pure CSS class change (`aspect-[3/4]` → `aspect-square`) — no new attack surface, no input, no data flow. |
</threat_model>

<verification>
- `npx vitest run tests/components/profile/ProfileWatchCard.test.tsx` — passes;
  asserts `aspect-square` and equal-height chain.
- `npx vitest run tests/components/settings/ProfileSection.test.tsx` — passes;
  asserts footer note removed and `ProfileEditForm` rendered.
- `npx tsc --noEmit` — no type errors after the prop additions.
- Manual smoke (optional): `/settings#profile` shows an editable form; editing the
  display name + bio and saving persists; `/u/{username}` still reachable via the link.
</verification>

<success_criteria>
- GAP-43-04 closed: profile watch card image is `aspect-square`, cards ~60px
  shorter, equal-height behavior preserved (PLSH-04).
- GAP-43-05 closed: Settings > Profile tab is a functional profile-edit surface
  reusing `ProfileEditForm`; "coming in the next update" note removed; "View public
  profile" link retained; no new mutation path.
- Both tests updated and green; `tsc` clean.
</success_criteria>

<output>
After completion, create `.planning/phases/43-polish-pass/43-07-SUMMARY.md`
</output>
