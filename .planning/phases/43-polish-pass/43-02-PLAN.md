---
phase: 43-polish-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/profile/ProfileWatchCard.tsx
  - tests/components/profile/ProfileWatchCard.test.tsx
autonomous: true
requirements: [PLSH-03, PLSH-04]
must_haves:
  truths:
    - "Wishlist and grail watch cards show no wear badge and no last-worn line"
    - "Owned watch cards still show the wear badge and last-worn line"
    - "Brand and model appear above the watch image"
    - "Every card in a grid row has identical outer height regardless of metadata or photo"
    - "D-04: ProfileWatchCard is restructured — brand+model above image, ~4/5-aspect image, flex-1 text block below holding the remaining fields"
    - "D-05: every card in a given grid has identical outer height regardless of metadata completeness or photo presence"
    - "D-12: ProfileWatchCard renders no wear details (badge + last-worn line) for wishlist/grail watches"
  artifacts:
    - path: "src/components/profile/ProfileWatchCard.tsx"
      provides: "Restructured equal-height watch card with status-gated wear UI"
      contains: "isWishlistLike"
  key_links:
    - from: "src/components/profile/ProfileWatchCard.tsx Card"
      to: "equal-height grid layout"
      via: "h-full flex flex-col on Card, flex-1 on text block"
      pattern: "h-full flex flex-col"
    - from: "wear badge + last-worn line"
      to: "watch.status"
      via: "!isWishlistLike gate"
      pattern: "!isWishlistLike"
---

<objective>
Restructure `ProfileWatchCard` so brand + model sit above the image, the
remaining fields live in a flex-1 text block below, and every card in a grid
row has identical height. Suppress all wear UI (badge + last-worn line) for
wishlist and grail watches.

Purpose: PLSH-04 — cards currently vary in height with metadata completeness
and photo presence. PLSH-03 — wishlist cards must show no wear details (D-12).
Output: Modified `ProfileWatchCard.tsx` and a new test file covering wear
suppression and the brand/model-above-image layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-polish-pass/43-CONTEXT.md
@.planning/phases/43-polish-pass/43-RESEARCH.md
@.planning/phases/43-polish-pass/43-PATTERNS.md
@.planning/phases/43-polish-pass/43-UI-SPEC.md

# AGENTS.md: this is a non-standard Next.js 16. Read the relevant guide in
# node_modules/next/dist/docs/ before writing any next/image-specific code.

# Project memory flags an aspect-ratio / object-fit CSS-chain blind spot:
# the 6-pillar checker validated declared tokens but not whether the CSS chain
# produced the claimed visual contract. This plan's acceptance criteria assert
# the CSS chain explicitly.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure ProfileWatchCard layout and gate wear UI (PLSH-03, PLSH-04)</name>
  <files>src/components/profile/ProfileWatchCard.tsx, tests/components/profile/ProfileWatchCard.test.tsx</files>
  <read_first>
    - src/components/profile/ProfileWatchCard.tsx (the file being modified — read the full current layout, the derived values block, and the existing `isWishlistLike` computation)
    - .planning/phases/43-polish-pass/43-PATTERNS.md § "src/components/profile/ProfileWatchCard.tsx" (full target structure with exact Tailwind classes)
    - .planning/phases/43-polish-pass/43-UI-SPEC.md § "2. ProfileWatchCard — Restructured" (layout structure, equal-height contract, wear suppression, CSS-chain assertions)
    - .planning/phases/43-polish-pass/43-RESEARCH.md § "Pattern 2" and "Pitfall 2" (aspect-ratio-on-Card pitfall, grid equal-height mechanics)
    - src/components/ui/card.tsx (Card / CardContent component — confirm they forward className)
    - tests/components/profile/ (directory — check for an existing ProfileWatchCard test file)
  </read_first>
  <behavior>
    - Test 1: a watch with `status='wishlist'` renders NO wear badge ("Worn today" / "Not worn recently" absent) and NO last-worn line.
    - Test 2: a watch with `status='grail'` renders no wear badge and no last-worn line.
    - Test 3: a watch with `status='collection'` (owned) that is worn today renders the "Worn today" badge AND a last-worn line.
    - Test 4: the rendered card places the brand and model text before the image container in DOM order.
    - Test 5: a watch with no photo renders the WatchIcon placeholder, not a broken image.
  </behavior>
  <action>
    Restructure the `ProfileWatchCard` return JSX (per D-04, D-05, D-12 and the
    43-PATTERNS.md target). Keep ALL existing derived values unchanged:
    `safeUrl`, `days`, `isWornToday`, `isStale`, `lastWornLabel`, `tag`,
    `isWishlistLike`, `primary`, `primaryLabel`, `priceLine`. Keep the imports
    and the `ProfileWatchCardProps` interface unchanged.
    New structure inside the existing `<Link href={...}>`:
    1. Outer `<Card>` gets `className="group cursor-pointer overflow-hidden transition-shadow hover:shadow-lg h-full flex flex-col"`.
       The `h-full flex flex-col` is the equal-height key — do NOT put any
       `aspect-*` class on the Card.
    2. Brand/model header `<div className="px-3 pt-3 pb-1">` ABOVE the image:
       a `<p className="text-sm font-normal text-muted-foreground truncate">`
       for `watch.brand`, then a `<p className="text-base font-semibold leading-tight truncate">`
       for `watch.model`.
    3. Image container `<div className="relative aspect-[3/4] bg-muted">` —
       the `aspect-[3/4]` class goes on THIS div only. Render `<Image src={safeUrl} ... fill className="object-cover" />`
       when `safeUrl` is set, else the `<WatchIcon className="size-10 text-muted-foreground/40" />`
       placeholder centered in a full-size flex div. The `object-cover` class
       MUST be present on `<Image>`.
    4. Wear badge — absolutely positioned inside the image container, rendered
       ONLY when `!isWishlistLike && (isWornToday || isStale)`. Keep the
       existing badge classNames (`bg-accent text-accent-foreground` for worn
       today; `bg-background text-foreground shadow ring-1 ring-border` for
       stale) and `absolute top-2 left-2`.
    5. Text block `<CardContent className="p-3 flex flex-col gap-1 flex-1">` —
       use `flex-1` here, NOT a fixed `min-h`. D-04 phrases this as a
       "fixed / min-height text block", but `flex-1` deliberately replaces
       `min-h` as the equal-height mechanism: per RESEARCH.md Pitfall 2, a fixed
       `min-h` only sets a per-card floor and does NOT equalize cards across a
       grid row, whereas `flex-1` on the text block (combined with
       `h-full flex flex-col` on the Card) makes the block absorb the grid row's
       height uniformly. This is a documentation note — the technical approach
       is `flex-1`. Render in order: the tag `<Badge>` if `tag` is set; the
       last-worn line `<p className="text-xs text-muted-foreground">{lastWornLabel}</p>`
       ONLY when `!isWishlistLike`; the `priceLine` `<p>` if set; the wishlist
       notes `<p>` when `showWishlistMeta && watch.notes`.
    Per D-12 the `!isWishlistLike` gate suppresses BOTH the badge AND the
    last-worn line. Aspect ratio is `aspect-[3/4]` (D-04 Claude's discretion,
    UI-SPEC default). Then create `tests/components/profile/ProfileWatchCard.test.tsx`
    covering the five behaviors above.
  </action>
  <verify>
    <automated>npm test -- ProfileWatchCard && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - In `ProfileWatchCard.tsx`, the outer `<Card>` has the literal classes `h-full flex flex-col` and has NO `aspect-` class
    - The image-container `<div>` has the literal class `aspect-[3/4]` and is the ONLY element with an `aspect-` class
    - `<Image>` has the literal class `object-cover`
    - The text-block `<CardContent>` has class `flex-1` and NO `min-h-` class — `flex-1` is the deliberate equal-height mechanism replacing `min-h` per RESEARCH.md Pitfall 2 (D-04's "min-height text block")
    - Brand `<p>` and model `<p>` appear in source BEFORE the image-container `<div>`
    - The wear badge JSX and the last-worn-line `<p>` are both wrapped in a `!isWishlistLike` condition
    - `npm test -- ProfileWatchCard` passes all five behavior tests
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>ProfileWatchCard has brand/model above the image, a flex-1 text block, equal-height cards, and no wear UI on wishlist/grail watches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. `ProfileWatchCard` is a pure client-side presentational
component rendering data already loaded into the page.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | Information Disclosure | `ProfileWatchCard.tsx` | accept | No new attack surface — the restructure renders the same `watch` fields already passed to the component. Wear-UI suppression on wishlist cards is a display choice, not a security control. |
</threat_model>

<verification>
- `npm test -- ProfileWatchCard` passes.
- `npx tsc --noEmit` clean.
- Manual CSS-chain assertion (43-UI-SPEC.md § CSS-Chain Assertions, project
  memory blind spot): at 375px viewport, render the collection grid with four
  cards of mixed metadata (tag+wear+price / tag only / no metadata / no photo);
  all four in a row must have identical total height.
</verification>

<success_criteria>
- Wishlist and grail cards render no wear badge and no last-worn line (PLSH-03).
- Brand + model render above the image.
- Cards in a grid row are identical height regardless of metadata/photo (PLSH-04).
- `aspect-[3/4]` is on the image div only; `object-cover` is on `<Image>`.
</success_criteria>

<output>
After completion, create `.planning/phases/43-polish-pass/43-02-SUMMARY.md`
</output>
