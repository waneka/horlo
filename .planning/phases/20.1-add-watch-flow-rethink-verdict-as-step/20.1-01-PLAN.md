---
phase: 20.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/watch/AddWatchFlow.test.tsx
  - src/components/watch/PasteSection.test.tsx
  - src/components/watch/VerdictStep.test.tsx
  - src/components/watch/WishlistRationalePanel.test.tsx
  - src/components/watch/RecentlyEvaluatedRail.test.tsx
  - src/components/profile/AddWatchCard.test.tsx
  - src/components/profile/WishlistTabContent.test.tsx
  - src/components/search/WatchSearchRowsAccordion.test.tsx
  - tests/app/watch-new-page.test.ts
  - tests/integration/add-watch-flow-search-cta.test.ts
autonomous: true
requirements:
  - ADD-01
  - ADD-02
  - ADD-03
  - ADD-04
  - ADD-05
  - ADD-06
  - ADD-07
tags:
  - tests
  - state-machine
  - react-testing-library

must_haves:
  truths:
    - "Every Wave 1+ test command in 20.1-VALIDATION.md has a corresponding RED test file on disk"
    - "Each scaffolded test imports the component-under-test or route-under-test (forces a NotImplemented compile/runtime error until later waves create the source files)"
    - "No scaffolded test is marked `.skip` — all are RED-failing assertions that go GREEN when downstream tasks ship"
    - "The existing extract-route integration test gains a new `it()` block that asserts `body.catalogId` exists in the response"
    - "The existing catalog-page test gains a new `it()` block that asserts the 3 CTAs (Wishlist / Collection / Skip) render in non-self-via-cross-user framing"
  artifacts:
    - path: "src/components/watch/AddWatchFlow.test.tsx"
      provides: "RED tests for ADD-01, ADD-03, ADD-04, ADD-07 state-machine transitions"
      contains: "describe('Phase 20.1 Plan 04 — AddWatchFlow"
    - path: "src/components/watch/PasteSection.test.tsx"
      provides: "RED tests for ADD-05 manual-entry link + URL input behavior"
      contains: "describe('Phase 20.1 Plan 03 — PasteSection"
    - path: "src/components/watch/VerdictStep.test.tsx"
      provides: "RED tests for verdict-ready render: spec preview + CollectionFitCard + 3 buttons + D-06 empty-collection"
      contains: "describe('Phase 20.1 Plan 03 — VerdictStep"
    - path: "src/components/watch/WishlistRationalePanel.test.tsx"
      provides: "RED tests for ADD-02 textarea pre-fill + commit shape + Pitfall 5 blank-notes preservation"
      contains: "describe('Phase 20.1 Plan 03 — WishlistRationalePanel"
    - path: "src/components/watch/RecentlyEvaluatedRail.test.tsx"
      provides: "RED tests for D-14 FIFO eviction + click-to-reopen"
      contains: "describe('Phase 20.1 Plan 03 — RecentlyEvaluatedRail"
    - path: "src/components/profile/AddWatchCard.test.tsx"
      provides: "RED tests for D-15 text + aria-label change"
      contains: "describe('Phase 20.1 Plan 02 — AddWatchCard"
    - path: "src/components/profile/WishlistTabContent.test.tsx"
      provides: "RED tests for D-16 end-of-grid CTA when isOwner"
      contains: "describe('Phase 20.1 Plan 02 — WishlistTabContent"
    - path: "src/components/search/WatchSearchRowsAccordion.test.tsx"
      provides: "RED tests for ADD-06 inline 3 CTAs in Accordion.Panel + stopPropagation behavior"
      contains: "describe('Phase 20.1 Plan 05 — WatchSearchRowsAccordion CTAs"
    - path: "tests/app/watch-new-page.test.ts"
      provides: "RED tests for Pitfall 1 — Server Component awaits searchParams; intent=owned whitelist"
      contains: "describe('Phase 20.1 Plan 04 — /watch/new searchParams"
    - path: "tests/integration/add-watch-flow-search-cta.test.ts"
      provides: "RED test for ADD-06 end-to-end — search accordion Wishlist CTA → addWatch invocation"
      contains: "describe('Phase 20.1 Plan 05 — /search accordion Wishlist commit"
  key_links:
    - from: "Wave 0 test scaffolds"
      to: "Wave 1+ component implementations"
      via: "imports component-under-test from @/components/watch/* etc"
      pattern: "from '@/components/(watch|profile|search)/"
    - from: "Wave 0 extension to phase17-extract-route-wiring.test.ts"
      to: "Plan 02 D-08 implementation"
      via: "asserts body.catalogId in response"
      pattern: "body\\.catalogId"

user_setup: []
---

<objective>
Stand up RED-failing test scaffolds that cover every ADD-01..ADD-07 acceptance criterion plus the supporting D-04, D-05, D-08, D-15, D-16, D-18 decisions and Pitfalls 1, 2, 3, 5. Every Wave 1+ task has a `<verify>` block that runs one of these tests; this plan exists so those `<verify>` blocks aren't `MISSING — Wave 0 must create…` placeholders.

Purpose: Per Nyquist Rule from VALIDATION.md, every task in waves 1+ must have an automated `<verify>` command. This plan creates the test files so those commands exist.
Output: Ten test files (8 NEW, 2 EXTEND) — all RED, all autonomous, all run via `npx vitest run <file>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-add-watch-flow-rethink-verdict-as-step/20.1-CONTEXT.md
@.planning/phases/20.1-add-watch-flow-rethink-verdict-as-step/20.1-RESEARCH.md
@.planning/phases/20.1-add-watch-flow-rethink-verdict-as-step/20.1-VALIDATION.md
@.planning/phases/20.1-add-watch-flow-rethink-verdict-as-step/20.1-UI-SPEC.md

# Existing patterns to mirror in test scaffolds
@src/components/insights/CollectionFitCard.test.tsx
@tests/integration/phase17-extract-route-wiring.test.ts
@tests/app/catalog-page.test.ts

# Source files that will be created in later waves (referenced by tests)
@src/components/insights/CollectionFitCard.tsx
@src/components/insights/VerdictSkeleton.tsx
@src/components/search/useWatchSearchVerdictCache.ts
@src/lib/verdict/types.ts
@src/lib/extractors/types.ts

<interfaces>
<!-- Types and contracts the test scaffolds must reference. Tests imports these
     and reference downstream component-under-test by absolute path. -->

From src/lib/verdict/types.ts (Phase 20 lock — do NOT modify):
```typescript
export type Framing = 'same-user' | 'cross-user' | 'self-via-cross-user'
export interface VerdictBundleFull {
  framing: 'same-user' | 'cross-user'
  label: SimilarityLabel
  headlinePhrasing: string
  contextualPhrasings: string[]
  mostSimilar: VerdictMostSimilar[]
  roleOverlap: boolean
}
export interface VerdictBundleSelfOwned {
  framing: 'self-via-cross-user'
  ownedAtIso: string
  ownerHref: string
}
export type VerdictBundle = VerdictBundleFull | VerdictBundleSelfOwned
```

From src/lib/extractors/types.ts:
```typescript
export interface ExtractedWatchData {
  brand?: string
  model?: string
  reference?: string
  movement?: MovementType
  caseSizeMm?: number
  // ... see file for full shape
}
export interface ExtractionResult {
  data: ExtractedWatchData
  source: 'structured' | 'html' | 'llm' | 'merged'
  confidence: 'high' | 'medium' | 'low'
  fieldsExtracted: string[]
  llmUsed: boolean
}
```

Component contracts the test scaffolds will assert against (defined by later waves):

`<AddWatchFlow collectionRevision={number} initialCatalogId={string|null} initialIntent={'owned'|null} initialCatalogPrefill={ExtractedWatchData|null}>` — Plan 04
`<PasteSection url={string} onUrlChange={(u:string)=>void} onExtract={()=>void} onManualEntry={()=>void} pending={boolean} disabled={boolean}>` — Plan 03
`<VerdictStep extracted={ExtractedWatchData} verdict={VerdictBundle|null} pending={boolean} pendingTarget={'wishlist'|'collection'|'skip'|null} onWishlist={()=>void} onCollection={()=>void} onSkip={()=>void}>` — Plan 03
`<WishlistRationalePanel verdict={VerdictBundle|null} initialNotes={string} onConfirm={(notes:string)=>void} onCancel={()=>void} pending={boolean}>` — Plan 03
`<RecentlyEvaluatedRail entries={RailEntry[]} onSelect={(entry:RailEntry)=>void}>` — Plan 03
`<AddWatchCard variant?: 'collection'|'wishlist'>` — Plan 02 (D-15 + D-16 shared variant)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold component-level RED tests (5 watch-flow files + 2 profile files)</name>
  <files>src/components/watch/AddWatchFlow.test.tsx, src/components/watch/PasteSection.test.tsx, src/components/watch/VerdictStep.test.tsx, src/components/watch/WishlistRationalePanel.test.tsx, src/components/watch/RecentlyEvaluatedRail.test.tsx, src/components/profile/AddWatchCard.test.tsx, src/components/profile/WishlistTabContent.test.tsx</files>
  <read_first>
    - src/components/insights/CollectionFitCard.test.tsx (RTL test pattern to mirror — vi.mock for next/link, render + screen.getByText assertions, buildWatch helper)
    - src/lib/verdict/types.ts (VerdictBundle / VerdictBundleFull / VerdictBundleSelfOwned shapes used in test fixtures)
    - src/lib/extractors/types.ts (ExtractedWatchData shape used in test fixtures)
    - src/components/profile/AddWatchCard.tsx (current "Add Watch" text + aria-label="Add Watch" — D-15 changes both)
    - src/components/profile/WishlistTabContent.tsx (current shape; D-16 adds end-of-grid card when isOwner)
    - vitest.config.ts (verify the existing alias pattern — server-only shim, jsdom env)
    - .planning/phases/20.1-add-watch-flow-rethink-verdict-as-step/20.1-VALIDATION.md (test names + commands MUST match the table verbatim)
    - .planning/phases/20.1-add-watch-flow-rethink-verdict-as-step/20.1-UI-SPEC.md §State Machine + §3-Button Visual Hierarchy + §Copywriting Contract (assertions reference these copy strings)
  </read_first>
  <action>
    Create 7 RED test files. Each file MUST:
    - Use `import { describe, it, expect, vi } from 'vitest'` and `import { render, screen, fireEvent } from '@testing-library/react'`
    - Mock `next/link` and `next/navigation` where needed using the same factory pattern as `src/components/insights/CollectionFitCard.test.tsx`
    - Import the component-under-test from its expected absolute path (`@/components/watch/AddWatchFlow`, etc.) — these imports WILL fail in Wave 0 because the source files don't exist yet; that is the desired RED state
    - Use `describe('Phase 20.1 Plan {NN} — {ComponentName}', () => { … })` naming so the failure breadcrumbs back to the responsible plan
    - Mark NO test with `.skip` — every test must be a real assertion that flips GREEN when the source ships

    File-by-file requirements:

    **`src/components/watch/AddWatchFlow.test.tsx`** (covers ADD-01, ADD-03, ADD-04, ADD-07):
    - Import: `import { AddWatchFlow } from '@/components/watch/AddWatchFlow'`
    - Mock: `vi.mock('@/app/actions/verdict', () => ({ getVerdictForCatalogWatch: vi.fn() }))`, `vi.mock('@/app/actions/watches', () => ({ addWatch: vi.fn() }))`, `vi.mock('next/navigation', () => ({ useRouter: () => ({ push: vi.fn(), refresh: vi.fn(), back: vi.fn() }) }))`
    - Mock fetch globally: `global.fetch = vi.fn()` and use `vi.mocked(fetch).mockResolvedValue(...)` per test
    - Test 1 (ADD-01 happy path): mock `/api/extract-watch` to return `{ success: true, catalogId: 'cat-uuid', data: { brand: 'Omega', model: 'Speedmaster' }, ... }`; mock verdict action to return `{ success: true, data: <VerdictBundleFull fixture> }`; render `<AddWatchFlow collectionRevision={3} initialCatalogId={null} initialIntent={null} initialCatalogPrefill={null} />`; type URL into input by `screen.getByPlaceholderText(/Paste a product page URL/i)`; click `screen.getByRole('button', { name: 'Extract Watch' })`; assert `screen.getByText('Working...')` appears, then `screen.findByText('Add to Wishlist')` and `screen.getByText('Add to Collection')` and `screen.getByText('Skip')` all exist after the round-trip
    - Test 2 (ADD-03 Collection path): from verdict-ready, click "Add to Collection"; assert WatchForm-style fields render (brand input prefilled with "Omega"); assert no `<select>` for status with options visible (status locked to 'owned' — should be hidden or read-only)
    - Test 3 (ADD-04 Skip path): from verdict-ready, click "Skip"; assert state resets to idle (URL input visible again, value cleared); assert a "Recently evaluated" chip with text "Omega Speedmaster" appears below input
    - Test 4 (ADD-07 extraction-failed): mock fetch to return `{ ok: false, status: 500 }` with body `{ error: 'Failed to extract watch data from URL.' }`; click Extract; assert `screen.getByText("Extraction didn't work")` and `screen.getByText('Continue manually')` and `screen.getByRole('alert')` all render
    - Test 5 (Pitfall 1 deep-link prefill): pass `initialCatalogId='cat-deep'`, `initialIntent='owned'`, `initialCatalogPrefill={{ brand: 'Rolex', model: 'Submariner' }}`; assert flow advances directly to `form-prefill` state on mount (no paste, no extracting); assert WatchForm-style brand input shows "Rolex"

    **`src/components/watch/PasteSection.test.tsx`** (covers ADD-05):
    - Import: `import { PasteSection } from '@/components/watch/PasteSection'`
    - Test 1 (URL input change): render with `onUrlChange` spy; type into input; assert spy called with typed string
    - Test 2 (Extract button click): render with `url='https://example.com/watch'` + `onExtract` spy; click button; assert spy called once
    - Test 3 (ADD-05 manual-entry link click): render with `onManualEntry` spy; click `screen.getByText(/or enter manually/i)`; assert spy called once; assert `onExtract` was NOT called
    - Test 4 (disabled during extraction): render with `pending={true}`; assert input has `disabled` attribute and Extract button shows "Working..."

    **`src/components/watch/VerdictStep.test.tsx`** (covers verdict-ready visuals + D-06):
    - Import: `import { VerdictStep } from '@/components/watch/VerdictStep'`
    - Mock: `vi.mock('@/components/insights/CollectionFitCard', () => ({ CollectionFitCard: ({ verdict }: any) => <div data-testid="cfc">{verdict.headlinePhrasing}</div> }))`, `vi.mock('next/image', () => ({ default: (p: any) => <img src={p.src} alt={p.alt} /> }))`
    - Test 1 (verdict + 3 buttons render): render with `extracted={{ brand: 'Omega', model: 'Speedmaster', imageUrl: 'https://x/y.jpg' }}` + `verdict={<VerdictBundleFull fixture with headlinePhrasing='Core Fit'>}`; assert `screen.getByText('Omega Speedmaster')`; assert `screen.getByTestId('cfc')` shows 'Core Fit'; assert all 3 buttons render with names "Add to Wishlist", "Add to Collection", "Skip"
    - Test 2 (D-06 empty-collection): render with `verdict={null}`; assert `<CollectionFitCard>` is NOT rendered (no `cfc` testId); assert text "Your collection is empty — fit score not available yet." is present; assert all 3 buttons still render
    - Test 3 (3-button click handlers): render with all 3 spies; click each button; assert each spy called exactly once
    - Test 4 (pending state on Wishlist): render with `pending={true}` and `pendingTarget='wishlist'`; assert Wishlist button text shows "Saving..." and all 3 buttons have disabled attribute

    **`src/components/watch/WishlistRationalePanel.test.tsx`** (covers ADD-02 + Pitfall 5):
    - Import: `import { WishlistRationalePanel } from '@/components/watch/WishlistRationalePanel'`
    - Test 1 (textarea pre-fill from verdict): render with `verdict={<VerdictBundleFull fixture with contextualPhrasings=['Aligns with my heritage-driven taste — overlaps strongly with Submariner']>}` and `initialNotes={''}`; assert textarea value contains "Aligns with my heritage-driven taste"
    - Test 2 (Pitfall 5 — blank textarea preserves blank): render with verdict pre-fill; clear textarea via `fireEvent.change(textarea, { target: { value: '' } })`; click "Save to Wishlist"; assert `onConfirm` called with `''` (NOT the verdict copy)
    - Test 3 (edit + confirm): pre-fill, change textarea to "my custom note", click confirm; assert `onConfirm` called with "my custom note"
    - Test 4 (cancel): click cancel button; assert `onCancel` called once

    **`src/components/watch/RecentlyEvaluatedRail.test.tsx`** (covers D-14):
    - Import: `import { RecentlyEvaluatedRail } from '@/components/watch/RecentlyEvaluatedRail'`
    - Mock `next/image`
    - Test 1 (chips render): render with 3 `RailEntry` items (each with brand/model/catalogId/imageUrl/extracted); assert `screen.getByText('Recently evaluated')` heading; assert 3 chips render with `role='listitem'` parent items
    - Test 2 (chip click): render with `onSelect` spy; click first chip; assert spy called with that entry
    - Test 3 (empty rail hidden): render with `entries={[]}`; assert `screen.queryByText('Recently evaluated')` returns null
    - Test 4 (aria labels): assert each chip button has `aria-label="{brand} {model} — re-evaluate"` exactly

    **`src/components/profile/AddWatchCard.test.tsx`** (covers D-15 + D-16):
    - Import: `import { AddWatchCard } from '@/components/profile/AddWatchCard'`
    - Mock `next/link`
    - Test 1 (D-15 default Collection variant text): render `<AddWatchCard />`; assert `screen.getByText('Add to Collection')` is present (NOT "Add Watch"); assert link's `aria-label` is "Add to Collection"; assert `href="/watch/new"`
    - Test 2 (D-16 wishlist variant text): render `<AddWatchCard variant="wishlist" />`; assert `screen.getByText('Add to Wishlist')`; assert `aria-label="Add to Wishlist"`; assert `href="/watch/new"` (still the same canonical entry — D-16 spec)
    - Test 3 (default variant when prop omitted): render `<AddWatchCard />`; assert text is "Add to Collection" (default variant === 'collection' per D-15)

    **`src/components/profile/WishlistTabContent.test.tsx`** (covers D-16):
    - Import: `import { WishlistTabContent } from '@/components/profile/WishlistTabContent'`
    - Mock `next/link`, `next/image`, and `vi.mock('@/components/profile/ProfileWatchCard', () => ({ ProfileWatchCard: () => <div data-testid="pwc" /> }))`
    - Test 1 (isOwner=true with watches → end-of-grid AddToWishlist card renders): render with `watches=[<2 fixture watches>]`, `wearDates={}`, `isOwner={true}`; assert 2 `pwc` testIds present; assert exactly one element with text "Add to Wishlist" and `aria-label="Add to Wishlist"` is present (D-16 end-of-grid card)
    - Test 2 (isOwner=false → no AddToWishlist card): render same watches, `isOwner={false}`; assert NO element with text "Add to Wishlist" present
    - Test 3 (empty wishlist + isOwner=true → empty state still renders, NO end-of-grid card per D-16 spec): render `watches=[]`, `isOwner={true}`; assert "Your wishlist is empty." present; assert "Add to Wishlist" text NOT present (Phase 25 owns empty-state CTAs)
  </action>
  <acceptance_criteria>
    - File `src/components/watch/AddWatchFlow.test.tsx` exists; `grep -c "describe('Phase 20.1 Plan 04 — AddWatchFlow" src/components/watch/AddWatchFlow.test.tsx` ≥ 1
    - File `src/components/watch/PasteSection.test.tsx` exists; `grep -c "or enter manually" src/components/watch/PasteSection.test.tsx` ≥ 1
    - File `src/components/watch/VerdictStep.test.tsx` exists; `grep -c "Your collection is empty" src/components/watch/VerdictStep.test.tsx` ≥ 1
    - File `src/components/watch/WishlistRationalePanel.test.tsx` exists; `grep -c "Save to Wishlist" src/components/watch/WishlistRationalePanel.test.tsx` ≥ 1; `grep -c "Pitfall 5" src/components/watch/WishlistRationalePanel.test.tsx` ≥ 1
    - File `src/components/watch/RecentlyEvaluatedRail.test.tsx` exists; `grep -c "Recently evaluated" src/components/watch/RecentlyEvaluatedRail.test.tsx` ≥ 1; `grep -c "re-evaluate" src/components/watch/RecentlyEvaluatedRail.test.tsx` ≥ 1
    - File `src/components/profile/AddWatchCard.test.tsx` exists; `grep -c "Add to Collection" src/components/profile/AddWatchCard.test.tsx` ≥ 1; `grep -c "variant=\"wishlist\"" src/components/profile/AddWatchCard.test.tsx` ≥ 1
    - File `src/components/profile/WishlistTabContent.test.tsx` exists; `grep -c "isOwner" src/components/profile/WishlistTabContent.test.tsx` ≥ 1
    - All 7 files compile but FAIL when run: `npx vitest run src/components/watch/AddWatchFlow.test.tsx src/components/watch/PasteSection.test.tsx src/components/watch/VerdictStep.test.tsx src/components/watch/WishlistRationalePanel.test.tsx src/components/watch/RecentlyEvaluatedRail.test.tsx src/components/profile/AddWatchCard.test.tsx src/components/profile/WishlistTabContent.test.tsx 2>&1 | grep -E "(FAIL|Cannot find module)"` returns at least one match per file (RED state confirmed)
    - No `.skip` tokens: `grep -rn "\.skip\(" src/components/watch/*.test.tsx src/components/profile/AddWatchCard.test.tsx src/components/profile/WishlistTabContent.test.tsx` returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/watch/AddWatchFlow.test.tsx src/components/watch/PasteSection.test.tsx src/components/watch/VerdictStep.test.tsx src/components/watch/WishlistRationalePanel.test.tsx src/components/watch/RecentlyEvaluatedRail.test.tsx src/components/profile/AddWatchCard.test.tsx src/components/profile/WishlistTabContent.test.tsx 2>&1 | tail -20 ; echo "EXPECTED: tests fail (RED state) — module-not-found or assertion failures"</automated>
  </verify>
  <done>All 7 test files exist on disk; each contains the named describe block; each fails with module-not-found or assertion error when run (RED state); no `.skip` tokens; grep proofs pass.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold integration + page-level tests + extend existing extract-route + catalog-page tests</name>
  <files>tests/app/watch-new-page.test.ts, tests/integration/add-watch-flow-search-cta.test.ts, src/components/search/WatchSearchRowsAccordion.test.tsx, tests/integration/phase17-extract-route-wiring.test.ts, tests/app/catalog-page.test.ts</files>
  <read_first>
    - tests/app/catalog-page.test.ts (existing pattern — vi.hoisted mocks, vi.mock(@/db), Server Component direct invocation pattern; this plan EXTENDS this file with new it() blocks)
    - tests/integration/phase17-extract-route-wiring.test.ts (existing pattern — DATABASE_URL gate via `process.env.DATABASE_URL ? describe : describe.skip`, fetchAndExtract mocked, NextRequest direct invocation; this plan EXTENDS this file with a new `it()` asserting body.catalogId in response)
    - src/components/search/WatchSearchRowsAccordion.tsx (current shape — Accordion.Root + Accordion.Item + Accordion.Panel; Plan 05 will add 3 buttons inside Accordion.Panel)
    - src/app/api/extract-watch/route.ts (current response: `NextResponse.json({ success: true, ...result })` line 114-117; Plan 02 D-08 adds `catalogId` to that)
    - src/app/catalog/[catalogId]/page.tsx (current shape — Plan 05 D-05 adds 3 CTAs as Client Component child below `<CollectionFitCard>` in non-self-via-cross-user framing)
    - .planning/phases/20.1-add-watch-flow-rethink-verdict-as-step/20.1-RESEARCH.md §Pitfall 1 (Promise searchParams) and §Pitfall 2 (Accordion stopPropagation)
  </read_first>
  <action>
    Create 3 NEW test files and EXTEND 2 existing test files.

    **NEW: `tests/app/watch-new-page.test.ts`** (covers Pitfall 1 + intent whitelist):
    - Use the `vi.hoisted` mock pattern from `tests/app/catalog-page.test.ts` (mockGetCurrentUser, mockGetWatchesByUser, mockGetCatalogById)
    - Mock `@/lib/auth`, `@/data/watches`, `@/data/catalog` and the `<AddWatchFlow>` component as a stub: `vi.mock('@/components/watch/AddWatchFlow', () => ({ AddWatchFlow: (props: any) => <div data-testid="awf" data-props={JSON.stringify(props)} /> }))`
    - Test 1 (await searchParams): import `NewWatchPage`; render with `await NewWatchPage({ searchParams: Promise.resolve({}) })`; assert no throw; assert returned JSX includes the heading text "Add a watch — or just evaluate one"
    - Test 2 (deep-link `?catalogId=X&intent=owned`): mock `getCatalogById` to return a fixture catalog entry; call with `searchParams: Promise.resolve({ catalogId: '00000000-0000-4000-8000-000000000000', intent: 'owned' })`; assert the rendered AddWatchFlow received props `initialCatalogId='00000000-0000-4000-8000-000000000000'` and `initialIntent='owned'` and `initialCatalogPrefill` is non-null
    - Test 3 (intent whitelist — non-'owned' intent rejected): call with `searchParams: Promise.resolve({ catalogId: '...', intent: 'malicious-string' })`; assert `initialIntent` prop on AddWatchFlow is `null` (intent must be the literal string 'owned' or null — strict whitelist per RESEARCH §Security Domain item 2)
    - Test 4 (collectionRevision passed): mock `getWatchesByUser` to return array of length 7; assert `collectionRevision={7}` prop passed to AddWatchFlow

    **NEW: `tests/integration/add-watch-flow-search-cta.test.ts`** (covers ADD-06 end-to-end):
    - Use vi.hoisted mock pattern; mock `@/app/actions/watches` `addWatch` and `@/app/actions/verdict` `getVerdictForCatalogWatch`
    - Mock `next/navigation` `useRouter` to return `{ push: vi.fn(), refresh: vi.fn() }`
    - Mock `sonner` `toast` with `{ success: vi.fn(), error: vi.fn() }`
    - Render `<WatchSearchRowsAccordion results=[fixture row with catalogId, brand='Omega', model='Speedmaster'] q='' collectionRevision={1} />`
    - Test 1 (Wishlist commit): expand the accordion (click row); wait for verdict mock to resolve; click "Add to Wishlist" button inside the panel; assert `addWatch` was called with object containing `{ status: 'wishlist', brand: 'Omega', model: 'Speedmaster' }`; assert `toast.success` was called
    - Test 2 (Collection navigation): expand accordion; click "Add to Collection" button; assert `useRouter().push` was called with a URL matching `/watch/new?catalogId={catalogId}&intent=owned`; assert `addWatch` was NOT called from this surface (D-04 RESEARCH Open Q1 recommendation b — navigate, don't inline-commit)
    - Test 3 (stopPropagation Pitfall 2): assert that clicking "Add to Wishlist" does NOT call any Accordion onValueChange — verify by checking accordion `data-state` attribute does not flip to "closed" on the same item

    **NEW: `src/components/search/WatchSearchRowsAccordion.test.tsx`** (covers ADD-06 unit-level + Pitfall 2):
    - Use the same mock layer as the integration test above but pure unit shape
    - Test 1 (3 CTAs render inside panel after verdict resolves): mock `getVerdictForCatalogWatch` to return a `VerdictBundleFull` with `framing: 'cross-user'`; render component, expand row, wait for `<CollectionFitCard>` mock to render; assert all three buttons present in DOM with names "Add to Wishlist", "Add to Collection", "Hide"
    - Test 2 (CTAs use stopPropagation — Pitfall 2): use `fireEvent.click(button, { stopPropagation: vi.fn() })` and assert via spy that the click did not bubble — alternative impl: query the button element, attach a click listener to its parent Accordion.Item, fire click on button, assert parent listener was NOT called (or call count is 0 after stopPropagation)
    - Test 3 (Hide button collapses panel): expand row, click "Hide"; assert next render has the panel hidden (test by `screen.queryByText('Add to Wishlist')` returning null after the click)

    **EXTEND: `tests/integration/phase17-extract-route-wiring.test.ts`** (D-08):
    - Append a new `it()` block inside the existing `maybe(...)` describe: `it('D-08 — response shape includes catalogId field (Phase 20.1)', async () => { ... })`
    - Reuse the existing fetchAndExtract mock setup and POST handler invocation from the file's first test
    - Assert `body.catalogId` is a string (UUID-like) when extraction returns `brand+model`
    - Add a second new it: `it('D-08 — response.catalogId is null when extraction has no brand or model', async () => { ... })` — mock fetchAndExtract to return data with brand=undefined; assert `body.catalogId === null`

    **EXTEND: `tests/app/catalog-page.test.ts`** (D-05):
    - Mock the new `<CatalogPageActions>` Client Component child Plan 05 will create: `vi.mock('@/components/watch/CatalogPageActions', () => ({ CatalogPageActions: (props: any) => <div data-testid="cpa" data-framing={props.framing} /> }))`
    - Append a new `it()` block: `it('D-05 — renders 3 CTAs (CatalogPageActions) when framing is cross-user', async () => { ... })` — mock viewer collection length > 0; mock viewerOwnedRow=null; render the page; assert `screen.getByTestId('cpa')` present and `data-framing="cross-user"`
    - Append: `it('D-05 — does NOT render CTAs when framing is self-via-cross-user', async () => { ... })` — mock viewerOwnedRow to return a row; assert `screen.queryByTestId('cpa')` returns null
    - Append: `it('D-05 — does NOT render CTAs when collection is empty (no verdict)', async () => { ... })` — mock viewer collection length === 0; assert no cpa
  </action>
  <acceptance_criteria>
    - File `tests/app/watch-new-page.test.ts` exists; `grep -c "Add a watch — or just evaluate one" tests/app/watch-new-page.test.ts` ≥ 1; `grep -c "intent: 'malicious-string'" tests/app/watch-new-page.test.ts` ≥ 1
    - File `tests/integration/add-watch-flow-search-cta.test.ts` exists; `grep -c "stopPropagation" tests/integration/add-watch-flow-search-cta.test.ts` ≥ 1; `grep -c "intent=owned" tests/integration/add-watch-flow-search-cta.test.ts` ≥ 1
    - File `src/components/search/WatchSearchRowsAccordion.test.tsx` exists; `grep -c "Pitfall 2" src/components/search/WatchSearchRowsAccordion.test.tsx` ≥ 1
    - `grep -c "D-08 — response shape includes catalogId" tests/integration/phase17-extract-route-wiring.test.ts` ≥ 1
    - `grep -c "D-05 — renders 3 CTAs" tests/app/catalog-page.test.ts` ≥ 1
    - `grep -c "D-05 — does NOT render CTAs when framing is self-via-cross-user" tests/app/catalog-page.test.ts` ≥ 1
    - The 3 NEW files all FAIL with module-not-found when run (RED state — components don't exist yet); the 2 EXTENDED files have new it() blocks that fail (D-08 catalogId field absent; CatalogPageActions mock loads but the page doesn't render it yet)
    - Existing tests in the 2 extended files MUST still pass (no regression to existing assertions): `npx vitest run tests/integration/phase17-extract-route-wiring.test.ts tests/app/catalog-page.test.ts 2>&1 | grep -E "(passed|Tests )"` — the previously-passing tests should still appear in the passed count
    - No `.skip` tokens: `grep -rn "\.skip\(" tests/app/watch-new-page.test.ts tests/integration/add-watch-flow-search-cta.test.ts src/components/search/WatchSearchRowsAccordion.test.tsx` returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/app/watch-new-page.test.ts tests/integration/add-watch-flow-search-cta.test.ts src/components/search/WatchSearchRowsAccordion.test.tsx tests/integration/phase17-extract-route-wiring.test.ts tests/app/catalog-page.test.ts 2>&1 | tail -30 ; echo "EXPECTED: new it()s + new files fail (RED) but existing it()s in extended files still pass"</automated>
  </verify>
  <done>3 NEW files exist with named describe/it blocks; 2 EXTENDED files have new it() blocks tagged D-08/D-05; all NEW assertions FAIL (RED state) without breaking existing passing tests; no `.skip` tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → file system | Tests read source files; no security boundary crossed (development context) |
| Vitest mocks → @/lib/auth | Tests mock auth bypassing UnauthorizedError; intentional for unit-test isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-01-01 | Tampering | Test scaffolds masking real bugs via overly-permissive mocks | mitigate | Each test asserts SPECIFIC values from CONTEXT.md/UI-SPEC.md (e.g., "Add to Collection" exact string, `aria-label="{brand} {model} — re-evaluate"` exact pattern) — overly-loose `toHaveBeenCalled()` without arg checks is rejected. Acceptance criteria use grep on exact strings. |
| T-20.1-01-02 | Repudiation | RED tests silently passing because component-under-test compiles but does nothing useful | mitigate | Every test imports a SPECIFIC named export from a not-yet-created module. Vitest fails with `Cannot find module '@/components/watch/AddWatchFlow'` until Wave 1 creates the file. Acceptance criteria explicitly verify RED state via `grep -E "(FAIL|Cannot find module)"`. |

This plan creates only test files; no production code; no auth/data/network surfaces. Standard test isolation patterns from existing suite apply.
</threat_model>

<verification>
After both tasks complete:
1. `npx vitest run src/components/watch/*.test.tsx src/components/profile/AddWatchCard.test.tsx src/components/profile/WishlistTabContent.test.tsx src/components/search/WatchSearchRowsAccordion.test.tsx tests/app/watch-new-page.test.ts tests/integration/add-watch-flow-search-cta.test.ts` — expect ALL to fail with RED state messages (module-not-found OR assertion failures) — this is the desired Wave 0 outcome
2. `npx vitest run tests/integration/phase17-extract-route-wiring.test.ts tests/app/catalog-page.test.ts` — existing tests in these two files MUST still pass; only the new it() blocks fail
3. `grep -rn "\.skip(\|\.skip\b" src/components/watch/*.test.tsx src/components/profile/AddWatchCard.test.tsx src/components/profile/WishlistTabContent.test.tsx src/components/search/WatchSearchRowsAccordion.test.tsx tests/app/watch-new-page.test.ts tests/integration/add-watch-flow-search-cta.test.ts` returns 0 matches
</verification>

<success_criteria>
- All 10 test files referenced in 20.1-VALIDATION.md "Wave 0 Requirements" exist (8 NEW, 2 EXTEND)
- Every test scaffold imports the component-under-test by absolute path so the file is RED until that source ships
- Existing tests in `phase17-extract-route-wiring.test.ts` and `catalog-page.test.ts` still pass; only the newly-appended it() blocks fail (RED)
- No test is `.skip`ped
- Test names match the table in 20.1-VALIDATION.md verbatim so Wave 1+ tasks can reference them as `<verify>` commands
- nyquist_compliant gate is now SATISFIED for all downstream waves
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-add-watch-flow-rethink-verdict-as-step/20.1-01-SUMMARY.md` with:
- List of all 10 test files created/extended
- Confirmation that all 10 are RED (output of the verify command)
- Confirmation that existing assertions in the 2 extended files still pass
- Pointer forward: "Wave 1 plans (Plan 02, Plan 03) flip these scaffolds GREEN"
</output>
