---
phase: 20.1-add-watch-flow-rethink-verdict-as-step
verified: 2026-04-30T16:30:00Z
status: human_needed
score: 7/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Visual smoke — paste a real watch URL into /watch/new"
    expected: "URL input shows 'Working...' briefly, then verdict card + 3 buttons (Wishlist primary / Collection outlined / Skip ghost) render together as a single coherent gesture; D-07 sync wait + single reveal contract"
    why_human: "Real fetch + Anthropic API roundtrip latency feel + animation/transition smoothness cannot be measured programmatically; ADD-01 'single coherent gesture' is a UX feel quality"
  - test: "Wishlist commit smoke — click 'Add to Wishlist' from verdict-ready"
    expected: "WishlistRationalePanel slides in below verdict; textarea pre-filled with verdict.contextualPhrasings[0]; user can edit/blank/accept; clicking 'Save to Wishlist' shows toast 'Added to wishlist', resets URL input, returns to idle"
    why_human: "Sonner toast appearance + textarea auto-focus feel + state transition smoothness require visual confirmation"
  - test: "Skip path + Recently evaluated rail smoke"
    expected: "Click Skip from verdict-ready → URL input clears, focus returns to input, a chip with watch thumbnail + brand/model appears in 'Recently evaluated' rail below the input; click chip → verdict re-opens from cache instantly"
    why_human: "Auto-focus behavior + chip rendering + cache instant-re-open are visual/interactive qualities"
  - test: "Manual entry inline link smoke"
    expected: "Click 'or enter manually' below URL input → WatchForm renders without verdict step; status field is editable Select (not locked)"
    why_human: "Layout transition + form rendering needs visual confirmation"
  - test: "Extraction failure + recovery smoke"
    expected: "Paste a known-bad URL (e.g. 404 host) → 'Extraction didn't work' card with role='alert' + reason text + 'Continue manually' button + 'Try another URL' button; click 'Continue manually' → WatchForm with any partial brand/model preserved"
    why_human: "ADD-07 'no dead-end' affordance feels a UX quality; verifying the partial-data carry needs a real failing extract"
  - test: "/search?tab=watches inline 3 CTAs smoke"
    expected: "Search for a watch → expand row in accordion → verdict appears with 3 inline buttons (Wishlist/Collection/Hide); click Wishlist → toast + panel collapses; click Collection → navigates to /watch/new with prefilled form; click Hide → panel collapses; clicking any button does NOT collapse the panel via bubble-up (Pitfall 2)"
    why_human: "Accordion stopPropagation behavior + animation transitions are visual/interactive UX qualities"
  - test: "/catalog/[catalogId] cross-user 3 CTAs smoke"
    expected: "Visit a /catalog/[catalogId] page for a watch you don't own with a non-empty collection → CollectionFitCard + 3 CTAs (Wishlist/Collection/Skip) render below; visit a watch you DO own → existing 'You own this' callout, no CTAs; visit one with empty collection → no verdict card, no CTAs"
    why_human: "Conditional rendering across 3 framings is a layout/visual qualification; smoke needs real authenticated session"
  - test: "Deep-link /watch/new?catalogId=X&intent=owned smoke"
    expected: "Visiting the URL with a valid catalogId UUID → page renders WatchForm prefilled with extracted spec + status field rendered as read-only 'owned' chip (not Select); user fills ownership-only fields and submits"
    why_human: "End-to-end deep-link → form-prefill short-circuit needs real catalog data + form interaction"
---

# Phase 20.1: Add-Watch Flow Rethink + Verdict-as-Step Verification Report

**Phase Goal:** The add-watch flow is reorganized so URL-paste → verdict preview → status decision (wishlist / owned / skip) is a single coherent gesture. The "skip" path covers the lightweight evaluate-only use case (paste a watch found elsewhere; see fit; bail) without requiring a separate /evaluate route. Manual entry stays as a secondary affordance.

**Verified:** 2026-04-30T16:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria + Plan must_haves)

| #   | Truth                                                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Pasting a URL into the add-watch flow surfaces the verdict preview before any commit                                                                                                 | ✓ VERIFIED | `AddWatchFlow.tsx:97-168` — `handleExtract` POSTs to `/api/extract-watch`, transitions through `extracting` (with VerdictSkeleton), calls `getVerdictForCatalogWatch`, lands on `verdict-ready` state which renders `<VerdictStep verdict={...} />`. Test `AddWatchFlow.test.tsx` ADD-01 happy path PASSES (5/5 tests).                                                                                                              |
| 2   | From the verdict preview, the user can commit to wishlist, commit to collection, or skip — all three paths work end-to-end                                                           | ✓ VERIFIED | `VerdictStep.tsx:407-500` renders 3 buttons "Add to Wishlist" / "Add to Collection" / "Skip"; `AddWatchFlow.tsx:171-204` wires `handleWishlist` → `wishlist-rationale-open`, `handleCollection` → `form-prefill`, `handleSkip` → `idle` + rail entry. All 3 paths covered by AddWatchFlow.test (Tests 2 ADD-03 Collection, Test 3 ADD-04 Skip) + WishlistRationalePanel.test.tsx (Pitfall 5 blank preserve).                          |
| 3   | The wishlist commit path optionally captures verdict rationale in `notes`                                                                                                            | ✓ VERIFIED | `WishlistRationalePanel.tsx:40-58` — textarea pre-fills via `defaultRationale(verdict)` from `verdict.contextualPhrasings[0]`; `handleConfirm` calls `onConfirm(notes)` verbatim (Pitfall 5: blank passes through as ''). `AddWatchFlow.tsx:217-220` `buildAddWatchPayload` passes notes to `addWatch`. WishlistRationalePanel.test 4/4 PASS.                                                                                         |
| 4   | The collection commit path prefills the existing form with extracted fields                                                                                                          | ✓ VERIFIED | `AddWatchFlow.tsx:344-351` — `state.kind === 'form-prefill'` renders `<WatchForm mode="create" lockedStatus="owned" watch={extractedToPartialWatch(...)}/>`. WatchForm.tsx accepts `lockedStatus?: WatchStatus` prop (verified `grep -c "lockedStatus" WatchForm.tsx` = 8); status field renders read-only chip with `aria-readonly="true"` when lockedStatus set. WatchForm.lockedStatus.test 4/4 PASS.                              |
| 5   | Manual entry remains accessible and bypasses the verdict step                                                                                                                        | ✓ VERIFIED | `PasteSection.tsx` renders "or enter manually" `<button type="button">` below URL input (verified grep); `AddWatchFlow.tsx:252-254` `handleManualEntry` transitions directly to `manual-entry` state which renders `<WatchForm mode="create" />` without lockedStatus and without verdict step. PasteSection.test 4/4 PASS (manual-entry link click test verifies onManualEntry called, onExtract NOT called).                       |
| 6   | Extraction failure inside the flow preserves any partial data and offers manual continuation (no dead-end)                                                                           | ✓ VERIFIED | `AddWatchFlow.tsx:121-127` and `:159-167` — on `!res.ok` or thrown error, sets state to `extraction-failed` with sanitized reason; `:364-402` renders Card with `role="alert"`, displays partial brand/model when present, "Continue manually" button transitions to `manual-entry` carrying `state.partial`. WR-02 fix renders `state.reason` (line 373-377). AddWatchFlow.test ADD-07 PASSES.                                     |
| 7   | Catalog deep-link from `/search?tab=watches` (FIT-04 inline-expand "Add to..." follow-up) routes into this same flow with `catalogId` prefilling                                     | ✓ VERIFIED | `WatchSearchRowsAccordion.tsx:139-145` — `handleAddToCollection` calls `router.push('/watch/new?catalogId=${encodeURIComponent(r.catalogId)}&intent=owned')`. `app/watch/new/page.tsx:51-55` validates catalogId UUID regex; `:42` awaits searchParams (Next 16); `:45` whitelists intent to literal 'owned'; `:57-60` parallel-fetches `getWatchesByUser` + `hydrateCatalogPrefill` and passes to AddWatchFlow. `AddWatchFlow.tsx:70-73` deep-link short-circuits to `form-prefill` when all 3 deep-link props present. tests/app/watch-new-page.test 4/4 PASS. tests/integration/add-watch-flow-search-cta 3/3 PASS. |

**Score:** 7/7 truths verified

### Required Artifacts (Three-Level Verification)

| Artifact | Expected | Exists | Substantive | Wired | Data Flows | Status |
| -------- | -------- | ------ | ----------- | ----- | ---------- | ------ |
| `src/components/watch/AddWatchFlow.tsx` | State-machine orchestrator (9 states, 4 leaves composed, useState<FlowState>, RAIL_MAX=5, Pitfalls 1/3/5/6/8 mitigated) | ✓ | ✓ (478 LOC, all 9 state branches, all leaves imported) | ✓ (rendered by `app/watch/new/page.tsx:67`) | ✓ (handleExtract fetches /api/extract-watch, addWatch Server Action, getVerdictForCatalogWatch) | ✓ VERIFIED |
| `src/components/watch/flowTypes.ts` | FlowState 9-variant union + RailEntry + PendingTarget | ✓ | ✓ (9 variants verified, all 3 exports present) | ✓ (imported by AddWatchFlow + RecentlyEvaluatedRail) | ✓ N/A (type module) | ✓ VERIFIED |
| `src/components/watch/PasteSection.tsx` | Idle URL input + Extract Watch + manual-entry link, controlled (no internal fetch/useState for business state) | ✓ | ✓ ('use client', verbatim placeholder, "or enter manually" button) | ✓ (rendered in idle/extracting/extraction-failed branches) | ✓ (props-controlled — onUrlChange, onExtract, onManualEntry) | ✓ VERIFIED |
| `src/components/watch/VerdictStep.tsx` | Spec preview + CollectionFitCard (or empty notice) + 3-button row | ✓ | ✓ (imports byte-locked CollectionFitCard, 3 buttons with verbatim copy, D-06 empty notice) | ✓ (rendered in verdict-ready / wishlist-rationale-open / submitting-wishlist) | ✓ (verdict prop drives render branches; pendingTarget drives per-button labels) | ✓ VERIFIED |
| `src/components/watch/WishlistRationalePanel.tsx` | Textarea pre-filled with verdict copy; Pitfall 5 blank-preserve | ✓ | ✓ ('use client', defaultRationale uses contextualPhrasings[0], onConfirm(notes) literal) | ✓ (rendered in wishlist-rationale-open / submitting-wishlist branches) | ✓ (verdict prop → defaultRationale; textarea state owned by panel; commits via onConfirm) | ✓ VERIFIED |
| `src/components/watch/RecentlyEvaluatedRail.tsx` | Session-only chip rail; null on empty; aria-label format | ✓ | ✓ ('use client', returns null on empty, em-dash aria-label, h-8 chip height) | ✓ (rendered in idle/extracting/verdict-ready/extraction-failed; hidden in form-prefill/manual-entry/wishlist-rationale-open per UI-SPEC) | ✓ (entries prop array; onSelect callback; Plan 04 RAIL_MAX=5 FIFO + dedupe) | ✓ VERIFIED |
| `src/components/watch/WatchForm.tsx` | lockedStatus prop (D-12); UrlImport mount removed | ✓ | ✓ (8 lockedStatus references, aria-readonly chip when set, finalStatus = lockedStatus ?? formData.status) | ✓ (rendered in form-prefill + manual-entry branches of AddWatchFlow) | ✓ (lockedStatus prop drives status field render + submit value) | ✓ VERIFIED |
| `src/components/watch/CatalogPageActions.tsx` | Client Component child for /catalog cross-user 3 CTAs | ✓ | ✓ ('use client', 3 buttons, addWatch / router.push / router.back wiring, Pitfall 6 comment-only) | ✓ (rendered by `app/catalog/[catalogId]/page.tsx` when actionsSpec is non-null) | ✓ (catalogId + spec props drive payload + nav; framing prop reserved) | ✓ VERIFIED |
| `src/components/profile/AddWatchCard.tsx` | variant?: 'collection' \| 'wishlist'; default "Add to Collection" | ✓ | ✓ (3 "Add to Collection/Wishlist" matches, 7 variant references, dashed-border preserved) | ✓ (CollectionTabContent default variant, WishlistTabContent variant="wishlist") | ✓ (variant prop drives label + aria-label) | ✓ VERIFIED |
| `src/components/profile/WishlistTabContent.tsx` | End-of-grid AddToWishlist when isOwner+populated | ✓ | ✓ (variant="wishlist" rendered, isOwner && conditional, empty-state branch unchanged) | ✓ (rendered on profile wishlist tab) | ✓ (isOwner prop gates render) | ✓ VERIFIED |
| `src/app/watch/new/page.tsx` | Server Component awaiting Promise searchParams; intent whitelist + UUID regex | ✓ | ✓ (Promise<{...}> typing, await searchParams, sp.intent === 'owned' whitelist, UUID regex) | ✓ (Next 16 page handler — Next.js routing wires it) | ✓ (renders <AddWatchFlow> with computed props) | ✓ VERIFIED |
| `src/app/catalog/[catalogId]/page.tsx` | Renders CatalogPageActions when framing='cross-user' AND collection.length>0 | ✓ | ✓ (CatalogPageActions imported + conditionally rendered, actionsSpec gating, WR-01 UUID regex via notFound()) | ✓ (Next 16 page handler) | ✓ (actionsSpec built from catalogEntry; framing computed from viewerOwnedRow + collection length) | ✓ VERIFIED |
| `src/app/api/extract-watch/route.ts` | D-08 — response includes `catalogId: string \| null` | ✓ | ✓ (8 catalogId references, "Phase 20.1 D-08" comment marker, response shape extended at line 119) | ✓ (consumed by AddWatchFlow.handleExtract via fetch JSON parse) | ✓ (catalogId variable assigned by upsertCatalogFromExtractedUrl, returned in NextResponse.json) | ✓ VERIFIED |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `app/watch/new/page.tsx` | `AddWatchFlow.tsx` | `<AddWatchFlow collectionRevision initialCatalogId initialIntent initialCatalogPrefill />` | ✓ WIRED | Verified at page.tsx:67-72 |
| `AddWatchFlow.handleExtract` | `/api/extract-watch` | `fetch('/api/extract-watch', { method: 'POST', body: JSON.stringify({ url }) })` | ✓ WIRED | AddWatchFlow.tsx:115-119; catalogId field consumed at :130 |
| `AddWatchFlow.handleExtract` | `getVerdictForCatalogWatch` | Server Action call inside async handler | ✓ WIRED | AddWatchFlow.tsx:155 |
| `AddWatchFlow.handleWishlistConfirm` | `addWatch` | Server Action call inside startTransition | ✓ WIRED | AddWatchFlow.tsx:217-220 |
| `AddWatchFlow` | `useWatchSearchVerdictCache` | Hook call with collectionRevision (cross-surface coherence with /search) | ✓ WIRED | AddWatchFlow.tsx:79 |
| `WatchSearchRowsAccordion` | `addWatch` (Wishlist commit) | Server Action call inside startTransition | ✓ WIRED | WatchSearchRowsAccordion.tsx:106-137 |
| `WatchSearchRowsAccordion` | `/watch/new?catalogId=X&intent=owned` (Collection nav) | router.push | ✓ WIRED | WatchSearchRowsAccordion.tsx:144 |
| `CatalogPageActions` | `addWatch` | Server Action call inside startTransition | ✓ WIRED | CatalogPageActions.tsx:91 |
| `CatalogPageActions` | `/watch/new?catalogId=X&intent=owned` | router.push | ✓ WIRED | CatalogPageActions.tsx:107 |
| `app/catalog/[catalogId]/page.tsx` | `CatalogPageActions` | `<CatalogPageActions catalogId spec framing />` (when actionsSpec non-null) | ✓ WIRED | Verified via tests/app/catalog-page.test (3 D-05 it() blocks PASS) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| AddWatchFlow `state.extracted` | ExtractedWatchData | `/api/extract-watch` POST response (`data.data`) | ✓ Real (extractor pipeline returns brand/model/spec) | ✓ FLOWING |
| AddWatchFlow `state.verdict` | VerdictBundle \| null | `getVerdictForCatalogWatch` Server Action | ✓ Real (Phase 20 byte-locked compute against viewer collection) | ✓ FLOWING |
| AddWatchFlow `state.catalogId` | string \| null | `/api/extract-watch` response `catalogId` field (D-08) | ✓ Real (UUID from upsertCatalogFromExtractedUrl) | ✓ FLOWING |
| WatchSearchRowsAccordion verdict | VerdictBundle | `getVerdictForCatalogWatch` (Phase 20 cache or compute) | ✓ Real | ✓ FLOWING |
| CatalogPageActions spec | CatalogActionsSpec | Built from `catalogEntry` (Server Component fetched via `getCatalogById`) | ✓ Real (catalog row fields) | ✓ FLOWING |
| RecentlyEvaluatedRail entries | RailEntry[] | useState in AddWatchFlow; populated by handleSkip with FIFO cap 5 | ✓ Real (session-only state — D-14 explicit) | ✓ FLOWING |
| WatchForm prefill (form-prefill state) | Watch | `extractedToPartialWatch(state.extracted, 'owned')` | ✓ Real (mapped from extracted fields) | ✓ FLOWING |
| AddWatchFlow rail.verdict | VerdictBundle \| null | useWatchSearchVerdictCache.get(catalogId) ?? entry.verdict | ✓ Real (cache or stored bundle from prior compute) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| All Phase 20.1 component tests pass | `npx vitest run` (9 files: AddWatchFlow, page test, WatchForm.lockedStatus, PasteSection, VerdictStep, WishlistRationalePanel, RecentlyEvaluatedRail, AddWatchCard, WishlistTabContent) | 35/35 PASS | ✓ PASS |
| Cross-surface tests pass | `npx vitest run WatchSearchRowsAccordion + add-watch-flow-search-cta + catalog-page` | 14/14 PASS | ✓ PASS |
| UrlImport file deleted | `test ! -f src/components/watch/UrlImport.tsx` | DELETED OK | ✓ PASS |
| No UrlImport imports remain | `grep -rn "import.*UrlImport" src/` | 0 matches | ✓ PASS |
| FlowState 9 variants present | `grep -c "kind: 'idle'\|kind: 'extracting'\|...\|kind: 'extraction-failed'"` flowTypes.ts | 9 | ✓ PASS |
| catalogId in extract-watch response | `grep -n "catalogId," src/app/api/extract-watch/route.ts` | 3 occurrences (assignments + response) | ✓ PASS |
| Pitfall 2 stopPropagation triple in accordion | `grep -c "e.stopPropagation()" WatchSearchRowsAccordion.tsx` | 4 (3 handlers + comment) | ✓ PASS |
| Deep-link URL contract emitted from both surfaces | `grep -c "router.push.*catalogId.*intent=owned"` | 2 in WatchSearchRowsAccordion + 2 in CatalogPageActions | ✓ PASS |
| Pitfall 6 — no photoSourcePath WRITES | `grep -n "photoSourcePath" AddWatchFlow + WatchSearchRowsAccordion + CatalogPageActions` | All 9 occurrences are comment-only assertions; 0 writes | ✓ PASS |
| D-19 engine byte-lock | `git log --since="2026-04-29 17:00" -- src/lib/similarity.ts src/lib/verdict/composer.ts` | 0 commits during 20.1 | ✓ PASS |
| WR-01 UUID validation on /catalog page | `grep -n "notFound\|UUID" src/app/catalog/[catalogId]/page.tsx` | UUID regex + notFound() at line 41-46 | ✓ PASS |
| WR-02 extraction-failed reason rendered | `grep -c "state.reason" AddWatchFlow.tsx` | 2 (capture + render at line 373-377) | ✓ PASS |

### Requirements Coverage (ADD-01..ADD-07)

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| ADD-01 | 20.1-01, 20.1-04 | URL-paste flow runs extract → CollectionFitCard verdict preview → 3-button status decision in a single coherent flow | ✓ SATISFIED | AddWatchFlow.handleExtract pipeline; AddWatchFlow.test ADD-01 happy path PASS |
| ADD-02 | 20.1-01, 20.1-03, 20.1-04 | "Add to Wishlist" CTA from verdict preview commits a wishlist-status row with extracted fields and optional verdict rationale captured in `notes` | ✓ SATISFIED | WishlistRationalePanel pre-fill + Pitfall 5 verbatim onConfirm(notes); buildAddWatchPayload at AddWatchFlow.tsx:454-480 |
| ADD-03 | 20.1-01, 20.1-04 | "Add to Collection" CTA from verdict preview commits an owned-status row with extracted fields prefilled | ✓ SATISFIED | form-prefill state renders WatchForm with lockedStatus="owned" + extractedToPartialWatch prefill |
| ADD-04 | 20.1-01, 20.1-04 | "Skip" CTA from verdict preview closes the flow with no commit and offers "evaluate another" | ✓ SATISFIED | handleSkip → idle reset + auto-focus URL input + rail entry append (D-14); test ADD-04 PASS |
| ADD-05 | 20.1-01, 20.1-03, 20.1-04 | Manual entry path is preserved as a secondary affordance and bypasses the verdict step | ✓ SATISFIED | PasteSection "or enter manually" button; AddWatchFlow.handleManualEntry → manual-entry state → WatchForm without lockedStatus |
| ADD-06 | 20.1-01, 20.1-02, 20.1-05 | Catalog deep-link from `/search?tab=watches` Evaluate CTA offers an "Add to Wishlist / Collection" path that prefills via `catalogId`, reusing same verdict step | ✓ SATISFIED | WatchSearchRowsAccordion 3 inline CTAs (Plan 05) + CatalogPageActions on /catalog/[catalogId] (Plan 05) + deep-link UUID regex receive at /watch/new (Plan 04); page deep-link short-circuits to form-prefill |
| ADD-07 | 20.1-01, 20.1-04 | Extraction failure inside new flow shows fallback that preserves any partially-extracted data and offers manual continuation (no dead-end) | ✓ SATISFIED | extraction-failed branch with role="alert" + state.reason rendered (WR-02 fix) + state.partial preserved + Continue manually button → manual-entry state with partial carry |

**Coverage check:** No orphaned requirements. REQUIREMENTS.md maps ADD-01..ADD-07 exclusively to Phase 20.1; all 7 IDs claimed by at least one plan in this phase. Plan 02's frontmatter declares only ADD-06 (the API response shape change supports the search/catalog deep-link path), but ADD-01..ADD-05 + ADD-07 are covered by Plans 01/03/04/05 collectively.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | — | — | No blocking anti-patterns. The 9 `photoSourcePath` references are all comment-only assertions of Pitfall 6 mitigation (NEVER set on URL-extract surface). The pre-existing tsc errors in `RecentlyEvaluatedRail.test.tsx` (RailEntry verdict field missing) noted in Plan 04 SUMMARY are test-fixture-only structural type errors that do not affect runtime; tests pass. |

### Human Verification Required

8 items need human/visual testing (see frontmatter `human_verification` for full descriptions):

1. **Visual smoke — paste a real watch URL into /watch/new** — verify single coherent gesture timing
2. **Wishlist commit smoke** — verify rationale panel slide-in + toast + reset feel
3. **Skip path + Recently evaluated rail smoke** — verify auto-focus + chip render + cache instant re-open
4. **Manual entry inline link smoke** — verify layout transition + WatchForm without verdict
5. **Extraction failure + recovery smoke** — verify partial-data carry into manual fallback
6. **/search?tab=watches inline 3 CTAs smoke** — verify Pitfall 2 stopPropagation behavior + nav
7. **/catalog/[catalogId] cross-user 3 CTAs smoke** — verify framing-conditional render across 3 modes
8. **Deep-link /watch/new?catalogId=X&intent=owned smoke** — verify form-prefill short-circuit + read-only status chip

### Gaps Summary

**No actionable gaps.** All 7 roadmap success criteria are programmatically verified. All 13 required artifacts exist, are substantive, are wired into the flow, and have data flowing through them. All 11 key links verified. All 12 behavioral spot-checks pass. Requirements coverage complete (ADD-01..ADD-07).

The phase status is `human_needed` (not `passed`) because the goal is fundamentally about UX feel ("single coherent gesture", animation timing, auto-focus behavior, toast appearance, real-fetch latency) which cannot be validated programmatically. The 8 human verification items above represent the qualitative smoke-test surface that needs eyes-on confirmation before declaring the phase production-ready.

The pre-existing baseline failures noted by the orchestrator (`tests/app/explore.test.tsx` ×3 and `tests/no-raw-palette.test.ts` ×2 for `font-medium`) are out of scope for Phase 20.1 — they pre-date the phase and were not introduced by any 20.1 plan. They are NOT counted as 20.1 gaps per orchestrator guidance.

The CR-01 / WR-01 / WR-02 review-fix pass is reflected in current state:
- CR-01 (WishlistRationalePanel test fixture) → fixed in commit 313ad6c
- WR-01 (UUID validation on catalog page) → fixed in commit 777996f
- WR-02 (extraction-failed.reason rendered) → fixed in commit d20ecd3

---

_Verified: 2026-04-30T16:30:00Z_
_Verifier: Claude (gsd-verifier)_
