---
phase: 20.1-add-watch-flow-rethink-verdict-as-step
plan: 03
subsystem: ui
tags: [react, nextjs, typescript, client-component, state-machine, controlled-component]

# Dependency graph
requires:
  - phase: 20.1-01
    provides: RED test scaffolds (PasteSection.test.tsx, VerdictStep.test.tsx, WishlistRationalePanel.test.tsx, RecentlyEvaluatedRail.test.tsx) — exist in sibling worktree, will merge in via Plan 04 wave gate
  - phase: 20
    provides: byte-locked <CollectionFitCard> + VerdictBundle types (consumed unmodified by VerdictStep per D-19)
  - phase: 19.1
    provides: ExtractedWatchData shape + extractor pipeline (consumed via @/lib/extractors barrel by all 5 files)
provides:
  - flowTypes.ts shared FlowState (9-variant discriminated union) + RailEntry + PendingTarget — locked contracts for Plan 04 orchestrator
  - PasteSection (idle-state controlled URL input + Extract Watch + manual-entry button)
  - VerdictStep (verdict-ready spec preview + CollectionFitCard + 3-button row with D-11 hierarchy)
  - WishlistRationalePanel (D-13 inline expand textarea pre-filled from verdict.contextualPhrasings[0]; Pitfall 5 blank-preserve)
  - RecentlyEvaluatedRail (D-14 session-only chip rail; null on empty entries)
affects: [20.1-04, 20.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Controlled leaf components: no internal useState for business state, no fetch, no Server Action imports — state + handlers come from props"
    - "Discriminated union state machine over flow phases (FlowState by `kind`)"
    - "React 19 ref-as-prop on shadcn function components (Textarea forwards ref via {...props} to <textarea>)"
    - "Pure-presentation rule for leaves: orchestration belongs to Plan 04's <AddWatchFlow>"

key-files:
  created:
    - src/components/watch/flowTypes.ts
    - src/components/watch/PasteSection.tsx
    - src/components/watch/VerdictStep.tsx
    - src/components/watch/WishlistRationalePanel.tsx
    - src/components/watch/RecentlyEvaluatedRail.tsx
  modified: []

key-decisions:
  - "Plan 04 owns all state — leaves are pure controlled components per the 'split orchestrator from primitives' principle in the plan objective"
  - "Pitfall 5 enforced literally as `onConfirm(notes)` (no `||` fallback). Anti-pattern grep check has 0 matches."
  - "RecentlyEvaluatedRail returns null on empty (not an empty container) per UI-SPEC §Copywriting Contract: rail is hidden, not empty-stated"

patterns-established:
  - "Leaf component contract: controlled props only — url/value via prop, onChange via prop, pending state via prop, no internal network or persistence"
  - "VerdictStep imports byte-untouched <CollectionFitCard verdict={...}/> per D-19 — no extra props passed; the only prop is the typed VerdictBundle"
  - "Pre-fill ghosting prevention (Pitfall 5): textarea state is OWNED by WishlistRationalePanel (this is the only leaf with internal useState), but commit fires `onConfirm(textarea.value)` verbatim — empty string included"

requirements-completed: [ADD-01, ADD-02, ADD-04, ADD-05]

# Metrics
duration: ~10 min
completed: 2026-04-30
---

# Phase 20.1 Plan 03: Add-Watch Flow Leaf Components Summary

**Five Client Component leaves (flowTypes.ts + PasteSection + VerdictStep + WishlistRationalePanel + RecentlyEvaluatedRail) implementing controlled-component contracts for Plan 04's `<AddWatchFlow>` orchestrator to compose, with Pitfall 5 blank-preserve and D-19 byte-untouched CollectionFitCard.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-30T14:48:00Z (approx)
- **Completed:** 2026-04-30T14:58:27Z
- **Tasks:** 3
- **Files created:** 5

## Accomplishments

- **flowTypes.ts** — exports the locked FlowState 9-variant discriminated union, RailEntry interface, and PendingTarget alias that downstream Plan 04 imports
- **PasteSection** — controlled URL-input component with verbatim UI-SPEC copy ("Extract Watch", "Working...", "Paste a product page URL — Omega, Rolex, Hodinkee...", "or enter manually"); aria-busy on input + form-onSubmit Enter-key trigger; "or enter manually" rendered as `<button type="button">` (T-20.1-03-04 mitigation: cannot accidentally submit form)
- **VerdictStep** — spec preview Card + byte-untouched `<CollectionFitCard verdict={...}/>` (D-19) OR D-06 empty-collection notice + 3-button row (D-11 hierarchy: default/outline/ghost; mobile-stacked → tablet-row); pendingTarget controls per-button "Saving..." / "Skipping..." labels; aria-live="polite" wrapper for verdict-ready transitions
- **WishlistRationalePanel** — pre-fills textarea from `verdict.contextualPhrasings[0]` via `defaultRationale()` helper; falls back to `''` when verdict is null (D-06) or framing='self-via-cross-user' (defensive; not actually reachable for Wishlist commits in Phase 20.1); Pitfall 5 enforced literally as `onConfirm(notes)` — anti-pattern `onConfirm(notes || …)` confirmed absent via grep; auto-focus on mount; aria-describedby per UI-SPEC §Accessibility Contract
- **RecentlyEvaluatedRail** — returns null on empty entries (D-14: rail hidden, no placeholder); chips with thumbnails OR Watch icon fallback + brand/model + aria-label `"{brand} {model} — re-evaluate"` (em-dash verbatim) + 32px chip height (h-8) per UI-SPEC §Spacing Scale; receives entries as prop, no localStorage/sessionStorage anywhere (T-20.1-03-03 mitigation)

## Task Commits

Each task was committed atomically (with `--no-verify` per parallel-execution protocol):

1. **Task 1: flowTypes shared union + PasteSection idle UI** — `799f34c` (feat)
2. **Task 2: VerdictStep + RecentlyEvaluatedRail leaves** — `178ab44` (feat)
3. **Task 3: WishlistRationalePanel with Pitfall 5 blank-preserve** — `ad20182` (feat)

## Files Created/Modified

**Created:**
- `src/components/watch/flowTypes.ts` (53 LOC) — FlowState 9-variant discriminated union + RailEntry + PendingTarget
- `src/components/watch/PasteSection.tsx` (75 LOC) — controlled URL input + Extract Watch + manual-entry button
- `src/components/watch/VerdictStep.tsx` (130 LOC) — spec preview + CollectionFitCard + 3-button row
- `src/components/watch/WishlistRationalePanel.tsx` (105 LOC) — Card + Textarea pre-filled + Cancel + Save to Wishlist
- `src/components/watch/RecentlyEvaluatedRail.tsx` (60 LOC) — chip rail with thumbnails or Watch icon fallback

**Modified:** none.

## Key Contracts (for Plan 04 wiring)

```typescript
// PasteSection
{ url, onUrlChange, onExtract, onManualEntry, pending, disabled? }

// VerdictStep
{ extracted, verdict (VerdictBundle | null), pending, pendingTarget, onWishlist, onCollection, onSkip }

// WishlistRationalePanel
{ verdict (VerdictBundle | null), initialNotes?, onConfirm: (notes) => void, onCancel, pending }

// RecentlyEvaluatedRail
{ entries: RailEntry[], onSelect: (entry: RailEntry) => void }
```

Plan 04's `<AddWatchFlow>` orchestrator will:
- own `useState<FlowState>` (the 9-variant union from flowTypes.ts)
- own `useState<RailEntry[]>` (capped to 5 with FIFO eviction)
- thread the right leaf based on `state.kind` (idle → PasteSection; verdict-ready → VerdictStep; wishlist-rationale-open → WishlistRationalePanel)
- compose RecentlyEvaluatedRail beneath the active leaf in idle/verdict-ready states

## Decisions Made

None — followed the plan as written. Every prop name, copy string, ARIA attribute, and class utility came verbatim from the plan body which was already byte-aligned with UI-SPEC.md (em-dash placement, `h-8` chip height, "Recently evaluated" heading, `— re-evaluate` aria-label format).

## Deviations from Plan

None — plan executed exactly as written. The only environmental note: the worktree was created from an old `main` commit (`0dcdf85`, pre-Phase-17) instead of the expected feature-branch HEAD (`b321296`). The `<worktree_branch_check>` protocol's `git reset --soft` left the working tree out-of-sync with the new HEAD (the plan files and Phase 20 substrate were not present). Followed the protocol's continuation guidance with `git reset --hard b321296` to align the working tree with the correct base; this is permitted because no work-in-progress existed in the worktree (fresh executor start).

## Issues Encountered

**1. Worktree base mismatch.** Worktree HEAD was at an older commit (`0dcdf85`) on `main` rather than the expected feature-branch base (`b321296`). Diagnosis: `git merge-base HEAD $EXPECTED_BASE` returned the older commit, indicating the worktree branch was created off an older `main`. Resolution: hard-reset to the expected base per the `<worktree_branch_check>` protocol to bring the plan files and Phase 20 substrate (CollectionFitCard, VerdictBundle types) into the working tree. No work-in-progress was lost (fresh executor).

**2. Plan 01 RED tests not present in this worktree.** `depends_on: [20.1-01]` indicates Plan 01's test scaffolds should exist; they don't in this worktree (Plan 01 is running in a sibling worktree as a parallel-wave executor). Per parallel-execution semantics, the orchestrator merges Wave 1 worktrees together; the RED tests will be present and the implementations here will flip them GREEN at merge time. Plan 03's grep-based acceptance criteria (verbatim copy, prop shapes, `'use client'` directives, ARIA, no fetch/persistence/Server-Action imports) were used as the in-worktree verification surface, all PASS.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 04 (AddWatchFlow orchestrator)** ready to import flowTypes.ts and compose all four leaves into the state-driven flow.
- All 4 prop contracts locked above; Plan 04 cannot drift them without breaking the leaves' assumptions.
- All verification gates pass: no `localStorage`/`sessionStorage`, no `fetch(`, no Server Action imports in any of the 5 files. tsc clean for all new files (8 pre-existing TS errors unchanged — `LayoutProps`, `DesktopTopNav` test, etc., all PROJECT.md `Active`-tracked).

## Self-Check: PASSED

Verified after writing this SUMMARY:
- `[ -f src/components/watch/flowTypes.ts ]` → FOUND
- `[ -f src/components/watch/PasteSection.tsx ]` → FOUND
- `[ -f src/components/watch/VerdictStep.tsx ]` → FOUND
- `[ -f src/components/watch/WishlistRationalePanel.tsx ]` → FOUND
- `[ -f src/components/watch/RecentlyEvaluatedRail.tsx ]` → FOUND
- Commit `799f34c` (Task 1) → FOUND
- Commit `178ab44` (Task 2) → FOUND
- Commit `ad20182` (Task 3) → FOUND

## Threat Flags

None — no new security surface introduced. The threat register's 4 STRIDE entries (T-20.1-03-01 through T-20.1-03-04) all marked `mitigate` are satisfied by the implementation:
- **T-20.1-03-01** (notes pre-fill ghosting): `onConfirm(notes)` literal in WishlistRationalePanel; anti-pattern grep returns 0
- **T-20.1-03-02** (XSS via brand/model text): React renders text children as escaped strings; no `dangerouslySetInnerHTML` in any leaf
- **T-20.1-03-03** (rail localStorage leak): no `localStorage`/`sessionStorage` in any of the 5 files
- **T-20.1-03-04** (manual-entry accidental submit): "or enter manually" is `<button type="button">`, not type="submit"

---
*Phase: 20.1-add-watch-flow-rethink-verdict-as-step*
*Completed: 2026-04-30*
