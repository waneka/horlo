---
status: resolved
phase: 20.1-add-watch-flow-rethink-verdict-as-step
source: [20.1-VERIFICATION.md]
started: 2026-04-30T16:30:00Z
updated: 2026-04-30T19:38:00Z
resolved_by: [20.1-06, 20.1-07, 20.1-08]
---

## Current Test

[testing complete]

## Tests

### 1. Visual smoke — paste a real watch URL into /watch/new
expected: URL input shows 'Working...' briefly, then verdict card + 3 buttons (Wishlist primary / Collection outlined / Skip ghost) render together as a single coherent gesture; D-07 sync wait + single reveal contract.
result: issue
reported: "extract works but i see 'Your collection is empty — fit score not available yet.' when i do have a few pieces in my collection"
severity: major

### 2. Wishlist commit smoke — click 'Add to Wishlist' from verdict-ready
expected: WishlistRationalePanel slides in below verdict; textarea pre-filled with verdict.contextualPhrasings[0]; user can edit/blank/accept; clicking 'Save to Wishlist' shows toast 'Added to wishlist', resets URL input, returns to idle.
result: issue
reported: "the textarea is empty, no pre-fill. might be related to the previous issue i noted. everything else works as expected"
severity: major
related: 1

### 3. Skip path + Recently evaluated rail smoke
expected: Click Skip from verdict-ready → URL input clears, focus returns to input, a chip with watch thumbnail + brand/model appears in 'Recently evaluated' rail below the input; click chip → verdict re-opens from cache instantly.
result: issue
reported: "i clicked skip and i don't see a recently evaluated rail - just looks like i landed on the page from scratch"
severity: major

### 4. Manual entry inline link smoke
expected: Click 'or enter manually' below URL input → WatchForm renders without verdict step; status field is editable Select (not locked).
result: pass

### 5. Extraction failure + recovery smoke
expected: Paste a known-bad URL (e.g. 404 host) → 'Extraction didn't work' card with role='alert' + reason text + 'Continue manually' button + 'Try another URL' button; click 'Continue manually' → WatchForm with any partial brand/model preserved.
result: pass
note: "User noted no way to back out of manual-entry mode and return to URL extract flow without refreshing the page — logged as separate gap related to Test 4."

### 6. /search?tab=watches inline 3 CTAs smoke
expected: Search for a watch → expand row in accordion → verdict appears with 3 inline buttons (Wishlist/Collection/Hide); click Wishlist → toast + panel collapses; click Collection → navigates to /watch/new with prefilled form; click Hide → panel collapses; clicking any button does NOT collapse the panel via bubble-up (Pitfall 2).
result: issue
reported: "broken - clicking the row or even the 'evaluate' cta on the row doesn't expand it. expand seems broken"
severity: blocker

### 7. /catalog/[catalogId] cross-user 3 CTAs smoke
expected: Visit a /catalog/[catalogId] page for a watch you don't own with a non-empty collection → CollectionFitCard + 3 CTAs (Wishlist/Collection/Skip) render below; visit a watch you DO own → existing 'You own this' callout, no CTAs; visit one with empty collection → no verdict card, no CTAs.
result: pass
note: "Test passes — CollectionFitCard + 3 CTAs render as expected for cross-user case. Separate concern (NOT a 20.1 gap): user reports that /catalog/[catalogId] watch detail content has been severely reduced and needs a dedicated redesign phase to make the cross-user view incredibly compelling (full watch details, prominent CollectionFit, 3 CTAs). Captured under Deferred Follow-Ups for backlog/seed promotion."

### 8. Deep-link /watch/new?catalogId=X&intent=owned smoke
expected: Visiting the URL with a valid catalogId UUID → page renders WatchForm prefilled with extracted spec + status field rendered as read-only 'owned' chip (not Select); user fills ownership-only fields and submits.
result: pass

## Summary

total: 8
passed: 4
issues: 4
pending: 0
skipped: 0
blocked: 0

## Gaps

> **Cross-cutting note:** Diagnosis confirmed gaps 1, 2, and 3 share a single upstream root cause — `state.verdict === null` and/or `state.catalogId === ''` in `verdict-ready` state for users with a non-empty collection. The triggers are silent failures in `/api/extract-watch` (catalog upsert swallowed in try/catch, returns `catalogId: null`) and/or `getVerdictForCatalogWatch` Server Action (returns `success: false` without surfacing the reason). Fixing the upstream signal + downstream conflations resolves all three. Gaps 4 and 5 are independent.

- truth: "Verdict card on /watch/new should reflect actual collection state — when collection is non-empty, fit score must be computed and rendered (not the empty-collection fallback message)."
  status: resolved
  reason: "User reported: extraction works, but the verdict card displays 'Your collection is empty — fit score not available yet.' even though the user has multiple watches in their collection."
  severity: major
  test: 1
  root_cause: "Compound: (A) DOWNSTREAM — VerdictStep.tsx lines 76–83 conflate `verdict === null` with 'empty collection' and unconditionally render the empty-collection copy whenever verdict is null, regardless of actual collection size. (B) UPSTREAM — AddWatchFlow.handleExtract has three paths to `verdict: null`: (1) `collectionRevision === 0` (legitimate empty case), (2) `!catalogId` from /api/extract-watch returning `catalogId: null` because `upsertCatalogFromExtractedUrl` threw inside a silent try/catch (extract-watch/route.ts:50–76), or (3) `getVerdictForCatalogWatch` returning `success: false` (catalog-row read-after-write timing, Zod validation, or computeViewerTasteProfile/computeVerdictBundle throw). For this user, path (2) or (3) is firing. Test 7 (catalog page) passes for the same user, isolating the failure to the /watch/new Server-Action path."
  artifacts:
    - path: "src/components/watch/VerdictStep.tsx"
      issue: "Lines 76–83: empty-collection copy is gated only on `verdict === null`, never on `collectionRevision`."
    - path: "src/components/watch/AddWatchFlow.tsx"
      issue: "Lines 132–158: three `verdict: null` setState branches in `handleExtract`; the latter two have no client-side observability (no toast, no console.warn)."
    - path: "src/app/api/extract-watch/route.ts"
      issue: "Lines 50–76: catalog upsert wrapped in silent try/catch — only `console.error` to server, returns `catalogId: null` to client with no error indicator."
    - path: "src/app/actions/verdict.ts"
      issue: "Lines 54 and 68–71: returns `{ success: false }` on multiple silent paths; client maps any non-success to `bundle = null` without surfacing the reason."
    - path: "src/data/catalog.ts"
      issue: "Lines 191–234: `upsertCatalogFromExtractedUrl` returns null if the SQL execution shape doesn't yield a row id — possible silent failure point."
  missing:
    - "Thread `collectionRevision` (or `hasCollection: boolean`) from AddWatchFlow into VerdictStep and gate the empty-collection copy on `collectionRevision === 0`. Render a distinct fallback ('Couldn\\'t compute fit right now…') when `verdict === null && collectionRevision > 0`."
    - "Surface upstream failures: in extract-watch/route.ts return an explicit `catalogIdError` string when the upsert throws; in AddWatchFlow.handleExtract add `console.warn` before each `setState({ verdict: null })` so engineers can see which path fired; bubble `getVerdictForCatalogWatch` error string back to the client."
    - "Add a RED test: extract returns `catalogId: null`, user has non-empty collection, assert the new 'couldn\\'t compute fit' copy renders (not the empty-collection copy)."
  debug_session: ".planning/debug/verdict-empty-collection-message.md"

- truth: "WishlistRationalePanel textarea must be pre-filled with verdict.contextualPhrasings[0] when the user opens it from verdict-ready state."
  status: resolved
  reason: "User reported: textarea is empty, no pre-fill. User suspects this is related to Test 1 — likely the same upstream cause (verdict object not computed/passed correctly when a non-empty collection exists)."
  severity: major
  test: 2
  related: [1]
  root_cause: "SAME ROOT CAUSE as gap 1 (confirmed by diagnosis). `WishlistRationalePanel.defaultRationale(verdict)` (lines 42–46) correctly returns `verdict.contextualPhrasings[0] ?? ''` — and returns '' when `verdict === null`. composer.ts lines 59–72 guarantee `contextualPhrasings` is non-empty whenever a bundle is built (fallback to `[DESCRIPTION_FOR_LABEL[result.label]]`), so an empty textarea implies `verdict === null`, not `contextualPhrasings.length === 0`. Fixing the upstream null-verdict cause from gap 1 resolves this gap automatically."
  artifacts:
    - path: "src/components/watch/WishlistRationalePanel.tsx"
      issue: "Lines 42–46: `defaultRationale(null)` returns '' — symptom only, not root cause."
    - path: "src/components/watch/AddWatchFlow.tsx"
      issue: "Lines 142 and 155–156: silent `verdict: null` setState branches (same as gap 1)."
  missing:
    - "Resolve gap 1 — same fix-set restores pre-fill for non-empty-collection case."
    - "Add a RED test: AddWatchFlow with verdict containing `contextualPhrasings: ['Foo']`, click Add to Wishlist, assert textarea value === 'Foo'."
  debug_session: ".planning/debug/wishlist-textarea-not-prefilled.md"

- truth: "After clicking Skip from verdict-ready, the URL input clears and a 'Recently evaluated' rail with a chip for the just-skipped watch must appear below the input; clicking the chip should re-open the cached verdict."
  status: resolved
  reason: "User reported: clicked Skip and there is no 'Recently evaluated' rail visible — the page looks like a fresh load, no chip is rendered."
  severity: major
  test: 3
  related: [1]
  root_cause: "Compound: (A) PROXIMATE — `AddWatchFlow.handleSkip` (lines 186–204) silently no-ops the rail-entry push when `state.catalogId === ''` (the guard `if (state.catalogId)` on line 189). `setUrl('')` and `setState({ kind: 'idle' })` still execute (input clears, focus returns) — exactly the user-reported symptom. (B) UPSTREAM — `state.catalogId === ''` is set in `handleExtract` at lines 136 and 145, both triggered when `/api/extract-watch` returns `catalogId: null` (silent catalog-upsert failure, same upstream as gap 1). The test fixture in `AddWatchFlow.test.tsx:156–200` always provides `catalogId: 'cat-uuid'` (truthy), so the empty-catalogId Skip branch is uncovered — explaining why this shipped."
  artifacts:
    - path: "src/components/watch/AddWatchFlow.tsx"
      issue: "Lines 186–204: handleSkip's `if (state.catalogId)` guard silently skips the rail push when catalogId is empty string. Lines 133–147: handleExtract sets catalogId='' on extract-route failure."
    - path: "src/components/watch/RecentlyEvaluatedRail.tsx"
      issue: "Line 27: correctly returns null on empty entries — not buggy itself, just starved by upstream."
    - path: "src/app/api/extract-watch/route.ts"
      issue: "Lines 50–76: same silent catalog-upsert try/catch as gap 1 — returns `catalogId: null` without an error signal."
    - path: "src/components/watch/AddWatchFlow.test.tsx"
      issue: "Lines 156–200 (ADD-04 Skip test): fixture always provides truthy catalogId; empty-catalogId branch is not covered."
  missing:
    - "Fix handleSkip to push to rail even when catalogId is empty: synthesize a stable rail-entry id from `${brand}|${model}` (or generate a uuid) so the chip renders. Either widen `RailEntry.catalogId` semantics to allow null/'' or add a separate `RailEntry.id` field distinct from the catalog UUID."
    - "Resolve gap 1's upstream catalog-upsert failure — surfacing the error and/or adding a minimal-row fallback so brand+model always produces a real catalogId reduces this entire failure mode."
    - "Add a RED test: extract returns `catalogId: null` with brand+model populated, click Skip, assert chip visible in rail."
  debug_session: ".planning/debug/recently-evaluated-rail-missing.md"

- truth: "After clicking 'or enter manually', the user must have a discoverable way to back out of manual-entry mode and return to the URL extract flow without refreshing the page (e.g. a 'Cancel' / 'Use URL instead' link)."
  status: resolved
  reason: "User reported during Test 5: 'once you click \"enter manually\" i don't see a way to close the manual upload and get back to the extract url flow, i had to refresh the page'."
  severity: minor
  test: 4
  root_cause: "Spec gap inherited from 20.1 UI-SPEC, not an implementation defect. AddWatchFlow's `manual-entry` render branch (lines 354–361) renders only `<WatchForm>` with no surrounding chrome — no Cancel button, no 'Use URL instead' link. The `FlowState` state machine has two ingress edges into `manual-entry` (`handleManualEntry`, `handleContinueManually`) but zero egress edges back to `idle`. The 20.1-04-SUMMARY explicitly documented the manual-entry exit as '(back via WatchForm submit/router.back)' — Plan 04 implemented exactly what was specified. Critically, `handleStartOver` (lines 261–264) ALREADY implements the correct idle-restore (`setUrl('')` + `setState({ kind: 'idle' })`); it's wired only to the extraction-failed branch's 'Try another URL' button (line 396–398). The fix is purely additive JSX — no state-machine changes required."
  artifacts:
    - path: "src/components/watch/AddWatchFlow.tsx"
      issue: "Lines 354–361: manual-entry render branch renders <WatchForm> with no Cancel/back affordance. Lines 261–264: existing handleStartOver is the correct handler — just no UI button wired to it from manual-entry mode."
  missing:
    - "Inside the `manual-entry` render branch, prepend a quiet text-link button styled like PasteSection's 'or enter manually' link (PasteSection.tsx lines 73–80), copy: '← Use URL instead' or 'Cancel — paste a URL instead', wired to `handleStartOver`."
    - "Apply the same affordance to the extraction-failed → manual-entry path (entered via `handleContinueManually`) so users can also back out of post-failure manual entry."
    - "Add a unit test: render AddWatchFlow in `manual-entry` state, click the new back affordance, assert state transitions to `idle` and URL input is empty."
  debug_session: ".planning/debug/no-escape-from-manual-entry.md"

- truth: "On /search?tab=watches, clicking a row (or its 'Evaluate' CTA) must expand the accordion to reveal the inline verdict + 3 CTAs (Wishlist/Collection/Hide)."
  status: resolved
  reason: "User reported: 'clicking the row or even the \"evaluate\" cta on the row doesn't expand it. expand seems broken' — the entire inline-verdict-on-search surface is unreachable, so none of the 3-CTA behaviors (Wishlist/Collection/Hide, Pitfall 2 bubble-up guard) can be exercised."
  severity: blocker
  test: 6
  related: [1]
  root_cause: "Compound, primarily a Tailwind data-attribute selector mismatch. (A) PRIMARY — `WatchSearchRowsAccordion.tsx:169` styles the `<Accordion.Panel>` with `data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0`, but base-ui's `AccordionPanel` exposes `data-open` (no value), NOT `data-state=\"open\"` (verified in `node_modules/@base-ui/react/accordion/panel/AccordionPanelDataAttributes.js`). All four selectors NEVER match — no animation runs, the panel may technically expand but the visual change is so subtle the user perceives it as 'broken'. The Phase 20 SUMMARY noted base-ui sets `data-panel-open/aria-expanded` (not data-state) but the className wasn't updated to compensate. (B) LIKELY CONTRIBUTING — even if the panel opens, the inline verdict computation goes through the same `getVerdictForCatalogWatch` path as gap 1; if it returns degenerate content (empty headline, blank phrasings), CollectionFitCard renders essentially blank, reinforcing the 'didn't change' perception. (C) UX confusion — the 'Evaluate' element on `WatchSearchRow.tsx:70–81` is a styled `<span>` with `pointer-events-none`, ambiguous as a button. Unit tests in `WatchSearchRowsAccordion.test.tsx` (3/3) pass because they dispatch clicks directly on the trigger and assert state, not visual output."
  artifacts:
    - path: "src/components/search/WatchSearchRowsAccordion.tsx"
      issue: "Line 169: data-attribute selectors `data-[state=open]:` / `data-[state=closed]:` never match base-ui's `data-open` attribute → animation classes never apply → row appears not to expand visually."
    - path: "src/components/search/WatchSearchRow.tsx"
      issue: "Lines 70–81: 'Evaluate' element is a `<span>` with `pointer-events-none` styled like a button — ambiguous affordance contributing to user confusion."
    - path: "src/app/actions/verdict.ts"
      issue: "Same shared verdict pipeline as gap 1 — degenerate verdict content makes any expansion look blank."
  missing:
    - "Replace `data-[state=open]:` / `data-[state=closed]:` with `data-[open]:` / `not-data-[open]:` (or use the CSS variable `--accordion-panel-height` from base-ui's AccordionPanelCssVars for a height transition). Verify against the live base-ui DOM attribute, not the class names from a different lib."
    - "Add an instrumentation log (temporary `console.log('[accordion] handleValueChange', next)`) and confirm the click is firing — this distinguishes 'state updates fine, panel mounts blank' (verdict pipeline issue, ties to gap 1) from 'click handler not firing' (event-binding issue). The fix MUST be confirmed by direct browser observation, not just unit tests, since unit tests already pass while the user UX is broken."
    - "Reconsider the 'Evaluate' span treatment — either make it a real button with explicit click handler, swap to a chevron-only visual cue, or add 'Tap to expand' helper text. Avoid `pointer-events-none` styled-as-button patterns."
    - "Add an integration test that renders the row, clicks it, waits for the panel to actually become visible (e.g. assert visible content rather than just role/aria), and asserts the verdict content renders non-empty."
  debug_session: ".planning/debug/search-row-expand-broken.md"

## Deferred Follow-Ups

> Out of scope for 20.1 gap-closure — candidates for backlog or a future redesign phase.

- title: "Redesign /catalog/[catalogId] cross-user view as a compelling, full-detail experience"
  source_test: 7
  user_intent: "When a user clicks a watch they don't own, they need an incredibly compelling view of the watch with the full details. Collection fit should be highly visible. The 3 CTAs should also be on the page. Current implementation has reduced watch details too far. User explicitly suggested adding a dedicated phase to fully redesign this page from scratch."
  recommended_action: "Capture as backlog/seed (e.g. /gsd-add-backlog or /gsd-plant-seed) — scoped as a standalone redesign phase, not a 20.1 gap fix."
