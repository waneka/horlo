---
status: human_needed
phase: 20.1
scope: gap-closure (plans 06, 07, 08 + inline extended-UX from 07 verification)
must_haves_verified: 16/16
plans: [20.1-06, 20.1-07, 20.1-08]
gaps_resolved: [1, 2, 3, 4, 5]
generated: 2026-04-30
---

# Phase 20.1 Gap-Closure Verification

Re-verification of 20.1 after the gap-closure pass. The original 20.1-VERIFICATION.md (status: passed) flagged 8 human-verification items, of which 4 came back as failed gaps (1, 2, 3, 4, 5). This report verifies the gap-closure plans 20.1-06, 20.1-07, 20.1-08 + the inline extended-UX fix from 20.1-07.

## Gap-Closure Summary

All 5 originally-flagged UAT gaps are programmatically closed at code level.

### Gap 1 — verdict-empty-collection conflation (Plan 20.1-06)
- VerdictStep.tsx now has `hasCollection` prop driving three-way fallback copy split
- Verified at `VerdictStep.tsx:88-98`

### Gap 2 — wishlist textarea empty (Plan 20.1-06)
- Regression-guard test confirms pre-fill behavior
- Was a downstream consequence of gap 1's null-verdict; resolved automatically by the gap-1 fix

### Gap 3 — no rail chip after Skip (Plan 20.1-06)
- handleSkip always pushes; synthesizes `synth:{brand}|{model}` id when catalogId empty
- Silent `if (state.catalogId)` guard removed
- Verified at `AddWatchFlow.tsx:210-235`

### Gap 4 — no manual-entry exit (Plan 20.1-08)
- "← Cancel — paste a URL instead" link wired to existing `handleStartOver`
- Verified at `AddWatchFlow.tsx:391-407`

### Gap 5 — search-row accordion expand (Plan 20.1-07 + extended UX)
- Selectors corrected from `data-[state=open]:` (never matched) to `data-[open]:`/`data-[ending-style]:` (base-ui DOM contract)
- Inline extended UX: split row affordance (row=Link to /catalog, chevron=trigger), Hide CTA dropped, All-tab consistency fix
- Verified at `WatchSearchRowsAccordion.tsx:131`, `WatchSearchRow.tsx:30-69`, `AllTabResults.tsx:106-110`

## Observability (Plan 20.1-06 Part B)

- `/api/extract-watch` returns `catalogIdError: string | null` with sanitized reason codes
- `AddWatchFlow.handleExtract` emits 3 `console.warn('[AddWatchFlow] verdict=null path: ...')` statements identifying which silent path fired (`AddWatchFlow.tsx:144, 160, 175`)
- `getVerdictForCatalogWatch` JSDoc locks the stable-error-string contract (`verdict.ts:30-34`)

## Test Evidence

- 49/49 tests PASS across 7 modified test files
  - Watch-area: AddWatchFlow.test.tsx (10), VerdictStep.test.tsx, WishlistRationalePanel.test.tsx, WatchSearchRowsAccordion.test.tsx (6)
  - Search-area: WatchSearchRow.test.tsx (11), AllTabResults.test.tsx (7), add-watch-flow-search-cta.test.tsx (3)
- All 8 expected commits present in git log: `8814f94`, `6608e28`, `2ec3c7d`, `e3d1c75`, `d4de451`, `0a7e757`, `0ef5863`, `5fdda06`

## Requirements Coverage

ADD-01, ADD-02, ADD-04, ADD-05, ADD-06, ADD-07 all SATISFIED via gap-closure plans. ADD-03 was already verified in original 20.1-VERIFICATION.md and is not in gap-closure scope. No orphaned requirements.

## Human Verification Required

Status is `human_needed` because the original UAT gaps were surfaced by browser-based human testing — the same medium is required to confirm closure end-to-end against real services (catalog DB upsert, verdict compute, etc.).

Inline-verified during this session (already approved by user via /search?tab=watches):
- Item 6: /search Watches tab — split row affordance, 2 CTAs (no Hide), chevron-only trigger ✓
- Item 7: /search All tab — Watches section consistency ✓ (implementation complete; user did not browser-verify)

Pending browser verification:
1. Real-URL paste with non-empty collection — CollectionFitCard renders (gap 1)
2. Wishlist commit textarea pre-fill on real verdict (gap 2)
3. Skip rail chip — both happy path AND synth-id fallback (gap 3)
4. Manual entry Cancel link — direct ingress (gap 4)
5. Manual entry Cancel link — post-failure ingress (gap 4)
8. Browser console observability — verify warn statements appear when null-verdict path fires
