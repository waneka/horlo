---
phase: 20.1-add-watch-flow-rethink-verdict-as-step
verified: 2026-04-30T19:50:00Z
status: human_needed
score: 16/16 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 7/7
  scope: gap-closure (UAT gaps 1, 2, 3, 4, 5 + inline extended UX from Plan 07)
  closure_plans: [20.1-06, 20.1-07, 20.1-08]
  gaps_closed:
    - "UAT gap 1: 'Your collection is empty' copy with non-empty collection (verdict-null conflation)"
    - "UAT gap 2: WishlistRationalePanel textarea empty (no pre-fill) — same upstream cause as gap 1"
    - "UAT gap 3: No 'Recently evaluated' rail chip after Skip (silent guard on empty catalogId)"
    - "UAT gap 4: No way to back out of manual-entry without page refresh"
    - "UAT gap 5: Search-row accordion does not visibly expand (data-attribute selector mismatch)"
  inline_extended_ux:
    - "Split row affordance: row body = Link to /catalog/[catalogId]; chevron = sole accordion trigger"
    - "Drop Hide CTA — chevron toggle replaces it"
    - "All-tab consistency — All-tab Watches section uses WatchSearchRowsAccordion"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Visual smoke — paste a real watch URL into /watch/new with a non-empty collection"
    expected: "Verdict card renders with CollectionFitCard headline + phrasings (NOT the 'Your collection is empty' copy and NOT the 'Couldn't compute fit' fallback). 3 buttons render together as a single coherent gesture."
    why_human: "End-to-end real fetch + Anthropic API + verdict compute + DB upsert path requires authenticated session and live services; no programmatic substitute for the full pipeline timing/feel."
  - test: "Wishlist commit smoke — click 'Add to Wishlist' from verdict-ready"
    expected: "WishlistRationalePanel slides in below verdict; textarea pre-filled with verdict.contextualPhrasings[0] (the gap-2 fix observed end-to-end)."
    why_human: "Pre-fill is verified by unit test in isolation, but only a real verdict round-trip confirms the upstream fix produces non-null verdict on the live extract path."
  - test: "Skip path + Recently evaluated rail smoke — fresh Skip from verdict-ready"
    expected: "Click Skip → URL input clears, focus returns to input, a chip appears in 'Recently evaluated' rail. Test BOTH the happy path (real catalogId) AND the synth-id fallback (e.g. extraction with brand/model but catalog upsert silently fails or returns null id)."
    why_human: "Synth-id rail entry is unit-tested but visual chip rendering with real thumbnail URL needs eyes-on confirmation."
  - test: "Manual entry back affordance smoke — click 'or enter manually' then back out"
    expected: "WatchForm renders with '← Cancel — paste a URL instead' link above it. Clicking the link returns to the URL paste view (paste input visible, value empty)."
    why_human: "The new gap-4 affordance is unit-tested, but visual placement above WatchForm and copy clarity ('Cancel — paste a URL instead') benefit from real-user reading."
  - test: "Manual entry back affordance from extraction-failed path"
    expected: "Trigger an extraction failure (404 host) → click 'Continue manually' → see manual-entry with the same '← Cancel — paste a URL instead' affordance above WatchForm. Click it → return to idle, URL cleared."
    why_human: "Post-failure manual-entry is unit-tested; eyes-on confirms the cancel link still renders correctly when reached via the alternate ingress path."
  - test: "/search?tab=watches inline 3 CTAs + chevron-only trigger"
    expected: "Search returns a row → row body click navigates to /catalog/[catalogId] (NEW behavior — primary action). Chevron click expands the accordion → verdict + 2 inline CTAs (Add to Wishlist, Add to Collection) appear (NO 'Hide' button — chevron click again collapses the panel)."
    why_human: "The split row/chevron affordance is the inline extended UX delivered in Plan 07's verification follow-up; only real interaction confirms split-target behavior reads correctly to a fresh user."
  - test: "/search All-tab Watches section consistency"
    expected: "Switch to the All tab and confirm the Watches section renders the same accordion shell as the Watches tab (chevron trigger, /catalog navigation on row click)."
    why_human: "Tab-level consistency was the inline fix during verification; needs confirmation that the All-tab section now matches."
  - test: "Console observability smoke — open DevTools and reproduce a verdict-null path"
    expected: "If catalog upsert returns null OR fails, the browser console shows '[AddWatchFlow] verdict=null path: <branch>' with a catalogIdError context payload. This is the Part B observability surface — engineers tailing the console during reproduction can identify which silent path fired."
    why_human: "Diagnostic surface is unit-tested for one branch (catalogId-missing); browser console verification confirms the observability is wired and visible to operators."
---

# Phase 20.1: Gap-Closure Re-Verification Report

**Phase Goal:** The add-watch flow is reorganized so URL-paste → verdict preview → status decision (wishlist / owned / skip) is a single coherent gesture. The "skip" path covers the lightweight evaluate-only use case without requiring a separate /evaluate route. Manual entry stays as a secondary affordance.

**Re-verification Scope:** Five UAT gaps surfaced during initial verification (recorded in `20.1-VERIFICATION.md`, status=human_needed) + extended UX work that landed inline during Plan 07's human checkpoint:

- UAT gap 1, 2, 3 → Plan 06 (`20.1-06-PLAN.md` / `20.1-06-SUMMARY.md`)
- UAT gap 5 → Plan 07 (`20.1-07-PLAN.md` / `20.1-07-SUMMARY.md`)
- UAT gap 4 → Plan 08 (`20.1-08-PLAN.md` / `20.1-08-SUMMARY.md`)
- Inline extended UX (split row affordance, drop Hide, All-tab consistency) → committed under Plan 07 (commit `0a7e757`)

**Verified:** 2026-04-30T19:50:00Z
**Status:** human_needed
**Re-verification:** Yes — gap closure pass over 20.1-VERIFICATION.md's deferred human-verification items.

---

## Goal Achievement (Gap-Closure must_haves)

### Plan 20.1-06 must_haves (UAT gaps 1, 2, 3 — verdict-null observability + honest fallback)

| #   | Truth                                                                                                                                                                                              | Status     | Evidence |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | When the viewer has a non-empty collection and verdict computation fails, the verdict-ready render shows a 'Couldn\'t compute fit' fallback (NOT the empty-collection copy)                       | ✓ VERIFIED | `VerdictStep.tsx:88-98` — three-way branch: `verdict ? CollectionFitCard : hasCollection ? "Couldn't compute fit right now…" : "Your collection is empty…"`. Test `AddWatchFlow.test.tsx` "UAT gap 1 — non-empty collection + null catalogId surfaces 'Couldn't compute fit' copy" PASSES. |
| 2   | When the viewer has an empty collection, the verdict-ready render shows the empty-collection copy as before (no regression)                                                                       | ✓ VERIFIED | `VerdictStep.tsx:94-97` — final ternary leaf preserves the existing D-06 copy "Your collection is empty — fit score not available yet." Test `VerdictStep.test.tsx` "renders empty-collection copy when verdict=null AND hasCollection=false" PASSES. |
| 3   | WishlistRationalePanel pre-fills its textarea with verdict.contextualPhrasings[0] whenever a non-null verdict is computed (existing behavior preserved)                                            | ✓ VERIFIED | Regression-guard test `WishlistRationalePanel.test.tsx` "pre-fills textarea with verdict.contextualPhrasings[0] when verdict is a full bundle" PASSES. The pre-fill behavior was always correct in `WishlistRationalePanel.tsx`; gap 2 was an upstream null-verdict consequence which is now resolved by the upstream Part A fix. |
| 4   | Skip from verdict-ready always pushes a chip to the Recently evaluated rail — even when catalogId is empty/null — using a synthesized stable rail entry id                                         | ✓ VERIFIED | `AddWatchFlow.tsx:210-235` — `handleSkip` always synthesizes `synth:{brand}|{model}` when state.catalogId is empty and pushes a RailEntry. Test `AddWatchFlow.test.tsx` "UAT gap 3 — Skip with null catalogId still pushes a chip to Recently evaluated rail" PASSES. The previous `if (state.catalogId)` guard is gone (verified — 0 grep matches). |
| 5   | /api/extract-watch surfaces an explicit catalogIdError indicator when the catalog upsert throws; the client logs it via console.warn                                                              | ✓ VERIFIED | `route.ts:53-93` — captures three classes of errors into `catalogIdError`: 'brand/model missing from extraction', 'catalog upsert returned null id', 'catalog upsert threw: <truncated msg>'. Returned in JSON at `route.ts:140`. AddWatchFlow consumes it at `AddWatchFlow.tsx:138`. |
| 6   | AddWatchFlow.handleExtract emits a console.warn before each setState({ verdict: null }) branch identifying which path fired                                                                       | ✓ VERIFIED | `AddWatchFlow.tsx:144` (collection-empty), `:160` (catalogId-missing), `:175` (verdict-failed). All three present (grep "console.warn.*verdict=null path" returns 3). Each warn includes a context payload (catalogId/catalogIdError/error). |
| 7   | getVerdictForCatalogWatch returns its error string verbatim to the client, not collapsed to null; AddWatchFlow logs the error string when surfacing the 'Couldn\'t compute fit' fallback           | ✓ VERIFIED | `verdict.ts:44, 49, 59, 75` — error strings are stable: 'Not authenticated', 'Invalid request', 'Watch not found', "Couldn't compute verdict.". JSDoc at `verdict.ts:30-34` locks the observability contract. AddWatchFlow logs `v.error` on failure (`AddWatchFlow.tsx:175`). |

### Plan 20.1-07 must_haves (UAT gap 5 — search-row accordion expand)

| #   | Truth                                                                                                                                                                            | Status     | Evidence |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | Clicking a search row's Accordion.Trigger expands the accordion and renders the verdict + (3→2) inline CTAs visibly                                                              | ✓ VERIFIED | `WatchSearchRowsAccordion.tsx:120-127` — `Accordion.Trigger` is the chevron only (slot pattern via `WatchSearchRow.trigger` prop). Panel className uses `data-[open]:animate-in data-[open]:fade-in-0` (matching base-ui's `data-open` attribute). Test "data-open DOM contract assertion" PASSES; integration test "tests/components/search/WatchSearchRow.test.tsx" + "tests/integration/add-watch-flow-search-cta.test.tsx" — 21/21 PASS. |
| 2   | The previous Tailwind data-[state=open]: / data-[state=closed]: selectors that NEVER matched base-ui's data-open attribute are replaced with selectors that DO match              | ✓ VERIFIED | `WatchSearchRowsAccordion.tsx:131` — selectors are now `data-[open]:` and `data-[ending-style]:`. `grep -E "data-\[state="` returns 0 matches in WatchSearchRowsAccordion.tsx (anti-regression test in WatchSearchRowsAccordion.test.tsx asserts this regex). |
| 3   | An integration test asserts visible verdict content appears (not just role/state assertions) after click                                                                          | ✓ VERIFIED | `WatchSearchRowsAccordion.test.tsx` — 6 tests including "data-open DOM contract" + "className anti-regression against data-[state=open]:" + "Chevron toggle collapses panel" + "Row body navigates to /catalog/[catalogId]". 6/6 PASS. |
| 4   | The 'Evaluate' span on WatchSearchRow is no longer ambiguous as a button — replaced with split affordance (row=Link, chevron=trigger)                                              | ✓ VERIFIED | Inline extended UX delivered: `WatchSearchRow.tsx:30-68` — row body is now a `<Link href="/catalog/${catalogId}">`; the parent component owns the chevron `Accordion.Trigger` via the `trigger` slot prop. No `buttonVariants` reference, no `isOpen` prop, no "Evaluate"/"Tap to evaluate" copy in WatchSearchRow.tsx. The split is cleaner than the original Plan 07 spec (which proposed "Tap to evaluate" copy). |
| 5   | Before declaring fix complete, a one-time instrumentation log confirmed the click handler IS firing                                                                                | ✓ VERIFIED | Plan 07 Task 1 added temp `console.log('[accordion] handleValueChange', next)`; user confirmed in browser (Plan 07 SUMMARY: "verified by user 2026-04-30"); Task 4 removed the instrumentation. `grep -c "console.log.*accordion"` returns 0. |

### Plan 20.1-08 must_haves (UAT gap 4 — manual-entry exit)

| #   | Truth                                                                                                                                                                               | Status     | Evidence |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | On /watch/new, when the user is in manual-entry mode (after clicking 'or enter manually' OR after extraction failure → 'Continue manually'), they see a discoverable in-page affordance to back out of manual-entry and return to the URL extract flow without refreshing the page | ✓ VERIFIED | `AddWatchFlow.tsx:391-407` — manual-entry render branch wraps `<WatchForm>` in `<div className="space-y-4">` with a "← Cancel — paste a URL instead" `<button>` rendered above. The button is plain text-link styled (matches PasteSection's "or enter manually" treatment for visual symmetry). |
| 2   | The new affordance reuses the existing handleStartOver handler (no new state-machine logic)                                                                                         | ✓ VERIFIED | `AddWatchFlow.tsx:395` — `onClick={handleStartOver}`. `handleStartOver` defined at `AddWatchFlow.tsx:292-295` does `setUrl('')` + `setState({ kind: 'idle' })`. `grep -c "handleStartOver"` returns 4 (definition + 3 onClick callsites: extraction-failed "Try another URL", manual-entry Cancel, comment reference). No new state-machine logic added. |
| 3   | Clicking the affordance transitions FlowState back to { kind: 'idle' } and clears the URL input                                                                                     | ✓ VERIFIED | Verified via test `AddWatchFlow.test.tsx` "UAT gap 4 — back-link from direct manual-entry returns to idle and clears URL" PASSES (paste input visible after click; value === ''). |
| 4   | Both manual-entry ingress paths (handleManualEntry from PasteSection, handleContinueManually from extraction-failed) gain the back affordance                                       | ✓ VERIFIED | The Cancel button renders inside the unconditional `state.kind === 'manual-entry'` branch; both ingress paths land in the same render branch. Tests `AddWatchFlow.test.tsx` cover both paths: direct ingress test PASSES; "UAT gap 4 — back-link from post-failure manual-entry returns to idle and clears URL" PASSES. |

**Score:** 16/16 truths verified across all three closure plans.

---

## Required Artifacts (Three-Level Verification)

| Artifact | Expected | Exists | Substantive | Wired | Data Flows | Status |
| -------- | -------- | ------ | ----------- | ----- | ---------- | ------ |
| `src/components/watch/VerdictStep.tsx` | hasCollection: boolean prop drives empty-vs-failed-fit copy split | ✓ | ✓ (5 hasCollection refs; three-way branch at lines 88-98) | ✓ (rendered by AddWatchFlow at lines 342-352 and 358-367) | ✓ (verdict prop drives CollectionFitCard render; hasCollection drives fallback copy branch) | ✓ VERIFIED |
| `src/components/watch/AddWatchFlow.tsx` | hasCollection threading + handleSkip synthesizer + 3 console.warn observability + manual-entry Cancel link | ✓ | ✓ (3 hasCollection refs, 1 synth: prefix, 3 console.warn diagnostic, "Cancel — paste a URL instead" present) | ✓ (rendered by app/watch/new/page.tsx) | ✓ (handleExtract reads catalogIdError; handleSkip pushes synth-id rail entries; handleStartOver wired to manual-entry Cancel) | ✓ VERIFIED |
| `src/app/api/extract-watch/route.ts` | catalogIdError indicator in JSON response | ✓ | ✓ (5 catalogIdError refs; let-binding declared, 3 assignments per branch, returned in NextResponse.json) | ✓ (consumed by AddWatchFlow.handleExtract) | ✓ (catalogIdError populated by 4 distinct paths; surfaced verbatim in response) | ✓ VERIFIED |
| `src/app/actions/verdict.ts` | Observability JSDoc lock on error contract (no code change) | ✓ | ✓ (JSDoc lines 30-34 document the stable-error-string contract) | ✓ (called by AddWatchFlow.handleExtract and WatchSearchRowsAccordion.handleValueChange) | ✓ (4 stable error strings on failure paths) | ✓ VERIFIED |
| `src/components/search/WatchSearchRowsAccordion.tsx` | Accordion.Panel data-[open]: + chevron-only trigger + drop Hide CTA | ✓ | ✓ (data-[open]: + data-[ending-style]: + data-[panel-open]: in className; ChevronDown imported; no "Hide" button JSX) | ✓ (rendered by SearchPageClient WatchesPanel + AllTabResults Watches section) | ✓ (catalogId-keyed Accordion.Item; verdict cache + lazy compute) | ✓ VERIFIED |
| `src/components/search/WatchSearchRow.tsx` | Row body = Link to /catalog/[id]; chevron passed as trigger slot prop | ✓ | ✓ (Link import, href={`/catalog/${result.catalogId}`}, trigger?: React.ReactNode prop, no isOpen / no buttonVariants / no "Evaluate" copy) | ✓ (rendered inside `WatchSearchRowsAccordion.Header`) | ✓ (result + q + trigger props) | ✓ VERIFIED |
| `src/components/search/AllTabResults.tsx` | Renders WatchSearchRowsAccordion (not bare WatchSearchRow) | ✓ | ✓ (line 106-110 — `<WatchSearchRowsAccordion results={watchesCapped} q={q} collectionRevision={collectionRevision} />`) | ✓ (rendered by SearchPageClient All-tab) | ✓ (collectionRevision threaded from SearchPageClient) | ✓ VERIFIED |
| `src/components/search/SearchPageClient.tsx` | Threads collectionRevision to AllTabResults | ✓ | ✓ (line 119 — `collectionRevision={collectionRevision}` prop) | ✓ (Server Component watch/new/page wires it) | ✓ (collectionRevision flows from page → SearchPageClient → AllTabResults → WatchSearchRowsAccordion) | ✓ VERIFIED |

---

## Key Link Verification (Gap-Closure Wiring)

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `AddWatchFlow.handleExtract` | `VerdictStep verdict-ready render` | `hasCollection={hasCollection}` prop threaded from `collectionRevision > 0` | ✓ WIRED | `AddWatchFlow.tsx:84, 345, 361` |
| `AddWatchFlow.handleSkip` | `RecentlyEvaluatedRail` | `setRail` with synthesized id when catalogId empty | ✓ WIRED | `AddWatchFlow.tsx:219-221` (synth id), `:230-232` (setRail) |
| `/api/extract-watch` | `AddWatchFlow.handleExtract` | `catalogIdError` JSON field consumed via `console.warn` | ✓ WIRED | `route.ts:140` (response), `AddWatchFlow.tsx:138` (consumption), `:144, 160` (warn surfaces it) |
| `getVerdictForCatalogWatch` | `AddWatchFlow.handleExtract` (verdict-failed warn) | `v.error` string surfaced via `console.warn` | ✓ WIRED | `AddWatchFlow.tsx:175` |
| `WatchSearchRowsAccordion.tsx Accordion.Panel` | `base-ui's data-open attribute` | `data-[open]:` + `data-[ending-style]:` Tailwind selectors matching base-ui DOM | ✓ WIRED | `WatchSearchRowsAccordion.tsx:131` (verified against `node_modules/@base-ui/react/accordion/panel/AccordionPanelDataAttributes.js`) |
| `WatchSearchRow click target (row body)` | `/catalog/[catalogId]` route | `<Link href={`/catalog/${result.catalogId}`}>` | ✓ WIRED | `WatchSearchRow.tsx:30-31` |
| `WatchSearchRow chevron slot` | `Accordion.Trigger` button | `trigger` slot prop populated by parent with Accordion.Trigger | ✓ WIRED | `WatchSearchRow.tsx:69` (renders `{trigger}`); `WatchSearchRowsAccordion.tsx:119-126` (passes Trigger as trigger prop) |
| `manual-entry render branch new button` | `handleStartOver` | `onClick={handleStartOver}` | ✓ WIRED | `AddWatchFlow.tsx:395` |

---

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| AddWatchFlow `hasCollection` | boolean | `collectionRevision > 0` (prop from page Server Component) | ✓ Real (collection length read server-side) | ✓ FLOWING |
| AddWatchFlow `catalogIdError` | string \| null | `data.catalogIdError ?? null` from extract-watch JSON | ✓ Real (route always sets it; null on success) | ✓ FLOWING |
| AddWatchFlow rail entry `catalogId` (synth case) | string | `synth:${brand.trim().toLowerCase()}|${model.trim().toLowerCase()}` | ✓ Real (deterministic from extracted brand/model) | ✓ FLOWING |
| WatchSearchRowsAccordion Accordion.Panel `data-open` | DOM attribute | base-ui sets it on the panel element when open | ✓ Real (verified via test `WatchSearchRowsAccordion.test.tsx` data-open DOM contract assertion) | ✓ FLOWING |
| WatchSearchRow row body `href` | string | `/catalog/${result.catalogId}` interpolated | ✓ Real (catalogId is a UUID from search results) | ✓ FLOWING |
| AddWatchFlow manual-entry Cancel `onClick` | function | `handleStartOver` reference | ✓ Real (function clears url + sets state idle; tested) | ✓ FLOWING |

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| All gap-closure component tests pass | `npx vitest run AddWatchFlow.test.tsx VerdictStep.test.tsx WishlistRationalePanel.test.tsx WatchSearchRowsAccordion.test.tsx` | 28/28 PASS (4 files) | ✓ PASS |
| Search-area extended UX tests pass | `npx vitest run tests/components/search/WatchSearchRow.test.tsx tests/components/search/AllTabResults.test.tsx tests/integration/add-watch-flow-search-cta.test.tsx` | 21/21 PASS (3 files) | ✓ PASS |
| `data-[state=` selectors purged from WatchSearchRowsAccordion | `grep -E "data-\[state=" src/components/search/WatchSearchRowsAccordion.tsx` | 0 matches | ✓ PASS |
| `data-[open]:` / `data-[ending-style]:` / `data-[panel-open]:` selectors present | `grep -nE "data-\[(open|ending-style|panel-open)" src/components/search/WatchSearchRowsAccordion.tsx` | 3 occurrences (panel-open in trigger className; open + ending-style in panel className) | ✓ PASS |
| `if (state.catalogId)` silent guard removed from handleSkip | `grep -nE "if \(state\.catalogId\)" src/components/watch/AddWatchFlow.tsx` | 0 matches in active code (only present as comment-historical reference at line 207) | ✓ PASS |
| 3 console.warn observability statements in handleExtract | `grep -cE "console.warn.*verdict=null path" src/components/watch/AddWatchFlow.tsx` | 3 | ✓ PASS |
| Manual-entry Cancel back affordance present | `grep -nE "Cancel.*paste a URL" src/components/watch/AddWatchFlow.tsx` | 2 occurrences (1 in JSX comment + 1 button text label) | ✓ PASS |
| handleStartOver wired to ≥3 callsites (manual-entry Cancel + extraction-failed Try another URL + comment reference) | `grep -c "handleStartOver" src/components/watch/AddWatchFlow.tsx` | 4 | ✓ PASS |
| Temporary instrumentation log removed | `grep -c "console.log.*accordion" src/components/search/WatchSearchRowsAccordion.tsx` | 0 | ✓ PASS |
| All 8 gap-closure commits present in git log | `git log --oneline --all \| grep -E "(8814f94\|6608e28\|2ec3c7d\|e3d1c75\|d4de451\|0a7e757\|0ef5863\|5fdda06)"` | 8/8 | ✓ PASS |
| AllTabResults uses WatchSearchRowsAccordion (not bare WatchSearchRow) | `grep -c "WatchSearchRowsAccordion" src/components/search/AllTabResults.tsx` | 2 (import + usage) | ✓ PASS |
| Hide CTA removed from WatchSearchRowsAccordion | `grep -nE "['\"]Hide['\"]" src/components/search/WatchSearchRowsAccordion.tsx` | Only present in JSDoc comment ('"Hide" CTA removed'); no JSX button | ✓ PASS |

---

## Requirements Coverage (ADD-01..ADD-07)

The plan frontmatters declare the following requirement IDs:
- Plan 20.1-06: ADD-01, ADD-02, ADD-04
- Plan 20.1-07: ADD-06
- Plan 20.1-08: ADD-05, ADD-07

Plus ADD-03 (covered by prior 20.1 plans, not a gap-closure target).

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | ------------- | ----------- | ------ | -------- |
| ADD-01 | 20.1-06 (gap closure) + prior 20.1 plans | URL-paste flow runs extract → CollectionFitCard verdict preview → 3-button status decision in a single coherent flow | ✓ SATISFIED | Verdict pipeline now has honest fallback copy (Couldn't compute fit) AND observability when null-verdict paths fire — gap 1 root cause was misleading the user about why no verdict appeared. |
| ADD-02 | 20.1-06 (gap closure) | "Add to Wishlist" CTA from verdict preview commits a wishlist-status row with extracted fields and optional verdict rationale captured in `notes` | ✓ SATISFIED | WishlistRationalePanel pre-fill regression-guard test PASSES; gap 2 was upstream null-verdict (resolved by gap 1 fix). |
| ADD-03 | (prior 20.1 plans — not in gap-closure scope) | "Add to Collection" CTA from verdict preview commits an owned-status row with extracted fields prefilled | ✓ SATISFIED (already verified in original 20.1-VERIFICATION.md) | No regression in gap-closure plans. |
| ADD-04 | 20.1-06 (gap closure) | "Skip" CTA from verdict preview closes the flow with no commit and offers "evaluate another" | ✓ SATISFIED | handleSkip always pushes a rail entry (synth-id when catalogId empty); test PASSES. |
| ADD-05 | 20.1-08 (gap closure) | Manual entry path is preserved as a secondary affordance and bypasses the verdict step | ✓ SATISFIED | manual-entry render branch unchanged + new "Cancel — paste a URL instead" exit affordance ensures users are not trapped (dead-end avoidance). |
| ADD-06 | 20.1-07 (gap closure) | Catalog deep-link from `/search?tab=watches` Evaluate CTA offers an "Add to Wishlist / Collection" path that prefills via `catalogId`, reusing same verdict step | ✓ SATISFIED | Accordion expand now visibly works (data-[open]: selectors); split row affordance lets users either navigate to /catalog/[id] OR expand inline; AllTabResults consistency fix means All tab matches Watches tab. |
| ADD-07 | 20.1-08 (gap closure) | Extraction failure inside new flow shows fallback that preserves any partially-extracted data and offers manual continuation (no dead-end) | ✓ SATISFIED | extraction-failed branch unchanged from prior verification; manual-entry exit affordance is a related dead-end fix that complements ADD-07's design intent (no path strands the user). |

**Coverage check:** No orphaned requirements. All 3 plans' declared requirement IDs (ADD-01, ADD-02, ADD-04, ADD-05, ADD-06, ADD-07) verified against implementation evidence. ADD-03 was not in gap-closure scope and was already verified in the original 20.1-VERIFICATION.md.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none — no blocking anti-patterns) | — | — | — | The 3 console.warn statements in AddWatchFlow.tsx are intentional Part B observability surface (not a stub); the hardcoded fallback strings in VerdictStep.tsx are user-visible copy (not stubs); the synth-id pattern in handleSkip is documented and isolated from real UUID validation paths. |

---

## Human Verification Required

8 items need human/visual testing (see frontmatter `human_verification` for full descriptions):

1. **Visual smoke — paste real URL with non-empty collection** — verify CollectionFitCard renders (no fallback copy), confirming the upstream null-verdict cause from gap 1 is resolved end-to-end.
2. **Wishlist commit smoke** — verify textarea pre-fill (gap 2) on real verdict round-trip.
3. **Skip path + Recently evaluated rail smoke** — verify chip rendering for both happy path AND synth-id fallback (gap 3).
4. **Manual entry back affordance smoke (direct ingress)** — verify "← Cancel — paste a URL instead" placement and click behavior.
5. **Manual entry back affordance smoke (post-failure ingress)** — verify same affordance reachable via extraction-failed → Continue manually path.
6. **/search?tab=watches inline 3 CTAs + chevron-only trigger** — verify split row affordance (row body navigates, chevron expands), 2 CTAs (Wishlist/Collection — no Hide), Pitfall 2 stopPropagation.
7. **/search All-tab Watches section consistency** — verify All-tab Watches uses the same accordion as Watches tab.
8. **Console observability smoke** — verify `[AddWatchFlow] verdict=null path: ...` warns appear in browser console when null-verdict path fires.

---

## Gaps Summary

**No actionable gaps.** All 16 gap-closure must_haves verified across 3 closure plans. All 8 expected commits present. All 49 tests across the 7 modified test files PASS (28 watch-area + 21 search-area). No regressions detected against the previous verification's baseline (7/7 original truths still hold). All 5 originally-flagged UAT gaps (1, 2, 3, 4, 5) are closed at code level.

Status remains `human_needed` (not `passed`) because:

1. UAT gaps were originally surfaced by browser-based human testing — the same medium is required to confirm closure.
2. Several gaps depend on real services (catalog DB upsert, Anthropic API, verdict compute pipeline) that cannot be exercised via unit/integration tests alone.
3. Plan 07's inline extended UX (split row affordance + drop Hide + All-tab consistency) was approved during the earlier in-browser verification but warrants a fresh confirmation pass alongside the gap-1/2/3/4 fixes.

The 8 human verification items above represent the qualitative smoke-test surface needed to formally close out Phase 20.1.

---

_Verified: 2026-04-30T19:50:00Z_
_Verifier: Claude (gsd-verifier)_
_Mode: re-verification (gap-closure for plans 20.1-06, 20.1-07, 20.1-08)_
