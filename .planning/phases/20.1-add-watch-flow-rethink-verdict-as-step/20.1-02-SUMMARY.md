---
phase: 20.1-add-watch-flow-rethink-verdict-as-step
plan: 02
subsystem: api
tags: [api, profile-cards, response-shape, react, next.js, tdd]

# Dependency graph
requires:
  - phase: 17-catalog-foundation
    provides: catalogId from upsertCatalogFromExtractedUrl
  - phase: 20.1-01
    provides: RED test scaffolds (created inline here since Plan 01 ran in parallel worktree)
provides:
  - "/api/extract-watch JSON response now includes catalogId field (D-08)"
  - "AddWatchCard accepts variant?: 'collection' | 'wishlist' (D-15 + D-16 shared component)"
  - "WishlistTabContent renders end-of-grid AddToWishlist card when isOwner+populated (D-16)"
affects:
  - 20.1-04 (AddWatchFlow consumes catalogId from extract-watch response)
  - 20.1-03 (atomic flow primitives use the shared AddWatchCard component pattern)
  - 25 (UX-02 will own EMPTY-state Wishlist CTA — out of scope for D-16)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Variant-prop on shared visual component instead of duplicate file (CONTEXT.md Open Q3 recommendation)"
    - "Default-value at destructure for prop fallback (variant = 'collection')"
    - "Single canonical entry-point href across variants — verdict step is shared"

key-files:
  created:
    - "src/components/profile/AddWatchCard.test.tsx"
    - "src/components/profile/WishlistTabContent.test.tsx"
  modified:
    - "src/app/api/extract-watch/route.ts"
    - "src/components/profile/AddWatchCard.tsx"
    - "src/components/profile/WishlistTabContent.tsx"
    - "tests/integration/phase17-extract-route-wiring.test.ts"

key-decisions:
  - "Plan 01 RED scaffolds were absent in this worktree (parallel wave 0 ran elsewhere) — test files for AddWatchCard + WishlistTabContent + extract-route D-08 it() blocks created inline here so the GREEN flip can be self-verified atomically; Plan 01's broader scaffold set still owns watch-flow/search/page-level tests"
  - "Variant-prop refactor (single component, two label-only variants) preserves byte-identical visual treatment — dashed-border + Plus icon + className unchanged"

patterns-established:
  - "Shared CTA card with variant prop — preferred over duplicate AddToWishlistCard file (per CONTEXT.md D-15/D-16 discretion + RESEARCH §Open Q3)"
  - "End-of-grid CTA on populated tab only; empty-state CTA copy is Phase 25 territory"

requirements-completed: [ADD-06]

# Metrics
duration: ~7min
completed: 2026-04-30
---

# Phase 20.1 Plan 02: Wave 1 leaf changes (D-08 + D-15 + D-16) Summary

**`/api/extract-watch` response gains `catalogId` field, AddWatchCard becomes a variant-prop component, and WishlistTabContent renders an end-of-grid "Add to Wishlist" card when the viewer owns a populated wishlist.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-04-30T07:54:00Z
- **Completed:** 2026-04-30T14:59:28Z
- **Tasks:** 2
- **Files modified:** 6 (3 source, 1 existing test extended, 2 test files created)

## Accomplishments

- **D-08:** `/api/extract-watch` JSON response now includes `catalogId: string | null` so Plan 04's AddWatchFlow can call `getVerdictForCatalogWatch` immediately on extraction success without a second round-trip
- **D-15:** `AddWatchCard` default variant text + aria-label changed from "Add Watch" → "Add to Collection"
- **D-16:** `AddWatchCard` accepts `variant?: 'collection' | 'wishlist'`; new wishlist variant renders "Add to Wishlist" with same dashed-border + Plus visual treatment
- **D-16 wiring:** `WishlistTabContent` renders `<AddWatchCard variant="wishlist" />` after the grid map when `isOwner && watches.length > 0` (empty-state branch unchanged — Phase 25 UX-02 owns that)
- **Self-verifying tests:** 9 RTL tests cover variant + isOwner gating + backwards-compat; 2 D-08 it() blocks added to extract-route-wiring.test.ts

## Task Commits

Each task committed atomically with `--no-verify` (parallel wave guidance):

1. **Task 1: D-08 catalogId in /api/extract-watch response** — `5d326a3` (feat)
2. **Task 2: AddWatchCard variant prop + WishlistTabContent end-of-grid CTA** — `9de0be7` (feat)

_Note: Both tasks are TDD per plan; the RED scaffolds for Task 2 were authored inline in the same commit as the GREEN implementation since Plan 01's broader scaffold set lives in a sibling worktree. Verified RED first via `npx vitest run` showing 5 failed before the GREEN edits to AddWatchCard.tsx and WishlistTabContent.tsx, then 9 passed after._

## Files Created/Modified

- `src/app/api/extract-watch/route.ts` — Added `catalogId` to NextResponse.json payload, single-line addition with Phase 20.1 D-08 comment marker (lines 114-121)
- `src/components/profile/AddWatchCard.tsx` — Refactored from no-props "Add Watch" to `variant?: 'collection' | 'wishlist'` with default 'collection' rendering "Add to Collection" and 'wishlist' rendering "Add to Wishlist"; visual classes preserved
- `src/components/profile/WishlistTabContent.tsx` — Added `import { AddWatchCard }`, destructures `isOwner`, renders `{isOwner && <AddWatchCard variant="wishlist" />}` after grid map; comment block on isOwner replaced with D-16 marker; empty-state branch untouched
- `src/components/profile/AddWatchCard.test.tsx` — 4 RTL tests (D-15 default, D-16 wishlist, default fallback, explicit collection)
- `src/components/profile/WishlistTabContent.test.tsx` — 5 RTL tests (isOwner=true populated, isOwner=false, isOwner undefined backwards-compat, empty+isOwner=true, empty+isOwner=false)
- `tests/integration/phase17-extract-route-wiring.test.ts` — Two new it() blocks: catalogId UUID present when brand+model extracted; catalogId null when brand/model absent

## Decisions Made

- **Inline RED authorship for Task 2 tests** — Plan 02 wave 1 depends on Plan 01 wave 0 RED scaffolds. Plan 01 ran in a different parallel worktree and its outputs are not present in this branch. Rather than block on cross-worktree dependency, the two test files this plan needs (AddWatchCard.test.tsx, WishlistTabContent.test.tsx) and the two D-08 it() blocks in phase17-extract-route-wiring.test.ts were authored inline. Tests verified RED before GREEN edits (5 failures observed, then 9 passes after implementation). The orchestrator merge step will reconcile any duplicate scaffolds from Plan 01's worktree — both authoring paths converge on the same assertions per the plan's behavior block. This is a Rule 3 (blocking issue) auto-fix: missing dependency was inline-resolved without changing scope.
- **Type-clean test fixtures** — used `undefined` (omit) instead of `null` for ExtractedWatchData optional fields (which the type declares as `T | undefined`). Reduced total tsc errors by 2 vs baseline. Existing pre-existing test errors elsewhere unchanged.

## Deviations from Plan

**1. [Rule 3 - Blocking] Authored Plan 01 test scaffolds inline**

- **Found during:** Task 2 setup
- **Issue:** Plan 02 declares dependency on Plan 01 wave 0 having created `src/components/profile/AddWatchCard.test.tsx`, `src/components/profile/WishlistTabContent.test.tsx`, and the D-08 it() blocks in `tests/integration/phase17-extract-route-wiring.test.ts`. None existed in this worktree (Plan 01 is executing in a parallel worktree per the wave-1-runs-with-wave-0 model only when Plan 01 is wave 0).
- **Fix:** Created both test files and added both D-08 it() blocks inline so this worktree could verify RED → GREEN flips per plan's TDD requirement. All assertions match the plan's `<behavior>` specifications verbatim.
- **Files modified:** `src/components/profile/AddWatchCard.test.tsx` (new), `src/components/profile/WishlistTabContent.test.tsx` (new), `tests/integration/phase17-extract-route-wiring.test.ts` (extended with 2 it() blocks)
- **Verification:** RED state confirmed first (5 failures pre-implementation), GREEN confirmed after implementation (9 passed; 3 D-08 tests skip cleanly without DATABASE_URL — same pattern as the existing CAT-08 test in the file)
- **Committed in:** Task 1 commit `5d326a3` (D-08 it() blocks); Task 2 commit `9de0be7` (AddWatchCard.test.tsx + WishlistTabContent.test.tsx)

---

**Total deviations:** 1 auto-fixed (1 blocking dependency)
**Impact on plan:** No scope change — same assertions, just authored in this worktree's git history instead of relying on a parallel worktree merge. Orchestrator can deduplicate at merge time if Plan 01's worktree also produces these files.

## Issues Encountered

- TS errors in initial test fixtures used `null` for optional fields whose declared type is `T | undefined`. Fixed by omitting null-valued optional fields. Pre-existing pattern in the same file uses `null` and produces baseline errors that this plan deliberately did not touch (out-of-scope per plan's "Do NOT touch any other line" guidance — limited to the new it() blocks).

## RED → GREEN Flips

| Test File | Pre-Implementation | Post-Implementation |
|-----------|-------------------|---------------------|
| `src/components/profile/AddWatchCard.test.tsx` | 4 failed (AddWatchCard renders "Add Watch", no variant prop) | 4 passed |
| `src/components/profile/WishlistTabContent.test.tsx` | 1 failed (no end-of-grid card), 4 passed (negative-cases already true) | 5 passed |
| `tests/integration/phase17-extract-route-wiring.test.ts` D-08 blocks | 2 skip (DB-gated) — would fail with `body.catalogId` undefined under DB | 2 skip (DB-gated) — would pass with route now returning catalogId |

Existing `CAT-08` it() block in the same integration file remains unchanged.

## Forward Pointer

**Plan 04 (AddWatchFlow orchestrator) consumes the `catalogId` response field via fetch in `<AddWatchFlow>`.** When `/api/extract-watch` resolves with `success: true`, the client reads `body.catalogId` and (if non-null) calls `getVerdictForCatalogWatch({ catalogId, collectionRevision })` to populate the verdict step. The null fallback (no brand/model extracted) advances to the Phase 25-bound categorized-error fallback path per ADD-07.

**Plan 03 (atomic flow primitives)** can reuse the variant-prop pattern if it needs sibling card components inside the new flow — D-16 establishes "shared component with variant" as the canonical pattern over duplicate files.

## Next Phase Readiness

- D-08, D-15, D-16 satisfied at code level; ready for Wave 2 orchestration plans (03/04/05) to consume
- No DB migrations, no schema changes, no new dependencies — purely additive surface changes
- CollectionTabContent unchanged — `<AddWatchCard />` (no props) defaults to 'collection' variant, matching prior behavior
- 9/9 component tests + 4/4 profile-tab-insights regression tests + 3/3 extract-route tests (skipped gracefully without DB) green

## Self-Check: PASSED

- File `src/app/api/extract-watch/route.ts` modified — verified `grep -n "catalogId," src/app/api/extract-watch/route.ts` returns line 119 (in 113-120 range)
- File `src/components/profile/AddWatchCard.tsx` modified — verified `grep -c "variant?: 'collection' | 'wishlist'" src/components/profile/AddWatchCard.tsx` = 1
- File `src/components/profile/WishlistTabContent.tsx` modified — verified `grep -c 'variant="wishlist"' src/components/profile/WishlistTabContent.tsx` = 1
- File `src/components/profile/AddWatchCard.test.tsx` created — verified exists
- File `src/components/profile/WishlistTabContent.test.tsx` created — verified exists
- Commit `5d326a3` exists — verified `git log --oneline | grep 5d326a3` matches
- Commit `9de0be7` exists — verified `git log --oneline | grep 9de0be7` matches
- 9/9 component tests pass — verified `npx vitest run src/components/profile/AddWatchCard.test.tsx src/components/profile/WishlistTabContent.test.tsx` shows 9 passed
- No regression in profile-tab-insights — verified 4/4 pass
- tsc baseline preserved (no new errors introduced; total dropped from 10 → 8)

---
*Phase: 20.1-add-watch-flow-rethink-verdict-as-step*
*Plan: 02*
*Completed: 2026-04-30*
