---
phase: 82
plan: 03
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - src/data/watches.ts
  - src/app/w/[ref]/edit/page.tsx
  - src/components/watch/WatchForm.tsx
  - src/components/watch/WatchForm.test.tsx
autonomous: true
requirements: [UI-03]
tags: [watch-form, read-only-chip, admin-link, is-admin, viewer-gate]
truths:
  - "D-82-06: Read-only rendering gates on watch.catalogId != null; catalogId = null legacy rows keep editable Input fields"
  - "D-82-07: 'Edit catalog mapping' link cluster gates on mode === 'edit' && catalogId != null && viewerIsAdmin === true — non-admin owners see chips without the cluster"
  - "D-82-08: Two-link cluster — 'Edit brand' → /admin/brands#brand-{brandId}, 'Edit family' → /admin/families?brandId={brandId}; both ghost-variant Button rendered as Next.js Link"
  - "RESEARCH Pitfall 4 Option B: getWatchById extended to LEFT JOIN brands + watch_families and project canonicalBrand + canonicalFamily; chip renders canonicalBrand ?? watch.brand for legacy-row safety"
  - "Viewer is_admin threaded from edit page via supabase.from('profiles').select('is_admin') per SEED-018 pattern at /watch/new L100-104"
must_haves:
  truths:
    - "On /w/[ref]/edit for a watch with catalogId != null, brand + model render as read-only chips (div with aria-readonly='true'); no editable Input for those fields"
    - "On /w/[ref]/edit for a legacy watch with catalogId = null, brand + model still render as editable Input (compatibility fallback)"
    - "When viewer is BOTH the owner AND is_admin=true AND catalogId != null AND mode='edit', the 'Edit brand' and 'Edit family' link cluster renders below (or beside) the chips"
    - "When viewer is admin=false, the link cluster is NOT in the DOM (no information disclosure)"
    - "Chip content sources from canonicalBrand (LEFT JOIN through catalogId) with fallback to watch.brand for pre-Phase-81 legacy rows"
  artifacts:
    - path: "src/data/watches.ts"
      provides: "getWatchById extended with brands + watchFamilies LEFT JOIN projection returning canonicalBrand/canonicalFamily"
      contains: "canonicalBrand"
    - path: "src/app/w/[ref]/edit/page.tsx"
      provides: "SSR fetch of is_admin from profiles; viewerIsAdmin prop threaded to WatchForm"
      contains: "viewerIsAdmin"
    - path: "src/components/watch/WatchForm.tsx"
      provides: "New viewerIsAdmin prop; brand/model read-only chip gated on catalogId; admin link cluster gated on mode+catalogId+viewerIsAdmin"
      contains: "aria-readonly=\"true\""
    - path: "src/components/watch/WatchForm.test.tsx"
      provides: "4 gate cases: catalogId!=null chip, viewerIsAdmin=false hidden cluster, viewerIsAdmin=true+edit+catalogId cluster shown, catalogId=null editable Input"
      min_lines: 60
  key_links:
    - from: "src/app/w/[ref]/edit/page.tsx"
      to: "src/components/watch/WatchForm.tsx"
      via: "prop drill: viewerIsAdmin"
      pattern: "viewerIsAdmin"
    - from: "WatchForm 'Edit brand' link"
      to: "/admin/brands#brand-{brandId}"
      via: "Next.js <Link>"
      pattern: "/admin/brands#brand-"
    - from: "WatchForm 'Edit family' link"
      to: "/admin/families?brandId={brandId}"
      via: "Next.js <Link>"
      pattern: "/admin/families?brandId="
---

<objective>
Wave 2 — UI-03. Extend `getWatchById` in `src/data/watches.ts` with LEFT JOIN projections on `brands` + `watch_families` to expose `canonicalBrand` + `canonicalFamily` on the returned shape (RESEARCH Open Question 1 Option B — belt-and-suspenders for pre-Phase-81 legacy rows). Update `src/app/w/[ref]/edit/page.tsx` to also SSR-fetch the viewer's `is_admin` via `supabase.from('profiles').select('is_admin')` (mirroring `/watch/new/page.tsx` L100-104). Modify `src/components/watch/WatchForm.tsx` to accept a new `viewerIsAdmin: boolean` prop, render brand + model as read-only chips when `watch.catalogId != null`, and render a two-link "Edit catalog mapping" cluster ONLY when `mode === 'edit' && watch.catalogId != null && viewerIsAdmin`. Ship `WatchForm.test.tsx` with all 4 gate combinations from VALIDATION.md.

Purpose: Lock the personal `watches` row against user free-text drift into non-canonical brand/model strings, while giving admin-owners a deep-link path into the operator queues that Plan 04/05 will ship. Non-admin owners see the read-only chips but no admin link — no information disclosure per T-82-04.

Output: Extended DAL (5 lines added), extended edit page (~5 lines added — supabase fetch + prop pass), modified WatchForm (chip fields + prop + link cluster), new WatchForm.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-add-watch-ui-operator-admin/82-CONTEXT.md
@.planning/phases/82-add-watch-ui-operator-admin/82-RESEARCH.md
@.planning/phases/82-add-watch-ui-operator-admin/82-PATTERNS.md
@.planning/phases/82-add-watch-ui-operator-admin/82-UI-SPEC.md
@.planning/phases/82-add-watch-ui-operator-admin/82-VALIDATION.md
@src/components/watch/WatchForm.tsx
@src/app/w/[ref]/edit/page.tsx
@src/data/watches.ts
@src/app/watch/new/page.tsx

<interfaces>
<!-- Contracts this plan produces + consumes. -->

Watch type (from Phase 81 D-81-02 — already has optional brandId/familyId):
```typescript
export interface Watch {
  id: string;
  brand: string;            // may be non-canonical for pre-Phase-81 legacy rows
  model: string;            // may be non-canonical for pre-Phase-81 legacy rows
  brandId?: string;         // Phase 81 addition — canonical brand FK
  familyId?: string;        // Phase 81 addition — canonical family FK
  catalogId: string | null; // FK to watches_catalog
  status: WatchStatus;
  // ... other fields
}
```

Extended getWatchById projection (this plan adds):
```typescript
// Returns Watch & { canonicalBrand: string | undefined; canonicalFamily: string | undefined }
// canonicalBrand comes from LEFT JOIN brands ON brands.id = watches_catalog.brand_id
// canonicalFamily comes from LEFT JOIN watch_families ON watch_families.id = watches_catalog.family_id
```

WatchForm new prop:
```typescript
interface WatchFormProps {
  // ...existing...
  viewerIsAdmin: boolean  // NEW — sourced server-side at edit page
}
```

Read-only chip element (verbatim from WatchForm.tsx L376-378 status-chip pattern per PATTERNS.md L484-500):
```jsx
<div
  aria-readonly="true"
  className="flex h-9 items-center rounded-md border border-input bg-muted px-3 text-sm capitalize"
>
  {canonicalBrand ?? watch.brand}
</div>
```

Admin link cluster (per D-82-08 + UI-SPEC § WatchForm Admin link cluster L193-199):
```jsx
{mode === 'edit' && watch.catalogId != null && viewerIsAdmin && (
  <div className="flex gap-2 mt-1">
    <Button variant="ghost" size="sm" render={<Link href={`/admin/brands#brand-${watch.brandId}`} />}>
      Edit brand
    </Button>
    <Button variant="ghost" size="sm" render={<Link href={`/admin/families?brandId=${watch.brandId}`} />}>
      Edit family
    </Button>
  </div>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend getWatchById with canonical brand/family JOINs + edit page is_admin fetch</name>
  <read_first>
    - src/data/watches.ts L200-235 area (getWatchById projection — RESEARCH § Pitfall 4 flags that this function does NOT currently project brands.name; extend it)
    - src/data/hierarchy.ts L61-80 (Phase 81 Plan 05 INNER JOIN pattern for reference — this task uses LEFT JOIN because catalogId can be null on legacy rows)
    - src/app/w/[ref]/edit/page.tsx (READ full file — PATTERNS.md L612-644 shows the current shape + target extension)
    - src/app/watch/new/page.tsx L94-104 (canonical is_admin fetch pattern — mirror this exactly at the edit page)
    - src/lib/types.ts (Watch type — verify optional brandId/familyId are present from Phase 81)
    - src/db/schema.ts L519-564 (brands + watchFamilies + watchesCatalog table shapes — verify brand_id/family_id FK columns exist on watches_catalog)
    - .planning/phases/82-add-watch-ui-operator-admin/82-RESEARCH.md § Pitfall 4 (Option B recommendation) and § Pattern 9 (chip pattern)
  </read_first>
  <behavior>
    - Test 1 (DAL — canonical brand projected): getWatchById for a watch whose catalog row has brand_id='abc' and the brands table has {id:'abc', name:'Hamilton'} returns an object with canonicalBrand === 'Hamilton'.
    - Test 2 (DAL — legacy catalogId=null): getWatchById for a watch with catalogId=null returns canonicalBrand === undefined and canonicalFamily === undefined.
    - Test 3 (DAL — canonical family projected): analogous to Test 1 for canonicalFamily.
    - Test 4 (edit page — SSR fetch pattern): Grep armor — `grep -c "profiles.*is_admin" src/app/w/\\[ref\\]/edit/page.tsx` returns ≥1 (is_admin query added).
    - Test 5 (edit page — WatchForm mount): Grep — `grep -c "viewerIsAdmin=" src/app/w/\\[ref\\]/edit/page.tsx` returns exactly 1 (prop passed at the WatchForm mount).
    - Test 6 (build gate): `npm run build` exits 0.
  </behavior>
  <action>
    (1) In `src/data/watches.ts` L200-235:
       - Locate `getWatchById(userId, ref)`. Currently it LEFT JOINs `watchesCatalog` and projects columns like `brandId`/`familyId`/`catalogId` per Phase 81. Extend the query with TWO additional LEFT JOINs:
         `.leftJoin(brands, eq(brands.id, watchesCatalog.brandId))`
         `.leftJoin(watchFamilies, eq(watchFamilies.id, watchesCatalog.familyId))`
       - Extend the SELECT projection to include `canonicalBrand: brands.name` and `canonicalFamily: watchFamilies.name`.
       - Add the shape to the return type: `Watch & { canonicalBrand: string | undefined; canonicalFamily: string | undefined }` (or extend the existing exported type — planner discretion). Callers that don't consume these fields (e.g. non-edit paths) are unaffected because they're additive.
       - `.map()` boundary: coerce Drizzle's `null` result on non-matching LEFT JOIN rows to `undefined` for downstream consumers (`canonicalBrand: row.canonicalBrand ?? undefined`) — mirrors Phase 81 Plan 01 `?? undefined` pattern per STATE.md L128.
       - Verify `brands` and `watchFamilies` are direct imports from `@/db/schema` at the top of `watches.ts` — per `[[reexport-only-doesnt-bind-locally]]`.

    (2) In `src/app/w/[ref]/edit/page.tsx` (PATTERNS.md L612-644):
       - Add the Supabase server client + is_admin fetch mirroring `src/app/watch/new/page.tsx` L100-104:
         ```typescript
         const supabase = await createSupabaseServerClient()
         const [watch, profileAdminRow] = await Promise.all([
           getWatchById(user.id, ref),
           supabase.from('profiles').select('is_admin').eq('id', user.id).single(),
         ])
         const viewerIsAdmin = Boolean(profileAdminRow?.data?.is_admin)
         ```
       - Import `createSupabaseServerClient` from wherever `/watch/new/page.tsx` imports it (typically `@/lib/supabase/server` or equivalent — grep the source file to get the exact import path).
       - At the WatchForm mount: pass the new prop `<WatchForm watch={watch} mode="edit" viewerIsAdmin={viewerIsAdmin} />`.

    (3) The WatchForm itself is NOT touched in this task — Task 2 handles the component-level changes. This task's build gate will fail with a TS error until Task 2 lands (WatchForm doesn't yet accept `viewerIsAdmin`). Two options: (a) run Task 1 + Task 2 as a single logical unit (recommended — commit together, verify at Task 2 gate); OR (b) add a TS shim on WatchForm in Task 1 (widen its Props interface with `viewerIsAdmin?: boolean` only) so build passes at Task 1 boundary. Take option (a) — build gate verifies at the plan boundary, not per-task, and this eliminates the Rule 3 auto-fix churn.
  </action>
  <verify>
    <automated>grep -c "canonicalBrand" src/data/watches.ts | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -c "canonicalFamily" src/data/watches.ts | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -c "profiles.*is_admin" src/app/w/\[ref\]/edit/page.tsx | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "viewerIsAdmin" src/app/w/\[ref\]/edit/page.tsx | awk '{ if ($1 >= 2) exit 0; else exit 1 }'</automated>
  </verify>
  <done>
    - getWatchById projects canonicalBrand + canonicalFamily via LEFT JOIN on brands + watchFamilies
    - Edit page SSR-fetches is_admin and passes viewerIsAdmin to WatchForm
    - Build gate deferred to Task 2 (widened WatchForm props land there)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WatchForm — read-only chips + admin link cluster + tests</name>
  <read_first>
    - src/components/watch/WatchForm.tsx (READ full file; focus on the brand field around L328-345 and the model field just after, PLUS the L370-380 status-chip pattern which is the CANONICAL analog per PATTERNS.md L484-500)
    - src/components/watch/WatchForm.test.tsx (if it exists — extend; if not — create fresh; VALIDATION.md L44-45 says ❌ Wave 0)
    - src/components/watch/SearchEntry.test.tsx (analog for jsdom + userEvent structure per PATTERNS.md L458)
    - src/components/watch/StructuredEntryPanel.test.tsx (analog for mocking heavy sub-components pattern L466-472)
    - src/components/ui/button.tsx (verify `render` prop pattern for Next.js Link — L214 area in ListIndexClient.tsx shows `render={<Link ... />}` verbatim)
    - .planning/phases/82-add-watch-ui-operator-admin/82-UI-SPEC.md § WatchForm Read-Only Chips (L177-201) — full copy + gate + a11y contract
    - .planning/phases/82-add-watch-ui-operator-admin/82-PATTERNS.md § WatchForm.tsx (L484-530)
    - .planning/phases/82-add-watch-ui-operator-admin/82-CONTEXT.md D-82-06 / D-82-07 / D-82-08 (locked gate semantics + link URLs)
  </read_first>
  <behavior>
    - Test 1 (chip render when catalogId!=null): mount `<WatchForm watch={{...w, catalogId:'cat-1', canonicalBrand:'Hamilton', brand:'Hamilton Watch'}} mode="edit" viewerIsAdmin={false} />`. Assert `screen.getByText('Hamilton')` (chip content sources from canonicalBrand). Assert `screen.queryByRole('textbox', { name: /brand/i })` returns null (no editable input for brand).
    - Test 2 (fallback to watch.brand when canonicalBrand undefined): mount with `catalogId:'cat-1', canonicalBrand: undefined, brand:'Hamilton'`. Assert `getByText('Hamilton')` (fallback path).
    - Test 3 (viewerIsAdmin=false hides cluster): mount as Test 1 with viewerIsAdmin={false}. Assert `queryByText('Edit brand')` returns null AND `queryByText('Edit family')` returns null.
    - Test 4 (viewerIsAdmin=true + mode=edit + catalogId!=null shows cluster): mount with viewerIsAdmin={true}, mode='edit', catalogId='cat-1', brandId='b-1'. Assert `getByRole('link', { name: /edit brand/i })` has `href` matching `/admin/brands#brand-b-1`. Assert `getByRole('link', { name: /edit family/i })` has `href` matching `/admin/families?brandId=b-1`.
    - Test 5 (mode='create' hides cluster even when admin): mount with viewerIsAdmin={true}, mode='create'. Assert `queryByText('Edit brand')` returns null (gate: mode==='edit').
    - Test 6 (catalogId=null legacy renders editable Input): mount `<WatchForm watch={{...w, catalogId:null}} mode="edit" viewerIsAdmin={true} />`. Assert `getByRole('textbox', { name: /brand/i })` is present (editable Input, not chip).
    - Test 7 (a11y — chip has aria-readonly): Test 1 assertion extended — the chip element carries `aria-readonly="true"`.
    - Test 8 (grep armor — link URLs): `grep -c "/admin/brands#brand-" src/components/watch/WatchForm.tsx` returns exactly 1; `grep -c "/admin/families?brandId=" src/components/watch/WatchForm.tsx` returns exactly 1.
  </behavior>
  <action>
    (1) In `src/components/watch/WatchForm.tsx`:
       - Extend `WatchFormProps` interface with `viewerIsAdmin: boolean` (REQUIRED — non-admin edit page still threads `false`, not undefined; matches the /watch/new pattern).
       - Destructure `viewerIsAdmin` from props alongside the existing `watch`, `mode`, etc.
       - Extend the destructured `watch` reads to include `canonicalBrand?: string` and `canonicalFamily?: string` from the Task 1 extended getWatchById projection. If the local `WatchFormProps` typing doesn't currently allow additional fields on `watch`, widen it — the fields are optional and additive.

       Brand field (~L328-345 per PATTERNS.md L502-513): replace the current `<Input id="brand" value={formData.brand} onChange={...} />` block with a conditional render:
       ```jsx
       {watch.catalogId != null ? (
         <div
           id="brand"
           aria-readonly="true"
           className="flex h-9 items-center rounded-md border border-input bg-muted px-3 text-sm capitalize"
         >
           {watch.canonicalBrand ?? watch.brand}
         </div>
       ) : (
         <Input id="brand" value={formData.brand} onChange={...} placeholder="e.g., Omega" />
       )}
       ```
       Preserve the surrounding Label + Grid structure exactly. The Label's `htmlFor="brand"` still points at the id — chips + inputs both carry `id="brand"`, so the Label click semantics degrade gracefully to a11y label-only when the field is read-only.

       Model field (immediately after — L344 area): apply the SAME conditional pattern with `watch.canonicalFamily ?? watch.model`.

       Admin link cluster (below the Model field's closing tag, still inside the same grid row or as a new row — planner discretion; recommend a new row BELOW the model field for visual clarity per UI-SPEC L193-199):
       ```jsx
       {mode === 'edit' && watch.catalogId != null && viewerIsAdmin && (
         <div className="flex gap-2 mt-1">
           <Button variant="ghost" size="sm" render={<Link href={`/admin/brands#brand-${watch.brandId}`} />}>
             Edit brand
           </Button>
           <Button variant="ghost" size="sm" render={<Link href={`/admin/families?brandId=${watch.brandId}`} />}>
             Edit family
           </Button>
         </div>
       )}
       ```
       Add `import Link from 'next/link'` at the top of WatchForm.tsx if not already imported.

       Per `[[button-medium-guardrail]]`: verify no raw `font-medium` was added to WatchForm anywhere in this diff — the Button primitive handles internal weight; chip content inherits from parent. Use `font-semibold` if any custom text emphasis is needed.

       Per `[[button-outline-dark-override]]`: the ghost variant Buttons here don't override bg-*, so no dark-pair concern. Do NOT add bg-* overrides to the cluster buttons.

    (2) Create `src/components/watch/WatchForm.test.tsx` per PATTERNS.md § WatchForm.test.tsx (L456-479) analog:
       - Import `describe, it, expect, vi, beforeEach` from vitest; `render, screen` from `@testing-library/react`.
       - Mock heavy sub-components: `vi.mock('@/components/watch/CatalogPhotoUploader', ...)` if WatchForm imports it (grep to confirm); mock any other Storage / EXIF / canvas / worker-adjacent components that jsdom cannot handle.
       - Provide minimal fixture Watch objects for each test case: `{ id, brand, model, brandId, catalogId, canonicalBrand, canonicalFamily, status, ... }` — most fields default-populated with sensible values; the load-bearing fields per test are `catalogId`, `canonicalBrand`, `brand`, `brandId`.
       - Ship all 8 tests per the Behavior list above. Keep each `it()` narrowly scoped to ONE assertion class (chip render OR admin gate OR fallback). Prefer `getByText` for canonicalBrand assertion, `getByRole('link', ...)` for admin cluster links, `queryByText` / `queryByRole` for the "not in DOM" cases.

       For Test 4 (link href assertion), the render pattern uses `<Button render={<Link href="..." />}>` — the rendered DOM is a `<a href="...">` (Link resolves to anchor). Use `getByRole('link', { name: /edit brand/i })` and read the `href` attribute. Note the href attribute may be an absolute-ish string; assert with `toHaveAttribute('href', '/admin/brands#brand-b-1')` or match with a regex if the router prepends locale/basepath.

    (3) Do NOT change any Server Action behavior in WatchForm — the chip is READ-ONLY; there is no submit path change here. The existing `editWatch` submit continues to run; when catalogId != null, the brand/model form values that reach the server are unchanged from the persisted watch (because the chip render doesn't wire into formData for those fields when disabled — but preserve the existing formData spread so the payload shape is stable). Per Phase 81 DISP-02: the Server Action already auto-overwrites brand/model with canonical values on write anyway.
  </action>
  <verify>
    <automated>grep -c 'aria-readonly="true"' src/components/watch/WatchForm.tsx | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -c '/admin/brands#brand-' src/components/watch/WatchForm.tsx | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c '/admin/families?brandId=' src/components/watch/WatchForm.tsx | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c "viewerIsAdmin" src/components/watch/WatchForm.tsx | awk '{ if ($1 >= 3) exit 0; else exit 1 }' && test -f src/components/watch/WatchForm.test.tsx && npx vitest run src/components/watch/WatchForm.test.tsx --reporter=basic 2>&1 | tail -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - WatchForm accepts viewerIsAdmin prop
    - Brand + model render as read-only chips when catalogId != null (with canonicalBrand ?? watch.brand source per Pitfall 4 Option B)
    - Admin link cluster renders ONLY when mode=edit + catalogId!=null + viewerIsAdmin
    - Legacy catalogId=null rows still render editable Input
    - WatchForm.test.tsx exists with 8 tests all passing (or a superset — planner may add more)
    - `npm run build` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit page → WatchForm | `viewerIsAdmin` sourced from `profiles.is_admin` server-side; passed as prop |
| WatchForm render | Admin cluster gated on prop value; non-admin viewers get no DOM disclosure |
| Chip display source | canonicalBrand from LEFT JOIN through brand_id; belt-and-suspenders vs `watch.brand` legacy free-text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04 (Phase-level) | Information Disclosure | WatchForm admin cluster render | mitigate | viewerIsAdmin sourced server-side at page render via `supabase.from('profiles').select('is_admin')`; WatchForm gates cluster JSX on prop value; non-admin owners see chips without the link cluster (no DOM presence) |
| T-82-P03-01 | Tampering | Client-side viewerIsAdmin flip | accept | Admin routes are gated at `/admin/*` layout via `assertOwner()` (server-side redirect on failure); the WatchForm's client-side gate is UX-only, not a security gate — a client flipping the prop reveals a link that goes to `/admin/brands` which then redirects on server-side auth failure |
| T-82-P03-02 | Tampering | brandId injection into admin URL | mitigate | `watch.brandId` sourced from the DB row via getWatchById; not user-writable at edit time. If a malicious client crafts a WatchForm mount with an arbitrary brandId, the resulting link goes to `/admin/brands#brand-{X}` which fails safely (the hash-scroll effect no-ops on unknown ids) |
| T-82-P03-03 | Data drift | Chip renders stale watch.brand for legacy rows | mitigate | RESEARCH Pitfall 4 Option B: chip renders `canonicalBrand ?? watch.brand` — LEFT JOIN through brand_id provides the canonical name; fallback preserves legacy visibility |
</threat_model>

<verification>
- `npm run build` exits 0
- All 8 WatchForm.test.tsx tests pass under `npx vitest run src/components/watch/WatchForm.test.tsx`
- Grep armor: aria-readonly=true count ≥2 (brand + model chips); admin URL literals exactly 1 each; viewerIsAdmin count ≥3 in WatchForm.tsx (prop + gate + condition)
- Manual sanity (dev walkthrough — Plan 06 covers): sign in as admin viewer, `/w/[ref]/edit` on a catalog-linked watch → chips + link cluster visible → click "Edit brand" → navigates to `/admin/brands#brand-{id}` (Plan 04 will make the page render the scroll+highlight)
</verification>

<success_criteria>
- Owners of catalog-linked watches see canonical brand/model chips on edit
- Admin-owners see the deep-link cluster; non-admin owners do not
- Legacy `catalogId = null` watches retain editable Inputs (compatibility fallback)
- Chip display sources from `canonicalBrand ?? watch.brand` — safe for pre-Phase-81 rows
- Zero information disclosure to non-admin editors (T-82-04 mitigated)
</success_criteria>

<output>
After completion, create `.planning/phases/82-add-watch-ui-operator-admin/82-03-SUMMARY.md`
</output>
