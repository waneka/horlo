---
phase: 82
plan: 06
type: execute
wave: 4
depends_on: [82-02, 82-03, 82-04, 82-05]
files_modified:
  - .planning/phases/82-add-watch-ui-operator-admin/82-POST-DEPLOY.md
autonomous: false
requirements: [UI-01, UI-02, UI-03, OPS-01, OPS-02]
tags: [local-first, human-verify, prod-deploy, uat, checkpoint]
truths:
  - "[[local-first-dev]]: verify /watch/new (BrandPicker + affordance + Find specs), /w/[ref]/edit (chips + admin link cluster), /admin/brands (queue + confirm/rename/merge), /admin/families (queue + add-alias round-trip) on npm run dev against local Supabase BEFORE prod push"
  - "[[mobile-ui-verify-on-prod]]: BrandPicker touch behavior + admin queue Dialog interactions verify on iPhone Safari against prod deployment"
  - "Bundled Vercel push per Phase 81 close pattern (single deploy for wave 2/3 changes; commit graph already staged on main)"
  - "Post-close: hand-correct STATE.md next_phase per [[phase-complete-999-1-misset]] recurrence — 5th recurrence expected"
must_haves:
  truths:
    - "Local-first walkthrough passes 4 canonical steps (add unknown brand + edit catalog-linked watch as admin/non-admin + walk brands queue merge + walk families queue add-alias round-trip)"
    - "Prod deploy succeeds; iPhone Safari UAT passes 3 canonical steps (BrandPicker on mobile, /admin/brands/families accessible, /admin/families?brandId= deep-link works)"
    - "All 5 phase requirements (UI-01, UI-02, UI-03, OPS-01, OPS-02) marked ✅ in REQUIREMENTS.md"
  artifacts:
    - path: ".planning/phases/82-add-watch-ui-operator-admin/82-POST-DEPLOY.md"
      provides: "Operator-facing UAT + prod deploy runbook with sign-off checkboxes for each of the 4 local + 3 prod verification steps"
  key_links:
    - from: "local-first walkthrough"
      to: "prod deploy git push"
      via: "human-verify checkpoint gates the push"
      pattern: "checkpoint:human-verify"
    - from: "prod deploy"
      to: "iPhone Safari UAT"
      via: "human-verify checkpoint gates the requirement flip"
      pattern: "checkpoint:human-verify"
---

<objective>
Wave 4 — final phase gate. Ship `82-POST-DEPLOY.md` operator runbook and drive two human-verify checkpoints: (a) local-first verification on `npm run dev` + local Supabase per `[[local-first-dev]]`, (b) prod deploy + iPhone Safari UAT per `[[mobile-ui-verify-on-prod]]`. Between (a) and (b), operator runs `git push` (bundled Vercel deploy per Phase 81 pattern). After (b) passes, mark all 5 requirements (UI-01, UI-02, UI-03, OPS-01, OPS-02) complete in `.planning/REQUIREMENTS.md`.

Purpose: Nothing new is authored in code here. Plan 06 is the phase-close gate that satisfies the DAL-backed + UI-heavy + Server-Action-heavy contract of Phase 82 against the two runtime environments (local + prod) that build + vitest cannot cover per the durable memory rules.

Output: One committed POST-DEPLOY.md + operator sign-off blocks + REQUIREMENTS.md flip; no other artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-add-watch-ui-operator-admin/82-CONTEXT.md
@.planning/phases/82-add-watch-ui-operator-admin/82-VALIDATION.md
@.planning/phases/82-add-watch-ui-operator-admin/82-01-SUMMARY.md
@.planning/phases/82-add-watch-ui-operator-admin/82-02-SUMMARY.md
@.planning/phases/82-add-watch-ui-operator-admin/82-03-SUMMARY.md
@.planning/phases/82-add-watch-ui-operator-admin/82-04-SUMMARY.md
@.planning/phases/82-add-watch-ui-operator-admin/82-05-SUMMARY.md

<interfaces>
<!-- Contracts this plan establishes for the operator. -->

POST-DEPLOY.md structure (per Phase 76/80/81 POST-DEPLOY precedents in STATE.md):
```markdown
# Phase 82 — POST-DEPLOY Runbook

## Step 1 — Local-First Verification (before push)
### 1a — Add unknown brand via BrandPicker + affordance (UI-01/02)
[commands + expected outcomes + sign-off]

### 1b — Edit catalog-linked watch as admin owner (UI-03)
[commands + expected outcomes + sign-off]

### 1c — Walk /admin/brands queue: confirm + rename + merge (OPS-01)
[SQL seed + commands + expected outcomes + sign-off]

### 1d — Walk /admin/families queue: rename + add-alias round-trip via ingest (OPS-02)
[SQL seed + commands + expected outcomes + sign-off]

## Step 2 — Bundled Prod Deploy
[git push command + Vercel deploy watch + smoke test URL]

## Step 3 — Prod UAT (iPhone Safari)
### 3a — BrandPicker on mobile (UI-01/02)
### 3b — /admin/brands + /admin/families desktop follow-up (OPS-01/02)
### 3c — /admin/families?brandId= deep-link from WatchForm (UI-03 + OPS-02 seam)

## Requirement Sign-Off Table
| Req | Status | Sign-off |
|-----|--------|----------|
| UI-01 | ⬜ | — |
| UI-02 | ⬜ | — |
| UI-03 | ⬜ | — |
| OPS-01 | ⬜ | — |
| OPS-02 | ⬜ | — |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 82-POST-DEPLOY.md operator runbook</name>
  <read_first>
    - .planning/phases/76-video-schema-storage-server-action/76-POST-DEPLOY.md (if extant — canonical POST-DEPLOY structure per STATE.md Performance Metrics)
    - .planning/phases/79-*-POST-DEPLOY.md OR .planning/phases/81-*-POST-DEPLOY.md if extant — closest precedent
    - .planning/phases/82-add-watch-ui-operator-admin/82-CONTEXT.md § Local-First Verification (L154-158) — 7 walkthrough steps (a)-(g)
    - .planning/phases/82-add-watch-ui-operator-admin/82-VALIDATION.md § Manual-Only Verifications (L68-77)
    - .planning/phases/82-add-watch-ui-operator-admin/82-UI-SPEC.md § Copywriting Contract (verify exact user-visible strings the operator must observe)
    - src/data/families.ts + src/data/brands.ts + BrandsQueue.tsx + FamiliesQueue.tsx (Plans 04/05 outputs — reference for the SQL seed commands the runbook must include)
    - .planning/STATE.md § Accumulated Context / Phase 81 Plan 04 (bundled-deploy pattern precedent)
  </read_first>
  <action>
    Create `.planning/phases/82-add-watch-ui-operator-admin/82-POST-DEPLOY.md` following the shape in `<interfaces>` above. Every step has:
    - A concrete command (or `npm run dev` sub-action)
    - The expected observable outcome (button visible, toast shown, DB row state, etc.)
    - An operator sign-off checkbox `- [ ]` that the operator ticks when green

    Concrete step content (planner writes, executor MUST include exactly as spelled out):

    **Step 1a — Add unknown brand via BrandPicker + affordance:**
    1. `npm run dev` (against local Supabase)
    2. Sign in as `viewer@horlo.test` (password `password123`) — non-admin
    3. Navigate to `/watch/new` → click "Not finding it? Add manually" to reach the structured entry panel
    4. In the Brand field (now BrandPicker), type "CustomWatchCoTest82" (a string guaranteed not to exist)
    5. Assert the affordance button appears with the text `Couldn't find that brand — add as "CustomWatchCoTest82"`
    6. Click the affordance → popup closes, typed string is locked in the picker
    7. Fill model="TestModel", type="watch" or minimum-required fields; click **Find specs**
    8. Assert the extract flow completes without blocking (may fail on the actual URL fetch — that's fine; the assertion is that the extract route returned NO client-visible error about the brand)
    9. Verify DB: `psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -c "SELECT id, name, needs_review FROM brands WHERE name = 'CustomWatchCoTest82';"` returns 1 row with `needs_review = true`
    10. Cleanup: `DELETE FROM brands WHERE name = 'CustomWatchCoTest82';` (unblocks Step 1c which needs a clean queue)

    **Step 1b — Edit catalog-linked watch as admin owner + admin link cluster (UI-03):**
    1. Sign in as tyler (admin) — the admin user
    2. Navigate to any owned watch's edit page `/w/[ref]/edit` for a watch with catalogId != null
    3. Assert the Brand and Model fields render as read-only chips (gray-muted div with the canonical strings) — NOT editable Input
    4. Assert below/beside the chips: two ghost-variant buttons "Edit brand" and "Edit family"
    5. Sign out; sign in as `viewer@horlo.test` (non-admin); navigate to the same edit page (must be that viewer's own watch — pick a viewer-owned catalog-linked watch)
    6. Assert the chips still render (D-82-06) BUT the "Edit brand" / "Edit family" cluster is NOT present in the DOM
    7. (Optional) SQL check: `UPDATE watches SET catalog_id = NULL WHERE id = '<one-watch-id>';` — reload edit page as viewer, assert editable `<input>` fields for brand + model reappear (legacy fallback); then revert with `UPDATE watches SET catalog_id = '<original>' WHERE id = ...`

    **Step 1c — /admin/brands queue: confirm + rename + merge (OPS-01):**
    1. Sign in as tyler (admin); navigate to `/admin/brands`
    2. Assert AdminSubNav shows 4 tabs: Curated Lists / Collection Paths / Brands / Families
    3. Seed a needs_review row: `INSERT INTO brands (name, slug, needs_review) VALUES ('TestBrand82Seed', 'testbrand82seed-abc123', true);` (adjust for whatever additional NOT NULL fields exist)
    4. Refresh `/admin/brands` → assert `TestBrand82Seed` floats to top of the list with a "needs review" Badge
    5. Click **Confirm as new** → toast "Brand confirmed." → row re-renders without the Badge; verify SQL: `SELECT needs_review FROM brands WHERE name = 'TestBrand82Seed';` returns `false`
    6. Click **Rename brand** → dialog opens with "Rename TestBrand82Seed" title; change name to "TestBrand82Renamed"; click **Rename brand** in dialog → toast + refresh; verify SQL: `SELECT name, slug FROM brands WHERE id = <id>;` — name updated, slug regenerated (new random suffix)
    7. Seed a second brand: `INSERT INTO brands (name, slug, needs_review) VALUES ('TestBrand82MergeSource', 'tb82ms-abc123', false);` + insert a family with `brand_id` pointing at it: `INSERT INTO watch_families (name, brand_id) VALUES ('TestFam82', '<merge-source-id>');`
    8. Click **Merge into…** on TestBrand82MergeSource → dialog opens; select TestBrand82Renamed via BrandPicker
    9. Assert WAI-ARIA radiogroup fires with copy "Source brand has 1 families. Merging will move all families to target. Continue?" and default radio "Move all 1 families to target" selected
    10. Confirm merge → toast "Brand merged." → row disappears from queue; verify SQL: (a) `SELECT COUNT(*) FROM brands WHERE id = <merge-source-id>;` returns 0 (source deleted); (b) `SELECT brand_id FROM watch_families WHERE name = 'TestFam82';` returns the target brand's ID
    11. Cleanup: `DELETE FROM watch_families WHERE name = 'TestFam82'; DELETE FROM brands WHERE name = 'TestBrand82Renamed';`
    12. Deep-link check: from Step 1b's WatchForm, click "Edit brand" → navigates to `/admin/brands#brand-{id}` → row scrolls into view + brief bg-accent/30 highlight pulses for ~1s

    **Step 1d — /admin/families queue: rename + add-alias round-trip (OPS-02):**
    1. Sign in as tyler (admin); navigate to `/admin/families`
    2. Pick a family from the queue (or seed one: `INSERT INTO watch_families (name, brand_id, needs_review) VALUES ('TestFamily82', '<some-brand-id>', true);`)
    3. Click **Add alias** → dialog "Aliases for TestFamily82" opens
    4. Type "Test Alias" (mixed case + spaces) → click **Add alias** → dialog stays open; chip strip now shows an "test alias" Badge (normalized to lowercase+trimmed)
    5. Type "Test Alias" AGAIN → click **Add alias** → dedup no-op (chip strip still has 1 entry)
    6. SQL: `SELECT aliases FROM watch_families WHERE name = 'TestFamily82';` returns `['test alias']`
    7. Click the X on the alias chip → toast "Alias removed." → chip disappears
    8. SQL: `SELECT aliases FROM watch_families WHERE name = 'TestFamily82';` returns `[]`
    9. Round-trip check: add alias "brut date"; then `/watch/new` → structured entry with brand=(TestFamily82's brand) + model="Brut Date" → Find specs → assert `/api/extract-watch` resolves the family via alias tier (SQL post-extract: `SELECT family_id FROM watches_catalog WHERE model = 'Brut Date' ORDER BY created_at DESC LIMIT 1;` — points at TestFamily82's id, NOT a newly-created family)
    10. Cleanup: `DELETE FROM watch_families WHERE name = 'TestFamily82';` (may cascade — if not, delete any catalog rows first)
    11. Deep-link filter check: navigate `/admin/families?brandId=<any-brand-uuid>` → banner "Showing families of X. Clear filter." + only that brand's families rendered

    **Step 2 — Bundled Prod Deploy:**
    1. Verify `git status` shows all Phase 82 commits landed on `main`
    2. `git push` (bundled Vercel deploy — mirrors Phase 81 close pattern per STATE.md)
    3. Watch Vercel deploy: `gh api /repos/horlo/horlo/deployments?environment=production&per_page=1 | jq '.[0].statuses_url'` OR check Vercel dashboard
    4. Smoke: `curl -sfI https://horlo.app/watch/new | head -1` returns 200 or 307-to-/login

    **Step 3a — Prod iPhone Safari UAT — BrandPicker on mobile (UI-01/02):**
    1. On iPhone Safari, sign in
    2. Navigate to `https://horlo.app/watch/new` → "Not finding it? Add manually"
    3. Tap the Brand field → BrandPicker popup opens (44px+ tap targets per D-82-05)
    4. Type an unknown brand → "Couldn't find that brand — add as X" affordance appears; TAP fires the callback + closes popup
    5. Tap Find specs → flow completes without a mobile-specific issue

    **Step 3b — Prod /admin/brands + /admin/families desktop follow-up (OPS-01/02):**
    1. On desktop Chrome/Safari at horlo.app, sign in as tyler (admin)
    2. Visit `/admin/brands` → the queue renders; existing production `needs_review = true` rows (if any) surface at top
    3. Visit `/admin/families` → analogous
    4. NO DESTRUCTIVE ACTION on prod without a plan — Confirm/Rename/Merge/Add-alias can be exercised on real prod rows only if operator explicitly opts in. Otherwise, defer to a Q&A round where operator picks a specific target.

    **Step 3c — /admin/families?brandId= deep-link seam (UI-03 + OPS-02):**
    1. On prod, admin → own a catalog-linked watch → `/w/[ref]/edit` → click "Edit family" → navigates to `/admin/families?brandId=<real-brand-id>` → filter banner + filtered queue

    **Requirement Sign-Off Table:**
    ```
    | Req | Status | Local Sign-off | Prod Sign-off |
    |-----|--------|----------------|---------------|
    | UI-01 | ⬜ | Step 1a | Step 3a |
    | UI-02 | ⬜ | Step 1a | Step 3a |
    | UI-03 | ⬜ | Step 1b | Step 3c |
    | OPS-01 | ⬜ | Step 1c | Step 3b |
    | OPS-02 | ⬜ | Step 1d | Step 3b/3c |
    ```

    Include a footer note: after all sign-offs green, run `.planning/REQUIREMENTS.md` flip via the update below (Task 2).
  </action>
  <verify>
    <automated>test -f .planning/phases/82-add-watch-ui-operator-admin/82-POST-DEPLOY.md && grep -c "Step 1a" .planning/phases/82-add-watch-ui-operator-admin/82-POST-DEPLOY.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "Step 3" .planning/phases/82-add-watch-ui-operator-admin/82-POST-DEPLOY.md | awk '{ if ($1 >= 3) exit 0; else exit 1 }' && grep -c "UI-01" .planning/phases/82-add-watch-ui-operator-admin/82-POST-DEPLOY.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "OPS-02" .planning/phases/82-add-watch-ui-operator-admin/82-POST-DEPLOY.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }'</automated>
  </verify>
  <done>
    - 82-POST-DEPLOY.md exists with all 4 local + 3 prod steps + sign-off table
    - Each step has a concrete command + expected outcome + operator checkbox
    - The commit that lands this file is the LAST commit before `git push`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Local-first verification — operator walkthrough on npm run dev</name>
  <what-built>
    All Phase 82 code from Plans 01-05: BrandPicker + StructuredEntryPanel wire-in + WatchForm read-only chips + admin link cluster + /admin/brands full queue (Confirm/Rename/Merge with pre-flight radiogroup + deep-link) + /admin/families full queue (Confirm/Rename/Add-alias/Remove-alias + ?brandId filter + banner) + AdminSubNav 4-tab extension + DAL foundations (listBrands, listBrandsForQueue, listFamiliesForQueue, canonical JOIN projection on getWatchById).
  </what-built>
  <how-to-verify>
    (Operator: work through `.planning/phases/82-add-watch-ui-operator-admin/82-POST-DEPLOY.md` Steps 1a-1d against `npm run dev` + local Supabase — the runbook has exact commands + SQL + expected outcomes. Below is the checkpoint summary.)

    1. `npm run dev` running against local Supabase (127.0.0.1:54321 / 54322)
    2. Sign in / seed rows / walk 4 canonical steps:
       - **1a**: `/watch/new` → BrandPicker → type unknown brand → affordance visible + click → popup closes → Find specs completes → `SELECT` proves `needs_review = true` row in `brands`
       - **1b**: `/w/[ref]/edit` as admin → read-only chips + admin link cluster; sign out + in as non-admin → chips visible + NO admin cluster; toggle catalogId to null → editable Input fallback
       - **1c**: `/admin/brands` → Confirm/Rename/Merge all round-trip + WAI-ARIA radiogroup pre-flight fires with family count; deep-link `#brand-{id}` scrolls + pulses
       - **1d**: `/admin/families` → Rename + Add-alias (normalized to lowercase) + Remove-alias round-trip; a subsequent structured-entry ingest resolves via alias tier
    3. All 4 sign-off checkboxes in Step 1 of POST-DEPLOY.md ticked ✅
    4. No console errors on any of the walked flows
  </how-to-verify>
  <resume-signal>Type "approved" (all 4 steps green) OR describe issues per step. On approval, executor moves to Task 3 (prod push).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Bundled prod deploy via git push</name>
  <what-built>
    Local-first verification complete; all Phase 82 commits already on `main`.
  </what-built>
  <how-to-verify>
    1. `git status` clean (or has only the just-committed 82-POST-DEPLOY.md + summaries)
    2. `git log --oneline main | head -20` shows all Plan 01-06 commits
    3. `git push` — bundles the wave 2/3 changes into a single Vercel deploy per Phase 81 close pattern
    4. Watch Vercel deploy complete (~2-3min)
    5. Smoke: `curl -sfI https://horlo.app/watch/new | head -1` returns 200 or 307
  </how-to-verify>
  <resume-signal>Type "pushed" once Vercel deploy completed successfully (or describe deploy failure). On deploy success, executor moves to Task 4 (prod UAT).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Prod UAT — iPhone Safari + desktop follow-ups</name>
  <what-built>
    Prod deploy of Phase 82 live at https://horlo.app.
  </what-built>
  <how-to-verify>
    (Operator: work through `82-POST-DEPLOY.md` Steps 3a/3b/3c on prod.)

    - **3a (iPhone Safari — mobile-critical per `[[mobile-ui-verify-on-prod]]`)**: BrandPicker on mobile — tap targets ≥44px; affordance visible on unknown brand; tap fires callback + closes popup; Find specs completes without mobile-specific issue
    - **3b (desktop, admin)**: `/admin/brands` and `/admin/families` render; NO destructive actions on prod without explicit intent
    - **3c (desktop, admin)**: `/admin/families?brandId=<real>` deep-link filter + banner render; WatchForm "Edit family" link navigates correctly
  </how-to-verify>
  <resume-signal>Type "approved" (all prod steps green) OR describe issues. On approval, executor moves to Task 5 (requirements flip + STATE.md hand-correction).</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Flip requirements to complete + hand-correct STATE.md per phase.complete recurrence</name>
  <read_first>
    - .planning/REQUIREMENTS.md § Traceability table (L114-138)
    - .planning/STATE.md (after phase.complete has run — will be miscorrected per `[[phase-complete-999-1-misset]]`)
    - Memory: `[[phase-complete-999-1-misset]]` (5th recurrence expected)
  </read_first>
  <action>
    (1) In `.planning/REQUIREMENTS.md`:
       - Flip UI-01 status from "Pending" to "Complete" in the Traceability table (L134) — also flip the checkbox at L75
       - Flip UI-02 status → "Complete" — checkbox at L76
       - Flip UI-03 status → "Complete" — checkbox at L77
       - Flip OPS-01 status → "Complete" — checkbox at L83
       - Flip OPS-02 status → "Complete" — checkbox at L84
       - Update the "Coverage" summary: v8.4 requirements 25 total; mapped 25 (100%); milestone COMPLETE after Phase 82 close
       - Update "Last updated" line to `2026-07-13 — Phase 82 close: UI-01/02/03 + OPS-01/02 shipped; v8.4 milestone complete`

    (2) In `.planning/ROADMAP.md`:
       - Flip `- [ ] Phase 82: Add-Watch UI + Operator Admin` to `- [x] Phase 82: Add-Watch UI + Operator Admin (completed 2026-07-13)`
       - Update `## Coverage` if that table exists in v8.4 section
       - Update the milestone header at L19: `🚧 v8.4 …` → `✅ v8.4 Catalog Brand+Model Canonicalization — Phases 78-82 (shipped 2026-07-13)` — but ONLY if all Phase 82 sign-offs are ✅; otherwise leave 🚧

    (3) STATE.md hand-correction per `[[phase-complete-999-1-misset]]`:
       - After phase.complete runs, verify STATE.md `progress.percent` does NOT exceed 100
       - Verify `next_phase` is not set to `999.1` — should be `null` (v8.4 milestone complete → next action is `/gsd-complete-milestone`)
       - If phase.complete set `next_phase: 999.1`, edit STATE.md to remove that field and set milestone status hint appropriately
       - Update `progress.completed_phases` from 4 → 5; `progress.completed_plans` add Phase 82's 6 plans (19 + 6 = 25)
       - `last_activity: 2026-07-13`
       - Update `## Session Continuity` last-activity block with Phase 82 close summary

    (4) Commit: `docs(state): close Phase 82 + v8.4 milestone`
  </action>
  <verify>
    <automated>grep -c "UI-01 | Phase 82 | Complete" .planning/REQUIREMENTS.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "OPS-02 | Phase 82 | Complete" .planning/REQUIREMENTS.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "Phase 82.*completed 2026" .planning/ROADMAP.md | awk '{ if ($1 >= 1) exit 0; else exit 1 }'</automated>
  </verify>
  <done>
    - All 5 requirements flipped to Complete in REQUIREMENTS.md
    - Phase 82 marked complete in ROADMAP.md
    - STATE.md hand-corrected per phase.complete recurrence
    - Single close commit lands on main
    - Next action set: run `/gsd-complete-milestone v8.4` (out of scope for this phase but the pointer stays in STATE.md)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator dev environment ↔ local Supabase | Local-first verification catches runtime issues build+vitest cannot |
| main branch → prod (Vercel) | Bundled deploy; single commit graph reviewed at prod-URL smoke |
| iPhone Safari → prod | Only environment where touch/scroll/44px targets are real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-P06-01 | Denial of Service | Rushed prod push before local UAT | mitigate | Task 2 (local human-verify) BLOCKS Task 3 (prod push) |
| T-82-P06-02 | Data corruption on prod merge | Operator experiments in /admin/brands on prod | mitigate | Step 3b runbook explicitly forbids destructive actions on prod without a plan — read-only navigation only in the UAT script |
| T-82-P06-03 | Milestone accounting drift | phase.complete miscorrections | mitigate | Task 5 explicitly hand-corrects STATE.md per `[[phase-complete-999-1-misset]]` (5th recurrence expected) |
</threat_model>

<verification>
- 82-POST-DEPLOY.md committed and accurate
- Task 2 checkpoint passes (all 4 local steps ✅)
- Task 3 deploy succeeds (Vercel green + smoke curl 200/307)
- Task 4 checkpoint passes (all 3 prod steps ✅)
- REQUIREMENTS.md UI-01/02/03/OPS-01/02 all marked Complete
- ROADMAP.md Phase 82 marked complete
- STATE.md hand-corrected (no 999.1, no >100% progress)
</verification>

<success_criteria>
- 5/5 Phase 82 requirements green on both local + prod
- v8.4 milestone code-complete after Phase 82 close (25/25 requirements shipped)
- Next action queued: `/gsd-complete-milestone v8.4`
</success_criteria>

<output>
After completion, create `.planning/phases/82-add-watch-ui-operator-admin/82-06-SUMMARY.md` documenting the walkthrough outcomes + deploy commit sha + prod UAT sign-off timestamps.
</output>
