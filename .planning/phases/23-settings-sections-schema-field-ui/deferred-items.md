# Phase 23 — Deferred Items

Out-of-scope discoveries logged during plan execution. NOT fixed by this phase.

## Plan 23-02 — discovered during execution

- **`tests/no-raw-palette.test.ts`** — pre-existing failures for `src/components/insights/CollectionFitCard.tsx` and `src/components/search/WatchSearchRow.tsx` using `font-medium`. Phase 23 Plan 02 does not touch these files.
- **`tests/app/explore.test.tsx`** — pre-existing failures (3 tests): `Discovery is coming.` heading missing, teaser copy mismatch, no svg rendered. Phase 23 Plan 02 does not touch `/explore`.
