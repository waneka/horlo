---
phase: 23
plan: 06
type: execute
wave: 2
# depends_on intentionally empty: Plan 06 only verifies files Phase 22 already shipped
# (NotificationsSection, PrivacySection, /preferences/page.tsx). It does NOT depend on
# Plans 02/03 functionally — those plans modify entirely separate components
# (PreferencesSection, PreferencesClient, AppearanceSection). Wave 2 placement is
# orchestrator-imposed for "verify after impl plans land" ordering.
depends_on: []
files_modified:
  - .planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md
autonomous: true
requirements: [SET-09, SET-11, SET-12]
must_haves:
  truths:
    - "M-3: SET-09 verified — NotificationsSection already renders PrivacyToggleRow instances for notifyOnFollow + notifyOnWatchOverlap inside the new tab frame; no diff (D-08)."
    - "M-5: SET-11 verified — PrivacySection already renders PrivacyToggleRow instances for profilePublic, collectionPublic, wishlistPublic inside the new tab frame; no diff (D-08)."
    - "M-6: SET-12 verified — /preferences/page.tsx returns redirect('/settings#preferences') (Phase 22 D-15); no diff."
    - "M-9: D-20 cleanup grep sweep returned ZERO orphan production-code references to 'Delete Account', 'Coming soon', or 'New Note Visibility' in src/, AND the only 'SettingsClient' references are JSDoc historical notes (not code-orphan imports)."
  artifacts:
    - path: .planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md
      provides: "Evidence document — grep output transcripts + page render confirmations for SET-09/11/12 + D-20 cleanup"
  key_links:
    - from: ".planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md"
      to: "src/components/settings/NotificationsSection.tsx, PrivacySection.tsx; src/app/preferences/page.tsx"
      via: "Documented evidence (grep transcripts, file inspection results)"
      pattern: "VERIFIED"
---

<objective>
Verify the three "no code change" requirements in Phase 23 (SET-09, SET-11, SET-12) and execute the bounded D-20 cleanup sweep. Produce a single evidence document recording grep transcripts and file inspections so the checker can audit without re-running the same commands.

Purpose: Phase 23's plan footprint claims that certain requirements are already satisfied by Phase 22 work — the tab migration WAS the restyle for SET-09/SET-11; the redirect WAS SET-12. The checker needs paper-trail evidence that these claims hold AT EXECUTION TIME, not just at planning time. The D-20 cleanup sweep is bounded: grep for Phase 22's deleted-stub leftovers (Delete Account, Coming soon, New Note Visibility, unused SettingsClient orphans). If the sweep finds any production-code orphan, fix it in this plan; if it finds zero (the planner's grep at planning time returned only JSDoc historical references inside NotificationsSection.tsx, PrivacySection.tsx, SettingsTabsShell.tsx — those are documentation comments, not code orphans), document the no-op result.

Output: One new evidence file `23-06-VERIFICATION.md`. NO production-code changes if the cleanup grep is clean (the expected outcome). If a true orphan is found, an additional commit deletes it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-settings-sections-schema-field-ui/23-CONTEXT.md
@.planning/phases/23-settings-sections-schema-field-ui/23-RESEARCH.md
@.planning/phases/23-settings-sections-schema-field-ui/23-02-SUMMARY.md
@.planning/phases/23-settings-sections-schema-field-ui/23-03-SUMMARY.md

@.planning/phases/22-settings-restructure-account-section/22-CONTEXT.md
@src/components/settings/NotificationsSection.tsx
@src/components/settings/PrivacySection.tsx
@src/components/settings/SettingsTabsShell.tsx
@src/app/preferences/page.tsx
@src/app/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SET-09, SET-11, SET-12 (no-code-change requirements)</name>
  <files>.planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/23-settings-sections-schema-field-ui/23-CONTEXT.md (D-08, D-15 from 22-CONTEXT.md)
    - src/components/settings/NotificationsSection.tsx (must contain SettingsSection wrapper + 2 PrivacyToggleRow instances for notifyOnFollow + notifyOnWatchOverlap)
    - src/components/settings/PrivacySection.tsx (must contain SettingsSection wrapper + 3 PrivacyToggleRow instances for profilePublic + collectionPublic + wishlistPublic)
    - src/app/preferences/page.tsx (must return redirect('/settings#preferences'))
    - src/app/settings/page.tsx (sanity check that the page composes SettingsTabsShell with all 6 sections)
  </read_first>
  <action>
Run the following commands and capture exact outputs into a new file `.planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md`. The output of each command is the evidence — paste verbatim into the document.

**Step 1 — SET-09 (NotificationsSection):**

Run:
```
grep -n 'PrivacyToggleRow\|notifyOnFollow\|notifyOnWatchOverlap\|SettingsSection' src/components/settings/NotificationsSection.tsx
```
Expected: at least 2 lines mentioning `notifyOnFollow` and `notifyOnWatchOverlap`; at least 1 line wrapping in `<SettingsSection title="...">`.

**Step 2 — SET-11 (PrivacySection):**

Run:
```
grep -n 'PrivacyToggleRow\|profilePublic\|collectionPublic\|wishlistPublic\|SettingsSection' src/components/settings/PrivacySection.tsx
```
Expected: at least 3 lines mentioning the three privacy fields; at least 1 line wrapping in `<SettingsSection title="...">`.

**Step 3 — SET-12 (/preferences redirect):**

Run:
```
cat src/app/preferences/page.tsx
```
Expected: file contains a `redirect('/settings#preferences')` call (or equivalent — accept the literal string "/settings#preferences" inside `redirect(...)`).

**Step 4 — Sanity check that all 6 sections are mounted in SettingsTabsShell:**

Run:
```
grep -n 'AccountSection\|ProfileSection\|PreferencesSection\|PrivacySection\|NotificationsSection\|AppearanceSection' src/components/settings/SettingsTabsShell.tsx
```
Expected: each of the 6 section names appears at least twice (once for import, once for render).

**Compose the verification document with the literal output of each command pasted in.** Use this exact template:

```
# Phase 23 — No-Code-Change Verification (Plan 06)

Date: <today>
Verifier: GSD execute-phase

## SET-09 — NotificationsSection (D-08, no diff)

Claim: Phase 22 D-01 placed PrivacyToggleRow instances inside NotificationsSection within SettingsSection frame; Phase 23 verifies, no diff.

Evidence (grep -n 'PrivacyToggleRow|notifyOnFollow|notifyOnWatchOverlap|SettingsSection' src/components/settings/NotificationsSection.tsx):

[PASTE ACTUAL GREP OUTPUT HERE]

Verdict: VERIFIED — notifyOnFollow and notifyOnWatchOverlap toggles render via PrivacyToggleRow inside SettingsSection. No code change required for SET-09.

***

## SET-11 — PrivacySection (D-08, no diff)

Claim: Phase 22 D-01 placed PrivacyToggleRow instances inside PrivacySection within SettingsSection frame; Phase 23 verifies, no diff.

Evidence (grep -n 'PrivacyToggleRow|profilePublic|collectionPublic|wishlistPublic|SettingsSection' src/components/settings/PrivacySection.tsx):

[PASTE ACTUAL GREP OUTPUT HERE]

Verdict: VERIFIED — profilePublic, collectionPublic, wishlistPublic toggles render via PrivacyToggleRow inside SettingsSection. No code change required for SET-11.

***

## SET-12 — /preferences redirect (Phase 22 D-15, no diff)

Claim: Phase 22 D-15 implemented /preferences/page.tsx as a server-side redirect to /settings#preferences; Phase 23 verifies, no diff.

Evidence (cat src/app/preferences/page.tsx):

[PASTE ACTUAL FILE CONTENTS HERE]

Verdict: VERIFIED — file returns redirect('/settings#preferences'). No code change required for SET-12.

***

## SettingsTabsShell composes all 6 sections (sanity)

Evidence (grep -n 'AccountSection|ProfileSection|PreferencesSection|PrivacySection|NotificationsSection|AppearanceSection' src/components/settings/SettingsTabsShell.tsx):

[PASTE ACTUAL GREP OUTPUT HERE]

Verdict: All 6 sections imported and rendered.
```

If any of the four grep results returns unexpected output (e.g., NotificationsSection no longer renders the toggles, or `/preferences/page.tsx` doesn't redirect), STOP and escalate to the user — that means Phase 22 regressed and Plan 06 cannot complete as a no-op. This is highly unlikely given the planner's pre-flight grep at planning time confirmed each was in place; the verification is a defensive checkpoint.
  </action>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo &amp;&amp; test -f .planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md &amp;&amp; grep -c "VERIFIED" .planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md | awk '{ if ($1 &gt;= 3) print "PASS"; else print "FAIL: only " $1 " VERIFIED tags found" }'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md` exists.
    - File contains at least 3 "Verdict: VERIFIED" lines (one each for SET-09, SET-11, SET-12).
    - The grep transcripts in the document are NOT placeholders ("[PASTE ...]") — they contain actual line-numbered grep output.
    - NotificationsSection.tsx evidence references both `notifyOnFollow` and `notifyOnWatchOverlap` and at least one `<PrivacyToggleRow>` usage.
    - PrivacySection.tsx evidence references all three: `profilePublic`, `collectionPublic`, `wishlistPublic`.
    - preferences/page.tsx evidence shows the redirect literal "/settings#preferences".
    - No production source file under `src/` is modified by this task (verify with `git diff src/`).
  </acceptance_criteria>
  <done>Evidence document committed; SET-09/SET-11/SET-12 are verified-no-change with paper trail.</done>
</task>

<task type="auto">
  <name>Task 2: D-20 cleanup grep sweep — append results to verification doc, fix any orphans found</name>
  <files>.planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/23-settings-sections-schema-field-ui/23-CONTEXT.md (D-20 — bounded sweep targets)
    - .planning/phases/22-settings-restructure-account-section/22-CONTEXT.md (Phase 22 D-04 — what was deleted)
    - .planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md (Task 1 output — append to it)
  </read_first>
  <action>
Run the four D-20 cleanup greps and append their results to `23-06-VERIFICATION.md` in a new section. The expected outcome based on the planner's pre-flight grep is: zero true orphans. The only matches expected are JSDoc historical references inside `SettingsTabsShell.tsx`, `NotificationsSection.tsx`, and `PrivacySection.tsx` (those mention "legacy SettingsClient.tsx" in comments documenting the migration history — not orphan imports or dead code).

**Step 1 — Run all four greps:**

```
echo '--- Delete Account ---'
grep -rn "Delete Account" src/ --include="*.ts" --include="*.tsx"

echo '--- Coming soon ---'
grep -rn "Coming soon\|coming soon" src/ --include="*.ts" --include="*.tsx"

echo '--- New Note Visibility ---'
grep -rn "New Note Visibility" src/ --include="*.ts" --include="*.tsx"

echo '--- SettingsClient ---'
grep -rn "SettingsClient" src/ --include="*.ts" --include="*.tsx"
```

**Step 2 — Classify each match:**
- If a match is inside a `/* ... */` or `//` JSDoc/comment block referring to historical context (e.g., "migrated from legacy SettingsClient.tsx"), it is NOT an orphan — it is documentation. Mark it "JSDoc historical reference (not orphan)".
- If a match is an `import` statement, a function call, JSX element, or live string literal in production code that depends on a deleted entity → it IS an orphan. Delete it in a small follow-up commit.
- If a match is inside a test file under `tests/`, evaluate separately — tests for deleted features should themselves be deleted (Phase 22 should have done this, but if any are missed, delete them here).

**Step 3 — Append to `23-06-VERIFICATION.md`:**

```
***

## D-20 Cleanup Sweep (Phase 22 leftovers)

Targets: Delete Account, Coming soon, New Note Visibility, SettingsClient

### "Delete Account" sweep

Command: grep -rn "Delete Account" src/ --include="*.ts" --include="*.tsx"

Output:
[PASTE ACTUAL OUTPUT HERE — empty output is the expected result]

Classification: <empty | JSDoc historical reference | ORPHAN — fixed in this plan>

### "Coming soon" sweep

Command: grep -rn "Coming soon\|coming soon" src/ --include="*.ts" --include="*.tsx"

Output:
[PASTE ACTUAL OUTPUT HERE]

Classification: <empty | JSDoc historical reference | ORPHAN — fixed in this plan>

### "New Note Visibility" sweep

Command: grep -rn "New Note Visibility" src/ --include="*.ts" --include="*.tsx"

Output:
[PASTE ACTUAL OUTPUT HERE]

Classification: <empty | JSDoc historical reference | ORPHAN — fixed in this plan>

### "SettingsClient" sweep

Command: grep -rn "SettingsClient" src/ --include="*.ts" --include="*.tsx"

Output:
[PASTE ACTUAL OUTPUT HERE — expected: 3 JSDoc historical references inside SettingsTabsShell.tsx, NotificationsSection.tsx, PrivacySection.tsx]

Classification: JSDoc historical reference (not orphan) — these are documentation comments tracking the Phase 22 migration history. Production code does not import or call SettingsClient.

***

## D-20 Verdict

[ZERO orphans found | <count> orphans found, fixed in commit <sha>]
```

**Step 4 — If any ORPHAN classification:**
- For each true orphan, do a focused edit deleting the orphan code (import, JSX, function call, or whole file if it's an unused module).
- After deletion: re-run all relevant tests (`npm test`) to confirm no regression.
- Append a "Fixes applied" subsection listing each file and the deleted lines.
- Commit message: `chore(23): remove Phase 22 stub orphans (Delete Account / Coming soon / etc.)`.

**Step 5 — If ZERO orphans (the expected outcome):**
- The verification doc records "ZERO orphans found".
- No production-code commit is required for this task.
- The planning-only commit at the end of the plan captures only the verification doc.
  </action>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo &amp;&amp; grep -c "D-20 Verdict\|orphans found" .planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The verification doc contains a "D-20 Cleanup Sweep" section.
    - Each of the four greps has a "Command" line, an "Output" block, and a "Classification" verdict.
    - The doc ends with a single "D-20 Verdict" line stating either "ZERO orphans found" or "<N> orphans found, fixed in commit <sha>".
    - If orphans were found and fixed, the file list in `git diff --name-only HEAD~1` matches the orphan files documented.
    - If zero orphans (expected): no production source file under `src/` is modified by this task.
    - The pre-existing test suite is green: `npm test 2>&amp;1 | tail -10` shows no new failures.
  </acceptance_criteria>
  <done>D-20 cleanup sweep documented; zero orphans (or any found are deleted); evidence recorded in 23-06-VERIFICATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Plan 06 is verification-and-cleanup only. No new code paths, no auth surface, no data flow change. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-01 | Repudiation | claims that SET-09/11/12 are verified | mitigate | Evidence document captures literal grep transcripts; the checker can re-run the same commands to confirm. No claim is taken on faith. |
| T-23-06-02 | Tampering | accidentally deleting non-orphan code during D-20 sweep | mitigate | Sweep is bounded to four specific strings; matches are classified before deletion; tests run after any deletion. JSDoc comments explicitly excluded from the orphan classification — only live production-code references count. |
</threat_model>

<verification>
- `.planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md` exists.
- Document has at least 3 "Verdict: VERIFIED" lines (SET-09/11/12).
- Document has a "D-20 Cleanup Sweep" section with all four greps documented.
- Document ends with a single "D-20 Verdict" line.
- If zero orphans: no `src/` file is modified by Plan 06.
- If orphans found: each is documented and tests still pass after deletion.
- Pre-existing test suite is green; no regressions.
</verification>

<success_criteria>
- [ ] Evidence file `23-06-VERIFICATION.md` exists with verbatim grep transcripts (no placeholder strings).
- [ ] SET-09 verified with NotificationsSection grep evidence.
- [ ] SET-11 verified with PrivacySection grep evidence.
- [ ] SET-12 verified with `/preferences/page.tsx` redirect evidence.
- [ ] D-20 sweep ran for all four targets; classification recorded for each match.
- [ ] D-20 verdict line states either "ZERO orphans found" or lists fix commits.
- [ ] If zero orphans (expected): no `src/` file is modified.
- [ ] No regression in pre-existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/23-settings-sections-schema-field-ui/23-06-SUMMARY.md` documenting:
- Pointer to `23-06-VERIFICATION.md` for the evidence
- Single-line verdict per requirement (SET-09 / SET-11 / SET-12 / D-20)
- Confirmation that no production-code change was required (or list of orphan-fix commits if any)
- Phase 23 milestone status: all 8 requirements (SET-07/08/09/10/11/12 + FEAT-07/08) covered across plans 02-06.
</output>
