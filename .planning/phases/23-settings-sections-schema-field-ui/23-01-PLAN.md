---
phase: 23
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/components/settings/preferences/CollectionGoalCard.test.tsx
  - tests/components/settings/preferences/OverlapToleranceCard.test.tsx
  - tests/components/settings/PreferencesClientEmbedded.test.tsx
  - tests/components/settings/AppearanceSection.test.tsx
  - tests/components/watch/WatchForm.notesPublic.test.tsx
  - tests/components/watch/WatchForm.isChronometer.test.tsx
  - tests/components/watch/WatchDetail.isChronometer.test.tsx
  - tests/actions/watches.notesPublic.test.ts
autonomous: true
requirements: [SET-07, SET-08, SET-10, FEAT-07, FEAT-08]
must_haves:
  truths:
    - "Eight RED test scaffolds exist that fail with 'missing module/element' errors before implementation begins."
    - "Each scaffold imports the not-yet-written symbol or asserts the not-yet-rendered element it covers."
    - "Vitest discovers and runs the new tests under tests/components/settings/, tests/components/watch/, tests/actions/."
  artifacts:
    - path: tests/components/settings/preferences/CollectionGoalCard.test.tsx
      provides: "RED scaffold for SET-07 — asserts 4 SelectItems including locked brand-loyalist label"
    - path: tests/components/settings/preferences/OverlapToleranceCard.test.tsx
      provides: "RED scaffold for SET-08 — asserts 3 SelectItems"
    - path: tests/components/settings/PreferencesClientEmbedded.test.tsx
      provides: "RED scaffold asserting embedded prop suppresses h1/subtitle/container and Collection Settings Card is gone"
    - path: tests/components/settings/AppearanceSection.test.tsx
      provides: "RED scaffold for SET-10 — Theme heading + InlineThemeSegmented mounted"
    - path: tests/components/watch/WatchForm.notesPublic.test.tsx
      provides: "RED scaffold for FEAT-07 WatchForm pill — default Public, toggles, submits"
    - path: tests/components/watch/WatchForm.isChronometer.test.tsx
      provides: "RED scaffold for FEAT-08 WatchForm Checkbox — hydrates from prop, submits"
    - path: tests/components/watch/WatchDetail.isChronometer.test.tsx
      provides: "RED scaffold for FEAT-08 WatchDetail row — only-if-true rendering"
    - path: tests/actions/watches.notesPublic.test.ts
      provides: "RED scaffold for FEAT-07 server action — Zod accepts notesPublic; revalidatePath /u/[username] called"
  key_links:
    - from: "tests/components/settings/preferences/CollectionGoalCard.test.tsx"
      to: "src/components/settings/preferences/CollectionGoalCard.tsx (NOT YET CREATED)"
      via: "import { CollectionGoalCard } from '@/components/settings/preferences/CollectionGoalCard'"
      pattern: "import.*CollectionGoalCard.*from.*@/components/settings/preferences/CollectionGoalCard"
    - from: "tests/actions/watches.notesPublic.test.ts"
      to: "src/app/actions/watches.ts insertWatchSchema"
      via: "addWatch / editWatch payload includes notesPublic"
      pattern: "notesPublic"
---

<objective>
Establish 8 RED test scaffolds before any production code changes (Wave 0 / Nyquist compliance per VALIDATION.md). Each scaffold imports a not-yet-written component or asserts a not-yet-rendered element, fails with a clear error, and unblocks downstream plans by giving every implementation task an `<automated>` verify command to point at.

Purpose: Tests-first is the project's locked Nyquist contract. Wave 1 plans cannot ship without an automated test the executor can run after each commit. Plan 01 produces those tests; Plans 02–05 turn them GREEN.

Output: 8 new test files under `tests/`. All tests fail when run; existing test suite remains green (no regressions to existing tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-settings-sections-schema-field-ui/23-CONTEXT.md
@.planning/phases/23-settings-sections-schema-field-ui/23-RESEARCH.md
@.planning/phases/23-settings-sections-schema-field-ui/23-UI-SPEC.md
@.planning/phases/23-settings-sections-schema-field-ui/23-VALIDATION.md

@tests/setup.ts
@src/components/preferences/PreferencesClient.tsx
@src/components/settings/AppearanceSection.tsx
@src/components/watch/WatchForm.tsx
@src/components/watch/WatchDetail.tsx
@src/app/actions/watches.ts
@src/lib/types.ts

<interfaces>
<!-- Existing types/exports the test scaffolds will import. Forward-references are explicit so the executor knows what NOT to import yet. -->

From `src/lib/types.ts`:
```typescript
export type CollectionGoal = 'balanced' | 'specialist' | 'variety-within-theme' | 'brand-loyalist'
export type OverlapTolerance = 'low' | 'medium' | 'high'
export interface UserPreferences {
  userId: string
  collectionGoal?: CollectionGoal
  overlapTolerance: OverlapTolerance
  // ...other fields
}
export interface Watch {
  id: string
  brand: string
  model: string
  // ...
  isChronometer?: boolean
  notesPublic?: boolean
}
```

From `src/components/preferences/PreferencesClient.tsx` (existing today, `embedded` prop NOT YET added):
```typescript
export function PreferencesClient(props: { preferences: UserPreferences /* embedded?: boolean — added in Plan 02 */ }): JSX.Element
```

From `src/components/watch/WatchForm.tsx` (existing today):
```typescript
export function WatchForm(props: {
  watch?: Watch
  onSave: (data: Omit<Watch, 'id'>) => void | Promise<void>
  onCancel?: () => void
  lockedStatus?: WatchStatus
}): JSX.Element
```

From `src/components/watch/WatchDetail.tsx` (existing today):
```typescript
export function WatchDetail(props: { watch: Watch }): JSX.Element
```

NOT YET WRITTEN (these imports MUST FAIL at the start of Wave 0):
- `src/components/settings/preferences/CollectionGoalCard.tsx` — created in Plan 02
- `src/components/settings/preferences/OverlapToleranceCard.tsx` — created in Plan 02
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 RED test scaffolds for Settings sections (CollectionGoalCard, OverlapToleranceCard, PreferencesClient embedded, AppearanceSection)</name>
  <files>
    tests/components/settings/preferences/CollectionGoalCard.test.tsx,
    tests/components/settings/preferences/OverlapToleranceCard.test.tsx,
    tests/components/settings/PreferencesClientEmbedded.test.tsx,
    tests/components/settings/AppearanceSection.test.tsx
  </files>
  <read_first>
    - .planning/phases/23-settings-sections-schema-field-ui/23-VALIDATION.md (Wave 0 requirements list)
    - .planning/phases/23-settings-sections-schema-field-ui/23-UI-SPEC.md (locked copy for Selects)
    - .planning/phases/23-settings-sections-schema-field-ui/23-CONTEXT.md (D-01..D-08)
    - tests/setup.ts (existing vitest + RTL setup)
    - tests/components/settings/PrivacySection.test.tsx OR an existing settings test (find with `ls tests/components/settings/`) for the established RTL+Select+Card pattern in this repo
    - src/components/preferences/PreferencesClient.tsx (existing structure to assert the embedded prop suppresses)
    - src/components/settings/AppearanceSection.tsx (current "Coming in next update" stub copy, to assert it's GONE)
    - src/components/settings/SettingsSection.tsx (heading rendering pattern — assert h2 with title)
    - src/components/layout/InlineThemeSegmented.tsx (existing role="radio" or button labels — assert they render inside AppearanceSection)
  </read_first>
  <action>
Create 4 new test files. Each test file MUST fail when run today (RED). Use vitest 2.1.9 + React Testing Library + jsdom — config and setup file already exist.

**Conventions for ALL files:**
- Top of file: `import { describe, it, expect } from 'vitest'`, `import { render, screen } from '@testing-library/react'`, `import userEvent from '@testing-library/user-event'`.
- Use `screen.getByRole`, `screen.getByText`, `screen.queryByText` over `getByTestId`.
- Use the locked literal copy strings from UI-SPEC § Copywriting Contract — DO NOT paraphrase.

***

**File 1: `tests/components/settings/preferences/CollectionGoalCard.test.tsx`** (creates the new directory `tests/components/settings/preferences/` — vitest auto-discovers nested folders).

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

// THIS IMPORT MUST FAIL until Plan 02 creates the file.
import { CollectionGoalCard } from '@/components/settings/preferences/CollectionGoalCard'

vi.mock('@/app/actions/preferences', () => ({
  savePreferences: vi.fn(async () => ({ success: true, data: {} })),
}))

describe('<CollectionGoalCard>', () => {
  it('renders Card with title "Collection goal" and the locked description', () => {
    render(<CollectionGoalCard initialGoal={undefined} />)
    expect(screen.getByText('Collection goal')).toBeInTheDocument()
    expect(
      screen.getByText('How do you want your collection to grow over time?'),
    ).toBeInTheDocument()
  })

  it('shows placeholder "Select a goal..." when initialGoal is undefined', () => {
    render(<CollectionGoalCard initialGoal={undefined} />)
    expect(screen.getByText('Select a goal...')).toBeInTheDocument()
  })

  it('renders all 4 SelectItem options including the locked Brand Loyalist copy when opened', async () => {
    const user = userEvent.setup()
    render(<CollectionGoalCard initialGoal={undefined} />)
    await user.click(screen.getByRole('combobox'))
    expect(screen.getByText('Balanced — Diverse collection across styles')).toBeInTheDocument()
    expect(screen.getByText('Specialist — Deep in one area')).toBeInTheDocument()
    expect(screen.getByText('Variety within a theme')).toBeInTheDocument()
    expect(screen.getByText('Brand Loyalist — Same maker, different models')).toBeInTheDocument()
  })

  it('calls savePreferences with the chosen goal when an option is selected', async () => {
    const { savePreferences } = await import('@/app/actions/preferences')
    const user = userEvent.setup()
    render(<CollectionGoalCard initialGoal={undefined} />)
    await user.click(screen.getByRole('combobox'))
    await user.click(screen.getByText('Brand Loyalist — Same maker, different models'))
    expect(savePreferences).toHaveBeenCalledWith({ collectionGoal: 'brand-loyalist' })
  })
})
```

***

**File 2: `tests/components/settings/preferences/OverlapToleranceCard.test.tsx`**

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

import { OverlapToleranceCard } from '@/components/settings/preferences/OverlapToleranceCard'

vi.mock('@/app/actions/preferences', () => ({
  savePreferences: vi.fn(async () => ({ success: true, data: {} })),
}))

describe('<OverlapToleranceCard>', () => {
  it('renders Card with title "Overlap tolerance" and the locked description', () => {
    render(<OverlapToleranceCard initialTolerance="medium" />)
    expect(screen.getByText('Overlap tolerance')).toBeInTheDocument()
    expect(
      screen.getByText('How strictly should we flag watches that overlap with what you already own?'),
    ).toBeInTheDocument()
  })

  it('renders all 3 SelectItem options when opened', async () => {
    const user = userEvent.setup()
    render(<OverlapToleranceCard initialTolerance="medium" />)
    await user.click(screen.getByRole('combobox'))
    expect(screen.getByText('Low — Flag any overlap')).toBeInTheDocument()
    expect(screen.getByText('Medium — Flag significant overlap')).toBeInTheDocument()
    expect(screen.getByText('High — Only flag major overlap')).toBeInTheDocument()
  })

  it('calls savePreferences with the chosen tolerance', async () => {
    const { savePreferences } = await import('@/app/actions/preferences')
    const user = userEvent.setup()
    render(<OverlapToleranceCard initialTolerance="medium" />)
    await user.click(screen.getByRole('combobox'))
    await user.click(screen.getByText('High — Only flag major overlap'))
    expect(savePreferences).toHaveBeenCalledWith({ overlapTolerance: 'high' })
  })
})
```

***

**File 3: `tests/components/settings/PreferencesClientEmbedded.test.tsx`**

```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'

import { PreferencesClient } from '@/components/preferences/PreferencesClient'
import type { UserPreferences } from '@/lib/types'

const stubPrefs: UserPreferences = {
  userId: 'test-user',
  preferredStyles: [],
  dislikedStyles: [],
  preferredDesignTraits: [],
  dislikedDesignTraits: [],
  preferredComplications: [],
  complicationExceptions: [],
  preferredDialColors: [],
  dislikedDialColors: [],
  overlapTolerance: 'medium',
  notes: '',
} as UserPreferences

describe('<PreferencesClient embedded>', () => {
  it('does NOT render the page <h1>Preferences</h1> chrome when embedded={true}', () => {
    render(<PreferencesClient preferences={stubPrefs} embedded />)
    expect(screen.queryByRole('heading', { level: 1, name: /preferences/i })).toBeNull()
  })

  it('does NOT render the page subtitle when embedded={true}', () => {
    render(<PreferencesClient preferences={stubPrefs} embedded />)
    expect(
      screen.queryByText(/configure your collecting taste to get personalized insights/i),
    ).toBeNull()
  })

  it('does NOT render a "Collection Settings" Card title (D-02 — Selects lifted to top Cards)', () => {
    render(<PreferencesClient preferences={stubPrefs} embedded />)
    expect(screen.queryByText('Collection Settings')).toBeNull()
  })

  it('does NOT render an "Overlap Tolerance" or "Collection Goal" Label (D-02 — moved out)', () => {
    render(<PreferencesClient preferences={stubPrefs} embedded />)
    expect(screen.queryByText(/overlap tolerance/i)).toBeNull()
    expect(screen.queryByText(/collection goal/i)).toBeNull()
  })

  it('STILL renders the taste-tag pickers (Style Preferences card remains)', () => {
    render(<PreferencesClient preferences={stubPrefs} embedded />)
    expect(screen.getByText(/style preferences/i)).toBeInTheDocument()
  })
})
```

***

**File 4: `tests/components/settings/AppearanceSection.test.tsx`**

```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'

import { AppearanceSection } from '@/components/settings/AppearanceSection'

describe('<AppearanceSection>', () => {
  it('renders SettingsSection with heading "Theme"', () => {
    render(<AppearanceSection />)
    expect(screen.getByRole('heading', { name: 'Theme' })).toBeInTheDocument()
  })

  it('does NOT render the legacy "coming in the next update" stub copy', () => {
    render(<AppearanceSection />)
    expect(
      screen.queryByText(/theme and visual preferences are coming in the next update/i),
    ).toBeNull()
  })

  it('renders the Light/Dark/System segmented control (InlineThemeSegmented)', () => {
    render(<AppearanceSection />)
    // InlineThemeSegmented exposes three buttons or radios with these labels.
    expect(screen.getByRole('button', { name: /light/i })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: /dark/i })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: /system/i })).toBeInTheDocument()
  })

  it('does NOT render any explanatory <CardDescription> under "Theme" (D-05 — no extra copy)', () => {
    const { container } = render(<AppearanceSection />)
    // SettingsSection h2 has no description sibling; assert no descendant <p>
    // sibling appears between the h2 and the segmented row.
    expect(container.querySelector('h2 + p')).toBeNull()
  })
})
```

**If `<InlineThemeSegmented>` exposes its options as `role="radio"` or another role rather than `button`, adjust the assertions accordingly — read `src/components/layout/InlineThemeSegmented.tsx` first to determine the actual roles.** The intent: assert all three theme labels are visible as interactive controls.
  </action>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo &amp;&amp; npm test -- tests/components/settings/preferences/CollectionGoalCard.test.tsx tests/components/settings/preferences/OverlapToleranceCard.test.tsx tests/components/settings/PreferencesClientEmbedded.test.tsx tests/components/settings/AppearanceSection.test.tsx 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist at the listed paths.
    - Running the tests fails (RED) — at minimum, CollectionGoalCard and OverlapToleranceCard fail with module-not-found errors importing `@/components/settings/preferences/...`. PreferencesClientEmbedded fails because the `embedded` prop is not yet recognized OR the Selects are still inside Collection Settings Card. AppearanceSection fails because the heading is "Appearance" not "Theme" and the stub copy still exists.
    - Each test file has a `describe(...)` block with at least 3 `it(...)` cases.
    - Locked copy strings appear LITERALLY: "Brand Loyalist — Same maker, different models" (em-dash), "Collection goal", "Overlap tolerance", "Theme", "How do you want your collection to grow over time?", "How strictly should we flag watches that overlap with what you already own?".
    - `grep -c 'em-dash\|—' tests/components/settings/preferences/CollectionGoalCard.test.tsx` returns at least 4 (one per long-form option label).
    - `grep -l "embedded" tests/components/settings/PreferencesClientEmbedded.test.tsx` returns the file path.
  </acceptance_criteria>
  <done>4 RED test scaffolds committed; running them fails with clear errors; no other tests regress.</done>
</task>

<task type="auto">
  <name>Task 2: Create 4 RED test scaffolds for Watch field exposures (WatchForm pill, WatchForm Checkbox, WatchDetail row, watches action)</name>
  <files>
    tests/components/watch/WatchForm.notesPublic.test.tsx,
    tests/components/watch/WatchForm.isChronometer.test.tsx,
    tests/components/watch/WatchDetail.isChronometer.test.tsx,
    tests/actions/watches.notesPublic.test.ts
  </files>
  <read_first>
    - .planning/phases/23-settings-sections-schema-field-ui/23-VALIDATION.md
    - .planning/phases/23-settings-sections-schema-field-ui/23-UI-SPEC.md (locked pill copy + Checkbox copy + Certification row copy)
    - .planning/phases/23-settings-sections-schema-field-ui/23-CONTEXT.md (D-09..D-19)
    - src/components/watch/WatchForm.tsx (FormData type, initialFormData, Notes Card structure, onSave signature)
    - src/components/watch/WatchDetail.tsx (existing Specifications dl pattern; productionYear row)
    - src/app/actions/watches.ts (insertWatchSchema — currently MISSING notesPublic; revalidatePath calls)
    - src/lib/types.ts (Watch type — has isChronometer and notesPublic optional)
    - tests/components/watch/ (any existing WatchForm tests for the established render+submit pattern)
    - tests/actions/ (any existing watches action tests for the mock pattern)
  </read_first>
  <action>
Create 4 new test files. Each MUST fail when run today (RED). Same vitest+RTL conventions as Task 1.

***

**File 1: `tests/components/watch/WatchForm.notesPublic.test.tsx`**

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

import { WatchForm } from '@/components/watch/WatchForm'

describe('<WatchForm> — notesPublic pill (FEAT-07)', () => {
  it('renders a "Visibility:" caption and a pill below the Notes textarea', () => {
    const onSave = vi.fn()
    render(<WatchForm onSave={onSave} />)
    expect(screen.getByText('Visibility:')).toBeInTheDocument()
    // The pill is a button[role="switch"]
    expect(screen.getByRole('switch')).toBeInTheDocument()
  })

  it('defaults to Public for a new watch (D-16 — matches DB default true)', () => {
    const onSave = vi.fn()
    render(<WatchForm onSave={onSave} />)
    const pill = screen.getByRole('switch')
    expect(pill).toHaveAttribute('aria-checked', 'true')
    expect(pill).toHaveTextContent('Public')
  })

  it('toggles between Public and Private on click', async () => {
    const user = userEvent.setup()
    const onSave = vi.fn()
    render(<WatchForm onSave={onSave} />)
    const pill = screen.getByRole('switch')
    expect(pill).toHaveTextContent('Public')
    await user.click(pill)
    expect(pill).toHaveTextContent('Private')
    expect(pill).toHaveAttribute('aria-checked', 'false')
  })

  it('submits notesPublic in the onSave payload', async () => {
    const user = userEvent.setup()
    const onSave = vi.fn(async () => {})
    render(<WatchForm onSave={onSave} />)
    // Fill required fields (brand, model) so submit succeeds
    await user.type(screen.getByLabelText(/brand/i), 'Rolex')
    await user.type(screen.getByLabelText(/^model/i), 'Submariner')
    // Toggle pill to Private
    await user.click(screen.getByRole('switch'))
    await user.click(screen.getByRole('button', { name: /save|add watch/i }))
    expect(onSave).toHaveBeenCalled()
    const submitted = onSave.mock.calls[0][0]
    expect(submitted.notesPublic).toBe(false)
  })

  it('hydrates notesPublic from the watch prop in edit mode', () => {
    const onSave = vi.fn()
    const existingWatch = {
      id: 'w1',
      brand: 'Omega', model: 'Speedmaster',
      status: 'owned' as const,
      movement: 'manual' as const,
      complications: [], styleTags: [], designTraits: [], roleTags: [],
      notesPublic: false,
    }
    // @ts-expect-error — minimal fixture; full Watch type has more fields
    render(<WatchForm watch={existingWatch} onSave={onSave} />)
    const pill = screen.getByRole('switch')
    expect(pill).toHaveTextContent('Private')
    expect(pill).toHaveAttribute('aria-checked', 'false')
  })

  it('aria-label changes between states for screen readers', async () => {
    const user = userEvent.setup()
    render(<WatchForm onSave={vi.fn()} />)
    const pill = screen.getByRole('switch')
    expect(pill).toHaveAttribute('aria-label', 'Note is public, click to make private')
    await user.click(pill)
    expect(pill).toHaveAttribute('aria-label', 'Note is private, click to make public')
  })
})
```

***

**File 2: `tests/components/watch/WatchForm.isChronometer.test.tsx`**

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

import { WatchForm } from '@/components/watch/WatchForm'

describe('<WatchForm> — isChronometer Checkbox (FEAT-08)', () => {
  it('renders a Chronometer-certified Checkbox in the Specifications card', () => {
    render(<WatchForm onSave={vi.fn()} />)
    // Locked label copy from UI-SPEC.
    expect(screen.getByText(/Chronometer-certified/)).toBeInTheDocument()
    // The qualifier is in muted text
    expect(screen.getByText(/\(COSC or equivalent\)/)).toBeInTheDocument()
  })

  it('defaults to unchecked for a new watch', () => {
    render(<WatchForm onSave={vi.fn()} />)
    const checkbox = screen.getByRole('checkbox', { name: /chronometer-certified/i })
    expect(checkbox).not.toBeChecked()
  })

  it('hydrates from watch.isChronometer === true in edit mode', () => {
    const existingWatch = {
      id: 'w1', brand: 'Rolex', model: 'Datejust',
      status: 'owned' as const, movement: 'automatic' as const,
      complications: [], styleTags: [], designTraits: [], roleTags: [],
      isChronometer: true,
    }
    // @ts-expect-error — minimal fixture
    render(<WatchForm watch={existingWatch} onSave={vi.fn()} />)
    const checkbox = screen.getByRole('checkbox', { name: /chronometer-certified/i })
    expect(checkbox).toBeChecked()
  })

  it('submits isChronometer: true in the onSave payload after toggling', async () => {
    const user = userEvent.setup()
    const onSave = vi.fn(async () => {})
    render(<WatchForm onSave={onSave} />)
    await user.type(screen.getByLabelText(/brand/i), 'Rolex')
    await user.type(screen.getByLabelText(/^model/i), 'Datejust')
    await user.click(screen.getByRole('checkbox', { name: /chronometer-certified/i }))
    await user.click(screen.getByRole('button', { name: /save|add watch/i }))
    expect(onSave).toHaveBeenCalled()
    const submitted = onSave.mock.calls[0][0]
    expect(submitted.isChronometer).toBe(true)
  })

  it('submits isChronometer: false (or omits it) when not toggled in a new watch', async () => {
    const user = userEvent.setup()
    const onSave = vi.fn(async () => {})
    render(<WatchForm onSave={onSave} />)
    await user.type(screen.getByLabelText(/brand/i), 'Rolex')
    await user.type(screen.getByLabelText(/^model/i), 'Datejust')
    await user.click(screen.getByRole('button', { name: /save|add watch/i }))
    expect(onSave).toHaveBeenCalled()
    const submitted = onSave.mock.calls[0][0]
    // Either explicitly false OR undefined is acceptable; the assertion is "not true".
    expect(submitted.isChronometer).not.toBe(true)
  })
})
```

***

**File 3: `tests/components/watch/WatchDetail.isChronometer.test.tsx`**

```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'

import { WatchDetail } from '@/components/watch/WatchDetail'

const baseWatch = {
  id: 'w1',
  userId: 'u1',
  brand: 'Rolex',
  model: 'Datejust',
  status: 'owned' as const,
  movement: 'automatic' as const,
  complications: [],
  styleTags: [],
  designTraits: [],
  roleTags: [],
  createdAt: new Date('2025-01-01'),
  updatedAt: new Date('2025-01-01'),
}

describe('<WatchDetail> — Certification row (FEAT-08 / D-11)', () => {
  it('renders a Certification row with "Chronometer" when watch.isChronometer === true', () => {
    // @ts-expect-error — minimal fixture
    render(<WatchDetail watch={{ ...baseWatch, isChronometer: true }} />)
    expect(screen.getByText('Certification')).toBeInTheDocument()
    expect(screen.getByText('Chronometer')).toBeInTheDocument()
  })

  it('does NOT render the Certification row when watch.isChronometer === false', () => {
    // @ts-expect-error
    render(<WatchDetail watch={{ ...baseWatch, isChronometer: false }} />)
    expect(screen.queryByText('Certification')).toBeNull()
    expect(screen.queryByText('Chronometer')).toBeNull()
  })

  it('does NOT render the Certification row when watch.isChronometer is null/undefined', () => {
    // @ts-expect-error
    render(<WatchDetail watch={baseWatch} />)
    expect(screen.queryByText('Certification')).toBeNull()
  })

  it('does NOT render the Certification row when watch.isChronometer is null (legacy row)', () => {
    // @ts-expect-error — null mirrors a DB row written before the column existed
    render(<WatchDetail watch={{ ...baseWatch, isChronometer: null }} />)
    expect(screen.queryByText('Certification')).toBeNull()
  })
})
```

***

**File 4: `tests/actions/watches.notesPublic.test.ts`**

This test asserts the Zod schema accepts `notesPublic` and that on edit, `revalidatePath('/u/[username]', 'layout')` fires. The test must mock the DAL and `next/cache` so it runs without a real DB.

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

vi.mock('next/cache', () => ({
  revalidatePath: vi.fn(),
  revalidateTag: vi.fn(),
}))

vi.mock('@/lib/auth', () => ({
  getCurrentUser: vi.fn(async () => ({ id: 'test-user-id', email: 't@example.com' })),
}))

vi.mock('@/data/watches', () => ({
  createWatch: vi.fn(async (userId: string, data: any) => ({ id: 'new-watch-id', userId, ...data })),
  updateWatch: vi.fn(async (userId: string, watchId: string, data: any) => ({ id: watchId, userId, ...data })),
  getWatchById: vi.fn(async () => ({ id: 'w1', userId: 'test-user-id', brand: 'X', model: 'Y' })),
}))

vi.mock('@/data/catalog', () => ({
  upsertCatalogFromUserInput: vi.fn(async () => null),
  linkWatchToCatalog: vi.fn(async () => undefined),
}))

vi.mock('@/data/profiles', () => ({
  getProfileById: vi.fn(async () => ({ userId: 'test-user-id', username: 'testuser' })),
}))

vi.mock('@/data/notifications', () => ({
  findOverlapRecipients: vi.fn(async () => []),
}))

vi.mock('@/data/activities', () => ({
  logActivity: vi.fn(async () => undefined),
}))

vi.mock('@/lib/notifications/logger', () => ({
  logNotification: vi.fn(async () => undefined),
}))

beforeEach(() => {
  vi.clearAllMocks()
})

describe('addWatch / editWatch — notesPublic + revalidation (FEAT-07)', () => {
  it('addWatch accepts notesPublic in the Zod schema and persists it', async () => {
    const { addWatch } = await import('@/app/actions/watches')
    const result = await addWatch({
      brand: 'Rolex',
      model: 'Submariner',
      status: 'owned',
      movement: 'automatic',
      complications: [],
      styleTags: [], designTraits: [], roleTags: [],
      notesPublic: false,
    })
    expect(result.success).toBe(true)
    const { createWatch } = await import('@/data/watches')
    expect(createWatch).toHaveBeenCalled()
    const passedData = (createWatch as any).mock.calls[0][1]
    expect(passedData.notesPublic).toBe(false)
  })

  it('editWatch accepts notesPublic and revalidates /u/[username] layout', async () => {
    const { editWatch } = await import('@/app/actions/watches')
    const { revalidatePath } = await import('next/cache')
    const result = await editWatch('w1', {
      notesPublic: false,
    })
    expect(result.success).toBe(true)
    // D-19: editWatch MUST revalidate the per-row note surface.
    expect(revalidatePath).toHaveBeenCalledWith('/u/[username]', 'layout')
  })

  it('addWatch revalidates /u/[username] layout (D-19 — covers the create-with-private-note case)', async () => {
    const { addWatch } = await import('@/app/actions/watches')
    const { revalidatePath } = await import('next/cache')
    await addWatch({
      brand: 'Rolex', model: 'Submariner',
      status: 'owned', movement: 'automatic',
      complications: [], styleTags: [], designTraits: [], roleTags: [],
      notesPublic: true,
    })
    expect(revalidatePath).toHaveBeenCalledWith('/u/[username]', 'layout')
  })

  it('rejects non-boolean notesPublic with a Zod error', async () => {
    const { addWatch } = await import('@/app/actions/watches')
    const result = await addWatch({
      brand: 'Rolex', model: 'Submariner',
      status: 'owned', movement: 'automatic',
      complications: [], styleTags: [], designTraits: [], roleTags: [],
      notesPublic: 'yes', // wrong type
    })
    expect(result.success).toBe(false)
  })
})
```

If any of the above DAL/util mock paths do not exist in the codebase, simplify the mock declarations to whatever is actually exported (read the existing tests/actions/ folder for the established mock layout). The functional intent is: the action accepts `notesPublic` and revalidates `/u/[username]`.
  </action>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo &amp;&amp; npm test -- tests/components/watch/WatchForm.notesPublic.test.tsx tests/components/watch/WatchForm.isChronometer.test.tsx tests/components/watch/WatchDetail.isChronometer.test.tsx tests/actions/watches.notesPublic.test.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist at the listed paths.
    - Tests fail (RED) when run today: WatchForm.notesPublic fails because no role="switch" pill exists; WatchForm.isChronometer fails because no Checkbox with that label exists; WatchDetail.isChronometer fails because no Certification row exists; watches.notesPublic fails because Zod rejects notesPublic OR revalidatePath('/u/[username]', 'layout') is not called.
    - Locked copy literal in scaffolds: "Visibility:", "Public", "Private", "Note is public, click to make private", "Note is private, click to make public", "Chronometer-certified", "(COSC or equivalent)", "Certification", "Chronometer".
    - `grep -n 'aria-checked' tests/components/watch/WatchForm.notesPublic.test.tsx` returns at least 2 lines.
    - `grep -n "revalidatePath.*'/u/\[username\]'" tests/actions/watches.notesPublic.test.ts` returns at least 2 lines.
    - No regression in existing test suite — `npm test 2>&amp;1 | grep -E 'Tests.*failed' | tail -5` shows only the 4 new files in the failure column (plus any pre-existing failures, if any).
  </acceptance_criteria>
  <done>4 RED test scaffolds committed; running them fails with clear errors; the existing test suite is otherwise unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Wave 0 produces test files only. No production code change, no auth/data surface introduced. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Information Disclosure | Test fixtures | accept | Test files use stub user IDs and synthetic data; no real user data, no real secrets, nothing committed that could leak. |
| T-23-01-02 | Tampering | Mock layer | accept | All mocks are scoped via `vi.mock` and reset per-test with `vi.clearAllMocks()` in beforeEach where state matters. No persistence layer is touched. |
</threat_model>

<verification>
- 8 new test files exist at the documented paths.
- Running each individually fails (RED) with a clear error message (module-not-found OR element-not-found OR assertion-mismatch).
- The existing test suite (excluding the 8 new RED files) remains green: `npm test 2>&1 | tail -20` shows the pre-existing pass count unchanged.
- No production source files modified in this plan.
</verification>

<success_criteria>
- [ ] All 8 RED scaffolds committed to git.
- [ ] Each scaffold imports the not-yet-written symbol it covers OR asserts a not-yet-rendered element.
- [ ] Locked copy strings (em-dashes, "Brand Loyalist — Same maker, different models", "Chronometer-certified", etc.) are byte-identical between scaffolds and UI-SPEC § Copywriting Contract.
- [ ] `npm test -- tests/components/settings/preferences/ tests/components/settings/AppearanceSection.test.tsx tests/components/settings/PreferencesClientEmbedded.test.tsx tests/components/watch/WatchForm.notesPublic.test.tsx tests/components/watch/WatchForm.isChronometer.test.tsx tests/components/watch/WatchDetail.isChronometer.test.tsx tests/actions/watches.notesPublic.test.ts 2>&1 | grep -E 'failed' returns at least 8 failure references.
- [ ] No source file under `src/` is modified by Plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/23-settings-sections-schema-field-ui/23-01-SUMMARY.md` documenting:
- Which 8 test scaffold files were created
- Confirmation that all 8 are RED (failure mode for each)
- Confirmation that the existing test suite is otherwise green
- Any deviation from the locked copy (should be zero)
</output>
