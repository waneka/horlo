---
phase: 48
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/catalog/[catalogId]/page.tsx
  - tests/app/catalog-page.test.ts
autonomous: true
requirements: [BUG-01]
must_haves:
  truths:
    - "A wishlist watch viewed via /catalog/[catalogId] does NOT trigger the 'You own this watch' callout"
    - "A grail watch viewed via /catalog/[catalogId] does NOT trigger the 'You own this watch' callout"
    - "A sold watch viewed via /catalog/[catalogId] does NOT trigger the 'You own this watch' callout"
    - "A watch the viewer actually owns (status='owned') STILL renders the 'You own this watch' callout (no regression)"
    - "tests/app/catalog-page.test.ts mocks @/data/profiles so getProfileById returns null safely under test"
  artifacts:
    - path: "src/app/catalog/[catalogId]/page.tsx"
      provides: "findViewerWatchByCatalogId with status='owned' filter on watches table query"
      contains: "eq(watchesTable.status, 'owned')"
    - path: "tests/app/catalog-page.test.ts"
      provides: "BUG-01 regression coverage for wishlist/grail/sold + retained owned-path test + @/data/profiles mock"
      contains: "BUG-01"
  key_links:
    - from: "src/app/catalog/[catalogId]/page.tsx findViewerWatchByCatalogId"
      to: "drizzle watches table where()"
      via: "and(eq userId, eq catalogId, eq status='owned')"
      pattern: "eq\\(watchesTable\\.status,\\s*'owned'\\)"
    - from: "tests/app/catalog-page.test.ts"
      to: "@/data/profiles getProfileById"
      via: "vi.mock with vi.hoisted mockGetProfileById returning null"
      pattern: "vi\\.mock\\(['\"]@/data/profiles['\"]"
---

<objective>
Fix BUG-01 — the catalog ownership mislabel — with the surgical one-line Drizzle filter change per D-02, and add regression coverage per D-10 so a wishlist/grail/sold watch viewed via /catalog/[catalogId] never triggers the "You own this watch" framing again. Also close the @/data/profiles mock gap that RESEARCH.md A1 / Pitfall 1 / PATTERNS.md flagged.

Purpose: Eliminate the live production bug where any non-owned `watches` row (wishlist, grail, sold) on a catalog page was being treated as owned and rendered with the `self-via-cross-user` framing + `YouOwnThisCallout`. After this plan, a wishlist watch falls through to the existing `collection.length > 0` cross-user verdict path (per D-03) and the existing wishlist-aware CTA — no new callout, no new framing branch.

Output: A modified `findViewerWatchByCatalogId` query, an extended catalog-page test file with three new BUG-01 regression cases + an explicit `@/data/profiles` mock, all existing tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/48-user-facing-bug-fixes/48-CONTEXT.md
@.planning/phases/48-user-facing-bug-fixes/48-RESEARCH.md
@.planning/phases/48-user-facing-bug-fixes/48-PATTERNS.md
@.planning/phases/48-user-facing-bug-fixes/48-VALIDATION.md

@src/app/catalog/[catalogId]/page.tsx
@tests/app/catalog-page.test.ts
@tests/setup.tsx
@src/db/schema.ts
@src/lib/types.ts

<interfaces>
<!-- Key contracts extracted from the codebase. Executor uses these directly — no exploration needed. -->

From src/lib/types.ts (WatchStatus enum):
- WatchStatus = 'owned' | 'wishlist' | 'sold' | 'grail'
- Only 'owned' is "truly owned" — the BUG-01 fix narrows the query to this single status.

From src/db/schema.ts line 91 (watches table status column):
- status: text('status', { enum: ['owned', 'wishlist', 'sold', 'grail'] }).notNull()
- watchesTable is the local alias for `watches` already imported in catalog/[catalogId]/page.tsx.

From src/app/catalog/[catalogId]/page.tsx (current findViewerWatchByCatalogId, lines 279-301):
- async function findViewerWatchByCatalogId(userId, catalogId): Promise<{ id, acquisitionDate } | null>
- Uses drizzle `db.select(...).from(watchesTable).where(and(eq(userId), eq(catalogId))).limit(1)`
- `and`, `eq`, `watchesTable`, `db` are ALL already imported (lines 3, 22 — no new imports needed)
- Caller at ~line 104 sets viewerOwnedRow → verdict.framing='self-via-cross-user' → YouOwnThisCallout

From src/app/catalog/[catalogId]/page.tsx line 9 (profiles import):
- import { getProfileById } from '@/data/profiles'
- Called at line 67 inside Promise.all — currently NOT mocked in catalog-page.test.ts
- tests/setup.tsx does NOT supply a global mock for @/data/profiles

From tests/app/catalog-page.test.ts vi.hoisted block (lines 3-28) + vi.mock block (lines 31-96):
- mockDbLimit drives findViewerWatchByCatalogId return shape — bypassing the .where() chain entirely.
- mockComputeVerdictBundle is the spy used to assert framing='cross-user' on non-owned cases.
- baseCatalogEntry (line 102) + validCatalogId (line 100) — reuse as-is.
- Pattern to follow for the new mock: `tests/app/watch-new-page.test.ts` (uses identical vi.hoisted + vi.mock idiom for @/data/profiles).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add @/data/profiles mock to catalog-page.test.ts vi.hoisted + vi.mock infrastructure</name>
  <files>tests/app/catalog-page.test.ts</files>
  <read_first>
    - tests/app/catalog-page.test.ts (full file — see lines 1-100 mock infrastructure)
    - tests/setup.tsx (confirm @/data/profiles is NOT globally mocked)
    - src/app/catalog/[catalogId]/page.tsx (line 9 import, line 67 Promise.all call site)
  </read_first>
  <behavior>
    - Test 1: All 8 existing tests in catalog-page.test.ts must still pass after the mock is added (regression guard for mock-infra change).
    - Test 2: The newly added `mockGetProfileById` is callable and defaults to resolving `null` in beforeEach (assertable via mockGetProfileById.mock.calls in any test that wants to inspect it; not required for default behavior).
  </behavior>
  <action>
    Extend the existing `vi.hoisted({ ... })` declaration block (lines 3-28) by adding `mockGetProfileById: vi.fn()` to the destructured names and the returned object. Add a sibling `vi.mock('@/data/profiles', () => ({ getProfileById: mockGetProfileById }))` line in the vi.mock cluster (lines 31-96) — placement adjacent to the other `@/data/*` mocks (after the existing `@/data/hierarchy` mock around line 62). In the existing `beforeEach` (lines 117-131), add a default `mockGetProfileById.mockResolvedValue(null)` so the import never returns undefined for any test. Follow the exact pattern documented in PATTERNS.md §"vi.hoisted + vi.mock test mock pattern" and the analog in tests/app/watch-new-page.test.ts (the same pattern is reused across app-level page tests in this project). Do NOT change any existing test assertions or any other mock. The purpose of this task is to close the A1 / Pitfall 1 gap before adding new test cases in Task 2 — addressing per D-10.
  </action>
  <verify>
    <automated>npx vitest run tests/app/catalog-page.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mockGetProfileById" tests/app/catalog-page.test.ts` returns at least 3 lines (one in vi.hoisted, one in vi.mock, one in beforeEach).
    - `grep -n "vi.mock('@/data/profiles'" tests/app/catalog-page.test.ts` returns exactly 1 hit.
    - `npx vitest run tests/app/catalog-page.test.ts` exits 0 with all 8 pre-existing test cases passing (no regressions from the mock-infra change).
    - No existing `it(...)` test bodies are modified by this task.
  </acceptance_criteria>
  <done>The @/data/profiles mock is present in catalog-page.test.ts via vi.hoisted + vi.mock + beforeEach default, all 8 pre-existing tests still pass, and the file is ready to host new BUG-01 cases in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 3 BUG-01 regression test cases (wishlist / grail / sold) + retain owned-path regression guard</name>
  <files>tests/app/catalog-page.test.ts</files>
  <read_first>
    - tests/app/catalog-page.test.ts (after Task 1 — see lines 116-219 describe block for the it(...) shape to copy)
    - .planning/phases/48-user-facing-bug-fixes/48-RESEARCH.md §"BUG-01 Regression Test Pattern" and §"Pitfall 3"
    - .planning/phases/48-user-facing-bug-fixes/48-PATTERNS.md §"tests/app/catalog-page.test.ts (MODIFY — BUG-01 regression tests)"
  </read_first>
  <behavior>
    - Test "BUG-01 — wishlist watch does NOT trigger 'You own this watch' callout" — given mockDbLimit returns [] (the FIXED query's behavior for status='wishlist'), assert mockComputeVerdictBundle is called exactly once AND mockComputeVerdictBundle.mock.calls[0][0].framing === 'cross-user'. Test currently does NOT exist — adding new.
    - Test "BUG-01 — grail watch does NOT trigger 'You own this watch' callout" — identical setup pattern as wishlist (mockDbLimit empty); assert mockComputeVerdictBundle called once with framing='cross-user'. New.
    - Test "BUG-01 — sold watch does NOT trigger 'You own this watch' callout" — identical pattern; assert mockComputeVerdictBundle called once with framing='cross-user'. New.
    - Existing test at line 157 ("renders 'You own this watch' callout when viewer already owns this catalog ref (D-08)") MUST still pass unchanged — this is the regression guard for the owned path. Do NOT modify it.

    Note: the 3 new tests deliberately mock identical `mockDbLimit.mockResolvedValue([])` — they simulate the fixed query (Task 3) returning empty for any non-owned status. The test names encode the user-visible bug being protected against; the mock asserts the post-fix behavior, not the watch status (the .where() filter never executes under the mock chain — RESEARCH.md Pitfall 3).
  </behavior>
  <action>
    Inside the existing `describe('D-10 /catalog/[catalogId] page (Plan 06)', () => { ... })` block, append three new `it(...)` test cases — one each for the wishlist, grail, and sold status — at the END of the describe block (after the last existing test). Each new test reuses the same prelude as the existing line-140 test (`mockGetCatalogById.mockResolvedValue(baseCatalogEntry); mockGetWatchesByUser.mockResolvedValue([{ id: 'mine-1' }]); mockDbLimit.mockResolvedValue([])`), then awaits `CatalogPage({ params: Promise.resolve({ catalogId: validCatalogId }) })`, then asserts `expect(mockComputeVerdictBundle).toHaveBeenCalledTimes(1)` AND `expect(mockComputeVerdictBundle.mock.calls[0][0].framing).toBe('cross-user')`. The test name strings must include the literal token `BUG-01` so the regression coverage is greppable. Do NOT touch the existing line-157 test ("renders 'You own this watch' callout when viewer already owns this catalog ref (D-08)") — it serves as the owned-path regression guard (Pitfall 3) and must remain unmodified. Follow PATTERNS.md §"BUG-01 regression test patterns" verbatim.
  </action>
  <verify>
    <automated>npx vitest run tests/app/catalog-page.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BUG-01" tests/app/catalog-page.test.ts` (post-task) returns at least 3 (the three new test name strings).
    - `npx vitest run tests/app/catalog-page.test.ts` exits 0 — total test count = 8 (pre-existing) + 3 (new) = 11 passing; 0 failing.
    - The pre-existing test at line 157 (matchable as `grep -n "renders \"You own this watch\" callout when viewer already owns" tests/app/catalog-page.test.ts`) still exists with no modifications to its body.
    - Each new test asserts `framing).toBe('cross-user')` — verify with `grep -c "framing).toBe('cross-user')" tests/app/catalog-page.test.ts` returns at least 4 (1 pre-existing line-146 + 3 new).
  </acceptance_criteria>
  <done>Three new BUG-01 regression tests exist (wishlist, grail, sold) and all PASS against the CURRENT (unfixed) page.tsx because mockDbLimit returns []; they will continue to pass after Task 3 fixes the query (because the fix is what makes mockDbLimit=[] the realistic behavior for non-owned statuses). The owned-path test remains untouched as the regression guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add status='owned' filter to findViewerWatchByCatalogId — the BUG-01 surgical fix (D-02)</name>
  <files>src/app/catalog/[catalogId]/page.tsx</files>
  <read_first>
    - src/app/catalog/[catalogId]/page.tsx (lines 1-30 for imports, lines 100-170 for the viewerOwnedRow caller, lines 270-302 for findViewerWatchByCatalogId)
    - .planning/phases/48-user-facing-bug-fixes/48-RESEARCH.md §"BUG-01 Deep Dive" and §"BUG-01 Fix — One-Line Query Change"
    - .planning/phases/48-user-facing-bug-fixes/48-PATTERNS.md §"src/app/catalog/[catalogId]/page.tsx"
  </read_first>
  <behavior>
    - Owned-path regression guard: the pre-existing test "renders 'You own this watch' callout when viewer already owns this catalog ref (D-08)" (line 157) must still pass — mockDbLimit returns the row, mocked .where() never executes, so the fix is transparent to this test.
    - All 3 BUG-01 regression tests added in Task 2 must remain green — mockDbLimit=[] still simulates the post-fix behavior.
    - No other test file is touched or expected to change behavior.
  </behavior>
  <action>
    In `src/app/catalog/[catalogId]/page.tsx`, modify the `.where(...)` clause inside `findViewerWatchByCatalogId` (currently line 290): expand the existing `and(eq(watchesTable.userId, userId), eq(watchesTable.catalogId, catalogId))` to a 3-argument form by adding `eq(watchesTable.status, 'owned')` as the third condition — per D-02 (only `status='owned'` rows are "truly owned"; wishlist/grail/sold must fall through). Reformat the `.where(and(...))` call across multiple lines if it improves readability — the project style allows either. Do NOT add new imports — `and`, `eq`, `watchesTable`, and `db` are already imported (lines 3 and 22 — verified). Do NOT change any other code in the file: do NOT touch the viewerOwnedRow assignment (~line 104), do NOT touch the `self-via-cross-user` framing branch, do NOT add a new framing branch, do NOT add an "On your wishlist" callout (explicitly deferred per CONTEXT.md §"Deferred Ideas" and D-03). The T-20-06-01 security comment above the function (about the query staying scoped by userId AND catalogId) remains accurate — adding a third filter only narrows the result set further; the RESEARCH.md §"Security Domain" analysis confirms no ASVS concern.
  </action>
  <verify>
    <automated>npx vitest run tests/app/catalog-page.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "eq(watchesTable.status, 'owned')" src/app/catalog/[catalogId]/page.tsx` returns exactly 1.
    - `grep -c "self-via-cross-user" src/app/catalog/[catalogId]/page.tsx` returns the same count as before the change (no new framing branch added — should be unchanged from pre-task value of 1).
    - `grep -c "On your wishlist\|on-your-wishlist\|onYourWishlist" src/app/catalog/[catalogId]/page.tsx` returns 0 (no new callout added — Deferred Idea per CONTEXT.md).
    - `npx vitest run tests/app/catalog-page.test.ts` exits 0 — 11 passing tests (8 pre-existing + 3 new BUG-01).
    - `npx vitest run` (full suite) exits 0 — no regressions elsewhere.
  </acceptance_criteria>
  <done>findViewerWatchByCatalogId now filters by status='owned'; wishlist/grail/sold rows return null → page falls through to the existing cross-user verdict path; the live BUG-01 mislabel is eliminated; all 11 catalog-page tests pass and the full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Component | User clicks /catalog/[catalogId] link; the server-side `CatalogPage` RSC resolves params and runs the Drizzle query inside `findViewerWatchByCatalogId` |
| Server Component → DB | `findViewerWatchByCatalogId` issues an authenticated Drizzle SELECT, already scoped by `userId` AND `catalogId` (T-20-06-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01-01 | Information disclosure | `findViewerWatchByCatalogId` ownership query (`src/app/catalog/[catalogId]/page.tsx` ~line 290) | accept | Per RESEARCH.md §"Security Domain": this change is purely additive — it adds a third filter (`status='owned'`) to a query already scoped by both `userId` AND `catalogId`. Adding a filter cannot widen access; it can only narrow rows. The pre-existing T-20-06-01 invariant ("query is scoped by BOTH userId AND catalogId — the viewer can never read another user's watches.id even if catalogIds collide") still holds. No ASVS L1 control is weakened. |
| T-48-01-02 | Tampering | `WatchStatus` enum / `status` column | accept | The `status` column is enum-constrained in `src/db/schema.ts` line 91 to `'owned' | 'wishlist' | 'sold' | 'grail'`. The DB enforces the enum; a malicious client cannot inject an arbitrary status. The filter `eq(watchesTable.status, 'owned')` is parameterized via Drizzle and not interpolated as a string. |
| T-48-01-03 | Repudiation | regression test coverage | mitigate | D-10 — add 3 new test cases (wishlist/grail/sold) that fail loudly if the `status='owned'` filter is ever removed or if a future refactor reintroduces non-owned rows into `viewerOwnedRow`. Tests live in `tests/app/catalog-page.test.ts` and run on every CI invocation via `npx vitest run`. |

**Net assessment:** This plan introduces no high-severity threats. It strictly narrows an existing query and adds regression coverage. No authentication, authorization, session, or cryptographic surface area changes (per RESEARCH.md §"Security Domain").
</threat_model>

<verification>
- `npx vitest run tests/app/catalog-page.test.ts` exits 0 with 11 passing tests (8 pre-existing + 3 new BUG-01)
- `npx vitest run` (full suite) exits 0 — no regressions
- `grep -c "eq(watchesTable.status, 'owned')" src/app/catalog/[catalogId]/page.tsx` returns 1
- `grep -c "BUG-01" tests/app/catalog-page.test.ts` returns at least 3
- `grep -c "vi.mock('@/data/profiles'" tests/app/catalog-page.test.ts` returns 1
- No new callout markup added to page.tsx (D-03 — Deferred Idea preserved)
</verification>

<success_criteria>
BUG-01 is closed: a watch in `status='wishlist'` (or `grail` / `sold`) on `/catalog/[catalogId]` never triggers the `self-via-cross-user` framing or the `YouOwnThisCallout`. An actually-owned watch (`status='owned'`) STILL renders the "You own this watch" callout (regression-guarded by the line-157 test). Regression coverage (3 new test cases per D-10) is committed. The `@/data/profiles` mock gap (A1/Pitfall 1) is closed. ROADMAP success criteria #1 and #3 are met.
</success_criteria>

<output>
After completion, create `.planning/phases/48-user-facing-bug-fixes/48-01-SUMMARY.md` summarizing the BUG-01 fix, the 3 new regression tests, the @/data/profiles mock-infra addition, and the test-suite green state.
</output>
