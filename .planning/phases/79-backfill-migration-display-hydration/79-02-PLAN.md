---
phase: 79
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/v8.4-brand-canonicalization.ts
  - tests/unit/scripts/v8.4-host-detect.test.ts
  - tests/unit/scripts/v8.4-strict-gate.test.ts
autonomous: true
requirements: [MIG-02]
tags: [migration, postgres, tsx-script, brand-apply]

must_haves:
  truths:
    - "scripts/v8.4-brand-canonicalization.ts exports new helpers: parseArgs (extended), isLocalDatabaseUrl, ResolvedBrand, BrandDecisionMap, buildBrandMap, strictPreflightGate (brand-only initial scope) per D-79-01 + D-79-02 + D-79-07"
    - "parseArgs accepts new flags --apply and --mode=brands|families|both; rejects --apply without --mode=both per Pitfall 7 (D-79-05)"
    - "Re-run safety gate D-79-04 fires BEFORE the strict gate: SELECT count(*) FROM watches_catalog WHERE brand_id IS NULL OR family_id IS NULL — if zero, exits 0 with `Already applied — nothing to do.`"
    - "Strict pre-flight gate D-79-01 refuses on (a) needs-review status, (b) unknown token (per D-78-02 grammar), (c) merge:<uuid> brand target missing in brands.id, (d) live catalog brand absent from .planning/v8.4-brand-merge-decisions.md"
    - "Interactive prod confirmation D-79-02 fires only when isLocalDatabaseUrl(connStr) === false; prints summary block + waits for stdin 'yes' via node:readline/promises; silent on local 127.0.0.1:54322"
    - "MIG-02 brand apply path: INSERT new brands (33 rows per current operator file; all needs_review=false per D-79-09) with RETURNING id to reify the in-memory BrandDecisionMap; then UPDATE watches_catalog SET brand_id = <resolved> WHERE lower(trim(brand)) = <brand_norm> AND (brand_id IS NULL OR brand_id <> <resolved>)"
    - "All NEW writes happen inside one outer sql.begin(async tx => { ... }) callback (Plan 04 will extend this callback with family + hydration + post-flight — Plan 02 lands the brand-only scaffold + the transaction wrapper)"
    - "process.exit never appears inside the sql.begin callback per Pitfall 2 (only throw new Error(...) for failures; outer try/catch handles exit)"
    - "Unit tests for isLocalDatabaseUrl and strictPreflightGate (brand-only scope) green; the Plan 01 stubs lose their `.todo` markers for D-79-01 brand cases + D-79-02 cases"
    - "scripts/v8.4-brand-canonicalization.ts contains zero `= ANY(${arr})` patterns per [[drizzle-sql-any-array-pitfall]] forward armor"
  artifacts:
    - path: scripts/v8.4-brand-canonicalization.ts
      provides: "parseArgs extended (apply + mode), isLocalDatabaseUrl, confirmIfProd, ResolvedBrand, BrandDecisionMap, buildBrandMap, strictPreflightGate (brand-only), idempotentReRunGate, applyBrandPath wrapped in sql.begin"
      exports: [parseArgs, isLocalDatabaseUrl, ResolvedBrand, BrandDecisionMap, buildBrandMap, strictPreflightGate]
    - path: tests/unit/scripts/v8.4-host-detect.test.ts
      provides: "Greened from Plan 01 RED stub — all D-79-02 it.todo callsites converted to it() and pass"
    - path: tests/unit/scripts/v8.4-strict-gate.test.ts
      provides: "Greened (brand-only refuse cases + brand pass case) — family-only it.todo callsites remain todo for Plan 03"
  key_links:
    - from: "scripts/v8.4-brand-canonicalization.ts (new strictPreflightGate)"
      to: "brands.id existence check"
      via: "sql`SELECT id FROM brands WHERE id IN ${sql(brandMergeUuids)}` (postgres-lib helper form; NOT `= ANY(${arr})`)"
      pattern: "SELECT id FROM brands WHERE id IN"
    - from: "scripts/v8.4-brand-canonicalization.ts (new applyBrandPath)"
      to: "watches_catalog.brand_id UPDATE"
      via: "sql.begin → per-brand UPDATE loop"
      pattern: "UPDATE watches_catalog\\s+SET brand_id"
---

<objective>
Extend scripts/v8.4-brand-canonicalization.ts in place with the apply-path scaffold: parseArgs extension (`--apply`, `--mode=brands|families|both`), local-vs-prod host detection (D-79-02), idempotent re-run gate (D-79-04), strict pre-flight gate for brands (D-79-01), in-memory BrandDecisionMap (D-79-07 brand-only side), and the brand-only write path inside a `sql.begin(async tx => ...)` callback that MIG-02 requires. Green the Plan 01 stubs for D-79-02 (all cases) and D-79-01 (brand cases + brand pass case).

Purpose: MIG-02 — brand backfill with idempotent re-run and atomic transaction scaffold. This plan delivers the "extract → INSERT new brands → reify map → UPDATE catalog brand_id" critical path that Phase 79's apply is built around, in isolation from the family + hydration + post-flight steps (those land in Plans 03 + 04). The sql.begin wrapper is opened here so Plan 04 can extend it without restructuring.

Output: Updated scripts/v8.4-brand-canonicalization.ts (~530 LOC from Phase 78's 345); 2 unit test files greened.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 79 context (decisions + research + patterns)
@.planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md
@.planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md
@.planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md
@.planning/phases/79-backfill-migration-display-hydration/79-01-SUMMARY.md

# The file Plan 02 extends + the operator-edited input
@scripts/v8.4-brand-canonicalization.ts
@.planning/v8.4-brand-merge-decisions.md

# Schema reference (read-only — Plan 02 writes against this)
@src/db/schema.ts

# Transaction idiom analog (the project's ONLY existing sql.begin caller)
@scripts/repair-drizzle-journal.ts

# IN-list parameter binding canonical positive example
@src/data/recommendations.ts

# Wave 0 stubs Plan 02 greens
@tests/unit/scripts/v8.4-host-detect.test.ts
@tests/unit/scripts/v8.4-strict-gate.test.ts

<interfaces>
<!-- Phase 78 already-exported types — Plan 02 reuses these unchanged -->

From scripts/v8.4-brand-canonicalization.ts (existing — DO NOT modify shape):
- export interface BrandRow { brand_raw: string; brand_normalized: string; proposed_target_id: string | null }
- export interface Candidate { name: string; score: number }
- export interface ParsedArgs { regenerate: boolean; force: boolean }   // ← Plan 02 EXTENDS this shape
- export function parseArgs(argv?: string[]): ParsedArgs                  // ← Plan 02 EXTENDS the return shape
- export function formatCell(value: string | null | undefined): string
- export function buildRow(row: BrandRow, candidates: Candidate[]): string
- export function buildTableRows(rows: BrandRow[], candidatesByNormalized: Map<string, Candidate[]>): string[]
- export function parseExistingPreserved(content: string): Map<string, string>
- export function mergeForward(existingContent: string, freshRows: BrandRow[], candidatesByNormalized: Map<string, Candidate[]>): string[]

<!-- Phase 79 NEW exports landed by Plan 02 -->

NEW interface (extends ParsedArgs):
- export interface ParsedArgs { regenerate: boolean; force: boolean; apply: boolean; mode: 'brands' | 'families' | 'both' }

NEW types (per RESEARCH Pattern 2 + PATTERNS L267-288):
- export type ResolvedBrand =
  | { kind: 'existing'; uuid: string; canonicalName: string }
  | { kind: 'merge';    uuid: string; canonicalName: string }
  | { kind: 'new';      syntheticKey: string; rawName: string }
- export type BrandDecisionMap = Map<string, ResolvedBrand>

NEW pure helpers (testable without DB):
- export function isLocalDatabaseUrl(connStr: string): boolean
- export function slugify(name: string): string                                // ~3 LOC; per RESEARCH Don't Hand-Roll
- export function parseDecisionsTable(content: string): BrandDecisionRow[]     // GFM parser; reuses Phase 78 parseExistingPreserved shape

NEW interface (parsed decisions row):
- export interface BrandDecisionRow { brand_raw: string; brand_normalized: string; proposed_target_id: string | null; status: string }

NEW function — `buildBrandMap(rows: BrandDecisionRow[]): BrandDecisionMap`
  takes parsed decision rows, returns the in-memory map. 'auto-resolved' → kind:'existing' with proposed_target_id;
  'merge:<uuid>' → kind:'merge' with uuid extracted; 'new' → kind:'new' with syntheticKey = brand_normalized.

NEW function — `strictPreflightGate(sql, brandRows, existingBrandIdsFn)`
  Returns void on pass; throws Error with a clear human-readable message on each refuse case (D-79-01 cases a-d for brands). Family cases land in Plan 03. The `existingBrandIdsFn` is dependency-injected (function returning Promise<Set<string>>) so the unit test can stub it without a DB connection per PATTERNS L361-370.

NEW function — `idempotentReRunGate(sql)`
  Returns 'already-applied' | 'proceed'. Queries `count(*) FROM watches_catalog WHERE brand_id IS NULL OR family_id IS NULL`.

NEW function — `confirmIfProd(connStr, summary)`
  Returns void. If isLocalDatabaseUrl(connStr) → silent. Else prints summary block + reads 'yes' via node:readline/promises; process.exit(1) on decline.

NEW function — `applyBrandPath(tx, brandMap, decisions): Promise<{ brandsCreated: number; catalogRowsResolvedBrand: number }>`
  Called inside the sql.begin callback opened in main(). Executes steps 4.1 (INSERT new brands; reify map; invariant check) + 4.2 (UPDATE watches_catalog.brand_id loop). Returns counts for Plan 04's POST-DEPLOY summary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseArgs extension + isLocalDatabaseUrl + confirmIfProd + green host-detect stub</name>

  <files>scripts/v8.4-brand-canonicalization.ts, tests/unit/scripts/v8.4-host-detect.test.ts</files>

  <read_first>
- scripts/v8.4-brand-canonicalization.ts L86-97  (existing parseArgs; extension point)
- scripts/v8.4-brand-canonicalization.ts L225-237  (existing main() head; ParsedArgs use)
- scripts/import-prod-catalog.sh L36-45  (existing case-on-127.0.0.1 host detection idiom — alternative reference; Plan 02 uses new URL instead)
- scripts/generate-explore-covers.ts L125-132  (existing regex-on-127.0.0.1 reference)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L433-489  (Pattern 3 — isLocalDatabaseUrl + confirmIfProd)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L829-865  (Code Examples — full isLocalDatabaseUrl + confirmIfProd implementation)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L183-221  (Phase 79 pattern application)
- tests/unit/scripts/v8.4-host-detect.test.ts  (Plan 01 RED stub — 7 it.todo callsites to green)
  </read_first>

  <behavior>
- isLocalDatabaseUrl returns true for `postgres://postgres:postgres@127.0.0.1:54322/postgres` and `postgres://...@localhost:54322/postgres`
- Returns false for `postgres://...@aws-1.pooler.supabase.com:6543/postgres` and similar prod pooler URLs
- Returns false for `127.0.0.1:54323` (alt port — safety bias per D-79-02)
- Returns false for unparseable strings and empty string (fail closed per D-79-02)
- parseArgs accepts `--apply` (boolean), `--mode=brands|families|both` (literal union; default 'brands' for backward compat), and rejects `--apply && mode !== 'both'` early in main() per Pitfall 7
- confirmIfProd is silent on local URL; prints summary + reads 'yes' from stdin via node:readline/promises on non-local URL (the actual stdin-prompt path is not unit-tested — it's exercised by Plan 05 prod operator UAT)
  </behavior>

  <action>
In scripts/v8.4-brand-canonicalization.ts:

  (a) Add `import * as readline from 'node:readline/promises'` at top (after existing imports at L58-61).

  (b) Extend `ParsedArgs` interface (L86-89) to add `apply: boolean` and `mode: 'brands' | 'families' | 'both'`. Preserve existing fields.

  (c) Extend `parseArgs(argv)` (L91-97) to parse `--apply` (boolean: presence in flags set) and `--mode=<value>` (extract via argv.find(a => a.startsWith('--mode='))?.split('=')[1]; default 'brands' if absent; validate the value is in the literal union — throw clear error if not).

  (d) Add NEW exported helper `isLocalDatabaseUrl(connStr: string): boolean` per RESEARCH L433-453: wrap `new URL(connStr).host` check in try/catch; return `url.host === '127.0.0.1:54322' || url.host === 'localhost:54322'`; on URL parse throw, return false (fail closed). Place between existing parseArgs and formatCell.

  (e) Add NEW exported helper `slugify(name: string): string` per PATTERNS L292-294: 3-LOC pure function `name.toLowerCase().trim().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')`. Plan 02 needs this for the new brand INSERT in Task 3.

  (f) Add NEW interface `ApplySummary { brandsToCreate: number; catalogRowsToResolve: number; familiesToCreate: number; userWatchesToHydrate: number; aliasesToAppend: number }` (Plan 03 + 04 fill in the non-brand fields; Plan 02 only populates brand fields).

  (g) Add NEW non-exported helper `confirmIfProd(connStr, summary): Promise<void>` per RESEARCH L843-864:
     - If `isLocalDatabaseUrl(connStr)` → return immediately (silent local per D-79-02).
     - Else: console.log the summary block (5 lines, one per ApplySummary field).
     - readline.createInterface({input: process.stdin, output: process.stdout}); await rl.question('Type "yes" to proceed: ').
     - If answer.trim() !== 'yes', `console.error('[v8.4-brand-canon] Operator declined. Exiting without write.')` and `process.exit(1)` — NOTE: this process.exit is OUTSIDE the sql.begin callback per Pitfall 2, so it's safe.
     - finally rl.close().

  (h) Early validation block to add at the top of main() AFTER parseArgs and BEFORE the existing refuse-to-overwrite gate: if `args.apply && args.mode !== 'both'`, console.error('[v8.4-brand-canon] --apply requires --mode=both') and process.exit(1). Per Pitfall 7.

In tests/unit/scripts/v8.4-host-detect.test.ts:

  Replace all `it.todo(...)` calls with `it(...)` assertions per the Plan 01 stub spec. Use `import { isLocalDatabaseUrl } from '../../../scripts/v8.4-brand-canonicalization'` (uncomment the Plan 01 escape-hatch comment). Each test asserts the truth-table case (7 cases per the Plan 01 spec).

Mitigates T-79-02 (operator runs apply against wrong DB — host-based interactive gate is the mitigation).
  </action>

  <verify>
    <automated>npx vitest run tests/unit/scripts/v8.4-host-detect.test.ts && grep -c "isLocalDatabaseUrl" scripts/v8.4-brand-canonicalization.ts && grep -c "import \* as readline from 'node:readline/promises'" scripts/v8.4-brand-canonicalization.ts</automated>
  </verify>

  <done>
    - scripts/v8.4-brand-canonicalization.ts exports isLocalDatabaseUrl + slugify + confirmIfProd + extended ParsedArgs.
    - parseArgs accepts --apply + --mode flags.
    - Early validation block rejects --apply without --mode=both.
    - tests/unit/scripts/v8.4-host-detect.test.ts: 7 + 1 sanity = 8 tests pass, 0 todo, 0 failed.
    - grep for readline import returns 1; grep for isLocalDatabaseUrl returns ≥3 (definition + Task 2/3 callers).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BrandDecisionMap + parseDecisionsTable + buildBrandMap + strictPreflightGate (brand-only) + idempotentReRunGate + green strict-gate stub (brand cases)</name>

  <files>scripts/v8.4-brand-canonicalization.ts, tests/unit/scripts/v8.4-strict-gate.test.ts</files>

  <read_first>
- scripts/v8.4-brand-canonicalization.ts L155-171  (existing parseExistingPreserved — GFM table parser; reuse shape)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L391-431  (Pattern 2 — BrandDecisionMap shape + family parallel)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L867-935  (Code Examples — strictPreflightGate full implementation)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L937-953  (Code Examples — idempotentReRunGate)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L612-629  (IN-list parameter binding anti-pattern guardrail — use sql.join, NEVER ANY(arr))
- .planning/v8.4-brand-merge-decisions.md  (fixture for unit tests; the strict-gate test parses this verbatim)
- .planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md L34-35  (D-79-01 strict gate refuse cases verbatim)
- tests/unit/scripts/v8.4-strict-gate.test.ts  (Plan 01 RED stub — 6 brand it.todo callsites to green; 1 family-only callsite stays todo for Plan 03)
  </read_first>

  <behavior>
- parseDecisionsTable(content): splits brand-merge-decisions.md content into BrandDecisionRow[]; ignores header/separator/comment lines per existing parseExistingPreserved pattern. Each row has brand_raw, brand_normalized, proposed_target_id (string | null), status (string).
- buildBrandMap(rows): converts BrandDecisionRow[] to BrandDecisionMap. 'auto-resolved' → kind:'existing' (uuid from proposed_target_id, canonicalName from brand_raw). 'merge:<uuid>' → kind:'merge' (uuid extracted, canonicalName from brand_raw of the merge target row — keyed by brand_normalized matching the proposed_target_id's row). 'new' → kind:'new' (syntheticKey=brand_normalized, rawName=brand_raw). 'skip' → throw (not supported in Phase 79 — operator decision policy).
- strictPreflightGate refuse cases (brand-only scope; family-only refuses stay it.todo for Plan 03): (a) status === 'needs-review' on any brand row → throw, (b) status not in VALID_BRAND_STATUSES nor MERGE_RE → throw, (c) any merge:<uuid> uuid not in injected existingBrandIds set → throw, (d) live catalog brand_norm not in any decision row → throw. PASS case returns void.
- idempotentReRunGate(sql): returns 'already-applied' if count of unresolved (brand_id IS NULL OR family_id IS NULL) is 0; else 'proceed'. Logs the already-applied message via console.log per D-79-04.
  </behavior>

  <action>
In scripts/v8.4-brand-canonicalization.ts (continuing Task 1's edits):

  (a) Add NEW exported types per PATTERNS L267-275:
     - `export type ResolvedBrand = { kind: 'existing'; uuid: string; canonicalName: string } | { kind: 'merge'; uuid: string; canonicalName: string } | { kind: 'new'; syntheticKey: string; rawName: string }`
     - `export type BrandDecisionMap = Map<string, ResolvedBrand>`
     - `export interface BrandDecisionRow { brand_raw: string; brand_normalized: string; proposed_target_id: string | null; status: string }`

  (b) Add NEW exported `parseDecisionsTable(content): BrandDecisionRow[]`. Mirror parseExistingPreserved L155-171 shape: split on \n; skip lines not starting with `|`, separator lines (`| ---`), header lines (`| brand_raw`). Split each data line on `|`, trim cells. Cells per the Phase 78 GFM layout: cells[1]=brand_raw, cells[2]=brand_normalized, cells[3]=proposed_target_id (empty string → null), cells[4]=status. Return array of typed rows.

  (c) Add NEW exported `buildBrandMap(rows: BrandDecisionRow[]): BrandDecisionMap` per the behavior spec above. Key: brand_normalized. Validate status grammar inline (throw on 'skip' or unknown).

  (d) Add NEW exported `async function strictPreflightGate(sql, brandRows, existingBrandIdsFn): Promise<void>` per RESEARCH L867-935. Inject `existingBrandIdsFn: (uuids: string[]) => Promise<Set<string>>` so the unit test stubs it without a DB connection (per PATTERNS L361-370). Implementation:
     - VALID_BRAND_STATUSES = new Set(['auto-resolved', 'new'])
     - MERGE_RE = /^merge:[0-9a-f-]{36}$/i
     - For each brandRow: if (!VALID_BRAND_STATUSES.has(row.status) && !MERGE_RE.test(row.status)) throw new Error with the message from CONTEXT D-79-01.
     - Extract merge UUIDs from brandRows where MERGE_RE.test(status); call existingBrandIdsFn(uuids); for any missing uuid, throw.
     - Query distinct catalog brand_normalized values via `sql\`SELECT DISTINCT lower(trim(brand)) AS brand_normalized FROM watches_catalog\``; for any live brand_norm not in decidedBrands set (built from brandRows.brand_normalized), throw.
     - DO NOT use `= ANY(${arr})` ANYWHERE — use `sql\`SELECT id FROM brands WHERE id IN ${sql(brandMergeUuids)}\`` (postgres-lib helper form per PATTERNS L612-629).

  (e) Add NEW non-exported `async function idempotentReRunGate(sql): Promise<'already-applied' | 'proceed'>` per RESEARCH L937-953. SELECT count(*) FROM watches_catalog WHERE brand_id IS NULL OR family_id IS NULL. If 0 → console.log('[v8.4-brand-canon] Already applied — nothing to do. Exiting.') + return 'already-applied'. Else return 'proceed'.

In tests/unit/scripts/v8.4-strict-gate.test.ts:

  Replace 6 of the 7 brand-related `it.todo(...)` calls with `it(...)` assertions:
  - case (a) needs-review → assert throws with /needs-review/i message
  - case (b) unknown token → assert throws with /unknown.*status|status.*unresolved/i message  
  - case (c) merge:<uuid> brand target missing → use injected `existingBrandIdsFn` returning empty Set; assert throws with /merge.*not found/i
  - case (d) live catalog brand absent from decisions file → mock sql to return a brand_normalized not in fixtures; assert throws with /catalog has brand/i
  - PASS case → all fixtures valid; injected existingBrandIdsFn returns a Set containing the merge UUID; mock sql returns brand_normalized values that match fixtures; assert resolves to void (no throw)
  - Sanity it() — unchanged
  
  Leave family-only it.todo callsites (case d-family + case e-family + brand+family combined PASS case) as `it.todo` — those green in Plan 03.

Mitigates T-79-03 (operator-edited decisions file goes stale: strict pre-flight gate refuses on drift) + T-79-04 (re-run safety: idempotentReRunGate catches double-apply).
  </action>

  <verify>
    <automated>npx vitest run tests/unit/scripts/v8.4-strict-gate.test.ts && grep -c "export function buildBrandMap\|export function strictPreflightGate\|export function parseDecisionsTable" scripts/v8.4-brand-canonicalization.ts && (grep -c "= ANY(" scripts/v8.4-brand-canonicalization.ts | grep -q '^0$' || (echo "FAIL: = ANY(arr) pattern present — violates [[drizzle-sql-any-array-pitfall]]"; exit 1))</automated>
  </verify>

  <done>
    - scripts/v8.4-brand-canonicalization.ts exports ResolvedBrand, BrandDecisionMap, BrandDecisionRow, parseDecisionsTable, buildBrandMap, strictPreflightGate.
    - idempotentReRunGate exists (non-exported helper).
    - tests/unit/scripts/v8.4-strict-gate.test.ts: 6 brand + 1 PASS + 1 sanity = ~8 tests pass; family-only it.todo callsites remain todo (count varies per Plan 01 spec).
    - Zero `= ANY(` patterns in the script per [[drizzle-sql-any-array-pitfall]] forward armor.
    - grep returns 3 for the 3 NEW exported functions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Brand apply path inside sql.begin (INSERT new brands + reify map + UPDATE catalog brand_id) + main() mode dispatch wiring</name>

  <files>scripts/v8.4-brand-canonicalization.ts</files>

  <read_first>
- scripts/v8.4-brand-canonicalization.ts L225-335  (existing main() body — dry-run dispatch point)
- scripts/repair-drizzle-journal.ts L172-179  (THE project's only sql.begin caller — verbatim shape Phase 79 inherits)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L282-389  (Pattern 1 — sql.begin transactional atomicity full skeleton)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L692-826  (Code Examples — applyPath full 6-step skeleton; Plan 02 implements steps 4.1 + 4.2 only)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L82-126  (sql.begin pattern verbatim; per-row UPDATE loop pattern)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L267-288  (BrandDecisionMap reify pattern after Step 4.1 with `for v of brandMap.values() if v.kind === 'new' throw` invariant per Pitfall 3)
- src/db/schema.ts L518-537  (brands table — name, slug, needs_review columns)
- .planning/v8.4-brand-merge-decisions.md  (operator-edited input — 53 rows; 33 'new' rows; 1 merge — Plan 02's apply touches all 53)
  </read_first>

  <action>
In scripts/v8.4-brand-canonicalization.ts (continuing Tasks 1+2's edits):

  (a) Add NEW non-exported `async function applyBrandPath(tx, brandMap, decisions): Promise<{ brandsCreated: number; catalogRowsResolvedBrand: number }>` per RESEARCH L692-725:
     - Step 4.1: for each entry in brandMap where kind === 'new', INSERT INTO brands (name, slug, needs_review) VALUES (${rawName}, ${slugify(rawName)}, false) RETURNING id. Set brandMap entry to {kind:'existing', uuid: row.id, canonicalName: rawName}. Increment brandsCreated. needs_review = false hardcoded per D-79-09.
     - Invariant check (per Pitfall 3): after Step 4.1, iterate brandMap.values(); if any kind === 'new', throw new Error('brandMap not reified after Step 4.1'). The post-flight assertion would catch this downstream, but the explicit invariant produces a clearer diagnostic.
     - Step 4.2: for each [brandNorm, resolved] of brandMap.entries(), `UPDATE watches_catalog SET brand_id = ${resolved.uuid} WHERE lower(trim(brand)) = ${brandNorm} AND (brand_id IS NULL OR brand_id <> ${resolved.uuid})`. Accumulate result.count → catalogRowsResolvedBrand.
     - Return {brandsCreated, catalogRowsResolvedBrand}.

  (b) Restructure main() to dispatch by mode (D-79-05). Plan 02 ONLY implements the brand-only apply branch + the existing brand dry-run branch. Family branches stub-throw with `throw new Error('--mode=families implemented in Plan 03')`. The both-apply branch stub-throws with `throw new Error('--mode=both implemented in Plan 04')`. Concretely add ~70 LOC at the end of main()'s try block (before the existing sql.end() finally):

     ```
     if (args.apply) {
       // Phase 79 apply scaffold (Plan 02 lands brand-only; Plans 03/04 extend)
       
       // D-79-04 idempotent re-run gate
       const gateStatus = await idempotentReRunGate(sql)
       if (gateStatus === 'already-applied') return  // early exit; sql.end fires in finally

       // Read decisions file
       const brandContent = await readFile(OUTPUT_FILE, 'utf8')
       const brandRows = parseDecisionsTable(brandContent)
       
       // D-79-01 strict pre-flight gate (brand-only scope in Plan 02)
       const existingBrandIdsFn = async (uuids) => {
         const rows = await sql<{ id: string }[]>`SELECT id FROM brands WHERE id IN ${sql(uuids)}`
         return new Set(rows.map(r => r.id))
       }
       await strictPreflightGate(sql, brandRows, existingBrandIdsFn)
       
       // Build BrandDecisionMap
       const brandMap = buildBrandMap(brandRows)
       
       // Family scope — Plan 03 implements
       if (args.mode === 'families' || args.mode === 'both') {
         throw new Error('--mode=families and --mode=both apply paths land in Plans 03 + 04')
       }
       
       // Plan 02 scope — brand-only apply scaffold (NOT yet wrapped for prod use;
       // Plan 04 wraps the full 6-step transaction with confirmIfProd + summary).
       // For Plan 02 unit/integration testability, the brand-only apply runs in its
       // OWN sql.begin so Task 3 can verify the brand path independently.
       const counts = await sql.begin(async (tx) => {
         return await applyBrandPath(tx, brandMap, { brandRows })
       })
       
       console.log(`[v8.4-brand-canon] Plan 02 brand apply complete: ` +
         `${counts.brandsCreated} brands created, ` +
         `${counts.catalogRowsResolvedBrand} catalog rows resolved (brand_id)`)
       return
     }
     ```

  (c) Comment in script header noting the Plan 02 scope: brand-only apply scaffold; Plans 03+04 extend.

NO unit tests for applyBrandPath here — it's exercised by the Plan 01 integration stub `tests/integration/scripts/v8.4-apply-atomic.test.ts`, which stays `it.todo` until Plan 04 wires the full transaction (greening fragments incrementally would require integration-test forks per plan, which is over-engineering).

Mitigates T-79-01 (atomic transaction with auto-rollback on throw — partial state cannot persist) + T-79-04 (alias-append non-idempotency: brand UPDATE `AND brand_id IS NULL OR brand_id <> resolved` predicate makes re-run a no-op).

NO `process.exit` inside the sql.begin callback per Pitfall 2 — only throw.
  </action>

  <verify>
    <automated>npx vitest run tests/unit/scripts/v8.4-host-detect.test.ts tests/unit/scripts/v8.4-strict-gate.test.ts tests/unit/scripts/ && npm run test -- tests/static/ && grep -c "sql.begin" scripts/v8.4-brand-canonicalization.ts && (grep -c "process\.exit" scripts/v8.4-brand-canonicalization.ts | xargs -I{} test {} -le 5)</automated>
  </verify>

  <done>
    - applyBrandPath function exists with the 2-step pattern (INSERT new brands → reify map → invariant check → UPDATE catalog brand_id loop).
    - main() dispatches on args.apply + args.mode; brand-only apply branch reaches applyBrandPath inside sql.begin.
    - --mode=families and --mode=both apply paths throw clear "implemented in Plan 03/04" error (not crash).
    - Existing dry-run path unchanged — `npm run db:v8.4-brand-canon` (no flags) still works exactly as Phase 78 shipped.
    - All Plan 01 + 02 unit tests green; static tests green.
    - `grep -c "sql.begin"` in script returns ≥1 (Plan 02 opens one; Plan 04 may add/restructure).
    - `grep -c "process.exit"` ≤ 5 (top-level + Task 1 helpers + early-validation; NONE inside sql.begin callback).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-edited markdown → script parser | tampering surface; mitigated by strict gate |
| operator shell → script stdin | confirmation gate; mitigated by isLocalDatabaseUrl + interactive yes |
| script → Postgres (service-role via DATABASE_URL) | privileged write; service-role bypasses RLS by design (D-78-06 inheritance) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02 | Tampering / Confused Deputy | confirmIfProd in scripts/v8.4-brand-canonicalization.ts (Task 1) | mitigate | isLocalDatabaseUrl checks `new URL(connStr).host` against the local Supabase port; non-local → interactive `yes` gate before any write; unparseable URL fails closed (treats as prod) |
| T-79-03 | Tampering | strictPreflightGate in scripts/v8.4-brand-canonicalization.ts (Task 2) | mitigate | strict pre-flight refuses on any drift: needs-review status / unknown token / merge:<uuid> target missing in DB / live catalog brand absent from decisions file. No `--allow-skips` escape hatch per D-79-01 |
| T-79-04 | Integrity | applyBrandPath UPDATE catalog brand_id (Task 3) | mitigate | UPDATE predicate `AND (brand_id IS NULL OR brand_id <> resolved)` makes the UPDATE a no-op when brand_id is already correct; combined with the idempotentReRunGate at script entry, re-runs are safe |
| T-79-06 | Information Disclosure | confirmIfProd summary block (Task 1) | accept | summary prints brand/catalog counts (no UUIDs of users / no PII); UUIDs of `merge:` decisions go to stdout but project policy treats brand UUIDs as non-sensitive (necessary for operator audit) |
| T-INJ-01 | Tampering (SQL injection) | parseDecisionsTable + applyBrandPath (Task 2 + Task 3) | mitigate | all template-literal interpolations bind as postgres-lib parameters; brand_raw / merge UUIDs never concatenate into raw SQL; per RESEARCH § Security Domain |
</threat_model>

<verification>
- All Task 1+2 unit tests green: v8.4-host-detect.test.ts (8 tests), v8.4-strict-gate.test.ts (brand cases + sanity).
- scripts/v8.4-brand-canonicalization.ts builds clean: `npm run build` exits 0.
- Static prebuild guards green: `npm run test -- tests/static/`.
- Existing Phase 78 dry-run unchanged: `npm run db:v8.4-brand-canon -- --force` still produces the same artifact (regression-free; verify by spawning the script with DATABASE_URL set if available, OR via grep on the script that the Stage 1-4 dry-run blocks are intact).
- ZERO `= ANY(` patterns in the script per `[[drizzle-sql-any-array-pitfall]]` forward armor.
- ZERO `process.exit` calls INSIDE the sql.begin callback per Pitfall 2 (grep verify: process.exit appears only at top-level main() and in confirmIfProd's decline branch, NEVER inside `sql.begin(async ...)` callback).
- ROADMAP MIG-02 success criterion #1 (idempotent on re-run) partially satisfied — idempotentReRunGate fires; the UPDATE predicate `brand_id IS NULL OR <> resolved` keeps re-runs safe even if the gate ever misfires.
</verification>

<success_criteria>
- scripts/v8.4-brand-canonicalization.ts ~530 LOC (from Phase 78's 345), all new exports listed in the interfaces block present and named exactly.
- Phase 78 dry-run path 100% backward-compatible: `npm run db:v8.4-brand-canon` with no flags produces the same artifact as the Phase 78 ship.
- Plan 02's brand-only apply branch in main() runs end-to-end against a local DB with the committed operator-edited brand-merge-decisions.md (manually verifiable; integration tests stay `it.todo` until Plan 04 wires the full transaction).
- Plan 01 stubs for D-79-02 (all 7 cases) and D-79-01 (4 brand refuse cases + 1 brand PASS case) green.
- D-79-04 idempotent re-run gate verifiable manually: first apply succeeds, second apply prints "Already applied — nothing to do." and exits 0 (no DB writes).
</success_criteria>

<output>
After completion, create `.planning/phases/79-backfill-migration-display-hydration/79-02-SUMMARY.md` recording:
- New exports added (function signatures verbatim)
- Phase 78 dry-run regression-check result
- Plan 01 stub green counts (host-detect 7+1, strict-gate 6+1, others remain RED for Plans 03+04)
- Any deviations from spec per Rule 1 auto-fix policy
- Confirmation: zero `= ANY(` patterns, zero `process.exit` inside sql.begin callbacks
</output>
</content>
</invoke>