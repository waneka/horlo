---
phase: 79
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - scripts/v8.4-brand-canonicalization.ts
  - tests/unit/scripts/v8.4-family-build-decisions.test.ts
  - tests/unit/scripts/v8.4-strict-gate.test.ts
autonomous: true
requirements: [MIG-03]
tags: [migration, postgres, tsx-script, family-apply, aliases]

must_haves:
  truths:
    - "scripts/v8.4-brand-canonicalization.ts exports new family-side types and helpers: FamilyRow, FamilyDecisionRow, ResolvedFamily, FamilyDecisionMap, FamilyDecision, buildFamilyTableRows, buildFamilyMap, applyFamilyPath per D-79-05 + D-79-07 + MIG-03"
    - "--mode=families dry-run reads .planning/v8.4-brand-merge-decisions.md in-memory (D-79-07 Option 2), runs no DB writes (D-79-05 read-only invariant), emits .planning/v8.4-family-merge-decisions.md with GFM table columns brand | family_raw | normalized | proposed_target_id | status | candidates / notes"
    - "--mode=families dry-run inherits Phase 78 D-78-07 refuse-to-overwrite + --regenerate merge-forward gate for the new family artifact (symmetric with brand artifact)"
    - "Family dry-run candidate suggestions use word_similarity > 0.5 with public.f_unaccent diacritic folding per [[pg-trgm-word-similarity-for-brand-typos]] + Phase 78 R-FIND-02 search_path workaround"
    - "applyFamilyPath inside sql.begin executes (4.3) INSERT new watch_families with needs_review=false per D-79-09 RETURNING id to reify FamilyDecisionMap, (4.4) UPDATE watch_families SET aliases = aliases || ARRAY[<source_norm>] WHERE id = <target> AND NOT (aliases @> ARRAY[<source_norm>]) per D-79-06 idempotent alias append, (4.5) UPDATE watches_catalog SET family_id JOIN-scoped by brand_id + model_normalized per MIG-03"
    - "strictPreflightGate extended with family refuse cases: (a) family row needs-review, (b) family unknown status token, (c) family merge:<uuid> target missing in watch_families.id, (d) live catalog (brand, model) triple absent from family decisions file"
    - "Hamilton + Hamilton Watch (brand) raws collapse to ONE canonical brand_id in the BrandDecisionMap BEFORE family rows are emitted — the family file is deduped at emission time per D-79-07"
    - "Brut Date → Brut Datejust alias-append is exercised by unit fixtures (local seed lacks the case per Pitfall 8); the integration test stub stays it.todo for Plan 04 + Plan 05 live verification"
    - "Family dry-run integration regression: existing tests/integration/scripts/v8.4-readonly.test.ts (Phase 78) still passes — the family dry-run is read-only by construction (D-79-05)"
  artifacts:
    - path: scripts/v8.4-brand-canonicalization.ts
      provides: "FamilyRow + FamilyDecisionMap + buildFamilyTableRows + buildFamilyMap + applyFamilyPath + extended strictPreflightGate; --mode=families dry-run wired in main()"
      exports: [FamilyRow, FamilyDecisionRow, ResolvedFamily, FamilyDecisionMap, buildFamilyTableRows, buildFamilyMap]
    - path: tests/unit/scripts/v8.4-family-build-decisions.test.ts
      provides: "Greened from Plan 01 RED stub — all D-79-07 + D-79-06 it.todo callsites converted to it() and pass"
    - path: tests/unit/scripts/v8.4-strict-gate.test.ts
      provides: "Family-only it.todo callsites greened — full D-79-01 coverage (brand + family refuse cases + combined PASS case)"
  key_links:
    - from: "scripts/v8.4-brand-canonicalization.ts (--mode=families branch)"
      to: ".planning/v8.4-family-merge-decisions.md"
      via: "writeFile with buildFamilyTableRows output + refuse-to-overwrite gate"
      pattern: "v8\\.4-family-merge-decisions\\.md"
    - from: "scripts/v8.4-brand-canonicalization.ts (applyFamilyPath Step 4.4)"
      to: "watch_families.aliases column"
      via: "UPDATE watch_families SET aliases = aliases || ARRAY[$src]::text[] WHERE id = $tgt AND NOT (aliases @> ARRAY[$src]::text[])"
      pattern: "aliases = aliases \\|\\| ARRAY"
---

<objective>
Extend scripts/v8.4-brand-canonicalization.ts with the family-side surface: family dry-run path (`--mode=families`) writing `.planning/v8.4-family-merge-decisions.md`, family types + helpers (FamilyRow / FamilyDecisionMap / buildFamilyMap), extended strictPreflightGate (family refuse cases), and the applyFamilyPath inside a sql.begin transaction implementing MIG-03 (catalog family_id UPDATE + D-79-06 idempotent alias-append). Green the Plan 01 stubs for D-79-07 + D-79-06 family-build-decisions cases + the family refuse cases in strict-gate.

Purpose: MIG-03 — family backfill with operator-resolved typo cases routed into the canonical family's aliases array. Phase 80's INGEST-04 contract (alias containment lookup) depends on this plan populating aliases via D-79-06. The Brut Date → Brut Datejust canonical typo case is the alias-route end-to-end test (covered by fixtures because local seed lacks it per Pitfall 8).

Output: scripts/v8.4-brand-canonicalization.ts ~700 LOC (from Plan 02's ~530); 2 unit test files greened; the script can emit family-merge-decisions.md when run with `--mode=families`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 79 context (decisions + research + patterns)
@.planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md
@.planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md
@.planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md
@.planning/phases/79-backfill-migration-display-hydration/79-02-SUMMARY.md

# The file Plan 03 extends + the brand-side input the family dry-run consumes
@scripts/v8.4-brand-canonicalization.ts
@.planning/v8.4-brand-merge-decisions.md

# Schema reference (read-only — Plan 03 writes against this)
@src/db/schema.ts

# Phase 78 readonly invariant test (Plan 03 must NOT break this — family dry-run is read-only)
@tests/integration/scripts/v8.4-readonly.test.ts

# Wave 0 stubs Plan 03 greens
@tests/unit/scripts/v8.4-family-build-decisions.test.ts
@tests/unit/scripts/v8.4-strict-gate.test.ts

<interfaces>
<!-- Plan 02 exports — Plan 03 reuses without modification -->

From scripts/v8.4-brand-canonicalization.ts (Plan 02 shipped):
- export type ResolvedBrand = { kind: 'existing'|'merge'; uuid: string; canonicalName: string } | { kind: 'new'; syntheticKey: string; rawName: string }
- export type BrandDecisionMap = Map<string, ResolvedBrand>
- export interface BrandDecisionRow { brand_raw: string; brand_normalized: string; proposed_target_id: string | null; status: string }
- export function parseDecisionsTable(content: string): BrandDecisionRow[]
- export function buildBrandMap(rows: BrandDecisionRow[]): BrandDecisionMap
- export async function strictPreflightGate(sql, brandRows, existingBrandIdsFn): Promise<void>   // Plan 03 EXTENDS signature

<!-- Phase 79 NEW exports landed by Plan 03 -->

NEW interface — family-side analog of BrandRow:
- export interface FamilyRow { brand_raw: string; family_raw: string; family_normalized: string; proposed_target_id: string | null; candidates: Candidate[] }
- export interface FamilyDecisionRow extends FamilyRow { status: string }
- export type FamilyDecision = { kind: 'new'; brandUuid: string; name: string; compositeKey: string } | { kind: 'merge'; targetUuid: string; sourceModelRaw: string; compositeKey: string }

NEW types — family-side analog of BrandDecisionMap:
- export type ResolvedFamily =
  | { kind: 'existing'; uuid: string; canonicalName: string }
  | { kind: 'merge';    uuid: string; canonicalName: string; sourceModelRaw: string }
  | { kind: 'new';      syntheticKey: string; rawName: string; brandUuid: string }
- export type FamilyDecisionMap = Map<string, ResolvedFamily>           // key: composite `${brand_norm}|${model_norm}`

NEW pure helpers:
- export function buildFamilyRow(row: FamilyRow, status: string): string
- export function buildFamilyTableRows(rows: FamilyDecisionRow[]): string[]
- export function parseFamilyDecisionsTable(content: string): FamilyDecisionRow[]
- export function buildFamilyMap(rows: FamilyDecisionRow[], brandMap: BrandDecisionMap): FamilyDecisionMap
- export function parseCompositeKey(key: string): { brandNorm: string; modelNorm: string }

NEW DB-touching helpers (called from main() and from applyFamilyPath inside sql.begin):
- async function familyDryRun(sql, brandMap, args): Promise<void>     // emits family-merge-decisions.md
- async function applyFamilyPath(tx, familyMap, decisions): Promise<{ familiesCreated: number; aliasesAppended: number; catalogRowsResolvedFamily: number }>

NEW signature extension to strictPreflightGate per Plan 03 scope:
- export async function strictPreflightGate(sql, brandRows, familyRows, existingBrandIdsFn, existingFamilyIdsFn): Promise<void>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Family-side types + parsers + buildFamilyMap + green family-build-decisions stub</name>

  <files>scripts/v8.4-brand-canonicalization.ts, tests/unit/scripts/v8.4-family-build-decisions.test.ts</files>

  <read_first>
- scripts/v8.4-brand-canonicalization.ts (current state after Plan 02 — buildBrandMap + parseDecisionsTable shape)
- scripts/v8.4-brand-canonicalization.ts L114-148  (Phase 78 buildRow + buildTableRows — analog shape for family side)
- scripts/v8.4-brand-canonicalization.ts L155-207  (Phase 78 parseExistingPreserved + mergeForward — both reused unchanged for family side per PATTERNS L255)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L391-431  (Pattern 2 — ResolvedFamily shape + family map composite key form)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L237-296  (family builder pattern + reused parseExistingPreserved)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L551-572  (family-merge-decisions.md format — column delta from brand artifact = leading `brand` column)
- .planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md L44-49  (D-79-05 + D-79-06 + D-79-07)
- .planning/v8.4-brand-merge-decisions.md  (input to family dry-run; Hamilton merge collapse + 33 new brands inform what family rows appear)
- tests/unit/scripts/v8.4-seed021-golden.test.ts  (Phase 78 fixture pattern — Plan 03 family unit tests follow this shape)
- tests/unit/scripts/v8.4-family-build-decisions.test.ts  (Plan 01 RED stub — 5 it.todo callsites to green)
  </read_first>

  <behavior>
- parseFamilyDecisionsTable splits a GFM table with 6 cells per row (brand | family_raw | normalized | proposed_target_id | status | candidates) and returns FamilyDecisionRow[]
- buildFamilyMap takes FamilyDecisionRow[] + BrandDecisionMap. Composite key: `${lower(trim(row.brand_raw))}|${row.family_normalized}`. 'auto-resolved' → kind:'existing'. 'merge:<uuid>' → kind:'merge' with sourceModelRaw=row.family_raw. 'new' → kind:'new' with synthetic_key=compositeKey + brandUuid from brandMap entry (use uuid for 'existing'/'merge'; pass through syntheticKey for 'new' brand entries — reified at apply time post-Step-4.1).
- D-79-06: alias-idempotency is at the SQL layer; buildFamilyMap preserves every operator decision verbatim.
- Hamilton + Hamilton Watch BRAND merge collapse means: the dry-run query joining catalog through brandMap produces ONE Khaki Field family row (not two) because both brand_raws resolve to the same canonical brand_id.
  </behavior>

  <action>
In scripts/v8.4-brand-canonicalization.ts (continuing Plan 02's exports):

  (a) Add NEW exported types:
     - `export interface FamilyRow { brand_raw: string; family_raw: string; family_normalized: string; proposed_target_id: string | null; candidates: Candidate[] }`
     - `export interface FamilyDecisionRow extends FamilyRow { status: string }`
     - `export type ResolvedFamily = { kind: 'existing'; uuid: string; canonicalName: string } | { kind: 'merge'; uuid: string; canonicalName: string; sourceModelRaw: string } | { kind: 'new'; syntheticKey: string; rawName: string; brandUuid: string }`
     - `export type FamilyDecisionMap = Map<string, ResolvedFamily>`
     - `export type FamilyDecision = { kind: 'new'; brandUuid: string; name: string; compositeKey: string } | { kind: 'merge'; targetUuid: string; sourceModelRaw: string; compositeKey: string }`

  (b) Add NEW exported `buildFamilyRow(row, status): string`. 6 cells: brand_raw, family_raw, family_normalized, proposed_target_id || '', status, candidates.map(c => `${formatCell(c.name)} (${c.score.toFixed(2)})`).join(', '). Reuse `formatCell` from Phase 78.

  (c) Add NEW exported `buildFamilyTableRows(rows: FamilyDecisionRow[]): string[]`. Mirror buildTableRows L138-148. Header: `| brand | family_raw | normalized | proposed_target_id | status | candidates / notes |`. Separator: 6-cell GFM separator.

  (d) Add NEW exported `parseFamilyDecisionsTable(content: string): FamilyDecisionRow[]`. Mirror parseDecisionsTable shape. 6 cells per data line; cells[1]=brand_raw, cells[2]=family_raw, cells[3]=family_normalized, cells[4]=proposed_target_id (empty → null), cells[5]=status, cells[6]=candidates (parse into Candidate[] via regex `/(.+?) \((\d\.\d+)\)/g`).

  (e) Add NEW exported `parseCompositeKey(key: string): { brandNorm: string; modelNorm: string }`. Split on first `|`; return both halves.

  (f) Add NEW exported `buildFamilyMap(rows: FamilyDecisionRow[], brandMap: BrandDecisionMap): FamilyDecisionMap`:
     - For each row, compositeKey = `${lower(trim(row.brand_raw))}|${row.family_normalized}`
     - Per row.status: 'auto-resolved' → kind:'existing'; 'merge:<uuid>' → kind:'merge' with sourceModelRaw=row.family_raw; 'new' → kind:'new' with brandUuid (from brandMap.get(lower(trim(row.brand_raw))).uuid OR syntheticKey if the brand is still kind:'new' — the apply path reifies later)
     - Validate status grammar inline (throw on 'skip' or unknown).

In tests/unit/scripts/v8.4-family-build-decisions.test.ts:

  Replace all `it.todo(...)` calls for D-79-07 + D-79-06 with `it(...)` assertions. Uncomment imports. Fixtures:
  - Hamilton + Hamilton Watch (brand) → both auto-resolve to same brand_id in BrandDecisionMap → Khaki Field (family) appears ONCE in buildFamilyMap output (no duplicate from the merge-source brand path)
  - merge:<datejust-uuid> family decision → kind:'merge' entry with sourceModelRaw='Brut Date' in the map; buildFamilyTableRows applied twice on same fixture produces identical output rows
  - new brand placeholder family row → kind:'new' in the family map; assert the family row carries the syntheticKey forward without throwing.
  </action>

  <verify>
    <automated>npx vitest run tests/unit/scripts/v8.4-family-build-decisions.test.ts && grep -c "export function buildFamilyMap\|export function buildFamilyTableRows\|export function parseFamilyDecisionsTable\|export function parseCompositeKey" scripts/v8.4-brand-canonicalization.ts</automated>
  </verify>

  <done>
    - scripts/v8.4-brand-canonicalization.ts exports the 5 new family-side functions + 5 new types/interfaces.
    - tests/unit/scripts/v8.4-family-build-decisions.test.ts: all D-79-07 + D-79-06 it.todo callsites converted to it() and pass.
    - grep returns 4 for the 4 NEW exported functions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: --mode=families dry-run path + family refuse cases in strictPreflightGate + green family strict-gate stub</name>

  <files>scripts/v8.4-brand-canonicalization.ts, tests/unit/scripts/v8.4-strict-gate.test.ts</files>

  <read_first>
- scripts/v8.4-brand-canonicalization.ts L225-335  (current main() after Plan 02 — dispatch point for --mode=families)
- scripts/v8.4-brand-canonicalization.ts L260-308  (Phase 78 Stage 2/3 — distinct catalog brands + per-row word_similarity loop; Plan 03 mirrors for families)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L159-167  (system diagram — family dry-run path)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L867-935  (strictPreflightGate full impl; extend to family side per CONTEXT D-79-01 case d-family)
- .planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md L44-49  (D-79-05 + D-79-07 verbatim)
- .planning/v8.4-brand-merge-decisions.md  (current operator-edited input; the dry-run resolves brand_raw → canonical via this file in-memory)
- src/db/schema.ts L539-563  (watch_families schema)
- tests/unit/scripts/v8.4-strict-gate.test.ts  (Plan 01 RED stub — family-only it.todo callsites)
- tests/integration/scripts/v8.4-readonly.test.ts  (Phase 78 D-78-05 invariant — Plan 03 must not break)
  </read_first>

  <action>
In scripts/v8.4-brand-canonicalization.ts (continuing Task 1):

  (a) Define `FAMILY_OUTPUT_FILE = path.join(process.cwd(), '.planning/v8.4-family-merge-decisions.md')` near the existing OUTPUT_FILE constant.

  (b) Define `GFM_FAMILY_HEADER` and `GFM_FAMILY_SEPARATOR` constants (6 cells each).

  (c) Extend `strictPreflightGate` signature: add `familyRows: FamilyDecisionRow[]` and `existingFamilyIdsFn: (uuids: string[]) => Promise<Set<string>>` parameters. Repeat the 4 refuse cases for familyRows:
     - needs-review status → throw with /family.*needs-review/i message
     - unknown token → throw with /family.*unknown.*status/i
     - merge:<uuid> family target not in watch_families.id → throw with /family.*merge.*not found/i
     - live catalog (brand_norm, model_norm) triple absent from familyRows → throw with /catalog has.*(brand,.*model)|family/i. Query: `sql\`SELECT DISTINCT lower(trim(c.brand)) AS brand_norm, lower(trim(c.model)) AS model_norm FROM watches_catalog c\``; build decidedTriples = new Set(familyRows.map(r => \`${lower(trim(r.brand_raw))}|${r.family_normalized}\`)).

  (d) Add NEW non-exported `async function familyDryRun(sql, brandMap, args): Promise<void>`:
     - Refuse-to-overwrite gate on FAMILY_OUTPUT_FILE (mirror Phase 78 L228-237; same `--regenerate` / `--force` flags).
     - Stage 1: SELECT DISTINCT (c.brand, c.model, c.brand_normalized, c.model_normalized, wf.id AS proposed_target_id) FROM watches_catalog c LEFT JOIN brands b ON b.name_normalized = c.brand_normalized LEFT JOIN watch_families wf ON wf.brand_id = b.id AND wf.name_normalized = c.model_normalized ORDER BY c.brand_normalized, c.model_normalized. Emit one FamilyRow per (brand_raw, family_raw). The brand-side dedup is implicit: catalog brand_raws that collapse to the same canonical brand_id via brandMap produce ONE family row per (canonical_brand_uuid, model_normalized).
     - Stage 2: For unresolved family rows, per-row word_similarity > 0.5 lookup against `watch_families.name_normalized` SCOPED by canonical brand_id (use brandMap to find canonical brand_id for the row's brand_raw; query watch_families for that brand_id only). Use public.f_unaccent per [[pg-trgm-word-similarity-for-brand-typos]]; preserve SET search_path workaround (already set at connection time per Plan 02).
     - Stage 3: Compose artifact via buildFamilyTableRows OR mergeForward when --regenerate. Reuse parseExistingPreserved unchanged.
     - Stage 4: writeFile FAMILY_OUTPUT_FILE with header (mirror Phase 78 buildHeader; cite --mode=families and --apply --mode=both consumption) + tableLines.join('\n') + '\n'.
     - console.log summary: total rows, auto-resolved count, needs-review count.
     - NO sql write — verified by Phase 78 v8.4-readonly.test.ts.

  (e) Wire main() dispatch for `--mode=families` (without --apply): read brand decisions file via parseDecisionsTable; build brandMap; call familyDryRun(sql, brandMap, args). If brand file missing, throw clear error pointing operator to run `--mode=brands` dry-run first.

  (f) `--mode=families --apply` continues to throw the Plan 02 stub error — actual wiring lands in Plan 04 as part of `--mode=both --apply`.

In tests/unit/scripts/v8.4-strict-gate.test.ts:

  Replace remaining family-only it.todo calls per Plan 01 spec:
  - case (c)-family: merge:<uuid> family target not in watch_families.id — inject existingFamilyIdsFn returning empty Set; assert throws /family.*merge.*not found/i
  - case (d)-family: live catalog triple absent from family decisions file — mock sql to return a triple not in fixtures; assert throws /catalog has|family/i
  - combined PASS case: all brand + family fixtures valid; both existence-check fns return matching Sets; assert resolves to void
  </action>

  <verify>
    <automated>npx vitest run tests/unit/scripts/v8.4-strict-gate.test.ts tests/unit/scripts/v8.4-family-build-decisions.test.ts && npx vitest run tests/integration/scripts/v8.4-readonly.test.ts && grep -c "FAMILY_OUTPUT_FILE\|familyDryRun" scripts/v8.4-brand-canonicalization.ts</automated>
  </verify>

  <done>
    - Plan 01 stubs for D-79-01 family cases green (3 family it.todo → it() converted).
    - Phase 78 readonly integration test still passes — family dry-run is read-only.
    - strictPreflightGate signature extended; all callers updated.
    - familyDryRun emits .planning/v8.4-family-merge-decisions.md when invoked with `--mode=families` (manually verifiable).
    - grep returns ≥2 for FAMILY_OUTPUT_FILE + familyDryRun.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: applyFamilyPath (Steps 4.3 + 4.4 + 4.5) inside sql.begin — INSERT new families + idempotent alias-append + UPDATE catalog family_id</name>

  <files>scripts/v8.4-brand-canonicalization.ts</files>

  <read_first>
- scripts/v8.4-brand-canonicalization.ts (current state after Plan 02 + Plan 03 Task 1+2 — applyBrandPath shape to mirror)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L727-770  (Code Examples — applyFamilyPath Steps 4.3 + 4.4 + 4.5 full implementation)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L128-156  (idempotent alias-append SQL)
- .planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md L44-49  (D-79-06 alias auto-append from merge decisions)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L640-650  (Pitfall 3 — reify family map after Step 4.3 before Step 4.5)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L646-651  (Pitfall 4 — family resolution requires brand_id already populated)
- src/db/schema.ts L539-563  (watch_families.aliases column shape; GIN index on aliases shipped Phase 78)
- src/db/schema.ts L431-510  (watches_catalog: brand_normalized GENERATED, model_normalized GENERATED, family_id FK)
  </read_first>

  <action>
In scripts/v8.4-brand-canonicalization.ts:

  (a) Add NEW non-exported `async function applyFamilyPath(tx, familyMap, decisions): Promise<{ familiesCreated: number; aliasesAppended: number; catalogRowsResolvedFamily: number }>` per RESEARCH L727-770:
     - Step 4.3: for each entry in familyMap where kind === 'new', `INSERT INTO watch_families (brand_id, name, needs_review, aliases) VALUES (${brandUuid}, ${rawName}, false, '{}'::text[]) RETURNING id`. Set map entry to `{kind:'existing', uuid: row.id, canonicalName: rawName}`. Increment familiesCreated. needs_review=false per D-79-09.
     - Invariant check (Pitfall 3): `for (const v of familyMap.values()) if (v.kind === 'new') throw new Error('familyMap not reified after Step 4.3')`.
     - Step 4.4: iterate familyMap entries; for those with kind:'merge', extract sourceModelRaw and uuid. `UPDATE watch_families SET aliases = aliases || ARRAY[${sourceNorm}]::text[] WHERE id = ${targetUuid} AND NOT (aliases @> ARRAY[${sourceNorm}]::text[])` per PATTERNS L131-140. sourceNorm = sourceModelRaw.toLowerCase().trim(). Accumulate result.count → aliasesAppended.
     - Step 4.5: for each [compositeKey, resolved] of familyMap.entries(), parseCompositeKey(compositeKey) → {brandNorm, modelNorm}. `UPDATE watches_catalog c SET family_id = ${resolved.uuid} FROM brands b WHERE c.brand_id = b.id AND b.name_normalized = ${brandNorm} AND c.model_normalized = ${modelNorm} AND (c.family_id IS NULL OR c.family_id <> ${resolved.uuid})`. Accumulate result.count → catalogRowsResolvedFamily.
     - Return {familiesCreated, aliasesAppended, catalogRowsResolvedFamily}.

  (b) Do NOT wire applyFamilyPath into main() yet — Plan 04's job (single atomic transaction containing applyBrandPath + applyFamilyPath + hydration + post-flight). Plan 03 ships the function definition; Plan 04 calls it.

  (c) Add doc comment block at top of applyFamilyPath citing D-79-06 + D-79-09 + Pitfall 3 + Pitfall 4 + PATTERNS L131-140 source.

NO unit tests for applyFamilyPath directly — exercised by Plan 01 integration stub which lights up in Plan 04+05.

Mitigates T-79-04 (alias-append non-idempotency on rerun — UPDATE predicate `NOT (aliases @> ARRAY[$src])` is the SQL-layer idempotency gate).

NO `= ANY(${arr})` ANYWHERE per [[drizzle-sql-any-array-pitfall]]. NO `process.exit` inside the eventual sql.begin callback per Pitfall 2.
  </action>

  <verify>
    <automated>npx vitest run tests/unit/scripts/ && npm run build && grep -cE "applyFamilyPath|aliases = aliases \\|\\| ARRAY" scripts/v8.4-brand-canonicalization.ts && (test "$(grep -c '= ANY(' scripts/v8.4-brand-canonicalization.ts)" = "0" || (echo "FAIL: = ANY(arr) pattern present"; exit 1))</automated>
  </verify>

  <done>
    - applyFamilyPath function exists with 3-step shape (INSERT new families → reify map → invariant → UPDATE aliases idempotent → UPDATE catalog family_id loop).
    - All Plan 01/02/03 unit tests green.
    - npm run build exits 0.
    - grep returns ≥2 for applyFamilyPath + alias-append SQL pattern.
    - Zero `= ANY(` patterns in script per forward armor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-edited family markdown → script parser | tampering surface (same shape as brand artifact); mitigated by strict gate family-side extension |
| script → Postgres (service-role) | privileged write to watch_families.aliases (new column class); mitigated by D-79-06 SQL-layer idempotency + atomic transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03 | Tampering | strictPreflightGate family-side extension (Task 2) | mitigate | Family refuse cases mirror brand refuse cases: needs-review / unknown token / merge:<uuid> missing in watch_families.id / catalog triple absent from decisions file |
| T-79-04 | Integrity | applyFamilyPath Step 4.4 alias-append (Task 3) | mitigate | UPDATE predicate `NOT (aliases @> ARRAY[$src])` is the SQL-layer idempotency gate; combined with Plan 02's idempotentReRunGate, double-apply is safe even if the gate fails |
| T-79-01 | Integrity | applyFamilyPath inside future sql.begin (Task 3) | mitigate | Throwing inside applyFamilyPath (e.g. invariant fail after Step 4.3 reify, FK violation on INSERT) auto-triggers ROLLBACK once Plan 04 wraps it in sql.begin; partial state cannot persist |
| T-INJ-01 | Tampering (SQL injection) | parseFamilyDecisionsTable + applyFamilyPath (Tasks 1+3) | mitigate | All template-literal interpolations bind as postgres-lib parameters; family_raw / merge UUIDs never concatenate into raw SQL |
</threat_model>

<verification>
- All Task 1+2 unit tests green: v8.4-family-build-decisions.test.ts (5 D-79-07/06 + sanity), v8.4-strict-gate.test.ts (full coverage incl. family + combined PASS).
- Phase 78 readonly integration test still passes — family dry-run is read-only by construction (D-79-05).
- scripts/v8.4-brand-canonicalization.ts builds clean: `npm run build` exits 0.
- ZERO `= ANY(` patterns per `[[drizzle-sql-any-array-pitfall]]` forward armor.
- ZERO `process.exit` inside any sql.begin callback per Pitfall 2.
- ROADMAP MIG-03 success criterion #2 satisfied at function-definition level: `applyFamilyPath` exists with the 3-step shape; full end-to-end verification happens in Plan 04 + Plan 05.
</verification>

<success_criteria>
- scripts/v8.4-brand-canonicalization.ts ~700 LOC (from Plan 02's ~530), all new family-side exports present and named exactly per interfaces block.
- `--mode=families` dry-run end-to-end verifiable locally: `npm run db:v8.4-brand-canon -- --mode=families` reads the committed brand-merge-decisions.md, queries the local catalog, writes .planning/v8.4-family-merge-decisions.md with the expected GFM table.
- The refuse-to-overwrite + --regenerate symmetry holds for the family artifact (operator can edit, regenerate without losing prior edits).
- Plan 01 stubs for D-79-07 + D-79-06 (family-build-decisions) and D-79-01 family cases (strict-gate) green.
- applyFamilyPath function defined and ready for Plan 04 to wire into a single atomic transaction.
</success_criteria>

<output>
After completion, create `.planning/phases/79-backfill-migration-display-hydration/79-03-SUMMARY.md` recording:
- New family-side exports (function signatures verbatim)
- Phase 78 readonly invariant regression-check result
- Family dry-run smoke test result (artifact emitted, row count, auto-resolved vs needs-review breakdown)
- Plan 01 stub green counts
- Any deviations from spec per Rule 1 auto-fix policy
- Confirmation: zero `= ANY(` patterns; zero `process.exit` inside sql.begin
</output>
</content>
</invoke>