---
phase: 79
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/scripts/v8.4-host-detect.test.ts
  - tests/unit/scripts/v8.4-strict-gate.test.ts
  - tests/unit/scripts/v8.4-family-build-decisions.test.ts
  - tests/unit/scripts/v8.4-post-deploy-template.test.ts
  - tests/integration/scripts/v8.4-apply-atomic.test.ts
  - tests/integration/scripts/v8.4-apply-idempotent.test.ts
  - .planning/phases/79-backfill-migration-display-hydration/79-VALIDATION.md
autonomous: true
requirements: [MIG-02, MIG-03, MIG-04, DISP-03]
tags: [migration, tdd, wave-0, postgres, tsx-script]

must_haves:
  truths:
    - "Six Wave 0 RED stub files exist under tests/unit/scripts/ and tests/integration/scripts/ covering every Phase 79 testable behavior (D-79-01, D-79-02, D-79-03, D-79-04, D-79-06, D-79-07, D-79-08, D-79-09, D-79-10, MIG-02, MIG-03, MIG-04, DISP-03)"
    - "Each stub follows Phase 78 Wave 0 convention: first-line marker `// Phase 79 / 79-01-PLAN.md — Wave 0 RED stub.`, sanity `it()` returning positive discovery signal, and `it.todo()` callsites citing the decision/requirement ID in the assertion string"
    - "DATABASE_URL-gated integration suites use `const maybe = process.env.DATABASE_URL ? describe : describe.skip` per Phase 78 convention so vitest reports `↓ skipped` (not failed) when env unset"
    - "`npm run test -- tests/unit/scripts/ tests/integration/scripts/` exits 0 with at least 6 NEW test files (4 unit + 2 integration) discovered by vitest"
    - "`npm run build` exits 0 (Wave 0 stubs do not break the prebuild static guards)"
    - "79-VALIDATION.md Per-Task Verification Map populated with one row per requirement + decision the Wave 0 stubs cover, and frontmatter `wave_0_complete: true`, `nyquist_compliant: true`"
  artifacts:
    - path: tests/unit/scripts/v8.4-host-detect.test.ts
      provides: "Stub for D-79-02 isLocalDatabaseUrl: covers 127.0.0.1:54322 → true, localhost:54322 → true, prod pooler → false, alt-port → false, unparseable → false, empty → false"
    - path: tests/unit/scripts/v8.4-strict-gate.test.ts
      provides: "Stub for D-79-01 strictPreflightGate: 4 refuse cases (needs-review, unknown token, merge:<uuid> target missing, catalog drift) + 1 pass case"
    - path: tests/unit/scripts/v8.4-family-build-decisions.test.ts
      provides: "Stub for D-79-07 in-memory brand-decision map → family-row generation + D-79-06 alias idempotency (fixture-based, no DB)"
    - path: tests/unit/scripts/v8.4-post-deploy-template.test.ts
      provides: "Stub for D-79-10 renderPostDeployMarkdown template shape + D-79-08 unconditional hydration grep + MIG-04 predicate divergence grep"
    - path: tests/integration/scripts/v8.4-apply-atomic.test.ts
      provides: "DATABASE_URL-gated stub covering MIG-02 (brand_id resolved) + MIG-03 (family_id resolved + aliases appended) + MIG-04 (post-flight assertion + forced-fail rollback) + DISP-03 (hydration overwrites brand+model, preserves notes/serial) + D-79-03 (atomic rollback) + D-79-09 (new rows needs_review=false) + D-79-10 (POST-DEPLOY written)"
    - path: tests/integration/scripts/v8.4-apply-idempotent.test.ts
      provides: "DATABASE_URL-gated stub for D-79-04 already-applied gate (second --apply --mode=both prints `Already applied — nothing to do.` and exits 0)"
    - path: .planning/phases/79-backfill-migration-display-hydration/79-VALIDATION.md
      provides: "Per-Task Verification Map filled in; wave_0_complete: true; nyquist_compliant: true"
      contains: "wave_0_complete: true"
  key_links:
    - from: "tests/integration/scripts/v8.4-apply-atomic.test.ts"
      to: "scripts/v8.4-brand-canonicalization.ts"
      via: "spawnSync('npx', ['tsx', SCRIPT_PATH, '--apply', '--mode=both'])"
      pattern: "spawnSync.*v8\\.4-brand-canonicalization"
    - from: "tests/unit/scripts/v8.4-host-detect.test.ts"
      to: "scripts/v8.4-brand-canonicalization.ts"
      via: "import { isLocalDatabaseUrl } — commented-import escape hatch until Plan 02 ships the export"
      pattern: "isLocalDatabaseUrl"
---

<objective>
Create six Wave 0 RED stub test files covering every Phase 79 testable behavior (six requirements + ten decisions). Stubs follow the Phase 78 convention: first-line marker, sanity `it()` for positive discovery, `it.todo()` per assertion citing the D-79-NN / REQ-ID. DATABASE_URL-gated integration suites use `describe.skip` when env unset. Update 79-VALIDATION.md frontmatter to mark Wave 0 complete.

Purpose: Establish the RED side of TDD for Phase 79 before any production code lands. The downstream plans (02/03/04) un-`todo` these tests as each behavior ships. Without this scaffold, Phase 79's executors would write production code without a feedback loop and ship bugs into prod's high-blast-radius write phase.

Output: 6 new test files + 1 updated VALIDATION.md. No `scripts/v8.4-brand-canonicalization.ts` edits in this plan (downstream plans own that).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 79 context (decisions + research + patterns)
@.planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md
@.planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md
@.planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md
@.planning/phases/79-backfill-migration-display-hydration/79-VALIDATION.md

# Wave 0 convention precedent (Phase 78 stubs)
@tests/integration/scripts/v8.4-readonly.test.ts
@tests/integration/scripts/v8.4-brand-canonicalization.test.ts
@tests/unit/scripts/v8.4-md-artifact-schema.test.ts
@tests/unit/scripts/v8.4-regenerate-merge.test.ts
@tests/unit/scripts/v8.4-seed021-golden.test.ts

# Script the stubs target (read-only in this plan)
@scripts/v8.4-brand-canonicalization.ts

<interfaces>
<!-- Phase 78 already-exported types/functions the stubs reference (commented imports allowed until Plan 02 adds Phase 79 exports). -->

From scripts/v8.4-brand-canonicalization.ts:
- export interface BrandRow { brand_raw: string; brand_normalized: string; proposed_target_id: string | null }
- export interface Candidate { name: string; score: number }
- export interface ParsedArgs { regenerate: boolean; force: boolean }
- export function parseArgs(argv?: string[]): ParsedArgs
- export function formatCell(value: string | null | undefined): string
- export function buildRow(row: BrandRow, candidates: Candidate[]): string
- export function buildTableRows(rows: BrandRow[], candidatesByNormalized: Map<string, Candidate[]>): string[]
- export function parseExistingPreserved(content: string): Map<string, string>
- export function mergeForward(existingContent: string, freshRows: BrandRow[], candidatesByNormalized: Map<string, Candidate[]>): string[]

NEW exports added by Plan 02/03/04 (stubs reference these as commented imports
or `// TODO Plan 02: import ...`):
- export function isLocalDatabaseUrl(connStr: string): boolean                       // Plan 02 / D-79-02
- export type ResolvedBrand = { kind: 'existing'|'merge'; uuid: string; canonicalName: string } | { kind: 'new'; syntheticKey: string; rawName: string }
- export type BrandDecisionMap = Map<string, ResolvedBrand>                          // Plan 02 / D-79-07
- export interface FamilyRow { brand_raw: string; family_raw: string; family_normalized: string; proposed_target_id: string | null }  // Plan 03
- export type ResolvedFamily = { kind: 'existing'|'merge'; uuid: string; canonicalName: string; sourceModelRaw?: string } | { kind: 'new'; syntheticKey: string; rawName: string; brandUuid: string }
- export type FamilyDecisionMap = Map<string, ResolvedFamily>                        // Plan 03 / D-79-07
- export function buildBrandMap(rows: BrandRow[]): BrandDecisionMap                  // Plan 02
- export function buildFamilyTableRows(rows: FamilyRow[], candidatesByNormalized: Map<string, Candidate[]>): string[]  // Plan 03
- export function renderPostDeployMarkdown(args: {...}): string                      // Plan 04 / D-79-10
- export async function strictPreflightGate(sql, brandFile, familyFile, existsCheck): Promise<{ brandMap, familyMap, summary }>  // Plan 02/03 / D-79-01
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Unit stub files (host-detect, strict-gate, family-build-decisions, post-deploy-template)</name>

  <files>tests/unit/scripts/v8.4-host-detect.test.ts, tests/unit/scripts/v8.4-strict-gate.test.ts, tests/unit/scripts/v8.4-family-build-decisions.test.ts, tests/unit/scripts/v8.4-post-deploy-template.test.ts</files>

  <read_first>
- tests/unit/scripts/v8.4-md-artifact-schema.test.ts  (Phase 78 unit stub convention; pure-function in/out idiom)
- tests/unit/scripts/v8.4-regenerate-merge.test.ts  (Phase 78 merge-forward unit; closest analog to v8.4-strict-gate.test.ts)
- tests/unit/scripts/v8.4-seed021-golden.test.ts  (Phase 78 fixture-based pattern; closest analog to v8.4-family-build-decisions.test.ts)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L299-414  (analog assignments for each of the 4 unit stubs)
- .planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md L30-69  (decisions D-79-01..10 — cite in `it.todo` strings)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L1191-1213  (Per-Task Verification Map — what each unit test must cover)
- scripts/v8.4-brand-canonicalization.ts L1-100  (existing exports; stubs commented-import the Plan 02/03/04 NEW exports)
  </read_first>

  <action>
Create 4 unit stub files under tests/unit/scripts/. Each file:

  (a) First-line marker: `// Phase 79 / 79-01-PLAN.md — Wave 0 RED stub.`
  (b) Multi-line block comment describing what the stub gates (cite D-79-NN + REQ-ID).
  (c) Imports: `import { describe, it, expect } from 'vitest'`. NEW Plan 02/03/04 exports referenced as commented-out imports with `// TODO Plan NN: uncomment when exports land` markers (per Phase 77 commented-import escape hatch convention).
  (d) One sanity `it('Wave 0 RED stub loads', () => { expect(true).toBe(true) })` for positive vitest discovery (per Phase 78 D-78-08 convention).
  (e) `it.todo(...)` callsites per behavior; assertion string MUST cite the D-79-NN or REQ-ID being gated (per Phase 78 decision-ID citation pattern).

File contents per spec (use these exact `it.todo` strings; planner pre-allocates the per-decision coverage):

  1. `tests/unit/scripts/v8.4-host-detect.test.ts` (D-79-02; analog 79-PATTERNS.md L375-403):
     - `describe('Phase 79 — v8.4 isLocalDatabaseUrl (D-79-02)', () => {`
     - `it('Wave 0 RED stub loads', ...)` — sanity
     - `it.todo('D-79-02: returns true for postgres://...@127.0.0.1:54322/postgres')`
     - `it.todo('D-79-02: returns true for postgres://...@localhost:54322/postgres')`
     - `it.todo('D-79-02: returns false for Supabase pooler aws-1.pooler.supabase.com:6543')`
     - `it.todo('D-79-02: returns false for Supabase pooler aws-0.pooler.supabase.com:5432')`
     - `it.todo('D-79-02: returns false for 127.0.0.1:54323 alt-port (safety bias)')`
     - `it.todo('D-79-02: returns false for unparseable connection string (fail closed)')`
     - `it.todo('D-79-02: returns false for empty string (fail closed)')`

  2. `tests/unit/scripts/v8.4-strict-gate.test.ts` (D-79-01; analog 79-PATTERNS.md L343-371):
     - `describe('Phase 79 — v8.4 strict pre-flight gate (D-79-01)', () => {`
     - `it('Wave 0 RED stub loads', ...)` — sanity
     - `it.todo('D-79-01: refuses on row with status=needs-review')`
     - `it.todo('D-79-01: refuses on row with unknown status token (per D-78-02 grammar)')`
     - `it.todo('D-79-01: refuses on merge:<uuid> target not found in brands table (injected existence check returns empty)')`
     - `it.todo('D-79-01: refuses on merge:<uuid> target not found in watch_families table')`
     - `it.todo('D-79-01: refuses on live catalog brand absent from decisions file')`
     - `it.todo('D-79-01: refuses on live (brand, model) triple absent from family decisions file (MIG-03 + DISP-03 coverage)')`
     - `it.todo('D-79-01: passes when all rows terminal + all merge UUIDs valid + no catalog drift; returns { brandMap, familyMap, summary } (MIG-02 + MIG-03 coverage)')`

  3. `tests/unit/scripts/v8.4-family-build-decisions.test.ts` (D-79-07 + D-79-06; analog tests/unit/scripts/v8.4-seed021-golden.test.ts):
     - `describe('Phase 79 — v8.4 family-build-decisions (D-79-07 + D-79-06)', () => {`
     - `it('Wave 0 RED stub loads', ...)` — sanity
     - `it.todo('D-79-07: Hamilton + Hamilton Watch raws collapse to ONE canonical brand id in the in-memory map BEFORE family rows are emitted (no DB write)')`
     - `it.todo('D-79-07: merge:<uuid> brand decision does NOT leak duplicate family rows for the merged-source brand')`
     - `it.todo('D-79-07: new brand placeholder synthetic_key propagates as the family row scope key so operator can still resolve the family row by name')`
     - `it.todo('D-79-06: alias idempotent — buildFamilyTableRows applied twice on the same merge: decision produces the same emitted row (no duplicate alias in the output)')`
     - `it.todo('D-79-06 + MIG-03: Brut Date → Brut Datejust merge decision routes the source model_raw into the canonical family row\\'s alias column (fixture-only; no live DB needed)')`

  4. `tests/unit/scripts/v8.4-post-deploy-template.test.ts` (D-79-10 + D-79-08 + MIG-04; per 79-PATTERNS.md L408-413):
     - `describe('Phase 79 — v8.4 renderPostDeployMarkdown (D-79-10 + D-79-08 + MIG-04)', () => {`
     - `it('Wave 0 RED stub loads', ...)` — sanity
     - `it.todo('D-79-10: contains ## Apply Summary section with GFM count table substituted from args.counts')`
     - `it.todo('D-79-10: contains ## Post-Flight Assertion (MIG-04) section with the assertion SQL + results substituted')`
     - `it.todo('D-79-10: contains ## Operator Sign-Off Queries section with the 6 verification SQL blocks (Hamilton merge check #2 must reference brand_id = \\'20969364-...\\')')`
     - `it.todo('D-79-10: contains ## What this push does NOT do forward-armor section listing Phase 80/81/82 deferred work')`
     - `it.todo('MIG-04: source-grep — scripts/v8.4-brand-canonicalization.ts contains exactly one `IS DISTINCT FROM NULL` usage and zero `IS NULL` in the same SELECT block (predicate divergence per [[post-flight-assertion-predicate-divergence]])')`
     - `it.todo('D-79-08: source-grep — the hydration UPDATE has no WHERE clause filtering on watches.brand/model text (JOIN-only filter; unconditional per D-79-08)')`
     - `it.todo('D-79-09: source-grep — both INSERT INTO brands and INSERT INTO watch_families include needs_review = false / needs_review, false in the same statement (new rows default to false per D-79-09)')`

For ALL 4 unit files, include the Phase 78 `it('Wave 0 RED stub loads', ...)` sanity test as the FIRST `it` callsite so vitest reports `1 passed` per file even when every other test is `it.todo`. Per Phase 77 / Phase 78 commented-import escape-hatch convention, comment out the Plan 02/03/04 NEW exports with a `// TODO Plan NN: uncomment when exports land` marker.

Mitigates threats: none directly (test scaffolding); enables T-79-01/02/03/04/05 mitigation verification in downstream plans.
  </action>

  <verify>
    <automated>npx vitest run tests/unit/scripts/v8.4-host-detect.test.ts tests/unit/scripts/v8.4-strict-gate.test.ts tests/unit/scripts/v8.4-family-build-decisions.test.ts tests/unit/scripts/v8.4-post-deploy-template.test.ts</automated>
  </verify>

  <done>
    - 4 unit test files exist with correct first-line marker and decision-ID citations.
    - vitest reports `4 passed | N todo | 0 failed` across the 4 files (sanity it() passes; it.todo callsites reported as todo).
    - Every D-79-NN listed in the spec appears in at least one `it.todo` assertion string.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integration stub files (v8.4-apply-atomic, v8.4-apply-idempotent)</name>

  <files>tests/integration/scripts/v8.4-apply-atomic.test.ts, tests/integration/scripts/v8.4-apply-idempotent.test.ts</files>

  <read_first>
- tests/integration/scripts/v8.4-readonly.test.ts  (Phase 78 integration analog — DATABASE_URL gate, snapshot pattern, spawnSync, backup/restore decisions file)
- tests/integration/scripts/v8.4-brand-canonicalization.test.ts  (Phase 78 spawn-script integration analog)
- tests/integration/migrations/78-gin-index.test.ts  (DATABASE_URL-gated integration test idiom for migrations tier)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L416-507  (Phase 79 integration stub spec — snapshot helper deltas, backup/restore for BOTH brand + family decision files)
- .planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md L30-69  (decisions D-79-03, D-79-04, D-79-09, D-79-10 → integration coverage)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L1191-1213  (Per-Task Verification Map — which integration assertions cover which REQ-ID/D-79-NN)
- .planning/v8.4-brand-merge-decisions.md  (committed operator-edited fixture used by integration tests)
  </read_first>

  <action>
Create 2 integration stub files under tests/integration/scripts/. Each file:

  (a) First-line marker: `// Phase 79 / 79-01-PLAN.md — Wave 0 RED stub.`
  (b) Multi-line block comment describing what the stub gates (cite D-79-NN + REQ-ID).
  (c) DATABASE_URL gate per Phase 78 convention: `const maybe = process.env.DATABASE_URL ? describe : describe.skip` — wraps the suite so vitest reports `↓ skipped` (not failed) when env unset.
  (d) One sanity `it('Wave 0 RED stub loads', () => { expect(true).toBe(true) })` OUTSIDE the maybe block (positive discovery regardless of env).
  (e) `it.todo(...)` callsites per behavior INSIDE the `maybe('...', () => {})` block; cite the REQ-ID/D-79-NN in each assertion string.

File contents per spec:

  1. `tests/integration/scripts/v8.4-apply-atomic.test.ts` (MIG-02 + MIG-03 + MIG-04 + DISP-03 + D-79-03 + D-79-09 + D-79-10; analog tests/integration/scripts/v8.4-readonly.test.ts L1-127, INVERSE INTENT — asserts WRITES did happen):
     - imports: vitest describe/it/expect/beforeAll/afterAll, spawnSync from node:child_process, existsSync/readFileSync/writeFileSync/unlinkSync from node:fs, path, postgres
     - constants: SCRIPT_PATH = 'scripts/v8.4-brand-canonicalization.ts'; BRAND_PATH/BRAND_BACKUP/FAMILY_PATH/FAMILY_BACKUP under .planning/
     - One sanity `it('Wave 0 RED stub loads', ...)` outside maybe
     - `maybe('Phase 79 — v8.4 --apply --mode=both atomic transaction (MIG-02/03/04 + DISP-03 + D-79-03/09/10)', () => {`
     - `it.todo('MIG-02: --apply --mode=both populates watches_catalog.brand_id for every catalog row (post-snapshot resolved_brand_count equals total_catalog_count)')`
     - `it.todo('MIG-02 + B-78-01 + Hamilton merge: every catalog row with brand_raw in (Hamilton, Hamilton Watch) resolves to brand_id = \\'20969364-f3b1-4b1d-ab2f-e5d22e9ffabc\\'')`
     - `it.todo('MIG-02 + D-79-09: new brands rows (33 per current operator decisions) all have needs_review = false')`
     - `it.todo('MIG-03: --apply --mode=both populates watches_catalog.family_id for every catalog row (post-snapshot resolved_family_count equals total_catalog_count)')`
     - `it.todo('MIG-03 + D-79-06: alias-append idempotent — re-running --apply does not produce duplicate elements in any watch_families.aliases array')`
     - `it.todo('MIG-04: post-flight assertion uses positive predicate IS DISTINCT FROM NULL (snapshot via pg_stat_statements or stdout-grep) and rolls back when injected to fail')`
     - `it.todo('D-79-03: atomic rollback — when the post-flight assertion is force-failed (test injects an unresolved row via a fixture or env flag), every prior INSERT/UPDATE rolls back; pre vs post snapshot is byte-identical')`
     - `it.todo('DISP-03: every watches.catalog_id-non-NULL row has brand + model overwritten from canonical brands.name + watch_families.name (post-snapshot watches.brand for Hamilton-owners reads \\'Hamilton\\' not \\'Hamilton Watch\\')')`
     - `it.todo('DISP-03: hydration preserves watches.notes, .serial, .reference, .price_paid, .style_tags (snapshot non-hydrated columns; assert pre == post for those columns)')`
     - `it.todo('D-79-10: --apply writes .planning/phases/79-backfill-migration-display-hydration/79-POST-DEPLOY.md containing ## Apply Summary, ## Post-Flight Assertion (MIG-04), ## Operator Sign-Off Queries, ## What this push does NOT do sections')`
     - beforeAll backup/restore stub (per analog L68-104, extended for BOTH brand AND family decision files per 79-PATTERNS.md L666-681)

  2. `tests/integration/scripts/v8.4-apply-idempotent.test.ts` (D-79-04 + D-79-06; analog tests/integration/scripts/v8.4-brand-canonicalization.test.ts):
     - imports: vitest describe/it/expect/beforeAll/afterAll, spawnSync from node:child_process, postgres
     - One sanity `it('Wave 0 RED stub loads', ...)` outside maybe
     - `maybe('Phase 79 — v8.4 --apply --mode=both idempotent (D-79-04 + D-79-06)', () => {`
     - `it.todo('D-79-04: first --apply --mode=both run exits 0 and writes (counts > 0)')`
     - `it.todo('D-79-04: second --apply --mode=both run exits 0 with stdout matching /Already applied — nothing to do\\./ (no writes; pre vs post snapshot identical)')`
     - `it.todo('D-79-06: second --apply --mode=both does NOT append duplicate alias entries (post-second-run cardinality of every watch_families.aliases array equals post-first-run cardinality)')`
     - helper `runScript(args)` returning { exitCode, stdout, stderr } per 79-PATTERNS.md L483-495

Both files: do NOT spawn the script in this plan (it doesn't have the --apply flag yet). The integration stubs install the test harness shape; downstream plans 02/03/04 un-`todo` each assertion as the corresponding behavior ships.

Mitigates threats: T-79-01 (data integrity verification deferred to integration assertions) + T-79-05 (post-flight predicate divergence assertion gate).
  </action>

  <verify>
    <automated>npx vitest run tests/integration/scripts/v8.4-apply-atomic.test.ts tests/integration/scripts/v8.4-apply-idempotent.test.ts</automated>
  </verify>

  <done>
    - 2 integration test files exist with DATABASE_URL gate + first-line marker + decision-ID citations.
    - vitest reports `2 passed | N todo | 0 failed` when DATABASE_URL UNSET (sanity it() passes; gated suite skipped).
    - vitest reports `2 passed | N todo | 0 failed` when DATABASE_URL SET (sanity it() passes; gated it.todo callsites reported as todo).
    - Every REQ-ID (MIG-02, MIG-03, MIG-04, DISP-03) and every D-79-NN listed (D-79-03, D-79-04, D-79-06, D-79-09, D-79-10) appears in at least one `it.todo` assertion string.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 79-VALIDATION.md fill — Per-Task Verification Map + Wave 0 flags</name>

  <files>.planning/phases/79-backfill-migration-display-hydration/79-VALIDATION.md</files>

  <read_first>
- .planning/phases/79-backfill-migration-display-hydration/79-VALIDATION.md  (current draft skeleton; planner replaces the placeholder table)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L1180-1213  (Validation Architecture section + Phase Requirements → Test Map — source of truth for the row contents)
- .planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md  (Phase 78 reference shape — post-Wave-0 state, for the populated table format)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L297-507  (file → stub mapping for the table)
  </read_first>

  <action>
Rewrite 79-VALIDATION.md to a fully-populated state:

  (a) Frontmatter: flip `nyquist_compliant: false → true`, `wave_0_complete: false → true`, set `status: ready-for-plan-02`.

  (b) Test Infrastructure table — fill in:
     - Framework: `vitest@2.1.9`
     - Config file: `vitest.config.ts`
     - Quick run command: `npm run test -- tests/unit/scripts/`
     - Full suite command: `npm run test -- tests/unit/scripts/ tests/integration/scripts/`
     - Estimated runtime: ~30s unit, ~120s integration with DATABASE_URL

  (c) Sampling Rate — keep the existing structure, update commands to reference the actual ones above.

  (d) Per-Task Verification Map — replace the single placeholder row with one row per (Plan, Task, REQ-ID/D-79-NN) covered by the Wave 0 stubs. One row per `it.todo` assertion in Tasks 1+2 above is overkill; use one row per (test-file, REQ-or-D-79-NN) grouping (per 79-RESEARCH.md L1191-1208 — 15 rows total). Status column = `⬜ pending` for all (no Wave 1 work has greened any test yet). File-Exists column = `✅ W0` for every row.

  (e) Wave 0 Requirements — replace placeholder bullets with the 6 stub files Tasks 1+2 just created (mark all checked: `- [x] tests/unit/scripts/v8.4-host-detect.test.ts ...`).

  (f) Manual-Only Verifications — list:
     - prod operator UAT sign-off (DISP-03, MIG-04) — covered by Plan 05 sign-off SQL queries (`Why Manual: prod connection state cannot run unattended; operator types yes in prompt + reviews 6 verification queries`)
     - operator commits 79-POST-DEPLOY.md after review (D-79-10) — covered by Plan 05

  (g) Validation Sign-Off checklist — check all 6 items as complete.

  Mitigates threats: governance / verification gate; not a direct STRIDE entry.
  </action>

  <verify>
    <automated>node -e "const c = require('fs').readFileSync('.planning/phases/79-backfill-migration-display-hydration/79-VALIDATION.md','utf8'); const checks = [['frontmatter wave_0_complete:true', /^wave_0_complete:\s*true/m], ['frontmatter nyquist_compliant:true', /^nyquist_compliant:\s*true/m], ['MIG-02 cited', /MIG-02/], ['MIG-03 cited', /MIG-03/], ['MIG-04 cited', /MIG-04/], ['DISP-03 cited', /DISP-03/], ['stub host-detect listed', /v8\.4-host-detect/], ['stub strict-gate listed', /v8\.4-strict-gate/], ['stub family-build-decisions listed', /v8\.4-family-build-decisions/], ['stub post-deploy-template listed', /v8\.4-post-deploy-template/], ['stub apply-atomic listed', /v8\.4-apply-atomic/], ['stub apply-idempotent listed', /v8\.4-apply-idempotent/]]; let fail=0; for (const [n, re] of checks) { if (!re.test(c)) { console.error('MISSING:', n); fail++ } } if (fail) process.exit(1); console.log('PASS — all 12 checks present')"</automated>
  </verify>

  <done>
    - 79-VALIDATION.md frontmatter has wave_0_complete: true AND nyquist_compliant: true.
    - Per-Task Verification Map populated (one row per REQ-ID/D-79-NN grouping, 15 rows minimum; matches 79-RESEARCH.md L1191-1208).
    - All 6 stub files listed in Wave 0 Requirements section, all checked.
    - Every Phase 79 requirement ID (MIG-02, MIG-03, MIG-04, DISP-03) and every cited decision ID (D-79-01..10 used in Tasks 1+2) appears at least once.
    - The grep-gate verify script above prints `PASS — all 12 checks present` and exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → vitest runner | test files are project-owned; no external untrusted input |
| no DB boundary | this plan creates test stubs only; no live DB writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Tampering | tests/integration/scripts/v8.4-apply-atomic.test.ts (deferred coverage) | mitigate (via downstream plans) | This plan establishes the assertion harness; Plan 04 implements the atomic transaction whose rollback assertions land in this stub's `it.todo` for D-79-03 |
| T-79-05 | Integrity | tests/unit/scripts/v8.4-post-deploy-template.test.ts (deferred coverage) | mitigate (via downstream plans) | This plan establishes the grep-gate for `IS DISTINCT FROM NULL` predicate divergence; Plan 04 implements the actual assertion in scripts/v8.4-brand-canonicalization.ts |
| T-79-03 | Tampering | tests/unit/scripts/v8.4-strict-gate.test.ts (deferred coverage) | mitigate (via downstream plans) | This plan stubs the strict-gate's 4 refuse cases; Plan 02 implements `strictPreflightGate` and greens the assertions |
| T-PLAN-01 | Tampering | test files themselves | accept | files are project-owned; convention enforces first-line marker for traceability; ASVS L1 acceptable risk |
</threat_model>

<verification>
- All 6 stub files exist with first-line `// Phase 79 / 79-01-PLAN.md — Wave 0 RED stub.` marker.
- Every D-79-01..10 ID appears in at least one `it.todo` assertion string across the 6 stubs.
- Every Phase 79 requirement ID (MIG-02, MIG-03, MIG-04, DISP-03) appears in at least one `it.todo` assertion string.
- `npm run test -- tests/unit/scripts/v8.4-host-detect.test.ts tests/unit/scripts/v8.4-strict-gate.test.ts tests/unit/scripts/v8.4-family-build-decisions.test.ts tests/unit/scripts/v8.4-post-deploy-template.test.ts tests/integration/scripts/v8.4-apply-atomic.test.ts tests/integration/scripts/v8.4-apply-idempotent.test.ts` exits 0; reports 6+ passed (one sanity per file) + many todo + 0 failed.
- DATABASE_URL UNSET path: integration suites report `↓ skipped` per Phase 78 convention.
- DATABASE_URL SET path: integration suite sanity tests pass; gated `it.todo` callsites reported as todo (not failed).
- `npm run build` exits 0 (Wave 0 stubs do not break the prebuild static guards).
- 79-VALIDATION.md frontmatter: `wave_0_complete: true`, `nyquist_compliant: true`.
</verification>

<success_criteria>
- 6 stub files created: 4 unit + 2 integration.
- Phase 78 Wave 0 convention preserved exactly (first-line marker, sanity it, decision-ID citation pattern, describe.skip env gate).
- 79-VALIDATION.md frontmatter flags flipped + Per-Task Verification Map populated.
- vitest discovery shows the 6 new files; sanity tests pass; downstream plans inherit a working RED skeleton.
- No production code changed (scripts/v8.4-brand-canonicalization.ts untouched).
</success_criteria>

<output>
After completion, create `.planning/phases/79-backfill-migration-display-hydration/79-01-SUMMARY.md` recording:
- 6 stub files created + commit shas
- vitest discovery output (counts: passed/todo/skipped/failed)
- 79-VALIDATION.md frontmatter flip confirmation
- Any deviations from spec (per Rule 1 auto-fix policy)
</output>
</content>
</invoke>