---
phase: 33-discovery-audit
plan: 04
type: execute
wave: 4
depends_on: [33-03]
files_modified:
  - .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
autonomous: false
requirements: [DISC-10]
tags: [audit, documentation, discovery, browser-pass, decisions]

user_setup:
  - service: horlo.app production + fresh-signup-account fixture
    why: "D-02 mandates production browser walk; D-03 mandates owner + fresh-account viewer states"
    env_vars: []
    dashboard_config:
      - task: "Confirm horlo.app production is live and the owner account has followingCount ≥ 3 AND wearEventsCount ≥ 1 AND collection.length > 0"
        location: "https://horlo.app/u/{owner-username}"
      - task: "Create fresh-signup fixture account: signup at https://horlo.app/signup with throwaway email (e.g., phase33-audit+1@horlo.app); verify email via mail.horlo.app Resend SMTP; do NOT follow anyone, add any watches, or log any wear events"
        location: "https://horlo.app/signup"
      - task: "Identify ≥1 user pair where the owner-populated account follows a collector with confirmed catalog overlap (so G-12 common-ground browser walk produces a non-404 result)"
        location: "owner account /u/{follower}/common-ground"

must_haves:
  truths:
    - "All 4 decision verdicts (Q1–Q4 per D-17) carry a final value YES | NO | DEFERRED — no PENDING placeholders remain"
    - "Each decision's Rationale is 2–4 sentences citing audit findings (D-16)"
    - "Each decision's Cited rows: line lists ≥1 DISC-AUDIT-NN that EXISTS in the table"
    - "Each decision's Drives: line names the downstream phase / item the verdict gates"
    - "~25–30 high-stakes rows have updated evidence cells with `prod: <URL> + <observation>` (browser-pass confirmation per D-02)"
    - "checks/full.sh exits 0 — every D-13 rule passes against the finalized audit"
    - "Implements decisions: D-02 (browser spot-check is performed against production horlo.app — not local dev — and high-stakes rows have evidence cells rewritten from file:line to prod:URL+observation), D-03 (browser walk is performed from BOTH the populated owner account AND a fresh-signup account fixture so viewer_state assignments from Wave 2 are confirmed against actual rendered pages), D-04 (browser walk is performed at BOTH desktop ~1280px AND mobile ~390px viewports so D-04 viewport row-splits are confirmed against actual renderings), D-15 (Decisions § lives inline as the final § of 33-DISCOVERY-AUDIT.md — no separate decisions file), D-16 (each verdict uses the verdict + 2–4 sentence rationale + cited rows + drives template literal), D-17 (exactly 4 decisions Q1–Q4 — combine home+explore, lineage browse priority, dead-end closure priority, CAT-13 framing — with no 5th catch-all)"
    - "Zero files modified outside .planning/phases/33-discovery-audit/"
  artifacts:
    - path: ".planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md"
      provides: "Finalized audit: browser-confirmed evidence on high-stakes rows + 4 decisions with verdicts; D-13 5 rules all pass; STATE.md ready for Q1–Q4 entries"
      contains: "**Verdict:**"
  key_links:
    - from: "Decision Q1..Q4 Cited rows lines"
      to: "DISC-AUDIT-NN rows in the same file"
      via: "row-id citation"
      pattern: "DISC-AUDIT-[0-9]+"
    - from: "Audit decisions § final verdicts"
      to: ".planning/STATE.md §Key Decisions (v5.0)"
      via: "STATE.md update after audit ships (out of scope this phase; Phase 34/35/38/39 plans cite verdicts)"
      pattern: "Phase 33 Q[1-4]"
---

<objective>
Pass C — production browser spot-check (D-02, D-03, D-04). Walk ~25–30 high-stakes rows on horlo.app from BOTH the owner account AND the fresh-signup fixture account, at desktop ~1280px AND mobile ~390px. Update the audit table's `evidence` column for browser-confirmed rows from `file:line` to `prod: <URL> + <observation>`. Confirm or downgrade the WR-07 flagship Dead row's tag (Plan 02 marked it provisional Dead pending browser confirmation). Capture any newly-discovered runtime divergence (e.g., V-8 aspect-ratio CSS-chain failures from feedback memory) as additional rows or tag adjustments.

Pass D — author the 4 decision verdicts (D-15, D-16, D-17). Replace the PENDING placeholders in the Decisions § with explicit YES | NO | DEFERRED verdicts using the D-16 template, anchored to specific DISC-AUDIT-NN rows that justify each verdict.

Purpose: D-02 makes production the source-of-truth tag; D-03 ensures both populated and sparse states are walked; D-04 catches viewport divergence. The decisions § is what downstream Phases 34/35/38/39 cite — without verdicts, the audit is unfalsifiable per D-13 rule 5.

Output: 33-DISCOVERY-AUDIT.md is FINAL. checks/full.sh exits 0. Phase 33 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-discovery-audit/33-CONTEXT.md
@.planning/phases/33-discovery-audit/33-RESEARCH.md
@.planning/phases/33-discovery-audit/33-PATTERNS.md
@.planning/phases/33-discovery-audit/33-VALIDATION.md
@.planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
@.planning/phases/33-discovery-audit/33-03-SUMMARY.md
@.planning/seeds/SEED-004-v5-discovery-north-star.md

<interfaces>
<!-- Browser-pass priority list (RESEARCH.md §Conditional Rendering Map line 250 + §Browser-Pass Logistics): -->
<!--   10 highest-stakes gates: G-1, G-3, G-4, G-6, G-7, G-8, G-12, G-13, G-15, G-19 -->
<!--   Walk these at owner-populated + fresh-account at one viewport each = ~20 page loads -->
<!--   + alternate viewport for layout-divergent surfaces (G-1 mobile vs desktop, G-3/G-4 CatalogPageActions mobile bottom-sheet vs desktop) = ~25-30 total page loads -->
<!--   Time: ~1-2 hours -->

<!-- Browser-pass evidence format (RESEARCH.md §Browser-Pass Logistics URL pattern reference): -->
<!--   prod: https://horlo.app/explore (fresh-account, ~390px) — ExploreHero rendered with "Find your first three" copy; clicked 3rd suggested collector card → /u/<username> loaded with 200 -->
<!-- Append the prod: line to the existing file:line evidence (do NOT replace) so the source-pass anchor remains for re-verification: -->
<!--   src/components/explore/ExploreHero.tsx:NN; prod: https://horlo.app/explore (fresh-account, 390px) — Hero copy "Find your first three" rendered; "Browse popular collectors" → /explore/collectors loaded -->

<!-- Tag downgrade rules (browser observation may change source-pass tags): -->
<!--   - Source-pass Live → browser-pass Dead: target 404s, errors, or no-ops (capture observed failure mode in evidence) -->
<!--   - Source-pass Live → browser-pass Redundant: same destination already cited; cite the prior DISC-AUDIT-NN -->
<!--   - Source-pass Dead provisional (WR-07 wishlist.ts:206) → browser-pass confirms or reverts based on observed visual stale-ness after drag-reorder -->
<!--   - Source-pass missing → browser-pass Missing (no source affordance found for an Rdio-expected click): cite SEED-004 line 15 quote -->

<!-- Pass C scope discipline (RESEARCH.md Pitfall 4): -->
<!--   Browser pass walks ONLY the ~25-30 high-stakes rows + spot-checks for V-8 aspect-ratio failures. -->
<!--   Do NOT walk all ~150 rows. Source-pass + gate-annotation already certified the rest. -->

<!-- Pass D — Decision Q1..Q4 verdict-authoring template (D-16): -->
<!--   ### Decision QN: <question text> -->
<!--   **Verdict:** YES | NO | DEFERRED -->
<!--   **Rationale:** [2-4 sentences citing audit findings] -->
<!--   **Cited rows:** DISC-AUDIT-NN, DISC-AUDIT-MM -->
<!--   **Drives:** [downstream phase / item this verdict gates] -->

<!-- Pass D — Decision verdict guidance (per CONTEXT.md D-17 + RESEARCH.md Open Question Q4): -->
<!--   Q1 (Combine home and explore?): cite Live/Redundant rows that show overlap between / and /explore destinations (e.g., if PopularCollectors on /explore has same destinations as CollectorsLikeYou on /, that's a Redundant row pair → YES candidate). Cite Missing rows that suggest /explore content unique to /explore (e.g., GainingTractionWatches with no / equivalent → NO candidate). -->
<!--   Q2 (Lineage browse priority): cite Missing rows on /catalog/{id} where Rdio principle expects "walk to other watches in same family" but no affordance exists. If many such Missing rows → high priority (YES drives Phase 35 schema+UI scope). If few → DEFERRED to Phase 39 polish. Drives Phase 35 schema-only vs schema+UI scope per STATE.md line 77. -->
<!--   Q3 (Dead-end closure priority): count Dead rows + Missing rows; if WR-07 + significant Missing → YES (Phase 39 prioritizes dead-end closure as first work item). Drives Phase 39 polish item ordering per ROADMAP §Phase 39. -->
<!--   Q4 (CAT-13 discovery framing): cite verdict-related rows (G-4/G-6 verdict suppression rows) + any rows where catalog taste columns would visibly change verdict labels. Many such rows → discovery improvement; few → tech debt. Drives Phase 38 framing per ROADMAP §Phase 38. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Production browser spot-check pass (~25–30 rows, owner + fresh-account, desktop + mobile)</name>
  <what-built>
    Plans 01–03 produced a 130–250-row click-path audit table with file:line evidence and finalized viewer_state values. RESEARCH.md identifies the 10 highest-stakes runtime gates (G-1, G-3, G-4, G-6, G-7, G-8, G-12, G-13, G-15, G-19) — walking each at 2 viewer states + 2 viewports for the layout-divergent gates yields ~25–30 page loads. This task confirms the source-pass tags against actual rendered behavior on horlo.app production.

    The WR-07 flagship Dead row (DISC-AUDIT-NN on /u/{user}/wishlist citing src/app/actions/wishlist.ts:206) is the single most important confirmation: drag-reorder a wishlist row, watch whether the new order persists across a hard navigation (it should — DAL writes succeed) but whether the visible page state stays stale until manual refresh (revalidatePath silently no-ops, so the page doesn't re-render). Capture the observed user-visible failure mode in the evidence cell.
  </what-built>
  <how-to-verify>
    **Setup (one-time per audit):**

    1. Open horlo.app in Chrome DevTools with the device toolbar enabled (Cmd+Shift+M). Set device to "iPhone 12 Pro" (~390px width) for mobile walks; toggle off for desktop walks (~1280px window).
    2. Use TWO Chrome profiles (or one normal + one Incognito) to keep the owner cookie and fresh-account cookie isolated.
    3. **Owner account:** log in as the existing development account (the user's primary collector account). Confirm `followingCount ≥ 3 AND wearEventsCount ≥ 1 AND collection.length > 0` by visiting `/u/{owner-username}/collection` and `/u/{owner-username}/following`.
    4. **Fresh-account fixture:** visit https://horlo.app/signup in the SECOND Chrome profile. Use a throwaway email (e.g., phase33-audit+1@horlo.app via Gmail+ aliasing or SimpleLogin). Verify email via the Resend SMTP delivery to mail.horlo.app. **Do NOT follow anyone, add any watches, or log any wear events.** Confirm the fresh-account satisfies `followingCount=0 AND wearEventsCount=0 AND collection.length=0` by visiting `/u/{fresh-username}/collection` (should be empty).
    5. Identify ≥1 user pair where the owner account follows a collector with known catalog overlap, so G-12 common-ground browser walk produces a non-404 result. If no such pair exists, follow one collector with overlap from the owner account before starting.

    **Walk (~25–30 page loads, 1–2 hours):**

    For each of the 10 high-stakes gates, walk the affected rows in BOTH viewer states (owner + fresh-account) at the appropriate viewport. For each browser-observed row, OPEN 33-DISCOVERY-AUDIT.md, find the matching DISC-AUDIT-NN row, and APPEND a `prod:` evidence line to its evidence cell using the format from `<interfaces>`.

    1. **G-1 (/explore ExploreHero):** fresh-account → visit /explore → confirm ExploreHero renders with sparse-state copy → click each Hero CTA → record observed destination + 200/4xx. Also walk at mobile (~390px) per V-1 viewport split.
    2. **G-3 (/catalog ownership framing):** owner → visit /catalog/{some-uuid} where the owner OWNS the catalog ref → confirm "You own this" framing renders + CatalogPageActions ABSENT. Then visit /catalog/{some-uuid} where owner does NOT own → confirm CatalogPageActions visible.
    3. **G-4 (/catalog verdict suppression):** owner-populated → confirm verdict + 3 CTAs render. Fresh-account → visit same URL → confirm verdict suppressed. **Note any Missing rows where Rdio principle suggests an affordance is needed for fresh-account empty-collection state.**
    4. **G-6 (/watch verdict suppression):** same pattern as G-4 on /watch/{id}.
    5. **G-7 (/watch same-user vs cross-user):** owner viewing OWN watch → confirm Edit + Delete affordances visible. Owner viewing ANOTHER user's watch → confirm Edit + Delete absent.
    6. **G-8 (/u/{user}/collection LockedTabCard):** fresh-account → visit /u/{owner}/collection (owner has private collection) → confirm LockedTabCard renders with the lock UI. Confirm LockedTabCard is correctly tagged Live, NOT Dead (per Pitfall 2).
    7. **G-12 (/u/{user}/common-ground 404):** owner viewing follower with overlap → confirm Live render. Fresh-account viewing owner → confirm 404. Decide if this is a Dead row (404 is a dead-end) OR Missing row (Rdio expects a "explore other follows" fallback CTA on the 404 page).
    8. **G-13 (/u/{user}/insights owner-only):** owner → visit /u/{owner}/insights → confirm tab visible AND content renders. Fresh-account → visit /u/{owner}/insights directly via URL → confirm 404. Confirm fresh-account ProfileTabs OMITS the Insights link entirely (two-layer privacy).
    9. **G-15 (/u/{user}/notes per-row notesPublic):** owner → visit /u/{owner}/notes → confirm all rows visible with NoteVisibilityPill (DEBT-09). Fresh-account → visit same URL → confirm only `notesPublic !== false` rows visible.
    10. **G-19 (ProfileTabs visibility):** owner → ProfileTabs shows Insights tab; Common Ground tab visibility depends on overlap. Fresh-account → ProfileTabs hides Insights; Common Ground visible only when overlap.hasAny.

    **WR-07 flagship Dead row confirmation:**

    11. Owner → visit /u/{owner}/wishlist → drag-reorder a wishlist row → DO NOT manually refresh → click around to /u/{owner}/collection then back to /u/{owner}/wishlist → observe whether the new order is visible OR the old order persists (the WR-07 silent no-op produces stale visual state). Hard refresh (Cmd+Shift+R) and observe whether the new order finally appears (confirms DAL write succeeded; the page just didn't revalidate). Capture the observed regression in the WR-07 row's evidence: `prod: https://horlo.app/u/{owner}/wishlist (owner-populated, desktop) — drag-reordered row 3 above row 1; navigated to /collection then back; row 1 visually still on top; hard refresh restored row 3 on top → confirms WR-07 silent no-op user-visible regression`.

    **V-8 aspect-ratio CSS-chain spot-check (per feedback_ui_spec_css_chain_blind_spot.md):**

    12. At mobile (~390px), inspect WywtSlide / ProfileWatchCard / DiscoveryWatchCard rendered images. Look for black-bar bug (image has wrong aspect ratio AND object-fit produces visible letterbox bars). If observed, ADD a Dead row (or annotate the existing card row with browser-pass evidence noting the visual failure). If NOT observed (Phase 30 work was correct after all), no action.

    **Verdict-evidence rationale capture:**

    13. While walking, build a mental map for the Pass D verdicts:
        - Q1 (Combine home and explore?): note any Redundant rows discovered (overlap between / and /explore destinations) AND any Missing rows unique to one surface.
        - Q2 (Lineage browse priority): on /catalog/{id} owner-populated walks, look for absent affordances to walk to other watches in the same family — these are Missing-row candidates that drive Q2 priority.
        - Q3 (Dead-end closure priority): count total Dead + Missing rows observed; if WR-07 + multiple others → high priority verdict.
        - Q4 (CAT-13 discovery framing): on /catalog/{id} verdict rows, note whether catalog taste would visibly change the rendered verdict label (e.g., a Submariner's case_size + style + bracelet would change a "Hard Mismatch" to "Core Fit").

    **Update the audit file:**

    For every row touched in the browser walk, edit 33-DISCOVERY-AUDIT.md and APPEND the prod: evidence line to the row's evidence cell. If a row's tag changes (Live → Dead, source-Dead provisional → confirmed Dead, source-Live → Redundant), update the tag column. If a NEW Missing row is discovered (an Rdio-violation absence), ADD a new row with the next sequential DISC-AUDIT-NN id and tag=Missing, evidence=`Rdio violation: <description>; prod: <URL> — <observation>`.

    **Run quick.sh after edits:** `bash .planning/phases/33-discovery-audit/checks/quick.sh` must exit 0.

    **Confirm progress:**

    Once browser walk + audit-file updates are complete, type "approved" to proceed to Pass D (decision authoring).
  </how-to-verify>
  <resume-signal>Type "approved" once browser walk completes and the audit file's evidence cells have prod: lines for the ~25–30 high-stakes rows. Or describe specific issues / divergences that block Pass D.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pass D — author the 4 decision verdicts (Q1..Q4 per D-17) using the D-16 template anchored to specific DISC-AUDIT-NN rows</name>
  <files>
    .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
  </files>
  <read_first>
    - .planning/phases/33-discovery-audit/33-CONTEXT.md (D-15 inline placement; D-16 verdict template; D-17 four-decision lock)
    - .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md (CURRENT STATE — read the entire Click-Path Audit table to identify which rows justify each verdict)
    - .planning/phases/33-discovery-audit/33-RESEARCH.md (lines 530–537: Open Question Q4 — guidance on what the CAT-13 framing verdict actually decides)
    - .planning/seeds/SEED-004-v5-discovery-north-star.md (lines 11–22 — Rdio principle context for Q1, Q2 framing)
    - .planning/ROADMAP.md (lines 132–142 §Phase 33 success criterion #3 — verbatim Q1–Q4 wording; lines 144–215 §Phase 34/35/38/39 — Drives: target phases per Q2/Q3/Q4)
    - .planning/STATE.md (lines 65–77 §Key Decisions — Phase 39 + Phase 35 audit-conditional scope; Q2 + Q3 verdicts will be added here AFTER Phase 33 ships)
  </read_first>
  <action>
    Replace the 4 PENDING placeholders in the Decisions § of 33-DISCOVERY-AUDIT.md with final verdicts using the D-16 template. Each verdict MUST:

    1. Replace `**Verdict:** PENDING <!-- ... -->` with `**Verdict:** YES` OR `**Verdict:** NO` OR `**Verdict:** DEFERRED` (no other values per D-13 rule 5b).
    2. Replace the `**Rationale:**` placeholder with 2–4 sentences citing audit findings (D-16). The rationale must reference observable patterns in the table (e.g., "5 Redundant rows on / and /explore share destinations…" or "0 Missing rows for lineage browse on /catalog/{id} suggests…").
    3. Replace the `**Cited rows:**` placeholder with a comma-separated list of ≥1 DISC-AUDIT-NN row ids that EXIST in the table (D-13 rule 5d, 5e). Reference rows that genuinely justify the verdict — D-13 rule 5e fails the audit if cited rows are absent or unrelated.
    4. Replace the `**Drives:**` placeholder with a one-line statement of the downstream phase / item the verdict gates.

    Specific guidance per question:

    **Q1: Combine home and explore?**
    - Examine the table for Redundant rows where `/` and `/explore` share destinations (e.g., CollectorsLikeYou on / and PopularCollectors on /explore both linking to /u/{username}). If many → YES candidate (consolidating reduces the redundancy). If few → NO (each surface earns its keep). DEFERRED is acceptable if the audit reveals the question depends on Phase 39 work (e.g., "consolidation only viable after editorial Featured slot ships per DISC-09").
    - Cited rows: pick 2–4 specific DISC-AUDIT-NN ids that show the redundancy pattern (or absence of it).
    - Drives: "Phase 39 polish item: consolidation work item Y/N" OR "Phase 39 keeps surfaces separate; consolidation deferred to v5.x".

    **Q2: Lineage browse priority**
    - Examine /catalog/{catalogId} rows for Missing tags where the Rdio principle expects "walk to other watches in same family" but no affordance exists. Many Missing rows → YES (high priority; Phase 35 ships schema+UI). Few or none → DEFERRED (Phase 35 ships schema-only; UI affordances move to Phase 39 or v5.x).
    - Cited rows: list the specific Missing-row ids on /catalog/{catalogId}.
    - Drives: per STATE.md line 77 — "Phase 35 schema-only vs schema+UI scope; Phase 39 lineage-browse polish scope".

    **Q3: Dead-end closure priority**
    - Count total Dead + Missing rows (`grep -c '| Dead |'` + `grep -c '| Missing |'`). If WR-07 + multiple others → YES (Phase 39 prioritizes dead-end closure as first work item). If only WR-07 → DEFERRED (close in a small follow-up; Phase 39 prioritizes other work first). If zero (unlikely) → NO (no dead-ends to close; surprising; document why).
    - Cited rows: list the Dead + Missing row ids (limit to ~5 most impactful for readability).
    - Drives: per ROADMAP §Phase 39 — "Phase 39 polish item ordering: dead-end closure first if YES".

    **Q4: CAT-13 discovery framing**
    - Examine /catalog/{catalogId} and /watch/{id} verdict rows. Are there many rows where catalog taste columns would visibly change the verdict label (e.g., a Hard Mismatch becoming Core Fit when style_tags + case_material align)? Many such rows → YES (CAT-13 is a discovery improvement, not tech debt). Few → NO (CAT-13 is tech debt; small verdict-quality nudge). DEFERRED if the audit can't tell without running the rewire.
    - Cited rows: list verdict rows on /catalog/{catalogId} and /watch/{id} (the G-4/G-6 verdict-suppression rows + verdict-rendering rows).
    - Drives: per ROADMAP §Phase 38 lines 193–203 — "Phase 38 framing: discovery improvement vs tech debt".

    After authoring all 4 verdicts, run `bash .planning/phases/33-discovery-audit/checks/full.sh`. This MUST exit 0. If it fails, fix the failure (most likely cause: cited DISC-AUDIT-NN does not exist; misspelled tag value; missing Verdict line on a decision).

    Update the frontmatter `decision:` field from `pending` to `decided`.

    Update the frontmatter `status:` field from `draft` to `decided`.
  </action>
  <verify>
    <automated>bash .planning/phases/33-discovery-audit/checks/full.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash .planning/phases/33-discovery-audit/checks/full.sh` exits 0 (every D-13 rule passes + D-09 sequencing check passes)
    - `grep -c '^\*\*Verdict:\*\*' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns 4
    - `grep -E '^\*\*Verdict:\*\* (YES|NO|DEFERRED)$' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md | wc -l | tr -d ' '` returns 4
    - `grep -c '^\*\*Verdict:\*\* PENDING' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns 0
    - `grep -c '^\*\*Cited rows:\*\*' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns 4
    - Every Cited rows: line references ≥1 DISC-AUDIT-NN that exists: while-loop check from full.sh rule 5e passes (already enforced by full.sh exit 0 above)
    - Every Drives: line is non-placeholder text: `grep -c '^\*\*Drives:\*\* \[Wave 3 fills' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns 0
    - Frontmatter `decision:` is `decided`: `grep -c '^decision: decided' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns 1
    - Frontmatter `status:` is `decided`: `grep -c '^status: decided' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns 1
    - **≥ 20 rows must have `prod:` evidence** (Pass C target is 25–30; floor of 20 enforces auditor commitment to the browser walk and prevents shortcutting Task 1's 25–30 row mandate): `[ "$(grep -c '^| DISC-AUDIT-.*prod:' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md)" -ge 20 ]`
    - WR-07 row carries browser-pass confirmation: `grep -c 'wishlist\.ts:206.*prod:' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns ≥ 1 OR `awk -F'\\|' '/^\| DISC-AUDIT-/ { ev=$7; gsub(/^ | $/, "", ev); if (ev ~ /wishlist\.ts:206/ && ev ~ /prod:/) found=1 } END { exit !found }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` exits 0
    - Total row count between 130 and 250: same range as Plan 03
    - Row IDs flat sequential, no gaps, no duplicates: same checks as Plans 02/03
    - `git status --porcelain | grep -v '^?? \.planning/phases/33-discovery-audit/' | grep -v '^ M \.planning/phases/33-discovery-audit/'` returns no lines (zero changes outside phase dir)
  </acceptance_criteria>
  <done>4 decision verdicts authored with YES|NO|DEFERRED, rationale 2–4 sentences citing rows, Cited rows references existing DISC-AUDIT-NN ids, Drives names downstream phase/item. full.sh exits 0. Frontmatter status + decision flipped to "decided". Phase 33 ships.</done>
</task>

</tasks>

<verification>
After both tasks complete, the audit is FINAL:

```bash
# Full validation suite — must exit 0
bash .planning/phases/33-discovery-audit/checks/full.sh

# Confirm 4 verdicts, zero PENDING
grep -E '^\*\*Verdict:\*\* (YES|NO|DEFERRED)$' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md | wc -l | tr -d ' '
# Expect: 4

grep -c '^\*\*Verdict:\*\* PENDING' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
# Expect: 0

# Confirm browser-pass evidence on high-stakes rows (Pass C target 25–30; floor 20 enforces auditor commitment)
grep -c '^| DISC-AUDIT-.*prod:' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
# Expect: ≥ 20 (preferably 25-30)

# Confirm WR-07 row has both source + browser evidence
awk -F'\\|' '/^\| DISC-AUDIT-/ { ev=$7; gsub(/^ | $/, "", ev); if (ev ~ /wishlist\.ts:206/) print $2 ": " ev }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
# Expect: 1 line containing both "wishlist.ts:206" AND "prod:"

# Zero changes outside the phase directory
git status --porcelain | grep -v '^?? \.planning/phases/33-discovery-audit/' | grep -v '^ M \.planning/phases/33-discovery-audit/' | wc -l | tr -d ' '
# Expect: 0
```
</verification>

<success_criteria>
- 4 decision verdicts present (Q1–Q4), each YES|NO|DEFERRED, each with rationale + ≥1 cited DISC-AUDIT-NN that exists, each with Drives statement
- ~25–30 high-stakes rows have prod: browser-pass evidence appended (floor of 20 enforced by Task 2 acceptance)
- WR-07 wishlist.ts:206 row carries confirmed Dead OR reverted Live with browser evidence
- checks/full.sh exits 0 (D-13 rules 1–5 + D-09 sequencing)
- Frontmatter `status: decided`, `decision: decided`
- Zero changes outside `.planning/phases/33-discovery-audit/`
- Phase 33 ROADMAP success criteria #1–5 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/33-discovery-audit/33-04-SUMMARY.md` describing:
- 4 verdicts authored with verdict + cited rows summary (e.g., "Q1: NO — cited DISC-AUDIT-23, DISC-AUDIT-67")
- Browser-pass row count (how many rows updated with prod: evidence)
- WR-07 final tag (Dead confirmed OR Live reverted)
- V-8 aspect-ratio findings (any black-bar Dead rows added)
- Newly-discovered Missing rows (if any) added during browser pass
- full.sh exit 0 confirmation
- Phase 33 success criteria #1–5 satisfaction matrix
- Hand-off note: STATE.md "Key Decisions (v5.0)" should gain Q1–Q4 verdict entries (out of scope for Phase 33; downstream Phase 34/35/38/39 plans will reference verdicts via DISC-AUDIT-NN row ids)
- Phase 34 unblock: "audit must not reveal scope-reducing findings before migration work begins" per ROADMAP §Phase 34 line 146 — confirm whether Q2/Q3 verdicts reveal any scope-reducing findings; if NO, Phase 34 unblocked.
</output>
</output>
</content>
</invoke>