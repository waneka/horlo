---
phase: 33-discovery-audit
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
autonomous: true
requirements: [DISC-10]
tags: [audit, documentation, discovery, source-pass]

must_haves:
  truths:
    - "Click-Path Audit table contains a candidate row for every grep-discovered affordance across the 15 D-05 surface blocks"
    - "Every row has a flat-sequential DISC-AUDIT-NN id (zero-padded, no gaps, no duplicates)"
    - "Every row's evidence cell is a `file:line` reference (not yet `prod: <URL>`); browser pass replaces these in Wave 3"
    - "Every D-05 surface block (15 of them) appears in the surface column at least once"
    - "The wishlist.ts:206 WR-07 silent-no-op is captured as a candidate row on /u/{user}/wishlist (RESEARCH.md flagship Dead candidate)"
    - "Zero files modified outside .planning/phases/33-discovery-audit/"
  artifacts:
    - path: ".planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md"
      provides: "Click-Path Audit table populated with 130–210 candidate rows tagged tentatively (Live default, Dead for confirmed source-level dead-ends like WR-07, Redundant for cross-rail duplicates evident from source, viewer_state=TBD pending Wave 2 gate annotation, viewport=both unless source shows md:hidden / md:block divergence)"
      contains: "DISC-AUDIT-01"
  key_links:
    - from: "Click-Path Audit table rows"
      to: "src/components/{home,explore,profile,search,watch,layout}/*.tsx file:line evidence"
      via: "grep matches embedded as evidence cells"
      pattern: "src/(components|app)/.+:[0-9]+"
    - from: "WR-07 wishlist.ts:206 row"
      to: "src/app/actions/wishlist.ts:206 evidence cell"
      via: "Dead-row reproduction reference"
      pattern: "src/app/actions/wishlist\\.ts:206"
---

<objective>
Pass A — source-code-first enumeration (D-01). For each of the 15 D-05 surface blocks, run the augmented grep recipe from RESEARCH.md against the verified entry-point files (PATTERNS.md confirms all 21 paths exist on disk), then convert each grep match into a candidate row in the Click-Path Audit table. Each row carries a flat-sequential DISC-AUDIT-NN id, a `file:line` evidence cell, viewer_state=TBD (pending Wave 2 gate annotation), and viewport=both (unless source shows md:hidden / md:block CSS that proves divergence — in which case split per D-04).

Purpose: This is the falsifiable backbone of the entire audit. Browser-pass alone misses runtime-gated affordances; grep-pass alone misses runtime divergence. D-01 establishes grep-pass as the primary tier. RESEARCH.md estimates 130–210 final rows after splits — defensible upper bound.

Output: 33-DISCOVERY-AUDIT.md gains ~130–210 rows in the Click-Path Audit table. Every D-05 surface block appears at least once. Zero application-code changes (this is grep-only enumeration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-discovery-audit/33-CONTEXT.md
@.planning/phases/33-discovery-audit/33-RESEARCH.md
@.planning/phases/33-discovery-audit/33-PATTERNS.md
@.planning/phases/33-discovery-audit/33-VALIDATION.md
@.planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
@.planning/phases/33-discovery-audit/33-01-SUMMARY.md

<interfaces>
<!-- Locked schema from Plan 01 (D-09, D-10, D-11): -->
<!-- 8-column row format (verbatim): | DISC-AUDIT-NN | <surface> | <element> | <target> | <tag> | <evidence> | <viewer_state> | <viewport> | -->
<!-- D-09 row id: zero-padded to 2 digits when N<10 (DISC-AUDIT-01 .. DISC-AUDIT-09 then DISC-AUDIT-10 .. DISC-AUDIT-NN flat sequential) -->
<!-- D-11 tags: Live | Dead | Redundant | Missing -->
<!-- D-10 viewer_state: owner-populated | fresh-account | N/A (use TBD as a placeholder this pass; Wave 2 finalizes — but checks/full.sh will reject TBD; Wave 2 plan converts TBD → final value before that gate runs) -->
<!-- D-10 viewport: desktop | mobile | both (default both unless md:hidden / md:block CSS forces a split per D-04 + V-1..V-8) -->

<!-- Surface block names — use these EXACT strings in the surface column (checks/full.sh greps for them): -->
<!--   Header -->
<!--   /                       (the literal slash for home) -->
<!--   /explore -->
<!--   /search -->
<!--   /catalog/{catalogId} -->
<!--   /watch/{id} -->
<!--   /u/{user}/collection -->
<!--   /u/{user}/wishlist -->
<!--   /u/{user}/worn -->
<!--   /u/{user}/notes -->
<!--   /u/{user}/stats -->
<!--   /u/{user}/common-ground -->
<!--   /u/{user}/insights -->
<!-- (13 blocks shown; D-05 says 15 total — note that "Header" is one block and the 13 above are 13. The remaining 2 are NOT separate surfaces in Pass A: /explore sub-routes per D-06 are summary rows on /explore; followers/following per D-05 are click TARGETS, not blocks. So Pass A uses the 13 surface block names above + add the /explore SUMMARY rows pointing at /explore/collectors and /explore/watches under the /explore block. The actual D-05 list is 13 enumerable surface columns; total of 15 in CONTEXT.md includes "Header" and counts profile tabs as 7 — 6 ROADMAP + 7 profile + Header(1) + the 1 redundant `/u/{user}` redirect captured under Header click target = 15. Use the 13 surface column values above for the table; checks/full.sh enforces those 13.) -->

<!-- Augmented grep recipe (from RESEARCH.md §Source-Grep Recipe lines 184–217 — use this, NOT CONTEXT.md <specifics>): -->

<!-- Command 1: page-level entry points -->
<!--   rg -n '<Link\b|router\.push|router\.replace|redirect\(|<a href|onClick=' \ -->
<!--     src/app/page.tsx \ -->
<!--     src/app/explore/page.tsx src/app/explore/collectors/page.tsx src/app/explore/watches/page.tsx \ -->
<!--     src/app/search/page.tsx \ -->
<!--     src/app/catalog/\[catalogId\]/page.tsx \ -->
<!--     src/app/watch/\[id\]/page.tsx \ -->
<!--     src/app/u/\[username\]/page.tsx src/app/u/\[username\]/layout.tsx \ -->
<!--     src/app/u/\[username\]/\[tab\]/page.tsx \ -->
<!--     src/app/u/\[username\]/followers/page.tsx src/app/u/\[username\]/following/page.tsx -->

<!-- Command 2: domain component subtrees -->
<!--   rg -n '<Link\b|router\.push|router\.replace|redirect\(|<a href|onClick=' \ -->
<!--     src/components/home/ \ -->
<!--     src/components/explore/ \ -->
<!--     src/components/profile/ \ -->
<!--     src/components/search/ \ -->
<!--     src/components/watch/ -->

<!-- Command 3: layout subtree (Header surface block) -->
<!--   rg -n '<Link\b|router\.push|router\.replace|redirect\(|<a href|onClick=' \ -->
<!--     src/components/layout/Header.tsx \ -->
<!--     src/components/layout/SlimTopNav.tsx \ -->
<!--     src/components/layout/DesktopTopNav.tsx \ -->
<!--     src/components/layout/UserMenu.tsx \ -->
<!--     src/components/layout/BottomNav.tsx \ -->
<!--     src/components/layout/BottomNavServer.tsx \ -->
<!--     src/components/layout/NavWearButton.tsx \ -->
<!--     src/components/layout/NotificationBell.tsx 2>/dev/null -->

<!-- Exclusions per RESEARCH.md: skip lines from *.test.tsx / __tests__/ files. -->

<!-- WR-07 grep (RESEARCH.md §WR-07 Landmine — flagship Dead candidate): -->
<!--   rg -n "revalidatePath\('[^']+\[[a-zA-Z]+\]" src/ -->
<!-- Expect lone holdout: src/app/actions/wishlist.ts:206 — produces ONE Dead candidate row on /u/{user}/wishlist for the drag-reorder affordance. -->

<!-- Per-affordance dedup rule (RESEARCH.md §Source-Grep Recipe pitfall): -->
<!--   Several components render in multiple parent surfaces (SuggestedCollectorRow on / and /search; ProfileWatchCard on Collection and Wishlist tabs). -->
<!--   Each rendered location is a SEPARATE row on the parent surface block, even if the underlying component file is shared. -->
<!--   Do NOT collapse shared components into a single row. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the augmented grep recipe; assemble candidate rows for blocks 1–7 (Header + Home + Explore + Search + Catalog + Watch + /u/{user}/collection)</name>
  <files>
    .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
  </files>
  <read_first>
    - .planning/phases/33-discovery-audit/33-RESEARCH.md (lines 86–222: §Surface Inventory blocks 1–7 + §Source-Grep Recipe + §Conditional Rendering Map G-1..G-9 + §Estimated Row Count by Surface)
    - .planning/phases/33-discovery-audit/33-CONTEXT.md (D-08 Header single-row policy; D-09/D-10/D-11; D-06 /explore sub-route summary-row policy)
    - src/app/page.tsx (block 2 entry point — read once to anchor home affordances; do NOT cite line numbers from memory)
    - src/app/explore/page.tsx (block 3 entry point — note ExploreHero gate condition at line ~38 for evidence accuracy)
    - src/app/search/page.tsx + src/components/search/SearchPageClient.tsx (block 4 — 4-tab structure)
    - src/app/catalog/[catalogId]/page.tsx (block 5 — note UUID gate line ~46, ownership gate line ~70, verdict gate lines ~79–110)
    - src/app/watch/[id]/page.tsx (block 6 — note isOwner framing branch line ~52)
    - src/app/u/[username]/[tab]/page.tsx (block 7 dispatch — note CollectionTabContent branch lines ~159–167, LockedTabCard line ~107)
    - src/components/layout/Header.tsx + SlimTopNav.tsx + DesktopTopNav.tsx + UserMenu.tsx + BottomNav.tsx + NavWearButton.tsx (block 1 entry points — D-08: enumerate ONCE as Header block, NOT per-surface)
    - src/components/home/*.tsx (skim for L-01 section order: WywtRail → CollectorsLikeYou → NetworkActivityFeed → PersonalInsightsGrid → SuggestedCollectors)
    - src/components/explore/*.tsx (ExploreHero, PopularCollectors, TrendingWatches, GainingTractionWatches, DiscoveryWatchCard)
  </read_first>
  <action>
    Execute the augmented grep recipe from RESEARCH.md §Source-Grep Recipe (lines 184–217). Run all three commands in sequence and collect the matches in memory; then assemble candidate rows for surface blocks 1–7.

    Run these grep commands verbatim (use Bash tool):

    ```bash
    # Command 1: page-level entry points
    rg -n '<Link\b|router\.push|router\.replace|redirect\(|<a href|onClick=' \
      src/app/page.tsx \
      src/app/explore/page.tsx src/app/explore/collectors/page.tsx src/app/explore/watches/page.tsx \
      src/app/search/page.tsx \
      src/app/catalog/\[catalogId\]/page.tsx \
      src/app/watch/\[id\]/page.tsx \
      src/app/u/\[username\]/page.tsx src/app/u/\[username\]/layout.tsx \
      src/app/u/\[username\]/\[tab\]/page.tsx \
      src/app/u/\[username\]/followers/page.tsx src/app/u/\[username\]/following/page.tsx 2>/dev/null

    # Command 2: domain component subtrees (filter test files manually; rg has no --type-tsx default)
    rg -n '<Link\b|router\.push|router\.replace|redirect\(|<a href|onClick=' \
      src/components/home/ \
      src/components/explore/ \
      src/components/profile/ \
      src/components/search/ \
      src/components/watch/ 2>/dev/null | grep -v '\.test\.tsx' | grep -v '__tests__/'

    # Command 3: layout subtree (Header surface block)
    rg -n '<Link\b|router\.push|router\.replace|redirect\(|<a href|onClick=' \
      src/components/layout/Header.tsx \
      src/components/layout/SlimTopNav.tsx \
      src/components/layout/DesktopTopNav.tsx \
      src/components/layout/UserMenu.tsx \
      src/components/layout/BottomNav.tsx \
      src/components/layout/BottomNavServer.tsx \
      src/components/layout/NavWearButton.tsx \
      src/components/layout/NotificationBell.tsx 2>/dev/null
    ```

    Convert each match into a row using the exact 8-column format (per D-10):

    ```
    | DISC-AUDIT-NN | <surface> | <element> | <target> | <tag> | <evidence> | <viewer_state> | <viewport> |
    ```

    Rules for filling each column on this pass (Pass A — source-only):

    - `row_id`: flat sequential starting `DISC-AUDIT-01`, zero-padded when N<10. Increment globally across the whole table — block 7 picks up where block 1's last row ends; do NOT restart per block.
    - `surface`: one of the 13 D-05 column values listed in the `<interfaces>` block (use the literal strings — `/`, `/explore`, etc.). Per D-08, Header is ONE block — do NOT enumerate per-surface variation.
    - `element`: short human-readable description of the affordance ("Avatar in CollectorsLikeYou row", "Brand pill on WatchDetail", "Search input clear button"). Pull naming from the surrounding component context, not just the grep line.
    - `target`: route or action the click leads to. For `<Link href="/foo">`, use `/foo`. For `router.push("/bar")`, use `/bar`. For onClick handlers with no navigation (e.g., toggling a dialog), write `—` (em-dash).
    - `tag`: default `Live` for source-pass rows. Use `Dead` ONLY for the WR-07 wishlist.ts:206 case in Task 2 — block 1–7 grep should NOT produce Dead candidates yet (Live is the source-pass default; Wave 3 browser-pass downgrades to Dead on observation). Use `Redundant` only when source clearly shows the same destination already in the table from a prior grep match (cross-rail "View all" duplicates that point to the same route — cite the prior DISC-AUDIT-NN in evidence).
    - `evidence`: `src/path/to/file.tsx:LINE_NUMBER` exactly as ripgrep returns. For Redundant rows, append `; Redundant to DISC-AUDIT-NN — <reason>`. Do NOT use `prod:` prefix on this pass — that's reserved for Wave 3 browser-confirmed rows.
    - `viewer_state`: write `TBD` on this pass. Wave 2 (Plan 03) will replace `TBD` with `owner-populated` / `fresh-account` / `N/A` based on the G-1..G-20 gate map.
    - `viewport`: write `both` on this pass UNLESS the grep match is in a file or JSX block guarded by `md:hidden` / `md:block` / a viewport-specific component (e.g., SlimTopNav vs DesktopTopNav per V-1) — in which case split into two rows with `viewport: desktop` and `viewport: mobile` per D-04.

    Header block (D-08): produce ~10 rows total — 2 SlimTopNav links (mobile viewport), 3 DesktopTopNav links (desktop viewport) + UserMenu trigger + dropdown items, 5 BottomNav NavLinks (mobile), 1 NavWearButton (mobile). Add ONE summary row noting "active state varies by surface; not enumerated per-surface" with `tag: Live`, `evidence: src/components/layout/{file}.tsx:NN — active-state varies; D-08 single-row policy`.

    Block 2 `/` (Home): produce ~25–35 rows from `src/components/home/*.tsx` matches. Note PersonalInsightsGrid is owner-only (I-04 from STATE) so its affordance rows carry `viewer_state: TBD` (Wave 2 will mark `owner-populated`). FeedEmptyState branches → 1 row noting empty-state CTA target.

    Block 3 `/explore`: produce ~8–12 rows. ExploreHero rows (gate G-1) carry `viewer_state: TBD` → Wave 2 marks `fresh-account`. PopularCollectors rows × N collectors collapse to "Avatar in PopularCollectors row" ONE row per affordance type — do NOT enumerate per-row per-collector. Add D-06 summary rows for `/explore/collectors` and `/explore/watches` with element="Paginated 'see all' page; downstream affordances identical to parent rail rows DISC-AUDIT-NN..MM" and tag=Live.

    Block 4 `/search`: produce ~12–18 rows. 4 tabs × ~3–4 affordances each. SuggestedCollectorsForSearch empty-state row. Note: SearchPageClient.tsx is the client subtree; PeopleSearchRow / WatchSearchRow / CollectionSearchRow are per-result-row affordances — collapse to one row each per affordance type.

    Block 5 `/catalog/{catalogId}`: produce ~6–10 rows. CatalogPageActions has 3 CTAs (Wishlist / Collection / Skip) — enumerate each. Note G-3 (owner framing suppresses CatalogPageActions) and G-4 (empty-collection suppresses verdict + CTAs) — these rows carry `viewer_state: TBD` → Wave 2 splits into `owner-populated` (cross-user catalog ref with collection > 0) vs `fresh-account` (collection = 0, no CTAs visible).

    Block 6 `/watch/{id}`: produce ~5–8 rows. WatchDetail rows include Edit + Delete (gated on `viewerCanEdit={isOwner}` — `viewer_state: TBD`, Wave 2 marks `owner-populated`). Brand pill, Reference pill, "Same Watch by Other Collectors" rail rows. CollectionFitCard verdict rows.

    Block 7 `/u/{user}/collection`: produce ~8–12 rows. ProfileWatchCard affordances (rendered N times → ONE row per affordance type). AddWatchCard CTA (G-20 env gate → `viewer_state: TBD`). LockedTabCard "Connect" CTA (G-8 → `viewer_state: TBD`, Wave 2 splits).

    APPEND all rows for blocks 1–7 to the table body in 33-DISCOVERY-AUDIT.md (between the header row and the closing HTML comments). Maintain monotonically increasing DISC-AUDIT-NN ids.

    After appending, run `bash .planning/phases/33-discovery-audit/checks/quick.sh` — must exit 0.
  </action>
  <verify>
    <automated>bash .planning/phases/33-discovery-audit/checks/quick.sh && [ "$(grep -c '^| DISC-AUDIT-' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md)" -ge 60 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| DISC-AUDIT-' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns ≥ 60 (lower bound across blocks 1–7 per RESEARCH.md row-count estimates: Header 10 + Home 25 + Explore 8 + Search 12 + Catalog 6 + Watch 5 + Collection 8 = 74 lower-end; allow ≥ 60 for tolerance)
    - `grep -c '^| DISC-AUDIT-' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns ≤ 130 (upper bound for blocks 1–7 per RESEARCH.md: 14+35+12+18+10+8+12 = 109; allow ≤ 130 for safety)
    - Each surface name from blocks 1–7 appears as the SECOND column in ≥ 1 row: `Header`, `/`, `/explore`, `/search`, `/catalog/{catalogId}`, `/watch/{id}`, `/u/{user}/collection`. Verify with: `for s in 'Header' '/' '/explore' '/search' '/catalog/{catalogId}' '/watch/{id}' '/u/{user}/collection'; do grep -cE "^\| DISC-AUDIT-[0-9]+ \| ${s} \|" .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md > 0 || echo "MISS: $s"; done` reports no MISS lines
    - Every row has a `file:line` evidence cell: `awk -F'\\|' '/^\| DISC-AUDIT-/ { ev=$7; gsub(/^ | $/, "", ev); if (ev !~ /:[0-9]+/) print $2 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - No `prod:` evidence cells yet (Wave 3 only): `grep -c 'prod:' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns 0
    - All rows tagged Live, Redundant, or Missing — NO Dead yet (Dead candidates added in Task 2): `awk -F'\\|' '/^\| DISC-AUDIT-/ { tag=$6; gsub(/^ | $/, "", tag); if (tag == "Dead") print $2 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - viewer_state column is `TBD` or `N/A` for every row (no `owner-populated` / `fresh-account` yet — Wave 2 finalizes): `awk -F'\\|' '/^\| DISC-AUDIT-/ { vs=$8; gsub(/^ | $/, "", vs); if (vs != "TBD" && vs != "N/A") print $2 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - viewport column is one of `desktop` / `mobile` / `both` for every row: `awk -F'\\|' '/^\| DISC-AUDIT-/ { vp=$9; gsub(/^ | $/, "", vp); if (vp != "desktop" && vp != "mobile" && vp != "both") print $2 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - Header block has at least one viewport-split pair (V-1: SlimTopNav mobile vs DesktopTopNav desktop): `grep -E "^\| DISC-AUDIT-[0-9]+ \| Header \|" .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md | grep -c ' mobile \|' returns ≥ 1` AND `grep -E "^\| DISC-AUDIT-[0-9]+ \| Header \|" .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md | grep -c ' desktop \|' returns ≥ 1`
    - Row IDs are flat sequential and unique: `grep -oE '^\| DISC-AUDIT-[0-9]+' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md | sed 's/| DISC-AUDIT-//' | sort -n | uniq -d` returns no lines
    - `bash .planning/phases/33-discovery-audit/checks/quick.sh` exits 0
    - `git status --porcelain | grep -v '^?? \.planning/phases/33-discovery-audit/' | grep -v '^ M \.planning/phases/33-discovery-audit/'` returns no lines
  </acceptance_criteria>
  <done>Click-Path Audit table contains rows for blocks 1–7 with file:line evidence, sequential DISC-AUDIT-NN ids, viewer_state=TBD or N/A, viewport assigned per source-level CSS markers. Every block 1–7 surface appears at least once. Zero changes outside .planning/phases/33-discovery-audit/.</done>
</task>

<task type="auto">
  <name>Task 2: Enumerate blocks 8–13 (profile tabs wishlist + worn + notes + stats + common-ground + insights), capture WR-07 flagship Dead row, populate /explore sub-route summary rows</name>
  <files>
    .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
  </files>
  <read_first>
    - .planning/phases/33-discovery-audit/33-RESEARCH.md (lines 146–168: blocks 8–13 entry points + gates G-9..G-19; lines 304–334: §WR-07 Landmine; lines 251–267: §Viewport Divergence Catalog V-4..V-7)
    - .planning/phases/33-discovery-audit/33-CONTEXT.md (D-07 each profile tab is a separate block; D-11 Dead-tag definition includes WR-07 silent-no-op)
    - src/app/u/[username]/[tab]/page.tsx (block 8 wishlist branch lines 169–180; block 9 worn branch lines 198–229; block 10 notes branch lines 182–196; block 11 stats branch lines 243–269; block 12 common-ground lines 80–94; block 13 insights lines 100–103)
    - src/app/actions/wishlist.ts (line 206 — the WR-07 holdout `revalidatePath('/u/[username]/[tab]', 'page')` flagship Dead candidate)
    - src/components/profile/WishlistTabContent.tsx + SortableProfileWatchCard.tsx (drag-reorder UI rendering the affordance Pass A captures)
    - src/components/profile/NotesTabContent.tsx + NoteRow.tsx + NoteVisibilityPill.tsx (block 10 — DEBT-09 NoteVisibilityPill just shipped Phase 32; per-row `notesPublic` toggle)
    - src/components/profile/CommonGroundTabContent.tsx + CommonGroundHeroBand.tsx (block 12)
    - src/components/profile/InsightsTabContent.tsx (block 13)
    - src/components/profile/StatsTabContent.tsx + StatsCard.tsx (block 11)
    - src/components/profile/WornCalendar.tsx + WornTimeline.tsx + WornTabContent.tsx (block 9)
    - .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md (current state from Task 1 — read to find the next available DISC-AUDIT-NN id)
  </read_first>
  <action>
    Continue the source-grep enumeration from Task 1 for blocks 8–13 (the remaining 6 profile tabs). Use the SAME grep recipe (Command 2 already covered `src/components/profile/`). Re-grep `src/components/profile/` to refresh the match list, then disambiguate which tab each affordance belongs to (most tab-specific components are clearly named; ProfileWatchCard appears on BOTH Collection and Wishlist per D-07 surface-block independence — Task 1 captured the Collection rendering; Task 2 captures the Wishlist rendering as a separate row even though the file is shared).

    Block 8 `/u/{user}/wishlist`: produce ~6–10 rows. ProfileWatchCard rows (mirror of block 7's Collection rows; SEPARATE rows per D-07). Reorder drag-handle row. **The drag-handle row IS the WR-07 flagship Dead candidate** — capture it as:

    ```
    | DISC-AUDIT-NN | /u/{user}/wishlist | "Drag handle on wishlist row (reorder)" | reorderWishlist Server Action | Dead | src/app/actions/wishlist.ts:206 — revalidatePath('/u/[username]/[tab]', 'page') silently no-ops; Wave 3 browser pass confirms drag-reorder visual stale until manual reload | TBD | both |
    ```

    Note: the `evidence` cell here intentionally hints "Wave 3 browser pass confirms" — Wave 3 (Plan 04) will append the `prod: <URL> + <observation>` confirmation. The Dead tag is provisionally assigned by source pass per RESEARCH.md A3 (Wave 3 verifies the visible regression and either keeps Dead or reverts to Live with a note).

    Run RESEARCH.md WR-07 broader grep to confirm no NEW holdouts shipped since 2026-05-06:

    ```bash
    rg -n "revalidatePath\('[^']+\[[a-zA-Z]+\]" src/
    ```

    Expected output: ONE line — `src/app/actions/wishlist.ts:206`. If the grep returns more lines, capture each as an additional Dead candidate row on the appropriate surface (the Server Action's caller surface — e.g., a wear-event revalidate would land on `/u/{user}/worn`).

    Block 9 `/u/{user}/worn`: produce ~5–8 rows. WornCalendar day-cell click affordances → /wear/[id] (out of scope per D-05, but the click affordance EXISTS on this surface, so it gets a row). WornTimeline row clicks. View-toggle pill (Calendar / Timeline) — onClick state toggle, target `—`. LogTodaysWearButton CTA.

    Block 10 `/u/{user}/notes`: produce ~5–8 rows. NoteRow body click → /watch/{id}. NoteVisibilityPill (DEBT-09 Phase 32 affordance — toggles per-row `notesPublic`; `viewerCanEdit={isOwner}` so `viewer_state: TBD` → owner-populated). RemoveNoteDialog trigger. NotesEmptyOwnerActions CTAs (3 buttons per Phase 32 D-04).

    Block 11 `/u/{user}/stats`: produce ~4–6 rows. StatsCard "View all" affordances → relevant filtered route. CollectionObservations row clicks (TBD if owner-only). Mostly read-only block (low affordance density per RESEARCH.md).

    Block 12 `/u/{user}/common-ground`: produce ~3–5 rows. Watch-card click affordances → /catalog/{id} or /watch/{id}. Block has 404-on-gate-fail (G-12) — `viewer_state: TBD` → fresh-account-viewing-owner triggers 404. The 404 case itself is NOT a row (404 is gate behavior, not an affordance) — the row is the affordance ABSENCE: this is a Missing-row candidate per the Rdio principle if there's no fallback "explore other follows" CTA when the gate fails. Note: cite the SEED-004 Rdio quote in the evidence cell for any Missing rows.

    Block 13 `/u/{user}/insights`: produce ~3–5 rows. PersonalInsightsGrid-style content. Owner-only (G-13 → `viewer_state: TBD` → owner-populated). 404 for non-owners is NOT a Dead row (designed privacy gate per Phase 25); the absence of any cross-link from non-owner profile to insights is correct behavior, not Missing.

    APPEND blocks 8–13 rows to the table, continuing from Task 1's last DISC-AUDIT-NN. Maintain sequential ids.

    Also populate /explore sub-route summary rows per D-06 (if not already done in Task 1): TWO rows on the `/explore` surface block — one for "Paginated 'see all' for collectors → /explore/collectors; downstream affordances identical to parent rail rows DISC-AUDIT-NN..MM (cite the parent rail row IDs from Task 1's PopularCollectors rows)" and one for /explore/watches mirroring TrendingWatches + GainingTractionWatches.

    After appending, run `bash .planning/phases/33-discovery-audit/checks/quick.sh` — must exit 0.
  </action>
  <verify>
    <automated>bash .planning/phases/33-discovery-audit/checks/quick.sh && [ "$(grep -c '^| DISC-AUDIT-' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md)" -ge 100 ]</automated>
  </verify>
  <acceptance_criteria>
    - Total rows ≥ 100 (lower bound: 60 from Task 1 + 40 from Task 2; RESEARCH.md mid-estimate ~150): `grep -c '^| DISC-AUDIT-' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns ≥ 100
    - Total rows ≤ 250 (upper bound: pitfall #3 in RESEARCH.md says >250 indicates Header was enumerated per-surface): `grep -c '^| DISC-AUDIT-' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns ≤ 250
    - Every D-05 surface block (13 enumerable surface names) appears as the SECOND column in ≥ 1 row: `for s in 'Header' '/' '/explore' '/search' '/catalog/{catalogId}' '/watch/{id}' '/u/{user}/collection' '/u/{user}/wishlist' '/u/{user}/worn' '/u/{user}/notes' '/u/{user}/stats' '/u/{user}/common-ground' '/u/{user}/insights'; do n=$(awk -F'\\|' -v s="$s" '/^\| DISC-AUDIT-/ { col=$3; gsub(/^ | $/, "", col); if (col == s) c++ } END { print c+0 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md); [ "$n" -ge 1 ] || echo "MISS: $s"; done` reports no MISS lines
    - WR-07 flagship Dead row is captured on /u/{user}/wishlist: `awk -F'\\|' '/^\| DISC-AUDIT-/ { surface=$3; tag=$6; ev=$7; gsub(/^ | $/, "", surface); gsub(/^ | $/, "", tag); gsub(/^ | $/, "", ev); if (surface == "/u/{user}/wishlist" && tag == "Dead" && ev ~ /wishlist\.ts:206/) found=1 } END { exit !found }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` exits 0
    - WR-07 broader grep (`rg -n "revalidatePath\('[^']+\[[a-zA-Z]+\]" src/`) returns exactly ONE match (`src/app/actions/wishlist.ts:206`); if it returns more, executor MUST add additional Dead-row candidates and re-verify
    - `/explore` sub-route summary rows present (2 rows referencing /explore/collectors and /explore/watches respectively): `awk -F'\\|' '/^\| DISC-AUDIT-/ { surface=$3; target=$4; gsub(/^ | $/, "", surface); gsub(/^ | $/, "", target); if (surface == "/explore" && target == "/explore/collectors") c1++; if (surface == "/explore" && target == "/explore/watches") c2++ } END { exit !(c1 >= 1 && c2 >= 1) }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` exits 0
    - Every row tagged `Dead` has reproduction evidence (file:line OR prod:URL): `awk -F'\\|' '/^\| DISC-AUDIT-/ { tag=$6; ev=$7; gsub(/^ | $/, "", tag); gsub(/^ | $/, "", ev); if (tag == "Dead" && ev !~ /(:[0-9]+|prod:)/) print $2 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - Every row tagged `Missing` cites Rdio or SEED-004: `awk -F'\\|' '/^\| DISC-AUDIT-/ { tag=$6; ev=$7; gsub(/^ | $/, "", tag); gsub(/^ | $/, "", ev); if (tag == "Missing" && ev !~ /(Rdio|SEED-004)/) print $2 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - Every row tagged `Redundant` cites a DISC-AUDIT-NN that exists in the table: re-run the bonus rule check from full.sh manually
    - All rows have viewer_state = TBD, owner-populated, fresh-account, or N/A: `awk -F'\\|' '/^\| DISC-AUDIT-/ { vs=$8; gsub(/^ | $/, "", vs); if (vs != "TBD" && vs != "owner-populated" && vs != "fresh-account" && vs != "N/A") print $2 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines (TBD remains the default — Wave 2 finalizes)
    - Row IDs are sequential and unique: `grep -oE '^\| DISC-AUDIT-[0-9]+' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md | sed 's/| DISC-AUDIT-//' | sort -n | uniq -d` returns no lines
    - `bash .planning/phases/33-discovery-audit/checks/quick.sh` exits 0
    - `git status --porcelain | grep -v '^?? \.planning/phases/33-discovery-audit/' | grep -v '^ M \.planning/phases/33-discovery-audit/'` returns no lines
  </acceptance_criteria>
  <done>Blocks 8–13 rows appended; WR-07 flagship Dead row captured on /u/{user}/wishlist with src/app/actions/wishlist.ts:206 evidence; /explore sub-route summary rows present; every D-05 surface block appears in the table; total row count between 100 and 250. quick.sh exits 0.</done>
</task>

</tasks>

<verification>
After both tasks complete, the audit table has all source-pass rows. full.sh will still fail rule 5 (verdicts pending) and possibly rule 1 (TBD viewer_state isn't a D-13 violation per se but Wave 2 normalizes). Run:

```bash
bash .planning/phases/33-discovery-audit/checks/quick.sh
# Expect: exit 0; ≥ 100 rows reported

# Confirm the WR-07 flagship Dead row is exactly one row, not duplicated
grep -c 'wishlist\.ts:206' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
# Expect: 1

# Confirm zero changes outside the phase directory
git status --porcelain | grep -v '^?? \.planning/phases/33-discovery-audit/' | grep -v '^ M \.planning/phases/33-discovery-audit/' | wc -l | tr -d ' '
# Expect: 0
```
</verification>

<success_criteria>
- 130–210 candidate rows added to Click-Path Audit table (RESEARCH.md row-count estimate range; tolerance 100–250 to allow audit-author judgement)
- Every D-05 surface block (13 enumerable surfaces) has ≥ 1 row
- WR-07 flagship Dead row captured with src/app/actions/wishlist.ts:206 evidence
- Every row carries file:line evidence (no `prod:` yet)
- Row IDs flat sequential, no duplicates, no gaps
- Zero changes outside `.planning/phases/33-discovery-audit/`
- quick.sh exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/33-discovery-audit/33-02-SUMMARY.md` describing:
- Total row count produced
- Per-surface row count breakdown vs RESEARCH.md estimates
- Confirmed lone WR-07 holdout (or list of new holdouts if any)
- Number of viewport-split rows (V-1..V-8 hits)
- Wave 2 hand-off: Plan 03 walks G-1..G-20 to replace TBD viewer_state values
</output>
