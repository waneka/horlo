---
phase: 71
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/static/AddWatchFlow.no-verdict-step.test.ts
  - tests/static/AddWatchFlow.no-collection-fit-card.test.ts
  - package.json
autonomous: true
requirements:
  - CLNP-02
  - CLNP-03
tags:
  - static-guards
  - dead-code-cleanup
  - vitest-environment-node
  - prebuild
must_haves:
  truths:
    - "Per D-02, both new guards use the imports-only regex `from ['\"](?:.*\\/)?ComponentName['\"]` pattern mirroring tests/static/CollectionFitCard.no-engine.test.ts; both ship with `// @vitest-environment node` as line 1 (Memory project_vitest_static_node_env.md — without it, Vercel prebuild externalizes node:fs and readFileSync becomes undefined, repeating the Phase 59 prod-deploy failure)."
    - "Per D-04, CLNP-03 enumerates exactly 8 add-flow files (AddWatchFlow.tsx, SearchEntry.tsx, StructuredEntryPanel.tsx, ConfirmStep.tsx, DupeBanner.tsx, WatchForm.tsx, WatchPhotoStep.tsx, ExtractErrorCard.tsx) in a module-scope ADD_FLOW_FILES const and asserts each individually."
    - "Per D-05, this plan ships guards BEFORE Plan 71-02 deletes any source files; the guards pass vacuously today because AddWatchFlow.tsx already has zero imports of the dead components (RESEARCH §5 confirmed zero non-test consumers), so 71-02's deletes land with active enforcement in the same PR."
    - "Per RESEARCH §'Static Guard Prebuild Wiring' and Pitfall 4, `package.json` `scripts.prebuild` is extended from `vitest run tests/static/legacy-watch-routes.test.ts` to `vitest run tests/static/` so the Vercel build gate covers BOTH new guards (and every other static guard). Build is the authoritative gate per Memory project_baseline_not_green_build_is_gate.md — full `tsc --noEmit` and `vitest run` carry pre-existing baseline noise."
    - "Per D-01 forward-coordination (the RecentlyEvaluatedRail delete + RailEntry/PendingTarget sweep), CLNP-04 enforcement happens in Plan 71-02; this plan does not touch RecentlyEvaluatedRail or flowTypes.ts."
    - "Per D-03 forward-coordination (AddWatchFlow.tsx JSDoc reword), Plan 71-02 is the consumer that rewords the JSDoc to name `tests/static/AddWatchFlow.no-verdict-step.test.ts` as the enforcement mechanism shipped here."
  artifacts:
    - path: "tests/static/AddWatchFlow.no-verdict-step.test.ts"
      provides: "CLNP-02 static guard — imports-only regex for VerdictStep, WishlistRationalePanel, PasteSection in src/components/watch/AddWatchFlow.tsx"
      contains: "// @vitest-environment node"
      min_lines: 30
    - path: "tests/static/AddWatchFlow.no-collection-fit-card.test.ts"
      provides: "CLNP-03 static guard — per-file loop over 8 ADD_FLOW_FILES checking no CollectionFitCard import"
      contains: "// @vitest-environment node"
      min_lines: 30
    - path: "package.json"
      provides: "prebuild script extended to run all tests/static/ guards on Vercel build"
      contains: "vitest run tests/static/"
  key_links:
    - from: "package.json scripts.prebuild"
      to: "tests/static/"
      via: "vitest run tests/static/"
      pattern: "vitest run tests/static/"
    - from: "tests/static/AddWatchFlow.no-verdict-step.test.ts"
      to: "src/components/watch/AddWatchFlow.tsx"
      via: "readFileSync + regex assertion"
      pattern: "AddWatchFlow.tsx"
    - from: "tests/static/AddWatchFlow.no-collection-fit-card.test.ts"
      to: "8 add-flow files"
      via: "for-loop over ADD_FLOW_FILES + readFileSync per file"
      pattern: "ADD_FLOW_FILES"
---

<objective>
Ship the two static guards that enforce CLNP-02 (no VerdictStep/WishlistRationalePanel/PasteSection imports in `AddWatchFlow.tsx`) and CLNP-03 (no `CollectionFitCard` imports across the 8-file add-flow tree), and wire the Vercel `prebuild` hook to run the full `tests/static/` directory. Guards pass vacuously today (RESEARCH §5 + §6 verified) so the deletes in Plan 71-02 land with active enforcement already in place.

Purpose: Prevent silent reintroduction of the verdict-flow surface (CLNP-02) and prevent fit analysis from contaminating the data-entry add-flow (CLNP-03). Vercel prebuild wiring (RESEARCH §"Static Guard — Prebuild Wiring") closes the gap where new guards would run via `npm test` but not via `next build`.

Output: Two new test files in `tests/static/` + a one-line edit to `package.json`'s `prebuild` script. Zero source-code changes; zero behavior changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-dead-code-cleanup-static-guards/71-CONTEXT.md
@.planning/phases/71-dead-code-cleanup-static-guards/71-RESEARCH.md
@.planning/phases/71-dead-code-cleanup-static-guards/71-PATTERNS.md
@.planning/phases/71-dead-code-cleanup-static-guards/71-VALIDATION.md

# Binding precedents — copy structure and directive verbatim
@tests/static/CollectionFitCard.no-engine.test.ts
@tests/static/legacy-watch-routes.test.ts

# Current state of files referenced by guards (existence check + vacuous-pass verification)
@src/components/watch/AddWatchFlow.tsx
@package.json

<interfaces>
<!-- Existing static guard pattern — copy regex shape + existsSync vacuous-pass guard from CollectionFitCard.no-engine.test.ts. -->
<!-- Copy `// @vitest-environment node` directive block (lines 1-7) verbatim from legacy-watch-routes.test.ts — it is the authoritative precedent. -->

From tests/static/CollectionFitCard.no-engine.test.ts:
- Imports: `import { describe, it, expect } from 'vitest'` and `import { existsSync, readFileSync } from 'node:fs'`
- Pattern: vacuous-pass guard `if (!existsSync(filePath)) return` inside each `it()` block
- Regex shape: `expect(src).not.toMatch(/from ['"](?:.*\/)?ComponentName['"]/)` for relative-path component imports
- Docstring header style: `"Phase NN <REQ-ID> + Pitfall N: <Subject> MUST NOT ..."`

From tests/static/legacy-watch-routes.test.ts (lines 1-7) — REQUIRED directive block:
- Line 1: `// @vitest-environment node` (no preceding blank line, no preceding comment)
- Lines 2-7: 6-line comment block explaining why node env is required (externalized node:fs in jsdom)
- Line 35: `import { readFileSync, readdirSync, statSync, existsSync } from 'node:fs'` (use `node:` prefix)
- Lines 136-153: per-file loop pattern over a collected array (structural template for CLNP-03's loop over ADD_FLOW_FILES)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLNP-02 static guard (no-verdict-step)</name>
  <files>tests/static/AddWatchFlow.no-verdict-step.test.ts</files>
  <read_first>
    - tests/static/CollectionFitCard.no-engine.test.ts (regex + existsSync vacuous-pass structural template)
    - tests/static/legacy-watch-routes.test.ts lines 1-7 (mandatory `// @vitest-environment node` directive block to copy verbatim)
    - src/components/watch/AddWatchFlow.tsx (target file the guard reads; verify zero VerdictStep/WishlistRationalePanel/PasteSection imports exist today so the new guard passes vacuously on first run)
    - .planning/phases/71-dead-code-cleanup-static-guards/71-PATTERNS.md §"tests/static/AddWatchFlow.no-verdict-step.test.ts (CLNP-02)" for the verbatim guard template
  </read_first>
  <action>
    Create `tests/static/AddWatchFlow.no-verdict-step.test.ts` per D-02 + PATTERNS.md §CLNP-02 verbatim template. Structure (in order):

    1. Line 1: `// @vitest-environment node` — NO preceding blank line, NO preceding any other content. Required by Memory project_vitest_static_node_env.md; without it Vercel prebuild externalizes `node:fs` and `readFileSync` becomes undefined (Phase 59 cost a failed prod deploy this way).
    2. Lines 2-7: copy the 6-line comment block from legacy-watch-routes.test.ts lines 2-7 verbatim (explains why node env is required — referenced for reviewer cognitive consistency with Phase 64 precedent).
    3. Imports: `import { describe, it, expect } from 'vitest'` then `import { existsSync, readFileSync } from 'node:fs'` (use `node:` prefix per CollectionFitCard precedent).
    4. JSDoc header: `Phase 71 CLNP-02 + Pitfall 1: AddWatchFlow.tsx MUST NOT import the deleted verdict-flow components (VerdictStep, WishlistRationalePanel, PasteSection). These files are deleted in Plan 71-02. This guard prevents reintroduction.` + blank comment line + `While AddWatchFlow.tsx does not exist, the test passes vacuously.`
    5. `describe('Phase 71 CLNP-02 — AddWatchFlow no-verdict-step invariant', () => { ... })` containing a module-scope `const flowPath = 'src/components/watch/AddWatchFlow.tsx'` and 3 `it()` blocks: one each for VerdictStep, WishlistRationalePanel, PasteSection.
    6. Each `it()` body: `if (!existsSync(flowPath)) return` then `const src = readFileSync(flowPath, 'utf8')` then `expect(src).not.toMatch(/from ['"](?:.*\/)?<ComponentName>['"]/)`. The `(?:.*\/)?` segment matches any optional leading path so `'./VerdictStep'`, `'../watch/VerdictStep'`, and `'@/components/watch/VerdictStep'` all match.

    Do NOT add any other assertions, fixtures, helpers, or imports. The guard targets import statements only (D-02) — JSDoc prose mentioning the dead component names in AddWatchFlow.tsx (lines 35-36, 52) is INTENTIONALLY not flagged because the imports-only regex skips it. The PATTERNS.md §"Complete CLNP-02 structure" block is the literal template.
  </action>
  <verify>
    <automated>npx vitest run tests/static/AddWatchFlow.no-verdict-step.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/static/AddWatchFlow.no-verdict-step.test.ts` exists.
    - `head -1 tests/static/AddWatchFlow.no-verdict-step.test.ts` outputs exactly `// @vitest-environment node` (NO leading whitespace, NO preceding line).
    - `grep -c "from 'node:fs'" tests/static/AddWatchFlow.no-verdict-step.test.ts` returns at least 1.
    - `grep -c "VerdictStep\|WishlistRationalePanel\|PasteSection" tests/static/AddWatchFlow.no-verdict-step.test.ts` returns at least 3 (one occurrence per `it()` block / regex).
    - `npx vitest run tests/static/AddWatchFlow.no-verdict-step.test.ts` exits 0 with all 3 `it()` cases GREEN (vacuous-pass works because AddWatchFlow.tsx exists today but has zero imports of the three dead components — RESEARCH §5 verified).
    - Sanity-check the guard's DETECT capability by temporarily appending `\nimport VerdictStep from './VerdictStep'\n` to a scratch copy of AddWatchFlow.tsx in a tmp location and running the regex manually; do NOT commit any modification to the real AddWatchFlow.tsx. Detection proof can also be demonstrated in PR description without code.
  </acceptance_criteria>
  <done>
    CLNP-02 static guard committed. `npx vitest run tests/static/AddWatchFlow.no-verdict-step.test.ts` exits 0 (3/3 green). Guard would FAIL if any of the three dead component names were imported into AddWatchFlow.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CLNP-03 static guard (no-collection-fit-card across 8 add-flow files)</name>
  <files>tests/static/AddWatchFlow.no-collection-fit-card.test.ts</files>
  <read_first>
    - tests/static/CollectionFitCard.no-engine.test.ts (regex shape + existsSync vacuous-pass)
    - tests/static/legacy-watch-routes.test.ts lines 1-7 (`// @vitest-environment node` directive) and lines 136-153 (per-file loop structural template)
    - .planning/phases/71-dead-code-cleanup-static-guards/71-PATTERNS.md §"tests/static/AddWatchFlow.no-collection-fit-card.test.ts (CLNP-03)" for the verbatim guard template
    - .planning/phases/71-dead-code-cleanup-static-guards/71-CONTEXT.md §D-04 for the exact 8-file ADD_FLOW_FILES enumeration (binding)
    - Each of the 8 ADD_FLOW_FILES — confirm they exist today and contain zero `CollectionFitCard` imports (RESEARCH §6 verified once already; sample-check 2-3 of them to confirm post-Phase-70 state hasn't drifted)
  </read_first>
  <action>
    Create `tests/static/AddWatchFlow.no-collection-fit-card.test.ts` per D-02 + D-04 + PATTERNS.md §CLNP-03 verbatim template. Structure (in order):

    1. Line 1: `// @vitest-environment node` — same hard rule as Task 1. NO preceding content.
    2. Lines 2-7: copy the 6-line legacy-watch-routes.test.ts comment block verbatim (same as Task 1).
    3. Imports: `import { describe, it, expect } from 'vitest'` then `import { existsSync, readFileSync } from 'node:fs'`.
    4. JSDoc header: `Phase 71 CLNP-03 + Pitfall 1: No file in the add-flow component tree MUST import CollectionFitCard. The add-flow is a data-entry surface; fit analysis belongs on the watch-detail / insights surfaces only.` + blank line + `Mirrors Phase 20 D-04 (CollectionFitCard.no-engine.test.ts) for the add-flow tree. While a listed file does not exist, its check passes vacuously.`
    5. Module-scope `const ADD_FLOW_FILES = [...]` listing exactly the 8 paths from CONTEXT.md D-04, in this exact order (so the test report ordering matches reviewer expectations): `src/components/watch/AddWatchFlow.tsx`, `src/components/watch/SearchEntry.tsx`, `src/components/watch/StructuredEntryPanel.tsx`, `src/components/watch/ConfirmStep.tsx`, `src/components/watch/DupeBanner.tsx`, `src/components/watch/WatchForm.tsx`, `src/components/watch/WatchPhotoStep.tsx`, `src/components/watch/ExtractErrorCard.tsx`.
    6. `describe('Phase 71 CLNP-03 — add-flow no-CollectionFitCard invariant', () => { for (const filePath of ADD_FLOW_FILES) { it(`${filePath} does not import CollectionFitCard`, () => { if (!existsSync(filePath)) return; const src = readFileSync(filePath, 'utf8'); expect(src).not.toMatch(/from ['"].*CollectionFitCard['"]/) }) } })` — per-file loop generates 8 named test cases. Regex shape per PATTERNS.md is `/from ['"].*CollectionFitCard['"]/` (the leading `.*` absorbs any path prefix; no `(?:.*\/)?` needed).

    Do NOT add a transitive resolver, an AST walker, or a glob. The hardcoded list IS the contract per D-04 — maintenance cost (new add-flow file requires manual update) is acceptable; any new add-flow file is a deliberate architectural change worth a code-review touch (CONTEXT.md §"Deferred Ideas — Add-flow tree growth strategy").
  </action>
  <verify>
    <automated>npx vitest run tests/static/AddWatchFlow.no-collection-fit-card.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/static/AddWatchFlow.no-collection-fit-card.test.ts` exists.
    - `head -1 tests/static/AddWatchFlow.no-collection-fit-card.test.ts` outputs exactly `// @vitest-environment node` (no leading whitespace, no preceding line).
    - `grep -c "AddWatchFlow.tsx\|SearchEntry.tsx\|StructuredEntryPanel.tsx\|ConfirmStep.tsx\|DupeBanner.tsx\|WatchForm.tsx\|WatchPhotoStep.tsx\|ExtractErrorCard.tsx" tests/static/AddWatchFlow.no-collection-fit-card.test.ts` returns at least 8 (one path per ADD_FLOW_FILES entry).
    - `grep -c "CollectionFitCard" tests/static/AddWatchFlow.no-collection-fit-card.test.ts` returns at least 2 (one in JSDoc header naming the forbidden import + one in the regex).
    - `npx vitest run tests/static/AddWatchFlow.no-collection-fit-card.test.ts` exits 0 with all 8 generated test cases GREEN (vacuous-pass works because all 8 files exist today and none import CollectionFitCard — RESEARCH §6 verified).
    - The test report names each of the 8 `it()` cases distinctly via the templated `${filePath}` (so a future regression names which add-flow file violated the invariant).
  </acceptance_criteria>
  <done>
    CLNP-03 static guard committed. `npx vitest run tests/static/AddWatchFlow.no-collection-fit-card.test.ts` exits 0 (8/8 green). Guard would FAIL on any of the 8 ADD_FLOW_FILES if `CollectionFitCard` were imported from any path.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend package.json prebuild to run all tests/static/ guards</name>
  <files>package.json</files>
  <read_first>
    - package.json line 7 (current `prebuild` script — `vitest run tests/static/legacy-watch-routes.test.ts`)
    - .planning/phases/71-dead-code-cleanup-static-guards/71-RESEARCH.md §"Static Guard — Prebuild Wiring" + §"Common Pitfalls — Pitfall 4" (rationale + risk if not extended)
    - .planning/phases/71-dead-code-cleanup-static-guards/71-PATTERNS.md §"package.json scripts.prebuild (config change)" for the verbatim target value
    - tests/static/ directory listing (`ls tests/static/`) — sanity-check that NO test file under tests/static/ has unintended side effects when run during prebuild (existing guards are file-read-only, both new guards will be too)
  </read_first>
  <action>
    Edit `package.json` line 7: change `"prebuild": "vitest run tests/static/legacy-watch-routes.test.ts",` to `"prebuild": "vitest run tests/static/",`. This is a single trailing path change — drop the explicit filename, keep the directory + trailing slash. No other lines change.

    Rationale (RESEARCH §"Static Guard — Prebuild Wiring" + Pitfall 4): the original single-file prebuild dates to Phase 59's initial guard. Subsequent guards (CollectionFitCard.no-engine, ppr-dynamic-before-use-cache, followed-owners-module-rsc, the two new Phase 71 guards) run via `npm test` but not via `next build`. Vercel's prebuild hook fires before `next build`, so consolidating to the directory ensures every present and future static guard blocks the Vercel deploy. Maintenance is simpler than enumerating filenames.

    This change is Claude's Discretion territory per CONTEXT.md (D-Discretion — RESEARCH recommends the extension and CONTEXT.md does not forbid it). The Phase 71 PRD planning_context explicitly directs this extension as part of Plan 71-01.

    Do NOT change any other `scripts.*` value. Do NOT add new scripts. Do NOT reorder fields. The minimal diff is `-vitest run tests/static/legacy-watch-routes.test.ts` / `+vitest run tests/static/`.
  </action>
  <verify>
    <automated>npm run prebuild</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"prebuild": "vitest run tests/static/"' package.json` returns 1 (exact target string present).
    - `grep -c '"prebuild": "vitest run tests/static/legacy-watch-routes.test.ts"' package.json` returns 0 (old single-file value removed).
    - `npm run prebuild` exits 0 (entire `tests/static/` directory runs and all guards pass — Tasks 1 and 2's new guards pass vacuously today, the existing 3+ guards already pass).
    - The prebuild run includes BOTH new guards from Tasks 1 and 2 (verify by checking the vitest stdout output names `AddWatchFlow.no-verdict-step.test.ts` and `AddWatchFlow.no-collection-fit-card.test.ts` in the run summary).
    - `node -e "const p=require('./package.json'); process.exit(p.scripts.prebuild === 'vitest run tests/static/' ? 0 : 1)"` exits 0.
  </acceptance_criteria>
  <done>
    `package.json` `prebuild` extended to `vitest run tests/static/`. `npm run prebuild` runs the full static-guard directory (including both new Phase 71 guards) and exits 0. Vercel prebuild now blocks any future deploy that introduces a VerdictStep/WishlistRationalePanel/PasteSection import in AddWatchFlow.tsx or a CollectionFitCard import in any of the 8 add-flow files.
  </done>
</task>

</tasks>

<verification>
After all three tasks land:

1. `npx vitest run tests/static/` exits 0 — all static guards (existing + 2 new) GREEN.
2. `npm run prebuild` exits 0 — confirms the Vercel build gate now covers the new guards.
3. `head -1 tests/static/AddWatchFlow.no-verdict-step.test.ts` outputs `// @vitest-environment node`.
4. `head -1 tests/static/AddWatchFlow.no-collection-fit-card.test.ts` outputs `// @vitest-environment node`.
5. No source code changes outside `tests/static/` + `package.json` (zero behavior change).
6. Plan 71-02 can now safely delete the dead component files; if any delete misses a sweep site, Plan 71-01's guards still pass (imports were never reintroduced) but `npm run build` catches the residue.

Phase 71's `npm run build` exit 0 is the authoritative gate per Memory `project_baseline_not_green_build_is_gate.md`. Do NOT use full `vitest run` or full `tsc --noEmit` as the gate — both carry pre-existing baseline noise (Phase 69 Plan 04 CommentGateLocked font-medium failure et al.).
</verification>

<success_criteria>
- [ ] `tests/static/AddWatchFlow.no-verdict-step.test.ts` exists, line 1 is `// @vitest-environment node`, 3 it() cases GREEN.
- [ ] `tests/static/AddWatchFlow.no-collection-fit-card.test.ts` exists, line 1 is `// @vitest-environment node`, 8 it() cases GREEN (one per ADD_FLOW_FILES entry).
- [ ] `package.json` `scripts.prebuild` is `"vitest run tests/static/"` (no filename).
- [ ] `npm run prebuild` exits 0; output names both new guards.
- [ ] Both new guards would FAIL if a forbidden import were introduced (regex shape verified against existing CollectionFitCard.no-engine.test.ts precedent).
- [ ] Zero changes to source code in `src/` (this plan is test+config only).
</success_criteria>

<output>
After completion, create `.planning/phases/71-dead-code-cleanup-static-guards/71-01-SUMMARY.md` per the standard summary template. Include: what shipped (2 guards + prebuild extension), test run output (3+8+all-existing all green), the prebuild diff, and a forward-coordination note that Plan 71-02 will reword `AddWatchFlow.tsx`'s JSDoc per D-03 to name `tests/static/AddWatchFlow.no-verdict-step.test.ts` as the enforcement mechanism.
</output>
