---
phase: 59-unified-route-variant-c
plan: 03
type: execute
wave: 3
depends_on: [59-01, 59-02]
files_modified:
  - src/components/insights/SleepingBeautiesSection.tsx
  - src/components/insights/GoodDealsSection.tsx
  - src/components/insights/CollectionFitCard.tsx
  - src/components/home/MostWornThisMonthCard.tsx
  - src/components/home/ActivityRow.tsx
  - src/components/home/RecommendationCard.tsx
  - src/components/home/SleepingBeautyCard.tsx
  - src/components/profile/StatsTabContent.tsx
  - src/components/profile/ProfileWatchCard.tsx
  - src/components/profile/NoteRow.tsx
  - src/components/profile/NotesEmptyOwnerActions.tsx
  - src/components/watch/WatchCard.tsx
  - src/components/watch/WatchDetail.tsx
  - src/components/wear/WearDetailHero.tsx
  - src/components/notifications/NotificationRow.tsx
  - src/app/explore/lists/[id]/page.tsx
  - src/components/explore/DiscoveryWatchCard.tsx
  - src/components/explore/PathCard.tsx
  - src/components/search/WatchSearchRow.tsx
  - src/app/watch/[id]/page.tsx
  - src/app/watch/[id]/edit/page.tsx
  - src/app/catalog/[catalogId]/page.tsx
autonomous: true
requirements: [ROUTE-02, ROUTE-03, ROUTE-04]
must_haves:
  truths:
    - "Every internal watch link literal points at /w/[ref] — grid cards, search rows, discovery rails, add-watch deep-links, computed notification deep-links (ROUTE-04/D-03)"
    - "Ownership/per-user surfaces emit watches.id; discovery surfaces emit catalogId — ID type discipline preserved (D-03)"
    - "The computed NotificationRow.resolveHref deep-link returns /w/[ref] (D-12)"
    - "/watch/[id]/page.tsx, /watch/[id]/edit/page.tsx, and /catalog/[catalogId]/page.tsx are deleted — 404 by absence (ROUTE-02/D-02)"
    - "/watch/new stays at its current path (D-10) — not re-pointed, not deleted"
    - "The CI guard passes (green) once all literals are re-pointed and legacy files deleted; it is PROVEN to fail the build when a legacy literal is re-planted (ROUTE-03/D-11)"
  artifacts:
    - path: "src/components/notifications/NotificationRow.tsx"
      provides: "computed deep-link returning /w/[ref] instead of /watch/[id]"
      contains: "/w/${"
  key_links:
    - from: "all 26 link-literal sites"
      to: "/w/[ref] unified route"
      via: "path-prefix rewrite /watch/ or /catalog/ to /w/ (id variable preserved per D-03)"
      pattern: "/w/\\$\\{"
    - from: "npm run build"
      to: "tests/static/legacy-watch-routes.test.ts"
      via: "prebuild hook fails non-zero on any surviving legacy literal"
      pattern: "prebuild"
---

<objective>
Complete the hard cutover: re-point all 26 internal watch-link literals across 21 files to `/w/[ref]` (ROUTE-04), delete the three legacy `page.tsx` files (ROUTE-02), and PROVE the CI guard fails the build on a planted legacy literal (ROUTE-03). This is the migration bulk of the phase. The full file→line→literal→id-type table is in 59-RESEARCH.md §"Link-Surface Enumeration" — every one of the 26 literals must be covered.

Purpose: After Plan 02 created the unified route, the re-pointed links resolve correctly; deleting the legacy pages makes any missed link 404 loudly (D-02). The guard (built RED in Plan 01) turns green here and is proven to be a real build gate, not an assumed one.

Output: 19 re-pointed link-literal files + 3 deleted legacy page files. Net: the CI guard is GREEN, `npm run build` succeeds, and a planted-literal experiment confirms the build fails non-zero with a legacy link present.

Locked constraints:
- ID type discipline (D-03): ownership/per-user surfaces emit `watches.id`; discovery surfaces (Explore, Search) emit `catalogId`. Preserve the existing id variable at each site; ONLY change the path prefix `/watch/` or `/catalog/` to `/w/`. Do NOT swap watch.id for watch.catalogId or vice versa.
- `/watch/new` STAYS (D-10/D-13) — never re-point or delete it. The allowlist in the guard protects it.
- Zero redirects: the catalog page deletion removes the last `redirect()` (D-08); do not introduce any new one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-unified-route-variant-c/59-CONTEXT.md
@.planning/phases/59-unified-route-variant-c/59-RESEARCH.md
@.planning/phases/59-unified-route-variant-c/59-PATTERNS.md
@.planning/phases/59-unified-route-variant-c/59-VALIDATION.md
@.planning/phases/59-unified-route-variant-c/59-01-SUMMARY.md
@.planning/phases/59-unified-route-variant-c/59-02-SUMMARY.md

<interfaces>
<!-- The exact 26 literals to re-point. Verified by live grep 2026-05-25. Change ONLY the path prefix to /w/. -->

DETAIL links — emit watches.id, target /w/${...} (Group A, 12 active callsites):
- src/components/insights/SleepingBeautiesSection.tsx:44  href={`/watch/${watch.id}`}
- src/components/insights/GoodDealsSection.tsx:48        href={`/watch/${w.id}`}
- src/components/insights/CollectionFitCard.tsx:71       href={`/watch/${watch.id}`}
- src/components/home/MostWornThisMonthCard.tsx:21       href={`/watch/${watch.id}`}
- src/components/home/ActivityRow.tsx:55                 href={`/watch/${row.watchId}`}      (activities.watchId maps to watches.id)
- src/components/home/RecommendationCard.tsx:22          href={`/watch/${rec.representativeWatchId}`}
- src/components/home/SleepingBeautyCard.tsx:33          href={`/watch/${watch.id}`}
- src/components/profile/StatsTabContent.tsx:62          href={`/watch/${watch.id}`}
- src/components/profile/ProfileWatchCard.tsx:63         href={`/watch/${watch.id}`}
- src/components/profile/NoteRow.tsx:62                  href={`/watch/${watch.id}`}   (+ edit at :96)
- src/components/watch/WatchCard.tsx:35                  href={`/watch/${watch.id}`}
- src/components/wear/WearDetailHero.tsx:111             href={`/watch/${watchId}`}      (wearEvents.watchId maps to watches.id)

COMPUTED deep-link — D-12, must turn green in the guard:
- src/components/notifications/NotificationRow.tsx:142   return `/watch/${watchId}`

EDIT links — emit watches.id, target /w/${...}/edit (Group B, 3 active callsites):
- src/components/profile/NotesEmptyOwnerActions.tsx:53   router.push(`/watch/${watchId}/edit#notes`)
- src/components/profile/NoteRow.tsx:96                  render={<Link href={`/watch/${watch.id}/edit`} />}
- src/components/watch/WatchDetail.tsx:226               href={`/watch/${watch.id}/edit`}

CATALOG links — emit catalogId, target /w/${...} (Group C, 4 files / 7 literals):
- src/app/explore/lists/[id]/page.tsx:91,110             href={`/catalog/${item.catalogId}`}
- src/components/explore/DiscoveryWatchCard.tsx:30        href={`/catalog/${watch.id}`}   (var named watch.id but IS catalogEntry.id — keep the variable)
- src/components/explore/PathCard.tsx:97,134,143          href={`/catalog/${node.catalogId}`}
- src/components/search/WatchSearchRow.tsx:31             href={`/catalog/${result.catalogId}`}

DELETE (route 404-by-absence + last redirect removal):
- src/app/watch/[id]/page.tsx          (also carried the :187/190 edit links — already re-emitted as /w/ in Plan 02's unified page)
- src/app/watch/[id]/edit/page.tsx     (moved to /w/[ref]/edit in Plan 02)
- src/app/catalog/[catalogId]/page.tsx (carries the D-08 redirect at :112 — deleted entirely)

DO NOT TOUCH (allowlisted, D-10/D-13):
- /watch/new across DesktopTopNav, AddWatchCard, WatchPickerDialog, AddWatchFlow, CollectionTabContent, WishlistTabContent, CatalogPageActions, WatchSearchRowsAccordion, NotesTabContent, src/lib/watchFlow/destinations.ts
- /u/[username]?focusWatch=... in NotificationRow.tsx:137 (query param, not a path)
- /explore/lists/[id], /admin/lists/[id], /wear/[id] (not watch paths)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point detail + edit + computed deep-link literals (watches.id surfaces)</name>
  <read_first>
    - .planning/phases/59-unified-route-variant-c/59-RESEARCH.md (Link-Surface Enumeration Groups A and B + Pitfall 4 ID-type discipline + Pitfall 3 computed deep-link)
    - .planning/phases/59-unified-route-variant-c/59-PATTERNS.md (section "26 link-literal sites" — the mechanical-rewrite examples for detail/edit/computed/router.push)
    - src/components/notifications/NotificationRow.tsx (the resolveHref function ~142 — D-12 computed return; confirm the :137 /u/ query-param link is NOT touched)
    - Each Group A + Group B file at the line in the interfaces table before editing it
  </read_first>
  <action>
    Re-point all watches.id-emitting literals by changing ONLY the path prefix from `/watch/` to `/w/`, preserving the id variable verbatim. Group A (detail, 12 sites): `/watch/${X}` becomes `/w/${X}` in SleepingBeautiesSection:44, GoodDealsSection:48, CollectionFitCard:71, MostWornThisMonthCard:21, ActivityRow:55, RecommendationCard:22, SleepingBeautyCard:33, StatsTabContent:62, ProfileWatchCard:63, NoteRow:62, WatchCard:35, WearDetailHero:111. Computed deep-link (D-12): in NotificationRow.tsx:142 change `return \`/watch/${watchId}\`` to `return \`/w/${watchId}\``; leave the :137 `/u/${...}?focusWatch=` link untouched (it is a query param, not a watch path). Group B (edit, 3 sites): `/watch/${X}/edit...` becomes `/w/${X}/edit...` in NotesEmptyOwnerActions:53 (router.push form, keep `#notes`), NoteRow:96 (render Link form), WatchDetail:226 (inside the 'use client' island — `watch.id` is available as a prop). Do NOT touch any `/watch/new` reference. After editing, run the guard against these files; they must no longer be flagged.
  </action>
  <verify>
    <automated>npm run test -- tests/static/legacy-watch-routes.test.ts 2>&1 | grep -E "src/components/(insights|home|profile|watch|wear|notifications)" || echo "NO_COMPONENT_VIOLATIONS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rEn "/watch/\$\{" src/components/` returns NO matches except allowlisted `/watch/new` (all detail/edit literals now use `/w/`)
    - `src/components/notifications/NotificationRow.tsx` contains `return \`/w/${watchId}\`` and no `/watch/${watchId}` (D-12)
    - `src/components/notifications/NotificationRow.tsx` ~line 137 `/u/${...}?focusWatch=` link is unchanged
    - No `/watch/new` reference was altered (D-10)
    - All 12 Group A + 3 Group B id variables are unchanged (only the path prefix changed; D-03)
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>All watches.id-emitting detail/edit/computed links point at /w/[ref]; /watch/new untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point catalog (discovery) literals + delete the three legacy pages</name>
  <read_first>
    - .planning/phases/59-unified-route-variant-c/59-RESEARCH.md (Link-Surface Enumeration Group C + section "Route 404-by-Absence" + Pitfall 5 partial-deletion warning)
    - .planning/phases/59-unified-route-variant-c/59-PATTERNS.md (section "26 link-literal sites" discovery example — DiscoveryWatchCard variable is named watch.id but IS catalogId; keep the variable)
    - Each Group C file at its line(s) before editing
    - src/app/catalog/[catalogId]/page.tsx (confirm findViewerWatchByCatalogId was already extracted to the DAL in Plan 01 before deleting; confirm the :112 redirect is the one being removed by deletion)
  </read_first>
  <action>
    Group C (catalog to /w/, discovery surfaces emit catalogId): change `/catalog/${X}` to `/w/${X}` preserving the id variable in explore/lists/[id]/page.tsx:91 and :110 (`item.catalogId`), DiscoveryWatchCard:30 (`watch.id` — DO NOT change to watch.catalogId; the variable already holds the catalogEntry id per RESEARCH §C, keep it verbatim), PathCard:97,134,143 (`node.catalogId`), WatchSearchRow:31 (`result.catalogId`). These remain catalogId-typed refs (D-03) — only the prefix changes.

    Then delete the three legacy page files (ROUTE-02, route 404-by-absence) using `git rm`: `src/app/watch/[id]/page.tsx`, `src/app/watch/[id]/edit/page.tsx`, and `src/app/catalog/[catalogId]/page.tsx`. Deleting `catalog/[catalogId]/page.tsx` removes the last `redirect()` on a watch route (the D-08 unwind) — before deleting, confirm `findViewerWatchByCatalogId` already lives in `src/data/watches.ts` (Plan 01) so nothing breaks. Do NOT delete `src/app/watch/new/` (D-10). Pitfall 5: delete BOTH `watch/[id]/page.tsx` AND `watch/[id]/edit/page.tsx` so no orphan edit route survives at the old path. After deletion, neither `src/app/watch/[id]/` nor `src/app/catalog/[catalogId]/` should contain a page.tsx.
  </action>
  <verify>
    <automated>test ! -f "src/app/watch/[id]/page.tsx" && test ! -f "src/app/watch/[id]/edit/page.tsx" && test ! -f "src/app/catalog/[catalogId]/page.tsx" && test -f "src/app/watch/new/page.tsx" && echo "DELETED_AND_NEW_KEPT" && (grep -rEn "/catalog/\$\{" src/ | grep -vE "/w/" || echo "NO_CATALOG_LITERALS")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rEn "/catalog/\$\{" src/` returns NO matches (all 7 catalog literals now use `/w/`)
    - DiscoveryWatchCard:30 still uses its existing `watch.id` variable (catalogId-typed; D-03 — variable unchanged, only prefix)
    - `src/app/watch/[id]/page.tsx`, `src/app/watch/[id]/edit/page.tsx`, `src/app/catalog/[catalogId]/page.tsx` do NOT exist (ROUTE-02/D-02; git rm)
    - `src/app/watch/new/page.tsx` still exists (D-10)
    - No `redirect(` remains in `src/app/` for any watch route (D-08; catalog page deleted)
    - `npx tsc --noEmit` reports no new errors (findViewerWatchByCatalogId resolves from the DAL)
  </acceptance_criteria>
  <done>All discovery links point at /w/[ref]; the three legacy pages are deleted (404 by absence); /watch/new kept; the last redirect removed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the CI guard is green AND fails the build on a planted legacy literal</name>
  <read_first>
    - tests/static/legacy-watch-routes.test.ts (the guard from Plan 01 — confirm both ROUTE-02 absence + ROUTE-03 scan blocks)
    - package.json (the prebuild hook from Plan 01)
    - .planning/phases/59-unified-route-variant-c/59-RESEARCH.md (Open Question 2 + Assumption A4 — the MEDIUM-confidence prebuild→Vercel-build linkage that this task PROVES locally)
    - .planning/phases/59-unified-route-variant-c/59-VALIDATION.md (Wave 0 Requirements — the prebuild hook must fail the build)
  </read_first>
  <action>
    First, run the guard and the full suite to confirm GREEN now that all 26 literals are re-pointed and the three legacy pages are deleted: `npm run test -- tests/static/legacy-watch-routes.test.ts` (both ROUTE-02 absence + ROUTE-03 scan blocks must pass), then `npm run test` (full Vitest suite must be green). Then PROVE the build gate (ROUTE-03/D-11; closes RESEARCH Assumption A4 / Open Question 2, MEDIUM confidence on whether Vercel runs prebuild): (a) plant a deliberate legacy literal in a real source file — e.g. add a line `const _guardProof = \`/watch/${'x'}\`` to a component file; (b) run `npm run build` and capture the exit code — it MUST be non-zero, failing in the `prebuild` step before `next build` runs; (c) remove the planted literal; (d) run `npm run build` again — it MUST exit 0 and complete the build. Record both exit codes in the SUMMARY as the proof. Because this project is build-gated (`.env.local` needed at build per MEMORY), run the build locally without worktree isolation (use_worktrees is permanently false). If `next build` fails for an unrelated reason (e.g. env) but the `prebuild` step demonstrably fired and gated on the planted literal, document that the GUARD step fired non-zero (the ROUTE-03 contract is the prebuild gate, not the full Next compile).
  </action>
  <verify>
    <automated>npm run test -- tests/static/legacy-watch-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- tests/static/legacy-watch-routes.test.ts` PASSES (green) — both the ROUTE-02 absence block and the ROUTE-03 scan block (no legacy literals remain)
    - `npm run test` (full suite) passes
    - With a planted `/watch/${...}` literal in a source file, `npm run build` exits NON-ZERO (the prebuild guard fails the build — ROUTE-03/D-11 proven, not assumed)
    - After removing the planted literal, `npm run build` exits 0 (or the prebuild guard step demonstrably passes if an unrelated env issue blocks full next-build)
    - The two build exit codes are recorded in the SUMMARY as the build-gate proof
  </acceptance_criteria>
  <done>The CI guard is green, and the planted-literal experiment proves the prebuild hook fails the build on any surviving legacy link literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → source files | The guard scan is the only automated completeness check for the hard cutover; a missed literal lets a dead link ship |
| Browser → deleted legacy routes | After deletion, requests to /watch/[id] or /catalog/[catalogId] must 404, never silently resolve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-10 | Tampering | CI guard build-gate linkage | mitigate | Prove the prebuild hook fails `npm run build` non-zero with a planted legacy literal (closes RESEARCH A4 MEDIUM-confidence); the gate is demonstrated, not assumed (ROUTE-03/D-11). |
| T-59-11 | Information Disclosure | ID-type cross-wiring | mitigate | Preserve each site's existing id variable (D-03); a discovery surface emitting watches.id (or vice versa) could resolve to the wrong copy. Acceptance criteria assert the variable is unchanged, only the prefix. |
| T-59-12 | Denial of Service (dead links) | Legacy route deletion completeness | accept/mitigate | Pitfall 5: delete BOTH watch/[id] page AND its edit page so no orphan owner-edit route survives at the old path. Guard ROUTE-02 absence block asserts all three files gone. |
| T-59-13 | Tampering | Residual redirect on watch route | mitigate | Deleting catalog/[catalogId]/page.tsx removes the last redirect() (D-08); acceptance criterion greps src/app for any remaining watch-route redirect (Router Cache poisoning avoidance). |
</threat_model>

<verification>
- `grep -rEn "/(watch|catalog)/\$\{" src/ | grep -vE "/w/|/watch/new"` returns EMPTY (all 26 literals migrated, /watch/new preserved)
- The three legacy `page.tsx` files do not exist; `src/app/watch/new/page.tsx` does
- `npm run test -- tests/static/legacy-watch-routes.test.ts` is GREEN
- `npm run test` (full suite) is green
- Planted-literal `npm run build` exits non-zero; clean `npm run build` exits 0 (or prebuild step demonstrably passes)
</verification>

<success_criteria>
- All 26 internal watch links point at /w/[ref] with correct id-type per surface (ROUTE-04/D-03)
- The computed NotificationRow deep-link returns /w/[ref] (D-12)
- Legacy /watch/[id], /watch/[id]/edit, /catalog/[catalogId] pages deleted — 404 by absence (ROUTE-02/D-02)
- /watch/new kept (D-10); zero redirects remain on watch routes (D-08)
- CI guard green and proven to fail the build on a re-planted legacy literal (ROUTE-03/D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/59-unified-route-variant-c/59-03-SUMMARY.md`
</output>
