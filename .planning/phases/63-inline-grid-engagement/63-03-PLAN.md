---
phase: 63-inline-grid-engagement
plan: 03
type: execute
wave: 2
depends_on: [63-01, 63-02]
files_modified:
  - src/app/u/[username]/[tab]/page.tsx
  - src/components/profile/CollectionTabContent.tsx
  - src/components/profile/WishlistTabContent.tsx
  - src/components/profile/SortableProfileWatchCard.tsx
  - src/components/profile/ProfileWatchCard.tsx
autonomous: true
requirements: [GRID-01, GRID-02, GRID-03, GRID-04, GRID-05]
tags: [profile, grid, chips, like, comment, optimistic, gate, threading]

must_haves:
  truths:
    - "A non-owner authenticated viewer can like a watch from a grid card in one tap with optimistic flip + silent rollback — GRID-01/D-04/D-05"
    - "Tapping the 💬 chip opens the compose-only WatchCommentSheet without navigating to detail — GRID-02/D-06"
    - "The card's ♥ and 💬 counts update optimistically after a like or posted comment — GRID-03/D-07"
    - "The full comment thread is NOT shown inline — only the composer is reachable from the grid — GRID-04"
    - "The 💬 chip is hidden entirely for a non-mutual viewer on a foreign wishlist card (♥ chip stays) — GRID-05/D-09"
    - "Owner cards show NO chips and NO scrim — the existing static count line is unchanged — D-03"
    - "Tapping a chip never navigates to /w/[ref]; tapping elsewhere on the card still opens detail — D-02"
  artifacts:
    - path: "src/app/u/[username]/[tab]/page.tsx"
      provides: "Widened counts type (liked+canComment) threaded with viewerId into both tab contents; ProfileTabContent NOT marked 'use cache'"
      contains: "viewerId"
    - path: "src/components/profile/CollectionTabContent.tsx"
      provides: "viewerId + liked + canComment threaded into each ProfileWatchCard"
      contains: "canComment"
    - path: "src/components/profile/WishlistTabContent.tsx"
      provides: "viewerId + liked + canComment threaded into non-owner ProfileWatchCard; owner Sortable path unchanged"
      contains: "canComment"
    - path: "src/components/profile/ProfileWatchCard.tsx"
      provides: "Overlay ♥ + 💬 chips (non-owner only) with scrim; optimistic like; sheet trigger; gated 💬"
      contains: "WatchCommentSheet"
  key_links:
    - from: "src/app/u/[username]/[tab]/page.tsx"
      to: "CollectionTabContent + WishlistTabContent"
      via: "viewerId prop + counts Record carrying liked/canComment"
      pattern: "viewerId=\\{viewerId\\}"
    - from: "src/components/profile/ProfileWatchCard.tsx ♥ chip"
      to: "toggleLikeAction"
      via: "onClick → preventDefault+stopPropagation → optimistic flip → toggleLikeAction in useTransition"
      pattern: "toggleLikeAction\\(\\{ type: 'watch'"
    - from: "src/components/profile/ProfileWatchCard.tsx 💬 chip"
      to: "WatchCommentSheet"
      via: "onClick → preventDefault+stopPropagation → setSheetOpen(true); sheet gated on canComment"
      pattern: "setSheetOpen\\(true\\)"
---

<objective>
Surface the engagement affordances on the profile grid: thread `viewerId` + per-card `liked`/`canComment` from the RSC down to `ProfileWatchCard`, then render two overlay pill chips (♥ + 💬) over the image for non-owner authenticated viewers. The ♥ chip mirrors the `LikeButton` optimistic pattern; the 💬 chip opens the Plan 02 `WatchCommentSheet`. Owner cards keep today's static count line (D-03); the 💬 chip is hidden for gated foreign-wishlist viewers (GRID-05/D-09); chip taps never navigate (D-02).

Purpose: This is the user-facing payload of Phase 63 — it consumes Plan 01's enriched counts and Plan 02's sheet to deliver one-tap like + inline compose directly on the grid.
Output: `viewerId`/`liked`/`canComment` threaded through page.tsx → Collection/Wishlist/Sortable tab contents → ProfileWatchCard; overlay chips + scrim + optimistic state + sheet wiring in ProfileWatchCard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-inline-grid-engagement/63-CONTEXT.md
@.planning/phases/63-inline-grid-engagement/63-RESEARCH.md
@.planning/phases/63-inline-grid-engagement/63-PATTERNS.md
@.planning/phases/63-inline-grid-engagement/63-UI-SPEC.md
@.planning/phases/63-inline-grid-engagement/63-VALIDATION.md
@src/components/watch/WatchCommentSheet.tsx

<interfaces>
<!-- Plan 01 widened the counts shape (now flows from getBatchedWatchCountsCached): -->
Record<string, { likeCount: number; commentCount: number; liked: boolean; canComment: boolean }>

<!-- Plan 02 sheet contract (consume, do not modify): -->
<WatchCommentSheet
  open={boolean}
  onOpenChange={(v: boolean) => void}
  watch={Watch}
  viewerId={string | null}
  onSuccess={() => void}   // parent: bump commentCount + close sheet + toast('Comment posted')
/>

<!-- LikeButton optimistic contract to mirror (verified src/components/shared/LikeButton.tsx): -->
<!-- useState(initialLiked) + useState(initialCount) + useTransition; on click preventDefault+stopPropagation; -->
<!-- optimistic flip; toggleLikeAction({type:'watch', id}); on !success rollback BOTH silently (no toast); on success reconcile {liked, count}. -->
<!-- Count span rendered only when (liked || count > 0). disabled={pending} blocks double-tap. -->

<!-- New ProfileWatchCard props to add: isOwner?, viewerId?, liked?, canComment? -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread viewerId + liked/canComment from RSC through tab-content components</name>
  <files>src/app/u/[username]/[tab]/page.tsx, src/components/profile/CollectionTabContent.tsx, src/components/profile/WishlistTabContent.tsx, src/components/profile/SortableProfileWatchCard.tsx</files>
  <read_first>
    - .planning/phases/63-inline-grid-engagement/63-PATTERNS.md (sections for page.tsx :374-405 threading, CollectionTabContent :20-31/:186-193, WishlistTabContent :34-42/:95-105/:148-156/:268-275, and SortableProfileWatchCard :8-14/:98-104 — all have exact line numbers + before/after)
    - src/app/u/[username]/[tab]/page.tsx (the file being modified — read the viewerId resolution + getBatchedWatchCountsCached call + counts type annotation + both tab-content renders at :366-406; CONFIRM the `unstable_instant = false` export and the ProfileTabContent function are present and must stay untouched)
    - src/components/profile/CollectionTabContent.tsx (the file being modified — read the Props interface at :20-31 and the ProfileWatchCard render at :186-193)
    - src/components/profile/WishlistTabContent.tsx (the file being modified — read the Props interface at :34-42, the non-owner ProfileWatchCard render at :95-105, the OwnerWishlistGrid sub-component props at :148-156, and its SortableProfileWatchCard render at :268-275)
    - src/components/profile/SortableProfileWatchCard.tsx (the file being modified — read its props interface + ProfileWatchCard passthrough at :8-14/:98-104)
    - .planning/phases/63-inline-grid-engagement/63-RESEARCH.md (Pattern 6 viewerId threading + Pitfall 4 'use cache' scope + Pitfall 8 unstable_instant)
  </read_first>
  <action>
    In `page.tsx` (ProfileTabContent, around :374-405): widen the `counts` Record type annotation to `Record<string, { likeCount: number; commentCount: number; liked: boolean; canComment: boolean }>` (the WatchCounts widening from Plan 01 already flows through `getBatchedWatchCountsCached` + `Object.fromEntries`). Add `viewerId={viewerId}` as a prop to BOTH `<CollectionTabContent ...>` and `<WishlistTabContent ...>`. `viewerId` is already resolved in the uncached ProfileTabContent scope — pass it as-is. CRITICAL: do NOT add `'use cache'` to ProfileTabContent; do NOT add `getCurrentUser()`/`cookies()`/`headers()` inside `getBatchedWatchCountsCached`; do NOT touch the `export const unstable_instant = false` line (PERMANENT — Phase 51/52, MEMORY `project_ppr_dynamic_before_use_cache`).

    In `CollectionTabContent.tsx`: extend `counts` prop type to include `liked: boolean; canComment: boolean`; add `viewerId?: string | null` to the Props interface. In the `<ProfileWatchCard>` render (:186-193) add `isOwner={isOwner}`, `viewerId={viewerId}`, `liked={counts?.[watch.id]?.liked}`, `canComment={counts?.[watch.id]?.canComment}`.

    In `WishlistTabContent.tsx`: same Props extension (`counts` liked/canComment + `viewerId?`). In the NON-OWNER `<ProfileWatchCard>` render (:95-105) add `isOwner={false}`, `viewerId={viewerId}`, `liked={...}`, `canComment={...}`. Extend `OwnerWishlistGrid` sub-component's `counts` prop type to the widened shape. In its `<SortableProfileWatchCard>` render (:268-275) leave `liked`/`canComment` UNPASSED — chips are non-owner-only (D-03), so the owner drag path never renders them and must not collide with dnd-kit.

    In `SortableProfileWatchCard.tsx`: optional. Per D-03 the owner is the only user who sees this component, so chips never render on it. You MAY add `liked?: boolean`/`canComment?: boolean` pass-through props for symmetry (mirroring the existing likeCount/commentCount passthrough at :8-14/:98-104), but it is not required and they will not be set by the wishlist owner path. Do NOT introduce chip rendering here.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (the widened counts type + new props type-check end to end)
    - Source assertion: `grep -c "viewerId={viewerId}" "src/app/u/[username]/[tab]/page.tsx"` returns 2 (both tab contents)
    - Source assertion: `grep -c "canComment" src/components/profile/CollectionTabContent.tsx` ≥1 AND `grep -c "canComment" src/components/profile/WishlistTabContent.tsx` ≥1
    - Source assertion: `grep -c "'use cache'" "src/app/u/[username]/[tab]/page.tsx"` is unchanged from baseline (ProfileTabContent NOT newly cached) — run `git diff "src/app/u/[username]/[tab]/page.tsx"` and confirm no added `'use cache'` and no change to the `unstable_instant` line
    - Source assertion: in WishlistTabContent the SortableProfileWatchCard render does NOT add liked/canComment props — `grep -n "SortableProfileWatchCard" src/components/profile/WishlistTabContent.tsx` block shows no canComment prop (D-03)
  </acceptance_criteria>
  <done>viewerId + liked + canComment flow from page.tsx through Collection/Wishlist tab contents into the non-owner ProfileWatchCard; owner Sortable path is chip-free; ProfileTabContent stays uncached; unstable_instant untouched; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Add overlay ♥/💬 chips + scrim + optimistic state + sheet wiring to ProfileWatchCard</name>
  <files>src/components/profile/ProfileWatchCard.tsx</files>
  <read_first>
    - .planning/phases/63-inline-grid-engagement/63-PATTERNS.md (section "src/components/profile/ProfileWatchCard.tsx" — the exact new props, imports, the existing wear-badge positioning analog at :87-98, the full scrim+chip overlay JSX block, the optimistic-state handlers, and the owner count-line at :117-136 to keep unchanged)
    - .planning/phases/63-inline-grid-engagement/63-UI-SPEC.md (GridLikeChip + GridCommentChip + chip container placement, the chip scrim CSS chain assertion, Interaction Contract, Copywriting Contract aria-labels, Accessibility Contract touch targets)
    - src/components/profile/ProfileWatchCard.tsx (the file being modified — read the full component: the `<Link href={`/w/${watch.id}`}>` wrapper at :63, the `relative aspect-square bg-muted` image div + wear badge at :87-98, the static count line at :117-136, and the existing Heart/MessageCircle imports at :5)
    - src/components/shared/LikeButton.tsx (the analog — read the optimistic pattern at :44-126: useState/useTransition, preventDefault+stopPropagation, optimistic flip, rollback-on-failure-silent, reconcile-on-success, disabled={pending}, count-only-when-liked-or-count>0)
    - src/components/watch/WatchCommentSheet.tsx (the Plan 02 component being consumed — confirm its props: open/onOpenChange/watch/viewerId/onSuccess)
    - node_modules/next/dist/docs (read the relevant Server-Actions-from-client-component guide before calling toggleLikeAction/addComment-derived state — AGENTS.md: NON-STANDARD Next.js 16)
    - .planning/phases/63-inline-grid-engagement/63-RESEARCH.md (Pitfall 1 chip-tap-navigates, Pitfall 2 chips-for-owner, Pitfall 3 stale-liked, Pitfall 7 composeKey, Pattern 7 onPointerDown reset)
  </read_first>
  <action>
    Add four optional props to `ProfileWatchCardProps`: `isOwner?: boolean`, `viewerId?: string | null`, `liked?: boolean`, `canComment?: boolean`. Add imports: `useState`, `useTransition` from 'react'; `toast` from 'sonner'; `toggleLikeAction` from '@/app/actions/reactions'; `WatchCommentSheet` from '@/components/watch/WatchCommentSheet'. (`Heart`, `MessageCircle`, `cn` already imported.)

    Add optimistic state in the function body: `likedState`/`setLikedState` = `useState(liked ?? false)`; `likeCountState`/`setLikeCountState` = `useState(likeCount ?? 0)`; `[likePending, startLikeTransition] = useTransition()`; `commentCountState`/`setCommentCountState` = `useState(commentCount ?? 0)`; `[sheetOpen, setSheetOpen] = useState(false)`.

    `handleLikeClick(e)`: call `e.preventDefault()` + `e.stopPropagation()` FIRST (D-02 — stops the wrapping `<Link>` navigation). Compute `nextLiked = !likedState` and `nextCount = nextLiked ? likeCountState + 1 : likeCountState - 1`; set both optimistically; `startLikeTransition(async () => { const result = await toggleLikeAction({ type: 'watch', id: watch.id }); if (!result.success) { setLikedState(likedState); setLikeCountState(likeCountState); console.error(...); return } setLikedState(result.data.liked); setLikeCountState(result.data.count) })`. Silent rollback — NO failure toast (D-05).

    `handleCommentClick(e)`: `e.preventDefault()` + `e.stopPropagation()` then `setSheetOpen(true)` (D-02/D-06).

    `handleCommentSuccess()`: `setCommentCountState((n) => n + 1)` (optimistic bump, D-07), `setSheetOpen(false)`, `toast('Comment posted')` (D-07 — the parent owns the success toast; the sheet delegates here).

    Inside the `relative aspect-square bg-muted` image div, AFTER the wear badge (after :98, before that div closes), render the chip block ONLY when `!isOwner` (D-03 — owner sees no chips/scrim): a scrim `<div className="absolute inset-x-0 bottom-0 h-12 bg-black/55 pointer-events-none" />`, then a chip row `<div className="absolute bottom-2 left-2 z-10 flex gap-2">` containing: the ♥ button (always present for non-owner, D-04) with `aria-pressed={likedState}`, `aria-busy={likePending}`, `aria-label={likedState ? 'Unlike' : 'Like'}`, `disabled={likePending}`, `onClick={handleLikeClick}`, classes `rounded-full bg-black/30 px-2 py-1 flex items-center gap-1 text-white text-xs tabular-nums min-h-[44px] min-w-[44px] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` (+ `opacity-50 cursor-wait` when pending), a `Heart size-4` with `fill={likedState ? 'currentColor' : 'none'}` + `text-red-400`/`text-white/90`, and the count span only when `(likedState || likeCountState > 0)`; AND the 💬 button rendered ONLY when `canComment` (D-09 gate — hidden for gated foreign-wishlist viewers) with `aria-label="Add a comment"`, `onClick={handleCommentClick}`, same pill classes, a `MessageCircle size-4 text-white/90`, count span only when `commentCountState > 0`. After the chip row, render `<WatchCommentSheet open={sheetOpen} onOpenChange={setSheetOpen} watch={watch} viewerId={viewerId ?? null} onSuccess={handleCommentSuccess} />` (also inside the `!isOwner` block).

    Keep the existing static count line at :117-136 EXACTLY as-is (it remains the sole count display for owner cards). Do NOT remove it. The chip block is gated on `!isOwner`, the static line is the owner display — no conflict. Use Tailwind utilities inline (no CSS modules), absolute `@/*` imports. The ♥ chip uses `onClick` (a fresh-per-interaction toggle, not a one-shot mount-time reset — consistent with Phase 62's "social comment button keeps onClick"); the sheet-open resets via `onOpenChange`. Do NOT reset chip state on mount (MEMORY `project_router_cache_stale_instance` — seed from props; cache-tag bust from Plan 01 handles re-hydration).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Source assertion: `grep -c "e.preventDefault()" src/components/profile/ProfileWatchCard.tsx` ≥2 AND `grep -c "e.stopPropagation()" src/components/profile/ProfileWatchCard.tsx` ≥2 (both chip handlers stop the Link — D-02)
    - Source assertion: `grep -c "bg-black/55" src/components/profile/ProfileWatchCard.tsx` returns 1 (the scrim — D-01)
    - Source assertion: `grep -c "min-h-\[44px\]" src/components/profile/ProfileWatchCard.tsx` ≥2 (both chip touch targets — WCAG 2.5.5)
    - Source assertion: the chip block is gated on `!isOwner` and the 💬 button is gated on `canComment` — `grep -c "!isOwner" src/components/profile/ProfileWatchCard.tsx` ≥1 AND `grep -c "canComment &&" src/components/profile/ProfileWatchCard.tsx` ≥1
    - Source assertion: `grep -c "toggleLikeAction({ type: 'watch'" src/components/profile/ProfileWatchCard.tsx` returns 1; `grep -c "WatchCommentSheet" src/components/profile/ProfileWatchCard.tsx` ≥1
    - Source assertion: the static count line at the former :117-136 is still present — `grep -c "text-muted-foreground tabular-nums" src/components/profile/ProfileWatchCard.tsx` ≥1 (owner display kept — D-03)
    - human_needed (prod): tap ♥ → optimistic flip + no nav; tap 💬 → sheet opens + no nav; tap card body → /w/[ref] opens; owner sees no chips; gated foreign-wishlist viewer sees ♥ but no 💬; post-comment toast fires + count bumps; navigate-away/back shows fresh state. Bundle into ONE deploy, build-gate, push origin main → verify on Vercel (MEMORY `feedback_mobile_ui_verify_on_prod`). Test instructions in 63-VALIDATION.md "Manual-Only Verifications".
  </acceptance_criteria>
  <done>ProfileWatchCard renders the ♥ + 💬 overlay chips (non-owner only, with scrim, 44px touch targets, chip-tap≠navigate); ♥ is optimistic with silent rollback; 💬 opens WatchCommentSheet and is gated on canComment; owner static count line unchanged; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (chip) → Server Action (toggleLike / addComment via sheet) | viewer interaction crosses into server mutation |
| RSC → client component props (viewerId, canComment) | server-resolved identity + gate flag passed to the client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08 | Elevation of Privilege (gate bypass by UI manipulation) | hidden 💬 chip on a gated card | mitigate | The `canComment` hide is UX ONLY. The REAL gate is `createComment` re-checking `canViewerCommentOnTarget` server-side (throws `CommentGateError`) + the service-role DAL (MEMORY `project_rls_subquery_caller_rls`). A viewer who un-hides the chip in devtools and posts is still rejected server-side. No new bypass surface. |
| T-63-09 | Information Disclosure | viewerId threaded into the client bundle | accept | `viewerId` is the viewer's OWN id (already available client-side via the auth session on `/u/*`); it is never another user's id. Used only for the CommentCompose anon-bounce parity path. No third-party identity is exposed. |
| T-63-10 | Tampering (IDOR) | watch.id as like/comment target from the client | mitigate | Both `toggleLikeAction` and `addCommentAction` resolve ownerId from the DB and validate input with Zod `.strict()`; the chip passes only `{type:'watch', id: watch.id}`. Identical to the detail-page LikeButton/CommentCompose path. |
| T-63-11 | Information Disclosure (stale/cross-viewer liked state) | optimistic chip state seeded from cached counts | mitigate | `liked` is seeded from the viewer-scoped `getBatchedWatchCountsCached` (tag `viewer:{viewerId}:counts`); Plan 01 busts that tag on every like/comment so re-hydration is fresh and per-viewer. Optimistic state covers the immediate frame; the tag covers navigate-back. |

No new high-severity threat. The chips surface existing, already-gated primitives; the server gate is unchanged and authoritative.
</threat_model>

<verification>
- `npm run build` exits 0 (authoritative gate — MEMORY `project_baseline_not_green_build_is_gate`).
- `npm run test` (full suite) green after the wave merge (Plan 01 added the data/action coverage; this plan adds no new test files but must not regress existing ones).
- Source assertions confirm: chip-tap≠navigate (preventDefault+stopPropagation), owner gate (!isOwner), comment gate (canComment &&), scrim (bg-black/55), 44px touch targets, compose-only (WatchCommentSheet has no thread — Plan 02 boundary).
- Touch/optimistic/sheet/gate behavior verified on PROD in one bundled deploy (human_needed — MEMORY `feedback_mobile_ui_verify_on_prod`); instructions in 63-VALIDATION.md.
</verification>

<success_criteria>
- Non-owner authenticated viewer sees ♥ + (when canComment) 💬 overlay chips on every grid card; owner sees none.
- ♥ chip likes optimistically with silent rollback; chip tap never navigates.
- 💬 chip opens the compose-only sheet; posting bumps the count + fires 'Comment posted'; full thread only via /w/[ref].
- Gated foreign-wishlist viewer sees ♥ but no 💬.
- Build exits 0; full test suite green; prod verification bundled into one deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/63-inline-grid-engagement/63-03-SUMMARY.md`
</output>
