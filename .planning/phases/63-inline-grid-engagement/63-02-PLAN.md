---
phase: 63-inline-grid-engagement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/watch/WatchCommentSheet.tsx
autonomous: true
requirements: [GRID-02, GRID-04]
tags: [comments, sheet, compose, bottom-sheet, watch]

must_haves:
  truths:
    - "A compose-only bottom sheet exists that posts a comment on a watch target without rendering the thread — GRID-02/GRID-04"
    - "The sheet shows the watch identity (thumbnail + brand + model) so the viewer knows what they are commenting on — GRID-02"
    - "On post success the textarea clears (composeKey re-mount) and the parent is signaled; on failure typed text is kept — GRID-02/D-08"
  artifacts:
    - path: "src/components/watch/WatchCommentSheet.tsx"
      provides: "Compose-only bottom sheet: Sheet + watch identity header + CommentCompose; calls addCommentAction with a watch target; NO CommentList/CommentThread"
      min_lines: 40
      contains: "addCommentAction"
  key_links:
    - from: "src/components/watch/WatchCommentSheet.tsx"
      to: "addCommentAction"
      via: "handleSubmit fires addCommentAction({ type: 'watch', id: watch.id, body }) inside useTransition"
      pattern: "addCommentAction\\(\\{\\s*type: 'watch'"
    - from: "src/components/watch/WatchCommentSheet.tsx"
      to: "CommentCompose"
      via: "renders CommentCompose with key={composeKey}; re-mounts on success to clear textarea"
      pattern: "key=\\{composeKey\\}"
---

<objective>
Create the new compose-only bottom sheet `WatchCommentSheet` that lets a viewer post a single comment on a watch from the grid without navigating to detail. It shows the watch identity header (thumbnail + brand/model) and the existing `CommentCompose` — and deliberately renders NO `CommentList`/`CommentThread` (GRID-04 compose-only boundary). This component is consumed by Plan 03's 💬 chip.

Purpose: D-06 chose Option A (a new lightweight sheet) over repurposing `WearCommentHost`, which hardcodes `type: 'wear'` and renders a full thread. A clean new file keeps the compose-only boundary explicit and avoids props surgery on an unrelated component.
Output: `src/components/watch/WatchCommentSheet.tsx` — a `'use client'` bottom sheet reusing `Sheet`, `CommentCompose`, `addCommentAction`, the `composeKey` re-mount pattern, and `sonner`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-inline-grid-engagement/63-CONTEXT.md
@.planning/phases/63-inline-grid-engagement/63-RESEARCH.md
@.planning/phases/63-inline-grid-engagement/63-PATTERNS.md
@.planning/phases/63-inline-grid-engagement/63-UI-SPEC.md

<interfaces>
<!-- Props contract for WatchCommentSheet (verified target per PATTERNS.md + UI-SPEC). -->
interface WatchCommentSheetProps {
  open: boolean
  onOpenChange: (v: boolean) => void
  watch: Watch                   // identity header (thumbnail + brand + model)
  viewerId: string | null        // passed to CommentCompose (anon bounce path; always non-null on /u/*)
  onSuccess: () => void          // parent bumps commentCount + closes sheet + fires 'Comment posted' toast
}

<!-- Reused existing contracts (do NOT re-implement): -->
<!-- CommentCompose props: { viewerId, pending, onSubmit(body: string) } — retains body on failure; clear-on-success is the PARENT's job (re-mount via key). -->
<!-- addCommentAction({ type: 'watch', id, body }) → { success: true, data } | { success: false, error }. -->
<!-- Sheet/SheetContent side="bottom" — backed by @base-ui/react Dialog; Escape + backdrop dismiss handled by primitive. -->
<!-- getSafeImageUrl(watch.imageUrl) for the thumbnail src; Watch type from @/lib/types. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WatchCommentSheet compose-only bottom sheet</name>
  <files>src/components/watch/WatchCommentSheet.tsx</files>
  <read_first>
    - .planning/phases/63-inline-grid-engagement/63-PATTERNS.md (section "src/components/watch/WatchCommentSheet.tsx (NEW)" — has the exact imports list, props interface, core handleSubmit pattern, sheet structure, and the bottom-side CSS chain to copy)
    - .planning/phases/63-inline-grid-engagement/63-UI-SPEC.md (the `WatchCommentSheet` Component Inventory entry + Copywriting Contract + the "Sheet bottom-side slide chain" CSS assertion + Accessibility Contract sr-only title)
    - src/components/wear/WearCommentHost.tsx (the analog — read the bottom-sheet variant Sheet/SheetContent skeleton at :88-115 and note WearCommentHost hardcodes `type: 'wear'` at :86; do NOT reuse it directly)
    - src/components/comment/CommentList.tsx (read the composeKey re-mount pattern at :78, :131-132 and the handleSubmit→addComment shape — this is the clear-on-success-only pattern to mirror)
    - src/components/comment/CommentCompose.tsx (read its props interface and confirm it does NOT clear body on submit — the comment says CommentList clears it; the sheet re-mounts via key)
    - src/components/ui/sheet.tsx (read the Sheet/SheetContent/SheetHeader/SheetTitle exports + the side="bottom" data-attr classes)
    - node_modules/next/dist/docs (read the relevant Server-Actions / client-component guide before wiring addCommentAction from a client component — AGENTS.md: this is a NON-STANDARD Next.js 16)
    - src/lib/images.ts (confirm getSafeImageUrl signature for the thumbnail src)
  </read_first>
  <action>
    Create `src/components/watch/WatchCommentSheet.tsx` as a `'use client'` component with the `WatchCommentSheetProps` interface from the interfaces block. Inside: `const [composeKey, setComposeKey] = useState(0)` and `const [pending, startTransition] = useTransition()`; `const safeUrl = getSafeImageUrl(watch.imageUrl)`.

    `handleSubmit(body: string)` wraps `addCommentAction({ type: 'watch', id: watch.id, body })` in `startTransition`. On `!result.success`: fire `toast.error('Failed to post comment. Please try again.')`, `console.error(...)`, and do NOT increment composeKey (D-08 — keep typed text for retry). On success: `setComposeKey((k) => k + 1)` (re-mounts CommentCompose → clears textarea, Pitfall 7) then call `onSuccess()` (the parent closes the sheet, bumps the count, and fires the 'Comment posted' toast — do NOT fire that success toast here; the parent owns it per D-07).

    Render `<Sheet open={open} onOpenChange={onOpenChange}>` → `<SheetContent side="bottom">` (omit `max-h-[60vh] overflow-y-auto` — compose-only is short; keep the `mx-auto w-full max-w-[640px] px-4 pb-[calc(1.5rem+env(safe-area-inset-bottom))]` inner wrapper from WearCommentHost). `<SheetHeader>` with `<SheetTitle className="sr-only">Add a comment</SheetTitle>` then the identity header: a 48×48 `rounded-md overflow-hidden bg-muted` thumbnail (`<Image src={safeUrl} fill className="object-cover" />` only when `safeUrl` is truthy, else nothing), `watch.brand` as `text-sm text-muted-foreground truncate`, `watch.model` as `text-base font-semibold leading-tight truncate`. Below the header render ONLY `<CommentCompose key={composeKey} viewerId={viewerId} pending={pending} onSubmit={handleSubmit} />`.

    HARD GRID-04 BOUNDARY: do NOT import or render `CommentList` or `CommentThread`. The sheet is compose-only. Use absolute `@/*` imports, `import type { Watch }` for the type, no barrel files, Tailwind utilities inline.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "WatchCommentSheet" || true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "CommentList\|CommentThread" src/components/watch/WatchCommentSheet.tsx` returns 0 (GRID-04 compose-only — no thread import/render)
    - Source assertion: `grep -c "addCommentAction({ type: 'watch'" src/components/watch/WatchCommentSheet.tsx` returns 1 (watch target, not wear)
    - Source assertion: `grep -c "key={composeKey}" src/components/watch/WatchCommentSheet.tsx` returns 1 (clear-on-success re-mount)
    - Source assertion: `grep -c "'use client'" src/components/watch/WatchCommentSheet.tsx` returns 1
    - Source assertion: file does NOT call `toast('Comment posted')` (success toast is the parent's job per D-07) — `grep -c "toast('Comment posted')" src/components/watch/WatchCommentSheet.tsx` returns 0; `toast.error(` IS present (D-08 failure toast)
    - `npm run build` exits 0 (the file compiles; component is type-correct against CommentCompose + addCommentAction + Sheet)
    - human_needed (prod): sheet opens on 💬 tap, shows watch identity + textarea, Post closes + clears + bumps count; verified in Plan 03 once wired (MEMORY `feedback_mobile_ui_verify_on_prod`)
  </acceptance_criteria>
  <done>WatchCommentSheet exists as a compose-only bottom sheet posting a watch-target comment; clears textarea on success only; keeps text + fires failure toast on failure; no thread rendered; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (sheet) → Server Action (addCommentAction) | viewer-typed comment body crosses into server execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-05 | Tampering (XSS) | comment body rendered/stored | mitigate | React escapes by default; the body is never `dangerouslySetInnerHTML`. `addCommentSchema` (Zod `.strict()`, `.trim().min(1).max(500)`) + DB CHECK already cap+sanitize on the server. This plan adds no new rendering of untrusted HTML. |
| T-63-06 | Elevation of Privilege (gate bypass) | posting from the sheet when the viewer is gated | mitigate | The sheet is only mounted by Plan 03 when `canComment` is true (UX), AND `addCommentAction` → `createComment` re-checks `canViewerCommentOnTarget` server-side and throws `CommentGateError`. The service-role DAL + server re-check is the REAL gate (MEMORY `project_rls_subquery_caller_rls`); the sheet posts through the same gated action used by the detail page. No new bypass surface. |
| T-63-07 | Tampering (IDOR) | watch.id passed as the comment target | mitigate | `addCommentAction` resolves ownerId from the DB by `id`; it does not trust client-supplied ownership. The sheet passes only `{ type: 'watch', id: watch.id, body }`; Zod `.strict()` rejects extras. Unchanged from the detail-page path. |

No new high-severity threat. The sheet posts through the existing, already-gated `addCommentAction`.
</threat_model>

<verification>
- `npm run build` exits 0 (the new file compiles against existing CommentCompose/addCommentAction/Sheet contracts).
- Source assertions above confirm the GRID-04 compose-only boundary (no CommentList/CommentThread) and the watch-target wiring.
- Runtime sheet behavior is verified on prod once Plan 03 wires the 💬 chip (human_needed — MEMORY `feedback_mobile_ui_verify_on_prod`).
</verification>

<success_criteria>
- `WatchCommentSheet` exists, is `'use client'`, compose-only (no thread), and posts a watch-target comment via `addCommentAction`.
- Textarea clears on success only (composeKey re-mount); keeps text + failure toast on failure.
- Success toast is delegated to the parent's `onSuccess` (D-07).
- Build exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/63-inline-grid-engagement/63-02-SUMMARY.md`
</output>
