---
phase: 60-multi-photo-schema-dal
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified: []
autonomous: false
requirements: [PHOTO-01]
must_haves:
  truths:
    - "The Phase 60 Supabase migration is applied to PROD via supabase db push --linked (the authoritative prod path; drizzle-kit push is local-only)"
    - "On prod, watches.image_url is backfilled losslessly then dropped — the pre-drop DO $$ assertion passes with zero missed rows (SC1, D-07, D-11)"
    - "On prod, the watch_photos table, (watch_id, sort_order) index, owner-scoped RLS policies, and watch-photos bucket all exist (post-migration DO $$ assertion passes)"
    - "On prod, watches_catalog is untouched - no LLM/factual/photo investment wiped (SC5 satisfied trivially per D-10/D-11)"
    - "On prod, a watch's cover resolves through the service-role DAL (the real read gate); the RLS subquery-caller gotcha is verified non-blocking for owner reads (project_rls_subquery_caller_rls)"
  artifacts: []
  key_links:
    - from: "supabase/migrations/20260525000000_phase60_watch_photos.sql"
      to: "production database"
      via: "supabase db push --linked"
      pattern: "supabase db push"
---

<objective>
Apply the Phase 60 schema migration to PRODUCTION via the authoritative `supabase db push --linked` path, and verify on prod that the lossless backfill + column drop completed, the table/index/RLS/bucket exist, and `watches_catalog` was not touched. This is the operator-run, prod-verified gate that closes SC1/SC5 for real (build + types pass WITHOUT the push — that is a false-positive; the push is mandatory).

Purpose: Build and type checks pass against the Drizzle config, not the live DB, so the migration must be explicitly applied to prod and verified there. Per project deploy discipline, DB schema changes are confirmed on prod, not just locally (local DB has RLS OFF on pre-existing tables, so cross-table RLS behavior is not reproducible locally).
Output: prod DB with watch_photos + dropped image_url + bucket + RLS; verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-multi-photo-schema-dal/60-CONTEXT.md
@.planning/phases/60-multi-photo-schema-dal/60-RESEARCH.md
@.planning/phases/60-multi-photo-schema-dal/60-01-PLAN.md
@.planning/phases/60-multi-photo-schema-dal/60-03-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply the Phase 60 migration to prod + verify</name>
  <files>supabase/migrations/20260525000000_phase60_watch_photos.sql</files>
  <action>
    Operator-run prod migration apply. Confirm Plans 01 and 03 are merged and the cover-resolving DAL + repointed readers are deployed/deploying (never drop the column on prod before readers are repointed). Run `supabase db push --linked` from the project root to apply `supabase/migrations/20260525000000_phase60_watch_photos.sql` to PROD; confirm both DO $$ assertions pass and the transaction COMMITs (watch for a silently-skipped migration — the filename must be exactly 14 digits, gotcha 1 from project_drizzle_supabase_db_mismatch). Then verify on prod via the Supabase SQL editor / psql: watch_photos table exists, watches.image_url column is gone, watch_photos_watch_id_sort_idx exists, >=4 RLS policies on watch_photos, watch-photos bucket exists, sort_order=0 backfill count >= pre-migration non-null image_url count, and watches_catalog is unchanged (D-10). Smoke-check the deployed app (profile grid + watch detail page) for rendered covers and no dropped-column 500s. This is autonomous:false because supabase db push --linked targets the live prod DB and the DROP COLUMN is irreversible — the operator confirms before approving.
  </action>
  <what-built>
    Plan 01 wrote the authoritative migration `supabase/migrations/20260525000000_phase60_watch_photos.sql` (CREATE TABLE watch_photos -> backfill watches.image_url -> pre-drop DO $$ lossless assert -> DROP COLUMN image_url -> RLS -> watch-photos bucket -> post-migration DO $$ assert, all in BEGIN/COMMIT). Plan 03 repointed every direct image_url reader so prod app code reads the cover, not the dropped column. This task applies the migration to PROD and verifies it.

    Why this is operator-run (autonomous: false): `supabase db push --linked` targets the live production database. Per project deploy discipline (MEMORY feedback_mobile_ui_verify_on_prod, project_drizzle_supabase_db_mismatch), prod schema changes are run/confirmed by the operator. The DROP COLUMN is irreversible — the backfill must be confirmed before the operator approves.
  </what-built>
  <how-to-verify>
    1. Confirm Plans 01 and 03 are merged and the app code (cover-resolving DAL + repointed readers) is deployed or deploying — never drop the column on prod before the readers are repointed (the migration's transaction makes the DB change atomic, but the app must read the new shape).
    2. Run the prod push from the project root:
       `supabase db push --linked`
       Watch for: the migration `20260525000000_phase60_watch_photos.sql` applies; both `DO $$` assertions pass (no RAISE EXCEPTION); the transaction COMMITs. Gotcha check (project_drizzle_supabase_db_mismatch): if the migration is silently skipped, confirm the filename is exactly 14 digits with no suffix letter.
    3. Verify on prod (via the Supabase SQL editor or `psql` against the prod connection string):
       - `SELECT count(*) FROM information_schema.tables WHERE table_name='watch_photos';` returns 1
       - `SELECT count(*) FROM information_schema.columns WHERE table_name='watches' AND column_name='image_url';` returns 0 (column dropped)
       - `SELECT count(*) FROM pg_indexes WHERE indexname='watch_photos_watch_id_sort_idx';` returns 1
       - `SELECT count(*) FROM pg_policies WHERE tablename='watch_photos';` returns >= 4
       - `SELECT count(*) FROM storage.buckets WHERE id='watch-photos';` returns 1
       - `SELECT count(*) FROM watch_photos WHERE sort_order=0;` is >= the number of watches that had a non-null image_url before the migration (lossless backfill)
       - Confirm `watches_catalog` is untouched: its row count and `image_url` column are unchanged (D-10).
    4. Smoke-check the deployed app: load a profile collection grid and a watch detail page on prod — covers render (cover storagePath or catalog fallback), no 500s from a dropped-column query (the Plan 03 repoint is the guard). The RLS subquery-caller gotcha is non-blocking for owner reads because the service-role DAL is the read gate.
  </how-to-verify>
  <verify>
    <automated>MANUAL — operator runs `supabase db push --linked` against prod and confirms the verification queries in how-to-verify (prod-only; local DB cannot reproduce RLS behavior). No local automated gate.</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --linked` applied `20260525000000_phase60_watch_photos.sql` to prod with both DO $$ assertions passing and a clean COMMIT
    - On prod: `watch_photos` table exists, `watches.image_url` column is gone, `watch_photos_watch_id_sort_idx` exists, >= 4 RLS policies on watch_photos, `watch-photos` bucket exists
    - On prod: lossless backfill confirmed (sort_order=0 row count >= pre-migration non-null image_url count)
    - On prod: `watches_catalog` row count and columns unchanged (D-10 / SC5)
    - Deployed app renders watch covers and detail pages on prod with no dropped-column query errors
  </acceptance_criteria>
  <done>The Phase 60 migration is applied to prod cleanly, all prod verification queries return expected values, watches_catalog is untouched, and the deployed app reads covers without dropped-column errors.</done>
  <resume-signal>Type "approved" once the prod push has applied cleanly and all prod verification queries return the expected values, or describe the failure (e.g. a DO $$ RAISE EXCEPTION, a skipped migration, or a dropped-column query error in the app).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration artifact -> production database | The push is the only path that mutates prod schema; irreversible DROP COLUMN must follow a verified backfill |
| anon/authenticated client -> watch_photos on prod (RLS ON) | Prod RLS differs from local (RLS OFF on pre-existing tables); verify owner-read path on prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-PRODDROP | Tampering | irreversible DROP COLUMN on prod data | mitigate | In-transaction backfill + pre-drop DO $$ lossless assertion RAISEs and aborts the whole transaction (rolls back) if any image_url row is unbacked; operator confirms readers are repointed (Plan 03) before approving |
| T-60-SKIP | Tampering | silently skipped migration (filename gotcha) | mitigate | Filename is exactly 14 digits (verified Plan 01); operator confirms the migration actually applied in the push output |
| T-60-PRODRLS | Information Disclosure | RLS subquery-caller gotcha on prod | accept (service-role DAL is real gate) | Owner reads go through the service-role DAL which bypasses RLS; the watch_photos SELECT policy is anon-block + owner backstop; verified non-blocking by the prod smoke check |
</threat_model>

<verification>
- Prod push applied cleanly with both DO $$ assertions passing.
- All prod verification queries return expected values (table/index/policies/bucket present; image_url gone; backfill lossless; catalog untouched).
- Deployed app renders covers and detail pages with no dropped-column errors.
</verification>

<success_criteria>
- SC1 closed on prod: multiple-photo model live, single-image field superseded (PHOTO-01).
- SC5 satisfied: lossless migration on prod without wiping watches_catalog (D-10 / D-11).
- RLS subquery-caller gotcha verified non-blocking for owner reads on prod.
</success_criteria>

<output>
After completion, create `.planning/phases/60-multi-photo-schema-dal/60-04-SUMMARY.md`.
</output>
