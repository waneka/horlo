---
phase: 57.1-comment-ui-polish-own-watch-suppression
verified: 2026-05-24T18:00:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Desktop centering of wears-lane comment drawer (SC-1)"
    expected: "On /wears/[username], opening the comment drawer at 1024/1280/1440px shows content centered at ~640px, not stretched full-width"
    why_human: "Visual layout across viewport widths cannot be verified programmatically"
  - test: "iPhone home indicator clearance in wears drawer (SC-4)"
    expected: "On a real iPhone, the bottom of the comment drawer (compose box) sits clear of the home indicator bar"
    why_human: "safe-area-inset-bottom CSS value requires a real device; cannot measure visually in code"
  - test: "Optimistic badge increment/decrement feel (SC-5)"
    expected: "Posting a comment on the wears-lane or /wear/[wearEventId] immediately increments the count badge with no page refresh; deleting decrements it"
    why_human: "Interaction timing and optimistic update feel requires live interaction"
  - test: "/watch/[id] count badge updates after post (D-01)"
    expected: "After posting a comment on a watch, the WatchDetail count badge updates within a server roundtrip (via router.refresh), without a manual page reload"
    why_human: "RSC re-fetch timing requires a live network round-trip to verify"
  - test: "Own-watch: no compose box, no gate copy (SC-6) — BEHAVIOR REVERSAL NOTE"
    expected: "On YOUR OWN watch at /watch/[id], there is NO compose box AND NO 'Follow [username] to comment' gate copy — the comment list (if any) renders with nothing above it. NOTE: Phase 57 UAT Test 1 stated 'owner always sees the compose box' — SC-6 DELIBERATELY REVERSES this for watches only. The new correct behavior is owner sees NO compose box. Do not flag this as a regression."
    why_human: "Visual absence of a UI element and absence of a locked-state CTA can only be confirmed on the deployed app"
---

# Phase 57.1: Comment UI Polish + Own-Watch Suppression — Verification Report

**Phase Goal:** Polish the Phase 57 comment surfaces from prod UAT (center wears-lane comment drawer with desktop max-width, move Post button inline with compose input, render edit/delete controls inline, add drawer bottom spacing, make comment-count badge update optimistically on post) and suppress the compose box on the viewer's own watches (reverses owner-can-comment for watches only; wears and all other viewers unchanged).
**Verified:** 2026-05-24T18:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 (SC-1) | Wears-lane comment drawer content centered at max-width on desktop | VERIFIED | `WearCommentHost.tsx:94` — inner div `mx-auto w-full max-w-[640px] pb-[...]` inside `SheetContent`; `SheetContent` itself keeps full viewport width; inline variant's existing `md:max-w-[600px] md:mx-auto` is untouched (SC-1 is bottom-sheet only) |
| 2 (SC-2) | Post button sits inline with compose input on all surfaces | VERIFIED | `CommentCompose.tsx:50` root is `flex items-end gap-2`; textarea wrapped in `flex-1 min-w-0` column; Post button is a sibling with `flex-shrink-0 self-end min-h-[44px] min-w-[44px]`; old `justify-between` row is absent |
| 3 (SC-3 + D-04) | Edit/delete controls and delete confirm render inline with comment meta row | VERIFIED | `CommentItem.tsx:99` — meta row wrapped in `flex items-center gap-1 flex-wrap`; Pencil/Trash2 buttons at `size-3.5` inside that row (lines 114-138); `isDeleting` confirm also inside that same flex row (lines 140-158); old `gap-2 mt-1` and `gap-1 mt-1` below-body blocks are absent |
| 4 (SC-4) | Wears-lane drawer has adequate bottom spacing | VERIFIED | `WearCommentHost.tsx:94` — inner wrapper includes `pb-[calc(1.5rem+env(safe-area-inset-bottom))]` in the same class string as `max-w-[640px]` |
| 5 (SC-5 / CMNT-08 / CMNT-09) | Comment-count badge updates optimistically on post without refresh | VERIFIED | `WearCard.tsx:109` — `const [localCommentCount, setLocalCommentCount] = useState(commentCount)`; both badge guards read `localCommentCount` (lines 181, 205); four `onCountChange?.(` call sites in `CommentList.tsx` with correct signs: +1 on insert (L106), -1 on rollback (L116), -1 on delete (L151), +1 on delete-rollback (L171); `Math.max(0, n + delta)` guard on both `WearCommentHost` usages in WearCard (lines 235, 249) |
| 6 (SC-6 / CMNT-01 / CMNT-09 / GATE-03) | Owner sees no compose box on own watch; all other viewers unchanged; owner still sees count badge | VERIFIED | `page.tsx:59` — `const canCommentDisplay = isOwner ? false : canComment`; `page.tsx:172,177` — `canComment={canCommentDisplay}` and `suppressCompose={isOwner}` passed to `CommentThread`; `CommentThread.tsx:78` — `suppressCompose={suppressCompose}` passed to `CommentList`; `CommentList.tsx:179` — `{!suppressCompose && (canComment ? <CommentCompose> : <CommentGateLocked>)}` renders null when suppressed; count read at `page.tsx:78` keeps raw `canComment` (CMNT-09 preserved for owner); `WearCommentHost.tsx` never receives `suppressCompose` (GATE-03 path unchanged) |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/components/comment/CommentCompose.tsx` | SC-2 inline Post button layout | VERIFIED | `flex items-end` root; `flex-1 min-w-0` textarea wrapper; `flex-shrink-0 self-end min-h-[44px] min-w-[44px]` Post button; no `justify-between` |
| `src/components/comment/CommentItem.tsx` | SC-3 + D-04 inline controls on meta row | VERIFIED | `flex items-center gap-1 flex-wrap` meta container; Pencil/Trash2 at `size-3.5` inside row; `isDeleting` confirm inside row; no old below-body blocks |
| `src/components/wear/WearCommentHost.tsx` | SC-1 + SC-4 inner wrapper; SC-5 onCountChange pass-through | VERIFIED | `max-w-[640px] mx-auto pb-[calc(1.5rem+env(safe-area-inset-bottom))]` inner div in bottom-sheet branch; `onCountChange?` on both union variants (5 occurrences); no `suppressCompose` |
| `src/components/comment/CommentList.tsx` | SC-5 onCountChange branches; D-01 router.refresh; SC-6 suppressCompose | VERIFIED | 4 `onCountChange?.(` calls; `useRouter` from `next/navigation`; `router.refresh()` gated by `refreshRouteOnMutation`; `suppressCompose?: boolean` prop; compose slot returns null when suppressed |
| `src/components/wear/WearCard.tsx` | SC-5 local commentCount state + onCountChange on both hosts | VERIFIED | `useState(commentCount)` on line 109; `localCommentCount` referenced 5 times; `Math.max(0, n + delta)` on both `WearCommentHost` usages |
| `src/components/comment/CommentThread.tsx` | SC-6 suppressCompose pass-through; D-01 refreshRouteOnMutation | VERIFIED | `suppressCompose?: boolean` in `CommentThreadProps`; destructured; passed to `CommentList`; `refreshRouteOnMutation` prop set (bare boolean = true); no `use cache` directive |
| `src/app/watch/[id]/page.tsx` | SC-6 canCommentDisplay derivation; suppressCompose={isOwner}; count read on raw canComment | VERIFIED | `canCommentDisplay = isOwner ? false : canComment` on line 59; `suppressCompose={isOwner}` on line 177; `commentCount = canComment` on line 78 (raw canComment preserved) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `page.tsx` | `CommentThread` | `canComment={canCommentDisplay}` + `suppressCompose={isOwner}` | WIRED | Lines 172, 177 — both props present |
| `CommentThread.tsx` | `CommentList` | `suppressCompose={suppressCompose}` + `refreshRouteOnMutation` | WIRED | Lines 78, 81 — both props passed |
| `CommentList.tsx` | `WearCard` local state | `onCountChange?.( +1/-1)` at 4 optimistic branches | WIRED | Lines 106, 116, 151, 171 — correct signs |
| `CommentList.tsx` | Next.js router (RSC re-fetch) | `if (refreshRouteOnMutation) { router.refresh() }` | WIRED | Lines 138-140 — gated to /watch/[id] surface only via `refreshRouteOnMutation` (WR-02 fix: commit 51c19d3) |
| `WearCard.tsx` | `WearCommentHost` (both variants) | `onCountChange={(delta) => setLocalCommentCount((n) => Math.max(0, n + delta))}` | WIRED | Lines 235, 249 |
| `WearCommentHost.tsx` | `CommentList` (both variants) | `onCountChange={onCountChange}` | WIRED | Lines 108, 133 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `WearCard.tsx` badge | `localCommentCount` | `useState(commentCount)` initialized from server prop; `onCountChange` deltas from `CommentList` | Yes — seeded from real server-resolved count; deltas from real add/delete actions | FLOWING |
| `CommentList.tsx` router.refresh | N/A (RSC trigger) | `refreshRouteOnMutation` prop from `CommentThread` — fires after `addCommentAction` success | N/A — triggers RSC re-fetch, not a data variable | FLOWING |
| `page.tsx` canCommentDisplay | `canCommentDisplay` | `isOwner ? false : canComment` where `canComment` is from `canViewerCommentOnTarget` DAL call | Yes — derived from real DAL result | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — this is a CSS/JSX polish phase. The modified components are client-side React; no standalone runnable entry points exist for the comment surfaces without a live database connection. Per project convention, behavioral verification occurs on the Vercel prod deploy.

### Probe Execution

Step 7c: No probes defined or applicable for this phase (UI/CSS-only, no migration scripts, no shell probes in PLAN or SUMMARY).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CMNT-01 | Plans 01, 03 | A user can post a comment on an individual watch (refined: owner suppressed at display layer only) | SATISFIED | `suppressCompose={isOwner}` removes compose from owner's view; DAL GATE-04 unchanged; non-owner compose path unaffected |
| CMNT-08 | Plans 01, 02 | New comment appears optimistically at top; rolls back on failure | SATISFIED | Four `onCountChange` branches in `CommentList`; SC-3 inline controls on comment rows; badge updates via `localCommentCount` |
| CMNT-09 | Plans 02, 03 | Comment count shows on watch detail and wear detail | SATISFIED | `WearCard` badge reads `localCommentCount` (local state); `page.tsx` count read keeps raw `canComment` so owner badge preserved |
| GATE-03 | Plan 03 | Non-mutual-follower on wishlist watch sees locked-state CTA | SATISFIED | `WearCommentHost` never passes `suppressCompose`; `CommentList` `!suppressCompose &&` guard leaves the `canComment ? compose : gate` branch intact for non-owners |

Note on REQUIREMENTS.md traceability table: CMNT-01, CMNT-08, CMNT-09, and GATE-03 are all mapped to Phase 57 as "Complete" in the traceability table. Phase 57.1 refines these requirements — the traceability table is not contradicted because Phase 57.1 is marked as an inserted polish phase that refines (not replaces) Phase 57 work. No update to the traceability table is required; the requirements remain satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/components/comment/CommentList.tsx` | 69 | Stale comment: "composeBody ref for clearing on success — CommentCompose is controlled by the parent" — `composeKey` is a remount counter, not a ref; CommentCompose owns its own state | Info | Misleads future readers; no behavioral impact (IN-02 from REVIEW.md) |

No TBD/FIXME/XXX markers found in phase-modified files. No unresolved debt markers. The IN-02 stale comment is informational only.

**WR-01 known limitation (from REVIEW.md):** The delete path does not call `router.refresh()` on `/watch/[id]`, so after a comment delete the WatchDetail count badge stays at the pre-delete value until a full page reload. The WR-02 fix commit (51c19d3) explicitly documents this as a "known best-effort limitation consistent with the documented /watch/[id] count caveat." This is not a gap against any phase success criterion (SC-5/D-01 only specify optimistic count on wears + post success on watch; delete-path refresh is not a stated requirement).

### Human Verification Required

The following must be confirmed on the Vercel prod deploy. See frontmatter `human_verification` for structured test instructions.

**1. Desktop centering of wears-lane drawer (SC-1)**

Test: Open the wears-lane comment drawer at 1024, 1280, and 1440px viewport widths.
Expected: Comment content column is centered at ~640px, not stretched full-width.
Why human: Visual layout cannot be verified by source grep.

**2. iPhone home indicator clearance (SC-4)**

Test: On a real iPhone, open the wears-lane bottom-sheet comment drawer.
Expected: Compose box and last comment sit clear of the home indicator bar.
Why human: `safe-area-inset-bottom` requires real-device verification.

**3. Optimistic count badge feel (SC-5)**

Test: Post a comment on the wears-lane or `/wear/[wearEventId]`; then delete it.
Expected: Count badge increments immediately on post; decrements on delete — no refresh required.
Why human: Interaction timing and optimistic update smoothness requires live interaction.

**4. /watch/[id] count badge update after post (D-01)**

Test: Post a comment on a watch you can comment on.
Expected: The count badge in the WatchDetail header updates within a server roundtrip after the post (no manual refresh).
Why human: RSC re-fetch timing requires a live network roundtrip.

**5. Own-watch: no compose, no gate copy (SC-6) — BEHAVIOR REVERSAL**

Test: View YOUR OWN watch at `/watch/[id]`.
Expected: There is NO compose box AND NO "Follow [username] to comment" gate copy. The comment list (if any) renders with nothing above it.
IMPORTANT: Phase 57 UAT Test 1 stated "owner always sees the compose box." SC-6 DELIBERATELY REVERSES this for watches. The new correct behavior is the owner sees NO compose box. This is not a bug.
Why human: Visual absence of UI elements must be confirmed on the deployed app.

### Gaps Summary

No gaps. All 6 roadmap success criteria are verified structurally. The WR-01 known limitation (delete-path refresh on `/watch/[id]`) does not violate any success criterion. The WR-03 Math.max clamp desync risk is a documented design tradeoff (D-03) and not a criterion violation.

---

_Verified: 2026-05-24T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
