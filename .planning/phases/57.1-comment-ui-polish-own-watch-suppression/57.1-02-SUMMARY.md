---
phase: 57.1-comment-ui-polish-own-watch-suppression
plan: 02
subsystem: ui
tags: [react, nextjs, optimistic-ui, zustand, client-state]

requires:
  - phase: 57-comment-thread-ui-feed-extension-grid-counts
    provides: WearCommentHost, CommentList, WearCard with commentCount prop chain established
  - phase: 57.1-comment-ui-polish-own-watch-suppression plan 01
    provides: SC-2/SC-3 CommentCompose inline Post button + CommentItem inline edit/delete controls

provides:
  - SC-1: bottom-sheet drawer content centered at max-w-[640px] mx-auto on desktop
  - SC-4: inner wrapper pb-[calc(1.5rem+env(safe-area-inset-bottom))] clears iPhone home indicator
  - SC-5/CMNT-08/CMNT-09: WearCard local commentCount state + onCountChange callback chain (WearCard -> WearCommentHost -> CommentList)
  - D-01: router.refresh() on post success in CommentList refreshes /watch/[id] RSC count badge
  - D-03: optimistic count is local-only with Math.max(0) guard; self-corrects on next page load

affects: [57.1-plan-03-own-watch-suppression, /wears/[username], /wear/[wearEventId], /watch/[id]]

tech-stack:
  added: []
  patterns:
    - "Optimistic count callback chain: WearCard local useState -> onCountChange prop -> WearCommentHost pass-through -> CommentList call sites"
    - "router.refresh() from next/navigation for RSC re-fetch after client mutation (D-01 pattern)"
    - "Safe-area-inset bottom padding via CSS calc() in bottom-sheet inner wrapper"

key-files:
  created: []
  modified:
    - src/components/wear/WearCommentHost.tsx
    - src/components/comment/CommentList.tsx
    - src/components/wear/WearCard.tsx

key-decisions:
  - "onCountChange is optional on both WearCommentHostProps union variants — no existing callers break"
  - "router.refresh() placed after setComposeKey (Pitfall 3 ordering: compose cleared first, then refresh)"
  - "localCommentCount in WearCard uses Math.max(0, n + delta) guard — prevents negative badge display"
  - "onCountChange rollback covers both gate-reject and generic-fail paths with single -1 call (per plan D-03 scope)"
  - "No onCountChange call on success reconcile path — the optimistic +1 already stands per UI-SPEC D-03 note"
  - "router.refresh() fires on all CommentList surfaces (wears + watch) — harmless on wears as local state survives RSC merge per Next.js docs"

patterns-established:
  - "Optimistic count propagation via optional callback (not context/store): minimal blast radius, scoped to WearCard"
  - "router.refresh() (next/navigation) as the correct client-side RSC refresh mechanism — NOT refresh() from next/cache (server-only)"

requirements-completed: [CMNT-08, CMNT-09]

duration: 7min
completed: 2026-05-24
---

# Phase 57.1 Plan 02: SC-1+SC-4 Drawer Centering + SC-5 Optimistic Count + D-01 router.refresh Summary

**Wears bottom-sheet comment drawer centered at 640px with safe-area padding; comment count badge updates optimistically via WearCard local state + onCountChange callback chain; /watch/[id] RSC re-fetches count after post via router.refresh()**

## Performance

- **Duration:** 7 min
- **Started:** 2026-05-24T16:52:12Z
- **Completed:** 2026-05-24T16:59:00Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- SC-1 + SC-4: bottom-sheet SheetContent gets an inner `<div className="mx-auto w-full max-w-[640px] pb-[calc(1.5rem+env(safe-area-inset-bottom))]">` wrapper — centers comment drawer content at 640px on desktop, clears iPhone home indicator
- SC-5 + CMNT-08/09: WearCard now owns `localCommentCount` state initialized from prop; the full `WearCard → WearCommentHost → CommentList` onCountChange callback chain propagates optimistic +1/-1 deltas; badge reads local state not raw prop
- D-01: CommentList imports useRouter from next/navigation and calls router.refresh() after setComposeKey on post success — /watch/[id] RSC count badge updates without manual refresh

## Task Commits

1. **Task 1: SC-1+SC-4+SC-5 in WearCommentHost** - `0b566c1` (feat)
2. **Task 2: SC-5 onCountChange + D-01 router.refresh in CommentList** - `afbb5e1` (feat)
3. **Task 3: SC-5 local count state in WearCard** - `49b9fda` (feat)

## Files Created/Modified

- `src/components/wear/WearCommentHost.tsx` - Added inner 640px/safe-area wrapper in bottom-sheet variant; added onCountChange? to both union variants and pass-through to CommentList
- `src/components/comment/CommentList.tsx` - Added useRouter import + const; added onCountChange? prop; wired four optimistic branches (+1 insert, -1 rollback, -1 delete, +1 rollback-delete); added router.refresh() on success
- `src/components/wear/WearCard.tsx` - Added localCommentCount useState; replaced both badge guard/value references; added onCountChange with Math.max(0) guard on both WearCommentHost usages

## Decisions Made

- **router.refresh() fires on all surfaces (not scoped to /watch/[id]):** The plan's interface notes explicitly allow firing on wears surfaces since local optimistic state survives the RSC merge per Next.js docs. The simpler approach (no conditional) is correct.
- **Single -1 in insert rollback covers both gate-reject and generic-fail:** The gate path also sets canComment=false; the count decrement is complementary and correct in both code paths.
- **No count call on success reconcile path:** The UI-SPEC note is explicit — the optimistic +1 from insert already stands.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Pre-existing lint errors (1090 problems) exist in the codebase across unrelated files. None were introduced by this plan's changes. WearCommentHost, CommentList, and WearCard showed zero lint errors.

## Known Stubs

None. All three files wire real data flows — no placeholder values or hardcoded empty states introduced.

## Threat Flags

No new security-relevant surface introduced. All changes are client-side optimistic UI (display-only deltas in local state) and a standard router.refresh() RSC re-fetch. The existing STRIDE register in the plan's threat_model covers all changes (T-57.1-02/03/04 all accepted).

## Self-Check

- [x] WearCommentHost.tsx modified and committed at 0b566c1
- [x] CommentList.tsx modified and committed at afbb5e1
- [x] WearCard.tsx modified and committed at 49b9fda
- [x] npm run build: green (Compiled successfully + 34/34 static pages)
- [x] Structural greps: all pass (max-w-[640px], pb-calc, onCountChange x5, router.refresh(), useState(commentCount), localCommentCount x5, Math.max)

## Self-Check: PASSED

## Next Phase Readiness

- Plan 03 (SC-6 own-watch compose suppression) can proceed — it edits CommentList.tsx (now complete) and CommentThread.tsx + page.tsx (untouched by this plan)
- The onCountChange prop is already wired on CommentList; Plan 03 only adds suppressCompose — no conflict

---
*Phase: 57.1-comment-ui-polish-own-watch-suppression*
*Completed: 2026-05-24*
