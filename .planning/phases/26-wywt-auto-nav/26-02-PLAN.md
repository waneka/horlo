---
phase: 26-wywt-auto-nav
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/wywt/ComposeStep.tsx
autonomous: true
requirements: [WYWT-20]
tags: [next, navigation, useTransition, sonner, react-19]

must_haves:
  truths:
    - "After a successful WYWT post (both `uploadWearPhoto` and `logWearWithPhoto` resolve without error), `router.push('/wear/${wearEventId}')` fires inside the existing `useTransition` callback, BEFORE `onSubmitted()` closes the dialog."
    - "If `uploadWearPhoto` returns an `{error}` shape, `router.push` does NOT fire (early return preserves the existing error UX) — guarantees no race lands the user on `/wear/{id}` before the storage object exists."
    - "If `logWearWithPhoto` returns `{success: false, error}`, `router.push` does NOT fire (early return preserves the existing error UX) — guarantees no race lands the user on `/wear/{id}` before the DB row exists."
    - "The `toast.success('Wear logged')` call is removed from `handleSubmit` — the destination page IS the celebration moment (D-03)."
    - "The `import { toast } from 'sonner'` line is removed if no other `toast.*` call remains in `ComposeStep.tsx` (D-03 import-hygiene rule)."
    - "Navigation uses `router.push` (not `router.replace`) so browser back returns to the trigger page (D-04)."
    - "`useRouter` is imported from `next/navigation` (matches established pattern in CollectionTabContent.tsx)."
  artifacts:
    - path: "src/components/wywt/ComposeStep.tsx"
      provides: "Submit handler that auto-navigates to /wear/{wearEventId} after both awaits succeed"
      contains: "router.push(`/wear/${wearEventId}`)"
  key_links:
    - from: "src/components/wywt/ComposeStep.tsx (handleSubmit)"
      to: "Next.js client router"
      via: "useRouter().push inside startTransition"
      pattern: "router\\.push\\(`/wear/\\$\\{wearEventId\\}`\\)"
---

<objective>
Insert `router.push('/wear/${wearEventId}')` into the existing `handleSubmit` `useTransition` callback in `ComposeStep.tsx`, after both `uploadWearPhoto` and `logWearWithPhoto` resolve successfully and BEFORE `onSubmitted()`. Remove the now-redundant `toast.success('Wear logged')` call (and the `import { toast } from 'sonner'` line if no other usage remains).

Purpose: WYWT-20 — the v3.0 deferred celebration moment ships. The user lands on the artifact page (Plan 26-01's Suspense-wrapped `/wear/{id}`) instead of an ephemeral toast that disappears in 4 seconds.

Output: A single modified file (`ComposeStep.tsx`) with the navigation wired in and the toast removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-wywt-auto-nav/26-CONTEXT.md
@src/components/wywt/ComposeStep.tsx
@src/components/wywt/WywtPostDialog.tsx
@AGENTS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted directly from the codebase. -->

From src/components/wywt/ComposeStep.tsx (current handleSubmit, lines 242–280):
```tsx
const handleSubmit = () => {
  setError(null)
  startTransition(async () => {
    try {
      if (photoBlob) {
        const upload = await uploadWearPhoto(viewerId, wearEventId, photoBlob)
        if ('error' in upload) {
          setError('Photo upload failed — please try again.')
          return
        }
      }
      const result = await logWearWithPhoto({ ... })
      if (!result.success) {
        setError(result.error)
        return
      }
      // H-2: toast fires from Client Component only.
      toast.success('Wear logged')
      onSubmitted()
    } catch (err) {
      console.error('[ComposeStep] submit error:', err)
      setError('Could not log that wear. Please try again.')
    }
  })
}
```

The `wearEventId` is passed in as a prop (line 94) — already available, do NOT re-derive.
The `onSubmitted` callback closes the dialog (line 102 / 192 in WywtPostDialog).

Established pattern (existing in this codebase, e.g. src/components/profile/CollectionTabContent.tsx):
```tsx
import { useRouter } from 'next/navigation'
const router = useRouter()
router.push('/some/path')
```

Toast usage audit (must verify in Task 1):
The current file imports `toast` only for the `toast.success('Wear logged')` call at line 273. After removal, `grep -c "toast\\." src/components/wywt/ComposeStep.tsx` should return 0 — at which point the `import { toast } from 'sonner'` line on line 10 must also be removed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire router.push, remove toast call, remove now-orphaned toast import</name>
  <files>src/components/wywt/ComposeStep.tsx</files>
  <read_first>
    - src/components/wywt/ComposeStep.tsx (full file — the surgical change is in handleSubmit ~lines 242–280, the imports are at top)
    - src/components/wywt/WywtPostDialog.tsx (for context — wearEventId is generated via useMemo(crypto.randomUUID(), [open]) and threaded as a prop; same id is used by Storage path + Server Action insert, so this is the canonical id to push to)
    - .planning/phases/26-wywt-auto-nav/26-CONTEXT.md (D-03 toast removal, D-04 push not replace, D-07 ordering is LOCKED)
  </read_first>
  <action>
This task makes **three coordinated edits** to `src/components/wywt/ComposeStep.tsx`. All three must land in a single change to keep the file in a compilable, lint-clean state.

**EDIT A — Add the `useRouter` import.**

Find the import block at the top of the file. Replace the existing line:

```tsx
import { toast } from 'sonner'
```

…by:

1. Removing it entirely (per EDIT C below).
2. Adding a new import line: `import { useRouter } from 'next/navigation'`

Place the `useRouter` import in alphabetical order with the other `next/*` imports if any exist; otherwise place it alongside the other framework imports near the top (above the `@/components/*` block). The exact line text MUST be:

```tsx
import { useRouter } from 'next/navigation'
```

**EDIT B — Insert `router.push` in `handleSubmit`, per D-07 locked order.**

Inside the `ComposeStep` function body, near the top (alongside `const [pending, startTransition] = useTransition()` on line 104), add:

```tsx
const router = useRouter()
```

Then, in the `handleSubmit` `startTransition` callback, replace this exact block (currently lines ~270–274):

```tsx
        if (!result.success) {
          setError(result.error)
          return
        }
        // H-2: toast fires from Client Component only. Plan 03a Server
        // Action does NOT import sonner.
        toast.success('Wear logged')
        onSubmitted()
```

with:

```tsx
        if (!result.success) {
          setError(result.error)
          return
        }
        // D-04 (Phase 26): standard router.push so browser back returns
        // to the trigger page. D-07 ordering is LOCKED — both awaits
        // (uploadWearPhoto + logWearWithPhoto) must succeed before this
        // fires; otherwise the user lands on /wear/{id} before the row
        // exists and sees a 404.
        router.push(`/wear/${wearEventId}`)
        onSubmitted()
```

The order in the `try` block is now:
1. `await uploadWearPhoto(...)` (only if `photoBlob` exists; early return on `{error}`)
2. `await logWearWithPhoto({...})` (early return if `!result.success`)
3. `router.push(\`/wear/${wearEventId}\`)`
4. `onSubmitted()`

This is D-07 verbatim. DO NOT reorder these four steps. DO NOT pull `router.push` outside the `try` block. DO NOT swap `router.push` for `router.replace` (D-04).

**EDIT C — Remove the toast import (it has no remaining usages).**

After EDIT B, the only `toast.*` reference in the file is gone. Remove the line:

```tsx
import { toast } from 'sonner'
```

(currently line 10) — and remove the `// H-2: toast fires from Client Component only. Plan 03a Server Action does NOT import sonner.` comment block above the deleted `toast.success` call (it's now misleading — that comment is preserved historically in the file's top docblock at line ~75 which can stay, since that's an architectural note about Server Actions, not an annotation on the deleted line).

The top docblock comment about H-2 (around lines 75–77) MAY stay — it's documenting the architectural rule that Server Actions don't import sonner, which is still true and still useful context for future readers.

**Verification of import hygiene:** After all three edits, `grep -c "toast" src/components/wywt/ComposeStep.tsx` should return 0 IF EITHER (a) no other toast usage exists in the file, OR a small number (≤2) representing only the historical comment in the top docblock. If you find any other `toast.success` / `toast.error` / `toast(` call elsewhere in the file you did not anticipate, STOP and surface — but per CONTEXT.md D-03 and the file-as-read, line 273 is the only call site.

**DO NOT touch:**
- The H-2 architectural comment in the top docblock (lines ~70–77) — that comment is about Server Actions, still true.
- The `try` / `catch` shape — keep `catch (err)` and `setError('Could not log that wear. Please try again.')`.
- The order of the early-return guards.
- Any other `handleTapCamera`, `handlePhotoReady`, `handleRetake`, `handleChooseAnother`, `handleCancelCamera`, `handleRemovePhoto` handlers — they are out of scope.

If anything about `useRouter` semantics on Next.js 16 client components is uncertain, consult `node_modules/next/dist/docs/` BEFORE writing per AGENTS.md.
  </action>
  <verify>
    <automated>grep -q "import { useRouter } from 'next/navigation'" src/components/wywt/ComposeStep.tsx &amp;&amp; grep -q "const router = useRouter()" src/components/wywt/ComposeStep.tsx &amp;&amp; grep -q "router.push(\`/wear/\${wearEventId}\`)" src/components/wywt/ComposeStep.tsx &amp;&amp; ! grep -q "toast.success('Wear logged')" src/components/wywt/ComposeStep.tsx &amp;&amp; ! grep -q "^import { toast } from 'sonner'" src/components/wywt/ComposeStep.tsx &amp;&amp; ! grep -q "router.replace" src/components/wywt/ComposeStep.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint -- src/components/wywt/ComposeStep.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { useRouter } from 'next/navigation'"` returns 1.
    - `grep -c "const router = useRouter()"` returns 1.
    - `grep -c "router.push(\`/wear/\${wearEventId}\`)"` returns 1 (D-04 push, D-07 wearEventId from prop).
    - `grep -c "router.replace"` returns 0 (D-04 forbids replace).
    - `grep -c "toast.success('Wear logged')"` returns 0 (D-03 — toast removed).
    - `grep -c "^import { toast } from 'sonner'"` returns 0 (D-03 — orphaned import removed).
    - `grep -c "toast\\." src/components/wywt/ComposeStep.tsx` returns 0 (no remaining toast call sites — confirms import removal is safe).
    - The `router.push` line appears AFTER the `if (!result.success)` early-return guard (verifiable by line ordering: `awk '/if \(!result.success\)/{a=NR} /router.push/{b=NR} END{exit !(a < b)}' src/components/wywt/ComposeStep.tsx` exits 0).
    - The `router.push` line appears BEFORE the `onSubmitted()` call (verifiable by `awk '/router.push/{a=NR} /onSubmitted\(\)/{if(!b)b=NR} END{exit !(a < b)}' src/components/wywt/ComposeStep.tsx` exits 0 — first onSubmitted() in handleSubmit comes after router.push).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint -- src/components/wywt/ComposeStep.tsx` exits 0.
  </acceptance_criteria>
  <done>ComposeStep.tsx imports useRouter from next/navigation, calls router.push(`/wear/${wearEventId}`) inside the existing startTransition between the success guard and onSubmitted(), and the orphaned toast.success + sonner import are removed. TypeScript and lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action (`logWearWithPhoto`) | Existing Phase 15 trust boundary. `wearEventId`, `note`, `visibility`, `hasPhoto` all validated server-side. No new surface from this plan. |
| Browser → Storage (`uploadWearPhoto`) | Existing Phase 15 trust boundary. Same `wearEventId` used here is used in the post-submit `router.push` URL. No new surface. |
| Submit handler → Browser router | New: `router.push(`/wear/${wearEventId}`)` is a CLIENT navigation. The destination URL is constructed from a UUID that the same client just generated (`useMemo(crypto.randomUUID(), [open])` in WywtPostDialog) — no untrusted input crosses into the URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering / Race | `router.push` firing before `logWearWithPhoto` resolves | mitigate | D-07 enforces strict ordering: both awaits MUST resolve `result.success === true` before `router.push` runs. Verified by acceptance criterion `awk '/if \(!result.success\)/{a=NR} /router.push/{b=NR} END{exit !(a < b)}'` — the early-return guard precedes the navigation. If `logWearWithPhoto` returns `{success: false}` the function early-returns, preserving existing error UX. |
| T-26-06 | Tampering / Race | `router.push` firing before `uploadWearPhoto` resolves | mitigate | Same `try` block, same early-return pattern. The `if ('error' in upload)` guard at the top of the photo branch guarantees the storage object exists before the DB insert (and thus before navigation). The DB row's existence guarantees the destination page won't 404. |
| T-26-07 | Information Disclosure | `wearEventId` in URL after `router.push` | accept | The destination page (`/wear/[wearEventId]`) is already publicly addressable via the existing DAL visibility gate (Phase 15 D-20/D-21 — `getWearEventByIdForViewer` enforces three-tier visibility). Putting the id in a URL the user themselves just submitted is the same exposure as the existing share/profile-feed flow. No new attack surface. |
</threat_model>

<verification>
- TypeScript compiles cleanly.
- ESLint passes on `ComposeStep.tsx`.
- File-level grep confirms: `useRouter` imported, `router.push` placed after success guard and before `onSubmitted()`, `toast.success('Wear logged')` removed, sonner import removed (assuming no other toast usage in the file).
- Manual smoke (deferred to executor): submit a successful WYWT post → dialog closes → URL changes to `/wear/<uuid>` → no toast appears → browser back returns to trigger page (router.push semantics).
- Regression check (deferred to executor): trigger a deliberate `logWearWithPhoto` failure (e.g., interrupted network) → URL does NOT change → inline `role="alert"` error appears → dialog stays open.
</verification>

<success_criteria>
1. After a successful WYWT post (both `uploadWearPhoto` and `logWearWithPhoto` resolve), the user is auto-navigated to `/wear/${wearEventId}` via `router.push` inside the existing `startTransition`.
2. `router.push` fires AFTER the success guard and BEFORE `onSubmitted()` (D-07 locked order).
3. The `toast.success('Wear logged')` call is removed (D-03).
4. The `import { toast } from 'sonner'` is removed (D-03 import hygiene).
5. Navigation uses `router.push`, not `router.replace` (D-04).
6. TypeScript and ESLint pass on the modified file.
</success_criteria>

<output>
After completion, create `.planning/phases/26-wywt-auto-nav/26-02-SUMMARY.md`.
</output>
