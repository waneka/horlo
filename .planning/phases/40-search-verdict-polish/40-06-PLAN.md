---
phase: 40
plan: 06
type: execute
wave: 2
depends_on: [40-02, 40-03]
files_modified:
  - src/components/insights/CollectionFitCompareTable.tsx
  - src/components/insights/CollectionFitCard.tsx
autonomous: true
requirements: [FIT-05]
tags: [verdict, ui, pure-renderer, compare-table]
must_haves:
  truths:
    - "CollectionFitCompareTable renders a 2-column compare table with 6 taste-dimension rows when both sides pass the confidence gate"
    - "FIT-05 section is ABSENT (module-absent-not-empty per D-15) when either side fails the gate or when verdict.mostSimilar.length === 0"
    - "Section title reads exactly 'Compare with the {brand} {model} you own' using mostSimilar[0].watch.brand/model"
    - "Column headers read 'This watch' (left, candidate) and 'Your {brand} {model}' (right)"
    - "Delta phrase line below the table renders the output of computeDeltaPhrase from 40-03"
    - "CollectionFitCard.tsx still passes tests/static/CollectionFitCard.no-engine.test.ts — no imports of @/lib/similarity or @/lib/verdict/composer"
    - "CollectionFitCompareTable.tsx itself imports no engine modules — only @/lib/types, @/lib/verdict/fit-delta, @/components/ui/badge, @/lib/utils"
    - "Scalar values render as bar + percentage; null scalars render as em-dash; archetype/era display with displayEnum transform; motifs render as Badge cluster"
  artifacts:
    - path: "src/components/insights/CollectionFitCompareTable.tsx"
      provides: "Pure-renderer compare table component for FIT-05"
      contains: "CollectionFitCompareTable"
    - path: "src/components/insights/CollectionFitCard.tsx"
      provides: "FIT-05 section mounted between mostSimilar list (line 86) and role-overlap warning (line 89)"
      contains: "CollectionFitCompareTable"
  key_links:
    - from: "src/components/insights/CollectionFitCard.tsx"
      to: "src/components/insights/CollectionFitCompareTable.tsx"
      via: "JSX render in CardContent"
      pattern: "<CollectionFitCompareTable"
    - from: "src/components/insights/CollectionFitCompareTable.tsx"
      to: "src/lib/verdict/fit-delta.ts computeDeltaPhrase"
      via: "named import"
      pattern: "computeDeltaPhrase"
---

<objective>
Build the FIT-05 pairwise drill-down section: a 2-column compare table that renders below the existing `mostSimilar` list in `CollectionFitCard`, comparing the candidate watch against `mostSimilar[0].watch` across 6 CAT-13 taste dimensions, with a single-line delta phrase below. The component is a pure renderer with no engine imports, sitting inside the Phase 20 D-04 invariant boundary. Confidence gating (D-15) hides the entire section when either side has low or null confidence — module-absent-not-empty per Phase 39b D-39b-07 pattern.

Purpose: Deliver FIT-05's user-facing surface. With the type extension from 40-02 + delta helper from 40-03 in place, this plan wires them into the existing card.

Output: One NEW component file (`CollectionFitCompareTable.tsx`) + one CollectionFitCard edit to mount it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-search-verdict-polish/40-CONTEXT.md
@.planning/phases/40-search-verdict-polish/40-RESEARCH.md
@.planning/phases/40-search-verdict-polish/40-PATTERNS.md
@.planning/phases/40-search-verdict-polish/40-UI-SPEC.md
@.planning/phases/40-search-verdict-polish/40-02-SUMMARY.md
@.planning/phases/40-search-verdict-polish/40-03-SUMMARY.md
@AGENTS.md

<interfaces>
From `src/lib/types.ts:220-229` `CatalogTasteAttributes`:
```
formality: number | null
sportiness: number | null
heritageScore: number | null
primaryArchetype: PrimaryArchetype | null   // 10 closed values
eraSignal: EraSignal | null                  // 3 closed values: 'vintage-leaning' | 'modern' | 'contemporary'
designMotifs: string[]
confidence: number | null
extractedFromPhoto: boolean
```

From `src/lib/verdict/types.ts` (post-40-02): `VerdictBundleFull.candidateCatalogTaste: CatalogTasteAttributes | null`.

From `src/lib/verdict/fit-delta.ts` (post-40-03): `export function computeDeltaPhrase(candidate: CatalogTasteAttributes, owned: CatalogTasteAttributes): string`.

From `src/components/insights/CollectionFitCard.tsx`:
- Line 5: existing `import type { VerdictBundle } from '@/lib/verdict/types'` — already covers the extended type.
- Lines 62-86: `mostSimilar` list block ends at line 86 (closing `</div>`).
- Lines 88-94: role-overlap warning block.
- Insertion point: BETWEEN line 86 (after closing `</div>` of mostSimilar) and line 88 (`{/* Role-overlap warning */}` comment + roleOverlap conditional). This is inside `<CardContent>` (lines 44-95), so the FIT-05 section gets the same `space-y-4` parent spacing.

**Watch.catalogTaste optionality** (load-bearing for the D-15 gate): `Watch.catalogTaste?: CatalogTasteAttributes | null` — the field is OPTIONAL on the `Watch` interface, meaning at runtime it may be `null` OR `undefined`. The owned-side guard MUST use loose `!= null` (which matches both `null` and `undefined`), NOT strict `!== null` (which only matches `null` and lets `undefined` pass through, producing a runtime TypeError on the subsequent `.confidence` access). PATTERNS.md § CollectionFitCard insertion (line 596-598) shows `!== null` for this check — that snippet is WRONG for this specific check and must be overridden in the executor's implementation. The candidate-side guard (`verdict.candidateCatalogTaste`) is post-40-02 typed exactly `CatalogTasteAttributes | null` (never undefined), so strict `!== null` is correct on that side.

From `src/components/ui/badge.tsx`: `Badge` accepts `variant?: 'default'|'secondary'|'destructive'|'outline'` and any standard div props. `variant="outline"` is the chosen motif chip variant (UI-SPEC §Design Motifs rendering line 446).

From `tests/static/CollectionFitCard.no-engine.test.ts` (lines 1-41): three import-boundary assertions on `src/components/insights/CollectionFitCard.tsx` ONLY. The new file `CollectionFitCompareTable.tsx` is NOT covered by this guard — but the project-wide convention says new files in `src/components/insights/` should follow the same pure-renderer rule. Adding `CollectionFitCompareTable.tsx` to a static guard is OPTIONAL but recommended for forward compatibility.

UI-SPEC copy templates and structural rules:
- Section title: `Compare with the {ownedBrand} {ownedModel} you own` (single h4)
- Column headers: `This watch` (left) / `Your {ownedBrand} {ownedModel}` (right)
- 6 dimension rows in order: Formality, Sportiness, Heritage, Archetype, Era, Design Motifs
- Scalar cells: bar `<div className="h-1.5 rounded-full bg-border flex-1 overflow-hidden">` with inner accent fill at `width: ${Math.round(value * 100)}%`, plus numeric percentage `<span className="text-xs text-muted-foreground w-8 text-right shrink-0">{...}%</span>`. Null → em-dash `—`.
- Enum cells: `displayEnum(value)` = `(val ?? '—').replace(/_/g, ' ').replace(/\\b\\w/g, c => c.toUpperCase())`.
- Motif cells: chip cluster — one Badge per motif, capitalized first letter. Empty → em-dash.
- Grid: `grid grid-cols-2 gap-px bg-border rounded-md overflow-hidden text-sm` (the `gap-px` + `bg-border` creates 1px row/column dividers). Each cell has `bg-background px-2 py-2` (header cells: `bg-muted ... text-xs uppercase tracking-wide font-semibold`).
- 2 columns at ALL breakpoints per D-13 (no `sm:grid-cols-2` responsive variant).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CollectionFitCompareTable pure-renderer component</name>
  <files>src/components/insights/CollectionFitCompareTable.tsx</files>
  <read_first>
    - src/components/insights/CollectionFitCard.tsx (entire file — references the pattern this new component lives alongside)
    - src/components/ui/badge.tsx (Badge variant API)
    - src/lib/types.ts (CatalogTasteAttributes interface)
    - src/lib/verdict/fit-delta.ts (post-40-03 — computeDeltaPhrase signature)
    - .planning/phases/40-search-verdict-polish/40-PATTERNS.md § `src/components/insights/CollectionFitCompareTable.tsx` (NEW) — full code skeleton including ScalarCell, displayEnum, MotifsCell helpers
    - .planning/phases/40-search-verdict-polish/40-UI-SPEC.md § Surface 2 FIT-05 + § 6 Compare Rows + § Section Structure
    - .planning/phases/40-search-verdict-polish/40-CONTEXT.md § D-12 through § D-15
    - tests/no-raw-palette.test.ts (font-medium forbidden)
  </read_first>
  <behavior>
    File starts with no `'use client'` directive AND no `'server-only'` directive — it's a universal pure component (passes through whichever bundle the parent lives in). `CollectionFitCard.tsx` has no `'use client'` directive (it is a Server Component per Phase 20), so importing `CollectionFitCompareTable` from it must not require client-only APIs.

    Imports (exactly these; no forbidden imports):
    - `import { Badge } from '@/components/ui/badge'`
    - `import type { CatalogTasteAttributes } from '@/lib/types'`
    - `import { computeDeltaPhrase } from '@/lib/verdict/fit-delta'`

    Exports:
    - `export function CollectionFitCompareTable(props: CollectionFitCompareTableProps): JSX.Element`
    - `CollectionFitCompareTableProps` interface: `{ candidate: CatalogTasteAttributes; owned: CatalogTasteAttributes; ownedBrand: string; ownedModel: string }`

    Layout — render in order:
    1. Wrapping `<div className="flex flex-col gap-3">`
    2. Section title: `<h4 className="text-base font-semibold text-foreground">Compare with the {ownedBrand} {ownedModel} you own</h4>`
    3. Compare grid: `<div className="grid grid-cols-2 gap-px bg-border rounded-md overflow-hidden text-sm">` with 14 cells total (2 header cells + 12 dimension cells = 6 rows of 2 cells each, plus the header row). Each cell uses `bg-background px-2 py-2 text-foreground text-xs` (except header cells which use `bg-muted px-2 py-2 font-semibold text-foreground text-xs uppercase tracking-wide`).
    4. Delta phrase: `<p className="text-sm text-muted-foreground">{computeDeltaPhrase(candidate, owned)}</p>`

    Cell content rules:
    - **Header row:** left = `This watch`, right = `Your {ownedBrand} {ownedModel}` (interpolated).
    - **Dimension rows** (6 total): each row is two cells. The dimension LABEL is NOT a separate column per UI-SPEC §Section Structure — instead each row shows candidate VALUE on the left and owned VALUE on the right with no left-side label column. (Re-read UI-SPEC §Section Structure example: the grid has 2 columns; each row is `candidate value | owned value`. A small dimension label can be prepended ABOVE each row as a separate non-grid element, OR the dimension can be inferred from cell content.) PATTERNS.md §Core table pattern shows the structure as a `<table>` semantic with `<th>` row headers. Use the `<table>` form for screen-reader compatibility per UI-SPEC §Accessibility ("auto-resolved: use `<table>`").

    Revised structure (semantic table for a11y):
    ```
    <table className="w-full text-sm">
      <thead>
        <tr>
          <th className="sr-only">Dimension</th>
          <th className="bg-muted px-2 py-2 font-semibold text-foreground text-xs uppercase tracking-wide text-left">This watch</th>
          <th className="bg-muted px-2 py-2 font-semibold text-foreground text-xs uppercase tracking-wide text-left">Your {ownedBrand} {ownedModel}</th>
        </tr>
      </thead>
      <tbody className="divide-y divide-border">
        <tr><th scope="row" className="px-2 py-2 text-left text-xs text-muted-foreground font-normal">Formality</th><td>{ScalarCell value=candidate.formality}</td><td>{ScalarCell value=owned.formality}</td></tr>
        ...5 more rows...
      </tbody>
    </table>
    ```

    Actually, UI-SPEC §Section Structure shows a 2-column grid without a left label column. PATTERNS.md §Core table pattern shows `grid grid-cols-2` and lists 6 dimension rows as 2-cell pairs (no label cell). The dimension labels appear as compact prefixes ABOVE each value pair OR are omitted entirely (relying on a separate "Formality / Sportiness / etc." legend row).

    **Reconciliation:** Use a 3-column semantic `<table>` (label / candidate / owned) for screen-reader compatibility per UI-SPEC §Accessibility while visually compressing the label column with `text-xs text-muted-foreground` to keep it compact. `grid-cols-[auto_1fr_1fr]` is the CSS-grid equivalent if a `<table>` is undesirable, but the `<table>` is preferred per UI-SPEC §Accessibility ("auto-resolved: use `<table>` for screen-reader compatibility; dimension labels as row headers"). D-13's "max 2 items on mobile" refers to the VALUE columns (no third value column ever) — adding a compact label column is consistent with this constraint.

    Helper functions (file-private, NOT exported):
    - `ScalarCell({ value }: { value: number | null })` — renders the bar+percentage cell content per UI-SPEC §Scalar rendering lines 423-436. Null → `<span className="text-xs text-muted-foreground">—</span>`.
    - `displayEnum(val: string | null): string` — `val == null ? '—' : val.replace(/_/g, ' ').replace(/\\b\\w/g, c => c.toUpperCase())`.
    - `MotifsCell({ motifs }: { motifs: string[] })` — renders flex-wrap of `<Badge variant="outline" className="text-xs px-1.5 py-0">{capitalize(m)}</Badge>` per motif; empty → em-dash.

    A11y: scalars get `role="meter" aria-valuenow={pct} aria-valuemin={0} aria-valuemax={100} aria-label="{dimension} {pct}%"` on the outer bar wrapper (UI-SPEC §Accessibility line 561). Header cells use `scope="col"`; row label `<th>` uses `scope="row"`.

    **Forbidden imports (none of these):** `@/lib/similarity`, `@/lib/verdict/composer`, `server-only`, `@/lib/verdict/viewerTasteProfile`. The static guard at `tests/static/CollectionFitCard.no-engine.test.ts` checks the PARENT card only; the new compare table inherits the convention and remains pure-renderer.

    Note on row labels: the 6 row labels are "Formality", "Sportiness", "Heritage", "Archetype", "Era", "Design Motifs" (per UI-SPEC §6 Compare Rows table at lines 411-417). Rendered via a row `<th scope="row" className="px-2 py-2 text-left text-xs text-muted-foreground font-normal">{label}</th>`.
  </behavior>
  <action>Create `src/components/insights/CollectionFitCompareTable.tsx` with the exports + helper functions described above. Use the semantic `<table>` form with 3 columns (`label | candidate | owned`) for screen-reader compatibility; visually compress the label column with `text-xs text-muted-foreground font-normal`. Each `<th scope="row">` carries the dimension label; the two `<td>` cells hold the rendered values from ScalarCell / displayEnum / MotifsCell. Wrap the whole table in `<div className="flex flex-col gap-3">` with the section title `<h4 className="text-base font-semibold text-foreground">` above and the `<p className="text-sm text-muted-foreground">` delta phrase below. Compute the delta phrase via `computeDeltaPhrase(candidate, owned)` from the helper module. Use `font-semibold` everywhere a heading-weight class is needed; never `font-medium` (lint guard). Capitalize motif labels via `m.charAt(0).toUpperCase() + m.slice(1)` (UI-SPEC §Motifs Cell line 445).</action>
  <verify>
    <automated>npx vitest run tests/no-raw-palette.test.ts tests/static/CollectionFitCard.no-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/insights/CollectionFitCompareTable.tsx` exists
    - `grep -c "font-medium" src/components/insights/CollectionFitCompareTable.tsx` returns 0
    - `grep -E "from ['\"]@/lib/(similarity|verdict/composer)['\"]" src/components/insights/CollectionFitCompareTable.tsx` returns 0 matches (no forbidden imports)
    - `grep -c "computeDeltaPhrase" src/components/insights/CollectionFitCompareTable.tsx` returns at least 2 (import + invocation)
    - `grep -c "scope=\"row\"" src/components/insights/CollectionFitCompareTable.tsx` returns at least 6 (one per dimension row)
    - `tests/no-raw-palette.test.ts` green
    - `tests/static/CollectionFitCard.no-engine.test.ts` green (unchanged — CollectionFitCard not yet modified in this task)
    - `npx tsc --noEmit 2>&1 | grep -E "CollectionFitCompareTable\\.tsx" | wc -l` returns 0
  </acceptance_criteria>
  <done>Compare table component built; no forbidden imports; lint + static guards green. Commit: `feat(40-06): create CollectionFitCompareTable pure-renderer for FIT-05`.</done>
</task>

<task type="auto">
  <name>Task 2: Mount FIT-05 section in CollectionFitCard with D-15 confidence gate</name>
  <files>src/components/insights/CollectionFitCard.tsx</files>
  <read_first>
    - src/components/insights/CollectionFitCard.tsx (entire file — particularly lines 28-97, the body of CollectionFitCard function)
    - .planning/phases/40-search-verdict-polish/40-PATTERNS.md § `src/components/insights/CollectionFitCard.tsx` — insert FIT-05 section (lines 581-611) — NOTE: the `!== null` shown at lines 596-598 is INCORRECT for the owned-side guard; see <action> below for the override.
    - .planning/phases/40-search-verdict-polish/40-UI-SPEC.md § Surface 2 FIT-05 + § Confidence Gate (module-absent-not-empty)
    - .planning/phases/40-search-verdict-polish/40-CONTEXT.md § D-12 + § D-15
    - src/lib/types.ts (confirm `Watch.catalogTaste?: CatalogTasteAttributes | null` is OPTIONAL — that's why the owned-side guard uses loose `!= null`)
    - tests/static/CollectionFitCard.no-engine.test.ts (lines 1-41 — the import boundary it enforces)
  </read_first>
  <behavior>
    Insert into `src/components/insights/CollectionFitCard.tsx`:

    1. Add `import { CollectionFitCompareTable } from '@/components/insights/CollectionFitCompareTable'` to the top imports (alongside the existing imports at lines 1-5). This import is allowed by the static guard — `@/components/insights/CollectionFitCompareTable` is not in the forbidden list.

    2. Insert the FIT-05 section in the JSX BETWEEN the closing `</div>` of the `mostSimilar` block (currently line 86) and the `{/* Role-overlap warning */}` comment (currently line 88). The section is a confidence-gated JSX expression:
       ```
       {/* FIT-05 — Pairwise taste drill-down (D-12 through D-16; D-15 module-absent-not-empty) */}
       {verdict.mostSimilar.length > 0 &&
         verdict.candidateCatalogTaste !== null &&
         verdict.candidateCatalogTaste.confidence !== null &&
         verdict.candidateCatalogTaste.confidence >= 0.5 &&
         verdict.mostSimilar[0].watch.catalogTaste != null &&
         verdict.mostSimilar[0].watch.catalogTaste.confidence !== null &&
         verdict.mostSimilar[0].watch.catalogTaste.confidence >= 0.5 && (
         <CollectionFitCompareTable
           candidate={verdict.candidateCatalogTaste}
           owned={verdict.mostSimilar[0].watch.catalogTaste}
           ownedBrand={verdict.mostSimilar[0].watch.brand}
           ownedModel={verdict.mostSimilar[0].watch.model}
         />
       )}
       ```
       Use `!= null` (loose) for the optional `catalogTaste` field on `Watch` since `Watch.catalogTaste?: CatalogTasteAttributes | null` may be either `null` OR `undefined` at runtime. Use `=== null` (strict) for `candidateCatalogTaste` since it is typed exactly `CatalogTasteAttributes | null` post-40-02 (never undefined).

    3. The new section sits ABOVE the role-overlap warning (lines 88-94 unchanged) so the visual order in the card body is: headline → contextual → mostSimilar list → **FIT-05 compare** → role-overlap warning.

    DO NOT modify:
    - Lines 1-5 except for adding the new `import` statement on line 6
    - The `CollectionFitCardProps` interface
    - The early-return for `framing === 'self-via-cross-user'`
    - The mostSimilar list (lines 62-86) — Phase 39 D-07 Link wraps preserved
    - The role-overlap warning (lines 88-94)
    - `YouOwnThisCallout` function or `formatOwnedDate` helper
  </behavior>
  <action>Apply the named import addition and the FIT-05 JSX insertion exactly as specified. The 7-clause confidence gate matches D-15 precisely — both sides must have non-null `catalogTaste` AND non-null `confidence` AND `confidence >= 0.5`. When the gate fails for any reason (no collection, low confidence, null taste data), the section is ABSENT — no placeholder, no skeleton, no "data unavailable" message. The existing card sections (headline, contextual, mostSimilar, role-overlap) continue to render in all cases. The new import is permitted by the static guard since `@/components/insights/CollectionFitCompareTable` is not in the forbidden list — verify by re-running `tests/static/CollectionFitCard.no-engine.test.ts` post-edit.

  **CRITICAL OVERRIDE — owned-side null check operator:** The owned-side `catalogTaste` guard MUST use loose `!= null` (NOT strict `!== null`) because `Watch.catalogTaste?: CatalogTasteAttributes | null` is an OPTIONAL field on the `Watch` interface — at runtime it may be either `null` or `undefined`. Strict `!== null` only filters out `null`, letting `undefined` flow through to the next clause where `.confidence` access throws a runtime TypeError. The PATTERNS.md code skeleton at § CollectionFitCard insertion (lines 596-598) shows `!== null` — that snippet is WRONG for this specific check and must be overridden when authoring this insertion. Use loose `!= null` for both `verdict.mostSimilar[0].watch.catalogTaste != null` guard clauses. The candidate-side guard `verdict.candidateCatalogTaste !== null` STAYS strict since post-40-02 typing locks it to exactly `CatalogTasteAttributes | null` (never undefined). Also update PATTERNS.md § CollectionFitCard insertion to flip those two lines from `!== null` to `!= null` on the owned side, if practical, so the documented pattern is correct for future readers.</action>
  <verify>
    <automated>npx vitest run tests/static/CollectionFitCard.no-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CollectionFitCompareTable" src/components/insights/CollectionFitCard.tsx` returns at least 2 (import + JSX usage)
    - `grep -c "candidateCatalogTaste" src/components/insights/CollectionFitCard.tsx` returns at least 2 (gate check + prop pass)
    - `grep -c "catalogTaste != null" src/components/insights/CollectionFitCard.tsx` returns at least 1 (owned-side loose null check — D-15 + optional-field correctness)
    - `grep -E "from ['\"]@/lib/(similarity|verdict/composer|verdict/viewerTasteProfile)['\"]" src/components/insights/CollectionFitCard.tsx` returns 0 matches (forbidden imports STILL not present)
    - `tests/static/CollectionFitCard.no-engine.test.ts` green (3 assertions still pass)
    - `npx tsc --noEmit 2>&1 | grep -E "CollectionFitCard\\.tsx" | wc -l` returns 0
    - The confidence-gate expression has at least 6 `&&` operators (combining the mostSimilar-length check + 6 individual property checks: 3 for candidate side, 3 for owned side)
  </acceptance_criteria>
  <done>FIT-05 mounted in CollectionFitCard with the full D-15 confidence gate; owned-side guard uses loose `!= null` per `Watch.catalogTaste` optionality; pure-renderer invariant preserved. Commit: `feat(40-06): mount FIT-05 compare section in CollectionFitCard with confidence gate`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verdict bundle → CollectionFitCompareTable | `candidateCatalogTaste` and `mostSimilar[0].watch.catalogTaste` are produced server-side by `computeVerdictBundle` (Plan 40-02). They are RSC-serializable plain objects with primitive fields. |
| CollectionFitCompareTable → DOM | All rendered text (brand/model/motif labels, displayEnum output) flows through React JSX auto-escaping. No `dangerouslySetInnerHTML`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-18 | Tampering | XSS via brand/model/motif strings | mitigate | React auto-escapes all interpolated strings. No `dangerouslySetInnerHTML`. |
| T-40-19 | Information Disclosure | exposing low-confidence taste data | mitigate | D-15 confidence gate hides the entire section when either side has `confidence < 0.5` or `null` — module-absent-not-empty. |
| T-40-20 | Tampering | bypassing pure-renderer invariant via transitive import | mitigate | `tests/static/CollectionFitCard.no-engine.test.ts` enforces the boundary at compile-time scan; new `CollectionFitCompareTable.tsx` imports only `@/lib/types`, `@/lib/verdict/fit-delta`, `@/components/ui/badge`, `@/lib/utils` — none forbidden. |
| T-40-21 | Denial of Service | extremely large designMotifs array | accept | Catalog taste data is server-curated via Phase 19.1 closed vocab; max realistic motif count is ~4. No client-side iteration unbounded. |
</threat_model>

<verification>
```
npx vitest run tests/static/CollectionFitCard.no-engine.test.ts tests/no-raw-palette.test.ts
npx tsc --noEmit 2>&1 | grep -E "insights/(CollectionFitCard|CollectionFitCompareTable)\\.tsx" | wc -l
```
Expected: static guard green; lint green; 0 tsc errors in either file.

Manual smoke (recommended): sign in as `twwaneka@gmail.com`, visit `/search`, type `Submariner`, expand a verdict row — confirm:
1. Verdict header + contextual phrasings render unchanged
2. "Most Similar in Collection" list renders unchanged
3. NEW "Compare with the {brand} {model} you own" section renders below mostSimilar IF both sides have confidence >= 0.5
4. Role-overlap warning renders below FIT-05 (when applicable)
5. For a watch with `catalogTaste IS NULL` in DB (e.g., a freshly-imported reference): FIT-05 section is absent; all other sections render
6. For a watch where `catalogTaste` is `undefined` on the in-memory Watch (Drizzle LEFT JOIN row with no catalog row): FIT-05 section is absent; no TypeError thrown — loose `!= null` guard covers this case.
</verification>

<success_criteria>
- New file `src/components/insights/CollectionFitCompareTable.tsx` exists with the named exports and helper functions
- No forbidden imports in either touched file
- `tests/static/CollectionFitCard.no-engine.test.ts` green
- `tests/no-raw-palette.test.ts` green
- 0 new tsc errors in any touched file
- 7-clause confidence gate in CollectionFitCard.tsx matches D-15 precisely
- Owned-side `catalogTaste` guards use loose `!= null` (matching both `null` and `undefined` for the optional field)
- Compare table renders 6 dimensions in the order Formality / Sportiness / Heritage / Archetype / Era / Design Motifs
- Section visually sits between mostSimilar list and role-overlap warning
</success_criteria>

<output>
Create `.planning/phases/40-search-verdict-polish/40-06-SUMMARY.md` documenting:
- The semantic-table-with-3-columns choice (vs PATTERNS.md's 2-column grid) and the a11y rationale
- The 7-clause confidence-gate expression
- The owned-side loose `!= null` override vs PATTERNS.md's `!== null` — rationale: `Watch.catalogTaste` is optional, may be `undefined` at runtime
- Exact insertion line in CollectionFitCard.tsx
- Confirmation no forbidden imports introduced (static guard still green)
- A11y choices: scope="row" on dimension labels, role="meter" on scalar bars
</output>
