---
phase: 84-wear-history-depth
plan: 06
type: execute
wave: 3
depends_on: ["84-03", "84-04", "84-05"]
files_modified:
  - src/components/profile/WornTabContent.tsx
  - src/app/u/[username]/[tab]/page.tsx
autonomous: true
requirements: [WEAR-03, WEAR-04]

must_haves:
  truths:
    - "D-08: the Wear leaderboard section renders above the Timeline/Calendar toggle row on the Worn tab, always visible (not collapsible, not a third view)"
    - "D-08: the watch filter Select does not affect the leaderboard — the leaderboard receives the unfiltered viewer-gated events array"
    - "D-14: visitors see the leaderboard computed only from getWearEventsForViewer rows; for a non-owner viewer of a profile with collectionPublic=false, the leaderboard rows, filter options and watchMap contain only watches referenced by that viewer's visible wears"
    - "D-11: the leaderboard's watch source is status==='owned' watches (scoped per D-14)"
    - "No new await is added to [tab]/page.tsx and unstable_instant stays false (Pitfall 7)"
  artifacts:
    - path: "src/components/profile/WornTabContent.tsx"
      provides: "Leaderboard mounted above the view toggle in populated and empty states"
      contains: "<WearLeaderboard"
    - path: "src/app/u/[username]/[tab]/page.tsx"
      provides: "Worn branch passes scoped ownedWatches and watchMap"
      contains: "scopeWornTabWatches("
  key_links:
    - from: "src/app/u/[username]/[tab]/page.tsx"
      to: "src/lib/wornTabScope.ts#scopeWornTabWatches"
      via: "call inside tab === 'worn' branch using settings.collectionPublic"
      pattern: "scopeWornTabWatches\\(\\{"
    - from: "src/components/profile/WornTabContent.tsx"
      to: "src/components/profile/WearLeaderboard.tsx"
      via: "events (unfiltered) + ownedWatches props"
      pattern: "<WearLeaderboard events=\\{events\\} watches=\\{ownedWatches\\}"
---

<objective>
WEAR-03/WEAR-04 (wiring): mount `WearLeaderboard` (84-05) at the top of the Worn tab above the Timeline/Calendar row (D-08). Then fix the privacy gap RESEARCH Pitfall 5 found by routing the Worn-tab page's watch lists through `scopeWornTabWatches` (84-03). Result: a non-owner viewer of a private collection never receives, through leaderboard zero-wear rows, the filter dropdown or the watchMap, an owned watch they could not see on the Collection tab (D-14).

Purpose: this connects the tested leaderboard to live, viewer-gated data on the rendered route without disturbing the page's Suspense/`connection()` structure.
Output: two edited files. The local-dev walk happens in 84-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-wear-history-depth/84-CONTEXT.md
@.planning/phases/84-wear-history-depth/84-RESEARCH.md
@.planning/phases/84-wear-history-depth/84-03-SUMMARY.md
@.planning/phases/84-wear-history-depth/84-04-SUMMARY.md
@.planning/phases/84-wear-history-depth/84-05-SUMMARY.md

Rendered path (traced): `/u/[username]/[tab]`, then `ProfileTabPage` (sync; `await connection()`; `<Suspense>`), then `ProfileTabContent` (async), then `tab === 'worn'` branch (~L426-495), then `<WornTabContent>`. `settings` is already destructured from `resolved` at the top of ProfileTabContent (`const { profile, settings, wearEvents: ownerWearEvents } = resolved`), so no new fetch is needed.

<interfaces>
From src/lib/wornTabScope.ts (84-03):
export function scopeWornTabWatches<W extends { id: string; status: string }>(input: { isOwner: boolean; collectionPublic: boolean; watches: W[]; eventWatchIds: ReadonlySet<string> }): { ownedWatches: W[]; mapWatches: W[] }

From src/components/profile/WearLeaderboard.tsx (84-05):
export function WearLeaderboard(props: { events: Array<{ watchId: string; wornDate: string }>; watches: Array<{ id: string; brand: string; model: string; imageUrl?: string | null }> }): JSX.Element | null   // null when watches is empty

Current worn branch (page.tsx): `const events = await getWearEventsForViewer(viewerId, profile.id)`; `const watches = await signCoverUrls(resolved.watches)`; `watchMap = Object.fromEntries(watches.map(...))`; then `<WornTabContent ... watchMap={watchMap} ... ownedWatches={watches.filter((w) => w.status === 'owned')} />`.

WornTabContent after 84-04: owner empty state renders the card with `<LogTodaysWearButton ... className="w-full" />`; non-owner empty state renders "Nothing here yet."; populated state renders the header row (ViewTogglePill + filter Select + owner LogTodaysWearButton) then Timeline/Calendar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount WearLeaderboard above the view toggle in WornTabContent</name>
  <files>src/components/profile/WornTabContent.tsx</files>
  <read_first>
    - src/components/profile/WornTabContent.tsx (file being modified, post-84-04 state)
    - src/components/profile/WearLeaderboard.tsx (props contract)
    - .planning/phases/84-wear-history-depth/84-UI-SPEC.md ("Window-control segmented tabs" placement + Spacing lg between sections)
  </read_first>
  <action>
    In src/components/profile/WornTabContent.tsx (per D-08 / D-11):
    - (1) Add `import { WearLeaderboard } from './WearLeaderboard'`.
    - (2) Populated state: the outer `<div className="flex flex-col gap-4">` becomes `<div className="flex flex-col gap-6">` (UI-SPEC lg = 24px between the leaderboard block and the toggle section). Insert `<WearLeaderboard events={events} watches={ownedWatches} />` as its FIRST child, directly above the existing header row div. Wrap the existing header row plus the Timeline/Calendar render in a `<div className="flex flex-col gap-4">` so their internal spacing is unchanged.
    - (3) Pass the unfiltered `events` prop, NOT `filtered`, so the watch filter Select does not affect the leaderboard (D-08).
    - (4) Empty states (`events.length === 0`), both the owner and the non-owner branch: return `<div className="flex flex-col gap-6"><WearLeaderboard events={events} watches={ownedWatches} />{existing empty card}</div>`. This keeps the section "always visible" (D-08) and lists owned watches at "0 wears" (D-11). WearLeaderboard renders null when `ownedWatches` is empty, so a scoped non-owner or a zero-watch owner sees only the card.
    - (5) Do not change the ViewTogglePill, the filter Select, the LogTodaysWearButton props or the Timeline/Calendar props.
  </action>
  <verify>
    <automated>npx vitest run tests/components/profile/WearLeaderboard.test.tsx tests/components/profile/LogTodaysWearButton.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<WearLeaderboard events={events} watches={ownedWatches} />" src/components/profile/WornTabContent.tsx` returns 3 (populated + owner empty + non-owner empty)
    - `grep -c "<WearLeaderboard events={filtered}" src/components/profile/WornTabContent.tsx` returns 0
    - In the populated return, the `<WearLeaderboard` line number is lower than the `<ViewTogglePill` line number: `awk '/<WearLeaderboard/{w=NR} /<ViewTogglePill/{v=NR} END{exit !(w<v)}' src/components/profile/WornTabContent.tsx` exits 0
    - `grep -c "import { WearLeaderboard } from './WearLeaderboard'" src/components/profile/WornTabContent.tsx` returns 1
    - `grep -cE "font-medium|font-bold" src/components/profile/WornTabContent.tsx` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The leaderboard appears above the Timeline/Calendar controls in every Worn tab state that has owned watches to rank, fed by unfiltered events.</done>
</task>

<task type="auto">
  <name>Task 2: Scope Worn-tab watch lists by viewer in page.tsx (D-14 / T-84-LEAK)</name>
  <files>src/app/u/[username]/[tab]/page.tsx</files>
  <read_first>
    - src/app/u/[username]/[tab]/page.tsx (file being modified: lines 1-60 structural comments, ~L168 connection/Suspense, ~L295 collection gate, ~L426-495 worn branch)
    - src/lib/wornTabScope.ts (84-03 helper)
    - tests/static/ppr-dynamic-before-use-cache.test.ts (ordering guard this edit must keep green)
    - node_modules/next/dist/docs/01-app/01-getting-started/15-instant.md (skim only; confirms why the structure above Suspense is untouchable, AGENTS.md)
  </read_first>
  <action>
    In src/app/u/[username]/[tab]/page.tsx, inside the existing `if (tab === 'worn')` branch only:
    - (1) Add `import { scopeWornTabWatches } from '@/lib/wornTabScope'` to the import block.
    - (2) Immediately after `const watches = await signCoverUrls(resolved.watches)`, add `const { ownedWatches: scopedOwnedWatches, mapWatches } = scopeWornTabWatches({ isOwner, collectionPublic: settings.collectionPublic, watches, eventWatchIds: new Set(events.map((e) => e.watchId)) })`. Precede it with a comment citing Phase 84 D-14 / RESEARCH Pitfall 5 / T-84-LEAK: the Worn tab has no tab-level lock (Phase 12), so its watch lists mirror the Collection tab's collectionPublic gate, and non-owners only receive watches their visible wears reference.
    - (3) Build `watchMap` from `mapWatches.map(...)` instead of `watches.map(...)`, keeping the same `{ id, brand, model, imageUrl: w.imageUrl ?? null }` projection.
    - (4) Pass `ownedWatches={scopedOwnedWatches}` to WornTabContent instead of `watches.filter((w) => w.status === 'owned')`.
    - (5) Change nothing else: no new `await`, no edits above the worn branch, no change to `unstable_instant`, `connection()`, the Suspense wrapper, the wear-photo signing loop, or any other tab branch.
    - Do not reintroduce a LockedTabCard for the worn tab. Visitors must still see gated wears and the leaderboard (D-14).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/wornTabScope.test.ts tests/static/ppr-dynamic-before-use-cache.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { scopeWornTabWatches } from '@/lib/wornTabScope'" "src/app/u/[username]/[tab]/page.tsx"` returns 1
    - `grep -c "scopeWornTabWatches({" "src/app/u/[username]/[tab]/page.tsx"` returns 1
    - `grep -c "collectionPublic: settings.collectionPublic" "src/app/u/[username]/[tab]/page.tsx"` returns at least 2 (existing common-ground call + new worn call)
    - `grep -c "ownedWatches={scopedOwnedWatches}" "src/app/u/[username]/[tab]/page.tsx"` returns 1
    - `grep -c "mapWatches.map(" "src/app/u/[username]/[tab]/page.tsx"` returns 1
    - `grep -c "export const unstable_instant = false" "src/app/u/[username]/[tab]/page.tsx"` returns 1
    - `git diff "src/app/u/[username]/[tab]/page.tsx" | grep -c "^+.*await "` returns 0 (no new awaits)
    - `npx vitest run tests/static/ppr-dynamic-before-use-cache.test.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The Worn tab sends owners their full lists, sends collection-public visitors the full owned list, and sends private-collection visitors only watches referenced by their visible wears. The page structure is unchanged and the build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → client RSC payload | watchMap and ownedWatches are serialized to the browser for any viewer of /u/[username]/worn |
| viewer identity → DAL | getWearEventsForViewer applies the per-row three-tier visibility gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-LEAK | Information Disclosure | [tab]/page.tsx worn branch → WornTabContent/WearLeaderboard | mitigate | `scopeWornTabWatches({ isOwner, collectionPublic: settings.collectionPublic, watches, eventWatchIds })` limits `ownedWatches` and `watchMap` for non-owners. Private collections expose only watches already revealed by gated wears. Unit-proven in 84-03 and walked cross-user in 84-07. |
| T-84-06 | Information Disclosure | leaderboard counts | mitigate | Counts are derived only from `events` returned by `getWearEventsForViewer`: owner sees all; others need profile_public, public wears, and followers-only wears when following. Private and hidden wears never reach the client, so they never contribute. |
| T-84-07 | Denial of Service | PPR / React #419 recurrence | mitigate | No new await and no structural change above Suspense. Guarded by `tests/static/ppr-dynamic-before-use-cache.test.ts` and a diff check for added awaits. |
</threat_model>

<verification>
- `npx vitest run tests/unit/wornTabScope.test.ts tests/unit/leaderboard.test.ts tests/components/profile/WearLeaderboard.test.tsx tests/components/profile/LogTodaysWearButton.test.tsx tests/static/ppr-dynamic-before-use-cache.test.ts` exits 0.
- `npm run test` (full suite) shows no new failures attributable to Phase 84 files. Baseline failures elsewhere are expected.
- `npm run build` exits 0 (authoritative).
- No push until 84-07.
</verification>

<success_criteria>
The Worn tab shows the Wear leaderboard above Timeline/Calendar, fed by viewer-gated wears and privacy-scoped owned watches.
</success_criteria>

<output>
Create `.planning/phases/84-wear-history-depth/84-06-SUMMARY.md` when done
</output>
