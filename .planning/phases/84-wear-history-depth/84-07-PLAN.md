---
phase: 84-wear-history-depth
plan: 07
type: execute
wave: 4
depends_on: ["84-01", "84-02", "84-06"]
files_modified: []
autonomous: false
requirements: [WEAR-01, WEAR-02, WEAR-03, WEAR-04]

must_haves:
  truths:
    - "WEAR-01 / D-15: in npm run dev against local Supabase, tapping a Timeline row and a Calendar selected-day row each lands on that wear's /wear/[id] page"
    - "WEAR-02 / D-01..D-07: the owner logs a photo-less wear on a past date from the Worn tab's single 'Log a wear' form; it appears immediately on that date and writes no activity row, while a today-dated log does write a watch_worn activity"
    - "D-05: re-opening the form on the same date shows the just-logged watch disabled"
    - "WEAR-03 / WEAR-04 / D-08..D-13: the Wear leaderboard sits above Timeline/Calendar, defaults to 3 mo, re-ranks on window change, supports arrow-key navigation, shows zero-wear rows and Show all, and rows open /w/[id]"
    - "D-14: a second seeded user viewing a profile with collection_public=false sees no leaderboard rows for watches they have no visible wear for"
  artifacts: []
  key_links: []
---

<objective>
Phase gate required by CLAUDE.md Local-First Development: exercise every Phase 84 code path in `npm run dev` against local Supabase as seeded users before anything is pushed. Automation prepares and post-checks the local database. The human walks the real UI, including the two cases unit tests cannot cover: Router Cache freshness after `updateTag`, and cross-user privacy on a private collection.

Purpose: `npm run build` and vitest mocks do not execute SQL or real Server Action round-trips (see the Drizzle ANY() incident in CLAUDE.md).
Output: a verified phase, ready for /gsd:verify-work and push. No source files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/84-wear-history-depth/84-CONTEXT.md
@.planning/phases/84-wear-history-depth/84-VALIDATION.md
@.planning/phases/84-wear-history-depth/84-06-SUMMARY.md

Local DB access: `psql postgresql://postgres:postgres@127.0.0.1:54322/postgres` (psql at /opt/homebrew/opt/libpq/bin/psql; container `supabase_db_horlo`). Tables:
- `profiles(id, username)`
- `profile_settings(user_id, profile_public, collection_public)`
- `watches(id, user_id, brand, model, status)`
- `wear_events(id, user_id, watch_id, worn_date text, note, photo_url, visibility)`
- `activities(id, user_id, type, watch_id, metadata, created_at)`
- `follows`

Seeded users (password `password123`): viewer@horlo.test, vintage-anna@horlo.test, modern-mike@horlo.test, dress-dan@horlo.test.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight local environment and produce the walk sheet</name>
  <files>(none — read-only checks; local-only DB fixture setup)</files>
  <read_first>
    - ./CLAUDE.md (Local-First Development section)
    - .planning/phases/84-wear-history-depth/84-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>
    Run these checks in order and record the outputs in the plan SUMMARY draft.

    (1) Run `supabase status` and confirm the local stack is running. If it is not, run `supabase start`. Confirm `.env.development.local` exists (do not print its contents).

    (2) Run `npx vitest run tests/unit/WornTimeline.test.tsx tests/components/profile/WornCalendar.test.tsx tests/actions/wearEventsBackfill.test.ts tests/components/profile/LogTodaysWearButton.test.tsx tests/components/profile/WearLeaderboard.test.tsx tests/unit/leaderboard.test.ts tests/unit/wornTabScope.test.ts` and `npm run build`.
    - The build must exit 0.
    - Every test added by Phase 84 must pass.
    - A failure is acceptable only if it is in an `it()` block that existed before Phase 84. Confirm with `git log -L` or `git blame` on that test's line that it predates this phase, and record it as baseline.

    (3) Confirm the duplicate-day backstop exists locally:
    - Run `psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -tAc "select count(*) from pg_indexes where tablename='wear_events' and indexname='wear_events_unique_day'"`.
    - Local constraints can drift after Drizzle pushes (project memory). If the count is 0, restore it LOCALLY ONLY with `alter table wear_events add constraint wear_events_unique_day unique (user_id, watch_id, worn_date)` and note it in the SUMMARY.
    - Never run this against prod.

    (4) Pick walk fixtures with psql:
    - For each seeded username, count owned watches (`status='owned'`) and wear_events, and read `profile_settings.collection_public`.
    - Choose OWNER = the seeded user with at least 2 owned watches and at least 1 wear (prefer vintage-anna), and VISITOR = a different seeded user.
    - Find an OWNER owned watch with zero wear_events (the zero-wear row to look for).
    - Record whether VISITOR follows OWNER.

    (5) Record OWNER's current `collection_public` as ORIGINAL_COLLECTION_PUBLIC. Then run `update profile_settings set collection_public = true where user_id = '<OWNER id>'` locally so the walk starts from a known state.

    (6) Write the walk sheet (OWNER username and id, VISITOR username, follow status, zero-wear watch name, ORIGINAL_COLLECTION_PUBLIC, today's date) into the SUMMARY draft. Start `npm run dev` in the background and confirm the server responds.
  </action>
  <verify>
    <automated>npm run build && psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -tAc "select count(*) from pg_indexes where tablename='wear_events' and indexname='wear_events_unique_day'"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - All Phase 84-added tests in the targeted vitest command pass; any failure is documented with blame evidence that it predates Phase 84
    - The wear_events_unique_day index count query returns 1 (restored locally if it was missing)
    - The walk sheet records OWNER, VISITOR, follow status, zero-wear watch and ORIGINAL_COLLECTION_PUBLIC
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/login` returns 200
  </acceptance_criteria>
  <done>The local stack is up, gates are green, the DB backstop is confirmed, and a concrete walk sheet exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Local-dev walk of all four WEAR requirements</name>
  <files>(none — manual browser verification)</files>
  <read_first>
    - The Task 1 walk sheet in the SUMMARY draft
  </read_first>
  <action>
    Present the walk below to the user with Task 1's concrete OWNER/VISITOR values substituted, then wait for the resume signal. Do not proceed to Task 3 until the user replies "approved". If the user reports failures, stop and record the step numbers and observations in the SUMMARY for gap closure.
  </action>
  <what-built>Phase 84: linked Worn-tab rows (84-01), the logBackfillWear action (84-02), the unified "Log a wear" form (84-04), and the Wear leaderboard (84-05) wired with viewer scoping (84-06). The dev server is running at http://localhost:3000 against local Supabase.</what-built>
  <how-to-verify>
    Sign in at http://localhost:3000/login as OWNER (password `password123`) and open /u/OWNER/worn.

    1. Leaderboard (WEAR-03/04, D-08..D-13):
       - A "Wear leaderboard" card sits ABOVE the Timeline/Calendar pill. "3 mo" is highlighted in accent.
       - Rows show rank, thumbnail, Brand Model, "N wears" and an accent bar. The top row's bar is full width.
       - The zero-wear watch from the walk sheet appears near the bottom with "0 wears".
       - Click "All time", then "1 mo": the ranking changes, or the "No wears in this window." note appears.
       - Tab onto the control and press ArrowRight / ArrowLeft / Home / End: the selection moves and focus follows.
       - If there are more than 5 owned watches, "Show all" expands and "Show less" collapses.
       - Choose a watch in the watch filter Select: the leaderboard does NOT change.
       - Tap a leaderboard row: it opens /w/[id] for that watch. Go back.
    2. Row links (WEAR-01, D-15):
       - In Timeline, tap a wear row: it lands on /wear/[id] for that wear (photo-less wears show the watch image). Go back.
       - Switch to Calendar, tap a day with wears, then tap a row in the panel below the grid: it lands on /wear/[id].
       - Tapping a day cell only selects the day.
    3. Backfill (WEAR-02, D-01..D-07):
       - There is exactly one "Log a wear" button.
       - Open it: Date shows today, and the date picker does not allow a future date.
       - Change Date to about 10 days ago and pick an owned watch with no wear that day. Add the note "phase 84 backfill", leave visibility on Public, and press "Log wear".
       - The dialog closes and, WITHOUT a hard refresh, the wear appears in Timeline under that past date and in Calendar on that day. The leaderboard count for that watch rises in windows that include the date.
       - Reopen "Log a wear", set the same past date, and confirm that watch shows disabled with "Already logged".
       - Set Date back to today, log a different owned watch with visibility Followers, reopen, and confirm it shows "Worn today" disabled.
    4. Feed behavior (D-06): open / (home). The 10-days-ago backfill must NOT appear as a new "worn" activity.
    5. Visitor gating (D-14):
       - As OWNER, go to Settings and turn Collection visibility OFF.
       - Sign in as VISITOR in a private window and open /u/OWNER/worn.
       - The Collection tab shows the locked card.
       - On the Worn tab, the leaderboard lists ONLY watches that appear in wears VISITOR can see: no "0 wears" row for the zero-wear watch.
       - The Followers wear from step 3 counts only if VISITOR follows OWNER.
       - The watch filter dropdown lists only those same watches.
       - As OWNER, turn Collection visibility back ON, reload as VISITOR, and confirm the zero-wear row reappears.
    6. Narrow width (devtools 375px):
       - The window pills fit on one row.
       - The Log a wear dialog scrolls, with its footer reachable.
       - Mobile-Safari zoom behavior is verified later on prod per project memory.
  </how-to-verify>
  <verify>
    <automated>MISSING — manual walk; Task 3 performs the automated DB assertions that corroborate steps 3-4</automated>
  </verify>
  <acceptance_criteria>
    - The user replies "approved"
    - Otherwise the user lists failing step numbers, which are recorded in the SUMMARY
  </acceptance_criteria>
  <done>The user has approved the walk of steps 1-6.</done>
  <resume-signal>Type "approved", or list the failing step numbers with what you saw</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-walk database assertions and fixture restore</name>
  <files>(none — read-only assertions + local settings restore)</files>
  <read_first>
    - The Task 1 walk sheet values in the SUMMARY draft (OWNER id, ORIGINAL_COLLECTION_PUBLIC)
  </read_first>
  <action>
    Using psql against 127.0.0.1:54322:

    (1) Run `select id, watch_id, worn_date, photo_url, visibility from wear_events where user_id = '<OWNER id>' and note = 'phase 84 backfill'`. Expect exactly 1 row: worn_date earlier than today's date, photo_url null, visibility 'public'. Record BACKFILL_WATCH_ID.

    (2) D-06: run `select watch_id, metadata->>'visibility' as vis, created_at from activities where user_id = '<OWNER id>' and type = 'watch_worn' and created_at > now() - interval '3 hours' order by created_at`.
    - BACKFILL_WATCH_ID must NOT appear, unless it is also the watch logged for today.
    - The today-dated watch from walk step 3 MUST appear exactly once, with vis 'followers'.

    (3) Run `select watch_id, worn_date, count(*) from wear_events where user_id = '<OWNER id>' group by 1, 2 having count(*) > 1`. It must return 0 rows.

    (4) Run `update profile_settings set collection_public = <ORIGINAL_COLLECTION_PUBLIC> where user_id = '<OWNER id>'` (local only). Leave the walk-created wear rows in place, since local data is re-seedable, and record their ids in the SUMMARY.

    (5) Stop the background dev server.
  </action>
  <verify>
    <automated>psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -tAc "select count(*) from wear_events where note = 'phase 84 backfill' and photo_url is null"</automated>
  </verify>
  <acceptance_criteria>
    - Query (1) returns exactly 1 row with photo_url null and worn_date earlier than today
    - Query (2) contains no row for BACKFILL_WATCH_ID (unless it was also the today log) and exactly one row for the today-dated watch
    - Query (3) returns 0 rows
    - `select collection_public from profile_settings where user_id = '<OWNER id>'` equals ORIGINAL_COLLECTION_PUBLIC
  </acceptance_criteria>
  <done>The database confirms a photo-less past-date wear, no activity for it, one activity for the today wear, no duplicates, and restored fixture settings. The phase is ready for /gsd:verify-work and push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human walk ↔ local Supabase | Local-only data mutations for verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-LEAK | Information Disclosure | Worn tab cross-user view | mitigate | Walk step 5 verifies scoping end-to-end with two real sessions and collection_public toggled off and on. |
| T-84-08 | Tampering | local DB fixture edits | mitigate | All psql writes target 127.0.0.1:54322 only (collection_public set/restore plus a conditional local constraint restore). The original value is recorded in Task 1 and restored in Task 3, and nothing runs against the linked prod project. |
</threat_model>

<verification>
- Human walk approved.
- Post-walk DB assertions pass.
- Only after approval: push per the normal flow.
</verification>

<success_criteria>
All four WEAR requirements are demonstrated in the running app against a real database, including privacy scoping across two users and D-06 activity suppression confirmed at the SQL level.
</success_criteria>

<output>
Create `.planning/phases/84-wear-history-depth/84-07-SUMMARY.md` when done
</output>
