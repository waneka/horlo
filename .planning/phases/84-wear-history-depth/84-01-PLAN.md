---
phase: 84-wear-history-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/profile/WornTimeline.tsx
  - src/components/profile/WornCalendar.tsx
  - tests/unit/WornTimeline.test.tsx
  - tests/components/profile/WornCalendar.test.tsx
autonomous: true
requirements: [WEAR-01]

must_haves:
  truths:
    - "D-15: every Timeline wear row is a link to /wear/[id] for that wear event"
    - "D-15: every Calendar selected-day panel row is a link to /wear/[id]; calendar day cells keep select-a-day behavior and are not links"
    - "Linked rows keep their existing visual shell and gain a hover state, a focus-visible ring, and a trailing chevron"
  artifacts:
    - path: "src/components/profile/WornTimeline.tsx"
      provides: "Timeline rows wrapped in next/link to /wear/[id]"
      contains: "/wear/${e.id}"
    - path: "src/components/profile/WornCalendar.tsx"
      provides: "Selected-day panel rows wrapped in next/link to /wear/[id]"
      contains: "/wear/${event.id}"
  key_links:
    - from: "src/components/profile/WornTimeline.tsx"
      to: "src/app/wear/[wearEventId]/page.tsx"
      via: "Link href"
      pattern: "href=\\{`/wear/\\$\\{e\\.id\\}`\\}"
    - from: "src/components/profile/WornCalendar.tsx"
      to: "src/app/wear/[wearEventId]/page.tsx"
      via: "Link href"
      pattern: "href=\\{`/wear/\\$\\{event\\.id\\}`\\}"
---

<objective>
WEAR-01: make every wear entry on the Worn tab tap through to its existing `/wear/[id]` detail page, in both the Timeline view and the Calendar view's selected-day panel (D-15).

Purpose: the Worn tab lists wears but they are dead ends today. `/wear/[wearEventId]` already exists, applies viewer gating and renders photo-less wears, so this plan changes only the two row renderers. No route changes.
Output: two components with linked rows, plus extended RTL tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/84-wear-history-depth/84-CONTEXT.md
@.planning/phases/84-wear-history-depth/84-UI-SPEC.md
@.planning/phases/84-wear-history-depth/84-PATTERNS.md

Rendered path (traced, not a dead island): `src/app/u/[username]/[tab]/page.tsx` (tab === 'worn') renders `WornTabContent`, which renders `WornTimeline` (view 'timeline') or `WornCalendar` (view 'calendar'). Both files are live.

<interfaces>
Existing row data shape (both components, unchanged):
interface WearEventLite { id: string; watchId: string; wornDate: string; note: string | null; photoUrl: string | null }
interface WatchSummary { id: string; brand: string; model: string; imageUrl: string | null }

Destination route (unchanged): src/app/wear/[wearEventId]/page.tsx. Param is the wear event id.

Whole-row Link idiom to copy (src/components/home/MostWornThisMonthCard.tsx):
Link href, className "rounded-lg focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"

next/link test mock precedent: tests/components/home/WatchPickerDialog.test.tsx lines 8-25 (vi.mock('next/link', ...) rendering a plain anchor with href).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Link WornTimeline rows to /wear/[id]</name>
  <files>src/components/profile/WornTimeline.tsx, tests/unit/WornTimeline.test.tsx</files>
  <read_first>
    - src/components/profile/WornTimeline.tsx (file being modified; row shell at lines ~70-92)
    - tests/unit/WornTimeline.test.tsx (file being extended; fixtures WATCH_WITH_IMAGE / WATCH_WITHOUT_IMAGE)
    - src/components/home/MostWornThisMonthCard.tsx (whole-row Link + focus ring idiom)
    - tests/components/home/WatchPickerDialog.test.tsx (lines 1-30, next/link mock shape)
    - .planning/phases/84-wear-history-depth/84-UI-SPEC.md (section "Timeline/Calendar row linking")
  </read_first>
  <behavior>
    - Test A: rendering one event `{ id: 'wear-1', watchId: 'watch-1', ... }` yields a link (role "link") whose href attribute is exactly `/wear/wear-1` and whose text includes "Rolex Submariner".
    - Test B: two events on the same day render two links with hrefs `/wear/wear-1` and `/wear/wear-2`, one per event, and no extra links.
    - Test C: the link element's className contains `focus-visible:ring-2` and `hover:bg-muted/40`.
    - Test D: the existing WPIC-03 photo-preference tests in the file still pass unchanged.
  </behavior>
  <action>
    Per D-15, first add the new `it()` blocks from behavior Tests A-C to tests/unit/WornTimeline.test.tsx under a new `describe('WornTimeline — WEAR-01 row links (D-15)')`. If the real next/link does not render an anchor with `href` under jsdom, add a top-of-file `vi.mock('next/link', ...)` that matches the WatchPickerDialog.test.tsx mock. Run the tests and confirm they fail (RED).

    Then edit src/components/profile/WornTimeline.tsx:
    - Add `import Link from 'next/link'`.
    - Change the lucide import to `import { ChevronRight, Watch as WatchIcon } from 'lucide-react'`.
    - Inside `dayEvents.map`, the `<li key={e.id}>` keeps its key and loses its className.
    - Its children move into a `<Link href={`/wear/${e.id}`}>` with className `flex items-center gap-3 rounded-lg border bg-card p-2 hover:bg-muted/40 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`. This moves the existing row shell classes onto the Link and adds the hover and focus states.
    - Keep the thumbnail block (size-10) and the Brand Model text div exactly as they are.
    - Append `<ChevronRight className="ml-auto size-4 shrink-0 text-muted-foreground/40" aria-hidden />` as the Link's last child.
    - Do not add 'use client'. The file stays a shared component and Link works in both environments.
    - Do not add `font-medium` or `font-bold` (no-raw-palette guard).
    - The row hit area is p-2 + size-10 = 56px, which clears the 44px UI-SPEC minimum. No extra padding needed.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/WornTimeline.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'href={`/wear/${e.id}`}' src/components/profile/WornTimeline.tsx` returns 1
    - `grep -c "import Link from 'next/link'" src/components/profile/WornTimeline.tsx` returns 1
    - `grep -c 'ChevronRight' src/components/profile/WornTimeline.tsx` returns at least 2 (import + usage)
    - `grep -cE 'font-medium|font-bold' src/components/profile/WornTimeline.tsx` returns 0
    - Rendered-path check: `grep -c '<WornTimeline' src/components/profile/WornTabContent.tsx` returns at least 1
    - `npx vitest run tests/unit/WornTimeline.test.tsx` exits 0 with the new WEAR-01 tests included
  </acceptance_criteria>
  <done>Each Timeline row is a single link to /wear/[id] with a hover state, a focus ring and a trailing chevron. Tests assert the exact hrefs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Link WornCalendar selected-day panel rows to /wear/[id]</name>
  <files>src/components/profile/WornCalendar.tsx, tests/components/profile/WornCalendar.test.tsx</files>
  <read_first>
    - src/components/profile/WornCalendar.tsx (file being modified; day cells lines ~189-254 stay untouched; panel rows lines ~266-298)
    - tests/components/profile/WornCalendar.test.tsx (file being extended; `initialSelectedDate` test-only prop)
    - tests/components/home/WatchPickerDialog.test.tsx (lines 1-30, next/link mock shape)
    - .planning/phases/84-wear-history-depth/84-UI-SPEC.md (section "Timeline/Calendar row linking")
  </read_first>
  <behavior>
    - Test A: render with events e1 (2026-05-03, note 'breakfast wear') and e2 (2026-05-10, note 'evening wear') and `initialSelectedDate="2026-05-03"`. A link with href `/wear/e1` exists and contains "breakfast wear". No link with href `/wear/e2` exists.
    - Test B: from the Test A state, `fireEvent.click(screen.getByLabelText(/View wear events for 2026-05-10/))` shows a link with href `/wear/e2`. The `/wear/e1` link is no longer in the document (disappearance assertion).
    - Test C: the day-cell element found by `getByLabelText(/View wear events for 2026-05-10/)` is not an anchor: tagName is 'DIV', role is 'button', and it has no href attribute.
    - Test D: the three existing tests in the file still pass unchanged.
  </behavior>
  <action>
    Per D-15, first add the `it()` blocks from behavior Tests A-C to tests/components/profile/WornCalendar.test.tsx in a new `describe('WornCalendar — WEAR-01 panel row links (D-15)')`. Always pass `initialSelectedDate` so selection is deterministic regardless of today's month. Add the next/link mock if the real Link does not render an href under jsdom. Confirm RED.

    Then edit src/components/profile/WornCalendar.tsx:
    - Add `import Link from 'next/link'`.
    - Reuse the existing `ChevronRight` import from line 5. Do not add a second lucide import.
    - In the selected-day panel's `selectedEvents.map`, `<li key={event.id}>` keeps its key and loses its className.
    - Its children move into `<Link href={`/wear/${event.id}`}>` with className `-mx-2 flex items-start gap-3 rounded-lg p-2 hover:bg-muted/40 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`. The `-mx-2` keeps the thumbnail aligned with the panel heading now that the row has p-2.
    - Keep the size-12 thumbnail, the `truncate text-sm font-semibold` brand/model paragraph and the note paragraph unchanged.
    - Append `<ChevronRight className="ml-auto size-4 shrink-0 self-center text-muted-foreground/40" aria-hidden />` as the Link's last child.
    - Keep the `<ul className="space-y-3">` wrapper. The li elements are block-level, so space-y stacks correctly.
    - Do NOT touch the grid day-cell `div role="button"` block, `onClick={() => setSelectedDate(key)}`, the month nav, or `formatDateLabel`.
  </action>
  <verify>
    <automated>npx vitest run tests/components/profile/WornCalendar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'href={`/wear/${event.id}`}' src/components/profile/WornCalendar.tsx` returns 1
    - `grep -c "import Link from 'next/link'" src/components/profile/WornCalendar.tsx` returns 1
    - `grep -c "from 'lucide-react'" src/components/profile/WornCalendar.tsx` returns 1 (no duplicate import)
    - `grep -c 'onClick={() => setSelectedDate(key)}' src/components/profile/WornCalendar.tsx` returns 1 (day-cell behavior preserved)
    - `grep -cE 'font-medium|font-bold' src/components/profile/WornCalendar.tsx` returns 0
    - Rendered-path check: `grep -c '<WornCalendar' src/components/profile/WornTabContent.tsx` returns at least 1
    - `npx vitest run tests/components/profile/WornCalendar.test.tsx` shows the new WEAR-01 tests passing
  </acceptance_criteria>
  <done>Panel rows below the calendar grid link to /wear/[id]. Day cells still only select a day. Tests cover the link appearing, the old link disappearing after reselect, and the cell staying a non-link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /wear/[wearEventId] | A tapped link sends a wear id to an existing auth-gated, viewer-gated route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Information Disclosure | WornTimeline/WornCalendar links | accept | Links only expose wear ids already present in the viewer-gated `events` array from `getWearEventsForViewer`. The destination re-gates with `getWearEventByIdForViewer` and calls `notFound()` on deny. No new data is exposed. |
| T-84-02 | Spoofing | /wear/[id] URL guessing | accept | Unchanged route. Its gating is independent of how the user arrived, and ids are UUIDs. |
</threat_model>

<verification>
- `npx vitest run tests/unit/WornTimeline.test.tsx tests/components/profile/WornCalendar.test.tsx` exits 0 for the Phase 84 tests. The pre-existing month-dependent calendar tests may carry baseline failures; do not chase unrelated red tests.
- `npx vitest run tests/no-raw-palette.test.ts` shows no failures for the two modified files.
- `npm run build` exits 0 (authoritative gate).
- Do not push. The phase-level local-dev walk (84-07) gates the push.
</verification>

<success_criteria>
Tapping any Timeline row or Calendar selected-day row navigates to that wear's /wear/[id] page. Calendar day cells keep their select-a-day behavior.
</success_criteria>

<output>
Create `.planning/phases/84-wear-history-depth/84-01-SUMMARY.md` when done
</output>
