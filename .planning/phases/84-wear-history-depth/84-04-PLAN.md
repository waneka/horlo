---
phase: 84-wear-history-depth
plan: 04
type: execute
wave: 2
depends_on: ["84-02"]
files_modified:
  - src/components/profile/LogTodaysWearButton.tsx
  - src/components/profile/WornTabContent.tsx
  - tests/components/profile/LogTodaysWearButton.test.tsx
autonomous: true
requirements: [WEAR-02]

must_haves:
  truths:
    - "D-01: the owner's single 'Log a wear' button on the Worn tab opens one form; there is no separate past-wear button and no calendar-cell entry point"
    - "D-07: the same form handles today and past dates; the owner zero-wear empty-state 'Log a wear' CTA opens this same form"
    - "D-02: the date field is a native input type=date defaulting to browser-local today with max = today and no min"
    - "D-03: the form fields are owned watch + date + optional note (200-char counter) + visibility; there is no photo field"
    - "D-04: visibility defaults to Public every time the form opens; nothing is remembered between opens"
    - "D-05: whenever the date changes, watches already logged on that date render disabled (aria-disabled) via getWornTodayIdsForUserAction called with the selected date"
    - "Submitting calls logBackfillWear with the selected wornDate and a freshly computed client today; server errors render in a role=alert slot and the dialog stays open"
  artifacts:
    - path: "src/components/profile/LogTodaysWearButton.tsx"
      provides: "Unified 'Log a wear' trigger + dialog form"
      contains: "logBackfillWear"
    - path: "tests/components/profile/LogTodaysWearButton.test.tsx"
      provides: "Form + date-aware preflight tests"
      min_lines: 120
  key_links:
    - from: "src/components/profile/LogTodaysWearButton.tsx"
      to: "src/app/actions/wearEvents.ts#getWornTodayIdsForUserAction"
      via: "useEffect keyed on [open, wornDate, viewerId]"
      pattern: "getWornTodayIdsForUserAction\\(\\{\\s*userId: viewerId,\\s*today: wornDate"
    - from: "src/components/profile/LogTodaysWearButton.tsx"
      to: "src/app/actions/wearEvents.ts#logBackfillWear"
      via: "startTransition submit"
      pattern: "logBackfillWear\\("
    - from: "src/components/profile/WornTabContent.tsx"
      to: "src/components/profile/LogTodaysWearButton.tsx"
      via: "header row + owner empty state"
      pattern: "<LogTodaysWearButton"
---

<objective>
WEAR-02 (client half): rebuild `LogTodaysWearButton` into the unified "Log a wear" form. It has an owned-watch list with date-aware duplicate disabling, a native date field (max today), an optional note and a visibility control, and it submits to `logBackfillWear` (84-02). This form becomes the one entry point everywhere on the Worn tab (D-01/D-07), including the owner zero-wear empty state.

Purpose: a collector can catch up on wears they forgot to log, with no photo, from the Worn tab. RESEARCH Pitfall 1 applies: the current component is a bare `<Select>` with NO preflight at all, so the disabled-row logic is built fresh by porting `WatchPickerDialog`'s pattern, not "extended".

Discretion choices (documented):
- The watch picker becomes a `role="listbox"` of `role="option"` buttons, same as WatchPickerDialog, instead of the Base UI `<Select>`. This gives real per-row disabled state, a trailing micro-label, and deterministic jsdom tests.
- Disabled-row label is "Worn today" when the selected date is today, otherwise "Already logged" (RESEARCH Open Question 2 default).
- The dialog stays a `Dialog` at all breakpoints (UI-SPEC).
- The owner empty-state CTA switches from WywtPostDialog (today-only photo flow) to this form per D-07 ("Log a wear always opens the form"). Otherwise a zero-wear owner could not backfill. The photo flow remains reachable from the nav Wear button and the WYWT rail.
- The file name stays `LogTodaysWearButton.tsx`, so the traced render path and imports do not churn; copy changes to "Log a wear".

Output: rebuilt component, WornTabContent call sites updated, new component test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-wear-history-depth/84-CONTEXT.md
@.planning/phases/84-wear-history-depth/84-UI-SPEC.md
@.planning/phases/84-wear-history-depth/84-PATTERNS.md
@.planning/phases/84-wear-history-depth/84-02-SUMMARY.md

Rendered path (traced): `[tab]/page.tsx` (worn) renders `WornTabContent`, which renders `{isOwner && <LogTodaysWearButton watches={watchOptions} />}` (line ~159). The owner empty state (lines ~94-121) currently renders a Button plus `WywtPostDialog`.

<interfaces>
From src/app/actions/wearEvents.ts (84-02):
export async function logBackfillWear(input: { watchId: string; wornDate: string; today: string; note: string | null; visibility: WearVisibility }): Promise<ActionResult<{ wearEventId: string }>>
export async function getWornTodayIdsForUserAction(input: { userId: string; today: string }): Promise<string[]>
(returns [] when userId !== caller; `today` is any YYYY-MM-DD, so pass the selected date)

From src/lib/wear.ts: export function todayLocalISO(now?: Date): string
From src/lib/wearVisibility.ts: type WearVisibility = 'public' | 'followers' | 'private'
From src/components/wywt/VisibilitySegmentedControl.tsx:
export function VisibilitySegmentedControl({ value, onChange, disabled }: { value: WearVisibility; onChange: (v: WearVisibility) => void; disabled?: boolean })
(buttons carry aria-pressed; aria-labels 'Private — only you', 'Followers only', 'Public — anyone on Horlo')

Primitives: '@/components/ui/dialog' (Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter), '@/components/ui/button' (Button), '@/components/ui/input' (Input: already text-base md:text-sm), '@/components/ui/textarea' (Textarea), '@/components/ui/label' (Label)

Current WornTabContent props: events, watchMap, isOwner, username, viewerId: string | null, ownedWatches: Watch[]; derived `watchOptions: {id, brand, model}[]` sorted A→Z.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild LogTodaysWearButton as the unified "Log a wear" form (tests first)</name>
  <files>src/components/profile/LogTodaysWearButton.tsx, tests/components/profile/LogTodaysWearButton.test.tsx</files>
  <read_first>
    - src/components/profile/LogTodaysWearButton.tsx (file being rebuilt)
    - src/components/home/WatchPickerDialog.tsx (lines ~138-235: empty-state copy + disabled-row listbox pattern)
    - src/components/wywt/ComposeStep.tsx (lines ~655-700: "+ Add a note" progressive disclosure + 0/200 counter)
    - src/components/wywt/VisibilitySegmentedControl.tsx (reuse verbatim)
    - src/components/watch/WatchForm.tsx (lines ~800-815: native type=date precedent)
    - tests/components/WywtPostDialog.test.tsx (lines 1-100: action + next/link mock setup for a Dialog-based component)
    - .planning/phases/84-wear-history-depth/84-UI-SPEC.md (Copywriting Contract + "Backfill / unified log form")
  </read_first>
  <behavior>
    Test setup:
    - `vi.mock('@/app/actions/wearEvents', () => ({ logBackfillWear: (...a) => mockLog(...a), getWornTodayIdsForUserAction: (...a) => mockPreflight(...a) }))`
    - `vi.mock('@/lib/wear', async (orig) => ({ ...(await orig()), todayLocalISO: () => '2026-09-12' }))`
    - watches: `[{ id: W1, brand: 'Omega', model: 'Speedmaster' }, { id: W2, brand: 'Tudor', model: 'Black Bay' }]`; viewerId VIEWER (UUID)
    - mockPreflight resolves [] by default

    Cases:
    - T1 the trigger button reads "Log a wear". Before click, no dialog title is present. After click, a dialog with heading "Log a wear" is present.
    - T2 on open, the date input (label "Date") has value '2026-09-12', `max` attribute '2026-09-12', and no `min` attribute.
    - T3 on open, mockPreflight is called with `{ userId: VIEWER, today: '2026-09-12' }`. When it resolves [W1], the W1 option has aria-disabled="true" and shows "Worn today", and the W2 option is enabled.
    - T4 changing the date to '2026-09-01' (fireEvent.change) calls mockPreflight with `{ userId: VIEWER, today: '2026-09-01' }`. When that call resolves [W2], W2 is disabled with "Already logged", W1 is enabled again, and the "Worn today" text is no longer in the document (disappearance).
    - T5 Public is the default: the button labelled 'Public — anyone on Horlo' has aria-pressed="true".
    - T6 the submit button "Log wear" is disabled until a watch is selected. After selecting W2 and changing the date to '2026-08-20', clicking "Log wear" calls mockLog with `{ watchId: W2, wornDate: '2026-08-20', today: '2026-09-12', note: null, visibility: 'public' }`. On `{ success: true }` the dialog heading disappears.
    - T7 when mockLog resolves `{ success: false, error: 'Already logged this watch on that date.' }`, a role="alert" element shows that text and the dialog stays open.
    - T8 note: "+ Add a note" reveals a textarea with placeholder "Add a note…" and counter "0/200". Typing '  lume shot  ' shows counter "13/200". Submitting sends note 'lume shot'.
    - T9 reset on reopen: change date to '2026-09-01', open the note and type 'x', click "Cancel", reopen. The date is back to '2026-09-12', the textarea is not present, and "+ Add a note" is visible.
    - T10 selecting a watch that the next preflight returns as already logged clears the selection. Select W1, change the date, preflight resolves [W1], and "Log wear" is disabled again.
    - T11 with an empty watches array, opening shows "Add a watch first" and no date input.
  </behavior>
  <action>
    First write tests/components/profile/LogTodaysWearButton.test.tsx covering T1-T11. Use `@testing-library/react` with `fireEvent` and `waitFor`/`findBy*` for async preflight resolution. Confirm RED against the current file.

    Then rebuild src/components/profile/LogTodaysWearButton.tsx ('use client').

    Props: `{ watches: WatchSummary[]; viewerId: string; className?: string }`, where `WatchSummary = { id: string; brand: string; model: string }`.

    State:
    - open
    - watchId ('')
    - wornDate ('')
    - maxDate ('')
    - note ('')
    - noteOpen (false)
    - visibility (WearVisibility, 'public')
    - error (string | null)
    - wornOnDateIds (ReadonlySet<string>, empty)
    - `[pending, startTransition] = useTransition()`

    One-shot reset (Router Cache lesson: reset on the open event, never on mount):
    - `handleOpen()` computes `const t = todayLocalISO()`.
    - It sets maxDate=t, wornDate=t, watchId='', note='', noteOpen=false, visibility='public', error=null, then open=true.
    - The trigger `<Button variant="default" className={className} onClick={handleOpen}>` renders `<Plus className="mr-1 size-4" />` and "Log a wear" (per UI-SPEC: primary CTA, no bg-accent override).
    - The Dialog `onOpenChange={setOpen}`.

    Preflight (D-05):
    - `useEffect` with deps [open, wornDate, viewerId]. Return early when `!open || !wornDate`. Keep a `let cancelled = false` flag.
    - Call `getWornTodayIdsForUserAction({ userId: viewerId, today: wornDate })`.
    - `.then((ids) => { if (cancelled) return; const set = new Set(ids); setWornOnDateIds(set); setWatchId((cur) => (set.has(cur) ? '' : cur)) })`.
    - `.catch(() => { if (!cancelled) setWornOnDateIds(new Set()) })`.
    - Cleanup sets `cancelled = true`.
    - No synchronous setState in the effect body.

    Dialog body, order per UI-SPEC:
    - (1) `DialogContent className="max-h-[90dvh] overflow-y-auto"` with `DialogHeader`/`DialogTitle` "Log a wear".
    - (2) If `watches.length === 0`: render the WatchPickerDialog empty-state copy verbatim ("Add a watch first" as DialogDescription heading text, plus its body sentence) and an "Add watch" link to the same href WatchPickerDialog uses. Render no form fields.
    - (3) Otherwise, in a `flex flex-col gap-4` stack:
      - (a) Watch field: `<p id="log-wear-watch-label" className="text-sm font-semibold">Which watch?</p>`, then `<ul role="listbox" aria-labelledby="log-wear-watch-label" className="max-h-56 overflow-y-auto rounded-lg border">`. Each `<li>` holds a `<button type="button" role="option" aria-selected={watchId === w.id} aria-disabled={isLogged} disabled={isLogged || pending} onClick={() => { if (!isLogged) setWatchId(w.id) }}>`.
        - Button className: `flex min-h-11 w-full items-center gap-2 px-3 py-2 text-left transition`, plus `opacity-50 cursor-not-allowed` when isLogged, else `bg-accent text-accent-foreground` when selected (accent is the selected token), else `hover:bg-muted/40`.
        - Content: brand in `text-sm font-semibold`, model in `text-sm`.
        - When isLogged, a trailing `<span className="ml-auto text-xs text-muted-foreground">` shows `wornDate === maxDate ? 'Worn today' : 'Already logged'`.
      - (b) Date field: `<Label htmlFor="log-wear-date">Date</Label>` plus `<Input id="log-wear-date" type="date" value={wornDate} max={maxDate} onChange={(e) => { setWornDate(e.target.value); setError(null) }} disabled={pending} />`. No min. Do NOT add a `text-sm` className to Input (iOS auto-zoom guard; the primitive already sets text-base md:text-sm).
      - (c) Note: when noteOpen, render `<Textarea value={note} onChange=... maxLength={200} placeholder="Add a note…" aria-label="Wear note" className="resize-none" disabled={pending} />`. No `text-sm` on Textarea. Below it, the counter `<p>` `{note.length}/200` uses `text-destructive font-semibold` at 200 and `text-muted-foreground font-normal` otherwise, with `mt-1 text-right text-xs`. When closed, render a `<button type="button" onClick={() => setNoteOpen(true)} className="self-start text-xs font-semibold text-muted-foreground underline underline-offset-2 hover:text-foreground">+ Add a note</button>`. Accent is not used for static text per UI-SPEC.
      - (d) `<VisibilitySegmentedControl value={visibility} onChange={setVisibility} disabled={pending} />`.
      - (e) Error slot `{error && <p role="alert" className="text-sm text-destructive">{error}</p>}`.
    - (4) DialogFooter: `<Button variant="outline" onClick={() => setOpen(false)} disabled={pending}>Cancel</Button>` with no bg override, so no dark: pair is needed. Then `<Button onClick={handleSubmit} disabled={!canSubmit}>{pending ? 'Logging…' : 'Log wear'}</Button>`.
      - `canSubmit = !pending && watchId !== '' && !wornOnDateIds.has(watchId) && wornDate !== '' && wornDate <= maxDate`.

    handleSubmit:
    - `setError(null)`, then `startTransition(async () => { ... })`.
    - Inside: `const result = await logBackfillWear({ watchId, wornDate, today: todayLocalISO(), note: note.trim() ? note.trim() : null, visibility })`.
    - `if (!result.success) { setError(result.error); return }`, then `setOpen(false)`.
    - `today` is recomputed at submit so a dialog left open across midnight still sends the real local day (D-06 correctness).

    Remove the `markAsWorn` import and the `Select` imports. Do not add `font-medium`/`font-bold`. Commit `feat(84-04): unified Log a wear form with date-aware preflight`.
  </action>
  <verify>
    <automated>npx vitest run tests/components/profile/LogTodaysWearButton.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "logBackfillWear" src/components/profile/LogTodaysWearButton.tsx` returns at least 2 (import + call)
    - `grep -c "markAsWorn" src/components/profile/LogTodaysWearButton.tsx` returns 0
    - `grep -c "getWornTodayIdsForUserAction" src/components/profile/LogTodaysWearButton.tsx` returns at least 2
    - `grep -c 'type="date"' src/components/profile/LogTodaysWearButton.tsx` returns 1
    - `grep -c "max={maxDate}" src/components/profile/LogTodaysWearButton.tsx` returns 1
    - `grep -c "min=" src/components/profile/LogTodaysWearButton.tsx` returns 0
    - `grep -c "VisibilitySegmentedControl" src/components/profile/LogTodaysWearButton.tsx` returns at least 2
    - `grep -c "Log Today" src/components/profile/LogTodaysWearButton.tsx` returns 0
    - `grep -cE "font-medium|font-bold|bg-accent text-accent-foreground hover:bg-accent/90" src/components/profile/LogTodaysWearButton.tsx` returns 0
    - `grep -cE "<(Input|Textarea)[^>]*text-sm" src/components/profile/LogTodaysWearButton.tsx` returns 0
    - `npx vitest run tests/components/profile/LogTodaysWearButton.test.tsx` exits 0 with 11 passing tests
  </acceptance_criteria>
  <done>The unified form renders owned watches with date-aware disabling, a date capped at today, a note with counter and visibility defaulting to Public. It submits to logBackfillWear and resets on every open. All 11 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Route every Worn-tab "Log a wear" entry point to the unified form</name>
  <files>src/components/profile/WornTabContent.tsx</files>
  <read_first>
    - src/components/profile/WornTabContent.tsx (file being modified; owner empty state lines ~94-121, header row line ~159)
    - src/components/profile/LogTodaysWearButton.tsx (new props from Task 1)
    - .planning/phases/83-polish-sweep/83-02-worn-tab-owned-only-dropdown-SUMMARY.md (POLISH-02: watchOptions derives from ownedWatches; keep it)
  </read_first>
  <action>
    In src/components/profile/WornTabContent.tsx (per D-01 / D-07):
    - (1) Header row: replace `{isOwner && <LogTodaysWearButton watches={watchOptions} />}` with `{isOwner && viewerId && <LogTodaysWearButton watches={watchOptions} viewerId={viewerId} />}`.
    - (2) Owner empty state (`events.length === 0 && isOwner && viewerId`): keep the card shell and copy ("No wears logged yet." / "Track which watch you wore on which day."). Inside `<div className="mx-auto mt-6 max-w-xs">`, replace the `<Button ... onClick={() => setWywtOpen(true)}>Log a wear</Button>` and the sibling `<WywtPostDialog .../>` with `<LogTodaysWearButton watches={watchOptions} viewerId={viewerId} className="w-full" />`. Drop the fragment wrapper if it becomes unnecessary.
    - (3) Remove the now-unused `wywtOpen` state, the `WywtPostDialog` import and the `Button` import, but only if nothing else in the file uses them.
    - (4) Keep `watchOptions` derived from `ownedWatches` (POLISH-02), the watch filter Select, ViewTogglePill, and the Timeline/Calendar render untouched.
    - Do not add the leaderboard here; 84-06 does that.
  </action>
  <verify>
    <automated>npx vitest run tests/components/profile/LogTodaysWearButton.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<LogTodaysWearButton" src/components/profile/WornTabContent.tsx` returns 2
    - `grep -c "viewerId={viewerId}" src/components/profile/WornTabContent.tsx` returns at least 2
    - `grep -c "WywtPostDialog" src/components/profile/WornTabContent.tsx` returns 0
    - `grep -c "setWywtOpen" src/components/profile/WornTabContent.tsx` returns 0
    - `grep -c "ownedWatches" src/components/profile/WornTabContent.tsx` returns at least 3 (POLISH-02 derivation preserved)
    - `grep -c '<SelectItem value="all">All watches</SelectItem>' src/components/profile/WornTabContent.tsx` returns 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both owner entry points (header button and zero-wear empty-state CTA) open the unified form, and the build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → logBackfillWear / getWornTodayIdsForUserAction | User-entered date, note, visibility and selected watch id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-DATE | Tampering | date input `max` | transfer | The client `max` is UX only. The authoritative future-date rejection is server-side in logBackfillWear (84-02, test T3). |
| T-84-DUP | Tampering | preflight disabled rows | transfer | Disabling is a UX hint. The `wear_events_unique_day` constraint plus the 23505 friendly error in 84-02 is the backstop. The form surfaces that error in role=alert (test T7). |
| T-84-04 | Information Disclosure | getWornTodayIdsForUserAction | accept | The existing action returns [] when userId !== caller (T-15-16). The form only passes the owner's own viewerId, and the button renders only when isOwner. |
</threat_model>

<verification>
- `npx vitest run tests/components/profile/LogTodaysWearButton.test.tsx` exits 0.
- `npx vitest run tests/no-raw-palette.test.ts tests/static/no-text-sm-on-native-form-controls.test.ts` shows no new failures for the modified files.
- `npm run build` exits 0.
- No push until 84-07.
</verification>

<success_criteria>
From the Worn tab, the owner opens "Log a wear", picks an owned watch (already-logged watches for the chosen date are disabled), picks today or any past date, optionally adds a note and changes visibility, and submits a photo-less wear.
</success_criteria>

<output>
Create `.planning/phases/84-wear-history-depth/84-04-SUMMARY.md` when done
</output>
