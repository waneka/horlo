---
phase: 45-cms-data-model-admin-routes
plan: 02
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - supabase/migrations/20260518210000_phase45_cms_covers_bucket.sql
  - src/lib/storage/cmsCovers.ts
autonomous: true
requirements: [CMS-03]
must_haves:
  truths:
    - "D-14: a curated list's cover image is set by device upload to a new public Supabase Storage bucket cms-covers with 4 owner-gated RLS policies"
    - "D-14: a browser-callable uploadCmsCover() helper reuses the Phase 43 upload pipeline (EXIF strip + canvas re-encode + size guard) and returns a public URL"
  artifacts:
    - path: "supabase/migrations/20260518210000_phase45_cms_covers_bucket.sql"
      provides: "cms-covers bucket + 4 is_admin-gated storage.objects RLS policies"
      contains: "cms-covers"
    - path: "src/lib/storage/cmsCovers.ts"
      provides: "buildCmsCoverPath, generateCmsCoverFilename, uploadCmsCover"
      exports: ["buildCmsCoverPath", "generateCmsCoverFilename", "uploadCmsCover"]
  key_links:
    - from: "src/lib/storage/cmsCovers.ts uploadCmsCover()"
      to: "cms-covers bucket"
      via: "supabase.storage.from('cms-covers').upload"
      pattern: "cms-covers"
---

<objective>
Create the `cms-covers` Supabase Storage bucket (a separate migration file
mirroring the Phase 43 avatar-bucket split) and the browser-safe
`src/lib/storage/cmsCovers.ts` upload helper.

Purpose: Curated list cover images (CMS-03, D-14) are uploaded to this bucket;
the cover doubles as the Phase 47 hero image. Splitting the bucket migration
into its own file mirrors the proven Phase 43 pattern (avatar bucket creation
was split for transaction-boundary reasons).

Output: One new migration file, one new storage helper module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-cms-data-model-admin-routes/45-CONTEXT.md
@.planning/phases/45-cms-data-model-admin-routes/45-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- The cms-covers bucket gates writes on is_admin (only the owner uploads),
     not on a per-user folder predicate. Path convention: {listId}/{filename}.jpg.
     uploadCmsCover uses upsert: false — each upload gets a fresh UUID filename. -->

cmsCovers.ts exports — src/lib/storage/avatarPhotos.ts is the structural analog:
  buildCmsCoverPath(listId: string, filename: string): string
  generateCmsCoverFilename(): string   // UUID-stamped .jpg
  uploadCmsCover(listId, jpeg: Blob, filename?): Promise<{ publicUrl } | { error }>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the cms-covers bucket + RLS migration</name>
  <files>supabase/migrations/20260518210000_phase45_cms_covers_bucket.sql</files>
  <read_first>
    - supabase/migrations/20260516000000_phase43_avatar_bucket.sql (bucket creation lines 16-30; DO $$ sanity block lines 68-87)
    - supabase/migrations/20260517000000_phase43_avatar_select_policy.sql (the 4th SELECT policy added because upsert needs an object lookup)
    - .planning/phases/45-cms-data-model-admin-routes/45-PATTERNS.md §"20260518210000_phase45_cms_covers_bucket.sql" (lines 155-230 — exact SQL)
    - .planning/phases/45-cms-data-model-admin-routes/45-RESEARCH.md §"Cover Image Upload" and §"Pitfall 2: Forgetting the SELECT Storage Policy for upsert"
  </read_first>
  <action>
    D-14: create `supabase/migrations/20260518210000_phase45_cms_covers_bucket.sql`
    — the new public Supabase Storage bucket curated-list cover images upload to
    (the cover doubles as the Phase 47 hero image; predictable hosting for LCP).
    The timestamp must be later than `20260518200000` so ordering is correct
    (D-10 gotcha 2). The migration:
    (1) `INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)`
    creating bucket id `cms-covers`, public `true`, 4 MB limit (`4194304`),
    allowed MIME types `image/jpeg, image/png, image/webp`, with `ON CONFLICT (id) DO NOTHING`.
    (2) Four RLS policies on `storage.objects` named `cms_covers_select_own`,
    `cms_covers_insert_own`, `cms_covers_update_own`, `cms_covers_delete_own`
    (each preceded by `DROP POLICY IF EXISTS`). Every policy's predicate is
    `bucket_id = 'cms-covers' AND EXISTS (SELECT 1 FROM public.profiles WHERE id = (SELECT auth.uid()) AND is_admin)`.
    The SELECT policy is mandatory even though uploads use `upsert: false` —
    Supabase Storage still does an object lookup; Phase 43 hit this exact 403 bug
    (Pitfall 2). UPDATE needs both `USING` and `WITH CHECK`.
    (3) A `DO $$ ... END $$` block asserting exactly 1 `cms-covers` bucket and
    exactly 4 policies matching `cms_covers_%`; RAISE EXCEPTION otherwise.
    This file depends on `profiles.is_admin` existing — it is created by the
    Plan 01 migration `20260518200000`, which orders first.
  </action>
  <verify>
    <automated>grep -c "create policy\|CREATE POLICY" supabase/migrations/20260518210000_phase45_cms_covers_bucket.sql</automated>
  </verify>
  <acceptance_criteria>
    - D-14: file `supabase/migrations/20260518210000_phase45_cms_covers_bucket.sql` exists and creates the `cms-covers` device-upload bucket
    - Migration creates bucket id `cms-covers` with `file_size_limit` 4194304
    - `grep -c "CREATE POLICY\|create policy" ...` returns 4
    - All four policy predicates contain `bucket_id = 'cms-covers'` and the `is_admin` EXISTS clause
    - Migration contains a `DO $$` block asserting `policy_count <> 4` raises
    - The timestamp `20260518210000` is later than `20260518200000`
  </acceptance_criteria>
  <done>cms-covers bucket migration written with 4 is_admin-gated policies and a sanity assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Write the cmsCovers.ts storage helper</name>
  <files>src/lib/storage/cmsCovers.ts</files>
  <read_first>
    - src/lib/storage/avatarPhotos.ts (exact structural analog — path builder, filename generator, uploader)
    - src/lib/storage/catalogSourcePhotos.ts (alternate bucket-helper pattern)
    - .planning/phases/45-cms-data-model-admin-routes/45-PATTERNS.md §"src/lib/storage/cmsCovers.ts" (lines 641-699 — full file structure to copy)
  </read_first>
  <action>
    D-14: create `src/lib/storage/cmsCovers.ts` modeled exactly on
    `src/lib/storage/avatarPhotos.ts` — reuse the Phase 43 upload pipeline /
    bucket-helper pattern. Export:
    (1) `buildCmsCoverPath(listId, filename)` — returns `${listId}/${filename}`;
    throws `TypeError` if `listId` is empty or `filename` contains a `/`.
    (2) `generateCmsCoverFilename()` — returns a `crypto.randomUUID()`-stamped
    `.jpg` filename (with the same non-crypto fallback as avatarPhotos.ts).
    (3) `uploadCmsCover(listId, jpeg, filename?)` — uses
    `createSupabaseBrowserClient()`, uploads to bucket `cms-covers` with
    `contentType: 'image/jpeg'` and `upsert: false` (UUID filenames prevent
    collision — a cover "replace" uploads a new file), and on success returns
    `{ publicUrl }` from `getPublicUrl(path)` (public bucket — no signed URL).
    On failure returns `{ error }`.
    Do NOT add a server-side signed-URL helper — the bucket is public.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - D-14: `src/lib/storage/cmsCovers.ts` exports `buildCmsCoverPath`, `generateCmsCoverFilename`, `uploadCmsCover`
    - `grep "'cms-covers'" src/lib/storage/cmsCovers.ts` matches the bucket id constant
    - `uploadCmsCover` passes `upsert: false` to `.upload()`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>cmsCovers.ts helper compiles and uploads to the cms-covers bucket returning a public URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser upload → storage.objects | A non-owner must not be able to write to cms-covers |
| public CDN read → cms-covers | Bucket is public-read by design (cover doubles as hero LCP image) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-06 | Tampering | cms-covers bucket upload | mitigate | INSERT/UPDATE/DELETE storage.objects policies gated on `bucket_id = 'cms-covers'` AND `is_admin` EXISTS predicate |
| T-45-07 | DoS | oversized upload to cms-covers | mitigate | Bucket `file_size_limit` 4 MB; client-side 4 MB guard surfaces a toast before upload (Plan 05) |
| T-45-08 | Information Disclosure | public read of cms-covers objects | accept | Bucket is intentionally public — cover images are non-sensitive editorial assets and serve as Phase 47 hero images via CDN |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- cms-covers migration present with 4 policies and a sanity assertion
- `src/lib/storage/cmsCovers.ts` exports the three named functions
</verification>

<success_criteria>
- The `cms-covers` bucket is created by a correctly-ordered migration file
- 4 is_admin-gated RLS policies protect storage.objects for that bucket
- `uploadCmsCover()` is browser-callable and returns a public CDN URL
</success_criteria>

<output>
After completion, create `.planning/phases/45-cms-data-model-admin-routes/45-02-SUMMARY.md`.
</output>
</content>
