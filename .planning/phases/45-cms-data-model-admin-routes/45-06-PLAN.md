---
phase: 45-cms-data-model-admin-routes
plan: 06
type: execute
wave: 4
depends_on: [45-05]
files_modified: []
autonomous: false
requirements: [CMS-10]
must_haves:
  truths:
    - "Ten collection paths exist in the collection_paths table in published state"
    - "Each seed path has a seed watch and a path-type label from the four-value vocabulary"
  artifacts: []
  key_links:
    - from: "collection_paths rows"
      to: "watches_catalog"
      via: "seed_catalog_id + collection_path_nodes.catalog_id"
      pattern: "seed_catalog_id"
---

<objective>
Author the ten seed collection paths (CMS-10) through the `/admin/paths` admin
UI built in Plan 05, and publish each one.

Purpose: The "Where Collections Go" module in Phase 47 needs published
collection paths to render. CONTEXT.md D-16 and RESEARCH.md Pitfall 5 require
this content be authored through the UI (not a SQL seed) — catalog UUIDs
diverge between local and prod, so an id-keyed migration seed would be a no-op
on one DB. The owner selects the watches and themes at execution time.

Output: Ten published `collection_paths` rows authored by the owner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-cms-data-model-admin-routes/45-CONTEXT.md
@.planning/phases/45-cms-data-model-admin-routes/45-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Author ten seed collection paths through the admin UI</name>
  <files>(no source files — content authored into the collection_paths / collection_path_nodes tables via the UI)</files>
  <action>
    This is a human-action checkpoint. CMS-10 content (which real watches, which
    editorial themes) must be authored by the owner through the `/admin/paths` UI
    — Claude cannot select the content, and a SQL seed is forbidden because
    catalog UUIDs diverge between local and prod (RESEARCH.md Pitfall 5). Claude's
    role: ensure the dev server is running, confirm `/admin/paths` loads, then
    pause and present the authoring instructions in `<how-to-verify>` to the
    owner. Resume only on the resume-signal. After resume, run the verification
    queries to confirm ten published paths exist.
  </action>
  <what-built>
    Plan 05 shipped the `/admin/paths` authoring UI: a path index, a "New Path"
    button, and a path editor with a seed-watch picker, up to three follow-on
    watch slots with per-node rationale, an editorial rationale field, a
    four-value path-type chip selector, and draft/publish controls.
  </what-built>
  <how-to-verify>
    This task requires the owner to author content — Claude cannot select which
    real watches and editorial themes the paths cover. Steps for the owner:

    1. Start the dev server (`npm run dev`) and sign in as the owner
       (twwaneka@gmail.com).
    2. Navigate to `/admin/paths`. Confirm the page loads (non-owners are
       redirected — that gate is verified separately).
    3. For each of TEN collection paths:
       a. Click "New Path".
       b. Pick a seed watch via the search-as-you-type picker.
       c. Add 1–3 follow-on watches, each with a short rationale ("why this
          next?").
       d. Write the path's editorial rationale.
       e. Select one path-type chip — "Going Deeper", "Branching Out",
          "Trading Up", or "Filling a Gap".
       f. Click "Save Draft", then "Publish Path".
    4. Return to `/admin/paths` and confirm ten paths are listed, each showing a
       "Published" badge.
    5. Spread the path-type labels across the four-value vocabulary where the
       content naturally fits — do not force an even split, but avoid using only
       one label for all ten.

    Suggested theme starting points (the owner may substitute freely):
    - A "Going Deeper" path within one genre (e.g. dive watches).
    - A "Branching Out" path crossing genres (e.g. sport → dress).
    - A "Trading Up" path moving up a tier.
    - A "Filling a Gap" path adding an unowned role.

    Acceptance: ten rows in `collection_paths` with `status = 'published'`,
    each with a `seed_catalog_id` and a `path_type` in the four-value set.
    Confirm with: `SELECT count(*) FROM collection_paths WHERE status = 'published';`
    (expect 10) and `SELECT path_type, count(*) FROM collection_paths GROUP BY path_type;`.
  </how-to-verify>
  <verify>
    <automated>MANUAL — after resume, confirm: SELECT count(*) FROM collection_paths WHERE status = 'published' returns 10</automated>
  </verify>
  <resume-signal>Type "10 paths published" once all ten collection paths are authored and in published state, or describe any issue encountered with the authoring UI.</resume-signal>
  <done>Ten collection paths exist in the collection_paths table with status = 'published', each with a seed watch and a four-value path-type label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner authoring session → CMS write actions | Content authoring crosses owner-gated Server Actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-22 | Tampering | path nodes referencing non-existent catalog watches | mitigate | The watch picker only surfaces real `watches_catalog` rows via `searchCatalogWatches`; FK constraints on `seed_catalog_id` and `collection_path_nodes.catalog_id` reject invalid references |
| T-45-23 | Information Disclosure | a draft path leaking to a public surface before publish | accept | Phase 45 ships no public surface; Phase 47 quality-gates on `status = 'published'`; this checkpoint explicitly publishes each path |
</threat_model>

<verification>
- `SELECT count(*) FROM collection_paths WHERE status = 'published'` returns 10
- Each published path has a non-null `seed_catalog_id`
- Each published path's `path_type` is one of the four D-16 values (DB CHECK guarantees this)
</verification>

<success_criteria>
- Ten collection paths are authored through the `/admin/paths` UI
- All ten are in published state
- Path-type labels are spread across the four-value vocabulary where content fits
</success_criteria>

<output>
After completion, create `.planning/phases/45-cms-data-model-admin-routes/45-06-SUMMARY.md`.
Record the ten path titles/themes and their path-type labels for Phase 47 reference.
</output>
