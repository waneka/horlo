---
phase: 45-cms-data-model-admin-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/db/schema.ts
  - supabase/migrations/20260518200000_phase45_cms_tables.sql
  - src/lib/auth.ts
autonomous: true
requirements: [CMS-01, CMS-02, CMS-09]
must_haves:
  truths:
    - "Five CMS tables exist in the local DB after drizzle-kit push"
    - "profiles has an is_admin boolean column; the owner row has is_admin = true"
    - "Non-owner authenticated reads of draft curated_lists/collection_paths return zero rows"
    - "Deleting a watches_catalog row referenced by a CMS table raises a foreign-key RESTRICT error"
    - "assertOwner() resolves for the owner and throws UnauthorizedError for a non-admin user"
  artifacts:
    - path: "supabase/migrations/20260518200000_phase45_cms_tables.sql"
      provides: "is_admin column, 5 CMS tables, RLS, path_type CHECK, cms_settings seed row, owner is_admin update"
      contains: "create table"
    - path: "src/db/schema.ts"
      provides: "Drizzle definitions for curatedLists, curatedListItems, collectionPaths, collectionPathNodes, cmsSettings + profiles.isAdmin"
      contains: "curatedLists"
    - path: "src/lib/auth.ts"
      provides: "assertOwner() server-only helper"
      exports: ["assertOwner"]
  key_links:
    - from: "src/lib/auth.ts assertOwner()"
      to: "profiles.is_admin"
      via: "supabase select is_admin"
      pattern: "is_admin"
    - from: "supabase/migrations/20260518200000_phase45_cms_tables.sql"
      to: "watches_catalog.id"
      via: "ON DELETE RESTRICT foreign keys"
      pattern: "on delete restrict"
---

<objective>
Build the Phase 45 data foundation: install `react-markdown`, add the five CMS
tables and the `profiles.is_admin` column to both the Drizzle schema and a raw
SQL migration (RLS, CHECK constraint, owner seed), push the schema to the local
DB, and add the `assertOwner()` server-only helper.

Purpose: Every downstream Phase 45 plan (DAL, Server Actions, admin routes)
depends on these tables, the `is_admin` column, and `assertOwner()` existing.
This plan ships nothing user-visible — it is the contract layer.

Output: Updated `src/db/schema.ts`, one new migration file, an updated
`src/lib/auth.ts`, and `react-markdown` in `package.json`. Local DB has all
five tables after the [BLOCKING] schema push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-cms-data-model-admin-routes/45-CONTEXT.md
@.planning/phases/45-cms-data-model-admin-routes/45-RESEARCH.md
@.planning/phases/45-cms-data-model-admin-routes/45-PATTERNS.md
@AGENTS.md

<interfaces>
<!-- Drizzle column-shape targets. RESEARCH.md §Schema Design + PATTERNS.md
     §src/db/schema.ts are the authoritative source — read them before writing. -->

Existing FK pattern (RESTRICT) — src/db/schema.ts watchLineageEdges:
  predecessorCatalogId: uuid('predecessor_catalog_id').notNull()
    .references(() => watchesCatalog.id, { onDelete: 'restrict' })

Existing text-enum status pattern — src/db/schema.ts watches:
  status: text('status', { enum: [...] }).notNull()

assertOwner() target — src/lib/auth.ts getCurrentUser() (lines 12-20) is the
exact analog to extend; UnauthorizedError already exported there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-markdown and add Drizzle schema definitions</name>
  <files>package.json, package-lock.json, src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts (the file being modified — read the profiles table block, the watchLineageEdges RESTRICT-FK block, the watches status-enum block, the notifications table comment block)
    - .planning/phases/45-cms-data-model-admin-routes/45-PATTERNS.md §"src/db/schema.ts — 5 new tables + profiles.is_admin" (lines 234-359 — exact column definitions to mirror)
    - .planning/phases/45-cms-data-model-admin-routes/45-RESEARCH.md §"Schema Design" (lines 426-562)
  </read_first>
  <action>
    Run `npm install react-markdown` (locked dependency, version ^10.1.0 per STATE.md).
    Then add to `src/db/schema.ts`:
    (1) An `isAdmin: boolean('is_admin').notNull().default(false)` column on the
    existing `profiles` pgTable, placed after `avatarUrl`.
    (2) Five new pgTable exports — `curatedLists`, `curatedListItems`,
    `collectionPaths`, `collectionPathNodes`, `cmsSettings` — with the exact
    column shapes, indexes, and FK onDelete behaviors specified in 45-PATTERNS.md
    lines 254-350. Key constraints: `curatedListItems.catalogId` and
    `collectionPathNodes.catalogId` and `collectionPaths.seedCatalogId` reference
    `watchesCatalog.id` with `onDelete: 'restrict'` (D-07); `listId`/`pathId`
    child FKs use `onDelete: 'cascade'`; `cmsSettings.pinnedListId` uses
    `onDelete: 'set null'`. `collectionPaths.pathType` is plain `text('path_type')`
    — NO pgEnum (D-17). `cmsSettings.heroFormat` is a text enum with values
    `featured_list` and `featured_collector` (SEED-008 forward-compat). Add the
    unique constraint `curated_list_items_unique_pair` on (listId, catalogId).
    (3) A comment block above the CMS tables stating that CHECK constraints
    (`path_type_check`, `cms_settings_single_row`) and RLS policies live in
    `supabase/migrations/20260518200000_phase45_cms_tables.sql`, and that Drizzle
    is the source of truth for column types only.
    Import `boolean` and `unique` from `drizzle-orm/pg-core` if not already imported.
    Do NOT add any pgEnum.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies includes `react-markdown` at ^10.x
    - `src/db/schema.ts` exports `curatedLists`, `curatedListItems`, `collectionPaths`, `collectionPathNodes`, `cmsSettings`
    - `grep -c "onDelete: 'restrict'" src/db/schema.ts` increases by exactly 3 (curatedListItems.catalogId, collectionPathNodes.catalogId, collectionPaths.seedCatalogId)
    - `profiles` table definition contains `isAdmin: boolean('is_admin')`
    - No `pgEnum` is introduced for `path_type` — `grep "path_type" src/db/schema.ts` shows `text('path_type')`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>react-markdown installed; five CMS tables + profiles.isAdmin defined in Drizzle schema; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Write the CMS tables + RLS migration and add assertOwner()</name>
  <files>supabase/migrations/20260518200000_phase45_cms_tables.sql, src/lib/auth.ts</files>
  <read_first>
    - supabase/migrations/20260420000001_social_tables_rls.sql (RLS naming convention `{table}_{operation}_own`, the `(SELECT auth.uid())` InitPlan wrapper)
    - supabase/migrations/20260516000000_phase43_avatar_bucket.sql (the DO $$ sanity-assertion block pattern, lines 68-87)
    - src/lib/auth.ts (the file being modified — read getCurrentUser() lines 12-20 and the UnauthorizedError class)
    - .planning/phases/45-cms-data-model-admin-routes/45-PATTERNS.md §"20260518200000_phase45_cms_tables.sql" (lines 34-151 — exact SQL excerpts) and §"src/lib/auth.ts — add assertOwner()" (lines 363-403)
    - .planning/phases/45-cms-data-model-admin-routes/45-RESEARCH.md §"Pattern 4: RLS Policies" and §"RLS Two-Layer Publish Gating"
  </read_first>
  <action>
    Create `supabase/migrations/20260518200000_phase45_cms_tables.sql`. The
    migration must, in order:
    (1) `ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS is_admin boolean NOT NULL DEFAULT false`.
    (2) `CREATE TABLE` for all five CMS tables matching the Drizzle column shapes
    from Task 1 — `curated_lists`, `curated_list_items`, `collection_paths`,
    `collection_path_nodes`, `cms_settings` — with the same FK ON DELETE behaviors
    (RESTRICT on catalog references, CASCADE on parent references, SET NULL on
    `cms_settings.pinned_list_id`), the unique `curated_list_items_unique_pair`,
    and the indexes named in 45-PATTERNS.md.
    (3) `ALTER TABLE public.collection_paths ADD CONSTRAINT collection_paths_path_type_check CHECK (path_type IN ('Going Deeper', 'Branching Out', 'Trading Up', 'Filling a Gap'))` (D-16, D-17).
    (4) `ALTER TABLE public.cms_settings ADD CONSTRAINT cms_settings_single_row CHECK (id = 1)` then `INSERT INTO public.cms_settings (id) VALUES (1) ON CONFLICT DO NOTHING`.
    (5) `ENABLE ROW LEVEL SECURITY` on all five tables. For `curated_lists` and
    `collection_paths`: a public-read SELECT policy `USING (status = 'published')`
    for roles `authenticated, anon` (D-03 layer 1); an owner SELECT policy
    `USING (EXISTS (SELECT 1 FROM public.profiles WHERE id = (SELECT auth.uid()) AND is_admin))`;
    and owner INSERT/UPDATE/DELETE policies with the same EXISTS predicate (D-02).
    For `curated_list_items` and `collection_path_nodes` (no status column): an
    owner-all SELECT policy (EXISTS predicate) PLUS a public `authenticated, anon`
    SELECT policy (so the Phase 47 list-detail page can read items of published
    lists; the parent `status` gate is enforced at the DAL join layer); plus owner
    INSERT/UPDATE/DELETE. For `cms_settings`: public `authenticated, anon` SELECT
    (no sensitive data — Phase 47 hero reads it) plus owner UPDATE. Use the
    `{table}_{operation}_own` / `{table}_select_published` naming convention.
    (6) `UPDATE public.profiles SET is_admin = true WHERE id = (SELECT id FROM auth.users WHERE email = 'twwaneka@gmail.com')` (D-04 — email-keyed, cross-DB-stable).
    (7) A `DO $$ ... END $$` sanity-assertion block verifying the `is_admin`
    column exists and all five CMS tables exist; RAISE EXCEPTION on mismatch.
    Add NO SECURITY DEFINER function — D-07's plain RESTRICT FK approach avoids
    it; the SECDEF REVOKE pattern is intentionally NOT used here.

    Then add `assertOwner()` to `src/lib/auth.ts` after `getCurrentUserFull()`,
    exactly as specified in 45-PATTERNS.md lines 388-402: it calls
    `getCurrentUser()`, selects `is_admin` from `profiles` for that user, and
    throws `UnauthorizedError('Not an admin')` when `is_admin` is falsy; returns
    the user otherwise.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "create policy\|CREATE POLICY" supabase/migrations/20260518200000_phase45_cms_tables.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260518200000_phase45_cms_tables.sql` exists
    - Migration contains `ADD COLUMN IF NOT EXISTS is_admin boolean`
    - Migration contains `collection_paths_path_type_check` with the four exact path-type strings
    - Migration contains `cms_settings_single_row` CHECK and `INSERT INTO public.cms_settings (id) VALUES (1)`
    - Migration contains the email-keyed `UPDATE public.profiles SET is_admin = true`
    - `grep -i "security definer" supabase/migrations/20260518200000_phase45_cms_tables.sql` returns nothing
    - Migration contains a `DO $$` assertion block that RAISEs on missing tables
    - `src/lib/auth.ts` exports `assertOwner` and references `is_admin`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Migration file written with 5 tables, RLS, CHECK constraints, owner seed, sanity block; assertOwner() added to auth.ts; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push schema to the local database</name>
  <files>(no files modified — DB operation)</files>
  <read_first>
    - .planning/phases/45-cms-data-model-admin-routes/45-RESEARCH.md §"Migration Strategy" (lines 581-615 — local vs prod split, the 4 prod-push gotchas)
    - MEMORY: project_drizzle_supabase_db_mismatch (drizzle-kit push is LOCAL ONLY; prod uses supabase db push --linked)
  </read_first>
  <action>
    Run `npx drizzle-kit push` to apply the Drizzle schema changes (the five new
    tables + `profiles.is_admin`) to the LOCAL development database. This is
    LOCAL-ONLY — do NOT run `supabase db push --linked`; the committed migration
    SQL file from Task 2 is the prod artifact and is applied in a later deploy
    step outside this phase. drizzle-kit push only creates column shapes — it
    does NOT apply the RLS policies, CHECK constraints, or the `is_admin` owner
    seed from the migration SQL. If exercising RLS / CHECK / the owner seed
    locally is needed for verification, additionally apply the migration SQL to
    the local DB via the project's local-migration path (docker exec psql — see
    the project_local_db_reset memory note). Build and type checks pass WITHOUT
    this push because types come from `schema.ts`, not the live DB — that is the
    false-positive this BLOCKING task exists to prevent. Verification of this
    phase cannot proceed until the local DB reflects the live schema.
  </action>
  <verify>
    <automated>node -e "const{db}=require('./src/db');db.execute&&console.log('db module loads')" 2>/dev/null || echo "verify table presence via psql: \\dt should list curated_lists, curated_list_items, collection_paths, collection_path_nodes, cms_settings"</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` completes without error
    - Querying the local DB lists all five tables: `curated_lists`, `curated_list_items`, `collection_paths`, `collection_path_nodes`, `cms_settings`
    - The local `profiles` table has an `is_admin` column
    - `supabase db push --linked` was NOT run (prod is untouched this phase)
  </acceptance_criteria>
  <done>Local DB schema reflects the five CMS tables and profiles.is_admin; downstream plans can run against a live local schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated DB role → Postgres RLS | Untrusted reads/writes cross here; RLS is the gate |
| non-owner session → CMS tables | A logged-in non-owner must never read drafts or write any CMS row |
| catalog mutation → CMS FK graph | A catalog delete must not orphan a CMS reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information Disclosure | curated_lists / collection_paths draft rows | mitigate | Public SELECT policy `USING (status = 'published')` for authenticated+anon; owner-only SELECT policy gated on `is_admin` EXISTS predicate |
| T-45-02 | Elevation of Privilege | INSERT/UPDATE/DELETE on all 5 CMS tables | mitigate | Write policies gated on `EXISTS (SELECT 1 FROM profiles WHERE id = (SELECT auth.uid()) AND is_admin)` |
| T-45-03 | Tampering / DoS | watches_catalog row referenced by a CMS table | mitigate | FK `ON DELETE RESTRICT` on catalog-referencing columns — DB refuses the delete |
| T-45-04 | Elevation of Privilege | profiles.is_admin column | accept | Single-owner project; `is_admin` set only by migration via email key. RLS on profiles already restricts UPDATE to `id = auth.uid()`; a future second user cannot self-grant admin without a separate migration — re-check when a 2nd user signs up |
| T-45-05 | Tampering | cms_settings multi-row insert | mitigate | `CHECK (id = 1)` single-row constraint blocks any second settings row |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Migration file present and well-formed (CREATE POLICY count ≥ 20, CHECK constraints present, no SECURITY DEFINER)
- Local DB has all five CMS tables and `profiles.is_admin` after drizzle-kit push
- `assertOwner` exported from `src/lib/auth.ts`
</verification>

<success_criteria>
- Five CMS tables exist in the local DB with the correct FK ON DELETE behaviors
- `profiles.is_admin` exists; the owner row is seeded `true` by the migration
- `assertOwner()` is callable and gates on `is_admin`
- The migration contains no SECURITY DEFINER function (D-07 confirmed)
- react-markdown is installed
</success_criteria>

<output>
After completion, create `.planning/phases/45-cms-data-model-admin-routes/45-01-SUMMARY.md`.
Record explicitly in the SUMMARY:
- That the migration contains NO SECURITY DEFINER function, so the
  `project_supabase_secdef_grants` REVOKE pattern was correctly not applied.
- D-10 forward-flag: the RESTRICT FKs from CMS tables onto `watches_catalog`
  will block a future bulk catalog wipe/TRUNCATE (cf. Phase 36 catalog wipes,
  v5.2 expansion). A later catalog-mutation phase must account for this.
</output>
</invoke>
