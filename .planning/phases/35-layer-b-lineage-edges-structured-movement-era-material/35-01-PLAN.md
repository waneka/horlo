---
phase: 35
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/static/hierarchy.lineage-3-node.test.ts
autonomous: true
requirements:
  - CAT-16
threat_refs: []
tags:
  - test
  - static-guard
  - wave0
  - phase35
must_haves:
  truths:
    - "tests/static/hierarchy.lineage-3-node.test.ts exists in repo and is runnable by vitest"
    - "Test asserts presence of CYCLE id SET is_cycle USING path in src/data/hierarchy.ts"
    - "Test asserts presence of depth < 10 in src/data/hierarchy.ts"
    - "Test asserts export of getLineageForReference function in src/data/hierarchy.ts"
    - "Test asserts import 'server-only' present in src/data/hierarchy.ts"
    - "Test passes vacuously while hierarchy.ts is missing (Wave 0 guard pattern)"
  artifacts:
    - path: "tests/static/hierarchy.lineage-3-node.test.ts"
      provides: "Wave 0 static guard for ROADMAP SC#2 + SC#3 (G1, G2, G3 from VALIDATION.md)"
      contains: "CYCLE\\s+id\\s+SET\\s+is_cycle\\s+USING\\s+path"
  key_links:
    - from: "tests/static/hierarchy.lineage-3-node.test.ts"
      to: "src/data/hierarchy.ts"
      via: "existsSync + readFileSync source scan with regex assertions"
      pattern: "existsSync.*hierarchy"
---

<objective>
Wave 0 static guard test. Create `tests/static/hierarchy.lineage-3-node.test.ts` BEFORE any production code so executors in Wave 2 (Plan 04 — `src/data/hierarchy.ts`) have a red→green target instead of writing untested CTE code. Mirrors the `tests/static/CollectionFitCard.no-engine.test.ts` pattern: `existsSync` guard for vacuous-pass while the target file does not yet exist, then `readFileSync` + regex assertions for the four required source-level invariants from CAT-16 SC#2 + SC#3.

Purpose: Satisfy VALIDATION.md Wave 0 dependency. Unblock Plan 04 (DAL hierarchy.ts) by giving it a deterministic test target. Gates G1, G2, G3 from 35-VALIDATION.md depend on this file.

Output: One new test file. Test passes vacuously now; same test enforces correctness once Plan 04 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-layer-b-lineage-edges-structured-movement-era-material/35-CONTEXT.md
@.planning/phases/35-layer-b-lineage-edges-structured-movement-era-material/35-VALIDATION.md

<interfaces>
<!-- Pattern target — copy this shape exactly. -->
From tests/static/CollectionFitCard.no-engine.test.ts:
```typescript
import { describe, it, expect } from 'vitest'
import { existsSync, readFileSync } from 'node:fs'

describe('Phase 20 D-04 — <CollectionFitCard> pure-renderer invariant', () => {
  const cardPath = 'src/components/insights/CollectionFitCard.tsx'

  it('does not import @/lib/similarity', () => {
    if (!existsSync(cardPath)) {
      return   // Vacuous pass until file is created
    }
    const src = readFileSync(cardPath, 'utf8')
    expect(src).not.toMatch(/from ['"]@\/lib\/similarity['"]/)
  })
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Wave 0 static guard test for hierarchy.ts CTE invariants</name>
  <files>tests/static/hierarchy.lineage-3-node.test.ts</files>
  <read_first>
    - tests/static/CollectionFitCard.no-engine.test.ts (full pattern analog — copy structure)
    - .planning/phases/35-layer-b-lineage-edges-structured-movement-era-material/35-VALIDATION.md §"Wave 0 Requirements" + §"Validation Gate Table" gates G1/G2/G3
    - .planning/phases/35-layer-b-lineage-edges-structured-movement-era-material/35-RESEARCH.md §10 "Test Harness" — recommended skeleton verbatim
    - .planning/phases/35-layer-b-lineage-edges-structured-movement-era-material/35-CONTEXT.md §D-08 (CYCLE clause + depth<10 contract)
    - vitest.config.ts (confirm jsdom env + globals: true)
  </read_first>
  <action>
Create `tests/static/hierarchy.lineage-3-node.test.ts` with EXACTLY this content (copy verbatim, do not paraphrase):

```typescript
import { describe, it, expect } from 'vitest'
import { existsSync, readFileSync } from 'node:fs'

const HIERARCHY_PATH = 'src/data/hierarchy.ts'

describe('Phase 35 CAT-16 SC#2/SC#3 — hierarchy.ts recursive CTE safety guards', () => {
  it('hierarchy.ts file exists', () => {
    // Wave 0 vacuous-pass: while Plan 04 has not yet created the file, this test passes.
    // Once Plan 04 ships, the assertion becomes load-bearing.
    if (!existsSync(HIERARCHY_PATH)) {
      return
    }
    expect(existsSync(HIERARCHY_PATH)).toBe(true)
  })

  it('every WITH RECURSIVE query includes the Postgres 15 CYCLE clause (G1)', () => {
    if (!existsSync(HIERARCHY_PATH)) return
    const src = readFileSync(HIERARCHY_PATH, 'utf-8')
    expect(src).toMatch(/CYCLE\s+id\s+SET\s+is_cycle\s+USING\s+path/i)
  })

  it('every WITH RECURSIVE query includes the depth < 10 guard (G1)', () => {
    if (!existsSync(HIERARCHY_PATH)) return
    const src = readFileSync(HIERARCHY_PATH, 'utf-8')
    expect(src).toMatch(/depth\s*<\s*10/)
  })

  it('getLineageForReference function is exported (G2)', () => {
    if (!existsSync(HIERARCHY_PATH)) return
    const src = readFileSync(HIERARCHY_PATH, 'utf-8')
    expect(src).toMatch(/export\s+(async\s+)?function\s+getLineageForReference/)
  })

  it("imports 'server-only' (G3)", () => {
    if (!existsSync(HIERARCHY_PATH)) return
    const src = readFileSync(HIERARCHY_PATH, 'utf-8')
    expect(src).toMatch(/import ['"]server-only['"]/)
  })
})
```

The 5 `it()` blocks map directly to gates G1/G2/G3 from 35-VALIDATION.md. Vacuous-pass via `existsSync` guard is intentional Wave 0 pattern (mirrors `CollectionFitCard.no-engine.test.ts`).
  </action>
  <verify>
    <automated>npx vitest run tests/static/hierarchy.lineage-3-node.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/static/hierarchy.lineage-3-node.test.ts` exists
    - File contains literal string `'src/data/hierarchy.ts'`
    - File contains regex literal `/CYCLE\s+id\s+SET\s+is_cycle\s+USING\s+path/i`
    - File contains regex literal `/depth\s*<\s*10/`
    - File contains regex literal `/export\s+(async\s+)?function\s+getLineageForReference/`
    - File contains regex literal `/import ['"]server-only['"]/`
    - File imports `describe, it, expect` from `vitest`
    - File imports `existsSync, readFileSync` from `node:fs`
    - `npx vitest run tests/static/hierarchy.lineage-3-node.test.ts` exits 0 (vacuous pass while hierarchy.ts is absent)
    - 5 `it(...)` blocks present (count via `grep -c "  it(" tests/static/hierarchy.lineage-3-node.test.ts` = 5)
  </acceptance_criteria>
  <done>Test file exists; vitest passes vacuously; ready for Plan 04 to convert vacuous-pass to load-bearing-pass by creating src/data/hierarchy.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (test-only file) | Pure static source-scan test; no runtime data; no untrusted input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-TEST-01 | Tampering | tests/static/hierarchy.lineage-3-node.test.ts | accept | Test file is committed to repo; tampering would surface in code review and `npx vitest run` regression. No production code path. |
</threat_model>

<verification>
- `npx vitest run tests/static/hierarchy.lineage-3-node.test.ts` exits 0
- Vitest reports 5 passing tests (all vacuous via `existsSync` guard)
- File present in `git status`
</verification>

<success_criteria>
- Wave 0 dependency from 35-VALIDATION.md satisfied
- Plan 04 (DAL hierarchy.ts) can run with a red-target test ready to flip green on first correct implementation
- Gates G1, G2, G3 from 35-VALIDATION.md have a runnable test command
</success_criteria>

<output>
After completion, create `.planning/phases/35-layer-b-lineage-edges-structured-movement-era-material/35-01-SUMMARY.md`
</output>
