---
phase: 76-video-schema-storage-paths-server-action
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - .planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md
autonomous: false
requirements:
  - VID-07
  - VID-08
  - VID-09
  - VID-10
  - VID-11
  - VID-12
  - VID-16
tags:
  - verification
  - post-deploy
  - supabase-prod-push
must_haves:
  truths:
    - "Per durable memory `project_baseline_not_green_build_is_gate`: `npm run build` (exit 0) is the authoritative gate for the phase — verified here as the last automated step. Pre-existing test-file tsc errors and ≥1 pre-existing test failure (CommentGateLocked font-medium per the memory) are baseline noise and do NOT count against Phase 76"
    - "Per durable memory `project_drizzle_supabase_db_mismatch`: `supabase db push --linked` is the prod path; this plan documents the human step (filename, ordering, gotchas) but does NOT execute it autonomously — the human runs it after PR merge, NOT during the autonomous wave (autonomous:false on the migration task)"
    - "Per ROADMAP success-criterion 5 (VID-11): the existing photo flow through `logWearWithPhoto` is provably untouched — verified by Phase 15's existing integration test (`tests/integration/phase15-wywt-photo-flow.test.ts`) still passing after Plan 01/02/03 ship. If it fails, that is a Phase 76 regression that must close before /gsd-verify-work"
    - "Per RESEARCH §Open Questions #5: the existing `wear_photos_select_three_tier` SELECT RLS policy on storage.objects uses `split_part(storage.filename(name), '.', 1)` which extracts the wear_event_id UUID identically from `.jpg` and `.mp4` filenames — the post-deploy doc captures the manual verification step (upload as user A, attempt signed-URL as user B → 403) per 76-VALIDATION.md Manual-Only Verifications"
    - "Per CLAUDE.md / AGENTS.md: this plan does NOT modify any application code — it runs verification commands and documents the human post-deploy step. No Next.js doc lookup required"
    - "Per 76-VALIDATION.md Validation Sign-Off: this plan flips `nyquist_compliant` to `true` once all VID-NN automated verifies have green status from Plans 01-03"
  artifacts:
    - path: .planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md
      provides: "Human-runnable runbook for the production schema push: prerequisites (SUPABASE_ACCESS_TOKEN set, on main branch after merge), the exact `supabase db push --linked` command, expected output, rollback note (transactional migration — failure rolls back automatically), and the 1-line manual RLS verification per 76-VALIDATION.md Manual-Only Verifications row"
      min_lines: 40
  key_links:
    - from: ".planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md"
      to: "supabase/migrations/20260622000000_phase76_video_schema.sql"
      via: "the migration file the runbook tells the human to push to prod via `supabase db push --linked`"
      pattern: "20260622000000_phase76_video_schema"
    - from: "Verification task (Task 1) output"
      to: ".planning/phases/76-video-schema-storage-paths-server-action/76-VALIDATION.md"
      via: "writes all 7 VID-NN green-status rows into the per-task verification map (or surfaces red rows for the operator)"
      pattern: "VID-0[789]|VID-1[0-2]|VID-16"
---

<objective>
Run the full Phase 76 verification pass: targeted vitest on all Plan 01/02/03 test files, full vitest suite (to confirm no cross-phase regression — especially `tests/integration/phase15-wywt-photo-flow.test.ts` for VID-15), and `npm run build` exit 0 (the authoritative gate). Then write `76-POST-DEPLOY.md` documenting the human-runnable `supabase db push --linked` step for after-PR-merge prod application of the schema migration + bucket MIME UPDATE.

Purpose: bind the four-plan Phase 76 deliverable into a single shippable unit and prepare the operator handoff. The DB CHECK constraint + bucket MIME UPDATE cannot reach prod via Drizzle — they live in the Supabase migration file only and require explicit `supabase db push --linked` execution. Documenting the runbook here (rather than expecting institutional memory) prevents the recurrence pattern from `project_drizzle_supabase_db_mismatch` (filename ordering, extension schema, enum-bound dependents, prod-only constraint application).

Output:
- All Plan 01/02/03 automated verifies green (or operator-surfaced red status with concrete next step)
- `npm run build` exit 0 confirmed
- `tests/integration/phase15-wywt-photo-flow.test.ts` still green (VID-15 regression check)
- `.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md` runbook written
- 76-VALIDATION.md per-task verification map updated with green status; `nyquist_compliant: true`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/76-video-schema-storage-paths-server-action/76-RESEARCH.md
@.planning/phases/76-video-schema-storage-paths-server-action/76-PATTERNS.md
@.planning/phases/76-video-schema-storage-paths-server-action/76-VALIDATION.md
@.planning/phases/76-video-schema-storage-paths-server-action/76-01-PLAN.md
@.planning/phases/76-video-schema-storage-paths-server-action/76-02-PLAN.md
@.planning/phases/76-video-schema-storage-paths-server-action/76-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full Phase 76 verification — targeted tests, full vitest, npm run build, Phase 15 regression check</name>
  <files></files>
  <read_first>
    - .planning/phases/76-video-schema-storage-paths-server-action/76-VALIDATION.md (per-task verification map — confirms which test files map to which VID-NN)
    - durable memory `project_baseline_not_green_build_is_gate` (build is the gate; full test suite has pre-existing failures)
  </read_first>
  <acceptance_criteria>
    - `npx vitest run tests/integration/phase76-video-schema.test.ts` exits 0 (5 tests pass with DATABASE_URL, OR 1 file cleanly skipped without — both green from Plan 04's perspective)
    - `npx vitest run tests/unit/buildWearVideoPath.test.ts` exits 0 with 6 tests passed
    - `npx vitest run tests/actions/wearEventsVideo.test.ts` exits 0 with 9 tests passed
    - `npx vitest run tests/integration/phase15-wywt-photo-flow.test.ts` exits 0 (regression check for VID-15 — must still pass; if it fails, Plan 03 broke `logWearWithPhoto` and must be fixed before /gsd-verify-work)
    - `npm run build` exits 0 — the gate per durable memory `project_baseline_not_green_build_is_gate`
    - Output of `grep -c "export async function logWearWithPhoto" src/app/actions/wearEvents.ts` is exactly 1 (VID-15 regression — `logWearWithPhoto` was not duplicated or renamed by Plan 03)
    - Output of `grep -c "export async function logWearEventWithPhoto" src/data/wearEvents.ts` is exactly 1 (VID-15 regression — `logWearEventWithPhoto` was not duplicated or renamed by Plan 03)
  </acceptance_criteria>
  <action>
    Execute the verification pipeline in order — sequentially, not in parallel (each command's exit code matters; subsequent steps gate on the previous's success). Per durable memory `project_baseline_not_green_build_is_gate`, treat ONLY these targeted commands as the phase gate; do NOT attribute pre-existing failures in unrelated test files to Phase 76. Step 1: `npx vitest run tests/integration/phase76-video-schema.test.ts` — assert exit 0; tail the last 30 lines to extract "Test Files 1 passed" or "1 skipped". Step 2: `npx vitest run tests/unit/buildWearVideoPath.test.ts` — assert exit 0 with "6 passed". Step 3: `npx vitest run tests/actions/wearEventsVideo.test.ts` — assert exit 0 with "9 passed". Step 4 (CRITICAL VID-15 REGRESSION GATE): `npx vitest run tests/integration/phase15-wywt-photo-flow.test.ts` — assert exit 0 (skipped acceptable when DATABASE_URL absent; if it RUNS and fails, that means Plan 03 broke the photo flow and is a phase-blocking regression). Step 5: `npm run build` — assert exit 0; tail the last 50 lines to confirm "Compiled successfully" or "✓ Compiled" appears. Step 6: regression grep counts — `grep -c "export async function logWearWithPhoto" src/app/actions/wearEvents.ts` must equal 1; `grep -c "export async function logWearEventWithPhoto" src/data/wearEvents.ts` must equal 1. If ANY of Steps 1-6 fails, do NOT proceed to Task 2 — surface the failure to the operator with the exact command, the failing assertion, and the next-step recommendation (typically: fix the failing test in the responsible plan + re-run /gsd-execute-phase for that plan). The vitest invocations may exceed default 2-minute Bash timeout if the full integration suite spins up Drizzle — pass `timeout: 300000` (5 minutes) on the verifying Bash calls.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/buildWearVideoPath.test.ts tests/actions/wearEventsVideo.test.ts tests/integration/phase76-video-schema.test.ts tests/integration/phase15-wywt-photo-flow.test.ts 2>&1 | tail -40 | grep -qE "Test Files\s+[0-9]+ passed" && npm run build 2>&1 | grep -qE "Compiled successfully|✓ Compiled" && grep -c "export async function logWearWithPhoto" /Users/tylerwaneka/Documents/horlo/src/app/actions/wearEvents.ts | grep -q "^1$" && grep -c "export async function logWearEventWithPhoto" /Users/tylerwaneka/Documents/horlo/src/data/wearEvents.ts | grep -q "^1$"</automated>
  </verify>
  <done>
    - All 4 targeted vitest invocations pass (or cleanly skip under env-gate)
    - `npm run build` exits 0
    - `logWearWithPhoto` + `logWearEventWithPhoto` each appear exactly once (VID-15 regression guards green)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write .planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md runbook</name>
  <files>.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md</files>
  <read_first>
    - supabase/migrations/20260622000000_phase76_video_schema.sql (the migration the runbook tells the human to apply to prod)
    - .planning/phases/76-video-schema-storage-paths-server-action/76-VALIDATION.md §Manual-Only Verifications (the post-deploy RLS check)
    - durable memory `project_drizzle_supabase_db_mismatch` (4 prod-push gotchas: filename, ordering, extension schema, enum-bound dependents)
    - The most recent successful `supabase db push --linked` runbook precedent — search `.planning/milestones/v7.0-phases/` or `v8.0-phases/` for any `*-POST-DEPLOY.md` or `*-PROD-DEPLOY.md` file as a style template if present (use `find .planning/milestones -name "*POST*DEPLOY*"` and `find .planning/milestones -name "*PROD*DEPLOY*"`)
  </read_first>
  <acceptance_criteria>
    - File exists at `.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md`
    - File contains a 5-section structure: (1) Prerequisites, (2) Pre-flight check, (3) The push command, (4) Expected output + verification, (5) Manual RLS check (per 76-VALIDATION.md Manual-Only Verifications)
    - References the exact migration filename `20260622000000_phase76_video_schema.sql`
    - References the durable-memory gotchas (`project_drizzle_supabase_db_mismatch` — filename ordering, enum-bound dependents)
    - Notes that the migration is wrapped in BEGIN/COMMIT — partial-failure rollback is automatic; no manual rollback script needed
    - Notes that the `wear-photos` bucket `allowed_mime_types` UPDATE is part of the migration — no separate dashboard step
    - The manual RLS check section gives the EXACT steps to upload an .mp4 as user A and verify user B's signed-URL request returns 403
  </acceptance_criteria>
  <action>
    Create `.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md` (Markdown, ~50-100 lines). Front matter: `# Phase 76 — Post-Deploy Runbook` + `Status: pending operator execution after PR merge`. Section 1 (`## Prerequisites`): bullets — `SUPABASE_ACCESS_TOKEN` env var set (or `supabase login` completed); on the `main` branch with Phase 76 commits merged; Vercel deploy has succeeded (so the Drizzle-pushed local schema agrees with the prod schema before the migration runs). Section 2 (`## Pre-flight check`): bullets — confirm the migration file exists at `supabase/migrations/20260622000000_phase76_video_schema.sql`; confirm it sorts AFTER `20260620204341_delete_orphan_users_and_add_auth_delete_trigger.sql` lexicographically (durable memory gotcha #1); `supabase migration list --linked` should show `20260622000000_phase76_video_schema` as `Pending` in the Remote column. Section 3 (`## Apply the migration to prod`): the exact command `supabase db push --linked` — note that it is idempotent (every section uses `IF NOT EXISTS` / `DO $$ ... NOT EXISTS` guards) so re-running is safe if it half-fails. Section 4 (`## Expected output + verification`): bullets — output ends with "Applying migration 20260622000000_phase76_video_schema.sql..." and "Finished supabase db push."; then `supabase migration list --linked` shows `20260622000000_phase76_video_schema` as Applied in both Local and Remote columns. Then via the Supabase dashboard SQL editor or `psql` against the prod connection string: `SELECT typname FROM pg_type WHERE typname = 'media_type';` returns one row; `\d wear_events` shows the three new columns + the CHECK constraint `wear_events_video_paths_required`; `SELECT allowed_mime_types FROM storage.buckets WHERE id = 'wear-photos';` includes `video/mp4` in the array. Section 5 (`## Manual RLS check — wear-photos `.mp4` SELECT (per 76-VALIDATION.md)`): EXACT human steps — (a) log in as a test user on horlo.app via the prod URL; (b) use the Supabase dashboard Storage UI to upload a tiny mp4 to `wear-photos/{test-user-id}/00000000-0000-4000-8000-000000000001.mp4`; (c) sign out, sign in as a SECOND test user; (d) attempt to fetch a signed URL for the same path via the prod app's wear-detail page (or directly via the dashboard's `create_signed_url` RPC); (e) confirm the response is a 403 / error / NULL URL — the SELECT RLS policy `wear_photos_select_three_tier` should reject the cross-user read; (f) document the result (PASS / FAIL with details) in the post-deploy summary. Section 6 (`## Gotcha reminders` — from durable memory `project_drizzle_supabase_db_mismatch`): brief bullet list — (1) filename ordering (timestamps must lexicographically sort correctly); (2) ordering relative to other pending migrations; (3) extension schema (`storage.*` references work because the extension is in the `storage` schema); (4) enum-bound dependents (the new `media_type` enum is freshly created, no rename/drop risk this phase, but the gotcha applies for any future change to the enum). Footer: `Once Sections 1-5 pass, mark this phase complete via /gsd-phase-complete 76. The 76-POST-DEPLOY-RESULT.md sibling file may be created at that time to record actual prod output if desired.` Per CLAUDE.md: this is documentation only — no code changes. The Write tool is the right tool; no `npm run build` re-run needed (Task 1 covered it).
  </action>
  <verify>
    <automated>test -f /Users/tylerwaneka/Documents/horlo/.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md && grep -c "## Prerequisites" /Users/tylerwaneka/Documents/horlo/.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md | grep -q "^1$" && grep -c "supabase db push --linked" /Users/tylerwaneka/Documents/horlo/.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "20260622000000_phase76_video_schema" /Users/tylerwaneka/Documents/horlo/.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "wear_events_video_paths_required" /Users/tylerwaneka/Documents/horlo/.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "video/mp4" /Users/tylerwaneka/Documents/horlo/.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "wear_photos_select_three_tier\|RLS" /Users/tylerwaneka/Documents/horlo/.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md | awk '{print ($1 >= 1)}' | grep -q 1 && wc -l /Users/tylerwaneka/Documents/horlo/.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md | awk '{print ($1 >= 40)}' | grep -q 1</automated>
  </verify>
  <done>
    - File exists at the correct path
    - 6 sections present (Prerequisites / Pre-flight / Apply / Expected output / Manual RLS / Gotchas)
    - Migration filename referenced ≥2 times
    - Bucket MIME `video/mp4` documented
    - RLS check section present
    - File is ≥40 lines (substantive runbook)
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 [HUMAN — after PR merge to main]: Run `supabase db push --linked` to apply Phase 76 migration to prod</name>
  <files>(none — runs prod schema push commands via supabase CLI; no source files modified)</files>
  <read_first>
    - .planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md (the full operator runbook — Sections 1-5 are the executable steps)
    - supabase/migrations/20260622000000_phase76_video_schema.sql (the migration being applied to prod)
    - durable memory `project_drizzle_supabase_db_mismatch` (4 prod-push gotchas — filename, ordering, extension schema, enum-bound dependents)
  </read_first>
  <acceptance_criteria>
    - Operator-confirmed: `supabase migration list --linked` shows `20260622000000_phase76_video_schema` as Applied on both Local and Remote columns
    - Operator-confirmed: prod `\d wear_events` (via Supabase dashboard SQL editor or `psql` against prod connection string) shows the 3 new columns + the CHECK constraint
    - Operator-confirmed: prod `SELECT allowed_mime_types FROM storage.buckets WHERE id = 'wear-photos';` includes `video/mp4`
    - Operator-confirmed: Manual RLS check from runbook Section 5 passes — cross-user `.mp4` signed-URL request returns 403
    - Operator types `prod-migration-applied` (or pastes the error output for triage)
  </acceptance_criteria>
  <action>
    HUMAN-DRIVEN — this task pauses execution after PR merge and asks the operator to follow `76-POST-DEPLOY.md` Sections 1-5. Cannot be autonomously run because: (a) prod credentials (`SUPABASE_ACCESS_TOKEN` or `supabase login`) live in the operator's local environment, not in Claude's; (b) the migration is irreversible on prod and the operator should observe each step's output before proceeding; (c) the manual RLS check (Section 5) requires a real second user account and a browser session, which Claude cannot drive. Step-by-step: (1) Verify Section 1 Prerequisites are met — `SUPABASE_ACCESS_TOKEN` set, on `main` after PR merge, Vercel deploy succeeded. (2) Run Section 2 Pre-flight check — `supabase migration list --linked` should show the new migration as `Pending` on the Remote column. (3) Run Section 3 — `supabase db push --linked`; observe "Applying migration 20260622000000_phase76_video_schema.sql..." and "Finished supabase db push.". (4) Run Section 4 verification queries against prod — confirm enum exists, columns added, CHECK present, bucket MIME includes `video/mp4`. (5) Run Section 5 Manual RLS check on horlo.app prod with two test user accounts — confirm cross-user `.mp4` SELECT returns 403. (6) If any step fails (most likely failure: pending unrelated migration in `supabase/migrations/` that needs operator review before Phase 76's migration can apply), leave the phase open and surface the error to the planner. The 76-POST-DEPLOY.md runbook is the authoritative source of exact commands; this task's `<action>` is a meta-pointer to that runbook for the operator pause.
  </action>
  <verify>
    <automated>echo "HUMAN-VERIFIED checkpoint — no automated verify; operator confirmation via resume-signal 'prod-migration-applied' is the gate. See <acceptance_criteria> for the 5 things the operator must confirm." && true</automated>
  </verify>
  <done>
    - Operator types `prod-migration-applied` confirming all 5 acceptance criteria above
    - Prod DB has media_type enum + 3 new wear_events columns + CHECK constraint + updated bucket MIME
    - Manual RLS check (runbook Section 5) reports PASS
  </done>
  <what-built>
    Plans 01-03 have shipped on `main`. The local Drizzle schema and the Supabase migration file are in sync. The prod database does NOT yet contain the `media_type` enum, the 3 new `wear_events` columns, the CHECK constraint, or the updated bucket MIME allowlist. Phase 77's UI cannot ship until the prod DB has these objects.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/76-video-schema-storage-paths-server-action/76-POST-DEPLOY.md` (the runbook Task 2 just wrote)
    2. Follow Sections 1-4 (Prerequisites → Pre-flight → Apply → Expected output)
    3. Follow Section 5 (Manual RLS check) on horlo.app prod
    4. If any step fails, leave the phase open and surface the error (most likely failure: a pending unrelated migration from the supabase/migrations/ directory that needs operator review before the Phase 76 migration can apply)
  </how-to-verify>
  <resume-signal>Type "prod-migration-applied" once `supabase db push --linked` has completed and `supabase migration list --linked` shows `20260622000000_phase76_video_schema` as Applied on both Local and Remote columns. Or paste the error output for triage.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → prod Postgres (`supabase db push --linked`) | The CLI authenticates via `SUPABASE_ACCESS_TOKEN` (or `supabase login` credentials) — operator-trust boundary, not application-trust boundary |
| Phase 76 migration → existing prod data | The migration is additive only (no DROP/RENAME/UPDATE-of-photo_url); existing photo rows are not touched. Inline post-flight assertion (`WHERE media_type::text = 'video'` → 0 rows expected) is the runtime invariant check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04-MIGRATION-OUT-OF-ORDER | Tampering (migration sequencing) | filename timestamp ordering | mitigate | Plan 04 Task 2 §Pre-flight explicitly instructs the operator to confirm lexicographic ordering relative to `20260620204341_delete_orphan_users_and_add_auth_delete_trigger.sql`. Per durable memory `project_drizzle_supabase_db_mismatch` gotcha #1, this is the most common prod-push failure mode. |
| T-76-04-PARTIAL-FAILURE | Availability (atomic transaction failure) | BEGIN/COMMIT migration boundary | mitigate | The migration wraps every statement in a single `BEGIN;`/`COMMIT;` (Plan 01 acceptance criteria + verify gate); if the post-flight `RAISE EXCEPTION` fires, the whole transaction rolls back — no partial schema state ships. Runbook Section 3 explicitly notes the idempotence guarantee (re-runnable). |
| T-76-04-RLS-MISMATCH | Information disclosure (cross-user `.mp4` SELECT) | wear-photos `wear_photos_select_three_tier` policy | accept | Per RESEARCH §Open Questions #2 + #5: the existing policy's `split_part(storage.filename(name), '.', 1)` extracts the wear_event_id UUID from both `.jpg` and `.mp4` identically — no policy change needed. The runbook Section 5 Manual RLS check is the empirical verification. If it fails, that is a Phase 76 blocker — fix forward by amending the RLS policy in a follow-up migration. |
| T-76-04-OPERATOR-AUTH | Authentication (operator runs the prod push) | `SUPABASE_ACCESS_TOKEN` | accept | Operator-trust boundary outside Phase 76's scope — operator's local environment is assumed trusted. The CLI authenticates against Supabase's prod control plane. |
</threat_model>

<verification>
- Task 1 verify command exits 0 (all 4 targeted vitest files green-or-skipped + `npm run build` exits 0 + VID-15 regression grep counts exact)
- Task 2 verify command exits 0 (`76-POST-DEPLOY.md` exists with the 6 required sections + ≥40 lines)
- After Task 3 (human): `supabase migration list --linked | grep 20260622000000_phase76_video_schema` shows `Applied` on both Local and Remote columns
- After Task 3 (human): `psql` or Supabase SQL editor confirms `\d wear_events` shows the 3 new columns + the CHECK constraint
- After Task 3 (human): Manual RLS check from runbook Section 5 reports PASS (cross-user `.mp4` signed-URL request returns 403)
</verification>

<success_criteria>
- All Phase 76 automated verifies green: VID-07/08/09/10/11/12/16 each have at least one green test result from Plans 01-03
- `npm run build` exit 0 confirmed
- VID-15 regression preserved: `logWearWithPhoto` and `logWearEventWithPhoto` each exported exactly once; Phase 15 integration test still green
- Post-deploy runbook (`76-POST-DEPLOY.md`) written and operator-readable
- Prod migration applied via `supabase db push --linked` (Task 3 human step)
- Manual RLS verification passes (Task 3 human step, runbook Section 5)
- 76-VALIDATION.md `nyquist_compliant: true` flipped after Task 1 succeeds (this may be done by the executor as a small additional edit, or surfaced as a deferred operator step depending on tooling)
</success_criteria>

<output>
After completion, create `.planning/phases/76-video-schema-storage-paths-server-action/76-04-SUMMARY.md` per the GSD summary template. Include: (a) the exit codes / pass-counts from each of the 4 vitest invocations + npm run build; (b) confirmation that VID-15 regression grep counts both equal 1; (c) the path to the new `76-POST-DEPLOY.md` runbook; (d) note that Task 3 (the prod push) is operator-blocked — the SUMMARY explicitly notes the phase is "code-complete, awaiting prod migration push" until the operator runs `supabase db push --linked` and the post-deploy verification passes.
</output>
