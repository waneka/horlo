---
id: 50.1-01
phase: 50.1
plan_num: 1
plan: 01
name: Page-layer redirect + test rewrite + JSDoc/TODO updates
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [ARCH-02]
files_modified:
  - src/app/catalog/[catalogId]/page.tsx
  - tests/app/catalog-page.test.ts
threat_model_complete: true

must_haves:
  truths:
    - "When the authenticated viewer owns a row in `watches` with status='owned' and the requested catalogId, GET `/catalog/[catalogId]` issues a server-side redirect to `/watch/${viewerOwnedRow.id}` (SC#1)"
    - "Wishlist, grail, and sold rows on the same catalogId do NOT trigger the redirect — the cross-user verdict path still renders (SC#3, SC#6 / BUG-01 invariant)"
    - "`findViewerWatchByCatalogId` retains the `status='owned'` filter at the Drizzle predicate level (SC#2 Option B from RESEARCH §2; required by SC#3/SC#6)"
    - "A `TODO: revisit for Variant C in v7.0` comment exists immediately above the `<OtherOwnersRoster>` JSX render anchor (SC#5)"
    - "The stale D-08 JSDoc paragraph on `CatalogPage` (lines 36-38) is updated to reflect ARCH-02 redirect behavior, not the removed framing flip"
  artifacts:
    - path: "src/app/catalog/[catalogId]/page.tsx"
      provides: "Server-component catalog detail page with page-layer redirect on viewer-owned ref"
      contains: "import { notFound, redirect } from 'next/navigation'"
    - path: "tests/app/catalog-page.test.ts"
      provides: "vitest coverage proving redirect-on-owned + no-redirect-on-wishlist/grail/sold + SC#5 TODO presence"
      contains: "mockRedirect"
  key_links:
    - from: "src/app/catalog/[catalogId]/page.tsx if (viewerOwnedRow) branch"
      to: "next/navigation redirect()"
      via: "direct call with template-string URL `/watch/${viewerOwnedRow.id}`"
      pattern: "redirect\\(`/watch/\\$\\{viewerOwnedRow\\.id\\}`\\)"
    - from: "tests/app/catalog-page.test.ts vi.mock('next/navigation', ...)"
      to: "ARCH-02 redirect assertion + 3 BUG-01 negative tests + SC#5 static assertion"
      via: "vi.hoisted(() => vi.fn(...)) throw-with-digest pattern from insights-retirement.test.tsx"
      pattern: "mockRedirect.*toHaveBeenCalledWith"
---

<objective>
Implement the Variant B page-layer redirect for `/catalog/[catalogId]` when the authenticated viewer owns the catalog ref. Delete the in-route D-08 "self-via-cross-user" framing flip (lines 107-115 of `src/app/catalog/[catalogId]/page.tsx`); replace it with a single `redirect(\`/watch/${viewerOwnedRow.id}\`)` call. Simplify `findViewerWatchByCatalogId` per RESEARCH §2 Option B (keep `status='owned'` filter, simplify return shape). Add the SC#5 v7.0 TODO comment above the `<OtherOwnersRoster>` anchor. Update the stale D-08 JSDoc. Rewrite the catalog-page test file's two D-08 callout assertions as redirect assertions (Idiom 1 from RESEARCH §4 — augment the existing `vi.mock('next/navigation', ...)` to also mock `redirect`).

Purpose: Retire the Phase 48 BUG-01 maintenance tax by canonicalizing the URL when the viewer owns the watch. The two-route split (`/catalog/[catalogId]` for cross-user, `/watch/[id]` for owner) is preserved for non-owners; Variant C's unified `/w/[ref]` is deferred to v7.0.

Output: Modified server page with redirect; modified test file proving the redirect fires on owned and does NOT fire on wishlist/grail/sold; SC#5 TODO comment placed; stale JSDoc updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50.1-url-canonicalization/50.1-RESEARCH.md
@.planning/phases/50.1-url-canonicalization/50.1-VALIDATION.md
@.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md

# Files being modified — executor MUST read these in full before editing
@src/app/catalog/[catalogId]/page.tsx
@tests/app/catalog-page.test.ts

# Canonical analog: the redirect-assertion idiom this plan replicates
@tests/app/insights-retirement.test.tsx
@src/app/u/[username]/page.tsx

# Proxy boundary reference — the redirect must NOT live here
@src/proxy.ts

<interfaces>
<!-- Key contracts extracted from the codebase so the executor does not have to explore. -->

From `next/navigation` (used in 8 existing call sites):
```typescript
export function redirect(url: string): never  // throws NEXT_REDIRECT; must NOT be inside try/catch
export function notFound(): never
```

From `src/app/catalog/[catalogId]/page.tsx:282-308` (current `findViewerWatchByCatalogId` signature):
```typescript
async function findViewerWatchByCatalogId(
  userId: string,
  catalogId: string,
): Promise<{ id: string; acquisitionDate: string | null } | null>
```

Target post-Option-B signature (return type simplified — `id` is all the consumer needs after the redirect):
```typescript
async function findViewerWatchByCatalogId(
  userId: string,
  catalogId: string,
): Promise<{ id: string } | null>
```

From `tests/app/insights-retirement.test.tsx:3-9` (canonical mock-throw redirect pattern to replicate):
```typescript
vi.mock('next/navigation', () => ({
  redirect: vi.fn((url: string) => {
    const err: any = new Error('NEXT_REDIRECT')
    err.digest = `NEXT_REDIRECT;push;${url};307`
    throw err
  }),
}))
```

From `tests/app/catalog-page.test.ts:1-35` (existing `vi.hoisted()` block — the new `mockRedirect` MUST be added here so the mock factory at line 86 can reference it):
```typescript
const {
  mockGetCurrentUser, mockGetCatalogById, mockGetWatchesByUser, ...,
  mockNotFound,    // existing — keep
  // mockRedirect,  // <- ADD this entry, throwing NEXT_REDIRECT digest
  ...
} = vi.hoisted(() => ({ ... }))
```

From `tests/app/catalog-page.test.ts:86` (existing mock — augment, do NOT replace):
```typescript
vi.mock('next/navigation', () => ({ notFound: mockNotFound }))
// becomes:
// vi.mock('next/navigation', () => ({ notFound: mockNotFound, redirect: mockRedirect }))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <id>50.1-01-01</id>
  <name>Task 1: Modify `src/app/catalog/[catalogId]/page.tsx` — add redirect, delete D-08 flip, simplify findViewerWatchByCatalogId, add SC#5 TODO, update JSDoc</name>
  <files>src/app/catalog/[catalogId]/page.tsx</files>
  <read_first>
    - `src/app/catalog/[catalogId]/page.tsx` (READ IN FULL — 308 lines; you will edit lines 1, 30-45, 107-115, 226-231, 282-308)
    - `.planning/phases/50.1-url-canonicalization/50.1-RESEARCH.md` §2 (current state map + exact block to delete + Option B simplification), §3 (the `redirect()` call pattern, especially Pattern B from `src/app/insights/page.tsx`), §6 (SC#5 TODO placement at line 231), §8 Risk 4 (DO NOT drop the `status='owned'` filter — this is the binding constraint), §8 Risk 2 (redirect must NOT be inside try/catch), §10 Q1 (Option B rationale), §10 Q4 (recommended inline comment text), §10 Q5 (JSDoc update)
    - `src/app/u/[username]/page.tsx` (analog for the inline "OUTSIDE try/catch" comment style)
    - `src/app/insights/page.tsx` (analog — closest pattern to the conditional redirect this task implements)
  </read_first>
  <action>
Edit `src/app/catalog/[catalogId]/page.tsx` with these five concrete changes — each MUST be applied; do not omit any. The file has no `try {` blocks today, so the redirect is safely in clean control flow (RESEARCH §8 Risk 2 verified).

(1) **Imports — line 1.** Change `import { notFound } from 'next/navigation'` to `import { notFound, redirect } from 'next/navigation'`. Named import, alphabetical order preserved (`notFound` before `redirect` is NOT alphabetical, but matches the convention in this codebase — see `src/app/u/[username]/page.tsx`; either order is acceptable as long as both names are imported).

(2) **JSDoc — lines 30-45.** The current JSDoc documents D-07 and D-08 framing rules. The D-08 paragraph (currently lines 36-38: "D-08: when viewer already owns a watch with this catalogId → 'You own this' framing — no verdict computed; link points to viewer's per-user /watch/[id].") is now stale. Replace that bullet with: a new bullet documenting ARCH-02 — when the viewer owns a watch with this catalogId, the page issues a 307 redirect to `/watch/[id]` (the per-user route) instead of rendering. Keep the D-07 bullet ("when viewer collection is empty → no verdict card") unchanged. Keep the security note about `findViewerWatchByCatalogId` scope (currently lines 42-44) unchanged. Add a one-line reference to the source decision: see `.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md` §8 (Variant B).

(3) **D-08 framing flip — lines 107-115.** DELETE the current `if (viewerOwnedRow) { verdict = { framing: 'self-via-cross-user', ownedAtIso: ..., ownerHref: ... } }` block in its entirety (lines 108-115, the body of the `if (viewerOwnedRow)` branch, plus the two trailing "Phase 20.1 D-05" comment lines at 114-115). REPLACE the body with a single `redirect()` call:

  - The branch becomes: `if (viewerOwnedRow) { /* ARCH-02 — viewer owns this catalog ref; canonicalize to /watch/[id]. redirect() throws NEXT_REDIRECT and must NOT be wrapped in try/catch. */ redirect(\`/watch/${viewerOwnedRow.id}\`) }`. The comment text MUST match the wording recommended in RESEARCH §10 Q4 (use template literal with backticks for the URL).
  - Do NOT add a `return` after `redirect()` — `redirect()` throws and the function never returns from this branch.
  - Do NOT wrap in try/catch.
  - The `else if (collection.length > 0) {` branch at line 116 stays UNCHANGED.
  - The `else {` branch at line 148 (NSV-20 empty-collection) stays UNCHANGED.

(4) **SC#5 TODO comment — above line 231.** Currently the JSX at lines 226-231 has a Phase 39b comment block above `<OtherOwnersRoster ... />`. INSERT a new JSX comment block IMMEDIATELY above the existing `<OtherOwnersRoster>` JSX element (between the closing `*/}` of the Phase 39b NSV-18 comment and the `<OtherOwnersRoster>` opening tag), with the exact text from RESEARCH §6: a multi-line JSX comment starting `{/* TODO: revisit for Variant C in v7.0 — when the multi-photo carousel ...` and including the SEED-013 reference and the `.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md` §5 + §8.1 source citation. The substring `revisit for Variant C in v7.0` MUST appear verbatim (the SC#5 static assertion in Task 2 greps for it).

(5) **`findViewerWatchByCatalogId` simplification — lines 282-308 (Option B from RESEARCH §2).** Apply these sub-edits inside the function body:

  - Change return type from `Promise<{ id: string; acquisitionDate: string | null } | null>` to `Promise<{ id: string } | null>`.
  - In the `.select({...})` projection, REMOVE `acquisitionDate: watchesTable.acquisitionDate,` and `createdAt: watchesTable.createdAt,` — keep only `id: watchesTable.id`.
  - KEEP all three `and()` predicates UNCHANGED: `eq(watchesTable.userId, userId)`, `eq(watchesTable.catalogId, catalogId)`, and **especially** `eq(watchesTable.status, 'owned')` at the current line 296. The `status='owned'` filter is the BUG-01 invariant per RESEARCH §8 Risk 4 — if you remove it, the redirect would fire on wishlist/grail/sold rows and the WR-01 predicate-AST guard test would fail. PRESERVE this filter.
  - DELETE the `iso` computation (current lines 304-306 — `const iso = row.acquisitionDate !== null ? row.acquisitionDate : new Date(row.createdAt).toISOString()`).
  - Simplify the return at line 307: from `return { id: row.id, acquisitionDate: iso }` to `return { id: row.id }`.
  - DELETE the now-stale comment lines about `acquisitionDate` / `createdAt` / `VerdictBundleSelfOwned.ownedAtIso` (current lines 301-303). KEEP the JSDoc on the function (lines 274-281) but UPDATE it: change the description from "find a row for the D-08 callout — we need its id + acquisitionDate" to "find a row for the ARCH-02 redirect — we need its id to build `/watch/[id]`". KEEP the T-20-06-01 security note (currently lines 279-280) verbatim — it's still accurate (userId + catalogId scoping unchanged).
  - Keep the function name `findViewerWatchByCatalogId` (do not rename).

Per RESEARCH §10 Q1: this is Option B from §2. SC#2's wording in the ROADMAP says "drops the status filter or is simplified accordingly (planner's call — both are correct once the flip is gone)" — Option B is the only choice that satisfies SC#3 + SC#6 (binding constraints). Documented in the plan; executor should not deviate without raising a question.
  </action>
  <acceptance_criteria>
    - `grep -c "^import { notFound, redirect } from 'next/navigation'$" src/app/catalog/[catalogId]/page.tsx` returns `1`
    - `grep -c "redirect(\`/watch/\${viewerOwnedRow.id}\`)" src/app/catalog/[catalogId]/page.tsx` returns `1`
    - `grep -c "framing: 'self-via-cross-user'" src/app/catalog/[catalogId]/page.tsx` returns `0`
    - `grep -c "VerdictBundleSelfOwned" src/app/catalog/[catalogId]/page.tsx` returns `0`
    - `grep -c "ownedAtIso" src/app/catalog/[catalogId]/page.tsx` returns `0`
    - `grep -c "ownerHref" src/app/catalog/[catalogId]/page.tsx` returns `0`
    - `grep -c "revisit for Variant C in v7.0" src/app/catalog/[catalogId]/page.tsx` returns at least `1`
    - `grep -c "eq(watchesTable.status, 'owned')" src/app/catalog/[catalogId]/page.tsx` returns `1` (BUG-01 invariant preserved per Option B)
    - `grep -c "acquisitionDate" src/app/catalog/[catalogId]/page.tsx` returns `0` (Option B drops the now-unused acquisitionDate plumbing)
    - File does not contain `try {` anywhere (verifies redirect is not wrapped per RESEARCH §8 Risk 2): `grep -c "try {" src/app/catalog/[catalogId]/page.tsx` returns `0`
    - `npm run lint -- src/app/catalog/[catalogId]/page.tsx` exits 0
    - `npx tsc --noEmit` exits 0 (no type regressions from the simplified return shape)
  </acceptance_criteria>
  <verify>
    <automated>npm run lint -- src/app/catalog/[catalogId]/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>
    Page imports `redirect` from `next/navigation`. The `if (viewerOwnedRow)` body is a single-line `redirect()` call with the ARCH-02 inline comment; the inline `VerdictBundleSelfOwned` construction is gone. `findViewerWatchByCatalogId` returns `{ id: string } | null`, drops `acquisitionDate` + `createdAt` from the SELECT, and KEEPS the `eq(watchesTable.status, 'owned')` predicate. The SC#5 TODO comment is in place above `<OtherOwnersRoster>`. The JSDoc on `CatalogPage` no longer documents the D-08 framing flip. Lint + tsc are clean.
  </done>
</task>

<task type="auto">
  <id>50.1-01-02</id>
  <name>Task 2: Rewrite catalog-page test file — augment `next/navigation` mock with `redirect`; convert D-08 callout tests to ARCH-02 redirect tests; add SC#5 static assertion</name>
  <files>tests/app/catalog-page.test.ts</files>
  <read_first>
    - `tests/app/catalog-page.test.ts` (READ IN FULL — 339 lines; you will edit the vi.hoisted block at lines 4-35, the vi.mock at line 86, and rewrite the two D-08 tests at lines 175-203; you will ADD one new test for SC#5 TODO assertion)
    - `tests/app/insights-retirement.test.tsx` (CANONICAL pattern — lines 3-9 show the `vi.hoisted(() => vi.fn(...))`-style throw-with-digest mock; lines 30-37 show the `await expect(...).rejects.toThrow('NEXT_REDIRECT')` + `expect(redirect).toHaveBeenCalledWith(...)` assertion shape; lines 47-52 show the `fs.readFileSync` static assertion pattern that will be copied for SC#5)
    - `.planning/phases/50.1-url-canonicalization/50.1-RESEARCH.md` §4 (test infra recon — Idiom 1 is the chosen pattern; the mock-augmentation pattern is detailed at lines 333-339), §5 (the BUG-01 edge-case table — wishlist/grail/sold must NOT trigger the redirect; existing tests at lines 268-293 KEEP unchanged)
    - `src/app/catalog/[catalogId]/page.tsx` (the file post-Task-1; reference the redirect call to align the assertion URL pattern)
  </read_first>
  <action>
Edit `tests/app/catalog-page.test.ts` with these four concrete changes:

(1) **Augment `vi.hoisted()` block — lines 4-35.** Inside the destructured object, ADD a new entry `mockRedirect` to the hoisted exports list. Inside the factory function returned to `vi.hoisted(() => ({ ... }))`, ADD a new factory entry: `mockRedirect: vi.fn((url: string) => { const err: any = new Error('NEXT_REDIRECT'); err.digest = \`NEXT_REDIRECT;push;${url};307\`; throw err })`. This MUST match the canonical pattern in `tests/app/insights-retirement.test.tsx:3-9` — the digest format `NEXT_REDIRECT;push;${url};307` is the Next.js internal convention and the test assertions depend on it.

(2) **Augment `vi.mock('next/navigation', ...)` — current line 86.** Change `vi.mock('next/navigation', () => ({ notFound: mockNotFound }))` to `vi.mock('next/navigation', () => ({ notFound: mockNotFound, redirect: mockRedirect }))`. Do NOT add a separate `vi.mock` block — augment the existing one. Keep `notFound: mockNotFound` (still used by the `'returns 404 when catalogId does not exist'` test at line 151).

(3) **Convert the two D-08 callout tests at lines 175-203 to ARCH-02 redirect tests.**

  - **Existing test at lines 175-189** (`'renders "You own this watch" callout when viewer already owns this catalog ref (D-08)'`): RENAME the test description string to `'ARCH-02 — redirects to /watch/{viewer.watches.id} when viewer already owns this catalog ref'`. Rewrite the body: keep the same `mockGetCatalogById` + `mockGetWatchesByUser` + `mockDbLimit` setup (the row shape with `acquisitionDate` and `createdAt` still works because Task 1's Option B drops them from the SELECT projection but the test mock can keep them in the return value — extra fields are harmless). Change the assertion section: replace the current `expect(mockComputeVerdictBundle).not.toHaveBeenCalled()` + `expect(rendered).toMatch(/self-via-cross-user/)` with two assertions: (a) `await expect(CatalogPage({ params: Promise.resolve({ catalogId: validCatalogId }) })).rejects.toThrow('NEXT_REDIRECT')` (the page promise rejects because `redirect()` throws), and (b) `expect(mockRedirect).toHaveBeenCalledWith('/watch/mine-1')` (the mock `mockDbLimit` returns `id: 'mine-1'`, so the redirect target is `/watch/mine-1`). Also keep `expect(mockComputeVerdictBundle).not.toHaveBeenCalled()` — the redirect throws before the cross-user verdict path runs.

  - **Existing test at lines 191-203** (`'callout link points to /watch/{viewer.watches.id} — per-user UUID, not catalog UUID'`): RENAME the test description string to `'ARCH-02 — redirect target is /watch/{viewer.watches.id} (per-user UUID), not /watch/{catalogId}'`. Rewrite the body to assert the redirect URL matches the per-user UUID and is NOT the catalog UUID. Setup unchanged (`mockDbLimit.mockResolvedValue([{ id: 'per-user-uuid-abc', ... }])`). Replace the assertions: change to `await expect(CatalogPage(...)).rejects.toThrow('NEXT_REDIRECT')`, then `expect(mockRedirect).toHaveBeenCalledWith('/watch/per-user-uuid-abc')`, then `expect(mockRedirect).not.toHaveBeenCalledWith(\`/watch/${validCatalogId}\`)` (negative — never the catalog UUID).

  - The two `D-05 — renders 3 CTAs ...` tests at lines 210-237 STAY UNCHANGED — they cover non-owned framing paths.
  - The three `BUG-01 — wishlist|grail|sold` tests at lines 268-293 STAY UNCHANGED — they cover the not-owned `mockDbLimit.mockResolvedValue([])` path, which post-Task-1 still falls through to the cross-user verdict branch (no redirect). Their assertions (`expect(mockComputeVerdictBundle).toHaveBeenCalledTimes(1)` + `framing === 'cross-user'`) continue to hold.
  - The WR-01 predicate-AST guard test at lines 305-338 STAYS UNCHANGED — Task 1's Option B preserves `eq(watchesTable.status, 'owned')`, so the predicate AST still contains `'owned'`.
  - The `D-39b-04 — DOES render CTAs when collection is empty (NSV-20)` test at line 239 STAYS UNCHANGED.
  - The `D-07` test at line 167 STAYS UNCHANGED.

(4) **Add SC#5 static assertion test — append inside the `describe` block.** Copy the file-read pattern from `tests/app/insights-retirement.test.tsx:47-52`. Add a new `it()` block: `it('SC#5 — TODO comment for v7.0 Variant C is present at the OtherOwnersRoster anchor', async () => { const fs = await import('node:fs'); const content = fs.readFileSync('src/app/catalog/[catalogId]/page.tsx', 'utf8'); expect(content).toContain('revisit for Variant C in v7.0') })`. Place this test near the end of the existing `describe` block (after the WR-01 predicate guard test at line 338, before the closing `})` at line 339). The grep substring `revisit for Variant C in v7.0` MUST match the comment text Task 1 inserted.

Do NOT add Playwright. Do NOT add new test framework dependencies. The file changes are purely additive to existing vitest infrastructure.
  </action>
  <acceptance_criteria>
    - `grep -c "mockRedirect" tests/app/catalog-page.test.ts` returns at least `4` (one in vi.hoisted declaration list, one in vi.hoisted factory body, one in vi.mock('next/navigation', ...), and at least one in each rewritten ARCH-02 test — minimum 4)
    - `grep -c "NEXT_REDIRECT" tests/app/catalog-page.test.ts` returns at least `3` (digest string in the hoisted factory, plus assertion in each ARCH-02 redirect test)
    - `grep -c "ARCH-02 — redirects to /watch" tests/app/catalog-page.test.ts` returns `1`
    - `grep -c "ARCH-02 — redirect target is /watch" tests/app/catalog-page.test.ts` returns `1`
    - `grep -c "SC#5 — TODO comment for v7.0 Variant C" tests/app/catalog-page.test.ts` returns `1`
    - `grep -c "revisit for Variant C in v7.0" tests/app/catalog-page.test.ts` returns at least `1` (in the SC#5 assertion)
    - `grep -c "self-via-cross-user" tests/app/catalog-page.test.ts` returns `0` (D-08 framing literal is gone from tests)
    - `grep -c "You own this watch" tests/app/catalog-page.test.ts` returns at most `1` (allowable in the BUG-01 test descriptions which read "...does NOT trigger 'You own this watch' callout"; this is a description string, not behavior)
    - The three BUG-01 tests (wishlist/grail/sold) are PRESENT and UNCHANGED: `grep -c "BUG-01 — wishlist watch does NOT trigger" tests/app/catalog-page.test.ts` returns `1`; same for grail and sold
    - WR-01 predicate-AST guard test is PRESENT and UNCHANGED: `grep -c "BUG-01 — query is scoped to status='owned' (predicate guard)" tests/app/catalog-page.test.ts` returns `1`
    - `npx vitest run tests/app/catalog-page.test.ts` exits 0 with at least 13 tests passing (the original 12 + 1 new SC#5 assertion; the two D-08 tests are RENAMED+REWRITTEN not added, so net count is +1)
    - `npx vitest run tests/app/catalog-page.test.ts -t "ARCH-02"` exits 0 with at least 2 tests passing
    - `npx vitest run tests/app/catalog-page.test.ts -t "BUG-01"` exits 0 with at least 4 tests passing (3 status-row tests + 1 predicate guard)
    - `npx vitest run tests/app/catalog-page.test.ts -t "predicate guard"` exits 0 with 1 test passing (WR-01 invariant preserved)
    - `npm run lint -- tests/app/catalog-page.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/app/catalog-page.test.ts && npm run lint -- tests/app/catalog-page.test.ts</automated>
  </verify>
  <done>
    `tests/app/catalog-page.test.ts` has the `mockRedirect` augmentation in the `vi.hoisted` block and the `vi.mock('next/navigation', ...)` factory. The two D-08 callout tests are renamed and rewritten as ARCH-02 redirect-assertion tests. The three BUG-01 status-row tests and the WR-01 predicate-AST guard test are UNCHANGED. A new SC#5 static assertion test grep-checks the page file for `'revisit for Variant C in v7.0'`. The full file runs green; ARCH-02 + BUG-01 + predicate-guard subsets all pass; lint is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/catalog/[catalogId]` | Untrusted catalogId param crosses here (already UUID-regex gated at page.tsx:52-54) |
| Page → DB (Drizzle SELECT inside `findViewerWatchByCatalogId`) | Untrusted user.id + catalogId crosses here (already AND-scoped + parameterized via Drizzle) |
| Page → `next/navigation` redirect() | Trusted internal call; URL constructed from DB-read UUID, not from request input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.1-01 | Tampering / Information Disclosure | redirect() target URL | mitigate | `redirect(\`/watch/${viewerOwnedRow.id}\`)` interpolates a DB-fetched UUID (Drizzle column `watchesTable.id`, type uuid) — never user-controlled input. Open-redirect class is structurally impossible because the path prefix `/watch/` is a static template literal and the UUID slot is type-constrained at the DB layer. |
| T-50.1-02 | Information Disclosure (cross-user leak) | findViewerWatchByCatalogId query scope | mitigate | The Drizzle predicate is `and(eq(watchesTable.userId, userId), eq(watchesTable.catalogId, catalogId), eq(watchesTable.status, 'owned'))` — `userId` is from `await getCurrentUser()` (authenticated viewer); `catalogId` is from the URL param (UUID-regex gated). Cannot return another user's `watches.id` even if catalogIds collide across users. T-20-06-01 invariant from Phase 20 is preserved by Option B (Task 1 keeps all three predicates). |
| T-50.1-03 | Denial of Service / Cache Poisoning | Router Cache regression risk if redirect later moves to proxy.ts | mitigate (regression guard in Plan 02) | The redirect lives at the page layer per memory `feedback_proxy_router_cache_poisoning`. Variant B is structurally safe at the page layer. Plan 02 adds an automated test that grep-asserts `src/proxy.ts` contains no `/catalog/` redirect logic — prevents a future refactor from regressing to a proxy-layer redirect that would poison the Router Cache. |

All threats: severity LOW (T-50.1-01, T-50.1-02) or MEDIUM-mitigated (T-50.1-03). No `[BLOCKING]` threats. `threat_model_complete: true`.
</threat_model>

<verification>
- All acceptance criteria for Task 1 + Task 2 pass.
- `npx vitest run tests/app/catalog-page.test.ts` exits 0 with ≥ 13 tests passing.
- `npm run lint` exits 0.
- `npx tsc --noEmit` exits 0.
- Spot grep: `grep -E "framing.*'self-via-cross-user'" src/app/catalog/[catalogId]/page.tsx` returns no output (the framing literal is fully removed from the page).
- BUG-01 invariant: `eq(watchesTable.status, 'owned')` predicate remains in the file (Option B preserved this — required by SC#3/SC#6).
</verification>

<success_criteria>
- SC#1 satisfied: page imports `redirect`, calls `redirect(\`/watch/${viewerOwnedRow.id}\`)` immediately after the conditional, and the inline `VerdictBundleSelfOwned` construction at the original lines 107-115 is deleted (grep returns 0 for `self-via-cross-user`, `VerdictBundleSelfOwned`, `ownedAtIso`, `ownerHref` in the page).
- SC#2 satisfied via RESEARCH §2 Option B: `findViewerWatchByCatalogId` keeps the `status='owned'` filter (required by SC#3+SC#6 — the misleading SC#2 wording yields to the binding constraints per RESEARCH §10 Q1) AND drops the now-unused `acquisitionDate` + `createdAt` SELECT fields, simplifying the return type to `{ id: string } | null`.
- SC#3 satisfied: the rewritten ARCH-02 tests assert redirect-on-owned; the three existing BUG-01 tests assert no-redirect-on-wishlist/grail/sold (kept verbatim); all run under `npx vitest run`.
- SC#5 satisfied: `TODO: revisit for Variant C in v7.0` comment present above `<OtherOwnersRoster>`; a dedicated static test asserts the comment is present in the file.
- SC#6 satisfied: the 12 existing tests in catalog-page.test.ts still pass (D-07, NSV-20, BUG-01×3, WR-01 predicate guard, D-05 CTAs, etc.); the two rewritten ARCH-02 tests replace the two former D-08 callout tests; full file count is 13.
</success_criteria>

<output>
After completion, create `.planning/phases/50.1-url-canonicalization/50.1-01-SUMMARY.md` documenting: (a) Option B decision rationale (RESEARCH §10 Q1), (b) the JSDoc update, (c) the SC#5 TODO comment placement, (d) the test rewrites and the new SC#5 static assertion, (e) BUG-01 invariant preservation verified by passing WR-01 predicate-AST guard test.
</output>
