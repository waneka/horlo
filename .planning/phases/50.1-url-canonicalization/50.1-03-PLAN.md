---
id: 50.1-03
phase: 50.1
plan_num: 3
plan: 03
name: REQUIREMENTS.md traceability — ARCH-02 In Progress → Complete
type: execute
wave: 2
depends_on: [50.1-01, 50.1-02]
autonomous: true
requirements: [ARCH-02]
files_modified:
  - .planning/REQUIREMENTS.md
threat_model_complete: true

must_haves:
  truths:
    - "REQUIREMENTS.md line 23 has its checkbox flipped from `[ ]` to `[x]` for ARCH-02 (SC#7 — Complete state at phase verify)"
    - "REQUIREMENTS.md line 64 traceability row for ARCH-02 changes from `Pending` to `Complete (2026-05-XX)` where the date is the actual phase-close date (SC#7)"
    - "ARCH-01 status remains `[x]` Complete and `Complete (2026-05-20)` in the traceability table — unchanged by this plan (RESEARCH §7: ARCH-01 is already marked Complete)"
    - "The `Last updated` footer line is refreshed with today's date and a one-line entry noting Phase 50.1 close + ARCH-02 marked Complete"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table reflecting ARCH-02 phase close"
      contains: "ARCH-02 | Phase 50.1 | Complete"
  key_links:
    - from: ".planning/REQUIREMENTS.md line 23 checkbox"
      to: "ARCH-02 status"
      via: "Markdown checkbox state flip"
      pattern: "- \\[x\\] \\*\\*ARCH-02\\*\\*"
    - from: ".planning/REQUIREMENTS.md line 64 traceability row"
      to: "ARCH-02 phase-close status"
      via: "Markdown table cell update"
      pattern: "\\| ARCH-02 \\| Phase 50.1 \\| Complete"
---

<objective>
Update `.planning/REQUIREMENTS.md` to mark ARCH-02 Complete after Phase 50.1 ships. This is the phase-close traceability step required by SC#7. Plan 03 lives in Wave 2 because the REQUIREMENTS.md "Complete" mark depends on Plan 01 + Plan 02 having shipped (and their verify gates having passed). Per RESEARCH §7, ARCH-01 is already marked `[x]` Complete — this plan touches only the ARCH-02 row.

Purpose: Close the SC#7 deliverable. The traceability table is the canonical project-level source of truth for which requirements are complete; until this row flips, the phase is not officially closed.

Output: Modified `.planning/REQUIREMENTS.md` with ARCH-02 checkbox flipped, traceability row updated to `Complete (YYYY-MM-DD)`, and footer `Last updated` line refreshed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50.1-url-canonicalization/50.1-RESEARCH.md
@.planning/phases/50.1-url-canonicalization/50.1-VALIDATION.md

# File being modified — executor MUST read in full before editing
@.planning/REQUIREMENTS.md

# Plan 01 + Plan 02 SUMMARYs — confirm both shipped before flipping the traceability row
# (Executor consults these when running this plan; they exist post Wave 1.)
@.planning/phases/50.1-url-canonicalization/50.1-01-SUMMARY.md
@.planning/phases/50.1-url-canonicalization/50.1-02-SUMMARY.md

<interfaces>
<!-- Current state of REQUIREMENTS.md per RESEARCH §7, line refs verified. -->

From `.planning/REQUIREMENTS.md:22-23` (Architecture block):
```markdown
- [x] **ARCH-01**: A spike compares ... Complete
- [ ] **ARCH-02**: URL canonicalization — when the viewer owns a catalog ref ... Source: .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md §8 (Primary recommendation: Variant B) + §9 (Ship-Now: YES).
```

From `.planning/REQUIREMENTS.md:57-64` (Traceability table):
```markdown
| Requirement | Phase | Status |
|-------------|-------|--------|
| BUG-01 | Phase 48 | Complete (2026-05-19) |
| BUG-02 | Phase 48 | Complete (2026-05-19) |
| TAX-01 | Phase 49 | Complete (2026-05-19) |
| TAX-02 | Phase 49.1 | Complete (2026-05-20) |
| ARCH-01 | Phase 50 | Complete (2026-05-20) |
| ARCH-02 | Phase 50.1 | Pending |
```

From `.planning/REQUIREMENTS.md:73` (footer):
```markdown
*Last updated: 2026-05-20 — ARCH-02 added mid-milestone per Phase 50 spike Ship-Now verdict ...*
```

The precedent for this update flow is the Phase 49.1 close (TAX-02 went from `[ ]` → `[x]` and `In Progress` → `Complete (2026-05-20)` in the same edit pattern).
</interfaces>
</context>

<tasks>

<task type="auto">
  <id>50.1-03-01</id>
  <name>Task 1: Update REQUIREMENTS.md — ARCH-02 checkbox `[ ]` → `[x]`, traceability `Pending` → `Complete (YYYY-MM-DD)`, footer refresh</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (READ IN FULL — 73 lines; you will edit lines 23, 64, and 73)
    - `.planning/phases/50.1-url-canonicalization/50.1-RESEARCH.md` §7 (the exact lines to update + the Phase 49.1 precedent + the wording for the footer line)
    - `.planning/phases/50.1-url-canonicalization/50.1-01-SUMMARY.md` and `.planning/phases/50.1-url-canonicalization/50.1-02-SUMMARY.md` (confirm Plan 01 + Plan 02 both shipped before flipping the traceability row — if either summary file does not exist or its outcome was not green, ABORT and request orchestrator guidance)
  </read_first>
  <action>
Apply three concrete edits to `.planning/REQUIREMENTS.md`. The current YYYY-MM-DD date is the phase-close date — use the actual date the executor runs this task (e.g., `2026-05-20` if same day, or whatever the calendar date is when Plan 01 + Plan 02 SUMMARYs are confirmed green).

(1) **Line 23 — checkbox flip.** Find the line that currently reads `- [ ] **ARCH-02**: URL canonicalization — when the viewer owns a catalog ref ...`. Change ONLY the checkbox state: `- [ ]` → `- [x]`. Do NOT modify the rest of the line — the description, source citation, and surrounding context all stay verbatim. The replacement is a single-character edit inside the brackets.

(2) **Line 64 — traceability table row.** Find the row that currently reads `| ARCH-02 | Phase 50.1 | Pending |`. Change ONLY the third cell: `Pending` → `Complete (YYYY-MM-DD)` where YYYY-MM-DD is the phase-close date. Match the formatting of the surrounding rows (e.g., `Complete (2026-05-20)` — same parenthesized date style used for ARCH-01 on the row above). Do NOT change the first two cells (`ARCH-02` and `Phase 50.1`).

(3) **Line 73 — footer `Last updated` refresh.** Replace the current `*Last updated: ...*` line with: `*Last updated: YYYY-MM-DD — Phase 50.1 closed; ARCH-02 marked Complete (page-layer redirect Variant B shipped; Phase 48 BUG-01 maintenance tax retired). ARCH-01 unchanged.*`. Use the same phase-close date as edits (1) and (2). The trailing italics `*` markers stay.

Do NOT modify lines 1-22, 24-63, 65-72, or any other line of the file. Do NOT alter the coverage summary on lines 66-69 ("v5.2 requirements: 6 total ...") — the count of 6 total is unchanged by this phase (ARCH-02 was added at phase-open, not phase-close).

Pre-flight checks before editing:
  - Confirm `.planning/phases/50.1-url-canonicalization/50.1-01-SUMMARY.md` exists and reports a green outcome (Task 1 + Task 2 of Plan 01 both passed).
  - Confirm `.planning/phases/50.1-url-canonicalization/50.1-02-SUMMARY.md` exists and reports a green outcome (Task 1 of Plan 02 passed; existing 8 proxy tests + 1 new test all green).
  - If either pre-flight check fails, ABORT this plan. The REQUIREMENTS.md flip is the LAST artifact written in the phase — it must not race ahead of green tests.
  </action>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*ARCH-02\*\*" .planning/REQUIREMENTS.md` returns `1` (checkbox flipped)
    - `grep -c "^- \[ \] \*\*ARCH-02\*\*" .planning/REQUIREMENTS.md` returns `0` (no remaining unchecked ARCH-02)
    - `grep -E "^\| ARCH-02 \| Phase 50.1 \| Complete \(20" .planning/REQUIREMENTS.md` returns 1 line (table row updated with `Complete (YYYY-MM-DD)` shape)
    - `grep -c "^| ARCH-02 | Phase 50.1 | Pending |" .planning/REQUIREMENTS.md` returns `0` (no remaining `Pending` for ARCH-02)
    - `grep -c "^- \[x\] \*\*ARCH-01\*\*" .planning/REQUIREMENTS.md` returns `1` (ARCH-01 unchanged — still `[x]`)
    - `grep -c "^| ARCH-01 | Phase 50 | Complete (2026-05-20) |" .planning/REQUIREMENTS.md` returns `1` (ARCH-01 row unchanged)
    - `grep -c "Phase 50.1 closed" .planning/REQUIREMENTS.md` returns `1` (footer refreshed)
    - The coverage line `v5.2 requirements: 6 total` is still present unchanged: `grep -c "v5.2 requirements: 6 total" .planning/REQUIREMENTS.md` returns `1`
    - `wc -l .planning/REQUIREMENTS.md` returns `73` (no lines added or removed — only character-level edits)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^- \[x\] \*\*ARCH-02\*\*" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -E "^\| ARCH-02 \| Phase 50.1 \| Complete \(20" .planning/REQUIREMENTS.md | wc -l | grep -q "^[[:space:]]*1$" && grep -c "Phase 50.1 closed" .planning/REQUIREMENTS.md | grep -q "^1$"</automated>
  </verify>
  <done>
    `.planning/REQUIREMENTS.md` line 23 has `- [x] **ARCH-02**`. Line 64 reads `| ARCH-02 | Phase 50.1 | Complete (YYYY-MM-DD) |`. Footer line 73 is refreshed with the Phase 50.1 close note. ARCH-01's row and checkbox are untouched. File length unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan is a project-tracking document edit; no runtime trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.1-04 | Tampering (process integrity) | Premature traceability flip (marking ARCH-02 Complete before Plan 01 + Plan 02 verify) | mitigate | The task's `<action>` block requires pre-flight checks: both Plan 01 and Plan 02 SUMMARYs must exist and be green BEFORE the executor edits REQUIREMENTS.md. Plan 03's `depends_on: [50.1-01, 50.1-02]` field and `wave: 2` placement enforce this at the workflow level. |

All threats: severity LOW. No `[BLOCKING]` threats. `threat_model_complete: true`.
</threat_model>

<verification>
- All acceptance criteria for Task 1 pass.
- Manual visual confirmation: read REQUIREMENTS.md and confirm the ARCH-02 row reads "Complete (YYYY-MM-DD)" matching the actual phase-close date.
</verification>

<success_criteria>
- SC#7 satisfied: ARCH-01 is marked Complete (already was, per RESEARCH §7) and ARCH-02 is updated from `Pending` to `Complete (YYYY-MM-DD)` after this phase verifies. The checkbox on line 23 is flipped to `[x]`. The traceability table on line 64 reflects the close. The footer's `Last updated` line documents the Phase 50.1 close.
</success_criteria>

<output>
After completion, create `.planning/phases/50.1-url-canonicalization/50.1-03-SUMMARY.md` documenting: (a) ARCH-02 marked Complete with the actual phase-close date, (b) the three lines edited (23, 64, 73), (c) pre-flight verification that Plan 01 + Plan 02 both shipped green before the flip, (d) ARCH-01 row confirmed unchanged.
</output>
