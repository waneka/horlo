---
phase: 31-v4-0-verification-backfill
plan: 03
type: execute
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - .planning/milestones/v4.0-MILESTONE-AUDIT.md
autonomous: true
requirements: [DEBT-07, DEBT-08]
tags: [audit, closure, documentation, v4.0-backfill]

must_haves:
  truths:
    - "Pre-existing `v4.0-MILESTONE-AUDIT.md` body (frontmatter lines 1-52, executive summary, per-phase status table, requirements coverage, cross-phase integration, Nyquist compliance, recommended remediation, decision section, original footer line 207) is BYTE-EQUAL pre/post Phase 31. Verified by `git diff 9d87293^..HEAD -- .planning/milestones/v4.0-MILESTONE-AUDIT.md` showing ZERO deletions in lines 1-207 (the lines that exist before Phase 31's append)."
    - "A new `## Closure (<actual date> — Phase 31 v4.0 Verification Backfill)` section is appended at the bottom of the file (D-08)."
    - "Closure section cites BOTH new file paths verbatim: `.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md` and `.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md`."
    - "Closure section quotes BOTH new file scores verbatim: Phase 23 `4/5 success criteria VERIFIED + 1 GAP (FEAT-07 server-action regression)`; Phase 24 `5/5 success criteria PASS`."
    - "Closure section explicitly notes the FEAT-07 server-action regression surfaced during Phase 31 audit (per D-discretion + RESEARCH §Critical regression Option A) and references the new follow-up tracking item (proposed DEBT-09)."
    - "Closure section ends with the asymmetry-resolved line: 'v4.0 audit asymmetry resolved. Remaining tech_debt items (23-05 SUMMARY, VALIDATION frontmatter, Phase 999.1 archival, traceability table staleness, ~33 human UAT) remain as documented in the original tech_debt block above and are out of Phase 31 scope.' (verbatim per D-08)."
    - "Audit frontmatter `phases: 10/12 fully verified, 2/12 partial verification` line on line 7 is UNCHANGED (D-10 — that statement was true at audit time)."
    - "Audit `tech_debt:` block (lines 14-46) is UNCHANGED (D-09)."
    - "Audit per-phase status table (lines 79-91) is UNCHANGED (D-09)."
    - "BOTH archive paths cited by the Closure exist (verified by Plans 31-01 + 31-02 deliverables) BEFORE this Closure is written — closure cites only paths that exist."
    - "Nyquist Dimension 8 N/A — documentation phase per VALIDATION.md §Out of Nyquist Scope."
  artifacts:
    - path: ".planning/milestones/v4.0-MILESTONE-AUDIT.md"
      provides: "Closed v4.0 milestone audit with append-only Closure section"
      contains: "all original content (byte-equal in lines 1-207) + new `## Closure (<date> — Phase 31 v4.0 Verification Backfill)` section appended at bottom"
      preserves: "frontmatter, executive summary, per-phase status table, requirements coverage table, cross-phase integration, Nyquist compliance table, recommended remediation, decision section, original footer line 207"
  key_links:
    - from: ".planning/milestones/v4.0-MILESTONE-AUDIT.md ## Closure section"
      to: ".planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md"
      via: "Markdown link / verbatim path mention"
      pattern: "v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md"
    - from: ".planning/milestones/v4.0-MILESTONE-AUDIT.md ## Closure section"
      to: ".planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md"
      via: "Markdown link / verbatim path mention"
      pattern: "v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md"
---

<objective>
Append a `## Closure` section to the bottom of `.planning/milestones/v4.0-MILESTONE-AUDIT.md` recording that the v4.0 verification asymmetry is resolved by Phase 31. The pre-existing audit body MUST remain byte-equal — the audit is a snapshot taken at v4.0 close and rewriting it loses signal (D-09/D-10).

Purpose:
- Close the audit's recommended remediation item #3 (audit line 178: "Run goal-backward verifier on Phase 23 + Phase 24") by recording the closure inline.
- Provide bidirectional linkage: Plans 31-01/02 produced VERIFICATION.md files that reference the audit; this plan ensures the audit references them back.
- Surface the FEAT-07 server-action regression as a new follow-up item (RESEARCH §Critical regression Option A) without modifying the original tech_debt block.

Output: One edited file: `.planning/milestones/v4.0-MILESTONE-AUDIT.md` with the new `## Closure` section appended after line 207.

This plan runs in Wave 2 because it requires the artifacts produced by Plans 31-01 and 31-02 to exist (and have known scores) before citing them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-v4-0-verification-backfill/31-CONTEXT.md
@.planning/phases/31-v4-0-verification-backfill/31-RESEARCH.md
@.planning/phases/31-v4-0-verification-backfill/31-VALIDATION.md
@.planning/milestones/v4.0-MILESTONE-AUDIT.md

<wave_1_outputs>
<!-- These files MUST exist before this plan runs (Wave 2 dependency on Plans 31-01 + 31-02). -->
.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md
.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md

<!-- Read both files BEFORE writing the Closure to extract the actual `score:` strings to quote verbatim. -->
</wave_1_outputs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Closure section to v4.0-MILESTONE-AUDIT.md</name>
  <read_first>
    - `.planning/phases/31-v4-0-verification-backfill/31-CONTEXT.md` (decisions D-08, D-09, D-10; specifics; deferred ideas)
    - `.planning/phases/31-v4-0-verification-backfill/31-RESEARCH.md` §"Code Examples" → "Append-only Closure section (D-08 pattern)" lines 593-608 (the proposed Closure prose template; adapt — do not copy literally)
    - `.planning/milestones/v4.0-MILESTONE-AUDIT.md` (FULL FILE — confirm current line count; identify the exact insertion point at the very end after `_Audited 2026-05-02T23:30:00Z by /gsd-audit-milestone v4.0_`)
    - `.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md` (Plan 31-01 output — extract the `score:` frontmatter value and `verified:` ISO timestamp to quote verbatim in the Closure)
    - `.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md` (Plan 31-02 output — extract the `score:` frontmatter value to quote verbatim)
  </read_first>
  <action>
    Append the Closure section. This is an Edit operation — NEVER overwrite the file.

    **Step 1 — pre-flight: confirm Wave 1 artifacts exist:**
    ```bash
    test -f .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md
    test -f .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md
    ```
    Both must exist (created by Plans 31-01 + 31-02). If either is missing, abort and surface to operator.

    **Step 2 — extract the actual scores to quote in the Closure:**
    ```bash
    grep -E '^score:' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md
    grep -E '^score:' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md
    grep -E '^verified:' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md
    ```
    Capture the exact strings. Use the verified date as the Closure date (or `date -u +%Y-%m-%d` if simultaneous).

    **Step 3 — capture audit byte-state for verification afterward:**
    ```bash
    wc -l .planning/milestones/v4.0-MILESTONE-AUDIT.md   # current line count (expected: ~207)
    git log --oneline -1 -- .planning/milestones/v4.0-MILESTONE-AUDIT.md   # last commit touching the file
    git diff HEAD -- .planning/milestones/v4.0-MILESTONE-AUDIT.md   # confirm clean working copy before edit
    ```

    **Step 4 — append the Closure section** using the Edit tool's "old_string → new_string" pattern, anchored on the existing footer line `_Audited 2026-05-02T23:30:00Z by `/gsd-audit-milestone v4.0`_` (line ~207).

    Use Edit with:
    - `old_string`: `_Audited 2026-05-02T23:30:00Z by `/gsd-audit-milestone v4.0`_` (the existing terminal line — verify its exact content first via `tail -5` of the file)
    - `new_string`: The same line, followed by the new Closure section.

    The new content to append (everything AFTER the existing terminal line) should be:

    ```markdown
    _Audited 2026-05-02T23:30:00Z by `/gsd-audit-milestone v4.0`_

    ---

    ## Closure (<YYYY-MM-DD> — Phase 31 v4.0 Verification Backfill)

    The v4.0 verification asymmetry recorded in this audit (`tech_debt` block lines 17-22 for Phase 23 + lines 21-24 for Phase 24, plus per-phase status table lines 88-89) is now closed. Phase 31 produced two phase-level verification artifacts using the canonical Phase 22-VERIFICATION.md goal-backward shape:

    - [`.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md`](v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md) — score: `<verbatim score from Plan 31-01 output>`; status: `human_needed` (5 UAT items pending per audit line 19, carried into the new artifact's frontmatter `human_verification` array). **Phase 31 audit surfaced an FEAT-07 server-action regression** — commit `4d362ff` cited at audit line 111 + v4.0-REQUIREMENTS.md line 93 is NOT an ancestor of HEAD; `src/app/actions/watches.ts` is missing the `notesPublic` Zod field and the `revalidatePath('/u/[username]', 'layout')` call; `tests/actions/watches.notesPublic.test.ts` 0/4 PASS on current main. The regression is documented as a Gap row in the new 23-VERIFICATION.md and recommended for tracking as a new follow-up item (proposed `DEBT-09`) in v4.1 close, v5.0 onboarding work, or a dedicated quick-task. **Phase 31 did NOT remediate inline** — audit-only scope per CONTEXT D-Out-of-scope.
    - [`.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md`](v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md) — score: `<verbatim score from Plan 31-02 output>`; status: `passed`. All 5 success criteria + 7 REQ-IDs (DEBT-03/04/05/06 + TEST-04/05/06) VERIFIED on current main; 51 tests across 5 files PASS; no human UAT pending.

    v4.0 audit asymmetry resolved. Remaining tech_debt items (23-05 SUMMARY, VALIDATION frontmatter, Phase 999.1 archival, traceability table staleness, ~33 human UAT) remain as documented in the original tech_debt block above and are out of Phase 31 scope.

    Per D-09 / D-10, the original audit `tech_debt:` frontmatter, per-phase status table, and `phases: 10/12 fully verified, 2/12 partial verification` line are intentionally UNCHANGED — they are a frozen snapshot of state at audit time (2026-05-02). This Closure section is the chronological postscript that proves the audit acted on its own findings.

    _Closed: <YYYY-MM-DD>_
    _Closure plan: 31-03_
    _Closure phase: 31-v4-0-verification-backfill_
    ```

    Replace `<YYYY-MM-DD>` with the actual date (e.g., `2026-05-05` — capture from `date -u +%Y-%m-%d`).
    Replace `<verbatim score from Plan 31-01 output>` with the literal score string from `grep -E '^score:' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md` (e.g., `"4/5 success criteria VERIFIED + 1 GAP (FEAT-07 server-action regression)"`).
    Replace `<verbatim score from Plan 31-02 output>` with the literal score string from the corresponding Plan 31-02 grep (e.g., `"5/5 success criteria PASS"`).

    **Critical writing rules:**
    - Use the Edit tool. NEVER use Write (which would overwrite the entire file). NEVER use heredoc.
    - The Edit's `old_string` must include enough preceding context to be unique — anchor on the full footer line `_Audited 2026-05-02T23:30:00Z by `/gsd-audit-milestone v4.0`_`. If a unique-match check fails, expand the `old_string` to include 2-3 preceding lines.
    - The `old_string` line itself MUST appear in the `new_string` UNCHANGED (you are appending AFTER it, not replacing it).
    - Quote BOTH scores verbatim. Do NOT paraphrase. Do NOT change Phase 31-01's `4/5 success criteria VERIFIED + 1 GAP (FEAT-07 server-action regression)` to `4/5 + 1 GAP` or any abbreviated form.
    - The asymmetry-resolved sentence must appear verbatim per D-08.
    - Do NOT modify any line above the existing footer (D-09 / D-10 — frontmatter, tech_debt block, per-phase table, etc. all stay byte-equal).
    - Do NOT change the audit's frontmatter `status: tech_debt` to `closed` or `passed` — the snapshot stays as-was per D-09/D-10. The Closure section is the chronological postscript, not an in-place rewrite.
  </action>
  <verify>
    <automated>
test -f .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && grep -c '^## Closure' .planning/milestones/v4.0-MILESTONE-AUDIT.md | grep -qE '^1$' \
  && grep -qE '^## Closure \(20[0-9]{2}-[0-9]{2}-[0-9]{2} . Phase 31 v4.0 Verification Backfill\)' .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && grep -qE 'v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md' .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && grep -qE 'v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md' .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && grep -qE '4/5 success criteria VERIFIED' .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && grep -qE '5/5 success criteria PASS' .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && grep -qE 'FEAT-07' .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && grep -qE 'DEBT-09' .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && grep -qE 'v4\.0 audit asymmetry resolved' .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && grep -qE 'phases: 10/12 fully verified, 2/12 partial verification' .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && grep -qE '^status: tech_debt$' .planning/milestones/v4.0-MILESTONE-AUDIT.md \
  && git diff -- .planning/milestones/v4.0-MILESTONE-AUDIT.md | grep -E '^-[^-]' | grep -v -E '^--- ' | wc -l | grep -qE '^[[:space:]]*0$'
    </automated>

    The final check (zero deletion lines in `git diff`) is the strongest invariant: it proves byte-equality of the pre-existing body. Read it as: "every line in the diff that starts with `-` (deletion) and is not the diff header must total zero." If any pre-existing audit line was modified, this check fails.

    Manual cross-checks:
    - Run `git diff -- .planning/milestones/v4.0-MILESTONE-AUDIT.md` and visually confirm only ADDITIONS appear (lines prefixed with `+`).
    - Run `git diff --stat -- .planning/milestones/v4.0-MILESTONE-AUDIT.md` and confirm the line-count delta matches the expected ~15-20 added lines.
    - Run `tail -30 .planning/milestones/v4.0-MILESTONE-AUDIT.md` and confirm the new Closure section reads naturally after the original footer.
  </verify>
  <acceptance_criteria>
    - File `.planning/milestones/v4.0-MILESTONE-AUDIT.md` is modified by exactly one Edit operation (append after the existing footer line at line 207)
    - `grep -c '^## Closure' .planning/milestones/v4.0-MILESTONE-AUDIT.md` returns exactly 1
    - `## Closure (<YYYY-MM-DD> — Phase 31 v4.0 Verification Backfill)` heading present (real date, not literal `<YYYY-MM-DD>`)
    - Both new VERIFICATION.md paths cited verbatim in the Closure section
    - Both scores cited verbatim (matches `grep -E '^score:' <new file>` output exactly)
    - "v4.0 audit asymmetry resolved" sentence present verbatim
    - "phases: 10/12 fully verified, 2/12 partial verification" frontmatter line still present (unchanged per D-10)
    - Audit `tech_debt:` block (lines 14-46) unchanged per D-09 (verifiable via `git diff` showing only additions, no deletions)
    - Audit per-phase status table (lines 79-91) unchanged per D-09
    - Audit frontmatter `status: tech_debt` unchanged (Closure is postscript, not in-place rewrite)
    - Closure section explicitly mentions FEAT-07 regression and proposed DEBT-09 follow-up
    - `git diff` shows ZERO deletion lines on pre-existing content (only additions; only `+` prefixed lines, no `-` prefixed lines outside the diff header)
  </acceptance_criteria>
  <done>
    Closure section appended to bottom of v4.0-MILESTONE-AUDIT.md with both new VERIFICATION.md paths cited, both scores quoted verbatim, FEAT-07 regression + DEBT-09 follow-up surfaced, asymmetry-resolved line present, original audit body byte-equal (no deletions in git diff). The audit's frozen snapshot of v4.0-close state is preserved; the Closure is the chronological postscript proving Phase 31 acted on the audit's own remediation recommendation #3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | No production code changes; documentation-only audit closure. No new authn / authz / data flow surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | v4.0-MILESTONE-AUDIT.md pre-existing body | mitigate | Use Edit tool with anchored `old_string` ending at the existing footer line; the strongest verification gate is `git diff` showing zero deletion lines (`^-` lines outside diff header). D-09 / D-10 explicitly forbid in-place edits to the frontmatter, tech_debt block, or per-phase table. |

No new attack surface. Asset list: Markdown audit artifact in `.planning/milestones/`. No secrets, no PII, no executable code.
</threat_model>

<verification>
After Task 1 completes, all `<verify><automated>` checks pass, and the audit's pre-existing body is byte-equal to its pre-Phase-31 state.

End-to-end Phase 31 invariant (validated by `/gsd-verify-work 31`):
1. `.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md` exists (Plan 31-01 ✓)
2. `.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md` exists (Plan 31-02 ✓)
3. `.planning/milestones/v4.0-MILESTONE-AUDIT.md` has a `## Closure` section appended (Plan 31-03 ✓)
4. `git diff 9d87293^..HEAD -- .planning/milestones/v4.0-MILESTONE-AUDIT.md` shows append-only diff (Plan 31-03 ✓)

DEBT-07 satisfied by Plans 31-01 (artifact creation) + 31-03 (audit closure linkage).
DEBT-08 satisfied by Plans 31-02 (artifact creation) + 31-03 (audit closure linkage).
</verification>

<success_criteria>
- All `<acceptance_criteria>` bullets in Task 1 are met
- `git diff -- .planning/milestones/v4.0-MILESTONE-AUDIT.md` shows ONLY additions (no `-` prefixed lines outside diff headers)
- The new Closure section provides the bidirectional link: audit → new VERIFICATION.md files; new VERIFICATION.md files (per Plans 31-01 + 31-02) → audit lines 17-22 + 21-24
- v4.0 audit asymmetry recorded by `audit:` frontmatter line 7 (`phases: 10/12 fully verified, 2/12 partial verification`) is now closed at the artifact level — both Phase 23 and Phase 24 have phase-level VERIFICATION.md files (audit frontmatter line stays unchanged per D-10; Closure section is the chronological postscript)
</success_criteria>

<output>
After completion, create `.planning/phases/31-v4-0-verification-backfill/31-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. The SUMMARY.md should record:
- The exact date used in the Closure section heading
- The exact two scores quoted (verbatim from Plans 31-01 + 31-02 outputs)
- Confirmation that `git diff` showed zero deletion lines on pre-existing content
- Line-count delta from the Edit (e.g., "audit grew from 207 lines to ~225 lines, all additions in the new Closure section")
</output>
