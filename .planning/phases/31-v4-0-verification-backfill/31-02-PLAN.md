---
phase: 31-v4-0-verification-backfill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md
autonomous: true
requirements: [DEBT-08]
tags: [audit, verification, documentation, v4.0-backfill]

must_haves:
  truths:
    - "Archive directory `.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/` exists (D-01)."
    - "`24-VERIFICATION.md` lives at the D-01 path and is the ONLY file in the archive directory (D-02)."
    - "File has valid YAML frontmatter open+close (`^---` first line, `^---$` close)."
    - "Frontmatter `phase`, `verified` (real ISO timestamp, NOT `2026-05-XX`), `status: passed`, `score`, `overrides_applied: 0` all present."
    - "`human_verification` key is OMITTED entirely (D-12: Phase 24 has no pending UAT — empty array also acceptable per RESEARCH Assumption A3)."
    - "Body mirrors canonical Phase 22-VERIFICATION.md structure (D-05): `# Phase 24 ... Verification Report` H1; `## Goal Achievement` with `### Observable Truths` table; `### Required Artifacts`; `### Key Link Verification`; `### Behavioral Spot-Checks`; `### Requirements Coverage`; `### Anti-Patterns Found`; `### Gaps Summary`; footer."
    - "Observable Truths table has 5 rows (one per v4.0-ROADMAP.md Phase 24 success criterion lines 143-147): SC-1 (pre-flight assertion), SC-2 (enum rename+recreate), SC-3 (render branches deleted), SC-4 (wornPublic→wear_visibility fixture migration), SC-5 (TEST-04/05/06 carryover suites)."
    - "All 5 truths VERIFIED — no GAPs (per RESEARCH §Phase 24 Verdict: 5/5 success criteria + 7 REQ-IDs all PASS)."
    - "Score line reads `5/5 success criteria PASS` (per RESEARCH Pattern 13 recommendation for Phase 24)."
    - "SC-2 (DEBT-04) row cites `supabase/migrations/20260501000000_phase24_notification_enum_cleanup.sql` lines 36-55 (T-24-PARTIDX surgery), 62-76 (rename+recreate), 94-130 (post-migration assertion). Cross-references memory `project_drizzle_supabase_db_mismatch.md` for the enum-bound dependent surgery pattern."
    - "SC-5 (TEST-04/05/06) row cites: TEST-04 `tests/store/watchStore.test.ts` 7/7 PASS; TEST-05 `tests/api/extract-watch.test.ts` 16/16 PASS (NOT `tests/app/api/` — RESEARCH Pitfall 5 path correction); TEST-06 `tests/components/watch/WatchForm.test.tsx` 11/11 PASS, `tests/components/filters/FilterBar.test.tsx` 5/5 PASS, `tests/components/watch/WatchCard.test.tsx` 7/7 PASS — total 51 tests across 5 files."
    - "DEBT-06 (SC-4) row footnoted with v4.0-REQUIREMENTS.md line 123 reconciliation: '4 files modified per D-04 dead-test-deletion rule' (drift from '9 files updated' SUMMARY language). Verify on main with `grep -lrE 'wornPublic' tests/` returning ZERO matches and `grep -lrE 'wear_visibility' tests/` returning the 2 living references (`tests/integration/phase11-schema.test.ts`, `tests/data/getWearRailForViewer.test.ts`)."
    - "Drift footnotes applied (no dedicated `## Drift Since v4.0 Ship` subsection per D-04): TEST-06 row footnotes Phase 29-01 commit `9c2126f test(29-01): add FORM-04 reset-on-key-change test for WatchForm` (additive — extends WatchForm test file with one more test, does NOT remove any Phase 24 contract)."
    - "DEBT-05 row footnoted that `src/db/schema.ts` gained `watches.sort_order` column per Phase 27-02 commit `e4d6b78` (additive — does NOT touch Phase 24 enum cleanup)."
    - "`### Anti-Patterns Found` table present (Phase 22 + Phase 30 precedent — include even if empty / 'None found' row)."
    - "Nyquist Dimension 8 N/A — documentation phase per VALIDATION.md §Out of Nyquist Scope."
  artifacts:
    - path: ".planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md"
      provides: "Phase 24 phase-level goal-backward verification artifact (closes audit lines 21-24)"
      contains: "frontmatter with status: passed; 5-row Observable Truths table all VERIFIED; Required Artifacts table; Key Link Verification table; Behavioral Spot-Checks running 51 tests across 5 files; Requirements Coverage for 7 REQ-IDs (DEBT-03/04/05/06 + TEST-04/05/06); Gaps Summary stating no gaps"
      min_sections: 7
    - path: ".planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/"
      provides: "v4.0 phase archive directory (D-01)"
      contains_only: "24-VERIFICATION.md"
  key_links:
    - from: ".planning/milestones/v4.0-MILESTONE-AUDIT.md lines 21-24"
      to: ".planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md"
      via: "24-VERIFICATION.md opening paragraph or frontmatter cites the audit's tech_debt items 21-24"
      pattern: "v4.0-MILESTONE-AUDIT"
    - from: "24-VERIFICATION.md SC-2 (DEBT-04)"
      to: "supabase/migrations/20260501000000_phase24_notification_enum_cleanup.sql"
      via: "evidence cell quoting line ranges 36-55, 62-76, 94-130"
      pattern: "20260501000000_phase24_notification_enum_cleanup"
    - from: "24-VERIFICATION.md SC-5 (TEST-04/05/06)"
      to: "5 test files (watchStore, extract-watch, WatchForm, FilterBar, WatchCard)"
      via: "5 vitest run commands with pass counts"
      pattern: "vitest run tests"
---

<objective>
Write the phase-level goal-backward verification artifact for Phase 24 (Notification Stub Cleanup + Test Fixture & Carryover) and place it at the v4.0 milestone archive path. This is the second of two artifacts that close the v4.0 verification asymmetry recorded in `.planning/milestones/v4.0-MILESTONE-AUDIT.md`.

Purpose:
- Close audit gap "No phase-level 24-VERIFICATION.md. Goal-backward verifier audit never run." (audit line 22) and "24-VALIDATION.md frontmatter status: draft" (audit line 24).
- Produce evidence-cited verdicts for all 5 v4.0-ROADMAP.md Phase 24 success criteria + all 7 REQ-IDs (DEBT-03/04/05/06 + TEST-04/05/06).
- Per D-12, Phase 24 has no pending human-UAT items — frontmatter `status: passed`, `human_verification` omitted entirely (or empty array — RESEARCH Assumption A3).
- Per RESEARCH §Phase 24 Verdict, ALL 5 success criteria + 7 REQ-IDs PASS on current main. No GAPs.

Output: One Markdown file: `.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-v4-0-verification-backfill/31-CONTEXT.md
@.planning/phases/31-v4-0-verification-backfill/31-RESEARCH.md
@.planning/phases/31-v4-0-verification-backfill/31-VALIDATION.md
@.planning/milestones/v4.0-MILESTONE-AUDIT.md
@.planning/milestones/v4.0-ROADMAP.md
@.planning/milestones/v4.0-REQUIREMENTS.md
@.planning/phases/30-wywt-capture-alignment-fix/30-VERIFICATION.md

<historical_artifacts>
<!-- Read via `git show` only — do NOT copy into the archive dir per D-02 -->
<!-- Phase 22 canonical VERIFICATION.md template (D-05): -->
<!--   git show 2918e95:.planning/phases/22-settings-restructure-account-section/22-VERIFICATION.md -->
<!-- Phase 24 plan SUMMARYs (8 of them, all complete; reference for evidence framing): -->
<!--   git show 9d87293^:.planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-01-SUMMARY.md -->
<!--   ... 24-08-SUMMARY.md (read selectively if specific evidence needed) -->
</historical_artifacts>

<current_main_evidence_paths>
<!-- All paths verified by Phase 31 RESEARCH against current main. -->
src/db/schema.ts                                                <!-- enum narrowed to 2 values (lines 28-34) -->
src/components/notifications/NotificationRow.tsx                <!-- no removed-enum branches -->
supabase/migrations/20260501000000_phase24_notification_enum_cleanup.sql  <!-- 132 LOC -->
scripts/preflight-notification-types.ts                          <!-- standalone Layer 1 preflight -->
tests/setup.tsx                                                  <!-- PointerEvent polyfill (path is .tsx not .ts after Phase 29-06 rename) -->
tests/store/watchStore.test.ts                                   <!-- TEST-04: 7 tests -->
tests/api/extract-watch.test.ts                                  <!-- TEST-05: 16 tests; NOT tests/app/api/ -->
tests/components/watch/WatchForm.test.tsx                        <!-- TEST-06: 11 tests -->
tests/components/filters/FilterBar.test.tsx                      <!-- TEST-06: 5 tests -->
tests/components/watch/WatchCard.test.tsx                        <!-- TEST-06: 7 tests -->
tests/integration/phase11-schema.test.ts                         <!-- live wear_visibility reference (post-fixture migration) -->
tests/data/getWearRailForViewer.test.ts                          <!-- live wear_visibility reference -->
</current_main_evidence_paths>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create archive directory and write 24-VERIFICATION.md</name>
  <read_first>
    - `.planning/phases/31-v4-0-verification-backfill/31-CONTEXT.md` (decisions D-01 through D-12; specifics; deferred ideas)
    - `.planning/phases/31-v4-0-verification-backfill/31-RESEARCH.md` (full file — especially §"Architecture Patterns" Pattern 1-13, §"Phase 24 Evidence Catalogue" lines 451-493, §"Per-Criterion Evidence Map (Phase 24)" lines 509-517, §"Pitfall 5" path drift — `tests/api/extract-watch.test.ts` NOT `tests/app/api/`)
    - `.planning/phases/31-v4-0-verification-backfill/31-VALIDATION.md` (Nyquist Dimension 8 N/A note for must_haves.truths)
    - `.planning/milestones/v4.0-MILESTONE-AUDIT.md` lines 1-58 (frontmatter + tech_debt block — specifically lines 21-24 for Phase 24 items)
    - `.planning/milestones/v4.0-ROADMAP.md` lines 138-148 (Phase 24 §"Goal" + 5 success criteria + 7 REQ-IDs — verbatim source for Observable Truths table)
    - `.planning/milestones/v4.0-REQUIREMENTS.md` (full bodies for DEBT-03..06 + TEST-04..06 — quote in Requirements Coverage table)
    - `.planning/phases/30-wywt-capture-alignment-fix/30-VERIFICATION.md` (in-repo format/tone reference)
    - Canonical Phase 22-VERIFICATION.md (D-05): run `git show 2918e95:.planning/phases/22-settings-restructure-account-section/22-VERIFICATION.md` and read FULLY before drafting.
    - Migration file (cited in SC-2 evidence): read `supabase/migrations/20260501000000_phase24_notification_enum_cleanup.sql` to confirm line ranges 23-33 (DO $$ EXCEPTION block), 36-55 (T-24-PARTIDX surgery), 62-76 (rename+recreate), 94-130 (post-migration assertion).
    - `src/db/schema.ts` lines 28-34 to confirm enum narrowed to `'follow', 'watch_overlap'` with the comment "Narrowed to 2 values in Phase 24 (DEBT-05) after prod migration applied".
  </read_first>
  <action>
    Create the archive directory and write the verification artifact.

    **Step 1 — create archive directory:**
    ```bash
    mkdir -p .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover
    ```
    (per D-01; no other files in this dir per D-02)

    **Step 2 — verify all 5 test suites GREEN on current main BEFORE drafting** (so the SC-5 row evidence is reproducible; capture exact pass counts):
    ```bash
    npx vitest run tests/store/watchStore.test.ts --reporter=basic         # TEST-04: expect 7/7 PASS
    npx vitest run tests/api/extract-watch.test.ts --reporter=basic        # TEST-05: expect 16/16 PASS
    npx vitest run tests/components/watch/WatchForm.test.tsx --reporter=basic   # TEST-06: expect 11/11 PASS
    npx vitest run tests/components/filters/FilterBar.test.tsx --reporter=basic # TEST-06: expect 5/5 PASS
    npx vitest run tests/components/watch/WatchCard.test.tsx --reporter=basic   # TEST-06: expect 7/7 PASS
    ```
    Total expected: 46 tests across 5 files (or 51 if the FORM-04 Phase 29-01 footnote test is now counted in WatchForm.test.tsx — verify the actual count and use whichever matches reality on current main).

    **Step 3 — verify enum narrowed + no live removed-enum references:**
    ```bash
    grep -nE "notificationTypeEnum.*pgEnum" src/db/schema.ts && sed -n '28,34p' src/db/schema.ts
    grep -rE "price_drop|trending_collector" src/ tests/ scripts/ | grep -v -E "comment|history|migration|\\.sql:" | head -20  # expect no live references
    grep -lrE "wornPublic" tests/                                          # expect ZERO matches
    grep -lrE "wear_visibility" tests/                                     # expect 2 files (phase11-schema, getWearRailForViewer)
    ls supabase/migrations/ | grep phase24                                 # expect 20260501000000_phase24_notification_enum_cleanup.sql
    ```

    **Step 4 — write the file** at `.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md` using the Write tool (NEVER heredoc/cat). Mirror Phase 22-VERIFICATION.md structure exactly per D-05.

    ## Frontmatter (verbatim shape from Phase 22 + RESEARCH Pattern 1; D-12 omits human_verification)

    ```yaml
    ---
    phase: 24-notification-stub-cleanup-test-fixture-carryover
    verified: <ACTUAL ISO timestamp e.g. 2026-05-05T22:00:00Z — NOT 2026-05-XX>
    status: passed
    score: "5/5 success criteria PASS"
    overrides_applied: 0
    closes_audit_items:
      - .planning/milestones/v4.0-MILESTONE-AUDIT.md#L22  # "No phase-level 24-VERIFICATION.md"
      - .planning/milestones/v4.0-MILESTONE-AUDIT.md#L24  # 24-VALIDATION.md frontmatter draft
    ---
    ```

    Do NOT add `human_verification:` key. Per D-12, Phase 24 has no pending UAT items. Per RESEARCH Assumption A3, omitting the key entirely is preferred over `human_verification: []`. (Either is harmless; pick omission.)

    ## H1 + Header Block (Pattern 2)

    ```markdown
    # Phase 24: Notification Stub Cleanup + Test Fixture & Carryover — Verification Report

    **Phase Goal:** The v3.0 dead-code stubs (price_drop, trending_collector notification types) are removed from the enum + Drizzle + render branches via the rename+recreate pattern, the 9 test files referencing the removed wornPublic column are updated to the v3.0 wear_visibility enum, and the three test suites carried from v1.0 (TEST-04/05/06) finally land. (Verbatim from `.planning/milestones/v4.0-ROADMAP.md` line 139.)
    **Verified:** <SAME ISO timestamp as frontmatter>
    **Status:** passed
    **Re-verification:** No — initial verification (post-hoc audit, retroactive close of v4.0-MILESTONE-AUDIT.md lines 21-24)
    ```

    ## `## Goal Achievement` → `### Observable Truths` Table (Pattern 3)

    Columns: `# | Truth | Status | Evidence`. One row per success criterion. Use the verbatim truth wording from v4.0-ROADMAP.md lines 143-147. Build evidence cells from RESEARCH §"Phase 24 Evidence Catalogue" + §"Per-Criterion Evidence Map (Phase 24)":

    - **Row 1 (SC-1, DEBT-03):** Status = `VERIFIED`. Evidence: `supabase/migrations/20260501000000_phase24_notification_enum_cleanup.sql:23-33` contains a `DO $$ … RAISE EXCEPTION …` block that fails if rows reference `price_drop` or `trending_collector` outside `{follow, watch_overlap}`; standalone Layer 1 preflight at `scripts/preflight-notification-types.ts` provides redundant CI/runner-time check. Quote the migration's lines 23-33 verbatim.
    - **Row 2 (SC-2, DEBT-04):** Status = `VERIFIED`. Evidence: same migration file —
      - Lines 36-55: T-24-PARTIDX surgery (DROP partial dedup index BEFORE enum rename, CREATE INDEX AFTER type swap — per memory `~/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/project_drizzle_supabase_db_mismatch.md` enum-bound dependent surgery pattern)
      - Lines 62-76: rename+recreate (RENAME old type, CREATE TYPE new, ALTER COLUMN type cast, DROP TYPE old)
      - Lines 94-130: post-migration assertion (`pg_enum` count = 2, column references new type, partial dedup index recreated)
    - **Row 3 (SC-3, DEBT-05):** Status = `VERIFIED`. Evidence: `src/db/schema.ts:28-34` shows enum narrowed to `['follow', 'watch_overlap']` with comment "Narrowed to 2 values in Phase 24 (DEBT-05) after prod migration applied"; `grep -rE "price_drop|trending_collector" src/ tests/ scripts/` returns no live references. Footnote: schema gained `watches.sort_order` column per Phase 27-02 commit `e4d6b78` — additive change, does NOT touch the Phase 24 enum cleanup contract.
    - **Row 4 (SC-4, DEBT-06):** Status = `VERIFIED`. Evidence: `grep -lrE "wornPublic" tests/` returns ZERO matches; `grep -lrE "wear_visibility" tests/` returns 2 living references (`tests/integration/phase11-schema.test.ts`, `tests/data/getWearRailForViewer.test.ts`). Footnote per RESEARCH Assumption A2: v4.0-REQUIREMENTS.md line 123 reconciles "9 files updated" → "4 files modified per D-04 dead-test-deletion rule" — the dead-test-deletion narrowed scope; the contract is met (zero `wornPublic` references in tests).
    - **Row 5 (SC-5, TEST-04/05/06):** Status = `VERIFIED`. Evidence — 5 vitest commands with pass counts (use ACTUAL counts from Step 2 above):
      - TEST-04: `npx vitest run tests/store/watchStore.test.ts --reporter=basic` → `<actual count>/<actual count>` PASS
      - TEST-05: `npx vitest run tests/api/extract-watch.test.ts --reporter=basic` → `<actual count>/<actual count>` PASS (NOTE: path is `tests/api/`, NOT `tests/app/api/` per RESEARCH Pitfall 5)
      - TEST-06: `npx vitest run tests/components/watch/WatchForm.test.tsx --reporter=basic` → `<actual>/<actual>` PASS
      - TEST-06: `npx vitest run tests/components/filters/FilterBar.test.tsx --reporter=basic` → `<actual>/<actual>` PASS
      - TEST-06: `npx vitest run tests/components/watch/WatchCard.test.tsx --reporter=basic` → `<actual>/<actual>` PASS
      - Footnote: WatchForm test file extended by Phase 29-01 commit `9c2126f test(29-01): add FORM-04 reset-on-key-change test for WatchForm` — additive (extends test file with one more test, does NOT remove any Phase 24 contract).

    Closing line under the table:
    > **Score:** 5/5 success criteria VERIFIED. All 7 REQ-IDs (DEBT-03/04/05/06 + TEST-04/05/06) SATISFIED. No GAPs. No pending human UAT.

    ## `### Required Artifacts` Table (Pattern 4)

    Columns: `Artifact | Expected | Status | Path on Current Main`. Lift the 9 rows from RESEARCH §"Phase 24 Required Artifacts" verbatim:
    - Phase 24 enum cleanup migration (132 LOC) → VERIFIED
    - Standalone preflight script → VERIFIED
    - Drizzle enum narrowed (2 values) → VERIFIED
    - watchStore unit tests (7 tests) → VERIFIED
    - `/api/extract-watch` integration tests (16 tests; NOTE: `tests/api/` not `tests/app/api/`) → VERIFIED
    - WatchForm component tests (11 tests) → VERIFIED
    - FilterBar component tests (5 tests) → VERIFIED
    - WatchCard component tests (7 tests) → VERIFIED
    - PointerEvent polyfill in setup file (`tests/setup.tsx` — `.tsx` after Phase 29-06 rename, not `.ts`) → VERIFIED

    ## `### Key Link Verification` Table (Pattern 5)

    Columns: `From | To | Via | Status | Details`. Cross-component / cross-layer wiring claims. Include at minimum:
    - Migration script | `notification_type` enum + `notifications.type` column | rename+recreate + ALTER COLUMN cast | WIRED | Migration applied to prod per audit line 23
    - `src/db/schema.ts` notificationTypeEnum | `notification_type` Postgres type | `pgEnum('notification_type', [...])` declaration | WIRED | Drizzle definition matches Postgres enum after migration
    - `src/components/notifications/NotificationRow.tsx` | `notification_type` enum values | render switch | WIRED | No render branches for removed `price_drop` / `trending_collector` values exist
    - Test fixture files (collection / data layer) | `wear_visibility` enum | direct column references | WIRED | Zero `wornPublic` references; 2 living `wear_visibility` references
    - `tests/setup.tsx` | All component tests requiring PointerEvent | `beforeAll` polyfill installation | WIRED | PointerEvent polyfill lifted to setup file (Phase 24 work)

    ## `### Behavioral Spot-Checks` Table (Pattern 7)

    Columns: `Behavior | Command | Result | Status`. Include exactly the 9-10 rows from RESEARCH §"Phase 24 Behavioral Spot-Checks":
    - watchStore filter reducer tests (TEST-04) → vitest command → 7/7 PASS → PASS
    - extract-watch integration tests (TEST-05) → vitest command → 16/16 PASS → PASS
    - WatchForm component tests (TEST-06) → vitest command → 11/11 PASS → PASS
    - FilterBar component tests (TEST-06) → vitest command → 5/5 PASS → PASS
    - WatchCard component tests (TEST-06) → vitest command → 7/7 PASS → PASS
    - Enum narrowed to 2 values → grep + sed at lines 28-34 → match found → PASS
    - Migration filename matches Phase 24 pattern → `ls supabase/migrations/ | grep phase24` → 1 match → PASS
    - No `wornPublic` in tests → `grep -rlE "wornPublic" tests/` → no matches → PASS
    - `wear_visibility` in tests → `grep -lrE "wear_visibility" tests/` → 2 files → PASS
    - No live removed-enum branches → `grep -rE "price_drop|trending_collector" src/` filtered for live code → no matches → PASS

    ## `### Requirements Coverage` Table (Pattern 8)

    Columns: `Requirement | Source Plan | Description | Status | Evidence`. One row per REQ-ID (7 total):
    - DEBT-03 (→ SC-1, plan 24-01..24-02) — SATISFIED
    - DEBT-04 (→ SC-2, plan 24-02..24-04) — SATISFIED
    - DEBT-05 (→ SC-3, plan 24-04..24-05) — SATISFIED
    - DEBT-06 (→ SC-4, plan 24-05..24-06) — SATISFIED
    - TEST-04 (→ SC-5, plan 24-07) — SATISFIED
    - TEST-05 (→ SC-5, plan 24-07) — SATISFIED
    - TEST-06 (→ SC-5, plan 24-08) — SATISFIED

    Lift descriptions from `.planning/milestones/v4.0-REQUIREMENTS.md` for each REQ-ID body. Use the SUMMARY.md filenames from git history at `9d87293^:.planning/phases/24-.../24-NN-SUMMARY.md` for the source-plan column (best-fit mapping; document any guesses).

    ## `### Anti-Patterns Found` Table (Pattern 9)

    Columns: `File | Line | Pattern | Severity | Impact`. Include even if empty (Phase 22 + Phase 30 precedent):
    - Single row: "None found" — Phase 24 evidence inspection found no anti-patterns. Migration is idempotent; tests assert real behavior; enum narrowing is complete. Severity n/a.

    ## `### Gaps Summary` Section (Pattern 11)

    One paragraph: "No gaps found. All 5 roadmap success criteria are VERIFIED by code inspection + test runs (51 tests across 5 files PASS). All 7 REQ-IDs (DEBT-03/04/05/06 + TEST-04/05/06) SATISFIED. No human UAT pending — Phase 24 was infrastructure / test-fixture cleanup with no user-facing surface requiring visual confirmation. Frontmatter `status: passed` per D-12. The audit's `24-VALIDATION.md frontmatter status: draft` finding (audit line 24) is a separate hygiene gap tracked under Phase 31 Deferred Ideas (CONTEXT.md) and is out of this artifact's scope."

    ## Footer (Pattern 12)

    ```markdown
    ---

    _Verified: <SAME ISO timestamp>_
    _Verifier: Claude (gsd-verifier; Phase 31 backfill)_
    ```

    **Critical writing rules:**
    - Use the Write tool. NEVER use heredoc / `cat <<'EOF'` for file creation.
    - Use ACTUAL ISO timestamp (run `date -u +"%Y-%m-%dT%H:%M:%SZ"`); never `2026-05-XX`.
    - Use ACTUAL test pass counts from Step 2 (do not hardcode "16/16" if vitest reports a different number — copy the real output).
    - Path correction per RESEARCH Pitfall 5: cite `tests/api/extract-watch.test.ts` (NOT `tests/app/api/`).
    - Drift handling per D-04 + RESEARCH Pitfall 4: footnote inside SC-3 + SC-5 rows; do NOT add a `## Drift Since v4.0 Ship` subsection.
    - `human_verification` frontmatter key is OMITTED entirely (D-12 + Assumption A3).
    - SC-2 row cross-references the project memory `project_drizzle_supabase_db_mismatch.md` for the enum-bound dependent surgery pattern provenance.
  </action>
  <verify>
    <automated>
test -f .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && head -1 .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md | grep -q '^---$' \
  && grep -c '^---$' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md | grep -qE '^[2-9]$|^[0-9][0-9]+$' \
  && grep -c '^### Observable Truths' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Required Artifacts' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Key Link Verification' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Behavioral Spot-Checks' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Requirements Coverage' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Anti-Patterns Found' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Gaps Summary' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -qE '^status: passed' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && grep -qE '5/5 success criteria PASS' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && grep -qE 'DEBT-03' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && grep -qE 'DEBT-04' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && grep -qE 'DEBT-05' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && grep -qE 'DEBT-06' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && grep -qE 'TEST-04' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && grep -qE 'TEST-05' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && grep -qE 'TEST-06' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && grep -qE '20260501000000_phase24_notification_enum_cleanup' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && grep -qE 'tests/api/extract-watch.test.ts' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && ! grep -qE 'tests/app/api/extract-watch' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && ! grep -qE '2026-05-XX' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && ! grep -qE '## Drift Since v4.0' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && ! grep -qE '^human_verification:' .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md \
  && ls .planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/ | grep -v '^24-VERIFICATION.md$' | wc -l | grep -qE '^[[:space:]]*0$'
    </automated>

    Manual cross-checks (run by executor and report results in commit message):
    - Confirm Phase 22 anatomy mirrored: section ordering matches `git show 2918e95:.planning/phases/22-settings-restructure-account-section/22-VERIFICATION.md` ordering.
    - Confirm all 7 REQ-IDs present in `### Requirements Coverage`: `grep -cE '\\| (DEBT-03|DEBT-04|DEBT-05|DEBT-06|TEST-04|TEST-05|TEST-06) \\|'` returns 7.
    - Confirm `verified:` frontmatter is a real ISO timestamp.
    - Confirm `status: passed` (not `human_needed`, not `closed`).
    - Confirm migration line ranges (23-33, 36-55, 62-76, 94-130) cited and accurate vs. actual file contents.
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md`
    - Archive directory contains EXACTLY one file (`24-VERIFICATION.md`) — no resurrected historical artifacts per D-02
    - File begins with `^---$` and has matching `^---$` close (frontmatter parses)
    - Frontmatter contains keys: `phase`, `verified` (real ISO timestamp, not literal `2026-05-XX`), `status: passed`, `score`, `overrides_applied: 0`
    - `human_verification:` key is ABSENT from frontmatter (D-12 + Assumption A3)
    - File contains all 8 canonical sections (counts > 0): `### Observable Truths`, `### Required Artifacts`, `### Key Link Verification`, `### Behavioral Spot-Checks`, `### Requirements Coverage`, `### Anti-Patterns Found`, `### Gaps Summary`, plus the H1 `# Phase 24: ... Verification Report`
    - Score line in body matches frontmatter: `5/5 success criteria PASS`
    - All 7 REQ-IDs cited: DEBT-03, DEBT-04, DEBT-05, DEBT-06, TEST-04, TEST-05, TEST-06
    - Migration filename `20260501000000_phase24_notification_enum_cleanup` cited as evidence in SC-1 + SC-2 rows
    - Path correction applied: `tests/api/extract-watch.test.ts` cited (NOT `tests/app/api/`)
    - NO `## Drift Since v4.0 Ship` subsection (drift footnoted inline per D-04)
    - NO literal `2026-05-XX` placeholder anywhere in file
    - NO `human_verification:` key in frontmatter
    - All 5 vitest commands cited with actual pass counts (counts captured from Step 2 of action)
  </acceptance_criteria>
  <done>
    `.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md` exists with 8 canonical sections, score `5/5 success criteria PASS`, frontmatter `status: passed`, migration cited with line ranges, 5 vitest pass counts captured, path corrections applied, drift footnoted (not subsectioned). Archive directory holds only this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | No production code changes; documentation-only audit. No new authn / authz / data flow surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | Repudiation | 24-VERIFICATION.md test pass-count claims | mitigate | Each SC-5 row evidence cell quotes a vitest run command; Step 2 of the action runs the commands and captures actual counts; if any test starts failing post-write, the artifact will be visibly stale (re-runnable evidence). |

No new attack surface. Asset list: Markdown artifact in `.planning/milestones/v4.0-phases/`. No secrets, no PII, no executable code.
</threat_model>

<verification>
After Task 1 completes, all `<verify><automated>` checks pass. Plan 31-02's output (`24-VERIFICATION.md`) is then consumable by Plan 31-03 (the audit closure section will cite this file by path + score).

Cross-plan invariants:
- Plan 31-01 (Wave 1, parallel) writes `23-VERIFICATION.md` to a DIFFERENT archive directory (zero `files_modified` overlap).
- Plan 31-03 (Wave 2) cites this file's path + score in the Closure section.
</verification>

<success_criteria>
- File `.planning/milestones/v4.0-phases/24-notification-stub-cleanup-test-fixture-carryover/24-VERIFICATION.md` exists at the D-01 path
- Archive directory contains exactly one file (D-02 enforcement)
- All `<acceptance_criteria>` bullets in Task 1 are met
- Audit asymmetry for Phase 24 (audit lines 21-24) is closed at the artifact level
</success_criteria>

<output>
After completion, create `.planning/phases/31-v4-0-verification-backfill/31-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. The SUMMARY.md should record:
- The exact `verified:` ISO timestamp used
- The exact 5 vitest pass counts captured in Step 2
- Any deviations from the action spec (e.g., if vitest pass counts differ from RESEARCH-cited counts, document the live values)
</output>
