---
phase: 31-v4-0-verification-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md
autonomous: true
requirements: [DEBT-07]
tags: [audit, verification, documentation, v4.0-backfill]

must_haves:
  truths:
    - "Archive directory `.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/` exists (D-01)."
    - "`23-VERIFICATION.md` lives at the D-01 path and ONLY this file (D-02 — no resurrected CONTEXT/PLAN/SUMMARY/RESEARCH)."
    - "File has valid YAML frontmatter open+close (`^---` first line, `^---$` close)."
    - "Frontmatter `phase`, `verified` (ISO timestamp, NOT `2026-05-XX`), `status: human_needed`, `score`, `overrides_applied: 0`, `human_verification` array of 5 items (D-11) all present."
    - "Each `human_verification` entry has `test`, `expected`, `why_human` keys (Phase 22 shape)."
    - "All 5 carryover UAT items present: preferences persistence + brand-loyalist, analyzeSimilarity new-preference re-read, cross-surface theme sync, notesPublic cross-page revalidation, chronometer Checkbox→Certification row."
    - "Body mirrors canonical Phase 22-VERIFICATION.md structure (D-05): `# Phase 23 ... Verification Report` H1; `## Goal Achievement` with `### Observable Truths` table; `### Required Artifacts`; `### Key Link Verification`; `### Behavioral Spot-Checks`; `### Requirements Coverage`; `### Anti-Patterns Found`; `### Human Verification Required`; `### Gaps Summary`; footer."
    - "Observable Truths table has 5 rows (one per v4.0-ROADMAP.md Phase 23 success criterion): SC-1 (collectionGoal+overlapTolerance), SC-2 (notifications toggles), SC-3 (privacy + appearance), SC-4 (notesPublic FEAT-07 — STATUS=GAP), SC-5 (isChronometer FEAT-08)."
    - "FEAT-07 row status = `GAP`; cited evidence includes `git merge-base --is-ancestor 4d362ff HEAD` returns 1, `grep -nE 'notesPublic' src/app/actions/watches.ts` returns no matches, `tests/actions/watches.notesPublic.test.ts` 0/4 PASS — 4 FAIL (Option A from RESEARCH §Critical regression)."
    - "Score line reads `4/5 success criteria VERIFIED + 1 GAP (FEAT-07 server-action regression)` or equivalent (matches Score Format table in RESEARCH §Architecture Patterns Pattern 13)."
    - "SC-2 (SET-09) and SC-3 (SET-11) Privacy + SET-12 redirect rows cite `git show 9d87293^:.planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md` as evidence source per D-07 (single-line entry; do not re-derive)."
    - "Path corrections (per RESEARCH Pitfall 5) applied: `src/components/layout/InlineThemeSegmented.tsx` (NOT `src/components/theme/`), `src/components/settings/preferences/CollectionGoalCard.tsx` + `OverlapToleranceCard.tsx` (NOT flat under settings/)."
    - "Drift footnote applied (NOT a dedicated `## Drift Since v4.0 Ship` subsection per D-04 + RESEARCH Pitfall 4): WatchForm `fbe3522` v4.1 commit footnoted on FEAT-07 + FEAT-08 Observable Truth rows; actions/watches.ts `aaf66a4` sort_order footnoted on FEAT-07 row."
    - "Auxiliary regression — `tests/components/settings/preferences/` (CollectionGoalCard.test.tsx, OverlapToleranceCard.test.tsx) failing 0/3 due to `useRouter` test-infra bug — documented under `### Anti-Patterns Found` with severity Info (NOT under Observable Truths) per RESEARCH Pitfall 6."
    - "`### Gaps Summary` includes a dedicated paragraph stating the FEAT-07 server-action regression and recommending tracking as new follow-up DEBT-09 (per RESEARCH §Critical regression Option A recommendation)."
    - "Nyquist Dimension 8 N/A — documentation phase produces no executable code per VALIDATION.md §Out of Nyquist Scope; no verify-phase Dimension 8 expectation."
  artifacts:
    - path: ".planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md"
      provides: "Phase 23 phase-level goal-backward verification artifact (closes audit lines 17-22)"
      contains: "frontmatter with status: human_needed; 5-row Observable Truths table; Required Artifacts table including notesPublic Zod field MISSING (regression); Anti-Patterns Found with useRouter test-infra info row; 5-item Human Verification Required section; Gaps Summary with FEAT-07 regression paragraph"
      min_sections: 7
    - path: ".planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/"
      provides: "v4.0 phase archive directory (D-01)"
      contains_only: "23-VERIFICATION.md (no resurrected historical artifacts per D-02)"
  key_links:
    - from: ".planning/milestones/v4.0-MILESTONE-AUDIT.md lines 14-22"
      to: ".planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md"
      via: "23-VERIFICATION.md opening paragraph or frontmatter cites the audit's tech_debt items 17-22 (bidirectional closure)"
      pattern: "v4.0-MILESTONE-AUDIT"
    - from: "23-VERIFICATION.md SC-2 + SC-3 (SET-09/11) + SET-12 rows"
      to: "git history `9d87293^:.planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md`"
      via: "single-line citation per D-07 — sub-plan no-diff carryover"
      pattern: "23-06-VERIFICATION"
    - from: "23-VERIFICATION.md SC-4 (FEAT-07) GAP row"
      to: "current `main` evidence: `src/app/actions/watches.ts`, `tests/actions/watches.notesPublic.test.ts`, commit ancestry `git merge-base --is-ancestor 4d362ff HEAD`"
      via: "evidence cell quoting all three checks"
      pattern: "4d362ff|notesPublic|watches.notesPublic.test"
---

<objective>
Write the phase-level goal-backward verification artifact for Phase 23 (Settings Sections + Schema-Field UI) and place it at the v4.0 milestone archive path. This is one of the two artifacts that close the v4.0 verification asymmetry recorded in `.planning/milestones/v4.0-MILESTONE-AUDIT.md` lines 17-22.

Purpose:
- Close audit gap "No phase-level 23-VERIFICATION.md — only sub-plan 23-06-VERIFICATION.md exists" (audit line 17).
- Produce evidence-cited verdicts for all 5 v4.0-ROADMAP.md Phase 23 success criteria + all 8 REQ-IDs (SET-07/08/09/10/11/12 + FEAT-07/08).
- Carry the 5 pending Phase 23 human-UAT items into the artifact's `human_verification` frontmatter per D-11.
- Surface the FEAT-07 server-action regression honestly (Option A from RESEARCH §Critical regression — separate GAP row, score `4/5 VERIFIED + 1 GAP`, recommend new DEBT-09 in `### Gaps Summary`). Do NOT remediate inline — Phase 31 is audit-only per CONTEXT D-Out-of-scope.

Output: One Markdown file: `.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-v4-0-verification-backfill/31-CONTEXT.md
@.planning/phases/31-v4-0-verification-backfill/31-RESEARCH.md
@.planning/phases/31-v4-0-verification-backfill/31-VALIDATION.md
@.planning/milestones/v4.0-MILESTONE-AUDIT.md
@.planning/milestones/v4.0-ROADMAP.md
@.planning/milestones/v4.0-REQUIREMENTS.md
@.planning/phases/30-wywt-capture-alignment-fix/30-VERIFICATION.md

<historical_artifacts>
<!-- Read via `git show` only — do NOT copy into the archive dir per D-02 -->
<!-- Phase 22 canonical VERIFICATION.md template (D-05): -->
<!--   git show 2918e95:.planning/phases/22-settings-restructure-account-section/22-VERIFICATION.md -->
<!-- Phase 23 sub-plan no-diff verification (D-07 evidence source for SET-09/11/12): -->
<!--   git show 9d87293^:.planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md -->
<!-- Phase 23 human UAT items (D-11 carryover source): -->
<!--   git show 9d87293^:.planning/phases/23-settings-sections-schema-field-ui/23-HUMAN-UAT.md -->
<!-- Phase 23 CONTEXT (decision basis for FEAT-07 D-19 revalidatePath claim): -->
<!--   git show 9d87293^:.planning/phases/23-settings-sections-schema-field-ui/23-CONTEXT.md -->
</historical_artifacts>

<current_main_evidence_paths>
<!-- All paths verified by Phase 31 RESEARCH against current main (RESEARCH §Pitfall 5). -->
src/components/settings/PreferencesSection.tsx
src/components/settings/preferences/CollectionGoalCard.tsx     <!-- preferences/ subdir, NOT flat -->
src/components/settings/preferences/OverlapToleranceCard.tsx   <!-- preferences/ subdir, NOT flat -->
src/components/settings/NotificationsSection.tsx
src/components/settings/PrivacySection.tsx
src/components/settings/AppearanceSection.tsx
src/components/layout/InlineThemeSegmented.tsx                 <!-- layout/, NOT theme/ -->
src/app/preferences/page.tsx
src/components/watch/WatchForm.tsx
src/components/watch/WatchDetail.tsx
src/components/profile/NoteVisibilityPill.tsx
src/app/actions/watches.ts                                     <!-- notesPublic field MISSING — regression -->
tests/actions/watches.notesPublic.test.ts                      <!-- 0/4 PASS — confirms regression -->
tests/components/watch/WatchForm.isChronometer.test.tsx        <!-- 5/5 PASS -->
tests/components/watch/WatchDetail.isChronometer.test.tsx      <!-- 4/4 PASS -->
tests/components/watch/WatchForm.notesPublic.test.tsx          <!-- 6/6 PASS — UI level only -->
tests/components/settings/preferences/                          <!-- 0/3 PASS — useRouter test-infra bug, NOT contract regression -->
</current_main_evidence_paths>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create archive directory and write 23-VERIFICATION.md</name>
  <read_first>
    - `.planning/phases/31-v4-0-verification-backfill/31-CONTEXT.md` (decisions D-01 through D-11; specifics; deferred ideas)
    - `.planning/phases/31-v4-0-verification-backfill/31-RESEARCH.md` (full file — especially §"Architecture Patterns" Pattern 1-13, §"Phase 23 Evidence Catalogue", §"Per-Criterion Evidence Map (Phase 23)", §"Critical regression — FEAT-07 commit not on main", §"Pitfall 4" drift inventory, §"Pitfall 5" path drift, §"Pitfall 6" useRouter test-infra)
    - `.planning/phases/31-v4-0-verification-backfill/31-VALIDATION.md` (Nyquist Dimension 8 N/A note for must_haves.truths)
    - `.planning/milestones/v4.0-MILESTONE-AUDIT.md` lines 1-58 (frontmatter + tech_debt block; this is what the new file closes)
    - `.planning/milestones/v4.0-ROADMAP.md` lines 126-136 (Phase 23 §"Goal" + 5 success criteria + 8 REQ-IDs — verbatim source for Observable Truths table)
    - `.planning/milestones/v4.0-REQUIREMENTS.md` (full bodies for SET-07..12, FEAT-07/08 — quote in Requirements Coverage table)
    - `.planning/phases/30-wywt-capture-alignment-fix/30-VERIFICATION.md` (in-repo format/tone reference)
    - Canonical Phase 22-VERIFICATION.md (D-05): run `git show 2918e95:.planning/phases/22-settings-restructure-account-section/22-VERIFICATION.md` and read the FULL output before drafting. This is the structural template to mirror exactly.
    - 23-06-VERIFICATION.md (D-07 sub-plan): run `git show 9d87293^:.planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md` to extract the SET-09 / SET-11 / SET-12 verdict prose to cite.
    - 23-HUMAN-UAT.md (D-11 carryover): run `git show 9d87293^:.planning/phases/23-settings-sections-schema-field-ui/23-HUMAN-UAT.md` to extract the 5 pending UAT items verbatim.
    - 23-CONTEXT.md (D-19 revalidatePath origin for FEAT-07 row evidence framing): run `git show 9d87293^:.planning/phases/23-settings-sections-schema-field-ui/23-CONTEXT.md`
    - Current `src/` files cited as evidence: `src/components/settings/PreferencesSection.tsx`, `src/components/settings/preferences/CollectionGoalCard.tsx`, `src/components/settings/preferences/OverlapToleranceCard.tsx`, `src/components/settings/AppearanceSection.tsx`, `src/components/layout/InlineThemeSegmented.tsx`, `src/components/watch/WatchForm.tsx` (lines 81, 125, 575-577, 648-665), `src/components/watch/WatchDetail.tsx` (lines 287-295), `src/app/actions/watches.ts` (verify notesPublic + revalidatePath('/u/[username]', 'layout') BOTH absent on current main).
  </read_first>
  <action>
    Create the archive directory and write the verification artifact.

    **Step 1 — create archive directory:**
    ```bash
    mkdir -p .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui
    ```
    (per D-01; no other files in this dir per D-02)

    **Step 2 — verify FEAT-07 regression evidence on current main BEFORE drafting** (so the GAP row evidence is reproducible):
    ```bash
    git merge-base --is-ancestor 4d362ff HEAD; echo $?    # expect 1 (NOT ancestor)
    grep -nE "notesPublic|notes_public" src/app/actions/watches.ts     # expect no matches
    grep -nE "revalidatePath\\('/u/" src/app/actions/watches.ts        # expect no matches
    npx vitest run tests/actions/watches.notesPublic.test.ts --reporter=basic  # expect 0/4 PASS — 4 FAIL
    ```
    Capture the exact pass/fail counts and exit codes; cite verbatim in the GAP row evidence column.

    **Step 3 — verify chronometer + UI-level notesPublic tests are GREEN on current main:**
    ```bash
    npx vitest run tests/components/watch/WatchForm.isChronometer.test.tsx --reporter=basic     # expect 5/5 PASS
    npx vitest run tests/components/watch/WatchDetail.isChronometer.test.tsx --reporter=basic   # expect 4/4 PASS
    npx vitest run tests/components/watch/WatchForm.notesPublic.test.tsx --reporter=basic       # expect 6/6 PASS (UI only)
    ```

    **Step 4 — verify auxiliary test-infra failure (Pitfall 6):**
    ```bash
    npx vitest run tests/components/settings/preferences/ --reporter=basic   # expect 0/3 PASS — fails with "useRouter is not mounted" — document under Anti-Patterns
    ```

    **Step 5 — write the file** at `.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md` using the Write tool (NEVER heredoc/cat). Mirror Phase 22-VERIFICATION.md structure exactly per D-05. Use Phase 30's tone for table style.

    ## Frontmatter (verbatim shape from Phase 22 + RESEARCH Pattern 1)

    ```yaml
    ---
    phase: 23-settings-sections-schema-field-ui
    verified: <ACTUAL ISO timestamp e.g. 2026-05-05T22:00:00Z — NOT 2026-05-XX>
    status: human_needed
    score: "4/5 success criteria VERIFIED + 1 GAP (FEAT-07 server-action regression)"
    overrides_applied: 0
    closes_audit_items:
      - .planning/milestones/v4.0-MILESTONE-AUDIT.md#L17  # "No phase-level 23-VERIFICATION.md"
      - .planning/milestones/v4.0-MILESTONE-AUDIT.md#L19  # 23-HUMAN-UAT.md status partial - 5 pending
    human_verification:
      - test: "Preferences persistence + brand-loyalist option"
        expected: "Selected option remains 'Brand Loyalist' after refresh; saving indicator briefly visible; no error banner"
        why_human: "Requires real cookie/localStorage round-trip across a page reload + visual confirmation of saving indicator. JSDOM cookie semantics + RTL rerender boundaries cannot reliably exercise the persist-then-refresh flow."
      - test: "analyzeSimilarity reads new preference on next read"
        expected: "Verdict label changes (e.g. fewer Hard Mismatch flags) — confirms analyzeSimilarity() reads the new preference on next render"
        why_human: "Requires real preference write → real watch detail re-render with the new preference value influencing the similarity verdict. End-to-end semantic check across two surfaces; cannot be programmatically asserted without a full integration harness."
      - test: "Cross-surface theme sync (D-06 duplicate-by-design)"
        expected: "Both surfaces stay in sync via the horlo-theme cookie; no flash of unstyled content; theme changes immediately"
        why_human: "Requires real cookie round-trip + DOM repaint observation across two surfaces (AppearanceSection + UserMenu InlineThemeSegmented). JSDOM cookie semantics + ThemeProvider context boundaries cannot be reliably exercised by RTL."
      - test: "notesPublic cross-page revalidation (D-19)"
        expected: "Cross-page revalidation works: revalidatePath('/u/[username]', 'layout') invalidates the user-scoped layout cache so the per-row pill re-renders with the new visibility immediately"
        why_human: "Requires Next.js layout-level revalidation across two routes (/watch/[id]/edit → /u/{username}/notes) — but see Gaps Summary: this UAT cannot pass while the FEAT-07 server-action regression is unresolved (the revalidatePath call site is absent on current main)."
      - test: "Chronometer end-to-end (Checkbox toggle → Certification row appears)"
        expected: "Row renders only when isChronometer === true; lucide Check icon at text-foreground (NOT text-accent); gap-1 between icon and label"
        why_human: "Visual rendering verification across the WatchForm submit path → WatchDetail re-render. Component tests cover unit behavior; the end-to-end visual confirmation in the live app remains a human check."
    ---
    ```

    ## H1 + Header Block (Pattern 2)

    ```markdown
    # Phase 23: Settings Sections + Schema-Field UI — Verification Report

    **Phase Goal:** The five Settings sections beyond Account are populated with the schema-driven knobs that exist in the database today but have no user-facing edit surface — exposing collectionGoal, overlapTolerance, notifyOnFollow/notifyOnWatchOverlap, theme switch, privacy toggles, plus per-note notesPublic and isChronometer on watches. (Verbatim from `.planning/milestones/v4.0-ROADMAP.md` line 127.)
    **Verified:** <SAME ISO timestamp as frontmatter>
    **Status:** human_needed
    **Re-verification:** No — initial verification (post-hoc audit, retroactive close of v4.0-MILESTONE-AUDIT.md lines 17-22)
    ```

    ## `## Goal Achievement` → `### Observable Truths` Table (Pattern 3)

    Columns: `# | Truth | Status | Evidence`. One row per success criterion. Use the verbatim truth wording from v4.0-ROADMAP.md lines 131-135. Build evidence cells from RESEARCH §"Phase 23 Evidence Catalogue" and §"Per-Criterion Evidence Map (Phase 23)":

    - **Row 1 (SC-1, SET-07/08):** Status = `VERIFIED (code level)`. Evidence: `src/components/settings/PreferencesSection.tsx:1-2,25-26` (imports CollectionGoalCard, OverlapToleranceCard); `src/components/settings/preferences/CollectionGoalCard.tsx` exists; `src/components/settings/preferences/OverlapToleranceCard.tsx` exists; UAT items 1+2 pending per frontmatter (real-cookie round-trip + analyzeSimilarity re-read = human-only).
    - **Row 2 (SC-2, SET-09):** Status = `VERIFIED (no-diff carryover via 23-06 sub-plan)`. Evidence: cite `git show 9d87293^:.planning/phases/23-settings-sections-schema-field-ui/23-06-VERIFICATION.md` §"SET-09" — single line per D-07. Do NOT replay the multi-grep block.
    - **Row 3 (SC-3, SET-10/11):** Status = `VERIFIED (code level)`. Evidence — split: SET-11 cites `23-06-VERIFICATION.md` §"SET-11" per D-07; SET-10 cites `src/components/settings/AppearanceSection.tsx:2,23` (imports `InlineThemeSegmented` from `@/components/layout/InlineThemeSegmented`; renders inside `<SettingsSection title="Theme">`) — explicit footnote noting actual path is `src/components/layout/`, NOT `src/components/theme/` (per RESEARCH Pitfall 5). UAT item 3 pending.
    - **Row 4 (SC-4, FEAT-07):** Status = `GAP`. Evidence (Option A — separate GAP row per RESEARCH §Critical regression):
      - WatchForm UI exists at `src/components/watch/WatchForm.tsx:648-665` (Public/Private pill — verified GREEN by `tests/components/watch/WatchForm.notesPublic.test.tsx` 6/6 PASS at the UI level)
      - Per-row pill exists at `src/components/profile/NoteVisibilityPill.tsx`
      - **Server action gap:** `git merge-base --is-ancestor 4d362ff HEAD; echo $?` returns **1** (commit `4d362ff` cited by audit line 111 + v4.0-REQUIREMENTS.md FEAT-07 line 93 is NOT an ancestor of HEAD)
      - `grep -nE "notesPublic" src/app/actions/watches.ts` returns no matches (Zod insertWatchSchema lacks the field; WatchForm sends it via formData spread; Zod silently strips)
      - `grep -nE "revalidatePath\\('/u/" src/app/actions/watches.ts` returns no matches (the `revalidatePath('/u/[username]', 'layout')` cross-page sync the audit + 23-HUMAN-UAT.md test #4 describe does NOT exist on main)
      - `npx vitest run tests/actions/watches.notesPublic.test.ts --reporter=basic` returns **0/4 PASS — 4 FAIL** (Phase 23-05 RED scaffold is RED on main)
      - **Footnote:** v4.1 Phase 28-05 commit `fbe3522 feat(28-05): rewrite AddWatchFlow + WatchForm commit handlers (UX-09 + ADD-08)` modified the WatchForm submit path. The handler rewrite did not introduce the server-action gap — the gap is `5991c3f`-era ship-state per RESEARCH Assumption A6. The actions/watches.ts gained sortOrder per Phase 27 commits `442dca9` + `aaf66a4` (additive; does NOT remove any Phase 23 contract).
    - **Row 5 (SC-5, FEAT-08):** Status = `VERIFIED (code level)`. Evidence: `src/components/watch/WatchForm.tsx:81,125,575-577` (default false, edit hydration, Checkbox); `src/components/watch/WatchDetail.tsx:287-295` (Certification row gated on `watch.isChronometer === true`); `tests/components/watch/WatchForm.isChronometer.test.tsx` 5/5 PASS; `tests/components/watch/WatchDetail.isChronometer.test.tsx` 4/4 PASS. Footnote: WatchForm submit path was rewritten by Phase 28-05 commit `fbe3522` — the isChronometer Checkbox + default false + edit hydration semantics survived the rewrite (test pass counts unchanged). UAT item 5 pending.

    Closing line under the table:
    > **Score:** 4/5 success criteria VERIFIED + 1 GAP. The GAP is the FEAT-07 server-action regression (commit `4d362ff` not on main); WatchForm UI shipped but the server-side persistence + cross-page sync did not. See `### Gaps Summary` below for the new follow-up tech_debt recommendation.

    ## `### Required Artifacts` Table (Pattern 4)

    Columns: `Artifact | Expected | Status | Path on Current Main`. Lift the 13 rows from RESEARCH §"Phase 23 Required Artifacts" verbatim — including the two MISSING regression rows for `actions/watches.ts notesPublic Zod field` and `actions/watches.ts revalidatePath call`.

    ## `### Key Link Verification` Table (Pattern 5)

    Columns: `From | To | Via | Status | Details`. Lift the 8 rows from RESEARCH §"Phase 23 Key Links" verbatim, including the **NOT WIRED** row for `WatchForm → per-row NoteVisibilityPill via revalidatePath('/u/[username]', 'layout')` (call site missing).

    ## `### Behavioral Spot-Checks` Table (Pattern 7)

    Columns: `Behavior | Command | Result | Status`. Include exactly the 7 rows from RESEARCH §"Phase 23 Behavioral Spot-Checks":
    - WatchForm Chronometer test (5/5 PASS)
    - WatchDetail Chronometer test (4/4 PASS)
    - WatchForm notesPublic UI test (6/6 PASS)
    - **actions notesPublic test (0/4 PASS — 4 FAIL — regression confirmed; Status = FAIL)**
    - Preferences cards tests (0/3 PASS — useRouter test-infra bug; Status = FAIL with note "test-infra, not contract")
    - AppearanceSection imports `InlineThemeSegmented` from layout (grep result PASS)
    - `/preferences` redirects (cite 23-06-VERIFICATION.md per D-07 — single PASS line)

    ## `### Requirements Coverage` Table (Pattern 8)

    Columns: `Requirement | Source Plan | Description | Status | Evidence`. One row per REQ-ID (8 total): SET-07, SET-08 (both → SC-1), SET-09 (→ SC-2), SET-10, SET-11 (both → SC-3), SET-12 (bonus — `/preferences` redirect), FEAT-07 (→ SC-4 GAP), FEAT-08 (→ SC-5). Lift descriptions from `.planning/milestones/v4.0-REQUIREMENTS.md`. Status: SET-07..12 + FEAT-08 = `SATISFIED`; FEAT-07 = `GAP`.

    ## `### Anti-Patterns Found` Table (Pattern 9)

    Columns: `File | Line | Pattern | Severity | Impact`. At minimum include:
    - `tests/components/settings/preferences/CollectionGoalCard.test.tsx` + `OverlapToleranceCard.test.tsx` — `useFormFeedback` calls `useRouter()` without a mocked `next/navigation` AppRouter — Severity Info — Impact: 3 tests fail at infrastructure level; component contract unaffected (verified by direct render in production); test-infra debt, NOT a Phase 23 SET-07/08 contract regression. Reference RESEARCH §Pitfall 6.
    - `src/app/actions/watches.ts` — missing `notesPublic` Zod field + missing `revalidatePath('/u/[username]', 'layout')` call — Severity High — Impact: FEAT-07 GAP (separate row above); recommend new DEBT-09 follow-up.

    ## `### Human Verification Required` Section (Pattern 10)

    For each of the 5 frontmatter `human_verification` entries: `#### N. <test title>`, `**Test:**` block (the `test:` step from 23-HUMAN-UAT.md verbatim), `**Expected:**` block, `**Why human:**` block. Use the verbatim wording from 23-HUMAN-UAT.md (read via `git show 9d87293^:.../23-HUMAN-UAT.md`).

    For UAT item #4 (notesPublic cross-page revalidation), add a `**Blocked by:**` line: "FEAT-07 GAP — server-side persistence + revalidatePath call are absent on main; this UAT cannot pass until the regression is remediated. See Gaps Summary."

    ## `### Gaps Summary` Section (Pattern 11)

    Two paragraphs:

    1. **FEAT-07 server-action regression.** State the commit ancestry finding (`4d362ff` not on main), the absent Zod field + revalidatePath call, the 4/4 RED test, and the 23-HUMAN-UAT.md item #4 dependency. Note that the audit (line 111) and v4.0-REQUIREMENTS.md (line 93) cited this commit as the implementation evidence — the audit was written from the workplan, not against shipped main. Recommend tracking as new follow-up tech_debt item (suggested ID: DEBT-09 — "FEAT-07 notesPublic server-action regression: re-add notesPublic to insertWatchSchema in src/app/actions/watches.ts + add revalidatePath('/u/[username]', 'layout') call on addWatch + editWatch success") to be addressed in v4.1 close, v5.0 onboarding work, or a dedicated quick-task. **Phase 31 does NOT remediate inline — audit-only scope per CONTEXT D-Out-of-scope.**

    2. **Other gaps:** None at the code level for the other 4 success criteria. The 5 pending human-UAT items are not gaps — they are pending visual / interactive verifications that JSDOM/RTL cannot exercise.

    ## Footer (Pattern 12)

    ```markdown
    ---

    _Verified: <SAME ISO timestamp>_
    _Verifier: Claude (gsd-verifier; Phase 31 backfill)_
    ```

    **Critical writing rules:**
    - Use the Write tool. NEVER use heredoc / `cat <<'EOF'` for file creation.
    - Use ACTUAL ISO timestamp (run `date -u +"%Y-%m-%dT%H:%M:%SZ"`); never `2026-05-XX`.
    - Cite path corrections explicitly so future readers do not re-introduce the drift (per RESEARCH Pitfall 5).
    - Every Observable Truth Evidence cell quotes a path+line OR a grep command + result OR a test name + pass count (per D-06). No prose-only evidence.
    - SET-09, SET-11, SET-12 rows = single-line citation of `23-06-VERIFICATION.md` per D-07. Do NOT re-derive.
    - The FEAT-07 GAP framing is Option A from RESEARCH (separate GAP row, score `4/5 + 1 GAP`, recommend new DEBT-09). Do NOT switch to Option B (split status) or Option C (silent re-derive). Do NOT remediate inline.
    - The new DEBT-09 recommendation lives ONLY in `### Gaps Summary` of this file (and is referenced by the audit's Closure section in Plan 31-03). Do NOT edit `.planning/REQUIREMENTS.md` or any audit body in this plan — that is out of Phase 31 scope.
    - Drift handling per D-04 + RESEARCH Pitfall 4: footnote inside FEAT-07 + FEAT-08 rows; do NOT add a `## Drift Since v4.0 Ship` subsection.
  </action>
  <verify>
    <automated>
test -f .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && head -1 .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -q '^---$' \
  && grep -c '^---$' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -qE '^[2-9]$|^[0-9][0-9]+$' \
  && grep -c '^### Observable Truths' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Required Artifacts' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Key Link Verification' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Behavioral Spot-Checks' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Requirements Coverage' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Anti-Patterns Found' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Human Verification Required' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -c '^### Gaps Summary' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -qE '^[1-9]' \
  && grep -qE '^status: human_needed' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && grep -qE '4/5.*VERIFIED.*GAP' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && grep -qE 'FEAT-07' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && grep -qE '4d362ff' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && grep -qE 'DEBT-09' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && grep -qE '23-06-VERIFICATION' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && grep -qE 'src/components/layout/InlineThemeSegmented' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && grep -qE 'src/components/settings/preferences/CollectionGoalCard' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && grep -cE '^      - test:' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md | grep -qE '^5$' \
  && ! grep -qE '2026-05-XX' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && ! grep -qE '## Drift Since v4.0' .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md \
  && ls .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/ | grep -v '^23-VERIFICATION.md$' | wc -l | grep -qE '^[[:space:]]*0$'
    </automated>

    Manual cross-checks (run by executor and report results in commit message):
    - Confirm Phase 22 anatomy mirrored: section ordering matches `git show 2918e95:.planning/phases/22-settings-restructure-account-section/22-VERIFICATION.md` ordering.
    - Confirm all 8 REQ-IDs present in `### Requirements Coverage`: `grep -cE '\\| (SET-07|SET-08|SET-09|SET-10|SET-11|SET-12|FEAT-07|FEAT-08) \\|'` returns 8.
    - Confirm score includes "4/5" + "1 GAP" + "FEAT-07".
    - Confirm `verified:` frontmatter is a real ISO timestamp (e.g. `grep -E '^verified: 2026-05-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$'`).
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md`
    - Archive directory contains EXACTLY one file (`23-VERIFICATION.md`) — no resurrected CONTEXT/PLAN/SUMMARY/RESEARCH/REVIEW/VALIDATION per D-02
    - File begins with `^---$` and has at least one matching `^---$` close (frontmatter parses)
    - Frontmatter contains keys: `phase`, `verified` (real ISO timestamp, not literal `2026-05-XX`), `status: human_needed`, `score`, `overrides_applied: 0`, `human_verification` array of EXACTLY 5 entries
    - Each `human_verification` entry has `test:`, `expected:`, `why_human:` keys (Phase 22 shape)
    - The 5 carryover UAT items match 23-HUMAN-UAT.md verbatim wording: "Preferences persistence + brand-loyalist option", "analyzeSimilarity reads new preference on next read", "Cross-surface theme sync (D-06 duplicate-by-design)", "notesPublic cross-page revalidation (D-19)", "Chronometer end-to-end (Checkbox toggle → Certification row appears)"
    - File contains all 9 canonical sections (counts > 0): `### Observable Truths`, `### Required Artifacts`, `### Key Link Verification`, `### Behavioral Spot-Checks`, `### Requirements Coverage`, `### Anti-Patterns Found`, `### Human Verification Required`, `### Gaps Summary`, plus the H1 `# Phase 23: ... Verification Report`
    - Score line in body matches frontmatter: contains `4/5` + `VERIFIED` + `GAP` + `FEAT-07`
    - FEAT-07 evidence cell cites: `4d362ff` (commit), `notesPublic` (field), `tests/actions/watches.notesPublic.test.ts` (4/4 FAIL)
    - SET-09 + SET-11 + SET-12 rows reference `23-06-VERIFICATION.md` (D-07 single-line citation)
    - Path corrections present: `src/components/layout/InlineThemeSegmented` (NOT theme/), `src/components/settings/preferences/CollectionGoalCard` + `OverlapToleranceCard` (NOT flat)
    - Anti-Patterns Found section documents the `useRouter` test-infra failure with severity Info
    - Gaps Summary recommends new DEBT-09 follow-up; explicitly states Phase 31 does NOT remediate inline
    - NO `## Drift Since v4.0 Ship` subsection (drift footnoted inline per D-04 + RESEARCH Pitfall 4)
    - NO literal `2026-05-XX` placeholder anywhere in file
  </acceptance_criteria>
  <done>
    `.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md` exists, has the 9 canonical sections, all 5 UAT items in frontmatter, FEAT-07 GAP row with `4d362ff` evidence trail, score `4/5 + 1 GAP`, sub-plan citation pattern for SET-09/11/12, path corrections applied, drift footnoted (not subsectioned), Anti-Patterns Found documents test-infra debt, Gaps Summary recommends new DEBT-09. Archive directory holds only this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | No production code changes; documentation-only audit. No new authn / authz / data flow surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Repudiation | 23-VERIFICATION.md evidence claims | mitigate | Every Observable Truth evidence cell cites a path+line, grep command+result, OR test pass count per D-06. FEAT-07 regression confirmed via 3 independent methods (commit ancestry, grep on current main, vitest run) per RESEARCH §Critical regression. |

No new attack surface. Asset list: Markdown artifact in `.planning/milestones/v4.0-phases/`. No secrets, no PII, no executable code.
</threat_model>

<verification>
After Task 1 completes, all `<verify><automated>` checks pass. Plan 31-01's output (`23-VERIFICATION.md`) is then consumable by Plan 31-03 (the audit closure section will cite this file by path + score).

Cross-plan invariants:
- Plan 31-02 (Wave 1, parallel) writes `24-VERIFICATION.md` to a DIFFERENT archive directory (zero `files_modified` overlap).
- Plan 31-03 (Wave 2) cites this file's path + score in the Closure section.
</verification>

<success_criteria>
- File `.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md` exists at the D-01 path
- Archive directory contains exactly one file (D-02 enforcement)
- All `<acceptance_criteria>` bullets in Task 1 are met
- Audit asymmetry for Phase 23 (audit lines 17-22) is closed at the artifact level (the new file IS the audit closure for those lines)
</success_criteria>

<output>
After completion, create `.planning/phases/31-v4-0-verification-backfill/31-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. The SUMMARY.md should record:
- The exact `verified:` ISO timestamp used in the new file
- The exact score string committed
- The 4 evidence-verification commands run (FEAT-07 ancestry + grep + vitest + chronometer test counts)
- Any deviations from the action spec (none expected)
</output>
