# Phase 19.1: Catalog Taste Enrichment - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-29
**Phase:** 19.1-catalog-taste-enrichment
**Areas discussed:** Closed vocabularies, Enrichment call mechanics, Confidence + backfill, WatchForm UX

---

## Closed vocabularies

### Q1: design_motifs vocabulary approach

| Option | Description | Selected |
|--------|-------------|----------|
| Lock now from proposal | Claude drafts 25-30 motif list; user reviews/edits in this discussion; lock before research | |
| Defer to plan-time | Researcher proposes draft list during /gsd-plan-phase research; user reviews in plan checker | ✓ |
| Empirical first-pass | Ship 19.1 with freeform motif output; observe top ~30; pin in v4.1 | |

**User's choice:** Defer to plan-time
**Notes:** Vocab pinning happens during the planner's research stage, with user review at the plan-checker round.

### Q2: crowd_signals final cut

| Option | Description | Selected |
|--------|-------------|----------|
| 7-value curated | Drop overlapping (vintage-hunter, dress-watch-purist, on-trend); keep 7 durable identities | |
| All 10 | Keep full original list, accept overlap with era_signal / primary_archetype | |
| I'll redraft | Free-form: tell me which to keep/drop/rename | (override) |

**User's choice:** Free-form override — **scrap crowd_signals from the schema entirely**
**Notes:** "After some thought, let's scrap this attribute from the schema as well. I don't think the juice is worth the squeeze." Schema delta vs locked memory architecture: 7 fields + 1 boolean instead of 8 fields + 1 boolean.

### Q3: Vocabulary violation handling

| Option | Description | Selected |
|--------|-------------|----------|
| Drop silently | Zod schema filters invalid values; matches existing validateAndCleanData | |
| Drop + log warning | Same filter + console.warn / drift_log table; vocab drift auditable | ✓ |
| Coerce to nearest | Map invalid → closest valid via simple rules; risky | |

**User's choice:** Drop + log warning
**Notes:** Validator filter + warning side-effect so vocab drift is observable.

### Q4: Vocabulary home (TS vs DB)

| Option | Description | Selected |
|--------|-------------|----------|
| TS constants only | src/lib/taste/vocab.ts arrays + Zod enums; column type stays text[] | ✓ |
| DB CHECK constraint | Postgres CHECK on each array element; ALTER TABLE on every change | |
| Both (TS + CHECK) | Belt and suspenders; doubles migration burden | |

**User's choice:** TS constants only
**Notes:** Mirrors existing STYLE_TAGS / DESIGN_TRAITS pattern in src/lib/constants.ts.

---

## Enrichment call mechanics

### Q1: URL extract path structure

| Option | Description | Selected |
|--------|-------------|----------|
| Combined single prompt | One Anthropic call returns spec + taste together | |
| Two-pass (sequential) | Spec call unchanged; taste call after row commit using catalog spec + image | ✓ |
| Two-pass lazy | Same as two-pass, but only if catalog_id has no taste yet | |

**User's choice:** Two-pass sequential
**Notes:** Spec extraction stays byte-identical to avoid regressing Phase 17 / 19 contracts. First-write-wins (D-13) avoids re-running on the same catalog_id anyway, so the lazy variant collapses into this.

### Q2: Sync vs fire-and-forget

| Option | Description | Selected |
|--------|-------------|----------|
| Fire-and-forget | Mirror CAT-08; row commits, taste promise unawaited; badges appear later | ✓ |
| Sync await | Block UI 2-5s for text, 5-8s for vision; badges visible immediately | |
| Hybrid by source | URL = sync, manual = fire-and-forget | |

**User's choice:** Fire-and-forget
**Notes:** "Fire-and-forget with some sort of messaging on the watch detail page that some fields are pending? idk maybe that's not worth it." Pending UI captured as Claude's discretion in CONTEXT (D-Disc), low priority.

### Q3: Failure handling

| Option | Description | Selected |
|--------|-------------|----------|
| Log + null + move on | Future write fills it; same fire-and-forget pattern | |
| One auto-retry + null | Single retry with backoff; catches transient blips | |
| Manual re-enrichment script | npm run db:reenrich-taste with explicit flags | ✓ |

**User's choice:** Manual re-enrichment script
**Notes:** Operator-driven retry via CLI flags; transient blip = NULL until next manual sweep.

### Q4: Model + structured output

| Option | Description | Selected |
|--------|-------------|----------|
| Sonnet 4.5 + tool-use schema | Newer model; tool_use with input_schema; Zod-mirrored | ✓ |
| Match existing llm.ts model | claude-sonnet-4-20250514 + JSON-extraction regex | |
| Mixed: Sonnet vision, Haiku text | ~5x cost reduction on no-photo path; two prompts | |

**User's choice:** "Sonnet 4.5" + tool-use schema
**Notes:** Per the session knowledge update (2026-02-27), the latest Sonnet ID is `claude-sonnet-4-6` — CONTEXT D-11 locks the latest model. User likely meant "the latest Sonnet" rather than literal 4.5. Plan-time: confirm model ID at execution time.

---

## Confidence + backfill

### Q1: Confidence threshold

| Option | Description | Selected |
|--------|-------------|----------|
| ≥0.6 = use it | Permissive default; below 0.6 falls back to generic | |
| ≥0.7 = use it | Tighter gate; lower coverage | |
| Two-tier | ≥0.7 full, 0.5-0.7 hedged, <0.5 generic; most nuanced | ✓ |
| You decide | Defer to Phase 20 implementation | |

**User's choice:** Two-tier
**Notes:** Phase 20 owns the actual copy generation; 19.1 stores confidence and exposes it to callers.

### Q2: Re-enrichment policy

| Option | Description | Selected |
|--------|-------------|----------|
| First-write-wins | Mirror Phase 17 D-13; never overwrite | |
| Higher-confidence-wins | Subsequent attempts overwrite if confidence > stored | |
| Force-only via script | Live flow never re-enriches; only db:reenrich-taste | ✓ |

**User's choice:** Force-only via script
**Notes:** Most controlled; updates flow exclusively through CLI flags (--force, --confidence-below, --catalog-id, --dry-run).

### Q3: Backfill script shape

| Option | Description | Selected |
|--------|-------------|----------|
| Standalone TS + dry-run | scripts/backfill-taste.ts mirroring Phase 17 D-14 pattern | ✓ |
| Pg_cron incremental | Daily cron processes N rows per night | |
| On-demand only | No backfill; existing rows never tasted | |

**User's choice:** Standalone TS + dry-run
**Notes:** Documented post-deploy step in deploy-db-setup.md, not auto-run.

### Q4: Missing API key behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Skip + log | console.warn only; matches existing extractWithLlm | |
| Skip + structured event | Structured event emit for observability | ✓ |
| Hard fail | Refuse to attempt; surfaces to user | |

**User's choice:** Skip + structured event
**Notes:** Local dev / CI work without the key; production misconfig surfaces in observability.

---

## WatchForm UX

### Q1: Photo home

| Option | Description | Selected |
|--------|-------------|----------|
| Catalog image_url | Phase 17 D-06 columns; cross-user visible; image_source_quality='user_uploaded' | ✓ |
| Per-user watches.imageUrl | Catalog gets only the boolean; strict per-user authorship | |
| Per-user + catalog cache | Photo on user row; catalog caches if it had none | |

**User's choice:** Catalog image_url (option 1)
**Notes:** "Eventually we'll need some TOS or something." TOS work captured as deferred — D-21 cross-user implication acknowledged; v4.0 ships personal-MVP.

### Q2: Photo bucket

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse wear-photos | Existing Phase 15 bucket, mixed-purpose | |
| New catalog-source-photos bucket | Dedicated bucket with same RLS + folder + HEIC + EXIF | ✓ |

**User's choice:** New catalog-source-photos bucket
**Notes:** Cleaner separation; future divergence in retention/access easier.

### Q3: Read-only badges UX

| Option | Description | Selected |
|--------|-------------|----------|
| Read-only badges, post-enrichment | Page refresh reveals badges after async enrichment | |
| Pending placeholder | "Analyzing taste…" muted block; swaps on next render | |
| Defer to Phase 20 | Zero badge UI in 19.1; verdict copy + badges land in Phase 20 | ✓ |

**User's choice:** Defer to Phase 20
**Notes:** Tightest scope. Phase 19.1 form change is purely "hide pickers + add photo field"; verdict UX is Phase 20 work.

### Q4: Owner override path

| Option | Description | Selected |
|--------|-------------|----------|
| Read-only forever | Taste is canonical; no override; force-rerun via script | |
| Per-user override | Pencil icon dialog; per-user override stored on watches row | |
| Defer override to v4.x | Read-only in 19.1; override surface = future small phase | ✓ |

**User's choice:** Defer override to v4.x
**Notes:** Captures the gap explicitly; gives time to see if real users hit bad enrichments.

---

## Final round

| Option | Description | Selected |
|--------|-------------|----------|
| I'm ready for context | Write CONTEXT.md with the decisions captured | ✓ |
| Photo upload details | Size limits, formats, encouragement copy, position | |
| Cost monitoring | Hard daily cap, per-user rate limit | |
| Phase 17 schema migration shape | Column ordering, generated columns, etc. | |

**User's choice:** I'm ready for context
**Notes:** All four selected areas resolved; deferred items captured. Memory entry needs updating after commit (crowd_signals dropped from schema).

---

## Claude's Discretion

- Specific design_motifs candidate list (researcher draft → user review in plan checker)
- Pending UI ("Analyzing taste…") on WatchDetail when taste fields are NULL — user said "idk maybe that's not worth it"
- Photo upload field copy, position, accepted formats, size limits (mirror Phase 15 wear-photos defaults)
- Backfill batch size default (20 vs 10 vs 50)
- Service module file structure inside `src/lib/taste/`
- Whether `image_source_quality = 'user_uploaded'` is added via single ALTER TABLE statement or split DROP + ADD
- Whether `design_motifs` gets array-element CHECK constraint (rigorous) or stays TS-only-validated (cheap)
- Specific structured-event taxonomy keys for `no_api_key` and `enrichment_failed`
- Whether the re-enrichment script writes a per-row audit row or just logs to stdout

## Deferred Ideas

(See CONTEXT.md `<deferred>` for the full list — Phase 20 verdict copy, owner override, TOS for user-uploaded photos becoming canonical catalog images, analyzeSimilarity rewire to v5.0, tag taxonomy audit to v5.0, re-enrichment automation, crowd_signals as derived view, A/B testing infra, bulk re-enrichment from richer model, stable hash dedup.)
