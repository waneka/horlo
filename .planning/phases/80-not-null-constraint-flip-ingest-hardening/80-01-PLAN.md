---
phase: 80-not-null-constraint-flip-ingest-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/slug.ts
  - tests/unit/data/catalog-resolver.test.ts
  - tests/integration/migrations/80-not-null-constraint.test.ts
  - tests/integration/data/catalog-resolver-against-local-db.test.ts
autonomous: true
requirements:
  - CANON-01
  - CANON-02
  - INGEST-01
  - INGEST-02
  - INGEST-03
  - INGEST-04
tags:
  - phase-80
  - resolver
  - tests
  - slug

must_haves:
  truths:
    - "Resolver unit test file exists with 10 RED test stubs (8 branches per D-80-01..02 + empty-model placeholder + re-extract idempotency)"
    - "NOT NULL constraint integration test file exists with 3 RED introspection assertions (CANON-01, CANON-02, 23502 INSERT)"
    - "Local-DB resolver integration test file exists with 4 RED branches (Tier 1 brand, Tier 2 brand fuzzy, family alias hit, brand auto-create cleanup)"
    - "Slug helper `slugify(name)` exported from `src/lib/slug.ts` is shared between Phase 79 script + Phase 80 resolver (extracted per Open Question #2)"
    - "All test commands fail predictably — `npm run test -- catalog-resolver` shows pending/red status; `npm run test -- 80-not-null-constraint` skips on no DATABASE_URL or fails on local"
  artifacts:
    - path: "src/lib/slug.ts"
      provides: "Pure helper extracted from scripts/v8.4-brand-canonicalization.ts L160-167; exports slugify(name: string): string"
      contains: "export function slugify"
    - path: "tests/unit/data/catalog-resolver.test.ts"
      provides: "10 RED unit test stubs covering INGEST-01..04 branches + idempotency + empty-model"
      contains: "describe('Phase 80 — catalog-resolver"
    - path: "tests/integration/migrations/80-not-null-constraint.test.ts"
      provides: "RED introspection + 23502 assertions for CANON-01 + CANON-02"
      contains: "is_nullable"
    - path: "tests/integration/data/catalog-resolver-against-local-db.test.ts"
      provides: "RED end-to-end resolver tests against local Supabase (alias hit + auto-create + cleanup)"
      contains: "resolveBrandId"
  key_links:
    - from: "tests/unit/data/catalog-resolver.test.ts"
      to: "src/data/catalog-resolver.ts (Plan 02)"
      via: "import { resolveBrandId, resolveFamilyId } from '@/data/catalog-resolver'"
      pattern: "from '@/data/catalog-resolver'"
    - from: "src/lib/slug.ts"
      to: "scripts/v8.4-brand-canonicalization.ts (Phase 79) + src/data/catalog-resolver.ts (Plan 02)"
      via: "shared import"
      pattern: "@/lib/slug"
---

<objective>
Land Wave 0 RED test stubs and the shared `slugify` helper that downstream plans will green. Tests describe the brand+family resolver contract (D-80-01 clear-gap, D-80-02 alias-before-fuzzy, D-80-04 four log event types) so the resolver implementation in Plan 02 is test-driven, and define the migration assertions Plan 04 will satisfy.

Purpose: Move all test files and shared utility extractions out of the path of the resolver-write plan, so Plan 02 stays narrowly focused on green-ing existing red tests. This also surfaces the slug-collision strategy decision (D-80 Discretion / Open Question #3) ONCE at the helper extraction step.
Output: 1 extracted helper (`src/lib/slug.ts`), 3 test files seeded with RED stubs that exactly match the test plan in 80-RESEARCH.md and 80-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-CONTEXT.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-PATTERNS.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-VALIDATION.md

@scripts/v8.4-brand-canonicalization.ts
@tests/integration/migrations/78-gin-index.test.ts
@tests/unit/getPublicWearPicsForWatch.test.ts

<interfaces>
<!-- Expected resolver contract from PATTERNS.md L177-201 — tests in this plan import these. -->
<!-- Plan 02 will create src/data/catalog-resolver.ts to satisfy this contract. -->

From src/data/catalog-resolver.ts (to be created Plan 02):
```typescript
export const BRAND_FUZZY_MIN_SCORE = 0.6
export const BRAND_FUZZY_CLEAR_GAP = 0.1
export const FAMILY_FUZZY_MIN_SCORE = 0.6

export type ResolveDecision =
  | { tier: 'exact'; decision: 'matched' }
  | { tier: 'alias'; decision: 'matched' }
  | { tier: 'fuzzy'; decision: 'matched'; score: number; runnerUp?: { id: string; name: string; score: number } }
  | { tier: 'auto_create'; decision: 'no_candidates_auto_create' }
  | { tier: 'auto_create'; decision: 'tied_auto_create'; runnerUp?: { id: string; name: string; score: number } }

export interface BrandResolution { brandId: string; decision: ResolveDecision }
export interface FamilyResolution { familyId: string; decision: ResolveDecision }

export function resolveBrandId(rawBrand: string): Promise<BrandResolution>
export function resolveFamilyId(brandId: string, rawModel: string): Promise<FamilyResolution>
```

From scripts/v8.4-brand-canonicalization.ts L160-167 (existing — extracting verbatim):
```typescript
export function slugify(name: string): string {
  return name.toLowerCase().trim().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract `slugify` helper to `src/lib/slug.ts` and rewire Phase 79 script import</name>
  <files>src/lib/slug.ts, scripts/v8.4-brand-canonicalization.ts</files>
  <read_first>
    - scripts/v8.4-brand-canonicalization.ts (lines 160-167 — verbatim source)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-PATTERNS.md § `src/lib/slug.ts` (NEW — extracted helper)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md § Open Question #2 + #3 + #8 (slug collision decision)
    - src/lib/utils.ts (sibling pattern — pure utility exports)
  </read_first>
  <action>
    Create `src/lib/slug.ts` exporting two pure functions per Open Question #8 (planner-chosen path: UUID suffix pre-emptively for resolver auto-create rows — sacrifices URL prettiness for `needs_review=true` rows but eliminates slug collision retry path entirely). Function 1: `slugify(name: string): string` — verbatim copy of L160-167 from `scripts/v8.4-brand-canonicalization.ts` with JSDoc rewritten to drop Phase-79-specific language ("Slug generator shared by Phase 79 backfill script + Phase 80 ingest resolver"). Function 2: `slugifyWithRandomSuffix(name: string): string` — calls `slugify(name)` then appends `-` + a 6-character random suffix from `crypto.randomUUID().slice(0, 6).replace(/-/g, '').slice(0, 6)` (use Node's `node:crypto` module — server-only). Add `import 'server-only'` IF the file uses crypto; otherwise leave plain (slugify is pure-string and safe in client bundles). Decision: keep `slug.ts` client-safe by using `Math.random()` for the suffix is NOT acceptable (collision risk); instead, put `slugifyWithRandomSuffix` BEHIND `'server-only'` import via a separate file `src/lib/slug-server.ts` if needed, OR keep BOTH functions in `src/lib/slug.ts` with `import 'server-only'` at the top (resolver is the only consumer of `slugifyWithRandomSuffix` and is server-only anyway). Pick the single-file `server-only` form for simplicity. Update `scripts/v8.4-brand-canonicalization.ts` to import `slugify` from `@/lib/slug` (verify nothing else changes — function body must produce identical output for the same input as the original inline version). Cite D-80 Discretion / Open Question #8 in the slugifyWithRandomSuffix JSDoc explaining WHY pre-emptive suffix.
  </action>
  <verify>
    <automated>
      npm run lint -- src/lib/slug.ts &&
      grep -c "^export function slugify" src/lib/slug.ts | grep -v '^#' &&
      grep -c "^export function slugifyWithRandomSuffix" src/lib/slug.ts | grep -v '^#' &&
      grep -c "from '@/lib/slug'" scripts/v8.4-brand-canonicalization.ts | grep -v '^#'
    </automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/slug.ts` exists with `import 'server-only'` (required because slugifyWithRandomSuffix uses node:crypto).
    - `slugify('Acme Co!')` returns `'acme-co'` (verbatim Phase 79 behavior — verifiable by a quick `node -e` REPL or import in a test).
    - `slugifyWithRandomSuffix('Acme Co')` returns a string matching `/^acme-co-[a-f0-9]{6}$/`.
    - `scripts/v8.4-brand-canonicalization.ts` imports `slugify` from `@/lib/slug` (no inline definition remains).
    - JSDoc on `slugifyWithRandomSuffix` cites Open Question #8 / Phase 80 D-80 Discretion item.
    - `npm run lint` exits 0 on the new file.
  </acceptance_criteria>
  <done>
    Shared slugify helper extracted; Phase 79 script rewired; slug collision strategy locked at pre-emptive UUID suffix for resolver auto-create per Open Question #8.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED unit test stubs for `catalog-resolver` (10 cases — INGEST-01..04 + empty-model + idempotency)</name>
  <files>tests/unit/data/catalog-resolver.test.ts</files>
  <read_first>
    - tests/unit/getPublicWearPicsForWatch.test.ts (analog — DAL unit test with mocked db.execute per PATTERNS § Analog 4)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-PATTERNS.md § `tests/unit/data/catalog-resolver.test.ts` (NEW)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md § Test Plan unit cases (table rows 1-10)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-VALIDATION.md § Per-Task Verification Map (80-01-01..80-01-08)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-CONTEXT.md § Decisions (D-80-01 clear-gap, D-80-04 log payload fields)
  </read_first>
  <behavior>
    - Test 1 (INGEST-01 / Brand Tier 1 exact): mock `db.execute` first call returns `[{ id: 'brand-uuid', name: 'Hamilton' }]`; assert `result.decision` matches `{ tier: 'exact', decision: 'matched' }` and `result.brandId === 'brand-uuid'`; assert NO `console.log` event fired.
    - Test 2 (INGEST-02 / Brand Tier 2 clear-gap matched): mock Tier 1 returns `[]`, Tier 2 returns `[{ id: 'A', name: 'Hamilton', score: 0.85 }, { id: 'B', name: 'Hamilton Watch', score: 0.62 }]`; gap = 0.23 ≥ 0.1; assert `result.brandId === 'A'`, `result.decision.tier === 'fuzzy'`, `result.decision.score === 0.85`, `result.decision.runnerUp.id === 'B'`; assert console.log called with first arg `'[extract-watch] fuzzy_brand_match'` and second arg whitelist of 8 fields per D-80-04: `input_raw`, `decision: 'matched'`, `matched_id`, `matched_name`, `score`, `runner_up_id`, `runner_up_name`, `runner_up_score`.
    - Test 3 (INGEST-02 / Brand Tier 2 ambiguous → tied_auto_create): mock Tier 2 returns `[{ id: 'A', score: 0.72 }, { id: 'B', score: 0.68 }]`; gap = 0.04 < 0.1; assert falls through to Tier 3 — `result.decision.decision === 'tied_auto_create'`, `result.decision.tier === 'auto_create'`, runnerUp included; assert TWO console.log events fired: first `fuzzy_brand_match` with `decision: 'tied_auto_create'`, second `brand_auto_created`.
    - Test 4 (INGEST-03 / Brand Tier 3 auto-create no candidates): mock Tier 1 returns `[]`, Tier 2 returns `[]`, Tier 3 INSERT returns `[{ id: 'new-uuid', was_created: true }]`; assert `result.brandId === 'new-uuid'`, `result.decision.decision === 'no_candidates_auto_create'`, console.log fired with `'[extract-watch] brand_auto_created'`.
    - Test 5 (INGEST-04 / Family Tier 1 exact): mock first family call returns `[{ id: 'fam-uuid', name: 'Khaki Field' }]`; assert `result.familyId === 'fam-uuid'`, `result.decision.tier === 'exact'`; assert Tier 2/3/4 mocks NEVER consumed (queue length unchanged after the call beyond first dequeue).
    - Test 6 (INGEST-04 / Family Tier 2 alias hit beats fuzzy): mock Tier 1 returns `[]`, Tier 2 (alias `@>`) returns `[{ id: 'fam-uuid', name: 'Brut Datejust' }]`; assert `result.decision.tier === 'alias'`, `result.familyId === 'fam-uuid'`, AND assert Tier 3 fuzzy mock is NEVER consumed (this is the D-80-02 "alias before fuzzy" assertion).
    - Test 7 (INGEST-04 / Family Tier 3 fuzzy): mock Tier 1 + 2 empty, Tier 3 returns `[{ id: 'fam-uuid', name: 'Portugieser', score: 0.78 }]`; assert `result.decision.tier === 'fuzzy'`, `score === 0.78`, console.log fired with `'[extract-watch] fuzzy_family_match'` and payload includes `brand_id` (the scope of the lookup per D-80-04).
    - Test 8 (INGEST-04 / Family Tier 4 auto-create): mock all tiers empty, Tier 4 INSERT returns `[{ id: 'new-fam', was_created: true }]`; assert `console.log` fired with `'[extract-watch] family_auto_created'` and payload includes `brand_id`.
    - Test 9 (Empty model_raw → placeholder): call `resolveFamilyId(brandId, '   ')`; assert effective model becomes `'(unspecified)'` (per RESEARCH § Empty `model_raw` handling recommendation b); assert auto-create flows to Tier 4 with `name = '(unspecified)'`.
    - Test 10 (Re-extract idempotency): call `resolveBrandId('Acme')` twice with the SAME mock sequence (auto-create on first call; on second call, mock Tier 1 returns the now-existing row); assert both calls return the same `brandId` value.
  </behavior>
  <action>
    Create `tests/unit/data/catalog-resolver.test.ts` matching the PATTERNS.md skeleton (L317-346). Use a queue-based `db.execute` mock: `let execQueue: Array<unknown[]> = []` + `vi.mock('@/db', () => ({ db: { execute: vi.fn(() => Promise.resolve(execQueue.shift() ?? [])) } }))`. Spy on `console.log` via `vi.spyOn(console, 'log').mockImplementation(() => {})` in `beforeEach`; reset `execQueue` in `beforeEach`. Each test case enqueues N rows into `execQueue` matching the SQL calls in tier order (Tier 1 SELECT → Tier 2 SELECT → Tier 3 INSERT). Each test ends with `.todo` removed and either `expect(...).toBe(...)` assertions, OR uses `it.todo('...')` for the stub-only state. Per the Wave-0-RED convention from Phase 78/79, KEEP test bodies wired with `.todo` OR with assertions that ALL fail because `src/data/catalog-resolver.ts` does not yet exist (the import will throw `Cannot find module`). Choose the import-fail path: include real `expect` assertions but lock the entire file behind a single top-level dynamic import that fails because the module is missing. Add `// @vitest-environment jsdom` pragma (default — no DB needed). Stub `vi.mock('@/db', ...)` and `vi.mock('@/lib/slug-server', ...)` — wait, slug is in `@/lib/slug` per Task 1. Don't mock slugify in the unit tests; let the real implementation run. The whitelist assertion for log payloads uses `expect(logSpy.mock.calls[0][1]).toEqual(expect.objectContaining({ input_raw: ..., decision: ..., matched_id: ..., ... }))` AND `expect(Object.keys(logSpy.mock.calls[0][1])).toEqual(expect.arrayContaining(['input_raw','decision','matched_id','matched_name','score','runner_up_id','runner_up_name','runner_up_score']))` (verify whitelist exhaustively for T-80-06 mitigation per RESEARCH § Security Domain).
  </action>
  <verify>
    <automated>
      grep -cE "^\s*it\(['\"]" tests/unit/data/catalog-resolver.test.ts | grep -v '^#' &&
      grep -c "fuzzy_brand_match\|fuzzy_family_match\|brand_auto_created\|family_auto_created" tests/unit/data/catalog-resolver.test.ts | grep -v '^#' &&
      grep -c "input_raw\|runner_up_score" tests/unit/data/catalog-resolver.test.ts | grep -v '^#' &&
      grep -c "BRAND_FUZZY_CLEAR_GAP\|tied_auto_create\|no_candidates_auto_create" tests/unit/data/catalog-resolver.test.ts | grep -v '^#' &&
      (npm run test -- catalog-resolver 2>&1 | grep -E "10 (todo|fail|red)" || npm run test -- catalog-resolver 2>&1 | grep -qE "(Cannot find module|fail|red)")
    </automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/data/catalog-resolver.test.ts` exists with `// @vitest-environment jsdom` pragma at line 1.
    - 10 `it(...)` cases in the file (count via grep), each named per the behavior list above.
    - All 4 log event types appear as string literals in the file: `fuzzy_brand_match`, `fuzzy_family_match`, `brand_auto_created`, `family_auto_created`.
    - All 8 D-80-04 payload fields appear in at least one assertion: `input_raw`, `decision`, `matched_id`, `matched_name`, `score`, `runner_up_id`, `runner_up_name`, `runner_up_score` (T-80-06 whitelist mitigation).
    - The `tied_auto_create` and `no_candidates_auto_create` decision discriminators appear in assertions (D-80-01 dual-decision audit).
    - `BRAND_FUZZY_CLEAR_GAP` constant reference appears at least once in the file (test asserts the 0.1 threshold).
    - `npm run test -- catalog-resolver` reports 10 RED/failing cases or `Cannot find module '@/data/catalog-resolver'` (Wave 0 RED state).
    - NEVER asserts on the full Watch object — only the whitelisted payload keys (T-80-06 mitigation surfaced in the test itself).
  </acceptance_criteria>
  <done>
    Resolver unit test file in place with 10 RED stubs; Plan 02 must turn all 10 green without modifying this file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED integration tests for NOT NULL constraint + local-DB resolver behavior</name>
  <files>tests/integration/migrations/80-not-null-constraint.test.ts, tests/integration/data/catalog-resolver-against-local-db.test.ts</files>
  <read_first>
    - tests/integration/migrations/78-gin-index.test.ts (full file — direct template per PATTERNS § Analog G)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md § Test Plan integration tests (L478-517)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-PATTERNS.md § Pattern G (vitest integration test gated on DATABASE_URL with node env)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-VALIDATION.md § Per-Task Verification Map (80-04-01, 80-04-02) + Manual-Only Verifications
    - Memory `[[vitest-static-node-env]]` — `// @vitest-environment node` required; jsdom-default fails on Vercel prebuild
    - Memory `[[drizzle-supabase-db-mismatch]]` — `prepare: false` for Supabase pooler
  </read_first>
  <action>
    Create TWO test files. (a) `tests/integration/migrations/80-not-null-constraint.test.ts` — top of file: `// @vitest-environment node` pragma, then `import postgres from 'postgres'`, `import { describe, it, expect, beforeAll, afterAll } from 'vitest'`. Use `const maybe = process.env.DATABASE_URL ? describe : describe.skip` (skip when DATABASE_URL absent — keeps CI / jsdom-default green). Open connection in beforeAll with `postgres(connStr, { max: 1, prepare: false })`; close in afterAll. THREE test cases: (1) `'brand_id is NOT NULL in information_schema (CANON-01)'` — SELECT `is_nullable` from information_schema.columns where table_schema='public' AND table_name='watches_catalog' AND column_name='brand_id'; expect 'NO'; (2) `'family_id is NOT NULL in information_schema (CANON-02)'` — same shape for `family_id`; (3) `'INSERT with brand_id=NULL raises 23502 (CANON-01 enforcement)'` — `await expect(sql\`INSERT INTO watches_catalog (brand, model, source, brand_id, family_id) VALUES ('Test', 'Test', 'user_promoted', NULL, NULL)\`).rejects.toMatchObject({ code: '23502' })`. Plan 04 will green these.
    (b) `tests/integration/data/catalog-resolver-against-local-db.test.ts` — same pragma + DATABASE_URL gate. Four test cases per RESEARCH § L515-517 + PATTERNS § L394-402: (1) `'Brand Tier 1 (exact match against seeded Hamilton brand) — INGEST-01'` — call `resolveBrandId('Hamilton')`; assert returned `brandId` equals the SELECT result for `WHERE name_normalized = 'hamilton'`; assert `decision.tier === 'exact'`; (2) `'Brand Tier 2 (fuzzy clear-gap on Hamilon typo) — INGEST-02'` — call `resolveBrandId('Hamilon')`; assert `decision.tier === 'fuzzy'` and `decision.score > 0.6` and the returned id matches Hamilton's id; (3) `'Family Tier 2 (alias hit on Brut Date → Brut Datejust) — INGEST-04'` — in beforeAll, conditionally seed the alias if absent (`UPDATE watch_families SET aliases = aliases || ARRAY['brut date']::text[] WHERE name_normalized = 'brut datejust' AND brand_id = (SELECT id FROM brands WHERE name_normalized = 'brut') AND NOT (aliases @> ARRAY['brut date']::text[])`); then `resolveFamilyId('<brut-uuid>', 'Brut Date')`; assert `decision.tier === 'alias'` and returned family name is canonical 'Brut Datejust'; (4) `'Brand Tier 3 (auto-create with needs_review=true) — INGEST-03'` — call `resolveBrandId('Acme Chronograph Co Phase80 Test')` (use a clearly-novel string); assert a new row exists in `brands` WHERE `name = 'Acme Chronograph Co Phase80 Test'` AND `needs_review = true`; afterAll cleanup: `DELETE FROM brands WHERE name = 'Acme Chronograph Co Phase80 Test'` (only safe because there is no FK fan-out yet — guard with a count check). Both files use the `as unknown as Array<{...}>` cast pattern per PATTERNS § Pattern B.
  </action>
  <verify>
    <automated>
      grep -c "@vitest-environment node" tests/integration/migrations/80-not-null-constraint.test.ts | grep -v '^#' &&
      grep -c "@vitest-environment node" tests/integration/data/catalog-resolver-against-local-db.test.ts | grep -v '^#' &&
      grep -c "23502" tests/integration/migrations/80-not-null-constraint.test.ts | grep -v '^#' &&
      grep -c "prepare: false" tests/integration/migrations/80-not-null-constraint.test.ts | grep -v '^#' &&
      grep -c "describe.skip" tests/integration/migrations/80-not-null-constraint.test.ts | grep -v '^#' &&
      grep -c "resolveBrandId\|resolveFamilyId" tests/integration/data/catalog-resolver-against-local-db.test.ts | grep -v '^#' &&
      grep -c "brut date\|Brut Datejust" tests/integration/data/catalog-resolver-against-local-db.test.ts | grep -v '^#'
    </automated>
  </verify>
  <acceptance_criteria>
    - Both files have `// @vitest-environment node` on line 1 (memory `[[vitest-static-node-env]]`).
    - Both files use `postgres(connStr, { max: 1, prepare: false })` connection (Supabase pooler requirement).
    - Both files use `const maybe = process.env.DATABASE_URL ? describe : describe.skip` gate.
    - `80-not-null-constraint.test.ts` has 3 `it(...)` cases covering CANON-01, CANON-02, and the 23502 INSERT assertion.
    - `catalog-resolver-against-local-db.test.ts` has 4 `it(...)` cases covering Tier 1 exact brand, Tier 2 fuzzy clear-gap brand, Tier 2 family alias hit, Tier 3 brand auto-create.
    - Alias seed in `catalog-resolver-against-local-db.test.ts` uses single-element `ARRAY[${value}]::text[]` literal — NOT a `= ANY(${arr})` spread (memory `[[drizzle-sql-any-array-pitfall]]`).
    - The auto-create test in `catalog-resolver-against-local-db.test.ts` cleans up in `afterAll` (DELETE WHERE name = the test fixture string).
    - When DATABASE_URL is set to local Supabase (`127.0.0.1:54322`), `npm run test -- 80-not-null-constraint` and `npm run test -- catalog-resolver-against-local-db` both produce RED output (constraint not yet flipped; resolver module not yet built).
    - When DATABASE_URL is unset, both files describe-skip (CI green).
  </acceptance_criteria>
  <done>
    Both integration test files in place with RED stubs; Plan 03 greens the alias-hit + Tier 1 + Tier 2 cases; Plan 04 greens the CANON-01/02 + 23502 cases.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → @/db mock | Unit tests mock the DB; resolver runs against fake rows — no SQL injection surface in tests, but the mock must mirror the production query order or false-positive greens are possible |
| test → local Supabase | Integration tests open a real connection to `127.0.0.1:54322`; DATABASE_URL gate prevents accidental prod hits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-V1 | Information Disclosure | Unit test payload assertion | mitigate | Tests assert log payload field WHITELIST per T-80-06 — never asserts log contains the full Watch object or request body; per D-80-04 only 8 fields are valid keys for fuzzy events |
| T-80-V2 | Tampering | Integration auto-create test against local DB | mitigate | Test uses an obvious-fixture brand name ('Acme Chronograph Co Phase80 Test') so accidental run against prod doesn't pollute real data; afterAll DELETE guards with explicit `WHERE name = '<exact fixture>'` |
| T-80-V3 | Spoofing | DATABASE_URL gate bypass | mitigate | `describe.skip` when DATABASE_URL absent — CI cannot accidentally hit a DB; jsdom default for unit tests means no postgres lib loaded |
| T-80-V5 | Tampering | Wave-0 stub state | accept | RED state intentional — Plan 02/03/04 are required to green these; checker/verifier confirms transition |
</threat_model>

<verification>
- `npm run lint` exits 0 across modified files (per Phase 79 hygiene).
- `grep -E "^export function (slugify|slugifyWithRandomSuffix)$" src/lib/slug.ts | grep -v '^#' | wc -l` returns 2.
- `npm run test -- catalog-resolver` outputs 10 RED/pending cases (resolver module missing).
- `npm run test -- 80-not-null-constraint` describes-skips OR fails RED with local DATABASE_URL set (constraint not yet flipped).
- `grep -rn "from '@/lib/slug'" scripts/v8.4-brand-canonicalization.ts | grep -v '^#'` shows the Phase 79 script now imports `slugify` from the shared location.
- No new test file imports `@/data/catalog-resolver` via a top-level static import that breaks vitest collection — use either dynamic import inside test bodies OR rely on `vi.mock` resolution gracefully reporting "Cannot find module" (acceptable RED signal).
</verification>

<success_criteria>
- 3 test files written, all RED.
- 1 helper file extracted; Phase 79 script rewired.
- All 10 unit test names match the behavior list (verifiable by grep).
- All 4 D-80-04 event type strings appear in unit tests (whitelist enforced).
- Slug collision strategy locked (D-80 Discretion / Open Question #8 → pre-emptive UUID suffix).
- No production code modified in this plan (resolver + upsert helpers + schema untouched).
</success_criteria>

<output>
After completion, create `.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-01-SUMMARY.md` recording:
- Slug helper extracted to `src/lib/slug.ts` (with `slugifyWithRandomSuffix` for auto-create per Open Question #8).
- 10 RED unit tests + 7 RED integration tests in place.
- Plan 02 contract: green all 10 unit tests by implementing the resolver per the interfaces block.
- Plan 03 contract: green Tier 1/2 brand + family alias integration tests by wiring the resolver into both upsert helpers.
- Plan 04 contract: green CANON-01/02 + 23502 INSERT tests by applying the NOT NULL migration locally.
</output>
