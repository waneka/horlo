---
phase: 80-not-null-constraint-flip-ingest-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 80-02
files_modified:
  - src/data/catalog.ts
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
  - INGEST-04
tags:
  - phase-80
  - dal
  - upsert

must_haves:
  truths:
    - "`upsertCatalogFromExtractedUrl` invokes `resolveBrandId` + `resolveFamilyId` BEFORE the existing INSERT and attaches `brand_id` + `family_id` to the inserted row"
    - "`upsertCatalogFromUserInput` invokes resolver and attaches both FKs (within the existing CTE pattern)"
    - "Neither upsert path adds `brand_id` or `family_id` to its `ON CONFLICT` UPDATE clause — pre-existing FKs are preserved on conflict (D-80 Discretion item iii)"
    - "The `/api/extract-watch` JSON response envelope is UNCHANGED — no new `brandResolution` / `familyResolution` fields (D-80-04 silent contract)"
    - "Plan 01 local-DB integration tests (`Brand Tier 1 against seeded Hamilton`, `Brand Tier 2 fuzzy clear-gap on Hamilon`, `Family Tier 2 alias hit`) are GREEN when DATABASE_URL is set to local Supabase"
    - "Brand Tier 3 auto-create integration test is GREEN (a clean `'Acme Chronograph Co Phase80 Test'` extract produces a new brand row with `needs_review = true`)"
  artifacts:
    - path: "src/data/catalog.ts"
      provides: "Both upsert helpers wired to resolver; brand_id + family_id in INSERT column + VALUES; ON CONFLICT preserves existing FKs"
      contains: "resolveBrandId"
  key_links:
    - from: "src/data/catalog.ts § upsertCatalogFromExtractedUrl"
      to: "src/data/catalog-resolver.ts"
      via: "await resolveBrandId(input.brand) + await resolveFamilyId(brandId, input.model)"
      pattern: "resolveBrandId|resolveFamilyId"
    - from: "src/data/catalog.ts § upsertCatalogFromUserInput"
      to: "src/data/catalog-resolver.ts"
      via: "same call pattern before the CTE INSERT"
      pattern: "resolveBrandId|resolveFamilyId"
    - from: "src/app/api/extract-watch/route.ts"
      to: "src/data/catalog.ts (unchanged interface)"
      via: "existing call sites L226 + L367 unchanged"
      pattern: "upsertCatalogFromExtractedUrl|upsertCatalogFromUserInput"
---

<objective>
Wire the Plan 02 resolver into BOTH upsert helpers in `src/data/catalog.ts`. Pass-through resolved `brand_id` and `family_id` to the existing INSERT shapes WITHOUT touching the route handler, request/response envelope, or any client surface (D-80-04 silent contract).

Purpose: Close the canonical-drift loop on the INGEST side BEFORE the NOT NULL constraint flips. Per D-80-03 staged deploy, this code ships to prod FIRST, then a manual extract proves both FKs land, then Plan 04's migration runs. If only one upsert path is wired, that's the bug D-80-03's soak step catches.
Output: Modified `src/data/catalog.ts` with resolver calls inserted at the top of both upsert helpers and `brand_id` + `family_id` added to both INSERTs. Zero route changes. Zero client changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-CONTEXT.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-PATTERNS.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-02-SUMMARY.md

@src/data/catalog.ts
@src/data/catalog-resolver.ts
@src/app/api/extract-watch/route.ts

<interfaces>
<!-- Existing upsert call sites (route.ts) — UNCHANGED by this plan. -->

src/app/api/extract-watch/route.ts L226 (URL extract branch):
  catalogId = await catalogDAL.upsertCatalogFromExtractedUrl({
    brand: result.data.brand,
    model: result.data.model,
    reference: result.data.reference ?? null,
    ...spec fields
  })

src/app/api/extract-watch/route.ts L367 (structured input branch):
  catalogId = await catalogDAL.upsertCatalogFromUserInput({
    brand: extracted.brand,
    model: extracted.model,
    reference: extracted.reference ?? null,
  })

<!-- Resolver contract (from Plan 02 — already shipped). -->

import { resolveBrandId, resolveFamilyId } from '@/data/catalog-resolver'
const { brandId } = await resolveBrandId(rawBrand)
const { familyId } = await resolveFamilyId(brandId, rawModel)

<!-- INSERT shape changes (additive — column + VALUE in same positions). -->

upsertCatalogFromExtractedUrl INSERT (L198-217):
  + columns: brand_id, family_id (appended to existing column list)
  + values: ${brandId}, ${familyId} (appended in matching positions)
  + ON CONFLICT DO UPDATE SET clause: UNCHANGED — D-80 Discretion iii — pre-existing FKs preserved on conflict

upsertCatalogFromUserInput CTE INSERT (L143-149):
  + columns: brand_id, family_id (appended to existing 4-column list)
  + values: ${brandId}, ${familyId} (appended)
  + DO NOTHING clause unchanged — on conflict, UNION SELECT side returns the pre-existing row's id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire resolver into `upsertCatalogFromExtractedUrl` (URL branch)</name>
  <files>src/data/catalog.ts</files>
  <read_first>
    - src/data/catalog.ts lines 178-244 (the function being modified)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-PATTERNS.md § `src/data/catalog.ts § upsertCatalogFromExtractedUrl *(MODIFIED — additive resolver call + 2 INSERT columns)*`
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md § Re-extract Behavior on `ON CONFLICT` (Discretion item iii — Option (i))
    - src/data/catalog-resolver.ts (the contract being consumed)
  </read_first>
  <action>
    Modify `src/data/catalog.ts § upsertCatalogFromExtractedUrl` (L178-244).
    Step 1 — import: add `import { resolveBrandId, resolveFamilyId } from '@/data/catalog-resolver'` to the top-of-file import section (alongside existing data-layer imports).
    Step 2 — call resolver BEFORE the existing INSERT, after the sanitizers complete (so after the `safeImageUrl`/`safeImageSourceUrl`/`safeStyleTags`/etc. block at L181-188). Add EXACTLY:
      `const { brandId } = await resolveBrandId(input.brand)`
      `const { familyId } = await resolveFamilyId(brandId, input.model)`
    The destructuring intentionally drops `decision` — the route response envelope stays silent per D-80-04. If a future phase needs the decision, the route can call resolver directly; this plan does not surface it.
    Step 3 — extend the INSERT column list (L199-205) by appending `, brand_id, family_id` at the end of the column list (preserve existing 19-column order; do NOT reorder).
    Step 4 — extend the VALUES list (L206-217) by appending `, ${brandId}, ${familyId}` at the end.
    Step 5 — VERIFY the `ON CONFLICT ON CONSTRAINT watches_catalog_natural_key DO UPDATE SET` clause (L218-239) is UNCHANGED. Per RESEARCH § Re-extract Behavior — columns NOT mentioned in `DO UPDATE SET` are AUTOMATICALLY left at their existing values. Adding `brand_id` and `family_id` to the SET clause would violate D-80 Discretion item iii — `DO NOT` add them. The acceptance criteria includes a grep negation to verify nothing was inadvertently added.
    Step 6 — No change to the function signature, no change to the return type, no change to `UrlExtractedCatalogInput` type. The caller (route.ts L226) sees the same async interface; only the database side changes.
    Per D-80-04, the resolver's structured log events fire via `console.log` inside the resolver itself — no additional logging here.
  </action>
  <verify>
    <automated>
      grep -c "^import.*resolveBrandId.*resolveFamilyId\|^import.*resolveFamilyId.*resolveBrandId" src/data/catalog.ts | grep -v '^#' &&
      awk '/^export async function upsertCatalogFromExtractedUrl/,/^}/' src/data/catalog.ts | grep -c "resolveBrandId\|resolveFamilyId" | grep -v '^#' &&
      awk '/^export async function upsertCatalogFromExtractedUrl/,/^}/' src/data/catalog.ts | grep -c "brand_id, family_id\|brand_id,\s*family_id" | grep -v '^#' &&
      awk '/^export async function upsertCatalogFromExtractedUrl/,/^}/' src/data/catalog.ts | grep -c "\${brandId}, \${familyId}\|\${brandId},\s*\${familyId}" | grep -v '^#' &&
      ! awk '/ON CONFLICT ON CONSTRAINT watches_catalog_natural_key DO UPDATE SET/,/RETURNING id/' src/data/catalog.ts | grep -E "brand_id\s*=|family_id\s*=" &&
      npm run lint -- src/data/catalog.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - Import line for `resolveBrandId, resolveFamilyId` from `@/data/catalog-resolver` added at top of file.
    - Two `await` calls (brand THEN family) appear at the top of `upsertCatalogFromExtractedUrl`, AFTER the sanitizer block and BEFORE the INSERT.
    - INSERT column list ends with `..., complications, brand_id, family_id` (additive, no reorder).
    - VALUES list ends with `..., ${toTextArraySql(safeComplications)}, ${brandId}, ${familyId}`.
    - `ON CONFLICT DO UPDATE SET` clause does NOT contain `brand_id =` or `family_id =` (D-80 Discretion iii — pre-existing FKs preserved on conflict).
    - Function signature, return type, and `UrlExtractedCatalogInput` type are UNCHANGED.
    - `npm run lint -- src/data/catalog.ts` exits 0.
    - No changes to `src/app/api/extract-watch/route.ts` (per CONTEXT § Canonical Refs "Phase 80 ships ZERO route changes").
  </acceptance_criteria>
  <done>
    URL extract branch attaches resolved `brand_id` + `family_id` on insert; pre-existing FKs survive conflicts (operator merges from Phase 82 will be safe).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire resolver into `upsertCatalogFromUserInput` (structured input branch)</name>
  <files>src/data/catalog.ts</files>
  <read_first>
    - src/data/catalog.ts lines 138-164 (the CTE-based function being modified)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-PATTERNS.md § `src/data/catalog.ts § upsertCatalogFromUserInput *(MODIFIED)*`
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md § Re-extract Behavior on `ON CONFLICT` — second paragraph on the structured-input branch
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-CONTEXT.md § Claude's Discretion item ii (empty model handling)
    - src/data/catalog-resolver.ts (the contract being consumed)
  </read_first>
  <action>
    Modify `src/data/catalog.ts § upsertCatalogFromUserInput` (L138-164).
    Step 1 — call resolver BEFORE the existing `db.execute(sql\`WITH ins AS ...\`)` block. Add EXACTLY (after the destructuring on L141 `const { brand, model, reference } = input`):
      `const { brandId } = await resolveBrandId(brand)`
      `const { familyId } = await resolveFamilyId(brandId, model)`
    Step 2 — extend the CTE INSERT column list (L145) FROM:
      `INSERT INTO watches_catalog (brand, model, reference, source)`
    TO:
      `INSERT INTO watches_catalog (brand, model, reference, source, brand_id, family_id)`
    Step 3 — extend the VALUES list (L146) FROM:
      `VALUES (${brand}, ${model}, ${reference}, 'user_promoted')`
    TO:
      `VALUES (${brand}, ${model}, ${reference}, 'user_promoted', ${brandId}, ${familyId})`
    Step 4 — VERIFY `ON CONFLICT ON CONSTRAINT watches_catalog_natural_key DO NOTHING` (L147) is UNCHANGED. On conflict, the CTE returns 0 rows from `ins`; the `UNION ALL SELECT id FROM watches_catalog WHERE brand_normalized = ...` side returns the pre-existing row's id. Since the pre-existing row's `brand_id` + `family_id` columns are not touched (DO NOTHING), Discretion item iii is satisfied automatically.
    Step 5 — VERIFY no change to the UNION SELECT side (L150-160): the WHERE-clause matches on natural-key normalized fields, NOT on `brand_id`/`family_id`. The id returned is the pre-existing row's id; downstream callers see the same data regardless of whether the resolver returned a different/fresher FK.
    Step 6 — Open Question #6 verification: when resolver returns FRESH `brandId`/`familyId` that DIFFER from the existing row's FKs (because an operator merged in `/admin/brands` between extracts), the existing row WINS because `DO NOTHING + UNION SELECT pre-existing` doesn't update the FKs. The function returns the pre-existing id. The UNION SELECT side does not need a brand_id/family_id filter — natural-key normalization is sufficient and the CONFLICT semantic preserves the existing FKs.
    Empty-model behavior (Discretion ii): the resolver handles empty `model` internally by routing to `(unspecified)` family per Plan 02 Task 2. This function does NOT need a separate empty-gate; route handler already gates `if (extracted.brand && extracted.model)` BEFORE calling here (route.ts L366). If model is bare whitespace and somehow reaches this function, resolver auto-creates `(unspecified)` family on the resolved brand. Acceptable per "user flow never blocks".
  </action>
  <verify>
    <automated>
      awk '/^export async function upsertCatalogFromUserInput/,/^}/' src/data/catalog.ts | grep -c "resolveBrandId\|resolveFamilyId" | grep -v '^#' &&
      awk '/^export async function upsertCatalogFromUserInput/,/^}/' src/data/catalog.ts | grep -c "brand_id, family_id\|brand_id,\s*family_id" | grep -v '^#' &&
      awk '/^export async function upsertCatalogFromUserInput/,/^}/' src/data/catalog.ts | grep -c "\${brandId}, \${familyId}\|\${brandId},\s*\${familyId}" | grep -v '^#' &&
      awk '/^export async function upsertCatalogFromUserInput/,/^}/' src/data/catalog.ts | grep -c "ON CONFLICT ON CONSTRAINT watches_catalog_natural_key DO NOTHING" | grep -v '^#' &&
      npm run lint -- src/data/catalog.ts &&
      npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - Two `await` resolver calls appear at the top of `upsertCatalogFromUserInput`, AFTER the destructuring and BEFORE the `db.execute` CTE call.
    - CTE INSERT column list reads `(brand, model, reference, source, brand_id, family_id)` (6 columns, additive).
    - VALUES list reads `(${brand}, ${model}, ${reference}, 'user_promoted', ${brandId}, ${familyId})` (6 values, matching positions).
    - `ON CONFLICT ON CONSTRAINT watches_catalog_natural_key DO NOTHING` clause UNCHANGED (no DO UPDATE introduced).
    - UNION SELECT side (L150-160) UNCHANGED — natural-key WHERE preserved.
    - `npm run build` succeeds with no new type errors (baseline TS errors per memory `[[baseline-not-green-build-is-gate]]` may remain).
    - Function signature and return type UNCHANGED.
  </acceptance_criteria>
  <done>
    Structured input branch attaches resolved `brand_id` + `family_id` on insert; on natural-key conflict, pre-existing FKs survive (operator merges safe). Both upsert paths now write FKs — the prerequisite for Plan 04's NOT NULL flip is met.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Plan 01 local-DB integration tests turn GREEN against local Supabase</name>
  <files>(no file modifications — verification-only run; local DB state read/seeded)</files>
  <read_first>
    - tests/integration/data/catalog-resolver-against-local-db.test.ts (the 4-case test from Plan 01 Task 3)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md § Local-First Verification Recipe Step 0 (Brut alias precondition)
    - .planning/STATE.md (confirm Phase 79 `--apply` against local complete — alias rows populated)
    - Memory `[[local-first-dev]]` — verification gate is non-optional
    - CLAUDE.md § Local-First Development
  </read_first>
  <action>
    Confirm the local Supabase has the alias precondition for the Brut Date test (RESEARCH § L539-553). Run the conditional alias seed against local (idempotent — `NOT (aliases @> ARRAY['brut date']::text[])` guard):
    ```
    psql $DATABASE_URL -c "UPDATE watch_families SET aliases = aliases || ARRAY['brut date']::text[] WHERE name_normalized = 'brut datejust' AND brand_id = (SELECT id FROM brands WHERE name_normalized = 'brut') AND NOT (aliases @> ARRAY['brut date']::text[]);"
    ```
    (If no `brut`/`brut datejust` rows exist locally, skip this seed — Test 6 in Plan 01 already wraps the seed in a `beforeAll` idempotent guard and the test will degrade gracefully via the `NOT (aliases @>)` check.)
    Run the local-DB integration tests with DATABASE_URL set to local Supabase:
    ```
    DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres npm run test -- catalog-resolver-against-local-db
    ```
    Expected: all 4 tests GREEN.
    - Tier 1: `resolveBrandId('Hamilton')` returns the seeded Hamilton brand id (decision.tier === 'exact').
    - Tier 2: `resolveBrandId('Hamilon')` returns the Hamilton brand id with `decision.tier === 'fuzzy'` and `decision.score > 0.6`.
    - Family Tier 2 alias: `resolveFamilyId(<brut-id>, 'Brut Date')` returns the canonical Brut Datejust family.
    - Tier 3 brand auto-create: `resolveBrandId('Acme Chronograph Co Phase80 Test')` creates a new brand row with `needs_review = true`; afterAll cleanup removes the row.
    If a test fails: read the resolver output and the test failure carefully — most likely failure modes are (a) `public.f_unaccent` not resolving on local (verify `\df extensions.unaccent` and quick-260623-uua migration applied locally), (b) the `brut`/`brut datejust` rows missing locally (re-run Phase 79 `--apply` against local), (c) `xmax = 0 AS was_created` column-cast issue in postgres.js mode (cast via `as unknown as Array<...>` then access). Do NOT modify the resolver to make tests green — fix the test fixture or the local DB state. The resolver contract is locked by Plan 02 unit tests.
    Verify nothing else broke: `npm run test` full suite green (excluding any pre-existing baseline failures per memory `[[baseline-not-green-build-is-gate]]`). Verify `npm run build` exits 0.
  </action>
  <verify>
    <automated>
      DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres npm run test -- catalog-resolver-against-local-db 2>&1 | tail -20 &&
      npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - All 4 local-DB integration tests PASS when DATABASE_URL points at local Supabase (`127.0.0.1:54322`).
    - The auto-create test's afterAll cleanup successfully deletes the test brand row (`SELECT count(*) FROM brands WHERE name = 'Acme Chronograph Co Phase80 Test'` returns 0 after the test).
    - No new brand rows in local `brands` other than the test fixture during the test run; the test fixture is fully cleaned up.
    - `npm run build` exits 0.
    - `npm run lint` exits 0 on modified files.
    - If a test is unexpectedly RED (e.g. local DB missing brut/hamilton rows), DOCUMENT the prerequisite in 80-03-SUMMARY.md and DO NOT modify the resolver. The Plan 02 unit tests already prove the resolver behavior in isolation — local-DB tests verify it against real data.
  </acceptance_criteria>
  <done>
    Plan 01 local-DB integration tests are GREEN. Resolver behavior verified end-to-end against the seeded local catalog. The phase is ready for the constraint flip (Plan 04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route.ts → catalog.ts upsert helpers | The route has already authenticated the caller (`getCurrentUser()` gate at route.ts L141-148); upsert helpers run under service-role; resolver inherits service-role from upstream call site |
| upsert helper → resolver | In-process call; no new boundary; resolver's SQL injection / race-safety / log disclosure mitigations apply (see Plan 02 threat model) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-08 | Tampering | Re-extract conflict path overwriting operator-merged FKs | mitigate | `upsertCatalogFromExtractedUrl` DO UPDATE SET clause does NOT include `brand_id` or `family_id` (D-80 Discretion iii); `upsertCatalogFromUserInput` uses DO NOTHING + UNION SELECT — both paths preserve existing FKs on conflict. Operator merges in /admin/brands (Phase 82) survive subsequent re-extracts |
| T-80-09 | Availability | Resolver failure (DB down, fuzzy query timeout) bubbles up through upsert and the route's catch wraps it as non-fatal `catalogIdError` | accept | Existing route try/catch at L222-225 + L364-365 already wraps upsert errors as non-fatal; resolver throws bubble up to the same catch; the route still returns 200 with `catalogIdError` populated and the user flow continues (per D-12 catalog-non-fatal contract) |
| T-80-10 | Information Disclosure | Adding `brand_id`/`family_id` to the response envelope | mitigate | Plan does NOT modify the route's response envelope; D-80-04 contract verified; future telemetry-in-response is a deferred phase concern |
</threat_model>

<verification>
- `npm run test -- catalog-resolver` (unit tests from Plan 01) still GREEN after this plan (10/10).
- `npm run test -- catalog-resolver-against-local-db` GREEN with DATABASE_URL=local (4/4).
- `npm run build` exits 0.
- `npm run lint -- src/data/catalog.ts` exits 0.
- `git diff src/app/api/extract-watch/route.ts` shows zero changes (route untouched per Phase 80 contract).
- `git diff src/components/` shows zero changes (D-80-04 silent client surface).
- Grep gate: `! awk '/ON CONFLICT ON CONSTRAINT watches_catalog_natural_key DO UPDATE SET/,/RETURNING id/' src/data/catalog.ts | grep -E "brand_id\s*=|family_id\s*="` (proves Discretion iii — FKs not in SET clause).
- A quick `npm run dev` walkthrough with structured-input `{ brand: "Hamilton", model: "Khaki Field" }` (Local-First gate; see Plan 05) shows the inserted catalog row has both FKs populated; verifiable by `SELECT brand_id, family_id FROM watches_catalog WHERE brand = 'Hamilton' AND model = 'Khaki Field' ORDER BY created_at DESC LIMIT 1`. Full Local-First Verification Recipe lives in Plan 05.
</verification>

<success_criteria>
- Both upsert helpers call resolver and write FKs.
- Both ON CONFLICT paths preserve existing FKs (Discretion iii).
- 4 local-DB integration tests GREEN.
- Zero route + client changes.
- Plan 04 (NOT NULL flip) safe to proceed: every new catalog row now lands with non-NULL FKs.
</success_criteria>

<output>
After completion, create `.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-03-SUMMARY.md` recording:
- Resolver wired into BOTH upsert helpers.
- Discretion iii verified: ON CONFLICT preserves existing FKs.
- Plan 01 local-DB integration tests GREEN (or any unexpected RED documented with cause).
- Plan 04 contract: schema.ts notNull() + hand-written SQL migration; constraint test from Plan 01 must turn GREEN.
- Open Question #6 closed: structured-input branch's DO NOTHING + UNION SELECT pattern correctly preserves existing FKs when operator merges in Phase 82.
</output>
