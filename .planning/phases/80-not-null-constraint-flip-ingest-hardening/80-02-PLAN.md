---
phase: 80-not-null-constraint-flip-ingest-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 80-01
files_modified:
  - src/data/catalog-resolver.ts
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
  - INGEST-04
tags:
  - phase-80
  - resolver
  - dal

must_haves:
  truths:
    - "`resolveBrandId(rawBrand)` returns the existing brand id when an exact `name_normalized` match exists (INGEST-01)"
    - "`resolveBrandId(rawBrand)` returns a fuzzy-matched brand id when `word_similarity >= 0.6` AND top beats runner-up by `>= 0.1` (D-80-01 clear-gap rule, INGEST-02)"
    - "`resolveBrandId(rawBrand)` falls through to auto-create with `decision: 'tied_auto_create'` when fuzzy candidates exist but the clear-gap fails (D-80-01 ambiguous fall-through)"
    - "`resolveBrandId(rawBrand)` atomically auto-creates with `needs_review = true` when no fuzzy candidates clear 0.6 (INGEST-03)"
    - "`resolveFamilyId(brandId, rawModel)` runs exact → alias → fuzzy → auto-create order (D-80-02, INGEST-04); alias tier hits Brut Date → Brut Datejust BEFORE fuzzy is consulted"
    - "All 4 structured `console.log` event types fire with the exact whitelist of payload fields per D-80-04; no full Watch object or request body ever logged (T-80-06 mitigation)"
    - "Empty `model_raw` (trim === '') routes to a `(unspecified)` placeholder family (Discretion item ii, recommendation b)"
    - "Atomic auto-create uses `ON CONFLICT ON CONSTRAINT brands_name_normalized_unique DO UPDATE SET needs_review = brands.needs_review RETURNING id, (xmax = 0) AS was_created` (race-safe per Pattern Analog 4)"
    - "All 10 unit tests from Plan 01 are GREEN after this plan completes"
  artifacts:
    - path: "src/data/catalog-resolver.ts"
      provides: "Pure-async resolver helpers; `resolveBrandId`, `resolveFamilyId` exports + ResolveDecision type + 3 module constants"
      contains: "resolveBrandId"
      min_lines: 150
  key_links:
    - from: "src/data/catalog-resolver.ts"
      to: "src/db (Drizzle client) + brands + watch_families tables"
      via: "db.execute<{...}>(sql`...`) raw SQL"
      pattern: "from '@/db'"
    - from: "src/data/catalog-resolver.ts"
      to: "src/lib/slug"
      via: "slugifyWithRandomSuffix import for atomic auto-create"
      pattern: "from '@/lib/slug'"
    - from: "src/data/catalog-resolver.ts"
      to: "structured logs (Vercel)"
      via: "console.log('[extract-watch] <event_type>', payload)"
      pattern: "\\[extract-watch\\] (fuzzy_brand_match|fuzzy_family_match|brand_auto_created|family_auto_created)"
---

<objective>
Implement `src/data/catalog-resolver.ts` so all 10 RED unit tests from Plan 01 turn GREEN. The resolver is the single source of truth for D-80-01 (clear-gap fuzzy), D-80-02 (4-tier family lookup), and D-80-04 (structured log events) — Plans 03/04/05 consume this contract without modifying it.

Purpose: Land the resolver in isolation BEFORE wiring it into the route's upsert helpers. Plan 02 stays narrow (one new file, no integration touches) so test-driven green is observable per-task.
Output: Single new file at `src/data/catalog-resolver.ts` exporting `resolveBrandId` + `resolveFamilyId` + `ResolveDecision` + 3 constants. 10 unit tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-CONTEXT.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-PATTERNS.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-01-SUMMARY.md

@src/data/catalog.ts
@scripts/v8.4-brand-canonicalization.ts

<interfaces>
<!-- Existing tables the resolver reads/writes (from src/db/schema.ts L518-563). -->

brands table (post-Phase-78):
  id uuid PK
  name text NOT NULL
  name_normalized text GENERATED AS (lower(trim(name))) STORED
  slug text NOT NULL UNIQUE
  needs_review boolean NOT NULL DEFAULT false
  CONSTRAINT brands_name_normalized_unique UNIQUE (name_normalized)
  CONSTRAINT brands_slug_unique UNIQUE (slug)

watch_families table (post-Phase-78):
  id uuid PK
  brand_id uuid NOT NULL REFERENCES brands(id)
  name text NOT NULL
  name_normalized text GENERATED AS (lower(trim(name))) STORED
  slug text NULLABLE
  aliases text[] NOT NULL DEFAULT '{}'
  needs_review boolean NOT NULL DEFAULT false
  CONSTRAINT watch_families_brand_name_unique UNIQUE (brand_id, name_normalized)
  INDEX watch_families_aliases_gin_idx USING gin (aliases array_ops)

<!-- Module constants (already named in PATTERNS § L185-187). -->

export const BRAND_FUZZY_MIN_SCORE = 0.6
export const BRAND_FUZZY_CLEAR_GAP = 0.1
export const FAMILY_FUZZY_MIN_SCORE = 0.6
export const UNSPECIFIED_FAMILY_NAME = '(unspecified)'

<!-- Exports the resolver must produce (Plan 01 unit tests assert against these). -->

export type ResolveDecision =
  | { tier: 'exact'; decision: 'matched' }
  | { tier: 'alias'; decision: 'matched' }
  | { tier: 'fuzzy'; decision: 'matched'; score: number; runnerUp?: { id: string; name: string; score: number } }
  | { tier: 'auto_create'; decision: 'no_candidates_auto_create' }
  | { tier: 'auto_create'; decision: 'tied_auto_create'; runnerUp?: { id: string; name: string; score: number } }

export interface BrandResolution { brandId: string; decision: ResolveDecision }
export interface FamilyResolution { familyId: string; decision: ResolveDecision }

export async function resolveBrandId(rawBrand: string): Promise<BrandResolution>
export async function resolveFamilyId(brandId: string, rawModel: string): Promise<FamilyResolution>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `resolveBrandId` (3-tier with D-80-01 clear-gap)</name>
  <files>src/data/catalog-resolver.ts</files>
  <read_first>
    - tests/unit/data/catalog-resolver.test.ts (the contract — must green tests 1-4 + 10 after this task)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-PATTERNS.md § Analogs 2 (exact SELECT), 3 (fuzzy `word_similarity`), 4 (atomic auto-create), 6 (structured log emission)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md § Resolver SQL Shapes (Brand Tier 1, 2, 3) + Atomic Auto-Create Pattern
    - src/data/catalog.ts L1-11 (server-only + imports), L162-163 (db.execute cast pattern), L549-562 (word_similarity + public.f_unaccent usage)
    - Memory `[[pg-trgm-word-similarity-for-brand-typos]]` — `word_similarity` NOT `similarity`
    - Memory `[[drizzle-sql-any-array-pitfall]]` — verify NO `= ANY(${arr})` spread used
  </read_first>
  <action>
    Create `src/data/catalog-resolver.ts`. Top of file: `import 'server-only'`, `import { db } from '@/db'`, `import { sql } from 'drizzle-orm'`, `import { slugifyWithRandomSuffix } from '@/lib/slug'`. Export module constants `BRAND_FUZZY_MIN_SCORE = 0.6`, `BRAND_FUZZY_CLEAR_GAP = 0.1`, `FAMILY_FUZZY_MIN_SCORE = 0.6`, `UNSPECIFIED_FAMILY_NAME = '(unspecified)'`. Export `ResolveDecision`, `BrandResolution`, `FamilyResolution` types per the interfaces block. Implement `resolveBrandId(rawBrand: string): Promise<BrandResolution>`:
    Step A — Tier 1 exact: `db.execute<{ id: string; name: string }>(sql\`SELECT id, name FROM brands WHERE name_normalized = lower(trim(${rawBrand})) LIMIT 1\`)`; cast via `as unknown as Array<{...}>` (Pattern B); if a row exists, return `{ brandId, decision: { tier: 'exact', decision: 'matched' } }` with NO console.log (D-80-04 only emits on fuzzy + auto-create).
    Step B — Tier 2 fuzzy: `db.execute<{ id: string; name: string; score: number }>(sql\`SELECT id, name, word_similarity(lower(public.f_unaccent(${rawBrand})), lower(public.f_unaccent(name_normalized))) AS score FROM brands WHERE word_similarity(lower(public.f_unaccent(${rawBrand})), lower(public.f_unaccent(name_normalized))) >= ${BRAND_FUZZY_MIN_SCORE} ORDER BY score DESC LIMIT 2\`)`. Apply Open Question #4 (diacritic folding via `public.f_unaccent`) per recommendation. Cast and inspect: if 0 rows → fall through to Step C with `decision: 'no_candidates_auto_create'`. If 1 row → return matched immediately with NO runnerUp. If 2 rows AND `top.score - runnerUp.score >= BRAND_FUZZY_CLEAR_GAP` → emit `console.log('[extract-watch] fuzzy_brand_match', { input_raw: rawBrand, decision: 'matched', matched_id: top.id, matched_name: top.name, score: top.score, runner_up_id: runnerUp.id, runner_up_name: runnerUp.name, runner_up_score: runnerUp.score })` and return matched. If 2 rows AND gap < 0.1 (ambiguous tie) → emit `console.log('[extract-watch] fuzzy_brand_match', { ..., decision: 'tied_auto_create', runner_up_id, runner_up_name, runner_up_score })` then fall through to Step C with `decision: 'tied_auto_create'` and `runnerUp` preserved.
    Step C — Tier 3 atomic auto-create: `db.execute<{ id: string; was_created: boolean }>(sql\`INSERT INTO brands (name, slug, needs_review) VALUES (${rawBrand}, ${slugifyWithRandomSuffix(rawBrand)}, true) ON CONFLICT ON CONSTRAINT brands_name_normalized_unique DO UPDATE SET needs_review = brands.needs_review RETURNING id, (xmax = 0) AS was_created\`)`. If `was_created === true` emit `console.log('[extract-watch] brand_auto_created', { input_raw: rawBrand, decision: <'tied_auto_create' | 'no_candidates_auto_create'>, matched_id: row.id, matched_name: rawBrand, runner_up_id: runnerUp?.id ?? null, runner_up_name: runnerUp?.name ?? null, runner_up_score: runnerUp?.score ?? null })` (T-80-06 whitelist). If `was_created === false` (race lost — another transaction inserted first), do NOT emit the auto-create event (per D-80-04 semantics — log only ACTUAL insertions); the returned id is the same canonical brand. Per Open Question #3 / #8, Plan 01 chose pre-emptive UUID suffix via `slugifyWithRandomSuffix` — eliminates slug collision retry path entirely (Open Question #3 closed via Question #8 path).
    All SQL uses parameterized `sql\`... ${value} ...\`` form — never `sql.raw()` with user input (T-80-01 mitigation). All console.log payloads use the EXACT 8-field whitelist per D-80-04 — no full Watch object, no request body (T-80-06).
  </action>
  <verify>
    <automated>
      grep -c "^export async function resolveBrandId" src/data/catalog-resolver.ts | grep -v '^#' &&
      grep -c "BRAND_FUZZY_MIN_SCORE\|BRAND_FUZZY_CLEAR_GAP" src/data/catalog-resolver.ts | grep -v '^#' &&
      grep -c "brands_name_normalized_unique" src/data/catalog-resolver.ts | grep -v '^#' &&
      grep -c "word_similarity" src/data/catalog-resolver.ts | grep -v '^#' &&
      grep -c "public.f_unaccent" src/data/catalog-resolver.ts | grep -v '^#' &&
      grep -c "xmax = 0" src/data/catalog-resolver.ts | grep -v '^#' &&
      grep -c "fuzzy_brand_match\|brand_auto_created" src/data/catalog-resolver.ts | grep -v '^#' &&
      ! grep -q "= ANY(\${" src/data/catalog-resolver.ts &&
      ! grep -q "sql.raw" src/data/catalog-resolver.ts &&
      npm run test -- catalog-resolver -t "Brand Tier"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/data/catalog-resolver.ts` exists with `import 'server-only'` on line 1.
    - All 3 module constants exported with the exact values `0.6`, `0.1`, `0.6` (D-80-01 + D-80-02).
    - SQL uses `sql\`... ${value} ...\`` parameterized binding everywhere (T-80-01); zero `sql.raw` occurrences.
    - Tier 2 query uses `word_similarity` (memory `[[pg-trgm-word-similarity-for-brand-typos]]`) wrapped in `public.f_unaccent` on BOTH sides (Open Question #4 recommended).
    - Tier 3 INSERT uses exact constraint name `brands_name_normalized_unique` (verified in `supabase/migrations/20260510000000_phase34_brands_families.sql` L22-24 and schema.ts L535).
    - Tier 3 RETURNING clause includes `(xmax = 0) AS was_created` (Pattern Analog 4 race-safety signal).
    - No `= ANY(${...})` array spread (memory `[[drizzle-sql-any-array-pitfall]]`).
    - `needs_review = true` literal appears in the auto-create INSERT VALUES (Phase 80 is the FIRST writer of `true` per Specifics + D-79-09).
    - Tests 1, 2, 3, 4 from Plan 01 (brand tier branches + idempotency) all PASS when run with `npm run test -- catalog-resolver -t "Brand Tier"`.
    - Console.log payload includes ALL 8 D-80-04 fields for fuzzy match events; 5 fields for auto-create events (per Plan 01 Test 2 + 4 whitelist assertions).
  </acceptance_criteria>
  <done>
    Brand resolver implemented; brand tier unit tests 1-4 + 10 (idempotency) PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `resolveFamilyId` (4-tier with alias-before-fuzzy + empty-model placeholder)</name>
  <files>src/data/catalog-resolver.ts</files>
  <read_first>
    - tests/unit/data/catalog-resolver.test.ts (tests 5-9 are the contract for this task)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-PATTERNS.md § Analogs 5 (alias containment), 4 (atomic auto-create for family)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md § Resolver SQL Shapes (Family Tier 1, 2, 3, 4) + Empty `model_raw` handling (Discretion ii recommendation b)
    - src/data/catalog-resolver.ts (the file from Task 1 — extend, don't recreate)
    - Memory `[[drizzle-sql-any-array-pitfall]]` — single-element ARRAY literal is SAFE; spread is NOT
  </read_first>
  <action>
    Extend `src/data/catalog-resolver.ts` with `resolveFamilyId(brandId: string, rawModel: string): Promise<FamilyResolution>`.
    Step 0 — Empty-model placeholder (D-80 Discretion ii, recommendation b): if `rawModel.trim() === ''`, set `effectiveModel = UNSPECIFIED_FAMILY_NAME` (`'(unspecified)'`). All subsequent tier queries use `effectiveModel` (which means tier 1/2/3 will likely miss on first encounter and Tier 4 auto-creates the placeholder). DO NOT route to a separate code path — same 4-tier flow with the substituted model.
    Step A — Tier 1 exact (brand-scoped): `db.execute<{ id: string; name: string }>(sql\`SELECT id, name FROM watch_families WHERE brand_id = ${brandId} AND name_normalized = lower(trim(${effectiveModel})) LIMIT 1\`)`. If hit → return `{ familyId, decision: { tier: 'exact', decision: 'matched' } }` with NO log.
    Step B — Tier 2 alias containment (D-80-02 BEFORE fuzzy): `db.execute<{ id: string; name: string }>(sql\`SELECT id, name FROM watch_families WHERE brand_id = ${brandId} AND aliases @> ARRAY[lower(trim(${effectiveModel}))]::text[] ORDER BY created_at ASC LIMIT 1\`)`. Single-element ARRAY literal — NOT a spread (`[[drizzle-sql-any-array-pitfall]]` does NOT apply). If hit → return `{ familyId, decision: { tier: 'alias', decision: 'matched' } }` with NO log (alias hit is operator-curated; not a fuzzy match).
    Step C — Tier 3 fuzzy (brand-scoped, NO clear-gap rule per D-80-02): `db.execute<{ id: string; name: string; score: number }>(sql\`SELECT id, name, word_similarity(lower(public.f_unaccent(${effectiveModel})), lower(public.f_unaccent(name_normalized))) AS score FROM watch_families WHERE brand_id = ${brandId} AND word_similarity(lower(public.f_unaccent(${effectiveModel})), lower(public.f_unaccent(name_normalized))) >= ${FAMILY_FUZZY_MIN_SCORE} ORDER BY score DESC LIMIT 1\`)`. LIMIT 1 (asymmetric vs brand) — top score wins. If hit → emit `console.log('[extract-watch] fuzzy_family_match', { brand_id: brandId, input_raw: rawModel, decision: 'matched', matched_id: row.id, matched_name: row.name, score: row.score, runner_up_id: null, runner_up_name: null, runner_up_score: null })` (8-field whitelist + `brand_id` as scope marker per D-80-04) and return matched.
    Step D — Tier 4 atomic auto-create: `db.execute<{ id: string; was_created: boolean }>(sql\`INSERT INTO watch_families (brand_id, name, needs_review, aliases) VALUES (${brandId}, ${effectiveModel}, true, '{}'::text[]) ON CONFLICT ON CONSTRAINT watch_families_brand_name_unique DO UPDATE SET needs_review = watch_families.needs_review RETURNING id, (xmax = 0) AS was_created\`)`. Emit `console.log('[extract-watch] family_auto_created', { brand_id: brandId, input_raw: rawModel, decision: 'no_candidates_auto_create', matched_id: row.id, matched_name: effectiveModel, runner_up_id: null, runner_up_name: null, runner_up_score: null })` ONLY when `was_created === true`. The `'{}'::text[]` literal is NOT an array spread.
    Constraint name `watch_families_brand_name_unique` verified in `supabase/migrations/20260510000000_phase34_brands_families.sql` L47-50 and schema.ts L561.
  </action>
  <verify>
    <automated>
      grep -c "^export async function resolveFamilyId" src/data/catalog-resolver.ts | grep -v '^#' &&
      grep -c "aliases @> ARRAY\[" src/data/catalog-resolver.ts | grep -v '^#' &&
      grep -c "watch_families_brand_name_unique" src/data/catalog-resolver.ts | grep -v '^#' &&
      grep -c "fuzzy_family_match\|family_auto_created" src/data/catalog-resolver.ts | grep -v '^#' &&
      grep -c "UNSPECIFIED_FAMILY_NAME\|'(unspecified)'" src/data/catalog-resolver.ts | grep -v '^#' &&
      ! grep -q "= ANY(\${" src/data/catalog-resolver.ts &&
      npm run test -- catalog-resolver
    </automated>
  </verify>
  <acceptance_criteria>
    - `resolveFamilyId` exported as async function with signature matching the interfaces block.
    - Tier 2 alias query uses `aliases @> ARRAY[lower(trim(${value}))]::text[]` single-element literal (memory `[[drizzle-sql-any-array-pitfall]]` mitigation).
    - Tier ordering is exact → alias → fuzzy → auto-create (D-80-02) — verifiable by source-order grep: `name_normalized = lower(trim` appears BEFORE `aliases @>` which appears BEFORE `word_similarity(` which appears BEFORE `INSERT INTO watch_families`.
    - Tier 3 fuzzy uses `LIMIT 1` (not LIMIT 2 — asymmetric vs brand per D-80-02).
    - Empty-model handling: `UNSPECIFIED_FAMILY_NAME` constant or literal `'(unspecified)'` appears in the substitution branch.
    - Tier 4 INSERT uses `watch_families_brand_name_unique` constraint and `'{}'::text[]` empty array literal.
    - `family_auto_created` and `fuzzy_family_match` log events include `brand_id` field in payload (D-80-04 family-event scope marker).
    - ALL 10 unit tests from Plan 01 PASS — `npm run test -- catalog-resolver` exits 0 with 10/10 passing.
    - Race-safety: family Tier 4 uses `DO UPDATE SET needs_review = watch_families.needs_review RETURNING id, (xmax = 0)` — same Pattern Analog 4 shape as brand Tier 3.
  </acceptance_criteria>
  <done>
    Family resolver implemented; all 10 unit tests GREEN. Resolver module complete and ready for upsert wire-in (Plan 03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM extraction → resolver input | `rawBrand` + `rawModel` are LLM-extracted strings from public watch URLs; untrusted but length-capped (.max(200)) at the route's Zod gate |
| resolver → Postgres | SQL constructed via Drizzle `sql` template tag with parameterized binding; never string-concatenated |
| resolver → console.log | Server logs emitted to Vercel; whitelisted payload fields only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | resolver SQL queries | mitigate | All `${value}` interpolations inside `sql\`...\`` template tag (Drizzle parameter binding); zero `sql.raw` occurrences; verified by grep `! grep -q "sql.raw" src/data/catalog-resolver.ts` |
| T-80-02 | Denial of Service | Mass auto-create flood | accept | Auth gate already enforced at route boundary (route.ts L141-148); auto-create requires authenticated user; rate-limiting is Vercel/auth-layer concern; `needs_review = true` quarantines rows for Phase 82 operator cleanup |
| T-80-03 | Spoofing | Resolver bypassing route auth | mitigate | `import 'server-only'` boundary — resolver is build-time-rejected if imported from a client component; resolver itself does NOT check auth (correct DAL pattern); route enforces auth above the call site |
| T-80-05 | Availability | Slug collision on auto-create (`brands_slug_unique` 23505) | mitigate | Pre-emptive UUID suffix via `slugifyWithRandomSuffix` (Open Question #3 closed via Question #8 path) — eliminates 23505 retry code path entirely; sacrifices slug prettiness for `needs_review = true` rows (operator renames via Phase 82) |
| T-80-06 | Information Disclosure | Structured log payloads | mitigate | Whitelist exactly 8 fields per fuzzy event + 5 fields per auto-create event per D-80-04; NEVER log full Watch object or request body; Plan 01 Task 2 enforces this via `Object.keys` assertion in unit tests |
| T-80-07 | Tampering | Race on auto-create (concurrent ingests for same novel name) | mitigate | `ON CONFLICT ON CONSTRAINT <name> DO UPDATE SET <col> = <table>.<col> RETURNING id, (xmax = 0)` — Postgres UNIQUE constraint serializes; loser's DO UPDATE returns the same canonical id; both callers receive the same FK (Pattern Analog 4) |
</threat_model>

<verification>
- `npm run test -- catalog-resolver` exits 0 with 10/10 passing.
- `npm run lint -- src/data/catalog-resolver.ts` exits 0.
- `grep -c "import 'server-only'" src/data/catalog-resolver.ts | grep -v '^#'` returns 1.
- `grep -c "sql.raw\|= ANY(\${" src/data/catalog-resolver.ts` returns 0 (T-80-01 + drizzle-array-pitfall mitigations).
- Source-order assertion: `grep -n "name_normalized = lower(trim\|aliases @>\|word_similarity(\|INSERT INTO watch_families" src/data/catalog-resolver.ts` confirms D-80-02 tier ordering inside `resolveFamilyId`.
- No production callers updated yet — Plan 03 wires this in.
</verification>

<success_criteria>
- All 10 unit tests from Plan 01 GREEN.
- One new file (`src/data/catalog-resolver.ts`) — no other src/ files modified in this plan.
- Module exports stable per interfaces block (Plans 03/04/05 import these names verbatim).
- Pattern Analog 4 atomic auto-create shape (ON CONFLICT ON CONSTRAINT + DO UPDATE no-op SET + xmax=0) used for BOTH brand and family.
- D-80-04 log payload whitelist enforced (T-80-06).
</success_criteria>

<output>
After completion, create `.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-02-SUMMARY.md` recording:
- Resolver module shipped with brand + family helpers.
- All 10 Plan 01 unit tests GREEN.
- 4 D-80-04 log event types emitted with whitelisted payloads.
- Plan 03 contract: import `resolveBrandId` + `resolveFamilyId` from `@/data/catalog-resolver`; call BOTH before each INSERT in BOTH upsert helpers; add `brand_id` + `family_id` to INSERT column + VALUES lists; OMIT them from `DO UPDATE SET`.
</output>
