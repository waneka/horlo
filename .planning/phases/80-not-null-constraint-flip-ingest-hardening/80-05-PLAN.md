---
phase: 80-not-null-constraint-flip-ingest-hardening
plan: 05
type: execute
wave: 5
depends_on:
  - 80-04
files_modified:
  - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md
autonomous: false
requirements:
  - CANON-01
  - CANON-02
  - INGEST-01
  - INGEST-02
  - INGEST-03
  - INGEST-04
tags:
  - phase-80
  - post-deploy
  - operator
  - staged-deploy

must_haves:
  truths:
    - "Local-First Verification Recipe is executed: 4 URL/structured extractions against `npm run dev` cover all resolver tier paths (Tier 1 exact, Tier 2 fuzzy clear-gap, Tier 3 auto-create, Tier 2 family alias)"
    - "All 4 local extract verifications succeed — SQL queries confirm `brand_id` + `family_id` populated; structured logs show expected event types in `npm run dev` output"
    - "POST-DEPLOY.md captures the THREE D-80-03 staged-deploy steps as ordered operator instructions (code deploy → manual extract proof → migration push)"
    - "Operator (Tyler) gives explicit go-ahead at the human-action checkpoint before the prod migration push is documented as complete"
    - "After prod migration applies: prod `information_schema` SQL queries confirm `is_nullable = 'NO'` on both columns; 6 sign-off SQL queries (mirroring Phase 79 POST-DEPLOY pattern) all pass"
  artifacts:
    - path: ".planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md"
      provides: "Operator-facing staged deploy runbook with plain-English explanations per D-80 Discretion tone item"
      contains: "supabase db push --linked"
      min_lines: 80
  key_links:
    - from: "80-POST-DEPLOY.md"
      to: "prod Supabase"
      via: "operator runs `supabase db push --linked` after manual extract proof"
      pattern: "supabase db push --linked"
    - from: "Local-First Verification Recipe"
      to: "all 4 resolver tier paths"
      via: "structured-input branch + URL extract branch tested via `npm run dev`"
      pattern: "Tier 1|Tier 2|Tier 3|Tier 4"
---

<objective>
Execute the Local-First Verification Recipe (gate per CLAUDE.md), capture the staged-deploy runbook in operator-facing `80-POST-DEPLOY.md`, and pause for the human-action checkpoint that gates prod migration push.

Purpose: D-80-03 staged deploy is the chosen safety pattern — ingest code ships first, manual extract proves both upsert paths attach FKs on prod, THEN the constraint migration runs. This plan is the bridge between "all tests green locally" and "prod constraint live". The plain-English tone (per D-80 Discretion / CONTEXT § Tone) keeps the runbook readable when read months from now in incident-response context.
Output: Local verification log captured; POST-DEPLOY.md with three ordered steps + 6 sign-off SQL queries; operator checkpoint pauses execution until Tyler explicitly approves the staged sequence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-CONTEXT.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-04-SUMMARY.md

@.planning/phases/79-backfill-migration-display-hydration/79-POST-DEPLOY.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md

@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute the Local-First Verification Recipe (4 extractions + verification SQL)</name>
  <files>.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-05-SUMMARY.md (verification outcomes recorded here)</files>
  <read_first>
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md § Local-First Verification Recipe (L527-625)
    - CLAUDE.md § Local-First Development
    - Memory `[[local-first-dev]]`
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-04-SUMMARY.md (confirm NOT NULL migration applied locally before this step)
  </read_first>
  <action>
    Verify preconditions: `supabase status` shows local running; `psql $DATABASE_URL -c "SELECT is_nullable FROM information_schema.columns WHERE table_name='watches_catalog' AND column_name IN ('brand_id', 'family_id');"` returns both rows as `NO` (Plan 04 applied locally).
    Conditional alias precondition for Test (d): `psql $DATABASE_URL -c "SELECT name, aliases FROM watch_families WHERE array_length(aliases, 1) > 0 LIMIT 5;"`. If empty AND a `brut datejust` row exists, run the idempotent seed from RESEARCH § L547-553. If no brut data locally, skip Test (d) and document the gap.
    Start dev server: `npm run dev` (background). Confirm http://localhost:3000 reachable.
    For each of the 4 tests, sign in as `viewer@horlo.test` / `password123` (or use a curl with auth cookies). Record per-test: (a) what you submitted, (b) the structured log events that appeared in the dev server console, (c) the verification SQL output.
    
    **Test (a) — Brand exact match (Tier 1):**
    Submit via URL extract: a Hamilton product URL (the LLM will extract `brand: "Hamilton"`).
    Expected: catalog row created with `brand_id = <existing Hamilton id>`; NO new brand row created; NO `fuzzy_brand_match` log event in dev server output.
    Verify SQL: `SELECT c.id, c.brand_id, b.name AS canonical_brand FROM watches_catalog c JOIN brands b ON b.id = c.brand_id WHERE c.brand = 'Hamilton' ORDER BY c.created_at DESC LIMIT 1;`
    
    **Test (b) — Brand fuzzy clear-gap match (Tier 2):**
    Submit via structured input branch: POST to the structured-input path with `{ brand: "Hamilon", model: "Khaki Field" }`. (Find the route signature in `route.ts` ~L350 or use AddWatchFlow's structured panel.)
    Expected: resolver scores `Hamilon` vs `hamilton` (high `word_similarity`); top score wins clear-gap → existing Hamilton id attached. Dev server logs `[extract-watch] fuzzy_brand_match { ..., decision: 'matched', input_raw: 'Hamilon', matched_name: 'Hamilton', score: > 0.6, ... }`.
    Verify SQL: same as Test (a) but the WHERE clause is `c.brand = 'Hamilon'`. The `brand_id` column should match the canonical Hamilton id (not a new row).
    
    **Test (c) — Brand auto-create (Tier 3):**
    Submit structured input: `{ brand: "Acme Chronograph Co Phase80 Test", model: "Model X Phase80 Test" }`.
    Expected: brand auto-creates with `needs_review = true`; family auto-creates with `needs_review = true`. TWO log events: `[extract-watch] brand_auto_created { ..., decision: 'no_candidates_auto_create', matched_name: 'Acme Chronograph Co Phase80 Test', ... }` and `[extract-watch] family_auto_created { ..., brand_id: '<new>', matched_name: 'Model X Phase80 Test', ... }`.
    Verify SQL:
    ```
    SELECT name, needs_review, slug FROM brands WHERE name = 'Acme Chronograph Co Phase80 Test';
    -- expect: 1 row, needs_review=true, slug matching /acme-chronograph-co-phase80-test-[a-f0-9]{6}/
    SELECT name, needs_review FROM watch_families WHERE name = 'Model X Phase80 Test' AND brand_id = (SELECT id FROM brands WHERE name = 'Acme Chronograph Co Phase80 Test');
    -- expect: 1 row, needs_review=true
    ```
    Clean up after verification: `DELETE FROM watches WHERE brand = 'Acme Chronograph Co Phase80 Test';` (if any user watch was created), `DELETE FROM watches_catalog WHERE brand = 'Acme Chronograph Co Phase80 Test';`, `DELETE FROM watch_families WHERE name = 'Model X Phase80 Test';`, `DELETE FROM brands WHERE name = 'Acme Chronograph Co Phase80 Test';`. Guard each DELETE with the exact-match WHERE.
    
    **Test (d) — Family alias hit (Tier 2 family):**
    Only run if local has the brut precondition seeded. Structured input: `{ brand: "Brut", model: "Brut Date" }`.
    Expected: brand resolves Tier 1 to canonical Brut id; family hits alias path → resolves to canonical `Brut Datejust` family. NO new family row; NO `fuzzy_family_match` log event (alias hit is silent — only fuzzy + auto-create log).
    Verify SQL: `SELECT c.family_id, f.name AS canonical_family FROM watches_catalog c JOIN watch_families f ON f.id = c.family_id WHERE c.brand = 'Brut' AND c.model = 'Brut Date' ORDER BY c.created_at DESC LIMIT 1;` — `f.name` should be `'Brut Datejust'`.
    
    Record EACH test's outcome (pass / fail / skipped + reason) in 80-05-SUMMARY.md. If ANY test fails (not skipped), STOP — do not proceed to Task 2. The failure is a Plan 02 or Plan 03 regression that must be fixed before the prod push runbook is written. If Tests (a) (b) (c) pass and (d) is skipped due to missing local seed, that's acceptable but flag in summary.
  </action>
  <verify>
    <automated>
      psql $DATABASE_URL -t -c "SELECT is_nullable FROM information_schema.columns WHERE table_name='watches_catalog' AND column_name='brand_id';" | tr -d ' ' | grep -x "NO" &&
      psql $DATABASE_URL -t -c "SELECT count(*) FROM brands WHERE name = 'Acme Chronograph Co Phase80 Test';" | tr -d ' ' | grep -x "0"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 4 test outcomes recorded in 80-05-SUMMARY.md with test name, submitted input, verification SQL output, and dev-server log lines for events.
    - Tests (a), (b), (c) PASS (the bare-minimum gate before prod push).
    - Test (d) PASS or documented-SKIP with reason ("local catalog missing brut/brut datejust rows").
    - Auto-create test fixture cleanup verified (`SELECT count(*) FROM brands WHERE name = '<fixture>';` returns 0 after cleanup).
    - Dev-server console output captured (paste relevant log lines into summary) — confirms D-80-04 event types fire with expected payloads.
  </acceptance_criteria>
  <done>
    Local-First gate satisfied; all resolver tier paths exercised against the seeded local catalog.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author `80-POST-DEPLOY.md` operator runbook (plain-English staged-deploy steps + 6 sign-off SQL queries)</name>
  <files>.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md</files>
  <read_first>
    - .planning/phases/79-backfill-migration-display-hydration/79-POST-DEPLOY.md (most recent precedent)
    - .planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md (alternate template)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-RESEARCH.md § Migration Plan (the migration body shipped in Plan 04) + § Local-First Verification Recipe Step 5 (prod-side sign-off queries)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-CONTEXT.md § Decisions D-80-03 (staged deploy) + § Claude's Discretion "Tone" item (plain English)
  </read_first>
  <action>
    Create `.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md`. Structure:
    
    **Header** (status: pending until operator runs Task 3): phase name, date written, depends-on (Plan 04 complete), goal in plain English ("Get the new resolver code to prod, prove it works on real data, then turn on the database guardrail that prevents the canonical drift from ever creeping back in").
    
    **D-80-03 Staged Deploy — Three ordered steps** (write each step as a separate H3 section with plain-English explanation + exact commands):
    
    Step 1: Deploy the ingest code to prod.
    - Plain English: "This pushes the resolver code + the upsert helpers that now write `brand_id` and `family_id` to every new catalog row. The database itself still allows NULL in those columns, so old code paths aren't affected. The risk if we skip this step: the migration would land while old code is still running, and the next URL extract would crash with a 23502 NULL violation."
    - Commands: `git push` (Vercel auto-deploys); operator confirms deploy success in Vercel dashboard.
    - Verification: After the deploy lands, watch Vercel logs for ~5 minutes — should be quiet (no extracts during this window are expected to fail).
    
    Step 2: Manual extract proof (Tyler runs ONE extract on prod and verifies via Supabase SQL editor).
    - Plain English: "Before we flip the NOT NULL switch, we run ONE real extract on prod and look at the database. If both `brand_id` and `family_id` columns are filled in on the new catalog row, the resolver wire-up is solid in both upsert paths. If either is NULL, STOP — there's a wire-up bug and the migration would brick the next ingest. The cheapest test: pick a Hamilton URL — we know Hamilton is in the catalog, so we expect the exact-match path to fire and attach the canonical Hamilton brand id."
    - Commands: Tyler signs into prod, runs the AddWatchFlow URL extract on the Hamilton URL `https://www.hamiltonwatch.com/en-us/khaki-field-mechanical-h69439931.html`.
    - Verification SQL (run in Supabase SQL editor):
      ```sql
      SELECT c.id, c.brand, c.model, c.brand_id, c.family_id, b.name AS canonical_brand, f.name AS canonical_family
      FROM watches_catalog c
      LEFT JOIN brands b ON b.id = c.brand_id
      LEFT JOIN watch_families f ON f.id = c.family_id
      WHERE c.brand = 'Hamilton' AND c.created_at > now() - interval '10 minutes'
      ORDER BY c.created_at DESC LIMIT 1;
      ```
    - Pass condition: `brand_id` AND `family_id` BOTH non-NULL; `canonical_brand = 'Hamilton'`.
    - If FAIL: do not proceed to Step 3; investigate which upsert path didn't fire the resolver.
    
    Step 3: Push the NOT NULL migration to prod.
    - Plain English: "With the resolver proven on real data, the constraint flip is safe — every new row will satisfy it, and Phase 79's backfill already made every existing row satisfy it. The `supabase db push --linked` runs the hand-written SQL migration (the same one Plan 04 ran against local) against prod. The migration's pre-flight assertion will refuse to run if it finds any NULL row (a paranoid double-check — Phase 79 already proved there are none)."
    - Commands: `supabase db push --linked`.
    - Verification SQL (the 6 sign-off queries — paste each into the Supabase SQL editor):
      ```sql
      -- 1. brand_id is NOT NULL
      SELECT is_nullable FROM information_schema.columns
       WHERE table_schema='public' AND table_name='watches_catalog' AND column_name='brand_id';
      -- expect: NO
      
      -- 2. family_id is NOT NULL
      SELECT is_nullable FROM information_schema.columns
       WHERE table_schema='public' AND table_name='watches_catalog' AND column_name='family_id';
      -- expect: NO
      
      -- 3. No NULL rows (sanity)
      SELECT count(*) FROM watches_catalog WHERE brand_id IS NULL OR family_id IS NULL;
      -- expect: 0
      
      -- 4. Hamilton test row from Step 2 still has its FKs
      SELECT brand_id, family_id FROM watches_catalog WHERE brand = 'Hamilton' AND created_at > now() - interval '20 minutes' ORDER BY created_at DESC LIMIT 1;
      -- expect: both non-NULL
      
      -- 5. Auto-create / needs_review queue check (should be empty if no auto-creates happened in Step 2)
      SELECT count(*) FROM brands WHERE needs_review = true;
      -- expect: 0 (or whatever count operator expects)
      
      -- 6. Phase 80 migration recorded in supabase migration ledger
      SELECT version, name FROM supabase_migrations.schema_migrations WHERE version = '20260626000000';
      -- expect: 1 row
      ```
    - If ANY query fails: investigate the failure, do not declare Phase 80 complete. The migration is wrapped in BEGIN/COMMIT so failure rolls back.
    
    **Forward-armor section**: note that Phase 81 (RECO-01..04 + DISP-01/02) consumes the NOT NULL guarantee — recommender JOIN-through paths can drop NULL-defensive code. Phase 82 (`/admin/brands` + `/admin/families`) consumes the `needs_review = true` queue produced by this phase's auto-create path. Carry forward: monitor `SELECT count(*) FROM brands WHERE needs_review = true` over the next 7 days; spike = ingest bug; near-zero = ingest healthy.
    
    **Status table** at top: `## Status` with rows `Step 1: pending`, `Step 2: pending`, `Step 3: pending`. Operator (Tyler) updates these as steps complete in Task 3.
    
    Tone: plain English throughout, no acronym walls. CONTEXT § Discretion Tone item is the contract here.
  </action>
  <verify>
    <automated>
      ls .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md &&
      grep -c "Step 1\|Step 2\|Step 3" .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md | grep -v '^#' &&
      grep -c "supabase db push --linked" .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md | grep -v '^#' &&
      grep -c "CANON-01\|CANON-02" .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md | grep -v '^#' &&
      grep -c "information_schema" .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md | grep -v '^#' &&
      grep -c "needs_review = true\|needs_review=true" .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md | grep -v '^#' &&
      wc -l .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md | awk '{print $1}' | (read lines; [ "$lines" -ge 80 ] && echo "ok")
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at the correct path.
    - All 3 steps documented as separate H3 sections with plain-English explanations.
    - 6 sign-off SQL queries present in Step 3.
    - Plain-English tone (no nested acronym walls; D-80 Discretion Tone item).
    - References Plan 04's migration filename (`20260626000000_phase80_catalog_brand_family_not_null.sql`).
    - Forward-armor section notes Phase 81 + Phase 82 dependencies.
    - Status table at top with 3 `pending` rows.
    - Minimum 80 lines (real runbook, not a stub).
  </acceptance_criteria>
  <done>
    Operator-facing runbook complete; Tyler can read it standalone and execute steps 1-3.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator executes 3-step staged deploy + records outcomes in POST-DEPLOY.md</name>
  <files>.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md (operator updates status rows + pastes SQL results)</files>
  <action>Operator (Tyler) executes the three D-80-03 staged-deploy steps documented in 80-POST-DEPLOY.md and records outcomes inline. See the &lt;how-to-verify&gt; block below for the step-by-step sequence (this checkpoint task pauses the autonomous flow; resumption requires Tyler's explicit signal per &lt;resume-signal&gt; below).</action>
  <verify>
    <automated>psql "$PROD_DATABASE_URL" -t -c "SELECT is_nullable FROM information_schema.columns WHERE table_schema='public' AND table_name='watches_catalog' AND column_name='brand_id';" | tr -d ' ' | grep -x "NO" &amp;&amp; psql "$PROD_DATABASE_URL" -t -c "SELECT count(*) FROM watches_catalog WHERE brand_id IS NULL OR family_id IS NULL;" | tr -d ' ' | grep -x "0"</automated>
  </verify>
  <done>All 3 POST-DEPLOY.md status rows marked `complete`; 6 sign-off SQL queries return expected results; operator resume-signal received.</done>
  <what-built>
    - Plan 04: local DB has NOT NULL constraints on `brand_id` + `family_id`; hand-written prod-portable migration is at `supabase/migrations/20260626000000_phase80_catalog_brand_family_not_null.sql`.
    - Plan 05 Task 1: Local-First Verification Recipe executed — all 4 resolver tier paths verified against local Supabase.
    - Plan 05 Task 2: `80-POST-DEPLOY.md` runbook written with plain-English staged-deploy steps + verification SQL.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md` end-to-end. Confirm the three steps match D-80-03.
    2. Step 1: Run `git push` and confirm Vercel deploys the ingest code (catalog-resolver.ts + the resolver-wired upsert helpers). Wait for deploy success. Update POST-DEPLOY.md status row from `pending` to `complete (YYYY-MM-DD HH:MM)`.
    3. Step 2: Sign in to prod, run the Hamilton URL extract (or pick any URL where you already know the brand+model exists in the catalog). Run the Step 2 verification SQL in Supabase SQL editor. Confirm `brand_id` AND `family_id` both non-NULL. Paste the result row into POST-DEPLOY.md under Step 2. Update status row from `pending` to `complete`. If either FK is NULL: STOP, file a follow-up todo, do not proceed to Step 3.
    4. Step 3: Run `supabase db push --linked`. Confirm the migration applies cleanly (Supabase CLI reports success; no `RAISE EXCEPTION` from the pre-flight DO $$ block). Run all 6 sign-off SQL queries. Paste each query's result row into POST-DEPLOY.md under Step 3. Update status row from `pending` to `complete`.
    5. Confirm all 3 status rows are `complete` and the 6 sign-off queries all returned expected results.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - "Phase 80 prod migration applied; all 6 sign-off queries pass" → Plan complete, Phase 80 ready for `/gsd-verify-work`.
    - "Step 2 failed — brand_id was NULL on the Hamilton row" → STOP; investigate which upsert path didn't fire the resolver; raise a follow-up plan or revise Plan 03.
    - "Step 3 failed — Supabase reported [error]" → Investigate the failure; the BEGIN/COMMIT wrapper rolled back so prod is in a known state.
    - "Defer — running tomorrow" → leave POST-DEPLOY.md status rows as `pending`; mark plan as paused.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → real prod data | Operator manually verifying on prod; SSH/web auth gates apply; Tyler personally types the commands |
| dev server log inspection → no PII | Phase 80 log payloads are LLM-extracted brand/model strings from public web pages — no PII per T-80-06 mitigation; Vercel logs retain per their default policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-PROD-01 | Availability | Prod migration blocks on existing NULL rows | mitigate | Phase 79 MIG-04 already proved zero NULLs at commit time; Plan 04 migration has pre-flight DO $$ that aborts with explicit error if NULLs reappear; `supabase db push --linked` rolls back atomically on RAISE EXCEPTION |
| T-80-PROD-02 | Tampering | Operator accidentally runs migration before code deploys | mitigate | D-80-03 staged sequence enforced in POST-DEPLOY.md ordering; Step 2 manual extract proof is the gate; resume-signal explicitly enumerates the "Step 2 failed" path |
| T-80-PROD-03 | Information Disclosure | Operator pastes prod SQL results into POST-DEPLOY.md | accept | Result rows are catalog FK ids + brand/family names — no PII; safe to commit to repo per existing Phase 78/79 POST-DEPLOY pattern |
| T-80-PROD-04 | Denial of Service | AccessExclusive lock during prod ALTER | accept | ~205 prod rows = sub-millisecond lock per RESEARCH § Migration Plan; not a real-traffic concern at v8.4 scale |
</threat_model>

<verification>
- 80-POST-DEPLOY.md exists, ≥80 lines, contains all 3 staged-deploy steps.
- Local-First Verification Recipe executed; outcomes captured in 80-05-SUMMARY.md.
- Operator checkpoint resume-signal received from Tyler.
- After operator confirms Step 3 complete, prod `information_schema.columns` reports `is_nullable = 'NO'` for both `watches_catalog.brand_id` and `watches_catalog.family_id` (verified via the 6 sign-off queries).
- The prod `supabase_migrations.schema_migrations` table shows the new Phase 80 migration version recorded.
</verification>

<success_criteria>
- Phase 80 complete on prod: NOT NULL constraints live; resolver code shipping FKs on every new ingest.
- 6/6 sign-off queries pass.
- POST-DEPLOY.md status rows all `complete` with timestamps.
- No 23502 errors in Vercel logs after migration applies (the gate that catches "I forgot to wire the resolver into one upsert path").
- Phase 81 (RECO-01..04 + DISP-01/02) can safely consume `brand_id IS NOT NULL` as an invariant.
- Phase 82 (`/admin/brands` queue) has a real producer wired (the resolver's auto-create path).
</success_criteria>

<output>
After completion, create `.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-05-SUMMARY.md` recording:
- Local-First Verification Recipe outcomes (4 tests, pass/fail/skipped per test with verification SQL output + log captures).
- POST-DEPLOY.md status: all 3 steps `complete` with timestamps.
- 6 sign-off SQL query results (paste verbatim).
- Any deviations from the runbook (if Tyler had to adjust anything during execution).
- Phase 80 phase-level closure: ROADMAP update, STATE.md update, REQUIREMENTS.md flip CANON-01/02 + INGEST-01..04 from `Pending` to `Complete`.
- Confirm `needs_review = true` queue count baseline established (for Phase 82 to consume).
</output>
