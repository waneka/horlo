---
phase: 29-nav-profile-chrome-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/profile/ProfileTabs.tsx
  - tests/components/profile/ProfileTabs.test.tsx
autonomous: true
requirements:
  - PROF-10
tags:
  - ui
  - tabs
  - profile
  - prof-10
  - tailwind4

must_haves:
  truths:
    - "TabsList in ProfileTabs has the LOCKED LITERAL className: w-full justify-start gap-2 overflow-x-auto overflow-y-hidden pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden"
    - "Active-tab indicator (after:bottom-[-5px]) lives INSIDE the TabsList clip box because pb-2 (8px) exceeds the indicator's 5px overshoot"
    - "Vertical scroll-bar no longer appears on tab-strip overflow"
    - "Vertical scroll gestures pass through to page-level scroll (overflow-y: hidden does not consume the vertical axis)"
    - "src/components/ui/tabs.tsx is NOT modified — fix is local to ProfileTabs only (CONTEXT D-09)"
  artifacts:
    - path: "src/components/profile/ProfileTabs.tsx"
      provides: "ProfileTabs TabsList className with horizontal-only scroll override"
      contains: "overflow-x-auto overflow-y-hidden pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden"
    - path: "tests/components/profile/ProfileTabs.test.tsx"
      provides: "Test asserting all 4 PROF-10 utility classes are present on TabsList"
      contains: "PROF-10 horizontal-only scroll className override"
  key_links:
    - from: "src/components/profile/ProfileTabs.tsx:65"
      to: "TabsList rendered in /u/[username]/layout.tsx:136-142"
      via: "consumer-side className override (does NOT touch tabsListVariants in tabs.tsx)"
      pattern: "overflow-y-hidden"
    - from: "tests/components/profile/ProfileTabs.test.tsx"
      to: "container.querySelector('[data-tab-id]')?.parentElement"
      via: "JSDOM className inspection (D-11 — reliable in JSDOM)"
      pattern: "expect\\(cls\\)\\.toContain"
---

<objective>
Append four Tailwind utility classes to the `TabsList` className in `ProfileTabs.tsx:65` so the profile tab strip scrolls horizontally only — vertical scroll is suppressed without losing the active-tab underline indicator (PROF-10).

Locked literal className per CONTEXT D-06/D-07/D-08:
`w-full justify-start gap-2 overflow-x-auto overflow-y-hidden pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden`

Purpose: Closes 1 of 3 success criteria for Phase 29 ("On `/u/[username]`, the profile tab strip scrolls horizontally only when its tabs overflow — no vertical scroll-bar appears, no vertical-scroll gesture is consumed by the tab strip on touch or trackpad").

Output: One className string updated in `ProfileTabs.tsx`; one new className-assertion test appended to `ProfileTabs.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-nav-profile-chrome-cleanup/29-CONTEXT.md
@.planning/phases/29-nav-profile-chrome-cleanup/29-RESEARCH.md
@.planning/phases/29-nav-profile-chrome-cleanup/29-PATTERNS.md
@.planning/phases/29-nav-profile-chrome-cleanup/29-UI-SPEC.md
@src/components/profile/ProfileTabs.tsx
@tests/components/profile/ProfileTabs.test.tsx

<interfaces>
<!-- Locked literal source — verbatim from CONTEXT D-06/D-07/D-08 + UI-SPEC. -->

ProfileTabs.tsx CURRENT (lines 63-66):
```tsx
      <TabsList
        variant="line"
        className="w-full justify-start gap-2 overflow-x-auto"
      >
```

ProfileTabs.tsx TARGET (only line 65 changes — locked literal):
```tsx
      <TabsList
        variant="line"
        className="w-full justify-start gap-2 overflow-x-auto overflow-y-hidden pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden"
      >
```

Class additions (4 utilities, ALL locked):
- `overflow-y-hidden` (D-06) — clip the active-tab indicator's `bottom: -5px` overshoot; suppress vertical scroll
- `pb-2` (D-07) — 8px padding-bottom; pulls the indicator inside the clip box (5px overshoot + 3px clearance)
- `[scrollbar-width:none]` (D-08) — Firefox: hide horizontal scrollbar
- `[&::-webkit-scrollbar]:hidden` (D-08) — WebKit/Blink: hide horizontal scrollbar

Class preservation (UNCHANGED):
- `w-full`, `justify-start`, `gap-2`, `overflow-x-auto`

DO NOT EDIT (CONTEXT D-09):
- `src/components/ui/tabs.tsx` (tabsListVariants cva at lines 26-39; TabsTrigger active-tab indicator at line 64) — READ-ONLY references
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 4 utilities to TabsList className in ProfileTabs.tsx</name>
  <files>src/components/profile/ProfileTabs.tsx</files>
  <read_first>
    - src/components/profile/ProfileTabs.tsx (the entire file — 81 lines; verify line 65 holds the current `className="w-full justify-start gap-2 overflow-x-auto"`)
    - .planning/phases/29-nav-profile-chrome-cleanup/29-CONTEXT.md §"PROF-10 — Profile Tab Strip Horizontal-Only Scroll" (D-06..D-11)
    - .planning/phases/29-nav-profile-chrome-cleanup/29-PATTERNS.md §"2. src/components/profile/ProfileTabs.tsx — PROF-10 className append"
    - .planning/phases/29-nav-profile-chrome-cleanup/29-UI-SPEC.md §"Visual Diff Contract — PROF-10"
    - .planning/phases/29-nav-profile-chrome-cleanup/29-RESEARCH.md §"Pattern 2: Tailwind 4 arbitrary-variant scrollbar hiding (PROF-10)" + §"Pitfall 7: PROF-10 className override does not modify shared tabsListVariants"
  </read_first>
  <action>
    On line 65 of `src/components/profile/ProfileTabs.tsx`, replace EXACTLY:

    ```
    className="w-full justify-start gap-2 overflow-x-auto"
    ```

    with EXACTLY:

    ```
    className="w-full justify-start gap-2 overflow-x-auto overflow-y-hidden pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden"
    ```

    No other change. The existing 4 utilities (`w-full justify-start gap-2 overflow-x-auto`) are PRESERVED in their current order; the 4 new utilities are APPENDED in the order: `overflow-y-hidden pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden`. The exact final string (single space between every utility) is the locked literal from CONTEXT D-06.

    Pitfall 7 enforcement: DO NOT touch `src/components/ui/tabs.tsx`. The fix is consumer-local only. After the edit, `git diff --stat` should show ONE file changed (`src/components/profile/ProfileTabs.tsx`) when scoped to source files; `src/components/ui/tabs.tsx` MUST be absent from the diff.

    DO NOT touch:
    - Lines 1-5 (imports — `usePathname`, `Tabs`, `TabsList`, `TabsTrigger` all stay)
    - Lines 7-26 (`BASE_TABS`, `COMMON_GROUND_TAB`, `OWNER_INSIGHTS_TAB` constants)
    - Lines 28-44 (`ProfileTabsProps` interface)
    - Lines 46-60 (component body — `pathname.endsWith('/${t.id}')` activeTab logic)
    - Lines 67-77 (tab list rendering loop)

    Tailwind 4 verification: per RESEARCH Pattern 2, the patterns `[scrollbar-width:none]` and `[&::-webkit-scrollbar]:hidden` compile to standard CSS via Tailwind 4's arbitrary-variant syntax. They are NOT yet used elsewhere in this codebase (RESEARCH grep confirms ZERO matches in `src/`); Phase 29 introduces this pattern. No `tailwind.config.ts` change is needed (project uses `@tailwindcss/postcss` ^4 with auto-discovery).
  </action>
  <verify>
    <automated>npm run test -- tests/components/profile/ProfileTabs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'overflow-y-hidden' src/components/profile/ProfileTabs.tsx` returns `1`.
    - `grep -c 'pb-2' src/components/profile/ProfileTabs.tsx` returns `1`.
    - `grep -c '\[scrollbar-width:none\]' src/components/profile/ProfileTabs.tsx` returns `1`.
    - `grep -c '\[&::-webkit-scrollbar\]:hidden' src/components/profile/ProfileTabs.tsx` returns `1`.
    - `grep -c 'overflow-x-auto' src/components/profile/ProfileTabs.tsx` returns `1` (preserved).
    - `grep -c 'w-full justify-start gap-2 overflow-x-auto overflow-y-hidden pb-2 \[scrollbar-width:none\] \[&::-webkit-scrollbar\]:hidden' src/components/profile/ProfileTabs.tsx` returns `1` (full locked-literal string present in this exact order).
    - `git diff --stat src/components/ui/tabs.tsx` shows NO changes (Pitfall 7 — primitive untouched).
    - `npx tsc --noEmit --project tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>
    `ProfileTabs.tsx:65` className contains all 8 utilities in the exact locked-literal order; `src/components/ui/tabs.tsx` UNCHANGED; typecheck clean; tests pass after Task 2 lands.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append PROF-10 className-assertion test to tests/components/profile/ProfileTabs.test.tsx</name>
  <files>tests/components/profile/ProfileTabs.test.tsx</files>
  <read_first>
    - tests/components/profile/ProfileTabs.test.tsx (the entire file — 118 lines; verify the existing describe block ends around line 117 and that the file uses `container.querySelector('[data-tab-id="..."]')` patterns at lines 18, 33, 45, 67)
    - src/components/profile/ProfileTabs.tsx (verify `data-tab-id` is set on each TabsTrigger at line 72 — confirms the parent-traversal selector for finding TabsList works)
    - .planning/phases/29-nav-profile-chrome-cleanup/29-CONTEXT.md §"D-11" (test contract)
    - .planning/phases/29-nav-profile-chrome-cleanup/29-PATTERNS.md §"6. tests/components/profile/ProfileTabs.test.tsx — className assertion (PROF-10)"
  </read_first>
  <action>
    Append a new describe block AFTER all existing tests in `tests/components/profile/ProfileTabs.test.tsx`. Do NOT modify any existing test (the existing tab-rendering / Insights-gate / Common-Ground tests assert structural shape, not className — they continue to pass).

    Use the EXACT literal template from PATTERNS.md §6:

    ```tsx
    describe('ProfileTabs — PROF-10 horizontal-only scroll className override', () => {
      it('TabsList has overflow-x-auto AND overflow-y-hidden + scrollbar-hiding utilities + pb-2', () => {
        const { container } = render(<ProfileTabs username="tyler" />)
        // The TabsList is the parent of all [data-tab-id] triggers.
        const firstTrigger = container.querySelector('[data-tab-id]')
        const tabsList = firstTrigger?.parentElement
        expect(tabsList).toBeTruthy()
        const cls = tabsList!.className
        // Preserved utilities (Phase 14 lock):
        expect(cls).toContain('w-full')
        expect(cls).toContain('justify-start')
        expect(cls).toContain('gap-2')
        expect(cls).toContain('overflow-x-auto')
        // PROF-10 additions (CONTEXT D-06/D-07/D-08):
        expect(cls).toContain('overflow-y-hidden')
        expect(cls).toContain('pb-2')
        expect(cls).toContain('[scrollbar-width:none]')
        expect(cls).toContain('[&::-webkit-scrollbar]:hidden')
      })
    })
    ```

    Per CONTEXT D-11: className inspection is reliable in JSDOM; computed-style assertions are brittle and NOT used. The vertical-scroll-passthrough behavior is a manual UAT gate (D-10 / VALIDATION.md §"Manual-Only Verifications") — JSDOM doesn't simulate touch/trackpad gesture forwarding. Do not attempt a unit test for that.

    Do NOT add new mocks; the existing mock scaffold at lines 1-8 (`vi.mock('next/navigation', () => ({ usePathname: () => '/u/tyler/collection' }))`) covers everything ProfileTabs needs in this test.
  </action>
  <verify>
    <automated>npm run test -- tests/components/profile/ProfileTabs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ProfileTabs — PROF-10 horizontal-only scroll className override" tests/components/profile/ProfileTabs.test.tsx` returns `1`.
    - `grep -c "expect(cls).toContain('overflow-y-hidden')" tests/components/profile/ProfileTabs.test.tsx` returns `1`.
    - `grep -c "expect(cls).toContain('pb-2')" tests/components/profile/ProfileTabs.test.tsx` returns `1`.
    - `grep -c "expect(cls).toContain('\\[scrollbar-width:none\\]')" tests/components/profile/ProfileTabs.test.tsx` returns `1`.
    - `grep -c "expect(cls).toContain('\\[&::-webkit-scrollbar\\]:hidden')" tests/components/profile/ProfileTabs.test.tsx` returns `1`.
    - `npm run test -- tests/components/profile/ProfileTabs.test.tsx` PASSES — all tests green (existing 7 tests + new className-assertion test).
    - `npx tsc --noEmit --project tsconfig.json` exits 0.
    - The 7 existing tests in the file (tab rendering, Insights gate, Common Ground gate) still pass — no regression.
  </acceptance_criteria>
  <done>
    New describe block appended; PROF-10 className-assertion test passes (after Task 1 lands); existing 7 tests preserved and still pass; typecheck clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | PROF-10 is a CSS-only consumer-side change. No new untrusted-input boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | I (Information Disclosure) | ProfileTabs scroll behavior | accept | Per Phase 29 RESEARCH §"Security Domain": Phase 29 is UI-only, threat surface is minimal. Constraining scroll axis on a tab strip changes no data exposure surface. |
| T-29-03-02 | T (Tampering) | Tailwind 4 arbitrary-variant utilities | accept | `[scrollbar-width:none]` and `[&::-webkit-scrollbar]:hidden` compile to standard CSS at build time via Tailwind 4's arbitrary-variant syntax (RESEARCH Pattern 2). They cannot be tampered with at runtime; they're static class names. |
</threat_model>

<verification>
After both tasks complete:

```bash
npm run test -- tests/components/profile/ProfileTabs.test.tsx
npx tsc --noEmit --project tsconfig.json
git diff --stat src/components/ui/tabs.tsx     # MUST be empty (Pitfall 7)
```

Expected: tests green, typecheck clean, primitive (`tabs.tsx`) untouched.

Manual UAT (deferred to phase-end UAT, not this plan):
1. Visit `/u/{username}/collection` on a narrow viewport (mobile size).
2. Tab strip overflow: confirm horizontal scroll works (touch/trackpad).
3. Confirm NO horizontal scrollbar visible (D-08).
4. Confirm NO vertical scrollbar appears at any time (D-06).
5. Vertical scroll gesture on the tab strip → page scrolls (gesture passes through; D-10).
6. Confirm active-tab underline indicator visible relative to active tab (unchanged geometry; clipped inside parent's `pb-2` region).
</verification>

<success_criteria>
- ProfileTabs.tsx line 65 contains all 4 added utilities in the exact locked-literal order.
- Existing 4 utilities (`w-full`, `justify-start`, `gap-2`, `overflow-x-auto`) preserved in their original positions.
- `src/components/ui/tabs.tsx` UNCHANGED (Pitfall 7 — primitive untouched, CONTEXT D-09 enforced).
- New className-assertion test in ProfileTabs.test.tsx passes.
- All existing ProfileTabs tests still pass.
- TypeScript clean.
</success_criteria>

<output>
After completion, create `.planning/phases/29-nav-profile-chrome-cleanup/29-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`.

Note in the SUMMARY:
- The 4 utilities added (in order: overflow-y-hidden, pb-2, [scrollbar-width:none], [&::-webkit-scrollbar]:hidden).
- `tabs.tsx` NOT modified (Pitfall 7 / CONTEXT D-09 honored).
- Manual UAT items punted to phase-end UAT (D-10 vertical-scroll passthrough cannot be unit tested).
- Tailwind 4 arbitrary-variant utilities are FIRST USE in this codebase (RESEARCH grep confirmed); future maintainers can grep `[&::-webkit-scrollbar]` to find the pattern.
</output>
