---
phase: 29-nav-profile-chrome-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/UserMenu.tsx
  - tests/components/layout/UserMenu.test.tsx
autonomous: true
requirements:
  - NAV-16
tags:
  - ui
  - navigation
  - chrome
  - nav-16

must_haves:
  truths:
    - "UserMenu dropdown does NOT render a 'Profile' DropdownMenuItem regardless of username state"
    - "UserMenu dropdown order is: email-label → Separator → Settings → Separator → Theme → Separator → Sign out"
    - "Both surrounding DropdownMenuSeparator instances (line 68 and line 75 in pre-edit numbering) STAY (per UI-SPEC D-01 wording precision note)"
    - "Avatar Link → /u/${username}/collection remains the primary path to profile (Phase 25 dual-affordance unaffected)"
    - "Test 4 (Profile dropdown link) is DELETED; Test 3 is rewritten without profileIdx; Test 5 (null-username branch) still passes"
  artifacts:
    - path: "src/components/layout/UserMenu.tsx"
      provides: "UserMenu component with Profile DropdownMenuItem block deleted at original lines 69-73"
      contains: "<DropdownMenuItem render={<Link href=\"/settings\">Settings</Link>} />"
    - path: "tests/components/layout/UserMenu.test.tsx"
      provides: "Updated tests for the new dropdown structure"
      contains: "NAV-16: Profile row removed"
  key_links:
    - from: "src/components/layout/UserMenu.tsx"
      to: "(removed) DropdownMenuItem Profile row"
      via: "JSX deletion"
      pattern: "Profile"
    - from: "tests/components/layout/UserMenu.test.tsx"
      to: "screen.queryByRole('link', { name: /^profile$/i })"
      via: "Test 5 assertion remains globally true after NAV-16"
      pattern: "queryByRole.*profile"
---

<objective>
Delete the redundant `Profile` DropdownMenuItem block from `UserMenu.tsx` (NAV-16). The avatar Link → `/u/${username}/collection` (shipped in Phase 25 D-01..D-04) remains the canonical path to profile; the dropdown row is duplicate. Both surrounding `<DropdownMenuSeparator />` instances STAY per UI-SPEC §"Visual Diff Contract — NAV-16 D-01 wording precision."

Purpose: Honors CONTEXT D-01..D-05 and UI-SPEC's wording-precision override of D-01. Closes 1 of 3 success criteria for Phase 29 ("UserMenu dropdown no longer shows a Profile item; the avatar Link remains the primary path; UserMenu still exposes Settings, Theme segmented control, and Sign out").

Output: `src/components/layout/UserMenu.tsx` 4 lines shorter (Profile DropdownMenuItem block + its `{username && (` wrapper deleted); `tests/components/layout/UserMenu.test.tsx` Test 3 rewritten and Test 4 deleted; Test 5 unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-nav-profile-chrome-cleanup/29-CONTEXT.md
@.planning/phases/29-nav-profile-chrome-cleanup/29-RESEARCH.md
@.planning/phases/29-nav-profile-chrome-cleanup/29-PATTERNS.md
@.planning/phases/29-nav-profile-chrome-cleanup/29-UI-SPEC.md
@src/components/layout/UserMenu.tsx
@tests/components/layout/UserMenu.test.tsx

<interfaces>
<!-- Locked literal source — see PATTERNS.md §1 for the exact before/after JSX. -->

UserMenu.tsx CURRENT (lines 67-75):
```tsx
        <DropdownMenuSeparator />
        {username && (
          <DropdownMenuItem
            render={<Link href={`/u/${username}/collection`}>Profile</Link>}
          />
        )}
        <DropdownMenuItem render={<Link href="/settings">Settings</Link>} />
        <DropdownMenuSeparator />
```

UserMenu.tsx TARGET (4 lines deleted; both separators STAY):
```tsx
        <DropdownMenuSeparator />
        <DropdownMenuItem render={<Link href="/settings">Settings</Link>} />
        <DropdownMenuSeparator />
```

UI-SPEC D-01 wording precision: D-01 says "Delete the `Profile` `<DropdownMenuItem>` at `UserMenu.tsx:69-73` AND the trailing `<DropdownMenuSeparator />` at line 75". The UI-SPEC overrides D-01: BOTH surrounding separators STAY. The "trailing separator collapse" wording in D-01 refers conceptually to the dropdown post-deletion (one row gone), NOT to literal separator removal. Asserted dropdown order in tests post-edit: email-label → Separator → Settings → Separator → Theme → Separator → Sign out.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Profile DropdownMenuItem block from UserMenu.tsx (lines 69-73 + the {username && (` wrapper)</name>
  <files>src/components/layout/UserMenu.tsx</files>
  <read_first>
    - src/components/layout/UserMenu.tsx (the entire file — 145 lines; verify the `{username && (` wrapper starts at line 69 and the closing `)}` is at line 73 in the current file)
    - .planning/phases/29-nav-profile-chrome-cleanup/29-CONTEXT.md §"NAV-16 — UserMenu Profile Row Removal" (D-01..D-05)
    - .planning/phases/29-nav-profile-chrome-cleanup/29-PATTERNS.md §"1. src/components/layout/UserMenu.tsx — NAV-16 deletion"
    - .planning/phases/29-nav-profile-chrome-cleanup/29-UI-SPEC.md §"Visual Diff Contract — NAV-16" (full delta clarification + the D-01 wording precision note)
  </read_first>
  <action>
    Delete the entire `{username && ( <DropdownMenuItem render={<Link href={`/u/${username}/collection`}>Profile</Link>} /> )}` block (lines 69-73 in the current file, plus the line containing `)}` which closes the `{username &&` wrapper). The result is that the file shrinks by exactly the JSX block specified in the PATTERNS.md §1 "BEFORE → AFTER" — 4 lines removed, replaced by zero lines.

    The post-edit JSX MUST read EXACTLY (with surrounding context preserved verbatim):

    ```tsx
            <DropdownMenuSeparator />
            <DropdownMenuItem render={<Link href="/settings">Settings</Link>} />
            <DropdownMenuSeparator />
    ```

    DO NOT delete either `<DropdownMenuSeparator />`. Both stay. The separator at the original line 68 (now bracketing email-label / Settings) STAYS. The separator at the original line 75 (now bracketing Settings / Theme) STAYS. This is the UI-SPEC wording-precision override of CONTEXT D-01.

    DO NOT touch:
    - Lines 1-15 (imports — DropdownMenuItem, DropdownMenuSeparator, etc. all stay; we removed a USE of DropdownMenuItem but the import remains because `Settings` and `Sign out` still use it).
    - Lines 41-67 (props, !user branch, dropdownContent setup through the email DropdownMenuLabel + its trailing separator).
    - Line 74 (Settings DropdownMenuItem — UNCHANGED, just moves up by 4 lines).
    - Lines 75-95 (Theme block, Sign out form-button, closing tags).
    - Lines 97-112 (no-username chevron-only branch — D-03: this branch was already conditional on `{username && ...}` so the deletion is a no-op for its render tree).
    - Lines 114-145 (with-avatar render branch — Phase 25 D-01..D-04 dual-affordance lock; UI-SPEC explicitly preserves byte-identity here).

    After the edit, run `grep -c "Profile" src/components/layout/UserMenu.tsx | grep -v '^#'` — expect 0 (file no longer mentions "Profile" anywhere). Note: comments at the top of the file mention "Phase 25 NAV-13 / D-01..D-04 dual-affordance" and "Profile / Settings / Theme / Sign out" — those JSDoc comments at lines 17-39 may need a single line update (line 25 mentions "Profile / Settings / Theme / Sign out" in the dropdown content list). Update that JSDoc line to read `Settings / Theme / Sign out` to keep documentation truthful (post-deletion).

    Per Phase 25 D-04 lock relaxation (CONTEXT D-02): the JSDoc adjustment is the only deviation from byte-identity beyond the deletion itself. Settings, Theme, Sign out, avatar Link, chevron Button — all UNCHANGED.
  </action>
  <verify>
    <automated>npm run test -- tests/components/layout/UserMenu.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*\*\|^/\*\|^[[:space:]]*//' src/components/layout/UserMenu.tsx | grep -c "Profile"` returns `0` (Profile string is absent from non-comment code; comment-stripping uses `grep -v` to drop JSDoc/inline comments — the JSDoc updated in this task contains zero "Profile" references after edit).
    - `grep -c "Profile" src/components/layout/UserMenu.tsx` returns `0` (extra strict — even comments updated; verifies the JSDoc adjustment landed).
    - `grep -c "<DropdownMenuItem render={<Link href=\"/settings\">Settings</Link>}" src/components/layout/UserMenu.tsx` returns `1` (Settings row preserved).
    - `grep -c "DropdownMenuSeparator" src/components/layout/UserMenu.tsx` returns `4` — the import (1) + three separator JSX usages (between email-label/Settings, between Settings/Theme, between Theme/Signout) = 4 total. The Profile-block separator at original line 68 STAYS; the Settings-block separator at original line 75 STAYS.
    - `grep -c "{username &&" src/components/layout/UserMenu.tsx` returns `0` (the only `{username &&` block in the file was the deleted Profile wrapper; the chevron-only branch at line 97-112 uses `if (!username) {` not the `{username && (` JSX wrapper pattern).
    - File compiles: `npx tsc --noEmit --project tsconfig.json` exits 0.
    - Tasks 2 below land first then run `npm run test -- tests/components/layout/UserMenu.test.tsx` — all UserMenu tests pass after both edits.
  </acceptance_criteria>
  <done>
    UserMenu.tsx no longer renders the Profile DropdownMenuItem; both surrounding separators preserved; JSDoc updated to remove the stale "Profile / Settings / Theme / Sign out" listing; typecheck clean. Per Phase 25 D-04 relaxation in NAV-16 D-02, all other dropdown surface elements remain byte-identical.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests/components/layout/UserMenu.test.tsx — rewrite Test 3, delete Test 4</name>
  <files>tests/components/layout/UserMenu.test.tsx</files>
  <read_first>
    - tests/components/layout/UserMenu.test.tsx (the entire file — 184 lines; locate Test 3 starting around line 75 and Test 4 starting around line 91, Test 5 at line 126-147)
    - .planning/phases/29-nav-profile-chrome-cleanup/29-CONTEXT.md §"D-05" (test rewrite specification)
    - .planning/phases/29-nav-profile-chrome-cleanup/29-PATTERNS.md §"5. tests/components/layout/UserMenu.test.tsx — Tests 3 + 4 update (NAV-16)"
    - .planning/phases/29-nav-profile-chrome-cleanup/29-RESEARCH.md §"Pitfall 6: Test 4 deletion in UserMenu.test.tsx"
  </read_first>
  <action>
    Three edits to the test file, in order:

    **Edit 1 — Rewrite Test 3** (currently `Test 3 — dropdown contains all sections in order: Email / Profile / Settings / Theme / Sign out`):

    Replace with the EXACT literal:

    ```tsx
    it('Test 3 — dropdown contains all sections in order: Email / Settings / Theme / Sign out (NAV-16)', () => {
      render(<UserMenu {...aliceProps} />)
      const content = screen.getByTestId('dropdown-content')
      const text = content.textContent ?? ''
      const emailIdx = text.indexOf('alice@example.com')
      const settingsIdx = text.indexOf('Settings')
      const themeIdx = text.indexOf('Theme')
      const signOutIdx = text.indexOf('Sign out')
      expect(emailIdx).toBeGreaterThanOrEqual(0)
      expect(settingsIdx).toBeGreaterThan(emailIdx)
      expect(themeIdx).toBeGreaterThan(settingsIdx)
      expect(signOutIdx).toBeGreaterThan(themeIdx)
      // NAV-16: Profile row removed; assertion is no longer present.
      expect(text).not.toMatch(/Profile/)
    })
    ```

    **Edit 2 — Delete Test 4** (currently `Test 4 — Profile dropdown item links to /u/${username}/collection`):

    DELETE the entire `it(...)` block. Per CONTEXT D-05: the avatar Link assertion in Test 2 already covers the `/u/${username}/collection` navigation path; per RESEARCH Pitfall 6: Test 5's `expect(screen.queryByRole('link', { name: /^profile$/i })).toBeNull()` already covers "no Profile link in DOM" for ALL branches (was previously null-only; now globally true).

    **Edit 3 — Verify Test 5 (null-username branch) is UNCHANGED**:

    Read Test 5 (around line 126-147) and CONFIRM that the assertion `expect(screen.queryByRole('link', { name: /^profile$/i })).toBeNull()` is still present and untouched. After NAV-16, this assertion is correct for ALL branches (not just null-username). Do NOT remove or modify Test 5.

    Test numbering: After deletion of Test 4, the original Test 5 becomes the file's "Test 5" position structurally, but the test description string stays unchanged (RESEARCH Pitfall 6 — Test 5 already passes the globally-true assertion). Do NOT renumber tests in their `it()` description strings.

    No other tests change. Mock scaffold (lines 10-44) preserved verbatim.
  </action>
  <verify>
    <automated>npm run test -- tests/components/layout/UserMenu.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Test 3 — dropdown contains all sections in order: Email / Settings / Theme / Sign out (NAV-16)" tests/components/layout/UserMenu.test.tsx` returns `1`.
    - `grep -c "Test 4 — Profile dropdown item links to" tests/components/layout/UserMenu.test.tsx` returns `0` (Test 4 deleted).
    - `grep -c "profileIdx" tests/components/layout/UserMenu.test.tsx` returns `0` (no profileIdx references anywhere in the file post-edit).
    - `grep -c "queryByRole.*name: /\\^profile\\$/i" tests/components/layout/UserMenu.test.tsx` returns at least `1` (Test 5 / null-branch assertion preserved — exact regex may vary in source, this grep tolerates).
    - `grep -c "expect(text).not.toMatch(/Profile/)" tests/components/layout/UserMenu.test.tsx` returns `1` (NAV-16 negative assertion in Test 3).
    - `npm run test -- tests/components/layout/UserMenu.test.tsx` PASSES (all remaining tests green; the file now contains 1 fewer test than before).
    - `npx tsc --noEmit --project tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>
    Test 3 rewritten with email/Settings/Theme/Sign out ordering (no profileIdx, plus an explicit `not.toMatch(/Profile/)` assertion); Test 4 deleted; Test 5 (null-username branch) preserved. All tests in UserMenu.test.tsx pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Static dropdown UI | UserMenu renders a Server Component that hydrates a client-side DropdownMenu from Base UI. The dropdown surfaces are rendered from server-trusted props (`user`, `username`, `avatarUrl`); no untrusted user input is reflected into JSX in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | I (Information Disclosure) | UserMenu dropdown | accept | Phase 29 RESEARCH §"Security Domain" notes Phase 29 actually CLOSES one stale-state leak vector (the FORM-04 bug). NAV-16 has no information-disclosure surface — deleting a UI affordance reduces surface, never adds it. |
| T-29-02-02 | T (Tampering) | UserMenu render path | accept | Per UI-SPEC §"Out of Scope": the avatar Link, AvatarDisplay sizing, chevron Button, hit targets, and Phase 25 D-01..D-04 dual-affordance contracts are UNCHANGED. The Profile row was already conditionally hidden via `{username && ...}`; deleting the wrapped block is a no-op for the !username branch's render tree (D-03). No new tamperable surface. |
| T-29-02-03 | E (Elevation of Privilege) | URL routing via avatar Link | accept | Avatar Link already routes to `/u/${username}/collection` and that route is gated by Phase 8 RLS. Deletion of the duplicate dropdown affordance does not change the gate. |
</threat_model>

<verification>
After both tasks complete:

```bash
npm run test -- tests/components/layout/UserMenu.test.tsx
npx tsc --noEmit --project tsconfig.json
grep -c "Profile" src/components/layout/UserMenu.tsx
```

Expected: tests green, typecheck clean, grep returns `0`.

Manual UAT (deferred to phase-end UAT, not this plan):
1. Sign in. Click avatar → routes to `/u/{username}/collection`.
2. Click chevron → dropdown opens with Settings, Theme, Sign out only.
3. No Profile row visible in dropdown.
4. Settings → routes to `/settings`.
5. Sign out → logs out via Server Action.
</verification>

<success_criteria>
- UserMenu.tsx no longer contains the Profile DropdownMenuItem block (grep `Profile` returns 0 post-edit, including the JSDoc adjustment).
- Both surrounding `<DropdownMenuSeparator />` instances STAY (UI-SPEC D-01 wording precision honored).
- Test 3 rewritten with email/Settings/Theme/Sign out ordering and `not.toMatch(/Profile/)` assertion.
- Test 4 deleted entirely.
- Test 5 (null-username branch) preserved verbatim — its assertion is now globally true.
- All UserMenu tests pass.
- TypeScript clean.
</success_criteria>

<output>
After completion, create `.planning/phases/29-nav-profile-chrome-cleanup/29-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`.

Note in the SUMMARY:
- Lines deleted from UserMenu.tsx (4 lines of JSX + JSDoc adjustment).
- Test 3 rewritten; Test 4 deleted; Test 5 unchanged.
- UI-SPEC D-01 wording precision honored: BOTH separators preserved.
- Phase 25 D-04 lock relaxed minimally per NAV-16 D-02.
</output>
