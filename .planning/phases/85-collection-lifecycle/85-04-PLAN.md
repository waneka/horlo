---
phase: 85-collection-lifecycle
plan: 04
type: execute
wave: 3
depends_on: ["85-02", "85-03"]
files_modified:
  - src/app/actions/__tests__/moveWishlistToCollection.test.ts
  - src/app/actions/__tests__/watches-recs-invalidation.test.ts
  - src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx
  - src/components/watch/ConfirmStep.test.tsx
  - src/components/watch/AddWatchFlow.test.tsx
  - src/data/__tests__/reactions-comments-gate.test.ts
  - tests/integration/phase59-unified-route.test.ts
  - tests/lib/tasteTags.test.ts
  - tests/data/comments.test.ts
  - tests/data/searchCatalogWatches.test.ts
  - tests/data/getFollowedOwnersForCatalog.test.ts
  - tests/data/getCollectorsForCatalog.test.ts
autonomous: true
requirements: [LIFE-01]

must_haves:
  truths:
    - "LIFE-01 / D-01: no source or test file uses 'sold' as a status value (only as a disposal reason) — `grep -rn \"'sold'\" src tests | grep -vi disposal` prints nothing"
    - "D-01: every test fixture that modeled a sold watch now models previously_owned with semantically identical expectations (allowlist predicates exclude previously_owned exactly where they excluded sold)"
    - "npm run build exits 0 at the end of this plan, after 85-02 and 85-03 have landed — this is the rename sweep's build gate that all Wave-4+ plans depend on"
  artifacts:
    - path: "tests/integration/phase59-unified-route.test.ts"
      provides: "unified-route fixture using previouslyOwnedWatchId"
      contains: "previously_owned"
    - path: "src/app/actions/__tests__/moveWishlistToCollection.test.ts"
      provides: "Case 5 rejects previously_owned watch"
      contains: "Cannot move previously_owned watch to collection"
  key_links:
    - from: "repo-wide 'sold' status literal sweep"
      to: "npm run build"
      via: "WatchStatus union no longer contains sold, so any leftover literal is a type error"
      pattern: "previously_owned"
---

<objective>
Close the `sold` → `previously_owned` rename: sweep every test fixture off the sold status, prove no status-`'sold'` literal remains anywhere in `src` or `tests`, and pass the `npm run build` gate.

Purpose: 85-02 (domain core) and 85-03 (divestments retirement + source references) leave the TypeScript world almost consistent; the remaining type errors live in test fixtures. This plan is the single build gate for the whole rename so Wave-4 behavior plans (85-05, 85-06, 85-07) start from a green build.

Output: compiling, building repo in which no test models a sold status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/85-collection-lifecycle/85-CONTEXT.md
@.planning/phases/85-collection-lifecycle/85-RESEARCH.md
@.planning/phases/85-collection-lifecycle/85-02-SUMMARY.md
@.planning/phases/85-collection-lifecycle/85-03-SUMMARY.md

<interfaces>
After 85-02: src/lib/types.ts export type WatchStatus = 'owned' | 'wishlist' | 'previously_owned' | 'grail'; export type DisposalReason = 'sold' | 'lost' | 'gifted' | 'stolen' | 'traded'
After 85-03: src/app/actions/watches.ts status: z.enum(['owned', 'wishlist', 'grail', 'previously_owned']); moveWishlistToCollection rejects non-wishlist/grail statuses with error `Cannot move ${status} watch to collection`
Legitimate remaining `'sold'` literals (all on lines containing "disposal"/"Disposal"/"DISPOSAL"): DisposalReason type, DISPOSAL_REASONS constant, disposalReasonEnum pgEnum in src/db/schema.ts, and the 85-02 getWatchByIdForViewer negative assertion carrying `// retired status literal, now only a disposal reason`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep src/ colocated test fixtures off the sold status</name>
  <files>src/app/actions/__tests__/moveWishlistToCollection.test.ts, src/app/actions/__tests__/watches-recs-invalidation.test.ts, src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx, src/components/watch/ConfirmStep.test.tsx, src/components/watch/AddWatchFlow.test.tsx, src/data/__tests__/reactions-comments-gate.test.ts</files>
  <read_first>
    - Each listed test file at the lines found by `grep -n "'sold'\|\"sold\"\|sold" <file>` (mechanical one-to-three-line literal swaps)
  </read_first>
  <action>
Mechanical literal sweep (D-01). In each file replace status value 'sold' with 'previously_owned', including union type annotations (e.g. `'owned' | 'wishlist' | 'grail' | 'sold'` → `... | 'previously_owned'`), test titles and comments describing the status. Expectations stay semantically identical because every production predicate that excluded sold is an allowlist that also excludes previously_owned:
  - moveWishlistToCollection.test.ts Case 5: status 'previously_owned', expected error 'Cannot move previously_owned watch to collection', title updated.
  - ProfileWatchCard-priceLine.test.tsx: 'previously_owned + pricePaid=3000 → renders "Paid: $3,000"' (price bucket unchanged).
  - reactions-comments-gate.test.ts: the formerly-sold row stays comment-visible (status !== 'wishlist' predicate unchanged; research Open Question 1 RESOLVED as accepted residual).
  - AddWatchFlow.test.tsx: status literal/union swap only.
  - watches-recs-invalidation.test.ts (~L246) and ConfirmStep.test.tsx (~L12): comment text only.
For any vitest failure, confirm with `git blame` that the failing `it()` predates Phase 85 and fails identically without these edits (e.g. run the file at HEAD~ in a scratch copy); record such baseline failures in the SUMMARY with the blame evidence.
  </action>
  <verify>
    <automated>npx vitest run src/app/actions/__tests__/moveWishlistToCollection.test.ts src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx src/data/__tests__/reactions-comments-gate.test.ts src/components/watch/ConfirmStep.test.tsx src/components/watch/AddWatchFlow.test.tsx && test "$(grep -n "'sold'\|\"sold\"" src/app/actions/__tests__/moveWishlistToCollection.test.ts src/app/actions/__tests__/watches-recs-invalidation.test.ts src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx src/components/watch/ConfirmStep.test.tsx src/components/watch/AddWatchFlow.test.tsx src/data/__tests__/reactions-comments-gate.test.ts | grep -vi disposal | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Cannot move previously_owned watch to collection" src/app/actions/__tests__/moveWishlistToCollection.test.ts` returns 1
    - None of the six files contains a `'sold'` / `"sold"` literal outside a line mentioning disposal
    - Targeted vitest has no failures other than ones recorded as pre-existing baseline with blame evidence
  </acceptance_criteria>
  <done>All src/-colocated fixtures model previously_owned instead of sold, with unchanged semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep tests/ fixtures, run the repo-wide literal gate, and pass the build</name>
  <files>tests/integration/phase59-unified-route.test.ts, tests/lib/tasteTags.test.ts, tests/data/comments.test.ts, tests/data/searchCatalogWatches.test.ts, tests/data/getFollowedOwnersForCatalog.test.ts, tests/data/getCollectorsForCatalog.test.ts</files>
  <read_first>
    - Each listed test file at the lines found by `grep -n "'sold'\|\"sold\"\|soldWatch" <file>`
    - `grep -rn "'sold'\|\"sold\"" src tests | grep -vi disposal` output (anything left after Task 1 and 85-02/85-03)
  </read_first>
  <action>
Mechanical literal sweep (D-01), same rules as Task 1:
  - tests/integration/phase59-unified-route.test.ts: status 'sold' → 'previously_owned'; rename variables like soldWatchId → previouslyOwnedWatchId; update titles/comments.
  - tests/data/comments.test.ts: the formerly-sold row stays comment-visible (status !== 'wishlist' predicate unchanged; research Open Question 1 RESOLVED as accepted residual).
  - tests/data/getFollowedOwnersForCatalog.test.ts / getCollectorsForCatalog.test.ts / searchCatalogWatches.test.ts / tests/lib/tasteTags.test.ts: previously_owned rows excluded exactly where sold rows were.
Then run the repo-wide gate `grep -rn "'sold'\|\"sold\"" src tests | grep -vi disposal`. If it prints any line in a file outside this plan and outside 85-02/85-03's files_modified, apply the same literal swap there and list the extra file in the SUMMARY as a deviation (the research sweep may have missed it). If it prints a line inside 85-02/85-03's files, fix it the same way and note it in the SUMMARY.
Finally run `npm run build`; it MUST exit 0 (build is the authoritative gate — project memory: baseline tsc/test suites carry pre-existing failures). Fix any type error caused by the rename in place; do not suppress with casts or `// @ts-expect-error`.
  </action>
  <verify>
    <automated>test "$(grep -rn "'sold'\|\"sold\"" src tests | grep -vi "disposal" | wc -l | tr -d ' ')" = "0" && npx vitest run tests/lib/tasteTags.test.ts tests/data/comments.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "'sold'\|\"sold\"" src tests | grep -vi disposal` prints nothing
    - `npm run build` exits 0
    - `npx vitest run tests/lib/tasteTags.test.ts tests/data/comments.test.ts tests/data/searchCatalogWatches.test.ts tests/data/getFollowedOwnersForCatalog.test.ts tests/data/getCollectorsForCatalog.test.ts` — no failures other than ones recorded as pre-existing baseline with blame evidence (integration suites that need a live DB may skip)
  </acceptance_criteria>
  <done>The repo compiles, builds, and no test models a sold status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-owner → comments/reactions data layer | Stale watch_id for a previously-owned watch could still hit comment/reaction reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-07 | Information Disclosure | comments/reactions on previously-owned watches via stale watch_id | accept | Research Open Question 1 (RESOLVED): `status !== 'wishlist'` data-layer checks unchanged and fixtures assert that behavior; D-14 scopes only the /w/[id] page; no UI path exposes the id to visitors after 85-06/85-07. Candidate follow-up, out of locked scope. |
</threat_model>

<verification>
- Task 1 targeted vitest passes (or baseline failures documented).
- `grep -rn "'sold'\|\"sold\"" src tests | grep -vi disposal` empty.
- `npm run build` exits 0.
</verification>

<success_criteria>
One leave-the-collection status exists across source and tests, and the repo builds green so Wave-4 plans start on a compiling base.
</success_criteria>

<output>
Create `.planning/phases/85-collection-lifecycle/85-04-SUMMARY.md` when done
</output>
