---
phase: 85-collection-lifecycle
plan: 11
type: execute
wave: 7
depends_on: ["85-01", "85-02", "85-03", "85-04", "85-05", "85-06", "85-07", "85-08", "85-09", "85-10"]
files_modified: []
autonomous: false
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04, LIFE-05, LIFE-06]

must_haves:
  truths:
    - "LIFE-03 / D-05 / D-06 / D-08: in npm run dev against local Supabase, the owner marks an owned watch previously owned from the collection card ⋯ menu (no navigation on tap), picks a reason plus optional amount and date, sees 'Moved to previously owned', stays on the Collection tab, and the card leaves the grid without a hard refresh"
    - "LIFE-01 / LIFE-02: SQL confirms the disposed row has status previously_owned with the chosen disposal_reason, sell_price and disposal_date, and the 85-01 sold fixture reads previously_owned / sold"
    - "LIFE-05 / D-15 / D-16: the 'Show previously owned (N)' toggle is off by default, reveals muted cards with a '{Reason} · {Mon YYYY}' badge and no wear line, and is off again after leaving and returning to the tab (result recorded either way)"
    - "D-04 / D-07: the owned watch edit form offers no 'Previously owned' status; the previously-owned watch's edit form shows editable disposal fields, and changing its status back to Owned clears disposal_reason / sell_price / disposal_date in SQL"
    - "LIFE-04 / D-09 / D-10 / D-11 / D-12: wishlist → owned via edit form and via add-flow Move to collection each show confetti plus 'Added to your collection!'; grail → owned shows 'Grail acquired!'; reduced motion shows the toast without confetti; a normal edit shows only 'Watch updated'; nothing prompts for price or photos"
    - "D-13 / D-14: a second seeded user sees no toggle and no previously-owned cards on the owner's Collection or Notes tabs, gets a 404 on the previously-owned watch's /w/[id], and on /wear/[id] for a wear of that watch sees unlinked brand/model"
    - "D-17: the previously-owned watch's wears still appear in the owner's Worn tab Timeline/Calendar and open /wear/[id], while the leaderboard and log-a-wear picker exclude it"
    - "LIFE-06 / D-18 / D-19: the disposed model is not recommended on the owner's home rail and the Collection Fit verdict on that model's catalog page no longer treats it as owned or as a similar/duplicate collection watch"
    - "Prod schema is pushed only by the operator via supabase db push --linked, before the code deploy, after this walk passes"
  artifacts: []
  key_links: []
---

<objective>
Phase gate required by CLAUDE.md Local-First Development: exercise every Phase 85 code path in `npm run dev` against local Supabase as seeded users, verify the database state with SQL, and only then hand the operator the prod migration + deploy step.

Purpose: vitest mocks don't execute SQL and `npm run build` doesn't run queries (the Drizzle ANY() incident). This phase is the first DB-touching phase of v9.0, so the walk also covers cross-user gating and the Router Cache freshness of the card leaving the grid.

Output: a verified phase and an operator-applied prod migration. No source files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/85-collection-lifecycle/85-CONTEXT.md
@.planning/phases/85-collection-lifecycle/85-VALIDATION.md
@.planning/phases/85-collection-lifecycle/85-01-SUMMARY.md

Local DB: `psql postgresql://postgres:postgres@127.0.0.1:54322/postgres` (psql at /opt/homebrew/opt/libpq/bin/psql; container supabase_db_horlo). Tables:
- `profiles(id, username)`, `profile_settings(user_id, profile_public, collection_public, wishlist_public)`
- `watches(id, user_id, brand, model, reference, status, catalog_id, notes, notes_public, disposal_reason, sell_price, disposal_date)`
- `watches_catalog(id, brand, model, reference)`
- `wear_events(id, user_id, watch_id, worn_date, note, photo_url, visibility)`
- `follows(follower_id, following_id)`

Seeded users (password `password123`): viewer@horlo.test, vintage-anna@horlo.test, modern-mike@horlo.test, dress-dan@horlo.test.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight gates, fixtures and walk sheet</name>
  <files>(none — read-only checks plus local-only fixture rows)</files>
  <read_first>
    - ./CLAUDE.md (Local-First Development)
    - .planning/phases/85-collection-lifecycle/85-VALIDATION.md (Manual-Only Verifications)
    - .planning/phases/85-collection-lifecycle/85-01-SUMMARY.md (SOLD_FIXTURE_ID, BEFORE/AFTER counts)
  </read_first>
  <action>
All DB writes target 127.0.0.1:54322 only. Record every output in the SUMMARY draft.

(1) `supabase status` (start if needed). Confirm `.env.development.local` exists (do not print it).

(2) Gates: `npm run build` must exit 0. Run `npx vitest run src/app/actions/__tests__/watches-lifecycle.test.ts src/app/actions/__tests__/moveWishlistToCollection.test.ts src/lib/__tests__/celebrate.test.ts src/lib/__tests__/disposal.test.ts src/components/profile/__tests__/CollectionTabContent.test.tsx src/components/profile/__tests__/MarkPreviouslyOwnedDialog.test.tsx src/components/profile/__tests__/ProfileWatchCard-lifecycle.test.tsx tests/components/watch/WatchForm.lifecycle.test.tsx tests/components/wear/WearPhotoOverlays.linkable.test.tsx tests/data/getWatchByIdForViewer.test.ts tests/lib/previouslyOwnedExclusion.test.ts src/data/__tests__/recommendations.test.ts`. Every Phase 85 test must pass; any failure must be in an it() that predates Phase 85 (prove with git blame) and is recorded as baseline.

(3) Schema present: `select count(*) from information_schema.columns where table_name='watches' and column_name in ('disposal_reason','sell_price','disposal_date')` returns 3. If 0, re-run 85-01 Task 3's apply command.

(4) Fixtures. OWNER = vintage-anna (fall back to another seeded user with ≥2 owned watches). VISITOR = viewer (or any other seeded user). Query and record:
  - OWNER id; owned watches (id, brand, model); wishlist and grail watches; `profile_settings.collection_public` and `profile_public` as ORIGINAL_* values; whether VISITOR follows OWNER.
  - DISPOSE_WATCH = an OWNER owned watch that has ≥1 wear_event (if none, pick any owned watch and note that Task 2 step 1 logs a wear on it first).
  - PROMOTE_EDIT_WATCH = an OWNER wishlist watch. If OWNER has none, insert one locally: copy a watches_catalog row OWNER has no watch for, `insert into watches (user_id, brand, model, reference, status, catalog_id, notes) ... 'wishlist' ... 'phase85 wishlist fixture'`.
  - PROMOTE_ADDFLOW_WATCH = a second OWNER wishlist watch (insert a second fixture the same way if needed); record its catalog brand/model for searching in the add flow.
  - GRAIL_WATCH = an OWNER grail watch; if none, insert a fixture copying another unowned catalog row with status 'grail' and notes 'phase85 grail fixture'.
  - Confirm SOLD_FIXTURE_ID from 85-01 still reads `previously_owned|sold`.
(5) Locally set OWNER `collection_public = true` and `profile_public = true` so the visitor walk starts from a known state.
(6) Start `npm run dev` in the background and confirm `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/login` returns 200. Write the walk sheet (OWNER username/id, VISITOR, follow status, DISPOSE_WATCH, PROMOTE_EDIT_WATCH, PROMOTE_ADDFLOW_WATCH search text, GRAIL_WATCH, SOLD_FIXTURE_ID, ORIGINAL_* values, fixture ids inserted).
  </action>
  <verify>
    <automated>npm run build && /opt/homebrew/opt/libpq/bin/psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -tAc "select count(*) from information_schema.columns where table_name='watches' and column_name in ('disposal_reason','sell_price','disposal_date')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Phase 85 targeted tests pass (baseline exceptions documented with blame)
    - Column query returns 3
    - Walk sheet records OWNER, VISITOR, follow status, DISPOSE_WATCH, PROMOTE_EDIT_WATCH, PROMOTE_ADDFLOW_WATCH, GRAIL_WATCH, SOLD_FIXTURE_ID and ORIGINAL_* settings
    - `curl` to /login returns 200
  </acceptance_criteria>
  <done>Gates are green, fixtures exist, and the dev server is running with a concrete walk sheet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Local-dev walk of LIFE-01..06 and cross-user gating</name>
  <files>(none — manual browser verification)</files>
  <read_first>
    - The Task 1 walk sheet in the SUMMARY draft
  </read_first>
  <action>
Present the walk below with Task 1's concrete values substituted, then wait. Do not proceed to Task 3 until the user replies "approved". If the user reports failures, record step numbers and observations in the SUMMARY for gap closure and stop.
  </action>
  <what-built>Phase 85: previously_owned status + disposal columns (85-01..85-04), lifecycle Server Actions (85-05), owner-only toggle and data split (85-06), wear link gating (85-07), disposal dialog + card menu + muted cards (85-08), promotion celebration (85-09), edit-form correction/undo (85-10). Dev server at http://localhost:3000 against local Supabase.</what-built>
  <how-to-verify>
    Sign in as OWNER (password `password123`).

    0. Migrated row: open /u/OWNER/collection, turn on "Show previously owned" and confirm the 85-01 sold fixture shows as a muted card with a "Sold" badge. Turn the toggle off.
    1. Disposal (LIFE-03, D-05/D-06/D-08):
       - If DISPOSE_WATCH has no wear yet, log one from /u/OWNER/worn first, then return to /u/OWNER/collection.
       - On the DISPOSE_WATCH card tap the ⋯ button at the image's top-right: a menu opens and the page does NOT navigate.
       - Choose "Mark as previously owned". The dialog shows Reason (Sold, Traded, Gifted, Lost, Stolen), Disposal date pre-filled with today whose picker won't allow a future day, and Amount received. "Mark as previously owned" is disabled until a reason is picked.
       - Use arrow keys to move through reasons; pick Traded. Type a future date manually (e.g. next year): submit shows "Disposal date can't be in the future."
       - Set the date to 2026-03-02, amount 1500, submit. Toast "Moved to previously owned"; you stay on the Collection tab; the card disappears without a refresh; the profile header's collection count drops by 1.
    2. Toggle (LIFE-05, D-15/D-16):
       - The toggle reads "Show previously owned (N)" and is off. Turn it on: previously-owned cards appear after all owned cards, dimmed, with "Traded · Mar 2026" top-left, no "Worn …" line or badge.
       - Type part of DISPOSE_WATCH's brand into search: its card still shows while the toggle is on.
       - Its ⋯ menu offers "Edit" only.
       - Go to the Worn tab, then back to Collection: record whether the toggle is off (expected off).
    3. Edit / undo (D-04/D-07):
       - Open an OWNED watch's edit page (/w/<id>/edit): the status dropdown lists Owned, Wishlist, Grail — no "Previously owned". Leave without saving.
       - From DISPOSE_WATCH's ⋯ → Edit: status shows "Previously owned" and the Reason/Disposal date/Amount fields show Traded / 2026-03-02 / 1500. Change amount to 1800 and save.
       - Reopen its edit page, change status to Owned: the disposal fields disable and "Changing status will clear the disposal details recorded above." appears. Save. Back on /u/OWNER/collection the watch is in the owned grid again (no badge, no dim).
       - Dispose of DISPOSE_WATCH again from the card (reason Sold, clear the date field, no amount) so the remaining steps have a previously-owned watch; badge reads "Sold".
    4. Celebration (LIFE-04, D-09..D-12):
       - Edit PROMOTE_EDIT_WATCH, set status Owned, save: confetti bursts and "Added to your collection!" shows and survives the navigation to /. No "Watch updated" toast, no prompts.
       - Edit GRAIL_WATCH, set status Owned, save: confetti + "Grail acquired!".
       - Open /watch/new, search for PROMOTE_ADDFLOW_WATCH, pick it, press "Move to Collection" on the wishlist dupe banner: confetti + "Added to your collection!".
       - Edit any owned watch's notes and save: only "Watch updated", no confetti.
       - DevTools → Rendering → Emulate prefers-reduced-motion: reduce. Revert PROMOTE_EDIT_WATCH to Wishlist (save), then set it back to Owned: toast shows, no confetti. Turn emulation off.
    5. Worn tab history (D-17): /u/OWNER/worn — DISPOSE_WATCH's wear still appears in Timeline and Calendar and opens /wear/[id]; DISPOSE_WATCH is NOT in the leaderboard or the "Log a wear" picker.
    6. Visitor gating (D-13/D-14): sign in as VISITOR in a private window.
       - /u/OWNER/collection: no "Show previously owned" toggle; no dimmed cards.
       - /u/OWNER/notes: DISPOSE_WATCH is not listed (give it a public note first as OWNER if it has none).
       - Visit http://localhost:3000/w/<DISPOSE_WATCH id>: 404.
       - Open /wear/<a wear id of DISPOSE_WATCH> (from the walk sheet or OWNER's Worn tab URL): the wear loads, and brand/model at the bottom of the photo is plain text, not a link. As OWNER the same text is a link.
    7. Recommender / similarity (LIFE-06, D-18/D-19), as OWNER:
       - Home (/): the "From collectors like you" rail does not show DISPOSE_WATCH's model.
       - From Search → Watches, open DISPOSE_WATCH's model catalog page: it shows the Collection Fit verdict (not the owned view), and the verdict does not list DISPOSE_WATCH as a similar or duplicate watch in your collection.
    8. Narrow width (375px devtools): the ⋯ trigger is tappable without opening the card, the dialog scrolls with its footer reachable, and the toolbar toggle fits. Real mobile Safari is verified on prod after deploy (project memory).
  </how-to-verify>
  <verify>
    <automated>MISSING — manual walk; Task 3 runs the SQL assertions that corroborate steps 1, 3 and 4</automated>
  </verify>
  <acceptance_criteria>
    - The user replies "approved"
    - Otherwise the user lists failing step numbers, recorded in the SUMMARY
  </acceptance_criteria>
  <done>The user has approved steps 0-8.</done>
  <resume-signal>Type "approved", or list the failing step numbers with what you saw</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-walk SQL assertions and fixture restore</name>
  <files>(none — read-only assertions plus local settings restore)</files>
  <read_first>
    - The Task 1 walk sheet values in the SUMMARY draft
  </read_first>
  <action>
Using psql against 127.0.0.1:54322 only:

(1) `select status, disposal_reason, sell_price, disposal_date from watches where id = '<DISPOSE_WATCH id>'` → `previously_owned|sold||` (second disposal: reason sold, no amount, no date). This also proves D-04 cleared the Traded/1800/2026-03-02 values before the re-disposal wrote new ones (a leftover sell_price 1800 or date would show here).
(2) `select status, disposal_reason, sell_price, disposal_date from watches where id in ('<PROMOTE_EDIT_WATCH>','<GRAIL_WATCH>','<PROMOTE_ADDFLOW_WATCH>')` → all `owned` with the three disposal columns NULL.
(3) `select count(*) from watches where status not in ('owned','wishlist','grail','previously_owned')` → 0.
(4) `select count(*) from watches where status <> 'previously_owned' and (disposal_reason is not null or sell_price is not null or disposal_date is not null)` → 0 (D-02/D-04 invariant across all users).
(5) `select count(*) from divestments where created_at > now() - interval '6 hours'` → 0 (D-03: nothing wrote divestments during the walk).
(6) `select status, disposal_reason from watches where id = '<SOLD_FIXTURE_ID>'` → `previously_owned|sold`.
(7) Restore OWNER `profile_settings.collection_public` / `profile_public` to ORIGINAL_* values. Leave walk-created/fixture rows in place (local data is re-seedable) and list their ids in the SUMMARY.
(8) Stop the background dev server.
  </action>
  <verify>
    <automated>/opt/homebrew/opt/libpq/bin/psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -tAc "select (select count(*) from watches where status not in ('owned','wishlist','grail','previously_owned')) || '|' || (select count(*) from watches where status <> 'previously_owned' and (disposal_reason is not null or sell_price is not null or disposal_date is not null))"</automated>
  </verify>
  <acceptance_criteria>
    - Verify query prints `0|0`
    - Query (1) returns previously_owned|sold with NULL sell_price and disposal_date
    - Query (2) returns three owned rows with NULL disposal columns
    - Query (5) returns 0 and query (6) returns previously_owned|sold
    - OWNER profile_settings equal ORIGINAL_* values
  </acceptance_criteria>
  <done>The database confirms disposal persistence, undo clearing, promotion writes, the rename invariant and no divestments writes; fixtures are restored.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Operator-applied prod migration, then deploy</name>
  <files>(none — operator actions against the linked prod project)</files>
  <read_first>
    - supabase/migrations/20260913000000_phase85_collection_lifecycle.sql
    - Task 3 results in the SUMMARY (must be all green before this step)
  </read_first>
  <action>
Present these operator steps and wait. Claude must NOT run `supabase db push --linked`, `drizzle-kit push`, or `git push` itself. Order matters: the new code selects the new columns, so the prod migration must land before the deploy (old code tolerates the migrated schema).
  </action>
  <what-built>A verified local implementation and an idempotent migration whose transaction aborts on any failed post-flight assertion.</what-built>
  <how-to-verify>
    1. `supabase migration list --linked` — confirm `20260913000000_phase85_collection_lifecycle` is the only pending migration.
    2. In the Supabase SQL editor (prod, read-only query): `select status, count(*) from watches group by 1 order by 1;` — confirm only owned / wishlist / sold / grail appear and note the sold count.
    3. `supabase db push --linked` — must complete without an exception (NOTICE shows the migrating sold count).
    4. Prod SQL editor: `select status, count(*) from watches group by 1 order by 1;` (sold gone, previously_owned = prior sold count) and `select count(*) from pg_policies where tablename='comments' and policyname in ('comments_select','comments_insert') and (coalesce(qual,'')||coalesce(with_check,'')) like '%previously_owned%';` → 2.
    5. Push the Phase 85 commits to main and let the deploy finish.
    6. On a real iPhone (Safari) against prod: tap ⋯ on a collection card (menu opens, no navigation), open the dialog (no input zoom), cancel.
    Reply "deployed" with the step-2 and step-4 counts, or describe what failed.
  </how-to-verify>
  <verify>
    <automated>MISSING — operator-only prod actions; counts reported in the resume signal are recorded in the SUMMARY</automated>
  </verify>
  <acceptance_criteria>
    - The operator replies "deployed" with the before/after status counts, recorded in the SUMMARY
    - The reported previously_owned count after push equals the reported sold count before push, and the reported sold count after push is 0
  </acceptance_criteria>
  <done>Prod carries the Phase 85 schema and the code is deployed.</done>
  <resume-signal>Type "deployed" with the before/after counts, or describe the failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human walk ↔ local Supabase | Local-only fixture inserts and settings edits |
| operator ↔ prod Supabase | One-way status rewrite + RLS policy re-create |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-LEAK | Information Disclosure | previously-owned history across users | mitigate | Walk step 6 verifies Collection, Notes, /w/[id] and /wear/[id] as a second real session. |
| T-85-27 | Tampering | local fixture edits | mitigate | All psql writes target 127.0.0.1:54322; ORIGINAL_* settings recorded in Task 1 and restored in Task 3; nothing runs against the linked project. |
| T-85-28 | Tampering / Availability | prod migration | mitigate | Operator-only, after local walk; migration list check before push; single transaction with post-flight assertions; before/after counts compared; DB migrated before code deploy. |
</threat_model>

<verification>
- Local walk approved.
- Post-walk SQL assertions pass.
- Operator confirms prod migration and deploy with matching counts.
</verification>

<success_criteria>
All six LIFE requirements are demonstrated against a real database, including cross-user privacy and SQL-level confirmation of disposal persistence, undo clearing and the rename invariant, and prod is migrated before deploy.
</success_criteria>

<output>
Create `.planning/phases/85-collection-lifecycle/85-11-SUMMARY.md` when done
</output>
