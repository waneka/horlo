---
phase: 85-collection-lifecycle
plan: 09
type: execute
wave: 5
depends_on: ["85-05"]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/celebrate.ts
  - src/lib/__tests__/celebrate.test.ts
  - src/components/watch/AddWatchFlow.tsx
  - src/components/watch/AddWatchFlow.test.tsx
  - src/components/watch/WatchForm.tsx
  - tests/components/watch/WatchForm.celebration.test.tsx
autonomous: false
requirements: [LIFE-04]

must_haves:
  truths:
    - "LIFE-04 / D-09: the client celebrates only when the server returns promoted: true — from the edit form (editWatch, wishlist or grail → owned) and from the add-flow 'Move to collection' (moveWishlistToCollection) — never from its own guess"
    - "D-10: a promotion fires a canvas-confetti burst plus a celebratory sonner toast 'Added to your collection!' that is distinct from the normal 'Watch updated' / 'Moved to collection' toasts, and both are triggered immediately BEFORE the existing router.push so they survive navigation"
    - "D-10 / Claude's discretion: under prefers-reduced-motion: reduce the confetti is skipped and the toast still shows"
    - "D-11: grail → owned uses the same effect with the toast 'Grail acquired!' (no bigger effect)"
    - "D-12: the celebration toast has no action button, no price-paid prompt and no photo link"
    - "A non-promotion edit still shows exactly one 'Watch updated' toast; a non-promotion move still shows 'Moved to collection' with its View action"
    - "canvas-confetti is installed only after a human confirms package legitimacy (blocking-human gate)"
  artifacts:
    - path: "src/lib/celebrate.ts"
      provides: "celebratePromotion(promotedFrom) — reduced-motion-gated confetti + celebratory toast"
      exports: ["celebratePromotion"]
    - path: "src/lib/__tests__/celebrate.test.ts"
      provides: "reduced-motion and copy tests"
      min_lines: 40
  key_links:
    - from: "src/components/watch/AddWatchFlow.tsx handleMoveToCollection"
      to: "src/lib/celebrate.ts"
      via: "if (result.data.promoted) celebratePromotion(result.data.promotedFrom) before router.push(dest)"
      pattern: "celebratePromotion\\("
    - from: "src/components/watch/WatchForm.tsx edit branch"
      to: "src/lib/celebrate.ts"
      via: "result.data.promoted → celebratePromotion before router.push('/')"
      pattern: "celebratePromotion\\("
---

<objective>
Ship the LIFE-04 promotion moment: after a human confirms the `canvas-confetti` package is legitimate, install it, wrap it in a tiny reduced-motion-aware `celebratePromotion` helper that also fires the celebratory toast, and call it from the two promotion paths using the server's `promoted` / `promotedFrom` signal (85-05).

Purpose: the promotion must read differently from a normal edit (D-10) and survive the post-save navigation. Firing before `router.push` sidesteps the Router Cache stale-instance hazard (no destination-page mount effect is involved); sonner's portal toast and canvas-confetti's body-appended canvas both outlive the soft navigation (85-RESEARCH Pattern 3).

Output: dependency added, `src/lib/celebrate.ts`, and celebration wiring in AddWatchFlow and WatchForm.

Discretion choices:
- Library: canvas-confetti (no React wrapper, no runtime fetch, ~6.3M weekly downloads). Loaded with a dynamic `import('canvas-confetti')` inside the helper so it never evaluates during SSR of these client components.
- Toast copy: 'Added to your collection!' (wishlist) / 'Grail acquired!' (grail), per UI-SPEC.
- WatchForm edit mode stops passing `successMessage` to useFormFeedback and fires its own toast after the result is known, so a promotion shows only the celebration toast and a normal edit shows only 'Watch updated'.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/85-collection-lifecycle/85-CONTEXT.md
@.planning/phases/85-collection-lifecycle/85-RESEARCH.md
@.planning/phases/85-collection-lifecycle/85-UI-SPEC.md
@.planning/phases/85-collection-lifecycle/85-05-SUMMARY.md

<interfaces>
From 85-05:
  src/lib/types.ts: export interface WatchEditResult { watch: Watch; promoted: boolean; promotedFrom: 'wishlist' | 'grail' | null }
  editWatch(watchId, data): Promise<ActionResult<WatchEditResult>>
  moveWishlistToCollection(watchId, opts?): Promise<ActionResult<WatchEditResult>>

src/components/watch/AddWatchFlow.tsx handleMoveToCollection (~L564-590): await moveWishlistToCollection → on failure toast.error → dest = initialReturnTo ?? defaultDestinationForStatus('owned', viewerUsername) → if actionHref toast.success('Moved to collection', { action: { label: 'View', onClick } }) → setUrl('') → setState({ kind: 'search-idle' }) → router.push(dest)

src/components/watch/WatchForm.tsx handleSubmit (~L203-316): successMessage = mode === 'edit' ? 'Watch updated' : ...; run(async () => { ... result = mode === 'edit' && watch ? await editWatch(watch.id, formData) : await addWatch(submitData); if (result.success && mode === 'create') {...router.push(dest)} else if (result.success) { router.push('/') }; return result }, mode === 'create' ? (...) : { successMessage })

src/lib/hooks/useFormFeedback.ts run(action, opts): toast fires only if opts.successMessage or opts.successAction is provided; otherwise success state is set with no toast.

tests/setup.tsx L49-60: window.matchMedia is stubbed globally for jsdom (override per test with vi.spyOn / Object.defineProperty).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm canvas-confetti package legitimacy before install</name>
  <files>(none — human verification)</files>
  <read_first>
    - .planning/phases/85-collection-lifecycle/85-RESEARCH.md (§Package Legitimacy Audit — package tagged [ASSUMED] provenance, slopcheck [OK])
  </read_first>
  <action>
Present the verification steps below to the user and wait. This gate is never auto-approvable (workflow.auto_advance is ignored for package legitimacy). Do not run any npm install until the user replies "approved". If the user rejects, stop the plan and record the rejection in the SUMMARY; the celebration then needs a library decision before 85-09 can resume.
  </action>
  <what-built>Nothing yet. 85-RESEARCH selected `canvas-confetti` (npm) and `@types/canvas-confetti` (dev) for the D-10 confetti burst. The package name came from training knowledge, so provenance is [ASSUMED] even though the registry check and slopcheck passed.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/canvas-confetti — confirm the publisher/repository is github.com/catdad/canvas-confetti, weekly downloads are in the millions, and the latest version is 1.9.x.
    2. Open https://github.com/catdad/canvas-confetti — confirm it is the long-established repo (many years of history, thousands of stars) and the README documents `confetti({ particleCount, spread, origin })` and a `disableForReducedMotion` option.
    3. Open https://www.npmjs.com/package/@types/canvas-confetti — confirm it is published from DefinitelyTyped.
    4. Reply "approved" to install both, or describe concerns.
  </how-to-verify>
  <verify>
    <automated>MISSING — human legitimacy verification; Task 2 verifies the installed package resolves</automated>
  </verify>
  <acceptance_criteria>
    - The user replies "approved" (recorded verbatim in the SUMMARY), or the rejection is recorded and the plan stops
  </acceptance_criteria>
  <done>A human has confirmed canvas-confetti and @types/canvas-confetti are the legitimate packages.</done>
  <resume-signal>Type "approved" to install canvas-confetti, or describe your concerns</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install canvas-confetti and build celebratePromotion</name>
  <files>package.json, package-lock.json, src/lib/celebrate.ts, src/lib/__tests__/celebrate.test.ts</files>
  <read_first>
    - package.json (dependencies / devDependencies blocks)
    - tests/setup.tsx (L40-70 matchMedia stub)
    - src/lib/wear.ts (small-utility module style and header-comment convention)
    - node_modules/canvas-confetti/README.md (after install — confirm default export call signature and options before wiring)
  </read_first>
  <behavior>
    - matchMedia('(prefers-reduced-motion: reduce)').matches false → celebratePromotion('wishlist') calls the mocked confetti default export once with { particleCount: 100, spread: 70, origin: { y: 0.6 } } and toast.success once with 'Added to your collection!'
    - celebratePromotion('grail') → toast.success('Grail acquired!') and confetti called
    - reduced motion true → confetti NOT called, toast.success('Added to your collection!') still called
    - celebratePromotion(null) → toast copy 'Added to your collection!'
    - toast.success is called with only the message (no second options argument carrying an action)
  </behavior>
  <action>
(1) Run `npm install canvas-confetti` and `npm install -D @types/canvas-confetti`. Confirm package.json lists canvas-confetti under dependencies and @types/canvas-confetti under devDependencies. Read node_modules/canvas-confetti/README.md to confirm the API matches before coding.

(2) Write src/lib/__tests__/celebrate.test.ts first: `vi.mock('canvas-confetti', () => ({ default: vi.fn() }))`, `vi.mock('sonner', () => ({ toast: { success: vi.fn() } }))`; control reduced motion by redefining window.matchMedia per test to return { matches: <bool>, media, addEventListener, removeEventListener, addListener, removeListener, onchange: null, dispatchEvent }. Because the helper awaits a dynamic import, `await celebratePromotion(...)` in tests.

(3) Create src/lib/celebrate.ts (no 'use client' needed — only imported by client components; no React import). Header comment: Phase 85 LIFE-04 / D-10 / D-11 / D-12; fire BEFORE router.push (Router Cache stale-instance memory; toast portal + body canvas survive soft navigation); reduced motion skips confetti only. Export `async function celebratePromotion(promotedFrom: 'wishlist' | 'grail' | null): Promise<void>`: first `toast.success(promotedFrom === 'grail' ? 'Grail acquired!' : 'Added to your collection!')` (synchronous, so the toast is queued before any navigation); then if `typeof window !== 'undefined'` and `!window.matchMedia('(prefers-reduced-motion: reduce)').matches`, `const { default: confetti } = await import('canvas-confetti')` and call `confetti({ particleCount: 100, spread: 70, origin: { y: 0.6 } })`. Wrap the import+call in try/catch that console.error's and swallows (celebration is non-critical).
  </action>
  <verify>
    <automated>node -e "require.resolve('canvas-confetti')" && npx vitest run src/lib/__tests__/celebrate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); process.exit(p.dependencies['canvas-confetti'] && p.devDependencies['@types/canvas-confetti'] ? 0 : 1)"` exits 0
    - `grep -c "prefers-reduced-motion: reduce" src/lib/celebrate.ts` returns 1
    - `grep -c "import('canvas-confetti')" src/lib/celebrate.ts` returns 1 and there is no top-level static `import confetti` line
    - `grep -c "Grail acquired!" src/lib/celebrate.ts` returns 1 and `grep -c "Added to your collection!" src/lib/celebrate.ts` returns 1
    - All <behavior> cases pass
  </acceptance_criteria>
  <done>A single helper owns confetti + celebratory copy with a reduced-motion gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Celebrate from AddWatchFlow "Move to collection" and the WatchForm edit save</name>
  <files>src/components/watch/AddWatchFlow.tsx, src/components/watch/AddWatchFlow.test.tsx, src/components/watch/WatchForm.tsx, tests/components/watch/WatchForm.celebration.test.tsx</files>
  <read_first>
    - src/components/watch/AddWatchFlow.tsx (L1-40 imports; L560-595 handleMoveToCollection)
    - src/components/watch/AddWatchFlow.test.tsx (L1-60 mocks; L395-420 T-70-04 move test)
    - src/components/watch/WatchForm.tsx (L1-45 imports; L131-316 state + handleSubmit)
    - tests/components/watch/WatchForm.test.tsx (L1-60 mocks incl. editWatch + router; the edit-mode hydration test ~L198-230)
    - src/lib/hooks/useFormFeedback.ts (L122-200 — toast suppression when no successMessage/successAction)
  </read_first>
  <behavior>
    - AddWatchFlow: moveWishlistToCollection resolves { success: true, data: { watch: {...}, promoted: true, promotedFrom: 'wishlist' } } → celebratePromotion called once with 'wishlist', toast.success NOT called with 'Moved to collection', and router.push('/u/tester/collection') is called AFTER celebratePromotion (assert via mock.invocationCallOrder)
    - AddWatchFlow: data { promoted: false, promotedFrom: null } → celebratePromotion not called; 'Moved to collection' toast with View action still fires; push still called
    - WatchForm edit mode, watch status 'wishlist', user picks Owned, editWatch resolves { success: true, data: { watch, promoted: true, promotedFrom: 'wishlist' } } → celebratePromotion('wishlist') called before router.push('/'); toast.success('Watch updated') NOT called
    - WatchForm edit mode, promotedFrom 'grail' → celebratePromotion('grail')
    - WatchForm edit mode, promoted false → toast.success('Watch updated') called exactly once; celebratePromotion not called; router.push('/')
    - WatchForm create mode behavior unchanged (existing WatchForm tests still pass)
  </behavior>
  <action>
Write tests first (mock '@/lib/celebrate' → { celebratePromotion: vi.fn() } in both files; update the existing T-70-04 mock data to the WatchEditResult shape `{ watch: { id: 'wish-id-001', status: 'owned' }, promoted: true, promotedFrom: 'wishlist' }` and add the non-promoted variant), confirm RED, then:

(1) AddWatchFlow.tsx: `import { celebratePromotion } from '@/lib/celebrate'`. In handleMoveToCollection replace the toast block with: `if (result.data.promoted) { void celebratePromotion(result.data.promotedFrom) } else if (actionHref) { toast.success('Moved to collection', { action: { label: 'View', onClick: () => router.push(actionHref) } }) }` — keep setUrl(''), setState({ kind: 'search-idle' }) and router.push(dest) after it, in that order. Comment: Phase 85 D-09/D-10 — fire before router.push (Pitfall 3).

(2) WatchForm.tsx: `import { celebratePromotion } from '@/lib/celebrate'`. In the run() closure's edit branch (`else if (result.success)`), before `router.push('/')`: `if ('promoted' in result.data && result.data.promoted) { void celebratePromotion(result.data.promotedFrom) } else { toast.success('Watch updated') }` (import `toast` from 'sonner' if not already imported). Change the run() options for edit mode from `{ successMessage }` to `{}` so useFormFeedback does not fire a second toast; keep create-mode options exactly as they are. Keep the `successMessage` constant for create mode; if TS flags it unused in edit mode, restructure minimally. Add a comment citing D-09/D-10/D-12 (no action button, no prompts).
Narrow `result.data` safely: the create branch uses `'id' in result.data`; the edit branch uses `'promoted' in result.data` so the addWatch/editWatch union type-checks.
  </action>
  <verify>
    <automated>npx vitest run src/components/watch/AddWatchFlow.test.tsx tests/components/watch/WatchForm.celebration.test.tsx tests/components/watch/WatchForm.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "celebratePromotion(" src/components/watch/AddWatchFlow.tsx` returns 1 and `grep -c "celebratePromotion(" src/components/watch/WatchForm.tsx` returns 1
    - In AddWatchFlow.tsx the `celebratePromotion(` line number is lower than the `router.push(dest)` line number inside handleMoveToCollection
    - `grep -c "'Moved to collection'" src/components/watch/AddWatchFlow.tsx` still returns 1
    - All <behavior> cases pass; existing WatchForm and AddWatchFlow tests pass (or pre-existing failures documented with blame)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both promotion paths celebrate from the server signal before navigating, and non-promotion saves keep their normal single toast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules / client bundle | Third-party code executes in every user's browser |
| Server Action result → client celebration | Client trusts only the server's promoted flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-SC | Tampering | npm install canvas-confetti / @types/canvas-confetti | mitigate | slopcheck [OK] in research + blocking-human checkpoint (Task 1) verifying npmjs.com and the catdad GitHub repo before install; lockfile committed. |
| T-85-22 | Spoofing | celebration trigger | accept | Purely cosmetic; driven by server-computed promoted flag (85-05); a client forging it only fools itself. |
| T-85-23 | Denial of Service (a11y) | confetti animation | mitigate | prefers-reduced-motion skips confetti; toast remains; helper errors are swallowed so a load failure never blocks navigation. |
</threat_model>

<verification>
- Legitimacy checkpoint approved.
- celebrate, AddWatchFlow and WatchForm tests pass.
- `npm run build` exits 0.
</verification>

<success_criteria>
LIFE-04: promoting a wishlist or grail watch to owned shows confetti plus a distinct celebratory toast on both promotion paths, respecting reduced motion.
</success_criteria>

<output>
Create `.planning/phases/85-collection-lifecycle/85-09-SUMMARY.md` when done
</output>
