---
phase: 85-collection-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - src/lib/types.ts
  - src/lib/constants.ts
  - src/data/watches.ts
  - src/data/recommendations.ts
  - tests/data/getWatchByIdForViewer.test.ts
  - src/data/__tests__/recommendations.test.ts
  - tests/lib/previouslyOwnedExclusion.test.ts
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-06]

must_haves:
  truths:
    - "LIFE-01 / D-01: WatchStatus is 'owned' | 'wishlist' | 'previously_owned' | 'grail' in src/lib/types.ts and WATCH_STATUSES / WATCH_STATUS_LABELS carry previously_owned (the remaining source and fixture literals are swept by 85-03 and 85-04)"
    - "LIFE-02 / D-02: Watch carries optional disposalReason / sellPrice / disposalDate, round-tripped by mapRowToWatch and written by mapDomainToRow with the 'key in data' idiom so an explicitly-present undefined clears the column to NULL"
    - "D-14: a non-owner's getWatchByIdForViewer predicate matches status IN ('owned','grail') for collection_public — previously_owned never matches for visitors, while the owner short-circuit still returns the owner's own previously-owned watch"
    - "D-18: the recommender's viewer exclusion set includes previously_owned, so a model the viewer disposed of is not recommended back to them"
    - "D-19: analyzeSimilarity, computeGapFill and computeTasteOverlap keep their owned/grail allowlists and a regression test proves a previously_owned watch identical to the target is ignored by all three"
    - "D-17: Worn tab owned-only scoping (wornTabScope status === 'owned') is unchanged"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "WatchStatus union with previously_owned; DisposalReason; Watch disposal fields"
      contains: "previously_owned"
    - path: "src/lib/constants.ts"
      provides: "WATCH_STATUSES, WATCH_STATUS_LABELS, DISPOSAL_REASONS, DISPOSAL_REASON_LABELS"
      contains: "DISPOSAL_REASON_LABELS"
    - path: "src/data/watches.ts"
      provides: "disposal field mapping + D-14 visitor predicate"
      contains: "IN ('owned','grail')"
    - path: "tests/lib/previouslyOwnedExclusion.test.ts"
      provides: "LIFE-06 similarity/gap/overlap regression"
      min_lines: 40
  key_links:
    - from: "src/data/watches.ts getWatchByIdForViewer"
      to: "/w/[ref] notFound for visitors"
      via: "SQL predicate status IN ('owned','grail')"
      pattern: "IN \\('owned','grail'\\)"
    - from: "src/data/recommendations.ts exclusion loop"
      to: "excludeKey Set"
      via: "v.status === 'previously_owned'"
      pattern: "previously_owned"
---

<objective>
Start the `sold` → `previously_owned` rename at the domain core: types and constants, DAL mapping for the new disposal fields, the visitor-visibility predicate (D-14), the recommender exclusion set (D-18), and a LIFE-06 regression test proving similarity/gap-fill/taste-overlap ignore previously-owned watches.

Purpose: after 85-01 the DB and Drizzle schema no longer know `sold`; this plan makes the domain types and the data layer agree. LIFE-06 exclusion is mostly already-safe allowlists (research audit) — this plan makes the two real edits and locks the rest with a regression test.

Output: domain types/constants/DAL speak previously_owned; disposal fields mapped; visitors gated; recommender exclusion extended; targeted vitest green.

Scope boundary: this is the first of three rename plans. 85-03 (same wave, no shared files) retires the divestments dual-write and updates remaining source references; 85-04 (next wave) sweeps test fixtures and owns the `npm run build` gate. Do NOT run `npm run build` as this plan's gate — it is EXPECTED to fail on `'sold'` references owned by 85-03/85-04. Do not edit files outside this plan's files_modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/85-collection-lifecycle/85-CONTEXT.md
@.planning/phases/85-collection-lifecycle/85-RESEARCH.md
@.planning/phases/85-collection-lifecycle/85-PATTERNS.md
@.planning/phases/85-collection-lifecycle/85-01-SUMMARY.md

<interfaces>
src/lib/types.ts L1 (current): export type WatchStatus = 'owned' | 'wishlist' | 'sold' | 'grail'
src/lib/types.ts Watch provenance block (~L111-118): serial?, yearOfAcquisition?, condition?: ConditionGrade, boxPapers?, serviceHistory?, paidCurrency?, purchaseDate?: string
src/lib/constants.ts L131-136: export const WATCH_STATUSES = ['owned','wishlist','sold','grail'] as const
src/lib/constants.ts ~L150: export const CONDITION_GRADE_LABELS: Record<ConditionGrade, string> = {...}

src/data/watches.ts:
  function mapRowToWatch(row: WatchRow): Watch   (L17-60; provenance block L51-58)
  function mapDomainToRow(data: Partial<Watch>)  (L65-111; provenance block L101-108 uses `if ('x' in data) row.x = data.x ?? null`)
  export async function updateWatch(userId, watchId, data: Partial<Watch>): Promise<Watch>  (L401 — calls mapDomainToRow)
  export async function getWatchByIdForViewer(viewerId, watchId)  (L258; predicate L291-294 `IN ('owned','sold','grail')`)

src/data/recommendations.ts L271-277:
  for (const v of viewerWatches) { if (v.status === 'owned' || v.status === 'wishlist' || v.status === 'grail') { excluded.add(norm(v)) } }

src/lib/similarity.ts: export function analyzeSimilarity(targetWatch: Watch, collection: Watch[], preferences: UserPreferences): SimilarityResult  (L303; owned||grail filter L311-314)
src/lib/gapFill.ts: export function computeGapFill(...)  (L63; owned||grail L68)
src/lib/tasteOverlap.ts: export function computeTasteOverlap(...)  (L51; owned L59-60)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain types, constants, DAL mapping, D-14 predicate and D-18 exclusion</name>
  <files>src/lib/types.ts, src/lib/constants.ts, src/data/watches.ts, src/data/recommendations.ts, tests/data/getWatchByIdForViewer.test.ts, src/data/__tests__/recommendations.test.ts, tests/lib/previouslyOwnedExclusion.test.ts</files>
  <read_first>
    - src/lib/types.ts (L1-50 and the Watch interface ~L60-125)
    - src/lib/constants.ts (L120-185)
    - src/data/watches.ts (L1-112 mapping; L245-320 getWatchByIdForViewer)
    - src/data/recommendations.ts (L30-60 excludeKey; L255-300 exclusion loop)
    - tests/data/getWatchByIdForViewer.test.ts (PART A mock chain L1-60; Unit 8 at ~L180-192)
    - src/data/__tests__/recommendations.test.ts (L620-720 — Phase 81 exclusion-key test to mirror)
    - src/lib/similarity.ts (L300-330), src/lib/gapFill.ts (L55-80), src/lib/tasteOverlap.ts (L45-70)
    - tests/lib/tasteOverlap.test.ts (fixture builder style for Watch objects)
  </read_first>
  <behavior>
    - getWatchByIdForViewer: the captured `watch.where` SQL chunks, serialized to text, contain `IN ('owned','grail')` and contain neither `previously_owned` nor `'sold'` (D-14)
    - getWatchByIdForViewer: owner row with status 'previously_owned' (userId === viewer) returns non-null with isOwner true
    - recommendations: viewer has a previously_owned watch with brandId/familyId X; a catalog top-up row with the same FKs is DROPPED from the rail; a control row surfaces (D-18)
    - analyzeSimilarity(target, [previouslyOwnedTwinOfTarget], prefs) returns the same label/score as analyzeSimilarity(target, [], prefs) and mostSimilarWatches is empty (D-19)
    - computeGapFill and computeTasteOverlap produce the same output with and without an extra previously_owned watch in the collection (D-19)
  </behavior>
  <action>
(a) src/lib/types.ts: change L1 to `export type WatchStatus = 'owned' | 'wishlist' | 'previously_owned' | 'grail'` (D-01). Next to ConditionGrade add `// Phase 85 D-02: disposal reason pgEnum mirror (LIFE-02)` and `export type DisposalReason = 'sold' | 'lost' | 'gifted' | 'stolen' | 'traded'`. In the Watch interface directly after `purchaseDate?` add a `// Phase 85 D-02 — disposal metadata (all nullable; LIFE-02)` block: `disposalReason?: DisposalReason`, `sellPrice?: number`, `disposalDate?: string   // ISO 'YYYY-MM-DD'`.

(b) src/lib/constants.ts: WATCH_STATUSES becomes `['owned', 'wishlist', 'grail', 'previously_owned'] as const`. Add `export const WATCH_STATUS_LABELS: Record<WatchStatus, string>` = owned 'Owned', wishlist 'Wishlist', grail 'Grail', previously_owned 'Previously owned'. Add a Phase 85 block: `export const DISPOSAL_REASONS = ['sold', 'traded', 'gifted', 'lost', 'stolen'] as const` on ONE line (UI-SPEC radiogroup order) and `export const DISPOSAL_REASON_LABELS: Record<DisposalReason, string>` = sold 'Sold', traded 'Traded', gifted 'Gifted', lost 'Lost', stolen 'Stolen' (keys unquoted, so 85-04's `'sold'` grep gate is not tripped). Import WatchStatus / DisposalReason types from '@/lib/types' the same way ConditionGrade is imported.

(c) src/data/watches.ts: in mapRowToWatch add `disposalReason: row.disposalReason ?? undefined, sellPrice: row.sellPrice ?? undefined, disposalDate: row.disposalDate ?? undefined,` after purchaseDate. In mapDomainToRow add, after the purchaseDate line: `if ('disposalReason' in data) row.disposalReason = data.disposalReason ?? null`, same for sellPrice and disposalDate — use the `'key' in data` idiom, NOT `!== undefined`, because 85-05's D-04 undo passes the keys present with undefined values to clear them. In getWatchByIdForViewer change the predicate literal to `IN ('owned','grail')` and update the docblock (L248-253) and the inline comment (L289-290) to say previously_owned watches are owner-only per Phase 85 D-14 (visitors get null → notFound).

(d) src/data/recommendations.ts: in the exclusion loop add `|| v.status === 'previously_owned'` (D-18) and extend the comment above it to "Exclude viewer's owned/wishlist/grail/previously_owned (C-02 + Phase 85 D-18)". Do not touch the seed/candidate `status === 'owned'` checks at ~L136/184/293 (D-18: taste seed stays owned-only).

(e) Tests (write the failing assertions first, then the edits above make them pass):
  - tests/data/getWatchByIdForViewer.test.ts: replace Unit 8 ('non-owner + sold watch uses collection_public') with two cases: "Unit 8: predicate excludes previously_owned for non-owners (D-14)" which calls getWatchByIdForViewer, finds the `watch.where` call in `calls`, walks the drizzle SQL object's queryChunks recursively (StringChunk values are string arrays; nested SQL objects have their own queryChunks) to build one string, and asserts it contains `IN ('owned','grail')` and does not contain `previously_owned` or `'sold'`; and "Unit 8b: owner still receives own previously_owned watch" with a row whose userId === VIEWER and status 'previously_owned' asserting isOwner true. The single assertion line that checks for the absent `'sold'` literal MUST end with the trailing comment `// retired status literal, now only a disposal reason` — 85-04's repo-wide gate is `grep -rn "'sold'" src tests | grep -vi disposal`, and this comment exempts the one legitimate negative assertion. Keep the PART B integration block untouched except any 'sold' literal → 'previously_owned' with the expectation flipped to not-visible for non-owners if such a case exists.
  - src/data/__tests__/recommendations.test.ts: add one case modeled on the Phase 81 exclusion-key test: viewer's watch has status 'previously_owned' with brandId 'omega-uuid' / familyId 'speedmaster-uuid'; top-up rows include a catalog row with those FKs and a control row; assert the matching row id is absent and the control id is present.
  - tests/lib/previouslyOwnedExclusion.test.ts (new; pure functions, so no `// @vitest-environment node` needed): build a Speedmaster-like target and an identical watch (same brand/model/styleTags/roleTags/movement/caseSize) with status 'previously_owned' and a different id. Assert analyzeSimilarity parity vs empty collection, computeGapFill parity, computeTasteOverlap parity as described in <behavior>. Read each function's signature before writing arguments.

Do not run `npm run build` here (see objective scope boundary); vitest does not type-check, so the targeted tests are the gate.
  </action>
  <verify>
    <automated>npx vitest run tests/data/getWatchByIdForViewer.test.ts src/data/__tests__/recommendations.test.ts tests/lib/previouslyOwnedExclusion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type WatchStatus = 'owned' | 'wishlist' | 'previously_owned' | 'grail'" src/lib/types.ts` returns 1
    - `grep -c "export type DisposalReason = 'sold' | 'lost' | 'gifted' | 'stolen' | 'traded'" src/lib/types.ts` returns 1
    - `grep -c "DISPOSAL_REASONS = \['sold', 'traded', 'gifted', 'lost', 'stolen'\] as const" src/lib/constants.ts` returns 1
    - `grep -c "'disposalReason' in data" src/data/watches.ts` returns 1, and likewise for `'sellPrice' in data` and `'disposalDate' in data`
    - `grep -c "IN ('owned','grail')" src/data/watches.ts` returns 1 and `grep -c "IN ('owned','sold','grail')" src/data/watches.ts` returns 0
    - `grep -c "v.status === 'previously_owned'" src/data/recommendations.ts` returns 1
    - `grep -rn "'sold'\|\"sold\"" src/lib/types.ts src/lib/constants.ts src/data/watches.ts src/data/recommendations.ts tests/data/getWatchByIdForViewer.test.ts src/data/__tests__/recommendations.test.ts tests/lib/previouslyOwnedExclusion.test.ts | grep -vi disposal` prints nothing
    - The three targeted vitest files pass
  </acceptance_criteria>
  <done>Types/constants/DAL speak previously_owned, disposal fields round-trip, visitors cannot load a previously-owned watch, and LIFE-06 exclusion is enforced and regression-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → /w/[ref] via getWatchByIdForViewer | Service-role Drizzle query; the WHERE clause is the real privacy gate (RLS bypassed) |
| viewer → home recommendation rail | Server-computed exclusion set over viewer's own watches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-05 | Information Disclosure | getWatchByIdForViewer non-owner predicate | mitigate | Literal list narrowed to IN ('owned','grail'); Unit 8 serializes the SQL chunks and asserts previously_owned and 'sold' are absent (D-14). |
</threat_model>

<verification>
- Task 1 targeted vitest passes.
- Grep acceptance criteria pass on this plan's files.
- `npm run build` is intentionally NOT a gate here; 85-04 owns it after 85-03 lands.
</verification>

<success_criteria>
The domain types carry one leave-the-collection status, disposal fields are mapped end to end in the DAL, visitors are gated out of previously-owned watch pages, and the recommender excludes disposed models — all proven by targeted tests.
</success_criteria>

<output>
Create `.planning/phases/85-collection-lifecycle/85-02-SUMMARY.md` when done
</output>
