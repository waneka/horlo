---
phase: 85-collection-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - src/lib/types.ts
  - src/lib/constants.ts
  - src/data/watches.ts
  - src/data/recommendations.ts
  - tests/data/getWatchByIdForViewer.test.ts
  - src/data/__tests__/recommendations.test.ts
  - tests/lib/previouslyOwnedExclusion.test.ts
  - src/app/actions/watches.ts
  - src/app/actions/divestments.ts
  - tests/integration/phase37-rls.test.ts
  - src/components/filters/StatusToggle.tsx
  - src/components/watch/WatchCard.tsx
  - tests/static/WatchCard.sold-badge.test.tsx
  - src/lib/watchFlow/destinations.ts
  - src/lib/watchFlow/destinations.test.ts
  - src/components/watch/ConfirmStep.tsx
  - src/data/follows.ts
  - src/data/discovery.ts
  - src/data/catalog.ts
  - src/app/actions/__tests__/moveWishlistToCollection.test.ts
  - src/app/actions/__tests__/watches-recs-invalidation.test.ts
  - src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx
  - src/components/watch/ConfirmStep.test.tsx
  - src/components/watch/AddWatchFlow.test.tsx
  - src/data/__tests__/reactions-comments-gate.test.ts
  - tests/integration/phase59-unified-route.test.ts
  - tests/lib/tasteTags.test.ts
  - tests/data/comments.test.ts
  - tests/data/searchCatalogWatches.test.ts
  - tests/data/getFollowedOwnersForCatalog.test.ts
  - tests/data/getCollectorsForCatalog.test.ts
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-06]

must_haves:
  truths:
    - "LIFE-01 / D-01: WatchStatus is 'owned' | 'wishlist' | 'previously_owned' | 'grail'; no source or test file uses 'sold' as a status value (only as a disposal reason)"
    - "LIFE-02 / D-02: Watch carries optional disposalReason / sellPrice / disposalDate, round-tripped by mapRowToWatch and written by mapDomainToRow with the 'key in data' idiom so an explicitly-present undefined clears the column to NULL"
    - "D-03: recordDivestment and src/app/actions/divestments.ts are gone, editWatch no longer inserts into divestments or opens a db.transaction for status changes, and the divestments table-shape integration assertions still exist"
    - "D-14: a non-owner's getWatchByIdForViewer predicate matches status IN ('owned','grail') for collection_public — previously_owned never matches for visitors, while the owner short-circuit still returns the owner's own previously-owned watch"
    - "D-18: the recommender's viewer exclusion set includes previously_owned, so a model the viewer disposed of is not recommended back to them"
    - "D-19: analyzeSimilarity, computeGapFill and computeTasteOverlap keep their owned/grail allowlists and a regression test proves a previously_owned watch identical to the target is ignored by all three"
    - "D-17: Worn tab owned-only scoping (wornTabScope status === 'owned') is unchanged"
    - "npm run build exits 0 at the end of this plan"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "WatchStatus union with previously_owned; DisposalReason; Watch disposal fields"
      contains: "previously_owned"
    - path: "src/lib/constants.ts"
      provides: "WATCH_STATUSES, WATCH_STATUS_LABELS, DISPOSAL_REASONS, DISPOSAL_REASON_LABELS"
      contains: "DISPOSAL_REASON_LABELS"
    - path: "src/data/watches.ts"
      provides: "disposal field mapping + D-14 visitor predicate"
      contains: "IN ('owned','grail')"
    - path: "tests/lib/previouslyOwnedExclusion.test.ts"
      provides: "LIFE-06 similarity/gap/overlap regression"
      min_lines: 40
  key_links:
    - from: "src/data/watches.ts getWatchByIdForViewer"
      to: "/w/[ref] notFound for visitors"
      via: "SQL predicate status IN ('owned','grail')"
      pattern: "IN \\('owned','grail'\\)"
    - from: "src/data/recommendations.ts exclusion loop"
      to: "excludeKey Set"
      via: "v.status === 'previously_owned'"
      pattern: "previously_owned"
---

<objective>
Finish the `sold` → `previously_owned` rename across application code: domain types and constants, DAL mapping, the visitor-visibility predicate (D-14), the recommender exclusion set (D-18), retirement of the divestments dual-write (D-03), the dead component island, and every test fixture that used `'sold'` as a status. Ends build-green.

Purpose: after 85-01 the DB and Drizzle schema no longer know `sold`; this plan makes the TypeScript world agree so later plans can add behavior on a compiling base. LIFE-06 exclusion is mostly already-safe allowlists (research audit) — this plan makes the two real edits and locks the rest with a regression test.

Output: compiling codebase with one leave-the-collection status, disposal fields mapped, divestments writes removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/85-collection-lifecycle/85-CONTEXT.md
@.planning/phases/85-collection-lifecycle/85-RESEARCH.md
@.planning/phases/85-collection-lifecycle/85-PATTERNS.md
@.planning/phases/85-collection-lifecycle/85-01-SUMMARY.md

<interfaces>
src/lib/types.ts L1 (current): export type WatchStatus = 'owned' | 'wishlist' | 'sold' | 'grail'
src/lib/types.ts Watch provenance block (~L111-118): serial?, yearOfAcquisition?, condition?: ConditionGrade, boxPapers?, serviceHistory?, paidCurrency?, purchaseDate?: string
src/lib/constants.ts L131-136: export const WATCH_STATUSES = ['owned','wishlist','sold','grail'] as const
src/lib/constants.ts ~L150: export const CONDITION_GRADE_LABELS: Record<ConditionGrade, string> = {...}

src/data/watches.ts:
  function mapRowToWatch(row: WatchRow): Watch   (L17-60; provenance block L51-58)
  function mapDomainToRow(data: Partial<Watch>)  (L65-111; provenance block L101-108 uses `if ('x' in data) row.x = data.x ?? null`)
  export async function updateWatch(userId, watchId, data: Partial<Watch>): Promise<Watch>  (L401 — calls mapDomainToRow)
  export async function getWatchByIdForViewer(viewerId, watchId)  (L258; predicate L291-294 `IN ('owned','sold','grail')`)

src/data/recommendations.ts L271-277:
  for (const v of viewerWatches) { if (v.status === 'owned' || v.status === 'wishlist' || v.status === 'grail') { excluded.add(norm(v)) } }

src/lib/similarity.ts: export function analyzeSimilarity(targetWatch: Watch, collection: Watch[], preferences: UserPreferences): SimilarityResult  (L303; owned||grail filter L311-314)
src/lib/gapFill.ts: export function computeGapFill(...)  (L63; owned||grail L68)
src/lib/tasteOverlap.ts: export function computeTasteOverlap(...)  (L51; owned L59-60)

src/app/actions/watches.ts:
  L22 import { divestments, watches } from '@/db/schema'
  L20-21 import { eq, and } from 'drizzle-orm' / import { db } from '@/db'
  L41 status: z.enum(['owned', 'wishlist', 'sold', 'grail'])
  L678-736 isTransitioningToSold + db.transaction dual-write + else updateWatch
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain types, constants, DAL mapping, D-14 predicate and D-18 exclusion</name>
  <files>src/lib/types.ts, src/lib/constants.ts, src/data/watches.ts, src/data/recommendations.ts, tests/data/getWatchByIdForViewer.test.ts, src/data/__tests__/recommendations.test.ts, tests/lib/previouslyOwnedExclusion.test.ts</files>
  <read_first>
    - src/lib/types.ts (L1-50 and the Watch interface ~L60-125)
    - src/lib/constants.ts (L120-185)
    - src/data/watches.ts (L1-112 mapping; L245-320 getWatchByIdForViewer)
    - src/data/recommendations.ts (L30-60 excludeKey; L255-300 exclusion loop)
    - tests/data/getWatchByIdForViewer.test.ts (PART A mock chain L1-60; Unit 8 at ~L180-192)
    - src/data/__tests__/recommendations.test.ts (L620-720 — Phase 81 exclusion-key test to mirror)
    - src/lib/similarity.ts (L300-330), src/lib/gapFill.ts (L55-80), src/lib/tasteOverlap.ts (L45-70)
    - tests/lib/tasteOverlap.test.ts (fixture builder style for Watch objects)
  </read_first>
  <behavior>
    - getWatchByIdForViewer: the captured `watch.where` SQL chunks, serialized to text, contain `IN ('owned','grail')` and contain neither `previously_owned` nor `'sold'` (D-14)
    - getWatchByIdForViewer: owner row with status 'previously_owned' (userId === viewer) returns non-null with isOwner true
    - recommendations: viewer has a previously_owned watch with brandId/familyId X; a catalog top-up row with the same FKs is DROPPED from the rail; a control row surfaces (D-18)
    - analyzeSimilarity(target, [previouslyOwnedTwinOfTarget], prefs) returns the same label/score as analyzeSimilarity(target, [], prefs) and mostSimilarWatches is empty (D-19)
    - computeGapFill and computeTasteOverlap produce the same output with and without an extra previously_owned watch in the collection (D-19)
  </behavior>
  <action>
(a) src/lib/types.ts: change L1 to `export type WatchStatus = 'owned' | 'wishlist' | 'previously_owned' | 'grail'` (D-01). Next to ConditionGrade add `// Phase 85 D-02: disposal reason pgEnum mirror (LIFE-02)` and `export type DisposalReason = 'sold' | 'lost' | 'gifted' | 'stolen' | 'traded'`. In the Watch interface directly after `purchaseDate?` add a `// Phase 85 D-02 — disposal metadata (all nullable; LIFE-02)` block: `disposalReason?: DisposalReason`, `sellPrice?: number`, `disposalDate?: string   // ISO 'YYYY-MM-DD'`.

(b) src/lib/constants.ts: WATCH_STATUSES becomes `['owned', 'wishlist', 'grail', 'previously_owned'] as const`. Add `export const WATCH_STATUS_LABELS: Record<WatchStatus, string>` = owned 'Owned', wishlist 'Wishlist', grail 'Grail', previously_owned 'Previously owned'. Add a Phase 85 block: `export const DISPOSAL_REASONS = ['sold', 'traded', 'gifted', 'lost', 'stolen'] as const` on ONE line (UI-SPEC radiogroup order) and `export const DISPOSAL_REASON_LABELS: Record<DisposalReason, string>` = sold 'Sold', traded 'Traded', gifted 'Gifted', lost 'Lost', stolen 'Stolen' (keys unquoted, so the Task 3 `'sold'` grep gate is not tripped). Import WatchStatus / DisposalReason types from '@/lib/types' the same way ConditionGrade is imported.

(c) src/data/watches.ts: in mapRowToWatch add `disposalReason: row.disposalReason ?? undefined, sellPrice: row.sellPrice ?? undefined, disposalDate: row.disposalDate ?? undefined,` after purchaseDate. In mapDomainToRow add, after the purchaseDate line: `if ('disposalReason' in data) row.disposalReason = data.disposalReason ?? null`, same for sellPrice and disposalDate — use the `'key' in data` idiom, NOT `!== undefined`, because 85-03's D-04 undo passes the keys present with undefined values to clear them. In getWatchByIdForViewer change the predicate literal to `IN ('owned','grail')` and update the docblock (L248-253) and the inline comment (L289-290) to say previously_owned watches are owner-only per Phase 85 D-14 (visitors get null → notFound).

(d) src/data/recommendations.ts: in the exclusion loop add `|| v.status === 'previously_owned'` (D-18) and extend the comment above it to "Exclude viewer's owned/wishlist/grail/previously_owned (C-02 + Phase 85 D-18)". Do not touch the seed/candidate `status === 'owned'` checks at ~L136/184/293 (D-18: taste seed stays owned-only).

(e) Tests (write the failing assertions first, then the edits above make them pass):
  - tests/data/getWatchByIdForViewer.test.ts: replace Unit 8 ('non-owner + sold watch uses collection_public') with two cases: "Unit 8: predicate excludes previously_owned for non-owners (D-14)" which calls getWatchByIdForViewer, finds the `watch.where` call in `calls`, walks the drizzle SQL object's queryChunks recursively (StringChunk values are string arrays; nested SQL objects have their own queryChunks) to build one string, and asserts it contains `IN ('owned','grail')` and does not contain `previously_owned` or `'sold'`; and "Unit 8b: owner still receives own previously_owned watch" with a row whose userId === VIEWER and status 'previously_owned' asserting isOwner true. Keep the PART B integration block untouched except any 'sold' literal → 'previously_owned' with the expectation flipped to not-visible for non-owners if such a case exists.
  - src/data/__tests__/recommendations.test.ts: add one case modeled on the Phase 81 exclusion-key test: viewer's watch has status 'previously_owned' with brandId 'omega-uuid' / familyId 'speedmaster-uuid'; top-up rows include a catalog row with those FKs and a control row; assert the matching row id is absent and the control id is present.
  - tests/lib/previouslyOwnedExclusion.test.ts (new, `// @vitest-environment node` not needed — pure functions): build a Speedmaster-like target and an identical watch (same brand/model/styleTags/roleTags/movement/caseSize) with status 'previously_owned' and a different id. Assert analyzeSimilarity parity vs empty collection, computeGapFill parity, computeTasteOverlap parity as described in <behavior>. Read each function's signature before writing arguments.
  </action>
  <verify>
    <automated>npx vitest run tests/data/getWatchByIdForViewer.test.ts src/data/__tests__/recommendations.test.ts tests/lib/previouslyOwnedExclusion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type WatchStatus = 'owned' | 'wishlist' | 'previously_owned' | 'grail'" src/lib/types.ts` returns 1
    - `grep -c "export type DisposalReason = 'sold' | 'lost' | 'gifted' | 'stolen' | 'traded'" src/lib/types.ts` returns 1
    - `grep -c "DISPOSAL_REASONS = \['sold', 'traded', 'gifted', 'lost', 'stolen'\] as const" src/lib/constants.ts` returns 1
    - `grep -c "'disposalReason' in data" src/data/watches.ts` returns 1, and likewise for `'sellPrice' in data` and `'disposalDate' in data`
    - `grep -c "IN ('owned','grail')" src/data/watches.ts` returns 1 and `grep -c "IN ('owned','sold','grail')" src/data/watches.ts` returns 0
    - `grep -c "v.status === 'previously_owned'" src/data/recommendations.ts` returns 1
    - The three targeted vitest files pass
  </acceptance_criteria>
  <done>Types/constants/DAL speak previously_owned, disposal fields round-trip, visitors cannot load a previously-owned watch, and LIFE-06 exclusion is enforced and regression-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the divestments dual-write and update the remaining source references</name>
  <files>src/app/actions/watches.ts, src/app/actions/divestments.ts, tests/integration/phase37-rls.test.ts, src/components/filters/StatusToggle.tsx, src/components/watch/WatchCard.tsx, tests/static/WatchCard.sold-badge.test.tsx, src/lib/watchFlow/destinations.ts, src/lib/watchFlow/destinations.test.ts, src/components/watch/ConfirmStep.tsx, src/data/follows.ts, src/data/discovery.ts, src/data/catalog.ts</files>
  <read_first>
    - src/app/actions/watches.ts (L18-90 imports + zod; L394-567 moveWishlistToCollection; L569-771 editWatch)
    - src/app/actions/divestments.ts (whole file — confirm nothing else imports it: `grep -rn "actions/divestments" src tests`)
    - tests/integration/phase37-rls.test.ts (whole file — V-04..V-09 stay; the `recordDivestment dual-write (V-10; T-37-TXN-01)` describe and its setup/cleanup go)
    - src/components/filters/StatusToggle.tsx, src/components/watch/WatchCard.tsx, tests/static/WatchCard.sold-badge.test.tsx (dead island — confirm with `grep -rn "CollectionView\|StatusToggle\|WatchCard'" src/app` returning nothing)
    - src/lib/watchFlow/destinations.ts (L25-45), src/lib/watchFlow/destinations.test.ts (L60-75)
  </read_first>
  <action>
(a) src/app/actions/watches.ts (D-01/D-03):
  - L41 zod: `status: z.enum(['owned', 'wishlist', 'grail', 'previously_owned'])`. (Disposal zod fields and lifecycle rules are added in 85-03 — do not add them here.)
  - editWatch: delete the whole `isTransitioningToSold` block including the `db.transaction(...)` and the `divestments` insert; the write becomes the single `updatedWatch = await watchDAL.updateWatch(user.id, watchId, updatePayload)` call. Rewrite the Phase 37 priorRow comment (L610-616) so item (3) no longer mentions sold/recordDivestment — say the prior row feeds status-transition rules. Fix the DISP-02 comment (L653-659) that references the removed transaction path. Update the Phase 27 comment "owned+sold are a separate group" to "owned+previously_owned".
  - Remove now-unused imports: `divestments` (and `watches` if unused), `db`, and `eq, and` from drizzle-orm if no remaining use — check with grep inside the file before deleting each.
  - moveWishlistToCollection: no logic change; update the docblock step 4 and the T-70-03 comment from "sold/grail" to "previously_owned/grail", and the "owned/sold group" comment to "owned/previously_owned group".
  - addWatch comment "For owned/sold the DB-side default 0 is fine" → "owned/previously_owned"; editWatch explore comment "(owned ↔ wishlist ↔ sold ↔ grail)" → "(owned ↔ wishlist ↔ grail ↔ previously_owned)".
(b) Delete src/app/actions/divestments.ts entirely (D-03 retires recordDivestment). Keep the `Divestment` type in src/lib/types.ts and the table in schema (not dropped).
(c) tests/integration/phase37-rls.test.ts: delete the `describe('recordDivestment dual-write (V-10; T-37-TXN-01)', ...)` block and any beforeAll/afterAll, mocks (e.g. getCurrentUser mock) or fixture seeding used only by it. Update the header comment lines that describe T-37-TXN-01 / calling recordDivestment to state that Phase 85 D-03 retired the dual-write and the table-shape/RLS assertions remain. V-04..V-09 stay unchanged.
(d) Dead island (type-compile only, no UAT): StatusToggle option `{ value: 'previously_owned', label: 'Previously owned' }` replacing the sold option; WatchCard badge ternary `watch.status === 'previously_owned' ? 'secondary' : 'outline'`; tests/static/WatchCard.sold-badge.test.tsx assertion string updated to that exact ternary (rename nothing else; keep the file path).
(e) destinations.ts docblock: `status ∈ {'owned', 'previously_owned'} → /u/{username}/collection`; destinations.test.ts L69 → `defaultDestinationForStatus('previously_owned', 'twwaneka')` expecting '/u/twwaneka/collection'.
(f) Comment-only updates so no status-`'sold'` literal remains: ConfirmStep.tsx L85 ("Restricted union excludes 'previously_owned'"), follows.ts L248, discovery.ts L57 and L96/L115 trailing comments ("exclude previously_owned"), catalog.ts L457/703/713/857/867 ("'previously_owned' + 'grail' are NOT badged"). No logic changes in these files (research: allowlists already exclude the new status).
  </action>
  <verify>
    <automated>test ! -f src/app/actions/divestments.ts && ! grep -rn "recordDivestment\|actions/divestments\|isTransitioningToSold" src tests && ! grep -n "db.transaction" src/app/actions/watches.ts && npx vitest run src/lib/watchFlow/destinations.test.ts tests/static/WatchCard.sold-badge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/actions/divestments.ts does not exist
    - `grep -rn "recordDivestment" src tests` returns nothing
    - `grep -c "divestments" src/app/actions/watches.ts` returns 0
    - `grep -c "z.enum(\['owned', 'wishlist', 'grail', 'previously_owned'\])" src/app/actions/watches.ts` returns 1
    - tests/integration/phase37-rls.test.ts still contains `V-04` and `V-09` test titles and contains no `describe('recordDivestment`
    - destinations and WatchCard static tests pass
  </acceptance_criteria>
  <done>No code writes divestments, the sold transition branch is gone, and every non-test source reference to status 'sold' is updated.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep test fixtures off the sold status and pass the build gate</name>
  <files>src/app/actions/__tests__/moveWishlistToCollection.test.ts, src/app/actions/__tests__/watches-recs-invalidation.test.ts, src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx, src/components/watch/ConfirmStep.test.tsx, src/components/watch/AddWatchFlow.test.tsx, src/data/__tests__/reactions-comments-gate.test.ts, tests/integration/phase59-unified-route.test.ts, tests/lib/tasteTags.test.ts, tests/data/comments.test.ts, tests/data/searchCatalogWatches.test.ts, tests/data/getFollowedOwnersForCatalog.test.ts, tests/data/getCollectorsForCatalog.test.ts</files>
  <read_first>
    - Each listed test file at the lines found by `grep -rn "'sold'\|\"sold\"" src tests` (mechanical one-to-three-line literal swaps; this task's file count is high but each edit is a literal)
  </read_first>
  <action>
Mechanical literal sweep. In each file replace status value 'sold' with 'previously_owned', including union type annotations (e.g. `'owned' | 'wishlist' | 'grail' | 'sold'` → `... | 'previously_owned'`), test titles and comments describing the status, variable names like soldWatchId → previouslyOwnedWatchId in phase59-unified-route.test.ts. Expectations stay semantically identical because every production predicate that excluded sold is an allowlist that also excludes previously_owned:
  - moveWishlistToCollection.test.ts Case 5: status 'previously_owned', expected error 'Cannot move previously_owned watch to collection', title updated.
  - ProfileWatchCard-priceLine.test.tsx: 'previously_owned + pricePaid=3000 → renders "Paid: $3,000"' (price bucket unchanged).
  - reactions-comments-gate.test.ts / tests/data/comments.test.ts: the sold row stays comment-visible (status !== 'wishlist' predicate unchanged; research Open Question 1 accepted).
  - getFollowedOwnersForCatalog / getCollectorsForCatalog / searchCatalogWatches / tasteTags: previously_owned rows excluded exactly where sold rows were.
  - watches-recs-invalidation.test.ts L246 and ConfirmStep.test.tsx L12: comment text only.
Then run the gates below. For any vitest failure, check `git stash`-free baseline by running the same file at `git show HEAD:<path>` is not possible for renamed literals — instead confirm with `git blame` that the failing `it()` predates Phase 85 and fails identically without these edits; record such baseline failures in the SUMMARY. `npm run build` MUST exit 0.
  </action>
  <verify>
    <automated>test "$(grep -rn "'sold'\|\"sold\"" src tests | grep -vi "disposal" | wc -l | tr -d ' ')" = "0" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "'sold'\|\"sold\"" src tests | grep -vi disposal` prints nothing
    - `npm run build` exits 0
    - `npx vitest run src/app/actions/__tests__/moveWishlistToCollection.test.ts src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx tests/lib/tasteTags.test.ts tests/data/comments.test.ts src/data/__tests__/reactions-comments-gate.test.ts` — no failures other than ones recorded as pre-existing baseline with blame evidence
  </acceptance_criteria>
  <done>The repo compiles, builds, and no test models a sold status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → /w/[ref] via getWatchByIdForViewer | Service-role Drizzle query; the WHERE clause is the real privacy gate (RLS bypassed) |
| viewer → home recommendation rail | Server-computed exclusion set over viewer's own watches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-05 | Information Disclosure | getWatchByIdForViewer non-owner predicate | mitigate | Literal list narrowed to IN ('owned','grail'); Unit 8 serializes the SQL chunks and asserts previously_owned and 'sold' are absent (D-14). |
| T-85-06 | Tampering | divestments dual-write removal | mitigate | db.transaction and divestments insert deleted; grep gate asserts no recordDivestment / divestments references in actions; table-shape integration tests kept so the retained table is still verified. |
| T-85-07 | Information Disclosure | comments/reactions on previously-owned watches via stale watch_id | accept | Research Open Question 1: `status !== 'wishlist'` data-layer checks unchanged; D-14 scopes only the /w/[id] page; no UI path exposes the id to visitors after 85-04/85-05. Candidate follow-up, out of locked scope. |
</threat_model>

<verification>
- Task 1 targeted vitest passes.
- `grep -rn "'sold'\|\"sold\"" src tests | grep -vi disposal` empty.
- `npm run build` exits 0.
</verification>

<success_criteria>
One leave-the-collection status exists in code, disposal fields are mapped end to end, visitors are gated out of previously-owned watch pages, the recommender excludes disposed models, and divestments is no longer written.
</success_criteria>

<output>
Create `.planning/phases/85-collection-lifecycle/85-02-SUMMARY.md` when done
</output>
