---
phase: 85-collection-lifecycle
plan: 05
type: execute
wave: 3
depends_on: ["85-02"]
files_modified:
  - src/components/wear/WearDetailHero.tsx
  - src/components/wear/WearPhotoClient.tsx
  - src/components/wear/WearVideoClient.tsx
  - src/components/wear/WearCard.tsx
  - tests/components/wear/WearPhotoOverlays.linkable.test.tsx
  - src/data/wearEvents.ts
  - src/app/wear/[wearEventId]/page.tsx
autonomous: true
requirements: [LIFE-05]

must_haves:
  truths:
    - "D-14: on /wear/[id], a visitor viewing a wear of a previously-owned watch sees the brand/model as plain text — no link to /w/[id], which would 404 for them"
    - "D-14: the wear's owner still gets the brand/model link to their own previously-owned watch"
    - "D-17: wears of previously-owned watches still load on /wear/[id] under the existing wear visibility rules (no new gating on the wear itself)"
    - "Every other WearCard consumer keeps its current linking behavior (watchLinkable defaults to true)"
  artifacts:
    - path: "src/components/wear/WearDetailHero.tsx"
      provides: "WearPhotoOverlays watchLinkable prop (Link vs plain text)"
      contains: "watchLinkable"
    - path: "src/data/wearEvents.ts"
      provides: "watchStatus projection on getWearEventByIdForViewer"
      contains: "watchStatus: watches.status"
    - path: "tests/components/wear/WearPhotoOverlays.linkable.test.tsx"
      provides: "link vs no-link rendering test"
      min_lines: 30
  key_links:
    - from: "src/app/wear/[wearEventId]/page.tsx"
      to: "WearCard watchLinkable"
      via: "wear.userId === viewerId || wear.watchStatus !== 'previously_owned'"
      pattern: "watchStatus !== 'previously_owned'"
---

<objective>
Close D-14's linking rule on the wear detail surface: visitors can still open a wear of a watch that has since left the owner's collection (D-17), but the brand/model overlay must not link them to a watch page that now 404s for them.

Purpose: D-14 "Don't show visitors a link to the watch page from those surfaces if it would 404." The Worn tab's own rows already link to /wear/[id] (not /w/[id]) and its leaderboard is owned-only, so /wear/[id] is the only surface that needs a change.

Output: an optional `watchLinkable` prop threaded WearCard → photo/video/fallback layers → WearPhotoOverlays, computed server-side on the wear detail page from a new `watchStatus` projection.

Scope note: WearsLane (/wears/[username]) keeps the default (linkable) — it is not a D-14-named surface and its 48h window makes the case rare; recorded as accepted in the threat model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/85-collection-lifecycle/85-CONTEXT.md

<interfaces>
src/components/wear/WearDetailHero.tsx:
  interface WearPhotoOverlaysProps { username; displayName; avatarUrl; createdAt: Date; brand; model; hasPhoto: boolean; watchId: string }
  export function WearPhotoOverlays(props): JSX.Element — bottom overlay renders <Link href={`/w/${watchId}`} ... onClick={(e) => e.stopPropagation()}><span className="font-semibold">{brand}</span><span className="block">{model}</span></Link>
  export function WearDetailHero({ watchImageUrl, brand, model, altText, username, displayName, avatarUrl, createdAt, watchId }) — renders WearPhotoOverlays twice (image / no-image branches)
src/components/wear/WearPhotoClient.tsx: props include watchId: string; renders WearPhotoOverlays at ~L99, L119, L165
src/components/wear/WearVideoClient.tsx: props include watchId: string; renders WearPhotoOverlays at ~L66, L108
src/components/wear/WearCard.tsx: WearCardProps.watchId (L29); passes watchId to WearVideoClient / WearPhotoClient / WearDetailHero (L150-190)
src/data/wearEvents.ts: export async function getWearEventByIdForViewer(viewerUserId: string | null, wearEventId: string) — select projection L302-330 already innerJoins watches (brand: watches.brand, model: watches.model)
src/app/wear/[wearEventId]/page.tsx: WearDetailPage → <WearPhotoStreamed ... watchId={wear.watchId} viewerId={viewerId} .../> → <WearCard ... watchId={watchId} .../>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: watchLinkable prop through the wear photo layers</name>
  <files>src/components/wear/WearDetailHero.tsx, src/components/wear/WearPhotoClient.tsx, src/components/wear/WearVideoClient.tsx, src/components/wear/WearCard.tsx, tests/components/wear/WearPhotoOverlays.linkable.test.tsx</files>
  <read_first>
    - src/components/wear/WearDetailHero.tsx (whole file)
    - src/components/wear/WearPhotoClient.tsx (props + every WearPhotoOverlays usage)
    - src/components/wear/WearVideoClient.tsx (props + every WearPhotoOverlays usage)
    - src/components/wear/WearCard.tsx (L1-120 props/destructure; L145-195 photo layer)
    - tests/components/wear/WearCard.test.tsx (render/mocking conventions for these components)
  </read_first>
  <behavior>
    - WearPhotoOverlays with watchLinkable omitted → an anchor with href "/w/w-1" containing the brand text
    - WearPhotoOverlays with watchLinkable={false} → brand and model text are rendered, and no element has href "/w/w-1" (query all links; none match)
    - WearDetailHero with watchLinkable={false} and watchImageUrl null → no "/w/" link
  </behavior>
  <action>
Write the new test file first (RED), then:
1. WearDetailHero.tsx: add `/** Phase 85 D-14 — false when the viewer can't open /w/[watchId] (visitor + previously-owned watch). Defaults to true. */ watchLinkable?: boolean` to WearPhotoOverlaysProps, destructure with default `true`. In the bottom overlay render the existing `<Link>` when watchLinkable is true; otherwise render a `<div className={cn('text-sm', textClass)}>` containing the same two spans (same classes), no onClick. Add the same optional prop (default true) to WearDetailHero and pass it to both WearPhotoOverlays calls.
2. WearPhotoClient.tsx and WearVideoClient.tsx: add optional `watchLinkable?: boolean` (default true) to props and pass it to every WearPhotoOverlays render.
3. WearCard.tsx: add optional `watchLinkable?: boolean` to WearCardProps (doc comment citing D-14), destructure default true, pass to WearVideoClient, WearPhotoClient and WearDetailHero.
No visual class changes other than the Link→div swap.
  </action>
  <verify>
    <automated>npx vitest run tests/components/wear/WearPhotoOverlays.linkable.test.tsx tests/components/wear/WearCard.test.tsx tests/components/wear/WearCard.video.test.tsx tests/components/wear/WearVideoClient.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "watchLinkable" src/components/wear/WearDetailHero.tsx` is at least 4
    - `grep -c "watchLinkable" src/components/wear/WearPhotoClient.tsx`, `src/components/wear/WearVideoClient.tsx` and `src/components/wear/WearCard.tsx` are each at least 2
    - New test passes; the three existing wear component test files still pass
  </acceptance_criteria>
  <done>The wear overlay can render brand/model without a watch-page link, defaulting to today's linked behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Compute watchLinkable on /wear/[id] from the watch status</name>
  <files>src/data/wearEvents.ts, src/app/wear/[wearEventId]/page.tsx</files>
  <read_first>
    - src/data/wearEvents.ts (L280-370 getWearEventByIdForViewer)
    - src/app/wear/[wearEventId]/page.tsx (whole file)
  </read_first>
  <action>
1. src/data/wearEvents.ts: in getWearEventByIdForViewer's select projection add `watchStatus: watches.status,` next to brand/model with comment `// Phase 85 D-14 — lets the page hide the /w/[id] link for visitors on previously-owned watches`. No WHERE or gating change (D-17 — the wear stays visible under existing wear visibility).
2. src/app/wear/[wearEventId]/page.tsx: after `if (!wear) notFound()`, compute `const watchLinkable = wear.userId === viewerId || wear.watchStatus !== 'previously_owned'` with a D-14 comment. Add `watchLinkable: boolean` to WearPhotoStreamed's props type and destructure, pass `watchLinkable={watchLinkable}` from WearDetailPage into WearPhotoStreamed, and from WearPhotoStreamed into `<WearCard>`.
Do not add awaits, 'use cache', or cookie access beyond what already exists.
  </action>
  <verify>
    <automated>grep -n "watchStatus: watches.status" src/data/wearEvents.ts && grep -n "wear.watchStatus !== 'previously_owned'" "src/app/wear/[wearEventId]/page.tsx" && grep -c "watchLinkable" "src/app/wear/[wearEventId]/page.tsx" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "watchStatus: watches.status" src/data/wearEvents.ts` returns 1
    - `grep -c "watchLinkable" "src/app/wear/[wearEventId]/page.tsx"` is at least 4 (compute, prop type, destructure/pass, WearCard prop)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Visitors see an unlinked brand/model on wears of previously-owned watches; owners keep the link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → /wear/[id] | Viewer-gated wear detail; watch page link would reveal a gated destination |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-17 | Information Disclosure | /wear/[id] brand/model link | mitigate | watchLinkable computed server-side from wear.userId and watches.status; visitors get plain text (D-14). The watch page itself is independently gated by 85-02's DAL predicate. |
| T-85-18 | Information Disclosure | WearsLane (/wears/[username]) link on a recently disposed watch | accept | Not a D-14-named surface; 48h rail window; destination still 404s server-side so nothing leaks beyond the existing brand/model already shown on the wear. |
</threat_model>

<verification>
- Wear component tests pass.
- `npm run build` exits 0.
</verification>

<success_criteria>
D-14's no-dead-link rule holds on the wear detail page without changing wear visibility (D-17).
</success_criteria>

<output>
Create `.planning/phases/85-collection-lifecycle/85-05-SUMMARY.md` when done
</output>
