---
phase: 85-collection-lifecycle
plan: 08
type: execute
wave: 5
depends_on: ["85-06", "85-07"]
files_modified:
  - src/components/watch/WatchForm.tsx
  - tests/components/watch/WatchForm.lifecycle.test.tsx
autonomous: true
requirements: [LIFE-02, LIFE-03]

must_haves:
  truths:
    - "D-07: the edit/create form's status dropdown does not offer 'Previously owned' unless the watch being edited is already previously owned"
    - "D-07: editing a previously-owned watch shows status 'Previously owned' plus the same Reason / Disposal date / Amount received fields as the dialog, pre-filled from the watch and editable"
    - "D-04: changing a previously-owned watch's status to Owned, Wishlist or Grail disables the disposal fields and shows 'Changing status will clear the disposal details recorded above.'; saving sends the new status and the server clears the three columns"
    - "LIFE-02: saving an edited previously-owned watch persists changed disposal reason / amount / date, and the form sends its browser-local today so the server can reject a future date"
    - "Status options render human labels (Owned, Wishlist, Grail, Previously owned) instead of raw enum strings"
  artifacts:
    - path: "src/components/watch/WatchForm.tsx"
      provides: "D-07 status filtering, disposal fields for previously-owned watches, D-04 undo helper text, today in edit payload"
      contains: "Changing status will clear the disposal details recorded above."
    - path: "tests/components/watch/WatchForm.lifecycle.test.tsx"
      provides: "D-04/D-07 form behavior tests"
      min_lines: 60
  key_links:
    - from: "src/components/watch/WatchForm.tsx"
      to: "src/components/watch/DisposalFields.tsx"
      via: "<DisposalFields idPrefix=\"edit-disposal\" ... disabled={formData.status !== 'previously_owned'} />"
      pattern: "<DisposalFields"
    - from: "src/components/watch/WatchForm.tsx edit submit"
      to: "editWatch"
      via: "editWatch(watch.id, { ...formData, today: todayLocalISO() })"
      pattern: "today: todayLocalISO\\(\\)"
---

<objective>
Make the watch edit form honor the lifecycle rules: never offer "Previously owned" as a way to dispose of a watch (D-07 — the dialog is the only path), but let the owner correct an already-disposed watch's details or undo it back to another status (D-04), reusing the dialog's `DisposalFields`.

Purpose: D-04's undo entry point is the ⋯ "Edit" item on previously-owned cards (85-06), which lands on `/w/[id]/edit`. This plan makes that page useful for correction and undo.

Output: WatchForm changes and a lifecycle-focused component test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/85-collection-lifecycle/85-CONTEXT.md
@.planning/phases/85-collection-lifecycle/85-UI-SPEC.md
@.planning/phases/85-collection-lifecycle/85-06-SUMMARY.md
@.planning/phases/85-collection-lifecycle/85-07-SUMMARY.md

<interfaces>
From 85-06: src/components/watch/DisposalFields.tsx
  export function DisposalFields(props: { idPrefix: string; reason: DisposalReason | null; onReasonChange: (r: DisposalReason) => void; disposalDate: string; onDisposalDateChange: (v: string) => void; sellPrice: string; onSellPriceChange: (v: string) => void; maxDate: string; disabled?: boolean })
  (maxDate '' renders no max attribute)
From 85-02: src/lib/constants.ts WATCH_STATUSES = ['owned','wishlist','grail','previously_owned'] as const; WATCH_STATUS_LABELS: Record<WatchStatus,string>
From 85-03: editWatch(watchId, data) accepts disposalReason / sellPrice / disposalDate / today; rejects transitions INTO previously_owned; clears disposal fields when leaving previously_owned; ignores them for non-disposed watches
From src/lib/wear.ts: todayLocalISO() — browser-local; never call in a Server Action

src/components/watch/WatchForm.tsx (after 85-07):
  type FormData = Omit<Watch, 'id'>; formData seeded from `watch` at L145-186 (provenance fields L173-180)
  status field L421-452: lockedStatus chip OR <Select value={formData.status} onValueChange=...> mapping WATCH_STATUSES to <SelectItem><span className="capitalize">{status}</span></SelectItem>
  handleSubmit edit branch: editWatch(watch.id, formData) → promoted ? celebratePromotion : toast 'Watch updated' → router.push('/')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-07 status filtering, disposal fields and D-04 undo in WatchForm</name>
  <files>src/components/watch/WatchForm.tsx, tests/components/watch/WatchForm.lifecycle.test.tsx</files>
  <read_first>
    - src/components/watch/WatchForm.tsx (L1-60 imports/props; L131-330 state + handleSubmit; L415-460 status field)
    - src/components/watch/DisposalFields.tsx (from 85-06)
    - src/components/watch/WatchForm.lockedStatus.test.tsx (how the status Select/combobox is found in jsdom)
    - tests/components/watch/WatchForm.test.tsx (mocks for editWatch, router, CatalogPhotoUploader; edit-mode hydration test)
    - tests/components/watch/WatchForm.celebration.test.tsx (from 85-07 — celebrate mock)
  </read_first>
  <behavior>
    - Create mode: opening the status Select shows Owned, Wishlist, Grail and no 'Previously owned'
    - Edit mode, watch status 'owned': no 'Previously owned' option; no 'Reason' radiogroup rendered
    - Edit mode, watch status 'previously_owned' with disposalReason 'traded', sellPrice 1500, disposalDate '2026-03-02': status trigger shows 'Previously owned'; radiogroup 'Reason' present with Traded aria-checked="true"; Amount received value '1500'; Disposal date value '2026-03-02'; helper text 'Changing status will clear the disposal details recorded above.' NOT present
    - Same watch: change Amount received to '1800' and submit → editWatch called with (id, object) where object.status === 'previously_owned', object.sellPrice === 1800, object.disposalReason === 'traded', object.disposalDate === '2026-03-02', and typeof object.today === 'string' matching /^\d{4}-\d{2}-\d{2}$/
    - Same watch: select status Owned → helper text visible; the Reason radios and date/amount inputs are disabled; submit → editWatch called with object.status === 'owned'
    - After mount, the Disposal date input has a max attribute equal to the mocked todayLocalISO() value
  </behavior>
  <action>
Write the test file first (mock '@/lib/wear' todayLocalISO → '2026-09-13' while keeping other exports via importActual; mock actions and '@/lib/celebrate' as in the existing WatchForm tests), confirm RED, then edit WatchForm.tsx:

1. Imports: `WATCH_STATUS_LABELS` from '@/lib/constants'; `DisposalFields` from './DisposalFields' (match the file's existing relative-import style for sibling components, e.g. CatalogPhotoUploader) ; `todayLocalISO` from '@/lib/wear'; `DisposalReason` type.
2. Seed: in the watch-seeded formData add `disposalReason: watch.disposalReason, sellPrice: watch.sellPrice, disposalDate: watch.disposalDate` after purchaseDate.
3. `const isEditingPreviouslyOwned = watch?.status === 'previously_owned'` and `const statusOptions = WATCH_STATUSES.filter((s) => s !== 'previously_owned' || isEditingPreviouslyOwned)` with a comment citing D-07 (the Mark-as-previously-owned dialog is the only disposal path). Map `statusOptions` in the Select, rendering `{WATCH_STATUS_LABELS[status]}` (drop the `capitalize` span class).
4. Hydration-safe max: `const [disposalMaxDate, setDisposalMaxDate] = useState('')` and `useEffect(() => { setDisposalMaxDate(todayLocalISO()) }, [])` (the edit page is server-rendered; computing browser-local today during SSR would mismatch — React #418 memory). Import useEffect.
5. Directly below the status field's `</Select>` block (still inside the status `space-y-2` div, or as the next sibling div), when `isEditingPreviouslyOwned && !lockedStatus`, render `<DisposalFields idPrefix="edit-disposal" reason={formData.disposalReason ?? null} onReasonChange={(r) => setFormData((prev) => ({ ...prev, disposalReason: r }))} disposalDate={formData.disposalDate ?? ''} onDisposalDateChange={(v) => setFormData((prev) => ({ ...prev, disposalDate: v || undefined }))} sellPrice={formData.sellPrice != null ? String(formData.sellPrice) : ''} onSellPriceChange={(v) => setFormData((prev) => ({ ...prev, sellPrice: v === '' ? undefined : Number(v) }))} maxDate={disposalMaxDate} disabled={formData.status !== 'previously_owned'} />`, followed by `{formData.status !== 'previously_owned' && <p className="text-xs text-muted-foreground">Changing status will clear the disposal details recorded above.</p>}` (D-04; no separate confirmation dialog).
6. Submit: in the edit branch call `editWatch(watch.id, { ...formData, today: todayLocalISO() })` (client-owned today per 260622-exo; the server ignores disposal keys for non-disposed watches and clears them on undo). Leave create-mode submitData unchanged. Client-side: if `formData.status === 'previously_owned' && formData.disposalDate && formData.disposalDate > todayLocalISO()` set `errors.disposalDate = "Disposal date can't be in the future."` in validate() and render it under the fields with `role="alert" className="text-sm text-destructive"`.
No `font-medium`/`font-bold` in new JSX.
  </action>
  <verify>
    <automated>npx vitest run tests/components/watch/WatchForm.lifecycle.test.tsx tests/components/watch/WatchForm.test.tsx src/components/watch/WatchForm.lockedStatus.test.tsx tests/components/watch/WatchForm.celebration.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "s !== 'previously_owned' || isEditingPreviouslyOwned" src/components/watch/WatchForm.tsx` returns 1
    - `grep -c "WATCH_STATUS_LABELS\[status\]" src/components/watch/WatchForm.tsx` returns 1
    - `grep -c "Changing status will clear the disposal details recorded above." src/components/watch/WatchForm.tsx` returns 1
    - `grep -c "today: todayLocalISO()" src/components/watch/WatchForm.tsx` returns 1
    - `grep -c "<DisposalFields" src/components/watch/WatchForm.tsx` returns 1
    - All <behavior> cases pass; existing WatchForm test files pass (or pre-existing failures documented with blame); `npm run build` exits 0
  </acceptance_criteria>
  <done>The edit form can correct or undo a previously-owned watch and can never be used to dispose of one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner browser → editWatch | Full form payload including status, disposal fields and today |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-24 | Tampering | status dropdown bypass (crafted payload with status previously_owned) | transfer | Hiding the option is UX; 85-03's editWatch rejects transitions into previously_owned server-side (tested). |
| T-85-25 | Tampering | disposal date / today in edit payload | transfer | Server validates isoCalendarDate, isPlausibleClientToday and date <= today (85-03). |
| T-85-26 | Integrity | stale disposal fields after undo | transfer | Server clears all three columns on status exit regardless of payload (85-03 D-04); walk verifies NULLs in SQL (85-09). |
</threat_model>

<verification>
- WatchForm lifecycle + existing WatchForm tests pass.
- `npm run build` exits 0.
</verification>

<success_criteria>
D-04 and D-07 hold in the edit form: disposal only via the dialog, correction and undo via edit.
</success_criteria>

<output>
Create `.planning/phases/85-collection-lifecycle/85-08-SUMMARY.md` when done
</output>
