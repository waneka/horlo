---
phase: 85-collection-lifecycle
plan: 06
type: execute
wave: 4
depends_on: ["85-03"]
files_modified:
  - src/lib/disposal.ts
  - src/lib/__tests__/disposal.test.ts
  - src/components/watch/DisposalFields.tsx
  - src/components/profile/MarkPreviouslyOwnedDialog.tsx
  - src/components/profile/__tests__/MarkPreviouslyOwnedDialog.test.tsx
  - src/components/profile/WatchCardOverflowMenu.tsx
  - src/components/profile/ProfileWatchCard.tsx
  - src/components/profile/__tests__/ProfileWatchCard-lifecycle.test.tsx
autonomous: true
requirements: [LIFE-03, LIFE-05]

must_haves:
  truths:
    - "LIFE-03 / D-05: an owner sees a ⋯ (More options) trigger at the top-right of the image on their owned collection cards; clicking it opens a menu with 'Mark as previously owned' and does NOT navigate to /w/[id]; visitors and wishlist/grail cards never render the trigger"
    - "LIFE-03 / D-06: the dialog shows Reason (Sold, Traded, Gifted, Lost, Stolen — required WAI-ARIA radiogroup with arrow/Home/End keys), Disposal date (optional, pre-filled with browser-local today, max = today) and Amount received (optional) for every reason with no reason-specific hiding"
    - "D-06: a future disposal date is blocked client-side with \"Disposal date can't be in the future.\" and the dialog sends its own today to markWatchPreviouslyOwned so the server re-validates"
    - "D-08: on success the dialog closes, the owner stays on the Collection tab and a sonner toast reads 'Moved to previously owned'"
    - "D-16: a previously-owned card renders at opacity-60 with a top-left '{Reason} · {Mon YYYY}' badge ('{Reason}' alone when no date), no wear badge, no wear line, no like/comment chips, and its ⋯ menu offers 'Edit' linking to /w/[id]/edit instead of 'Mark as previously owned'"
    - "D-13: the reason·date badge and menu are only ever rendered for the owner (previously-owned cards only reach owners, and the menu is gated on isOwner)"
    - "D-12 / UI-SPEC: no destructive styling and no second confirmation step — disposal is reversible (D-04)"
  artifacts:
    - path: "src/components/profile/MarkPreviouslyOwnedDialog.tsx"
      provides: "disposal dialog calling markWatchPreviouslyOwned"
      contains: "markWatchPreviouslyOwned"
    - path: "src/components/watch/DisposalFields.tsx"
      provides: "shared reason radiogroup + date + amount fields (reused by WatchForm in 85-08)"
      contains: "role=\"radiogroup\""
    - path: "src/components/profile/WatchCardOverflowMenu.tsx"
      provides: "owner ⋯ menu with Link-safe trigger"
      contains: "More options"
    - path: "src/lib/disposal.ts"
      provides: "formatDisposalBadge(reason, date) with UTC month formatting"
      exports: ["formatDisposalBadge"]
  key_links:
    - from: "src/components/profile/MarkPreviouslyOwnedDialog.tsx"
      to: "src/app/actions/watches.ts markWatchPreviouslyOwned"
      via: "await markWatchPreviouslyOwned({ watchId, disposalReason, sellPrice, disposalDate, today: todayLocalISO() })"
      pattern: "markWatchPreviouslyOwned\\("
    - from: "src/components/profile/ProfileWatchCard.tsx"
      to: "MarkPreviouslyOwnedDialog"
      via: "rendered as a sibling of the card <Link> (outside it), controlled by lifted useState"
      pattern: "<MarkPreviouslyOwnedDialog"
---

<objective>
Build the LIFE-03 disposal flow on the live collection card: a shared disposal-fields component, the "Mark as previously owned" dialog, an owner-only ⋯ overflow menu that cannot trigger the card's Link, and the D-16 muted card treatment with a reason·date badge for previously-owned cards.

Purpose: the dialog is the only way to dispose of a watch (D-07), so it must always capture a reason. Build on `ProfileWatchCard` (rendered by `CollectionTabContent`), not the dead `WatchCard` island.

Output: 4 new source files, ProfileWatchCard changes, 3 test files.

Discretion choices:
- `DisposalFields` lives in `src/components/watch/` and is shared with WatchForm (85-08) so field markup is not forked (UI-SPEC).
- The menu is its own `WatchCardOverflowMenu` component (testable; Phase 86 reorder can suppress it by not rendering it).
- Link-click safety: the trigger calls `preventDefault()` + `stopPropagation()` (same guard as the like/comment chips), and the menu popup root calls `stopPropagation()` on click so portaled item clicks cannot React-bubble into the card Link. The dialog renders outside the Link (WatchCommentSheet precedent).
- Dialog state (reason/date/amount/error) resets when the dialog opens — an interaction — not on mount (Router Cache stale-instance memory).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/85-collection-lifecycle/85-CONTEXT.md
@.planning/phases/85-collection-lifecycle/85-UI-SPEC.md
@.planning/phases/85-collection-lifecycle/85-PATTERNS.md
@.planning/phases/85-collection-lifecycle/85-03-SUMMARY.md

<interfaces>
From 85-03 (src/app/actions/watches.ts):
  export async function markWatchPreviouslyOwned(input: MarkPreviouslyOwnedInput): Promise<ActionResult<Watch>>
  errors: 'Invalid request' | "Disposal date can't be in the future." | 'Watch not found' | 'Cannot mark <status> watch as previously owned' | "Couldn't update this watch." | 'Not authenticated' | 'Not found'
From src/lib/types.ts: MarkPreviouslyOwnedInput { watchId; disposalReason: DisposalReason; sellPrice?: number; disposalDate?: string; today: string }; DisposalReason; WatchStatus; Watch.disposalReason?/sellPrice?/disposalDate?
From src/lib/constants.ts (85-02): DISPOSAL_REASONS = ['sold','traded','gifted','lost','stolen'] as const; DISPOSAL_REASON_LABELS: Record<DisposalReason,string>
From src/lib/wear.ts: export function todayLocalISO(): string   (client-only use)
From src/components/ui/dropdown-menu.tsx: DropdownMenu, DropdownMenuTrigger (MenuPrimitive.Trigger.Props), DropdownMenuContent (Popup props + align/side), DropdownMenuItem (supports render={<Link .../>})
From src/components/ui/dialog.tsx: Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter (see export block ~L149)
ConfirmStep radiogroup precedent: src/components/watch/ConfirmStep.tsx L150-300 (handleKeyDown ArrowRight/Down, ArrowLeft/Up, Home, End; requestAnimationFrame focus of [data-value]; selected Button classes incl. dark: pairs)
ProfileWatchCard (current): whole card is <Link href={`/w/${watch.id}`}>; image div `relative aspect-square bg-muted`; wear badge `!isWishlistLike && (isWornToday || isStale)` top-left; wear line `!isWishlistLike && <p>{lastWornLabel}</p>`; like/comment chips gated `!isOwner`; WatchCommentSheet rendered after </Link> inside the fragment
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatDisposalBadge, DisposalFields and MarkPreviouslyOwnedDialog</name>
  <files>src/lib/disposal.ts, src/lib/__tests__/disposal.test.ts, src/components/watch/DisposalFields.tsx, src/components/profile/MarkPreviouslyOwnedDialog.tsx, src/components/profile/__tests__/MarkPreviouslyOwnedDialog.test.tsx</files>
  <read_first>
    - src/components/watch/ConfirmStep.tsx (L140-300 — radiogroup + price input placeholder "$")
    - src/components/profile/LogTodaysWearButton.tsx (Dialog layout, native date input with max, role="alert" error, pending label pattern — Phase 84)
    - src/lib/wear.ts (todayLocalISO header — client-only)
    - src/components/ui/dialog.tsx (exports)
    - tests/components/profile/LogTodaysWearButton.test.tsx (how Server Actions and sonner are mocked for a dialog test)
    - .planning/phases/85-collection-lifecycle/85-UI-SPEC.md (§Copywriting, §Disposal dialog, §Typography, §Color)
  </read_first>
  <behavior>
    - formatDisposalBadge('sold', '2026-03-02') === 'Sold · Mar 2026'; formatDisposalBadge('traded', undefined) === 'Traded'; formatDisposalBadge(undefined, '2026-03-02') === null; formatDisposalBadge('lost', '2026-01-01') === 'Lost · Jan 2026' (UTC — no off-by-one month)
    - Dialog open with watch {id:'w1', brand:'Omega', model:'Speedmaster'}: title 'Mark as previously owned'; description contains 'Record how Omega Speedmaster left your collection.'; radiogroup labelled 'Reason' with radios in order Sold, Traded, Gifted, Lost, Stolen; date input value equals mocked todayLocalISO() ('2026-09-13') and has max '2026-09-13'; amount input type number with placeholder '$'
    - Submit button 'Mark as previously owned' is disabled until a reason is chosen
    - Focus on Sold radio + ArrowRight → Traded becomes aria-checked="true"; End → Stolen; Home → Sold
    - Date set to '2026-09-20' then submit → alert text "Disposal date can't be in the future." and markWatchPreviouslyOwned NOT called
    - Choose Traded, amount '1500', date '2026-03-02', submit → markWatchPreviouslyOwned called once with { watchId: 'w1', disposalReason: 'traded', sellPrice: 1500, disposalDate: '2026-03-02', today: '2026-09-13' }; on { success: true } → toast.success('Moved to previously owned') and onOpenChange(false)
    - Choose Lost, clear amount and date, submit → called with sellPrice undefined and disposalDate undefined
    - Action resolves { success: false, error: 'boom' } → alert "Couldn't update this watch."; { success:false, error:"Disposal date can't be in the future." } → that exact text
    - While pending the submit label is 'Saving…' and is disabled
  </behavior>
  <action>
Write the two test files first (mock '@/app/actions/watches' markWatchPreviouslyOwned, 'sonner' toast, and '@/lib/wear' todayLocalISO → '2026-09-13'), confirm RED, then:

(1) src/lib/disposal.ts: `export function formatDisposalBadge(reason: DisposalReason | undefined, date: string | undefined): string | null` — null when no reason; label from DISPOSAL_REASON_LABELS; when date matches /^\d{4}-\d{2}-\d{2}$/ append ` · ` + Intl.DateTimeFormat('en-US', { month: 'short', year: 'numeric', timeZone: 'UTC' }).format(new Date(`${date}T00:00:00Z`)) (React #418 guard — pinned locale + UTC). The separator is a middle dot U+00B7 with single spaces.

(2) src/components/watch/DisposalFields.tsx ('use client', named export `DisposalFields`): controlled props { idPrefix: string; reason: DisposalReason | null; onReasonChange: (r: DisposalReason) => void; disposalDate: string; onDisposalDateChange: (v: string) => void; sellPrice: string; onSellPriceChange: (v: string) => void; maxDate: string; disabled?: boolean }. Layout `space-y-4`:
  - Section label `<p id={`${idPrefix}-reason-label`} className="text-sm font-semibold">Reason</p>` then `<div role="radiogroup" aria-labelledby=... aria-required="true" className="flex flex-wrap gap-2" onKeyDown=...>` mapping DISPOSAL_REASONS to `<Button type="button" role="radio" variant="outline" aria-checked data-value tabIndex>` — tabIndex 0 on the selected value, or on the first option when reason is null, -1 otherwise; keyboard handling copied from ConfirmStep.handleKeyDown (when reason is null treat index as -1 so ArrowRight selects the first). Selected classes exactly: 'border-accent bg-accent text-accent-foreground hover:bg-accent hover:text-accent-foreground dark:border-accent dark:bg-accent dark:text-accent-foreground dark:hover:bg-accent dark:hover:text-accent-foreground'; every button has 'min-h-[44px]'.
  - Date: `<Label htmlFor={`${idPrefix}-date`} className="font-semibold">Disposal date</Label>`, native `<Input type="date" id=... value={disposalDate} max={maxDate || undefined} className="text-base md:text-sm">` (an empty maxDate renders no max attribute — WatchForm in 85-08 sets it after mount to avoid a server-UTC vs browser-local hydration mismatch), helper `<p className="text-xs text-muted-foreground">Optional — defaults to today</p>`.
  - Amount: Label 'Amount received', `<Input type="number" inputMode="decimal" min={0} step="any" placeholder="$" className="text-base md:text-sm">`, helper 'Optional'.
  - `disabled` disables all controls. No font-medium / font-bold.

(3) src/components/profile/MarkPreviouslyOwnedDialog.tsx ('use client', named export): props { open: boolean; onOpenChange: (open: boolean) => void; watch: Pick<Watch, 'id' | 'brand' | 'model'> }. State reason (DisposalReason | null), disposalDate, sellPrice, error (string | null), pending via useTransition. Reset all state inside a `handleOpenChange(next)` wrapper when next === true (reason null, date todayLocalISO(), price '', error null) and ALSO initialize via a `useEffect` keyed on `open` becoming true (the parent opens it programmatically from a menu item, so the Dialog's onOpenChange(true) may not fire — the effect covers that path; this is a reset on the open interaction, not on mount). Submit handler: compute `const today = todayLocalISO()`; if disposalDate && disposalDate > today → setError("Disposal date can't be in the future.") and return; build the input (sellPrice '' → undefined else Number(sellPrice); disposalDate '' → undefined) and call markWatchPreviouslyOwned inside startTransition; on success → onOpenChange(false) then toast.success('Moved to previously owned') (exact D-08 string; no router.push — the action's invalidation refreshes the grid in place); on failure → setError(result.error === "Disposal date can't be in the future." ? that : "Couldn't update this watch.").
  Render `<Dialog open onOpenChange={handleOpenChange}><DialogContent>`: DialogHeader with DialogTitle 'Mark as previously owned' (text-base font-semibold) and DialogDescription `Record how {brand} {model} left your collection. You can undo this later from the edit page.`; body `<DisposalFields idPrefix={`dispose-${watch.id}`} maxDate={todayLocalISO()} .../>`; error slot `<p role="alert" className="text-sm text-destructive">`; DialogFooter with `<Button variant="outline" onClick={() => onOpenChange(false)}>Cancel</Button>` and `<Button variant="default" disabled={!reason || pending} onClick={handleSubmit}>{pending ? 'Saving…' : 'Mark as previously owned'}</Button>`. Not destructive-styled (UI-SPEC Color). No second confirmation.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/disposal.test.ts src/components/profile/__tests__/MarkPreviouslyOwnedDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "timeZone: 'UTC'" src/lib/disposal.ts` returns 1
    - `grep -c 'role="radiogroup"' src/components/watch/DisposalFields.tsx` returns 1 and `grep -c "dark:bg-accent" src/components/watch/DisposalFields.tsx` returns at least 1
    - `grep -c "Moved to previously owned" src/components/profile/MarkPreviouslyOwnedDialog.tsx` returns 1
    - `grep -c "variant=\"destructive\"" src/components/profile/MarkPreviouslyOwnedDialog.tsx` returns 0
    - `grep -c "font-medium\|font-bold" src/components/watch/DisposalFields.tsx src/components/profile/MarkPreviouslyOwnedDialog.tsx` totals 0
    - All <behavior> cases pass
  </acceptance_criteria>
  <done>A reusable disposal field set and a dialog that validates, commits through the owner-scoped action and confirms with the D-08 toast.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Owner ⋯ menu and previously-owned card treatment on ProfileWatchCard</name>
  <files>src/components/profile/WatchCardOverflowMenu.tsx, src/components/profile/ProfileWatchCard.tsx, src/components/profile/__tests__/ProfileWatchCard-lifecycle.test.tsx</files>
  <read_first>
    - src/components/profile/ProfileWatchCard.tsx (whole file)
    - src/components/wear/WearOverflowMenu.tsx (whole file — menu shape, handleOpenChange, finalFocus trick when a dialog follows)
    - tests/components/wear/WearOverflowMenu.test.tsx (how base-ui menus are opened and items found in jsdom)
    - src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx (buildWatch fixture + next/link / next/image / action mocks)
    - .planning/phases/85-collection-lifecycle/85-UI-SPEC.md (§⋯ overflow menu; §Muted card + reason·date badge CSS chain items 1-7)
  </read_first>
  <behavior>
    - isOwner + status 'owned': a button with aria-label 'More options' exists; `fireEvent.click(trigger)` returns false (defaultPrevented) and the menu shows 'Mark as previously owned'; selecting it renders a dialog titled 'Mark as previously owned' (mock MarkPreviouslyOwnedDialog as a stub rendering its `open` prop, or render the real one with the action mocked)
    - isOwner false + status 'owned': no 'More options' button
    - isOwner + status 'wishlist' or 'grail': no 'More options' button
    - isOwner + status 'previously_owned', disposalReason 'traded', disposalDate '2026-03-02': the card root element has class opacity-60; text 'Traded · Mar 2026' is present; none of 'Never worn', 'Worn today', 'Not worn recently', 'Worn yesterday' is present even when lastWornDate is today; opening the menu shows an 'Edit' item whose link href is '/w/<id>/edit' and no 'Mark as previously owned' item
    - isOwner + status 'previously_owned' with no disposalDate: badge text exactly 'Traded'
    - isOwner + status 'owned' with lastWornDate today: 'Worn today' badge still renders and the card has no opacity-60 class (regression)
    - price line for previously_owned with pricePaid 3000 still reads 'Paid: $3,000'
  </behavior>
  <action>
Write the test file first (RED), then:

(1) src/components/profile/WatchCardOverflowMenu.tsx ('use client', named export `WatchCardOverflowMenu`): props { watchId: string; status: WatchStatus; onMarkPreviouslyOwned: () => void; className?: string }. Return null unless status is 'owned' or 'previously_owned'. Local `open` state. `<DropdownMenu open onOpenChange>` with `<DropdownMenuTrigger aria-label="More options" onClick={(e) => { e.preventDefault(); e.stopPropagation() }} className={cn('inline-flex min-h-[44px] min-w-[44px] items-center justify-center rounded-full text-foreground', className)}>` containing lucide `MoreHorizontal` (size-5, aria-hidden) inside a small `rounded-full bg-background/90 p-1 shadow ring-1 ring-border` span for legibility over photos. `<DropdownMenuContent align="end" onClick={(e) => e.stopPropagation()}>` — comment: stops portaled item clicks from React-bubbling into ProfileWatchCard's <Link> (85-RESEARCH Pitfall 1/2). Items: status 'owned' → `<DropdownMenuItem onClick={() => { setOpen(false); onMarkPreviouslyOwned() }}>Mark as previously owned</DropdownMenuItem>` (use WearOverflowMenu's finalFocus approach so focus is not fought over by the dialog); status 'previously_owned' → `<DropdownMenuItem render={<Link href={`/w/${watchId}/edit`} />}>Edit</DropdownMenuItem>` (D-16 / D-04 undo entry). If a jsdom test proves the trigger's preventDefault stops base-ui from opening, move the preventDefault/stopPropagation to an `onClickCapture` on a wrapper span around the trigger instead and keep the same assertions.

(2) src/components/profile/ProfileWatchCard.tsx:
  - `const isPreviouslyOwned = watch.status === 'previously_owned'`; `const [disposeOpen, setDisposeOpen] = useState(false)`; `const disposalBadge = isPreviouslyOwned ? formatDisposalBadge(watch.disposalReason, watch.disposalDate) : null`.
  - Card className: wrap with cn(existing classes, isPreviouslyOwned && 'opacity-60') — on the whole Card (UI-SPEC item 1; no grayscale).
  - Top-left overlay: `isPreviouslyOwned ? (disposalBadge && <span className="absolute top-2 left-2 z-10 rounded-full bg-background/90 px-2 py-0.5 text-xs font-normal text-muted-foreground shadow ring-1 ring-border">{disposalBadge}</span>) : (!isWishlistLike && (isWornToday || isStale) && <existing wear badge/>)` (item 2).
  - Wear line condition becomes `!isWishlistLike && !isPreviouslyOwned` (item 3). Price line unchanged (item 4). Like/comment chips unchanged (item 5, already !isOwner).
  - Menu: inside the image div, `{isOwner && <WatchCardOverflowMenu watchId={watch.id} status={watch.status} onMarkPreviouslyOwned={() => setDisposeOpen(true)} className="absolute top-2 right-2 z-10" />}` (items 6-7).
  - Dialog: after `</Link>` and next to the WatchCommentSheet block, `{isOwner && watch.status === 'owned' && <MarkPreviouslyOwnedDialog open={disposeOpen} onOpenChange={setDisposeOpen} watch={watch} />}` with a comment mirroring the WatchCommentSheet "rendered OUTSIDE the <Link>" rationale.
  - Update the Phase 27 price-line comment "owned/sold (paid bucket)" to "owned/previously_owned (paid bucket)".
  </action>
  <verify>
    <automated>npx vitest run src/components/profile/__tests__/ProfileWatchCard-lifecycle.test.tsx src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "e.preventDefault()" src/components/profile/WatchCardOverflowMenu.tsx` is at least 1 and `grep -c "stopPropagation" src/components/profile/WatchCardOverflowMenu.tsx` is at least 2
    - `grep -c "isPreviouslyOwned && 'opacity-60'" src/components/profile/ProfileWatchCard.tsx` returns 1
    - `grep -c "!isWishlistLike && !isPreviouslyOwned" src/components/profile/ProfileWatchCard.tsx` returns 1
    - In ProfileWatchCard.tsx the `<MarkPreviouslyOwnedDialog` line appears after the `</Link>` line (line-number comparison via grep -n)
    - Both ProfileWatchCard test files pass; `npm run build` exits 0
  </acceptance_criteria>
  <done>Owners can open the disposal dialog from any owned collection card without navigating, and previously-owned cards read as muted history with a reason·date badge and an Edit entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner browser → markWatchPreviouslyOwned | Dialog input (reason, amount, date, today) crosses to the server |
| card UI → visitor | Menu and badge must never render for non-owners |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-19 | Tampering | dialog client-side validation | transfer | Client `max` and future-date check are UX only; authoritative validation (reason enum, price >= 0, date plausibility, ownership) is in 85-03's strict zod + DAL scoping. |
| T-85-20 | Information Disclosure | disposal badge / menu for visitors | mitigate | Menu gated on isOwner; previously-owned rows never reach visitors (85-04 server split), so the badge cannot render for them (D-13). Tested (isOwner false → no trigger). |
| T-85-21 | Spoofing (UI redress) | ⋯ trigger inside the card Link | mitigate | preventDefault + stopPropagation on trigger and popup root; test asserts the click is defaultPrevented so no unintended navigation fires. |
</threat_model>

<verification>
- disposal, dialog and card tests pass.
- `npm run build` exits 0.
</verification>

<success_criteria>
LIFE-03's collection-card disposal flow works end to end in components, and D-16's previously-owned card treatment is in place for 85-04's toggle to reveal.
</success_criteria>

<output>
Create `.planning/phases/85-collection-lifecycle/85-06-SUMMARY.md` when done
</output>
