---
phase: 55-server-actions-notification-dedup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/notifications/types.ts
  - src/lib/notifications/logger.ts
autonomous: true
requirements: [NOTIF-11, NOTIF-12, NOTIF-13, NOTIF-14]

must_haves:
  truths:
    - "logNotification accepts four new event types: watch_like, wear_like, watch_comment, wear_comment (NOTIF-11/12)"
    - "Like-type inserts use the raw-SQL ON CONFLICT DO NOTHING path so they hit the new partial UNIQUE dedup indexes; comment-type inserts use the standard Drizzle insert (no dedup) (NOTIF-14)"
    - "The logger reads notifyOnLike / notifyOnComment from profile_settings and skips the insert when the recipient opted out (carried-forward opt-out mechanic; columns at schema.ts:273-274)"
    - "The existing self-guard (recipient === actor early return) covers the new like and comment types so an owner is never self-notified (NOTIF-11/12 'never self')"
    - "Payload key alignment is exact: watch_like→watch_id, wear_like→wear_event_id, matching the dedup index expressions from plan 02 (NOTIF-13/14 LANDMINE)"
  artifacts:
    - path: "src/lib/notifications/types.ts"
      provides: "WatchLikePayload, WearLikePayload, WatchCommentPayload, WearCommentPayload + widened NotificationPayload union (NOTIF-13)"
      contains: "WearLikePayload"
    - path: "src/lib/notifications/logger.ts"
      provides: "Widened LogNotificationInput union + opt-out reads + 2 raw-SQL like branches (NOTIF-11/12/14)"
      contains: "watch_like"
  key_links:
    - from: "src/lib/notifications/logger.ts"
      to: "notifications (raw INSERT ... ON CONFLICT DO NOTHING)"
      via: "db.execute(sql`...`) for watch_like/wear_like"
      pattern: "ON CONFLICT DO NOTHING"
    - from: "src/lib/notifications/logger.ts"
      to: "profileSettings.notifyOnLike / notifyOnComment"
      via: "db.select opt-out read"
      pattern: "notifyOnLike"
---

<objective>
Extend the notification logger to handle the four new social event types. Add the payload interfaces
(NOTIF-13 data shape), widen the `LogNotificationInput` union with four branches, extend the
profile-settings opt-out read with `notifyOnLike` / `notifyOnComment`, and add two raw-SQL
`ON CONFLICT DO NOTHING` insert branches for the like types (so they hit the plan-02 dedup indexes)
plus comment types via the standard Drizzle insert.

Purpose: This is the shared notification foundation the plan-04 reaction action and plan-05 comment
actions both call. NOTIF-14 dedup is enforced jointly by the plan-02 indexes and this plan's raw-SQL
conflict clause — Drizzle's default .onConflictDoNothing() targets the PK and would silently NOT
dedup, so the raw SQL is load-bearing. NOTIF-11/12 "never self-notify" reuses the existing self-guard.
Output: 2 edited files (types.ts + logger.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-server-actions-notification-dedup/55-CONTEXT.md
@.planning/phases/55-server-actions-notification-dedup/55-RESEARCH.md
@.planning/phases/55-server-actions-notification-dedup/55-PATTERNS.md

<interfaces>
<!-- The logger as it exists today. Extracted from codebase. -->

Current logger union — src/lib/notifications/logger.ts:34-46: 'follow' | 'watch_overlap' branches.
Current opt-out SELECT — logger.ts:55-62: reads notifyOnFollow, notifyOnWatchOverlap only.
Current self-guard — logger.ts:50-51: `if (input.recipientUserId === input.actorUserId) return`.
Current watch_overlap raw-SQL branch — logger.ts:70-84: db.execute(sql`INSERT ... ON CONFLICT DO NOTHING`).
Current follow insert — logger.ts:87-92: db.insert(notifications).values({...}).

Confirmed schema columns (no schema edit needed) — src/db/schema.ts:
  notifyOnLike: boolean('notify_on_like').notNull().default(true)        [line 273]
  notifyOnComment: boolean('notify_on_comment').notNull().default(true)  [line 274]

Dedup index payload keys (from plan 02, MUST match the payload interfaces):
  notifications_watch_like_dedup → payload->>'watch_id'
  notifications_wear_like_dedup  → payload->>'wear_event_id'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the four notification payload interfaces to types.ts (NOTIF-13)</name>
  <behavior>
    - WatchLikePayload has key `watch_id` (string) — matches the watch_like dedup index expression
    - WearLikePayload has key `wear_event_id` (string) — matches the wear_like dedup index expression (NOT watch_id, NOT wear_id)
    - WatchCommentPayload / WearCommentPayload add comment_id + comment_preview on top of the like-payload shape
    - NotificationPayload union widens to include all four new interfaces
    - tests/unit/notifications/logger.test.ts NOTIF-13 cases assert these keys are present on the passed payload
  </behavior>
  <read_first>
    - src/lib/notifications/types.ts (the full current 24-line file — FollowPayload, WatchOverlapPayload, NotificationPayload union)
    - .planning/phases/55-server-actions-notification-dedup/55-PATTERNS.md (§ src/lib/notifications/types.ts — the 4 interfaces with the load-bearing key-alignment comments)
    - .planning/phases/55-server-actions-notification-dedup/55-RESEARCH.md (§ Notification Payload Types — exact field lists)
  </read_first>
  <action>
    Edit src/lib/notifications/types.ts. Add four exported interfaces:
      - WatchLikePayload { actor_username: string; actor_display_name: string | null; watch_id: string; watch_brand: string; watch_model: string }
      - WearLikePayload { actor_username: string; actor_display_name: string | null; wear_event_id: string; watch_brand: string; watch_model: string }
      - WatchCommentPayload (= WatchLikePayload fields + comment_id: string; comment_preview: string)
      - WearCommentPayload (= WearLikePayload fields + comment_id: string; comment_preview: string)
    Widen the NotificationPayload union to: FollowPayload | WatchOverlapPayload | WatchLikePayload | WearLikePayload | WatchCommentPayload | WearCommentPayload.
    Add an inline comment on the watch_id / wear_event_id fields stating they MUST match the dedup index keys (payload->>'watch_id' / payload->>'wear_event_id') from migration 20260522000002 or the partial index never fires.
    NAMING LANDMINE: the wear payloads use key `wear_event_id` (column-style), never `wear_id` or `wear_event`.
  </action>
  <verify>
    <automated>grep -q "WearLikePayload" src/lib/notifications/types.ts &amp;&amp; grep -q "wear_event_id" src/lib/notifications/types.ts &amp;&amp; ! grep -q "wear_id:" src/lib/notifications/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/notifications/types.ts exports WatchLikePayload, WearLikePayload, WatchCommentPayload, WearCommentPayload
    - WearLikePayload and WearCommentPayload use the key `wear_event_id` (exact string), not `wear_id`/`wear_event`
    - WatchCommentPayload and WearCommentPayload include comment_id and comment_preview fields
    - NotificationPayload union includes all four new interfaces
  </acceptance_criteria>
  <done>Four payload interfaces added with correct key alignment; union widened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen the logger union, add opt-out reads, and add raw-SQL like branches (NOTIF-11/12/14)</name>
  <behavior>
    - logNotification with type 'watch_like'/'wear_like' calls db.execute (raw SQL containing 'ON CONFLICT DO NOTHING'), NOT db.insert (NOTIF-14)
    - logNotification with type 'watch_comment'/'wear_comment' calls db.insert (standard Drizzle, no dedup)
    - When the recipient's notifyOnLike is false, a watch_like/wear_like produces no DB write; when notifyOnComment is false, a comment type produces no DB write
    - When recipient === actor, the function returns early with no DB write (existing self-guard, covers all new types)
    - All assertions live in tests/unit/notifications/logger.test.ts (plan 01 Task 3)
  </behavior>
  <read_first>
    - src/lib/notifications/logger.ts (the full current 97-line file — union 34-46, self-guard 50-51, opt-out SELECT 55-62, opt-out checks 64-68, watch_overlap raw-SQL branch 70-84, follow insert 87-92)
    - src/lib/notifications/types.ts (the four new payload types from Task 1 — import them)
    - .planning/phases/55-server-actions-notification-dedup/55-PATTERNS.md (§ src/lib/notifications/logger.ts — union widening, SELECT extension, opt-out branch additions, raw-SQL like branches, comment-type insert)
    - .planning/phases/55-server-actions-notification-dedup/55-RESEARCH.md (§ logger.ts Extension Points + § Dedup Index Mechanics — why Drizzle's .onConflictDoNothing() is wrong; bare ON CONFLICT DO NOTHING hits all partial indexes)
  </read_first>
  <action>
    Edit src/lib/notifications/logger.ts. Import WatchLikePayload, WearLikePayload, WatchCommentPayload, WearCommentPayload from './types'.
    1) Widen LogNotificationInput with four branches mirroring the existing shape: watch_like→WatchLikePayload, wear_like→WearLikePayload, watch_comment→WatchCommentPayload, wear_comment→WearCommentPayload.
    2) Extend the opt-out SELECT (lines 55-62) to also select notifyOnLike: profileSettings.notifyOnLike and notifyOnComment: profileSettings.notifyOnComment (columns verified at schema.ts:273-274).
    3) After the existing opt-out checks (lines 64-68), add: const notifyOnLike = settings?.notifyOnLike ?? true; const notifyOnComment = settings?.notifyOnComment ?? true; then `if ((input.type === 'watch_like' || input.type === 'wear_like') && !notifyOnLike) return;` and `if ((input.type === 'watch_comment' || input.type === 'wear_comment') && !notifyOnComment) return;`.
    4) Add two raw-SQL branches modeled on the watch_overlap branch (lines 70-84): for type 'watch_like' and for type 'wear_like', db.execute(sql`INSERT INTO notifications (user_id, actor_id, type, payload) VALUES (${input.recipientUserId}::uuid, ${input.actorUserId}::uuid, '<type>', ${input.payload}::jsonb) ON CONFLICT DO NOTHING`) then return. Use bare ON CONFLICT DO NOTHING (no constraint target) — PostgreSQL checks all partial UNIQUE indexes; Drizzle's .onConflictDoNothing() would target the PK and silently never dedup.
    5) Comment types ('watch_comment'/'wear_comment'): use the standard db.insert(notifications).values({ userId: input.recipientUserId, actorId: input.actorUserId, type: input.type, payload: input.payload }) path — generalize the existing follow insert so it covers comment types too (no dedup index for comments; each comment is a distinct event, NOTIF-12). Keep the outer try/catch so the logger never throws to the caller.
    NAMING LANDMINE: the raw SQL literal type strings are 'watch_like' and 'wear_like' (enum types), and the payload carries watch_id / wear_event_id respectively — keep the enum type, the DAL discriminator, and the payload key distinct.
  </action>
  <verify>
    <automated>npm test -- logger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/notifications/logger.ts LogNotificationInput union contains watch_like, wear_like, watch_comment, wear_comment branches
    - The opt-out SELECT reads profileSettings.notifyOnLike and profileSettings.notifyOnComment
    - logger.ts contains two like-type branches calling db.execute with `ON CONFLICT DO NOTHING`, each followed by return
    - Comment types route to db.insert (NOT db.execute) — no dedup
    - `npm test -- logger.test.ts` exits 0 (the plan-01 NOTIF-13/14 + opt-out + comment-insert cases now pass GREEN)
  </acceptance_criteria>
  <done>logger.ts handles all four new types; like types dedup via raw SQL; comment types use standard insert; opt-out reads added; logger.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action caller → logNotification | Actions (plans 04/05) pass a recipient, actor, and pre-resolved payload. The logger trusts these (the action is the trust gate) but enforces two safety invariants: never self-notify, and never duplicate a like notification. |
| logNotification → notifications table | The raw-SQL insert is the only write surface for like notifications; the ON CONFLICT clause is the dedup enforcement that pairs with the plan-02 indexes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-SPAM | Denial of Service (inbox) | logger like-insert branches | mitigate | Raw-SQL `ON CONFLICT DO NOTHING` (NOT Drizzle .onConflictDoNothing(), which targets the PK) hits the plan-02 partial UNIQUE indexes; rapid re-like after unlike inserts zero additional rows. Verified by logger.test.ts NOTIF-14 case. |
| T-55-SELF | Spoofing / nuisance | logger self-guard | mitigate | Existing `recipient === actor → return` short-circuit (logger.ts:51) covers the four new types; owner is never self-notified (NOTIF-11/12). |
| T-55-OPTOUT | Information Disclosure (unwanted contact) | opt-out read | mitigate | notifyOnLike/notifyOnComment read write-time; opted-out recipients get no row created. Default true on missing settings row preserves current behavior. |
</threat_model>

<verification>
- `npm test -- logger.test.ts` GREEN (NOTIF-13 payload-key cases, NOTIF-14 ON-CONFLICT cases, opt-out cases, comment-insert case, self-guard case all pass)
- logger.ts union covers all four new types; like types use raw SQL, comment types use Drizzle insert
- types.ts payload keys match the plan-02 dedup index expressions exactly
</verification>

<success_criteria>
- NOTIF-11/12 fan-out routing in place (with self-guard); NOTIF-13 payload shapes correct; NOTIF-14 dedup raw SQL wired
- Logger never throws to caller (try/catch preserved)
- No schema.ts edit (columns already present)
</success_criteria>

<output>
After completion, create `.planning/phases/55-server-actions-notification-dedup/55-03-SUMMARY.md`
</output>
