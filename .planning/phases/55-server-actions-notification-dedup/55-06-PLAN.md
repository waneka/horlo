---
phase: 55-server-actions-notification-dedup
plan: 06
type: execute
wave: 3
depends_on: [55-02, 55-04, 55-05]
files_modified: []
autonomous: false
requirements: [NOTIF-14]

must_haves:
  truths:
    - "The full unit suite is green before the prod push (reactions, comments, logger, follows, notifications)"
    - "The Phase 55 dedup migration is pushed to the linked prod DB via `supabase db push --linked` (D-03 — blocking human-action checkpoint, mirrors Phase 53-03)"
    - "Both partial UNIQUE indexes — notifications_watch_like_dedup and notifications_wear_like_dedup — are confirmed present on the LINKED prod notifications table after the push"
    - "NOTIF-14 dedup is enforced wherever the actions run (local AND prod), closing the false-positive verification gap where build/type checks pass without the indexes existing in prod"
  artifacts: []
  key_links:
    - from: "supabase/migrations/20260522000002_phase55_notif_like_dedup.sql"
      to: "linked prod notifications table"
      via: "supabase db push --linked"
      pattern: "notifications_watch_like_dedup"
---

<objective>
Push the Phase 55 dedup migration to production and confirm the two partial UNIQUE indexes exist on
the linked prod `notifications` table. This is the blocking human-action checkpoint that mirrors the
Phase 53-03 prod-push pattern (D-03).

Purpose: NOTIF-14 dedup is DB-enforced. Build and type checks pass WITHOUT the indexes existing in prod
(a false-positive verification state) — the phase cannot truly pass NOTIF-14 until the indexes are live
on the linked DB wherever the actions run. The like-notification write path (plans 04/05) is already
exercised by GREEN unit tests before this push, so the dedup contract is validated before it ships.
Output: prod indexes confirmed; no source files changed.

Pre-flight (operator awareness, see project memory project_drizzle_supabase_db_mismatch — the 4 prod-push
gotchas): filename ordering, the LOCAL-ONLY nature of drizzle-kit push, extension-schema, and
enum-bound dependents. This migration is pure CREATE UNIQUE INDEX (transactional, no ALTER TYPE),
so it is far lower-risk than the Phase 53 non-transactional enum push — but it is still a real prod DDL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-server-actions-notification-dedup/55-CONTEXT.md
@.planning/phases/55-server-actions-notification-dedup/55-02-SUMMARY.md
@.planning/phases/55-server-actions-notification-dedup/55-04-SUMMARY.md
@.planning/phases/55-server-actions-notification-dedup/55-05-SUMMARY.md

<interfaces>
<!-- The migration to push (authored + locally applied in plan 02). -->
supabase/migrations/20260522000002_phase55_notif_like_dedup.sql — two CREATE UNIQUE INDEX statements:
  notifications_watch_like_dedup ON notifications (user_id, actor_id, (payload->>'watch_id')) WHERE type = 'watch_like'
  notifications_wear_like_dedup  ON notifications (user_id, actor_id, (payload->>'wear_event_id')) WHERE type = 'wear_like'

Prod-push precedent: Phase 53-03 used `supabase db push --linked` as a blocking human-action checkpoint.
The WR-03 in-place edit (plan 02) is for LOCAL replay correctness only — it sends NO new statement to prod.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-push gate — full unit suite green</name>
  <files>none (verification-only task)</files>
  <read_first>
    - .planning/phases/55-server-actions-notification-dedup/55-VALIDATION.md (Sampling Rate — "Before /gsd-verify-work: full suite must be green")
    - .planning/phases/55-server-actions-notification-dedup/55-04-SUMMARY.md (reactions action landed)
    - .planning/phases/55-server-actions-notification-dedup/55-05-SUMMARY.md (comment actions landed)
  </read_first>
  <action>
    Run the full unit suite (`npm test`). Confirm reactions.test.ts, comments.test.ts, and tests/unit/notifications/logger.test.ts
    are all GREEN, and that no existing suite (follows, notifications, etc.) regressed. This is the gate that proves the
    like/comment notification write path — which depends on the dedup indexes for NOTIF-14 — is correct before pushing the
    indexes to prod. If anything is RED, STOP and fix before proceeding to the prod push.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green)
    - reactions.test.ts, comments.test.ts, tests/unit/notifications/logger.test.ts all pass
    - No regression in pre-existing suites
  </acceptance_criteria>
  <done>Full unit suite green; the notification write path is validated before the prod push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push the dedup migration to prod and confirm both indexes exist</name>
  <files>supabase/migrations/20260522000002_phase55_notif_like_dedup.sql (pushed to prod, not modified)</files>
  <what-built>
    Plan 02 authored supabase/migrations/20260522000002_phase55_notif_like_dedup.sql (two partial UNIQUE dedup
    indexes) and applied it to the LOCAL DB. The like/comment Server Actions (plans 04/05) and the logger ON CONFLICT
    path (plan 03) are implemented and unit-GREEN (Task 1 above). The only remaining step is the prod DDL push, which
    per project DB rules (drizzle-kit push is LOCAL ONLY; prod uses `supabase db push --linked`) and D-03 is a blocking
    human-action checkpoint — it cannot be safely automated in this environment.
  </what-built>
  <action>
    [checkpoint:human-action] The operator runs `supabase db push --linked` from the main repo (where .env.local / linked
    credentials exist — NOT a worktree, per the Phase 49.1 lesson that worktrees lack DATABASE_URL) to apply the
    20260522000002 dedup migration to the linked prod DB. This is the D-03 blocking checkpoint mirroring Phase 53-03.
    The WR-03 in-place edit to the Phase 53 file sends NO new statement to prod (its DO $$ assertion has no prod side-effect;
    that file is already applied). After the push, the operator confirms both notifications_watch_like_dedup and
    notifications_wear_like_dedup exist on the linked notifications table via `supabase migration list --linked` plus a
    pg_indexes query, and pastes the output into the SUMMARY. This step cannot be automated in this environment.
  </action>
  <how-to-verify>
    1. From the main repo (with .env.local / linked project credentials available — NOT a worktree, per the Phase 49.1
       lesson that worktrees lack DATABASE_URL):
         supabase db push --linked
       Review the planned statements; confirm only the new 20260522000002 migration's CREATE UNIQUE INDEX statements
       are pending (the WR-03 in-place edit to the Phase 53 file sends NO new statement — that file is already applied
       to prod; its DO $$ assertion has no prod side-effect).
    2. Confirm the migration applied without error.
    3. Verify both indexes exist on the linked prod notifications table, e.g.:
         supabase migration list --linked          (confirm 20260522000002 is applied)
         and/or a psql/SQL query against the linked DB:
         SELECT indexname FROM pg_indexes WHERE tablename = 'notifications'
           AND indexname IN ('notifications_watch_like_dedup','notifications_wear_like_dedup');
       Both index names MUST be returned.
    4. (Optional sanity) Confirm the partial predicate + payload key by inspecting \d+ notifications or pg_indexes.indexdef
       — watch_like keys on (payload->>'watch_id'), wear_like keys on (payload->>'wear_event_id').
    Paste the verification output (index list) into the SUMMARY so NOTIF-14 prod enforcement is auditable.
  </how-to-verify>
  <verify>
    Operator pastes the pg_indexes query output into the SUMMARY showing BOTH notifications_watch_like_dedup and
    notifications_wear_like_dedup present on the linked prod notifications table; `supabase migration list --linked`
    shows 20260522000002 applied.
  </verify>
  <acceptance_criteria>
    - `supabase db push --linked` applied the 20260522000002 migration to prod without error
    - pg_indexes query against the linked DB returns BOTH notifications_watch_like_dedup and notifications_wear_like_dedup
    - The index-presence output is recorded in the SUMMARY for auditability
  </acceptance_criteria>
  <resume-signal>Type "approved" once both notifications_watch_like_dedup and notifications_wear_like_dedup are confirmed present on the linked prod DB, or describe any push error encountered.</resume-signal>
  <done>Both dedup indexes confirmed present on the linked prod DB; NOTIF-14 enforced in prod; no false-positive verification state remaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local schema → prod schema | The prod push is the moment the dedup enforcement crosses from local to the live DB. A push that succeeds at the build/type layer but never lands the indexes in prod is a false-positive verification state — the actions would run in prod with no dedup. |
| operator → linked prod DB | The `--linked` push is a privileged operation requiring credentials that are intentionally NOT available to the autonomous agent (worktree has no DATABASE_URL); it is gated to the human operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-SPAM | Denial of Service (inbox) | prod dedup indexes | mitigate | The blocking checkpoint requires confirming both partial UNIQUE indexes exist on the linked prod DB before the phase is considered to satisfy NOTIF-14; without this, prod would dedup nothing. The resume-signal is gated on the index-presence query output. |
| T-55-FALSEPASS | Repudiation / verification integrity | prod-push verification | mitigate | The pre-push gate (Task 1, full suite green) plus the explicit index-presence query (Task 2) close the "build passes without indexes" false-positive. Index list output is pasted into the SUMMARY for auditability. |
</threat_model>

<verification>
- Full unit suite green (Task 1)
- `supabase db push --linked` applied the 20260522000002 migration to prod without error
- pg_indexes query against the linked DB returns BOTH notifications_watch_like_dedup and notifications_wear_like_dedup
- NOTIF-14 dedup is now enforced in prod, not just local
</verification>

<success_criteria>
- NOTIF-14 prod enforcement confirmed (both indexes live on the linked DB)
- No false-positive verification state remaining (indexes exist wherever the actions run)
- Phase 55 schema work complete end-to-end (local apply in plan 02 + prod push here)
</success_criteria>

<output>
After completion, create `.planning/phases/55-server-actions-notification-dedup/55-06-SUMMARY.md`
(include the prod index-presence query output).
</output>
