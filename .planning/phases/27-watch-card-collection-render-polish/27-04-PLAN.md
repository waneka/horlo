---
phase: 27-watch-card-collection-render-polish
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/profile/ProfileWatchCard.tsx
  - src/components/profile/CollectionTabContent.tsx
autonomous: true
requirements: [VIS-07, VIS-08]

must_haves:
  truths:
    - "ProfileWatchCard renders a status-driven price line replacing the legacy wishlist-only Target: $X block"
    - "Owned and sold watches with pricePaid show 'Paid: $X'; without pricePaid but with marketPrice show 'Market: $X'; with neither, no price line"
    - "Wishlist and grail watches with targetPrice show 'Target: $X'; without targetPrice but with marketPrice show 'Market: $X'; with neither, no price line"
    - "ProfileWatchCard's <Image sizes> attr equals '(max-width: 640px) 50vw, (max-width: 1024px) 50vw, 25vw'"
    - "CollectionTabContent grid wrapper uses grid-cols-2 (mobile 2-column) AND retains sm:grid-cols-2 lg:grid-cols-4 breakpoints"
    - "Number formatting uses .toLocaleString() with $ prefix and no decimals (e.g. '$4,200')"
    - "marketPrice never appears as a first-class display outside the fallback role (D-20)"
  artifacts:
    - path: src/components/profile/ProfileWatchCard.tsx
      provides: "Unified status-driven price line + updated Image sizes attr"
      contains: "isWishlistLike"
    - path: src/components/profile/CollectionTabContent.tsx
      provides: "grid-cols-2 wrapper for the watch grid (line ~161)"
      contains: "grid-cols-2"
  key_links:
    - from: src/components/profile/ProfileWatchCard.tsx
      to: Watch domain type
      via: "watch.status / watch.pricePaid / watch.targetPrice / watch.marketPrice"
      pattern: "isWishlistLike"
    - from: src/components/profile/ProfileWatchCard.tsx
      to: Next.js Image optimizer
      via: "<Image sizes=...>"
      pattern: "\\(max-width: 640px\\) 50vw"
---

<objective>
Land the card-content polish: status-driven price line (VIS-08) on `ProfileWatchCard`, updated `<Image sizes>` attr (D-13), and the 2-column mobile grid (VIS-07) on `CollectionTabContent`. The `WishlistTabContent` grid update is bundled with the drag UX in Plan 05 because both edits touch the same file's render branches.

Purpose: Wave 2 surface polish that ships independently of the drag UX wiring. Once Plan 04 lands, the card-content tests (Plan 01 Task 3 — 9 cases for ProfileWatchCard + 1 for CollectionTabContent) all turn GREEN.

Output: 2 modified files, 1 unified price-line render path, 1 updated sizes attr, 1 grid class change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-watch-card-collection-render-polish/27-CONTEXT.md
@.planning/phases/27-watch-card-collection-render-polish/27-RESEARCH.md
@.planning/phases/27-watch-card-collection-render-polish/27-PATTERNS.md
@.planning/phases/27-watch-card-collection-render-polish/27-VALIDATION.md
@.planning/phases/27-watch-card-collection-render-polish/27-UI-SPEC.md
@.planning/phases/27-watch-card-collection-render-polish/27-01-SUMMARY.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Card props the price-line logic must read; UI-SPEC styling lock; existing Image sizes string. -->

ProfileWatchCard current shape (`src/components/profile/ProfileWatchCard.tsx` line 19-23):
```typescript
interface ProfileWatchCardProps {
  watch: Watch
  lastWornDate: string | null
  showWishlistMeta?: boolean
}
```

Watch domain type (Plan 02 added `sortOrder` but card doesn't use it):
```typescript
interface Watch {
  id: string
  brand: string
  model: string
  status: 'owned' | 'wishlist' | 'sold' | 'grail'
  pricePaid?: number
  targetPrice?: number
  marketPrice?: number
  // ...
  sortOrder?: number
}
```

CONTEXT D-15..D-21 (price line lock):
- D-15 — single price line replaces the wishlist-only `Target: $X` block at lines 85-89
- D-16 — owned/sold → paid bucket; wishlist/grail → target bucket
- D-17 — paid bucket: pricePaid → marketPrice → hide; target bucket: targetPrice → marketPrice → hide
- D-18 — labels: `"Paid: $X"`, `"Target: $X"`, `"Market: $X"` (no bare numbers, no icons)
- D-19 — single style; no muted tone for marketPrice fallback
- D-20 — marketPrice ONLY in fallback role; v6.0 owns first-class market display
- D-21 — `value.toLocaleString()`; USD; no decimals

UI-SPEC price-line typography lock:
- Class: `mt-1 text-xs font-normal text-foreground`

UI-SPEC mobile grid lock (D-11):
- `grid grid-cols-2 gap-4 sm:grid-cols-2 lg:grid-cols-4` (replaces `grid-cols-1 ...`)

Image sizes attr (D-13):
- FROM: `(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw`
- TO:   `(max-width: 640px) 50vw, (max-width: 1024px) 50vw, 25vw`

Tests this plan must turn GREEN (from Plan 01):
- `src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx` — 9 it() blocks
- `src/components/profile/__tests__/CollectionTabContent.test.tsx` — 1 it() block

Existing render structure of ProfileWatchCard (DO NOT change):
- Line 42 `<Link href={...}>` wrap (preserved)
- Lines 44-70 image + worn-today/not-worn pill (preserved)
- Lines 71-94 CardContent block (price line lives here, replacing lines 85-89)
- Lines 90-93 notes preview (preserved as-is, gated on showWishlistMeta && watch.notes)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProfileWatchCard — replace wishlist-only Target: $X block with unified status-driven price line; update Image sizes attr</name>
  <read_first>
    - .planning/phases/27-watch-card-collection-render-polish/27-PATTERNS.md (lines 388-427 — exact 3-edit specification with code excerpts)
    - .planning/phases/27-watch-card-collection-render-polish/27-CONTEXT.md (D-13 sizes, D-15..D-21 price-line decisions)
    - .planning/phases/27-watch-card-collection-render-polish/27-UI-SPEC.md (Price Line section lines 240-280, Typography section lines 56-75)
    - .planning/phases/27-watch-card-collection-render-polish/27-RESEARCH.md (Code Examples §status-driven price line lines 786-806; Pitfall 6 — marketPrice fallback honesty)
    - src/components/profile/ProfileWatchCard.tsx (full file — current state at lines 1-99)
    - src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx (Plan 01 — 9 it() blocks the implementation must satisfy)
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md (Next.js 16 Image API — sizes semantics; AGENTS.md mandates reading bundled docs before any Next.js change)
  </read_first>
  <files>src/components/profile/ProfileWatchCard.tsx (MODIFY)</files>
  <behavior>
    - Test 1 (owned + pricePaid=4200): `Paid: $4,200` rendered exactly
    - Test 2 (owned + pricePaid undefined + marketPrice=8500): `Market: $8,500` rendered
    - Test 3 (owned + both undefined): no element matching `/Paid:|Target:|Market:/`
    - Test 4 (wishlist + targetPrice=15000, showWishlistMeta=true): `Target: $15,000`
    - Test 5 (wishlist + targetPrice undefined + marketPrice=8500, showWishlistMeta=true): `Market: $8,500`
    - Test 6 (wishlist + both undefined, showWishlistMeta=true): no price line
    - Test 7 (grail + targetPrice=50000): `Target: $50,000` (grail uses target bucket per D-16)
    - Test 8 (sold + pricePaid=3000): `Paid: $3,000` (sold uses paid bucket per D-16)
    - Test 9 (sizes attr): rendered <img sizes="(max-width: 640px) 50vw, (max-width: 1024px) 50vw, 25vw">
  </behavior>
  <action>
    Three precise edits to `src/components/profile/ProfileWatchCard.tsx`. Open the file and apply in order.

    **Edit 1 — Image sizes attr (line 50):**

    Change:
    ```typescript
    sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw"
    ```
    To:
    ```typescript
    sizes="(max-width: 640px) 50vw, (max-width: 1024px) 50vw, 25vw"
    ```

    **Edit 2 — Insert price-line helper above the JSX return:**

    Locate the function body (starts at line 19 with `export function ProfileWatchCard(...)`). The current JSX `return` begins at line 41 with `<Link href=...>`. Above that return statement (after the `tag` const declaration on line 39), insert the `priceLine` helper:

    ```typescript
      // Phase 27 (VIS-08, D-15..D-21) — status-driven price line.
      // Replaces the legacy wishlist-only `Target: $X` block (was at lines 85-89).
      // Single rendering path for all card variants:
      //   - owned/sold (paid bucket) → "Paid: $X" if pricePaid, else "Market: $X" if marketPrice, else hide
      //   - wishlist/grail (target bucket) → "Target: $X" if targetPrice, else "Market: $X" if marketPrice, else hide
      // marketPrice is ONLY surfaced as a fallback (D-20) — v6.0 Market Value owns
      // first-class market display.
      const isWishlistLike = watch.status === 'wishlist' || watch.status === 'grail'
      const primary = isWishlistLike ? watch.targetPrice : watch.pricePaid
      const primaryLabel = isWishlistLike ? 'Target' : 'Paid'
      const priceLine =
        primary != null
          ? `${primaryLabel}: $${primary.toLocaleString()}`
          : watch.marketPrice != null
            ? `Market: $${watch.marketPrice.toLocaleString()}`
            : null
    ```

    **Edit 3 — Replace the existing wishlist-only block (lines 85-89):**

    Locate:
    ```typescript
          {showWishlistMeta && watch.targetPrice != null && (
            <p className="mt-1 text-xs text-foreground">
              Target: ${watch.targetPrice.toLocaleString()}
            </p>
          )}
    ```

    Replace with (NOT gated on `showWishlistMeta` — D-15 makes the price line render unconditionally based on status):
    ```typescript
          {priceLine && (
            <p className="mt-1 text-xs font-normal text-foreground">
              {priceLine}
            </p>
          )}
    ```

    Note the className change: existing was `mt-1 text-xs text-foreground`; UI-SPEC line 70 locks `mt-1 text-xs font-normal text-foreground` — explicitly add `font-normal` for consistency (resolves to the same computed style on most browsers but matches the spec).

    **Preserve UNCHANGED:**
    - The `<Link href={`/watch/${watch.id}`}>` wrap on line 42
    - The image block (lines 44-58)
    - The worn-today/not-worn pill block (lines 58-69)
    - The brand line (line 72-74)
    - The model line (line 75)
    - The tag pill block (lines 76-83)
    - The wear label line (line 84)
    - The notes preview block (lines 90-93) — still gated on `showWishlistMeta && watch.notes`

    Verify after edit: render order in CardContent should be brand → model → tag → wear label → price line (NEW) → notes preview (when applicable).
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx 2>&1 | tee /tmp/p27-w2-card.log; grep -E "Tests +([0-9]+) passed|FAIL" /tmp/p27-w2-card.log</automated>
  </verify>
  <acceptance_criteria>
    - All 9 it() blocks in `src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx` pass GREEN
    - `grep -c "isWishlistLike" src/components/profile/ProfileWatchCard.tsx` == 1
    - `grep -c "marketPrice" src/components/profile/ProfileWatchCard.tsx` >= 1
    - `grep -c "(max-width: 640px) 50vw, (max-width: 1024px) 50vw, 25vw" src/components/profile/ProfileWatchCard.tsx` == 1
    - `grep -c "(max-width: 640px) 100vw" src/components/profile/ProfileWatchCard.tsx` == 0 (old sizes value gone)
    - `grep -c "showWishlistMeta && watch.targetPrice" src/components/profile/ProfileWatchCard.tsx` == 0 (legacy block removed)
    - `grep -c "showWishlistMeta && watch.notes" src/components/profile/ProfileWatchCard.tsx` == 1 (notes block preserved)
    - `grep -c "mt-1 text-xs font-normal text-foreground" src/components/profile/ProfileWatchCard.tsx` >= 1 (UI-SPEC class string)
    - `npx tsc --noEmit` shows no new errors in `src/components/profile/ProfileWatchCard.tsx`
    - The `<Link href={...}>` wrap is still present: `grep -c "<Link href={\`/watch/" src/components/profile/ProfileWatchCard.tsx` == 1
  </acceptance_criteria>
  <done>ProfileWatchCard renders status-driven price line + correct sizes attr; 9 Plan 01 test cases turn GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CollectionTabContent — change grid wrapper from grid-cols-1 to grid-cols-2 (VIS-07 / D-11)</name>
  <read_first>
    - .planning/phases/27-watch-card-collection-render-polish/27-PATTERNS.md (lines 600-615 — single-line class change)
    - .planning/phases/27-watch-card-collection-render-polish/27-CONTEXT.md (D-11, D-12 — grid breakpoint + don't add responsive variants)
    - .planning/phases/27-watch-card-collection-render-polish/27-UI-SPEC.md (Mobile Grid section lines 190-237)
    - src/components/profile/CollectionTabContent.tsx lines 150-172 (current grid wrapper at line 161)
    - src/components/profile/__tests__/CollectionTabContent.test.tsx (Plan 01 — single it() block this turns green)
  </read_first>
  <files>src/components/profile/CollectionTabContent.tsx (MODIFY)</files>
  <behavior>
    - Plan 01 test asserts: container has `.grid.grid-cols-2`, AND className contains `sm:grid-cols-2`, AND className contains `lg:grid-cols-4`.
  </behavior>
  <action>
    Open `src/components/profile/CollectionTabContent.tsx`. Locate line 161:
    ```typescript
    <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
    ```

    Replace with:
    ```typescript
    <div className="grid grid-cols-2 gap-4 sm:grid-cols-2 lg:grid-cols-4">
    ```

    SINGLE WORD CHANGE: `grid-cols-1` → `grid-cols-2`. NO other edits to this file.

    Per CONTEXT D-12 (UI-SPEC line 219-231), do NOT introduce responsive `sm:` / `md:` font/padding variants on the card. The card is rendered as-is at half-width; ship and iterate.

    Per PATTERNS.md line 614, the empty-state grid at lines 89-115 (which uses `sm:grid-cols-2`) is unrelated to the watch grid and stays as-is. Verify after edit that line 89-115 is untouched.
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot src/components/profile/__tests__/CollectionTabContent.test.tsx 2>&1 | tee /tmp/p27-w2-grid.log; grep -E "Tests +([0-9]+) passed|FAIL" /tmp/p27-w2-grid.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"grid grid-cols-2 gap-4 sm:grid-cols-2 lg:grid-cols-4"' src/components/profile/CollectionTabContent.tsx` == 1
    - `grep -c '"grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4"' src/components/profile/CollectionTabContent.tsx` == 0 (old class string gone)
    - The Plan 01 test passes: `npx vitest run --reporter=dot src/components/profile/__tests__/CollectionTabContent.test.tsx 2>&1 | grep "1 passed"` shows green
    - `npx tsc --noEmit` shows no new errors in `src/components/profile/CollectionTabContent.tsx`
    - `git diff src/components/profile/CollectionTabContent.tsx | grep "^[-+]"` shows ONLY the line 161 change (one `-` and one `+`)
  </acceptance_criteria>
  <done>Collection tab renders 2 columns on mobile; Plan 01 grid test turns GREEN; no other lines touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Watch domain → ProfileWatchCard render | DOM rendering of watch.pricePaid / watch.targetPrice / watch.marketPrice; no untrusted user content beyond what already renders (brand, model, notes are existing surfaces — no XSS expansion) |
| Card → Next.js Image optimizer | sizes attr is a pure layout hint; no security relevance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-CARD-01 | Information disclosure (price-line leaks privately-paid amount on a public profile) | ProfileWatchCard | accept | Wishlist+Collection privacy is governed by `wishlist_public` / `collection_public` flags at the DAL layer (existing two-layer privacy from Phase 12). If a non-owner can see the card at all, they're already inside the privacy gate. The price line surfaces what the owner has chosen to publish via their profile flags |
| T-27-CARD-02 | Tampering (XSS via crafted brand/model in card render) | ProfileWatchCard | accept | React 19 escapes interpolated strings by default; `${primaryLabel}: $${primary.toLocaleString()}` produces a string that is rendered as text inside `<p>`. No new attack surface vs. the pre-edit card which already renders `${watch.brand}`, `${watch.model}` |
| T-27-CARD-03 | DoS via giant pricePaid value | ProfileWatchCard | accept | Numeric DB column; bounded by Postgres `real` semantics; `.toLocaleString()` handles all valid floats safely |
</threat_model>

<verification>
- [ ] All 9 ProfileWatchCard-priceLine tests pass
- [ ] CollectionTabContent grid test passes
- [ ] Existing ProfileWatchCard render still works for owned/wishlist/sold/grail (smoke via the 9 test cases — no separate manual UAT needed for this plan; Plan 05 owns the visual UAT)
- [ ] `npx tsc --noEmit` clean for both modified files
- [ ] No `package.json` changes
</verification>

<success_criteria>
- ProfileWatchCard surfaces a single price line driven by status, with explicit labels and honest fallback to marketPrice
- Image sizes attr matches the new mobile 2-column layout
- CollectionTabContent grid is 2-column on mobile, unchanged on tablet/desktop
- Plan 01 card-content tests turn GREEN (10 cases total)
- WishlistTabContent.tsx is NOT touched — Plan 05 handles its grid update + drag wiring atomically
</success_criteria>

<output>
After completion, create `.planning/phases/27-watch-card-collection-render-polish/27-04-SUMMARY.md` documenting:
- Files modified (with line references)
- Test status: 10/10 GREEN (9 priceLine + 1 grid)
- Confirmation that the legacy wishlist-only Target block was REPLACED, not just supplemented (no double rendering on wishlist tab)
- Confirmation that the notes preview block remains gated on `showWishlistMeta && watch.notes`
- Note: WishlistTabContent grid update is owned by Plan 05 (atomic with drag wiring)
</output>
