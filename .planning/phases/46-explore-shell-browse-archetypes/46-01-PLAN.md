---
phase: 46-explore-shell-browse-archetypes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/archetype-config.ts
  - src/lib/archetype-config.test.ts
  - src/data/browse.ts
  - src/data/__tests__/browse.test.ts
  - src/data/__tests__/catalog-facets.test.ts
  - src/components/explore/__tests__/CollectorArchetypes.test.tsx
  - src/components/explore/ExploreHero.tsx
  - src/components/explore/PopularCollectors.tsx
  - src/components/explore/PopularCollectorRow.tsx
  - src/components/explore/TrendingWatches.tsx
  - src/components/explore/GainingTractionWatches.tsx
  - src/components/explore/DiscoveryWatchCard.tsx
  - src/data/discovery.ts
  - src/app/explore/collectors/page.tsx
  - src/app/explore/watches/page.tsx
autonomous: true
requirements: [EXPL-02, EXPL-03, EXPL-05]
must_haves:
  truths:
    - "Browse count DAL functions return correctly-shaped rows for brand, era, genre, archetype — counts cached with tag-based invalidation, eras grouped by eraSignal (D-18, D-19)"
    - "All 10 PRIMARY_ARCHETYPES values have a config entry with editorial displayName and description (D-15, D-16)"
    - "The retired Phase 18 Explore rails, their Explore-exclusive DAL readers, and the two see-all routes /explore/collectors and /explore/watches no longer exist; Home-shared code is left intact (D-01, D-02, D-03, D-04)"
  artifacts:
    - path: "src/lib/archetype-config.ts"
      provides: "10-entry ARCHETYPE_CONFIG lookup"
      contains: "ARCHETYPE_CONFIG"
    - path: "src/data/browse.ts"
      provides: "getBrowseBrandCounts, getBrowseEraCounts, getBrowseGenreCounts, getBrowseArchetypeCounts"
      exports: ["getBrowseBrandCounts", "getBrowseEraCounts", "getBrowseGenreCounts", "getBrowseArchetypeCounts"]
    - path: "src/data/__tests__/browse.test.ts"
      provides: "Wave 0 test scaffold for browse count shapes"
    - path: "src/data/__tests__/catalog-facets.test.ts"
      provides: "Wave 0 test scaffold for facet predicate extension"
    - path: "src/lib/archetype-config.test.ts"
      provides: "Wave 0 test verifying all 10 archetypes covered"
  key_links:
    - from: "src/data/browse.ts"
      to: "watches_catalog / brands tables"
      via: "db.execute(sql) GROUP BY queries"
      pattern: "GROUP BY"
    - from: "src/lib/archetype-config.ts"
      to: "src/lib/taste/vocab.ts PRIMARY_ARCHETYPES"
      via: "Record<PrimaryArchetype, ArchetypeConfig> keyed by all 10 values"
      pattern: "ARCHETYPE_CONFIG"
---

<objective>
Establish the foundation for Phase 46: create the archetype config, the Browse
count DAL, the Wave 0 test scaffolds the rest of the phase verifies against,
and cleanly retire the old Phase 18 Explore surface (D-01..D-04).

Purpose: Plans 02 and 03 both consume `ARCHETYPE_CONFIG` and `browse.ts`, and
both verify against the Wave 0 test files. The old Explore code must be deleted
before the new `/explore` shell (Plan 03) replaces `page.tsx`.
Output: `archetype-config.ts`, `browse.ts`, three test files, and the deletion
of six retired components + one DAL reader file + two routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-explore-shell-browse-archetypes/46-CONTEXT.md
@.planning/phases/46-explore-shell-browse-archetypes/46-RESEARCH.md
@.planning/phases/46-explore-shell-browse-archetypes/46-PATTERNS.md

<interfaces>
From src/lib/taste/vocab.ts:
```typescript
export const PRIMARY_ARCHETYPES = [
  'dress', 'dive', 'field', 'pilot', 'chrono',
  'gmt', 'racing', 'sport', 'tool', 'hybrid',
] as const
export const ERA_SIGNALS  // 3 values: 'vintage-leaning' | 'modern' | 'contemporary'
```
From src/lib/types.ts: `PrimaryArchetype`, `EraSignal` type aliases.
From src/data/catalog.ts § getTopStyleTags (lines 473-485): the canonical
`'use cache'` + `cacheLife('hours')` + `db.execute(sql\`...\`)` + `as unknown as Array<{...}>` pattern.
`src/db/schema.ts`: `watchesCatalog` (`primaryArchetype`, `eraSignal`, `brandId`),
`brands` (`id`, `name`, `slug`, `nameNormalized`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create archetype config + Wave 0 archetype-config test</name>
  <files>src/lib/archetype-config.ts, src/lib/archetype-config.test.ts</files>
  <read_first>
    - src/lib/taste/vocab.ts (PRIMARY_ARCHETYPES — the 10-value source of truth)
    - src/lib/types.ts (PrimaryArchetype type alias)
    - src/lib/taste/vocab.test.ts (existing test style for a vocab/config file)
    - .planning/phases/46-explore-shell-browse-archetypes/46-UI-SPEC.md § "Archetype chip display names and editorial descriptions" (the 10 approved display name / description pairs)
  </read_first>
  <behavior>
    - Test: every value in PRIMARY_ARCHETYPES has a key in ARCHETYPE_CONFIG (Object.keys(ARCHETYPE_CONFIG) covers all 10).
    - Test: each ARCHETYPE_CONFIG entry has a non-empty `displayName` and non-empty `description` string.
    - Test: each entry's `value` field equals its key.
    - Test: no raw archetype value (e.g. literal "chrono", "gmt") appears as a displayName — displayNames are editorial labels distinct from the raw value.
  </behavior>
  <action>
    Create `src/lib/archetype-config.ts` exporting an `ArchetypeConfig` interface
    (`value: PrimaryArchetype`, `displayName: string`, `description: string`) and
    a `Record<PrimaryArchetype, ArchetypeConfig>` named `ARCHETYPE_CONFIG` with one
    entry for every value in `PRIMARY_ARCHETYPES` (dress, dive, field, pilot,
    chrono, gmt, racing, sport, tool, hybrid). Use the UI-SPEC § "Archetype chip
    display names and editorial descriptions" table as the working default copy
    (D-16 — owner reviews this copy during UAT; the UI-SPEC proposed defaults are
    the locked-in working values per planner discretion). No zod schema — this is
    a static lookup. Import `PrimaryArchetype` from `@/lib/types`. Then create
    `src/lib/archetype-config.test.ts` covering the four behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/lib/archetype-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/archetype-config.ts` exports `ARCHETYPE_CONFIG` and `ArchetypeConfig`.
    - `Object.keys(ARCHETYPE_CONFIG).length === 10`.
    - `npx vitest run src/lib/archetype-config.test.ts` exits 0 with all assertions passing.
    - `npx tsc --noEmit` reports no error for the new file.
  </acceptance_criteria>
  <done>The config covers all 10 archetypes and the test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create browse.ts count DAL + Wave 0 browse + catalog-facet test scaffolds</name>
  <files>src/data/browse.ts, src/data/__tests__/browse.test.ts, src/data/__tests__/catalog-facets.test.ts</files>
  <read_first>
    - src/data/catalog.ts (lines 1, 473-485 — `getTopStyleTags` 'use cache' + db.execute(sql) pattern; lines 274-278, 337-403 — CatalogSearchFilters + searchCatalogWatches predicate builder this scaffold targets)
    - src/db/schema.ts (`watchesCatalog`, `brands` table definitions)
    - src/data/__tests__/curatedLists.test.ts (existing DAL test style — db setup, fixtures, assertions)
    - .planning/phases/46-explore-shell-browse-archetypes/46-RESEARCH.md § "Browse Index — Count Query Design" and § "Pattern 4"
  </read_first>
  <behavior>
    - browse.test.ts — getBrowseArchetypeCounts: returns Array of `{ archetype: string; count: number }`, count is a positive integer, only rows with non-null primary_archetype appear.
    - browse.test.ts — getBrowseEraCounts: returns Array of `{ era: string; count: number }`, era values are within ERA_SIGNALS.
    - browse.test.ts — getBrowseGenreCounts: returns Array of `{ genre: string; count: number }`, same shape as archetype counts.
    - browse.test.ts — getBrowseBrandCounts: returns Array of `{ brandId: string; name: string; slug: string; count: number }`, ordered by name_normalized ascending, only brands with count >= 1.
    - catalog-facets.test.ts — searchCatalogWatches with `{ archetype: 'dive' }` and empty query string returns only watches whose primaryArchetype is 'dive' (query-free facet run, D-11).
    - catalog-facets.test.ts — searchCatalogWatches with `{ era: <ERA_SIGNALS[0]> }` and empty query returns only watches with that eraSignal.
    - catalog-facets.test.ts — searchCatalogWatches with `{ genre: 'dive' }` behaves identically to `{ archetype: 'dive' }` (same primary_archetype column).
    - catalog-facets.test.ts — searchCatalogWatches with `{ brand: <known slug> }` returns only watches whose brandId resolves to that brand slug.
  </behavior>
  <action>
    Create `src/data/browse.ts` with `import 'server-only'` first, then exporting
    four async functions — `getBrowseArchetypeCounts`, `getBrowseEraCounts`,
    `getBrowseGenreCounts`, `getBrowseBrandCounts` — each following the
    `getTopStyleTags` pattern: `'use cache'` directive at function top,
    `cacheTag('explore', 'explore:browse')`, `cacheLife('hours')`, a
    `db.execute(sql\`...\`)` GROUP BY query, and a `as unknown as Array<{...}>`
    return cast. Use the four SQL queries from RESEARCH.md § "Pattern 4" verbatim:
    archetype/genre group by `primary_archetype WHERE NOT NULL`, era groups by
    `era_signal WHERE NOT NULL`, brand joins `brands` to `watches_catalog` on
    `brand_id` ordered by `name_normalized ASC`. Each function declares its return
    type explicitly (`Promise<Array<{...}>>`).

    Then create the two Wave 0 test scaffolds. `src/data/__tests__/browse.test.ts`
    covers the four browse-count behaviors above. `src/data/__tests__/catalog-facets.test.ts`
    covers the four searchCatalogWatches facet behaviors above — these tests will
    fail or be skipped until Plan 02 extends `CatalogSearchFilters`; mark the
    facet-extension assertions with `it.todo` or `it.skip` and a comment
    `// unskip in Plan 02 once searchCatalogWatches accepts brand/era/genre/archetype`.
    The browse.test.ts assertions must run live (browse.ts is fully built in this task).
  </action>
  <verify>
    <automated>npx vitest run src/data/__tests__/browse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/browse.ts` exports all four `getBrowse*Counts` functions.
    - `grep -c "'use cache'" src/data/browse.ts` returns 4 (one per function).
    - `grep -c "cacheTag('explore', 'explore:browse')" src/data/browse.ts` returns 4.
    - `npx vitest run src/data/__tests__/browse.test.ts` exits 0.
    - `src/data/__tests__/catalog-facets.test.ts` exists and contains the four facet behaviors as `it.todo`/`it.skip` with the unskip comment.
    - `npx tsc --noEmit` reports no error for the new files.
  </acceptance_criteria>
  <done>browse.ts queries return correctly-shaped data and the Wave 0 catalog-facet scaffold is in place for Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the Phase 18 Explore surface + CollectorArchetypes test scaffold</name>
  <files>src/components/explore/ExploreHero.tsx, src/components/explore/PopularCollectors.tsx, src/components/explore/PopularCollectorRow.tsx, src/components/explore/TrendingWatches.tsx, src/components/explore/GainingTractionWatches.tsx, src/components/explore/DiscoveryWatchCard.tsx, src/data/discovery.ts, src/app/explore/collectors/page.tsx, src/app/explore/watches/page.tsx, src/components/explore/__tests__/CollectorArchetypes.test.tsx</files>
  <read_first>
    - src/app/explore/page.tsx (current Phase 18 page — confirms which components it imports; this file is NOT deleted here, it is replaced in Plan 03)
    - src/data/discovery.ts (the readers being deleted — getTrendingCatalogWatches, getGainingTractionCatalogWatches, getMostFollowedCollectors)
    - .planning/phases/46-explore-shell-browse-archetypes/46-RESEARCH.md § "Old Explore Retire Scope (D-01..D-04)" (the verified safe-to-delete table + the "Check Before Deleting" notes)
    - src/data/wearEvents.ts (confirm getWearEventsCountByUser has no caller other than old explore/page.tsx)
    - src/components/explore/__tests__/curatedLists.test.ts is not the right analog — use src/components/search/WatchSearchRowsAccordion.test.tsx for the component-test style
  </read_first>
  <action>
    Delete the six Explore-exclusive components and the two retired routes per
    D-01..D-03: `ExploreHero.tsx`, `PopularCollectors.tsx`, `PopularCollectorRow.tsx`,
    `TrendingWatches.tsx`, `GainingTractionWatches.tsx`, `DiscoveryWatchCard.tsx`,
    `src/app/explore/collectors/page.tsx`, `src/app/explore/watches/page.tsx`.
    Delete the directories `src/app/explore/collectors/` and `src/app/explore/watches/`
    once their `page.tsx` is removed. From `src/data/discovery.ts`, delete the three
    retiring readers `getTrendingCatalogWatches`, `getGainingTractionCatalogWatches`,
    `getMostFollowedCollectors`; if those are the only exports, delete `discovery.ts`
    entirely — otherwise leave the file with its remaining exports intact.
    Per D-04, do NOT delete `src/data/wearEvents.ts` or `src/data/profiles.ts` — those
    files have non-Explore (Home) callers; only their imports into the old explore
    page go away in Plan 03. Do NOT delete or modify `src/app/explore/page.tsx` in
    this task — Plan 03 replaces it.

    All inbound links to `/explore/collectors` and `/explore/watches` live inside
    the six files being deleted (verified in RESEARCH.md); after deletion run a
    grep to confirm zero surviving references in `src/`.

    Then create `src/components/explore/__tests__/CollectorArchetypes.test.tsx`
    as a Wave 0 scaffold for the module Plan 03 builds: a test asserting the
    component renders `null` when its archetype-count data is empty (EXPL-02
    null-hide), and a test asserting it renders 10 chips when given 10 counts.
    Mark both with `it.skip` and a comment `// unskip in Plan 03 once CollectorArchetypes is built`.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'explore/collectors\|explore/watches\|TrendingWatches\|GainingTractionWatches\|PopularCollectors\|ExploreHero\|DiscoveryWatchCard' src/ --include='*.tsx' --include='*.ts' | grep -v __tests__)" && echo NO_DANGLING_REFS</automated>
  </verify>
  <acceptance_criteria>
    - The six component files and two route `page.tsx` files no longer exist (`test ! -e` passes for each).
    - `grep -rn 'explore/collectors\|explore/watches' src/` returns no matches outside test files.
    - `grep -rn 'TrendingWatches\|GainingTractionWatches\|PopularCollectors\|ExploreHero\|DiscoveryWatchCard' src/` returns no matches outside `__tests__/`.
    - `src/components/explore/__tests__/CollectorArchetypes.test.tsx` exists with two `it.skip` cases and the unskip comment.
    - `npx tsc --noEmit` reports no error introduced by the deletions (the old `src/app/explore/page.tsx` is the only remaining importer of the deleted components — its breakage is expected and resolved in Plan 03; if `tsc` flags only `src/app/explore/page.tsx`, that is acceptable for this task. If it flags any OTHER file, that file had an undiscovered import and must be repointed before completing.)
  </acceptance_criteria>
  <done>The old Explore surface is gone, no dangling references survive outside the soon-to-be-replaced explore page, and the CollectorArchetypes test scaffold is staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → browse DAL | Catalog rows read by GROUP BY queries; public-read RLS; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Information Disclosure | `browse.ts` count functions | mitigate | Browse counts are viewer-independent; functions take no `viewerId` argument and call no `getCurrentUser()` inside `'use cache'` scope — global cache entry shared safely across all users (no per-viewer data) |
| T-46-02 | Information Disclosure | catalog GROUP BY queries | accept | `watches_catalog` is public-read by RLS design; aggregate counts expose no PII or per-user data — no mitigation needed |
</threat_model>

<verification>
- `npx vitest run src/lib/archetype-config.test.ts src/data/__tests__/browse.test.ts` — both green.
- `grep -rn 'TrendingWatches\|explore/collectors' src/` — no matches outside `__tests__/`.
- `npx tsc --noEmit` — only `src/app/explore/page.tsx` may show errors (resolved by Plan 03).
</verification>

<success_criteria>
- `ARCHETYPE_CONFIG` covers all 10 `PRIMARY_ARCHETYPES`.
- `browse.ts` exports four working count functions, each cache-tagged `explore:browse`.
- Old Phase 18 Explore components, DAL readers, and the two see-all routes are deleted.
- Wave 0 test scaffolds (`browse.test.ts`, `catalog-facets.test.ts`, `archetype-config.test.ts`, `CollectorArchetypes.test.tsx`) exist for downstream plans to verify against.
</success_criteria>

<output>
After completion, create `.planning/phases/46-explore-shell-browse-archetypes/46-01-SUMMARY.md`
</output>
</output>
