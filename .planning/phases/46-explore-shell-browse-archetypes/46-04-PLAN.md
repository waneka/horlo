---
phase: 46-explore-shell-browse-archetypes
plan: 04
type: execute
wave: 3
depends_on: [46-02, 46-03]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [EXPL-01, EXPL-03, EXPL-05]
must_haves:
  truths:
    - "ROADMAP.md Phase 46 criterion #4 says 10 archetypes, not 8"
    - "REQUIREMENTS.md EXPL-05 says 10 archetypes, not eight"
    - "REQUIREMENTS.md EXPL-03 reflects that the price-band index is deferred from Phase 46"
    - "The end-to-end Browse/Archetypes → /search deep-link flow is verified intact"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 46 success criterion #4 amended 8 → 10"
      contains: "10 archetypes"
    - path: ".planning/REQUIREMENTS.md"
      provides: "EXPL-05 amended to 10; EXPL-03 price-band clause marked deferred"
      contains: "ten archetypes"
  key_links:
    - from: ".planning/ROADMAP.md Phase 46 criterion #4"
      to: "live PRIMARY_ARCHETYPES vocab (10 values)"
      via: "documentation amendment"
      pattern: "10 archetypes"
---

<objective>
Close Phase 46: amend the stale "8 archetypes" figure in ROADMAP.md and
REQUIREMENTS.md to 10 (D-15 housekeeping), reflect the Phase-46 price-band
deferral in EXPL-03, and run a final end-to-end integration check of the
Browse/Archetypes → `/search` deep-link flow.

Purpose: The roadmap and requirements still carry the stale "8" the live
`PRIMARY_ARCHETYPES` vocab contradicts. Phase 44 already verified 10-archetype
coverage; this plan makes the source artifacts consistent and confirms the
phase's cross-plan seams hold.
Output: amended ROADMAP.md + REQUIREMENTS.md, and a verified integration pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-explore-shell-browse-archetypes/46-CONTEXT.md
@.planning/phases/46-explore-shell-browse-archetypes/46-02-SUMMARY.md
@.planning/phases/46-explore-shell-browse-archetypes/46-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP.md and REQUIREMENTS.md — 8 → 10 archetypes + price-band deferral</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (§ "Phase 46: Explore Shell + Browse + Archetypes" — success criterion #4 with the stale "8 archetypes" / "all 8 chips" text)
    - .planning/REQUIREMENTS.md (§ "Explore Page (EXPL)" — EXPL-05 with "eight archetypes", EXPL-03 with the price-band clause)
    - .planning/phases/46-explore-shell-browse-archetypes/46-CONTEXT.md § D-15, § D-08, § Deferred Ideas (the rationale for both amendments)
  </read_first>
  <action>
    In `.planning/ROADMAP.md`, in the "Phase 46" success criteria, amend criterion
    #4: change "all 8 archetypes" to "all 10 archetypes" and "all 8 chips resolve
    to at least one result" to "all 10 chips resolve to at least one result". Add
    a parenthetical note `(amended 2026-05-19 from 8 — the live PRIMARY_ARCHETYPES
    vocab is 10, per Phase 44 D-16 / Phase 46 D-15)`.

    In `.planning/REQUIREMENTS.md` § "Explore Page (EXPL)", amend EXPL-05: change
    "chip rail of eight archetypes" to "chip rail of ten archetypes". Amend
    EXPL-03: leave the requirement text intact but append a note that the
    price-band index is deferred from Phase 46 to v6.0 Market Value (SEED-005)
    because `watches_catalog` has no price column — Phase 46 ships the brand, era,
    and genre indices only. Phrase the EXPL-03 note as a scope clarification, not
    a requirement deletion (the price-band index remains a v6.0 requirement).

    Do not change requirement IDs or check-state markers; these are text
    amendments only.
  </action>
  <verify>
    <automated>grep -q "10 archetypes" .planning/ROADMAP.md && grep -q "ten archetypes" .planning/REQUIREMENTS.md && ! grep -q "all 8 archetypes" .planning/ROADMAP.md && echo AMENDED</automated>
  </verify>
  <acceptance_criteria>
    - `grep "10 archetypes" .planning/ROADMAP.md` matches; `grep "all 8 archetypes" .planning/ROADMAP.md` returns nothing.
    - `grep "ten archetypes" .planning/REQUIREMENTS.md` matches; `grep "eight archetypes" .planning/REQUIREMENTS.md` returns nothing.
    - REQUIREMENTS.md EXPL-03 carries a note that the price-band index is deferred from Phase 46 to v6.0.
    - No requirement ID or `[ ]`/`[x]` marker was altered.
  </acceptance_criteria>
  <done>ROADMAP.md and REQUIREMENTS.md reflect 10 archetypes and the Phase-46 price-band deferral.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite integration verification of the Phase 46 deep-link flow</name>
  <files>.planning/phases/46-explore-shell-browse-archetypes/46-VALIDATION.md</files>
  <read_first>
    - .planning/phases/46-explore-shell-browse-archetypes/46-VALIDATION.md (the validation contract — record the integration result here)
    - .planning/phases/46-explore-shell-browse-archetypes/46-02-SUMMARY.md
    - .planning/phases/46-explore-shell-browse-archetypes/46-03-SUMMARY.md
  </read_first>
  <action>
    Run the full Vitest suite (`npx vitest run`) and `npx tsc --noEmit` and a
    production build (`npm run build`) to confirm the phase ships clean. Confirm
    the cross-plan seams: (a) `src/components/explore/CollectorArchetypes.tsx`
    chip hrefs use the `archetype` URL param that Plan 02's `useSearchState`
    reads; (b) `src/app/explore/brands/page.tsx` brand-row hrefs use the `brand`
    slug param that Plan 02's `searchSchema` validates and `searchCatalogWatches`
    resolves via slug subquery; (c) `src/data/browse.ts` count functions carry
    the `cacheTag('explore', 'explore:browse')` tag busted by the existing
    `revalidateTag('explore', 'max')` calls in watch Server Actions. Grep to
    confirm no surviving references to the retired Phase 18 components or routes.
    Record the integration result (commands run, pass/fail, any seam notes) in
    the "Per-Task Verification Map" and "Validation Sign-Off" sections of
    `46-VALIDATION.md`, and set `nyquist_compliant: true` in its frontmatter if
    all automated checks pass.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` exits 0 — full suite green (no skipped Phase 46 cases remain).
    - `npm run build` completes with no error.
    - `grep -rn 'explore/collectors\|explore/watches\|TrendingWatches\|PopularCollectors' src/` returns no matches outside `__tests__/`.
    - `46-VALIDATION.md` records the integration result and `nyquist_compliant: true` if all checks pass.
  </acceptance_criteria>
  <done>The full suite, type-check, and production build pass; the cross-plan deep-link seams are confirmed intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is documentation amendment + verification — it introduces no new code path or trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-09 | Tampering | (none) | accept | This plan amends planning docs and runs verification commands only — no executable code, no new input surface; the Phase 46 threat surface is fully covered by T-46-01..08 in Plans 01-03 |
</threat_model>

<verification>
- `grep "10 archetypes" .planning/ROADMAP.md` and `grep "ten archetypes" .planning/REQUIREMENTS.md` both match.
- `npx tsc --noEmit && npx vitest run && npm run build` — all pass.
- No surviving references to retired Phase 18 Explore code.
</verification>

<success_criteria>
- ROADMAP.md criterion #4 and REQUIREMENTS.md EXPL-05 say 10 archetypes.
- REQUIREMENTS.md EXPL-03 reflects the Phase-46 price-band deferral.
- Full suite, type-check, and production build pass.
- The Browse/Archetypes → `/search` deep-link seams are verified intact.
</success_criteria>

<output>
After completion, create `.planning/phases/46-explore-shell-browse-archetypes/46-04-SUMMARY.md`
</output>
