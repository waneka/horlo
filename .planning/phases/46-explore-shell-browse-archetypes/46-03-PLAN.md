---
phase: 46-explore-shell-browse-archetypes
plan: 03
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - src/app/explore/page.tsx
  - src/components/explore/CollectorArchetypes.tsx
  - src/components/explore/BrowseModule.tsx
  - src/components/explore/HeroModule.tsx
  - src/components/explore/CuratedListsRail.tsx
  - src/components/explore/WhereCollectionsGo.tsx
  - src/app/explore/brands/page.tsx
  - src/app/explore/eras/page.tsx
  - src/app/explore/genres/page.tsx
  - src/components/explore/__tests__/CollectorArchetypes.test.tsx
autonomous: true
requirements: [EXPL-01, EXPL-02, EXPL-03, EXPL-04, EXPL-05]
must_haves:
  truths:
    - "/explore renders a five-module shell — stacked on mobile, 2-column grid on desktop"
    - "Any module with no content renders null (no empty container)"
    - "Browse the Catalog shows 3 entry tiles (Brands, Eras, Genres) deep-linking to index pages"
    - "Each index page lists its groupings with accurate counts and deep-links to prefiltered /search"
    - "The Brands index has A-Z jump navigation"
    - "Collector Archetypes shows 10 chips, each with a watch-count badge, deep-linking to /search?archetype=..."
  artifacts:
    - path: "src/app/explore/page.tsx"
      provides: "5-module /explore shell"
      contains: "CollectorArchetypes"
    - path: "src/components/explore/CollectorArchetypes.tsx"
      provides: "10-chip archetype rail with count badges"
      contains: "ARCHETYPE_CONFIG"
    - path: "src/components/explore/BrowseModule.tsx"
      provides: "3-tile Browse entry module"
      contains: "explore/brands"
    - path: "src/app/explore/brands/page.tsx"
      provides: "Brands index with A-Z jump nav"
      contains: "letter-"
    - path: "src/app/explore/eras/page.tsx"
      provides: "Eras index"
    - path: "src/app/explore/genres/page.tsx"
      provides: "Genres index"
  key_links:
    - from: "src/components/explore/CollectorArchetypes.tsx"
      to: "src/data/browse.ts getBrowseArchetypeCounts"
      via: "'use cache' Server Component data fetch"
      pattern: "getBrowseArchetypeCounts"
    - from: "src/app/explore/brands/page.tsx"
      to: "/search?tab=watches&brand={slug}"
      via: "Link href per brand row"
      pattern: "search\\?tab=watches&brand"
    - from: "src/components/explore/CollectorArchetypes.tsx"
      to: "/search?tab=watches&archetype={value}"
      via: "Link href per chip"
      pattern: "search\\?tab=watches&archetype"
---

<objective>
Rebuild `/explore` as the five-module shell and ship its two live modules
(Collector Archetypes, Browse the Catalog) plus the three Browse index pages
(`/explore/brands`, `/explore/eras`, `/explore/genres`). The three Phase-47
editorial modules are present as null-returning slot stubs (EXPL-02).

Purpose: This is the visible deliverable of Phase 46 — the new Explore surface.
The deep-links it generates land on the `/search` facet extension built in
Plan 02.
Output: a new `explore/page.tsx`, five module components, three index pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-explore-shell-browse-archetypes/46-CONTEXT.md
@.planning/phases/46-explore-shell-browse-archetypes/46-RESEARCH.md
@.planning/phases/46-explore-shell-browse-archetypes/46-PATTERNS.md
@.planning/phases/46-explore-shell-browse-archetypes/46-UI-SPEC.md
@.planning/phases/46-explore-shell-browse-archetypes/46-01-SUMMARY.md

<interfaces>
From src/data/browse.ts (Plan 01):
```typescript
getBrowseArchetypeCounts(): Promise<Array<{ archetype: string; count: number }>>
getBrowseEraCounts(): Promise<Array<{ era: string; count: number }>>
getBrowseGenreCounts(): Promise<Array<{ genre: string; count: number }>>
getBrowseBrandCounts(): Promise<Array<{ brandId: string; name: string; slug: string; count: number }>>
```
From src/lib/archetype-config.ts (Plan 01):
```typescript
export const ARCHETYPE_CONFIG: Record<PrimaryArchetype, ArchetypeConfig>
// { value, displayName, description }
```
From src/lib/auth: `getCurrentUser()` — call in page Server Component scope,
OUTSIDE any 'use cache' boundary. proxy.ts already auth-gates /explore.
Era display map: 'vintage-leaning' → "Vintage Leaning", 'modern' → "Modern",
'contemporary' → "Contemporary".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 5-module shell + CollectorArchetypes + BrowseModule + Phase-47 null stubs</name>
  <files>src/app/explore/page.tsx, src/components/explore/CollectorArchetypes.tsx, src/components/explore/BrowseModule.tsx, src/components/explore/HeroModule.tsx, src/components/explore/CuratedListsRail.tsx, src/components/explore/WhereCollectionsGo.tsx, src/components/explore/__tests__/CollectorArchetypes.test.tsx</files>
  <read_first>
    - src/app/explore/page.tsx (current Phase 18 page — replaced wholesale; reuse the metadata + getCurrentUser auth pattern only)
    - src/data/browse.ts (the count functions this task consumes — created in Plan 01)
    - src/lib/archetype-config.ts (ARCHETYPE_CONFIG)
    - src/components/explore/__tests__/CollectorArchetypes.test.tsx (Wave 0 scaffold — unskip its two cases)
    - src/components/ui/badge.tsx (Badge primitive for count badges)
    - .planning/phases/46-explore-shell-browse-archetypes/46-PATTERNS.md § "src/app/explore/page.tsx", § "CollectorArchetypes.tsx", § "BrowseModule.tsx", § "Phase-47 null stubs" (the exact module composition + 'use cache' substitutions)
    - .planning/phases/46-explore-shell-browse-archetypes/46-UI-SPEC.md § "/explore Shell Layout", § "Browse the Catalog Module", § "Collector Archetypes Module", § "CSS Chain Assertions" items 1, 2, 5
  </read_first>
  <action>
    Replace `src/app/explore/page.tsx` with the new 5-module Server Component
    shell. Keep `export const metadata = { title: 'Explore — Horlo' }` and
    `const user = await getCurrentUser()` (auth assertion only — do NOT pass
    `user.id` to any module; Browse/Archetypes are viewer-independent). Render a
    `<main className="container mx-auto px-4 md:px-8 py-8 max-w-6xl">` wrapping
    `<div className="flex flex-col gap-6 md:grid md:grid-cols-2 md:gap-8">` with
    the five module components in order: `<HeroModule />` (`md:col-span-2`),
    `<CollectorArchetypes />` (`md:col-span-1`), `<BrowseModule />` (`md:col-span-1`),
    `<CuratedListsRail />` (`md:col-span-2`), `<WhereCollectionsGo />` (`md:col-span-2`).

    Create the three Phase-47 null stubs `HeroModule.tsx`, `CuratedListsRail.tsx`,
    `WhereCollectionsGo.tsx` — each an `async` function that returns `null` with a
    comment `// Phase 47 wires this. Per EXPL-02: absent is correct.`. No
    `'use cache'` on the null stubs.

    Create `src/components/explore/CollectorArchetypes.tsx` following the
    `TrendingWatches` pattern: `'use cache'`, `cacheTag('explore', 'explore:archetypes')`,
    `cacheLife('hours')`, fetch `getBrowseArchetypeCounts()`, `if (counts.length === 0) return null`
    (EXPL-02). Render `<section className="space-y-4">` with `<h2 className="text-xl font-semibold leading-tight text-foreground">Collector Archetypes</h2>`
    and a `<div className="flex flex-wrap gap-2">` of one chip per count row. Each
    chip is a `<Link href={\`/search?tab=watches&archetype=${value}\`}>` wrapping a
    button-styled element with `min-h-[44px] py-2.5 px-4 rounded-full border bg-secondary`,
    the `ARCHETYPE_CONFIG[value].displayName` label, and a `<Badge variant="secondary">`
    with the count. Iterate in `ARCHETYPE_CONFIG` order so all 10 archetypes appear
    even if a count row is missing (show count 0 for any archetype absent from the
    query result — though Phase 44 verified all 10 resolve).

    Create `src/components/explore/BrowseModule.tsx` following the same shell:
    `'use cache'`, `cacheTag('explore', 'explore:browse')`, `cacheLife('hours')`.
    Render `<h2>Browse the Catalog</h2>` and a `<div className="grid grid-cols-3 gap-3">`
    of exactly 3 tiles — Brands, Eras, Genres (no 4th Price-bands tile, D-08). Each
    tile is a `<Link>` to `/explore/brands`, `/explore/eras`, `/explore/genres`
    wrapping `<div className="flex flex-col items-center justify-center gap-2 rounded-xl bg-card border border-border p-4 min-h-12 hover:bg-muted transition-colors text-center">`
    with a lucide icon (`Building2`/`Clock`/`Tag`, `size-5 text-muted-foreground`)
    and a `<span className="text-sm font-semibold text-foreground">` label.

    Then unskip the two `it.skip` cases in `CollectorArchetypes.test.tsx` (null
    return on empty counts; 10 chips on full counts) and make them pass against
    the built component.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/explore/__tests__/CollectorArchetypes.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/explore/page.tsx` imports and renders all five module components in a `flex flex-col gap-6 md:grid md:grid-cols-2` container.
    - `CollectorArchetypes.tsx` has `'use cache'`, `cacheTag('explore', 'explore:archetypes')`, fetches `getBrowseArchetypeCounts`, and returns `null` on empty.
    - `CollectorArchetypes.tsx` renders 10 chips; each chip has `min-h-[44px]` and links to `/search?tab=watches&archetype=`.
    - `BrowseModule.tsx` renders exactly 3 tiles linking to `/explore/brands`, `/explore/eras`, `/explore/genres`; each tile container has `min-h-12`.
    - The three Phase-47 stub components return `null`.
    - `npx tsc --noEmit` passes; `CollectorArchetypes.test.tsx` cases are unskipped and green.
  </acceptance_criteria>
  <done>/explore renders the 5-module shell with two live modules and three hidden slots.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Brands, Eras, and Genres index pages</name>
  <files>src/app/explore/brands/page.tsx, src/app/explore/eras/page.tsx, src/app/explore/genres/page.tsx</files>
  <read_first>
    - src/data/browse.ts (getBrowseBrandCounts, getBrowseEraCounts, getBrowseGenreCounts)
    - src/app/explore/page.tsx (the rebuilt page from Task 1 — back-link target and auth pattern reference)
    - .planning/phases/46-explore-shell-browse-archetypes/46-PATTERNS.md § "src/app/explore/brands/page.tsx", § "eras/page.tsx and genres/page.tsx" (layout + back-link + A-Z section patterns)
    - .planning/phases/46-explore-shell-browse-archetypes/46-UI-SPEC.md § "Brands Index Page", § "Eras Index Page", § "Genres Index Page", § "A–Z Jump Nav", § "CSS Chain Assertions" item 3, § "Copywriting Contract" (era + genre display name maps, empty-state copy)
  </read_first>
  <action>
    Create three Server Component pages, each `<main className="container mx-auto px-4 md:px-8 py-8 max-w-2xl">`
    with `await getCurrentUser()` as an auth assertion (not passed to the DAL), a
    back-link `<Link href="/explore">` with a lucide `ChevronLeft` (`size-4`) and
    text "Explore", and an `<h1 className="text-xl font-semibold">` page title.
    These pages may use `'use cache'` at page scope (no per-viewer data beyond the
    auth assertion — keep `getCurrentUser()` OUTSIDE the cached scope; if page-level
    `'use cache'` conflicts with the auth call, fetch counts in a cached child
    component and keep the page itself uncached). Each empty-state uses the
    UI-SPEC § "Empty states" copy.

    `brands/page.tsx`: title "Brands". Fetch `getBrowseBrandCounts()`. Group the
    sorted brand rows by `name[0].toUpperCase()` in JS. Render a sticky A-Z jump
    nav `<nav aria-label="Jump to letter" className="sticky top-0 z-10 bg-background/95 backdrop-blur-sm border-b border-border py-2 mb-4">`
    containing a `<div className="flex gap-1 overflow-x-auto">` of all 26 letter
    anchors `<a href="#letter-{X}">`; letters with no brands get
    `opacity-30 pointer-events-none`. Render one `<section id="letter-{X}" className="scroll-mt-12 space-y-1">`
    per populated letter (the `scroll-mt-12` is required — CSS Chain Assertion 3),
    each with a letter heading and brand rows. Each brand row is a
    `<Link href={\`/search?tab=watches&brand=${slug}\`}>` showing the brand name
    and its count.

    `eras/page.tsx`: title "Eras". Fetch `getBrowseEraCounts()`. No A-Z nav. Render
    a plain list of era rows, each `<Link href={\`/search?tab=watches&era=${value}\`}>`
    with the era display name (mapped: `vintage-leaning`→"Vintage Leaning",
    `modern`→"Modern", `contemporary`→"Contemporary") and count.

    `genres/page.tsx`: title "Genres" (plainly "Genres", NOT "Archetypes" — the
    D-17 utility-vs-identity distinction). Fetch `getBrowseGenreCounts()`. No A-Z
    nav. Render a plain list of genre rows, each
    `<Link href={\`/search?tab=watches&genre=${value}\`}>` with the capitalized
    genre label (UI-SPEC genre display map: "GMT" uppercase, others title-case)
    and count. Use plain utility labels and counts — no editorial descriptions
    (that copy belongs only to Collector Archetypes).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All three `page.tsx` files exist under `src/app/explore/{brands,eras,genres}/`.
    - `brands/page.tsx` contains `id="letter-` section anchors with `scroll-mt-12` and a `sticky top-0` jump nav.
    - Each brand row links to `/search?tab=watches&brand=`, each era row to `&era=`, each genre row to `&genre=`.
    - `genres/page.tsx` page title is "Genres" (not "Archetypes") and rows have no editorial descriptions.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Three Browse index pages render grouped, counted lists deep-linking to prefiltered /search.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the /explore shell, modules, and index pages</name>
  <what-built>
    A new 5-module `/explore` shell (2 live modules, 3 hidden Phase-47 slots) and
    three Browse index pages (Brands with A-Z nav, Eras, Genres).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`. Visit `/explore`.
    2. Confirm only two modules render — Collector Archetypes and Browse the
       Catalog — with NO empty containers for Hero / Curated Lists / Where
       Collections Go (EXPL-02). On desktop ≥768px the two modules sit
       side-by-side; on mobile they stack.
    3. Confirm Collector Archetypes shows 10 chips with editorial names (e.g.
       "Dive Watch Devotee") and a count badge on each.
    4. Confirm Browse the Catalog shows exactly 3 tiles (Brands, Eras, Genres) —
       no 4th Price-bands tile.
    5. Tap an archetype chip — confirm it lands on `/search` prefiltered (verified
       functionally in Plan 02's checkpoint).
    6. Visit `/explore/brands` — confirm the A-Z jump nav is sticky at the top,
       tapping a letter scrolls to that section without the heading hiding under
       the nav bar, brand rows show counts, and tapping a brand opens prefiltered
       `/search`.
    7. Visit `/explore/eras` and `/explore/genres` — confirm grouped lists with
       counts and working deep-links; confirm the Genres page is titled "Genres"
       and reads as a plain utility list, visually distinct from the identity-copy
       Collector Archetypes module.
    8. Confirm the Archetypes module and the Genres index do not read as the same
       thing twice (D-17 distinction check).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /explore Server Component | Authenticated user reaches /explore; proxy.ts gate already enforced |
| DB → Browse module cache | Catalog counts read into globally-shared 'use cache' scopes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-06 | Information Disclosure | `CollectorArchetypes` / `BrowseModule` 'use cache' scopes | mitigate | Both modules are viewer-independent — they take no `viewerId` prop and call no `getCurrentUser()` inside `'use cache'`; auth stays in the page Server Component scope outside the cache boundary (RESEARCH.md Pitfall 2) |
| T-46-07 | Spoofing | `/explore` + index page auth | mitigate | `getCurrentUser()` called in every page Server Component; `proxy.ts` redirects unauthenticated requests before render — two-layer auth consistent with the retired Phase 18 page |
| T-46-08 | Tampering | index-page deep-link hrefs | accept | Generated `/search?...` hrefs carry catalog-derived slug/value strings only; any tampering by the user lands on the Plan 02 facet path where Zod `.max()` validation applies — no new surface |
</threat_model>

<verification>
- `npx tsc --noEmit` — no errors.
- `npx vitest run src/components/explore/__tests__/CollectorArchetypes.test.tsx` — green.
- Manual checkpoint Task 3 — all 8 steps confirmed.
</verification>

<success_criteria>
- `/explore` renders a 5-module shell — 2 live, 3 hidden — stacked on mobile, grid on desktop.
- Collector Archetypes shows 10 count-badged chips deep-linking to `/search?archetype=`.
- Browse the Catalog shows 3 tiles deep-linking to the index pages.
- The three index pages list counted groupings; Brands has working A-Z nav.
- The Genres index and Collector Archetypes are visually/editorially distinct (D-17).
</success_criteria>

<output>
After completion, create `.planning/phases/46-explore-shell-browse-archetypes/46-03-SUMMARY.md`
</output>
