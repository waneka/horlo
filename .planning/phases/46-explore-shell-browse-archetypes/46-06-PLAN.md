---
phase: 46-explore-shell-browse-archetypes
plan: 06
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/app/explore/brands/page.tsx
  - src/components/explore/CollectorArchetypes.tsx
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [EXPL-04, EXPL-05]
must_haves:
  truths:
    - "The /explore/brands A–Z nav wraps onto multiple lines responsively with no horizontal scrolling"
    - "Clicking an A–Z letter anchor smooth-scrolls to the target section"
    - "The A–Z nav pins below the global header, not behind it"
    - "Collector Archetypes renders only chips with a count ≥ 1 — zero-count chips are hidden"
    - "The Collector Archetypes module has an explanatory subtitle"
  artifacts:
    - path: "src/app/explore/brands/page.tsx"
      provides: "Responsive wrapping, smooth-scrolling, header-clearing sticky A–Z nav"
      contains: "flex-wrap"
    - path: "src/components/explore/CollectorArchetypes.tsx"
      provides: "Zero-count chip filter + module subtitle"
      contains: "count"
  key_links:
    - from: "src/app/explore/brands/page.tsx"
      to: "global header (h-12/h-16)"
      via: "sticky top-12 md:top-16"
      pattern: "top-12 md:top-16"
---

<objective>
Close five minor/major Phase 46 UAT gaps that are presentational fixes or
code-only data-gap handling, plus the roadmap/requirements housekeeping that
accompanies the archetype filter.

- G1 (minor): /explore/brands A–Z nav is a horizontal-scroll container — change
  to a wrapping flex row.
- G2 (minor): A–Z letter-anchor jumps are instant — add `scroll-smooth`.
- G3 (minor): A–Z nav uses `sticky top-0`, pinning it BEHIND the opaque global
  header (SlimTopNav h-12 / DesktopTopNav h-16, both `sticky top-0 z-50`) —
  change to `sticky top-12 md:top-16`.
- G4 (major): `tool` (0 watches) and `hybrid` (1 watch) archetypes are a
  genuine thin-catalog DATA gap, not a code bug (OWNER DECISION 2026-05-19).
  CollectorArchetypes must filter out any chip with count 0 — render only
  archetypes with ≥1 watch. Plus housekeeping: amend roadmap SC #4 /
  REQUIREMENTS.md EXPL-05 from "all 10 chips resolve" to "every visible chip
  resolves to ≥1 result". NO data migration or backfill.
- G6 (minor): the Collector Archetypes module header has no subtitle — add a
  short editorial one-liner.

Purpose: resolve the remaining UAT findings so Phase 46 ships clean.
Output: a polished /explore/brands A–Z nav, a thin-catalog-resilient archetype
rail, and corrected roadmap/requirements wording.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/g3-sticky-az-nav.md
@.planning/debug/g4-zero-count-archetype-chips.md
@./CLAUDE.md
@./AGENTS.md

<read_first>
- src/app/explore/brands/page.tsx — the file modified for G1/G2/G3. The A–Z
  nav is the `<nav aria-label="Jump to letter">` block at lines 71-94; its
  inner container `<div className="flex gap-1 overflow-x-auto scrollbar-none">`
  is the horizontal-scroll row (G1).
- src/components/explore/CollectorArchetypes.tsx — the file modified for
  G4/G6. The component builds `countMap` and maps `ARCHETYPE_ORDER` to chips
  (lines 43-71); the `<section>` header is lines 46-49.
- .planning/debug/g3-sticky-az-nav.md — confirms the header heights: SlimTopNav
  `h-12` (48px mobile), DesktopTopNav `h-16` (64px desktop), both
  `sticky top-0 z-50` opaque.
- .planning/debug/g4-zero-count-archetype-chips.md — confirms G4 is a data
  coverage gap; `getBrowseArchetypeCounts` is correct; do NOT touch the DAL.
- .planning/phases/46-explore-shell-browse-archetypes/46-UI-SPEC.md — typography
  contract. Lines 66-77: weights are 400/600 only; `text-sm text-muted-foreground`
  for muted descriptive copy. NEVER use `font-medium` (forbidden by the
  `no-raw-palette` test).
- node_modules/next/dist/docs/ — only if any Next.js 16 behavior is unclear
  (AGENTS.md: Next.js 16 has breaking changes).
</read_first>

<interfaces>
<!-- Verified facts. No codebase exploration needed. -->

Global header heights (from g3 debug, verified against source):
- SlimTopNav (mobile, <768px): `<header className="sticky top-0 z-50 ...">`,
  content row `h-12` → 48px.
- DesktopTopNav (≥768px): `<header className="sticky top-0 z-50 ...">`,
  content row `h-16` → 64px.
→ A–Z nav must pin at `top-12` (48px) mobile / `md:top-16` (64px) desktop.

CollectorArchetypes current logic:
- `counts` = `propCounts ?? await getBrowseArchetypeCounts()`; shape
  `Array<{ archetype: string; count: number }>`.
- `if (counts.length === 0) return null` — the whole-rail null-guard (KEEP).
- `countMap = new Map(counts.map(r => [r.archetype, r.count]))`.
- Renders one chip per `ARCHETYPE_ORDER` value; `count = countMap.get(value) ?? 0`.
→ G4 fix: render only `ARCHETYPE_ORDER` values whose resolved count ≥ 1.

Typography (UI-SPEC): module heading is `text-xl font-semibold`. A muted
subtitle should be `text-sm text-muted-foreground` at weight 400 — NO
`font-medium`, NO `font-bold` (no-raw-palette test forbids both).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the /explore/brands A–Z nav — wrap, smooth-scroll, header-clearing sticky (G1/G2/G3)</name>
  <files>src/app/explore/brands/page.tsx</files>
  <action>
    Three edits to the A–Z jump nav (the `<nav aria-label="Jump to letter">`
    block, currently lines 71-94):

    G1 — wrapping row: change the inner container from
    `<div className="flex gap-1 overflow-x-auto scrollbar-none">` to a wrapping
    flex row: `flex flex-wrap gap-1` (remove `overflow-x-auto` and
    `scrollbar-none`). Letters then wrap to ~2 lines at normal widths and up to
    ~3 on very small screens. No horizontal scrolling. Keep the letter anchor
    `<a>` styling; the `shrink-0 min-w-[28px]` on each letter is fine and helps
    even wrapping — leave it.

    G2 — smooth scroll: apply Tailwind `scroll-smooth` so letter-anchor jumps
    animate with ease-in-out. The simplest correct placement is on the root
    scroll container — the page `<main className="container ...">`. Add
    `scroll-smooth` to that `<main>`'s className. (Anchor jumps scroll the
    nearest scrollable ancestor / the document; `scroll-smooth` on the page
    root is sufficient and matches the existing `scroll-mt-12` offset already
    on the letter sections.)

    G3 — sticky pin target: change the `<nav>` className from
    `sticky top-0 z-10 ...` to `sticky top-12 md:top-16 z-10 ...`. This pins
    the A–Z nav BELOW the global header (SlimTopNav 48px mobile / DesktopTopNav
    64px desktop, both `sticky top-0 z-50`) instead of behind it. Keep `z-10`
    — it no longer competes with the header. Do not change `bg-background/95
    backdrop-blur-sm border-b border-border py-2 mb-4`.

    Do NOT change the letter sections' `scroll-mt-12` — it already offsets
    anchor landings for the mobile header and works alongside `scroll-smooth`.
    Do not introduce any `font-medium` / raw palette classes.
  </action>
  <verify>
    <automated>grep -n 'flex flex-wrap' src/app/explore/brands/page.tsx && grep -n 'scroll-smooth' src/app/explore/brands/page.tsx && grep -n 'sticky top-12 md:top-16' src/app/explore/brands/page.tsx && ! grep -n 'overflow-x-auto' src/app/explore/brands/page.tsx && echo "G1/G2/G3 source assertions PASS"</automated>
  </verify>
  <done>The A–Z nav inner container uses `flex flex-wrap` with no `overflow-x-auto`; the page `<main>` carries `scroll-smooth`; the `<nav>` uses `sticky top-12 md:top-16 z-10`. `npx tsc --noEmit` reports no new errors. `npx vitest run tests/no-raw-palette.test.ts` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Filter zero-count archetype chips + add module subtitle in CollectorArchetypes (G4/G6)</name>
  <files>src/components/explore/CollectorArchetypes.tsx</files>
  <action>
    G4 — hide zero-count chips (OWNER DECISION, locked): currently the
    component maps every `ARCHETYPE_ORDER` value to a chip and shows
    `countMap.get(value) ?? 0`, so `tool` (0 watches) and `hybrid` (1, or 0 on
    prod per the id-divergence amplifier) render as count-0 chips. Change the
    render so it iterates only the archetypes whose resolved count is ≥ 1.
    Concretely: derive the visible list by filtering `ARCHETYPE_ORDER` to
    values where `(countMap.get(value) ?? 0) >= 1`, and map THAT list to chips.
    Keep the existing whole-rail null-guard `if (counts.length === 0) return null`
    — and additionally, if the filtered visible list is empty, return null too
    (EXPL-02 absent-not-empty: no empty rail). Do NOT touch
    `getBrowseArchetypeCounts` or any DAL — G4 is a render-layer filter only,
    no data migration. `tool`/`hybrid` reappear automatically when v5.2 catalog
    expansion adds coverage.

    Update the file's top-of-file comment block: the line "Phase 44 verified
    all 10 archetypes resolve to ≥1 result" is now false — replace it with a
    note that the rail renders only archetypes with ≥1 catalog watch (zero-count
    archetypes are hidden per EXPL-02; tool/hybrid currently have thin/no
    coverage — a v5.2 catalog-expansion data gap, not a code bug).

    G6 — module subtitle: the `<section>` header currently has only the
    `<h2>Collector Archetypes</h2>`. Add a one-line editorial subtitle directly
    under the `<h2>`, inside the same header grouping. Copy: a short sentence
    framing archetypes as identity-based entry points into the catalog — e.g.
    "Browse the catalog by the kind of collector each watch speaks to."
    (final wording is editorial; keep it to one line, ≤ ~90 chars). Style it
    `text-sm text-muted-foreground` at the default weight 400 — do NOT use
    `font-medium` or `font-bold` (the `no-raw-palette` test forbids both;
    weights 400/600 only per UI-SPEC). Keep the `<h2>` as
    `text-xl font-semibold`. The `<section>` already has `space-y-4`; the
    h2+subtitle should sit together — wrap them in a small grouping `<div>` or
    use a tight `space-y` so the subtitle reads as part of the header, not as a
    sibling rail item.
  </action>
  <verify>
    <automated>npx vitest run tests/no-raw-palette.test.ts tests/data/__tests__/browse.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>CollectorArchetypes renders only archetype chips with count ≥ 1; returns null when no archetype has coverage; a `text-sm text-muted-foreground` subtitle sits under the `<h2>`. No `font-medium`/`font-bold`. `npx tsc --noEmit` reports no new errors. `no-raw-palette` test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Roadmap + REQUIREMENTS housekeeping — amend archetype coverage wording (G4)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    Amend the archetype-coverage wording to match the G4 owner decision
    (hide zero-count chips), mirroring how 46-04 amended the 8→10 figure.

    ROADMAP.md — Phase 46 Success Criterion #4 (line ~216). It currently reads
    "...each showing a watch-count badge; tapping a chip opens prefiltered
    search results with an archetype header; all 10 chips resolve to at least
    one result (amended 2026-05-19 from 8 ...)". Change the coverage clause
    from "all 10 chips resolve to at least one result" to "every visible chip
    resolves to at least one result — archetypes with zero catalog coverage
    are hidden per EXPL-02 (a thin-catalog data gap addressed by v5.2 catalog
    expansion, not a code defect)". Keep the existing "(amended 2026-05-19
    from 8 ...)" parenthetical and add a brief note that the
    every-visible-chip reframe is the 2026-05-19 G4 UAT amendment.

    REQUIREMENTS.md — EXPL-05 (line ~52). It currently reads "Collector
    Archetypes renders a chip rail of ten archetypes, each with a watch-count
    badge; tapping a chip opens prefiltered search results with an archetype
    header." Amend so it reflects that the rail renders one chip per archetype
    that has ≥1 catalog watch (zero-count archetypes hidden per EXPL-02); every
    visible chip resolves to ≥1 result. Extend the existing italic amendment
    note with the 2026-05-19 G4 reframe (do not delete the existing 8→10 note).

    Do not change any other roadmap/requirements content. These are
    documentation-only edits.
  </action>
  <verify>
    <automated>grep -n 'every visible chip' .planning/ROADMAP.md && grep -in 'zero.count\|≥1\|at least one' .planning/REQUIREMENTS.md | grep -i expl && echo "housekeeping assertions PASS"</automated>
  </verify>
  <done>ROADMAP.md SC #4 and REQUIREMENTS.md EXPL-05 both reframe coverage from "all chips" to "every visible chip resolves to ≥1 result", with zero-count archetypes hidden per EXPL-02. Existing 8→10 amendment notes are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog count data → CollectorArchetypes render | `getBrowseArchetypeCounts()` returns viewer-independent, RLS-public catalog counts; consumed inside a `'use cache'` scope. No user input crosses into this plan's surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-06-01 | Information Disclosure | CollectorArchetypes count badges | accept | Counts are public catalog aggregates (number of catalog watches per archetype) — no PII, no per-user data. Filtering zero-count chips reveals nothing sensitive; it reduces, not expands, what renders. The `'use cache'` + `cacheTag('explore', ...)` scope is unchanged. |
| T-46-06-02 | Tampering | /explore/brands A–Z anchor hrefs | accept | The A–Z nav anchors are static `#letter-X` fragment links generated from a hardcoded `LETTER_BUCKETS` constant — no user input, no outbound navigation. The G1/G2/G3 edits are CSS-class-only and introduce no new trust boundary. |
</threat_model>

<verification>
- `grep` source assertions in Task 1 and Task 3 verify pass.
- `npx vitest run tests/no-raw-palette.test.ts` — no forbidden `font-medium`/
  `font-bold`/raw-palette class introduced.
- `npx vitest run tests/data/__tests__/browse.test.ts` — Browse count DAL
  untouched and still green.
- `npx tsc --noEmit` — no new type errors.
- Manual (covered by execute-phase UAT): /explore/brands A–Z nav wraps with no
  horizontal scroll, letter taps smooth-scroll, the nav stays visible below the
  header on scroll; /explore Collector Archetypes shows only chips with count
  ≥ 1 and carries a subtitle.
</verification>

<success_criteria>
- The /explore/brands A–Z nav wraps responsively, smooth-scrolls on letter tap, and pins below the global header.
- CollectorArchetypes hides zero-count chips and shows an explanatory subtitle, with no `font-medium`/raw-palette violation.
- ROADMAP.md SC #4 and REQUIREMENTS.md EXPL-05 reflect the "every visible chip resolves" reframe.
</success_criteria>

<output>
After completion, create `.planning/phases/46-explore-shell-browse-archetypes/46-06-SUMMARY.md`
</output>
