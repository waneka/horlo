---
phase: 53-schema-rls-enum-extension
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified: []
autonomous: true
requirements: [SEC-01, SEC-06, LIKE-05, GATE-02, SEC-04]

must_haves:
  truths:
    - "Both Phase 53 migration files are applied to the LIVE LOCAL Postgres with all DO $$ assertions passing green"
    - "The Drizzle column shapes are materialized locally via drizzle-kit push (D-12 — drizzle-kit push is LOCAL ONLY; the raw SQL migrations remain authoritative for CHECK/index/RLS)"
    - "Deleting a watch cascades to its watch_likes and comments rows (SEC-06 fires, not just exists)"
    - "Inserting a duplicate (user_id, watch_id) like raises a 23505 unique violation (LIKE-05 fires)"
    - "notification_type enum reports 6 values on the live local DB after the enum migration applies"
  artifacts:
    - path: "supabase/migrations/20260522000000_phase53_likes_comments_rls.sql"
      provides: "Applied to live local DB — DO $$ assertions confirmed green"
    - path: "supabase/migrations/20260522000001_phase53_notification_enum.sql"
      provides: "Applied to live local DB (non-transactional) — enum count=6 confirmed"
  key_links:
    - from: "DDL + enum migration files"
      to: "live local Postgres (supabase_db_horlo, 127.0.0.1:54322)"
      via: "docker exec psql apply of raw SQL after drizzle-kit push (hybrid Drizzle+Supabase local sequence)"
      pattern: "docker exec.*psql"
---

<objective>
[BLOCKING] Apply the Phase 53 migrations to the LIVE LOCAL database so the in-migration `DO $$` assertions actually fire, then run the two manual smokes (cascade delete, duplicate-like) that prove the constraints ENFORCE rather than merely exist. This is the load-bearing verification step: build + `tsc --noEmit` pass on schema.ts WITHOUT the DB changing — the assertions ARE the test, and they only execute on apply.

Purpose: Convert the source-level artifacts from Plan 01 into a verified, applied local schema. No subsequent phase (54+) can safely build against tables that have not been confirmed to apply with green assertions.

Output: A local Postgres with `watch_likes`, `wear_likes`, `comments` tables, all RLS/REVOKE/constraints applied, the 6-value enum, and the two profile_settings columns — every `DO $$` assertion confirmed passing, plus cascade + duplicate-like smokes confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/53-schema-rls-enum-extension/53-CONTEXT.md
@.planning/phases/53-schema-rls-enum-extension/53-RESEARCH.md
@.planning/phases/53-schema-rls-enum-extension/53-VALIDATION.md

<environment>
<!-- Verified at planning time. -->
- Local Supabase stack is RUNNING. The Postgres container is `supabase_db_horlo`.
- Local Postgres URL: `postgresql://postgres:postgres@127.0.0.1:54322/postgres` (port 54322).
- Hybrid local apply: `supabase db reset` does NOT work standalone on this repo (it partially fails on `20260419999999_social_tables_create.sql` which duplicates Drizzle's CREATE TABLEs). The project-memory "Local DB reset workflow" documents the correct sequence: reset (partial fail OK), then `npx drizzle-kit push` (column shapes), then `docker exec psql < <each raw-SQL migration>`. For an INCREMENTAL apply of only the two NEW Phase 53 files (no full reset needed), see the action steps below.
- `drizzle-kit push` is LOCAL ONLY (never prod) — confirmed in project memory `project_drizzle_supabase_db_mismatch`.
- `npm test` runs vitest; no DB integration test framework — assertions live in-migration by design (VALIDATION.md).
</environment>
</context>

<tasks>

<task type="auto">
  <name>[BLOCKING] Task 1: Apply both Phase 53 migrations to the live local DB and confirm all DO $$ assertions pass</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260522000000_phase53_likes_comments_rls.sql (the DDL file written in Plan 01 — confirm it is transactional and ends with the DO $$ assertion block)
    - supabase/migrations/20260522000001_phase53_notification_enum.sql (the enum file — confirm NO BEGIN/COMMIT wrapper)
    - .planning/phases/53-schema-rls-enum-extension/53-RESEARCH.md §"Assumptions Log" A2 (the non-transactional risk: test on local FIRST) and §"Sampling Rate" / §"Manual-Only Verifications"
    - The project-memory "Local DB reset workflow" content reproduced in this plan's <environment> block above
  </read_first>
  <action>
    Apply the two new Phase 53 migrations to the LIVE LOCAL Postgres so the in-migration `DO $$` assertions fire. Two viable mechanics — choose based on whether a full reset is acceptable:

    PREFERRED — incremental apply (no full reset; existing local data preserved):
    1. First materialize the Drizzle column shapes locally: `npx drizzle-kit push` (this carries the new pgTable column shapes from schema.ts but NOT the CHECK/RLS/REVOKE/assertions — those live in the raw SQL). drizzle-kit push reads DATABASE_URL from .env.local (local 54322). Per Pitfall 4, the raw migration is idempotent (CREATE TABLE IF NOT EXISTS / DROP POLICY IF EXISTS / ADD COLUMN IF NOT EXISTS) so it no-ops over whatever push already created.
    2. Apply the DDL migration (transactional — the whole BEGIN/COMMIT incl. the DO $$ assertion block runs atomically): `docker exec -i supabase_db_horlo psql -U postgres -d postgres < supabase/migrations/20260522000000_phase53_likes_comments_rls.sql`. Capture the full stdout/stderr. The DO $$ assertions fire here; any failure RAISEs an exception and rolls back the whole transaction.
    3. Apply the enum migration (NON-transactional — `ALTER TYPE ADD VALUE` cannot run inside a transaction block, so each statement auto-commits): `docker exec -i supabase_db_horlo psql -U postgres -d postgres < supabase/migrations/20260522000001_phase53_notification_enum.sql`. The trailing DO $$ count assertion fires here. This MUST be tested on local first (A2 risk) before any prod push (Plan 03).

    FALLBACK — full reset (only if incremental apply leaves the local DB inconsistent): follow the documented hybrid sequence (`supabase db reset` → partial fail is expected → `npx drizzle-kit push` → `docker exec psql <` each prior raw-SQL migration in timestamp order → then the two Phase 53 files last).

    After applying, capture confirmation queries via `docker exec ... psql -c`:
    - `SELECT count(*) FROM pg_enum JOIN pg_type ON pg_enum.enumtypid = pg_type.oid WHERE pg_type.typname = 'notification_type';` → expect 6.
    - `SELECT tablename, count(*) FROM pg_policies WHERE tablename IN ('watch_likes','wear_likes','comments') GROUP BY tablename;` → expect watch_likes=3, wear_likes=3, comments=4.
    - `SELECT has_table_privilege('anon', 'public.comments', 'SELECT');` → expect false.

    If any `DO $$` assertion RAISEs (the apply exits non-zero with a `Phase 53 failed -- ...` message), the migration file authored in Plan 01 is wrong — do NOT patch around it here; report the exact failing assertion so Plan 01 can be revised. This task only PASSES when both files apply with zero raised exceptions.
  </action>
  <verify>
    <automated>docker exec -i supabase_db_horlo psql -U postgres -d postgres -t -c "SELECT count(*) FROM pg_enum JOIN pg_type ON pg_enum.enumtypid = pg_type.oid WHERE pg_type.typname = 'notification_type';" | tr -d '[:space:]'</automated>
  </verify>
  <acceptance_criteria>
    - The DDL migration apply (`docker exec ... < 20260522000000_...sql`) exits 0 with no `Phase 53 failed --` exception in output (the transactional DO $$ block passed).
    - The enum migration apply (`docker exec ... < 20260522000001_...sql`) exits 0 with no exception (the count=6 DO $$ passed).
    - `SELECT count(*) FROM pg_enum ... WHERE typname = 'notification_type'` returns exactly `6`.
    - `SELECT count(*) FROM pg_policies WHERE tablename = 'comments'` returns `4`; `watch_likes` and `wear_likes` each return `3`.
    - `SELECT has_table_privilege('anon', 'public.watch_likes', 'SELECT')`, `... 'public.wear_likes' ...`, and `... 'public.comments' ...` all return `f` (false) (SEC-01 enforced live).
  </acceptance_criteria>
  <done>Both migration files are applied to the live local DB. Every in-migration DO $$ assertion (table existence, FK cascade confdeltype, UNIQUE/CHECK existence, policy counts, anon-block, profile_settings columns, enum count=6) passed. Local schema now carries the full likes+comments layer.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test SEC-06 cascade delete and LIKE-05 duplicate-like enforcement on the live local DB</name>
  <files></files>
  <read_first>
    - .planning/phases/53-schema-rls-enum-extension/53-VALIDATION.md §"Manual-Only Verifications" (the cascade-delete and duplicate-like smoke instructions)
    - .planning/phases/53-schema-rls-enum-extension/53-RESEARCH.md §"Phase Requirements → Test Map" rows SEC-06 and LIKE-05 (the exact smoke queries)
  </read_first>
  <action>
    Prove the constraints ENFORCE (not just exist) against the live local DB using `docker exec ... psql`. The DO $$ assertions in Task 1 prove the constraints are PRESENT; these smokes prove they FIRE. Run inside a transaction that you ROLL BACK so no test data persists (BEGIN; ... ROLLBACK;), or use a throwaway local seed row.

    SEC-06 cascade smoke:
    1. Pick or insert a local `watches` row id and a local `users` row id (query `SELECT id FROM watches LIMIT 1` and `SELECT id FROM users LIMIT 1`; if the local DB is empty, insert a minimal user+watch first, or skip with a documented note).
    2. Insert a `watch_likes` row and a `comments` row referencing that watch.
    3. `DELETE FROM watches WHERE id = '<watch_id>'`.
    4. Assert `SELECT count(*) FROM watch_likes WHERE watch_id = '<watch_id>'` returns 0 AND `SELECT count(*) FROM comments WHERE watch_id = '<watch_id>'` returns 0 (the cascade fired). ROLLBACK afterward.

    LIKE-05 duplicate smoke:
    1. Insert a `watch_likes` row for a `(user_id, watch_id)` pair.
    2. Insert the SAME `(user_id, watch_id)` pair again.
    3. Assert the second insert raises a unique-violation error (SQLSTATE 23505 / message referencing `watch_likes_unique_pair`). ROLLBACK afterward.

    Record the observed outputs (cascade count = 0; second insert raised 23505) in the SUMMARY. If the local DB has no seed rows and inserting a minimal user is blocked by the auth trigger chain, document that the smoke was performed against a manually-inserted throwaway watch/user pair and rolled back.
  </action>
  <verify>
    <automated>docker exec -i supabase_db_horlo psql -U postgres -d postgres -t -c "SELECT conname FROM pg_constraint WHERE conname IN ('watch_likes_unique_pair','wear_likes_unique_pair') ORDER BY conname;" | tr -s '[:space:]' ' '</automated>
  </verify>
  <acceptance_criteria>
    - The cascade smoke confirms: after `DELETE FROM watches WHERE id = ?`, both `SELECT count(*) FROM watch_likes WHERE watch_id = ?` and `SELECT count(*) FROM comments WHERE watch_id = ?` return 0 (SEC-06 fires).
    - The duplicate-like smoke confirms: the second insert of the same `(user_id, watch_id)` pair raises an error referencing `watch_likes_unique_pair` (SQLSTATE 23505) (LIKE-05 fires).
    - `pg_constraint` query confirms both `watch_likes_unique_pair` and `wear_likes_unique_pair` exist.
    - All smoke writes were rolled back (no test rows persist in the local DB).
  </acceptance_criteria>
  <done>SEC-06 cascade and LIKE-05 duplicate-rejection are confirmed to ENFORCE on the live local DB, not merely exist. Test data rolled back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → live local DB | The authored SQL crosses into a real Postgres; assertions either pass (enforced) or RAISE (rolled back). |
| test smoke writes → local DB state | Smoke inserts must not pollute the local DB beyond the test transaction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-08 | Integrity (false-positive verification) | schema.ts build/tsc passing while DB unchanged | mitigate | This BLOCKING plan applies the migrations to the LIVE local DB so the DO $$ assertions actually fire — `tsc --noEmit` alone is a known false-OK (project memory: Rule 4 "local testing missed it"). The assertions ARE the test. |
| T-53-09 | Information Disclosure | anon retains SELECT on new tables after apply | mitigate | Task 1 live query `has_table_privilege('anon', ...)` returns false for all three tables (SEC-01 verified post-apply, not just authored). |
| T-53-03 (live) | Integrity | cascade FK present but not firing | mitigate | Task 2 cascade smoke performs a real DELETE and confirms child rows vanish (SEC-06 fires live). |
| T-53-05 (live) | Integrity | UNIQUE present but not rejecting | mitigate | Task 2 duplicate-insert smoke confirms a 23505 violation (LIKE-05 fires live). |
| T-53-10 | Tampering | enum ADD VALUE fails inside an implicit transaction on this Supabase runner (A2 risk) | mitigate | Enum migration applied via `docker exec psql` on local FIRST (Task 1); if `ADD VALUE cannot run inside a transaction block` surfaces, it is caught locally before the prod push (Plan 03), not in prod. |
</threat_model>

<verification>
- Both migration files apply to the live local DB with zero raised `DO $$` exceptions.
- Enum count = 6, policy counts (3/3/4), and anon-block (false ×3) confirmed by live queries.
- SEC-06 cascade and LIKE-05 duplicate-rejection confirmed by live smokes, then rolled back.
- This is the phase's load-bearing verification — Plan 01's artifacts are only proven correct once they apply green here.
</verification>

<success_criteria>
- The Phase 53 schema layer is materialized and verified on the live local DB.
- All in-migration assertions passed on apply (SEC-01, SEC-04 via zero-SECDEF, SEC-06, LIKE-05, GATE-02 policy-count/no-gate-subquery).
- Cascade and duplicate-like behaviors confirmed live.
- Local is proven safe to push to prod (Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/53-schema-rls-enum-extension/53-02-SUMMARY.md`. Record the captured query outputs (enum count, policy counts, anon-block results) and the smoke observations (cascade count=0, 23505 on duplicate).
</output>
