---
phase: 70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/watch/StructuredEntryPanel.tsx
  - src/components/watch/StructuredEntryPanel.test.tsx
  - src/components/watch/SearchEntry.tsx
  - src/components/watch/SearchEntry.test.tsx
  - src/data/watches.ts
  - src/components/watch/WatchForm.tsx
  - src/components/watch/WatchForm.lockedStatus.test.tsx
autonomous: true
requirements:
  - DUPE-01 (UI part)
  - DUPE-02
  - DUPE-03 (UI part)
  - CLNP-05
  - CLNP-06
tags:
  - addwatchflow
  - phase-70
  - wave-0
  - dal-extension
  - structured-input
must_haves:
  truths:
    - "RESEARCH §1 — StructuredEntryPanel.onSubmitStructured emit gains catalogId (single-line contract change at lines 60, 124, 156)"
    - "RESEARCH §2 — findViewerWatchByCatalogId return type gains reference (string | null) via leftJoin on watches_catalog"
    - "D-17 (planner discretion path A) — WatchForm.onWatchCreated extended to (watchId, dest, status) so AddWatchFlow can gate photos-pending on status==='owned' from the manual-entry branch"
    - "All Wave 0 patches are backward-compatible — every existing Phase 66/67/68/69 test that exercises these files MUST stay green after the patches"
    - "SearchEntry.tsx pass-through type for onSubmitStructured updated to match the widened StructuredEntryPanel contract (Pitfall #1 mitigation — TypeScript-graph propagation)"
    - "npm run build exits 0 after Wave 0 patches land (the gate per project_baseline_not_green_build_is_gate)"
  artifacts:
    - path: "src/components/watch/StructuredEntryPanel.tsx"
      provides: "onSubmitStructured callback widened to (result, catalogId)"
      contains: "onSubmitStructured: (result: ExtractedWatchData, catalogId: string | null) => void"
    - path: "src/data/watches.ts"
      provides: "findViewerWatchByCatalogId returns reference via watches_catalog JOIN"
      contains: "reference: string | null"
    - path: "src/components/watch/WatchForm.tsx"
      provides: "onWatchCreated callback widened to (watchId, dest, status)"
      contains: "onWatchCreated?: (watchId: string, destination: string, status: WatchStatus) => void"
    - path: "src/components/watch/SearchEntry.tsx"
      provides: "Pass-through onSubmitStructured prop type matches StructuredEntryPanel contract"
  key_links:
    - from: "src/components/watch/StructuredEntryPanel.tsx"
      to: "src/components/watch/SearchEntry.tsx"
      via: "onSubmitStructured prop pass-through"
      pattern: "onSubmitStructured.*catalogId"
    - from: "src/data/watches.ts findViewerWatchByCatalogId"
      to: "src/db/schema.ts watchesCatalog table"
      via: "leftJoin on watches.catalogId = watchesCatalog.id"
      pattern: "leftJoin\\(watchesCatalog"
    - from: "src/components/watch/WatchForm.tsx onWatchCreated"
      to: "consumers (AddWatchFlow Plan 05)"
      via: "third arg status: WatchStatus"
      pattern: "onWatchCreated\\?\\?\\(.*status"
---

<objective>
**Wave 0 prerequisite patches** for the Phase 70 main rewrite (Plans 02–05). RESEARCH §1 and §2 surfaced two TypeScript-graph coordination gaps that MUST land before the AddWatchFlow rewrite consumes the surfaces; PATTERNS.md #4 reaffirms the wave ordering. A third optional patch (WatchForm.onWatchCreated status arg, Open Question #1) is folded in here so the D-17 photos-pending gate has a clean contract to enforce in Plan 05.

Purpose: Eliminate three compiler-time blockers from the orchestrator rewrite by extending three contracts in dormant/active code paths — every patch is additive and backward-compatible. Without these patches, Plan 05 cannot pass the catalogId from StructuredEntryPanel through to findViewerWatchByCatalogId / addWatch, cannot read the existing watch's reference for DupeBanner's "View existing" link, and cannot enforce the D-17 photos-pending gate from the manual-entry branch.

Output: 4 source files patched + 3 test files updated (StructuredEntryPanel/SearchEntry/WatchForm test files reflect the widened signatures). No new files. Existing Phase 67/69 tests stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-CONTEXT.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-RESEARCH.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-PATTERNS.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-VALIDATION.md

# Files being patched (read once, extract patterns):
@src/components/watch/StructuredEntryPanel.tsx
@src/components/watch/SearchEntry.tsx
@src/data/watches.ts
@src/components/watch/WatchForm.tsx
@src/db/schema.ts

# Existing tests that MUST stay green (read line ranges only when needed):
@src/components/watch/StructuredEntryPanel.test.tsx
@src/components/watch/SearchEntry.test.tsx
@src/components/watch/WatchForm.lockedStatus.test.tsx
@src/components/watch/AddWatchFlow.test.tsx

<interfaces>
<!-- Extracted from codebase — executor uses these contracts directly without re-reading source. -->

From src/components/watch/StructuredEntryPanel.tsx (CURRENT lines 60, 156):
```typescript
// Line 60 — CURRENT signature
onSubmitStructured: (result: ExtractedWatchData) => void

// Line 152 — internal cache already stores catalogId (just not emitted)
cache.set(key, {
  catalogId: envelope.catalogId ?? '',
  extracted: envelope.data,
  catalogIdError: null,
})

// Line 156 — CURRENT emit
onSubmitStructured(envelope.data)
```

From src/data/watches.ts (CURRENT lines 295–322):
```typescript
export async function findViewerWatchByCatalogId(
  userId: string,
  catalogId: string,
  statuses: WatchStatus[],
): Promise<{ id: string; status: 'owned' | 'wishlist' } | null> {
  // ... projects only id + status, no JOIN to watches_catalog
}
```

From src/components/watch/WatchForm.tsx (CURRENT line ~70):
```typescript
onWatchCreated?: (watchId: string, destination: string) => void
```

From src/db/schema.ts (verified — watchesCatalog has `reference` column):
```typescript
export const watchesCatalog = pgTable('watches_catalog', { ..., reference: text('reference'), ... })
// No UNIQUE(userId, catalogId) on watches table (RESEARCH §D-08 verified — indexes only)
```

WatchStatus type from src/lib/types.ts:
```typescript
export type WatchStatus = 'owned' | 'wishlist' | 'sold' | 'grail'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch StructuredEntryPanel.onSubmitStructured emit + SearchEntry pass-through type</name>
  <files>src/components/watch/StructuredEntryPanel.tsx, src/components/watch/SearchEntry.tsx, src/components/watch/StructuredEntryPanel.test.tsx, src/components/watch/SearchEntry.test.tsx</files>
  <action>
    Implements RESEARCH §1 coordination gap (Pitfall #1 in PATTERNS.md). Three coordinated edits.

    (a) In `src/components/watch/StructuredEntryPanel.tsx`:
    - Update the `onSubmitStructured` prop type declaration at line ~60 from `(result: ExtractedWatchData) => void` to `(result: ExtractedWatchData, catalogId: string | null) => void`.
    - At the emit site (~line 156), pass the catalogId from the network envelope: `onSubmitStructured(envelope.data, envelope.catalogId ?? null)`. The cache write on the surrounding lines already captures `envelope.catalogId ?? ''` so the network field is known to exist; the emit just routes it.
    - If there is a SECOND emit site (cache-hit short-circuit branch) — verify by grep and route the cached `catalogId` value through identically: `onSubmitStructured(cached.extracted, cached.catalogId || null)` (the cache stores `catalogId: '' | string` per the existing write; coerce empty string to null at the boundary).

    (b) In `src/components/watch/SearchEntry.tsx`:
    - Locate the `onSubmitStructured` prop on SearchEntryProps (around line 74 per PATTERNS.md) and widen its type to `(result: ExtractedWatchData, catalogId: string | null) => void`.
    - Verify the pass-through into `<StructuredEntryPanel onSubmitStructured={...}>` in the JSX does not need a wrapper — TypeScript will enforce contravariant compatibility automatically since SearchEntry's caller (Plan 05 AddWatchFlow) supplies the widened handler.

    (c) Update tests to match the new signature:
    - In `src/components/watch/StructuredEntryPanel.test.tsx`, any `vi.fn()` typed for `onSubmitStructured` or any assertion `expect(onSubmit).toHaveBeenCalledWith(...)` must include the second `catalogId` argument. Use `expect(onSubmit).toHaveBeenCalledWith(expect.objectContaining({brand: '...'}), expect.any(String))` or pass `null` explicitly depending on the test envelope's catalogId.
    - In `src/components/watch/SearchEntry.test.tsx`, ditto for any test that mocks StructuredEntryPanel or asserts on the pass-through `onSubmitStructured` call.

    Guardrail: do NOT add `useTransition` or any new React hook here. Do NOT touch the cache hooks (`useStructuredExtractCache`) — they already store catalogId; this patch only routes it through the emit boundary.

    Reference: PATTERNS.md "StructuredEntryPanel.tsx — Wave 0 patch" section + RESEARCH §"StructuredEntryPanel onSubmitStructured" verified at StructuredEntryPanel.tsx:60, :151-156.
  </action>
  <verify>
    <automated>npx vitest run src/components/watch/StructuredEntryPanel.test.tsx src/components/watch/SearchEntry.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    StructuredEntryPanel.test.tsx + SearchEntry.test.tsx both green. The new `onSubmitStructured` signature emits `(extracted, catalogId)` in both network-success and cache-hit branches. Grep confirms one source-of-truth shape: `grep -nE "onSubmitStructured.*=>.*void" src/components/watch/StructuredEntryPanel.tsx src/components/watch/SearchEntry.tsx` shows the widened signature in BOTH files; the old single-arg form is gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch findViewerWatchByCatalogId — JOIN watches_catalog + project reference</name>
  <files>src/data/watches.ts</files>
  <action>
    Implements RESEARCH §2 coordination gap (Pitfall #2 in PATTERNS.md). DupeBanner's "View existing" link target requires the catalog row's `reference` field; the current DAL helper only projects `{id, status}` from `watches`.

    Edits to `src/data/watches.ts` `findViewerWatchByCatalogId` function (currently at ~lines 295–322):

    (a) Update the return type signature:
    - From: `Promise<{ id: string; status: 'owned' | 'wishlist' } | null>`
    - To: `Promise<{ id: string; status: 'owned' | 'wishlist'; reference: string | null } | null>`

    (b) Add the `watchesCatalog` import to the existing schema imports at the top of the file if not already imported. Use the same import style as the existing schema imports (e.g., `import { watches, watchesCatalog } from '@/db/schema'`).

    (c) Extend the Drizzle query:
    - Add `reference: watchesCatalog.reference` to the `.select({...})` projection.
    - Add `.leftJoin(watchesCatalog, eq(watches.catalogId, watchesCatalog.id))` after the `.from(watches)` clause (before `.where(...)`).
    - The existing WHERE clause (userId match + catalogId match + status in statuses) is unchanged.
    - The existing owned-wins ordering / `LIMIT 1` is unchanged.

    (d) Update the return mapping at the bottom of the function:
    - From: `return { id: row.id, status: row.status as 'owned' | 'wishlist' }`
    - To: `return { id: row.id, status: row.status as 'owned' | 'wishlist', reference: row.reference ?? null }`

    Backward-compat guardrail: any existing caller (Phase 67 tests) that destructures only `{id, status}` from the result continues to work — the added `reference` field is additive. No existing test should break.

    Threat surface (T-70-04): the JOIN reads the catalog row's authoritative reference server-side; DupeBanner's link is built from this verified value, not from any client-supplied or stale prop.

    Reference: PATTERNS.md "src/data/watches.ts — Wave 0 patch" section + RESEARCH §"findViewerWatchByCatalogId return shape" verified at src/data/watches.ts:295-322.
  </action>
  <verify>
    <automated>npx vitest run src/data/__tests__/ src/app/actions/__tests__/watches.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    No regression in existing DAL/Server-Action tests. `grep -n "reference: watchesCatalog.reference\|leftJoin(watchesCatalog" src/data/watches.ts` shows the new JOIN + projection. The return type signature now includes `reference: string | null` as a third field.
  </done>
</task>

<task type="auto">
  <name>Task 3: Patch WatchForm.onWatchCreated — add status arg for D-17 gate</name>
  <files>src/components/watch/WatchForm.tsx, src/components/watch/WatchForm.lockedStatus.test.tsx</files>
  <action>
    Implements RESEARCH Open Question #1 (per D-17 photos-pending gate requirement). The current `onWatchCreated?: (watchId, destination) => void` callback at WatchForm.tsx:~70 does not surface the committed watch's status. Plan 05 needs to gate photos-pending on `status === 'owned'` from the manual-entry branch (form-prefill is always owned via `lockedStatus='owned'` — trivially gated).

    Edits to `src/components/watch/WatchForm.tsx`:

    (a) Widen the `onWatchCreated` prop type:
    - From: `onWatchCreated?: (watchId: string, destination: string) => void`
    - To: `onWatchCreated?: (watchId: string, destination: string, status: WatchStatus) => void`
    - Add `import type { WatchStatus } from '@/lib/types'` if not already imported.

    (b) Update every call site of `onWatchCreated(...)` inside WatchForm to pass the committed status as the third argument:
    - On create-success, the status is available from the form's internal state (`formData.status` or equivalent — verify by reading the section that currently calls `onWatchCreated(watchId, dest)`).
    - Pass `formData.status` (or whatever the local committed-status variable is named) as the third arg.
    - There may be 1–2 call sites; route all of them.

    (c) Update `src/components/watch/WatchForm.lockedStatus.test.tsx`:
    - Any test that mocks `onWatchCreated` or asserts on its call args MUST update the expectation to include the third `status` argument.
    - For `lockedStatus="owned"` test cases, the expected status will be `'owned'`.
    - Use `expect(onWatchCreated).toHaveBeenCalledWith(watchId, expect.any(String), 'owned')` or similar.

    Backward-compat guardrail: all OTHER callers of WatchForm (search-grep for `<WatchForm` in the codebase) treat `onWatchCreated` as optional — passing a 2-arg handler to a 3-arg signature is fine via TypeScript's variadic-bivariance for unused parameters. Verify NO existing caller relies on the 2-arg shape strictly.

    Reference: PATTERNS.md WatchForm.tsx patch note + RESEARCH §"D-17: WatchForm's onWatchCreated doesn't pass status" + CONTEXT.md D-17.
  </action>
  <verify>
    <automated>npx vitest run src/components/watch/WatchForm.lockedStatus.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    WatchForm.lockedStatus.test.tsx green. The `onWatchCreated` prop type at WatchForm.tsx is now 3-arity. All in-file call sites pass the third `status` arg. `grep -n "onWatchCreated" src/components/watch/WatchForm.tsx` shows the widened type declaration AND each call site routing status through.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wave 0 build-gate — verify backward compatibility</name>
  <files>(no edits — verification only)</files>
  <action>
    The combined Wave 0 patches must NOT break any pre-existing test or compilation. This task is the explicit verification step required by the additional context: "Wave 0 verification: the Wave 0 patches MUST be backward-compatible (existing Phase 67/69 tests must still pass)."

    (a) Run the targeted test suites that exercise the patched files and their immediate consumers:
    - `npx vitest run src/components/watch/StructuredEntryPanel.test.tsx src/components/watch/SearchEntry.test.tsx src/components/watch/WatchForm.lockedStatus.test.tsx src/components/watch/AddWatchFlow.test.tsx src/app/actions/__tests__/watches.test.ts`
    - Also run any DAL test files under `src/data/__tests__/` that cover `watches.ts`.

    (b) Run `npm run build` to confirm the TypeScript graph compiles end-to-end. This is THE gate per `project_baseline_not_green_build_is_gate` memory. The Wave 0 patches widen contracts in three places — TypeScript will catch any consumer that needs to update its handler signature (and there should be NONE because the AddWatchFlow rewrite is Plans 02–05; current AddWatchFlow consumes the old single-arg `onSubmitStructured` via the dormant SearchEntry pass-through which is also widened).

    (c) Document any pre-existing baseline failures encountered. Per `project_baseline_not_green_build_is_gate`, full `npm run test` has ≥1 pre-existing failure unrelated to this phase — DO NOT attribute pre-existing red to Wave 0; only fail this task if `npm run build` exit code is non-zero OR if a previously-green test in the targeted set turns red.

    (d) If a test in the targeted set goes red as a result of these patches, fix the test (not the source) — Wave 0 contracts are LOCKED to enable Plans 02–05.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30 && echo "---TARGETED TESTS---" && npx vitest run src/components/watch/StructuredEntryPanel.test.tsx src/components/watch/SearchEntry.test.tsx src/components/watch/WatchForm.lockedStatus.test.tsx src/components/watch/AddWatchFlow.test.tsx src/app/actions/__tests__/watches.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    `npm run build` exits 0. Every previously-green test in the targeted set stays green. Any test that needed an update to match the widened signatures (Tasks 1–3 already updated their own test files) is also green. The TypeScript graph is consistent across the StructuredEntryPanel → SearchEntry → AddWatchFlow consumer chain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action (Plan 03's moveWishlistToCollection) | Untrusted client supplies watchId; Wave 0 DAL extension keeps the reference field server-authoritative (read from watches_catalog, not client) |
| Client → DAL (findViewerWatchByCatalogId) | Called from client orchestrator with user-supplied catalogId; the leftJoin reads catalog row server-side; no client-controlled reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04 | Spoofing | findViewerWatchByCatalogId DAL (Wave 0 patch) | mitigate | The `reference` field is JOINed from `watches_catalog` server-side (not client-supplied) — DupeBanner's "View existing" link consumes only this server-verified value. The pattern matches Phase 67 SECURITY-01 (catalog-row-authoritative reads). |
| T-70-WAVE0-01 | Tampering | WatchForm.onWatchCreated signature widening | accept | The new `status` arg is sourced from WatchForm's internal commit state, not from the network response or external input. Status validation already happens server-side in `addWatch` (Phase 67 Zod schema). |
</threat_model>

<verification>
**Plan-level phase verification hook (for /gsd-verify-work SC#1-5 tracing):**
- **SC#5 (Phase 70 — three-layer reset extension to new caches):** Wave 0 does NOT touch the caches directly, but it lays the contract surface for Plan 05's useLayoutEffect cleanup. Confirm via Task 4's `npm run build` exit 0 that the existing Phase 69 four-cache integration test in AddWatchFlow.test.tsx still compiles + passes.
- **SC#1 (DUPE-01/DUPE-03 wishlist→collection UPDATE path):** Wave 0 unblocks the DAL surface (Task 2 — reference field) and the structured-input emit surface (Task 1 — catalogId routing) that Plan 02 (DupeBanner) and Plan 05 (AddWatchFlow orchestrator) consume to render the banner + commit the UPDATE.
- **SC#2 (DUPE-02 "Add another copy"):** Wave 0 unblocks Plan 05's handleStructuredSubmit + handleUrlBackup ability to call findViewerWatchByCatalogId with catalogId-in-hand; the resulting `dupeContext.existingReference` drives the DupeBanner's "View existing" target.
</verification>

<success_criteria>
- All three Wave 0 patches land in one logical unit; `npm run build` exits 0 after Task 4.
- `grep -nE "onSubmitStructured.*ExtractedWatchData.*catalogId.*string \| null" src/components/watch/StructuredEntryPanel.tsx src/components/watch/SearchEntry.tsx` matches both files.
- `grep -n "reference: watchesCatalog.reference" src/data/watches.ts` matches once (the new SELECT projection).
- `grep -n "leftJoin(watchesCatalog" src/data/watches.ts` matches once (the new JOIN).
- `grep -nE "onWatchCreated\?:.*WatchStatus" src/components/watch/WatchForm.tsx` matches once (the widened type).
- Targeted test suites all green: StructuredEntryPanel.test.tsx, SearchEntry.test.tsx, WatchForm.lockedStatus.test.tsx, AddWatchFlow.test.tsx (Phase 69 four-cache integration unchanged), src/app/actions/__tests__/watches.test.ts.
- No untouched test in the repo turns red as a result of these patches (`npm run build` exit 0 is the authoritative signal).
</success_criteria>

<output>
After completion, create `.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-01-SUMMARY.md` summarizing:
- The exact line-change deltas for each of the three patched source files.
- The test-file updates (any expectation-arg additions) made to keep existing tests green.
- The grep verifications run to confirm the widened signatures are consistent across the StructuredEntryPanel → SearchEntry consumer chain.
- The `npm run build` exit status and any pre-existing baseline noise encountered (attribution per `project_baseline_not_green_build_is_gate`).
- Confirmation that the four-cache integration test in AddWatchFlow.test.tsx (Phase 69 CLNP-07 retrofit) is unchanged.
</output>
