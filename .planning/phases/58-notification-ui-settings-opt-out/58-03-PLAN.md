---
phase: 58-notification-ui-settings-opt-out
plan: 03
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - src/components/settings/NotificationsSection.tsx
  - tests/components/settings/NotificationsSection.test.tsx
autonomous: true
requirements: [NOTIF-15]
must_haves:
  truths:
    - "Settings → Notifications section renders four PrivacyToggleRow instances: New Followers, Watch Overlaps, Likes, Comments (D-09/D-10)"
    - "The section title reads 'Notifications' (renamed from 'Email notifications') (D-09)"
    - "The Likes toggle uses field='notifyOnLike'; the Comments toggle uses field='notifyOnComment' (D-10)"
    - "Toggling Likes calls updateProfileSettings with { field: 'notifyOnLike', value: false }; toggling Comments calls it with { field: 'notifyOnComment', value: false }"
    - "The NotificationsSection props Pick is widened to include notifyOnLike and notifyOnComment (D-11)"
  artifacts:
    - path: "src/components/settings/NotificationsSection.tsx"
      provides: "Four-toggle Notifications section with widened Pick props and renamed title"
      contains: "notifyOnComment"
    - path: "tests/components/settings/NotificationsSection.test.tsx"
      provides: "Updated assertions: 4 switches, 'Notifications' title, Likes + Comments SA-call-shape tests"
      contains: "notifyOnLike"
  key_links:
    - from: "src/components/settings/NotificationsSection.tsx Likes/Comments PrivacyToggleRow"
      to: "updateProfileSettings Server Action via PrivacyToggleRow"
      via: "field prop ('notifyOnLike'/'notifyOnComment') flows into the SA call"
      pattern: "field=\"notifyOn(Like|Comment)\""
    - from: "src/components/settings/NotificationsSection.tsx Pick<ProfileSettings, ...>"
      to: "src/data/profiles.ts ProfileSettings (widened in Plan 01)"
      via: "shared type"
      pattern: "notifyOnLike"
---

<objective>
Add the `notifyOnLike` and `notifyOnComment` opt-out toggles to the Settings notifications section, rename the section title to "Notifications", and widen the component's `ProfileSettings` Pick. Closes the NOTIF-15 UI side (the persistence chain landed in Plan 01; suppression already exists in `logger.ts`).

Purpose: Per D-09/D-11, the section currently shows two toggles titled "Email notifications" — a misnomer, since all four toggles gate in-app notification rows via the logger (no email is sent). This plan adds the two new `PrivacyToggleRow`s, renames the heading, and widens the props Pick so the page-supplied `ProfileSettings` (now carrying the new fields from Plan 01) flows through to the toggles.

Output: Four-toggle `NotificationsSection.tsx` with the renamed title and widened Pick, plus updated assertions in `NotificationsSection.test.tsx`. No change to `PrivacyToggleRow.tsx`, the settings page, or the shell (RESEARCH verified the page's bare `settings={props.settings}` pass-through needs no change once `ProfileSettings` is widened).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-notification-ui-settings-opt-out/58-CONTEXT.md
@.planning/phases/58-notification-ui-settings-opt-out/58-PATTERNS.md
@.planning/phases/58-notification-ui-settings-opt-out/58-UI-SPEC.md
@.planning/phases/58-notification-ui-settings-opt-out/58-01-SUMMARY.md

<interfaces>
<!-- Depends on Plan 01: ProfileSettings + VisibilityField widened, and the test fixture already carries notifyOnLike/notifyOnComment. -->

From src/data/profiles.ts (widened by Plan 01):
ProfileSettings now includes `notifyOnLike: boolean` and `notifyOnComment: boolean`.
VisibilityField now includes `'notifyOnLike' | 'notifyOnComment'` — so `field="notifyOnLike"` / `field="notifyOnComment"` type-check against PrivacyToggleRow's `field: VisibilityField` prop.

Current NotificationsSection.tsx (to modify):
- Props (lines 5-7): `settings: Pick<ProfileSettings, 'notifyOnFollow' | 'notifyOnWatchOverlap'>`.
- Title (line 18): `<SettingsSection title="Email notifications">`.
- Two PrivacyToggleRow instances (lines 20-31): New Followers (notifyOnFollow), Watch Overlaps (notifyOnWatchOverlap) — the structural template.

PrivacyToggleRow contract (src/components/settings/PrivacyToggleRow.tsx — UNCHANGED):
- Props: `label`, `description`, `field: VisibilityField`, `initialValue: boolean`.
- On toggle: `updateProfileSettings({ field, value: !current })`, optimistic via useOptimistic, snap-back on failure.

D-10 copy (verbatim):
- Likes → label "Likes", description "Get notified when someone likes your watches or wear posts.", field "notifyOnLike".
- Comments → label "Comments", description "Get notified when someone comments on your watches or wear posts.", field "notifyOnComment".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Likes + Comments toggles, rename title, widen Pick (D-09/D-10/D-11)</name>
  <files>src/components/settings/NotificationsSection.tsx</files>
  <read_first>
    - src/components/settings/NotificationsSection.tsx — the file being modified. Read the whole file (35 lines): the Props Pick (5-7), the title (18), and the two existing PrivacyToggleRow rows (20-31) as the exact structural template for the two new rows.
    - .planning/phases/58-notification-ui-settings-opt-out/58-PATTERNS.md § "3. NotificationsSection.tsx — 2 new toggles + rename title" — the verbatim widened Pick, the title rename, and the two new PrivacyToggleRow blocks with D-10 copy.
    - .planning/phases/58-notification-ui-settings-opt-out/58-UI-SPEC.md § "NotificationsSection" (the 4-row order table + Copywriting Contract) — confirms order (Followers, Overlaps, Likes, Comments) and the exact labels/descriptions.
    - src/components/settings/PrivacyToggleRow.tsx — read-only; confirms the `field: VisibilityField`, `initialValue` prop contract the new rows plug into. DO NOT modify.
    - .planning/phases/58-notification-ui-settings-opt-out/58-01-SUMMARY.md — confirms ProfileSettings + VisibilityField were widened (this plan depends on it).
  </read_first>
  <action>
    In `src/components/settings/NotificationsSection.tsx`:
    1. Widen the props Pick (D-11): `Pick<ProfileSettings, 'notifyOnFollow' | 'notifyOnWatchOverlap' | 'notifyOnLike' | 'notifyOnComment'>`.
    2. Rename the section title (D-09): `<SettingsSection title="Notifications">` (was "Email notifications").
    3. Add two `PrivacyToggleRow` instances inside the existing `<div className="divide-y divide-border">`, AFTER the Watch Overlaps row, in order (D-10 copy verbatim):
       - Likes: `label="Likes"`, `description="Get notified when someone likes your watches or wear posts."`, `field="notifyOnLike"`, `initialValue={settings.notifyOnLike}`.
       - Comments: `label="Comments"`, `description="Get notified when someone comments on your watches or wear posts."`, `field="notifyOnComment"`, `initialValue={settings.notifyOnComment}`.
    Do NOT modify `PrivacyToggleRow.tsx`, the settings page, or the shell — the existing `settings={props.settings}` pass-through already carries the new fields once `ProfileSettings` is widened (RESEARCH verified). Reference: implements D-09/D-10/D-11 for NOTIF-15.
  </action>
  <acceptance_criteria>
    - The Props Pick includes `'notifyOnLike'` and `'notifyOnComment'`.
    - The title string is exactly `"Notifications"` (no longer `"Email notifications"`).
    - There are four `PrivacyToggleRow` instances; the Likes row uses `field="notifyOnLike"` with the D-10 Likes description, and the Comments row uses `field="notifyOnComment"` with the D-10 Comments description.
    - `git diff --quiet src/components/settings/PrivacyToggleRow.tsx src/app/settings/page.tsx src/components/settings/SettingsTabsShell.tsx` (those files unchanged).
    - `npx tsc --noEmit` reports no type errors in `NotificationsSection.tsx`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && grep -q 'title="Notifications"' src/components/settings/NotificationsSection.tsx && ! grep -q "Email notifications" src/components/settings/NotificationsSection.tsx && grep -q 'field="notifyOnLike"' src/components/settings/NotificationsSection.tsx && grep -q 'field="notifyOnComment"' src/components/settings/NotificationsSection.tsx && [ "$(grep -c "PrivacyToggleRow" src/components/settings/NotificationsSection.tsx)" -ge 5 ] && git diff --quiet src/components/settings/PrivacyToggleRow.tsx src/app/settings/page.tsx src/components/settings/SettingsTabsShell.tsx && echo PASS</automated>
  </verify>
  <done>NotificationsSection renders four toggles in order with the renamed "Notifications" title and the widened Pick; PrivacyToggleRow, the settings page, and the shell are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update NotificationsSection tests — 4 switches, title, Likes/Comments SA-call shape</name>
  <files>tests/components/settings/NotificationsSection.test.tsx</files>
  <read_first>
    - tests/components/settings/NotificationsSection.test.tsx — the file being modified. Read the whole file: the `updateMock` hoist (14-19), the fixture (24-27, already widened by Plan 01), the `toHaveLength(2)` assertion (39), the `notifyOnFollow` regression test (42-50), and the `h2` card-frame test (52-55). Mirror the notifyOnFollow regression test for the two new toggles.
    - .planning/phases/58-notification-ui-settings-opt-out/58-PATTERNS.md § "NotificationsSection.test.tsx — update existing fixture + add assertions" — the switch-count change (2 → 4), the title assertion, and the Likes/Comments SA-call-shape test templates.
    - .planning/phases/58-notification-ui-settings-opt-out/58-VALIDATION.md § Per-Task Verification Map — the NOTIF-15 component-test rows (4 toggles, title "Notifications", Likes SA shape, Comments SA shape).
  </read_first>
  <action>
    In `tests/components/settings/NotificationsSection.test.tsx`:
    1. Update the switch-count assertion from `toHaveLength(2)` to `toHaveLength(4)` and update that test's name to reflect four toggles; add `expect(screen.getByText('Likes')).toBeInTheDocument()` and `expect(screen.getByText('Comments')).toBeInTheDocument()`.
    2. Add a title assertion that the rendered heading text is `"Notifications"` (e.g. `expect(screen.getByText('Notifications')).toBeInTheDocument()`), matching the renamed `SettingsSection title`.
    3. Add a Likes SA-shape test mirroring the existing `notifyOnFollow` regression: click the switch named "Likes" and assert `updateMock` was called with `{ field: 'notifyOnLike', value: false }`.
    4. Add a Comments SA-shape test: click the switch named "Comments" and assert `updateMock` was called with `{ field: 'notifyOnComment', value: false }`.
    The fixture already carries `notifyOnLike: true` / `notifyOnComment: true` (added in Plan 01) — do not re-add. Keep the existing `notifyOnFollow` regression and `h2` card-frame tests passing.
  </action>
  <acceptance_criteria>
    - The switch-count assertion is `toHaveLength(4)`.
    - A test asserts the heading text "Notifications" is in the document.
    - A test clicks the "Likes" switch and asserts `updateMock` called with `{ field: 'notifyOnLike', value: false }`.
    - A test clicks the "Comments" switch and asserts `updateMock` called with `{ field: 'notifyOnComment', value: false }`.
    - `npm run test -- tests/components/settings/NotificationsSection.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && grep -q "toHaveLength(4)" tests/components/settings/NotificationsSection.test.tsx && grep -q "field: 'notifyOnLike'" tests/components/settings/NotificationsSection.test.tsx && grep -q "field: 'notifyOnComment'" tests/components/settings/NotificationsSection.test.tsx && npm run test -- tests/components/settings/NotificationsSection.test.tsx</automated>
  </verify>
  <done>Tests assert four switches, the "Notifications" title, and the correct SA call shape for both new toggles; the full NotificationsSection suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client toggle → updateProfileSettings Server Action | The new Likes/Comments toggles submit `{ field, value }` to the same SA as the existing toggles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-06 | Tampering / mass-assignment | Likes/Comments PrivacyToggleRow `field` prop → updateProfileSettings | mitigate | The two new toggles pass only the literal `field` values `'notifyOnLike'` / `'notifyOnComment'`, which the SA's `z.enum(VISIBILITY_FIELDS)` allowlist (widened in Plan 01, T-58-01) accepts; any other field is still rejected. No new validation code added here — the control is the Plan 01 allowlist. |
| T-58-07 | Spoofing / IDOR | toggle persistence target row | accept | Unchanged — `updateProfileSettings` writes only the authenticated caller's own `profile_settings` row via `getCurrentUser()` → `updateProfileSettingsField(user.id, ...)`. Reusing the unmodified PrivacyToggleRow path introduces no new authz surface. |
</threat_model>

<verification>
- `npm run test -- tests/components/settings/NotificationsSection.test.tsx` green (4-switch + title + both SA-shape tests).
- At wave merge: `npm run test` full suite green AND `npm run build` green (DB-backed RSC routes need `.env.local`; do not run the build inside a worktree that omits it — see project memory).
- `git diff --quiet src/components/settings/PrivacyToggleRow.tsx src/app/settings/page.tsx src/components/settings/SettingsTabsShell.tsx`.
</verification>

<success_criteria>
- Four toggles render in order (Followers, Overlaps, Likes, Comments) under the renamed "Notifications" title.
- The Likes/Comments toggles persist via the existing `updateProfileSettings` SA with the correct field names.
- The props Pick is widened; the settings page/shell pass-through is untouched.
- NOTIF-15 UI side closed; suppression remains enforced by the pre-existing logger.
</success_criteria>

<output>
After completion, create `.planning/phases/58-notification-ui-settings-opt-out/58-03-SUMMARY.md`
</output>
