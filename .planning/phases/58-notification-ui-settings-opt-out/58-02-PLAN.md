---
phase: 58-notification-ui-settings-opt-out
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/notifications/NotificationRow.tsx
  - src/components/notifications/NotificationsInbox.tsx
  - tests/components/notifications/NotificationRow.test.tsx
  - tests/components/notifications/NotificationsInbox.test.tsx
autonomous: true
requirements: [NOTIF-16]
must_haves:
  truths:
    - "watch_like renders '{actor} liked your {model}' and wear_like renders '{actor} liked your {model} wear' (D-01)"
    - "watch_comment renders '{actor} commented on your {model}' and wear_comment renders '{actor} commented on your {model} wear' (D-01)"
    - "Comment rows render the payload.comment_preview on a muted clamped second line; like rows render NO second line (D-02)"
    - "Grouped like rows render '{actor} + {N-1} others liked your {model}' (D-03)"
    - "watch_like / watch_comment deep-link to /watch/{payload.watch_id}; wear_like / wear_comment deep-link to /wear/{payload.wear_event_id} (D-07)"
    - "watch_like and wear_like rows sharing target_id + UTC-day collapse into one row with actorCount = group size, most-recent actor wins (D-04)"
    - "watch_comment and wear_comment rows are never grouped (D-05)"
    - "Genuinely-unknown future notification types still return null (B-8 guard preserved, D-08)"
  artifacts:
    - path: "src/components/notifications/NotificationRow.tsx"
      provides: "B-8 guard widened to a 6-type allowlist + resolveHref and resolveCopy branches for the four new types + comment-preview second line"
      contains: "watch_like"
    - path: "src/components/notifications/NotificationsInbox.tsx"
      provides: "Collapse extended to group watch_like (by watch_id) and wear_like (by wear_event_id) per (type, target_id, UTC-day)"
      contains: "wear_like"
  key_links:
    - from: "src/components/notifications/NotificationsInbox.tsx collapse"
      to: "src/components/notifications/NotificationRow.tsx resolveCopy actorCount branch"
      via: "actorCount populated on the displayed grouped row, consumed by the '+ N others' copy"
      pattern: "actorCount"
    - from: "src/components/notifications/NotificationRow.tsx resolveHref"
      to: "/watch/[id] and /wear/[wearEventId] routes"
      via: "payload.watch_id / payload.wear_event_id read by exact field name"
      pattern: "/watch/|/wear/"
---

<objective>
Render the four new social notification types (`watch_like`, `wear_like`, `watch_comment`, `wear_comment`) in the existing bell/inbox with locked copy + deep-links, and group repeated likes on the same target into one row. Closes NOTIF-16.

Purpose: The DAL (`getNotificationsForViewer`) already returns all six types and the bell dot already lights for any unread row (Phase 53/55) — but `NotificationRow` early-returns `null` for the four new types (B-8 guard, line 52) and `NotificationsInbox` only collapses `watch_overlap`. This plan flips the guard, adds the render/href branches, and extends the collapse so likes group like overlaps already do.

Output: Extended `NotificationRow.tsx` (guard + `resolveHref` + `resolveCopy` + comment-preview line) and `NotificationsInbox.tsx` (like-aware collapse), plus extended tests in both existing test files. No DAL change, no logger change, no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-notification-ui-settings-opt-out/58-CONTEXT.md
@.planning/phases/58-notification-ui-settings-opt-out/58-RESEARCH.md
@.planning/phases/58-notification-ui-settings-opt-out/58-PATTERNS.md
@.planning/phases/58-notification-ui-settings-opt-out/58-UI-SPEC.md

<interfaces>
<!-- Load-bearing payload field names (from src/lib/notifications/types.ts — verified). resolveCopy/resolveHref must read these EXACT names; one character off and the dedup index / deep-link breaks. -->

WatchLikePayload: watch_id, watch_model
WearLikePayload: wear_event_id, watch_model
WatchCommentPayload: watch_id, watch_model, comment_id, comment_preview
WearCommentPayload: wear_event_id, watch_model, comment_id, comment_preview

NotificationRowData (src/components/notifications/NotificationRow.tsx:19-32) — type union ALREADY includes all 6 values (line 23); `actorCount?: number` (line 31) already exists. No type change needed.

Existing analog branches to mirror (NotificationRow.tsx):
- B-8 guard line 52: `if (row.type !== 'follow' && row.type !== 'watch_overlap') { return null }`
- actorClass lines 142-144: `isUnread ? 'font-semibold text-foreground' : 'font-normal text-foreground'`
- resolveHref watch_overlap branch lines 124-129 (cast-then-read pattern)
- resolveCopy watch_overlap branches lines 155-174 (single-actor 167-173, grouped "+ N others" 158-165 — D-03 phrase shape confirmed at line 162)
- render container lines 108-111 (copy + ` · {timeLabel}` inside `<div className="flex-1 min-w-0 text-sm text-foreground">`)

Existing analog (NotificationsInbox.tsx):
- collapseWatchOverlaps lines 76-110: group map + nonOverlap passthrough; the early guard line 81 `if (row.type !== 'watch_overlap')` is the extension point; toUtcDayKey (112-115) reused verbatim; the re-sort (103-108) reused verbatim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend NotificationRow — guard + 4 render branches + deep-links + comment preview (D-01/D-02/D-03/D-07/D-08)</name>
  <files>src/components/notifications/NotificationRow.tsx, tests/components/notifications/NotificationRow.test.tsx</files>
  <behavior>
    - watch_like single actor → copy text contains "liked your" and the model (e.g. "Submariner"); no second-line preview `<p>`.
    - wear_like single actor → copy text contains "liked your" and "{model} wear" (model followed by " wear"); no preview `<p>`.
    - watch_like with actorCount: 3 → copy text contains "+ 2 others liked your" and the model.
    - wear_like with actorCount: 3 → copy text contains "+ 2 others liked your" and "{model} wear".
    - watch_comment → copy text contains "commented on your" and the model; renders a muted clamped `<p>` containing payload.comment_preview text.
    - wear_comment → copy text contains "commented on your {model} wear"; renders the comment_preview `<p>`.
    - Like rows (watch_like, wear_like) render NO comment-preview `<p>`.
    - Clicking a watch_like / watch_comment row calls router.push with `/watch/{payload.watch_id}`.
    - Clicking a wear_like / wear_comment row calls router.push with `/wear/{payload.wear_event_id}`.
    - A row with an unknown future type (e.g. `'price_drop'`) still returns null (B-8 guard preserved).
  </behavior>
  <read_first>
    - src/components/notifications/NotificationRow.tsx — the file being modified. Read the whole file (181 lines): the B-8 guard (52), the render container (108-111), resolveHref (116-134), resolveCopy (136-180). Mirror the watch_overlap branch structure exactly for the four new types.
    - .planning/phases/58-notification-ui-settings-opt-out/58-PATTERNS.md § "1. NotificationRow.tsx — extend 4 render branches" — verbatim guard rewrite, resolveHref branch shapes, resolveCopy branch shapes, and the comment-preview `<p>` placement.
    - .planning/phases/58-notification-ui-settings-opt-out/58-UI-SPEC.md § "NotificationRow" (Render strings table D-01, Comment preview second line D-02, Deep-links table D-07) — the locked copy strings, the `text-xs text-muted-foreground line-clamp-2 mt-1` preview classes, and the href shapes.
    - tests/components/notifications/NotificationRow.test.tsx — the test file being extended. Read makeRow (line 50), getNotifCopyText (60), mockPush (23-26), and the watch_overlap single/grouped/B-8-guard describe blocks (110-176) as the structural template for the new describe blocks. Write the new tests FIRST (RED), then implement.
    - src/lib/notifications/types.ts lines 27-60 — read-only confirmation of the four payload field names. DO NOT modify.
  </read_first>
  <action>
    Write the new tests first (RED), then implement. In `src/components/notifications/NotificationRow.tsx`:

    1. Replace the B-8 guard (line 52) with a six-type allowlist (per PATTERNS.md): a `KNOWN_TYPES` array of `'follow', 'watch_overlap', 'watch_like', 'wear_like', 'watch_comment', 'wear_comment'`, and `if (!KNOWN_TYPES.includes(row.type)) { return null }`. Keep it AFTER the hooks (Rules of Hooks). The null fallback remains for any genuinely-unknown future type (D-08).

    2. In `resolveHref`, add two branches (D-07): for `watch_like` OR `watch_comment` read `(row.payload as { watch_id?: string }).watch_id ?? ''` and return `/watch/${watchId}`; for `wear_like` OR `wear_comment` read `(row.payload as { wear_event_id?: string }).wear_event_id ?? ''` and return `/wear/${wearEventId}`. No scroll-anchor suffix.

    3. In `resolveCopy`, add four branches mirroring the `watch_overlap` shape. Read `watchModel = (row.payload as { watch_model?: string }).watch_model ?? 'a watch'`. Copy strings (D-01, verbatim — actor in `<span className={actorClass}>`, model in `<span className="font-semibold text-foreground">`):
       - `watch_like`: single → "{actor} liked your {model}"; grouped (actorCount > 1) → "{actor} + {actorCount - 1} others liked your {model}".
       - `wear_like`: single → "{actor} liked your {model} wear"; grouped → "{actor} + {actorCount - 1} others liked your {model} wear" (the literal " wear" follows the model span).
       - `watch_comment`: "{actor} commented on your {model}" (NO grouped variant — D-05; comments are never grouped).
       - `wear_comment`: "{actor} commented on your {model} wear" (NO grouped variant).
       Use the existing `+` phrasing (NOT "and") to match `watch_overlap` (D-03).

    4. Comment preview second line (D-02): in the component body (alongside the `href`/`copy` locals around line 62-63), extract `const commentPreview = (row.payload as { comment_preview?: string })?.comment_preview ?? null`. In the render container (lines 108-111), add, as a sibling AFTER the time-label `<span>`, inside the same `<div className="flex-1 min-w-0 text-sm text-foreground">`: `{commentPreview && (<p className="text-xs text-muted-foreground line-clamp-2 mt-1">{commentPreview}</p>)}`. Because `commentPreview` is only present on comment payloads, like rows naturally render no `<p>`. Do NOT use dangerouslySetInnerHTML — render `comment_preview` as plain text children only (T-58-03).

    In `tests/components/notifications/NotificationRow.test.tsx`, add describe blocks mirroring the watch_overlap tests for: watch_like single, watch_like grouped, wear_like single + " wear", wear_like grouped, watch_comment (copy + preview `<p>` present), wear_comment (copy + " wear" + preview), a like-rows-have-no-preview negative assertion, and router.push deep-link assertions for each (`/watch/{id}` and `/wear/{wearEventId}`). Keep the existing B-8-guard test (unknown type → null) passing.

    Reference: implements D-01/D-02/D-03/D-07/D-08 for NOTIF-16. No change to the DAL or types.ts.
  </action>
  <acceptance_criteria>
    - `src/components/notifications/NotificationRow.tsx` `resolveHref` returns `/watch/{watch_id}` for watch_like and watch_comment, `/wear/{wear_event_id}` for wear_like and wear_comment.
    - `resolveCopy` returns copy containing "liked your" for like types and "commented on your" for comment types; wear types append " wear"; grouped like copy contains "+ {N-1} others liked your".
    - Comment rows render a `<p>` with classes `text-xs text-muted-foreground line-clamp-2 mt-1` containing `comment_preview`; like rows render no such `<p>`.
    - The B-8 guard returns null for an unknown type (e.g. `'price_drop'`).
    - No `dangerouslySetInnerHTML` anywhere in `NotificationRow.tsx`.
    - `npm run test -- tests/components/notifications/NotificationRow.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && ! grep -q "dangerouslySetInnerHTML" src/components/notifications/NotificationRow.tsx && grep -q "line-clamp-2" src/components/notifications/NotificationRow.tsx && grep -q "/wear/" src/components/notifications/NotificationRow.tsx && npm run test -- tests/components/notifications/NotificationRow.test.tsx</automated>
  </verify>
  <done>All four new types render the locked D-01 copy, comment rows show the muted clamped preview line and like rows do not, deep-links resolve to /watch and /wear, the B-8 guard still nulls unknown types, and the extended NotificationRow test suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend NotificationsInbox collapse to group watch_like + wear_like (D-04/D-05/D-06)</name>
  <files>src/components/notifications/NotificationsInbox.tsx, tests/components/notifications/NotificationsInbox.test.tsx</files>
  <behavior>
    - Two watch_like rows with the same payload.watch_id on the same UTC day collapse into one rendered row.
    - Two wear_like rows with the same payload.wear_event_id on the same UTC day collapse into one rendered row.
    - The collapsed like row carries actorCount equal to the group size (e.g. 2).
    - Most-recent actor wins for the displayed row (rows arrive newest-first; first in group is newest) and the displayed row's id is the most-recent row's id (D-06 read-target).
    - watch_like rows with the same watch_id but on DIFFERENT UTC days do NOT collapse (2 rows).
    - watch_comment rows with the same watch_id on the same day do NOT collapse — each is a separate row (D-05).
    - wear_comment rows are not grouped (D-05).
    - watch_like rows do not merge with watch_overlap rows sharing the same watch_id (type-prefixed key).
  </behavior>
  <read_first>
    - src/components/notifications/NotificationsInbox.tsx — the file being modified. Read collapseWatchOverlaps (76-110), toUtcDayKey (112-115), and the re-sort block (103-108). The early guard at line 81 `if (row.type !== 'watch_overlap')` is the extension point.
    - .planning/phases/58-notification-ui-settings-opt-out/58-PATTERNS.md § "2. NotificationsInbox.tsx — extend collapse to like types" — the type-prefixed key format `${row.type}|${targetId}|${toUtcDayKey(row.createdAt)}` and the Pitfall-1 interception rule.
    - .planning/phases/58-notification-ui-settings-opt-out/58-RESEARCH.md Pitfall 1 ("collapseWatchOverlaps — pass-through logic for likes") and Pitfall 2 — intercept watch_like / wear_like BEFORE the nonOverlap.push, and the type-prefixed key prevents watch_like merging with watch_overlap.
    - tests/components/notifications/NotificationsInbox.test.tsx — the test file being extended. Read makeRow (33), the NOTIF-08 watch_overlap collapse describe block (90-145, incl. the actorCount + most-recent-actor + different-model negative cases). Mirror this structure for the like-collapse + comment-no-collapse tests. Write tests FIRST (RED).
    - src/lib/notifications/types.ts lines 27-60 — confirm watch_like uses `watch_id` and wear_like uses `wear_event_id`. DO NOT modify.
  </read_first>
  <action>
    Write the new tests first (RED), then implement. In `src/components/notifications/NotificationsInbox.tsx`, extend the collapse function (executor's discretion per CONTEXT.md D-04: either generalize into a renamed helper or add parallel like branches — behavior must hold either way; the function is not exported, so a rename is fully internal):

    1. Intercept `watch_like` and `wear_like` rows BEFORE they fall through to `nonOverlap` (Pitfall 1). For `watch_like` the group key is `${row.type}|${(row.payload as { watch_id?: string }).watch_id ?? ''}|${toUtcDayKey(row.createdAt)}`; for `wear_like` use `(row.payload as { wear_event_id?: string }).wear_event_id ?? ''` as the targetId. The `row.type` prefix prevents collision with watch_overlap groups sharing the same watch_id (Pitfall 2).
    2. Reuse the existing group-merge mechanic: most-recent wins (`group[0]`), `actorCount: group.length`, push `{ ...mostRecent, actorCount }` to the collapsed list (D-04). The displayed row's `id` stays the most-recent row's id (D-06 — `markNotificationRead` targets it on tap).
    3. Comments (`watch_comment`, `wear_comment`) and `follow` continue to fall through to `nonOverlap` unchanged — never grouped (D-05).
    4. Reuse `toUtcDayKey` (112-115) verbatim and the re-sort block (103-108) verbatim after merge. Do NOT touch `bucketByDay` (D-06 bucketing unchanged).

    In `tests/components/notifications/NotificationsInbox.test.tsx`, add tests mirroring the NOTIF-08 collapse block: watch_like same-watch_id-same-day collapse → 1 row + actorCount 2 + most-recent-actor-wins; wear_like same-wear_event_id-same-day collapse → 1 row; watch_like same-id different-UTC-day → 2 rows (no collapse); watch_comment same-watch_id same-day → 2 rows (D-05 not grouped); and (if the existing rendered-row count assertion uses a test id) confirm watch_like does not merge with a watch_overlap row sharing the watch_id.

    Reference: implements D-04/D-05/D-06 for NOTIF-16. No DAL change.
  </action>
  <acceptance_criteria>
    - `src/components/notifications/NotificationsInbox.tsx` collapse function groups `watch_like` by `watch_id` and `wear_like` by `wear_event_id`, both keyed per (type, target_id, UTC-day).
    - The collapsed like row carries `actorCount` = group size and keeps the most-recent row's `id`.
    - `watch_comment` and `wear_comment` rows are NOT grouped (fall through to nonOverlap).
    - `bucketByDay` is unchanged (`git diff` shows no edit to lines 117-145).
    - `npm run test -- tests/components/notifications/NotificationsInbox.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && grep -q "wear_like" src/components/notifications/NotificationsInbox.tsx && grep -q "wear_event_id" src/components/notifications/NotificationsInbox.tsx && npm run test -- tests/components/notifications/NotificationsInbox.test.tsx</automated>
  </verify>
  <done>watch_like and wear_like rows collapse per (type, target, UTC-day) with correct actorCount and most-recent-actor, comments pass through ungrouped, bucketByDay is untouched, and the extended Inbox test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored notification payload → rendered DOM | `payload.comment_preview` (user-authored comment body, server-stored, sliced to 120 chars) and `payload.watch_id` / `payload.wear_event_id` (server-produced IDs) are rendered into copy text and hrefs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03 | Tampering / XSS (stored) | `comment_preview` second line in `NotificationRow.tsx` | mitigate | Render `comment_preview` as plain-text JSX children inside a `<p>` (React auto-escapes) — never `dangerouslySetInnerHTML`. Acceptance criteria assert `dangerouslySetInnerHTML` is absent from the file. |
| T-58-04 | Tampering / open redirect | `resolveHref` deep-links | accept | `watch_id` / `wear_event_id` are server-produced, owner-scoped IDs interpolated into same-origin path literals (`/watch/${id}`, `/wear/${id}`) — no protocol/host portion is user-controllable, so no open-redirect vector. IDs are not free text. |
| T-58-05 | Information disclosure | viewer-scoping of notification rows | accept | Notifications are already viewer-scoped by the DAL (`getNotificationsForViewer` WHERE user_id = viewerId, Phase 54/55). This plan only renders rows the DAL already returned — no new authz surface, confirmed not added. |
</threat_model>

<verification>
- `npm run test -- tests/components/notifications/NotificationRow.test.tsx tests/components/notifications/NotificationsInbox.test.tsx` green.
- `! grep -q "dangerouslySetInnerHTML" src/components/notifications/NotificationRow.tsx` (no raw-HTML render path).
- `git diff` on `NotificationsInbox.tsx` leaves `bucketByDay` (117-145) untouched.
- At wave merge: `npm run test` full suite green.
</verification>

<success_criteria>
- All four new notification types render with the locked D-01 copy and correct D-07 deep-links.
- Comment rows show the muted clamped preview second line; like rows do not (D-02).
- Likes collapse per (type, target, UTC-day) with actorCount; comments never collapse (D-04/D-05).
- The B-8 guard still nulls genuinely-unknown future types (D-08).
- No DAL, logger, or types.ts change.
</success_criteria>

<output>
After completion, create `.planning/phases/58-notification-ui-settings-opt-out/58-02-SUMMARY.md`
</output>
