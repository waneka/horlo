---
phase: 58-notification-ui-settings-opt-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/profiles.ts
  - src/app/actions/profile.ts
  - tests/components/settings/NotificationsSection.test.tsx
autonomous: true
requirements: [NOTIF-15]
must_haves:
  truths:
    - "ProfileSettings type carries notifyOnLike and notifyOnComment (D-09/D-11 persistence chain)"
    - "getProfileSettings returns notifyOnLike and notifyOnComment from the DB row"
    - "VisibilityField union and VISIBILITY_FIELDS allowlist both include notifyOnLike and notifyOnComment (D-10)"
    - "updateProfileSettings Server Action accepts { field: 'notifyOnLike' } and { field: 'notifyOnComment' } without returning 'Invalid settings payload'"
    - "NotificationsSection.test.tsx fixture compiles after the type widening"
  artifacts:
    - path: "src/data/profiles.ts"
      provides: "Widened ProfileSettings type, VisibilityField union, DEFAULT_SETTINGS, getProfileSettings return, and updateProfileSettingsField insert values"
      contains: "notifyOnLike"
    - path: "src/app/actions/profile.ts"
      provides: "Widened VISIBILITY_FIELDS allowlist driving the updateSettingsSchema Zod enum"
      contains: "notifyOnComment"
    - path: "tests/components/settings/NotificationsSection.test.tsx"
      provides: "Fixture widened with notifyOnLike + notifyOnComment so TS strict compiles"
      contains: "notifyOnLike: true"
  key_links:
    - from: "src/app/actions/profile.ts VISIBILITY_FIELDS"
      to: "updateSettingsSchema z.enum(VISIBILITY_FIELDS)"
      via: "tuple type widening"
      pattern: "z\\.enum\\(VISIBILITY_FIELDS\\)"
    - from: "src/data/profiles.ts ProfileSettings"
      to: "src/components/settings/NotificationsSection.tsx Pick"
      via: "shared type (widened in this plan, consumed by Plan 03)"
      pattern: "notifyOnLike"
---

<objective>
Close the notification-settings persistence chain so the `notifyOnLike` and `notifyOnComment` columns (which already exist in `src/db/schema.ts:273-274` with `DEFAULT true`) are reachable end-to-end from the toggle UI through to the DB. This is the foundational wave: the toggle UI in Plan 03 cannot compile or persist until the `ProfileSettings` type, the `VisibilityField` union, and the `VISIBILITY_FIELDS` Zod allowlist are widened here.

Purpose: RESEARCH.md identified three BLOCKING drift items — the two columns exist in the schema and the logger reads them, but the TypeScript type, the DAL return, and the Server Action allowlist all omit them. Without this plan a `PrivacyToggleRow` call with `field:'notifyOnLike'` is rejected at compile time (TS) and at runtime (the SA returns `{ success:false, error:'Invalid settings payload' }`).

Output: Widened types/DAL/allowlist in `profiles.ts` + `actions/profile.ts`, plus the widened test fixture in `NotificationsSection.test.tsx`. No schema change, no migration, no logger change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-notification-ui-settings-opt-out/58-CONTEXT.md
@.planning/phases/58-notification-ui-settings-opt-out/58-RESEARCH.md
@.planning/phases/58-notification-ui-settings-opt-out/58-PATTERNS.md

<interfaces>
<!-- Current shapes the executor mirrors. The two new columns already exist in the DB schema (schema.ts:273-274) but are absent from these TS constructs. -->

From src/data/profiles.ts (current — to widen, mirror the notifyOnFollow / notifyOnWatchOverlap entries in EACH construct):
- `ProfileSettings` interface (lines 7-16): has `notifyOnFollow: boolean` + `notifyOnWatchOverlap: boolean`; missing the two new booleans.
- `VisibilityField` union (lines 18-23): ends `| 'notifyOnFollow' | 'notifyOnWatchOverlap'`; missing the two new string literals.
- `DEFAULT_SETTINGS` (lines 25-32): has `notifyOnFollow: true` + `notifyOnWatchOverlap: true`; missing the two new defaults.
- `getProfileSettings` return block (lines 100-111): maps `notifyOnFollow: rows[0].notifyOnFollow` + `notifyOnWatchOverlap: rows[0].notifyOnWatchOverlap`; missing the two new mappings (the DB row already has the columns — Drizzle `select()` returns them).
- `updateProfileSettingsField` insert `.values({...})` (lines 155-171): has `notifyOnFollow: field === 'notifyOnFollow' ? value : true` + the watch-overlap analog; missing the two new conditional values. The `.onConflictDoUpdate` `set: { [field]: value, updatedAt: new Date() }` is already general — no change needed there.

From src/app/actions/profile.ts (current — to widen):
- `VISIBILITY_FIELDS` const (lines 56-62): a `[...] as const` tuple ending `'notifyOnFollow', 'notifyOnWatchOverlap'`. `updateSettingsSchema` (lines 64-69) does `z.enum(VISIBILITY_FIELDS)` — it auto-widens once the tuple gains the two new values. No change to the schema object or the `updateProfileSettings` function body.

From tests/components/settings/NotificationsSection.test.tsx (current):
- `settings` fixture (lines 24-27): `{ notifyOnFollow: true, notifyOnWatchOverlap: true }`. After the `ProfileSettings` widening this object is passed to `<NotificationsSection settings={...} />` whose Pick will include the two new required fields (Plan 03) — TS strict will error unless the fixture gains them now.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the ProfileSettings persistence chain (DRIFT-1, DRIFT-2 DAL side, DRIFT-3)</name>
  <files>src/data/profiles.ts, src/app/actions/profile.ts</files>
  <read_first>
    - src/data/profiles.ts — the file being modified. Read lines 7-32 (ProfileSettings, VisibilityField, DEFAULT_SETTINGS), 100-111 (getProfileSettings return), 155-171 (updateProfileSettingsField insert). Mirror the existing notifyOnFollow / notifyOnWatchOverlap entry in each of the five constructs.
    - src/app/actions/profile.ts — the file being modified. Read lines 50-69 (VISIBILITY_FIELDS const + updateSettingsSchema). Mirror the notifyOnFollow / notifyOnWatchOverlap tuple members.
    - .planning/phases/58-notification-ui-settings-opt-out/58-PATTERNS.md § "4. src/data/profiles.ts — 3 drift fixes" and § "5. src/app/actions/profile.ts" — the exact analog lines and the additions to make.
    - .planning/phases/58-notification-ui-settings-opt-out/58-RESEARCH.md § "profiles.ts" and § "actions/profile.ts" drift entries — confirms the five gaps and Pitfall 3 (widen both files in the same atomic commit).
    - src/db/schema.ts lines 267-274 — read-only confirmation that notifyOnLike (273) and notifyOnComment (274) columns already exist with `.notNull().default(true)`. DO NOT modify schema.ts.
  </read_first>
  <action>
    In `src/data/profiles.ts`, make all five mirror-additions, each placed immediately after the corresponding `notifyOnWatchOverlap` entry:
    1. `ProfileSettings` interface — add `notifyOnLike: boolean` and `notifyOnComment: boolean`.
    2. `VisibilityField` union — add `| 'notifyOnLike'` and `| 'notifyOnComment'`.
    3. `DEFAULT_SETTINGS` — add `notifyOnLike: true,` and `notifyOnComment: true,`.
    4. `getProfileSettings` return object — add `notifyOnLike: rows[0].notifyOnLike,` and `notifyOnComment: rows[0].notifyOnComment,` (the Drizzle `select()` row already carries these columns).
    5. `updateProfileSettingsField` insert `.values({...})` — add `notifyOnLike: field === 'notifyOnLike' ? value : true,` and `notifyOnComment: field === 'notifyOnComment' ? value : true,`. Leave the `.onConflictDoUpdate` `set` clause unchanged (it is already field-generic).

    In `src/app/actions/profile.ts`, add `'notifyOnLike',` and `'notifyOnComment',` to the `VISIBILITY_FIELDS` `as const` tuple (lines 56-62). Do NOT touch `updateSettingsSchema` or `updateProfileSettings` — `z.enum(VISIBILITY_FIELDS)` auto-widens from the tuple. This is the V5 Input-Validation control (T-58-01): the allowlist stays a fixed tuple of exactly the seven intended field names — do NOT replace it with `z.string()` or any open shape.

    Make both file edits in the same task (Pitfall 3: `VisibilityField` and `VISIBILITY_FIELDS` desync causes a TS-passes-but-SA-rejects bug). Reference: implements D-09/D-10/D-11 persistence wiring for NOTIF-15. NO schema.ts change, NO migration, NO logger.ts change.
  </action>
  <acceptance_criteria>
    - `src/data/profiles.ts` `ProfileSettings` interface contains both `notifyOnLike: boolean` and `notifyOnComment: boolean`.
    - `src/data/profiles.ts` `VisibilityField` union contains `'notifyOnLike'` and `'notifyOnComment'`.
    - `src/data/profiles.ts` `DEFAULT_SETTINGS`, `getProfileSettings` return, and `updateProfileSettingsField` insert values each contain `notifyOnLike` and `notifyOnComment`.
    - `src/app/actions/profile.ts` `VISIBILITY_FIELDS` array contains both `'notifyOnLike'` and `'notifyOnComment'`; `updateSettingsSchema` still uses `z.enum(VISIBILITY_FIELDS)` and the array remains an `as const` tuple of exactly 7 string literals (no `z.string()`).
    - `git diff src/db/schema.ts` is empty (schema unchanged); `git diff src/lib/notifications/logger.ts` is empty (logger unchanged).
    - `npx tsc --noEmit` reports no new errors in `src/data/profiles.ts` or `src/app/actions/profile.ts`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && grep -c "notifyOnLike\|notifyOnComment" src/data/profiles.ts | grep -qx "10" && grep -q "'notifyOnLike'" src/app/actions/profile.ts && grep -q "'notifyOnComment'" src/app/actions/profile.ts && git diff --quiet src/db/schema.ts src/lib/notifications/logger.ts && echo PASS</automated>
  </verify>
  <done>profiles.ts has all five constructs widened with notifyOnLike/notifyOnComment; actions/profile.ts VISIBILITY_FIELDS tuple widened; schema.ts and logger.ts untouched; TS strict compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the NotificationsSection test fixture (Pitfall 5)</name>
  <files>tests/components/settings/NotificationsSection.test.tsx</files>
  <read_first>
    - tests/components/settings/NotificationsSection.test.tsx — the file being modified. Read lines 24-27 (the `settings` fixture) and the existing `renders 2 PrivacyToggleRow instances` + `regression: notifyOnFollow toggle` tests (lines 35-50) for fixture-shape context. Do NOT add new toggle/title assertions here — those belong to Plan 03 (which extends this same file in Wave 2).
    - .planning/phases/58-notification-ui-settings-opt-out/58-RESEARCH.md Pitfall 5 — explains the fixture-only widening (TS strict errors after the ProfileSettings type widening from Task 1).
    - .planning/phases/58-notification-ui-settings-opt-out/58-VALIDATION.md § "Wave 0 Requirements" — the single Wave-0 fixture obligation.
  </read_first>
  <action>
    In the `settings` fixture object (lines 24-27 of `tests/components/settings/NotificationsSection.test.tsx`), add `notifyOnLike: true,` and `notifyOnComment: true,` alongside the existing `notifyOnFollow: true` / `notifyOnWatchOverlap: true`. This makes the fixture match the widened `Pick<ProfileSettings, ...>` that Plan 03 applies to `NotificationsSection` props. Do NOT change the two existing assertions (`toHaveLength(2)`, the `notifyOnFollow` regression) — Plan 03 updates those in Wave 2. This task only prevents the TS-strict fixture error; the existing two tests must still pass after this edit.
  </action>
  <acceptance_criteria>
    - The `settings` fixture in `tests/components/settings/NotificationsSection.test.tsx` contains `notifyOnLike: true` and `notifyOnComment: true`.
    - `npm run test -- tests/components/settings/NotificationsSection.test.tsx` exits 0 (the existing 3 tests still pass — `toHaveLength(2)` is unchanged because no new toggles are rendered yet).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && grep -q "notifyOnLike: true" tests/components/settings/NotificationsSection.test.tsx && grep -q "notifyOnComment: true" tests/components/settings/NotificationsSection.test.tsx && npm run test -- tests/components/settings/NotificationsSection.test.tsx</automated>
  </verify>
  <done>Fixture carries all four notify fields; the existing 3 NotificationsSection tests still pass green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → updateProfileSettings Server Action | Client-supplied `{ field, value }` crosses into a DB write of a `profile_settings` column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering / Elevation | `VISIBILITY_FIELDS` allowlist + `updateSettingsSchema` Zod enum in `src/app/actions/profile.ts` | mitigate | Widen the allowlist by adding ONLY the two intended literals `'notifyOnLike'` / `'notifyOnComment'` to the existing `as const` tuple. `z.enum(VISIBILITY_FIELDS)` rejects any other field name (mass-assignment protection T-08-04 preserved). Acceptance criteria assert exactly 7 tuple members and that the tuple is not replaced with `z.string()`. |
| T-58-02 | Spoofing / IDOR | `updateProfileSettings` caller identity | accept | Unchanged surface — `updateProfileSettings` already calls `getCurrentUser()` and writes only `user.id`'s own row via `updateProfileSettingsField(user.id, ...)`. No new authz code; the new fields inherit the existing per-caller scoping. |
</threat_model>

<verification>
- `npm run test -- tests/components/settings/NotificationsSection.test.tsx` green (existing 3 tests).
- `npx tsc --noEmit` (or `npm run build` at wave merge) reports no type errors from the widened `ProfileSettings` / `VisibilityField` / `VISIBILITY_FIELDS`.
- `git diff --quiet src/db/schema.ts src/lib/notifications/logger.ts` (no schema/logger change).
</verification>

<success_criteria>
- `ProfileSettings`, `VisibilityField`, `DEFAULT_SETTINGS`, `getProfileSettings`, and `updateProfileSettingsField` all carry `notifyOnLike` + `notifyOnComment`.
- `VISIBILITY_FIELDS` (and therefore the Zod enum) accepts the two new fields and nothing else new.
- The test fixture compiles under TS strict, unblocking Plan 03.
- No change to `schema.ts`, `logger.ts`, or any migration.
</success_criteria>

<output>
After completion, create `.planning/phases/58-notification-ui-settings-opt-out/58-01-SUMMARY.md`
</output>
