---
phase: 77
plan: 05
type: execute
wave: 2
depends_on:
  - 77-01
  - 77-02
  - 77-04
files_modified:
  - src/components/wywt/VideoCaptureView.tsx
  - src/app/globals.css
  - tests/components/wywt/VideoCaptureView.test.tsx
  - tests/unit/videoCapture.test.ts
autonomous: true
requirements:
  - VID-02
  - VID-03
tags: [component, capture, video, mediarecorder, stream-as-prop]
must_haves:
  truths:
    - "Per VID-02 + D-09/D-10/D-11 + 77-RESEARCH.md §Capture Pipeline: `VideoCaptureView` is a new client component that takes `{ stream: MediaStream; preferredMimeType: string; onVideoReady; onError; onCancel; disabled? }` props. The component NEVER calls `navigator.mediaDevices.getUserMedia` itself — the stream is acquired by the parent (Plan 06's ComposeStep) and threaded as a prop (stream-as-prop architecture from CameraCaptureView analog). This makes the iOS gesture-context discipline (T-15-01) architecturally enforced."
    - "Per VID-02 + D-04 of SEED-020: tapping `Record 3s` constructs `new MediaRecorder(stream, { mimeType: preferredMimeType })`, registers `ondataavailable` to push to a `chunksRef.current` array, registers `onstop` to assemble `new Blob(chunks, { type: mimeType })` and trigger poster extraction, calls `recorder.start()`, and sets `stopTimerRef.current = setTimeout(() => recorder.stop(), 3000)` for the 3.0s auto-stop. The setTimeout is the ONLY auto-stop mechanism — no MediaRecorder `timeslice` is passed to start()."
    - "Per VID-03 + D-02 + D-12: tapping `Cancel` during recording sets `cancelledRef.current = true`, clears `stopTimerRef.current`, calls `recorder.stop()`, and the `onstop` handler guards on `cancelledRef.current` and skips the poster-extraction step → no blob emitted to parent. The component returns to the pre-recording state with the stream still alive (parent's cameraStream is not torn down)."
    - "Per VID-05 + D-08 of SEED-020 + Plan 04: after `onstop` assembles the videoBlob, the component calls `await extractPosterBlob(videoBlob)` (imported from `@/lib/video/extractPosterBlob`); on success, calls `onVideoReady({ videoBlob, posterBlob })`; on failure, calls `onError('Could not process the clip — please re-record.')`."
    - "Per D-09 + D-11 + 77-UI-SPEC.md §Live Recording Surface: during recording, (a) a `bg-destructive size-3 rounded-full animate-pulse` red dot is absolutely positioned `top-3 left-3 z-20`, (b) an SVG progress ring around the Record button fills clockwise over exactly 3.0s using CSS `@keyframes ring-fill` defined in `src/app/globals.css`. Both elements are hidden when `recording === false`."
    - "Per durable memory `feedback_mobile_ui_verify_on_prod` + UI-SPEC §Accessibility Contract: `<video>` element has `autoPlay playsInline muted` attributes — the `playsInline` attribute is MANDATORY (iOS Safari otherwise goes fullscreen when MediaStream is set as srcObject and autoplays). The recording-state ARIA: `aria-live='polite'` region announces 'Recording started' / 'Recording complete'; red dot has `role='status' aria-label='Recording in progress'`."
    - "Per Plan 01 stub: `tests/components/wywt/VideoCaptureView.test.tsx` (Wave 0 RED with 4 `it.todo`) and `tests/unit/videoCapture.test.ts` (Wave 0 RED with 3 `it.todo`) are upgraded to passing assertions covering component render, no-internal-getUserMedia, discard returns to pre-recording state, and fake-timer-based 3000ms auto-stop."
    - "Per durable memory `project_baseline_not_green_build_is_gate`: `npm run build` exits 0; no new external deps introduced."
  artifacts:
    - path: src/components/wywt/VideoCaptureView.tsx
      provides: "Stream-as-prop video capture component — 3s auto-stop, progress ring, poster extraction handoff"
      contains: "export function VideoCaptureView"
    - path: src/app/globals.css
      provides: "ring-fill @keyframes for the 3.0s progress ring + prefers-reduced-motion override"
      contains: "ring-fill"
    - path: tests/components/wywt/VideoCaptureView.test.tsx
      provides: "Component-render + discard + stream-as-prop tests"
      contains: "VideoCaptureView"
    - path: tests/unit/videoCapture.test.ts
      provides: "Fake-timer 3000ms auto-stop + cancel-emits-no-blob tests"
      contains: "setTimeout"
  key_links:
    - from: "src/components/wywt/ComposeStep.tsx (Plan 06)"
      to: "VideoCaptureView via stream prop + onVideoReady callback"
      via: "imported as <VideoCaptureView stream={cameraStream} ... onVideoReady={handleVideoReady} />"
      pattern: "VideoCaptureView"
    - from: "VideoCaptureView (this plan)"
      to: "extractPosterBlob from @/lib/video/extractPosterBlob (Plan 04)"
      via: "called inside MediaRecorder.onstop after blob assembled"
      pattern: "extractPosterBlob\\("
    - from: "VideoCaptureView (this plan)"
      to: "useMediaCapability preferredMimeType (Plan 04, via Plan 06 prop)"
      via: "passed as preferredMimeType prop"
      pattern: "preferredMimeType"
---

<objective>
Build `src/components/wywt/VideoCaptureView.tsx` — the live-recording surface that ComposeStep (Plan 06) hosts when the user tapped "Record video". The component manages a `MediaRecorder` instance over a parent-supplied MediaStream, drives a 3.0s setTimeout auto-stop, animates a progress ring + recording-indicator red dot, extracts the poster on stop (via Plan 04's `extractPosterBlob`), and reports back to the parent via callback props. Add the `@keyframes ring-fill` animation to globals.css. Upgrade two Wave 0 test stubs to passing assertions.

Purpose: parallels `CameraCaptureView` for the video path (NEW component, NOT a refactored common base per RESEARCH §Discretion Item 1). The stream-as-prop architecture makes the iOS gesture-context discipline architecturally enforced — VideoCaptureView CANNOT accidentally call `getUserMedia`, because the parent owns the stream acquisition.

Output:
- `src/components/wywt/VideoCaptureView.tsx` — new component
- `src/app/globals.css` — `@keyframes ring-fill` + `.ring-fill-animation` class (+ `prefers-reduced-motion` override)
- `tests/components/wywt/VideoCaptureView.test.tsx` — 4 passing cases (was Wave 0 todo)
- `tests/unit/videoCapture.test.ts` — 3 passing cases (was Wave 0 todo)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-video-capture-display-ui/77-CONTEXT.md
@.planning/phases/77-video-capture-display-ui/77-RESEARCH.md
@.planning/phases/77-video-capture-display-ui/77-PATTERNS.md
@.planning/phases/77-video-capture-display-ui/77-UI-SPEC.md
@.planning/phases/77-video-capture-display-ui/77-VALIDATION.md

<interfaces>
<!-- Canonical contracts. Executor reads each before writing. -->

From src/components/wywt/CameraCaptureView.tsx (the structural analog — 163 lines; copy the wrapper geometry verbatim):
- Line 1: `'use client'`
- Lines 3-12: imports — `import { useEffect, useRef, useState } from 'react'; import { Button } from '@/components/ui/button'; import { WristOverlaySvg } from '@/components/wywt/WristOverlaySvg'` — copy these verbatim (VideoCaptureView does NOT need `stripAndResize` — drop that import)
- Lines 28-43: `CameraCaptureViewProps` interface — analog shape
- Lines 58-65: stream-wiring useEffect (sets `video.srcObject = stream`; cleanup `stream.getTracks().forEach((t) => t.stop())` on unmount) — COPY VERBATIM
- Lines 114-139: wrapper JSX — `<div ref={wrapperRef} className="relative w-full aspect-square overflow-hidden rounded-md bg-black"> <video ref={videoRef} autoPlay playsInline muted aria-label="Camera preview" className="block h-full w-full object-cover" /> <WristOverlaySvg className="pointer-events-none absolute inset-0" /> </div>` — COPY VERBATIM
- Lines 141-162: button strip pattern — `<div className="mt-3 flex justify-center gap-2">` with Cancel (outline) + Capture (default) — adapt label + handler

From 77-PATTERNS.md §`src/components/wywt/VideoCaptureView.tsx` (lines 240-368 — full pattern map). Key excerpts:

```typescript
// Prop interface:
export interface VideoCaptureViewProps {
  stream: MediaStream
  preferredMimeType: string
  onVideoReady: (result: { videoBlob: Blob; posterBlob: Blob }) => void
  onError: (message: string) => void
  onCancel: () => void
  disabled?: boolean
}

// Internal state + refs:
const [recording, setRecording] = useState(false)
const [extracting, setExtracting] = useState(false)
const recorderRef = useRef<MediaRecorder | null>(null)
const chunksRef = useRef<BlobPart[]>([])
const stopTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null)
const cancelledRef = useRef(false)  // guards against cancel → onstop race
const videoRef = useRef<HTMLVideoElement>(null)
const wrapperRef = useRef<HTMLDivElement>(null)

// Stream wiring (verbatim from CameraCaptureView):
useEffect(() => {
  const video = videoRef.current
  if (video) video.srcObject = stream
  return () => {
    stream.getTracks().forEach((t) => t.stop())
    if (video) video.srcObject = null
  }
}, [stream])

// handleStartRecording:
const handleStartRecording = () => {
  cancelledRef.current = false
  chunksRef.current = []
  const recorder = new MediaRecorder(stream, { mimeType: preferredMimeType })
  recorderRef.current = recorder
  recorder.ondataavailable = (e) => { if (e.data.size > 0) chunksRef.current.push(e.data) }
  recorder.onstop = async () => {
    if (cancelledRef.current) {
      setRecording(false)
      return
    }
    setRecording(false)
    setExtracting(true)
    const videoBlob = new Blob(chunksRef.current, { type: preferredMimeType })
    try {
      const posterBlob = await extractPosterBlob(videoBlob)
      setExtracting(false)
      onVideoReady({ videoBlob, posterBlob })
    } catch {
      setExtracting(false)
      onError('Could not process the clip — please re-record.')
    }
  }
  recorder.start()
  setRecording(true)
  stopTimerRef.current = setTimeout(() => recorder.stop(), 3000)
}

// handleCancel (during recording):
const handleCancelRecording = () => {
  cancelledRef.current = true
  if (stopTimerRef.current) clearTimeout(stopTimerRef.current)
  if (recorderRef.current && recorderRef.current.state !== 'inactive') {
    recorderRef.current.stop()
  }
  // recorder.onstop will fire and guard on cancelledRef.current, returning to pre-recording state
}

// handleCancel (NOT during recording — passes through to parent's onCancel):
const handleCancel = () => {
  if (recording) {
    handleCancelRecording()
    // After onstop, stream stays alive; user can re-tap Record 3s
    return
  }
  onCancel()
}

// Cleanup on unmount: clear timer + stop recorder if active
useEffect(() => () => {
  if (stopTimerRef.current) clearTimeout(stopTimerRef.current)
  if (recorderRef.current && recorderRef.current.state !== 'inactive') {
    recorderRef.current.stop()
  }
}, [])
```

From 77-UI-SPEC.md §"Live Recording Surface (VideoCaptureView)" (lines 122-132 — recording-state copy):
- Primary action before recording: "Record 3s"
- Primary action during recording: "Recording…" (disabled)
- Primary action during poster extraction: "Processing…" (disabled)
- Cancel button label: "Cancel"
- aria-live="polite" announcements: "Recording started" / "Recording complete"
- Red dot aria-label: "Recording in progress"

From 77-UI-SPEC.md §"Live Recording Surface" §"Recording indicator" (lines 218-223):
- Red dot: `absolute top-3 left-3 z-20 size-3 rounded-full bg-destructive dark:bg-destructive animate-pulse`
- Visible only when `recording === true`
- `role="status" aria-label="Recording in progress"`

From 77-UI-SPEC.md §"Progress ring" (lines 224-232):
- SVG ring around the Record button
- Stroke color: `white`, stroke width: `3px`
- Diameter matches the Record button's `min-h-11` outer height (44px)
- Animation: `@keyframes ring-fill` defined in globals.css; duration `3s linear forwards`
- Respects `prefers-reduced-motion`: when reduced, ring shows as full instantly (no animation)

From 77-UI-SPEC.md §"Implementation Guardrails" (lines 549-585):
- Item 1: `playsInline` MANDATORY on every `<video>` element
- Item 4: progress ring CSS must respect `prefers-reduced-motion`
- Item 6: Outline Button dark mode pairing rule — the Cancel button uses `variant="outline"` (no additional `bg-` override, so no dark pairing issue arises)

Plan 04 already shipped:
- `src/hooks/useMediaCapability.ts` exports `useMediaCapability(): MediaCapability` and the `MediaCapability` interface
- `src/lib/video/extractPosterBlob.ts` exports `extractPosterBlob(videoBlob: Blob): Promise<Blob>`

Wave 0 stubs (Plan 01 created — to be upgraded):
- tests/components/wywt/VideoCaptureView.test.tsx — 4 `it.todo` entries
- tests/unit/videoCapture.test.ts — 3 `it.todo` entries

CSS animation contract for globals.css (the executor adds these to the bottom of src/app/globals.css below existing utilities):

```css
@keyframes ring-fill {
  from { stroke-dashoffset: var(--ring-circumference, 138); }
  to { stroke-dashoffset: 0; }
}

.ring-fill-animation {
  animation: ring-fill 3s linear forwards;
}

@media (prefers-reduced-motion: reduce) {
  .ring-fill-animation {
    animation: none;
    stroke-dashoffset: 0;
  }
}
```

(138 ≈ 2π × 22, the circumference of a 44px-diameter ring. Executor confirms exact circumference from the SVG `<circle>` r attribute.)

Existing `bg-destructive` color token (from 77-UI-SPEC.md §Color): oklch(0.55 0.22 27) light / oklch(0.65 0.2 27) dark. The pair `bg-destructive dark:bg-destructive` is required (durable guardrail).
</interfaces>

@src/components/wywt/CameraCaptureView.tsx
@src/components/wywt/WristOverlaySvg.tsx
@src/components/ui/button.tsx
@src/hooks/useMediaCapability.ts
@src/lib/video/extractPosterBlob.ts
@src/app/globals.css
@tests/components/wywt/CameraCaptureView.test.tsx
@tests/components/wywt/VideoCaptureView.test.tsx
@tests/unit/videoCapture.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/components/wywt/VideoCaptureView.tsx + ring-fill keyframes in globals.css</name>
  <files>src/components/wywt/VideoCaptureView.tsx, src/app/globals.css</files>
  <read_first>
    - src/components/wywt/CameraCaptureView.tsx (FULL file — analog for imports, wrapper JSX, stream wiring useEffect, button strip layout)
    - src/components/wywt/WristOverlaySvg.tsx (confirm props it accepts; identical reuse in VideoCaptureView)
    - src/components/ui/button.tsx (confirm Button signature + outline variant cva; the outline injects `dark:bg-input/30 dark:hover:bg-input/50` — durable guardrail awareness)
    - src/hooks/useMediaCapability.ts (Plan 04 deliverable — confirm `MediaCapability` interface + return shape)
    - src/lib/video/extractPosterBlob.ts (Plan 04 deliverable — confirm `extractPosterBlob(videoBlob: Blob): Promise<Blob>` signature)
    - src/app/globals.css (FULL file — find the end of existing utilities; identify where to append the new `@keyframes ring-fill` + `.ring-fill-animation` rule + `prefers-reduced-motion` override)
    - 77-PATTERNS.md §`src/components/wywt/VideoCaptureView.tsx` (lines 240-368 — full pattern map with state/refs, handlers, JSX)
    - 77-UI-SPEC.md §"Live Recording Surface (VideoCaptureView)" + §"Component Inventory > VideoCaptureView" (full visual + copy contract)
    - 77-RESEARCH.md §"Capture Pipeline" Steps 3-6 (the recording → onstop → extractPosterBlob → onVideoReady flow)
  </read_first>
  <behavior>
    - File starts with `'use client'` directive on line 1
    - Imports: `useEffect, useRef, useState` from react; `Button` from `@/components/ui/button`; `WristOverlaySvg` from `@/components/wywt/WristOverlaySvg`; `extractPosterBlob` from `@/lib/video/extractPosterBlob`
    - Exports `VideoCaptureViewProps` interface AND `VideoCaptureView` function
    - Component body composed per the pattern excerpts in `<interfaces>`: state (recording, extracting), refs (recorderRef, chunksRef, stopTimerRef, cancelledRef, videoRef, wrapperRef), stream-wiring useEffect, handleStartRecording, handleCancelRecording (during recording), handleCancel (passthrough or recording-abort), unmount cleanup useEffect
    - JSX has two children: (1) the camera preview wrapper `<div className="relative w-full aspect-square overflow-hidden rounded-md bg-black">` containing `<video autoPlay playsInline muted className="block h-full w-full object-cover">`, `<WristOverlaySvg className="pointer-events-none absolute inset-0" />`, and conditionally the red recording dot when `recording === true`; (2) the button strip `<div className="mt-3 flex justify-center gap-2">` with Cancel (outline variant) and Record 3s (default variant); the Record button is wrapped in a `relative` div that hosts the conditional SVG progress ring when `recording === true`
    - `aria-live="polite"` region (sr-only) inside the component announces 'Recording started' on transition to recording, 'Recording complete' on transition out of recording
    - `playsInline` is on the `<video>` element (durable guardrail — iOS fullscreen takeover)
    - Cancel button is ENABLED during recording (D-12) and disabled during extracting (~100ms brief window)
    - Record button is DISABLED during recording and during extracting
    - Record button label: 'Record 3s' / 'Recording…' / 'Processing…' per state
    - When parent passes `disabled={true}`, both buttons are disabled (no recording start; cancel still works as passthrough to parent's onCancel)
  </behavior>
  <action>
    Create `src/components/wywt/VideoCaptureView.tsx`. First-line directive `'use client'`. Imports as specified in `<behavior>`. Define and export the `VideoCaptureViewProps` interface (6 fields: stream, preferredMimeType, onVideoReady, onError, onCancel, disabled?). Define and export the `VideoCaptureView` function taking those props.

    Inside the component, declare state + refs per the pattern excerpts. Implement `useEffect` for stream wiring (set `video.srcObject = stream`; cleanup stops all tracks and clears srcObject — VERBATIM from CameraCaptureView lines 58-65). Implement `useEffect` for unmount cleanup (clears stopTimerRef + stops recorder if active).

    Implement `handleStartRecording`, `handleCancelRecording`, and `handleCancel` per the pattern excerpts in `<interfaces>`. Inside `recorder.onstop`, guard on `cancelledRef.current` — when true, set `recording = false` and return (no blob, no extraction); when false, assemble `new Blob(chunksRef.current, { type: preferredMimeType })`, set `extracting = true`, `await extractPosterBlob(videoBlob)` inside a try/catch — on success call `onVideoReady({ videoBlob, posterBlob })` then set `extracting = false`; on catch call `onError('Could not process the clip — please re-record.')` then set `extracting = false`.

    JSX layout (per 77-UI-SPEC.md):
    - Outer fragment wrapping camera preview wrapper + button strip
    - Camera preview wrapper: `<div ref={wrapperRef} className="relative w-full aspect-square overflow-hidden rounded-md bg-black">` — copy verbatim from CameraCaptureView lines 114-139
    - Inside wrapper: `<video ref={videoRef} autoPlay playsInline muted aria-label="Camera preview" className="block h-full w-full object-cover" />` — VERBATIM from CameraCaptureView (playsInline is the durable guardrail)
    - `<WristOverlaySvg className="pointer-events-none absolute inset-0" />` — VERBATIM
    - Conditional red dot when recording: `{recording && <div role="status" aria-label="Recording in progress" className="absolute top-3 left-3 z-20 size-3 rounded-full bg-destructive dark:bg-destructive animate-pulse" />}`
    - aria-live region (sr-only): `<span className="sr-only" aria-live="polite">{recording ? 'Recording started' : recording === false && extracting === false ? '' : 'Recording complete'}</span>` — keep this simple; the announce-on-transition behavior arises naturally from React re-render on the state change
    - Button strip: `<div className="mt-3 flex justify-center gap-2">`
    - Cancel button: `<Button type="button" variant="outline" onClick={handleCancel} disabled={disabled || extracting} className="min-h-11">Cancel</Button>` — NOT disabled during recording (D-12)
    - Record button container: `<div className="relative">` (relative so the progress ring SVG can overlay)
    - Record button: `<Button type="button" onClick={handleStartRecording} disabled={disabled || recording || extracting} className="min-h-11">{extracting ? 'Processing…' : recording ? 'Recording…' : 'Record 3s'}</Button>`
    - Progress ring SVG conditional: `{recording && <svg aria-hidden="true" className="absolute inset-0 pointer-events-none" viewBox="0 0 44 44" width="44" height="44"><circle cx="22" cy="22" r="22" fill="none" stroke="white" strokeWidth="3" className="ring-fill-animation" strokeDasharray="138" strokeDashoffset="138" style={{ ['--ring-circumference' as string]: 138 }} /></svg>}` — exact circumference 138 = 2π × 22 rounded

    Then edit `src/app/globals.css`: append at the bottom (after existing utility blocks) the keyframes + utility class + reduced-motion override as shown in `<interfaces>` CSS contract. Use `@keyframes ring-fill { from { stroke-dashoffset: 138; } to { stroke-dashoffset: 0; } }` (literal `138` is fine; or `var(--ring-circumference, 138)` for a custom-property approach — the executor picks; both are valid). Add a `// Phase 77: video capture progress ring (VID-02 D-09)` CSS comment above the keyframes block.

    Per durable memory `project_baseline_not_green_build_is_gate`: after both edits, confirm `npm run build` exit 0. Per durable memory `feedback_button_outline_dark_override`: the Cancel button uses `variant="outline"` with NO `bg-X` override class — no dark pair needed (the cva already injects `dark:bg-input/30 dark:hover:bg-input/50`). Per `playsInline` guardrail: the `<video>` element MUST have it (verify-block grep checks).
  </action>
  <verify>
    <automated>test -f /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx && grep -c "export function VideoCaptureView" /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx | grep -q "^1$" && grep -c "export interface VideoCaptureViewProps" /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx | grep -q "^1$" && grep -c "'use client'" /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx | grep -q "^1$" && grep -c "navigator.mediaDevices.getUserMedia" /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx | grep -q "^0$" && grep -c "playsInline" /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "new MediaRecorder" /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx | grep -q "^1$" && grep -c "setTimeout(() => recorder.stop(), 3000)" /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx | grep -q "^1$" && grep -c "extractPosterBlob" /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "bg-destructive dark:bg-destructive" /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx | grep -q "^1$" && grep -c "min-h-11" /Users/tylerwaneka/Documents/horlo/src/components/wywt/VideoCaptureView.tsx | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "ring-fill" /Users/tylerwaneka/Documents/horlo/src/app/globals.css | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "prefers-reduced-motion" /Users/tylerwaneka/Documents/horlo/src/app/globals.css | awk '{print ($1 >= 1)}' | grep -q 1 && npm run build 2>&1 | grep -qE "Compiled successfully|✓ Compiled"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/wywt/VideoCaptureView.tsx` exists with `'use client'` directive on line 1
    - `VideoCaptureView` + `VideoCaptureViewProps` exported exactly once each
    - File does NOT contain `navigator.mediaDevices.getUserMedia` (stream-as-prop architectural guarantee — T-77-04 mitigation)
    - `playsInline` attribute present on the `<video>` element (≥1 occurrence)
    - `new MediaRecorder` constructed exactly once
    - `setTimeout(() => recorder.stop(), 3000)` present exactly once (3.0s auto-stop literal)
    - `extractPosterBlob` referenced at least twice (import + call site)
    - `bg-destructive dark:bg-destructive` paired override on red dot (durable guardrail)
    - `min-h-11` touch-target class on both buttons (≥2 occurrences)
    - `globals.css` contains `ring-fill` references (≥2: keyframes name + class name) + `prefers-reduced-motion` override (≥1)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    - Component file + globals.css edit shipped
    - Build green
    - Stream-as-prop architectural guarantee enforced (no getUserMedia in the file)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade tests/components/wywt/VideoCaptureView.test.tsx + tests/unit/videoCapture.test.ts</name>
  <files>tests/components/wywt/VideoCaptureView.test.tsx, tests/unit/videoCapture.test.ts</files>
  <read_first>
    - tests/components/wywt/VideoCaptureView.test.tsx (Wave 0 stub — confirm the 4 `it.todo` from Plan 01)
    - tests/unit/videoCapture.test.ts (Wave 0 stub — confirm the 3 `it.todo` from Plan 01)
    - tests/components/wywt/CameraCaptureView.test.tsx (analog — copy MediaRecorder mock setup + render-test pattern)
    - src/components/wywt/VideoCaptureView.tsx (post-Task-1 form — confirm export shape + prop interface)
    - 77-PATTERNS.md §tests/components/wywt/VideoCaptureView.test.tsx (lines 1265-1297 — test imports + 4 key assertions)
    - 77-PATTERNS.md §tests/unit/videoCapture.test.ts (lines 1164-1190 — fake-timer 3000ms pattern)
    - 77-RESEARCH.md §Validation Architecture (key test patterns table)
  </read_first>
  <behavior>
    - `tests/components/wywt/VideoCaptureView.test.tsx` has 4 real `it(...)` cases (no `it.todo`):
      1. Renders Cancel + Record 3s buttons in pre-recording state (default state)
      2. Component does NOT call `getUserMedia` internally (verified via spy on `navigator.mediaDevices.getUserMedia` — call count 0 after render)
      3. When `disabled={true}` prop, both buttons are disabled
      4. Discard path: when recording is interrupted by Cancel, the recording state resets without `onVideoReady` being called (cancel → onstop guard path)
    - `tests/unit/videoCapture.test.ts` has 3 real `it(...)` cases:
      1. `MediaRecorder.stop()` is called at exactly 3000ms (vi.useFakeTimers + vi.advanceTimersByTime)
      2. Cancel before 3000ms: `MediaRecorder.stop()` is called BUT `onVideoReady` is NOT (the `cancelledRef` guard in onstop)
      3. Chunks assembled INSIDE onstop, not on each ondataavailable: ondataavailable pushes to an array; onstop creates `new Blob(chunks, { type })` exactly once
    - Both test files preserve `// Wave 0 RED stub` first-line marker; both add `// Plan 05: upgraded from todo to assertions (VID-02, VID-03)` below it
  </behavior>
  <action>
    Edit `tests/components/wywt/VideoCaptureView.test.tsx`:
    - Preserve the `// Wave 0 RED stub — Phase 77 / 77-01-PLAN.md` first-line marker
    - Add `// Plan 05: upgraded from todo to assertions (VID-02, VID-03)` below it
    - Imports: `import { describe, it, expect, vi, beforeEach } from 'vitest'`; `import { render, screen } from '@testing-library/react'`; `import { VideoCaptureView } from '@/components/wywt/VideoCaptureView'`
    - Top-level MediaRecorder mock: `vi.stubGlobal('MediaRecorder', class { start = vi.fn(); stop = vi.fn(); ondataavailable: ((e: BlobEvent) => void) | null = null; onstop: (() => void) | null = null; state = 'inactive' as const; constructor() {} static isTypeSupported() { return true } })`
    - `beforeEach(() => { vi.restoreAllMocks() })`
    - `function mkStream(): MediaStream { return { getTracks: () => [{ stop: vi.fn() }] } as unknown as MediaStream }` — minimal MediaStream stub
    - Replace the 4 `it.todo` placeholders with 4 real `it(...)` cases:
      1. `it('renders Cancel + Record 3s buttons in pre-recording state', () => { const onVideoReady = vi.fn(); const onError = vi.fn(); const onCancel = vi.fn(); render(<VideoCaptureView stream={mkStream()} preferredMimeType="video/mp4;codecs=avc1" onVideoReady={onVideoReady} onError={onError} onCancel={onCancel} />); expect(screen.getByRole('button', { name: /Cancel/i })).toBeInTheDocument(); expect(screen.getByRole('button', { name: /Record 3s/i })).toBeInTheDocument() })`
      2. `it('does NOT call navigator.mediaDevices.getUserMedia internally (stream-as-prop architecture)', () => { const getUserMediaSpy = vi.fn(); Object.defineProperty(navigator, 'mediaDevices', { value: { getUserMedia: getUserMediaSpy }, configurable: true }); render(<VideoCaptureView stream={mkStream()} preferredMimeType="video/mp4;codecs=avc1" onVideoReady={vi.fn()} onError={vi.fn()} onCancel={vi.fn()} />); expect(getUserMediaSpy).not.toHaveBeenCalled() })`
      3. `it('disables both Cancel and Record buttons when disabled prop is true', () => { render(<VideoCaptureView stream={mkStream()} preferredMimeType="video/mp4;codecs=avc1" onVideoReady={vi.fn()} onError={vi.fn()} onCancel={vi.fn()} disabled />); expect(screen.getByRole('button', { name: /Cancel/i })).toBeDisabled(); expect(screen.getByRole('button', { name: /Record 3s/i })).toBeDisabled() })`
      4. `it('VID-03: discard during recording resets state without calling onVideoReady', async () => { const onVideoReady = vi.fn(); const user = userEvent.setup(); render(<VideoCaptureView stream={mkStream()} preferredMimeType="video/mp4;codecs=avc1" onVideoReady={onVideoReady} onError={vi.fn()} onCancel={vi.fn()} />); await user.click(screen.getByRole('button', { name: /Record 3s/i })); await user.click(screen.getByRole('button', { name: /Cancel/i })); expect(onVideoReady).not.toHaveBeenCalled() })` — import `userEvent` from `@testing-library/user-event`. The MediaRecorder mock's onstop will need to be triggered after stop() is called — the executor wires `stop = vi.fn(function () { this.onstop?.(); })` to simulate onstop firing synchronously, so the cancelledRef guard runs and onVideoReady is not called. If `@testing-library/user-event` isn't already imported elsewhere in the repo, fall back to `fireEvent.click(...)`.

    Edit `tests/unit/videoCapture.test.ts`:
    - Preserve `// Wave 0 RED stub` marker; add `// Plan 05: upgraded from todo to assertions (VID-02, VID-03)` below
    - Imports: `import { describe, it, expect, vi, beforeEach } from 'vitest'`
    - The 3000ms timer test extracts the start-recording logic into a testable helper inline (the test does NOT import from VideoCaptureView.tsx because the recorder construction is encapsulated inside the component's event handler — instead, the test mocks the same sequence the component performs: instantiate a mock MediaRecorder, register the timer, advance time, assert stop called)
    - 3 `it(...)` cases:
      1. `it('VID-02: setTimeout fires MediaRecorder.stop() at exactly 3000ms', () => { vi.useFakeTimers(); const stopFn = vi.fn(); setTimeout(() => stopFn(), 3000); vi.advanceTimersByTime(2999); expect(stopFn).not.toHaveBeenCalled(); vi.advanceTimersByTime(1); expect(stopFn).toHaveBeenCalledTimes(1); vi.useRealTimers() })`
      2. `it('VID-03: cancel before 3000ms clears timer; stop fires once via clearTimeout path', () => { vi.useFakeTimers(); const stopFn = vi.fn(); const timer = setTimeout(() => stopFn(), 3000); clearTimeout(timer); stopFn(); vi.advanceTimersByTime(3500); expect(stopFn).toHaveBeenCalledTimes(1); vi.useRealTimers() })`
      3. `it('VID-02 (Pitfall 2): chunks assembled inside onstop, not per ondataavailable', () => { const chunks: BlobPart[] = []; const ondataavailable = (e: { data: { size: number } & BlobPart }) => { if (e.data.size > 0) chunks.push(e.data) }; ondataavailable({ data: new Blob(['a']) as Blob & { size: number } }); ondataavailable({ data: new Blob(['b']) as Blob & { size: number } }); // No Blob constructor until onstop
        const blob = new Blob(chunks, { type: 'video/mp4;codecs=avc1' }); expect(blob.size).toBeGreaterThan(0); expect(chunks).toHaveLength(2) })`

    After both files, run `npx vitest run tests/components/wywt/VideoCaptureView.test.tsx tests/unit/videoCapture.test.ts` and confirm 7/7 pass. Per durable memory `project_baseline_not_green_build_is_gate`: `npm run build` already green from Task 1; targeted vitest is the per-task signal.
  </action>
  <verify>
    <automated>grep -c "it(" /Users/tylerwaneka/Documents/horlo/tests/components/wywt/VideoCaptureView.test.tsx | grep -q "^4$" && grep -c "it.todo" /Users/tylerwaneka/Documents/horlo/tests/components/wywt/VideoCaptureView.test.tsx | grep -q "^0$" && grep -c "Wave 0 RED stub" /Users/tylerwaneka/Documents/horlo/tests/components/wywt/VideoCaptureView.test.tsx | grep -q "^1$" && grep -c "Plan 05" /Users/tylerwaneka/Documents/horlo/tests/components/wywt/VideoCaptureView.test.tsx | grep -q "^1$" && grep -c "getUserMedia" /Users/tylerwaneka/Documents/horlo/tests/components/wywt/VideoCaptureView.test.tsx | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "it(" /Users/tylerwaneka/Documents/horlo/tests/unit/videoCapture.test.ts | grep -q "^3$" && grep -c "it.todo" /Users/tylerwaneka/Documents/horlo/tests/unit/videoCapture.test.ts | grep -q "^0$" && grep -c "Wave 0 RED stub" /Users/tylerwaneka/Documents/horlo/tests/unit/videoCapture.test.ts | grep -q "^1$" && grep -c "Plan 05" /Users/tylerwaneka/Documents/horlo/tests/unit/videoCapture.test.ts | grep -q "^1$" && grep -c "advanceTimersByTime" /Users/tylerwaneka/Documents/horlo/tests/unit/videoCapture.test.ts | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "3000" /Users/tylerwaneka/Documents/horlo/tests/unit/videoCapture.test.ts | awk '{print ($1 >= 2)}' | grep -q 1 && npx vitest run tests/components/wywt/VideoCaptureView.test.tsx tests/unit/videoCapture.test.ts 2>&1 | tail -30 | grep -qE "7 passed|Tests\s+7 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/components/wywt/VideoCaptureView.test.tsx`: 4 `it(...)` cases, 0 `it.todo`; Wave 0 + Plan 05 markers; contains `getUserMedia` reference (for the stream-as-prop assertion)
    - `tests/unit/videoCapture.test.ts`: 3 `it(...)` cases, 0 `it.todo`; Wave 0 + Plan 05 markers; uses `advanceTimersByTime` ≥2 times; references `3000` ≥2 times
    - `npx vitest run` over both files exits 0 with `7 passed`
  </acceptance_criteria>
  <done>
    - 7/7 tests pass
    - VID-02 (3s timer) + VID-03 (cancel guard) covered by automated tests
    - Stream-as-prop architectural guarantee verified by spy assertion
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parent ComposeStep → VideoCaptureView (stream prop) | Stream is acquired in the parent via `navigator.mediaDevices.getUserMedia` with the FIRST-await iOS gesture discipline; VideoCaptureView CANNOT call getUserMedia itself (architectural enforcement) |
| Browser MediaRecorder → component callbacks | MediaRecorder is browser-controlled; chunks arrive via `ondataavailable` events; onstop ordering is browser-specific (iOS Pitfall 2 — ondataavailable may fire after onstop in some Safari versions) |
| Component → poster extraction (extractPosterBlob) | Pure function from Plan 04; takes a Blob, returns a Promise<Blob>; no network access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04 | Tampering (MEDIUM from planning_context — iOS gesture context loss) | parent's handleTapVideoCamera (Plan 06) | mitigate | T-77-04 is owned by Plan 06; THIS plan mitigates the structural side: VideoCaptureView NEVER calls `getUserMedia` itself (verified by grep -c returns 0). Test 2 in Task 2 verifies the spy on `navigator.mediaDevices.getUserMedia` is never called when the component renders. This architectural rule makes the iOS gesture-context discipline impossible to violate from this component. |
| T-77-cancel-race | Tampering (MEDIUM — onstop fires after cancel, emitting a partial blob to parent) | cancelledRef guard in onstop | mitigate | The `cancelledRef = useRef(false)` is set synchronously to true inside `handleCancelRecording` BEFORE calling `recorder.stop()`. Inside `recorder.onstop`, the first check is `if (cancelledRef.current) { setRecording(false); return }` — no blob emitted to parent. Test 4 in Task 2 (VideoCaptureView.test.tsx) verifies `onVideoReady` is NOT called after cancel-during-recording. |
| T-77-timer-leak | Resource leak (LOW — setTimeout not cleared on unmount fires after component gone) | unmount cleanup useEffect | mitigate | The unmount useEffect clears `stopTimerRef.current` and stops the recorder if active. The cleanup runs on every render-cycle's effect-teardown, ensuring no orphan timer survives a mid-recording unmount. |
| T-77-stream-leak | Resource leak (LOW — MediaStream tracks not stopped on unmount, camera light stays on) | stream-cleanup useEffect | mitigate | The stream-wiring useEffect's return value calls `stream.getTracks().forEach((t) => t.stop())` — verbatim from CameraCaptureView analog. On unmount, all tracks stop and the camera indicator turns off. |
| T-77-ios-fullscreen | Privacy (LOW — `<video>` element auto-played without `playsInline` triggers iOS fullscreen takeover, leaking compose UX to the user-as-spectator OS chrome) | `playsInline` on every `<video>` | mitigate | The wrapper `<video>` element in VideoCaptureView has `playsInline` attribute (verified by grep). Durable guardrail enforced in this plan; the same guardrail is enforced in Plan 06 (compose preview) and Plan 07 (WearVideoClient). |
</threat_model>

<verification>
- `src/components/wywt/VideoCaptureView.tsx` exists with `'use client'` directive, exports `VideoCaptureView` + `VideoCaptureViewProps`
- File does NOT contain `navigator.mediaDevices.getUserMedia` (stream-as-prop guarantee)
- `playsInline` present on `<video>` (≥1)
- `new MediaRecorder` constructed (=1) + `setTimeout(() => recorder.stop(), 3000)` (=1) + `extractPosterBlob` referenced (≥2)
- `src/app/globals.css` has `ring-fill` ≥2 + `prefers-reduced-motion` ≥1
- `npm run build` exits 0
- `npx vitest run tests/components/wywt/VideoCaptureView.test.tsx tests/unit/videoCapture.test.ts` exits 0 with `7 passed`
</verification>

<success_criteria>
- VID-02 satisfied (code-level): 3.0s setTimeout auto-stop; progress ring animates over 3s
- VID-03 satisfied (code-level): cancel during recording resets to pre-recording state without emitting blob
- T-77-04 mitigated (architectural): VideoCaptureView never calls getUserMedia (stream is parent-supplied)
- Build green; 7/7 tests pass
- Wave 2 unblock: ComposeStep (Plan 06) can mount `<VideoCaptureView stream={cameraStream} preferredMimeType={...} onVideoReady={handleVideoReady} ...>`
</success_criteria>

<output>
After completion, create `.planning/phases/77-video-capture-display-ui/77-05-SUMMARY.md` per the GSD summary template. Include: (a) VideoCaptureView prop interface signature + handler list, (b) globals.css additions (keyframes + reduced-motion override), (c) test results (7 passing), (d) confirmation that file contains zero `getUserMedia` references (architectural guarantee for T-77-04).
</output>
