---
phase: 77
plan: 03
type: execute
wave: 1
depends_on:
  - 77-01
files_modified:
  - src/data/wearEvents.ts
  - tests/unit/dalMediaColumns.test.ts
autonomous: true
requirements:
  - WR-02
tags: [dal, foundation, wr-02, video-display-prerequisite]
must_haves:
  truths:
    - "Per WR-02 (from Phase 76 review, folded into Phase 77 per planning_context): the 4 explicit DAL readers in `src/data/wearEvents.ts` — `getWearEventByIdForViewer`, `getWearEventsForViewer` non-owner branch, `getWearRailForViewer`, `getActiveWearsForUser` — each include `mediaType`, `mediaPath`, `posterPath` in their explicit `.select({...})` lists. Returned rows surface video metadata to every render surface (Wave 3) that consumes these readers."
    - "Per 77-RESEARCH.md §Surface Inventory: `getAllWearEventsByUser` (used by `getWearEventsForViewer`'s owner-bypass branch per Open Question #5) is verified to either select-all (Drizzle `.select()` with no args returns every column including the new ones) or has the three columns added explicitly. The owner profile view sees video metadata identically to the cross-user view."
    - "Per CONTEXT.md deferred list: `getPublicWearPicsForWatch` is NOT touched in this plan. It hard-filters `isNotNull(wearEvents.photoUrl)` and the watch-detail carousel is out of scope per SEED-020 D-06 + CONTEXT.md deferred items."
    - "Per `tests/unit/dalMediaColumns.test.ts` (Wave 0 stub): replaced with 4 real assertions — one per modified reader — confirming each returns objects whose shape includes `mediaType`, `mediaPath`, `posterPath` even when null. The test uses the existing Drizzle mock pattern from `tests/unit/wearRail.test.ts` (the analog per 77-PATTERNS.md)."
    - "Per durable memory `project_baseline_not_green_build_is_gate`: `npm run build` exits 0 — TypeScript inference picks up the new columns from the Drizzle-inferred row type (Phase 76 Plan 01 added the columns to the schema). Every downstream call site (5 in `src/app/`) sees a wider union with the additive optional-by-default columns and does not break."
    - "Per VID-15 regression invariant: `photoUrl` column SELECT remains present in every modified reader; the photo branch in every consumer continues to receive `photoUrl` exactly as before. The new `mediaType`/`mediaPath`/`posterPath` fields are returned ALONGSIDE `photoUrl`, never replacing it."
  artifacts:
    - path: src/data/wearEvents.ts
      provides: "4 readers widened to SELECT mediaType, mediaPath, posterPath in addition to existing fields"
      contains: "mediaType: wearEvents.mediaType"
    - path: tests/unit/dalMediaColumns.test.ts
      provides: "4 real Vitest cases — one per modified reader — confirming WR-02 column inclusion"
      contains: "mediaType"
  key_links:
    - from: "src/components/wear/WearCard (Plan 07)"
      to: "wear event row.mediaType from getWearEventByIdForViewer / WearsLane reader"
      via: "discriminator on mediaType === 'video'"
      pattern: "mediaType.*video"
    - from: "src/components/home/WywtTile (Plan 08)"
      to: "WywtTile.mediaType + WywtTile.posterPath from getWearRailForViewer"
      via: "render branch"
      pattern: "tile.mediaType"
    - from: "src/app/wear/[wearEventId]/page.tsx (Plan 07)"
      to: "wear.mediaType + wear.mediaPath + wear.posterPath from getWearEventByIdForViewer"
      via: "signed-URL minting branch"
      pattern: "wear.mediaPath"
---

<objective>
Widen 4 DAL reader functions in `src/data/wearEvents.ts` to include `mediaType`, `mediaPath`, `posterPath` in their explicit `.select({...})` lists. This is the WR-02 fold-in from the Phase 76 review (per planning_context) — until these columns surface in returned rows, every Wave 3 render surface is blocked from branching on `mediaType === 'video'`.

Purpose: hard Wave 1 prerequisite for the Wave 3 display pipeline. The DB columns were added in Phase 76 Plan 01 (`media_type`, `media_path`, `poster_path`); without this widening, every consumer reads via the `WywtTile` / wear-event type that has the new fields (from Plan 02) but the DAL never populates them — meaning every render branch on `mediaType === 'video'` is false by accident.

Output:
- `getWearEventByIdForViewer`, `getWearEventsForViewer` (non-owner branch), `getWearRailForViewer`, `getActiveWearsForUser` each return objects with `mediaType`/`mediaPath`/`posterPath`
- `getAllWearEventsByUser` (owner-bypass path) verified to also return the 3 fields (via select-all OR explicit addition)
- `tests/unit/dalMediaColumns.test.ts` upgraded from Wave 0 todo stub to 4 passing assertions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/76-video-schema-storage-paths-server-action/76-01-PLAN.md
@.planning/phases/77-video-capture-display-ui/77-CONTEXT.md
@.planning/phases/77-video-capture-display-ui/77-RESEARCH.md
@.planning/phases/77-video-capture-display-ui/77-PATTERNS.md
@.planning/phases/77-video-capture-display-ui/77-VALIDATION.md

<interfaces>
<!-- Canonical DAL contracts. Executor reads each function's existing select shape before extending. -->

From src/db/schema (post-Phase-76 Plan 01):
- `wearEvents` table now has `mediaType` (enum `mediaTypeEnum`, default 'photo', NOT NULL), `mediaPath` (TEXT nullable), `posterPath` (TEXT nullable) — confirmed by 76-01-PLAN.md
- Drizzle's inferred row type for `wearEvents` includes these three columns
- The CHECK constraint `wear_events_video_paths_required` enforces video rows have both mediaPath + posterPath non-null

From src/data/wearEvents.ts — current SELECT shape (analog template; lines 297-319 of getWearEventByIdForViewer, partial excerpt per 77-PATTERNS.md §src/data/wearEvents.ts):
```typescript
.select({
  id: wearEvents.id,
  userId: wearEvents.userId,
  watchId: wearEvents.watchId,
  wornDate: wearEvents.wornDate,
  note: wearEvents.note,
  photoUrl: wearEvents.photoUrl,
  visibility: wearEvents.visibility,
  createdAt: wearEvents.createdAt,
  // ... JOINed fields ...
})
```

Three columns to ADD to every explicit `.select({...})` call:
```typescript
mediaType: wearEvents.mediaType,
mediaPath: wearEvents.mediaPath,
posterPath: wearEvents.posterPath,
```

Functions to modify (per 77-RESEARCH.md §Surface Inventory + 77-PATTERNS.md §src/data/wearEvents.ts):
1. `getWearEventByIdForViewer` — single-row reader for /wear/[id] page
2. `getWearEventsForViewer` — non-owner branch (the owner branch delegates to `getAllWearEventsByUser`)
3. `getWearRailForViewer` — home rail (returns `WywtTile[]` — the new `mediaType`/`posterPath` fields land directly on the returned tile)
4. `getActiveWearsForUser` — /wears/[username] stories lane reader

Functions to verify (NOT modify if already select-all):
- `getAllWearEventsByUser` (per 77-RESEARCH.md Open Question #5) — currently uses `.select()` without arguments OR an explicit list. If select-all, no change needed (Drizzle includes every column); if explicit list, ADD the 3 columns.

Functions to NOT touch (deferred per CONTEXT.md / SEED-020 D-06):
- `getPublicWearPicsForWatch` — hard-filters `isNotNull(wearEvents.photoUrl)`; watch-detail carousel is out of scope

From tests/unit/wearRail.test.ts (analog for dalMediaColumns.test.ts per 77-PATTERNS.md §tests/unit/dalMediaColumns.test.ts):
- Uses `vi.mock('@/db', ...)` to stub Drizzle's `db` chain
- Asserts mock `.select(...)` call arguments include the expected column references
- Mock pattern: builder chain returns an array of row objects with the expected shape

The dalMediaColumns test can use one of two strategies (executor picks):
(a) Mock the Drizzle chain and assert each reader returns objects with the new fields present (even if null)
(b) Mock the Drizzle chain and assert the `.select(...)` call argument includes `mediaType: wearEvents.mediaType` (key-reference identity)

Recommended: strategy (a) — assert shape of returned data. Less brittle to refactors of the select expression order.
</interfaces>

@src/data/wearEvents.ts
@tests/unit/dalMediaColumns.test.ts
@tests/unit/wearRail.test.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mediaType/mediaPath/posterPath to 4 explicit SELECTs in src/data/wearEvents.ts</name>
  <files>src/data/wearEvents.ts</files>
  <read_first>
    - src/data/wearEvents.ts (FULL file — read once; locate every `.select({...})` call signature; identify which are `getWearEventByIdForViewer`, `getWearEventsForViewer`, `getWearRailForViewer`, `getActiveWearsForUser`, `getAllWearEventsByUser`, `getPublicWearPicsForWatch`)
    - src/db/schema.ts (confirm `wearEvents.mediaType` / `.mediaPath` / `.posterPath` are exported and reachable via the existing `wearEvents` import in src/data/wearEvents.ts:1-7)
    - 77-PATTERNS.md §`src/data/wearEvents.ts` (lines 52-85 of 77-PATTERNS.md — the exact 3 columns to add and the "no WHERE/JOIN/ORDER touched" guarantee)
    - 77-RESEARCH.md §Surface Inventory (the 4-row table — which readers to modify, which to skip)
    - 77-CONTEXT.md §deferred — confirm `getPublicWearPicsForWatch` is out of scope (NOT touched)
  </read_first>
  <behavior>
    - `getWearEventByIdForViewer`'s explicit `.select({...})` gains 3 fields: `mediaType: wearEvents.mediaType, mediaPath: wearEvents.mediaPath, posterPath: wearEvents.posterPath`
    - `getWearEventsForViewer` non-owner branch's explicit `.select({...})` gains the same 3 fields
    - `getWearRailForViewer`'s explicit `.select({...})` gains the same 3 fields — the returned `WywtTile[]` now carries `mediaType` + `posterPath` (per Plan 02 type extension)
    - `getActiveWearsForUser`'s explicit `.select({...})` gains the same 3 fields
    - `getAllWearEventsByUser`: if it uses `.select()` (no args = select-all), NO CHANGE — Drizzle auto-includes the new columns; if it uses `.select({...})` explicit, ADD the 3 fields
    - `getPublicWearPicsForWatch`: NOT modified (deferred per CONTEXT.md)
    - WHERE clauses, JOINs, and ORDER BY are NEVER touched in any function
    - `photoUrl: wearEvents.photoUrl` remains present in every modified reader (VID-15 regression — photo branch in consumers continues to read `photoUrl`)
  </behavior>
  <action>
    Edit `src/data/wearEvents.ts`. For each of the 4 functions listed in `<behavior>`, locate the `.select({...})` call (use grep on the function name first, then read the surrounding 30-40 lines to find the select expression). For each, ADD three new lines to the select object's column list, grouped IMMEDIATELY after the existing `photoUrl: wearEvents.photoUrl` line (preserve the "media columns are grouped" reading convention): `mediaType: wearEvents.mediaType,`, `mediaPath: wearEvents.mediaPath,`, `posterPath: wearEvents.posterPath,`. Each line preceded by a single comment line `// Phase 77 WR-02: video metadata columns (additive — photo readers stay unchanged)` ONLY on the FIRST occurrence per function (one comment per function, not per column). Do NOT change the existing `photoUrl: wearEvents.photoUrl` line — that field is REQUIRED for the photo branch. Do NOT change column ordering of pre-existing fields. Do NOT touch WHERE, INNER JOIN, LEFT JOIN, ORDER BY, LIMIT, or any other clause. For `getAllWearEventsByUser`: locate the function, identify whether it uses `.select()` (no args) or `.select({...})` (explicit). If no args, leave unchanged (Drizzle returns all columns including the new ones) and leave a comment `// Phase 77 WR-02: select-all returns mediaType/mediaPath/posterPath automatically (added in Phase 76 Plan 01 schema)` above the `.select()` line. If explicit, ADD the same 3 columns. For `getPublicWearPicsForWatch`: NOT touched at all (NO grep, NO edit) — deferred per CONTEXT.md §deferred bullet 6. After all 4-5 edits, every downstream TypeScript consumer in `src/app/` and `src/components/` should compile unchanged because the return-type widening is additive (every existing field remains; 3 new fields are appended). Per durable memory `project_baseline_not_green_build_is_gate`: confirm `npm run build` exits 0 AND `npx vitest run` introduces no new failures BEFORE proceeding to Task 2.
  </action>
  <verify>
    <automated>grep -c "mediaType: wearEvents.mediaType" /Users/tylerwaneka/Documents/horlo/src/data/wearEvents.ts | awk '{print ($1 >= 4)}' | grep -q 1 && grep -c "mediaPath: wearEvents.mediaPath" /Users/tylerwaneka/Documents/horlo/src/data/wearEvents.ts | awk '{print ($1 >= 4)}' | grep -q 1 && grep -c "posterPath: wearEvents.posterPath" /Users/tylerwaneka/Documents/horlo/src/data/wearEvents.ts | awk '{print ($1 >= 4)}' | grep -q 1 && grep -c "photoUrl: wearEvents.photoUrl" /Users/tylerwaneka/Documents/horlo/src/data/wearEvents.ts | awk '{print ($1 >= 4)}' | grep -q 1 && grep -c "export async function getPublicWearPicsForWatch" /Users/tylerwaneka/Documents/horlo/src/data/wearEvents.ts | grep -q "^1$" && grep -c "Phase 77 WR-02" /Users/tylerwaneka/Documents/horlo/src/data/wearEvents.ts | awk '{print ($1 >= 4)}' | grep -q 1 && npm run build 2>&1 | grep -qE "Compiled successfully|✓ Compiled"</automated>
  </verify>
  <acceptance_criteria>
    - `mediaType: wearEvents.mediaType` appears at least 4 times (one per modified reader; possibly 5 if `getAllWearEventsByUser` was explicit)
    - Same count guarantee for `mediaPath` and `posterPath`
    - `photoUrl: wearEvents.photoUrl` count UNCHANGED (preserved in every existing reader — VID-15 regression guard)
    - `getPublicWearPicsForWatch` declaration still present exactly once (untouched)
    - `// Phase 77 WR-02` comment present at least 4 times
    - `npm run build` exits 0 — every downstream consumer compiles with the widened return type
  </acceptance_criteria>
  <done>
    - All 4 (or 5) explicit selects widened with the 3 new columns
    - photoUrl SELECT preserved (VID-15)
    - getPublicWearPicsForWatch untouched (deferred)
    - Build green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade tests/unit/dalMediaColumns.test.ts from Wave 0 stub to 4 passing assertions</name>
  <files>tests/unit/dalMediaColumns.test.ts</files>
  <read_first>
    - tests/unit/dalMediaColumns.test.ts (Wave 0 stub form — confirm the 4 `it.todo` placeholders from Plan 01)
    - tests/unit/wearRail.test.ts (analog per 77-PATTERNS.md §tests/unit/dalMediaColumns.test.ts — extract the Drizzle-mock chain pattern)
    - src/data/wearEvents.ts (post-Task-1 form — confirm the 4 readers' signatures and what each returns)
    - 77-PATTERNS.md §tests/unit/dalMediaColumns.test.ts (lines 1256-1262 — the test contract)
  </read_first>
  <behavior>
    - 4 real `it(...)` cases (replaces all 4 `it.todo` from Plan 01)
    - One case per affected reader: `getWearEventByIdForViewer`, `getWearEventsForViewer`, `getWearRailForViewer`, `getActiveWearsForUser`
    - Each case mocks the Drizzle `db` chain to return a fixed row with all three video fields populated; calls the reader; asserts the returned row exposes `mediaType`, `mediaPath`, `posterPath` keys (even when null)
    - Tests do NOT exercise the real DB (jsdom-safe, fully mocked); they exercise the FUNCTION'S COLUMN-PROPAGATION CONTRACT — that the field reaches the caller
    - Tests preserve the `// Wave 0 RED stub` first-line marker; add `// Plan 03: upgraded from todo to assertions (WR-02)` below it
  </behavior>
  <action>
    Edit `tests/unit/dalMediaColumns.test.ts`. Preserve the `// Wave 0 RED stub — Phase 77 / 77-01-PLAN.md` marker line. Add a second comment `// Plan 03: upgraded from todo to assertions (WR-02)` below it. Replace the 4 `it.todo` entries with 4 real `it(...)` cases. Use the Drizzle-mock pattern from `tests/unit/wearRail.test.ts`: `vi.mock('@/db', () => ({ db: { select: vi.fn().mockReturnThis(), from: vi.fn().mockReturnThis(), innerJoin: vi.fn().mockReturnThis(), leftJoin: vi.fn().mockReturnThis(), where: vi.fn().mockReturnThis(), orderBy: vi.fn().mockReturnThis(), limit: vi.fn().mockReturnThis(), then: ... } }))` — or whatever shape `wearRail.test.ts` uses; the executor should COPY that mock structure verbatim (it's the canonical analog per 77-PATTERNS.md). Adapt the `then` resolver to return a fixed array containing one row with `mediaType: 'video' as const, mediaPath: '${userId}/${wearEventId}.mp4', posterPath: '${userId}/${wearEventId}-poster.jpg'` (alongside all the other fields the reader is documented to return). For each case: (1) `it('getWearEventByIdForViewer returns mediaType/mediaPath/posterPath (WR-02)', ...)`; (2) `it('getWearEventsForViewer (non-owner) returns mediaType/mediaPath/posterPath (WR-02)', ...)`; (3) `it('getWearRailForViewer tiles include mediaType + posterPath (WR-02)', ...)`; (4) `it('getActiveWearsForUser returns mediaType/mediaPath/posterPath (WR-02)', ...)`. For each, after invoking the function, assert: `expect(result).toMatchObject(expect.objectContaining({ mediaType: 'video', mediaPath: ..., posterPath: ... }))` (or for list-returning readers, `expect(result[0]).toMatchObject(...)` or `expect(result.tiles[0]).toMatchObject(...)` depending on shape). After the edit, `npx vitest run tests/unit/dalMediaColumns.test.ts` exits 0 with `4 passed`. Per durable memory `project_baseline_not_green_build_is_gate`: targeted vitest is the per-task signal; `npm run build` is the gate. Importantly: do NOT delete the existing `// Wave 0 RED stub` marker — it's a planner-locatable origin for future agents. Do NOT add new test files; only modify the existing `dalMediaColumns.test.ts` stub. Per CLAUDE.md (Vitest is the canonical test framework): do not introduce other test runners.
  </action>
  <verify>
    <automated>grep -c "it(" /Users/tylerwaneka/Documents/horlo/tests/unit/dalMediaColumns.test.ts | grep -q "^4$" && grep -c "it.todo" /Users/tylerwaneka/Documents/horlo/tests/unit/dalMediaColumns.test.ts | grep -q "^0$" && grep -c "Wave 0 RED stub" /Users/tylerwaneka/Documents/horlo/tests/unit/dalMediaColumns.test.ts | grep -q "^1$" && grep -c "Plan 03" /Users/tylerwaneka/Documents/horlo/tests/unit/dalMediaColumns.test.ts | grep -q "^1$" && grep -c "getWearEventByIdForViewer" /Users/tylerwaneka/Documents/horlo/tests/unit/dalMediaColumns.test.ts | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "getWearRailForViewer" /Users/tylerwaneka/Documents/horlo/tests/unit/dalMediaColumns.test.ts | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "getActiveWearsForUser" /Users/tylerwaneka/Documents/horlo/tests/unit/dalMediaColumns.test.ts | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "mediaType" /Users/tylerwaneka/Documents/horlo/tests/unit/dalMediaColumns.test.ts | awk '{print ($1 >= 4)}' | grep -q 1 && npx vitest run tests/unit/dalMediaColumns.test.ts 2>&1 | tail -20 | grep -qE "4 passed|Test Files\s+1 passed"</automated>
  </verify>
  <acceptance_criteria>
    - 4 `it(...)` cases (0 `it.todo`)
    - Each case names one of the 4 affected readers
    - Each case asserts the returned row/tile has `mediaType`, `mediaPath`, `posterPath` keys
    - `// Wave 0 RED stub` and `// Plan 03:` markers both present
    - `npx vitest run tests/unit/dalMediaColumns.test.ts` exits 0 with `4 passed`
  </acceptance_criteria>
  <done>
    - 4/4 cases pass
    - WR-02 covered by automated test (Nyquist gate progresses)
    - Original Wave 0 marker preserved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAL → Server Components / Client Components | Readers expose `mediaType`/`mediaPath`/`posterPath` to all consumers — every render branch must trust the DAL's mediaType discriminator (server-controlled column) |
| `getPublicWearPicsForWatch` ↔ scope deferral | Hard-filter on `isNotNull(photoUrl)` is intentionally preserved — surfacing wear videos on `/w/[ref]` is deferred (SEED-020 D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-wr02-photo-regression | Tampering (HIGH — VID-15 — accidentally removing photoUrl from a SELECT breaks the photo render path) | additive SELECT extension | mitigate | Task 1 explicitly preserves `photoUrl: wearEvents.photoUrl` in every modified reader (verify grep count unchanged). Build green is the safety net. Acceptance criterion requires `photoUrl: wearEvents.photoUrl` count >= 4 (preserved). |
| T-77-wr02-where-drift | Tampering (LOW — accidentally narrowing a WHERE clause excludes video rows from the rail) | WHERE clause preservation | mitigate | Task 1 explicitly forbids touching WHERE/JOIN/ORDER BY. The instruction is "the WHERE clause, JOIN structure, and ordering are not touched." Verification relies on the build + targeted test green (any drift would surface in the dalMediaColumns test or wearRail integration test). |
| T-77-wr02-public-pics-leak | Information Disclosure (DEFERRED — surfacing video wears on /w/[ref] without owner-consent gating could leak unintended content) | `getPublicWearPicsForWatch` scope | accept | Per CONTEXT.md deferred bullet 6 + SEED-020 D-06: watch detail carousel stays photo-only in v8.3. Plan explicitly NOT touching this reader. Future plan (v8.4+ scope) will add owner-consent + visibility-tier gating before widening this reader. |
</threat_model>

<verification>
- `grep -c "mediaType: wearEvents.mediaType" src/data/wearEvents.ts` returns >= 4
- `grep -c "mediaPath: wearEvents.mediaPath" src/data/wearEvents.ts` returns >= 4
- `grep -c "posterPath: wearEvents.posterPath" src/data/wearEvents.ts` returns >= 4
- `grep -c "photoUrl: wearEvents.photoUrl" src/data/wearEvents.ts` count UNCHANGED from pre-plan (VID-15)
- `grep -c "export async function getPublicWearPicsForWatch" src/data/wearEvents.ts` returns 1 (untouched)
- `npm run build` exits 0
- `npx vitest run tests/unit/dalMediaColumns.test.ts` exits 0 with `4 passed`
</verification>

<success_criteria>
- WR-02 satisfied: 4 DAL readers expose `mediaType`/`mediaPath`/`posterPath` to consumers
- VID-15 invariant preserved: every reader still selects `photoUrl`; photo branch in every consumer continues to work
- `getPublicWearPicsForWatch` untouched (deferred per SEED-020 D-06)
- Wave 1 unblock complete — Wave 3 display surfaces can now discriminate on `mediaType`
- Build green
</success_criteria>

<output>
After completion, create `.planning/phases/77-video-capture-display-ui/77-03-SUMMARY.md` per the GSD summary template. Include: (a) which 4 (or 5) readers were modified — function-name list, (b) whether `getAllWearEventsByUser` was select-all or required explicit change, (c) confirmation `photoUrl` SELECT count unchanged (VID-15 evidence), (d) test result `4 passed`.
</output>
