---
phase: 66
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - src/app/api/extract-watch/route.ts
  - tests/api/extract-watch.test.ts
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-03, EXTR-08]
tags: [next16, route-handler, zod-discriminated-union, llm, structured-extraction, regression-baseline]

must_haves:
  truths:
    - "POST /api/extract-watch accepts a Zod-validated discriminated body `{ mode: 'url', url } | { mode: 'structured', brand, model, reference?, year? }` per D-07 (Zod discriminated union) and D-08 (schema colocated in route.ts top)"
    - "The URL branch (mode: 'url') preserves all 18 observable behaviors pinned by tests/api/extract-watch.test.ts:51-269 — zero regression (success criterion 2)"
    - "Brand AND model are required for structured mode; omitting either returns HTTP 400 with a Zod-derived error message (EXTR-03 + success criterion 3)"
    - "Structured-mode requests dispatch to extractFromStructuredInput (from llm-structured.ts) and NEVER call fetchAndExtract / cheerio — verified by the Plan 02 integration test asserting vi.spyOn(cheerio, 'load') is not called (EXTR-02 + success criterion 5)"
    - "Structured-mode catalog upsert calls upsertCatalogFromUserInput, NOT upsertCatalogFromExtractedUrl — per D-EXTR-08 (Pitfall 5 mitigation: prevents LLM-inferred values from overwriting truthful nulls); integration test asserts the distinction (success criterion 4)"
    - "Structured branch runs full URL-branch parity chain per D-03: enrichTasteAttributes({ source: 'structured-input', spec, photoSourcePath: null }) → updateCatalogTaste → revalidateTag('explore', 'max') with the two-arg form per D-04"
    - "The 5-category error taxonomy (host-403, structured-data-missing, LLM-timeout, quota-exceeded, generic-network) is reused unchanged per D-05; Anthropic SDK errors thrown by the structured branch funnel through the existing categorizeExtractionError"
    - "structured-data-missing user-facing copy is mode-branched per D-06: URL-mode copy unchanged; new structured-mode copy ('Couldn\\'t find specs for that watch. Try adding a reference number, or enter manually.') selected by the request mode"
    - "Every JSON response (success AND error) includes a `mode: 'url' | 'structured'` field so Phase 69's <ExtractErrorCard> can branch copy without client-side state tracking (D-06 mode-threading)"
    - "Structured branch applies the same post-extract gate as URL branch: when both brand AND model come back empty/whitespace, emit category 'structured-data-missing' at HTTP 422 (Discretion item: empty-output gate mirror)"
  artifacts:
    - path: "src/app/api/extract-watch/route.ts"
      provides: "POST handler with discriminated body dispatch — URL branch preserved verbatim, structured branch added"
      contains: "z.discriminatedUnion"
      exports: ["POST"]
    - path: "tests/api/extract-watch.test.ts"
      provides: "Integration coverage extended with structured-mode describe block + cheerio short-circuit assertion + DAL-function-selection assertion + mode-in-error-response assertion"
      contains: "structured mode"
  key_links:
    - from: "src/app/api/extract-watch/route.ts"
      to: "src/lib/extractors/llm-structured.ts"
      via: "import { extractFromStructuredInput } from '@/lib/extractors/llm-structured'"
      pattern: "extractFromStructuredInput"
    - from: "src/app/api/extract-watch/route.ts"
      to: "src/data/catalog.ts"
      via: "catalogDAL.upsertCatalogFromUserInput on structured branch (catalogDAL.upsertCatalogFromExtractedUrl on URL branch — unchanged)"
      pattern: "upsertCatalogFromUserInput"
    - from: "src/app/api/extract-watch/route.ts"
      to: "@/lib/taste/enricher"
      via: "enrichTasteAttributes({ source: 'structured-input', ... }) on structured branch"
      pattern: "source: 'structured-input'"
    - from: "tests/api/extract-watch.test.ts"
      to: "cheerio (spied)"
      via: "vi.spyOn(cheerio, 'load') — assert NOT called when mode is structured"
      pattern: "loadSpy.*not\\.toHaveBeenCalled\\|cheerio.*not"
---

<objective>
Extend `POST /api/extract-watch` with a Zod-discriminated body, route the URL branch verbatim (zero regression), and add a new structured branch that calls `extractFromStructuredInput` (from Plan 01), upserts via `upsertCatalogFromUserInput` (NOT the URL-branch's `upsertCatalogFromExtractedUrl`), runs the full Phase 19.1 taste-enrichment parity chain with `source: 'structured-input'`, fires `revalidateTag('explore', 'max')`, and returns an `ExtractedWatchData`-shaped envelope. Thread `mode` through every success AND error response body so Phase 69's `<ExtractErrorCard>` has a single source of truth for D-06 copy branching. Extend the integration test fixture with a `mode: 'structured'` describe block covering EXTR-01, EXTR-02 (cheerio spy), EXTR-03, EXTR-08 (catalog DAL spy), and the D-06 mode-in-error assertion.

Purpose: This is the entirety of Phase 66's user-visible behavior. Plan 01 delivered the LLM-call machinery in isolation; Plan 02 wires it into the route + pins the contract with integration tests. The URL branch is the highest-risk surface — 18 properties pinned by the existing fixture (RESEARCH §URL-Branch Behavior Contract). The structured branch is greenfield and adopts strict tool-use safely (D-02 chose structured-only scope).

Output: A 4-class artifact set — (1) the extended route handler with discriminated dispatch + mode threading; (2) the extended integration test with the structured-mode block; (3) the mode-branched `structured-data-missing` copy table per D-06; (4) full zero-regression preservation of the URL branch (verified by the pre-existing test fixture continuing to pass with at most additive `mode: 'url'` field assertions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-api-route-extension/66-CONTEXT.md
@.planning/phases/66-api-route-extension/66-RESEARCH.md
@.planning/phases/66-api-route-extension/66-PATTERNS.md
@.planning/phases/66-api-route-extension/66-VALIDATION.md
@.planning/phases/66-api-route-extension/66-01-SUMMARY.md

@src/app/api/extract-watch/route.ts
@tests/api/extract-watch.test.ts
@src/lib/extractors/llm-structured.ts
@src/data/catalog.ts

<interfaces>
<!-- Extracted from codebase + Plan 01 outputs. -->

From src/lib/extractors/llm-structured.ts (CREATED in Plan 01):
```typescript
export interface StructuredExtractionInput {
  brand: string
  model: string
  reference?: string
  year?: number
}
export async function extractFromStructuredInput(
  input: StructuredExtractionInput,
): Promise<ExtractedWatchData>
// Throws: 'ANTHROPIC_API_KEY not configured' / 'LLM tool_use block missing ...' / SDK errors
```

From src/lib/extractors/types.ts:
```typescript
export interface ExtractedWatchData {
  brand?: string; model?: string; reference?: string
  movement?: MovementType; complications?: string[]; isChronometer?: boolean
  caseSizeMm?: number; lugToLugMm?: number; waterResistanceM?: number
  strapType?: string; crystalType?: string; dialColor?: string
  styleTags?: string[]; designTraits?: string[]; marketPrice?: number; imageUrl?: string
}
```

From src/lib/taste/types.ts (EXTENDED in Plan 01):
```typescript
export type EnrichmentSource = 'manual' | 'url-extract' | 'backfill' | 'structured-input'
```

From src/data/catalog.ts (lines 138, 178 — VERIFIED via grep):
```typescript
// EXTR-08 TARGET — call this on structured branch:
export async function upsertCatalogFromUserInput(
  input: { brand: string; model: string; reference: string | null }
): Promise<string | null>

// URL-branch only — DO NOT call from structured:
export async function upsertCatalogFromExtractedUrl(
  input: { brand: string; model: string; reference: string | null; /* +14 spec fields incl imageSourceUrl */ }
): Promise<string | null>
```

From src/lib/taste/enricher.ts (URL branch callsite shape, route.ts:194-212):
```typescript
const taste = await enrichTasteAttributes({
  catalogId,
  source: 'url-extract' | 'structured-input',   // ← extended union
  spec: { brand, model, reference, movement, caseSizeMm, lugToLugMm, waterResistanceM,
          crystalType, dialColor, isChronometer, productionYear: null, complications },
  photoSourcePath: null,
})
```

From tests/api/extract-watch.test.ts (existing mock surface, lines 1-49):
```typescript
vi.mock('@/lib/auth', () => ({ UnauthorizedError, getCurrentUser: vi.fn().mockResolvedValue({ id, email }) }))
vi.mock('@/lib/extractors', () => ({ fetchAndExtract: (...) => mockFetchAndExtract(...) }))
vi.mock('@/lib/ssrf', () => ({ SsrfError: class extends Error {} }))
vi.mock('@/data/catalog', () => ({
  upsertCatalogFromExtractedUrl: vi.fn().mockResolvedValue('cat-123'),
  updateCatalogTaste: vi.fn().mockResolvedValue(undefined),
  // ← Plan 02 ADDS: upsertCatalogFromUserInput: vi.fn().mockResolvedValue('cat-456'),
}))
vi.mock('@/lib/taste/enricher', () => ({ enrichTasteAttributes: vi.fn().mockResolvedValue(null) }))
// ← Plan 02 ADDS: vi.mock('@/lib/extractors/llm-structured', () => ({ extractFromStructuredInput: vi.fn() }))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Zod discriminated-union schema + mode-branched CATEGORY_COPY + mode threading to route.ts</name>
  <files>src/app/api/extract-watch/route.ts</files>
  <read_first>
    - src/app/api/extract-watch/route.ts (CURRENT STATE — 311 LOC; the file is the analog for itself per PATTERNS.md §C1-C8)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §C1 (auth-first gate — PRESERVE VERBATIM, lines 80-90)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §C2 (current ad-hoc body parse — TO BE REPLACED by Zod discriminated union; hard constraint: existing tests pin exact strings `'URL is required'`, `'Invalid URL format'`, `'Only HTTP/HTTPS URLs are supported'`)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §C5 (CATEGORY_COPY + CATEGORY_HTTP_STATUS tables; D-06 unlocks ONE row for mode-branching)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §C7 (catch block — all error responses MUST carry mode)
    - .planning/phases/66-api-route-extension/66-RESEARCH.md §Pattern 2 (Zod v4 discriminated union exact syntax — `.issues` not `.errors`)
    - .planning/phases/66-api-route-extension/66-RESEARCH.md §URL-Branch Behavior Contract (18-property contract — every row is a zero-regression constraint)
    - .planning/phases/66-api-route-extension/66-RESEARCH.md §Recommended D-06 Copy + Mode-Threading Mechanism (the two-key `{ url, structured }` shape per category)
    - tests/api/extract-watch.test.ts lines 76-98 (the EXACT error strings the URL-branch test pins — `'URL is required'`, `'Invalid URL format'`, `'Only HTTP/HTTPS URLs are supported'`)
    - AGENTS.md (Next 16 — `revalidateTag('explore', 'max')` two-arg form is mandatory; Pitfall 4)
  </read_first>
  <behavior>
    - Test 1 (regression): existing URL-branch tests in tests/api/extract-watch.test.ts:51-269 continue to pass — the Zod schema's URL-mode parse failure surfaces a 400 response with one of the three locked error strings (planner choice: keep post-Zod manual `new URL()` validation for `'Invalid URL format'` / `'Only HTTP/HTTPS URLs are supported'`; Zod handles only `'URL is required'`-class via `z.string().min(1)`).
    - Test 2 (new — EXTR-01 happy path URL): `POST { mode: 'url', url: 'https://x.com' }` reaches `fetchAndExtract` exactly as today; response carries `mode: 'url'` field.
    - Test 3 (new — EXTR-01 happy path structured): `POST { mode: 'structured', brand: 'Omega', model: 'Speedmaster' }` is accepted by Zod; the structured branch dispatch fires (assertion belongs in Task 2 once `extractFromStructuredInput` is wired).
    - Test 4 (new — EXTR-01 reject missing mode): `POST {}` returns 400.
    - Test 5 (new — EXTR-01 reject invalid mode): `POST { mode: 'oops' }` returns 400.
    - Test 6 (new — EXTR-03): `POST { mode: 'structured', brand: 'Omega' }` (missing model) returns 400; `POST { mode: 'structured', model: 'X' }` (missing brand) returns 400.
    - Test 7 (new — D-06 mode threading): all 5 error categories return `mode: 'url' | 'structured'` in the response body matching the request mode.
  </behavior>
  <action>
    This is the surgical core of Phase 66 at the route layer. Modify `src/app/api/extract-watch/route.ts` in-place. Preserve EVERY URL-branch behavior in RESEARCH §URL-Branch Behavior Contract rows 1-18.

    **Edit 1 — Imports (top of file):** Add `import { z } from 'zod'`. Do NOT yet import `extractFromStructuredInput` (added in Task 2 — keeps this task independently testable for Zod + mode-threading correctness).

    **Edit 2 — Zod schema (after imports, before CATEGORY_COPY):** Add the discriminated-union schema per D-07 + D-08 (colocated). Use the EXACT shape from RESEARCH §Pattern 2 / §Recommended Zod schema:
    ```
    const urlBodySchema = z.object({ mode: z.literal('url'), url: z.string().min(1) })
    const structuredBodySchema = z.object({
      mode: z.literal('structured'),
      brand: z.string().min(1).max(200),
      model: z.string().min(1).max(200),
      reference: z.string().max(200).optional(),
      year: z.number().int().min(1900).max(2100).optional(),
    })
    const extractRequestSchema = z.discriminatedUnion('mode', [urlBodySchema, structuredBodySchema])
    type ExtractRequestBody = z.infer<typeof extractRequestSchema>
    ```
    The `.max(200)` on brand/model/reference is the T-66-02 (LLM prompt injection) mitigation — bounds user-supplied strings concatenated into the LLM user message.

    **Edit 3 — Mode-branched CATEGORY_COPY (replace lines 28-37):** Change the value type from `string` to `{ url: string; structured: string }`. Apply RESEARCH §Recommended D-06 Copy verbatim:
    - `'host-403'.url` = today's copy; `.structured` = same string (unreachable for structured, kept for shape symmetry)
    - `'structured-data-missing'.url` = today's copy ("Couldn't find watch info on this page. Try the original product page or enter manually.")
    - `'structured-data-missing'.structured` = `"Couldn't find specs for that watch. Try adding a reference number, or enter manually."` ← THE D-06 unlock
    - `'LLM-timeout'.url` and `.structured` = same string
    - `'quota-exceeded'.url` and `.structured` = same string
    - `'generic-network'.url` = today's copy; `.structured` = `"Something went wrong looking that up. Try again in a moment."`

    Selection becomes `CATEGORY_COPY[category][mode]`. Update the existing post-extract gate at lines 128-137 and EVERY emission site in the catch block (lines 256-308) to thread mode.

    **Edit 4 — Replace ad-hoc body parse (lines 92-118):** Inside the existing `try` block at line 92, replace the manual `body.url` extraction + `new URL()` + protocol check with:

    1. `const rawBody = await request.json()`.
    2. `const parsed = extractRequestSchema.safeParse(rawBody)`.
    3. On failure: hoist a `let mode: 'url' | 'structured' = 'url'` BEFORE the try (closure-scoped, default `'url'`) for the catch block; on Zod failure return `NextResponse.json({ error: parsed.error.issues[0]?.message ?? 'Invalid request', mode }, { status: 400 })` — per Pitfall 2 use `.issues` (Zod v4), NOT `.errors`.
    4. On success: `mode = parsed.data.mode; const body = parsed.data`.
    5. For URL mode ONLY: KEEP the `new URL()` validation + protocol allow-list AFTER Zod parse, returning the EXACT existing error strings (`'Invalid URL format'`, `'Only HTTP/HTTPS URLs are supported'`) — the test fixture pins these. Zod handles the `'URL is required'` case via `z.string().min(1)` failure with appropriate `.issues[0].message` (override message to `'URL is required'` if Zod's default doesn't match, via `z.string().min(1, 'URL is required')`).

    Hard constraint check: after this edit, the URL-branch tests at `tests/api/extract-watch.test.ts:76-98` MUST still pass with the three exact error strings.

    **Edit 5 — Mode dispatch around the URL pipeline:** Wrap the existing URL pipeline (lines 120-244) in a `if (body.mode === 'url') { ... }` block. The structured branch is added in Task 2 (a stub that returns 501 here would make Task 1 independently testable; cleaner: this task can include the dispatch shell with `if (body.mode === 'structured') { /* TODO Task 2 */ return NextResponse.json({ error: 'Not implemented', mode: 'structured' }, { status: 501 }) }` so the Zod test for `mode: 'structured'` getting accepted doesn't crash). Task 2 replaces the 501 stub with the real branch.

    **Edit 6 — Thread `mode` into every response body:** Every `NextResponse.json(...)` call in the file must include `mode: 'url' | 'structured'` (the closure-scoped variable). Both success envelope (line 236-244) and EVERY error emission (Zod-parse-fail, URL-validation-fail, post-extract-gate, SsrfError, host-403, LLM-timeout, quota-exceeded, generic-network fallthrough).

    **DO NOT change:**
    - The auth gate at lines 80-90 (PRESERVE VERBATIM per §C1 and D-Discretion: auth runs FIRST).
    - The `categorizeExtractionError` function (PRESERVE — D-05 reuses; works for both Anthropic SDK errors and SSRF/fetch errors).
    - `fetchAndExtract(url)` call or any URL-branch logic between lines 120 and 244 (other than wrapping in the dispatch `if (body.mode === 'url')` and adding `mode: 'url'` to response bodies).
    - The `revalidateTag('explore', 'max')` two-arg call shape (Pitfall 4).

    Expected file length change: +60 to +100 LOC (schema + dispatch shell + mode-branched copy table; minor refactor of post-extract-gate emit site).
  </action>
  <verify>
    <automated>
      cd /Users/tylerwaneka/Documents/horlo &amp;&amp; \
      grep -c "z\.discriminatedUnion" src/app/api/extract-watch/route.ts &amp;&amp; \
      grep -c "extractRequestSchema" src/app/api/extract-watch/route.ts &amp;&amp; \
      grep -c "structured: \"Couldn't find specs for that watch" src/app/api/extract-watch/route.ts &amp;&amp; \
      grep -c "mode: 'url'\|mode: 'structured'\|mode: body\.mode\|mode: mode\|mode," src/app/api/extract-watch/route.ts | head -1 &amp;&amp; \
      grep -c "revalidateTag('explore', 'max')" src/app/api/extract-watch/route.ts &amp;&amp; \
      grep -c "\.issues\[0\]" src/app/api/extract-watch/route.ts &amp;&amp; \
      ! grep -q "\.errors\[0\]" src/app/api/extract-watch/route.ts &amp;&amp; \
      npm run test -- tests/api/extract-watch.test.ts --run 2>&amp;1 | tail -30
    </automated>
  </verify>
  <done>
    Zod schema present (`extractRequestSchema` + `z.discriminatedUnion`); CATEGORY_COPY has nested `{ url, structured }` shape with the new structured copy for `structured-data-missing` and `generic-network`; every JSON response includes a `mode` field; Pitfall 2 satisfied (`.issues`, not `.errors`); URL-branch tests at `tests/api/extract-watch.test.ts:51-269` continue to pass (the regression baseline).
  </done>
  <acceptance_criteria>
    - source: `grep -c "z\\.discriminatedUnion" src/app/api/extract-watch/route.ts` returns ≥ `1`
    - source: `grep -c "urlBodySchema\\|structuredBodySchema\\|extractRequestSchema" src/app/api/extract-watch/route.ts` returns ≥ `3`
    - source: `grep -c "Couldn't find specs for that watch" src/app/api/extract-watch/route.ts` returns ≥ `1` (D-06 structured-mode copy unlock)
    - source: `grep -c "Couldn't find watch info on this page" src/app/api/extract-watch/route.ts` returns ≥ `1` (URL-mode copy PRESERVED)
    - source: `grep -c "Something went wrong looking that up" src/app/api/extract-watch/route.ts` returns ≥ `1` (D-06 structured generic-network copy)
    - source: `grep -c "URL is required" src/app/api/extract-watch/route.ts` returns ≥ `1` (locked URL-validation string preserved)
    - source: `grep -c "Invalid URL format" src/app/api/extract-watch/route.ts` returns ≥ `1`
    - source: `grep -c "Only HTTP/HTTPS URLs are supported" src/app/api/extract-watch/route.ts` returns ≥ `1`
    - source: `grep -c "\\.issues\\[0\\]" src/app/api/extract-watch/route.ts` returns ≥ `1` (Zod v4 — Pitfall 2)
    - source: `grep -c "\\.errors\\[0\\]" src/app/api/extract-watch/route.ts` returns `0` (no Zod-3-era code)
    - source: `grep -cE "let mode: 'url' \\| 'structured'|let mode = 'url'" src/app/api/extract-watch/route.ts` returns ≥ `1` (closure-scoped `mode` for catch-block default — see §<action> Edit 4 step 3; ensures Zod-parse-fail path still emits `mode: 'url'`)
    - source: `grep -c "revalidateTag('explore', 'max')" src/app/api/extract-watch/route.ts` returns ≥ `1` (Pitfall 4 — two-arg form preserved)
    - source: `grep -c "mode:" src/app/api/extract-watch/route.ts` returns ≥ `8` (mode threaded into all response paths)
    - behavior: `npm run test -- tests/api/extract-watch.test.ts --run` exits 0 (URL-branch regression baseline holds)
    - behavior: `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add structured-branch dispatch (extractFromStructuredInput + upsertCatalogFromUserInput + parity chain)</name>
  <files>src/app/api/extract-watch/route.ts</files>
  <read_first>
    - src/app/api/extract-watch/route.ts (CURRENT STATE — post-Task-1, with Zod schema + dispatch shell + URL branch intact; the structured branch currently returns a 501 stub)
    - src/lib/extractors/llm-structured.ts (Plan 01 output — the function being called)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §C3 (catalog upsert + observability — the URL branch's let-catalogId / let-catalogIdError envelope IS the analog; SWAP `upsertCatalogFromExtractedUrl` for `upsertCatalogFromUserInput` and the field list narrows from 18 to 3 fields)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §C4 (taste enrichment + revalidateTag — MIRROR with `source: 'structured-input'`)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §C6 (structured-data-missing gate — apply same brand+model empty gate to structured branch output)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §C8 (success envelope — add `mode: 'structured'`; compute `fieldsExtracted: Object.keys(data).filter(k => data[k] !== undefined)`; `source: 'llm'`; `confidence: 'medium'` per RESEARCH §Open Question 2 recommendation)
    - .planning/phases/66-api-route-extension/66-RESEARCH.md §System Architecture Diagram (the structured-branch flow)
    - .planning/phases/66-api-route-extension/66-RESEARCH.md §Open Questions 1, 2, 3 (response shape recommendations)
    - .planning/phases/66-api-route-extension/66-RESEARCH.md §Pitfall 3 (cheerio short-circuit — do not import the orchestrator)
    - .planning/phases/66-api-route-extension/66-RESEARCH.md §Pitfall 5 (EnrichmentSource type — Plan 01 already extended)
    - .planning/phases/66-api-route-extension/66-RESEARCH.md §Pitfall 6 (SDK error subclass detection — categorizeExtractionError is correct)
    - src/data/catalog.ts:138-164 (upsertCatalogFromUserInput exact signature)
    - src/lib/taste/enricher.ts:33-61 (enrichTasteAttributes signature; confirms `source` accepts EnrichmentSource union)
  </read_first>
  <behavior>
    - Test 1 (EXTR-02 — primary): when POST `{ mode: 'structured', brand: 'Omega', model: 'Speedmaster' }` succeeds, `cheerio.load` is NEVER called (asserted via `vi.spyOn(cheerio, 'load')` in Task 3).
    - Test 2 (EXTR-04 dispatch): `extractFromStructuredInput` is called once with `{ brand: 'Omega', model: 'Speedmaster' }` (or with optional `reference`/`year` when supplied).
    - Test 3 (EXTR-08 — primary): on structured success, `upsertCatalogFromUserInput` is called with `{ brand, model, reference }` (3 fields only); `upsertCatalogFromExtractedUrl` is NOT called.
    - Test 4 (D-03 parity): on structured success, `enrichTasteAttributes` is called with `source: 'structured-input'` and `photoSourcePath: null`; on enrichment success `updateCatalogTaste` is called; `revalidateTag('explore', 'max')` is called whenever `catalogId` is truthy.
    - Test 5 (Discretion empty-output gate): when `extractFromStructuredInput` returns `{}` (or only whitespace `brand`/`model`), the route returns HTTP 422 category `'structured-data-missing'` with the STRUCTURED copy variant (per D-06) and `mode: 'structured'`.
    - Test 6 (EXTR-03 response shape): success response has shape `{ success: true, catalogId, catalogIdError, data: ExtractedWatchData, source: 'llm', confidence: 'medium', fieldsExtracted: string[], llmUsed: true, mode: 'structured' }`.
    - Test 7 (D-05 error taxonomy reuse): when `extractFromStructuredInput` throws (any of: missing tool_use block, SDK timeout, SDK 429, missing API key), the error is funneled through the EXISTING `categorizeExtractionError` and produces the appropriate `host-403` / `LLM-timeout` / `quota-exceeded` / `generic-network` response (with `mode: 'structured'`).
  </behavior>
  <action>
    Replace the Task-1 `if (body.mode === 'structured') { return 501 }` stub with the real structured branch. The branch lives inside the SAME `try { ... } catch { ... }` block that wraps the URL branch — both branches share the existing catch's `categorizeExtractionError` dispatch (D-05).

    **Edit 1 — Import (top of file):** Add `import { extractFromStructuredInput } from '@/lib/extractors/llm-structured'` near the existing `import { fetchAndExtract } from '@/lib/extractors'`. DO NOT import from `@/lib/extractors` — that already exists for the URL branch. Critical: the structured branch never reaches `fetchAndExtract` at runtime.

    **Edit 2 — Structured branch body** (replaces the 501 stub from Task 1):

    Apply this exact flow inside `if (body.mode === 'structured') { ... }`:

    1. `const extracted = await extractFromStructuredInput({ brand: body.brand, model: body.model, reference: body.reference, year: body.year })` — pass only the fields that are defined on the discriminated body.

    2. **Post-extract gate** (per §C6 + Discretion empty-output gate): compute `brandPopulated = Boolean(extracted.brand?.trim())` and `modelPopulated = Boolean(extracted.model?.trim())`; if BOTH are false, return:
       ```
       NextResponse.json({
         success: false,
         error: CATEGORY_COPY['structured-data-missing']['structured'],
         category: 'structured-data-missing' as const,
         mode: 'structured' as const,
       }, { status: CATEGORY_HTTP_STATUS['structured-data-missing'] })
       ```

    3. **Catalog upsert with observability** (per §C3 — MIRROR URL branch's let-catalogId/catalogIdError envelope, but call `upsertCatalogFromUserInput`):
       ```
       let catalogId: string | null = null
       let catalogIdError: string | null = null
       try {
         if (extracted.brand && extracted.model) {
           catalogId = await catalogDAL.upsertCatalogFromUserInput({
             brand: extracted.brand,
             model: extracted.model,
             reference: extracted.reference ?? null,
           })
           if (!catalogId) catalogIdError = 'catalog upsert returned null id'
         } else {
           catalogIdError = 'brand/model missing from extraction'
         }
       } catch (err) {
         console.error('[extract-watch] catalog upsert failed (non-fatal):', err)
         catalogIdError = err instanceof Error
           ? `catalog upsert threw: ${err.message.slice(0, 200)}`
           : 'catalog upsert threw'
       }
       ```
       **CRITICAL:** NO `imageSourceUrl`, NO spec fields, NO `imageSourceQuality` — `upsertCatalogFromUserInput`'s signature accepts ONLY `{ brand, model, reference }`. This is the EXTR-08 / Pitfall 5 mitigation: LLM-inferred values must NOT overwrite truthful NULLs at the SQL layer.

    4. **Taste enrichment parity** (per §C4 + D-03 — MIRROR URL branch with `source: 'structured-input'`):
       ```
       if (catalogId && extracted.brand && extracted.model) {
         try {
           const { enrichTasteAttributes } = await import('@/lib/taste/enricher')
           const { updateCatalogTaste } = await import('@/data/catalog')
           const taste = await enrichTasteAttributes({
             catalogId,
             source: 'structured-input',
             spec: {
               brand: extracted.brand,
               model: extracted.model,
               reference: extracted.reference ?? null,
               movement: extracted.movement ?? null,
               caseSizeMm: extracted.caseSizeMm ?? null,
               lugToLugMm: extracted.lugToLugMm ?? null,
               waterResistanceM: extracted.waterResistanceM ?? null,
               crystalType: extracted.crystalType ?? null,
               dialColor: extracted.dialColor ?? null,
               isChronometer: extracted.isChronometer ?? null,
               productionYear: null,
               complications: extracted.complications ?? [],
             },
             photoSourcePath: null,
           })
           if (taste) await updateCatalogTaste(catalogId, taste)
         } catch (err) {
           console.error('[extract-watch] taste enrichment failed (non-fatal):', err)
         }
       }
       ```

    5. **revalidateTag** (per D-04 — fires when catalogId truthy, regardless of enrichment success): `if (catalogId) revalidateTag('explore', 'max')`. EXACT two-arg form (Pitfall 4).

    6. **Success response envelope** (per §C8 + RESEARCH §Open Questions 1 + 2):
       ```
       const fieldsExtracted = Object.keys(extracted).filter(
         (k) => extracted[k as keyof typeof extracted] !== undefined
       )
       return NextResponse.json({
         success: true,
         catalogId,
         catalogIdError,
         data: extracted,
         source: 'llm' as const,
         confidence: 'medium' as const,
         fieldsExtracted,
         llmUsed: true,
         mode: 'structured' as const,
       })
       ```

    **DO NOT change:**
    - The auth gate, URL branch, `categorizeExtractionError`, or any code outside the `if (body.mode === 'structured')` block.
    - The shared catch block at the bottom — it already handles the structured branch's thrown errors (D-05) and Task 1 already added `mode` threading to every error emission.

    **Error path verification (no new code needed):** Anthropic SDK errors thrown from `extractFromStructuredInput` propagate up to the existing catch (the structured branch is inside the same try). `categorizeExtractionError` maps them: AbortError → `LLM-timeout`, `.status === 429` → `quota-exceeded`, fallthrough → `generic-network`. All error responses already carry `mode: 'structured'` from Task 1.

    Expected file length change from Task 1 baseline: +60 to +90 LOC (the structured branch body).
  </action>
  <verify>
    <automated>
      cd /Users/tylerwaneka/Documents/horlo &amp;&amp; \
      grep -c "extractFromStructuredInput" src/app/api/extract-watch/route.ts &amp;&amp; \
      grep -c "upsertCatalogFromUserInput" src/app/api/extract-watch/route.ts &amp;&amp; \
      grep -c "source: 'structured-input'" src/app/api/extract-watch/route.ts &amp;&amp; \
      grep -c "from '@/lib/extractors/llm-structured'" src/app/api/extract-watch/route.ts &amp;&amp; \
      grep -c "if (body\.mode === 'structured')\|body\.mode === 'structured'" src/app/api/extract-watch/route.ts &amp;&amp; \
      ! grep -q "Not implemented" src/app/api/extract-watch/route.ts &amp;&amp; \
      ! grep -q "status: 501" src/app/api/extract-watch/route.ts &amp;&amp; \
      npm run build 2>&amp;1 | tail -10
    </automated>
  </verify>
  <done>
    Structured branch wired with `extractFromStructuredInput` → empty-output gate → `upsertCatalogFromUserInput` (NOT `upsertCatalogFromExtractedUrl`) → enrichment parity with `source: 'structured-input'` → `revalidateTag('explore', 'max')` → success envelope with `mode: 'structured'`. No 501 stubs remain. Build passes.
  </done>
  <acceptance_criteria>
    - source: `grep -c "extractFromStructuredInput" src/app/api/extract-watch/route.ts` returns ≥ `2` (import + call)
    - source: `grep -c "upsertCatalogFromUserInput" src/app/api/extract-watch/route.ts` returns ≥ `1` (structured-branch DAL call — the EXTR-08 critical assertion)
    - source: `grep -c "source: 'structured-input'" src/app/api/extract-watch/route.ts` returns ≥ `1` (D-03 parity discriminant)
    - source: `grep -c "source: 'url-extract'" src/app/api/extract-watch/route.ts` returns ≥ `1` (URL branch UNCHANGED — D-02)
    - source: `grep -c "from '@/lib/extractors/llm-structured'" src/app/api/extract-watch/route.ts` returns `1`
    - source: `grep -c "Not implemented\\|status: 501" src/app/api/extract-watch/route.ts` returns `0` (Task 1 stub removed)
    - source: `grep -c "revalidateTag('explore', 'max')" src/app/api/extract-watch/route.ts` returns ≥ `2` (one per branch — D-04)
    - source: `grep -c "confidence: 'medium'" src/app/api/extract-watch/route.ts` returns ≥ `1` (Open Question 2 resolution)
    - source: `grep -c "llmUsed: true" src/app/api/extract-watch/route.ts` returns ≥ `1`
    - source: `grep -c "fieldsExtracted" src/app/api/extract-watch/route.ts` returns ≥ `2` (URL branch via `...result` spread; structured branch direct compute)
    - source: `grep -cE "from '@/lib/extractors';?$" src/app/api/extract-watch/route.ts` returns `1` (URL barrel — anchored regex avoids false-positive match against the `llm-structured` import path, which is a longer prefix-superset string)
    - behavior: `npm run build` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests/api/extract-watch.test.ts with structured-mode describe block (EXTR-01, EXTR-02, EXTR-03, EXTR-08, D-06)</name>
  <files>tests/api/extract-watch.test.ts</files>
  <read_first>
    - tests/api/extract-watch.test.ts (CURRENT STATE — the URL-branch fixture; lines 1-49 mock setup, lines 51-269 the regression baseline)
    - src/app/api/extract-watch/route.ts (POST-Task-2 state — both branches wired)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §E1 (vitest mock setup — REQUIRED additions: `upsertCatalogFromUserInput` mock, `vi.mock('@/lib/extractors/llm-structured', ...)`, `vi.spyOn(cheerio, 'load')`)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §E2 (new describe block parallel structure)
    - .planning/phases/66-api-route-extension/66-PATTERNS.md §E3 (URL-branch fixture: DO NOT MODIFY existing assertions — only ADD `mode: 'url'` checks if explicitly desired; the URL-branch tests serve as the zero-regression contract)
    - .planning/phases/66-api-route-extension/66-RESEARCH.md §Cheerio Short-Circuit Enforcement (the executable test gate for EXTR-02)
    - .planning/phases/66-api-route-extension/66-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map — every EXTR-* row in this plan)
  </read_first>
  <behavior>
    See §E2 — each `it(...)` block in the new structured describe maps to one EXTR-* assertion. Test runner exits 0 with the new block's tests all green.
  </behavior>
  <action>
    Extend `tests/api/extract-watch.test.ts` with structured-mode coverage. DO NOT modify existing URL-branch describe blocks (they are the zero-regression contract — §E3).

    **Edit 1 — Mock setup additions (lines 1-49 area):**
    1. Add to the `vi.mock('@/data/catalog', ...)` factory: `upsertCatalogFromUserInput: vi.fn().mockResolvedValue('cat-structured-456')`.
    2. Add a new top-level mock: `vi.mock('@/lib/extractors/llm-structured', () => ({ extractFromStructuredInput: vi.fn() }))` and a `mockExtractFromStructuredInput` handle pattern matching `mockFetchAndExtract`.
    3. Add `import * as cheerio from 'cheerio'` and `const loadSpy = vi.spyOn(cheerio, 'load')` declaration (apply within `beforeEach` for fresh state, or as a top-level `let` reset in `beforeEach`).

    **Edit 2 — Add a new top-level describe block** (parallel to the existing one at line 51), titled `'POST /api/extract-watch — structured mode (Phase 66 EXTR-01..04, EXTR-08)'`. The block contains these `it(...)` cases (per §E2 + RESEARCH §Validation Architecture):

    **EXTR-01 (Zod discriminated union):**
    - `'returns 400 when mode is missing'` — POST `{}`; expect status 400; expect body to be a JSON with `error` and `mode: 'url'` (default per Task 1 hoisted-mode fallback).
    - `'returns 400 when mode is "invalid"'` — POST `{ mode: 'oops' }`; expect 400.
    - `'returns 400 when structured body is missing brand'` — POST `{ mode: 'structured', model: 'Speedmaster' }`; expect 400.
    - `'returns 400 when structured body is missing model'` — POST `{ mode: 'structured', brand: 'Omega' }`; expect 400.
    - `'accepts mode: "url" body unchanged'` — POST `{ mode: 'url', url: 'https://example.com/watch' }`; the URL branch's existing happy-path expectations hold (assert response includes `mode: 'url'`).

    **EXTR-02 (cheerio short-circuit — THE EXECUTABLE GATE):**
    - `'does NOT call cheerio.load when mode is structured'` — set `mockExtractFromStructuredInput.mockResolvedValue({ brand: 'Omega', model: 'Speedmaster' })`; POST `{ mode: 'structured', brand: 'Omega', model: 'Speedmaster' }`; assert `loadSpy).not.toHaveBeenCalled()` AND `mockFetchAndExtract).not.toHaveBeenCalled()` (defense in depth: even if a future contributor imports cheerio differently, fetchAndExtract NOT being called is the route-level signal).

    **EXTR-04 dispatch:**
    - `'calls extractFromStructuredInput with brand/model/reference/year on structured POST'` — POST `{ mode: 'structured', brand: 'Omega', model: 'Speedmaster', reference: '311.30', year: 2018 }`; assert `mockExtractFromStructuredInput` called once with `{ brand: 'Omega', model: 'Speedmaster', reference: '311.30', year: 2018 }`.

    **EXTR-08 (catalog DAL function selection — THE EXTR-08 CRITICAL ASSERTION):**
    - `'calls upsertCatalogFromUserInput (NOT upsertCatalogFromExtractedUrl) on structured success'`:
      - `mockExtractFromStructuredInput.mockResolvedValue({ brand: 'Omega', model: 'Speedmaster', reference: '311.30' })`.
      - POST `{ mode: 'structured', brand: 'Omega', model: 'Speedmaster', reference: '311.30' }`.
      - Assert `vi.mocked(catalogDAL.upsertCatalogFromUserInput).toHaveBeenCalledOnce()` AND `vi.mocked(catalogDAL.upsertCatalogFromUserInput).toHaveBeenCalledWith({ brand: 'Omega', model: 'Speedmaster', reference: '311.30' })` (3 fields ONLY — Pitfall 5).
      - Assert `vi.mocked(catalogDAL.upsertCatalogFromExtractedUrl).not.toHaveBeenCalled()`.

    **EXTR-03 (response shape parity):**
    - `'returns ExtractedWatchData shape with mode: "structured" on success'` — POST same as above; expect `body.success === true`, `body.catalogId === 'cat-structured-456'`, `body.data.brand === 'Omega'`, `body.source === 'llm'`, `body.confidence === 'medium'`, `body.llmUsed === true`, `body.mode === 'structured'`, `Array.isArray(body.fieldsExtracted)`.

    **D-06 (mode-branched error response):**
    - `'returns mode: "structured" and structured-mode copy in structured-data-missing error'` — `mockExtractFromStructuredInput.mockResolvedValue({})` (empty output triggers the gate); POST `{ mode: 'structured', brand: 'X', model: 'Y' }`; expect 422; expect `body.mode === 'structured'`; expect `body.category === 'structured-data-missing'`; expect `body.error` contains `"Couldn't find specs for that watch"` (the new structured-mode copy), NOT `"on this page"` (URL copy).
    - `'preserves URL-mode copy on URL-mode structured-data-missing'` — `mockFetchAndExtract.mockResolvedValue({ success: true, data: { /* no brand/model */ } })`; POST `{ mode: 'url', url: 'https://x.com' }`; expect 422; expect `body.error` contains `'on this page'` (URL-mode copy PRESERVED).

    **D-05 (error taxonomy reuse for structured branch):**
    - `'maps Anthropic 429 from extractFromStructuredInput to quota-exceeded with mode: "structured"'` — `mockExtractFromStructuredInput.mockRejectedValue(Object.assign(new Error('rate limited'), { status: 429 }))`; POST structured; expect status 503, `body.category === 'quota-exceeded'`, `body.mode === 'structured'`.
    - `'maps AbortError from extractFromStructuredInput to LLM-timeout with mode: "structured"'` — `mockExtractFromStructuredInput.mockRejectedValue(Object.assign(new Error('timeout'), { name: 'AbortError' }))`; POST structured; expect status 504, `body.category === 'LLM-timeout'`, `body.mode === 'structured'`.

    **Edit 3 — Optional regression assertion (only if URL-branch tests already accept the structural shape):** for one existing URL-branch success-path test, add an assertion that `body.mode === 'url'`. This is the only authorized modification to the URL-branch fixture (per §E3 — additive, not behavior-changing). Skip if the existing fixture's destructuring would fail; default position: ADD the assertion to one new dedicated `it('URL branch responses carry mode: "url"', ...)` test in the new describe block (cleaner than modifying existing tests).

    Expected file length change: +180 to +280 LOC (8-10 new `it(...)` cases + mock setup additions).
  </action>
  <verify>
    <automated>
      cd /Users/tylerwaneka/Documents/horlo &amp;&amp; \
      grep -c "structured mode\|Phase 66" tests/api/extract-watch.test.ts &amp;&amp; \
      grep -c "extractFromStructuredInput" tests/api/extract-watch.test.ts &amp;&amp; \
      grep -c "upsertCatalogFromUserInput" tests/api/extract-watch.test.ts &amp;&amp; \
      grep -c "loadSpy\|cheerio" tests/api/extract-watch.test.ts &amp;&amp; \
      grep -c "mode: 'structured'\|mode === 'structured'\|body\.mode" tests/api/extract-watch.test.ts &amp;&amp; \
      grep -c "Couldn't find specs for that watch" tests/api/extract-watch.test.ts &amp;&amp; \
      npm run test -- tests/api/extract-watch.test.ts --run 2>&amp;1 | tail -40
    </automated>
  </verify>
  <done>
    Test file extended with a `'POST /api/extract-watch — structured mode'` describe block containing all 11+ enumerated `it(...)` cases; the cheerio spy assertion fires (EXTR-02 executable gate); the DAL function selection assertion fires (EXTR-08 critical assertion); the D-06 mode-branched copy is asserted on both branches; all URL-branch existing tests still pass.
  </done>
  <acceptance_criteria>
    - source: `grep -c "structured mode\\|Phase 66" tests/api/extract-watch.test.ts` returns ≥ `1`
    - source: `grep -c "vi.mock('@/lib/extractors/llm-structured'" tests/api/extract-watch.test.ts` returns `1`
    - source: `grep -c "upsertCatalogFromUserInput: vi.fn()" tests/api/extract-watch.test.ts` returns `1` (mock setup)
    - source: `grep -c "upsertCatalogFromUserInput" tests/api/extract-watch.test.ts` returns ≥ `3` (mock setup + ≥ 2 assertions)
    - source: `grep -c "loadSpy\\|spyOn(cheerio" tests/api/extract-watch.test.ts` returns ≥ `1` (EXTR-02 executable gate)
    - source: `grep -c "not\\.toHaveBeenCalled" tests/api/extract-watch.test.ts` returns ≥ `3` (cheerio, fetchAndExtract, upsertCatalogFromExtractedUrl negative assertions)
    - source: `grep -c "Couldn't find specs for that watch" tests/api/extract-watch.test.ts` returns ≥ `1` (D-06 structured copy assertion)
    - source: `grep -c "on this page" tests/api/extract-watch.test.ts` returns ≥ `1` (D-06 URL copy preserved assertion)
    - source: `grep -cE "^\\s*it\\(" tests/api/extract-watch.test.ts | head -1` returns ≥ `15` (existing URL-branch + new structured block; conservative floor)
    - behavior: `npm run test -- tests/api/extract-watch.test.ts --run` exits 0 (all tests pass — new + regression baseline)
    - behavior: `npm run test -- tests/api/ tests/extractors/ --run` exits 0 (cross-suite green: route + extractor)
    - behavior: `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → route handler | Untrusted JSON body crosses here; auth gate runs FIRST (AUTH-04 / D-14); Zod schema validates discriminated body shape. |
| Route handler → `extractFromStructuredInput` | Validated `{ brand, model, reference?, year? }` flows server-side to Anthropic; brand/model bounded to 200 chars (T-66-02 mitigation). |
| Route handler → `upsertCatalogFromUserInput` | Validated 3-field input writes to `watches_catalog`; existing helper handles SQL parameterization (no new SQL surface in this plan). |
| Route handler → response client | Mode-branched error copy + sanitized error messages (T-25-04-01 preserved). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Elevation of privilege (auth bypass) | POST handler entry | mitigate | `getCurrentUser()` runs FIRST per AUTH-04/D-14 — PRESERVED VERBATIM in Task 1. Auth gate is the same line range as the URL branch; the structured branch only dispatches AFTER auth succeeds. Verified by `tests/api/extract-watch-auth.test.ts` (pre-existing auth fixture) continuing to pass. |
| T-66-02 | Tampering (LLM prompt injection) | `brand`/`model`/`reference` strings concatenated into LLM user message in `extractFromStructuredInput` | mitigate | Zod schema bounds `brand`/`model`/`reference` to `.max(200)` in Task 1 — prevents runaway concatenation. Forced `tool_choice` constrains output to schema-valid tool call. `validateAndCleanData` enum-checks every output field against project constants. System prompt explicitly instructs "Reply ONLY by calling extract_watch_from_identity". Defense in depth across three layers: input length bound, output schema constraint, enum-validated downstream. |
| T-66-03 | Information disclosure (error leak via response) | All error response bodies | mitigate (PRESERVED from Phase 25) | `error` field always sourced from `CATEGORY_COPY[category][mode]` — NEVER from `err.message`, stack, or `String(err)`. Server-side `console.error('Extraction error:', error)` is unchanged. `tests/api/extract-watch.test.ts` already pins this via assertions that response bodies never contain `/anthropic/i`, `/claude/i`, stack traces, or filesystem paths (lines 250-267) — the structured branch inherits via the shared catch. |
| T-66-04 | Tampering (catalog write authz) | `upsertCatalogFromUserInput` call site | accept (existing surface) | The DAL helper is server-only and runs after `getCurrentUser()`. The watches_catalog table is global-write (per CAT-06 — ON CONFLICT DO NOTHING means existing rows are not overwritten). No new authz seam introduced. The user cannot exfiltrate or corrupt other users' data via this path. |
| T-66-05 (NEW) | Cheerio short-circuit bypass via future contributor | Plan 02 structured branch | mitigate | EXTR-02 is the test-enforced gate. Task 3 adds `vi.spyOn(cheerio, 'load')` AND `expect(mockFetchAndExtract).not.toHaveBeenCalled()` — defense in depth. A future contributor cannot silently degrade the contract; CI fails on either signal. RESEARCH §Cheerio Short-Circuit Enforcement layers: (1) Plan 01 does not import `@/lib/extractors` barrel; (2) Plan 02 imports only `extractFromStructuredInput` from the dedicated module; (3) Plan 02 Task 3 spy assertion. |

**Severity assessment:** No HIGH. T-66-01 and T-66-03 are mitigated by preserving existing patterns. T-66-02 is the highest residual concern; the 200-char bound + forced tool-use + enum validation cover the realistic surface. `security_block_on=high` does not block this plan.
</threat_model>

<verification>
After all three tasks land, the executor MUST confirm:

1. `npm run build` exits 0 — TypeScript compiles with the discriminated body, mode threading, and structured branch wired.

2. `npm run test -- tests/api/extract-watch.test.ts --run` exits 0 with:
   - All pre-existing URL-branch tests (lines 51-269 of pre-edit file) continuing to pass — the zero-regression baseline (success criterion 2).
   - All new structured-mode tests passing — EXTR-01 (4 Zod cases), EXTR-02 (cheerio NOT called), EXTR-04 (dispatch call args), EXTR-08 (upsertCatalogFromUserInput called, upsertCatalogFromExtractedUrl NOT called), EXTR-03 (response shape), D-06 (mode-branched copy URL vs structured), D-05 (Anthropic 429 + timeout funneled through categorizeExtractionError).

3. `npm run test -- tests/extractors/ --run` exits 0 — Plan 01's unit tests continue to pass.

4. Manual integration check via the `<verification>` section's `<automated>` blocks (in each task) — every grep returns the documented count.

5. **D-XX gate citations** (mandatory for §13a decision-coverage gate):
   - D-01 (file location) — cited in Plan 01's `must_haves.truths`; here only by import.
   - D-02 (structured-only scope) — cited in Plan 01.
   - D-03 (full parity chain) — cited in this plan's `must_haves.truths`.
   - D-04 (revalidateTag always fires) — cited.
   - D-05 (5-category reuse) — cited.
   - D-06 (mode-branched copy + mode threading) — cited and tested.
   - D-07 (Zod discriminated union) — cited and implemented.
   - D-08 (schema colocated) — cited and implemented.

6. **Pitfall coverage** (mandatory):
   - Pitfall 1 (never `content[0]`) — Plan 01 task 2 acceptance criterion forbids `content[0]`.
   - Pitfall 2 (`.issues` not `.errors`) — Task 1 acceptance criterion `grep -c "\\.errors\\[0\\]"` returns `0`.
   - Pitfall 3 (cheerio short-circuit) — Task 3 spy assertion is the executable gate.
   - Pitfall 4 (`revalidateTag` two-arg form) — Task 1 + Task 2 acceptance criteria assert `revalidateTag('explore', 'max')` with both args.
   - Pitfall 5 (EnrichmentSource type) — Plan 01 already extended.
   - Pitfall 6 (SDK 429 detection via `.status`) — `categorizeExtractionError` PRESERVED.
   - Pitfall 7 (system vs user prompt split) — Plan 01.
   - Pitfall 8 (`toolUse.input` validation) — Plan 01.
</verification>

<success_criteria>
All 5 ROADMAP §Phase 66 success criteria pinned by integration tests in this plan:

1. POST `{ mode: 'structured', brand: 'Omega', model: 'Speedmaster' }` returns extracted data without cheerio call → Task 3 test `'does NOT call cheerio.load when mode is structured'`.

2. POST `{ mode: 'url', url: '...' }` zero regression → existing URL-branch tests at `tests/api/extract-watch.test.ts:51-269` continue to pass unchanged.

3. Brand AND model required; omitting either → 4xx validation error → Task 3 tests `'returns 400 when structured body is missing brand'` and `'returns 400 when structured body is missing model'`.

4. Structured branch creates catalog row via `upsertCatalogFromUserInput`, not `upsertCatalogFromExtractedUrl` → Task 3 test `'calls upsertCatalogFromUserInput (NOT upsertCatalogFromExtractedUrl) on structured success'`.

5. Integration test asserts no cheerio call when `mode === 'structured'` → Task 3 test cited in (1) above (the EXTR-02 executable gate).

Coverage of requirements:
- EXTR-01 — Zod discriminated body (Task 1) + 5 Task-3 it() cases.
- EXTR-02 — Cheerio short-circuit (Task 2 import discipline + Task 3 spy assertion).
- EXTR-03 — Brand/model required (Task 1 Zod) + response shape consistent (Task 2 envelope + Task 3 assertion).
- EXTR-08 — `upsertCatalogFromUserInput` selected (Task 2) + integration assertion (Task 3).

Coverage of decisions: D-03 through D-08 all cited in `must_haves.truths` and implemented in tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/66-api-route-extension/66-02-SUMMARY.md` documenting:
- Route handler diff summary: lines changed in `src/app/api/extract-watch/route.ts` (expected ~+150 LOC vs pre-Phase-66 baseline)
- The discriminated-union schema location + shape (D-07 / D-08)
- The new `{ url, structured }` shape of `CATEGORY_COPY` and the new structured-mode copy strings (D-06)
- The structured branch flow (extract → empty gate → upsert → enrich → revalidate → envelope) with `source: 'structured-input'` discriminant (D-03)
- The mode-threading mechanism (`mode: 'url' | 'structured'` in every JSON response — D-06 coordination point for Phase 69 `<ExtractErrorCard>`)
- Test fixture diff: new `'POST /api/extract-watch — structured mode'` describe block; test counts before/after
- Verified pitfall coverage (1, 2, 3, 4, 6 — all cited in Task acceptance criteria)
- Any deviations from PATTERNS.md §C1-C8 (expected: none — this is the canonical-pattern application case)
- Phase 67 / 69 coordination notes: `mode` field landed in response; Phase 69 `<ExtractErrorCard>` should branch on `body.mode` to select copy; Phase 67 `addWatch` Zod direction confirmed consistent
</output>