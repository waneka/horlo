---
phase: 62-public-wear-pics-on-watch-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - supabase/migrations/20260527000000_phase62_wear_hidden_from_detail.sql
  - tests/unit/getPublicWearPicsForWatch.test.ts
  - tests/unit/hideWearPic.test.ts
  - tests/unit/WornTimeline.test.tsx
  - tests/unit/wearRail.test.ts
autonomous: false
requirements: [WPIC-02, WPIC-04]
must_haves:
  truths:
    - "wear_events has a hidden_from_detail boolean column (NOT NULL DEFAULT false) in the local DB"
    - "The four Wave 0 test files exist and run (RED-allowed) so downstream tasks have a verify target"
    - "getWearRailForViewer source is captured as a guardrail baseline so any change is detectable (WPIC-04)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "hiddenFromDetail column on wearEvents table"
      contains: "hidden_from_detail"
    - path: "supabase/migrations/20260527000000_phase62_wear_hidden_from_detail.sql"
      provides: "Prod migration: ADD COLUMN hidden_from_detail + partial index"
      contains: "hidden_from_detail"
    - path: "tests/unit/getPublicWearPicsForWatch.test.ts"
      provides: "WPIC-01/WPIC-05 DAL union test scaffold"
    - path: "tests/unit/hideWearPic.test.ts"
      provides: "WPIC-02 hide/unhide test scaffold"
    - path: "tests/unit/WornTimeline.test.tsx"
      provides: "WPIC-03 photoUrl-preference test scaffold"
    - path: "tests/unit/wearRail.test.ts"
      provides: "WPIC-04 getWearRailForViewer unchanged guardrail"
  key_links:
    - from: "src/db/schema.ts"
      to: "local DB"
      via: "npx drizzle-kit push"
      pattern: "hidden_from_detail"
---

<objective>
Add the `hidden_from_detail` boolean column to `wear_events` (D-11), apply it to the local DB so downstream DAL work can verify against a real column, ship the parallel prod migration file, and create the four Wave 0 test scaffolds the rest of the phase verifies against.

Purpose: Phase 62 is a stitching phase. Everything downstream (the DAL union, the hide action, the carousel UI) depends on the `hidden_from_detail` column existing in the local DB — Drizzle types come from schema.ts so build/tsc/type checks pass WITHOUT the push, creating a false-positive verification state. This plan closes that gap and lays the test targets.
Output: Schema column + local push + prod migration file + 4 Wave 0 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-CONTEXT.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-RESEARCH.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-PATTERNS.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-VALIDATION.md

<interfaces>
<!-- Existing wearEvents table shape (src/db/schema.ts ~294-311). The new column inserts after `visibility`, before `createdAt`. -->
Existing wearEvents columns: id, userId, watchId, wornDate (text), note, photoUrl, visibility (wearVisibilityEnum public|followers|private), createdAt.
Indexes: wear_events_watch_worn_at_idx ON (watchId, wornDate); unique wear_events_unique_day ON (userId, watchId, wornDate).
Column builder pattern (from watchPhotos.sortOrder line ~346): `integer('sort_order').notNull().default(0)` — boolean variant is `boolean('hidden_from_detail').notNull().default(false)`.
`boolean` and `integer` are already imported in schema.ts (watchPhotos uses them).

<!-- Prod migration analog: supabase/migrations/20260525000000_phase60_watch_photos.sql — header comment block, BEGIN/COMMIT, IF NOT EXISTS idempotency. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hiddenFromDetail column to schema + write prod migration + Wave 0 test scaffolds</name>
  <files>src/db/schema.ts, supabase/migrations/20260527000000_phase62_wear_hidden_from_detail.sql, tests/unit/getPublicWearPicsForWatch.test.ts, tests/unit/hideWearPic.test.ts, tests/unit/WornTimeline.test.tsx, tests/unit/wearRail.test.ts</files>
  <read_first>
    - src/db/schema.ts (the wearEvents pgTable ~294-311; confirm boolean is imported, confirm column order, confirm the index block syntax)
    - supabase/migrations/20260525000000_phase60_watch_photos.sql (the prod-migration ANALOG: header comment block, BEGIN/COMMIT, IF NOT EXISTS pattern, partial index syntax)
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-PATTERNS.md (the schema + migration analog sections — exact column line and migration SQL)
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map — the four test files and which WPIC each covers)
    - tests/data/getWearRailForViewer.test.ts (existing rail test — reference for the wearRail guardrail test style and imports)
    - src/data/wearEvents.ts (getWearRailForViewer ~324-421 — the guardrail target; capture its current WHERE-predicate + 48h cutoff so the guardrail test asserts no change)
  </read_first>
  <action>
    In src/db/schema.ts, add `hiddenFromDetail: boolean('hidden_from_detail').notNull().default(false),` to the wearEvents pgTable column object, inserted after the `visibility` column and before `createdAt` (per D-11; PATTERNS.md schema section). Do NOT alter `visibility` or any existing column. `boolean` is already imported.

    Create supabase/migrations/20260527000000_phase62_wear_hidden_from_detail.sql mirroring the Phase 60 migration header + structure: a comment block citing D-11 and the dual-migration discipline, wrapped in BEGIN;/COMMIT;. Body: `ALTER TABLE wear_events ADD COLUMN IF NOT EXISTS hidden_from_detail boolean NOT NULL DEFAULT false;` then `CREATE INDEX IF NOT EXISTS wear_events_watch_id_public_visible_idx ON wear_events(watch_id, worn_date DESC) WHERE visibility = 'public' AND hidden_from_detail = false;`. No data backfill (DEFAULT false covers existing rows). This file is NOT applied here — it is applied at prod deploy time (`supabase db push --linked`, MEMORY feedback_mobile_ui_verify_on_prod).

    Create the four Wave 0 test scaffolds at the VALIDATION.md paths. These are RED-allowed scaffolds that downstream plans turn GREEN — but they must run (import without crashing, contain the described `it()`/`describe()` blocks, and reference the functions/components by their planned names so Plan 02/03/04 have a concrete verify target):
      - tests/unit/getPublicWearPicsForWatch.test.ts — WPIC-01 (public wear pic appears) + WPIC-05 (followers/private + hidden never returned). Import `getPublicWearPicsForWatch` from '@/data/wearEvents'. Stub data via the project's existing DAL test mock pattern (see tests/data/getWearRailForViewer.test.ts for the db mock approach).
      - tests/unit/hideWearPic.test.ts — WPIC-02 (hide sets hidden_from_detail=true and pic is excluded from a re-query; un-hide restores it; ownership re-check rejects a watch not owned by caller). Import the planned `hideWearPicAction`/`unhideWearPicAction` from '@/app/actions/wearEvents'.
      - tests/unit/WornTimeline.test.tsx — WPIC-03 (when an event has photoUrl, WornTimeline renders it; when null, falls back to watch.imageUrl). Render WornTimeline from '@/components/profile/WornTimeline'.
      - tests/unit/wearRail.test.ts — WPIC-04 GUARDRAIL: assert getWearRailForViewer is unchanged. Use a source-level assertion: read src/data/wearEvents.ts and assert the getWearRailForViewer function body still contains its 48h cutoff token and its `visibility, 'followers'` follows-branch predicate, AND assert it does NOT reference `hiddenFromDetail`/`hidden_from_detail`. (This is a structural guardrail — the rail must stay byte-for-byte unchanged per D-17.)

    For scaffolds where the target function/component does not yet exist (Plan 02/04), the test may be `describe.todo`/`it.todo` OR a failing-but-runnable assertion — the file MUST import without throwing at collection time. Prefer concrete failing assertions over `.skip` so downstream plans see RED→GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/wearRail.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "hidden_from_detail" src/db/schema.ts` returns the new column line; `git diff src/db/schema.ts` shows ONLY an added line in the wearEvents table (no change to visibility or any other column).
    - supabase/migrations/20260527000000_phase62_wear_hidden_from_detail.sql exists, contains `ADD COLUMN IF NOT EXISTS hidden_from_detail` and `CREATE INDEX IF NOT EXISTS wear_events_watch_id_public_visible_idx`, wrapped in BEGIN;/COMMIT;.
    - The four files tests/unit/getPublicWearPicsForWatch.test.ts, tests/unit/hideWearPic.test.ts, tests/unit/WornTimeline.test.tsx, tests/unit/wearRail.test.ts all exist.
    - `npx vitest run tests/unit/wearRail.test.ts -x` runs and the guardrail test PASSES (rail is unchanged at this point).
    - `npx vitest run tests/unit/getPublicWearPicsForWatch.test.ts tests/unit/hideWearPic.test.ts tests/unit/WornTimeline.test.tsx` collects without import-time crashes (RED assertions allowed; collection errors are NOT allowed).
  </acceptance_criteria>
  <done>hidden_from_detail column added to schema + prod migration written; all four Wave 0 test files exist and collect; wearRail guardrail passes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply schema to local DB via drizzle-kit push</name>
  <files>local Postgres (no source file)</files>
  <read_first>
    - $HOME/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/MEMORY.md entry project_drizzle_supabase_db_mismatch (drizzle-kit push is LOCAL ONLY; prod uses supabase db push --linked)
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-RESEARCH.md (Pitfall 4: hide_from_detail drift between local and prod schema)
  </read_first>
  <action>
    Apply the Task 1 schema change to the LOCAL DB only: run `npx drizzle-kit push` and confirm the ADD COLUMN if prompted (additive, non-destructive — one NOT NULL DEFAULT false column). Then verify the column landed: `psql "$DATABASE_URL" -c "\d wear_events"` (or the project's local psql-via-docker pattern) shows `hidden_from_detail | boolean | not null | false`. Do NOT run `supabase db push --linked` here — the prod migration applies at deploy time after the full phase build-gates (MEMORY feedback_mobile_ui_verify_on_prod). Flagged autonomous:false because drizzle-kit push may require interactive confirmation that cannot be suppressed.
  </action>
  <what-built>
    Task 1 added `hiddenFromDetail` to src/db/schema.ts. The local Postgres DB does NOT yet have the column. The DAL union (Plan 02) cannot be verified without it — Drizzle types come from schema.ts, so build/tsc/type checks pass WITHOUT the push, which is a false-positive verification state (RESEARCH Pitfall 4).
  </what-built>
  <how-to-verify>
    Run: `npx drizzle-kit push`
    If it prompts for confirmation of the ADD COLUMN, confirm it (this is additive, NOT a destructive change — it adds one NOT NULL DEFAULT false column).
    Then verify the column landed locally:
      `psql "$DATABASE_URL" -c "\d wear_events"` (or the project's local psql-via-docker pattern) and confirm `hidden_from_detail | boolean | not null | false` appears.
    This is flagged `autonomous: false` because drizzle-kit push may require interactive confirmation that cannot be suppressed. Do NOT run `supabase db push --linked` here — the prod migration applies at deploy time after the full phase build-gates (MEMORY feedback_mobile_ui_verify_on_prod).
  </how-to-verify>
  <verify>
    <automated>MISSING — manual: `npx drizzle-kit push` then confirm `hidden_from_detail` exists on local wear_events</automated>
  </verify>
  <acceptance_criteria>
    - Local DB `wear_events` table has a `hidden_from_detail boolean NOT NULL DEFAULT false` column (confirmed via `\d wear_events`).
    - Prod migration NOT applied yet (deferred to deploy time).
  </acceptance_criteria>
  <resume-signal>Type "pushed" once `hidden_from_detail` exists on the local wear_events table, or describe any drizzle-kit error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local schema → local DB | drizzle-kit push applies a column to the dev DB; additive only |
| schema.ts → prod DB | prod migration file applies at deploy via supabase db push --linked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | hidden_from_detail column semantics | mitigate | Column is a NEW boolean separate from `visibility` (D-11) — schema diff asserted to touch no existing column, so "hide from detail" can never be conflated with a visibility change (preserves WPIC-05 guardrail). |
| T-62-02 | Denial of Service | local/prod schema drift | mitigate | Dual-migration discipline: local push here, prod migration file shipped now and applied at deploy (RESEARCH Pitfall 4). Partial index covers the exact union query shape so the read path stays cheap. |
| T-62-03 | Information Disclosure | getWearRailForViewer regression | mitigate | Wave 0 guardrail test (wearRail.test.ts) asserts the rail function does NOT reference hidden_from_detail and retains its 48h cutoff (WPIC-04 / D-17). |
</threat_model>

<verification>
- `npx vitest run tests/unit/wearRail.test.ts -x` passes (rail unchanged baseline).
- `grep -c "hidden_from_detail" src/db/schema.ts` ≥ 1.
- Migration file exists with the ADD COLUMN + partial index.
- Local DB has the column (Task 2 checkpoint).
</verification>

<success_criteria>
- hidden_from_detail column in schema.ts + applied locally + prod migration file written.
- Four Wave 0 test files exist and collect; wearRail guardrail green.
- No existing wearEvents column modified.
</success_criteria>

<output>
After completion, create `.planning/phases/62-public-wear-pics-on-watch-detail/62-01-SUMMARY.md`
</output>
