---
phase: 62-public-wear-pics-on-watch-detail
plan: 03
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - src/app/u/[username]/[tab]/page.tsx
  - src/components/profile/WornTimeline.tsx
  - src/components/profile/WornCalendar.tsx
  - tests/unit/WornTimeline.test.tsx
autonomous: true
requirements: [WPIC-03]
must_haves:
  truths:
    - "A wear event with a photo shows the ACTUAL wear photo in the Wears tab (timeline + calendar), not the catalog/cover image"
    - "A wear event with no photo falls back to the watch cover/catalog image"
    - "Wear photo storage paths are signed via the admin client before reaching the client component"
  artifacts:
    - path: "src/components/profile/WornTimeline.tsx"
      provides: "photoUrl-preferred image source + WearEventLite.photoUrl field"
      contains: "photoUrl"
    - path: "src/components/profile/WornCalendar.tsx"
      provides: "photoUrl-preferred image source"
      contains: "photoUrl"
    - path: "src/app/u/[username]/[tab]/page.tsx"
      provides: "wear-photo admin-client signing + photoUrl passed to WornTabContent"
      contains: "wear-photos"
  key_links:
    - from: "src/app/u/[username]/[tab]/page.tsx"
      to: "wear-photos bucket"
      via: "createSupabaseAdminClient().storage.from('wear-photos').createSignedUrl"
      pattern: "wear-photos"
    - from: "src/components/profile/WornTimeline.tsx"
      to: "event.photoUrl"
      via: "getSafeImageUrl(e.photoUrl) ?? getSafeImageUrl(watch.imageUrl)"
      pattern: "getSafeImageUrl\\(e?\\.?photoUrl"
---

<objective>
Repoint the Wears tab to show each wear event's ACTUAL photo instead of the generic catalog/cover image (WPIC-03 / D-16): sign wear-photo storage paths with the admin client in the Wears tab RSC, thread `photoUrl` through `WearEventLite`, and make `WornTimeline` + `WornCalendar` prefer the event's own signed photo with the watch cover as fallback.

Purpose: Today the Wears tab strips `photoUrl` and renders `watch.imageUrl` for every entry, so a real-world wear photo is invisible. D-16 says the tab must surface the actual wear photo when present.
Output: Wears tab signs + passes photoUrl; timeline/calendar prefer it; WPIC-03 test green.

This plan is independent of the carousel/DAL-union work (Plan 02) and the schema (Plan 01) — it only consumes `getWearEventsForViewer`'s already-selected `photoUrl` field. It runs in Wave 2 parallel with Plan 02 (zero file overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-CONTEXT.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-RESEARCH.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-PATTERNS.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-UI-SPEC.md

<interfaces>
<!-- getWearEventsForViewer ALREADY selects photoUrl (raw storage path) — src/data/wearEvents.ts ~161-210. The Wears tab currently strips it. -->
getWearEventsForViewer(viewerId, profileId) → events including `photoUrl: string | null` (raw path {userId}/wearEventId.jpg).

<!-- WornTimeline WearEventLite interface (~12-17) — add photoUrl. WatchSummary (~5-10) has imageUrl. -->
interface WearEventLite { id; watchId; wornDate; note: string | null }  // + photoUrl: string | null

<!-- Image source today (WornTimeline ~65): const safe = watch ? getSafeImageUrl(watch.imageUrl) : null -->
<!-- getSafeImageUrl (imported ~line 3) https-only filter; a signed https URL passes through as-is. -->

<!-- Admin-client signing analog: signCoverUrls (src/lib/storage/signCoverUrls.ts) handles 'watch-photos' bucket; wear photos live in 'wear-photos'. createSupabaseAdminClient is available in this RSC. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WornTimeline + WornCalendar prefer event.photoUrl over watch cover</name>
  <files>src/components/profile/WornTimeline.tsx, src/components/profile/WornCalendar.tsx, tests/unit/WornTimeline.test.tsx</files>
  <behavior>
    - When a WearEventLite has a non-null photoUrl that passes getSafeImageUrl, WornTimeline renders THAT url.
    - When photoUrl is null (or fails getSafeImageUrl), WornTimeline falls back to the watch cover (getSafeImageUrl(watch.imageUrl)).
    - WornCalendar applies the identical preference at each cell that renders an image.
  </behavior>
  <read_first>
    - src/components/profile/WornTimeline.tsx (FULL: WearEventLite interface ~12-17, WatchSummary ~5-10, the image source line ~65 `getSafeImageUrl(watch.imageUrl)`, getSafeImageUrl import ~3)
    - src/components/profile/WornCalendar.tsx (FULL: locate every `getSafeImageUrl(watch.imageUrl)` / cover-image render; mirror the same change)
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-PATTERNS.md (WornTimeline/WornCalendar analog section — the exact two-line change `wearPhotoSafe ?? watchCoverSafe`)
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-UI-SPEC.md (Surface 5: no new placeholder; source priority only; fallback to catalog/cover)
    - tests/unit/WornTimeline.test.tsx (the Plan-01 scaffold to turn green)
  </read_first>
  <action>
    In WornTimeline.tsx, add `photoUrl: string | null` to the WearEventLite interface. Change the image source so it prefers the event's own photo: compute `const wearPhotoSafe = e.photoUrl ? getSafeImageUrl(e.photoUrl) : null` then `const safe = wearPhotoSafe ?? (watch ? getSafeImageUrl(watch.imageUrl) : null)` (PATTERNS.md). No new imports — getSafeImageUrl is already imported and a signed https URL passes through unchanged.

    Apply the identical preference in WornCalendar.tsx everywhere it currently renders `getSafeImageUrl(watch.imageUrl)` for a wear cell; add photoUrl to its event type if it has a local event interface.

    Turn tests/unit/WornTimeline.test.tsx GREEN: render WornTimeline with (a) an event that has a usable photoUrl → assert the wear photo url is used; (b) an event with photoUrl=null → assert the watch cover is used.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/WornTimeline.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - WearEventLite in WornTimeline.tsx includes `photoUrl` (grep).
    - `npx vitest run tests/unit/WornTimeline.test.tsx -x` PASSES (WPIC-03): photoUrl preferred when present, watch cover used as fallback when null.
    - WornCalendar.tsx references `photoUrl` in its image-source path (grep).
    - No change to the thumbnail markup itself (only the source expression changes) — `git diff` shows source-priority edits, not a markup rewrite.
  </acceptance_criteria>
  <done>WornTimeline + WornCalendar prefer event.photoUrl with cover fallback; WPIC-03 component test green.</done>
</task>

<task type="auto">
  <name>Task 2: Sign wear-photo paths in the Wears tab RSC and pass photoUrl to WornTabContent</name>
  <files>src/app/u/[username]/[tab]/page.tsx</files>
  <read_first>
    - src/app/u/[username]/[tab]/page.tsx (the worn-tab branch ~423-456: the `getWearEventsForViewer` call, the `signCoverUrls(resolved.watches)` call, the watchMap build, and the `events.map(...)` that currently strips photoUrl; confirm whether createSupabaseAdminClient is already imported or needs adding)
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-PATTERNS.md (the Wears-tab signing analog section — the distinct-path admin-client signing loop + the events.map photoUrl addition)
    - src/lib/storage/signCoverUrls.ts (the admin-client signing approach + getSafeImageUrl integration — the helper to mirror; bucket differs: wear-photos vs watch-photos)
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-RESEARCH.md (Critical File-by-File Findings §7 + Pitfall 6: unsigned wear photoUrl → placeholder; Open Question 3: sign for all permitted viewers)
    - $HOME/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/MEMORY.md entry project_ppr_dynamic_before_use_cache (admin client only for signing; never cookie client in a route touching 'use cache')
  </read_first>
  <action>
    In the worn-tab branch of src/app/u/[username]/[tab]/page.tsx, after `const events = await getWearEventsForViewer(...)`, sign the raw wear-photo paths with the admin client (D-19): create one `createSupabaseAdminClient()` instance, collect the distinct non-null `event.photoUrl` paths, sign each via `.storage.from('wear-photos').createSignedUrl(path, 3600)` inside a try/catch that maps to null on failure (fail-safe-to-placeholder), into a `Map<rawPath, signedUrl|null>`. Then in the existing `events.map(...)` that builds the WornTabContent props, ADD `photoUrl: e.photoUrl ? (signedMap.get(e.photoUrl) ?? null) : null` (it is currently stripped). Sign for all permitted viewers (Open Question 3 — consistent with cover signing). Use the admin client, NOT createSupabaseServerClient (PPR/cache gotcha). Do NOT alter the existing signCoverUrls/watchMap logic.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "wear-photos" src/app/u/[username]/[tab]/page.tsx` returns the new signing call.
    - The `events.map(...)` for WornTabContent now passes `photoUrl` (grep shows `photoUrl` inside the map).
    - Signing uses `createSupabaseAdminClient` — `grep -c "createSupabaseServerClient" src/app/u/[username]/[tab]/page.tsx` is unchanged from before the edit (no new cookie-client signing introduced).
    - `npm run build` exits 0 (authoritative gate — MEMORY project_baseline_not_green_build_is_gate).
  </acceptance_criteria>
  <done>Wears tab signs wear-photo paths via admin client and threads photoUrl into WornTabContent; build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wear-photos bucket → signed URL | raw storage path {userId}/… signed by admin client at RSC layer |
| Wears tab RSC → client (WornTabContent) | signed URLs cross to the browser as image src |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-09 | Information Disclosure / signing via cookie client (HIGH) | Wears tab signing | mitigate | Use `createSupabaseAdminClient()` only (D-19) — never `createSupabaseServerClient()` which reads cookies() and corrupts the PPR prerender boundary (MEMORY project_ppr_dynamic_before_use_cache). Acceptance check asserts no new cookie-client signing. |
| T-62-10 | Information Disclosure / path traversal (HIGH) | wear-photo path scope | mitigate | Paths are `{userId}/…` scoped (Phase 61 CR-02 IDOR fix). Admin-client signing validates against the bucket folder policy; signing failure returns null → placeholder (fail-safe, D-19). |
| T-62-11 | Information Disclosure / over-exposure | getWearEventsForViewer | accept | The tab already gates which wear events a viewer can see (existing 3-tier visibility predicate in getWearEventsForViewer); this plan only signs photoUrls for events that gate already permitted. No new exposure surface. |
</threat_model>

<verification>
- `npx vitest run tests/unit/WornTimeline.test.tsx -x` passes.
- `npm run build` exits 0.
- `grep "wear-photos" src/app/u/[username]/[tab]/page.tsx` present; no new createSupabaseServerClient signing.
</verification>

<success_criteria>
- Wears tab shows the actual wear photo when present, catalog cover otherwise (WPIC-03 / D-16).
- Signing via admin client only; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/62-public-wear-pics-on-watch-detail/62-03-SUMMARY.md`
</output>
