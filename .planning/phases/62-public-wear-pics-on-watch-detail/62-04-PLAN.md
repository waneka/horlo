---
phase: 62-public-wear-pics-on-watch-detail
plan: 04
type: execute
wave: 3
depends_on: [62-02]
files_modified:
  - src/app/w/[ref]/page.tsx
  - src/components/watch/WatchDetail.tsx
  - src/components/watch/WatchPhotoSection.tsx
autonomous: false
requirements: [WPIC-01, WPIC-02, WPIC-06]
must_haves:
  truths:
    - "A public wear pic appears in the /w/[ref] carousel: owner uploads first, then wear pics newest-worn first (WPIC-01/D-02)"
    - "Wear-pic slides carry a 'Worn · [date]' badge formatted in UTC/en-US (no hydration mismatch) (D-05/D-07)"
    - "On a wear-pic slide a viewer (owner or non-owner) can like and open a comment bottom sheet for that pic (WPIC-06)"
    - "In Edit mode the owner can hide / un-hide a specific wear-pic thumbnail; un-hide lives in the same Edit mode (WPIC-02/D-08/D-10)"
    - "The watch-level CommentThread, unstable_instant = false, and await connection() are untouched (PAGE-03 / D-15)"
  artifacts:
    - path: "src/components/watch/WatchPhotoSection.tsx"
      provides: "SignedWearPic type, merged slide order, Worn badge, inline social row, eye/hide Edit mode, comment sheet"
      contains: "SignedWearPic"
    - path: "src/app/w/[ref]/page.tsx"
      provides: "wear-pic fetch + admin-client signing + comment pre-fetch + slide assembly (both branches)"
      contains: "getPublicWearPicsForWatch"
    - path: "src/components/watch/WatchDetail.tsx"
      provides: "wearPics + viewer/owner props threaded to WatchPhotoSection"
      contains: "wearPics"
  key_links:
    - from: "src/app/w/[ref]/page.tsx"
      to: "getPublicWearPicsForWatch"
      via: "DAL union call + admin-client wear-photos signing"
      pattern: "getPublicWearPicsForWatch"
    - from: "src/components/watch/WatchPhotoSection.tsx"
      to: "WearCommentHost variant='bottom-sheet'"
      via: "comment-count tap opens sheet with wear target"
      pattern: "WearCommentHost|variant=.bottom-sheet"
    - from: "src/components/watch/WatchPhotoSection.tsx eye/hide"
      to: "hideWearPicAction/unhideWearPicAction"
      via: "onPointerDown toggle in Edit mode"
      pattern: "hideWearPicAction|unhideWearPicAction"
---

<objective>
Stitch the surfaced wear pics into the Phase 61 carousel (WPIC-01), attach the v6.0 like + comment layer to each wear-pic slide (WPIC-06), and add the owner per-pic hide/un-hide control in Edit mode (WPIC-02). The page RSC fetches public wear pics via the Plan 02 DAL union, signs them with the admin client, pre-fetches each pic's comments (Option A), and threads everything through WatchDetail → WatchPhotoSection.

Purpose: This is the user-visible payoff of the phase. All primitives exist — the carousel (Phase 61), the wear-targeted social layer (Phase 53-57), the WearCommentHost bottom-sheet (Phase 57.1), the admin-client signing pattern (Phase 61). This plan wires them together without re-architecting the page IA (that is Phase 64) and without disturbing the Cache Components contract.
Output: Carousel shows owner+wear slides with badge + social row + hide control; build green; touch behavior verified on prod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-CONTEXT.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-RESEARCH.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-PATTERNS.md
@.planning/phases/62-public-wear-pics-on-watch-detail/62-UI-SPEC.md

<interfaces>
<!-- From Plan 02 (src/data/wearEvents.ts): -->
getPublicWearPicsForWatch(watchId): Promise<Array<{ id; wornDate: string; photoUrl: string | null; hiddenFromDetail: boolean }>>
<!-- From Plan 02 (src/app/actions/wearEvents.ts): -->
hideWearPicAction(data: { wearEventId; watchId }): Promise<ActionResult<void>>
unhideWearPicAction(data: { wearEventId; watchId }): Promise<ActionResult<void>>

<!-- New client-side type to add to WatchPhotoSection (RESEARCH Pattern 2 / PATTERNS.md): keep DISTINCT from SignedPhoto, do not union. -->
export interface SignedWearPic { wearEventId: string; signedUrl: string | null; wornDate: string; hiddenFromDetail: boolean; initialLikeState: { liked: boolean; count: number }; commentCount: number; initialComments: CommentWithAuthor[] }

<!-- Existing WatchPhotoSection (DO NOT break): SignedPhoto {id, signedUrl, sortOrder} ~61-65; WatchPhotoSectionProps {photos, watchId, catalogFallbackUrl?, brandModel, viewerCanEdit?, userId?} ~67-75; editMode toggle onPointerDown ~600; useOptimistic for ids ~122 + deletedIds ~128. -->

<!-- WearCommentHost bottom-sheet prop contract (VERIFIED src/components/wear/WearCommentHost.tsx ~24-39): variant='bottom-sheet' REQUIRES open + onOpenChange. Props: wearEventId, open, onOpenChange, initialComments: CommentWithAuthor[], canComment, ownerFollowsViewer, viewerIsFollowing, ownerUserId, ownerUsername, viewerId: string|null, viewerAuthor: CommentAuthor|null, onCountChange?(delta). -->

<!-- WatchDetail (src/components/watch/WatchDetail.tsx ~34/100/149): threads signedPhotos → WatchPhotoSection when signedPhotos !== undefined. Add wearPics + viewer/owner props alongside. -->

<!-- /w/[ref]/page.tsx (VERIFIED): unstable_instant=false (line 45), await connection() (line 91) — MUST NOT TOUCH. Branch 1 owner-photo signing ~158-169; catalog/D-06 branch ~367-379. Already imports getLikesForTargetCached, getCommentsForTarget, createSupabaseAdminClient. CommentThread RSC sibling ~286 — DO NOT MODIFY. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch + sign public wear pics, pre-fetch comments, assemble slides (page RSC, both branches)</name>
  <files>src/app/w/[ref]/page.tsx, src/components/watch/WatchDetail.tsx</files>
  <read_first>
    - src/app/w/[ref]/page.tsx (FULL UnifiedWatchContent: Branch 1 ~114-260 incl. owner-photo signing ~158-169, the watch-level like/comment target build ~176-201, the WatchDetail render ~239-250, the CommentThread sibling ~286; the catalog/D-06 branch ~360-379. CONFIRM line 45 unstable_instant=false and line 91 await connection() — leave both untouched.)
    - src/components/watch/WatchDetail.tsx (FULL: WatchDetailProps ~34, the signedPhotos thread to WatchPhotoSection ~149-152, the viewerId/ownerUserId/ownerUsername props already present for the watch-level like/comment)
    - src/data/comments.ts (getCommentsForTarget for wear targets — line ~155; canViewerCommentOnTarget wear gate returns true line ~66) and src/data/reactions.ts (getLikesForTargetCached for {type:'wear',id})
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-PATTERNS.md (the w/[ref]/page.tsx self-analog signing block + "both branches need the wear-pic fetch+signing")
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-RESEARCH.md (Pattern 3 signing with fail-safe try/catch; Open Question 1 — Option A pre-fetch all comments; Critical Findings §5 + §8; Pitfall 2 cookie-client signing)
    - $HOME/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/MEMORY.md entries project_ppr_dynamic_before_use_cache + project_react_418_date_tz_hydration
  </read_first>
  <action>
    In src/app/w/[ref]/page.tsx, import `getPublicWearPicsForWatch` from '@/data/wearEvents'. In BOTH UnifiedWatchContent branches (Branch 1 ~158 and the catalog/D-06 branch ~367):
      1. Fetch `const rawWearPics = await getPublicWearPicsForWatch(watch.id)` (for ALL viewers — D-06: every wear pic is the watch owner's; not isOwner-gated).
      2. Sign each via the SAME admin client instance already created for owner photos (`createSupabaseAdminClient()` — create once per request, reuse) using `.storage.from('wear-photos').createSignedUrl(p.photoUrl, 60*60)` inside a try/catch returning signedUrl:null on failure (fail-safe-to-placeholder, D-19; closes the Phase 61 deferred item). Bucket is 'wear-photos', field is `p.photoUrl` (NOT storagePath).
      3. Pre-fetch like + comment state per wear pic (Option A): for each pic build `{type:'wear', id: pic.id}` and resolve `getLikesForTargetCached(viewerId, target)` and `getCommentsForTarget(viewerId, target)` in parallel (Promise.all over the <10 pics). Map results into the SignedWearPic shape: wearEventId, signedUrl, wornDate, hiddenFromDetail, initialLikeState {liked,count}, commentCount = comments.length, initialComments = comments.
      4. Pass the assembled `signedWearPics` to `<WatchDetail wearPics={signedWearPics} ... />` in both branches, alongside the existing signedPhotos and the owner/viewer props WearCommentHost needs (ownerUserId, ownerUsername, viewerId, viewerAuthor — reuse the same values already resolved for the watch-level CommentThread ~204).
    DO NOT modify the watch-level CommentThread block (~286), unstable_instant (line 45), or await connection() (line 91).

    In src/components/watch/WatchDetail.tsx, add `wearPics?: SignedWearPic[]` (import the type from WatchPhotoSection) plus the WearCommentHost owner/viewer props to WatchDetailProps, and thread them into the `<WatchPhotoSection ... />` render alongside `photos={signedPhotos}`. wearPics is optional (backward compat — same pattern as signedPhotos being optional).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getPublicWearPicsForWatch" src/app/w/[ref]/page.tsx` ≥ 2 (both branches fetch it).
    - `grep -c "wear-photos" src/app/w/[ref]/page.tsx` ≥ 2 (both branches sign in the wear-photos bucket).
    - `grep -n "unstable_instant = false" src/app/w/[ref]/page.tsx` still present at line 45; `grep -n "await connection()" src/app/w/[ref]/page.tsx` still present — `git diff` shows NO change to line 45, line 91, or the CommentThread block.
    - WatchDetail.tsx threads `wearPics` to WatchPhotoSection (grep).
    - `npm run build` exits 0 (authoritative gate).
  </acceptance_criteria>
  <done>Both page branches fetch+sign public wear pics and pre-fetch their like/comment state, threaded through WatchDetail to WatchPhotoSection; Cache Components contract untouched; build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: SignedWearPic type, merged slides, Worn badge, inline social row, eye/hide Edit mode + comment sheet</name>
  <files>src/components/watch/WatchPhotoSection.tsx</files>
  <read_first>
    - src/components/watch/WatchPhotoSection.tsx (FULL: SignedPhoto ~61-65, WatchPhotoSectionProps ~67-75, the component signature ~79-86, useOptimistic patterns ~122 + ~128, the embla carousel + selectedIndex logic, the filmstrip render, the editMode toggle onPointerDown ~600, the existing arrow-overlay class strings)
    - src/components/watch/SortablePhotoThumb.tsx (Cover badge overlay ~121-128 — class string for the Worn badge; delete × button ~131-145 — position/shape for the eye/hide button)
    - src/components/wear/WearCommentHost.tsx (bottom-sheet variant prop contract ~24-39 — reuse AS-IS; do NOT modify its CSS chain)
    - src/components/shared/LikeButton.tsx (LikeTarget usage, optimistic pattern, touch-target classes min-h-[44px] min-w-[44px], count display rules — reuse AS-IS with target={{type:'wear', id}})
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-UI-SPEC.md (Surfaces 1-4 + CSS Chain Assertions 1,3,4,5 + Copywriting Contract + Accessibility Contract — these are IMPLEMENTATION CONTRACTS: badge bg-background/80 legibility, opacity-50 on the IMAGE element only, social row conditional-render not visibility:hidden, position indicator counts the merged total, 44px touch targets, exact aria-labels and toast copy)
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-PATTERNS.md (the WatchPhotoSection analog section: SignedWearPic interface, Worn badge UTC formatter, eye/hide button overlay, useOptimistic hide pattern, WearCommentHost invocation)
    - .planning/phases/62-public-wear-pics-on-watch-detail/62-RESEARCH.md (Pitfall 1 React #418 badge UTC pin; Pitfall 7 comment-count sync via onCountChange)
    - $HOME/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/MEMORY.md entries project_react_418_date_tz_hydration + project_router_cache_stale_instance
  </read_first>
  <action>
    Extend WatchPhotoSection.tsx (keep all Phase 61 behavior intact):
      1. Add `export interface SignedWearPic` directly below SignedPhoto (DISTINCT type, not a union with SignedPhoto, for type-narrowing). Import `CommentWithAuthor` from '@/components/comment/types'.
      2. Extend WatchPhotoSectionProps with `wearPics?: SignedWearPic[]` plus the WearCommentHost owner/viewer props (ownerUserId, ownerUsername, viewerId: string|null, viewerAuthor). Merge slides at render: owner photos first (existing sortOrder), wear pics appended newest-first (already ordered by the DAL). The carousel position indicator (N/total) MUST count the merged total (CSS Chain Assertion 5).
      3. Worn badge (Surface 1): on wear-pic slides only, render the badge at `absolute bottom-2 left-2`, `bg-background/80 backdrop-blur-sm text-foreground text-xs font-semibold px-2 py-0.5 rounded`. Format the date with the MANDATORY UTC pin: `new Date(wornDate + 'T00:00:00Z').toLocaleDateString('en-US', { timeZone: 'UTC', month: 'short', day: 'numeric' })` (D-07 / React #418). Owner uploads + catalog fallback: no badge.
      4. Inline social row (Surface 2): rendered as `null` on owner/catalog slides; on a wear-pic active slide, a flex row BENEATH the carousel (between viewport and position indicator) with a `role="group" aria-label="Wear photo interactions"`. Reuse LikeButton with `target={{type:'wear', id: activeWearPic.wearEventId}}` (44px target, existing contract). A comment-count `<button>` (MessageCircle, 44px target, aria-label per Copywriting Contract) that opens the sheet. Conditional render — NOT visibility:hidden (CSS Chain Assertion 4).
      5. Comment sheet (Surface 3): WearCommentHost variant='bottom-sheet' with the prop contract from interfaces; `open`/`onOpenChange` from local state; `initialComments` from the active pic; `canComment={true}` (wear gate always open); `onCountChange` updates the slide's commentCount in local state (Pitfall 7). Do NOT add 'use cache' anywhere in this path.
      6. Eye/hide in Edit mode (Surface 4): wear-pic filmstrip thumbnails render in a parallel non-sortable wrapper (NOT SortablePhotoThumb — no drag, no delete, no Cover badge). Visible thumb: EyeOff button top-right (`absolute top-1 right-1`, ≥36px effective tap target). Hidden thumb: `opacity-50` on the IMAGE element only (CSS Chain Assertion 3) + a "Hidden" label strip + Eye (restore) button. The toggle uses `onPointerDown` (NOT onClick — MEMORY project_router_cache_stale_instance) and calls hideWearPicAction/unhideWearPicAction (imported from '@/app/actions/wearEvents'); use a useOptimistic flag mirroring the existing delete optimistic pattern; on failure revert + `toast.error("Couldn't update. Try again.")`. Success toast: "Hidden from this page" / "Shown on this page".
    Apply the exact class strings, aria-labels, and copy from UI-SPEC. Preserve the Phase 61 editMode reset on onPointerDown.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export interface SignedWearPic" src/components/watch/WatchPhotoSection.tsx` present.
    - Badge formatter pins UTC: `grep -n "T00:00:00Z" src/components/watch/WatchPhotoSection.tsx` AND `grep -n "timeZone: 'UTC'" src/components/watch/WatchPhotoSection.tsx` both present (D-07 React #418 guard).
    - The hide/unhide toggle uses `onPointerDown` (grep `onPointerDown` near hideWearPicAction), NOT onClick.
    - WearCommentHost invoked with `variant="bottom-sheet"` and the wear `wearEventId` (grep); no `'use cache'` added to WatchPhotoSection (grep -c "use cache" stays 0).
    - hideWearPicAction + unhideWearPicAction imported and called (grep).
    - The existing SignedPhoto interface + WatchPhotoSectionProps.photos are unchanged (git diff additive only); Phase 61 editMode onPointerDown toggle at ~600 still present.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Wear-pic slides render with UTC-pinned badge + inline like/comment row + bottom-sheet; Edit-mode eye/hide toggle wired to the server actions with optimistic update; opacity-50 on image only; build exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Prod UAT — carousel union, badge, hide toggle, comment sheet (touch behavior)</name>
  <action>
    Build-gate (`npm run build` exits 0 + `npx vitest run tests/unit/ -x` green), then deploy to prod (push origin main → Vercel) and run the 6 prod checks below AFTER the cache fills. Touch/swipe/sheet behavior + React #418 hydration verify on PROD only (empty test DB skips e2e — MEMORY feedback_mobile_ui_verify_on_prod); cold reads can false-positive (MEMORY project_ppr_dynamic_before_use_cache). Bundle the whole phase into one deploy before this checkpoint.
  </action>
  <what-built>
    The /w/[ref] carousel now unions public wear pics after owner uploads (newest-worn first) with a "Worn · [date]" badge, an inline like + comment row on wear-pic slides opening a bottom-sheet thread, and an owner eye/hide toggle in Edit mode. The Wears tab (Plan 03) shows actual wear photos. Code build-gates green; swipe/sheet/touch + React #418 hydration verify on PROD only (empty test DB skips e2e — MEMORY feedback_mobile_ui_verify_on_prod).
  </what-built>
  <how-to-verify>
    After `npm run build` exits 0, deploy to prod (push origin main → Vercel) and AFTER the cache fills (cold reads can false-positive — MEMORY project_ppr_dynamic_before_use_cache):
    1. WPIC-01: Open a /w/[ref] for a watch with public wear pics. Confirm owner uploads appear first, then wear pics newest-worn first. Position indicator counts ALL slides.
    2. WPIC-01/D-07: Confirm the "Worn · [date]" badge shows the correct UTC date with NO hydration flash (hard refresh AND soft-nav).
    3. WPIC-06: On a wear-pic slide, tap Like (optimistic toggle); tap the comment count → bottom sheet opens with that pic's thread; post a comment, dismiss by swipe/scrim → returns to carousel; count stays in sync.
    4. WPIC-02/D-09/D-10: As owner, enter "Edit photos"; tap eye on a wear-pic thumb → greys + "Hidden"; reload → still hidden in the carousel BUT still present in the Wears tab and (within 48h) the Home rail; toggle back → reappears.
    5. WPIC-05: View the same watch detail as a NON-owner (2nd account) → only public, non-hidden wear pics appear; no followers-only/private pic surfaces.
    6. WPIC-04: Confirm the Home wear rail still only shows wears within the 24/48h window (unchanged).
  </how-to-verify>
  <verify>
    <automated>MISSING — prod human UAT (touch/swipe/sheet + React #418 hydration verify on Vercel after cache fills, per MEMORY feedback_mobile_ui_verify_on_prod)</automated>
  </verify>
  <acceptance_criteria>
    - Carousel union, badge, hide/un-hide, comment sheet, and non-owner/visibility gating all behave as described on prod.
    - No React #418/#419 hydration error in the browser console after cache fills.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 6 checks pass on prod, or describe any failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wear-photos bucket → signed URL | raw {userId}/… path signed by admin client in the page RSC |
| page RSC → client island | signed URLs + pre-fetched like/comment state cross to WatchPhotoSection (any viewer) |
| client (Edit mode) → hide/unhide server action | owner-gated UI; server re-checks ownership (Plan 02) |
| 'use cache' boundary | watch-level CommentThread + getLikesForTargetCached must stay on the existing PPR contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-12 | Information Disclosure / signing disclosure (HIGH) | wear-pic URL signing in page RSC | mitigate | Sign via `createSupabaseAdminClient()` only, 60-min TTL, fail-safe-to-placeholder on error (D-19). Never the cookie client — would corrupt PPR prerender (Pitfall 2 / MEMORY project_ppr_dynamic_before_use_cache). Acceptance asserts wear-photos signing + untouched connection()/unstable_instant. |
| T-62-13 | Information Disclosure / non-public or hidden leak (HIGH) | slide payload | mitigate | The slide set comes from getPublicWearPicsForWatch (Plan 02) which filters visibility='public' AND NOT hidden_from_detail server-side. The client never receives a non-public/hidden row (WPIC-05/WPIC-02/D-12). UI hide-greying in Edit mode operates ONLY on already-public rows the owner chose to hide. |
| T-62-14 | Tampering / IDOR via hide UI (HIGH) | eye/hide toggle | mitigate | UI gate is viewerCanEdit (defense-in-depth); the real gate is the server action's ownership re-check + DAL ownership-scoped WHERE (Plan 02 T-62-04). A non-owner has no Edit affordance and any forged action call is rejected server-side. |
| T-62-15 | Repudiation / hydration corruption (MEDIUM) | Worn badge date | mitigate | UTC + en-US pinned formatter (D-07) prevents the React #418 server/client text mismatch class. Acceptance greps for the T00:00:00Z + timeZone:'UTC' tokens; prod UAT confirms no hydration flash. |
| T-62-16 | DoS / cache-contract regression (HIGH) | CommentThread / unstable_instant / await connection() | mitigate | This plan does NOT modify the watch-level CommentThread sibling, line 45, or line 91; the wear-pic comment path uses the already-uncached WearCommentHost client component with pre-fetched initialComments (Option A) — no new 'use cache' anywhere (PAGE-03 / D-15). Acceptance asserts via git diff + grep. |
</threat_model>

<verification>
- `npm run build` exits 0.
- `npx vitest run tests/unit/ -x` — all Wave 0 tests green (regression sweep).
- git diff confirms unstable_instant=false (45), await connection() (91), and the CommentThread block (~286) are untouched.
- Prod human UAT (Task 3) approved.
</verification>

<success_criteria>
- Public wear pics surface in the carousel, owner-first then newest-worn (WPIC-01).
- Wear-pic slides carry the UTC-pinned badge + working like + comment sheet (WPIC-06).
- Owner can hide/un-hide a wear pic in Edit mode without a visibility change (WPIC-02).
- Cache Components contract intact; build exits 0; prod UAT passes.
</success_criteria>

<output>
After completion, create `.planning/phases/62-public-wear-pics-on-watch-detail/62-04-SUMMARY.md`
</output>
