---
phase: 22
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/components/settings/SettingsTabsShell.test.tsx
  - tests/components/settings/AccountSection.test.tsx
  - tests/components/settings/EmailChangeForm.test.tsx
  - tests/components/settings/EmailChangePendingBanner.test.tsx
  - tests/components/settings/PasswordChangeForm.test.tsx
  - tests/components/settings/PasswordReauthDialog.test.tsx
  - tests/components/settings/StatusToastHandler.test.tsx
  - tests/components/settings/PrivacySection.test.tsx
  - tests/components/settings/NotificationsSection.test.tsx
  - tests/components/settings/PreferencesSection.test.tsx
  - tests/components/settings/ProfileSection.test.tsx
  - tests/app/auth-callback-route.test.ts
  - tests/app/preferences-redirect.test.ts
  - tests/lib/auth/lastSignInAt.test.ts
  - tests/components/settings/SettingsClient.test.tsx
  - src/lib/auth/lastSignInAt.ts
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-04
  - SET-05
  - SET-06
must_haves:
  truths:
    - "All Wave 0 test files from 22-VALIDATION.md exist as RED skeletons (failing or pending)."
    - "The lastSignInAt freshness helper module exists with unit tests covering the 24h threshold."
    - "The pre-Phase-22 SettingsClient.test.tsx is retired (deleted) since its assertions about D-02..D-04 deleted UI become invalid."
  artifacts:
    - path: "src/lib/auth/lastSignInAt.ts"
      provides: "isSessionStale(lastSignInAtIso: string | null | undefined, thresholdMs?: number): boolean and getLastSignInAgeMs(lastSignInAtIso): number | null helpers"
      exports: ["isSessionStale", "getLastSignInAgeMs"]
      min_lines: 25
    - path: "tests/lib/auth/lastSignInAt.test.ts"
      provides: "Unit tests for the freshness helper — null input → stale, < 24h → fresh, > 24h → stale, malformed ISO → stale"
      contains: "stale threshold 24h"
    - path: "tests/components/settings/SettingsTabsShell.test.tsx"
      provides: "Component tests for SET-01/02/03 — vertical orientation, pushState, hashchange, default tab, hash-with-querystring parser"
      contains: "renders 6 tabs in canonical order with vertical orientation"
    - path: "tests/components/settings/EmailChangeForm.test.tsx"
      provides: "Component tests for SET-04 — banner gates on new_email, input shows current email pre-confirm"
      contains: "banner gates on new_email"
    - path: "tests/components/settings/EmailChangePendingBanner.test.tsx"
      provides: "Component tests for SET-04 banner role/aria + resend behavior"
      contains: "renders aria-live status"
    - path: "tests/components/settings/PasswordChangeForm.test.tsx"
      provides: "Component tests for SET-05 fresh-session direct path AND server 401 reopens dialog (D-08 Option C defense-in-depth)"
      contains: "server 401 reopens dialog"
    - path: "tests/components/settings/PasswordReauthDialog.test.tsx"
      provides: "Component tests for SET-05 stale-session re-auth flow + neutral error"
      contains: "stale session re-auth flow"
    - path: "tests/components/settings/StatusToastHandler.test.tsx"
      provides: "Component test for D-13 toast on ?status=email_changed AND D-14 hash preservation when stripping ?status="
      contains: "strips status param preserving hash"
    - path: "tests/components/settings/PrivacySection.test.tsx"
      provides: "Component test for D-01 Privacy migration (3 PrivacyToggleRow instances render)"
    - path: "tests/components/settings/NotificationsSection.test.tsx"
      provides: "Component test for D-01 Notifications migration (2 PrivacyToggleRow toggles render)"
    - path: "tests/components/settings/PreferencesSection.test.tsx"
      provides: "Component test for D-01 — PreferencesClient embeds inside Preferences tab"
      contains: "embeds PreferencesClient unchanged"
    - path: "tests/components/settings/ProfileSection.test.tsx"
      provides: "Component test for D-19 read-only stub — displayName + @username + View public profile link"
    - path: "tests/components/settings/AccountSection.test.tsx"
      provides: "Top-level integration test that AccountSection composes EmailChangeForm + PasswordChangeForm"
    - path: "tests/app/auth-callback-route.test.ts"
      provides: "Route handler integration tests for SET-06 — 5-type redirect map, override matrix, same-origin guard, error fallthrough"
      contains: "email_change ignores next override"
    - path: "tests/app/preferences-redirect.test.ts"
      provides: "Page-level test for D-15 — /preferences → /settings#preferences with fragment preserved"
      contains: "redirects with hash preserved"
  key_links:
    - from: "src/lib/auth/lastSignInAt.ts"
      to: "tests/lib/auth/lastSignInAt.test.ts"
      via: "imports isSessionStale and getLastSignInAgeMs"
      pattern: "isSessionStale|getLastSignInAgeMs"
    - from: "tests/components/settings/PasswordChangeForm.test.tsx"
      to: "src/lib/auth/lastSignInAt.ts"
      via: "test file mocks or imports the helper to drive fresh/stale branches"
      pattern: "lastSignInAt"
---

<objective>
Wave 0 test scaffolding for Phase 22.

Purpose:
  - Create RED test skeletons for every test file listed in 22-VALIDATION.md so downstream Wave 1+ implementation tasks have an `<automated>` verify command from the moment they begin (Nyquist compliance).
  - Land the `src/lib/auth/lastSignInAt.ts` freshness helper plus passing unit tests — this is the only Wave 0 task that ships shipping code, because the helper has zero downstream dependencies and the tests are pure (no DOM, no network).
  - Retire the obsolete `tests/components/settings/SettingsClient.test.tsx` (its assertions about Collection chevron link, Coming-soon stubs, Account stubs, and Delete Account dialog all become invalid per CONTEXT.md D-02/D-03/D-04 — the file would cause cascading red tests once Plan 05 deletes the legacy `SettingsClient.tsx`).

Output:
  - 14 new test files (RED skeletons) under `tests/components/settings/`, `tests/app/`, `tests/lib/auth/`.
  - 1 new helper module `src/lib/auth/lastSignInAt.ts` (~25 LOC) with passing unit tests.
  - 1 deletion: `tests/components/settings/SettingsClient.test.tsx`.

This plan is foundational. Plans 02–05 reference these test files in their `<verify><automated>` blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-settings-restructure-account-section/22-CONTEXT.md
@.planning/phases/22-settings-restructure-account-section/22-RESEARCH.md
@.planning/phases/22-settings-restructure-account-section/22-VALIDATION.md
@.planning/phases/22-settings-restructure-account-section/22-UI-SPEC.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Existing test patterns to mirror — extracted from project tests directory -->

From `tests/components/settings/SettingsClient.test.tsx` (will be DELETED in this plan; included here so the executor can mirror the import + mock pattern):
```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'

vi.mock('@/app/actions/profile', () => ({
  updateProfileSettings: vi.fn(async () => ({ success: true, data: undefined })),
}))
```

From `src/components/settings/PrivacyToggleRow.tsx` props (used inside Privacy/Notifications section tests):
```typescript
interface PrivacyToggleRowProps {
  label: string
  description: string
  field: 'profilePublic' | 'collectionPublic' | 'wishlistPublic' | 'notifyOnFollow' | 'notifyOnWatchOverlap'
  initialValue: boolean
}
```

From Supabase auth-js types.d.ts (verified in 22-RESEARCH.md):
```typescript
export interface User {
  id: string
  email?: string                    // Current confirmed email
  new_email?: string                // Pending email
  email_change_sent_at?: string     // ISO timestamp
  last_sign_in_at?: string          // ISO timestamp — D-08 freshness signal (RECONCILED Option C)
}
```

From `package.json`:
```
"test": "vitest run"
"test:watch": "vitest"
```

From existing test config:
```
/Users/tylerwaneka/Documents/horlo/vitest.config.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lastSignInAt freshness helper + passing unit tests</name>
  <files>src/lib/auth/lastSignInAt.ts, tests/lib/auth/lastSignInAt.test.ts</files>
  <read_first>
    - .planning/phases/22-settings-restructure-account-section/22-RESEARCH.md (Critical Discrepancy section + Pattern 4 — note the RECONCILED D-08 + D-10 use `user.last_sign_in_at` per Option C, NOT JWT iat)
    - .planning/phases/22-settings-restructure-account-section/22-CONTEXT.md (D-08 RECONCILED 2026-04-30 + D-10 RECONCILED 2026-04-30 — see lines 45-53)
    - .planning/phases/22-settings-restructure-account-section/22-VALIDATION.md (Per-Task Verification Map: lastSignInAt unit tests are REQUIRED, NOT jwtIat)
    - src/lib/auth.ts (existing patterns to mirror — class UnauthorizedError + 'server-only' guard; this new file should NOT include 'server-only' since the helper runs client-side)
  </read_first>
  <behavior>
    Test 1: `isSessionStale(undefined)` returns `true` (defensive — no last_sign_in_at means stale).
    Test 2: `isSessionStale(null)` returns `true` (defensive — null means stale).
    Test 3: `isSessionStale('not-an-iso')` returns `true` (malformed string defaults to stale).
    Test 4: `isSessionStale(<ISO timestamp 1 hour ago>)` returns `false` (fresh).
    Test 5: `isSessionStale(<ISO timestamp 23 hours 59 minutes ago>)` returns `false` (still under threshold).
    Test 6: `isSessionStale(<ISO timestamp 24 hours 1 minute ago>)` returns `true` (just over threshold) — this is the "stale threshold 24h" assertion required by 22-VALIDATION.md.
    Test 7: `isSessionStale(<ISO 12 hours ago>, 6 * 3600 * 1000)` returns `true` when threshold overridden to 6h.
    Test 8: `getLastSignInAgeMs(<ISO 1 hour ago>)` returns ~3600000 (within ±5000ms tolerance for test runtime).
    Test 9: `getLastSignInAgeMs(undefined)` returns `null`.
    Test 10: `getLastSignInAgeMs('not-an-iso')` returns `null` (Date.parse returns NaN).
  </behavior>
  <action>
    Create `src/lib/auth/lastSignInAt.ts` exporting two pure functions:

    ```typescript
    /**
     * Returns the elapsed milliseconds since the last fresh sign-in, or null if
     * the input is missing or malformed.
     *
     * Used by Phase 22 password-change re-auth detection (RECONCILED D-08
     * 2026-04-30 — Option C: `user.last_sign_in_at` is the primary freshness
     * signal because it matches Supabase's server-side reauth check
     * (`session.created_at + 24h > now`) — verified in supabase/auth
     * `internal/api/user.go`. JWT `iat` was rejected because it rotates on
     * every silent token refresh and would let a 7-day-old session appear
     * fresh client-side while the server returns 401.)
     */
    export function getLastSignInAgeMs(
      lastSignInAtIso: string | null | undefined,
    ): number | null {
      if (lastSignInAtIso == null) return null
      const parsed = Date.parse(lastSignInAtIso)
      if (Number.isNaN(parsed)) return null
      return Date.now() - parsed
    }

    /**
     * Returns true when the session is older than `thresholdMs` (default 24h)
     * since the last fresh sign-in, OR when last_sign_in_at is null/malformed
     * (defensive default: assume stale to force re-auth).
     */
    export function isSessionStale(
      lastSignInAtIso: string | null | undefined,
      thresholdMs: number = 24 * 60 * 60 * 1000,
    ): boolean {
      const ageMs = getLastSignInAgeMs(lastSignInAtIso)
      if (ageMs === null) return true
      return ageMs > thresholdMs
    }
    ```

    Create `tests/lib/auth/lastSignInAt.test.ts` with 10 unit tests covering the behaviors above. Use `vi.useFakeTimers()` + `vi.setSystemTime(<fixed Date>)` to make the time-relative tests deterministic. Use plain ISO strings (`new Date(Date.now() - 3600000).toISOString()`) rather than mocking Date constructor.

    Required test names (these strings are referenced by 22-VALIDATION.md):
    - `it('returns true for null/undefined input', ...)` — covers Test 1 & 2
    - `it('returns true for malformed ISO', ...)` — covers Test 3
    - `it('returns false for fresh session under threshold (1h ago)', ...)` — Test 4
    - `it('returns false at 23h59m', ...)` — Test 5
    - `it('stale threshold 24h — returns true at 24h01m', ...)` — Test 6 (THIS NAME IS REQUIRED — 22-VALIDATION.md asserts on `-t "stale threshold 24h"`)
    - `it('honors custom threshold parameter', ...)` — Test 7
    - `it('getLastSignInAgeMs returns elapsed ms', ...)` — Test 8
    - `it('getLastSignInAgeMs returns null for null input', ...)` — Test 9
    - `it('getLastSignInAgeMs returns null for malformed ISO', ...)` — Test 10

    Run `npm test -- tests/lib/auth/lastSignInAt.test.ts` and confirm all 10 tests pass GREEN.
  </action>
  <verify>
    <automated>npm test -- tests/lib/auth/lastSignInAt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/auth/lastSignInAt.ts` exists.
    - File contains exactly two exports: `isSessionStale` and `getLastSignInAgeMs`.
    - File does NOT contain the string `'server-only'` (this helper runs client-side).
    - File contains the comment "RECONCILED D-08" referencing the 2026-04-30 reconciliation.
    - File `tests/lib/auth/lastSignInAt.test.ts` exists.
    - Test file contains the literal string `stale threshold 24h` in an `it(...)` name.
    - `npm test -- tests/lib/auth/lastSignInAt.test.ts` exits 0 and reports 10 passed tests.
    - `grep -c "isSessionStale\|getLastSignInAgeMs" tests/lib/auth/lastSignInAt.test.ts` returns at least 10 (each test calls one of the helpers).
  </acceptance_criteria>
  <done>
    `npm test -- tests/lib/auth/lastSignInAt.test.ts` passes with 10 GREEN tests; helper module + tests committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test scaffolds for tabs shell, account section, callback route, /preferences redirect</name>
  <files>tests/components/settings/SettingsTabsShell.test.tsx, tests/components/settings/AccountSection.test.tsx, tests/components/settings/EmailChangeForm.test.tsx, tests/components/settings/EmailChangePendingBanner.test.tsx, tests/components/settings/PasswordChangeForm.test.tsx, tests/components/settings/PasswordReauthDialog.test.tsx, tests/components/settings/StatusToastHandler.test.tsx, tests/app/auth-callback-route.test.ts, tests/app/preferences-redirect.test.ts</files>
  <read_first>
    - .planning/phases/22-settings-restructure-account-section/22-VALIDATION.md (Per-Task Verification Map — every `it(...)` name in this task MUST match a "-t" string in the map)
    - .planning/phases/22-settings-restructure-account-section/22-RESEARCH.md (Pattern 1-7 for expected component contracts; Pattern 5 for full route-handler shape)
    - .planning/phases/22-settings-restructure-account-section/22-UI-SPEC.md (Copywriting Contract — locked banner copy, locked dialog copy, status-toast copy)
    - .planning/phases/22-settings-restructure-account-section/22-CONTEXT.md (D-05 banner copy LOCKED; D-09 single-field dialog; D-11/D-12 redirect map; D-13/D-14 status strip + hash preservation)
    - tests/components/settings/SettingsClient.test.tsx (existing test mock pattern for `@/app/actions/profile` — replicate the mock)
    - src/components/ui/tabs.tsx (the tabs primitive — wrappers SettingsTabsShell will use)
    - src/components/ui/dialog.tsx (the dialog primitive — wrappers PasswordReauthDialog will use)
  </read_first>
  <action>
    Create RED test skeleton files. Each file uses `describe.todo` or `it.todo` for cases requiring components that don't exist yet, and uses real `it(...)` blocks (failing or pending) ONLY when scaffolding logic that is universally true (e.g., a render-and-find-by-text test against a component to be implemented).

    **PREFERRED SHAPE for each skeleton:** Real `describe(...)` block with an inner `it.todo('...')` for every required test name. Vitest reports `it.todo` as pending (yellow), keeps the suite GREEN, and downstream Plans 02–05 will replace `.todo` with real assertions when they implement the component.

    **2A. `tests/components/settings/SettingsTabsShell.test.tsx`** — covers SET-01/02/03 + hash parser:
    ```typescript
    import { describe, it } from 'vitest'

    describe('SettingsTabsShell — Phase 22 SET-01/02/03', () => {
      it.todo('renders 6 tabs in canonical order with vertical orientation')
      it.todo('uses pushState (not router.push) on tab change')
      it.todo('responds to hashchange event for browser back/forward')
      it.todo('default tab is account when hash is empty (D-17 replaceState)')
      it.todo('parses hash with querystring (#account?status=email_changed)')
      it.todo('falls through to account tab on unknown hash value')
    })
    ```

    **2B. `tests/components/settings/AccountSection.test.tsx`** — top-level composition:
    ```typescript
    import { describe, it } from 'vitest'

    describe('AccountSection — Phase 22 SET-04 + SET-05 composition', () => {
      it.todo('renders Email card with EmailChangeForm')
      it.todo('renders Password card with PasswordChangeForm')
      it.todo('email and password sections are separated by space-y-8')
    })
    ```

    **2C. `tests/components/settings/EmailChangeForm.test.tsx`** — SET-04:
    ```typescript
    import { describe, it, vi } from 'vitest'

    vi.mock('@/lib/supabase/client', () => ({
      createSupabaseBrowserClient: vi.fn(() => ({
        auth: {
          updateUser: vi.fn(async () => ({ data: { user: null }, error: null })),
        },
      })),
    }))

    describe('EmailChangeForm — Phase 22 SET-04', () => {
      it.todo('banner gates on new_email — banner only renders when pendingNewEmail is non-null')
      it.todo('input shows current email pre-confirmation (NOT new_email — T-22-S4 mitigation)')
      it.todo('submit calls updateUser({ email: newEmail })')
      it.todo('error path surfaces server error fallback copy')
    })
    ```

    **2D. `tests/components/settings/EmailChangePendingBanner.test.tsx`** — SET-04 banner:
    ```typescript
    import { describe, it, vi } from 'vitest'

    vi.mock('@/lib/supabase/client', () => ({
      createSupabaseBrowserClient: vi.fn(() => ({
        auth: { updateUser: vi.fn(async () => ({ error: null })) },
      })),
    }))

    describe('EmailChangePendingBanner — Phase 22 SET-04 D-05/D-06', () => {
      it.todo('renders aria-live status region with role="status" aria-live="polite"')
      it.todo('renders locked banner copy with bolded oldEmail and newEmail')
      it.todo('resend re-fires updateUser({ email: pendingEmail }) — D-06 same-call pattern')
      it.todo('does NOT render a Cancel button (D-06 explicit)')
    })
    ```

    **2E. `tests/components/settings/PasswordChangeForm.test.tsx`** — SET-05 fresh path + 401 reopen (D-08 Option C defense-in-depth):
    ```typescript
    import { describe, it, vi } from 'vitest'

    vi.mock('@/lib/supabase/client', () => ({
      createSupabaseBrowserClient: vi.fn(),
    }))

    describe('PasswordChangeForm — Phase 22 SET-05 D-10', () => {
      it.todo('fresh session updates directly (last_sign_in_at < 24h, no dialog)')
      it.todo('stale session opens re-auth dialog (last_sign_in_at > 24h)')
      it.todo('passwords-do-not-match validation surfaces inline')
      it.todo('length-too-short validation surfaces inline')
      it.todo('server 401 reopens dialog (RECONCILED D-08 Option C defense-in-depth)')
    })
    ```

    **2F. `tests/components/settings/PasswordReauthDialog.test.tsx`** — SET-05 stale path:
    ```typescript
    import { describe, it, vi } from 'vitest'

    vi.mock('@/lib/supabase/client', () => ({
      createSupabaseBrowserClient: vi.fn(),
    }))

    describe('PasswordReauthDialog — Phase 22 SET-05 D-09', () => {
      it.todo('stale session re-auth flow — signInWithPassword then updateUser({password})')
      it.todo('neutral error on signInWithPassword failure — copy is "Password incorrect."')
      it.todo('cancel closes dialog and leaves form populated')
      it.todo('renders single field for current password (D-09 single-field)')
    })
    ```

    **2G. `tests/components/settings/StatusToastHandler.test.tsx`** — D-13/D-14:
    ```typescript
    import { describe, it, vi } from 'vitest'

    vi.mock('sonner', () => ({
      toast: { success: vi.fn(), error: vi.fn() },
    }))

    describe('StatusToastHandler — Phase 22 D-13/D-14', () => {
      it.todo('fires toast.success on ?status=email_changed')
      it.todo('strips status param preserving hash (D-14 — router.replace(pathname + hash) NOT router.replace(pathname))')
      it.todo('does not fire toast on unknown status value')
      it.todo('uses ref guard to prevent Strict-Mode double-fire (FG-5)')
    })
    ```

    **2H. `tests/app/auth-callback-route.test.ts`** — SET-06 5-type redirect map. This file should NOT use `.todo` for ALL cases — it should set up a real test harness (mock `createSupabaseServerClient`, mock `verifyOtp`) and have cases STUBBED with `it.todo` because the route hasn't been refactored yet:
    ```typescript
    import { describe, it, vi, beforeEach } from 'vitest'

    vi.mock('@/lib/supabase/server', () => ({
      createSupabaseServerClient: vi.fn(),
    }))

    describe('GET /auth/callback — Phase 22 SET-06 5-type redirect map', () => {
      beforeEach(() => { vi.clearAllMocks() })
      it.todo('signup redirect — type=signup → /?status=email_confirmed')
      it.todo('recovery redirect — type=recovery → /reset-password')
      it.todo('email_change redirect with hash and status — /settings#account?status=email_changed')
      it.todo('email_change ignores next override — D-12 NEVER overridable')
      it.todo('signup honors next override — type=signup&next=/profile → /profile')
      it.todo('signup rejects offsite next — next=//evil.com → falls back to default')
      it.todo('magiclink and invite redirects — magiclink → /?status=signed_in; invite → /signup?status=invited')
      it.todo('email alias coerces to signup — type=email → coerced to signup redirect')
      it.todo('unknown type falls through to error — /login?error=invalid_link')
      it.todo('verifyOtp error falls through — /login?error=invalid_link')
    })
    ```

    **2I. `tests/app/preferences-redirect.test.ts`** — D-15:
    ```typescript
    import { describe, it } from 'vitest'

    describe('GET /preferences — Phase 22 D-15 redirect to /settings#preferences', () => {
      it.todo('redirects with hash preserved — Location header = /settings#preferences')
      it.todo('uses Next.js redirect() (server-side, not Client Component)')
    })
    ```

    Run `npm test -- tests/components/settings tests/app/auth-callback-route.test.ts tests/app/preferences-redirect.test.ts` and confirm:
    - All 9 test files load without import errors.
    - All `it.todo(...)` cases report as PENDING (yellow), not RED (failing).
    - Suite is GREEN at the file/run level (no failures, only pending todos).

    **CRITICAL — DO NOT use `it.skip`**, that hides the test count. `it.todo` reports the test name in vitest output and surfaces it as a pending TODO.
  </action>
  <verify>
    <automated>npm test -- tests/components/settings/SettingsTabsShell.test.tsx tests/components/settings/AccountSection.test.tsx tests/components/settings/EmailChangeForm.test.tsx tests/components/settings/EmailChangePendingBanner.test.tsx tests/components/settings/PasswordChangeForm.test.tsx tests/components/settings/PasswordReauthDialog.test.tsx tests/components/settings/StatusToastHandler.test.tsx tests/app/auth-callback-route.test.ts tests/app/preferences-redirect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 9 test files exist at the listed paths.
    - `grep -l "it.todo" tests/components/settings/SettingsTabsShell.test.tsx tests/components/settings/AccountSection.test.tsx tests/components/settings/EmailChangeForm.test.tsx tests/components/settings/EmailChangePendingBanner.test.tsx tests/components/settings/PasswordChangeForm.test.tsx tests/components/settings/PasswordReauthDialog.test.tsx tests/components/settings/StatusToastHandler.test.tsx tests/app/auth-callback-route.test.ts tests/app/preferences-redirect.test.ts` returns all 9 files.
    - `grep -c "it.skip" tests/components/settings/*.test.tsx tests/app/*.test.ts` is 0 (no skip — todo only).
    - `grep -l "stale threshold 24h" tests/components/settings/*.test.tsx` is empty (this name lives ONLY in `tests/lib/auth/lastSignInAt.test.ts` from Task 1).
    - The literal strings below MUST appear in `it.todo(...)` calls (verified via grep so 22-VALIDATION.md `-t` references resolve later):
      - `renders 6 tabs in canonical order with vertical orientation`
      - `uses pushState`
      - `responds to hashchange`
      - `default tab is account`
      - `parses hash with querystring`
      - `banner gates on new_email`
      - `input shows current email pre-confirmation`
      - `resend re-fires updateUser`
      - `renders aria-live status`
      - `fresh session updates directly`
      - `stale session re-auth flow`
      - `neutral error on signInWithPassword failure`
      - `server 401 reopens dialog`
      - `signup redirect`
      - `recovery redirect`
      - `email_change redirect with hash and status`
      - `email_change ignores next override`
      - `signup honors next override`
      - `signup rejects offsite next`
      - `magiclink and invite redirects`
      - `email alias coerces to signup`
      - `unknown type falls through to error`
      - `verifyOtp error falls through`
      - `strips status param preserving hash`
      - `redirects with hash preserved`
    - `npm test -- tests/components/settings tests/app/auth-callback-route.test.ts tests/app/preferences-redirect.test.ts` exits 0 (vitest reports `passed | todo` summary; no failures).
  </acceptance_criteria>
  <done>
    9 RED test scaffolds exist with `it.todo` cases matching all 22-VALIDATION.md `-t` references; full suite passes with todos pending; downstream plans can target specific test names.
  </done>
</task>

<task type="auto">
  <name>Task 3: Section migration test scaffolds + retire stale SettingsClient.test.tsx</name>
  <files>tests/components/settings/PrivacySection.test.tsx, tests/components/settings/NotificationsSection.test.tsx, tests/components/settings/PreferencesSection.test.tsx, tests/components/settings/ProfileSection.test.tsx, tests/components/settings/SettingsClient.test.tsx</files>
  <read_first>
    - tests/components/settings/SettingsClient.test.tsx (the file to be deleted — read first to understand which assertions become invalid per D-02..D-04)
    - .planning/phases/22-settings-restructure-account-section/22-CONTEXT.md (D-01 migration scope; D-19 Profile stub; D-02/D-03/D-04 deletions that invalidate the existing tests)
    - .planning/phases/22-settings-restructure-account-section/22-UI-SPEC.md (ProfileSection visual spec lines 401-424; PrivacySection wraps 3 toggles; NotificationsSection wraps 2 toggles)
    - src/components/preferences/PreferencesClient.tsx (the embedded component for the PreferencesSection embed test)
    - src/components/settings/PrivacyToggleRow.tsx (the toggle behavior — note its `field` prop union)
  </read_first>
  <action>
    **3A. DELETE `tests/components/settings/SettingsClient.test.tsx`** using `git rm` or filesystem delete. This file's assertions about the Collection chevron link, the Account stubs, the Coming-soon Theme, the Data Preferences section, and the Delete Account dialog ALL become invalid in Phase 22 per CONTEXT.md D-02 (Collection chevron removed), D-03 (Account stubs deleted), D-04 (Coming-soon stubs deleted). The file would cascade-fail the moment Plan 05 deletes the legacy `SettingsClient.tsx`. Per 22-VALIDATION.md "Wave 0 Requirements" line: "**Retire** existing `tests/components/settings/SettingsClient.test.tsx`".

    **3B. `tests/components/settings/PrivacySection.test.tsx`** — D-01 Privacy migration:
    ```typescript
    import { describe, it, vi } from 'vitest'

    vi.mock('@/app/actions/profile', () => ({
      updateProfileSettings: vi.fn(async () => ({ success: true, data: undefined })),
    }))

    describe('PrivacySection — Phase 22 D-01 migration', () => {
      it.todo('renders 3 PrivacyToggleRow instances — profilePublic, collectionPublic, wishlistPublic')
      it.todo('regression: profilePublic toggle behavior unchanged from SettingsClient')
      it.todo('renders inside a SettingsSection card frame')
    })
    ```

    **3C. `tests/components/settings/NotificationsSection.test.tsx`** — D-01 Notifications migration:
    ```typescript
    import { describe, it, vi } from 'vitest'

    vi.mock('@/app/actions/profile', () => ({
      updateProfileSettings: vi.fn(async () => ({ success: true, data: undefined })),
    }))

    describe('NotificationsSection — Phase 22 D-01 migration', () => {
      it.todo('renders 2 PrivacyToggleRow instances — notifyOnFollow, notifyOnWatchOverlap')
      it.todo('regression: notifyOnFollow toggle behavior unchanged from SettingsClient')
      it.todo('renders inside a SettingsSection card frame')
    })
    ```

    **3D. `tests/components/settings/PreferencesSection.test.tsx`** — D-01 PreferencesClient embed:
    ```typescript
    import { describe, it, vi } from 'vitest'

    vi.mock('@/app/actions/preferences', () => ({
      savePreferences: vi.fn(async () => ({ success: true, data: {} })),
    }))

    describe('PreferencesSection — Phase 22 D-01 embed', () => {
      it.todo('embeds PreferencesClient unchanged — preferences prop passes through')
      it.todo('does not double-wrap PreferencesClient in an outer card')
    })
    ```

    **3E. `tests/components/settings/ProfileSection.test.tsx`** — D-19 stub:
    ```typescript
    import { describe, it } from 'vitest'

    describe('ProfileSection — Phase 22 D-19 read-only stub', () => {
      it.todo('renders displayName when present, falls back to username')
      it.todo('renders @username at text-sm text-muted-foreground')
      it.todo('renders View public profile link to /u/{username}')
      it.todo('renders avatar image when avatarUrl is present')
      it.todo('renders muted bg-muted placeholder when avatarUrl is null')
      it.todo('renders "Profile editing coming in the next update." footer note')
    })
    ```

    Run the migration test scaffolds and verify GREEN (all `it.todo` pending). Verify the deleted file is gone.
  </action>
  <verify>
    <automated>test ! -f tests/components/settings/SettingsClient.test.tsx && npm test -- tests/components/settings/PrivacySection.test.tsx tests/components/settings/NotificationsSection.test.tsx tests/components/settings/PreferencesSection.test.tsx tests/components/settings/ProfileSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f tests/components/settings/SettingsClient.test.tsx` returns true (file deleted).
    - All 4 new migration test files exist.
    - Each test file contains the literal strings required by 22-VALIDATION.md, including the `it.todo` for "embeds PreferencesClient unchanged" in `PreferencesSection.test.tsx`.
    - `git status` shows the deletion of `tests/components/settings/SettingsClient.test.tsx` (deletion will be staged in the commit step).
    - `npm test -- tests/components/settings/PrivacySection.test.tsx tests/components/settings/NotificationsSection.test.tsx tests/components/settings/PreferencesSection.test.tsx tests/components/settings/ProfileSection.test.tsx` exits 0 with all todos pending.
  </acceptance_criteria>
  <done>
    Stale `SettingsClient.test.tsx` retired (deleted), 4 migration test scaffolds added, full suite GREEN with todos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime | Pure jsdom; no untrusted input crosses here. Tests do not exercise live Supabase. |
| Helper module | `src/lib/auth/lastSignInAt.ts` runs client-side. Input (`last_sign_in_at` ISO string) comes from Supabase auth-js User object — Supabase-controlled, not user-supplied. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-S5 | Tampering | `src/lib/auth/lastSignInAt.ts` | mitigate | Helper returns `true` (stale) for null/undefined/malformed input — defensive default forces re-auth dialog instead of silently bypassing it. Verified by Test 1, 2, 3 in `tests/lib/auth/lastSignInAt.test.ts`. Server-side enforcement via Supabase remains the authoritative gate (RECONCILED D-08 Option C: client signal mirrors server `session.created_at + 24h` check via `last_sign_in_at`; 401 catch is the second line). |
| T-22-S5b | Information disclosure | Test fixtures | accept | Tests use synthetic ISO timestamps generated at runtime (no real user data; no PII); jsdom env is sandboxed. |
| T-22-Test01 | DoS via flaky tests | All scaffolds use `it.todo` | mitigate | `.todo` is non-flaky by definition (always pending until replaced); Plans 02–05 replace each `.todo` with a real assertion when the corresponding component lands. No watch-mode flags used. |
</threat_model>

<verification>
- All 14 test files from `22-VALIDATION.md` "Wave 0 Requirements" exist (13 new + 1 deletion of stale file).
- `npm test -- tests/components/settings tests/app/auth-callback-route.test.ts tests/app/preferences-redirect.test.ts tests/lib/auth/lastSignInAt.test.ts` exits 0.
- `tests/lib/auth/lastSignInAt.test.ts` reports 10 GREEN tests (this is the only Wave 0 task with passing assertions; everything else is `.todo` pending).
- `tests/components/settings/SettingsClient.test.tsx` no longer exists.
- `git status` shows the staged deletion + 14 new files.
- Wave 1+ plans can target specific test names via `npm test -- <file> -t "<test name>"` because all `it.todo` strings exist verbatim in the source files.
</verification>

<success_criteria>
- [ ] `src/lib/auth/lastSignInAt.ts` exists with `isSessionStale` + `getLastSignInAgeMs` exports.
- [ ] `tests/lib/auth/lastSignInAt.test.ts` runs 10 GREEN tests.
- [ ] All 13 RED-skeleton test files exist with `it.todo(...)` cases matching every `-t` reference in 22-VALIDATION.md.
- [ ] Deleted `tests/components/settings/SettingsClient.test.tsx` no longer exists on disk.
- [ ] Quick command from 22-VALIDATION.md (`npm test -- tests/components/settings tests/app/auth-callback-route.test.ts tests/app/preferences-redirect.test.ts tests/lib/auth/lastSignInAt.test.ts`) exits 0 in under 30 seconds.
</success_criteria>

<output>
After completion, create `.planning/phases/22-settings-restructure-account-section/22-01-SUMMARY.md` describing:
- The lastSignInAt helper signature and rationale (RECONCILED D-08 Option C).
- The set of test files created and the convention used (`it.todo` pending pattern).
- Confirmation that the stale `SettingsClient.test.tsx` was retired.
- The full quick-test command for Plans 02–05 to copy.
</output>
