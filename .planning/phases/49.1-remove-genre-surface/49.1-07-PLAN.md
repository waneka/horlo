---
phase: 49.1
plan: 07
type: execute
wave: 3
depends_on: [49.1-06]
files_modified:
  - src/db/schema.ts
  - drizzle/0012_phase49_1_drop_primary_archetype.sql
autonomous: true
requirements: [TAX-02]
must_haves:
  truths:
    - "src/db/schema.ts watchesCatalog table no longer declares primary_archetype"
    - "A new drizzle migration file at drizzle/0012_phase49_1_drop_primary_archetype.sql contains ALTER TABLE watches_catalog DROP COLUMN IF EXISTS primary_archetype"
    - "The local Postgres dev DB no longer has the primary_archetype column on watches_catalog"
    - "drizzle/meta/_journal.json contains the new 0012 entry after drizzle-kit push runs"
  artifacts:
    - path: src/db/schema.ts
      provides: "watchesCatalog table without the primary_archetype column"
    - path: drizzle/0012_phase49_1_drop_primary_archetype.sql
      provides: "Idempotent DROP COLUMN IF EXISTS migration"
      contains: "DROP COLUMN IF EXISTS"
  key_links:
    - from: src/db/schema.ts
      to: drizzle/0012_phase49_1_drop_primary_archetype.sql
      via: "drizzle-kit push reads schema.ts diffs against the local DB and applies"
      pattern: "watchesCatalog"
---

<objective>
Apply the local-side database schema change: drop `primary_archetype` from `watchesCatalog` in `src/db/schema.ts`, author the corresponding drizzle migration `0012_phase49_1_drop_primary_archetype.sql`, and run `drizzle-kit push` against the local Postgres dev DB so the dev environment is in sync with the type system. This plan does NOT touch prod — Plan 08 owns the supabase prod migration.

Purpose: D-MIG-01 + D-MIG-02. Per the sequencing constraint, the drizzle migration may land before the prod supabase migration so dev environments stay aligned with code. All reader/writer code references to the column are already gone (Plans 02-06), so this column drop is safe at the local layer.

Output: 1 modified source file (schema.ts) + 1 new SQL migration file + verified local DB state with the column absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49.1-remove-genre-surface/49.1-CONTEXT.md
@.planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md
@.planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md

# Files being modified or created
@src/db/schema.ts
@drizzle.config.ts

# Existing analog for header-comment style
@drizzle/0011_phase38_catalog_id_notnull.sql

# Wave 2 SUMMARY - read for context on what code references existed
@.planning/phases/49.1-remove-genre-surface/49.1-06-SUMMARY.md

# Project memories that govern this plan
# project_drizzle_supabase_db_mismatch — drizzle-kit push is LOCAL ONLY
# project_db_wipeable_2026_05_09 — watches_catalog is NOT wipeable, but this is a schema drop not a data migration

<interfaces>
<!-- Analog header-comment style from drizzle/0011_phase38_catalog_id_notnull.sql -->

```sql
-- Phase 38 Plan 01 — watches.catalog_id NOT NULL (Drizzle type catch-up)
--
-- Phase 36 shipped the DB-level SET NOT NULL to production. This migration
-- brings the Drizzle schema in sync so drizzle-kit no longer diffs catalog_id
-- as nullable. Idempotent: safe to run when the constraint already exists.
--> statement-breakpoint
ALTER TABLE "watches" ALTER COLUMN "catalog_id" SET NOT NULL;
```

<!-- Drizzle config (drizzle.config.ts) -->

drizzle-kit out directory is `./drizzle` (not `./drizzle/migrations`), and dialect is postgresql. drizzle-kit push reads DATABASE_URL from .env.local.

<!-- Postgres CHECK constraint behavior -->

The `watches_catalog_primary_archetype_check` CHECK constraint added in supabase/migrations/20260430000000_phase19_1_taste_constraints.sql is dropped IMPLICITLY when the column is dropped (standard Postgres behavior). No explicit DROP CONSTRAINT statement is needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove primary_archetype column declaration from src/db/schema.ts watchesCatalog</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts (full watchesCatalog declaration around :334-405; line 390 is the primary_archetype declaration per RESEARCH)
    - .planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md §Pitfall 2 (schema.ts and DB column must change together)
  </read_first>
  <action>
    In src/db/schema.ts, inside the watchesCatalog pgTable declaration (around lines 334-405), DELETE the line at approximately :390 that reads `primary_archetype: text('primary_archetype')` (or whatever the exact drizzle column declaration is — verify by reading the file). The surrounding sibling columns (era_signal at approximately :388, design_motifs at approximately :392, formality, sportiness, heritage_score, etc.) stay UNCHANGED.

    Do NOT remove the `text` import from drizzle-orm/pg-core if other columns still use it (verify by grep).

    NOTE: After this edit, the local Postgres dev DB still has the column (until Task 2's drizzle-kit push runs). Any query against the dev DB will work but drizzle TypeScript types will not project the column anymore. This brief inconsistency window is acceptable because Task 2 follows immediately.
  </action>
  <verify>
    <automated>grep -n "primary_archetype" src/db/schema.ts || echo OK ; grep -nE "era_signal|design_motifs" src/db/schema.ts | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "primary_archetype" src/db/schema.ts` returns no matches
    - `grep -n "primaryArchetype" src/db/schema.ts` returns no matches
    - `grep -n "era_signal" src/db/schema.ts` still returns at least 1 match (sibling column preserved)
    - `grep -n "design_motifs" src/db/schema.ts` still returns at least 1 match (sibling column preserved)
    - The watchesCatalog declaration parses without TypeScript errors (no orphan property comma or syntax issue)
    - `npx tsc --noEmit` exits 0 (the type system is now FULLY clean — no references to primaryArchetype anywhere except inside the retained PrimaryArchetype union type at src/lib/types.ts:234-236 and inside ARCHETYPE_CONFIG at src/lib/archetype-config.ts)
  </acceptance_criteria>
  <done>schema.ts no longer declares the column; type system is fully clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Author drizzle/0012_phase49_1_drop_primary_archetype.sql and run drizzle-kit push against the local dev DB</name>
  <files>drizzle/0012_phase49_1_drop_primary_archetype.sql</files>
  <read_first>
    - drizzle/0011_phase38_catalog_id_notnull.sql (analog header-comment style and statement-breakpoint syntax)
    - drizzle/meta/_journal.json (confirm latest idx is 11; new file MUST be 0012)
    - .planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md §"Code Examples — Drizzle migration: DROP COLUMN with IF EXISTS"
    - .planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md §"Migration: drizzle/0012_phase49_1_drop_primary_archetype.sql"
  </read_first>
  <action>
    Create a new file at `drizzle/0012_phase49_1_drop_primary_archetype.sql` with this exact content (matching the analog's header-comment style and the `--> statement-breakpoint` separator used in 0011):

    ```
    -- Phase 49.1 — Remove genre surface. Drops the primary_archetype column from
    -- watches_catalog. The watches_catalog_primary_archetype_check CHECK constraint
    -- (added in supabase/migrations/20260430000000_phase19_1_taste_constraints.sql)
    -- is dropped implicitly by Postgres along with the column.
    --
    -- Idempotent: re-running is safe (IF EXISTS).
    -- Sequencing (D-MIG-02): runs LAST in dev (Plan 07). The prod supabase mirror
    -- (Plan 08) runs after all reader code has been deployed to prod.
    --> statement-breakpoint
    ALTER TABLE "watches_catalog" DROP COLUMN IF EXISTS "primary_archetype";
    ```

    Verify the filename ordinal is EXACTLY 0012 (the next sequential value after the existing 0011_phase38_catalog_id_notnull.sql). DO NOT use any date prefix — drizzle uses sequential ordinals; supabase uses date prefixes. The two conventions are different and must not be conflated.

    Then run `npx drizzle-kit push` against the local dev DB. This will:
    1. Read src/db/schema.ts (which no longer declares the column after Task 1)
    2. Compare against the local Postgres state
    3. Detect the drop
    4. Apply the ALTER TABLE statement
    5. Update drizzle/meta/_journal.json with the new 0012 entry

    Expected interactive prompt: drizzle-kit may ask to confirm the destructive change (DROP COLUMN). Respond `yes` / accept. If running in non-interactive mode, use `npx drizzle-kit push --strict=false` or whatever the equivalent flag is in drizzle-kit 0.31.10 (read `npx drizzle-kit push --help` first to confirm flag name; do NOT guess).

    After the push, verify with:
    - `npx drizzle-kit check` — should report no pending changes
    - `psql "$DATABASE_URL" -c "\\d watches_catalog"` (or equivalent) — column list should NOT include primary_archetype

    NOTE on supabase docker: if the local dev DB is the supabase_db_horlo docker container at 127.0.0.1:54322 (per RESEARCH §"Local Postgres (supabase docker)"), the docker container must be running. If `drizzle-kit push` fails to connect, instruct the user to run `supabase start` first.
  </action>
  <verify>
    <automated>test -f drizzle/0012_phase49_1_drop_primary_archetype.sql && grep -n "DROP COLUMN IF EXISTS" drizzle/0012_phase49_1_drop_primary_archetype.sql ; cat drizzle/meta/_journal.json 2>/dev/null | grep "0012" | head -3 ; echo "---verify column absence via drizzle introspect:"; npx drizzle-kit check 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f drizzle/0012_phase49_1_drop_primary_archetype.sql` exits 0
    - File contains the literal string `ALTER TABLE "watches_catalog" DROP COLUMN IF EXISTS "primary_archetype"`
    - File starts with the header comment block matching the analog's style
    - drizzle/meta/_journal.json contains an entry for the new migration with `idx: 12` (or whatever drizzle-kit increments to)
    - `npx drizzle-kit check` reports no pending schema diffs
    - Local Postgres `watches_catalog` table has no `primary_archetype` column (verified via `\d` introspection or `information_schema.columns` query)
    - The Wave 0 test `tests/integration/migration-drop-archetype.test.ts` transitions to fully green if DATABASE_URL is set (all three assertions pass; column-absent assertion was the last red one)
  </acceptance_criteria>
  <done>Drizzle migration file authored; local dev DB column dropped; journal updated; Wave 0 integration test green on local.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → local Postgres | drizzle-kit push uses DATABASE_URL from .env.local; LOCAL ONLY per memory project_drizzle_supabase_db_mismatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49.1-15 | D (Denial of service / data loss) | local watches_catalog table | accept | The column drop is destructive by design (D-MIG-01: no data backfill). On the local dev DB, ~100 rows lose their primary_archetype value; per spike Q1 99% of that signal is preserved in style_tags. Local dev is rebuildable from the bootstrap seed. |
| T-49.1-16 | T (Tampering) | drizzle/meta/_journal.json | mitigate | drizzle-kit owns this file; manual edits forbidden. The ordinal 0012 is auto-incremented by drizzle-kit push, not by the planner — the file is just created so drizzle-kit sees the intent. |
| T-49.1-17 | E (Elevation of privilege) | drizzle-kit push command | accept | drizzle-kit push has full DDL privilege on the local dev DB (it owns the connection). Mitigation: drizzle-kit prompts before destructive operations; the developer manually confirms. |
</threat_model>

<verification>
- `npx drizzle-kit check` reports no pending schema diffs
- `npx tsc --noEmit` exits 0
- `npm test` (full suite) exits 0, INCLUDING the Wave 0 migration integration test if DATABASE_URL is set
- The `primary_archetype` column is absent from the local Postgres watches_catalog table (verified via psql introspection or drizzle introspect)
</verification>

<success_criteria>
- ROADMAP SC#1 partial (local side): the column is dropped on the local dev DB
- D-MIG-01 applied: one drizzle migration file (0012_...) with idempotent DROP COLUMN IF EXISTS body
- D-MIG-02 applied: this plan runs AFTER all reader code is removed (Plans 02-06) but BEFORE the prod push (Plan 08)
- Wave 0 integration test fully green on local
- Prod push (Plan 08) can now run safely — no remaining code references to the column exist
</success_criteria>

<output>
After completion, create `.planning/phases/49.1-remove-genre-surface/49.1-07-SUMMARY.md`
</output>
