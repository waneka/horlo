---
phase: 49.1
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/similarity.ts
  - tests/similarity.test.ts
autonomous: true
requirements: [TAX-02]
must_haves:
  truths:
    - "TASTE_SUB_WEIGHTS is computed from TASTE_SUB_WEIGHTS_BASE via an algorithmic RESCALE (no hardcoded 0.50/0.25/0.25 magic numbers)"
    - "TASTE_SUB_WEIGHTS has exactly 3 keys: numericTrioCosine, eraMatch, motifsJaccard"
    - "The archetypeMatch categorical-match block at similarity.ts:124-127 is deleted"
    - "Effective taste contribution remains in [0, 0.20] (outer TASTE_WEIGHT unchanged)"
    - "THRESHOLDS and GOAL_THRESHOLDS objects are byte-identical to pre-49.1 values"
  artifacts:
    - path: src/lib/similarity.ts
      provides: "Rebalanced TASTE_SUB_WEIGHTS via algorithmic 1.25x rescale; archetype categorical block deleted"
      contains: "TASTE_SUB_WEIGHTS_BASE"
      exports: "TASTE_SUB_WEIGHTS (new public export added alongside existing `export { GOAL_THRESHOLDS, detectLoyalBrands }` so tests can assert the sum-to-1.0 invariant — see Task 1 step 4)"
  key_links:
    - from: src/lib/similarity.ts
      to: src/lib/types.ts
      via: "tasteSimilarityRaw01 reads CatalogTasteAttributes fields formality/sportiness/heritageScore/eraSignal/designMotifs (no primaryArchetype after this plan)"
      pattern: "TASTE_SUB_WEIGHTS\\.(numericTrioCosine|eraMatch|motifsJaccard)"
---

<objective>
Execute the D-SIM decision group: rebalance TASTE_SUB_WEIGHTS via the proportional rescale formula RESCALE = 1 / (1 - REMOVED_SUB_WEIGHT) and remove the archetype categorical-match block from tasteSimilarityRaw01. The rebalance follows the existing anti-magic-number pattern at similarity.ts:27-32 (the WEIGHTS literal). Outer TASTE_WEIGHT = 0.20 and the THRESHOLDS / GOAL_THRESHOLDS objects are UNCHANGED.

Purpose: D-SIM-01 + D-SIM-02 + D-SIM-03 + D-SIM-04. Engine-side weight redistribution. The 0.04 effective weight previously assigned to archetypeMatch is redistributed proportionally across numericTrioCosine, eraMatch, motifsJaccard so the surviving sub-weights still sum to 1.0.

Output: 2 modified files. Sub-weights are computed algorithmically, never hardcoded. Tests assert both the sum-to-1.0 invariant and the post-rescale values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49.1-remove-genre-surface/49.1-CONTEXT.md
@.planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md
@.planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md

# Files being modified
@src/lib/similarity.ts
@tests/similarity.test.ts

<interfaces>
<!-- From src/lib/similarity.ts (current state, pre-49.1) -->

Lines 10-12 forbid hardcoded rescaled values:
```
// Anti-pattern (forbidden): hardcoding rescaled values as magic numbers.
```

Lines 13-32 are the EXISTING precedent — EXISTING_WEIGHTS_BASE plus Object.fromEntries Map+RESCALE — that the new TASTE_SUB_WEIGHTS rebalance must mirror.

Lines 40-45 (current TASTE_SUB_WEIGHTS) include `archetypeMatch: 0.20` — this key goes away. The other three keys persist with new values via RESCALE.

Lines 124-127 (current archetype categorical-match block):
```
if (t1.primaryArchetype !== null && t2.primaryArchetype !== null && t1.primaryArchetype === t2.primaryArchetype) {
  contrib += TASTE_SUB_WEIGHTS.archetypeMatch * 1.0
}
```
DELETE this block. The surrounding era-match block at :130-132 STAYS UNCHANGED and is the sibling-survivor pattern.

TASTE_WEIGHT = 0.20 at line 24 is UNCHANGED. THRESHOLDS at lines 48-52 are UNCHANGED. GOAL_THRESHOLDS at lines 57-62 are UNCHANGED.

Current export surface (line 79): `export { GOAL_THRESHOLDS, detectLoyalBrands }`. TASTE_SUB_WEIGHTS is NOT currently exported. Task 1 step 4 ADDS it to the export surface so tests can import and assert on it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebalance TASTE_SUB_WEIGHTS via algorithmic RESCALE; delete archetypeMatch categorical block; assert invariants in tests/similarity.test.ts</name>
  <files>src/lib/similarity.ts, tests/similarity.test.ts</files>
  <read_first>
    - src/lib/similarity.ts (full file - especially lines 1-50 for the weights region and lines 100-140 for tasteSimilarityRaw01)
    - tests/similarity.test.ts (full file - locate any existing TASTE_SUB_WEIGHTS assertions or describe blocks; if absent, identify where to add the new it block)
    - .planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md §"src/lib/similarity.ts - TASTE_SUB_WEIGHTS rebalance (D-SIM-02)"
    - .planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md §"Pattern 3: Algorithmic weight rescale (anti-magic-number)"
  </read_first>
  <behavior>
    - similarity.ts exports a TASTE_SUB_WEIGHTS object with 3 keys whose values are computed by Object.fromEntries iterating TASTE_SUB_WEIGHTS_BASE entries multiplied by RESCALE.
    - TASTE_SUB_WEIGHTS_BASE has 3 keys: numericTrioCosine: 0.40, eraMatch: 0.20, motifsJaccard: 0.20 — annotated with "as const" and a comment that the values sum to 0.80 (after removing 0.20 archetypeMatch).
    - REMOVED_SUB_WEIGHT = 0.20 and RESCALE = 1 / (1 - REMOVED_SUB_WEIGHT) = 1.25 are top-level consts in the same file.
    - Resulting TASTE_SUB_WEIGHTS values: numericTrioCosine = 0.50, eraMatch = 0.25, motifsJaccard = 0.25 (computed, not literal).
    - The if-block at the old similarity.ts:124-127 reading t1.primaryArchetype + t2.primaryArchetype is GONE.
    - tasteSimilarityRaw01 still returns a value in [0, 1]; the outer caller multiplies by WEIGHTS.taste (0.20) so effective taste contribution is in [0, 0.20].
    - tests/similarity.test.ts contains an assertion that TASTE_SUB_WEIGHTS keys and values match the expected 3-key shape with values 0.50/0.25/0.25 AND an assertion that the sum is 1.0 (use Number.EPSILON or a tolerance of 1e-9 to handle floating-point rounding).
    - tests/similarity.test.ts contains an assertion that THRESHOLDS and GOAL_THRESHOLDS objects have UNCHANGED values (D-SIM-04 invariant) — load THRESHOLDS via the existing test import and assert each numeric value.
  </behavior>
  <action>
    In src/lib/similarity.ts:

    1. Replace the current TASTE_SUB_WEIGHTS block at lines 34-45 with the algorithmic rebalance. The new block introduces:
       - A const named TASTE_SUB_WEIGHTS_BASE with three keys: numericTrioCosine value 0.40, eraMatch value 0.20, motifsJaccard value 0.20, marked `as const`. Add a comment that this sums to 0.80 (the surviving budget after removing the 0.20 archetypeMatch).
       - A const named REMOVED_SUB_WEIGHT with value 0.20 (with a comment noting it was the pre-49.1 archetypeMatch share).
       - A const named RESCALE with value `1 / (1 - REMOVED_SUB_WEIGHT)` (the computed 1.25).
       - A const named TASTE_SUB_WEIGHTS computed via Object.fromEntries(Object.entries(TASTE_SUB_WEIGHTS_BASE).map(([k, v]) =&gt; [k, v * RESCALE])) with the type cast `{ [K in keyof typeof TASTE_SUB_WEIGHTS_BASE]: number }`.
       - Add a comment line referencing Phase 38 D-03 modified by Phase 49.1 D-SIM-01/D-SIM-02 explaining the internal split.

       Do NOT hardcode the rescaled values 0.50/0.25/0.25 anywhere in the file (this would violate the anti-pattern guard at lines 10-12).

    2. In tasteSimilarityRaw01 (around lines 100-138), DELETE the archetype categorical-match if-block at lines 124-127. The surrounding code (numericTrioCosine block at :114-122, eraMatch block at :130-132, motifsJaccard line at :135) stays UNCHANGED. After deletion the function reads only three CatalogTasteAttributes fields plus the trio: formality/sportiness/heritageScore (trio) + eraSignal + designMotifs. It no longer reads primaryArchetype.

    3. TASTE_WEIGHT at line 24 stays 0.20. EXISTING_WEIGHTS_BASE and WEIGHTS at lines 13-32 stay UNCHANGED. THRESHOLDS at lines 48-52 stay UNCHANGED. GOAL_THRESHOLDS at lines 57-62 stay UNCHANGED.

    In tests/similarity.test.ts:

    1. Add (or update) a describe block named "Phase 49.1 — TASTE_SUB_WEIGHTS rebalance (D-SIM-01..04)" with these it blocks:

       - "TASTE_SUB_WEIGHTS has exactly 3 keys: numericTrioCosine, eraMatch, motifsJaccard" — use Object.keys(TASTE_SUB_WEIGHTS).sort() and assert equality with ['eraMatch', 'motifsJaccard', 'numericTrioCosine'].
       - "TASTE_SUB_WEIGHTS values are 0.50 / 0.25 / 0.25 (within 1e-9 tolerance)" — assert each value with toBeCloseTo or expect(value).toBeGreaterThan(...) and toBeLessThan(...) with tight bounds. The values come from the algorithmic rescale, so floating-point drift is possible.
       - "TASTE_SUB_WEIGHTS values sum to 1.0" — `expect(Object.values(TASTE_SUB_WEIGHTS).reduce((a,b) =&gt; a+b, 0)).toBeCloseTo(1.0, 9)`.
       - "TASTE_SUB_WEIGHTS no longer has archetypeMatch key" — `expect('archetypeMatch' in TASTE_SUB_WEIGHTS).toBe(false)`.

    2. If THRESHOLDS or GOAL_THRESHOLDS are exported, add (or confirm an existing) it block asserting their values are byte-identical to pre-49.1: THRESHOLDS.low.coreFit = 0.7, THRESHOLDS.medium.coreFit = 0.75, THRESHOLDS.high.coreFit = 0.8 (and the other 6 numbers per similarity.ts:48-52). For GOAL_THRESHOLDS: the 4 collection-goal entries with their 12 numbers per similarity.ts:57-62. If THRESHOLDS / GOAL_THRESHOLDS are NOT currently exported, do NOT add an export in this task — instead add a comment-only TODO noting they should be export-asserted in a follow-up.

    3. Ensure these assertions do not require importing types that the type system has not yet updated (TASTE_SUB_WEIGHTS is a runtime value; safe to import as named export).

    4. TASTE_SUB_WEIGHTS is currently NOT exported. Expose it via `export { TASTE_SUB_WEIGHTS }` at the bottom of similarity.ts, alongside the existing `export { GOAL_THRESHOLDS, detectLoyalBrands }` line. This is a minor surface change but is required to assert the invariant. After this task, `TASTE_SUB_WEIGHTS` is a public export (documented in this plan's `must_haves.artifacts.contains` / `exports` block).
  </action>
  <verify>
    <automated>npm test -- tests/similarity.test.ts 2>&amp;1 | tail -15 ; grep -nE "primaryArchetype|archetypeMatch" src/lib/similarity.ts || echo OK ; grep -n "TASTE_SUB_WEIGHTS_BASE" src/lib/similarity.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TASTE_SUB_WEIGHTS_BASE" src/lib/similarity.ts` returns at least 2 matches (the const definition and at least one downstream Object.entries reference)
    - `grep -nE "primaryArchetype|archetypeMatch" src/lib/similarity.ts` returns no matches
    - `grep -nE "0\.50.*numericTrioCosine|numericTrioCosine.*0\.50" src/lib/similarity.ts` returns no matches (no magic-number hardcoding)
    - `grep -n "RESCALE = 1 / (1 - REMOVED_SUB_WEIGHT)" src/lib/similarity.ts` returns 1 match
    - `grep -nE "export \{[^}]*TASTE_SUB_WEIGHTS" src/lib/similarity.ts` returns 1 match (the new public export)
    - `npm test -- tests/similarity.test.ts` exits 0
    - All four new it blocks pass: 3-key check, values check, sum-to-1.0 check, no-archetypeMatch check
    - THRESHOLDS / GOAL_THRESHOLDS invariant assertion passes (or the TODO note is present if not export-asserted)
  </acceptance_criteria>
  <done>Algorithmic rescale lands; archetype categorical block deleted; sum-to-1.0 invariant asserted; outer TASTE_WEIGHT and thresholds unchanged; TASTE_SUB_WEIGHTS is a public export.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. The similarity engine is pure computation over already-validated CatalogTasteAttributes data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49.1-10 | T (Tampering) | src/lib/similarity.ts TASTE_SUB_WEIGHTS | mitigate | Algorithmic rescale via Object.fromEntries; no user-controlled inputs; the BASE values and REMOVED_SUB_WEIGHT are build-time consts. Tests assert sum-to-1.0 to catch any future drift. |
| T-49.1-11 | T (Tampering) | THRESHOLDS / GOAL_THRESHOLDS | mitigate | Invariant assertion ensures verdict-label thresholds remain byte-identical to pre-49.1 (D-SIM-04). Any future PR drifting these values fails the test. |
</threat_model>

<verification>
- `npm test -- tests/similarity.test.ts` exits 0
- The sum-to-1.0 invariant assertion is the new tripwire for any future weight-rebalance regression
- No archetype-related code path remains in similarity.ts (grep gates)
- THRESHOLDS and GOAL_THRESHOLDS unchanged
</verification>

<success_criteria>
- D-SIM-01 applied: numericTrioCosine 0.50, eraMatch 0.25, motifsJaccard 0.25 (achieved algorithmically)
- D-SIM-02 applied: no hardcoded rescaled values; TASTE_SUB_WEIGHTS_BASE + RESCALE pattern present
- D-SIM-03 applied: archetype categorical match block at :124-127 deleted
- D-SIM-04 applied: TASTE_WEIGHT 0.20 unchanged; THRESHOLDS and GOAL_THRESHOLDS unchanged
- tests/similarity.test.ts asserts all four invariants
- TASTE_SUB_WEIGHTS is added to the public export surface of similarity.ts
</success_criteria>

<output>
After completion, create `.planning/phases/49.1-remove-genre-surface/49.1-05-SUMMARY.md`
</output>
