---
phase: 49.1
plan: 06
type: execute
wave: 2
depends_on: [49.1-01, 49.1-02, 49.1-03, 49.1-04, 49.1-05]
files_modified:
  - src/lib/types.ts
  - src/app/catalog/[catalogId]/page.tsx
  - src/data/watches.ts
  - src/data/catalog.ts
  - src/lib/taste/enricher.ts
  - src/lib/taste/prompt.ts
  - src/lib/taste/vocab.ts
  - src/data/__tests__/watches-leftjoin.test.ts
  - src/lib/taste/enricher.test.ts
  - src/lib/taste/vocab.test.ts
  - tests/integration/catalog-taste.test.ts
  - tests/integration/catalog-taste-schema.test.ts
autonomous: true
requirements: [TAX-02]
must_haves:
  truths:
    - "CatalogTasteAttributes interface in src/lib/types.ts has no primaryArchetype field; PrimaryArchetype type union is retained for ARCHETYPE_CONFIG"
    - "src/app/catalog/[catalogId]/page.tsx catalogTaste literal does not project primaryArchetype"
    - "src/data/watches.ts getWatchesByUser LEFT JOIN SELECT does not project primaryArchetype; the row mapper does not write it"
    - "src/data/catalog.ts CatalogSearchFilters has no genre or archetype field; mapRowToCatalogEntry does not read primaryArchetype; updateCatalogTaste UPSERT SQL does not write primary_archetype; the archetype-wins tiebreaker block is gone"
    - "src/lib/taste/enricher.ts TASTE_TOOL schema does not contain primary_archetype; the required array does not include 'primary_archetype'"
    - "src/lib/taste/prompt.ts SYSTEM_FRAMING does not contain the primary_archetype line"
    - "src/lib/taste/vocab.ts TasteSchema does not contain primary_archetype; validateAndCleanTaste does not have the archetype block; PRIMARY_ARCHETYPES const is retained"
  artifacts:
    - path: src/lib/types.ts
      provides: "CatalogTasteAttributes shape without primaryArchetype; PrimaryArchetype union retained"
    - path: src/data/catalog.ts
      provides: "Filter interface, row mapper, UPSERT all stripped of primary_archetype"
    - path: src/lib/taste/enricher.ts
      provides: "LLM tool schema without primary_archetype"
    - path: src/lib/taste/vocab.ts
      provides: "TasteSchema + validateAndCleanTaste with archetype path removed; PRIMARY_ARCHETYPES retained for /explore"
  key_links:
    - from: src/app/catalog/[catalogId]/page.tsx
      to: src/lib/types.ts
      via: "catalogTaste literal matches CatalogTasteAttributes shape (D-39b-04 lock)"
      pattern: "CatalogTasteAttributes"
    - from: src/data/watches.ts
      to: src/lib/types.ts
      via: "getWatchesByUser LEFT JOIN projects into CatalogTasteAttributes shape"
      pattern: "catalogTaste"
    - from: src/lib/taste/enricher.ts
      to: src/lib/taste/vocab.ts
      via: "validateAndCleanTaste consumes the LLM tool output; both must drop the archetype path together"
      pattern: "validateAndCleanTaste"
---

<objective>
Execute the type-system unification (D-SCOPE-01e + Pitfall 3) and the enricher chain cleanup (Pitfall 4) as one atomic wave. This plan removes `primaryArchetype` from the `CatalogTasteAttributes` interface and from BOTH projection sites that build that shape (`/catalog/[catalogId]/page.tsx` manual projection AND `getWatchesByUser` LEFT JOIN in `src/data/watches.ts`) in lockstep — Phase 39b D-39b-04 identical-rendering lock demands these change atomically. It also removes the four LLM-enricher touch sites (`enricher.ts` tool schema + required, `prompt.ts` SYSTEM_FRAMING, `vocab.ts` TasteSchema + validateAndCleanTaste). Finally, it cleans the DAL: `CatalogSearchFilters.genre/archetype`, the archetype-wins tiebreaker at `src/data/catalog.ts:444-450`, `mapRowToCatalogEntry`'s primaryArchetype mapping at :82, and the `updateCatalogTaste` UPSERT column at :618.

Purpose: D-SCOPE-01e + Pitfall 3 (D-39b-04 lockstep) + Pitfall 4 (enricher 5-site atomicity). After this plan lands, the `primary_archetype` column has zero reader/writer code references — making it safe for Plan 07 to update `src/db/schema.ts` and apply the local drizzle migration.

Output: 7 modified source files + 5 modified test files. After this plan, the entire codebase apart from the live DB column (and `src/db/schema.ts`, which Plan 07 owns) is archetype-free at the type-system layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49.1-remove-genre-surface/49.1-CONTEXT.md
@.planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md
@.planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md

# Wave 1 SUMMARY files (read whichever are present — they document the contracts this plan must consume)
@.planning/phases/49.1-remove-genre-surface/49.1-02-SUMMARY.md
@.planning/phases/49.1-remove-genre-surface/49.1-04-SUMMARY.md

# Files being modified
@src/lib/types.ts
@src/app/catalog/[catalogId]/page.tsx
@src/data/watches.ts
@src/data/catalog.ts
@src/lib/taste/enricher.ts
@src/lib/taste/prompt.ts
@src/lib/taste/vocab.ts

# Existing tests being updated
@src/data/__tests__/watches-leftjoin.test.ts
@src/lib/taste/enricher.test.ts
@src/lib/taste/vocab.test.ts
@tests/integration/catalog-taste.test.ts
@tests/integration/catalog-taste-schema.test.ts

# Wave 0 integration test - should transition from skipping/failing to PASSING after this plan if DATABASE_URL is set AND Plan 07 has applied the local drizzle migration. Until then it remains red on the column-absent assertion.
@tests/integration/migration-drop-archetype.test.ts

<interfaces>
<!-- From src/lib/types.ts (current shape) -->

Lines 208 and 224 both contain `primaryArchetype: PrimaryArchetype | null`. The line at 208 is inside a CatalogEntry-shaped interface; the line at 224 is inside CatalogTasteAttributes. BOTH are deleted.

Lines 234-236 declare `PrimaryArchetype` as a union of the 10 archetype string literals. This union is RETAINED per D-EXPLORE-02 — it is the keytype of ARCHETYPE_CONFIG.

<!-- From src/data/catalog.ts (current shape) -->

Lines 82: `primaryArchetype: (row.primaryArchetype as PrimaryArchetype | null) ?? null` in mapRowToCatalogEntry. DELETE.

Lines 283-286: CatalogSearchFilters with `genre?: PrimaryArchetype` and `archetype?: PrimaryArchetype` fields. DELETE both.

Lines 444-450: the archetype-wins tiebreaker block (predicate that prefers archetype over genre or vice versa). DELETE — see CONTEXT D-SCOPE-01f and PATTERNS.

Line 618: the `primary_archetype = ${taste.primaryArchetype}` interpolation in the updateCatalogTaste UPSERT SQL. DELETE the column and its assignment from the SET clause.

<!-- From src/data/watches.ts (current shape) -->

Lines 130-162: getWatchesByUser LEFT JOIN against watchesCatalog. The SELECT clause projects `primaryArchetype` at approximately :134, and the row mapper builds a catalogTaste literal at approximately :157 with `primaryArchetype:` line. DELETE both.

<!-- From src/lib/taste/enricher.ts (current shape) -->

Line 38: `primary_archetype: { type: 'string', enum: [...PRIMARY_ARCHETYPES] }` inside TASTE_TOOL.input_schema.properties. DELETE.

Line 51: `'primary_archetype'` inside the `required` array. DELETE.

<!-- From src/lib/taste/prompt.ts (current shape) -->

Line 26: `primary_archetype (pick exactly one): ${PRIMARY_ARCHETYPES.join(', ')}` inside SYSTEM_FRAMING. DELETE this line.

<!-- From src/lib/taste/vocab.ts (current shape) -->

Lines 14-22: PRIMARY_ARCHETYPES const and PRIMARY_ARCHETYPE_SET. RETAIN PRIMARY_ARCHETYPES (consumed by the rewired getBrowseArchetypeCounts in Plan 03). PRIMARY_ARCHETYPE_SET may become unused — check and delete only if no consumer remains.

Line 60: `primary_archetype: z.string()` in TasteSchema. DELETE.

Lines 98-128: validateAndCleanTaste. The archetype path (lines 98-109 approximately, plus the `primaryArchetype: archetype,` line in the return literal) is DELETED. The era path (lines 111-122) is the surviving sibling pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop primaryArchetype from CatalogTasteAttributes; sync the two projection sites (catalog page + watches LEFT JOIN); update watches-leftjoin test</name>
  <files>src/lib/types.ts, src/app/catalog/[catalogId]/page.tsx, src/data/watches.ts, src/data/__tests__/watches-leftjoin.test.ts</files>
  <read_first>
    - src/lib/types.ts (full file — locate CatalogTasteAttributes at :222-230, CatalogEntry / related interface at :208, PrimaryArchetype union at :234-236)
    - src/app/catalog/[catalogId]/page.tsx (full file — locate the catalogTaste literal at :90-99 plus the surrounding RSC structure)
    - src/data/watches.ts (full file — locate getWatchesByUser at lines 100-180, especially the LEFT JOIN SELECT projection around :130-140 and the row mapper around :150-165)
    - src/data/__tests__/watches-leftjoin.test.ts (full file — locate archetype assertions)
    - .planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md §Pitfall 3 (the D-39b-04 lockstep risk)
    - .planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md §"src/lib/types.ts" and §"Catalog page projection update (D-SCOPE-01e)"
  </read_first>
  <behavior>
    - CatalogTasteAttributes has 7 fields (formality, sportiness, heritageScore, eraSignal, designMotifs, confidence, extractedFromPhoto) — no primaryArchetype.
    - PrimaryArchetype type union at :234-236 is RETAINED (consumed by ARCHETYPE_CONFIG per D-EXPLORE-02).
    - src/app/catalog/[catalogId]/page.tsx catalogTaste literal has 7 fields, exactly matching CatalogTasteAttributes — no primaryArchetype line.
    - src/data/watches.ts getWatchesByUser SELECT clause does not include primaryArchetype; the row mapper builds a catalogTaste literal with the same 7 fields — no primaryArchetype.
    - src/data/__tests__/watches-leftjoin.test.ts has no archetype-related fixtures or assertions.
    - tsc --noEmit reports zero errors in src/lib/types.ts, src/app/catalog/[catalogId]/page.tsx, src/data/watches.ts (any errors in src/data/catalog.ts or src/lib/taste/** are expected at this point and will resolve in Task 2).
  </behavior>
  <action>
    In src/lib/types.ts:

    1. Inside CatalogTasteAttributes at :222-230, DELETE the line `primaryArchetype: PrimaryArchetype | null`.
    2. If line 208 is a separate interface (e.g. CatalogEntry-like) carrying the same field, DELETE that line too. Read the file to confirm whether 208 is in CatalogEntry or a duplicate of CatalogTasteAttributes.
    3. KEEP the PrimaryArchetype union at :234-236 EXACTLY as-is (with the 10 string literal members). This type is consumed by ARCHETYPE_CONFIG in src/lib/archetype-config.ts per D-EXPLORE-02.

    In src/app/catalog/[catalogId]/page.tsx:

    1. In the catalogTaste literal at approximately :90-99, DELETE the line `primaryArchetype: catalogEntry.primaryArchetype` (or its equivalent shape).
    2. Surrounding 7 fields (formality, sportiness, heritageScore, eraSignal, designMotifs, confidence, extractedFromPhoto) stay UNCHANGED.

    In src/data/watches.ts:

    1. In the getWatchesByUser LEFT JOIN SELECT (around :130-140), DELETE the projection field `primaryArchetype: watchesCatalog.primaryArchetype` (or its equivalent — the exact identifier depends on the drizzle column alias).
    2. In the row mapper (around :150-165) that builds the catalogTaste literal, DELETE the `primaryArchetype:` line. The literal now has 7 fields matching CatalogTasteAttributes.

    In src/data/__tests__/watches-leftjoin.test.ts:

    1. Read the file to identify archetype-related fixtures/assertions. DELETE any `primaryArchetype: 'X'` from fixture literals. DELETE any assertion `expect(row.catalogTaste.primaryArchetype).toBe(...)`. If a whole it block was solely about archetype, delete that it block.

    Pitfall 3 atomicity: This task touches all three sites that share the CatalogTasteAttributes shape, so the TS compiler stays satisfied within this task's scope. Do not split this task across waves.
  </action>
  <verify>
    <automated>grep -nE "primaryArchetype" src/lib/types.ts src/app/catalog/\[catalogId\]/page.tsx src/data/watches.ts || echo OK ; npm test -- src/data/__tests__/watches-leftjoin.test.ts 2>&amp;1 | tail -10 ; npx tsc --noEmit 2>&amp;1 | grep -E "(types\.ts|catalog/\[catalogId\]/page\.tsx|data/watches\.ts)" | head -10 || echo "OK no tsc errors localized to these files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "primaryArchetype" src/lib/types.ts` returns no matches in CatalogTasteAttributes (lines around 222-230). The PrimaryArchetype union at :234-236 is preserved (so `grep -n "PrimaryArchetype" src/lib/types.ts` still returns matches).
    - `grep -n "primaryArchetype" src/app/catalog/[catalogId]/page.tsx` returns no matches
    - `grep -n "primaryArchetype" src/data/watches.ts` returns no matches
    - `grep -n "primaryArchetype" src/data/__tests__/watches-leftjoin.test.ts` returns no matches
    - `npm test -- src/data/__tests__/watches-leftjoin.test.ts` exits 0
    - `npx tsc --noEmit` reports no errors in these four files (errors in src/data/catalog.ts and src/lib/taste/** are expected until Task 2 completes)
  </acceptance_criteria>
  <done>Type-system unified; D-39b-04 lockstep preserved; watches-leftjoin test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip primary_archetype from src/data/catalog.ts (filter interface + tiebreaker + mapper + UPSERT) and from the 5 enricher-chain sites (enricher.ts + prompt.ts + vocab.ts); update enricher + vocab + catalog-taste integration tests</name>
  <files>src/data/catalog.ts, src/lib/taste/enricher.ts, src/lib/taste/prompt.ts, src/lib/taste/vocab.ts, src/lib/taste/enricher.test.ts, src/lib/taste/vocab.test.ts, tests/integration/catalog-taste.test.ts, tests/integration/catalog-taste-schema.test.ts</files>
  <read_first>
    - src/data/catalog.ts (full file — esp. mapRowToCatalogEntry around :75-110, CatalogSearchFilters interface around :270-300, the WHERE-builder with archetype-wins tiebreaker around :430-460, updateCatalogTaste UPSERT around :590-630)
    - src/lib/taste/enricher.ts (full file — locate TASTE_TOOL at lines 25-60 and the required array)
    - src/lib/taste/prompt.ts (full file — locate SYSTEM_FRAMING and the primary_archetype prompt line at :26)
    - src/lib/taste/vocab.ts (full file — locate PRIMARY_ARCHETYPES at :14-22, PRIMARY_ARCHETYPE_SET, TasteSchema at :55-70, validateAndCleanTaste at :85-135)
    - src/lib/taste/enricher.test.ts (full file — locate archetype assertions; identify whether they survive in modified form or are deleted)
    - src/lib/taste/vocab.test.ts (full file — locate validateAndCleanTaste archetype-path tests)
    - tests/integration/catalog-taste.test.ts (full file — locate VALID_TASTE fixture with primaryArchetype and row.primaryArchetype assertions)
    - tests/integration/catalog-taste-schema.test.ts (full file — locate schema-shape assertion that may reference primary_archetype)
    - .planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md §Pitfall 4 (5 enricher sites must change atomically)
    - .planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md §"src/lib/taste/vocab.ts + enricher.ts + prompt.ts" and §"src/data/catalog.ts"
  </read_first>
  <behavior>
    - src/data/catalog.ts: CatalogSearchFilters has no `genre` or `archetype` field. mapRowToCatalogEntry has no `primaryArchetype:` line. The archetype-wins tiebreaker block (the predicate that preferred archetype when both genre+archetype were set, or the archetype-precedence WHERE branch) is gone. updateCatalogTaste UPSERT SQL no longer references `primary_archetype` in the column list, the values list, or the ON CONFLICT SET clause.
    - src/lib/taste/enricher.ts: TASTE_TOOL.input_schema.properties has no `primary_archetype` key. The `required` array has no `'primary_archetype'` string. PRIMARY_ARCHETYPES may no longer be imported here (verify).
    - src/lib/taste/prompt.ts: SYSTEM_FRAMING has no line containing `primary_archetype`. PRIMARY_ARCHETYPES may no longer be imported here (verify).
    - src/lib/taste/vocab.ts: TasteSchema has no `primary_archetype` field. validateAndCleanTaste no longer has the archetype block (the `const archetype = PRIMARY_ARCHETYPE_SET.has(...)` block and the `primaryArchetype: archetype,` return literal line). The era block remains as the surviving sibling pattern.
    - PRIMARY_ARCHETYPES const at vocab.ts:14-22 is RETAINED — it is consumed by src/data/browse.ts (Plan 03) and is the source of the PrimaryArchetype union at types.ts:234-236.
    - PRIMARY_ARCHETYPE_SET may have no remaining consumers after validateAndCleanTaste is updated. If grep confirms zero consumers, delete it; otherwise leave it.
    - enricher.test.ts has no archetype-field assertion. vocab.test.ts has no validateAndCleanTaste archetype-path test. catalog-taste.test.ts VALID_TASTE fixture has no primaryArchetype key; row.primaryArchetype assertions are deleted. catalog-taste-schema.test.ts schema-shape assertion no longer expects primary_archetype.
    - After this task: npx tsc --noEmit reports ZERO errors in the entire repo (the type-system unification is complete; only the DB column drop remains, which Plan 07 handles).
  </behavior>
  <action>
    In src/data/catalog.ts:

    1. In CatalogSearchFilters (lines 283-286 region), DELETE the `genre?: PrimaryArchetype` field and the `archetype?: PrimaryArchetype` field. The other filter fields (era, brand, style, movement, size) stay UNCHANGED.
    2. In the WHERE-builder predicate block (around :430-460), DELETE the archetype-wins tiebreaker. Read the source to identify the exact lines: there is a block that adds `eq(watchesCatalog.primaryArchetype, filters.archetype)` or analogous, and a separate block for genre, plus a tiebreaker preferring one when both are set. ALL of these go away (the predicate field is gone from the filter interface; the WHERE clauses become unreachable).
    3. In mapRowToCatalogEntry around :82, DELETE the line `primaryArchetype: (row.primaryArchetype as PrimaryArchetype | null) ?? null` (or its equivalent shape). The other mapper fields stay UNCHANGED.
    4. In updateCatalogTaste UPSERT at around :618, the SQL INSERT/UPSERT clause currently writes `primary_archetype = ${taste.primaryArchetype}` (or the equivalent in drizzle's sql template). DELETE the column from the column list, DELETE the value from the values list, and DELETE the corresponding entry from the ON CONFLICT SET clause. Read the function carefully — the column appears in 3 places within the same SQL statement. Use drizzle's sql template tag conventions.
    5. After deletions, verify the `PrimaryArchetype` import at the top of the file: if no other reference remains in src/data/catalog.ts, remove the import.

    In src/lib/taste/enricher.ts:

    1. At line 38 (inside TASTE_TOOL.input_schema.properties), DELETE the `primary_archetype: { type: 'string', enum: [...PRIMARY_ARCHETYPES] }` line.
    2. At line 51 (inside the `required` array), DELETE the `'primary_archetype'` string entry.
    3. Verify the `PRIMARY_ARCHETYPES` import at the top: if no consumer remains in enricher.ts, remove the import.

    In src/lib/taste/prompt.ts:

    1. At line 26 inside SYSTEM_FRAMING, DELETE the `primary_archetype (pick exactly one): ${PRIMARY_ARCHETYPES.join(', ')}` line.
    2. Verify the `PRIMARY_ARCHETYPES` import: if no consumer remains, remove the import.

    In src/lib/taste/vocab.ts:

    1. At line 60 inside TasteSchema (the z.object literal), DELETE the `primary_archetype: z.string()` line.
    2. Inside validateAndCleanTaste (around lines 85-135), DELETE the archetype block. The block consists of:
       - the `const archetype = PRIMARY_ARCHETYPE_SET.has(wire.primary_archetype) ? ... : null` line(s)
       - the `if (!archetype) { console.warn(...) }` drift-log block (analogous to the era drift log)
       - the `primaryArchetype: archetype,` line in the return literal
    3. KEEP `PRIMARY_ARCHETYPES` at lines 14-22 EXACTLY (consumed by browse.ts post-Plan-03).
    4. If `PRIMARY_ARCHETYPE_SET` (a Set built from PRIMARY_ARCHETYPES) has no remaining consumer after the archetype block deletion, DELETE it. Otherwise keep it.
    5. KEEP the era path (around :111-122) UNCHANGED — it is the surviving sibling pattern.

    In src/lib/taste/enricher.test.ts:

    1. DELETE every assertion that the LLM tool output contains `primary_archetype` or that the persisted row has `primaryArchetype`. Fixtures with `primary_archetype: 'dive'` lose that key.
    2. Surviving tests on the other 6 taste fields stay UNCHANGED.

    In src/lib/taste/vocab.test.ts:

    1. DELETE the `validateAndCleanTaste` archetype-path tests (the describes/its asserting drift-log behavior on bad archetype values, and the happy-path returning `primaryArchetype: ...`).
    2. The era-path tests are the surviving sibling pattern — UNCHANGED.

    In tests/integration/catalog-taste.test.ts:

    1. DROP the `primaryArchetype: 'dress'` (or whatever value is set) from the VALID_TASTE fixture.
    2. DELETE any assertion `expect(row.primaryArchetype).toBe(...)` or `expect(persisted.primaryArchetype).toBe(...)`.
    3. Other assertions on formality/sportiness/heritageScore/eraSignal/designMotifs stay UNCHANGED.

    In tests/integration/catalog-taste-schema.test.ts:

    1. If the test asserts an exact column list (e.g. "watches_catalog has columns formality, sportiness, heritageScore, primary_archetype, era_signal, design_motifs, confidence"), DROP `primary_archetype` from the expected list.
    2. If the test queries `information_schema.columns` and expects specific count or names, update both.

    Atomicity (Pitfall 4): all 5 enricher touch sites change in this task. If any one is left in place, downstream tests will flag drift-log warnings or zod parse failures. Verify with the grep gate below before completing.
  </action>
  <verify>
    <automated>grep -nE "primary_archetype|primaryArchetype" src/data/catalog.ts src/lib/taste/enricher.ts src/lib/taste/prompt.ts src/lib/taste/vocab.ts || echo OK ; npm test -- src/data/__tests__/ src/lib/taste/ tests/integration/catalog-taste.test.ts tests/integration/catalog-taste-schema.test.ts 2>&amp;1 | tail -20 ; npx tsc --noEmit 2>&amp;1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "primary_archetype|primaryArchetype" src/data/catalog.ts src/lib/taste/enricher.ts src/lib/taste/prompt.ts src/lib/taste/vocab.ts` returns no matches (PRIMARY_ARCHETYPES — uppercase — is retained in vocab.ts and is a different identifier; the grep above does not match it because the regex is case-sensitive)
    - `grep -n "PRIMARY_ARCHETYPES" src/lib/taste/vocab.ts` still returns at least 1 match (the const is retained)
    - `grep -n "genre" src/data/catalog.ts` returns no matches inside CatalogSearchFilters or the WHERE-builder (it may appear in unrelated comments — inspect manually if matches occur)
    - `npm test -- src/data/__tests__/ src/lib/taste/ tests/integration/catalog-taste.test.ts tests/integration/catalog-taste-schema.test.ts` exits 0
    - `npx tsc --noEmit` reports ZERO errors across the whole repo (the type system is now fully unified; only the DB column remains, which is fine because src/db/schema.ts still declares the column — Plan 07 will remove it)
    - The Wave 0 integration test tests/integration/migration-drop-archetype.test.ts is still red on the "column absent" assertion (column still exists in DB) but is no longer red on the "row.catalogTaste has no primaryArchetype" assertion — this is the expected transitional state until Plan 07 drops the column.
  </acceptance_criteria>
  <done>All reader/writer code references to primary_archetype removed; type system fully unified; enricher chain consistently lacks the field; tests green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool output → enricher | Anthropic SDK returns tool-call JSON; validated by Zod TasteSchema then by validateAndCleanTaste. Removing the archetype field tightens the surface. |
| catalog page projection → ReferenceIdentityCard render | D-39b-04 identical-rendering lock — must change atomically with the type definition. This plan satisfies that constraint by editing types.ts + catalog page + watches LEFT JOIN in Task 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49.1-12 | T (Tampering) | src/lib/taste/enricher.ts TASTE_TOOL | mitigate | Removing primary_archetype from both `properties` and `required` reduces the LLM's attack surface; Zod parse still runs on every response so any drift produces a clean error. |
| T-49.1-13 | I (Information disclosure) | src/data/catalog.ts updateCatalogTaste UPSERT | accept | The UPSERT runs against the user's own catalog row; no cross-user data flow. Removing the column write narrows the write surface. |
| T-49.1-14 | T (Tampering) | src/lib/types.ts CatalogTasteAttributes | mitigate | Type-system unification means the TS compiler now catches any future code that tries to read/write the removed field. The check is build-time, no runtime cost. |
</threat_model>

<verification>
- `npm test` (full suite) exits 0
- `npx tsc --noEmit` exits 0
- `grep -rnE "primary_archetype|primaryArchetype" src/` returns matches ONLY in src/db/schema.ts (the column declaration — Plan 07 owns this) AND in src/lib/archetype-config.ts (the retained ARCHETYPE_CONFIG const which uses PrimaryArchetype keytype — D-EXPLORE-02 carve-out)
- `grep -rnE "primary_archetype|primaryArchetype" tests/` returns no matches
- The Wave 0 migration-drop-archetype.test.ts integration test is still red ONLY on the "column absent" assertion (column-in-DB still exists; Plan 07 will fix)
</verification>

<success_criteria>
- D-SCOPE-01e applied: catalog page projection drops primaryArchetype
- D-SCOPE-01f applied: src/app/actions/search.ts already cleaned in Plan 04; this plan cleans the DAL-side CatalogSearchFilters + tiebreaker
- D-39b-04 lockstep preserved: types.ts + catalog page + watches LEFT JOIN all change in Task 1 of this plan
- Pitfall 4 satisfied: all 5 enricher chain sites (enricher.ts schema, enricher.ts required, prompt.ts, vocab.ts TasteSchema, vocab.ts validateAndCleanTaste) cleaned in Task 2
- D-EXPLORE-02 carve-out preserved: PrimaryArchetype union + ARCHETYPE_CONFIG + PRIMARY_ARCHETYPES const all retained
- After this plan, only src/db/schema.ts (Plan 07) still references the column
</success_criteria>

<output>
After completion, create `.planning/phases/49.1-remove-genre-surface/49.1-06-SUMMARY.md`
</output>
