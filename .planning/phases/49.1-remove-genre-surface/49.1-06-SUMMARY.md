---
phase: 49.1-remove-genre-surface
plan: 06
subsystem: types
tags: [typescript, drizzle, zod, anthropic, taste-enricher, dal]

# Dependency graph
requires:
  - phase: 49.1
    provides: "Wave 1 plans 02-05 (verdict pivot to era, /explore + chip + route deletions, direct-UI archetype drops + searchSchema Zod, similarity TASTE_SUB_WEIGHTS rebalance)"
provides:
  - "CatalogTasteAttributes shape without primaryArchetype (D-SCOPE-01e applied)"
  - "CatalogEntry shape without primaryArchetype"
  - "Catalog DAL (CatalogSearchFilters + WHERE-builder + mapper + UPSERT) stripped of primary_archetype"
  - "LLM enricher chain (TASTE_TOOL schema + required + prompt + Zod TasteSchema + validateAndCleanTaste) stripped of primary_archetype"
  - "PrimaryArchetype union + ARCHETYPE_CONFIG + PRIMARY_ARCHETYPES const retained per D-EXPLORE-02"
  - "Zero reader/writer code references to primary_archetype outside src/db/schema.ts"
affects: [49.1-07, 49.1-08, 49-genre-spike-followups]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "D-39b-04 identical-rendering lock — all CatalogTasteAttributes projection sites change atomically"
    - "Pitfall 4 enricher 5-site atomicity — TASTE_TOOL.properties + TASTE_TOOL.required + SYSTEM_FRAMING + Zod TasteSchema + validateAndCleanTaste all change in one commit"
    - "Closed-vocab carve-out — drop the schema column but retain the vocab const for downstream filter callers (D-EXPLORE-02 / D-EXPLORE-01)"

key-files:
  created:
    - .planning/phases/49.1-remove-genre-surface/deferred-items.md
    - .planning/phases/49.1-remove-genre-surface/49.1-06-SUMMARY.md
  modified:
    - src/lib/types.ts
    - src/app/catalog/[catalogId]/page.tsx
    - src/data/watches.ts
    - src/data/catalog.ts
    - src/lib/taste/enricher.ts
    - src/lib/taste/prompt.ts
    - src/lib/taste/vocab.ts
    - src/lib/verdict/composer.ts
    - src/data/__tests__/watches-leftjoin.test.ts
    - src/lib/taste/enricher.test.ts
    - src/lib/taste/vocab.test.ts
    - src/lib/verdict/composer.test.ts
    - src/lib/verdict/confidence.test.ts
    - src/lib/verdict/shims.test.ts
    - tests/integration/catalog-taste.test.ts
    - tests/integration/catalog-taste-schema.test.ts
    - tests/integration/migration-drop-archetype.test.ts
    - tests/integration/add-watch-photo.test.ts
    - tests/app/catalog-page.test.ts
    - tests/app/watch-new-page.test.ts
    - tests/static/composer-engine-alignment.test.ts
    - tests/unit/lib/verdict/fit-delta.test.ts
    - tests/components/insights/CollectionFitCompareTable.test.tsx
    - tests/components/insights/ReferenceIdentityCard.test.tsx
    - tests/fixtures/catalogTaste.ts
    - tests/setup/mockAnthropic.ts
    - tests/actions/verdict.test.ts

key-decisions:
  - "D-39b-04 atomicity preserved across 4 projection sites: types.ts + catalog/[catalogId]/page.tsx + watches.ts LEFT JOIN + composer.ts candidateCatalogTaste"
  - "PRIMARY_ARCHETYPES const + PRIMARY_ARCHETYPE_SET handling: const kept (consumed by /explore CollectorArchetypes), SET removed (no remaining consumer after archetype validate block deletion)"
  - "Composer.ts:107 added to Task 1 scope as the 3rd CatalogTasteAttributes projection (not enumerated in plan's interface list but required by Pitfall 3 type-system lockstep)"
  - "Trailing test fixture cleanups (verdict.test.ts, ReferenceIdentityCard.test.tsx) committed as separate Rule 3 fix commit"

patterns-established:
  - "Sibling-survivor pattern (era_signal blocks): each removed primary_archetype block has a sibling era_signal block that demonstrates the post-modification shape"
  - "Closed-vocab const decoupling: vocab const can outlive the column it bounded — kept for downstream filter callers (D-EXPLORE-01)"

requirements-completed: [TAX-02]

# Metrics
duration: 18m
completed: 2026-05-20
---

# Phase 49.1 Plan 06: Type unification + DAL + 4-site enricher chain Summary

**Removed all reader/writer code references to `primary_archetype` from the type system, the catalog DAL (filter interface + WHERE-builder + mapper + UPSERT SQL), and the LLM enricher chain (TASTE_TOOL schema + required + prompt + Zod TasteSchema + validateAndCleanTaste) — preparing Plans 07/08 to safely drop the DB column.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-05-20T06:42:54Z
- **Completed:** 2026-05-20T07:00Z
- **Tasks:** 2
- **Files modified:** 27 (5 production source + 22 tests/fixtures/migration scaffolds)

## Accomplishments

- `CatalogTasteAttributes` and `CatalogEntry` shapes in `src/lib/types.ts` no longer carry `primaryArchetype`; the `PrimaryArchetype` union is retained for `ARCHETYPE_CONFIG` per D-EXPLORE-02
- Four `CatalogTasteAttributes` projection sites change in lockstep per D-39b-04 (Pitfall 3): `types.ts`, `app/catalog/[catalogId]/page.tsx`, `data/watches.ts` LEFT JOIN, `verdict/composer.ts` `candidateCatalogTaste`
- `src/data/catalog.ts` cleaned of all `primary_archetype` reader/writer code: filter fields, archetype-wins tiebreaker, row mapper, UPSERT SQL, unused type import
- LLM enricher chain (5-site atomicity per Pitfall 4) cleaned: `enricher.ts` tool schema + `required`, `prompt.ts` SYSTEM_FRAMING line, `vocab.ts` Zod TasteSchema + `validateAndCleanTaste` block + dead `PRIMARY_ARCHETYPE_SET`
- `PRIMARY_ARCHETYPES` const + `PrimaryArchetype` type re-export retained in `vocab.ts` (Pitfall 5: consumed by `/explore` `CollectorArchetypes` SQL `ANY()` filter)
- Test suite green: 5,229 passing / 337 skipped / 2 pre-existing failures (documented in deferred-items.md as out-of-scope)
- `npx tsc --noEmit` archetype-related error count: 0 (down from 17 mid-task; baseline pre-existing errors unchanged)

## Task Commits

Each task was committed atomically:

1. **Task 1: Drop primaryArchetype from CatalogTasteAttributes + 3 projection sites (D-39b-04 lockstep)** — `b4d7da0` (refactor)
2. **Task 2: Strip primary_archetype from catalog DAL + 5-site enricher chain** — `fc17127` (refactor)
3. **Trailing fixture cleanups (Rule 3 follow-on)** — `5a3123f` (fix)

**Plan metadata commit:** pending (will land alongside SUMMARY.md + STATE.md + ROADMAP.md updates).

## Files Created/Modified

### Production source (modified)
- `src/lib/types.ts` — drop `primaryArchetype` from `CatalogEntry` (line 208) and `CatalogTasteAttributes` (line 224); keep `PrimaryArchetype` union (line 234)
- `src/app/catalog/[catalogId]/page.tsx` — drop `primaryArchetype` from the manual `catalogTaste` projection (D-SCOPE-01e)
- `src/data/watches.ts` — drop `primaryArchetype` from `getWatchesByUser` LEFT JOIN SELECT and row mapper; drop unused `PrimaryArchetype` import
- `src/data/catalog.ts` — drop `genre`/`archetype` from `CatalogSearchFilters`, drop archetype-wins tiebreaker from `searchCatalogWatches`, drop `primaryArchetype` from `mapRowToCatalogEntry`, drop `primary_archetype` from `updateCatalogTaste` UPSERT SET, drop unused `PrimaryArchetype` type import, drop `genre`/`archetype` from `hasActiveFacet`
- `src/lib/taste/enricher.ts` — drop `primary_archetype` from `TASTE_TOOL.input_schema.properties` and the `required` array; drop unused `PRIMARY_ARCHETYPES` import
- `src/lib/taste/prompt.ts` — drop the `primary_archetype` SYSTEM_FRAMING instruction line; drop unused `PRIMARY_ARCHETYPES` import
- `src/lib/taste/vocab.ts` — drop `primary_archetype` from Zod `TasteSchema`, drop archetype block (set lookup + drift-log warn + return-literal field) from `validateAndCleanTaste`, drop dead `PRIMARY_ARCHETYPE_SET`; KEEP `PRIMARY_ARCHETYPES` const (D-EXPLORE-02)
- `src/lib/verdict/composer.ts` — drop `primaryArchetype` from the `candidateCatalogTaste` projection (the third site in the D-39b-04 lockstep, not enumerated in the plan's interface list but required by Pitfall 3)

### Tests (modified)
- `src/data/__tests__/watches-leftjoin.test.ts` — drop archetype fixture/assertion; assert `eraSignal` pass-through instead
- `src/lib/taste/enricher.test.ts` — drop `primaryArchetype` assertion from happy-path; drop archetype drift-log test (era-signal sibling test is the survivor)
- `src/lib/taste/vocab.test.ts` — drop `primary_archetype` from `VALID_WIRE`; drop `primary_archetype` drift-log test
- `src/lib/verdict/{composer,confidence,shims}.test.ts` — drop `primaryArchetype: null` from `CatalogEntry` fixtures
- `tests/integration/catalog-taste.test.ts` — drop `primaryArchetype` from `VALID_TASTE`; drop `row.primaryArchetype` assertion; rename "8 taste fields" → "7 taste fields"
- `tests/integration/catalog-taste-schema.test.ts` — drop `primary_archetype` CHECK-constraint tests (column drop ships in Plans 07/08); drop unused `PRIMARY_ARCHETYPES` import
- `tests/integration/migration-drop-archetype.test.ts` — drop `as unknown as CatalogTasteAttributes` cast (no longer needed)
- `tests/integration/add-watch-photo.test.ts` — drop `primaryArchetype` from enricher mock return
- `tests/app/{catalog-page,watch-new-page}.test.ts` — drop `primaryArchetype: null` from `baseCatalogEntry` literals
- `tests/static/composer-engine-alignment.test.ts` — drop `primaryArchetype` from `catalogEntryFrom` mapper
- `tests/unit/lib/verdict/fit-delta.test.ts` — drop `primaryArchetype: null` from `taste()` fixture builder; clean stale Plan-06 TODO comment
- `tests/components/insights/CollectionFitCompareTable.test.tsx` — drop `as unknown as CatalogTasteAttributes` cast
- `tests/components/insights/ReferenceIdentityCard.test.tsx` — drop `as unknown as CatalogTasteAttributes` cast; drop obsolete Plan-06 TODO
- `tests/fixtures/catalogTaste.ts` — drop `primaryArchetype` from all 8 shared `CatalogTasteAttributes` fixtures (subLikeTaste, datejustLikeTaste, speedyLikeTaste, tankLikeTaste, lowConfTaste, exactlyHalfConfTaste, justBelowHalfTaste, emptyMotifsTaste, nullNumericsTaste)
- `tests/setup/mockAnthropic.ts` — drop `primary_archetype` from `VALID_TOOL_USE_INPUT`
- `tests/actions/verdict.test.ts` — drop `primaryArchetype: null` from `fakeCatalogEntry` literal

### Documentation (created)
- `.planning/phases/49.1-remove-genre-surface/deferred-items.md` — logs the pre-existing `tests/integration/backfill-taste.test.ts` failure as out-of-scope

## Decisions Made

- **Composer.ts:107 added to Task 1 scope** (Rule 3): the plan's `<interfaces>` section enumerated 3 lockstep projection sites (types.ts, watches.ts LEFT JOIN, catalog page projection). Pitfall 3 demanded all sites move together. Composer.ts builds a 4th `CatalogTasteAttributes` projection at `candidateCatalogTaste`; dropping the field from the type would make this site fail tsc. Treated as the 4th D-39b-04 lockstep site and bundled with Task 1.
- **PRIMARY_ARCHETYPE_SET removed** (planner discretion per plan body): once the archetype block in `validateAndCleanTaste` is gone, `PRIMARY_ARCHETYPE_SET` has no consumer. `grep -rn` confirmed zero callers; deleted.
- **CHECK-constraint tests removed in catalog-taste-schema.test.ts**: the `primary_archetype` CHECK constraint will be dropped along with the column in Plans 07/08. Removing the tests now (rather than leaving them red after migration) keeps the suite clean; the era_signal + image_source_quality sibling CHECK tests remain.
- **`unknown` casts removed** in 3 test files (`CollectionFitCompareTable.test.tsx`, `migration-drop-archetype.test.ts`, `ReferenceIdentityCard.test.tsx`): these had been carrying TODOs anticipating the Plan 06 type drop. Cleaning them up is part of the type-system unification.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Added composer.ts:107 to Task 1 atomic scope**
- **Found during:** Task 1 (Drop primaryArchetype from CatalogTasteAttributes + projection sites)
- **Issue:** `src/lib/verdict/composer.ts:107` builds a 4th `CatalogTasteAttributes` projection (`candidateCatalogTaste`) that was not enumerated in the plan's `<interfaces>` section. Pitfall 3 (D-39b-04 identical-rendering lock) requires all sites that build the shape to change atomically; otherwise `tsc` fails on the projection-literal-vs-interface mismatch.
- **Fix:** Bundled composer.ts:107 into the Task 1 commit (b4d7da0) alongside types.ts + page.tsx + watches.ts
- **Files modified:** src/lib/verdict/composer.ts
- **Verification:** `npx tsc --noEmit` reports zero archetype-related errors post-commit
- **Committed in:** b4d7da0 (Task 1 atomic commit)

**2. [Rule 3 — Blocking] Cleaned up 10 test fixtures that used to set `primaryArchetype: null` on `CatalogEntry` / `CatalogTasteAttributes` literals**
- **Found during:** Task 1 (after dropping field from type, fixtures became excess-property TS errors or could be cleaned up)
- **Issue:** Dropping the field from the type produces TS error `TS2353: Object literal may only specify known properties` in 9 explicit-type fixture sites; the test files were also carrying TODO comments anticipating the drop.
- **Fix:** Removed the `primaryArchetype: null` (or `: 'dive' / 'dress'`) lines and the now-unnecessary `unknown` casts from the test fixtures. Tests still exercise the same code paths because `primaryArchetype` was always null/optional at these call sites.
- **Files modified:** see "Tests (modified)" list above; specifically composer.test.ts, confidence.test.ts, shims.test.ts, catalog-page.test.ts, watch-new-page.test.ts, composer-engine-alignment.test.ts, fit-delta.test.ts (Task 1 commit); catalog-taste.test.ts, catalogTaste.ts, mockAnthropic.ts, add-watch-photo.test.ts (Task 2 commit); verdict.test.ts, ReferenceIdentityCard.test.tsx (commit 3 — Rule 3 follow-on)
- **Verification:** `npx tsc --noEmit` reports zero archetype-related errors; `npx vitest run` reports 5,229 passing tests
- **Committed in:** b4d7da0, fc17127, 5a3123f (split across the three commits by file domain)

**3. [Rule 3 — Blocking] PRIMARY_ARCHETYPE_SET removed in vocab.ts**
- **Found during:** Task 2 (Strip primary_archetype from enricher chain)
- **Issue:** The plan called for deleting the archetype block from `validateAndCleanTaste`. After that block is gone, `PRIMARY_ARCHETYPE_SET` (built from PRIMARY_ARCHETYPES specifically for that block's `.has()` lookup) has no consumer.
- **Fix:** `grep -rn "PRIMARY_ARCHETYPE_SET" src/ tests/` returned zero remaining callers post-block-removal. Deleted the const (with a comment explaining why PRIMARY_ARCHETYPES itself stays). Plan body explicitly authorized this: "If grep confirms zero consumers, delete it; otherwise leave it."
- **Files modified:** src/lib/taste/vocab.ts
- **Verification:** `grep -rn "PRIMARY_ARCHETYPE_SET" src/ tests/` returns no matches; PRIMARY_ARCHETYPES const + re-export intact
- **Committed in:** fc17127 (Task 2 atomic commit)

---

**Total deviations:** 3 auto-fixed (all Rule 3 — blocking type-system propagation; no architectural changes)
**Impact on plan:** All auto-fixes were required to keep `tsc` clean after the type-system change. No scope creep — composer.ts and PRIMARY_ARCHETYPE_SET cleanup are direct consequences of the planned interface drop. The 10 test-fixture cleanups are likewise mechanical TS error fixes.

## Issues Encountered

- **Pre-existing test failure in `tests/integration/backfill-taste.test.ts`** (2 of 14 cases): The reenrich-taste.ts script invocation fails with `node: .env.local: not found`. Reproduced on the Plan 06 base commit before any edits — unrelated to this plan. Logged in `.planning/phases/49.1-remove-genre-surface/deferred-items.md` for a future polish phase.
- **Worktree was 30+ commits behind main on spawn:** The worktree at `agent-a951ba3c13d2f5c0b` was branched off `main` at the v4.8 (Phase 48 close) tip — before any Phase 49.1 commits landed. Pulled in via `git merge main --no-edit` at the top of the task; no conflicts.

## TDD Gate Compliance

Plan frontmatter declares `tdd="true"` per task, but the plan body explicitly establishes this as a **subtractive refactor with existing tests being repointed**, not a feature-add. No new RED → GREEN cycle is meaningful here:
- Task 1: dropped field from `CatalogTasteAttributes`; existing tests (`watches-leftjoin.test.ts`, others) repointed to the new shape; tsc + vitest both green.
- Task 2: dropped enricher path; existing tests (`enricher.test.ts`, `vocab.test.ts`, integration tests) repointed to the new shape; tsc + vitest both green.

The Wave 0 `tests/integration/migration-drop-archetype.test.ts` (Plan 01) IS the long-form RED test for this entire phase — it transitions from red on the type-projection assertion to green after this plan, and stays red on the column-absent assertion until Plans 07/08 ship.

Per the central GSD MVP+TDD gate predicate, this plan's tasks are **non-behavior-adding** (the `<behavior>` blocks describe shape invariants, not user-visible behavior changes), so the RED/GREEN gate does not fire.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 07 (Drizzle schema drop):** The DB column `watches_catalog.primary_archetype` has zero reader/writer code references outside `src/db/schema.ts:390`. Plan 07 can safely write the drizzle migration (`drizzle/migrations/0012_phase49_1_drop_primary_archetype.sql`) and remove the column declaration in schema.ts.
- **Plan 08 (Supabase prod migration):** Once Plan 07's local migration is applied, Plan 08 ships the mirror at `supabase/migrations/20260520xxxxxx_phase49_1_drop_primary_archetype.sql` and runs `supabase db push --linked`.
- **Wave 0 `tests/integration/migration-drop-archetype.test.ts`** transitions: today (after Plan 06) it's still red on "column absent" but green on the type-shape assertions. After Plan 07 (local), it goes fully green when `DATABASE_URL` points at the local DB. After Plan 08 (prod), it goes fully green against prod.

## Self-Check: PASSED

All 29 claimed files exist on disk; all 3 claimed task commits (b4d7da0, fc17127, 5a3123f) are present in `git log`. No missing artifacts.

---
*Phase: 49.1-remove-genre-surface*
*Completed: 2026-05-20*
