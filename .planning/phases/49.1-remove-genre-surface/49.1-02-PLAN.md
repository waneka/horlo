---
phase: 49.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/verdict/templates.ts
  - src/lib/verdict/composer.ts
  - src/lib/verdict/viewerTasteProfile.ts
  - src/lib/verdict/types.ts
  - src/lib/verdict/fit-delta.ts
  - src/lib/verdict/composer.test.ts
  - src/lib/verdict/shims.test.ts
  - src/lib/verdict/viewerTasteProfile.test.ts
  - tests/unit/lib/verdict/fit-delta.test.ts
autonomous: true
requirements: [TAX-02]
must_haves:
  truths:
    - "The three Phase-20-D-01 verdict templates (fills-a-hole, collection-skews-contrast, archetype-echo) discriminate on dominantEraSignal / eraSignal — not on primaryArchetype"
    - "The reserved era-echo template still exists and is narratively distinct from the rewritten templates per D-VERDICT-02"
    - "ViewerTasteProfile.dominantArchetype is gone from the interface, EMPTY_PROFILE, the SELECT clause, and every test fixture"
    - "fit-delta produces a 5-dimension ranked output (formality > sportiness > heritageScore > eraSignal > designMotifs); the 'Different archetype: X vs Y' phrase branch is gone"
  artifacts:
    - path: src/lib/verdict/templates.ts
      provides: "Era-axis pivot for the 3 archetype-using templates per D-VERDICT-01"
      contains: "dominantEraSignal"
    - path: src/lib/verdict/types.ts
      provides: "CandidateTasteSnapshot extended with eraSignal; ViewerTasteProfile without dominantArchetype"
      contains: "eraSignal"
    - path: src/lib/verdict/composer.ts
      provides: "Snapshot construction threads catalogEntry.eraSignal; drops primaryArchetype"
    - path: src/lib/verdict/viewerTasteProfile.ts
      provides: "EMPTY_PROFILE without dominantArchetype; mode<>() returns only era + topMotifs"
    - path: src/lib/verdict/fit-delta.ts
      provides: "5-dimension ranking; no archetype branch"
  key_links:
    - from: src/lib/verdict/templates.ts
      to: src/lib/verdict/types.ts
      via: "predicate reads (taste as CandidateTasteSnapshot).eraSignal"
      pattern: "taste\\.eraSignal"
    - from: src/lib/verdict/composer.ts
      to: src/lib/verdict/types.ts
      via: "snapshot literal includes eraSignal: catalogEntry.eraSignal"
      pattern: "eraSignal: catalogEntry\\.eraSignal"
---

<objective>
Execute the D-VERDICT decision group: pivot the three archetype-discriminating verdict templates (fills-a-hole, collection-skews-contrast, archetype-echo) to the era axis; drop dominantArchetype from ViewerTasteProfile; extend CandidateTasteSnapshot with eraSignal and thread it through composer; drop the primaryArchetype dimension from fit-delta. Update the four verdict-related test files to match.

Purpose: D-VERDICT-01 + D-VERDICT-03 + D-SCOPE-01c. The Phase 20 D-01 "ARE NOT optional" templates still fire after this phase — they just discriminate on era rather than archetype, so the spike's "remove-genre" verdict is honored without dropping mandated copy slots.

Output: Modified verdict-engine source + tests. No DB schema change in this plan; no UI change in this plan. Era pivot is engine-internal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49.1-remove-genre-surface/49.1-CONTEXT.md
@.planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md
@.planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md

# Files being modified
@src/lib/verdict/templates.ts
@src/lib/verdict/composer.ts
@src/lib/verdict/viewerTasteProfile.ts
@src/lib/verdict/types.ts
@src/lib/verdict/fit-delta.ts

# Existing tests being updated
@src/lib/verdict/composer.test.ts
@src/lib/verdict/shims.test.ts
@src/lib/verdict/viewerTasteProfile.test.ts
@tests/unit/lib/verdict/fit-delta.test.ts

<interfaces>
<!-- Key contracts the verdict engine depends on. Pulled from 49.1-PATTERNS.md and current source. -->

From src/lib/verdict/types.ts (current shape):
- `ViewerTasteProfile` interface has `dominantArchetype: PrimaryArchetype | null` and `dominantEraSignal: EraSignal | null`. The plan REMOVES the dominantArchetype field.
- `CandidateTasteSnapshot` interface currently has `primaryArchetype`, `heritageScore`, `formality`, `sportiness`, `confidence` (per types.ts:72-78). The plan REMOVES `primaryArchetype` and ADDS `eraSignal: EraSignal | null` (per RESEARCH §"Verdict template rewire" Open Question #1).

From src/lib/verdict/composer.ts:
- Snapshot construction at composer.ts:47-53 builds CandidateTasteSnapshot from `catalogEntry`. The plan DROPS the `primaryArchetype` line and ADDS `eraSignal: catalogEntry.eraSignal` line.

From src/lib/verdict/viewerTasteProfile.ts (current shape):
- EMPTY_PROFILE at :15-22 has `dominantArchetype: null` — DROP this line.
- SELECT at :47 projects `primaryArchetype: watchesCatalog.primaryArchetype` — DROP this line.
- mode<>() call at :77-81 computes dominantArchetype — DROP the whole 4-line block; keep the sibling dominantEraSignal block at :82-86.
- The `PrimaryArchetype` import at :5 may become unused after edits — remove if so.

From src/lib/verdict/fit-delta.ts:
- Lines 35, 51, 76, 105-106 contain the archetype dimension — DROP all four.
- Tie-break doc comment at :59-60 must become `formality > sportiness > heritageScore > eraSignal > designMotifs` (5 dims, not 6).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend CandidateTasteSnapshot with eraSignal; drop dominantArchetype from ViewerTasteProfile; update composer + viewerTasteProfile</name>
  <files>src/lib/verdict/types.ts, src/lib/verdict/composer.ts, src/lib/verdict/viewerTasteProfile.ts</files>
  <read_first>
    - src/lib/verdict/types.ts (full file — read both ViewerTasteProfile and CandidateTasteSnapshot interfaces)
    - src/lib/verdict/composer.ts (full file — locate the snapshot construction at lines 47-53)
    - src/lib/verdict/viewerTasteProfile.ts (full file — EMPTY_PROFILE at :15-22, SELECT at :47, mode<>() call at :77-81)
    - src/lib/verdict/viewerTasteProfile.test.ts (so tests guide the type changes; not modified in this task — test edits are Task 3)
    - .planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md §"src/lib/verdict/viewerTasteProfile.ts" and §"Pattern D: Sibling-survivor"
  </read_first>
  <behavior>
    - `CandidateTasteSnapshot` exports an `eraSignal: EraSignal | null` field and no longer exports `primaryArchetype`.
    - `ViewerTasteProfile` no longer exports `dominantArchetype`; `dominantEraSignal: EraSignal | null` remains.
    - `EMPTY_PROFILE` does not contain a `dominantArchetype` key.
    - `buildViewerTasteProfile()` SELECT clause does not project `primaryArchetype`; the return literal contains `dominantEraSignal` but not `dominantArchetype`.
    - composer.ts snapshot literal contains `eraSignal: catalogEntry.eraSignal` and does NOT contain `primaryArchetype: catalogEntry.primaryArchetype`.
    - `npx tsc --noEmit` passes after these edits (all references to the dropped fields in src/lib/verdict/ are updated; references outside this directory are left for Wave 2/Plan 06).
  </behavior>
  <action>
    In src/lib/verdict/types.ts: edit the `ViewerTasteProfile` interface to remove the `dominantArchetype: PrimaryArchetype | null` field; KEEP the existing `dominantEraSignal: EraSignal | null` field. Edit the `CandidateTasteSnapshot` interface (lines 72-78) to (a) remove the `primaryArchetype: PrimaryArchetype | null` field and (b) ADD `eraSignal: EraSignal | null` in its place using the same null-allowed pattern. Import `EraSignal` from `@/lib/types` if not already imported. If `PrimaryArchetype` is now unused in this file, remove its import.

    In src/lib/verdict/viewerTasteProfile.ts: (1) In EMPTY_PROFILE at lines 15-22, delete the `dominantArchetype: null,` line; keep `dominantEraSignal: null,` and the other surviving fields. (2) In the SELECT clause at line 47, delete the `primaryArchetype: watchesCatalog.primaryArchetype,` field. (3) In the return literal at lines 77-81, delete the 4-line `dominantArchetype: mode<PrimaryArchetype>(...)` block; keep the sibling 4-line `dominantEraSignal: mode<EraSignal>(...)` block UNCHANGED. (4) Remove the `PrimaryArchetype` import at line 5 if it has no other reference in the file after these edits.

    In src/lib/verdict/composer.ts: at the snapshot construction site (around lines 47-53), DELETE the line that reads `primaryArchetype: catalogEntry.primaryArchetype,` (or its equivalent shape; verify against the current source). ADD a new line `eraSignal: catalogEntry.eraSignal,` in the same literal. Confirm `catalogEntry` has an `eraSignal` field on its type (it does — see `src/lib/types.ts` CatalogEntry shape; that file is NOT modified in this plan, only consumed here).

    Do not touch any other file in this task. Do not touch tests in this task — Task 3 owns the test edits.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "(verdict/(types|composer|viewerTasteProfile))" | grep -v -E "^#" | head -20 ; echo "---exit:$?" ; npm test -- src/lib/verdict/viewerTasteProfile.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "dominantArchetype" src/lib/verdict/types.ts` returns no matches
    - `grep -n "dominantArchetype" src/lib/verdict/viewerTasteProfile.ts` returns no matches
    - `grep -n "primaryArchetype" src/lib/verdict/composer.ts` returns no matches
    - `grep -n "primaryArchetype" src/lib/verdict/types.ts` returns no matches (inside CandidateTasteSnapshot)
    - `grep -n "eraSignal" src/lib/verdict/types.ts` returns at least 2 matches (ViewerTasteProfile.dominantEraSignal + CandidateTasteSnapshot.eraSignal)
    - `grep -n "eraSignal: catalogEntry.eraSignal" src/lib/verdict/composer.ts` returns at least 1 match
    - `npx tsc --noEmit` produces no NEW errors localized to `src/lib/verdict/*.ts` (errors in OTHER files referencing the dropped fields are expected and will be cleared by Tasks 2/3 of this plan AND Plan 06)
  </acceptance_criteria>
  <done>Types + composer + viewerTasteProfile reflect the era pivot; tsc errors outside this plan's scope are documented but expected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pivot the 3 archetype-using verdict templates to the era axis; strip archetype dimension from fit-delta</name>
  <files>src/lib/verdict/templates.ts, src/lib/verdict/fit-delta.ts</files>
  <read_first>
    - src/lib/verdict/templates.ts (full file — locate fills-a-hole at :16-26, collection-skews-contrast at :38-48, archetype-echo at :85-93, and the reserved era-echo at :95-104)
    - src/lib/verdict/fit-delta.ts (full file — locate archetype lines at :35, 51, 76, 105-106 plus the tie-break doc comment at :59-60)
    - src/lib/verdict/types.ts (updated by Task 1 — confirm CandidateTasteSnapshot now has eraSignal)
    - .planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md §"src/lib/verdict/templates.ts" and §"src/lib/verdict/fit-delta.ts"
    - .planning/phases/49.1-remove-genre-surface/49.1-CONTEXT.md §D-VERDICT (decisions 01-04, esp. D-VERDICT-02 narrative-distinctness from era-echo)
  </read_first>
  <behavior>
    - `fills-a-hole` predicate fires when `taste.eraSignal` is set AND `profile.dominantEraSignal !== taste.eraSignal` AND result.label is `taste-expansion` or `outlier`. Template string contains the literal `${era}` placeholder and is narratively distinct from era-echo (per D-VERDICT-02). Required wording (per planner discretion within D-VERDICT-01/02): main template ends with the phrase "your first ${era} piece" (or analogous "first-of-era" framing); rationale phrase mirrors with first-person ("My first ${era} piece").
    - `collection-skews-contrast` predicate fires when both `profile.dominantEraSignal` and `taste.eraSignal` are set AND differ. Template contains BOTH `${dominant_era}` (or `${era_A}`) and `${contrast_era}` (or `${era_B}`) placeholders. Required framing (D-VERDICT-01): "Your collection skews ${dominant_era} — this is a ${contrast_era} piece" or analogous contrast wording.
    - `archetype-echo` (renamed in spirit but keeping the existing template `id` is permitted per D-VERDICT-02 "exact angle is planner discretion") pivots to era and remains narratively distinct from `era-echo`. Suggested framing: "another ${era} piece, leaning further in" or similar — distinct from era-echo's "Echoes the ${era} era of your collection".
    - The reserved `era-echo` template (id: 'era-echo', predicate returns null today) is UNCHANGED.
    - `fit-delta.ts` exports a 5-dimension ranked output. The dimensions array (or equivalent) lists `formality, sportiness, heritageScore, eraSignal, designMotifs` in that priority order. The `'Different archetype: X vs Y'` phrase case is removed.
  </behavior>
  <action>
    In src/lib/verdict/templates.ts:

    Edit the three archetype-using templates (`fills-a-hole`, `collection-skews-contrast`, `archetype-echo`). For each, rewrite the predicate to read `(taste as CandidateTasteSnapshot).eraSignal` and `profile.dominantEraSignal` instead of `taste.primaryArchetype` and `profile.dominantArchetype`. Rewrite the `template` and `rationaleTemplate` strings to use `${era}` (or `${dominant_era}` / `${contrast_era}` as needed) placeholders. Specific narrative locks per D-VERDICT-01/02:

    - `fills-a-hole`: predicate gates on label `taste-expansion | outlier` AND `taste.eraSignal` set AND `profile.dominantEraSignal !== taste.eraSignal`. Returns `{ era: taste.eraSignal }`. Template: `'Fills a hole in your collection — your first ${era} piece.'`. RationaleTemplate: `'My first ${era} piece — fills a real hole in what I own.'`.
    - `collection-skews-contrast`: predicate gates on both era fields set AND distinct. Returns `{ dominant_era: profile.dominantEraSignal, contrast_era: taste.eraSignal }`. Template: `'Your collection skews ${dominant_era} — this is a ${contrast_era} piece.'`. RationaleTemplate: `'My collection skews ${dominant_era}, and this is a ${contrast_era} piece.'`.
    - `archetype-echo`: KEEP the `id: 'archetype-echo'` (changing it would silently break any persisted/cached references). Pivot the predicate to era: gates on `taste.eraSignal` set AND `profile.dominantEraSignal === taste.eraSignal`. Returns `{ era: taste.eraSignal }`. Template MUST be narratively distinct from era-echo (D-VERDICT-02). Use: `'Another ${era} piece — leaning further into the era you favor.'`. RationaleTemplate: `'Another ${era} piece — leans further into the era I keep picking.'`.

    The reserved `era-echo` template (around lines 95-104) is UNCHANGED.

    In src/lib/verdict/fit-delta.ts:

    Delete the four archetype-dimension lines:
    - `:35` — the `archetypeDelta` const declaration line
    - `:51` — the `archetypeDelta === 0 &&` participation line
    - `:76` — the `if (archetypeDelta === 1) tryUpdate('primaryArchetype', archetypeDelta)` block
    - `:105-106` — the `case 'primaryArchetype': return 'Different archetype: ...'` branch

    Update the tie-break doc comment at :59-60 from `formality > sportiness > heritageScore > primaryArchetype > eraSignal > designMotifs` to `formality > sportiness > heritageScore > eraSignal > designMotifs` (5 dimensions). If the ranked-dimension list/type union elsewhere in the file references `'primaryArchetype'` (e.g., a string literal union for `tryUpdate`), remove that entry.
  </action>
  <verify>
    <automated>npm test -- src/lib/verdict/composer.test.ts tests/unit/lib/verdict/fit-delta.test.ts 2>&1 | tail -15 ; grep -nE "primaryArchetype|dominantArchetype|archetypeDelta|Different archetype" src/lib/verdict/templates.ts src/lib/verdict/fit-delta.ts || echo "OK no matches"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "primaryArchetype|dominantArchetype" src/lib/verdict/templates.ts` returns no matches
    - `grep -nE "primaryArchetype|archetypeDelta|Different archetype" src/lib/verdict/fit-delta.ts` returns no matches
    - `grep -n "dominantEraSignal" src/lib/verdict/templates.ts` returns at least 3 matches (one in each of the 3 rewritten predicates)
    - `grep -n "id: 'era-echo'" src/lib/verdict/templates.ts` returns exactly 1 match (the reserved template is preserved)
    - `grep -n "id: 'archetype-echo'" src/lib/verdict/templates.ts` returns exactly 1 match (the id is kept per planner intent)
    - The fit-delta tie-break comment lists exactly 5 dimensions: `formality`, `sportiness`, `heritageScore`, `eraSignal`, `designMotifs`
    - `npm test -- src/lib/verdict/composer.test.ts tests/unit/lib/verdict/fit-delta.test.ts` shows fewer FAILures than before this task (any remaining FAILs must come from outdated test fixtures, which Task 3 fixes)
  </acceptance_criteria>
  <done>Templates pivot to era; fit-delta is 5-dimensional; reserved era-echo template untouched; archetype-echo renamed in spirit but id preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Repoint verdict test fixtures (composer, shims, viewerTasteProfile, fit-delta) to era axis</name>
  <files>src/lib/verdict/composer.test.ts, src/lib/verdict/shims.test.ts, src/lib/verdict/viewerTasteProfile.test.ts, tests/unit/lib/verdict/fit-delta.test.ts</files>
  <read_first>
    - src/lib/verdict/composer.test.ts (full file — locate any archetype-template fixtures + dominantArchetype/primaryArchetype references)
    - src/lib/verdict/shims.test.ts (full file — esp. line 42 with `primaryArchetype: 'dive'` per CONTEXT)
    - src/lib/verdict/viewerTasteProfile.test.ts (full file — locate dominantArchetype assertions)
    - tests/unit/lib/verdict/fit-delta.test.ts (full file — esp. lines 51-54 with "Different archetype: Dive vs Dress")
    - src/lib/verdict/types.ts (updated by Task 1 — confirm the new shape for fixture literals)
    - .planning/phases/49.1-remove-genre-surface/49.1-CONTEXT.md §D-VERDICT-04 (Phase 20 D-01 lock preserved by the pivot — tests repoint to era, no override needed)
  </read_first>
  <behavior>
    - composer.test.ts archetype-template assertions become era-template assertions. Where a test previously asserted "fills-a-hole fires when candidate's primaryArchetype differs from profile.dominantArchetype", it now asserts "fills-a-hole fires when candidate's eraSignal differs from profile.dominantEraSignal". The Phase 20 D-01 "ARE NOT optional" coverage is preserved by the pivot.
    - shims.test.ts fixtures: any object literal with `primaryArchetype: 'dive'` (or any archetype value) loses that key and gains `eraSignal: 'modern'` (or whichever era value is contextually plausible).
    - viewerTasteProfile.test.ts: assertions on `result.dominantArchetype` are DELETED; assertions on `result.dominantEraSignal` are PRESERVED. EMPTY_PROFILE comparisons drop the `dominantArchetype: null` key from expected literals.
    - fit-delta.test.ts: the `'Different archetype: Dive vs Dress'` expectation at lines 51-54 is DELETED; if the case was the only assertion in its block, the whole `it(...)` block is deleted. Any test asserting "ranks 6 dimensions" becomes "ranks 5 dimensions" or is removed if the dimension count is no longer the load-bearing assertion.
  </behavior>
  <action>
    In src/lib/verdict/composer.test.ts: search for every fixture literal containing `primaryArchetype` and every test asserting on `dominantArchetype` / `primaryArchetype`. Replace the field with `eraSignal` (pick a plausible value matching the test's intent — `'modern'`, `'vintage'`, `'contemporary'`, or `'transitional'` based on `EraSignal` union members). For tests that asserted on the `fills-a-hole` / `collection-skews-contrast` / `archetype-echo` templates, repoint the input fixtures so the era axis triggers the predicate. The output `template` strings in assertions change from archetype copy to era copy (matching Task 2's new template strings).

    In src/lib/verdict/shims.test.ts: at the line referenced by CONTEXT (~:42) with `primaryArchetype: 'dive'`, delete that key. If the fixture is used by a `CandidateTasteSnapshot` test, also add `eraSignal: 'modern'` (or analogous) — but ONLY if the type now requires it. If `CandidateTasteSnapshot.eraSignal` is `EraSignal | null`, then `null` is acceptable and the cleanest minimal change is to either omit (TS error if required) or set to `null`.

    In src/lib/verdict/viewerTasteProfile.test.ts: delete every assertion on `dominantArchetype`. Keep every assertion on `dominantEraSignal`. EMPTY_PROFILE comparisons must drop the `dominantArchetype: null` key from `toEqual(...)` literals.

    In tests/unit/lib/verdict/fit-delta.test.ts: delete the lines around 51-54 asserting `'Different archetype: Dive vs Dress'`. If the surrounding `it(...)` block was solely about archetype, delete the whole `it(...)` block. If the test was structured around "ranks N dimensions", update N from 6 to 5. Also delete any fixture field `primaryArchetype: 'X'` and replace with the era-axis equivalent where the test was checking dimension priority.
  </action>
  <verify>
    <automated>npm test -- src/lib/verdict/ tests/unit/lib/verdict/ 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "primaryArchetype|dominantArchetype" src/lib/verdict/composer.test.ts src/lib/verdict/shims.test.ts src/lib/verdict/viewerTasteProfile.test.ts tests/unit/lib/verdict/fit-delta.test.ts` returns no matches
    - `grep -n "Different archetype" tests/unit/lib/verdict/fit-delta.test.ts` returns no matches
    - `npm test -- src/lib/verdict/composer.test.ts src/lib/verdict/shims.test.ts src/lib/verdict/viewerTasteProfile.test.ts tests/unit/lib/verdict/fit-delta.test.ts` exits 0 (all four files green)
    - `npm test -- src/lib/verdict/` exits 0 for the verdict-engine subtree (any cross-cutting test that still references the dropped column from outside this plan's file scope is expected to fail until Plan 06 lands — those failures are documented but not blocking for this plan)
  </acceptance_criteria>
  <done>All four verdict test files green; archetype references removed from fixtures and assertions; era axis is the new test ground truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. Verdict engine code is server-only and consumes already-validated catalog data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49.1-03 | T (Tampering) | src/lib/verdict/templates.ts | mitigate | Template `${era}` interpolation reads from `taste.eraSignal` which is a closed-vocab EraSignal value validated by the enricher; no user input flows into the template body |
| T-49.1-04 | I (Information disclosure) | src/lib/verdict/composer.ts | accept | Snapshot literal carries non-sensitive catalog taste attributes; same data flow as pre-49.1, just one less field |
</threat_model>

<verification>
- `npm test -- src/lib/verdict/ tests/unit/lib/verdict/` exits 0 for the verdict subtree
- No archetype references remain in any verdict-engine source or test file (`grep` gates above)
- TypeScript compile errors outside src/lib/verdict/ are expected (downstream callers still reference dropped fields) — Plan 06 will resolve them
- The reserved era-echo template is preserved (still has predicate returning null, same template string)
</verification>

<success_criteria>
- The three D-VERDICT-01 templates discriminate on era axis with planner-discretion narrative copy that meets D-VERDICT-02's "distinct from era-echo" lock
- D-VERDICT-03 fully applied: dominantArchetype gone from interface, EMPTY_PROFILE, SELECT, return literal
- D-SCOPE-01c fully applied: fit-delta is 5-dimensional; archetype branch and "Different archetype" phrase deleted
- D-VERDICT-04 satisfied: composer.test.ts archetype-template fixtures repointed to era; no override of Phase 20 D-01 needed
- Wave 2 (Plan 06) can land its type-system changes against this plan's exported contracts without rework
</success_criteria>

<output>
After completion, create `.planning/phases/49.1-remove-genre-surface/49.1-02-SUMMARY.md`
</output>
