---
phase: 49.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/migration-drop-archetype.test.ts
  - tests/app/explore-genres-404.test.ts
  - tests/components/insights/CollectionFitCompareTable.test.tsx
autonomous: true
requirements: [TAX-02]
must_haves:
  truths:
    - "A vitest integration test exists that asserts the absence of the primary_archetype column post-migration"
    - "A vitest test exists asserting src/app/explore/genres/page.tsx is absent on disk"
    - "A component test exists asserting CollectionFitCompareTable renders 5 rows after Row 4 (Archetype) is deleted"
  artifacts:
    - path: tests/integration/migration-drop-archetype.test.ts
      provides: "DATABASE_URL-gated integration test for the schema drop"
      contains: "describe.skip"
    - path: tests/app/explore-genres-404.test.ts
      provides: "Filesystem-absence smoke test for deleted route"
    - path: tests/components/insights/CollectionFitCompareTable.test.tsx
      provides: "Component test for 5-row compare table"
  key_links:
    - from: tests/integration/migration-drop-archetype.test.ts
      to: src/data/catalog.ts
      via: "DATABASE_URL gate + drizzle direct-db assertions"
      pattern: "information_schema.columns"
    - from: tests/components/insights/CollectionFitCompareTable.test.tsx
      to: src/components/insights/CollectionFitCompareTable.tsx
      via: "render() under jsdom; row count assertion"
      pattern: "queryAllByRole\\('row'\\)"
---

<objective>
Author the three Wave 0 test scaffolds required by `49.1-VALIDATION.md` BEFORE any production code changes. Each test must fail today (assertions reference state that does not yet exist: dropped column, absent file, 5-row table) — that is intentional. Subsequent waves will make them pass.

Purpose: Establish the Nyquist-compliant verification spine. Each later wave can re-run these tests as it lands its production changes; the tests transition from red → green as their target state arrives. This is the standard Wave-0-before-implementation pattern called out in `49.1-VALIDATION.md ## Wave 0 Requirements`.

Output: Three new test files at the paths listed in `files_modified`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49.1-remove-genre-surface/49.1-CONTEXT.md
@.planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md
@.planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md
@.planning/phases/49.1-remove-genre-surface/49.1-VALIDATION.md

# Analog files for test patterns
@tests/integration/catalog-taste.test.ts
@tests/components/insights/ReferenceIdentityCard.test.tsx
@vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author tests/integration/migration-drop-archetype.test.ts</name>
  <files>tests/integration/migration-drop-archetype.test.ts</files>
  <read_first>
    - tests/integration/catalog-taste.test.ts (DATABASE_URL-gated `maybe = describe.skip` idiom; drizzle direct-db assertion pattern)
    - tests/integration/catalog-taste-schema.test.ts (information_schema.columns assertion idiom if present)
    - src/db/schema.ts (current watchesCatalog declaration so the test references real columns)
    - src/data/catalog.ts:75-110 (mapRowToCatalogEntry shape)
    - .planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md §"Test analog: tests/integration/migration-drop-archetype.test.ts"
  </read_first>
  <action>
    Create a new vitest integration test mirroring the `maybe = process.env.DATABASE_URL ? describe : describe.skip` idiom from `tests/integration/catalog-taste.test.ts:8`. Test name: "Phase 49.1 — migration drops primary_archetype column". The file must include three test cases:

    1. "primary_archetype column is absent from watches_catalog" — issue `db.execute(sql\`SELECT column_name FROM information_schema.columns WHERE table_schema = 'public' AND table_name = 'watches_catalog' AND column_name = 'primary_archetype'\`)` and assert the returned array length is 0.
    2. "getWatchesByUser returns rows whose catalogTaste literal has no primaryArchetype key" — seed via `updateCatalogTaste` against a fixture catalog row, then call `getWatchesByUser(userId)` (import from `src/data/watches.ts`) and assert `Object.prototype.hasOwnProperty.call(row.catalogTaste, 'primaryArchetype')` is false while `row.catalogTaste.eraSignal` and `row.catalogTaste.formality` are present.
    3. "updateCatalogTaste succeeds without a primaryArchetype field on the input" — construct a `CatalogTasteAttributes`-shaped literal (per `src/lib/types.ts`) without `primaryArchetype`, call `updateCatalogTaste`, assert no throw and that the row is readable.

    Use the same `afterAll(() => db.execute(sql\`DELETE FROM watches_catalog WHERE brand = ${TEST_BRAND}\`))` cleanup as the analog. Use a TEST_BRAND prefix of `_test_phase49_1_${Date.now()}`. Import `describe`, `it`, `expect`, `afterAll` from vitest. Import `db` from `@/db`, `watchesCatalog` from `@/db/schema`, `sql`, `eq` from `drizzle-orm`. Import `updateCatalogTaste` from `@/data/catalog`. Import `getWatchesByUser` from `@/data/watches`. Import `CatalogTasteAttributes` type from `@/lib/types`.

    NOTE: This test will skip (not fail) on machines without DATABASE_URL set. That is the analog's behavior and is intentional.
  </action>
  <verify>
    <automated>npm test -- tests/integration/migration-drop-archetype.test.ts 2>&1 | grep -E "(skipped|PASS|FAIL)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/integration/migration-drop-archetype.test.ts
    - File contains the literal string `const maybe = process.env.DATABASE_URL ? describe : describe.skip`
    - File contains three `it(...)` calls referencing the three behaviors above
    - File imports `getWatchesByUser` from `@/data/watches`
    - File imports `updateCatalogTaste` from `@/data/catalog`
    - `npm test -- tests/integration/migration-drop-archetype.test.ts` either skips (no DATABASE_URL) or fails on assertion #1 (column still exists) — both are acceptable for Wave 0. The test must NOT throw on import/parse.
  </acceptance_criteria>
  <done>File exists; vitest can load it without parse errors; the three `it` cases are registered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author tests/app/explore-genres-404.test.ts (filesystem-absence smoke test)</name>
  <files>tests/app/explore-genres-404.test.ts</files>
  <read_first>
    - tests/no-evaluate-route.test.ts (filesystem-absence assertion idiom — analog per 49.1-PATTERNS.md)
    - vitest.config.ts (confirm tests/app/**/*.test.ts is in the include glob)
    - src/app/explore/genres/page.tsx (current file — confirm it still exists so the test starts red)
  </read_first>
  <action>
    Create a new vitest test using node's `fs` module to assert that `src/app/explore/genres/page.tsx` does NOT exist on disk. Test name: "Phase 49.1 — /explore/genres route is deleted".

    Implementation specifics:
    - Import `existsSync` from `node:fs` and `resolve` from `node:path`.
    - Compute `const target = resolve(__dirname, '../../src/app/explore/genres/page.tsx')` (or equivalent path resolution from the test file's location to the project root's `src/app/explore/genres/page.tsx`).
    - Assert `existsSync(target)` is `false`.
    - Also assert that the parent directory `src/app/explore/genres/` either does not exist OR contains no `page.tsx` (whichever is simpler to express).
    - Include a single `it("page.tsx is absent — route returns 404", () => { ... })` block inside one `describe("/explore/genres route deletion", ...)` block.

    This test MUST fail today (the file still exists) and MUST pass after Plan 03 deletes the route. That is the intended Wave 0 → Wave 1 red → green transition.
  </action>
  <verify>
    <automated>npm test -- tests/app/explore-genres-404.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/app/explore-genres-404.test.ts
    - File imports `existsSync` from `node:fs`
    - File contains an assertion that `existsSync(...src/app/explore/genres/page.tsx)` is `false`
    - `npm test -- tests/app/explore-genres-404.test.ts` exits non-zero today (file still exists — test is correctly red)
    - File parses without syntax errors
  </acceptance_criteria>
  <done>Test file exists; reports failure (file still present) — confirms test is wired to the right path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author tests/components/insights/CollectionFitCompareTable.test.tsx</name>
  <files>tests/components/insights/CollectionFitCompareTable.test.tsx</files>
  <read_first>
    - tests/components/insights/ReferenceIdentityCard.test.tsx (analog — same insights/ test folder, same component-test idiom)
    - src/components/insights/CollectionFitCompareTable.tsx (full file — confirm current 6-row structure and prop shape)
    - tests/setup.tsx (any global mocks needed for render under jsdom)
    - .planning/phases/49.1-remove-genre-surface/49.1-UI-SPEC.md §B (the 5-row contract)
  </read_first>
  <action>
    Create a new vitest component test for `CollectionFitCompareTable`. Use `@testing-library/react`'s `render` and either `queryAllByRole('row')` or `container.querySelectorAll('tbody tr')` to count rows. Test name: "Phase 49.1 — CollectionFitCompareTable renders 5 rows".

    Test cases (minimum two):
    1. "renders 5 rows (Formality, Sportiness, Heritage, Era, Design Motifs)" — render the component with a minimal valid pair of `candidate` and `owned` `CatalogTasteAttributes` literals (no `primaryArchetype` field — this asserts the post-49.1 shape). Assert the `<tbody>` contains exactly 5 `<tr>` elements. Assert the row-header text content (in order) includes "Formality", "Sportiness", "Heritage", "Era", "Design Motifs" AND does NOT include "Archetype".
    2. "last row (Design Motifs) uses thRowLastClass / tdLastClass — no border-b" — assert the last `<tr>`'s `<th>` does NOT contain the class `border-b` and the last `<td>` does NOT contain the class `border-b`. Use `container.querySelector('tbody tr:last-child th')` and inspect the `className` attribute.

    Use the existing fixture/mock approach from `ReferenceIdentityCard.test.tsx` — same `CatalogTasteAttributes` shape, same `render` style. Import the component as `import { CollectionFitCompareTable } from '@/components/insights/CollectionFitCompareTable'` (verify export style — default vs named — by reading the component first).

    This test MUST fail today (the table renders 6 rows because the Archetype row still exists) and MUST pass after Plan 04 deletes Row 4.
  </action>
  <verify>
    <automated>npm test -- tests/components/insights/CollectionFitCompareTable.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/components/insights/CollectionFitCompareTable.test.tsx
    - File imports `render` from `@testing-library/react`
    - File contains an assertion counting tbody rows to exactly 5
    - File contains an assertion that "Archetype" is NOT in the rendered row headers
    - `npm test -- tests/components/insights/CollectionFitCompareTable.test.tsx` exits non-zero today (the table renders 6 rows — test is correctly red)
  </acceptance_criteria>
  <done>Test file exists and parses; reports failure on row-count (6 ≠ 5) — confirms the test is wired correctly to the current 6-row table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → DB | Integration test connects to local Postgres via DATABASE_URL when present; gated to skip without it |
| test → filesystem | Smoke test reads filesystem paths; node:fs is trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49.1-01 | I (Information disclosure) | tests/integration/migration-drop-archetype.test.ts | mitigate | Test uses `_test_phase49_1_${Date.now()}` brand prefix + `afterAll` DELETE cleanup so it leaves no residue in the local DB |
| T-49.1-02 | T (Tampering) | tests/app/explore-genres-404.test.ts | accept | Filesystem path is hardcoded constant (no user input); node:fs `existsSync` is read-only |
</threat_model>

<verification>
- `npm test -- tests/integration/migration-drop-archetype.test.ts` either skips (no DATABASE_URL) or runs with assertion #1 currently red — both acceptable
- `npm test -- tests/app/explore-genres-404.test.ts` exits non-zero (file still present)
- `npm test -- tests/components/insights/CollectionFitCompareTable.test.tsx` exits non-zero (6 rows ≠ 5)
- All three files parse under vitest without syntax errors
- `npx tsc --noEmit` exits 0 (no type drift introduced by the new tests)
</verification>

<success_criteria>
- Three new test files exist at the paths in `files_modified`
- Each test is correctly red today (asserting state that does not yet exist)
- Each test will transition to green automatically when its target wave lands
- No production code modified in this plan — Wave 0 is test-only
</success_criteria>

<output>
After completion, create `.planning/phases/49.1-remove-genre-surface/49.1-01-SUMMARY.md`
</output>
