---
phase: 49.1-remove-genre-surface
plan: 05
subsystem: similarity-engine
tags: [typescript, similarity, weights, taste, vitest, refactor]

# Dependency graph
requires:
  - phase: 38-taste-engine-9th-dimension
    provides: "TASTE_SUB_WEIGHTS 4-key split + tasteSimilarityRaw01 archetype categorical-match block"
  - phase: 49-genre-vs-style-taxonomy-spike
    provides: "Ship-Now YES recommendation to remove the archetype taxonomy surface (§7 + §9)"
provides:
  - "Rebalanced TASTE_SUB_WEIGHTS (numericTrioCosine 0.50 / eraMatch 0.25 / motifsJaccard 0.25) computed algorithmically via TASTE_SUB_WEIGHTS_BASE + RESCALE = 1 / (1 - REMOVED_SUB_WEIGHT)"
  - "Public TASTE_SUB_WEIGHTS export so future tests can assert sub-budget invariants"
  - "Deleted archetype categorical-match block from tasteSimilarityRaw01 — engine no longer reads primaryArchetype"
  - "5 new invariant tests in tests/similarity.test.ts (3-key shape, value tolerance, sum-to-1.0, no-archetypeMatch, GOAL_THRESHOLDS byte-identical)"
affects:
  - "49.1-06 (types.ts primaryArchetype strip — engine already ignores the field; type removal becomes a pure cleanup)"
  - "49.1-07 (catalog page projection — same)"
  - "All downstream consumers of analyzeSimilarity() — behavior remains stable; only the 0.04 archetype-match contribution is gone, redistributed proportionally across the trio + era + motifs"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Algorithmic anti-magic-number weight rescale (TASTE_SUB_WEIGHTS_BASE + REMOVED_SUB_WEIGHT + RESCALE) — mirrors the existing EXISTING_WEIGHTS_BASE precedent at similarity.ts:27-32"
    - "Plan-level invariant tests as tripwires for future weight drift (sum-to-1.0 + GOAL_THRESHOLDS byte-identical)"

key-files:
  created: []
  modified:
    - "src/lib/similarity.ts"
    - "tests/similarity.test.ts"

key-decisions:
  - "D-SIM-01 applied algorithmically — values 0.50/0.25/0.25 are computed, never hardcoded (anti-magic-number guard at similarity.ts:10-12)"
  - "D-SIM-02 implementation mirrors the existing EXISTING_WEIGHTS_BASE pattern at lines 27-32 — the codebase precedent the planner pre-identified"
  - "D-SIM-03 deleted the 4-line archetype categorical-match block; tasteSimilarityRaw01 now reads only formality/sportiness/heritageScore (trio) + eraSignal + designMotifs"
  - "D-SIM-04 preserved — TASTE_WEIGHT (0.20), EXISTING_WEIGHTS_BASE, WEIGHTS, THRESHOLDS, and GOAL_THRESHOLDS literal values UNCHANGED"
  - "THRESHOLDS invariant: documented via TODO comment in tests/similarity.test.ts because THRESHOLDS is not currently exported; GOAL_THRESHOLDS (which IS exported and IS the load-bearing label-producing structure) is asserted byte-identical with toEqual(...)"

patterns-established:
  - "Algorithmic weight rescale — when removing a sub-weight from an N-key budget, define _BASE without the removed key, set REMOVED_SUB_WEIGHT = removed share, set RESCALE = 1 / (1 - REMOVED_SUB_WEIGHT), and map via Object.fromEntries(× RESCALE). The literal rescaled values (0.50/0.25/0.25 here) never appear in source."
  - "Sub-budget closure invariant test — any future weight-rebalance change must keep the surviving sub-weights summing to 1.0; the toBeCloseTo(1.0, 9) tripwire catches drift before it reaches production."

requirements-completed: [TAX-02]

# Metrics
duration: 6 min
completed: 2026-05-20
---

# Phase 49.1 Plan 05: Similarity TASTE_SUB_WEIGHTS Rebalance Summary

**Engine-side D-SIM redistribution — archetype 0.20 sub-weight removed; surviving trio + era + motifs rescaled algorithmically via 1.25x to keep sub-budget summed to 1.0; TASTE_WEIGHT 0.20 envelope and verdict thresholds unchanged.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-05-20T06:18:02Z
- **Completed:** 2026-05-20T06:24:00Z
- **Tasks:** 1 (TDD: RED + GREEN, no REFACTOR needed)
- **Files modified:** 2

## Accomplishments
- TASTE_SUB_WEIGHTS now computed via the same Object.fromEntries(× RESCALE) idiom used for WEIGHTS at lines 27-32 — anti-magic-number guard at similarity.ts:10-12 is satisfied
- Archetype categorical-match block (the 4 lines at the old similarity.ts:124-127 that read t1.primaryArchetype + t2.primaryArchetype) deleted from tasteSimilarityRaw01
- 5 new invariant tests added (4 PR-protective tripwires + 1 GOAL_THRESHOLDS byte-identical snapshot)
- 17 of 17 similarity tests pass; 38 of 38 dependent tests pass across taste-null, taste-present, composer-engine-alignment, tasteOverlap, and verdict shims test files

## Task Commits

Each TDD phase committed atomically:

1. **Task 1 RED — failing invariant tests** — `af6b00d` (test)
2. **Task 1 GREEN — algorithmic rescale + delete archetype block + export** — `acd51e3` (feat)

REFACTOR step skipped — implementation already clean (no duplication, comments map 1:1 to behavior, file structure mirrors the existing EXISTING_WEIGHTS_BASE precedent).

**Plan metadata:** _pending_ (final commit at end of execution)

## Files Created/Modified
- `src/lib/similarity.ts` — replaced TASTE_SUB_WEIGHTS literal with TASTE_SUB_WEIGHTS_BASE + REMOVED_SUB_WEIGHT + RESCALE + Object.fromEntries(× RESCALE); deleted archetype categorical-match block in tasteSimilarityRaw01; added TASTE_SUB_WEIGHTS to export surface
- `tests/similarity.test.ts` — imported TASTE_SUB_WEIGHTS + GOAL_THRESHOLDS; added "Phase 49.1 — TASTE_SUB_WEIGHTS rebalance (D-SIM-01..04)" describe block with 5 invariant assertions

## Decisions Made
- Skipped REFACTOR commit because the GREEN diff is already idiomatic — adding a refactor commit would have been churn for the sake of TDD ceremony
- Updated three downstream comment lines in tasteSimilarityRaw01 to remove all numeric weight literals (the old comments read `effective 0.04`, `effective 0.08`, etc.). Replaced with `computed sub-weight via RESCALE × TASTE_SUB_WEIGHTS_BASE.<key>`. This was needed because the contract's grep gate `numericTrioCosine.*0\.50` is a strict file-wide gate (not source-only); leaving `0.50` in a doc comment would have tripped it
- Asserted GOAL_THRESHOLDS via toEqual(literal) rather than per-field expect calls — simpler, catches structural drift (extra/missing keys), and reads at-a-glance as a snapshot
- Added TODO note in the test rather than exporting THRESHOLDS in this plan — the plan's Task 1 action explicitly allows this and the contract item "snapshot test" is satisfied by the load-bearing GOAL_THRESHOLDS assertion (THRESHOLDS is a fallback constant currently unused outside the file)

## Deviations from Plan

None - plan executed exactly as written.

The only minor judgment call was updating the doc comments in tasteSimilarityRaw01 to drop the `0.04` / `0.08` / `0.50` literal references. This was forced by the strict file-wide grep gate `grep -nE "0\.50.*numericTrioCosine|numericTrioCosine.*0\.50"`. The plan's `<acceptance_criteria>` block specified this gate; the doc comment update is a direct compliance action, not a deviation.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Verification Results

All plan-level acceptance gates verified:

- `grep -n "TASTE_SUB_WEIGHTS_BASE" src/lib/similarity.ts` → 6 matches (definition + Object.entries reference + 3 doc-comment references + type cast — exceeds the ≥2 minimum)
- `grep -nE "primaryArchetype|archetypeMatch" src/lib/similarity.ts` → no matches
- `grep -nE "0\.50.*numericTrioCosine|numericTrioCosine.*0\.50" src/lib/similarity.ts` → no matches (no magic-number hardcoding)
- `grep -n "RESCALE = 1 / (1 - REMOVED_SUB_WEIGHT)" src/lib/similarity.ts` → 1 match (line 52)
- `grep -nE "export \{[^}]*TASTE_SUB_WEIGHTS" src/lib/similarity.ts` → 1 match (line 90)
- `npm test -- tests/similarity.test.ts` → 17/17 pass

All 5 new it blocks pass:
1. 3-key shape (numericTrioCosine, eraMatch, motifsJaccard)
2. Values 0.50/0.25/0.25 within 1e-9 tolerance
3. Sum-to-1.0 closure invariant
4. archetypeMatch key absent
5. GOAL_THRESHOLDS byte-identical to pre-49.1

## Threat Flags

None — no new trust boundaries, network surface, or schema changes. The two threat-register entries (T-49.1-10 Tampering of TASTE_SUB_WEIGHTS; T-49.1-11 Tampering of THRESHOLDS/GOAL_THRESHOLDS) are both mitigated:
- T-49.1-10: algorithmic rescale via build-time consts (no user input); sum-to-1.0 tripwire test guards future drift
- T-49.1-11: GOAL_THRESHOLDS asserted byte-identical via toEqual; any future drift fails the test

## Next Phase Readiness
- Engine-side D-SIM work complete and isolated. The other Wave-1 plans (49.1-02 verdict templates, 49.1-03 explore rail rewire, 49.1-04 UI surface scope) are unaffected by this change because TASTE_SUB_WEIGHTS is consumed only inside tasteSimilarityRaw01.
- Wave 2 / 3 plans that strip `primaryArchetype` from CatalogTasteAttributes (types.ts) and the catalog page projection are now safe — the similarity engine has already stopped reading the field. Removing the field from the type system will be a pure cleanup that cannot regress behavior.
- The TODO note in tests/similarity.test.ts (re: THRESHOLDS export-assertion) is a follow-up tracked in the test file itself; no separate ticket needed.

## Self-Check: PASSED

- FOUND: src/lib/similarity.ts
- FOUND: tests/similarity.test.ts
- FOUND: .planning/phases/49.1-remove-genre-surface/49.1-05-SUMMARY.md
- FOUND commit: af6b00d (test RED)
- FOUND commit: acd51e3 (feat GREEN)

---
*Phase: 49.1-remove-genre-surface*
*Completed: 2026-05-20*
