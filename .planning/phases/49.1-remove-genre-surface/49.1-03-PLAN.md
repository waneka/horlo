---
phase: 49.1
plan: 03
type: execute
wave: 1
depends_on: ["49.1-01"]
files_modified:
  - src/components/search/GenreChips.tsx
  - src/components/search/ArchetypeChips.tsx
  - src/app/explore/genres/page.tsx
  - src/components/search/FilterDrawer.tsx
  - src/components/explore/BrowseModule.tsx
  - src/components/explore/CollectorArchetypes.tsx
  - src/data/browse.ts
  - src/data/__tests__/browse.test.ts
  - src/components/explore/__tests__/CollectorArchetypes.test.tsx
  - tests/components/search/DrawerChips.test.tsx
  - tests/components/search/FilterDrawer.test.tsx
autonomous: true
requirements: [TAX-02]
must_haves:
  truths:
    - "GenreChips.tsx, ArchetypeChips.tsx, and src/app/explore/genres/page.tsx no longer exist on disk"
    - "FilterDrawer.tsx renders 5 chip groups (Movement, CaseSize, Style, Brand, Era); no Genre or Archetype group"
    - "BrowseModule.tsx no longer renders a Genres tile or any /explore/genres link"
    - "CollectorArchetypes.tsx Link href targets /search?tab=watches&style=X (not ?archetype=X)"
    - "getBrowseArchetypeCounts() in src/data/browse.ts derives counts from unnest(style_tags) and not from primary_archetype"
    - "getBrowseGenreCounts() no longer exists in src/data/browse.ts"
  artifacts:
    - path: src/data/browse.ts
      provides: "Rewired getBrowseArchetypeCounts() reading style_tags; deleted getBrowseGenreCounts()"
      contains: "unnest(style_tags)"
    - path: src/components/explore/CollectorArchetypes.tsx
      provides: "Deep-link repoint from ?archetype= to ?style="
      contains: "?tab=watches&style="
    - path: src/components/search/FilterDrawer.tsx
      provides: "5 surviving chip groups; Genre + Archetype imports/props/renders gone"
  key_links:
    - from: src/components/explore/CollectorArchetypes.tsx
      to: src/data/browse.ts
      via: "getBrowseArchetypeCounts() async call in RSC"
      pattern: "getBrowseArchetypeCounts"
    - from: src/data/browse.ts
      to: "Postgres watches_catalog.style_tags"
      via: "sql`unnest(style_tags)` lateral join"
      pattern: "unnest\\(style_tags\\)"
---

<objective>
Execute the D-EXPLORE decision group plus the pure deletions: delete `GenreChips.tsx`, `ArchetypeChips.tsx`, and `src/app/explore/genres/page.tsx`; remove the Genres tile from `BrowseModule.tsx`; rewire `getBrowseArchetypeCounts()` in `src/data/browse.ts` to derive from `unnest(style_tags)`; delete `getBrowseGenreCounts()`; repoint `CollectorArchetypes.tsx` Link href from `?archetype=` to `?style=`; remove the Genre + Archetype chip groups from `FilterDrawer.tsx`; update the affected drawer / browse tests.

Purpose: D-EXPLORE-01 + D-EXPLORE-03 (rail rewire + deep-link repoint) plus the pure deletions called out in CONTEXT scope items 2, 4, 6. The /explore CollectorArchetypes rail is preserved per D-EXPLORE-02; `ARCHETYPE_CONFIG` and `PrimaryArchetype` type are retained (handled by NOT deleting `src/lib/archetype-config.ts`).

Output: 3 deleted files; 8 modified files (DAL + UI + tests). The `/explore/genres` route returns 404. The rail's deep-links route to multi-select StyleChips. The drawer naturally shortens by 2 chip groups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49.1-remove-genre-surface/49.1-CONTEXT.md
@.planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md
@.planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md
@.planning/phases/49.1-remove-genre-surface/49.1-UI-SPEC.md

# Files being modified
@src/data/browse.ts
@src/data/catalog.ts
@src/components/search/FilterDrawer.tsx
@src/components/search/GenreChips.tsx
@src/components/search/ArchetypeChips.tsx
@src/components/explore/BrowseModule.tsx
@src/components/explore/CollectorArchetypes.tsx
@src/app/explore/genres/page.tsx

# Existing tests being updated
@src/data/__tests__/browse.test.ts
@src/components/explore/__tests__/CollectorArchetypes.test.tsx
@tests/components/search/DrawerChips.test.tsx
@tests/components/search/FilterDrawer.test.tsx

# Wave 0 test that this plan transitions red -> green (depends_on: 49.1-01)
@tests/app/explore-genres-404.test.ts

# Analog files (for pattern transfer)
@src/lib/taste/vocab.ts

<interfaces>
<!-- From src/data/catalog.ts:533-544 — the EXACT pattern for the rewired SQL. -->

```typescript
// src/data/catalog.ts:533-544 — getTopStyleTags (the load-bearing analog)
export async function getTopStyleTags(limit = 8): Promise<string[]> {
  'use cache'
  cacheLife('hours')
  const rows = await db.execute(
    sql`SELECT tag, COUNT(*) AS freq
        FROM watches_catalog, unnest(style_tags) AS tag
        GROUP BY tag
        ORDER BY freq DESC
        LIMIT ${limit}`,
  )
  return (rows as unknown as Array<{ tag: string }>).map((r) => r.tag)
}
```

<!-- From src/lib/taste/vocab.ts:14-22 — the 10-value list to use as the WHERE filter array. -->

`PRIMARY_ARCHETYPES` is a `const` tuple of the 10 archetype keys (dress, dive, field, pilot, chrono, gmt, racing, sport, tool, hybrid).

<!-- From src/data/browse.ts (current shape, pre-rewire) — what the rewire must preserve. -->

```typescript
// src/data/browse.ts:44-56 — current getBrowseArchetypeCounts() — body changes; signature/cache scope preserved
export async function getBrowseArchetypeCounts(): Promise<Array<{ archetype: string; count: number }>> {
  'use cache'
  cacheTag('explore', 'explore:browse')
  cacheLife('hours')
  // ... SQL body REWIRED in this plan
}
```

The return type `Array<{ archetype: string; count: number }>` is the public contract — DO NOT change it. The cache tag scope (`'explore', 'explore:browse'`) and TTL (`'hours'`) are preserved.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewire getBrowseArchetypeCounts() to unnest(style_tags); delete getBrowseGenreCounts()</name>
  <files>src/data/browse.ts, src/data/__tests__/browse.test.ts</files>
  <read_first>
    - src/data/browse.ts (full file — locate getBrowseArchetypeCounts at :35-56, getBrowseGenreCounts at :82-105, and the surviving getBrowseEraCounts as a sibling-pattern reference)
    - src/data/catalog.ts:533-544 (the unnest(style_tags) analog — getTopStyleTags)
    - src/data/catalog.ts:610-625 (the sql.join parameter-binding analog from updateCatalogTaste's motifsSql block)
    - src/lib/taste/vocab.ts:14-22 (PRIMARY_ARCHETYPES const — confirm export name and value list)
    - src/data/__tests__/browse.test.ts (full file — locate the getBrowseGenreCounts describe block + the getBrowseArchetypeCounts describe block + the mock setup)
    - .planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md §"src/data/browse.ts" and §"Test analog: src/data/__tests__/browse.test.ts"
    - .planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md §"Code Examples — Rewired getBrowseArchetypeCounts()"
  </read_first>
  <action>
    In src/data/browse.ts:

    Replace the body of `getBrowseArchetypeCounts()` (at approximately lines 35-56) with the rewired SQL that reads `unnest(style_tags)` filtered to the 10-value `PRIMARY_ARCHETYPES` array. Preserve the function signature (`Promise<Array<{ archetype: string; count: number }>>`), the `'use cache'` directive, the `cacheTag('explore', 'explore:browse')` call, and the `cacheLife('hours')` call — only the SQL body changes. Use `sql.join(PRIMARY_ARCHETYPES.map((v) => sql\`${v}\`), sql\`, \`)` inside `ARRAY[...]` so values are parameterized. Add a top-of-file import: `import { PRIMARY_ARCHETYPES } from '@/lib/taste/vocab'` if not already present.

    The SQL body (per RESEARCH §Code Examples — Rewired getBrowseArchetypeCounts):

    `SELECT tag AS archetype, COUNT(*)::int AS count FROM watches_catalog, unnest(style_tags) AS tag WHERE tag = ANY(ARRAY[${sql.join(...)}]) GROUP BY tag ORDER BY count DESC`

    DELETE the entire `getBrowseGenreCounts` function (approximately lines 82-105). DELETE any associated import or doc comment specific to it.

    In src/data/__tests__/browse.test.ts:

    1. Delete the `getBrowseGenreCounts` import from the top-of-file import list.
    2. Delete the entire `describe('getBrowseGenreCounts', ...)` block (analogous to the existing pattern, lines for genre describe).
    3. Update the `describe('getBrowseArchetypeCounts', ...)` block: the test mocks `db.execute`, so the actual SQL string is not asserted by default; the mock fixture stays `[{ archetype: 'dive', count: 18 }, ...]`. Verify the existing test still passes after the source rewire (function signature is unchanged). Add ONE new `it("derives counts from style_tags (rewired SQL)", ...)` that inspects `(db.execute as Mock).mock.calls[0][0]` and asserts the SQL string contains the substring `unnest(style_tags)` AND does NOT contain `primary_archetype`. Use a simple `expect(callArg.toString()).toMatch(/unnest\(style_tags\)/)` style assertion.
  </action>
  <verify>
    <automated>npm test -- src/data/__tests__/browse.test.ts 2>&1 | tail -10 ; grep -c "getBrowseGenreCounts" src/data/browse.ts || true ; grep -c "unnest(style_tags)" src/data/browse.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getBrowseGenreCounts" src/data/browse.ts` returns no matches
    - `grep -n "unnest(style_tags)" src/data/browse.ts` returns at least 1 match (inside the rewired getBrowseArchetypeCounts)
    - `grep -n "primary_archetype" src/data/browse.ts` returns no matches
    - `grep -n "cacheTag('explore', 'explore:browse')" src/data/browse.ts` still returns 1 match in the getBrowseArchetypeCounts body (scope preserved)
    - `grep -n "import.*PRIMARY_ARCHETYPES" src/data/browse.ts` returns 1 match
    - `npm test -- src/data/__tests__/browse.test.ts` exits 0
    - The new "derives counts from style_tags" assertion passes
  </acceptance_criteria>
  <done>browse.ts rewired; getBrowseGenreCounts gone; tests updated and green; cache scope unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete GenreChips, ArchetypeChips, /explore/genres route; remove BrowseModule tile; repoint CollectorArchetypes deep-link; update CollectorArchetypes test</name>
  <files>src/components/search/GenreChips.tsx, src/components/search/ArchetypeChips.tsx, src/app/explore/genres/page.tsx, src/components/explore/BrowseModule.tsx, src/components/explore/CollectorArchetypes.tsx, src/components/explore/__tests__/CollectorArchetypes.test.tsx</files>
  <read_first>
    - src/components/search/GenreChips.tsx (full file — confirm it is a standalone client component before deletion)
    - src/components/search/ArchetypeChips.tsx (full file — confirm standalone before deletion)
    - src/app/explore/genres/page.tsx (full file — confirm route is standalone before deletion)
    - src/components/explore/BrowseModule.tsx (full file — locate the "Genres" tile at approximately :43 plus surrounding tile array context)
    - src/components/explore/CollectorArchetypes.tsx (full file — locate the Link href containing `?archetype=`)
    - src/components/explore/__tests__/CollectorArchetypes.test.tsx (full file — locate any href assertion mentioning `?archetype=`)
    - tests/app/explore-genres-404.test.ts (the Wave 0 test that should transition red → green after this task)
    - .planning/phases/49.1-remove-genre-surface/49.1-UI-SPEC.md §E (deep-link reroute) and §F (pure-deletion surfaces)
  </read_first>
  <action>
    Delete three files (use the project's standard rm — DO NOT replace with stubs):
    - `src/components/search/GenreChips.tsx`
    - `src/components/search/ArchetypeChips.tsx`
    - `src/app/explore/genres/page.tsx`

    If the `src/app/explore/genres/` directory is empty after deletion, remove the directory as well so Next 16's App Router does not detect an orphan route segment.

    In src/components/explore/BrowseModule.tsx: locate the tile array (around line 43 per RESEARCH) that includes an entry pointing to `/explore/genres` with title "Genres". Delete the entire tile object literal AND remove its trailing comma so the surrounding array literal stays syntactically valid. Verify the surrounding layout (grid or flex-wrap) reflows naturally per UI-SPEC §F BrowseModule layout note.

    In src/components/explore/CollectorArchetypes.tsx: change the `Link` href interpolation from `/search?tab=watches&archetype=${value}` to `/search?tab=watches&style=${value}` (per D-EXPLORE-03). If the Link href is constructed via template literal across multiple lines or with a `URLSearchParams`, change the param key from `archetype` to `style` in the same place. Do NOT change any other prop on the component. The visual contract per UI-SPEC §E is "preserved; href only".

    In src/components/explore/__tests__/CollectorArchetypes.test.tsx: update the href assertion from `?archetype=` to `?style=` to match the new behavior. Also update the mock for `getBrowseArchetypeCounts` if the test pre-loads a fixture that named `archetype` — the post-rewire return shape is identical (`Array<{ archetype: string; count: number }>`), so the fixture itself does NOT need to change keys; only the rendered href assertion changes.

    Verify the Wave 0 test transitions: `npm test -- tests/app/explore-genres-404.test.ts` should now exit 0 (file is gone).
  </action>
  <verify>
    <automated>test ! -e src/components/search/GenreChips.tsx && test ! -e src/components/search/ArchetypeChips.tsx && test ! -e src/app/explore/genres/page.tsx && echo "DELETED" ; npm test -- tests/app/explore-genres-404.test.ts src/components/explore/__tests__/CollectorArchetypes.test.tsx 2>&1 | tail -15 ; grep -nE "explore/genres|archetype=" src/components/explore/BrowseModule.tsx src/components/explore/CollectorArchetypes.tsx || echo "OK no residual refs"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e src/components/search/GenreChips.tsx` succeeds
    - `test ! -e src/components/search/ArchetypeChips.tsx` succeeds
    - `test ! -e src/app/explore/genres/page.tsx` succeeds
    - `grep -n "/explore/genres" src/components/explore/BrowseModule.tsx` returns no matches
    - `grep -n "?tab=watches&archetype=" src/components/explore/CollectorArchetypes.tsx` returns no matches
    - `grep -n "?tab=watches&style=" src/components/explore/CollectorArchetypes.tsx` returns at least 1 match
    - `npm test -- tests/app/explore-genres-404.test.ts` exits 0 (Wave 0 test now green)
    - `npm test -- src/components/explore/__tests__/CollectorArchetypes.test.tsx` exits 0
    - `grep -r "/explore/genres" src/` returns no matches (SC#6 from ROADMAP)
  </acceptance_criteria>
  <done>Three files deleted; BrowseModule tile gone; rail deep-link repointed; CollectorArchetypes test green; Wave 0 404 test now green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove Genre + Archetype chip groups from FilterDrawer; update DrawerChips and FilterDrawer tests</name>
  <files>src/components/search/FilterDrawer.tsx, tests/components/search/DrawerChips.test.tsx, tests/components/search/FilterDrawer.test.tsx</files>
  <read_first>
    - src/components/search/FilterDrawer.tsx (full file — locate GenreChips + ArchetypeChips imports at :10-11, props at the component signature, renders at :85-86, and the `handleClearAll` function — look for `onGenreChange(null)` and `onArchetypeChange(null)` calls)
    - tests/components/search/DrawerChips.test.tsx (full file — locate the GenreChips and ArchetypeChips describe blocks that go away with the components)
    - tests/components/search/FilterDrawer.test.tsx (full file — locate the `vi.mock` lines at approximately :133-138 and the archetype/genre props at approximately :174-175)
    - .planning/phases/49.1-remove-genre-surface/49.1-UI-SPEC.md §D (the 5-group post-modification drawer contract)
  </read_first>
  <action>
    In src/components/search/FilterDrawer.tsx:

    1. Delete the imports for `GenreChips` and `ArchetypeChips` at lines 10-11 (or wherever they appear in the import block).
    2. Update the component's props interface to remove every Genre/Archetype-related prop: `genreFilter`, `archetypeFilter`, `onGenreChange`, `onArchetypeChange` (or whatever names are in the current source — read first). If those props were passed in by a parent that this plan does not modify, that parent (Plan 04 — SearchPageClient) will drop them in its plan; in the meantime, removing them here may produce a TypeScript error at the parent call site, which is expected.
    3. In the JSX, delete the `<GenreChips ... />` and `<ArchetypeChips ... />` rendering blocks at lines 85-86 (the two consecutive chip group renders).
    4. In `handleClearAll` (or whatever the clear-all callback is named), delete the `onGenreChange(null)` and `onArchetypeChange(null)` calls. Keep all surviving clear-all calls (movement, size, style, brand, era) untouched.
    5. Surviving chip group order MUST be: Movement → CaseSize → Style → Brand → Era (5 groups, per UI-SPEC §D).

    In tests/components/search/DrawerChips.test.tsx: delete the entire `describe('GenreChips', ...)` block and the entire `describe('ArchetypeChips', ...)` block. These components no longer exist; the describes go with them. Other describes (MovementChips, CaseSizeChips, StyleChips, BrandChips, EraChips) remain UNTOUCHED.

    In tests/components/search/FilterDrawer.test.tsx:
    1. Delete the `vi.mock('@/components/search/GenreChips', ...)` and `vi.mock('@/components/search/ArchetypeChips', ...)` calls (referenced at approximately :133-138).
    2. Delete the `genreFilter` / `archetypeFilter` / `onGenreChange` / `onArchetypeChange` props from any test render call (approximately :174-175 plus any other place they appear).
    3. If a test specifically asserted "GenreChips is rendered" or "ArchetypeChips is rendered", delete that `it(...)` block entirely.
    4. Update any "render count" assertion (if present) from 7 chip groups to 5.
  </action>
  <verify>
    <automated>grep -nE "GenreChips|ArchetypeChips" src/components/search/FilterDrawer.tsx tests/components/search/DrawerChips.test.tsx tests/components/search/FilterDrawer.test.tsx || echo "OK no residual" ; npm test -- tests/components/search/FilterDrawer.test.tsx tests/components/search/DrawerChips.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "GenreChips|ArchetypeChips" src/components/search/FilterDrawer.tsx` returns no matches
    - `grep -nE "import.*(GenreChips|ArchetypeChips)" src/components/search/FilterDrawer.tsx` returns no matches
    - `grep -nE "onGenreChange|onArchetypeChange" src/components/search/FilterDrawer.tsx` returns no matches
    - `grep -nE "describe\('GenreChips|describe\('ArchetypeChips" tests/components/search/DrawerChips.test.tsx` returns no matches
    - `grep -nE "vi.mock.*GenreChips|vi.mock.*ArchetypeChips" tests/components/search/FilterDrawer.test.tsx` returns no matches
    - `npm test -- tests/components/search/FilterDrawer.test.tsx` exits 0
    - `npm test -- tests/components/search/DrawerChips.test.tsx` exits 0
  </acceptance_criteria>
  <done>FilterDrawer renders 5 chip groups; tests no longer reference GenreChips/ArchetypeChips; both drawer test files green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client URL → /explore Server Component | CollectorArchetypes Link href contains a closed-vocab `value` from ARCHETYPE_CONFIG — no user input |
| Server Component → DB | getBrowseArchetypeCounts SQL uses parameterized sql.join() for the 10-value array; no user input flows in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49.1-05 | T (Tampering) | src/data/browse.ts | mitigate | Rewired SQL uses sql.join() for parameter binding; the 10-value PRIMARY_ARCHETYPES list is a build-time const tuple, not user-supplied |
| T-49.1-06 | D (DoS) | /explore/genres route | accept | After deletion, an old client-side prefetch will 404. Per memory feedback_proxy_router_cache_poisoning the route is NOT proxy-gated (verified in RESEARCH §V4), so a 404 will not poison the Router Cache (which requires a 307 redirect). Risk: none. |
| T-49.1-07 | T (Tampering) | getBrowseArchetypeCounts cacheTag scope | accept | Cache scope (`'explore', 'explore:browse'`) preserved; no per-user data flows in; existing revalidateTag('explore', 'max') in catalog mutation paths continues to invalidate |
</threat_model>

<verification>
- `npm test -- src/data/__tests__/browse.test.ts src/components/explore/__tests__/CollectorArchetypes.test.tsx tests/components/search/FilterDrawer.test.tsx tests/components/search/DrawerChips.test.tsx tests/app/explore-genres-404.test.ts` exits 0
- `grep -r "/explore/genres" src/` returns no matches
- `grep -r "GenreChips\|ArchetypeChips" src/components/` returns no matches (the imports and renders are gone; the component files are deleted)
- `grep -n "unnest(style_tags)" src/data/browse.ts` returns at least 1 match
- The Wave 0 explore-genres-404 smoke test transitions to green
</verification>

<success_criteria>
- ROADMAP SC#2 partial: GenreChips.tsx, ArchetypeChips.tsx, /explore/genres/page.tsx deleted; FilterDrawer no longer imports/renders either chip group; BrowseModule no longer links to /explore/genres
- ROADMAP SC#6: `grep -r "/explore/genres" src/` returns zero matches
- D-EXPLORE-01 applied: getBrowseArchetypeCounts derives from style_tags; cache scope preserved
- D-EXPLORE-03 applied: rail Link href targets ?style=, not ?archetype=
- All four affected test files green after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/49.1-remove-genre-surface/49.1-03-SUMMARY.md`
</output>
