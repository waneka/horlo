---
phase: 49.1
plan: 08
type: execute
wave: 4
depends_on: [49.1-07]
files_modified:
  - supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql
autonomous: false
requirements: [TAX-02]
must_haves:
  truths:
    - "A new supabase migration file at supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql contains ALTER TABLE public.watches_catalog DROP COLUMN IF EXISTS primary_archetype"
    - "The prod Postgres DB (via supabase db push --linked) no longer has the primary_archetype column on watches_catalog"
    - "All reader code (Plans 02-07) has been merged AND deployed to prod BEFORE this prod push runs"
  artifacts:
    - path: supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql
      provides: "Prod-side mirror of the drizzle local migration"
      contains: "public.watches_catalog"
  key_links:
    - from: supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql
      to: drizzle/0012_phase49_1_drop_primary_archetype.sql
      via: "Identical SQL body, different file naming convention; supabase mirrors drizzle"
      pattern: "DROP COLUMN IF EXISTS"
---

<objective>
Apply the prod-side database schema change: author the supabase migration `20260519010000_phase49_1_drop_primary_archetype.sql` and run `supabase db push --linked` to drop `primary_archetype` from the production `watches_catalog` table. This is the FINAL plan in Phase 49.1 and must run ONLY AFTER all reader code (Plans 02-07) is merged AND deployed to prod, per D-MIG-02 sequencing.

Purpose: D-MIG-01 + D-MIG-02 final step. Per the project memory `project_drizzle_supabase_db_mismatch`, drizzle-kit push is local-only; prod uses `supabase db push --linked`. The four prod-push gotchas (filename, ordering, extension schema, enum-bound dependents) all apply.

This plan is `autonomous: false` because the prod push has irreversible consequences and the developer must confirm the deploy state before running it.

Output: 1 new SQL migration file + a successful `supabase db push --linked` run + prod DB column absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49.1-remove-genre-surface/49.1-CONTEXT.md
@.planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md
@.planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md

# Existing analog for header-comment style
@supabase/migrations/20260519000000_phase47_published_at.sql

# Plan 07 SUMMARY - confirm the local migration shipped first
@.planning/phases/49.1-remove-genre-surface/49.1-07-SUMMARY.md

# Project memories that govern this plan
# project_drizzle_supabase_db_mismatch — 4 prod-push gotchas
# project_db_wipeable_2026_05_09 — watches_catalog is NOT wipeable (carved out 2026-05-19); this plan is a schema drop not a data migration

<interfaces>
<!-- Analog file from supabase/migrations/20260519000000_phase47_published_at.sql -->

Analog header pattern: short Phase N D-XX preamble line + the ALTER TABLE statement qualified with `public.<table>`.

<!-- Filename ordering -->

Latest existing supabase migration is `20260519000000_phase47_published_at.sql`. New file MUST be timestamped strictly greater than 20260519000000. CONTEXT and RESEARCH propose `20260519010000_phase49_1_drop_primary_archetype.sql` (using a 1-hour offset on the same date).

<!-- The 4 prod-push gotchas (project_drizzle_supabase_db_mismatch) -->

1. Filename ordering: 20260519010000 > 20260519000000 ✓
2. Migration ordering: this is a forward-only schema DROP; no dependency on later migrations ✓
3. Extension schema: no extensions involved ✓
4. Enum-bound dependents: primary_archetype is a plain text column, NOT a pgEnum. The watches_catalog_primary_archetype_check CHECK constraint is dropped implicitly with the column. ✓
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Pre-flight checkpoint: confirm all reader code is deployed to prod before running this plan</name>
  <files>(none — checkpoint only)</files>
  <what-built>Plans 02-07 have landed locally and presumably been deployed to prod. Before authoring the supabase migration and running supabase db push --linked, the developer must confirm the production deployment state.</what-built>
  <decision>Has all reader/writer code from Phase 49.1 Plans 02-07 been merged to main AND deployed to production?</decision>
  <context>
    Per D-MIG-02 sequencing, the prod schema DROP runs LAST — after every reader/writer code path has been removed from prod. Running this plan against a prod that still has the old code will crash inserts and reads of watches_catalog (Pitfall 1).

    The reader/writer touch points removed by Plans 02-06:
    - Plan 02: verdict engine (templates.ts, composer.ts, viewerTasteProfile.ts, fit-delta.ts)
    - Plan 03: /explore (browse.ts getBrowseArchetypeCounts rewire, getBrowseGenreCounts deletion); FilterDrawer; GenreChips/ArchetypeChips/explore-genres deletions
    - Plan 04: ReferenceIdentityCard, CollectionFitCompareTable, SearchPageClient, search.ts Zod schema
    - Plan 05: similarity.ts archetype categorical block
    - Plan 06: types.ts CatalogTasteAttributes, catalog page projection, watches.ts LEFT JOIN, catalog.ts (filters/tiebreaker/mapper/UPSERT), enricher chain
    - Plan 07: schema.ts column declaration + local drizzle migration

    If any reader/writer code path is still active on prod at the moment the column drops, that path will throw at runtime.
  </context>
  <options>
    <option id="confirmed-deployed">
      <name>Confirmed: prod is at the post-Plan-07 code state</name>
      <pros>Safe to proceed with the prod push</pros>
      <cons>None — this is the expected path</cons>
    </option>
    <option id="not-yet-deployed">
      <name>Not yet deployed: hold this plan</name>
      <pros>Avoids the Pitfall 1 prod-crash failure mode</pros>
      <cons>Phase 49.1 stays open until the deploy lands; developer must remember to come back</cons>
    </option>
    <option id="prod-deploy-blocker">
      <name>Deploy is blocked for an unrelated reason</name>
      <pros>Surfaces the blocker before it cascades</pros>
      <cons>Phase 49.1 is paused</cons>
    </option>
  </options>
  <action>Pause and wait for the developer's selection of `confirmed-deployed`, `not-yet-deployed`, or `prod-deploy-blocker`. If `not-yet-deployed` or `prod-deploy-blocker`, abort the plan (and the phase remains open until the deploy lands). If `confirmed-deployed`, proceed to Task 1.</action>
  <verify>Manual: developer responds with a valid option id from the list above.</verify>
  <done>Developer has selected `confirmed-deployed`; prod is verified to be running post-Plan-07 code.</done>
  <resume-signal>Select: confirmed-deployed, not-yet-deployed, or prod-deploy-blocker</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Author supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql and run supabase db push --linked</name>
  <files>supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql</files>
  <read_first>
    - supabase/migrations/20260519000000_phase47_published_at.sql (analog header-comment style + public.table qualification)
    - drizzle/0012_phase49_1_drop_primary_archetype.sql (the local drizzle mirror authored in Plan 07 — verify body is identical)
    - .planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md §"Code Examples — Supabase prod migration (mirror, with the same SQL body)"
    - .planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md §"Migration: supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql"
    - Project memory project_drizzle_supabase_db_mismatch (in MEMORY.md — the 4 prod-push gotchas)
  </read_first>
  <action>
    Create a new file at `supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql`. The file must contain:

    1. A header-comment block (using `--` line comments) that includes:
       - A "Phase 49.1 — Mirror of drizzle/0012_phase49_1_drop_primary_archetype.sql" preamble
       - The phrase "for prod push via `supabase db push --linked`"
       - A reference to project memory `project_drizzle_supabase_db_mismatch` explaining that drizzle-kit push is LOCAL ONLY; prod uses supabase
       - A "4 prod-push gotchas" checklist documenting:
         - #1 filename ordering: 20260519010000 is strictly greater than 20260519000000 (the latest existing supabase migration, phase47_published_at)
         - #2 migration ordering: forward-only schema DROP; no dependency on later migrations
         - #3 extension schema: no extensions involved
         - #4 enum-bound dependents: primary_archetype is a plain text column (NOT a pgEnum); the `watches_catalog_primary_archetype_check` CHECK constraint is dropped implicitly with the column
       - A "Sequencing (D-MIG-02): runs AFTER all reader code is deployed to prod" line

    2. A blank line then the single SQL statement:

       `ALTER TABLE public.watches_catalog DROP COLUMN IF EXISTS primary_archetype;`

    The `public.watches_catalog` qualification matches the analog at 20260519000000. The `IF EXISTS` guard is idempotent against accidental re-runs.

    Then run the prod push:

    `supabase db push --linked`

    Expected supabase CLI behavior:
    1. Reads supabase/migrations/ for unapplied migrations
    2. Detects 20260519010000_phase49_1_drop_primary_archetype.sql as new
    3. Prompts for confirmation (destructive operation)
    4. Applies the ALTER TABLE statement against the linked prod DB
    5. Records the migration in the supabase_migrations.schema_migrations table

    After the push completes, verify with `supabase db diff --linked` — expected output: no pending changes (the schema matches what is in supabase/migrations/).

    Optional verification: run the rewired getBrowseArchetypeCounts SQL against prod manually (per RESEARCH §Assumption A3 mitigation) using psql against the prod DATABASE_URL. Query: `SELECT tag AS archetype, COUNT(*)::int AS count FROM watches_catalog, unnest(style_tags) AS tag WHERE tag = ANY(ARRAY['dress','dive','field','pilot','chrono','gmt','racing','sport','tool','hybrid']) GROUP BY tag ORDER BY count DESC`. Expected: returns ~10 rows with counts roughly matching the pre-drop primary_archetype counts (per spike Q1's 99% agreement evidence).

    IF supabase CLI is not installed locally, the prod push is BLOCKED — surface the missing-CLI condition to the developer with install instructions: `brew install supabase/tap/supabase`.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql && grep -n "public.watches_catalog DROP COLUMN IF EXISTS" supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql ; supabase db diff --linked 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql` exits 0
    - File contains the literal string `ALTER TABLE public.watches_catalog DROP COLUMN IF EXISTS primary_archetype`
    - File contains the header-comment block documenting the 4 prod-push gotchas
    - `supabase db push --linked` runs successfully (developer-confirmed; no error in output)
    - `supabase db diff --linked` reports no pending changes after the push
    - Prod `watches_catalog` table no longer has the `primary_archetype` column (verified via `psql ... -c "\\d watches_catalog"` against prod or via supabase studio)
    - The rewired getBrowseArchetypeCounts SQL returns ~10 rows with counts within tolerance of pre-drop values (RESEARCH §Assumption A3 mitigation passes)
  </acceptance_criteria>
  <done>Supabase migration authored; prod push complete; prod DB column absent; verification queries confirm style_tags-derived counts are healthy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Post-deploy verification: prod /explore + /search + /catalog still work</name>
  <files>(none — manual prod URL verification)</files>
  <what-built>Plan 08 Task 1 dropped primary_archetype from the prod watches_catalog table via supabase db push --linked. All reader code paths (Plans 02-07) have been deployed to prod previously. The post-drop runtime should function identically to the pre-drop runtime (per spike Q1's 99% agreement on style_tags vs primary_archetype).</what-built>
  <how-to-verify>
    Verify these prod URLs render without 500 errors:

    1. https://horlo.app/explore — confirm the page loads; the CollectorArchetypes rail shows ~10 archetype chips (Dive Watch Devotee, etc.) with counts; the Browse module shows tiles but NO "Genres" tile.
    2. https://horlo.app/explore/genres — confirm this returns 404 (or whatever Next.js's standard not-found response is). Per UI-SPEC §E it should NOT 500.
    3. https://horlo.app/search — confirm the search page loads; opening the filter drawer shows 5 chip groups (Movement, Case Size, Style, Brand, Era — NO Genre, NO Archetype).
    4. https://horlo.app/search?q=submariner — confirm results render; no archetype editorial header above results.
    5. Click into any watch on /search → /catalog/[catalogId] — confirm the ReferenceIdentityCard headline shows only the era (e.g., "Modern era") with no archetype tail. Confirm the CollectionFitCompareTable shows 5 rows (no Archetype row).
    6. If signed in, navigate to /watch/[id] of an owned watch — confirm the same ReferenceIdentityCard era-only headline (D-39b-04 identical-rendering lock).
    7. If the LLM enricher runs (add-watch flow), confirm it completes without `taste_enrichment_failed:zod_parse_failed` errors. Add a test watch via the URL-import flow if convenient.

    NO 500 errors on any of the above. The CollectorArchetypes rail counts should look near-identical to pre-deploy values (per spike Q1 99% agreement).
  </how-to-verify>
  <action>Pause for the developer to perform the 7 manual prod URL checks above. The developer responds with `approved` (all pass) or a description of the observed issue.</action>
  <verify>Manual: developer confirms all 7 prod URL checks pass.</verify>
  <done>Developer responds with `approved`; Phase 49.1 is complete on prod.</done>
  <resume-signal>Type "approved" if all 7 checks pass, or describe any prod issue observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → Supabase prod | supabase db push --linked uses the linked project ref + supabase access token; full DDL privilege on the prod DB |
| Postgres prod DB → consumers | All reader code already shipped that does not reference primary_archetype; the drop simply removes an unused column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49.1-18 | D (Data loss) | prod watches_catalog.primary_archetype | accept | Destructive by design (D-MIG-01: no data backfill). Spike Q1 evidence shows 99% of the signal survives in style_tags; the 1% loss (Rolex Daytona 16520 'racing' provenance) is acknowledged in CONTEXT. Rollback path (D-MIG-03): re-add column + re-run LLM enrichment — feasible-but-costly. |
| T-49.1-19 | D (Denial of service) | prod runtime | mitigate | Pre-flight checkpoint confirms all reader code is deployed before the drop. IF EXISTS guard makes the drop idempotent (re-runs are safe). |
| T-49.1-20 | E (Elevation of privilege) | supabase CLI access | accept | The developer's supabase access token has prod write privilege. This is unavoidable for any migration deploy and is the established workflow. |
| T-49.1-21 | T (Tampering) | supabase_migrations.schema_migrations journal | mitigate | The supabase CLI owns this journal; manual edits forbidden. Filename ordinal 20260519010000 is strictly increasing per gotcha #1. |
</threat_model>

<verification>
- `supabase db diff --linked` reports no pending changes
- Prod `watches_catalog` table has no `primary_archetype` column (psql or supabase studio)
- The rewired getBrowseArchetypeCounts SQL returns counts within tolerance of pre-drop values
- All 7 manual prod URL checks pass with no 500 errors
- ROADMAP SC#1 fully satisfied: column dropped from both schema.ts (Plan 07) and prod via supabase db push --linked (this plan)
- ROADMAP SC#7 partial: existing tests pass (full vitest suite green after all 8 plans)
</verification>

<success_criteria>
- ROADMAP SC#1 fully satisfied: watches_catalog.primary_archetype dropped on prod via one drizzle migration (Plan 07) + one supabase migration (this plan)
- D-MIG-01 + D-MIG-02 + D-MIG-04 all applied (D-MIG-03 rollback path remains documented but not invoked)
- 4 prod-push gotchas all addressed (filename ordering, no migration dependency, no extension schema, no enum-bound dependents — verified in file's header comment)
- Phase 49.1 closed: every ROADMAP SC#1-7 satisfied; all 8 plans shipped
</success_criteria>

<output>
After completion, create `.planning/phases/49.1-remove-genre-surface/49.1-08-SUMMARY.md` AND update `.planning/STATE.md` to record Phase 49.1 complete + the v5.2 milestone progress (Phase 49.1 was inserted mid-milestone; mark it shipped).
</output>
