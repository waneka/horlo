---
phase: 49.1
slug: remove-genre-surface
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-19
---

# Phase 49.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `49.1-RESEARCH.md ## Validation Architecture` (§663-741).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 2.1.9 (jsdom env) |
| **Config file** | `vitest.config.ts` |
| **Test globs** | `tests/**/*.test.{ts,tsx}` AND `src/**/*.test.{ts,tsx}` |
| **Setup file** | `tests/setup.tsx` (+ `tests/shims/server-only.ts` alias) |
| **Quick run command** | `npm test -- <path/to/file>` |
| **Full suite command** | `npm test` (`vitest run`) |
| **Estimated runtime** | ~60s full suite (~26 archetype-touching files + unaffected) |
| **Static checks** | `npm run lint` + `npx tsc --noEmit` (run on every gate) |

---

## Sampling Rate

- **After every task commit:** Run `npm test -- <touched_file>` (vitest single-file, < 5s typical)
- **After every plan wave:** Run `npm test` (full vitest suite) + `npm run lint` + `npx tsc --noEmit`
- **Before `/gsd-verify-work`:** Full suite green + lint clean + tsc clean
- **Max feedback latency:** ~60s (full suite)

---

## Per-Task Verification Map

> Populated by planner per-task. Below is the requirements-level mapping that
> the planner uses to derive task-level `<automated>` blocks.

| Req | Behavior | Test Type | Automated Command | Status |
|-----|----------|-----------|-------------------|--------|
| TAX-02 | Schema DROP survives upsert + read | integration | `npm test -- tests/integration/catalog-taste.test.ts tests/integration/catalog-taste-schema.test.ts` | ✅ existing (update) |
| TAX-02 | `TASTE_SUB_WEIGHTS` sum to 1.0 post-rebalance; effective taste ≤ 0.20 | unit | `npm test -- tests/similarity.test.ts` | ✅ existing |
| TAX-02 | Verdict thresholds (THRESHOLDS / GOAL_THRESHOLDS) byte-identical | unit | `npm test -- tests/similarity.test.ts` | ✅ existing (add invariant assertion) |
| TAX-02 | `/explore` CollectorArchetypes rail renders with style_tags-derived counts | component | `npm test -- src/components/explore/__tests__/CollectorArchetypes.test.tsx` | ✅ existing (mock shape update) |
| TAX-02 | `getBrowseArchetypeCounts()` returns `Array<{ archetype, count }>` from style_tags | unit | `npm test -- src/data/__tests__/browse.test.ts` | ✅ existing (drop genre describe; update archetype SQL expectation) |
| TAX-02 | `/explore/genres` route returns 404 (file deleted) | smoke | filesystem assertion via Wave 0 new test | ❌ Wave 0 |
| TAX-02 | No orphan imports; tsc clean | static | `npx tsc --noEmit` | ✅ existing |
| TAX-02 | Enricher round-trip without `primary_archetype` | integration | `npm test -- src/lib/taste/enricher.test.ts src/lib/taste/vocab.test.ts` | ✅ existing (drop archetype assertions) |
| TAX-02 | Verdict templates fire on era axis | unit | `npm test -- src/lib/verdict/composer.test.ts src/lib/verdict/confidence.test.ts` | ✅ existing (repoint fixtures) |
| TAX-02 | `ViewerTasteProfile` no longer carries `dominantArchetype`; EMPTY_PROFILE matches | unit | `npm test -- src/lib/verdict/viewerTasteProfile.test.ts` | ✅ existing (drop assertions) |
| TAX-02 | `fit-delta` produces 5-dimension ranking (no archetype) | unit | `npm test -- tests/unit/lib/verdict/fit-delta.test.ts` | ✅ existing (drop archetype assertion l.51-54) |
| TAX-02 | `CollectionFitCompareTable` renders 5 rows | component | `npm test -- tests/components/insights/CollectionFitCompareTable.test.tsx` (NEW) | ❌ Wave 0 (optional) |
| TAX-02 | `ReferenceIdentityCard` renders era-only headline when archetype absent | component | `npm test -- tests/components/insights/ReferenceIdentityCard.test.tsx` | ✅ existing (update assertions) |
| TAX-02 | `FilterDrawer` no longer renders Genre/Archetype chip groups | component | `npm test -- tests/components/search/FilterDrawer.test.tsx tests/components/search/DrawerChips.test.tsx` | ✅ existing (delete chip-group describes; remove vi.mock l.133-138 + props l.174-175) |
| TAX-02 | `SearchPageClient` no longer renders archetype header or genre/archetype chips | component | `npm test -- tests/components/search/SearchPageClientChips.test.tsx` | ✅ existing (delete Tests 1, 4, 5, 8) |
| TAX-02 | Search Server Action Zod schema rejects unknown `archetype`/`genre` (.strict()) | integration | `npm test -- tests/actions/verdict.test.ts src/data/__tests__/catalog-facets.test.ts` | ✅ existing (delete catalog-facets l.98-148 describes) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/integration/migration-drop-archetype.test.ts` — NEW. Assert (a) the `primary_archetype` column is absent post-migration; (b) `upsertCatalogFromExtractedUrl` succeeds without writing the column; (c) `getWatchesByUser` LEFT JOIN returns rows with `catalogTaste.formality/sportiness/etc.` present but no `primaryArchetype` field.
- [ ] `tests/app/explore-genres-404.test.ts` (or equivalent) — NEW. Smoke test asserting the deleted route file is absent (filesystem `fs.existsSync` OR module-resolution failure assertion).
- [ ] (Optional) `tests/components/insights/CollectionFitCompareTable.test.tsx` — NEW. Component test for the 5-row table (no dedicated test exists today).

*Vitest is already established. No framework install needed.*

---

## Validation Invariants (engine-side)

1. **`TASTE_SUB_WEIGHTS` sums to 1.0** — post-rebalance `0.50 + 0.25 + 0.25 = 1.00` (D-SIM-01).
2. **Effective taste contribution stays in [0, 0.20]** — `WEIGHTS.taste * tasteSimilarityRaw01(...) ≤ 0.20 * 1.0 = 0.20` (D-SIM-04 outer envelope unchanged).
3. **Verdict label thresholds unchanged** — `THRESHOLDS` and `GOAL_THRESHOLDS` objects byte-identical to pre-49.1 (D-SIM-04).
4. **`CatalogTasteAttributes` shape post-removal** — TypeScript compile check (no `primaryArchetype` field; `eraSignal`, `designMotifs`, etc. still present).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `supabase db push --linked` drops `primary_archetype` in prod | TAX-02 SC#1 | Prod migration is shell-only; cannot be exercised inside vitest | Author runs `supabase db push --linked` AFTER Waves 1–3 are deployed to prod; verifies via `supabase db diff` returning empty |
| `/explore/genres` returns 404 in dev (Next.js routing) | TAX-02 SC#6 | Dev-server route assertion is integration with the Next runtime, not unit-level | `npm run dev`, then `curl -o /dev/null -w "%{http_code}" http://localhost:3000/explore/genres` → expect `404` |
| `/search?archetype=dive` is rejected by Zod `.strict()` | TAX-02 SC#3 (acknowledged in D-EXPLORE-03) | URL-level Server Action validation flows through Next's request handler; assertion is browser-side | Manual: visit `/search?archetype=dive`, expect schema error / param stripped |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify block or Wave 0 dependency
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers the 2 NEW tests (migration + 404 smoke) + 1 optional (CompareTable)
- [x] No watch-mode flags in any test command
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter after planner per-task mapping is complete

**Approval:** planner signed off (Plans 01-08 derive task-level `<automated>` blocks from this map; Plan 01 is re-tagged `wave: 0` and Plans 03/04 hold `depends_on: ["49.1-01"]` so the three Wave 0 test files are guaranteed to exist before any Wave 1 plan begins).
