---
phase: 49.1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/insights/ReferenceIdentityCard.tsx
  - src/components/insights/CollectionFitCompareTable.tsx
  - src/components/search/SearchPageClient.tsx
  - src/app/actions/search.ts
  - tests/components/insights/ReferenceIdentityCard.test.tsx
  - tests/static/ReferenceIdentityCard.no-engine.test.ts
  - tests/components/search/SearchPageClientChips.test.tsx
  - src/data/__tests__/catalog-facets.test.ts
autonomous: true
requirements: [TAX-02]
must_haves:
  truths:
    - "ReferenceIdentityCard headline renders era-only — no archetype side, no separator"
    - "CollectionFitCompareTable renders 5 rows (Formality, Sportiness, Heritage, Era, Design Motifs); last row uses thRowLastClass/tdLastClass with no border-b"
    - "SearchPageClient no longer renders the archetype editorial header or the archetype/genre removable chips"
    - "searchSchema Zod object rejects unknown 'genre' and 'archetype' keys via .strict()"
  artifacts:
    - path: src/components/insights/ReferenceIdentityCard.tsx
      provides: "Era-only headline; ARCHETYPE_LABELS const deleted"
      contains: "ERA_LABELS"
    - path: src/components/insights/CollectionFitCompareTable.tsx
      provides: "5-row compare table; tableLastClass rebound to Motifs row"
    - path: src/components/search/SearchPageClient.tsx
      provides: "Slim results header; brand/era removable chips only"
    - path: src/app/actions/search.ts
      provides: "Zod schema with no genre/archetype keys; filters passthrough without those fields"
  key_links:
    - from: src/components/search/SearchPageClient.tsx
      to: src/app/actions/search.ts
      via: "URL params → searchSchema.parse() → server action"
      pattern: "searchSchema"
    - from: src/components/insights/ReferenceIdentityCard.tsx
      to: src/lib/types.ts
      via: "CatalogTasteAttributes consumer (no primaryArchetype read after this plan)"
      pattern: "taste\\.eraSignal|eraLabel"
---

<objective>
Execute the D-SCOPE direct-UI archetype removals on the four "modified survivor" UI surfaces called out in `49.1-UI-SPEC.md` §§A-D: ReferenceIdentityCard (era-only headline), CollectionFitCompareTable (5-row table), SearchPageClient (drop archetype editorial header + archetype/genre removable chips), and the search Server Action's Zod schema (drop genre/archetype keys). Update the affected component + action tests + the catalog-facets test that asserts on the archetype filter.

Purpose: D-SCOPE-01a, 01b, 01d, 01f locked decisions. Honest deletion of the archetype hook on direct-UI surfaces, NOT substitution with `style_tags[0]` (spike Q3 "sport" overloading risk). The CSS chains from UI-SPEC §§A-D land here.

Output: 4 modified UI/action source files + 4 modified test files. No DB schema change in this plan; no DAL filter cleanup in this plan (that lives in Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/49.1-remove-genre-surface/49.1-CONTEXT.md
@.planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md
@.planning/phases/49.1-remove-genre-surface/49.1-UI-SPEC.md
@.planning/phases/49.1-remove-genre-surface/49.1-RESEARCH.md

# Files being modified
@src/components/insights/ReferenceIdentityCard.tsx
@src/components/insights/CollectionFitCompareTable.tsx
@src/components/search/SearchPageClient.tsx
@src/app/actions/search.ts

# Existing tests being updated
@tests/components/insights/ReferenceIdentityCard.test.tsx
@tests/static/ReferenceIdentityCard.no-engine.test.ts
@tests/components/search/SearchPageClientChips.test.tsx
@src/data/__tests__/catalog-facets.test.ts

# Cross-reference — the Wave 0 compare-table test
@tests/components/insights/CollectionFitCompareTable.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop archetype from ReferenceIdentityCard headline; delete Row 4 from CollectionFitCompareTable</name>
  <files>src/components/insights/ReferenceIdentityCard.tsx, src/components/insights/CollectionFitCompareTable.tsx, tests/components/insights/ReferenceIdentityCard.test.tsx, tests/static/ReferenceIdentityCard.no-engine.test.ts</files>
  <read_first>
    - src/components/insights/ReferenceIdentityCard.tsx (full file — locate ARCHETYPE_LABELS const at :35-46, archetypeLabel computation around :59, headline JSX with archetype span at :73-77, hasHeadline guard at :61)
    - src/components/insights/CollectionFitCompareTable.tsx (full file — locate the 6 rows; specifically Row 4 "Archetype" at approximately :158-167, plus tableLastClass binding logic on Rows 5 (Era) and 6 (Motifs) at approximately :168-183)
    - tests/components/insights/ReferenceIdentityCard.test.tsx (full file — locate fixtures with primaryArchetype + assertions on "Dive Watch Devotee" / "Modern era · Dive Watch Devotee" copy)
    - tests/static/ReferenceIdentityCard.no-engine.test.ts (full file — confirm what it asserts; it's a no-engine import-isolation test)
    - tests/components/insights/CollectionFitCompareTable.test.tsx (the Wave 0 test — confirm its 5-row assertion is the post-modification target)
    - .planning/phases/49.1-remove-genre-surface/49.1-UI-SPEC.md §A and §B (CSS chain assertions for the era-only headline and the 5-row table last-border)
    - .planning/phases/49.1-remove-genre-surface/49.1-PATTERNS.md §"src/components/insights/ReferenceIdentityCard.tsx" and §"§B CollectionFitCompareTable Row Set"
  </read_first>
  <behavior>
    - ReferenceIdentityCard renders headline `p.text-base.font-semibold.text-foreground` containing only `span.truncate` with `{eraLabel}` — NO ` · ` separator, NO archetype span. When eraLabel is null, the headline `p` does not render at all (hasHeadline = Boolean(eraLabel)).
    - The ARCHETYPE_LABELS constant is gone from the file. Any archetypeLabel derivation is gone. Any taste.primaryArchetype read is gone.
    - The component is identically structured on both /watch/{id} and /catalog/{id} (Phase 39b D-39b-04 lock; both consume the same CatalogTasteAttributes shape — primaryArchetype is dropped from that type by Plan 06).
    - CollectionFitCompareTable renders a tbody with exactly 5 tr elements in the order: Formality, Sportiness, Heritage, Era, Design Motifs.
    - The last row's th uses thRowLastClass (no border-b), and its td uses tdLastClass (no border-b).
    - The previously-last Era row now uses thRowClass / tdClass (with border-b) — it is no longer the last row.
    - The Wave 0 test tests/components/insights/CollectionFitCompareTable.test.tsx transitions red to green after this task.
    - The existing tests/components/insights/ReferenceIdentityCard.test.tsx asserts era-only headline rendering; the "Modern era · Dive Watch Devotee" expectation becomes "Modern era".
  </behavior>
  <action>
    In src/components/insights/ReferenceIdentityCard.tsx perform these mechanical deletions and rewrites:

    1. Delete the ARCHETYPE_LABELS const at approximately lines 35-46.
    2. Delete any local archetypeLabel derivation (around line 59).
    3. Simplify the hasHeadline guard at line 61 from `Boolean(eraLabel || archetypeLabel)` to `Boolean(eraLabel)`.
    4. In the headline JSX at lines 73-77, remove the archetype span element and the " · " separator string. The post-modification JSX renders only the era label per UI-SPEC §A: a single p with class `text-base font-semibold text-foreground` containing one span with class `truncate` showing eraLabel.
    5. Remove any unused import — if taste.primaryArchetype was the only consumer of a PrimaryArchetype type import in this file, remove that import. KEEP all era-related imports.

    In src/components/insights/CollectionFitCompareTable.tsx:

    1. Delete Row 4 (the "Archetype" tr block) at approximately lines 158-167.
    2. Rebind classes on the surviving rows so the LAST visible row (Design Motifs, which was Row 6 and is now Row 5) uses thRowLastClass on its th and tdLastClass on its td. Concretely per UI-SPEC §B:
       - Row 4 (Era, was Row 5): th uses thRowClass (with border-b), td uses tdClass.
       - Row 5 (Design Motifs, was Row 6): th uses thRowLastClass (no border-b), td uses tdLastClass (no border-b).
    3. If there is any helper variable like tableLastClass or lastRowIndex driving the binding, update its value so the Motifs row is treated as the last row.

    In tests/components/insights/ReferenceIdentityCard.test.tsx:

    1. Locate every fixture literal containing primaryArchetype (e.g. value 'dive') and DELETE that key from each fixture. The fixture should match the post-Plan-06 CatalogTasteAttributes shape (no primaryArchetype). If the current type still has the field and this produces a TS error, cast via `as CatalogTasteAttributes` with a comment `// TODO Phase 49.1 Plan 06 — drop cast when CatalogTasteAttributes loses primaryArchetype`.
    2. Update any assertion of the form `expect(...).toHaveTextContent('Modern era · Dive Watch Devotee')` to `expect(...).toHaveTextContent('Modern era')`. Same for any other era-archetype concatenation.
    3. Delete any assertion that the archetype span exists (text "Devotee" or "Dive Watch" matchers).

    In tests/static/ReferenceIdentityCard.no-engine.test.ts:

    Read the file first. If it asserts on a specific list of imported symbols, drop ARCHETYPE_LABELS from that assertion. If it asserts the file does NOT import from verdict modules, that assertion stays — only the imported-symbol list changes.
  </action>
  <verify>
    <automated>grep -nE "ARCHETYPE_LABELS|archetypeLabel|primaryArchetype" src/components/insights/ReferenceIdentityCard.tsx || echo OK ; npm test -- tests/components/insights/ReferenceIdentityCard.test.tsx tests/static/ReferenceIdentityCard.no-engine.test.ts tests/components/insights/CollectionFitCompareTable.test.tsx 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "ARCHETYPE_LABELS|archetypeLabel" src/components/insights/ReferenceIdentityCard.tsx` returns no matches
    - `grep -n "primaryArchetype" src/components/insights/ReferenceIdentityCard.tsx` returns no matches
    - `grep -c "Archetype" src/components/insights/CollectionFitCompareTable.tsx` returns 0 (row header label gone)
    - The CollectionFitCompareTable file contains exactly 5 tr elements inside the tbody
    - `npm test -- tests/components/insights/CollectionFitCompareTable.test.tsx` exits 0 (Wave 0 test green: tbody has 5 rows; last row has no border-b)
    - `npm test -- tests/components/insights/ReferenceIdentityCard.test.tsx tests/static/ReferenceIdentityCard.no-engine.test.ts` exits 0
  </acceptance_criteria>
  <done>Era-only headline; 5-row table; tests green including the Wave 0 compare-table test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip archetype editorial header + archetype/genre removable chips from SearchPageClient; drop genre/archetype from searchSchema Zod</name>
  <files>src/components/search/SearchPageClient.tsx, src/app/actions/search.ts, tests/components/search/SearchPageClientChips.test.tsx, src/data/__tests__/catalog-facets.test.ts</files>
  <read_first>
    - src/components/search/SearchPageClient.tsx (full file — locate ARCHETYPE_CONFIG import at :13, the archetype URL param read at :97-99 + :115-117, the archetypeConfig derivation around :395, the archetype editorial header section at lines 392-414 and 481-487, the removable archetype Chip block at 418-454 and 493-526, the removable genre Chip block in the same regions, the hasInlineFacets derivation)
    - src/app/actions/search.ts (full file — locate the searchSchema Zod object at :24-38, the genre/archetype enum-of-PRIMARY_ARCHETYPES entries, the filters passthrough at :125-126)
    - tests/components/search/SearchPageClientChips.test.tsx (full file — locate Tests 1, 4, 5, 8 which reference archetype/genre per RESEARCH §Validation Architecture)
    - src/data/__tests__/catalog-facets.test.ts (full file — locate the 3 describe blocks at :98-148 covering archetype filter, genre filter, archetype-wins precedence)
    - .planning/phases/49.1-remove-genre-surface/49.1-UI-SPEC.md §C (the slim search header contract — what survives)
  </read_first>
  <behavior>
    - SearchPageClient no longer imports ARCHETYPE_CONFIG. No archetypeConfig variable. No archetype URL param destructured. No genre URL param destructured. No archetypeConfig-driven editorial section. No Chip variant="removable" block for archetype or genre. The brand and era removable chip blocks SURVIVE unchanged. hasInlineFacets becomes Boolean(brand || era) (was Boolean(brand || era || genre || archetype)).
    - The Zod searchSchema in src/app/actions/search.ts has no genre key and no archetype key. .strict() is preserved — old URLs with ?archetype= or ?genre= will be rejected by Zod (acceptable per D-EXPLORE-03). The filters passthrough at the end of the action drops the genre and archetype entries. The PRIMARY_ARCHETYPES import is removed if no longer used in this file (ERA_SIGNALS stays).
    - tests/components/search/SearchPageClientChips.test.tsx no longer contains tests for archetype/genre chips (Tests 1, 4, 5, 8 are deleted per RESEARCH).
    - src/data/__tests__/catalog-facets.test.ts no longer contains the 3 archetype/genre describe blocks at :98-148.
  </behavior>
  <action>
    In src/components/search/SearchPageClient.tsx perform these deletions in order:

    1. Delete the ARCHETYPE_CONFIG import at line 13 (or wherever it appears in the import block).
    2. Delete the archetype and genre props/URL-param fields from the component's props interface or useSearchParams destructure (around lines 97-99, 115-117).
    3. Delete the archetypeConfig variable derivation (around line 395 — pattern: `const archetypeConfig = archetype ? ARCHETYPE_CONFIG[archetype] : null`).
    4. Delete the archetype editorial section blocks at lines 392-414 AND 481-487 (the no-results variant and the results variant — both referenced in CONTEXT D-SCOPE-01d).
    5. Delete the inline removable archetype Chip block at lines 418-454 AND 493-526.
    6. Delete the inline removable genre Chip block in the same regions.
    7. Update hasInlineFacets from `Boolean(brand || era || genre || archetype)` to `Boolean(brand || era)`.
    8. Keep the brand and era removable chip blocks UNCHANGED.
    9. If archetype or genre appears anywhere else in the file (e.g. a Clear all callback, a URL builder helper), remove those references too.

    In src/app/actions/search.ts:

    1. In the searchSchema z.object({ ... }).strict() at lines 24-38, delete the two lines for genre and archetype enums.
    2. In the filters passthrough at lines 125-126, delete the `genre: parsed.data.genre,` and `archetype: parsed.data.archetype,` lines.
    3. Remove the PRIMARY_ARCHETYPES import (at line 11 or wherever) IF it has no other consumer in this file. Keep ERA_SIGNALS unchanged.
    4. .strict() is preserved — do NOT change this.

    In tests/components/search/SearchPageClientChips.test.tsx:

    1. Delete Tests 1, 4, 5, 8 (per RESEARCH §Validation Architecture and PATTERNS §"tests/components/search/SearchPageClientChips.test.tsx"). Read the file to identify the specific it blocks by their archetype/genre references.
    2. Surviving tests (the brand and era removable-chip cases) MUST stay green.

    In src/data/__tests__/catalog-facets.test.ts:

    1. Delete the three describe blocks at lines 98-148: one for the archetype filter, one for the genre filter, one for the archetype-wins precedence. Read the file to confirm exact block boundaries.
    2. Surviving describes (era facet, brand facet, etc.) stay UNTOUCHED.
  </action>
  <verify>
    <automated>grep -nE "ARCHETYPE_CONFIG|archetypeConfig" src/components/search/SearchPageClient.tsx || echo OK ; grep -nE "z\.enum\(PRIMARY_ARCHETYPES\)" src/app/actions/search.ts || echo OK ; npm test -- tests/components/search/SearchPageClientChips.test.tsx src/data/__tests__/catalog-facets.test.ts 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ARCHETYPE_CONFIG" src/components/search/SearchPageClient.tsx` returns no matches
    - `grep -n "archetypeConfig" src/components/search/SearchPageClient.tsx` returns no matches
    - `grep -nE "z\.enum\(PRIMARY_ARCHETYPES\)" src/app/actions/search.ts` returns no matches
    - `grep -nE "genre:.*parsed.data\.genre|archetype:.*parsed.data\.archetype" src/app/actions/search.ts` returns no matches
    - `grep -n ".strict()" src/app/actions/search.ts` still returns at least 1 match (invariant preserved)
    - `npm test -- tests/components/search/SearchPageClientChips.test.tsx` exits 0
    - `npm test -- src/data/__tests__/catalog-facets.test.ts` exits 0
    - SearchPageClient renders brand and era removable chips when those URL params are set; no archetype or genre chip ever renders
  </acceptance_criteria>
  <done>Slim search header per UI-SPEC §C; Zod schema rejects unknown archetype/genre keys; affected tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client URL → SearchPageClient | URL params parsed by useSearchParams; values flow into searchSchema.parse() in the Server Action |
| client → search Server Action | Zod .strict() rejects unknown keys including archetype/genre after this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49.1-08 | T (Tampering) | src/app/actions/search.ts searchSchema | mitigate | .strict() preserved; dropping the genre/archetype enum keys means unknown URL params are explicitly rejected. Acceptable per D-EXPLORE-03 — no external bookmarks expected for v5.1 chips launched less than one week before this removal. |
| T-49.1-09 | I (Information disclosure) | src/components/insights/ReferenceIdentityCard.tsx | accept | Era-only headline reveals strictly less information than the previous era + archetype concatenation. No new disclosure surface. |
</threat_model>

<verification>
- `npm test -- tests/components/insights/ tests/static/ReferenceIdentityCard.no-engine.test.ts tests/components/search/SearchPageClientChips.test.tsx src/data/__tests__/catalog-facets.test.ts` exits 0
- The Wave 0 compare-table test transitions to green
- `grep -nE "ARCHETYPE_LABELS|ARCHETYPE_CONFIG" src/components/` returns no matches (the only retained consumer is /explore/CollectorArchetypes — see Plan 03)
- Visual contract from UI-SPEC §§A-C is the rendered output
</verification>

<success_criteria>
- D-SCOPE-01a applied: era-only headline on ReferenceIdentityCard; ARCHETYPE_LABELS const deleted
- D-SCOPE-01b applied: 5-row CollectionFitCompareTable with correct last-row class bindings
- D-SCOPE-01d applied: SearchPageClient archetype editorial header + archetype/genre removable chips deleted; ARCHETYPE_CONFIG import removed at this surface
- D-SCOPE-01f applied: searchSchema Zod rejects unknown genre/archetype keys; filters passthrough drops both
- The Wave 0 compare-table test (Plan 01 Task 3) transitions red to green
</success_criteria>

<output>
After completion, create `.planning/phases/49.1-remove-genre-surface/49.1-04-SUMMARY.md`
</output>
