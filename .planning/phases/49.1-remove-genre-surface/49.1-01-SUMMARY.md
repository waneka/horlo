---
phase: 49.1-remove-genre-surface
plan: 01
subsystem: testing
tags: [vitest, integration-test, smoke-test, component-test, wave-0, archetype-removal, jsdom, drizzle, testing-library]

# Dependency graph
requires:
  - phase: 49
    provides: TAX-01 spike Ship-Now YES verdict (TAX-02 — remove genre surface)
provides:
  - tests/integration/migration-drop-archetype.test.ts (DATABASE_URL-gated red→green gate for schema drop in Plan 07/08)
  - tests/app/explore-genres-404.test.ts (filesystem-absence red→green gate for route deletion in Plan 03)
  - tests/components/insights/CollectionFitCompareTable.test.tsx (component red→green gate for Row-4 deletion in Plan 04)
affects: [49.1-03, 49.1-04, 49.1-05, 49.1-07, 49.1-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Wave 0 test scaffolds authored BEFORE production change — red today, green when target wave lands"
    - "DATABASE_URL-gated `maybe = describe.skip` idiom from catalog-taste.test.ts:8 reused"
    - "Filesystem-absence smoke test idiom from no-evaluate-route.test.ts:1-16 reused"
    - "Post-49.1 CatalogTasteAttributes literals cast through `unknown` until Plan 05 drops primaryArchetype from the type"

key-files:
  created:
    - tests/integration/migration-drop-archetype.test.ts
    - tests/app/explore-genres-404.test.ts
    - tests/components/insights/CollectionFitCompareTable.test.tsx
  modified: []

key-decisions:
  - "Cast post-49.1 taste literals through `unknown` (not type-narrowed) because `CatalogTasteAttributes.primaryArchetype` is still required today — keeps tests readable and unblocks Plan 05 without dual-file edits"
  - "Migration test #2 (getWatchesByUser) uses a sentinel UUID and a for-loop over results — vacuously passes when no rows exist; full coverage materializes once Plan 05 wires the read-path drop"
  - "404 smoke test uses both `existsSync` on page.tsx AND a `readdirSync` fallback on the parent directory — tolerates Plan 03's choice between file-only vs full-directory delete"

patterns-established:
  - "Pattern: Wave 0 scaffold tests reference the post-removal contract (no primaryArchetype key; 5 rows; absent file) — RED today, GREEN when target wave lands — no test rewrites needed mid-execution"

requirements-completed: [TAX-02]

# Metrics
duration: 3 min
completed: 2026-05-20
---

# Phase 49.1 Plan 01: Wave 0 Test Scaffolds Summary

**Three Wave 0 vitest test files authored — one DATABASE_URL-gated integration test for the `primary_archetype` schema drop, one filesystem-absence smoke test for the `/explore/genres` route deletion, and one jsdom component test for the 5-row CollectionFitCompareTable post-Row-4 deletion — all correctly RED today and wired to flip GREEN when Plans 03/04/07/08 land.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-20T06:10:15Z
- **Completed:** 2026-05-20T06:13:23Z
- **Tasks:** 3
- **Files created:** 3

## Accomplishments

- Authored the DATABASE_URL-gated integration test for the `primary_archetype` column drop (mirrors `catalog-taste.test.ts:8` skip idiom)
- Authored the filesystem-absence smoke test for `/explore/genres` route deletion (mirrors `no-evaluate-route.test.ts`)
- Authored the component test for the 5-row CollectionFitCompareTable (mirrors `ReferenceIdentityCard.test.tsx`)
- Verified each test file parses + registers under vitest 2.1.9 + jsdom; verified RED-state outcomes match the plan's `<acceptance_criteria>` (skipped / 1 failed / 1 failed | 1 passed)
- Verified no tsc errors introduced in any of the three new files

## Task Commits

Each task was committed atomically:

1. **Task 1: Author tests/integration/migration-drop-archetype.test.ts** — `113f8dc` (test)
2. **Task 2: Author tests/app/explore-genres-404.test.ts** — `1da116f` (test)
3. **Task 3: Author tests/components/insights/CollectionFitCompareTable.test.tsx** — `1419b00` (test)

**Plan metadata:** (this commit, post-SUMMARY)

## Files Created/Modified

- `tests/integration/migration-drop-archetype.test.ts` (NEW, 115 lines) — DATABASE_URL-gated integration test asserting (a) `primary_archetype` column absent from `information_schema.columns`, (b) `getWatchesByUser` returned `catalogTaste` has no `primaryArchetype` key, (c) `updateCatalogTaste` succeeds without the field on the input
- `tests/app/explore-genres-404.test.ts` (NEW, 39 lines) — `existsSync`-based smoke test asserting `src/app/explore/genres/page.tsx` is absent + `readdirSync` fallback on the parent directory
- `tests/components/insights/CollectionFitCompareTable.test.tsx` (NEW, 98 lines) — `@testing-library/react` jsdom component test counting `tbody tr` to 5 + asserting `border-b` absence on the last row

## Decisions Made

- **Cast post-49.1 taste literals through `unknown` instead of fully retyping** — `CatalogTasteAttributes.primaryArchetype` is still declared required in `src/lib/types.ts:224` today. The cast lets the Wave 0 tests express the post-removal contract without forcing a parallel type edit in this plan. After Plan 05 drops the field from the interface, the cast can be removed (mechanical cleanup, no semantic change).
- **Migration test case #2 uses a sentinel UUID and vacuously passes on empty result sets** — the LEFT-JOIN read path is tested in production-shape via existing tests; the Wave 0 assertion here is the *shape invariant* (no `primaryArchetype` key on any returned row), which holds even when there are zero rows for the sentinel user. Full integration coverage comes online when Plans 05/07/08 land and a real fixture user can be seeded.
- **404 smoke test guards both file-deletion and directory-deletion outcomes** — Plan 03's deletion choice (delete only `page.tsx` vs delete the whole `src/app/explore/genres/` directory) doesn't matter to this test; both are acceptable post-removal states.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. All three test files parse, register, and produce the plan-expected outcomes:

- Migration test → 3 skipped (no `DATABASE_URL` in this environment) — acceptable per plan
- 404 smoke test → 1 failed (`src/app/explore/genres/page.tsx` still exists) — correctly RED
- CompareTable component test → 1 failed | 1 passed (6 rows ≠ 5; last-row border-b assertion already passes because Row 6 already uses `*LastClass`) — correctly RED on the row-count assertion

Pre-existing tsc errors in unrelated test files (`tests/data/getMostFollowedCollectors.test.ts`, `tests/data/getTrendingCatalogWatches.test.ts`, `tests/integration/catalog-taste.test.ts`, `tests/integration/phase17-extract-route-wiring.test.ts`) were observed but are out of scope per `<scope_boundary>` — they do not reference any file touched by this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All three Wave 0 test scaffolds are in place. Plans 03 (delete `/explore/genres` route), 04 (delete Row 4 from `CollectionFitCompareTable`), 05 (drop `primaryArchetype` from `CatalogTasteAttributes` type + DAL read projection), and 07/08 (drizzle + supabase migrations dropping the column) can now reference these tests as their red→green gates.
- After Plan 05 lands, the `as unknown as CatalogTasteAttributes` casts in the new test files can be cleaned up (purely mechanical) — flagged so the consuming wave does not miss it.
- No production code was modified; the rest of the phase is unblocked.

## Self-Check: PASSED

- All 3 created files confirmed on disk via `[ -f ]`
- All 3 task commits (`113f8dc`, `1da116f`, `1419b00`) confirmed in `git log --oneline --all`
- Per-task `<acceptance_criteria>` rerun:
  - Task 1: file exists, contains `maybe = describe.skip` literal, 3 `it()` cases, imports `getWatchesByUser`/`updateCatalogTaste`, vitest skips cleanly (no DATABASE_URL) — PASS
  - Task 2: file exists, imports `existsSync`, asserts `false`, vitest exits non-zero, parses — PASS
  - Task 3: file exists, imports `render`, counts tbody rows to 5, asserts "Archetype" absent, vitest exits non-zero — PASS
- Plan-level `<verification>` rerun: all three test files behave as specified; no tsc errors in any of the three new files

---
*Phase: 49.1-remove-genre-surface*
*Completed: 2026-05-20*
