---
phase: 05
plan: 05
type: execute
wave: 4
depends_on: ["05-01", "05-03", "05-04"]
files_modified:
  - src/store/watchStore.ts
  - src/store/preferencesStore.ts
  - src/lib/hooks/useIsHydrated.ts
autonomous: true
requirements:
  - DATA-05
tags:
  - refactor
  - zustand
  - cleanup
must_haves:
  truths:
    - "watchStore.ts has no persist middleware, no CRUD methods, no watches array — only filter state"
    - "preferencesStore.ts is deleted"
    - "useIsHydrated hook is deleted (all callers removed in Plans 03/04)"
    - "All 7 grep gates from 05-VALIDATION.md return empty output"
    - "npm run build exits 0 and npm test exits 0"
  artifacts:
    - path: "src/store/watchStore.ts"
      provides: "filter-only Zustand store, no persist"
      contains: "WatchFilters"
  key_links:
    - from: "src/components/watch/CollectionView.tsx"
      to: "src/store/watchStore.ts"
      via: "useWatchStore((s) => s.filters)"
      pattern: "useWatchStore"
---

<objective>
Final cleanup: strip persist middleware, watches array, and all CRUD methods from watchStore; delete preferencesStore and useIsHydrated entirely. After this plan, the 7 grep gates from 05-VALIDATION.md all pass and Phase 5's code refactor is complete. Only the OPS-01 runbook execution checkpoint (Plan 06) remains.

Purpose: Plans 01/03/04 left watchStore's CRUD intact so the app could build at every intermediate step. Now that no consumer reads watches or calls CRUD from the store, this plan removes the dead code. The final watchStore matches 05-RESEARCH.md § Pattern 4 exactly.
Output: watchStore.ts reduced to ~30 lines (filter-only); preferencesStore.ts deleted; useIsHydrated.ts deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-migration-zustand-cleanup-similarity-rewire-prod-db-bootstrap/05-CONTEXT.md
@.planning/phases/05-migration-zustand-cleanup-similarity-rewire-prod-db-bootstrap/05-RESEARCH.md
@.planning/phases/05-migration-zustand-cleanup-similarity-rewire-prod-db-bootstrap/05-VALIDATION.md

@src/store/watchStore.ts
@src/store/preferencesStore.ts
@src/lib/hooks/useIsHydrated.ts
@src/lib/filtering.ts

<interfaces>
Target final shape of src/store/watchStore.ts (from 05-RESEARCH.md § Pattern 4):

    import { create } from 'zustand'
    import type { WatchStatus } from '@/lib/types'

    export interface WatchFilters {
      status: 'all' | WatchStatus
      styleTags: string[]
      roleTags: string[]
      dialColors: string[]
      priceRange: { min: number | null; max: number | null }
    }

    interface WatchFilterStore {
      filters: WatchFilters
      setFilter: <K extends keyof WatchFilters>(key: K, value: WatchFilters[K]) => void
      resetFilters: () => void
    }

    const defaultFilters: WatchFilters = {
      status: 'all',
      styleTags: [],
      roleTags: [],
      dialColors: [],
      priceRange: { min: null, max: null },
    }

    export const useWatchStore = create<WatchFilterStore>()((set) => ({
      filters: defaultFilters,
      setFilter: (key, value) =>
        set((state) => ({ filters: { ...state.filters, [key]: value } })),
      resetFilters: () => set({ filters: defaultFilters }),
    }))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip watchStore.ts to filter-only</name>
  <files>src/store/watchStore.ts</files>
  <read_first>
    - src/store/watchStore.ts (current 150-line version with persist, CRUD, watches array)
    - src/lib/filtering.ts (filterWatches already extracted in Plan 01 — NOTE: it imports WatchFilters from '@/store/watchStore', so the WatchFilters interface MUST remain exported)
    - .planning/phases/05-migration-zustand-cleanup-similarity-rewire-prod-db-bootstrap/05-RESEARCH.md § Pattern 4 (Demoted Zustand Store — Filter Only) — this is the exact target shape
    - .planning/phases/05-migration-zustand-cleanup-similarity-rewire-prod-db-bootstrap/05-VALIDATION.md § Grep-Verifiable Acceptance Criteria (#1)
  </read_first>
  <action>
    Replace the entire contents of src/store/watchStore.ts with the target shape from 05-RESEARCH.md § Pattern 4 (reproduced in the interfaces block above). Specifically remove:

    - import { persist } from 'zustand/middleware'
    - The watches: Watch[] state slice
    - addWatch, updateWatch, deleteWatch, markAsWorn methods
    - getWatchById selector
    - getFilteredWatches selector
    - The generateId helper (no longer used in the store)
    - The persist(...) wrapper including name, version, partialize, migrate

    Keep:
    - The WatchFilters interface EXPORT (filterWatches in src/lib/filtering.ts imports it)
    - The defaultFilters const
    - filters, setFilter, resetFilters

    After the edit, the file is approximately 30 lines. Verify by running npm run build immediately.
  </action>
  <verify>
    <automated>bash -c "grep -E 'persist|addWatch|deleteWatch|markAsWorn|updateWatch|watches\\s*:' src/store/watchStore.ts; test \$? -ne 0 && grep -q 'export interface WatchFilters' src/store/watchStore.ts && npm run build"</automated>
  </verify>
  <acceptance_criteria>
    - grep -E "persist|addWatch|deleteWatch|markAsWorn|updateWatch|watches\s*:" src/store/watchStore.ts returns NO output (05-VALIDATION.md grep gate #1)
    - grep -q "export interface WatchFilters" src/store/watchStore.ts succeeds (filterWatches still imports it)
    - grep -q "zustand/middleware" src/store/watchStore.ts returns NO output
    - npm run build exits 0
  </acceptance_criteria>
  <done>watchStore is reduced to filter-only shape matching 05-RESEARCH.md § Pattern 4. WatchFilters interface still exported. Build clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete preferencesStore.ts and useIsHydrated.ts</name>
  <files>src/store/preferencesStore.ts, src/lib/hooks/useIsHydrated.ts</files>
  <read_first>
    - src/store/preferencesStore.ts (current 47-line file — confirm nothing outside Plans 03/04 still imports it)
    - src/lib/hooks/useIsHydrated.ts (confirm file exists; per 05-RESEARCH.md Pitfall 7, all 5 callers were removed in Plans 03/04)
  </read_first>
  <action>
    Step A — Before deleting, sanity-check no imports remain:

        grep -rn "usePreferencesStore" src/
        # expected: no output — every caller was removed in Plans 01, 03, 04

        grep -rn "useIsHydrated" src/
        # expected: no output — all 5 callers were converted to Server Components in Plans 03/04

    If either grep returns any result, STOP and fix the offending import (remove or rewire) before deleting. Do not delete files with live importers.

    Step B — Delete the files:

        rm src/store/preferencesStore.ts
        rm src/lib/hooks/useIsHydrated.ts

    If src/lib/hooks/ becomes empty after the deletion, also remove the empty directory.

    Step C — Verify the build is clean:

        npm run build

    If build fails with a "module not found" error, the Step A grep missed something — find the importer and fix it before retrying.

    Step D — Run the full 05-VALIDATION.md grep gate suite to confirm Phase 5 code refactor is complete:

        grep -E "persist|addWatch|deleteWatch|markAsWorn|updateWatch|watches\s*:" src/store/watchStore.ts   # empty
        grep "'use client'" src/app/insights/page.tsx                                                       # empty
        grep -E "useWatchStore|usePreferencesStore" src/components/insights/SimilarityBadge.tsx             # empty
        grep -rn "useIsHydrated" src/app/                                                                   # empty
        grep "'use client'" src/app/page.tsx src/app/insights/page.tsx src/app/preferences/page.tsx         # empty
        npm run build                                                                                       # exit 0
        npm test -- --run src/lib/similarity                                                                # exit 0

    All seven must pass. If any fails, fix before marking the task done.
  </action>
  <verify>
    <automated>bash -c "! test -f src/store/preferencesStore.ts && ! test -f src/lib/hooks/useIsHydrated.ts && ! grep -rn 'usePreferencesStore' src/ && ! grep -rn 'useIsHydrated' src/ && npm run build && npm test -- --run src/lib/similarity"</automated>
  </verify>
  <acceptance_criteria>
    - src/store/preferencesStore.ts does NOT exist
    - src/lib/hooks/useIsHydrated.ts does NOT exist
    - grep -rn "usePreferencesStore" src/ returns NO output
    - grep -rn "useIsHydrated" src/ returns NO output (05-VALIDATION.md grep gate #4 fully passes)
    - All 7 grep gates from 05-VALIDATION.md return the expected empty output
    - npm run build exits 0
    - npm test -- --run src/lib/similarity exits 0
  </acceptance_criteria>
  <done>Dead stores and hook deleted. All 05-VALIDATION.md grep gates pass. Phase 5 code refactor portion is complete — only OPS-01 execution checkpoint (Plan 06) remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage (legacy watch-collection, user-preferences) | Per D-02, orphaned keys remain in user browsers; no code reads or writes them after this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | I (Information Disclosure) | orphaned localStorage | accept | Per D-02, legacy localStorage keys (watch-collection, user-preferences) are intentionally left orphaned. They are accessible only from the user's own browser, same-origin, and contain data the user already owned. No remote exposure. User can clear site data manually if desired. Acceptable because the single developer-user has no legacy data to leak. |
| T-05-05-02 | T (Tampering) | deleted store files with live importers | mitigate | Task 2 Step A runs explicit grep checks before deletion. If any importer remains, the task STOPS and forces a fix before rm. Step C verifies build afterward. Guards against silently breaking the app. |
| T-05-05-03 | D (Denial of Service) | build regression | mitigate | Task verify runs npm run build and the full test suite after deletion. Any TypeScript error from a missed importer surfaces immediately. |
</threat_model>

<verification>
- watchStore.ts is filter-only (grep gate #1 passes)
- preferencesStore.ts and useIsHydrated.ts are deleted
- All 7 grep gates from 05-VALIDATION.md pass
- npm run build exits 0
- npm test -- --run src/lib/similarity exits 0
</verification>

<success_criteria>
- Phase 5 DATA-05 refactor is structurally complete: store demoted, persist removed, similarity engine reads props
- All grep-verifiable acceptance criteria from 05-VALIDATION.md pass
- The app builds clean and the similarity regression suite is green
- Phase 5 remaining work: Plan 06 (OPS-01 runbook execution checkpoint)
</success_criteria>

<output>
After completion, create .planning/phases/05-migration-zustand-cleanup-similarity-rewire-prod-db-bootstrap/05-05-SUMMARY.md
</output>
