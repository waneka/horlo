---
phase: 64-detail-page-ia-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/watch/SpecsSublabel.tsx
  - src/components/comment/CommentThread.tsx
  - tests/static/watch-detail-ia-order.test.ts
  - tests/static/comment-thread-no-client.test.ts
  - tests/static/ppr-dynamic-before-use-cache.test.ts
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03, PAGE-04]

must_haves:
  truths:
    - "The jump-to-comments anchor has a stable DOM target (CommentThread section has id=comments)"
    - "SpecsSublabel is importable as a shared RSC-compatible component from both the hero island and Branch 3"
    - "The CommentThread privacy invariant (no use-client, no use-cache) is enforced by an automated CI guard"
    - "The IA child-order guard exists and asserts hero before comments before trailing before rails (RED until Plan 04 lands page.tsx + Hero)"
    - "The pre-existing PPR ordering guard genuinely matches the route admin client (createSupabaseAdminClient), not a vacuous createSupabaseServerClient pattern"
  artifacts:
    - path: "src/components/watch/SpecsSublabel.tsx"
      provides: "Shared one-line spec strip (movement, case mm, dial)"
      contains: "export function SpecsSublabel"
    - path: "tests/static/comment-thread-no-client.test.ts"
      provides: "PAGE-03 privacy guard for CommentThread"
      contains: "@vitest-environment node"
    - path: "tests/static/watch-detail-ia-order.test.ts"
      provides: "PAGE-01/02/03/04 IA child-order + B1-invariant guard"
      contains: "@vitest-environment node"
  key_links:
    - from: "src/components/comment/CommentThread.tsx"
      to: "WatchDetailHero jump anchor (Plan 02)"
      via: "id=comments on the wrapping section"
      pattern: "section id=comments"
    - from: "tests/static/ppr-dynamic-before-use-cache.test.ts"
      to: "src/app/w/[ref]/page.tsx admin-client ordering"
      via: "createSupabaseAdminClient before getLikesForTargetCached"
      pattern: "createSupabaseAdminClient"
---

<objective>
Build the Wave-1 foundations for the Phase 64 IA redesign: extract the shared SpecsSublabel component, give CommentThread a stable anchor id, and stand up the static validation guards (including repairing the pre-existing vacuous PPR ordering guard).

Purpose: These are the shared dependencies and the CI safety net that every subsequent plan relies on. The two new IA guards will be RED until Plan 04 wires page.tsx and the Hero island lands in Plan 02 — that is the intended TDD ordering (the IA guard file references WatchDetailHero.tsx, which does not exist yet). The privacy guard, the SpecsSublabel extraction, and the repaired PPR guard are GREEN immediately.
Output: SpecsSublabel.tsx, CommentThread.tsx (one-attribute change), two new static guards, and a corrected ppr-dynamic-before-use-cache.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-detail-page-ia-redesign/64-CONTEXT.md
@.planning/phases/64-detail-page-ia-redesign/64-RESEARCH.md
@.planning/phases/64-detail-page-ia-redesign/64-PATTERNS.md
@.planning/phases/64-detail-page-ia-redesign/64-VALIDATION.md

<interfaces>
SpecsSublabel current location: src/app/w/[ref]/page.tsx lines 752-768 (module-level, NOT exported).
Props: movement string|null, caseSizeMm number|null, dialColor string|null.
Renders a single muted paragraph joining the non-empty parts with a middot; returns null if all parts empty.

CommentThread anchor target: src/components/comment/CommentThread.tsx line ~66 currently the wrapping section with className mt-6. Keep the CRITICAL comment at lines 1-3 untouched.

Existing static-guard analog: tests/static/ppr-dynamic-before-use-cache.test.ts — copy its vitest-environment node header (lines 1-8), the node:fs/node:path imports, and the activeLineNumbers helper verbatim.
Privacy-guard analog: tests/static/legacy-watch-routes.test.ts — node-env header + readFileSync structure.

KNOWN PRE-EXISTING BUG (RESEARCH Pitfall 8 + VALIDATION Wave-0 audit, VERIFIED RED 2026-05-27): ppr-dynamic-before-use-cache.test.ts greps for createSupabaseServerClient but page.tsx uses createSupabaseAdminClient for all photo signing. The pattern matches ZERO active lines, so getLikesForTargetCached at line 191 has no preceding match and the test currently FAILS. The guard intent (signing client called before the cached helper in each branch) is correct; only the pattern string is wrong.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SpecsSublabel to a shared RSC-compatible component</name>
  <files>src/components/watch/SpecsSublabel.tsx, src/app/w/[ref]/page.tsx</files>
  <read_first>
    - src/app/w/[ref]/page.tsx lines 745-768 (the current SpecsSublabel function body + its Branch 3 usage site)
    - .planning/phases/64-detail-page-ia-redesign/64-PATTERNS.md SpecsSublabel section (Pattern 4 — exact source to copy)
  </read_first>
  <action>
    Create src/components/watch/SpecsSublabel.tsx with NO use-client directive (pure render function, legal to import from both RSC and client contexts). Copy the function body verbatim from page.tsx lines 752-768 and add the export keyword so it reads export function SpecsSublabel. Props: movement string|null, caseSizeMm number|null, dialColor string|null. Returns null when no parts present; otherwise the single muted paragraph joining parts with a middot separator.
    Then in page.tsx: DELETE the now-duplicated module-level SpecsSublabel function (lines ~752-768) and add import of SpecsSublabel from @/components/watch/SpecsSublabel to the import block. Leave the Branch 3 usage call site exactly where it is — it resolves to the imported component. Do NOT touch any other part of page.tsx in this task (branch reordering is Plan 04).
  </action>
  <verify>
    <automated>grep -q "export function SpecsSublabel" src/components/watch/SpecsSublabel.tsx && ! grep -q "use client" src/components/watch/SpecsSublabel.tsx && grep -q "SpecsSublabel } from '@/components/watch/SpecsSublabel'" "src/app/w/[ref]/page.tsx" && [ "$(grep -c 'function SpecsSublabel' 'src/app/w/[ref]/page.tsx')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - src/components/watch/SpecsSublabel.tsx contains the exact string: export function SpecsSublabel
    - src/components/watch/SpecsSublabel.tsx does NOT contain a use-client directive
    - src/app/w/[ref]/page.tsx imports SpecsSublabel from @/components/watch/SpecsSublabel
    - grep -c "function SpecsSublabel" on page.tsx returns 0 (no local duplicate remains)
    - npm run build exits 0 (Branch 3 still renders the spec strip via the imported component)
  </acceptance_criteria>
  <done>SpecsSublabel is a standalone exported RSC-compatible component imported by page.tsx; no local duplicate remains; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add comments anchor id, author two static guards, repair the PPR guard pattern</name>
  <files>src/components/comment/CommentThread.tsx, tests/static/comment-thread-no-client.test.ts, tests/static/watch-detail-ia-order.test.ts, tests/static/ppr-dynamic-before-use-cache.test.ts</files>
  <read_first>
    - src/components/comment/CommentThread.tsx (full — confirm the wrapping section line and that the CRITICAL comment lines 1-3 stay untouched)
    - tests/static/ppr-dynamic-before-use-cache.test.ts (full — copy node-env header + activeLineNumbers helper; locate the createSupabaseServerClient pattern to repair)
    - tests/static/legacy-watch-routes.test.ts (node-env header + readFileSync structure for the privacy guard)
    - .planning/phases/64-detail-page-ia-redesign/64-PATTERNS.md watch-detail-ia-order.test.ts section and comment-thread-no-client.test.ts section (exact assertions to implement)
    - .planning/phases/64-detail-page-ia-redesign/64-VALIDATION.md Wave 0 Requirements (the three deliverables + the Pitfall-8 audit)
  </read_first>
  <action>
    (a) In CommentThread.tsx, change the wrapping section (className mt-6) to add id="comments" before the className attribute. Change NOTHING else — the file MUST keep zero use-client and zero use-cache directives and stay export async function CommentThread. Do NOT alter the CRITICAL comment at lines 1-3.

    (b) Create tests/static/comment-thread-no-client.test.ts whose FIRST line is exactly the vitest-environment node directive comment (MEMORY project_vitest_static_node_env — without it the Vercel prebuild fails even though jsdom passes locally). Use readFileSync to load src/components/comment/CommentThread.tsx. Assert: the first 5 lines do NOT match a use-client directive; the first 5 lines do NOT match a use-cache directive; the full file matches export async function CommentThread. Slice to the first 5 lines for the directive checks so the CRITICAL prose comment (which names the strings) does not false-positive.

    (c) Create tests/static/watch-detail-ia-order.test.ts whose FIRST line is exactly the vitest-environment node directive comment. Copy the activeLineNumbers helper from the PPR guard. Read src/app/w/[ref]/page.tsx and src/components/watch/WatchDetailHero.tsx. Using activeLineNumbers against page.tsx assert: first WatchDetailHero line is less than first CommentThread line; first CommentThread line is less than first WatchDetailTrailing line; first WatchDetailTrailing line is less than first SameFamilyRail line. Against WatchDetailHero.tsx assert: source does NOT match a CommentThread import (B1 invariant, PAGE-03); source DOES match WatchPhotoSection (PAGE-04). This guard is EXPECTED to be RED until Plan 02 (WatchDetailHero.tsx) and Plan 04 (page.tsx reorder) land. Guard the readFileSync of the hero file with try/catch so an absent file yields a clean descriptive assertion failure (message: WatchDetailHero.tsx not found, lands in Plan 02) rather than an unhandled ENOENT throw.

    (d) Repair the pre-existing PPR guard (Pitfall 8): in tests/static/ppr-dynamic-before-use-cache.test.ts, change the dynamic-call pattern from createSupabaseServerClient to createSupabaseAdminClient so it matches the actual photo-signing client used in page.tsx. Update the surrounding describe/it titles and message strings to say createSupabaseAdminClient. Keep MAX_LOOKAHEAD and the ordering logic (admin client must appear before each getLikesForTargetCached call within the window). Do NOT change the getLikesForTargetCached pattern. After repair this guard must turn GREEN against the current page.tsx (admin client at ~163/184/444/499 precedes the cached calls at ~191/243/458/504).
  </action>
  <verify>
    <automated>grep -q 'section id="comments"' src/components/comment/CommentThread.tsx && head -1 tests/static/comment-thread-no-client.test.ts | grep -q "vitest-environment node" && head -1 tests/static/watch-detail-ia-order.test.ts | grep -q "vitest-environment node" && grep -q "createSupabaseAdminClient" tests/static/ppr-dynamic-before-use-cache.test.ts && npx vitest run tests/static/ppr-dynamic-before-use-cache.test.ts tests/static/comment-thread-no-client.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - src/components/comment/CommentThread.tsx contains the exact string: section id="comments"
    - src/components/comment/CommentThread.tsx first 5 lines contain neither a use-client nor a use-cache directive; file still has export async function CommentThread
    - tests/static/comment-thread-no-client.test.ts first line is the vitest-environment node directive comment
    - tests/static/watch-detail-ia-order.test.ts first line is the vitest-environment node directive comment and asserts hero-before-comments-before-trailing-before-rails plus the B1 no-CommentThread-import + WatchPhotoSection-present checks
    - tests/static/ppr-dynamic-before-use-cache.test.ts greps createSupabaseAdminClient (not createSupabaseServerClient) and PASSES against current page.tsx
    - npx vitest run tests/static/ppr-dynamic-before-use-cache.test.ts tests/static/comment-thread-no-client.test.ts exits 0
    - tests/static/watch-detail-ia-order.test.ts is RED at this point (expected — references WatchDetailHero.tsx from Plan 02 + reordered page.tsx from Plan 04); it must report a clean assertion failure, not crash
  </acceptance_criteria>
  <done>CommentThread has the id=comments anchor target; both new node-env static guards exist; the privacy guard + repaired PPR guard are GREEN; the IA-order guard is RED-by-design until Plans 02 and 04 land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor refactor → CommentThread privacy contract | A refactor could accidentally add 'use cache' (making comments cacheable) or 'use client' (stripping the async RSC privacy guarantee) |
| static guard pattern → route reality | A guard that greps a string the route does not actually use is vacuously green and silently stops protecting the invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Information Disclosure | CommentThread.tsx | mitigate | comment-thread-no-client.test.ts asserts no 'use cache' / 'use client' in the directive zone — turns the prose CRITICAL comment into an enforced CI guard (RESEARCH Security Domain row 2) |
| T-64-02 | Tampering | ppr-dynamic-before-use-cache.test.ts | mitigate | repair the vacuous createSupabaseServerClient pattern → createSupabaseAdminClient so the admin-client-before-cached-call ordering is genuinely enforced (Pitfall 8) |
| T-64-03 | Information Disclosure | IDOR on photo signing | accept (preserve existing control) | this plan touches no signing code; admin-client + {userId}/ storage paths unchanged; ordering guard above keeps the admin call positioned correctly |
</threat_model>

<verification>
- `npx vitest run tests/static/ppr-dynamic-before-use-cache.test.ts tests/static/comment-thread-no-client.test.ts --reporter=verbose` → exit 0 (both green)
- `npx vitest run tests/static/watch-detail-ia-order.test.ts` → RED with a descriptive assertion failure (expected; closes GREEN after Plans 02 + 04)
- `npm run build` → exit 0
</verification>

<success_criteria>
- SpecsSublabel extracted, exported, imported by page.tsx; no local duplicate; build green.
- CommentThread carries id="comments" and retains zero cache/client directives.
- Privacy guard + repaired PPR guard are GREEN; IA-order guard is RED-by-design.
</success_criteria>

<output>
After completion, create `.planning/phases/64-detail-page-ia-redesign/64-01-SUMMARY.md`
</output>
