---
phase: 64-detail-page-ia-redesign
plan: 03
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - src/components/watch/WatchDetailTrailing.tsx
autonomous: true
requirements: [PAGE-01, PAGE-03]

must_haves:
  truths:
    - "The trailing content (four full spec cards, gap-fill callout, notes) is a pure RSC with no 'use client' and no hooks/event handlers"
    - "computeGapFill runs inside the RSC (pure function; verified RSC-safe — gapFill.ts imports only types + detectLoyalBrands from similarity, which imports only types)"
    - "The Tracking card dates use toLocaleDateString en-US with timeZone UTC (React #418)"
    - "The trailing component can render AFTER CommentThread in the server tree, enabling comments-above-specs without CSS-order tricks (D-07)"
  artifacts:
    - path: "src/components/watch/WatchDetailTrailing.tsx"
      provides: "RSC trailing spec cards + gap-fill + notes for Branches 1 and 2-D06"
      contains: "export function WatchDetailTrailing"
  key_links:
    - from: "src/components/watch/WatchDetailTrailing.tsx"
      to: "src/lib/gapFill.ts"
      via: "computeGapFill(watch, collection, preferences) for wishlist/grail"
      pattern: "computeGapFill"
---

<objective>
Create the WatchDetailTrailing RSC by extracting the trailing portion of WatchDetail.tsx (lines 336-564): the four full spec cards (Specifications, Pricing, Classification, Tracking), the gap-fill callout, and the Notes card. This is pure display content with no hooks and no event handlers, so it becomes a Server Component — which is what lets CommentThread render ABOVE it in the server tree (D-07) without any CSS-order trickery.

Purpose: Splitting these cards out of the monolithic client island is the structural move that satisfies PAGE-02 (comments not buried). The trailing RSC also shrinks the client bundle. RESEARCH confirms computeGapFill and formatDate are pure and RSC-safe (gapFill.ts imports only types + detectLoyalBrands from similarity.ts, which imports only types — VERIFIED 2026-05-27).
Output: src/components/watch/WatchDetailTrailing.tsx (new RSC). The page.tsx wiring that renders it after CommentThread is Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-detail-page-ia-redesign/64-CONTEXT.md
@.planning/phases/64-detail-page-ia-redesign/64-PATTERNS.md
@.planning/phases/64-detail-page-ia-redesign/64-UI-SPEC.md

<interfaces>
Analog source (extract trailing subset): src/components/watch/WatchDetail.tsx lines 336-564.
  - Specifications card: lines 338-416 (dl grid grid-cols-2; movement via MOVEMENT_LABELS, caseSizeMm, etc.)
  - Pricing card: lines 418-443.
  - Classification/Tags card: lines 446-484.
  - Tracking card: lines 487-512 (uses formatDate(watch.acquisitionDate) and formatDate(lastWornDate ?? undefined) — MUST keep timeZone UTC; uses daysSince).
  - Gap-fill card: lines 516-544 (gated on wishlist/grail; renders computeGapFill result kinds).
  - Notes card: lines 549-562.

RSC shell analog: src/components/insights/OtherOwnersRoster.tsx (no 'use client', named export, typed props interface).
Pure-function deps (RSC-safe, VERIFIED): computeGapFill from @/lib/gapFill; daysSince from @/lib/wear; MOVEMENT_LABELS from @/lib/constants.
formatDate: copy verbatim from WatchDetail.tsx 106-119 (keep timeZone UTC) — define locally in this file.

UI-SPEC locked decisions (read 64-UI-SPEC.md):
  - D-11 gap-fill stays here (below comments, before Notes) — NOT in the hero.
  - Sub-ordering within trailing: Specifications → Pricing → Classification → Tracking → Gap-fill → Notes (preserve the current WatchDetail order).
  - Wrap in a single container (e.g. space-y-6) matching the existing card rhythm.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WatchDetailTrailing RSC</name>
  <files>src/components/watch/WatchDetailTrailing.tsx</files>
  <behavior>
    - The file has NO 'use client' directive anywhere.
    - It exports a function WatchDetailTrailing taking { watch, collection, preferences, lastWornDate }.
    - computeGapFill is called only for wishlist/grail status, inside the RSC.
    - The Tracking card formats dates with toLocaleDateString en-US timeZone UTC.
    - It renders the four spec cards, the gap-fill callout, and the Notes card — and nothing interactive (no useState/useTransition/onClick).
  </behavior>
  <read_first>
    - src/components/watch/WatchDetail.tsx (full — the source to extract lines 336-564; confirm each card's exact JSX, the gap-fill kinds, the Tracking-card formatDate calls, and the isWishlistLike condition)
    - .planning/phases/64-detail-page-ia-redesign/64-PATTERNS.md WatchDetailTrailing.tsx section (RSC shell pattern, formatDate pattern with timeZone UTC, specs card pattern, gap-fill card pattern) and the Date Formatting shared pattern
    - src/lib/gapFill.ts (confirm computeGapFill signature + the GapFill result kinds rendered by the gap-fill card)
    - src/components/insights/OtherOwnersRoster.tsx (the RSC shell / named-export / props-interface convention to mirror)
  </read_first>
  <action>
    Create src/components/watch/WatchDetailTrailing.tsx with NO 'use client' directive (pure RSC). Imports: Card/CardContent/CardDescription/CardHeader/CardTitle and Badge from the ui set, MOVEMENT_LABELS from @/lib/constants, computeGapFill from @/lib/gapFill, daysSince from @/lib/wear, and the Watch / UserPreferences types from @/lib/types. Define a local formatDate copied verbatim from WatchDetail.tsx 106-119 (keep the timeZone UTC pinning and the explanatory #418 comment).
    Define interface WatchDetailTrailingProps { watch: Watch; collection: Watch[]; preferences: UserPreferences; lastWornDate?: string | null }. In the body compute isWishlistLike (status wishlist or grail), gapFill = isWishlistLike ? computeGapFill(watch, collection, preferences) : null, and daysSinceWorn = daysSince(lastWornDate ?? undefined) where the current Tracking card uses it.
    Return a single container div (space-y-6) rendering, in order: the Specifications card (copy WatchDetail.tsx 338-416), the Pricing card (418-443), the Classification/Tags card (446-484), the Tracking card (487-512 — keep both formatDate calls and the daysSince usage), the Gap-fill card (516-544 — keep all gap-fill kind branches), and the Notes card (549-562). Copy each card's JSX verbatim; the only change is they now live in an RSC with props passed in instead of reading from the client-island closure. Do NOT add any hooks, event handlers, or 'use client'. Do NOT include the verdict (it lives in the hero, Plan 02) or the photo/title/like/owner-action blocks (hero).
  </action>
  <verify>
    <automated>! grep -q "use client" src/components/watch/WatchDetailTrailing.tsx && grep -q "export function WatchDetailTrailing" src/components/watch/WatchDetailTrailing.tsx && grep -q "computeGapFill" src/components/watch/WatchDetailTrailing.tsx && grep -q "timeZone: 'UTC'" src/components/watch/WatchDetailTrailing.tsx && ! grep -qE "useState|useTransition|onClick" src/components/watch/WatchDetailTrailing.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/watch/WatchDetailTrailing.tsx contains NO use-client directive (grep "use client" returns nothing)
    - File contains export function WatchDetailTrailing
    - File contains computeGapFill (gap-fill computed in the RSC)
    - File contains timeZone: 'UTC' (Tracking card #418 safety)
    - File contains NONE of: useState, useTransition, onClick (pure RSC, no interactivity)
    - File renders all four spec cards + gap-fill + Notes; it does NOT contain CollectionFitCard or LikeButton or WatchPhotoSection (those belong to the hero)
    - npm run build exits 0 (the RSC compiles; unreferenced until Plan 04 — fine for build)
  </acceptance_criteria>
  <done>WatchDetailTrailing is a pure RSC carrying the four spec cards, gap-fill, and Notes, with #418-safe Tracking dates and RSC-safe computeGapFill; it can be rendered after CommentThread in Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC display content → no interactive surface | The trailing cards are read-only; no mutation entry point should exist here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-07 | Tampering | WatchDetailTrailing | accept | pure display RSC; no event handlers or Server Actions are introduced — there is no mutation surface to abuse; the no-useState/useTransition/onClick assertion enforces this |
| T-64-08 | Information Disclosure | WatchDetailTrailing data props | accept (preserve existing control) | the RSC receives the same watch/collection/preferences already resolved server-side in page.tsx; no new data fetch, no signing, no request-time API moves into this component |
</threat_model>

<verification>
- `npm run build` → exit 0.
- The WatchDetailTrailing-before-SameFamilyRail assertion in tests/static/watch-detail-ia-order.test.ts is satisfied once Plan 04 wires page.tsx (this plan only creates the component).
- No new `human_needed` items — visual placement of the trailing section is covered by the Plan 04 deploy verification.
</verification>

<success_criteria>
- WatchDetailTrailing RSC exists: four spec cards + gap-fill + notes, no 'use client', no hooks, #418-safe Tracking dates, RSC-safe computeGapFill.
- Build green.
</success_criteria>

<output>
After completion, create `.planning/phases/64-detail-page-ia-redesign/64-03-SUMMARY.md`
</output>
