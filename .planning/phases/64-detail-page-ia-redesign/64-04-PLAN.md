---
phase: 64-detail-page-ia-redesign
plan: 04
type: execute
wave: 3
depends_on: [64-01, 64-02, 64-03]
files_modified:
  - src/app/w/[ref]/page.tsx
autonomous: false
requirements: [PAGE-01, PAGE-02, PAGE-03, PAGE-04]

must_haves:
  truths:
    - "Branch 1 (per-user) renders in canonical order: WatchDetailHero → Suspense CommentThread → WatchDetailTrailing → SameFamilyRail → LineageRail → footer/fresh-account CTAs"
    - "Branch 2-D06 (owner via catalogId) renders the same hero → comments → trailing → rails order"
    - "Branch 3 (pure catalog) renders the shell-only verdict-forward hero with OtherOwnersRoster + CatalogPageActions surfaced HIGH near the verdict, no comments, no multi-photo carousel"
    - "CommentThread stays an uncached async RSC Suspense sibling — never imported into a client island"
    - "unstable_instant = false stays at module scope and await connection() stays first in the page default export body"
    - "Storage URL signing stays on createSupabaseAdminClient, never the cookie client, positioned before each getLikesForTargetCached call"
    - "WatchPageSkeleton mirrors the new IA (hero grid + comment skeleton + spec-card skeleton)"
  artifacts:
    - path: "src/app/w/[ref]/page.tsx"
      provides: "All three render branches re-ordered to the canonical IA + updated skeleton"
      contains: "WatchDetailHero"
  key_links:
    - from: "src/app/w/[ref]/page.tsx Branch 1+2"
      to: "WatchDetailHero, CommentThread, WatchDetailTrailing"
      via: "server-tree child order hero → comments → trailing → rails"
      pattern: "WatchDetailHero"
    - from: "src/app/w/[ref]/page.tsx Branch 3"
      to: "OtherOwnersRoster + CatalogPageActions"
      via: "moved high near the verdict (D-13)"
      pattern: "OtherOwnersRoster"
    - from: "src/app/w/[ref]/page.tsx"
      to: "PPR #419 fix"
      via: "unstable_instant=false + await connection() first"
      pattern: "unstable_instant = false"
---

<objective>
Re-order the server-tree children of all three render branches in src/app/w/[ref]/page.tsx to the canonical IA, swap the monolithic WatchDetail island for the new WatchDetailHero + WatchDetailTrailing split, surface OtherOwnersRoster + CatalogPageActions high in Branch 3, and update WatchPageSkeleton to mirror the new layout — all while leaving the Phase 51/52 Cache Components plumbing byte-for-byte intact.

Purpose: This is the wiring that turns the Wave-2 components into the actual intentional hierarchy (PAGE-01/02/04) and resolves the two Phase-64 TODO comments in Branch 3 (D-13). It is the load-bearing plan: the #419 soft-nav fix, the comment privacy guarantee, and the admin-client signing order must survive unchanged (PAGE-03 / D-08).
Output: a fully re-ordered page.tsx whose IA child-order static guard (from Plan 01) turns GREEN, plus a prod human-verify checkpoint for the responsive/scroll behaviors that cannot be checked locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-detail-page-ia-redesign/64-CONTEXT.md
@.planning/phases/64-detail-page-ia-redesign/64-RESEARCH.md
@.planning/phases/64-detail-page-ia-redesign/64-PATTERNS.md
@.planning/phases/64-detail-page-ia-redesign/64-UI-SPEC.md
@.planning/phases/64-detail-page-ia-redesign/64-VALIDATION.md

<interfaces>
Route file: src/app/w/[ref]/page.tsx (768 lines pre-Plan-01; SpecsSublabel already extracted in Plan 01).

LOAD-BEARING — DO NOT MOVE OR ALTER (PAGE-03 / D-08; MEMORY project_ppr_dynamic_before_use_cache):
  - export const unstable_instant = false  (module scope, ~line 47)
  - await connection()  (FIRST statement in the UnifiedWatchPage default export body, ~line 93), followed by the Suspense fallback={WatchPageSkeleton} wrapping UnifiedWatchContent.
  - createSupabaseAdminClient() photo-signing calls (~163, ~184, ~444, ~499) MUST stay BEFORE their branch's getLikesForTargetCached calls (~191, ~243, ~458, ~504). The Plan-01-repaired guard enforces this.
  - Never import CommentThread into any 'use client' island.

Branch landmarks (current):
  - Branch 1 (per-user) container at ~305: WatchDetail (306) → ReferenceIdentityCard/caption (~328) → SameFamilyRail (353) → LineageRail (354) → Suspense CommentThread (360-371, LAST) → 3-CTA block.
  - Branch 2-D06 (owner via catalogId) container at ~551: WatchDetail (554) → SameFamilyRail (591) → LineageRail (592) → Phase-64 TODO (595) → Suspense CommentThread (597-608, LAST) → CTAs.
  - Branch 3 (pure catalog) container at ~657 (space-y-6): hero div + h1 + SpecsSublabel → CollectionFitCard (686) → ReferenceIdentityCard/caption → Phase-64 TODO (705) → OtherOwnersRoster (708) → SameFamilyRail (712) → LineageRail (713) → CatalogPageActions (717, LAST).

New components (Wave 2): WatchDetailHero (client island, Plan 02) and WatchDetailTrailing (RSC, Plan 03). Hero prop mapping is documented in 64-PATTERNS.md page.tsx section (Branch 1 new child order block).

UI-SPEC locked order (read 64-UI-SPEC.md Information Architecture Contract + Branch 3 + WatchPageSkeleton sections):
  Branches 1 & 2: WatchDetailHero → Suspense(CommentThreadSkeleton fallback){CommentThread} → WatchDetailTrailing → SameFamilyRail → LineageRail → fresh-account CTAs (page bottom).
  Branch 3: catalog hero shell (single image + title + ref + SpecsSublabel) → CollectionFitCard (if verdict) → ReferenceIdentityCard/caption empty-state → OtherOwnersRoster (HIGH) → CatalogPageActions (HIGH) → SameFamilyRail → LineageRail. Change container space-y-6 → space-y-8 for parity (D-14). NO CommentThread, NO multi-photo carousel.
  WatchPageSkeleton: hero grid lg:grid-cols-[3fr_2fr] gap-8 (left aspect-[3/4] carousel skeleton; right space-y-4 with brand/model/spec-strip/verdict/like skeletons) → h-32 comment skeleton → h-40 spec-cards skeleton; keep aria-hidden; pure JSX Skeleton primitives only — no dynamic API, no hooks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-order Branch 1 + Branch 2-D06 to hero → comments → trailing → rails and swap in the split components</name>
  <files>src/app/w/[ref]/page.tsx</files>
  <read_first>
    - src/app/w/[ref]/page.tsx (full — the three branches, the load-bearing lines 42-99 header comment block, the current Branch 1 and Branch 2-D06 child order, the exact CommentThread prop list at 360-371 and 597-608, the WatchDetail prop list at 306 and 554)
    - .planning/phases/64-detail-page-ia-redesign/64-PATTERNS.md page.tsx section (Branch 1 new child order block with the exact WatchDetailHero + CommentThread + WatchDetailTrailing prop wiring; the load-bearing DO-NOT-MOVE block)
    - .planning/phases/64-detail-page-ia-redesign/64-RESEARCH.md Server-Tree Reordering branch-by-branch + Cache Components / #419 Preservation + Pitfalls 1, 2, 3
    - node_modules/next/dist/docs (the Cache Components / connection() / Suspense streaming guidance — AGENTS.md mandates reading the relevant Next 16 doc before touching connection()/Suspense)
  </read_first>
  <action>
    In Branch 1 (per-user, container ~305): replace the WatchDetail element with WatchDetailHero, passing the hero prop subset per the 64-PATTERNS.md Branch-1 wiring (watch, verdict, viewerCanEdit=isOwner, viewerId=user.id, initialLikeState={liked: likeState.viewerHasLiked, count: likeState.count}, commentCount, signedPhotos, userId, wearPics, ownerUserId, ownerUsername, viewerAuthor, canCommentOnWears, ownerFollowsViewerForWears, viewerIsFollowingForWears, collection, lastWornDate). Then place the existing Suspense(CommentThreadSkeleton fallback){CommentThread …same props as today} IMMEDIATELY after the hero (moved up from last). Then render WatchDetailTrailing (watch, collection, preferences, lastWornDate) AFTER the CommentThread Suspense. Then SameFamilyRail, then LineageRail. Keep the fresh-account 3-CTA block at the very bottom (after rails). The hero now owns the empty-verdict slot (Plan 02); if a standalone ReferenceIdentityCard/caption empty-state sibling still exists in this branch, remove it so it is not duplicated.
    Apply the IDENTICAL transformation to Branch 2-D06 (owner via catalogId, container ~551): WatchDetailHero → Suspense CommentThread (moved up) → WatchDetailTrailing → SameFamilyRail → LineageRail → CTAs. Resolve the Phase-64 TODO at ~595 by leaving OtherOwnersRoster/CatalogPageActions ABSENT here (correct — this is the owner branch; RESEARCH confirms they are cross-user-only).
    DO NOT touch: export const unstable_instant = false; await connection(); the Suspense+WatchPageSkeleton wrapper in UnifiedWatchPage; any createSupabaseAdminClient() call or its position relative to getLikesForTargetCached. Do NOT import CommentThread into WatchDetailHero. Do NOT use any CSS order/flex-reverse trick — the DOM order must equal the visual order (D-07).
    After editing, run rm -rf .next once before any dev re-check (MEMORY project_turbopack_next_cache_stale_css).
  </action>
  <verify>
    <automated>grep -q "unstable_instant = false" "src/app/w/[ref]/page.tsx" && grep -q "await connection()" "src/app/w/[ref]/page.tsx" && grep -q "WatchDetailHero" "src/app/w/[ref]/page.tsx" && grep -q "WatchDetailTrailing" "src/app/w/[ref]/page.tsx" && npx vitest run tests/static/ppr-dynamic-before-use-cache.test.ts tests/static/watch-detail-ia-order.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx still contains export const unstable_instant = false at module scope and await connection() as the first statement of the page default export body (Pitfall 2 / D-08)
    - Branches 1 and 2 render WatchDetailHero then a Suspense-wrapped CommentThread then WatchDetailTrailing then SameFamilyRail then LineageRail (verified by the Plan-01 IA-order static guard turning GREEN)
    - tests/static/watch-detail-ia-order.test.ts exits 0 (hero < comments < trailing < SameFamilyRail in page.tsx)
    - tests/static/ppr-dynamic-before-use-cache.test.ts exits 0 (admin client still precedes each getLikesForTargetCached within the window)
    - page.tsx contains no flex-col-reverse / order-first / negative-order utility used to reposition comments (Pitfall 3)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Branches 1 and 2-D06 present the canonical hero → comments → trailing → rails → footer order with the split components wired and the Cache Components plumbing untouched; the IA-order and PPR guards are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Re-order Branch 3 to the shell-only catalog hero with roster + actions surfaced high, and update WatchPageSkeleton</name>
  <files>src/app/w/[ref]/page.tsx</files>
  <read_first>
    - src/app/w/[ref]/page.tsx Branch 3 (container ~657-725) and WatchPageSkeleton (~103-114)
    - .planning/phases/64-detail-page-ia-redesign/64-PATTERNS.md page.tsx section (Branch 3 new order block + the WatchPageSkeleton update block)
    - .planning/phases/64-detail-page-ia-redesign/64-UI-SPEC.md Branch 3 — Catalog Shell-Only Variant + WatchPageSkeleton Update sections
  </read_first>
  <action>
    Branch 3 (pure catalog): change the container class from space-y-6 to space-y-8 (D-14 parity). Keep the catalog hero shell as the existing RSC inline div (single image + h1 + reference + SpecsSublabel — no client island, no LikeButton; RESEARCH confirms Branch 3 has no like button today and adding one is out of scope). Keep CollectionFitCard (verdict, if non-null) and the ReferenceIdentityCard/caption empty-state immediately under the hero. Then MOVE OtherOwnersRoster and CatalogPageActions UP to immediately after the verdict/empty-state block (D-13: social proof + add-to-collection CTA near the verdict to drive discovery→collection). Then SameFamilyRail, then LineageRail. This makes CatalogPageActions no longer last. Resolve and delete the Phase-64 TODO comment at ~705 (the redesign now positions roster/actions). Do NOT add a CommentThread or a multi-photo carousel to Branch 3 (out of scope — no per-user target, single catalog image).
    Update WatchPageSkeleton (~103-114) to mirror the new IA per the 64-UI-SPEC WatchPageSkeleton block: a hero grid (lg:grid-cols-[3fr_2fr] gap-8) with a left aspect-[3/4] carousel Skeleton and a right space-y-4 column of brand/model/spec-strip/verdict/like Skeletons, then an h-32 comment Skeleton, then an h-40 spec-cards Skeleton; container mx-auto px-4 py-8 max-w-4xl space-y-8 with aria-hidden. Keep it pure JSX with Skeleton primitives — NO dynamic API (cookies/headers/params), NO 'use client' import, NO hooks (it must remain a valid Suspense fallback under the connection() opt-out).
    Run rm -rf .next before any dev re-check.
  </action>
  <verify>
    <automated>grep -q "max-w-4xl space-y-8" "src/app/w/[ref]/page.tsx" && grep -q "lg:grid-cols-\[3fr_2fr\]" "src/app/w/[ref]/page.tsx" && [ "$(grep -c 'Phase 64 IA redesign will resolve' 'src/app/w/[ref]/page.tsx')" = "0" ] && npx vitest run tests/static/ --reporter=basic && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Branch 3 container uses space-y-8 (D-14 parity)
    - In Branch 3, OtherOwnersRoster and CatalogPageActions render BEFORE SameFamilyRail/LineageRail (surfaced high; CatalogPageActions is no longer last)
    - Branch 3 contains NO CommentThread and NO multi-photo carousel
    - grep -c "Phase 64 IA redesign will resolve" on page.tsx returns 0 (both TODO comments resolved/removed)
    - WatchPageSkeleton uses lg:grid-cols-[3fr_2fr], is aria-hidden, and contains no dynamic API / hooks / 'use client'
    - npx vitest run tests/static/ exits 0 (full static suite green, including both new guards and the repaired PPR guard)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Branch 3 is a coherent shell-only catalog variant with roster + actions surfaced high and no comments/carousel; the skeleton mirrors the new IA; both Phase-64 TODOs are resolved; the full static suite and build are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Prod human-verify of responsive hierarchy + jump-scroll (bundled deploy)</name>
  <what-built>
    The full Phase 64 IA redesign is wired: photo-forward 2-column desktop hero with the elevated verdict, comments directly under the hero, full spec cards below comments, rails below that, footer/CTAs at the bottom; the hero comment count is a tap-to-scroll jump link to #comments; Branch 3 is a shell-only catalog hero with OtherOwnersRoster + CatalogPageActions surfaced high; the loading skeleton mirrors the new layout. The full static suite and npm run build pass locally. Per MEMORY feedback_mobile_ui_verify_on_prod, responsive/scroll/visual behavior verifies on PROD (empty local test DB skips e2e), bundled into one deploy and build-gated first.
  </what-built>
  <how-to-verify>
    1. Confirm npm run build exited 0 locally, then push origin main and wait for the Vercel deploy to finish. Verify on prod AFTER the route cache fills (a cold read can be a false positive for the #419 family — MEMORY project_ppr_dynamic_before_use_cache).
    2. DESKTOP (>= 1024px) — open a populated per-user watch at /w/[ref]: the hero is a 2-column grid (carousel left, verdict + title + like right); the CollectionFitCard verdict reads near the top, not at the bottom; comments appear directly below the hero, ABOVE the full spec cards and rails.
    3. MOBILE (< 1024px width) — the hero collapses to a single column (carousel on top, then title/verdict/like/actions); order remains hero → comments → spec cards → rails → footer.
    4. JUMP LINK — tap the hero comment count; the page smooth-scrolls to the comments section (#comments). On reduced-motion settings it jumps without animation (acceptable).
    5. SOFT-NAV #419 CHECK — navigate to a /w/[ref] page via an in-app link (not a hard refresh), then navigate to another and back; no React #419/404 error appears (the unstable_instant=false + connection() fix must still hold).
    6. CATALOG BRANCH — open a catalog-only /w/[ref] (a ref you do not own, viewed as a catalog entry): the hero reads verdict-forward; Other Owners + the add-to-collection actions sit high near the verdict; there are no comments and no multi-photo carousel.
    7. OWNER ACTIONS — as the owner, Mark-as-Worn / Edit / Delete are present in the hero; as a non-owner viewer they are absent.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any IA/responsive/scroll/#419 issue to open a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| soft-nav request → PPR static shell | A reorder that disturbs unstable_instant/connection() re-introduces the React #419 soft-nav abort on per-viewer-dynamic routes |
| client island import graph → CommentThread privacy | Wiring the hero must not pull CommentThread into a client module |
| admin signing client ordering → cached helper | The signing call must precede getLikesForTargetCached in each branch or the 'use cache' segment can capture request-time state |
| non-owner viewer → owner actions / catalog actions | Owner mutations must stay owner-gated; catalog roster/actions stay cross-user-appropriate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-09 | Denial of Service / availability | page.tsx PPR opt-out | mitigate (preserve existing control) | leave unstable_instant=false + await connection() + the Suspense/WatchPageSkeleton wrapper byte-for-byte; the prod soft-nav check in Task 3 verifies no #419 recurrence (Pitfall 2; MEMORY project_ppr_dynamic_before_use_cache) |
| T-64-10 | Information Disclosure | CommentThread placement | mitigate | CommentThread stays an uncached async RSC Suspense sibling moved up in the server tree; never imported into WatchDetailHero; the Plan-01 privacy + IA guards enforce this |
| T-64-11 | Tampering | admin-client signing order | mitigate | do not move createSupabaseAdminClient calls relative to getLikesForTargetCached; the repaired ppr-dynamic-before-use-cache guard (Plan 01) fails the build if the ordering breaks |
| T-64-12 | Spoofing / Elevation of Privilege | owner actions in reordered hero | mitigate (preserve existing control) | viewerCanEdit=isOwner is threaded into WatchDetailHero (Plan 02); Server Actions double-verify ownership; Task 3 step 7 confirms non-owner viewers see no owner actions |
| T-64-13 | Information Disclosure | IDOR on photo signing | accept (preserve existing control) | no signing logic changes; admin client + {userId}/ scoped storage paths unchanged by the reorder |
</threat_model>

<verification>
- `npx vitest run tests/static/ --reporter=verbose` → exit 0 (all guards green, including watch-detail-ia-order, comment-thread-no-client, and the repaired ppr-dynamic-before-use-cache).
- `npm run build` → exit 0 (the authoritative gate; ignore the ~77 pre-existing tsc test-file errors + 1 pre-existing non-static failure per MEMORY project_baseline_not_green_build_is_gate — the PPR guard that was the known pre-existing static failure is repaired in Plan 01).
- Responsive collapse, jump-scroll, soft-nav #419, catalog-branch layout, and owner-action visibility are `human_needed` on prod (Task 3 checkpoint; MEMORY feedback_mobile_ui_verify_on_prod).
</verification>

<success_criteria>
- All three branches present the canonical IA; comments are reachable directly under the hero; the carousel is the primary hero visual; Branch 3 surfaces roster/actions high with no comments/carousel.
- Cache Components plumbing (unstable_instant/connection()/admin-signing order/uncached CommentThread) is intact and guard-enforced.
- Skeleton mirrors the new IA; full static suite + build green; prod checkpoint approved.
</success_criteria>

<output>
After completion, create `.planning/phases/64-detail-page-ia-redesign/64-04-SUMMARY.md`
</output>
