---
phase: 67
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/actions/watches.ts
  - tests/actions/watches.test.ts
autonomous: true
requirements:
  - CONF-11
tags:
  - server-action
  - catalog
  - addWatch
  - zod

must_haves:
  truths:
    - "`insertWatchSchema` in `src/app/actions/watches.ts:22-68` gains an optional field `catalogId: z.string().uuid().optional()` (D-CONF-11 + Claude's Discretion §schema placement); the derived `updateWatchSchema = insertWatchSchema.partial()` picks it up automatically — `editWatch` ignoring it is a documented no-op (CONTEXT.md)"
    - "When `cleanData.catalogId` is supplied AND `catalogDAL.getCatalogById(cleanData.catalogId)` returns a row, `addWatch` SKIPS the existing `upsertCatalogFromUserInput` call at lines 127-140 and binds `createWatch` to the existing `catalogId` (CONF-11 success criterion 2)"
    - "When `cleanData.catalogId` is supplied AND `getCatalogById` returns null, `addWatch` returns `{ success: false, error: 'Catalog reference not found' }` and DOES NOT fall back to `upsertCatalogFromUserInput`, DOES NOT call `createWatch` (D-09 fail-fast)"
    - "Server-side identity override (D-10): when `catalogId` is supplied AND the catalog row exists, BEFORE `createWatch` runs, `cleanData.brand`, `cleanData.model`, and `cleanData.reference` are overwritten with `catalogRow.brand`, `catalogRow.model`, `catalogRow.reference` — client-supplied identity values are discarded for the identity tuple"
    - "Pitfall 3 mitigated (D-10 + Claude's Discretion §enricher reads): the `enrichTasteAttributes spec: { ... }` block at lines 179-192 reads from `cleanData.brand` / `cleanData.model` / `cleanData.reference` (post-override) — NOT from `parsed.data.brand` / `parsed.data.model` / `parsed.data.reference` (raw client input). All field reads inside the `spec:` block are switched to `cleanData.*`"
    - "D-11 enrichment-skip gate: when `cleanData.catalogId` is supplied AND `catalogRow.styleTags?.length > 0`, BOTH the `enrichTasteAttributes` block AND the `applyUserUploadedPhoto` photo write-through block at lines 154-200 are skipped; ALL OTHER side-effects (logActivity, findOverlapRecipients + logNotification, revalidatePath('/'), revalidatePath('/u/[username]', 'layout'), revalidateTag('profile:${username}', 'max'), revalidateTag('explore', 'max')) ALWAYS run regardless of catalogId-supplied branch"
    - "When `cleanData.catalogId` is supplied AND `catalogRow.styleTags` is empty/null, the enrichment block DOES run (legitimate re-enrichment trigger per D-11)"
    - "5 new integration tests in `tests/actions/watches.test.ts` describe block `'addWatch — catalogId branch (CONF-11)'` cover the five contract cases from CONTEXT.md (a)-(e); all green"
  artifacts:
    - path: "src/app/actions/watches.ts"
      provides: "Extended insertWatchSchema with optional catalogId + addWatch action with catalogId-supplied branch (D-09/D-10/D-11)"
      contains: "catalogId: z.string().uuid().optional()"
    - path: "tests/actions/watches.test.ts"
      provides: "5-case integration test suite for CONF-11 catalogId branch"
      contains: "addWatch — catalogId branch (CONF-11)"
  key_links:
    - from: "src/app/actions/watches.ts addWatch"
      to: "src/data/catalog.ts getCatalogById"
      via: "catalogDAL.getCatalogById(cleanData.catalogId)"
      pattern: "catalogDAL\\.getCatalogById"
    - from: "src/app/actions/watches.ts addWatch enrichTasteAttributes spec"
      to: "cleanData (post-override)"
      via: "spec: { brand: cleanData.brand, ... }"
      pattern: "brand: cleanData\\.brand"
---

<objective>
Extend `addWatch` (`src/app/actions/watches.ts:79-306`) and `insertWatchSchema` (`src/app/actions/watches.ts:22-68`) per CONTEXT.md decisions D-09, D-10, D-11 so the Phase 68 ConfirmStep can call `addWatch(data)` with `catalogId` supplied — binding the new watch row to the existing catalog row instead of upserting a duplicate, AND skipping redundant taste enrichment when the catalog row is already enriched.

**Purpose:** Without `catalogId` passthrough, every "I picked from typeahead" add would still call `upsertCatalogFromUserInput`, defeating the v8.0 search-first dedup premise (a typo'd brand/model could create a NEW catalog row even when the user explicitly picked a row from search). D-10's server-side identity override means the catalog row IS the truth for `(brand, model, reference)` once `catalogId` is supplied. D-11's enrichment-skip avoids a wasteful LLM call + signed-URL roundtrip on adds that pick already-enriched catalog rows.

**Output:** Modified `src/app/actions/watches.ts` (schema field + branched action body) + 5 new integration tests appended to `tests/actions/watches.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/67-server-action-dal-extensions/67-CONTEXT.md
@.planning/phases/67-server-action-dal-extensions/67-RESEARCH.md
@.planning/phases/67-server-action-dal-extensions/67-PATTERNS.md
@.planning/phases/67-server-action-dal-extensions/67-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/data/catalog.ts:254 (the function the new branch calls):
```typescript
export async function getCatalogById(id: string): Promise<CatalogEntry | null>
```

From src/lib/types.ts (CatalogEntry shape — `styleTags` is the D-11 already-enriched signal):
```typescript
// styleTags: string[] (notNull with default '{}'; column on watches_catalog)
```

From src/app/actions/watches.ts:22-68 (`insertWatchSchema` — `catalogId` lands as optional next to `imageUrl` at line 47 per Claude's Discretion):
```typescript
const insertWatchSchema = z.object({
  brand: z.string().min(1, 'Brand is required'),
  model: z.string().min(1, 'Model is required'),
  // ... other fields
  imageUrl: z.string().optional(),
  // INSERT POINT: catalogId: z.string().uuid().optional(),
  // ... rest
})
```

From src/app/actions/watches.ts:114-142 (current insertion point for the catalogId-supplied branch):
```typescript
const { sortOrder: _ignoredSortOrder, ...cleanData } = parsed.data       // line 114
void _ignoredSortOrder
let createPayload: typeof parsed.data = cleanData                         // line 117
if (cleanData.status === 'wishlist' || cleanData.status === 'grail') {    // lines 118-121
  const maxSort = await watchDAL.getMaxWishlistSortOrder(user.id)
  createPayload = { ...cleanData, sortOrder: maxSort + 1 }
}
// INSERT POINT: catalogId-supplied branch goes here (before line 127 upsert)
let catalogIdResult: string | null                                        // line 126
try {
  catalogIdResult = await catalogDAL.upsertCatalogFromUserInput({         // line 128
    brand: parsed.data.brand,
    model: parsed.data.model,
    reference: parsed.data.reference ?? null,
  })
} catch (err) { /* fail-loud */ }
// ...
const watch = await watchDAL.createWatch(user.id, catalogId, createPayload)  // line 142
```

From src/app/actions/watches.ts:174-200 (Pitfall 3 hot spot — switch parsed.data reads to cleanData reads inside the spec):
```typescript
const taste = await enrichTasteAttributes({
  catalogId,
  source: 'manual',
  spec: {
    brand: parsed.data.brand,          // CHANGE → cleanData.brand
    model: parsed.data.model,          // CHANGE → cleanData.model
    reference: parsed.data.reference ?? null,  // CHANGE → cleanData.reference ?? null
    movement: parsed.data.movement ?? null,    // keep parsed.data — D-10 only overrides identity fields
    // ... rest of spec fields keep parsed.data
  },
  photoSourcePath: photoPath,
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend insertWatchSchema + addWatch with catalogId branch (D-09/D-10/D-11) AND add 5 integration tests</name>
  <files>src/app/actions/watches.ts, tests/actions/watches.test.ts</files>
  <read_first>
    - src/app/actions/watches.ts (full file — read lines 1-18 for imports; lines 22-68 for `insertWatchSchema` to find the exact `imageUrl: z.string().optional()` line where `catalogId` lands as a sibling; lines 79-220 for the full `addWatch` body; lines 174-200 specifically for the `enrichTasteAttributes spec:` block where Pitfall 3 lives; lines 200-305 for the activity log + notifications + revalidates that MUST run unchanged)
    - src/data/catalog.ts:254-258 (`getCatalogById` signature + the `CatalogEntry` return shape — confirms `styleTags` is exposed)
    - .planning/phases/67-server-action-dal-extensions/67-PATTERNS.md §"src/app/actions/watches.ts — extend insertWatchSchema + addWatch" (canonical schema field placement + branch placement + D-11 guard structure)
    - .planning/phases/67-server-action-dal-extensions/67-RESEARCH.md "Pitfall 3" (enrichTasteAttributes spec must read cleanData not parsed.data after override)
    - .planning/phases/67-server-action-dal-extensions/67-CONTEXT.md D-09 + D-10 + D-11 (exact branch semantics)
    - .planning/phases/67-server-action-dal-extensions/67-CONTEXT.md Claude's Discretion §"Integration test for addWatch catalogId branch" (5 test cases enumerated)
    - tests/actions/watches.test.ts (full file — read lines 1-72 for existing mocks setup; lines 73-131 for describe block patterns; identify `validWatch` test fixture; confirm the existing `vi.mock('@/data/catalog', ...)` block at line 34 so the new mock entry for `getCatalogById` lands cleanly)
    - tests/actions/addwatch-catalog-resilience.test.ts (precedent — covers the existing fail-loud catalog upsert path; new tests must NOT overlap behavior coverage)
  </read_first>
  <behavior>
    **Production code behavior:**
    - Schema (D-CONF-11): `insertWatchSchema` accepts optional `catalogId: z.string().uuid().optional()` next to `imageUrl`. Invalid UUID format → Zod safeParse failure → existing field-error envelope. `updateWatchSchema = insertWatchSchema.partial()` derivation continues to work.
    - Branch placement (Claude's Discretion + 67-PATTERNS.md §"addWatch catalogId branch placement"): insert AFTER the `cleanData` destructure + `createPayload` build + wishlist-sortOrder block (lines 114-121), BEFORE the existing upsert at line 127.
    - D-09 fail-fast: if `cleanData.catalogId` truthy AND `getCatalogById(cleanData.catalogId)` returns null → return `{ success: false, error: 'Catalog reference not found' }` immediately; do NOT call `upsertCatalogFromUserInput`, do NOT call `createWatch`.
    - D-10 identity override: if `cleanData.catalogId` AND row exists → set `cleanData.brand = catalogRow.brand`, `cleanData.model = catalogRow.model`, `if (catalogRow.reference) cleanData.reference = catalogRow.reference`. The `createPayload` built earlier (line 117) is the same reference as `cleanData` when `status` is not wishlist/grail; when it IS wishlist/grail, `createPayload` was a spread copy → must mirror the override into `createPayload.brand/model/reference` OR re-derive `createPayload` after the override. Simplest: do the override BEFORE the wishlist-sortOrder branch so `createPayload`'s spread captures the overridden values. Re-order: catalogId-branch → wishlist-sortOrder → upsert.
    - Skip upsert: when `cleanData.catalogId` AND row exists, set `catalogId` directly from `cleanData.catalogId` and skip the entire `try { catalogIdResult = await catalogDAL.upsertCatalogFromUserInput(...) }` block at lines 127-140.
    - D-11 enrichment-skip: define `const alreadyEnriched = catalogRowForSkipCheck != null && (catalogRowForSkipCheck.styleTags?.length ?? 0) > 0`. Wrap BOTH the `applyUserUploadedPhoto` photo write-through block (lines 155-171) AND the `enrichTasteAttributes` block (lines 174-200) inside `if (!alreadyEnriched) { ... }`.
    - Pitfall 3 fix: inside the `enrichTasteAttributes spec:` block (lines 179-192), the three identity fields read from `cleanData` (post-override) — `brand: cleanData.brand`, `model: cleanData.model`, `reference: cleanData.reference ?? null`. The other spec fields (movement, caseSizeMm, lugToLugMm, waterResistanceM, crystalType, dialColor, isChronometer, productionYear, complications) keep their `parsed.data.*` reads — D-10 only overrides identity, not the rest.
    - ALL side-effects run regardless of branch (D-11 ALWAYS RUN): the `logActivity` block (lines ~204-222), the `findOverlapRecipients` + `logNotification` block (lines ~228-274), and the `revalidatePath`/`revalidateTag` block (lines ~276-296) are NOT inside any new conditional.

    **Test behavior (5 cases in tests/actions/watches.test.ts new describe block `'addWatch — catalogId branch (CONF-11)'`):**
    - (a) `catalogId` supplied + row exists → `upsertCatalogFromUserInput` is NOT called; `createWatch` is called with the supplied `catalogId` as the 2nd positional arg
    - (b) `catalogId` supplied + row missing → result equals `{ success: false, error: 'Catalog reference not found' }`; `createWatch` is NOT called
    - (c) `catalogId` supplied + client `brand: 'WRONG'` + catalog row has `brand: 'Omega'` → `createWatch` is called with `expect.objectContaining({ brand: 'Omega' })` (D-10 server-side override)
    - (d) `catalogId` + catalog row `styleTags: ['sport']` → `enrichTasteAttributes` is NOT called (D-11 already-enriched skip)
    - (e) `catalogId` + catalog row `styleTags: []` → `enrichTasteAttributes` IS called (D-11 legitimate re-enrichment)
  </behavior>
  <action>
    ## Part A — Schema change

    Locate `insertWatchSchema` at line 22 of `src/app/actions/watches.ts`. Add `catalogId: z.string().uuid().optional(),` as a new property adjacent to `imageUrl: z.string().optional(),` at line 47 (Claude's Discretion §"addWatch schema field addition"). Order/placement among siblings is the planner's discretion — `imageUrl` neighbor satisfies the CONTEXT spec.

    ## Part B — Action body change

    Restructure the catalog-resolution block (lines 114-142) per the canonical structure in 67-PATTERNS.md §"src/app/actions/watches.ts" §"addWatch catalogId branch placement", with this sequencing:
    1. The existing `const { sortOrder: _ignoredSortOrder, ...cleanData } = parsed.data` destructure at line 114 stays as-is.
    2. INSERT the catalogId-supplied branch HERE (before the wishlist-sortOrder branch — this guarantees identity override lands before `createPayload`'s spread in step 3):
       - `let catalogId: string`
       - `let catalogRowForSkipCheck: { styleTags?: string[] | null } | null = null`
       - `if (cleanData.catalogId) { const catalogRow = await catalogDAL.getCatalogById(cleanData.catalogId); if (!catalogRow) return { success: false, error: 'Catalog reference not found' }; cleanData.brand = catalogRow.brand; cleanData.model = catalogRow.model; if (catalogRow.reference) cleanData.reference = catalogRow.reference; catalogId = cleanData.catalogId; catalogRowForSkipCheck = catalogRow; }`
       - `else { /* the existing lines 126-140 upsert block, unchanged */ catalogId = catalogIdResult }`
    3. The wishlist-sortOrder block (originally lines 117-121) moves to AFTER the catalogId resolution and BEFORE `createWatch`. Rebuild `createPayload` from the (possibly overridden) `cleanData`.
    4. `createWatch(user.id, catalogId, createPayload)` at line 142 stays as-is.

    ## Part C — D-11 enrichment-skip gate + Pitfall 3 fix

    Wrap the photo write-through block (lines 155-171) and the `enrichTasteAttributes` block (lines 174-200) inside a single `if (!alreadyEnriched) { ... }` guard, where `alreadyEnriched` is computed as: `const alreadyEnriched = catalogRowForSkipCheck != null && (catalogRowForSkipCheck.styleTags?.length ?? 0) > 0`. Compute `alreadyEnriched` AFTER the catalogId resolution and BEFORE `createWatch` (so the guard is in scope at lines 154+). The existing outer `if (catalogId) { ... }` at line 151 stays unchanged.

    Inside the `enrichTasteAttributes spec:` block (lines 179-192), CHANGE three reads from `parsed.data.*` to `cleanData.*`:
    - `brand: parsed.data.brand` → `brand: cleanData.brand`
    - `model: parsed.data.model` → `model: cleanData.model`
    - `reference: parsed.data.reference ?? null` → `reference: cleanData.reference ?? null`
    Keep all OTHER spec fields reading from `parsed.data.*` (D-10 only overrides identity — movement, case dimensions, dial color, etc. come from client untouched).

    ## Part D — Confirm ALWAYS-RUN side-effects untouched

    Verify (BEFORE finishing) that the `logActivity` block (lines ~204-222), the `findOverlapRecipients` + `logNotification` block (lines ~228-274), and the `revalidatePath('/')` + `revalidatePath('/u/[username]', 'layout')` + `revalidateTag('profile:${username}', 'max')` + `revalidateTag('explore', 'max')` block (lines ~276-296) are NOT wrapped in any new conditional. Use `grep -n "revalidatePath\\|revalidateTag\\|logActivity\\|logNotification\\|findOverlapRecipients" src/app/actions/watches.ts` to spot-check.

    ## Part E — 5 integration tests

    Append a new `describe('addWatch — catalogId branch (CONF-11)', () => { ... })` block to `tests/actions/watches.test.ts`. Place it AFTER the existing describe blocks but BEFORE the file end. Lift the existing mock infrastructure (lines 1-72) — no new top-level mocks needed except:
    - EXTEND the existing `vi.mock('@/data/catalog', ...)` block at line 34 to ALSO mock `getCatalogById: vi.fn()`.

    Use the existing `validWatch` test fixture (defined elsewhere in the file) as the base for all 5 cases; spread + augment with `catalogId: 'cat-uuid-1'` (or `'cat-uuid-missing'` for case b) and case-specific overrides (e.g. `brand: 'WRONG'` for case c).

    Implement the 5 cases exactly per 67-PATTERNS.md §"tests/actions/watches.test.ts — add 5 catalogId branch cases":
    - (a) name: `'(a) catalogId supplied + row exists → no upsertCatalogFromUserInput call'`. Assertions: `expect(catalogDAL.upsertCatalogFromUserInput).not.toHaveBeenCalled()` AND `expect(watchDAL.createWatch).toHaveBeenCalledWith('u-1', 'cat-uuid-1', expect.anything())`.
    - (b) name: `'(b) catalogId supplied + row missing → { success: false, error: "Catalog reference not found" }'`. Mock `getCatalogById` to resolve null. Assertions: `expect(result).toEqual({ success: false, error: 'Catalog reference not found' })` AND `expect(watchDAL.createWatch).not.toHaveBeenCalled()`.
    - (c) name: `'(c) catalogId + client brand="WRONG" → created watch uses catalogRow.brand'`. Mock catalog row with `brand: 'Omega'`. Pass `brand: 'WRONG'` in input. Assertion: `expect(watchDAL.createWatch).toHaveBeenCalledWith('u-1', 'cat-uuid-1', expect.objectContaining({ brand: 'Omega' }))`.
    - (d) name: `'(d) catalogId + non-empty styleTags → enrichTasteAttributes NOT called'`. Mock catalog row with `styleTags: ['sport']`. Assertion: dynamic-import `{ enrichTasteAttributes } from '@/lib/taste/enricher'` then `expect(enrichTasteAttributes).not.toHaveBeenCalled()`.
    - (e) name: `'(e) catalogId + empty styleTags → enrichTasteAttributes IS called'`. Mock catalog row with `styleTags: []`. Assertion: `expect(enrichTasteAttributes).toHaveBeenCalled()`.

    Each test resets mocks via `beforeEach(() => vi.clearAllMocks())` inside the describe block.

    Use `vi.mocked(catalogDAL.getCatalogById).mockResolvedValue(catalogRow as any)` (the `as any` is acceptable here for partial CatalogEntry mocks — matches the existing test style in this file).

    DO NOT modify any other existing describe block.
  </action>
  <verify>
    <automated>npm test -- tests/actions/watches.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "catalogId: z.string().uuid().optional()" src/app/actions/watches.ts` returns 1 (inside `insertWatchSchema`)
    - `grep -c "Catalog reference not found" src/app/actions/watches.ts` returns 1 (D-09 error string)
    - `grep -c "catalogDAL.getCatalogById" src/app/actions/watches.ts` returns at least 1 (D-09 branch read)
    - `grep -c "cleanData.brand = catalogRow.brand" src/app/actions/watches.ts` returns 1 (D-10 identity override — brand)
    - `grep -c "cleanData.model = catalogRow.model" src/app/actions/watches.ts` returns 1 (D-10 — model)
    - `grep -c "cleanData.reference = catalogRow.reference" src/app/actions/watches.ts` returns 1 (D-10 — reference)
    - `grep -c "alreadyEnriched" src/app/actions/watches.ts` returns at least 2 (declaration + guard use)
    - `grep -c "catalogRowForSkipCheck" src/app/actions/watches.ts` returns at least 2
    - Inside the `enrichTasteAttributes spec:` block, identity fields read from `cleanData`: `grep -n "brand: cleanData.brand" src/app/actions/watches.ts` returns 1; `grep -n "model: cleanData.model" src/app/actions/watches.ts` returns 1; `grep -n "reference: cleanData.reference" src/app/actions/watches.ts` returns 1
    - Inside the same spec block, the old reads are GONE: `grep -nE "brand: parsed\\.data\\.brand|model: parsed\\.data\\.model" src/app/actions/watches.ts | grep -v "^#" | grep -v "spec.brand check" | grep -c .` returns 0 (identity fields no longer read from parsed.data inside spec — careful to exclude any unrelated occurrences; if grep returns >0, inspect each line and confirm none are inside the spec block before declaring this gate failed)
    - ALWAYS-RUN side-effects untouched: `grep -c "revalidatePath('/')" src/app/actions/watches.ts` returns 1; `grep -c "revalidateTag('explore'" src/app/actions/watches.ts` returns 1; `grep -c "logActivity" src/app/actions/watches.ts` returns at least 2 (import + call)
    - New tests landed in `tests/actions/watches.test.ts`: `grep -c "addWatch — catalogId branch (CONF-11)" tests/actions/watches.test.ts` returns 1; `grep -cE "it\\('\\([abcde]\\)" tests/actions/watches.test.ts` returns at least 5
    - Existing `vi.mock('@/data/catalog'` block extended: `grep -n "getCatalogById: vi.fn()" tests/actions/watches.test.ts` returns 1
    - All tests in `tests/actions/watches.test.ts` pass via `npm test -- tests/actions/watches.test.ts --run` (exit 0). This includes existing tests in the file (auth, NOTIF-03 wiring, addwatch-catalog-resilience) — no regressions.
    - No new TypeScript errors in `src/app/actions/watches.ts`: `npx tsc --noEmit 2>&1 | grep "src/app/actions/watches.ts" | wc -l` must equal 0
  </acceptance_criteria>
  <done>Schema field added; catalogId-supplied branch implemented in `addWatch` (D-09/D-10/D-11); enrichTasteAttributes spec reads switched to `cleanData` (Pitfall 3); ALL-RUN side-effects untouched; 5 new tests + all existing tests in `tests/actions/watches.test.ts` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action `addWatch` | Client posts watch data including optional `catalogId` UUID; user identity sourced from server session |
| Server Action → Catalog DAL | `getCatalogById(catalogId)` is a public-read by design (catalog rows are not user-private); user identity not required to read |
| Server Action → Watches DAL | `createWatch(userId, catalogId, payload)` — userId always from `getCurrentUser()`, never from client payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03-01 | Tampering | Client-supplied `brand` / `model` / `reference` when `catalogId` is supplied | mitigate | D-10 server-side override: when `catalogId` exists AND `getCatalogById` returns a row, server overwrites identity fields from the catalog row. Client cannot poison catalog identity via mismatched payload. Integration test (c) asserts this. |
| T-67-03-02 | Tampering | Catalog row poisoning via fabricated `catalogId` that points to deleted/nonexistent row | mitigate | D-09 fail-fast: `getCatalogById` null → return `{ success: false, error: 'Catalog reference not found' }`; NEVER fall back to `upsertCatalogFromUserInput` from client brand/model (would defeat the search-first dedup goal). Integration test (b) asserts this. |
| T-67-03-03 | Information Disclosure | `getCatalogById` leaking another user's data | accept | Catalog rows are public-read by design (RESEARCH §Security Domain V4); no user-private fields exposed. CatalogEntry contains only public catalog metadata. |
| T-67-03-04 | Elevation of Privilege | Unauthenticated caller forging `catalogId` | mitigate | Existing AUTH-04 / D-14 auth-first gate at line 81 (`try { user = await getCurrentUser() } catch { return ... 'Not authenticated' }`) runs BEFORE Zod parse and BEFORE any DAL read. Unchanged by this plan. |
| T-67-03-05 | Tampering | Invalid UUID format in `catalogId` payload | mitigate | Zod schema `catalogId: z.string().uuid().optional()` rejects malformed UUIDs at parse time → existing `Invalid watch data: catalogId: ...` envelope returned. |
| T-67-03-06 | Denial of Service | Bypassing taste enrichment to mass-add watches | accept | D-11 enrichment-skip is a performance optimization (avoid wasted LLM call on already-enriched rows); doesn't introduce a new DoS vector — the existing addWatch rate-limiting story is unchanged. |
</threat_model>

<verification>
- Quick sample (per 67-VALIDATION.md): `npm test -- tests/actions/watches.test.ts --run` exits 0; 5 new tests + all existing tests green.
- Manual code inspection: search-side audit for `parsed.data.brand` / `parsed.data.model` / `parsed.data.reference` reads in `src/app/actions/watches.ts` confirms none remain INSIDE the `enrichTasteAttributes spec:` block (Pitfall 3 fix).
- Build gate: `npm run build` exits 0 (authoritative baseline per project memory).
- Type-check: zero new TypeScript errors in `src/app/actions/watches.ts`.
</verification>

<success_criteria>
- `addWatch(data)` with valid `catalogId` skips `upsertCatalogFromUserInput`, binds to existing catalog row via `getCatalogById` — Phase 67 success criterion 2 satisfied.
- `addWatch(data)` with stale/invalid `catalogId` fails fast with `'Catalog reference not found'` — D-09 contract.
- D-10 server-side identity override prevents client brand/model from polluting catalog-bound watches.
- D-11 enrichment-skip avoids wasted LLM round-trip on already-enriched catalog rows; legitimately re-runs enrichment when `styleTags` is empty.
- 5 new integration tests + all existing tests in `tests/actions/watches.test.ts` green; zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/67-server-action-dal-extensions/67-03-SUMMARY.md` covering: the schema field placement chosen, the branch re-ordering decision (catalogId-resolution before wishlist-sortOrder so `createPayload` captures the override), the D-11 guard structure (alreadyEnriched + the `!alreadyEnriched` wrap), the Pitfall 3 spec-reads switch from parsed.data to cleanData (with exact line numbers), and the 5/5 + existing-tests green outcome.
</output>
