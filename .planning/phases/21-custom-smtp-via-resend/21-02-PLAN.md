---
phase: 21
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - docs/deploy-db-setup.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/21-custom-smtp-via-resend/evidence/
autonomous: false
requirements:
  - SMTP-01
  - SMTP-02
  - SMTP-03
  - SMTP-04
  - SMTP-05
  - SMTP-06
nyquist_compliant: true
user_setup:
  - service: resend
    why: "Create Resend API key (= SMTP password) AFTER mail.horlo.app shows Verified ✓"
    env_vars:
      - name: RESEND_SMTP_PASSWORD
        source: "resend.com/api-keys (Send-only scope; visible ONCE at creation; store in 1Password)"
    dashboard_config:
      - task: "Wait until every record on mail.horlo.app shows Verified ✓ before creating the API key"
        location: "Resend Dashboard → Domains → mail.horlo.app"
  - service: supabase
    why: "Wire SMTP credentials + flip three Auth toggles + audit Site URL; all dashboard-only"
    env_vars: []
    dashboard_config:
      - task: "Auth → Emails → SMTP Settings: enable Custom SMTP and paste Host/Port/Username/Password/Sender"
        location: "Supabase Dashboard → Authentication → Emails → SMTP Settings"
      - task: "Auth → URL Configuration: confirm Site URL = https://horlo.app (no trailing slash)"
        location: "Supabase Dashboard → Authentication → URL Configuration"
      - task: "Authentication → Users → Invite User (D-07 step 1 substitute)"
        location: "Supabase Dashboard → Authentication → Users → Invite User"
      - task: "Auth → Sign In/Providers → Email: flip three toggles ON (Confirm email, Secure email change, Secure password change)"
        location: "Supabase Dashboard → Authentication → Sign In/Providers → Email"

must_haves:
  truths:
    - "Resend dashboard shows Verified ✓ on every row of mail.horlo.app (SPF + DKIM + bounce MX + DMARC); confirmed by `dig +short` on at least the SPF and DKIM rows"
    - "Supabase Dashboard → Auth → Emails → SMTP Settings is wired to smtp.resend.com:465 with Username=resend, Sender Email=noreply@mail.horlo.app, Sender Name=Horlo"
    - "Supabase Site URL = https://horlo.app (no trailing slash); redirect URL allowlist includes https://horlo.app/**"
    - "D-07 gate (BLOCKING) passed: (a) Supabase Invite-User flow delivered an email to a controlled Gmail address landing in INBOX (not Spam); AND (b) a real Gmail end-to-end signup against prod completed — confirmation email arrived in Inbox, the link opened the auth-confirm flow, and the account was confirmed"
    - "Three Auth toggles are ON in production (Confirm email, Secure email change, Secure password change), AND the signup-form code change from Plan 21-01 is merged"
    - "docs/deploy-db-setup.md has a 'Phase 21 — Custom SMTP via Resend Backout' section using the existing **Footgun T-XX-...:** callout pattern, including the T-21-PREVIEWMAIL footgun (D-02 implication)"
    - ".planning/PROJECT.md Key Decisions reflects 'Email confirmation OFF → ON (via Resend SMTP, mail.horlo.app, 2026-04-30)'"
    - ".planning/REQUIREMENTS.md SMTP-06 row reads 'Deferred — pending staging Supabase project (see Phase 21 21-CONTEXT.md D-01)'"
  artifacts:
    - path: "docs/deploy-db-setup.md"
      provides: "Backout-plan section for SMTP regression"
      contains: "Phase 21 — Custom SMTP via Resend Backout"
    - path: ".planning/PROJECT.md"
      provides: "Updated Key Decisions row for email-confirmation status"
      contains: "Email confirmation"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SMTP-06 marked Deferred"
      contains: "Deferred — pending staging Supabase project"
    - path: ".planning/phases/21-custom-smtp-via-resend/evidence/"
      provides: "Screenshots of Verified ✓, SMTP settings, D-07 inbox shots, three toggles ON"
  key_links:
    - from: "Resend Verified ✓"
      to: "Supabase SMTP wiring"
      via: "Resend API key (Send-only scope) → pasted into Supabase Auth → SMTP password field"
      pattern: "smtp.resend.com:465 + Username=resend + Sender=noreply@mail.horlo.app"
    - from: "Supabase Invite-User + real Gmail signup"
      to: "Three-toggle flip"
      via: "Both must inbox at Gmail (not spam) per D-08 BEFORE flipping; spam-fold blocks the flip per D-09"
      pattern: "Inbox not Spam, link click reaches /auth/v1/verify and lands authenticated on horlo.app/"
---

<scope_note>
**Why 5 tasks (above the default 4-task warning threshold):** Tasks 1–4 are observe-and-record checkpoints (verify Resend ✓ → wire Supabase SMTP → run D-07 gate → flip toggles), each producing screenshot/dig evidence. Only Task 5 is an autonomous code/docs edit. The four checkpoint tasks have minimal executor context burn (read dashboards, transcribe values into evidence/) and form a single irreversible operational sequence that must run linearly with checkpointed evidence between each step. Splitting the sequence across plan boundaries would fracture the verify→wire→gate→flip→doc procedure without reducing per-plan scope. The 5-task count is intentional and bounded.
</scope_note>

<objective>
Pick up after DNS propagation completes (Resend shows Verified ✓), wire Supabase Auth to Resend SMTP, run the D-07 round-trip gate, and — only if both clauses pass and Gmail inboxes (D-08) — flip the three Auth toggles, append the backout-plan section to docs/deploy-db-setup.md, update PROJECT.md Key Decisions, and mark SMTP-06 as Deferred in REQUIREMENTS.md.

Purpose: This plan executes the irreversible-feeling steps. Up to D-07 everything is recoverable. After the toggle flips, new signups require email confirmation in production — which is exactly the desired state, but it means the D-07 gate is non-negotiable. Per D-09, a spam-foldered test email STOPS the plan; we investigate DKIM/SPF/DMARC before retrying.

Output:
- Resend Verified ✓ confirmed (manual + `dig`)
- Resend API key created (Send-only scope), stored in 1Password / `.env.local` (NEVER in git, NEVER in evidence/)
- Supabase Dashboard SMTP wired and Site URL audited
- D-07 round-trip evidence: Invite + real-Gmail signup screenshots, both showing Inbox (not Spam)
- Three toggles flipped ON in production
- docs/deploy-db-setup.md appended with Phase 21 backout section using **Footgun T-21-...:** pattern
- PROJECT.md Key Decisions updated
- REQUIREMENTS.md SMTP-06 marked Deferred
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-custom-smtp-via-resend/21-CONTEXT.md
@.planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md
@.planning/phases/21-custom-smtp-via-resend/21-VALIDATION.md
@.planning/phases/21-custom-smtp-via-resend/21-01-PLAN.md
@docs/deploy-db-setup.md

<interfaces>
<!-- Supabase SMTP field mapping (verbatim from RESEARCH.md) -->

| Supabase Field | Phase 21 Value | Source |
|----------------|----------------|--------|
| Enable Custom SMTP | ON | — |
| Sender name | Horlo | D-05 |
| Sender email | noreply@mail.horlo.app | D-04 |
| Host | smtp.resend.com | Resend universal |
| Port | 465 | CONTEXT.md SMTP-02; implicit SSL/TLS |
| Username | resend | Resend universal (literal string, NOT an email) |
| Password | <Resend API key, starts with `re_`> | resend.com/api-keys; Send-only scope |
| Reply-To | (leave blank) | D-06 — explicit non-config; replies bounce off `noreply@` |
| Min interval between emails | leave default | per-recipient throttle, not relevant for personal-MVP |

<!-- D-07 round-trip gate, reworded per RESEARCH.md Pitfall #1 -->
D-07 step 1 substitute: Supabase Dashboard → Authentication → Users → **Invite User** (the literal "Send test email" button does NOT exist).
D-07 step 2: real Gmail end-to-end signup at prod (https://horlo.app/signup) using a fresh `+suffix` alias.

<!-- Existing footgun pattern in docs/deploy-db-setup.md -->
The repo standard is `**Footgun T-XX-XX-XXXXX:**` (e.g., `**Footgun T-05-06-SMTPRATE:**`, `**Footgun T-17-BACKFILL-PROD-DB:**`). The new section MUST follow this pattern. Recommended footgun ID: `T-21-PREVIEWMAIL` (D-02 implication: previews share prod Supabase, so preview signups send real Resend mail).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [BLOCKING]: Confirm Resend Verified ✓, then create API key (Send-only, stored offline)</name>
  <read_first>
    - .planning/phases/21-custom-smtp-via-resend/21-01-SUMMARY.md (records submitted, what to expect Verified)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Resend API Key Lifecycle" (lines 95-101)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Pitfall 4: Resend API Key Lost After Creation" (lines 451-457)
  </read_first>
  <files>.planning/phases/21-custom-smtp-via-resend/evidence/ (NEW screenshot)</files>
  <action>
    Step 1 — Verify DNS propagation:
    ```
    dig +short TXT send.mail.horlo.app
    dig +short TXT resend._domainkey.mail.horlo.app
    dig +short TXT _dmarc.mail.horlo.app
    dig +short MX  send.mail.horlo.app
    ```
    Each MUST return non-empty values matching what was submitted in Plan 21-01 Task 2.
    - SPF: contains `v=spf1 include:amazonses.com`
    - DKIM: contains `p=` followed by a long base64 string
    - DMARC: contains `v=DMARC1; p=none`
    - MX: contains `feedback-smtp.<region>.amazonses.com`

    Save the dig output to `.planning/phases/21-custom-smtp-via-resend/evidence/dns-verified.md`.

    Step 2 — Confirm Resend Verified ✓:
    Open https://resend.com/domains → click `mail.horlo.app`. Every row in the records table MUST show "Verified" / green check. If any shows "Pending" → click the "Verify" or refresh button at the top; wait 5-15 minutes; re-try. If still pending after 1 hour, audit the DNS provider's records list against Resend's expected values (Pitfall 2: leftmost-label Name not FQDN).

    Take a screenshot of the Verified ✓ state and save to `.planning/phases/21-custom-smtp-via-resend/evidence/resend-verified.png`.

    Step 3 — Create Resend API key (only AFTER Verified ✓ — do not skip the gate above):
    1. Open https://resend.com/api-keys
    2. Click "Create API Key"
    3. Name: `horlo-smtp-prod-2026-04-30` (or similar; namespace by environment + date)
    4. Permission: **Sending access** (Send-only) — least privilege per RESEARCH.md
    5. Domain: `mail.horlo.app` (if Resend asks; otherwise leave default = all domains on the account)
    6. Click Create. **The key is shown ONCE.** Copy it immediately:
       a. Paste into 1Password (or your preferred password manager) under entry name "Horlo Resend SMTP — prod (mail.horlo.app)"
       b. Optionally also paste into a NEW line in `.env.local` (gitignored, NOT committed): `RESEND_SMTP_PASSWORD=re_xxxxxxxxxxxxxxxxxxxxxxxx`
       c. Do NOT paste into chat, screenshots, evidence/, .env.example, or any committed file.
    7. Click Done in Resend.

    **CRITICAL — T-21-01 (HIGH severity) mitigation:** If you accidentally include the API key in a screenshot, IMMEDIATELY revoke that key at resend.com/api-keys (red trash icon next to the key) and create a new one. The screenshot in evidence/resend-verified.png MUST NOT show the key.
  </action>
  <acceptance_criteria>
    - `dig +short TXT send.mail.horlo.app` returns the SPF value
    - `dig +short TXT resend._domainkey.mail.horlo.app` returns the DKIM public key
    - `dig +short TXT _dmarc.mail.horlo.app` returns `v=DMARC1; p=none;`
    - `dig +short MX send.mail.horlo.app` returns the Amazon SES feedback host with priority 10
    - File `evidence/dns-verified.md` exists with the dig output transcribed
    - File `evidence/resend-verified.png` exists showing every record row with the green check / Verified label (key NOT visible in screenshot)
    - Resend API key with Send-only scope is created and stored in 1Password (operator-attested; this is unverifiable from the CLI but required by Pitfall 4)
    - `grep -F "re_" evidence/` returns NOTHING (the key is NOT in evidence/) — automated guard against T-21-01
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/21-custom-smtp-via-resend/evidence/dns-verified.md && test -f .planning/phases/21-custom-smtp-via-resend/evidence/resend-verified.png && grep -q "v=spf1" .planning/phases/21-custom-smtp-via-resend/evidence/dns-verified.md && grep -q "DMARC1" .planning/phases/21-custom-smtp-via-resend/evidence/dns-verified.md && ! grep -rE "re_[A-Za-z0-9]{20,}" .planning/phases/21-custom-smtp-via-resend/evidence/ 2>/dev/null</automated>
  </verify>
  <done>Verified ✓ on every Resend row, dig output captured, API key created with Send-only scope and stored offline.</done>
  <resume-signal>Type "verified-and-keyed" once the screenshot, dig output, and password-manager entry are all in place.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Wire Supabase SMTP Settings + audit Site URL</name>
  <read_first>
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Supabase SMTP Field Mapping" (lines 184-198)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Site URL & Redirect URL Audit" (lines 267-289)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Pitfall 3: Site URL Trailing Slash" (lines 443-449)
    - .planning/phases/21-custom-smtp-via-resend/21-CONTEXT.md D-04, D-05
  </read_first>
  <files>.planning/phases/21-custom-smtp-via-resend/evidence/ (screenshots only)</files>
  <action>
    Step 1 — Wire SMTP:
    1. Open Supabase Dashboard for project `wdntzsckjaoqodsyscns` → Authentication → Emails → SMTP Settings
    2. Toggle **Enable Custom SMTP** to ON
    3. Fill the form verbatim:
       - **Sender name:** `Horlo`  (D-05)
       - **Sender email:** `noreply@mail.horlo.app`  (D-04)
       - **Host:** `smtp.resend.com`
       - **Port:** `465`
       - **Username:** `resend`  (literal string, NOT an email)
       - **Password:** paste the Resend API key from 1Password (starts with `re_`)
       - **Min interval between emails:** leave default
    4. Click Save.
    5. Take a screenshot of the saved settings page (with the Password field MASKED — Supabase masks it after save). Save as `.planning/phases/21-custom-smtp-via-resend/evidence/supabase-smtp-settings.png`.

    Step 2 — Audit Site URL + Redirect URL allowlist:
    1. Same Supabase Dashboard → Authentication → URL Configuration
    2. **Site URL** MUST be exactly `https://horlo.app` — NO trailing slash (Pitfall 3). If currently set to `https://horlo.app/`, delete the trailing slash and Save.
    3. **Redirect URLs** allowlist MUST include:
       - `https://horlo.app/**`
       - `https://horlo-*.vercel.app/**` (preview-deploy pattern; D-02 means previews share prod auth)
       - `http://localhost:3000/**` (already there for local dev; do not remove)
    4. Save if changed.
    5. Take a screenshot of the URL Configuration page → save as `.planning/phases/21-custom-smtp-via-resend/evidence/supabase-url-config.png`.

    Step 3 — Audit rate limit:
    1. Same dashboard → Authentication → Rate Limits
    2. Confirm "Rate limit for sending emails" is set to a sensible value (Supabase default after enabling Custom SMTP is 30/h; raise to 100 or 300 if expecting a smoke-test burst — but personal-MVP volume is fine at 30/h).
    3. No screenshot needed unless changing the value.

    Step 4 — Pre-flight DB check (low-risk; D-08 ripple safety):
    Open Supabase Dashboard → SQL Editor and run:
    ```sql
    SELECT count(*) AS unconfirmed_users
    FROM auth.users
    WHERE email_confirmed_at IS NULL;
    ```
    Expected: 0 (per RESEARCH.md "Toggle Interactions" — personal-MVP has only confirmed users today). Record the count in `evidence/preflight-unconfirmed-users.txt`. If > 0, note the count but do NOT block — flipping Confirm-email ON does not retroactively prompt existing unconfirmed users (verified in RESEARCH.md). Just be aware.
  </action>
  <acceptance_criteria>
    - Screenshot `evidence/supabase-smtp-settings.png` shows Host=smtp.resend.com, Port=465, Username=resend, Sender Email=noreply@mail.horlo.app, Sender Name=Horlo, Password=masked
    - Screenshot `evidence/supabase-url-config.png` shows Site URL = `https://horlo.app` (NO trailing slash) and the Redirect URL allowlist includes `https://horlo.app/**`
    - File `evidence/preflight-unconfirmed-users.txt` exists with the count
    - The Resend API key is NOT visible in any committed file or evidence/ image
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/21-custom-smtp-via-resend/evidence/supabase-smtp-settings.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/supabase-url-config.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/preflight-unconfirmed-users.txt && ! grep -rE "re_[A-Za-z0-9]{20,}" .planning/phases/21-custom-smtp-via-resend/evidence/ 2>/dev/null</automated>
  </verify>
  <done>Supabase SMTP wired exactly per the field-mapping table; Site URL = https://horlo.app (no trailing slash); redirect allowlist includes prod + preview patterns; key not leaked.</done>
  <resume-signal>Type "smtp-wired" when both screenshots and the preflight file are in evidence/.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [BLOCKING — D-07 ROUND-TRIP GATE]: Invite test + real-Gmail end-to-end signup, both must inbox-not-spam</name>
  <read_first>
    - .planning/phases/21-custom-smtp-via-resend/21-CONTEXT.md D-07, D-08, D-09
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "'Send Test Email' Reality vs D-07" (lines 200-222)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Pitfall 5: Gmail Spam-Foldering Even with SPF + DKIM Verified" (lines 459-475)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Pitfall 7: Inviting an Already-Existing Email" (lines 485-491)
    - .planning/phases/21-custom-smtp-via-resend/21-VALIDATION.md "Manual-Only Verifications" (D-07 row)
  </read_first>
  <files>.planning/phases/21-custom-smtp-via-resend/evidence/ (screenshots)</files>
  <action>
    **This is the non-negotiable blocking gate. Both clauses MUST pass before Task 4 (toggle flip) starts. Per D-09, if Gmail spam-folds either email, STOP and investigate DKIM/SPF/DMARC alignment — do NOT flip toggles and "monitor."**

    **Note (RESEARCH.md Pitfall #1):** The literal "Send test email" button referenced in CONTEXT.md D-07 step 1 does not exist in the Supabase Dashboard. Use the **Send Invitation** flow as documented below — it is the closest equivalent that exercises the full SMTP path.

    Clause A — Supabase Invitation send (substitute for the missing "Send test email"):
    1. Open Supabase Dashboard → Authentication → Users → click "Invite User"
    2. Email: `<youremail>+horlo-smtp-test1@gmail.com` (use a FRESH +suffix alias — Pitfall 7 prohibits reusing aliases on retries)
    3. Click Send invitation
    4. Open Gmail (the inbox of `<youremail>@gmail.com`); the invitation should arrive within ~10-30 seconds
    5. **Verify INBOX (not Spam):** if it lands in Spam, STOP per D-09. Document the spam-fold in `evidence/d07-clause-a-spam.md` and follow the Pitfall 5 / D-09 escalation: re-check DKIM/SPF/DMARC alignment via mxtoolbox.com or Resend dashboard's deliverability tab; do NOT continue this task.
    6. Verify the From header shows `Horlo <noreply@mail.horlo.app>` (D-04 + D-05).
    7. Take a screenshot of the inbox row (showing it is NOT in Spam) → `evidence/d07-clause-a-invite-inbox.png`. Take a screenshot of the open email's headers → `evidence/d07-clause-a-invite-headers.png`.

    Clause B — Real Gmail end-to-end signup (D-07 step 2):
    1. Open `https://horlo.app/signup` in a private/incognito browser window (NOT preview URL — must be production https://horlo.app per D-02 isolation)
    2. Email: `<youremail>+horlo-smtp-e2e1@gmail.com` (FRESH +suffix; different from Clause A's alias)
    3. Password: a fresh password
    4. Submit. Note: at this moment Confirm-email is still OFF in Supabase, so the form will redirect to `/`. **This intermediate redirect is expected and is NOT what we are testing here** — we are testing that SMTP/DNS/template round-trips correctly when an email IS sent. To force a confirm-email to be sent without flipping the dashboard toggle, instead trigger a password reset:
       a. After signup, log out
       b. Visit `/login` → click "Forgot password" (or whichever link points to the password reset flow)
       c. Enter `<youremail>+horlo-smtp-e2e1@gmail.com`
       d. Submit
    5. Open Gmail; the password-reset email should arrive within ~30 seconds
    6. **Verify INBOX (not Spam):** if Spam → STOP per D-09 (same escalation as Clause A).
    7. Verify From header = `Horlo <noreply@mail.horlo.app>`.
    8. Click the reset link in the email. It should:
       - Open in a browser
       - Hit `<project-ref>.supabase.co/auth/v1/verify?...` then redirect to `https://horlo.app/...`
       - Land on a page that lets you set a new password (the existing /auth/confirm or similar reset surface)
       - The redirect URL should NOT be `localhost`, NOT a 404, NOT a `/login` with an error parameter
    9. Set a new password, log in successfully → confirms the round trip works end-to-end.
    10. Screenshots: `evidence/d07-clause-b-reset-inbox.png` (Gmail inbox row), `evidence/d07-clause-b-reset-headers.png` (open email headers), `evidence/d07-clause-b-reset-success.png` (successful login after reset).
    11. Clean up: in Supabase Dashboard → Authentication → Users → delete the test user `<youremail>+horlo-smtp-e2e1@gmail.com`. Also delete the invited user from Clause A.

    Recording — write a one-page summary to `evidence/d07-gate-result.md`:
    - Clause A: PASS / FAIL (Inbox not Spam? From header correct?)
    - Clause B: PASS / FAIL (Inbox not Spam? Reset link reached horlo.app? Login worked?)
    - Overall gate disposition: PASS (proceed to Task 4) / FAIL (stop, investigate per D-09)
    - Date / time / Gmail aliases used (no real PII beyond the alias structure)

    If EITHER clause failed: STOP. Do NOT execute Task 4. Open a debug/issue note investigating DKIM/SPF/DMARC. Re-run this Task 3 after fixing.
  </action>
  <acceptance_criteria>
    - File `evidence/d07-gate-result.md` exists and reports BOTH Clause A and Clause B as PASS
    - Files `evidence/d07-clause-a-invite-inbox.png`, `evidence/d07-clause-a-invite-headers.png`, `evidence/d07-clause-b-reset-inbox.png`, `evidence/d07-clause-b-reset-headers.png`, `evidence/d07-clause-b-reset-success.png` all exist
    - Both Inbox screenshots clearly show the email landed in Inbox (not in Spam folder)
    - Both Header screenshots show `Horlo <noreply@mail.horlo.app>` in the From line
    - Test users deleted from Supabase Auth (operator-attested)
    - If either clause is FAIL, this task is INCOMPLETE and Task 4 MUST NOT run — repeat Task 3 after investigating per D-09
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/21-custom-smtp-via-resend/evidence/d07-gate-result.md && grep -q "Clause A: PASS" .planning/phases/21-custom-smtp-via-resend/evidence/d07-gate-result.md && grep -q "Clause B: PASS" .planning/phases/21-custom-smtp-via-resend/evidence/d07-gate-result.md && grep -q "Overall gate disposition: PASS" .planning/phases/21-custom-smtp-via-resend/evidence/d07-gate-result.md && test -f .planning/phases/21-custom-smtp-via-resend/evidence/d07-clause-a-invite-inbox.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/d07-clause-a-invite-headers.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/d07-clause-b-reset-inbox.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/d07-clause-b-reset-headers.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/d07-clause-b-reset-success.png</automated>
  </verify>
  <done>Both D-07 clauses passed (Inbox not Spam, From header correct, link round-trip worked); evidence saved; test users cleaned up. Task 4 may now proceed.</done>
  <resume-signal>Type "d07-passed" only after both clauses are GREEN and evidence/d07-gate-result.md says "Overall gate disposition: PASS". If FAIL, type "d07-failed" instead and we will re-investigate per D-09.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Flip three toggles ON (Confirm email → then Secure email change + Secure password change)</name>
  <read_first>
    - .planning/phases/21-custom-smtp-via-resend/evidence/d07-gate-result.md (Task 3 must be PASS — do not start otherwise)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Toggle Order" (lines 261-265)
    - .planning/phases/21-custom-smtp-via-resend/21-CONTEXT.md D-10 (signup-form code change must be merged)
  </read_first>
  <files>.planning/phases/21-custom-smtp-via-resend/evidence/ (screenshots)</files>
  <action>
    **Pre-flight check (do not skip):**
    1. Confirm Plan 21-01 Task 3 (signup-form.tsx amend) is merged to `main` — `git log --oneline main -- src/app/signup/signup-form.tsx` shows the recent amend commit. If not merged, STOP and merge first (D-10 prerequisite for SMTP-03; T-21-03 mitigation).
    2. Confirm Task 3 evidence file says "Overall gate disposition: PASS" — `grep "Overall gate disposition: PASS" .planning/phases/21-custom-smtp-via-resend/evidence/d07-gate-result.md`. If not, STOP.

    Step 1 — Flip Confirm email ON:
    1. Open Supabase Dashboard → Authentication → Sign In/Providers → Email
    2. Toggle **Confirm email** to ON
    3. Save
    4. Take screenshot → `evidence/toggle-confirm-email-on.png`

    Step 2 — Flip Secure email change ON:
    1. Same page
    2. Toggle **Secure email change** to ON
    3. Save
    4. Take screenshot → `evidence/toggle-secure-email-change-on.png`

    Step 3 — Flip Secure password change ON:
    1. Same page
    2. Toggle **Secure password change** to ON
    3. Save
    4. Take screenshot → `evidence/toggle-secure-password-change-on.png`

    Step 4 — Smoke test the full flip:
    1. Open prod `https://horlo.app/signup` in private window
    2. Sign up with `<youremail>+horlo-postflip1@gmail.com`
    3. **Verify the form now renders the "Check your email to confirm your account" message** (D-10 path — this is the production verification of the Plan 21-01 Task 3 code change)
    4. Open Gmail → the confirmation email should arrive in Inbox (not Spam) within ~30s
    5. Click the confirmation link → should land on horlo.app/ logged in
    6. Take screenshot of (a) the in-card "Check your email" state → `evidence/postflip-signup-card.png`, (b) the inbox confirmation arrival → `evidence/postflip-confirm-inbox.png`, (c) the post-confirm logged-in state → `evidence/postflip-logged-in.png`
    7. Clean up: delete the test user from Supabase Authentication → Users.
  </action>
  <acceptance_criteria>
    - Three toggle screenshots exist showing Confirm email ON, Secure email change ON, Secure password change ON in production project (`wdntzsckjaoqodsyscns`)
    - Smoke-test screenshots `evidence/postflip-signup-card.png` (shows "Check your email" message — confirms D-10), `evidence/postflip-confirm-inbox.png` (Inbox not Spam), `evidence/postflip-logged-in.png` exist
    - The signup-form code change is confirmed merged to main: `git log --oneline -1 -- src/app/signup/signup-form.tsx` shows a Phase 21 commit
    - Test user deleted (operator-attested)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/21-custom-smtp-via-resend/evidence/toggle-confirm-email-on.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/toggle-secure-email-change-on.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/toggle-secure-password-change-on.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/postflip-signup-card.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/postflip-confirm-inbox.png && test -f .planning/phases/21-custom-smtp-via-resend/evidence/postflip-logged-in.png && grep -q "data.session" src/app/signup/signup-form.tsx</automated>
  </verify>
  <done>All three toggles ON in production; post-flip smoke test confirms (a) signup-form renders "Check your email" message and (b) confirmation email round-trips to Inbox and logs the user in.</done>
  <resume-signal>Type "toggles-flipped" once all three screenshots and the smoke-test evidence are saved.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Append backout-plan section to docs/deploy-db-setup.md (SMTP-05) + update PROJECT.md + REQUIREMENTS.md (SMTP-06 deferral)</name>
  <read_first>
    - docs/deploy-db-setup.md (existing footgun callout pattern; new section appends after Phase 19.1 section at end of file)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Pattern 2: Footgun Callouts in Backout Doc" (lines 346-373)
    - .planning/phases/21-custom-smtp-via-resend/21-CONTEXT.md D-02 (preview-deploy implication)
    - .planning/PROJECT.md (find the "Email confirmation" / Key Decisions row to update)
    - .planning/REQUIREMENTS.md SMTP-06 line (currently "| SMTP-06 | Phase 21 — Custom SMTP via Resend | Pending |")
  </read_first>
  <files>docs/deploy-db-setup.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <action>
    Step 1 — Append "Phase 21 — Custom SMTP via Resend" section to `docs/deploy-db-setup.md`:

    Place at the END of the file (after the Phase 19.1 section). Use the existing `**Footgun T-XX-XX-XXXXX:**` callout pattern (verified in deploy-db-setup.md lines 24, 48, 109, 122, 178). New footgun ID: `T-21-PREVIEWMAIL`.

    Section content (paste verbatim, with date/values filled):

    ```markdown
    ## Phase 21 — Custom SMTP via Resend Backout

    Phase 21 wired Supabase Auth to Resend SMTP for `mail.horlo.app` and flipped Confirm email + Secure email change + Secure password change ON in prod (`wdntzsckjaoqodsyscns`). If DKIM regresses, deliverability tanks, or the Resend account is suspended, follow this procedure to restore Supabase hosted SMTP. Estimated downtime: ~5 min from incident detection to restoration.

    **Footgun T-21-PREVIEWMAIL:** [D-02] Vercel preview deployments share prod Supabase. Any signup from a preview URL (e.g. `horlo-git-feature-branch.vercel.app`) sends a real Resend email at production sender reputation cost. If a preview deploy triggers spam complaints, this Phase 21's flip can be regressed (see Backout triggers below). Operationally: developers should use `+suffix` Gmail aliases for preview testing (per Step 4 footgun) and treat preview signups as "real" sends.

    ### Backout triggers
    - DKIM "Verified ✓" regresses to "Pending" in Resend dashboard
    - Resend account suspended/throttled (notification from Resend support)
    - Bulk spam complaints on `mail.horlo.app`
    - > 10% bounce rate over 24h (visible in Resend dashboard)

    ### Backout procedure
    1. Supabase Dashboard → Authentication → Sign In/Providers → Email → toggle "Confirm email" OFF (incoming signups land without confirmation; existing confirmed users unaffected)
    2. Supabase Dashboard → Authentication → Emails → SMTP Settings → toggle "Enable Custom SMTP" OFF (reverts to Supabase hosted SMTP, 2/h cap restored — acceptable during incident response since Confirm-email is OFF)
    3. Supabase Dashboard → Authentication → Sign In/Providers → Email → leave "Secure password change" ON, leave "Secure email change" ON (these don't depend on Resend SMTP being healthy — they're stored flags exercised by Phase 22 UI)
    4. Update `.planning/PROJECT.md` Key Decisions: `Email confirmation ON → OFF (regressed YYYY-MM-DD due to <reason>)`
    5. Investigate Resend dashboard → Domain → DNS records (typical regression cause: DNS provider changed records; re-confirm against Resend's expected values per Plan 21-01 evidence)

    ### Backout footgun (context, not an action)
    The Resend API key remains valid after Supabase SMTP is disabled. To fully sever the integration, delete the API key at `resend.com/api-keys`. For incident-response posture, leave the key in place (faster rollback to ON when issue resolved).

    ### Recovery (after incident resolves)
    Re-run Plan 21-02 from Task 1 (Verified ✓ confirmation) onward. Site URL audit and SMTP wiring values are unchanged; only the toggles need to flip back ON in order: Confirm email → smoke-test → restore.
    ```

    Step 2 — Update `.planning/PROJECT.md` Key Decisions:

    Find the "Email confirmation" Key Decisions row (search for "Email confirmation" or similar). Update the entry from "OFF" to:
    `ON (via Resend SMTP, mail.horlo.app, 2026-04-30)`

    Adjust to match the existing PROJECT.md row format. If the row currently says something like "Email confirmation OFF — using Supabase hosted SMTP for personal-MVP" — replace with "Email confirmation ON (via Resend SMTP, mail.horlo.app) — set 2026-04-30 in Phase 21".

    Step 3 — Update `.planning/REQUIREMENTS.md` SMTP-06:

    Find the line `| SMTP-06 | Phase 21 — Custom SMTP via Resend | Pending |` in the Traceability table.

    Replace with:
    `| SMTP-06 | Phase 21 — Custom SMTP via Resend | Deferred — pending staging Supabase project (see Phase 21 21-CONTEXT.md D-01) |`

    Also update the SMTP-06 line in the "Email / Custom SMTP" section (line 97) — currently:
    `- [ ] **SMTP-06**: \`mail.staging.horlo.app\` (staging) and \`mail.horlo.app\` (prod) are separated for sender-reputation isolation`

    Append a deferral note inline:
    `- [ ] **SMTP-06**: \`mail.staging.horlo.app\` (staging) and \`mail.horlo.app\` (prod) are separated for sender-reputation isolation — **Deferred — pending staging Supabase project (see Phase 21 21-CONTEXT.md D-01)**`

    Step 4 — Run lint + typecheck + build (sanity, even though docs-only):
    ```
    npm run lint && npx tsc --noEmit
    ```
    These should pass unchanged since no code was edited.
  </action>
  <acceptance_criteria>
    - `grep -F "Phase 21 — Custom SMTP via Resend Backout" docs/deploy-db-setup.md` returns 1 match
    - `grep -F "Footgun T-21-PREVIEWMAIL" docs/deploy-db-setup.md` returns 1 match
    - `grep -F "Backout triggers" docs/deploy-db-setup.md` returns at least 1 match (this section)
    - `grep -F "Backout procedure" docs/deploy-db-setup.md` returns at least 1 match
    - `.planning/PROJECT.md` reflects "Email confirmation ON" (or equivalent post-flip language with the date 2026-04-30 and Resend reference)
    - `grep "SMTP-06.*Deferred" .planning/REQUIREMENTS.md` returns at least 1 match (Traceability row)
    - `grep -F "Deferred — pending staging Supabase project" .planning/REQUIREMENTS.md` returns at least 2 matches (line in main section + Traceability row)
    - The new docs/deploy-db-setup.md section does NOT contain the actual Resend API key (T-21-05 mitigation): `! grep -E "re_[A-Za-z0-9]{20,}" docs/deploy-db-setup.md`
    - `npm run lint && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Phase 21 — Custom SMTP via Resend Backout" docs/deploy-db-setup.md && grep -q "Footgun T-21-PREVIEWMAIL" docs/deploy-db-setup.md && grep -q "Backout triggers" docs/deploy-db-setup.md && grep -q "Backout procedure" docs/deploy-db-setup.md && grep -q "Email confirmation" .planning/PROJECT.md && grep -q "SMTP-06.*Deferred" .planning/REQUIREMENTS.md && grep -q "Deferred — pending staging Supabase project" .planning/REQUIREMENTS.md && ! grep -E "re_[A-Za-z0-9]{20,}" docs/deploy-db-setup.md && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>docs/deploy-db-setup.md has the Phase 21 backout section using **Footgun T-21-PREVIEWMAIL:** pattern; PROJECT.md Key Decisions reflects email confirmation ON; REQUIREMENTS.md SMTP-06 marked Deferred in both the main section and the Traceability table; no API key leaked into docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Resend Dashboard | Manual API-key creation; key visible ONCE; storage is operator-side (1Password / .env.local) |
| Operator → Supabase Dashboard | Manual SMTP-field entry; password masked after save |
| Public Internet → Supabase Auth → Resend → Gmail | DKIM-signed transactional email path; DMARC=p=none aligns the path |
| User browser → /auth/v1/verify → /horlo.app | Email link round-trip; Site URL must match exactly (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure (HIGH) | Resend API key | mitigate | Task 1 stores key in 1Password (operator-attested) and optionally `.env.local` (gitignored). Task 2 pastes key into Supabase Dashboard where it is masked. Tasks 1, 2, 5 acceptance criteria include `! grep -rE "re_[A-Za-z0-9]{20,}"` against evidence/ and docs to catch accidental leaks. Pitfall 4 mitigation: if leaked, immediately revoke at resend.com/api-keys and create a new key. |
| T-21-02 | Spoofing (MEDIUM) | mail.horlo.app email path | mitigate | Plan 21-01 published SPF + DKIM + DMARC `p=none`. Task 1 here verifies all three propagated and Resend confirms Verified ✓ on every row. Task 3 confirms Gmail accepts the path (Inbox, not Spam) — D-08. |
| T-21-03 | Tampering / sequencing (MEDIUM) | Account state during phase | mitigate | Task 4 has a pre-flight check that confirms (a) Plan 21-01 Task 3 signup-form code is merged to main and (b) D-07 gate (Task 3) PASSed before flipping. The dependency order is: Plan 21-01 ships → Resend Verified ✓ (Task 1) → SMTP wired (Task 2) → D-07 gate (Task 3) → toggle flip (Task 4). |
| T-21-04 | Information Disclosure (LOW — user enumeration) | Post-flip signup form | mitigate | Plan 21-01 Task 3 already implemented the no-session branch with NEUTRAL copy that does not differentiate "new account" from "email already exists". Task 4 smoke-test in this plan verifies the branch renders correctly in production. |
| T-21-05 | Information Disclosure (LOW) | Backout doc | mitigate | Task 5 acceptance criteria includes `! grep -E "re_[A-Za-z0-9]{20,}" docs/deploy-db-setup.md` — automated guard that the API key is not pasted into the backout doc. The doc references "the Resend API key" by description, never by value. |
</threat_model>

<verification>
Plan 21-02 is complete when:
1. Task 1: `dig` returns expected values for SPF, DKIM, DMARC, MX; Resend dashboard shows Verified ✓; API key created with Send-only scope and stored offline (NOT in evidence/)
2. Task 2: Supabase Dashboard SMTP wired exactly per the field-mapping table; Site URL = `https://horlo.app` (no trailing slash); redirect allowlist includes prod + preview patterns; preflight unconfirmed-users check recorded
3. Task 3: BOTH D-07 clauses passed Inbox-not-Spam (Gmail); From header = `Horlo <noreply@mail.horlo.app>`; reset link round-trips through prod; evidence/d07-gate-result.md says "Overall gate disposition: PASS"
4. Task 4: Three toggles ON in prod; post-flip smoke test confirms signup-form renders "Check your email" (D-10 verification) and confirmation email arrives at Inbox; test users cleaned up
5. Task 5: docs/deploy-db-setup.md has Phase 21 backout section with **Footgun T-21-PREVIEWMAIL:** pattern; PROJECT.md Key Decisions reflects email confirmation ON; REQUIREMENTS.md SMTP-06 marked Deferred (both main section and Traceability table); no API key in docs

Run before merging:
```
npm run lint && npx tsc --noEmit
ls .planning/phases/21-custom-smtp-via-resend/evidence/
grep -q "Phase 21 — Custom SMTP via Resend Backout" docs/deploy-db-setup.md
grep -q "SMTP-06.*Deferred" .planning/REQUIREMENTS.md
```
</verification>

<success_criteria>
SMTP-01: Resend Verified ✓ confirmed (final clause; Plan 21-01 submitted, Plan 21-02 verifies)
SMTP-02: Supabase wired to smtp.resend.com:465 with the field-mapping values verbatim; Invite-User flow successful (D-07 Clause A)
SMTP-03: Confirm email ON in production after D-07 PASSED and signup-form code is merged
SMTP-04: Secure email change + Secure password change ON in production
SMTP-05: docs/deploy-db-setup.md has the Phase 21 backout section using existing footgun pattern
SMTP-06: REQUIREMENTS.md SMTP-06 marked "Deferred — pending staging Supabase project (see Phase 21 21-CONTEXT.md D-01)" per D-01

D-07: Both clauses (Invite + real Gmail signup) passed at Gmail Inbox per D-08; D-09 enforced (no flip on spam-fold)
D-10: signup-form.tsx amend (Plan 21-01) is in production behind the Confirm-email ON toggle, and the post-flip smoke test confirms it
D-11: DMARC `v=DMARC1; p=none;` published and Verified ✓ at Resend (final clause)
</success_criteria>

<output>
After completion, create `.planning/phases/21-custom-smtp-via-resend/21-02-SUMMARY.md` containing:
- Verification of Resend Verified ✓ (date / time captured)
- Supabase SMTP field values as wired (NO API key)
- D-07 gate result (Clause A + Clause B disposition with screenshot file references)
- The three toggles' state in production (with screenshot references)
- Confirmation that signup-form code is merged AND smoke-tested under Confirm-email-ON
- Diff snapshot of docs/deploy-db-setup.md (the new section), PROJECT.md (Key Decisions update), REQUIREMENTS.md (SMTP-06 deferral) — DO NOT include the API key in this summary
- Closing note: phase ready for merge / phase-completion checklist
</output>
