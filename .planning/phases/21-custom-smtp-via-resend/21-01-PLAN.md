---
phase: 21
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/signup/signup-form.tsx
  - .planning/phases/21-custom-smtp-via-resend/evidence/
autonomous: false
requirements:
  - SMTP-01
nyquist_compliant: true
user_setup:
  - service: resend
    why: "Verify mail.horlo.app subdomain at Resend (D-03)"
    env_vars: []
    dashboard_config:
      - task: "Add Domain mail.horlo.app at resend.com/domains; transcribe issued DNS records"
        location: "Resend Dashboard → Domains → Add Domain"
  - service: dns_provider
    why: "Submit SPF + DKIM + DMARC + bounce-MX records at the registrar/DNS provider for horlo.app"
    env_vars: []
    dashboard_config:
      - task: "Add records under mail.horlo.app subdomain"
        location: "DNS provider determined by `dig NS horlo.app +short` (likely Vercel; pivot if elsewhere)"

must_haves:
  truths:
    - "Operator knows the authoritative DNS provider for horlo.app (`dig NS horlo.app +short` was run and result captured)"
    - "Resend dashboard shows mail.horlo.app added with the full record-set issued (SPF + DKIM + bounce MX)"
    - "All records issued by Resend (transcribed verbatim from the dashboard) plus DMARC `v=DMARC1; p=none;` are entered at the DNS provider; pending propagation"
    - "src/app/signup/signup-form.tsx renders an in-card 'Check your email to confirm your account' success state when signUp() returns { session: null }, AND preserves the existing immediate-session redirect path"
    - "Lint + typecheck + build pass on the amended signup-form"
    - "Evidence (screenshots / dig output) is saved under .planning/phases/21-custom-smtp-via-resend/evidence/"
  artifacts:
    - path: "src/app/signup/signup-form.tsx"
      provides: "Confirm-email-ready signup UX (D-10)"
      contains: "Check your email to confirm your account"
    - path: ".planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md"
      provides: "Operator-attested record of which records were submitted, what `dig` returned at submission time, and the Resend record-set screenshotted"
    - path: ".planning/phases/21-custom-smtp-via-resend/evidence/"
      provides: "Screenshot/dig-output dump for SMTP-01 audit trail"
  key_links:
    - from: "Resend dashboard (vendor)"
      to: "DNS provider records under mail.horlo.app"
      via: "Verbatim transcription of records issued by Resend (DKIM count is whatever Resend shows — do not hardcode)"
      pattern: "send.mail.horlo.app TXT (SPF), send.mail.horlo.app MX (Amazon SES feedback), resend._domainkey.mail.horlo.app TXT (DKIM), _dmarc.mail.horlo.app TXT (DMARC)"
    - from: "src/app/signup/signup-form.tsx"
      to: "Supabase Auth signUp() result handling"
      via: "Branch on data.session === null"
      pattern: "if (data?.session) router.push.*else.*setSignupSent"
---

<objective>
Submit DNS records for `mail.horlo.app` at the registrar (so DNS propagation can run asynchronously while we work on other things) AND amend `src/app/signup/signup-form.tsx` so that flipping Confirm-email ON in Plan 21-02 does not silently bounce new signups.

Purpose: DNS propagation is a non-deterministic 15min-24h wait. Plan 21-01 submits the records and walks away — Plan 21-02 picks up only after Resend shows "Verified ✓". The signup-form code change runs in parallel because it has no DNS dependency, and it is a hard prerequisite for the Plan 21-02 toggle flip (D-10 / SMTP-03).

Output:
- DNS records (SPF + DKIM as issued + DMARC + bounce MX) submitted at the DNS provider
- Resend dashboard shows `mail.horlo.app` Pending Verification on every row
- `src/app/signup/signup-form.tsx` renders "Check your email" when `signUp()` returns no session (preserves immediate-session path for backward safety)
- Evidence directory populated with `dig` output + Resend record-set screenshot
- Lint + typecheck + build all green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-custom-smtp-via-resend/21-CONTEXT.md
@.planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md
@.planning/phases/21-custom-smtp-via-resend/21-VALIDATION.md
@docs/deploy-db-setup.md
@src/app/signup/signup-form.tsx

<interfaces>
<!-- Existing client component; the amend preserves the existing happy-path. -->

From src/app/signup/signup-form.tsx (current shape, before amend):
```typescript
'use client'
// uses: useState, useRouter, createSupabaseBrowserClient, Card/CardHeader/CardTitle/CardContent/CardFooter, Input, Button, Label
// state: email, password, error, loading
// onSubmit: supabase.auth.signUp({ email, password }); on success: router.push('/'); router.refresh()
// PROBLEM (D-10): when Confirm-email is ON, signUp returns { data: { user, session: null }, error: null } —
// router.push('/') hits middleware which bounces back to /login because there's no session yet.
```

Amend pattern (D-10): branch on `data.session === null` to render "Check your email" inside the existing CardContent, hiding the form fields. Preserve the immediate-session redirect path for the OFF-toggle case (so toggling Confirm-email on/off does not require additional code changes).

Resend record set shape (RESEARCH.md "DNS Records for mail.horlo.app"):
- TXT @ `send.mail.horlo.app` → `v=spf1 include:amazonses.com ~all`
- MX @ `send.mail.horlo.app` → `feedback-smtp.<region>.amazonses.com` priority 10
- TXT @ `resend._domainkey.mail.horlo.app` → `p=<long-public-key>` (DKIM)
- (Resend may issue 1 or more DKIM records. Transcribe what the dashboard shows.)

DMARC record (D-11):
- TXT @ `_dmarc.mail.horlo.app` → `v=DMARC1; p=none;`  (no rua per CONTEXT.md cost-of-mailbox tradeoff)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Probe DNS provider and add domain at Resend</name>
  <read_first>
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "DNS Provider for `horlo.app`" (lines 110-128)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "DNS Records for `mail.horlo.app`" (lines 129-149)
    - .planning/phases/21-custom-smtp-via-resend/21-CONTEXT.md D-03, D-04, D-05
  </read_first>
  <files>.planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md (NEW)</files>
  <action>
    Step 1 — Probe DNS provider:
    ```
    dig NS horlo.app +short
    ```
    Save the output to `.planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md` under a heading "## NS probe — <date>".
    - If output contains `vercel-dns.com` → DNS is at Vercel; record-add UI is Vercel Dashboard → Domains → horlo.app → DNS records (or `vercel dns add` CLI).
    - If output is anything else (e.g., `cloudflare`, `domain.com`, `googledomains`) → DNS is elsewhere; record-add UI is whatever provider it points to. Note the provider in evidence/dns-submitted.md before continuing.

    Step 2 — Add domain at Resend:
    1. Open https://resend.com/domains
    2. Click "Add Domain" → enter `mail.horlo.app` (per D-03; subdomain, NOT apex)
    3. Resend will issue a record set. Take a full screenshot of the record-set page and save it to `.planning/phases/21-custom-smtp-via-resend/evidence/resend-record-set.png` (do NOT include the API key on this screenshot — the API key is created in a later task).
    4. Transcribe each row verbatim into `.planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md` under a heading "## Resend-issued record set — <date>". Format:
       ```
       | # | Type | Host (FQDN as Resend shows it) | Value | TTL |
       ```
       Do NOT hardcode the DKIM count — there may be 1, 2, or 3 DKIM rows. Transcribe whatever the dashboard shows. Do NOT modify any value.

    Note: leave Resend on this page — Task 2 references the same record set, and the API key (Task 4) is created from a different page (resend.com/api-keys).
  </action>
  <acceptance_criteria>
    - File `.planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md` exists with a "## NS probe" section showing `dig NS horlo.app +short` output
    - File `.planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md` has a "## Resend-issued record set" section listing every row Resend shows, transcribed verbatim
    - File `.planning/phases/21-custom-smtp-via-resend/evidence/resend-record-set.png` exists (screenshot of the Resend dashboard record-set page)
    - DNS provider for horlo.app is named in evidence/dns-submitted.md
    - Verify with: `ls -la .planning/phases/21-custom-smtp-via-resend/evidence/` returns dns-submitted.md and resend-record-set.png
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md && test -f .planning/phases/21-custom-smtp-via-resend/evidence/resend-record-set.png && grep -q "Resend-issued record set" .planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md && grep -q "NS probe" .planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md</automated>
  </verify>
  <done>Operator has run `dig NS horlo.app +short`, added `mail.horlo.app` at Resend, and transcribed the issued record set into evidence/dns-submitted.md with a screenshot saved.</done>
  <resume-signal>Type "records-transcribed" when evidence/dns-submitted.md is complete and the Resend dashboard is showing the full record-set page.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Submit DNS records (SPF + DKIM + DMARC + bounce MX) at the DNS provider</name>
  <read_first>
    - .planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md (the record set transcribed in Task 1)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "DMARC Posture" (lines 150-181)
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Pitfall 2: DNS Records Entered with FQDN Instead of Leftmost Label" (lines 433-441)
    - .planning/phases/21-custom-smtp-via-resend/21-CONTEXT.md D-11 (DMARC `v=DMARC1; p=none;`)
  </read_first>
  <files>.planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md (APPEND)</files>
  <action>
    Open the DNS provider UI identified in Task 1 (typically Vercel Dashboard → Domains → horlo.app → DNS records).

    For EACH row transcribed in evidence/dns-submitted.md "## Resend-issued record set", add the record at the DNS provider. **Critical (Pitfall 2):** the Name field at most providers (including Vercel) expects the LEFTMOST LABEL ONLY, not the FQDN. The provider auto-appends `horlo.app`. Strip the apex before pasting:

    | Resend shows (FQDN) | Enter at DNS provider as Name |
    |---------------------|-------------------------------|
    | `send.mail.horlo.app` | `send.mail` |
    | `resend._domainkey.mail.horlo.app` | `resend._domainkey.mail` |
    | (any other host Resend issued) | strip `.horlo.app` from the right |

    For each row:
    - Type: as Resend shows (TXT, MX, or CNAME — copy exactly)
    - Name: leftmost portion only (per table above)
    - Value: paste verbatim (no whitespace, no line breaks injected)
    - For MX: priority 10
    - TTL: 60 (Vercel default; equivalent low TTL elsewhere)

    Then add the DMARC record (D-11):
    - Type: TXT
    - Name: `_dmarc.mail`  (resolves to `_dmarc.mail.horlo.app`)
    - Value: `v=DMARC1; p=none;`  (no `rua=` — CONTEXT.md does not commit to a reporting mailbox)
    - TTL: 60

    After adding each record, hover/inspect the saved row in the provider UI and confirm the resolved FQDN matches what Resend showed (not `send.mail.horlo.app.horlo.app` — that's the Pitfall 2 footgun).

    Append to evidence/dns-submitted.md a "## Records submitted at <provider> — <date>" section listing each (Type, Name as entered, resolved FQDN as shown by provider). Take a screenshot of the provider's records list and save it as `evidence/dns-provider-records.png`.

    Do NOT wait for verification. DNS propagation is async; Plan 21-02 picks up after Resend shows Verified ✓. This task ends as soon as the records are submitted and the resolved FQDNs are confirmed.
  </action>
  <acceptance_criteria>
    - Every row from the Resend record set is present in the DNS provider's records list with the LEFTMOST-LABEL Name (not the FQDN — per Pitfall 2)
    - DMARC TXT record `v=DMARC1; p=none;` exists at `_dmarc.mail.horlo.app` per D-11
    - evidence/dns-submitted.md has a "## Records submitted at <provider>" section
    - evidence/dns-provider-records.png screenshot exists showing the saved records
    - For at least one record (e.g., the SPF), `dig +short TXT send.mail.horlo.app` MAY still return empty immediately (propagation pending) — that's expected; do NOT block on it
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Records submitted at" .planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md && grep -q "_dmarc.mail" .planning/phases/21-custom-smtp-via-resend/evidence/dns-submitted.md && test -f .planning/phases/21-custom-smtp-via-resend/evidence/dns-provider-records.png</automated>
  </verify>
  <done>Every Resend-issued record is at the DNS provider under leftmost-label Name; DMARC `v=DMARC1; p=none;` is published at `_dmarc.mail.horlo.app`; screenshot + transcription saved to evidence/.</done>
  <resume-signal>Type "records-submitted" when all DNS rows + DMARC are saved at the provider. Resend Verified ✓ is NOT required at this point — Plan 21-02 waits for it.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Amend signup-form.tsx for the no-session signup path (D-10 prerequisite for SMTP-03)</name>
  <read_first>
    - src/app/signup/signup-form.tsx (the file being modified)
    - .planning/phases/21-custom-smtp-via-resend/21-CONTEXT.md D-10
    - .planning/phases/21-custom-smtp-via-resend/21-RESEARCH.md "Pitfall 1: Confirm-email Flip Breaks Signup-Form Redirect" (lines 398-432)
  </read_first>
  <files>src/app/signup/signup-form.tsx</files>
  <behavior>
    - When `supabase.auth.signUp()` returns `{ data: { session: null }, error: null }` (Confirm-email ON path):
      - Set a `signupSent` state to `true`
      - Render an in-card success state inside the existing CardContent: hide the email/password Input fields, hide the submit button, render `<p>Check your email to confirm your account.</p>` (or similar plain-prose copy that does NOT differentiate between new-account and already-exists scenarios — per T-21-04 user-enumeration mitigation)
      - Do NOT call `router.push('/')`
      - Show the email the user entered as confirmation: e.g., `<p className="text-sm text-muted-foreground">Confirmation sent to {email}.</p>`
    - When `supabase.auth.signUp()` returns `{ data: { session: <truthy> }, error: null }` (Confirm-email OFF path — current behavior):
      - Continue calling `router.push('/')` and `router.refresh()` exactly as today (preserves backward safety so the OFF-toggle staging path still works)
    - When `supabase.auth.signUp()` returns an error: existing neutral copy "Could not create account." stays unchanged (T-21-04)
    - Loading state: while signup is in flight, button shows "Creating account…" (existing); after success in the no-session path, button is hidden, replaced by the success message
  </behavior>
  <action>
    Edit `src/app/signup/signup-form.tsx`:

    1. Replace the destructure on line 23 from `const { error: err } = await supabase.auth.signUp({ email, password })` to:
       ```typescript
       const { data, error: err } = await supabase.auth.signUp({ email, password })
       ```
    2. Add a new state variable: `const [signupSent, setSignupSent] = useState(false)`
    3. Replace the success-path block (lines 30-32 today: `// Email confirmations are off (D-09)... router.push('/'); router.refresh()`) with:
       ```typescript
       if (!data.session) {
         // Confirm-email is ON server-side; user must click the email link before logging in (D-10).
         setSignupSent(true)
         setLoading(false)
         return
       }
       // Immediate session (Confirm-email OFF — preserved for backward safety / staging).
       router.push('/')
       router.refresh()
       ```
    4. In the JSX, branch the CardContent body on `signupSent`:
       - If `signupSent === true`: render
         ```tsx
         <CardContent className="space-y-3">
           <p className="text-sm">Check your email to confirm your account.</p>
           <p className="text-muted-foreground text-sm">Confirmation sent to {email}.</p>
         </CardContent>
         ```
         and HIDE the existing CardFooter submit button (e.g., wrap the form in `{!signupSent && (<form ...>...</form>)}` plus a parallel `{signupSent && (<CardContent>...</CardContent>)}`, OR keep the form but conditionally render its body — pick whichever yields cleaner JSX given the existing `<form onSubmit={handleSubmit}>` wrapping CardContent + CardFooter).
       - If `signupSent === false` (default): existing form renders unchanged.
    5. Leave the "Already have an account? Sign in" link visible in both states (CardFooter or below the success message) so the user is never stranded.
    6. Do NOT add an additional comment about D-09 — the existing comment is stale; replace it with a comment referencing D-10. Sample comment: `// D-10: When Confirm-email is ON, signUp returns no session until the email link is clicked.`
    7. Do NOT change the error-path copy (`'Could not create account.'`) — T-21-04 user-enumeration mitigation.

    After editing, run lint + typecheck + build:
    ```
    npm run lint
    npx tsc --noEmit
    npm run build
    ```
    All three must pass. If any fail, fix the JSX/TS issue and re-run.
  </action>
  <acceptance_criteria>
    - `grep -F "data.session" src/app/signup/signup-form.tsx` returns at least one match (the new branch)
    - `grep -F "signupSent" src/app/signup/signup-form.tsx` returns at least 2 matches (state declaration + render branch)
    - `grep -F "Check your email" src/app/signup/signup-form.tsx` returns the success-state copy
    - `grep -F "router.push('/')" src/app/signup/signup-form.tsx` STILL returns a match (preserved backward path)
    - The error copy `'Could not create account.'` is unchanged: `grep -F "Could not create account." src/app/signup/signup-form.tsx` returns 1 match
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 (production build succeeds)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "data.session" src/app/signup/signup-form.tsx && grep -q "signupSent" src/app/signup/signup-form.tsx && grep -q "Check your email" src/app/signup/signup-form.tsx && grep -q "router.push('/')" src/app/signup/signup-form.tsx && grep -q "Could not create account." src/app/signup/signup-form.tsx && npm run lint && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>signup-form.tsx renders "Check your email to confirm your account" when signUp returns no session AND preserves the immediate-session redirect path; lint + typecheck + build all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Resend Dashboard | Manual data entry of subdomain; Resend issues record set |
| Operator → DNS Provider | Manual record entry; one record per row, leftmost-label Name |
| User browser → signup-form.tsx → Supabase Auth | Existing trust boundary, no new surface — but the no-session branch must not leak whether an email already exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Spoofing | DNS records for mail.horlo.app | mitigate | Publish DMARC `v=DMARC1; p=none;` (D-11) alongside SPF + DKIM in Task 2 — denies attackers an unauthenticated path; aligns Gmail's spam classifier signal so D-09 spam-fold investigation is rare. |
| T-21-04 | Information Disclosure | signup-form.tsx success state | mitigate | The new "Check your email" success state in Task 3 MUST render identical copy whether the email already exists or is new — Supabase returns the same `{ user, session: null }` shape in both cases, so as long as the form does not introspect `data.user` to differentiate, no enumeration leak is possible. The implementation in Task 3 only branches on `data.session`, NOT on `data.user.email_confirmed_at` or similar. Verified by code-review checklist in Task 3 acceptance criteria. |
| T-21-03 | Tampering / sequencing | Account state during phase | mitigate | The signup-form code change in Task 3 SHIPS BEFORE the toggle flip in Plan 21-02. Plan 21-02 explicitly depends on Plan 21-01. This plan's only Wave is Wave 1; Plan 21-02 is Wave 2. The dependency in this plan's frontmatter (`depends_on: []`) and Plan 21-02's (`depends_on: [21-01]`) encodes the sequencing. |
| T-21-05 (defensive) | Information Disclosure | evidence/ directory | mitigate | Evidence files (dns-submitted.md, screenshots) MUST NOT include the Resend API key. Task 1's screenshot is taken on the record-set page (no API key visible); the API key is created in Plan 21-02 and stored in 1Password / `.env.local`, never in evidence/. |
| T-21-01 (deferred) | Information Disclosure / Credential leak | Resend API key | accept-for-this-plan | API key is NOT created in Plan 21-01 (it's created in Plan 21-02 Task 1). Plan 21-01 only submits DNS records; nothing sensitive is generated yet. |
</threat_model>

<verification>
Plan 21-01 is complete when:
1. `dig NS horlo.app +short` was run and recorded; DNS provider is named in evidence/
2. Resend dashboard shows `mail.horlo.app` added with the full issued record-set screenshotted
3. Every record (SPF + DKIM as issued + bounce MX + DMARC `v=DMARC1; p=none;`) is submitted at the DNS provider with leftmost-label Name (Pitfall 2 mitigation)
4. `src/app/signup/signup-form.tsx` has the no-session branch + signupSent state + "Check your email" copy + preserves the existing immediate-session redirect
5. Lint + typecheck + build all green on the amended signup-form
6. Evidence directory has dns-submitted.md, resend-record-set.png, dns-provider-records.png
7. NO API key created yet (deferred to Plan 21-02 Task 1)
8. NO Supabase SMTP wiring yet (deferred to Plan 21-02)
9. Plan 21-02 is BLOCKED on Resend showing "Verified ✓" — that wait is asynchronous and does NOT belong inside Plan 21-01

Run before merging:
```
npm run lint && npx tsc --noEmit && npm run build
ls .planning/phases/21-custom-smtp-via-resend/evidence/
```
</verification>

<success_criteria>
SMTP-01 (partial — submit phase): every Resend-issued DNS record + DMARC TXT submitted at the DNS provider; evidence captured.
D-10: signup-form.tsx renders "Check your email" on no-session signup; preserves the OFF-toggle redirect.
D-11: DMARC `v=DMARC1; p=none;` published at `_dmarc.mail.horlo.app`.

Final SMTP-01 verification (Verified ✓ at Resend) happens at the start of Plan 21-02. This plan ends with records-submitted state.
</success_criteria>

<output>
After completion, create `.planning/phases/21-custom-smtp-via-resend/21-01-SUMMARY.md` containing:
- Which DNS provider was identified (via `dig NS`)
- Verbatim list of records submitted (Type / Name / Value / TTL)
- Confirmation that signup-form.tsx amend passed lint + typecheck + build
- Screenshot file references
- A note that Plan 21-02 must NOT start until `dig +short TXT send.mail.horlo.app` returns the SPF value AND Resend shows Verified ✓ on every row
</output>
