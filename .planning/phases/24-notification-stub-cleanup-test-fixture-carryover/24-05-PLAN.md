---
phase: 24-notification-stub-cleanup-test-fixture-carryover
plan: 05
type: execute
wave: 3
depends_on: ['24-03']
files_modified:
  - tests/integration/phase12-visibility-matrix.test.ts
  - tests/integration/home-privacy.test.ts
  - tests/data/getFeedForUser.test.ts
  - tests/data/getWearRailForViewer.test.ts
autonomous: true
requirements: [DEBT-06]
requirements_addressed: [DEBT-06]

must_haves:
  truths:
    - "No file under `tests/` references `wornPublic` or `worn_public` (excluding history-protected `// removed in Phase 12` comments if intentionally preserved)"
    - "All 4 wornPublic test files compile (`npx tsc --noEmit`) and the suite passes (`npm test`)"
    - "`tests/integration/home-privacy.test.ts` produces an identical pass/fail set BEFORE and AFTER the fixture rewrite (privacy-semantics regression-lock per RESEARCH.md threat T-24-FIXTURE)"
    - "The `_wornPublic` parameter at `tests/data/getWearRailForViewer.test.ts:363` is removed entirely (helper signature cleaned)"
  artifacts:
    - path: "tests/integration/phase12-visibility-matrix.test.ts"
      provides: "Phase 12 architecture assertions rewritten to reference wear_events.visibility (per D-04 positive-assertion rewrite rule)"
    - path: "tests/integration/home-privacy.test.ts"
      provides: "privacyByUser fixture narrowed to actual columns; wear-event seeds use wear_visibility enum"
    - path: "tests/data/getFeedForUser.test.ts"
      provides: "Phase-12 wornPublic anchor tests removed or rewritten to positive wear_events.visibility assertions"
    - path: "tests/data/getWearRailForViewer.test.ts"
      provides: "wornPublic fixture properties + _wornPublic parameter + Phase-12 negative-assertion tests removed; wear_visibility seeds substituted where semantics matter"
  key_links:
    - from: "test fixture seeds"
      to: "wear_events.visibility column"
      via: "explicit `visibility: 'public'` (or 'followers' / 'private') on db.insert(wearEvents)"
      pattern: "visibility: '(public|followers|private)'"
---

<objective>
Migrate 4 test files from the v3.0-removed `wornPublic` column to the v3.0 `wear_visibility` per-row enum on `wear_events`. Per D-04: rewrite negative-assertion tests as positive `wear_visibility` assertions where the architectural intent is worth preserving; delete one-shot Phase-12-cutover anchors that cannot regress; migrate fixture data shapes (delete `wornPublic` properties from seeds; substitute explicit `visibility` per-row where semantics matter).

Purpose: Implements DEBT-06. Per RESEARCH.md §"Empirical Reconciliation: 4 files, not 9", only 4 test files reference `wornPublic` in the current tree (the roadmap "9 files" count was historical or counted reference sites). Per RESEARCH.md threat T-24-FIXTURE: home-privacy.test.ts privacy semantics MUST regress-lock — run BEFORE and AFTER, identical pass/fail set.

Output: Four test files cleaned of `wornPublic` references; helper-signature `_wornPublic` parameter deleted; full suite green; privacy-semantics regression-lock confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-CONTEXT.md
@.planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-RESEARCH.md
@.planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-VALIDATION.md

# The 4 files this plan modifies — read each before touching:
@tests/integration/phase12-visibility-matrix.test.ts
@tests/integration/home-privacy.test.ts
@tests/data/getFeedForUser.test.ts
@tests/data/getWearRailForViewer.test.ts

# Schema reference for verification:
@src/db/schema.ts

# Sibling migration documenting the column removal:
@supabase/migrations/20260424000001_phase12_drop_worn_public.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture privacy-semantics baseline (T-24-FIXTURE regression-lock setup)</name>
  <files>(no files modified — this task captures a baseline before edits begin)</files>
  <read_first>
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-RESEARCH.md §"Common Pitfalls / Pitfall 5" (privacy regression risk explanation)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-VALIDATION.md §"Manual-Only Verifications" row 4 (the regression-lock procedure)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-RESEARCH.md threat table (T-24-FIXTURE row)
  </read_first>
  <action>
Run the two privacy-semantics test files BEFORE making any changes and capture the pass/fail set. This is mandatory per RESEARCH.md threat T-24-FIXTURE — fixture migration must NOT broaden access semantics; before/after pass-set must be identical.

```bash
# BASELINE — run BEFORE any edits in Tasks 2-5
npm test -- --run tests/integration/home-privacy.test.ts tests/integration/phase12-visibility-matrix.test.ts > /tmp/phase24-privacy-baseline.txt 2>&1
echo "BASELINE EXIT: $?" >> /tmp/phase24-privacy-baseline.txt
```

The output goes to `/tmp/phase24-privacy-baseline.txt` (not committed; comparison artifact). The baseline may include some currently-failing tests because `wornPublic` referenced in the current code is a stale shape — those failures are EXPECTED and form the baseline against which post-rewrite must be compared.

Note: if `home-privacy.test.ts` currently fails at TypeScript compile time (because Drizzle's `profileSettings` schema does not include `wornPublic` per RESEARCH.md File 2 analysis), the baseline will show that failure. The post-rewrite suite must produce a NARROWER failure set (the TS compile failure goes away; no new failures appear). This is the regression-lock.

This task produces no file changes — it ONLY captures the comparison artifact. Proceed to Task 2.
  </action>
  <verify>
    <automated>npm test -- --run tests/integration/home-privacy.test.ts tests/integration/phase12-visibility-matrix.test.ts 2>&amp;1 | tee /tmp/phase24-privacy-baseline.txt &gt; /dev/null; echo "captured"; test -s /tmp/phase24-privacy-baseline.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `/tmp/phase24-privacy-baseline.txt` exists and is non-empty
    - File contains baseline test output for both `home-privacy.test.ts` and `phase12-visibility-matrix.test.ts`
    - File documents which tests pass / fail / error in the current state (pre-rewrite reference)
  </acceptance_criteria>
  <done>Baseline captured for regression comparison after Tasks 2-5 land.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite tests/integration/phase12-visibility-matrix.test.ts (DEBT-06 file 1 of 4)</name>
  <files>tests/integration/phase12-visibility-matrix.test.ts</files>
  <read_first>
    - tests/integration/phase12-visibility-matrix.test.ts (full file — 4 wornPublic occurrences at lines 99, 188, 226, 315-329 per RESEARCH.md)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-RESEARCH.md §"Per-File `wornPublic` Rewrite Plan / File 1" lines 508-519 (per-line rewrite recipe)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-CONTEXT.md §"wornPublic Regression-Lock Tests" D-04 (the rewrite rule)
  </read_first>
  <action>
Apply 4 edits to `tests/integration/phase12-visibility-matrix.test.ts`:

1. **Line 99 comment** (`// wornPublic column dropped in Phase 12 Plan 06...`): Delete the entire comment line. The column was dropped 2026-04-24, no longer noteworthy in v4.0.

2. **Line 188 comment** (`// Even though Pp's wear is visibility='public' and wornPublic=true on // profileSettings, ...`): Delete the `wornPublic=true on profileSettings` clause from the multi-line comment. The remaining comment should reference only `visibility='public'` and `profile_public=false` semantics.

3. **Line 226 comment** (`// S follows nobody, so S's rail should not contain Of's tile even if // Of has a visibility='followers' wear with wornPublic=true on profileSettings.`): Delete the `wornPublic=true on profileSettings` clause; the remaining comment focuses on `visibility='followers'` and follow-relationship semantics.

4. **Lines 315-329** (`it('WYWT-11: profile_settings.worn_public column dropped post-migration', ...)`): Delete the entire `it(...)` block. Per D-04 + RESEARCH.md File 1: this is a one-shot Phase-12-cutover anchor that asserted the migration ran; it cannot regress (Drizzle's schema doesn't have `worn_public`; any future migration adding it would have to be explicit). The test served its purpose.

After edits:
- `grep -E "wornPublic|worn_public" tests/integration/phase12-visibility-matrix.test.ts` returns zero hits.
- The remaining tests in the file (lines 166-283 visibility-matrix coverage) are untouched — they already use positive `wear_visibility` assertions per RESEARCH.md.

Run the file's tests:
```bash
npm test -- --run tests/integration/phase12-visibility-matrix.test.ts
```
Surviving tests must pass.
  </action>
  <verify>
    <automated>! grep -E "wornPublic|worn_public" tests/integration/phase12-visibility-matrix.test.ts &amp;&amp; npm test -- --run tests/integration/phase12-visibility-matrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "wornPublic|worn_public" tests/integration/phase12-visibility-matrix.test.ts` returns zero hits
    - File no longer contains `it('WYWT-11: profile_settings.worn_public column dropped post-migration'`
    - `npm test -- --run tests/integration/phase12-visibility-matrix.test.ts` exits 0
    - Remaining tests in the file (visibility-matrix coverage) are unchanged in count (verify by counting `it(` before/after — only the WYWT-11 test should be removed)
  </acceptance_criteria>
  <done>4 wornPublic occurrences cleaned; one stale anchor test deleted; surviving tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite tests/integration/home-privacy.test.ts fixture data shape (DEBT-06 file 2 of 4)</name>
  <files>tests/integration/home-privacy.test.ts</files>
  <read_first>
    - tests/integration/home-privacy.test.ts (full file — 7 occurrences at lines 100-107 in `privacyByUser` fixture; comment at line ~10 may also reference wornPublic)
    - src/db/schema.ts (line 239 `wear_visibility` enum default — verifying default behavior covers seeds without explicit visibility)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-RESEARCH.md §"Per-File `wornPublic` Rewrite Plan / File 2" lines 521-560 (verbatim BEFORE/AFTER fixture shape)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-RESEARCH.md §"Common Pitfalls / Pitfall 5" (regression-lock procedure)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-RESEARCH.md §"Assumptions Log / A2" (default 'public' on wear_events.visibility verified at src/db/schema.ts:239)
  </read_first>
  <action>
Apply 2 edits to `tests/integration/home-privacy.test.ts`:

1. **Lines 100-107 (`privacyByUser` fixture)** — Narrow the type signature and seed values to remove `wornPublic`:

BEFORE (current — broken because column doesn't exist):
```ts
const privacyByUser: Record<
  string,
  { profilePublic: boolean; collectionPublic: boolean; wishlistPublic: boolean; wornPublic: boolean }
> = {
  [ids.V]: { profilePublic: true, collectionPublic: true, wishlistPublic: true, wornPublic: true },
  // ... 5 more rows with wornPublic: true ...
}
```

AFTER:
```ts
const privacyByUser: Record<
  string,
  { profilePublic: boolean; collectionPublic: boolean; wishlistPublic: boolean }
> = {
  [ids.V]: { profilePublic: true, collectionPublic: true, wishlistPublic: true },
  [ids.A]: { profilePublic: true, collectionPublic: true, wishlistPublic: true },
  [ids.B]: { profilePublic: true, collectionPublic: false, wishlistPublic: true },
  [ids.C]: { profilePublic: false, collectionPublic: true, wishlistPublic: true },
  [ids.D]: { profilePublic: true, collectionPublic: true, wishlistPublic: true },
  [ids.E]: { profilePublic: false, collectionPublic: true, wishlistPublic: true },
}
```

(Verify exact `ids.X` keys by reading the existing file — preserve whatever set is there; only the `wornPublic` field is dropped.)

2. **Wear event seed verification (lines 165-171 currently per RESEARCH.md File 2):** Per RESEARCH.md A2, `wear_events.visibility` defaults to `'public'` (verified at `src/db/schema.ts:239`). The current seeds use the default — that works correctly today as a no-op fix. To make the privacy semantics explicit (defensive), add `visibility: 'public' as const` to each `db.insert(wearEvents).values(...)` call where the test logic relies on the wear being public. Use the same value the test currently relies on by reading the test logic; do NOT broaden access (would violate T-24-FIXTURE).

   **CRITICAL:** If the existing seeds rely on the default `'public'`, leaving them unchanged is also acceptable — the regression-lock confirms semantics. Make them explicit ONLY if doing so doesn't change pass/fail outcomes. If in doubt, leave seeds unchanged.

3. **Top-of-file comment** (around line ~10 if present) referencing `wornPublic` privacy gates: update to reference `wear_visibility` per-row gating.

After edits:
- `grep -E "wornPublic|worn_public" tests/integration/home-privacy.test.ts` returns zero hits.
- Run the test file:
```bash
npm test -- --run tests/integration/home-privacy.test.ts
```

**Regression-lock check:** Compare the pass/fail set with `/tmp/phase24-privacy-baseline.txt` from Task 1. The post-rewrite result must be NARROWER or IDENTICAL — no test that passed before may now fail; no NEW assertion may pass that was failing before in a way that broadens access.
  </action>
  <verify>
    <automated>! grep -E "wornPublic|worn_public" tests/integration/home-privacy.test.ts &amp;&amp; npm test -- --run tests/integration/home-privacy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "wornPublic|worn_public" tests/integration/home-privacy.test.ts` returns zero hits
    - `privacyByUser` fixture type signature has 3 keys (`profilePublic`, `collectionPublic`, `wishlistPublic`), NOT 4
    - `npm test -- --run tests/integration/home-privacy.test.ts` exits 0
    - Pass-set comparison against `/tmp/phase24-privacy-baseline.txt` shows NO regression — every test that passed in baseline still passes; no new broadening-of-access tests pass
  </acceptance_criteria>
  <done>Fixture data shape migrated; wornPublic references gone; privacy regression-lock confirmed.</done>
</task>

<task type="auto">
  <name>Task 4: Clean tests/data/getFeedForUser.test.ts Phase-12 anchor tests (DEBT-06 file 3 of 4)</name>
  <files>tests/data/getFeedForUser.test.ts</files>
  <read_first>
    - tests/data/getFeedForUser.test.ts (full file — 3 occurrences per RESEARCH.md File 3: lines 191, 213, 240)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-RESEARCH.md §"Per-File `wornPublic` Rewrite Plan / File 3" lines 562-578 (per-test recommendations)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-CONTEXT.md §"wornPublic Regression-Lock Tests" D-04 (delete-or-rewrite discretion rule)
  </read_first>
  <action>
Apply per-test recommendations from RESEARCH.md File 3:

1. **Line 191** (`it('Phase 12: where clause contains no reference to wornPublic', ...)`): **DELETE** entire test. The column is gone from the schema; the negative assertion is dead weight. The positive replacement already exists at line 213 (`'Phase 12: where clause references activities.metadata->>'visibility' for watch_worn branch'`) — it locks the architectural intent.

2. **Line 213** (`'Phase 12: where clause references activities.metadata->>'visibility' for watch_worn branch'`): **KEEP UNCHANGED**. This is the rewritten target — already a positive assertion against the v3.0 architecture. Do NOT modify.

3. **Line 240** (`'Phase 12: select projection contains no wornPublic field'`): **DELETE** entire test. The column is gone; the projection cannot include it; negative assertion is dead.

   Optional alternative: rewrite to `'select projection includes activities.metadata visibility check'` if the planner prefers locking that the visibility metadata is read at projection time. **Recommend DELETE** per RESEARCH.md "Net delta: -2 tests, +0 tests" rationale.

Also: any top-of-file comment referencing wornPublic (RESEARCH.md File 3 mentions a comment) — update to reference `wear_events.visibility` / `activities.metadata->>'visibility'`.

After edits:
- `grep -E "wornPublic|worn_public" tests/data/getFeedForUser.test.ts` returns zero hits.
- Run the file:
```bash
npm test -- --run tests/data/getFeedForUser.test.ts
```
Surviving tests must pass. Net delta: -2 tests (the two deletions); test 213 remains as the architectural-intent anchor.
  </action>
  <verify>
    <automated>! grep -E "wornPublic|worn_public" tests/data/getFeedForUser.test.ts &amp;&amp; npm test -- --run tests/data/getFeedForUser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "wornPublic|worn_public" tests/data/getFeedForUser.test.ts` returns zero hits
    - File no longer contains `'Phase 12: where clause contains no reference to wornPublic'`
    - File no longer contains `'Phase 12: select projection contains no wornPublic field'`
    - File still contains `'Phase 12: where clause references activities.metadata` (the positive assertion that locks v3.0 architecture)
    - `npm test -- --run tests/data/getFeedForUser.test.ts` exits 0
  </acceptance_criteria>
  <done>Two negative-assertion Phase-12 anchor tests deleted; positive assertion preserved; remaining tests pass.</done>
</task>

<task type="auto">
  <name>Task 5: Clean tests/data/getWearRailForViewer.test.ts + remove _wornPublic helper parameter (DEBT-06 file 4 of 4)</name>
  <files>tests/data/getWearRailForViewer.test.ts</files>
  <read_first>
    - tests/data/getWearRailForViewer.test.ts (full file — 7 occurrences per RESEARCH.md File 4: lines 7, 124, 139, 164, 188, 275-297, 299-327, 363, 448-451)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-RESEARCH.md §"Per-File `wornPublic` Rewrite Plan / File 4" lines 580-595 (verbatim per-line rewrite recipe)
    - .planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-RESEARCH.md §"Specific Ideas" lines 178-184 (the `_wornPublic` parameter is a leftover compat-stub — D-04 explicitly calls this out for deletion)
  </read_first>
  <action>
Apply 8 edits to `tests/data/getWearRailForViewer.test.ts`:

1. **Line 7 comment** (`self-or-wornPublic privacy gate`): Replace `wornPublic` with `wear_visibility` to reflect v3.0 architecture.

2. **Line 124** (`wornPublic: true,` in fixture row): **DELETE the property** — Drizzle's `wearEvents` schema doesn't have it.

3. **Line 139** (`wornPublic: true,` in fixture row): **DELETE the property**.

4. **Line 164** (`wornPublic: false, // private, but self-rule admits it`): **DELETE the property**. Update the inline comment to reference the row's `visibility: 'private'` (which is already present per RESEARCH.md).

5. **Line 188** (`wornPublic: true,` in fixture row): **DELETE the property** — the row also has `visibility: 'public'`; the wornPublic was a transitional artifact.

6. **Lines 275-297** (`it('Unit 10 (Phase 12): where clause contains no reference to wornPublic', ...)`): **DELETE** entire test. Unit 9 at line 269 already asserts the leftJoin against follows is in place, which is the positive-shape assertion that locks architectural intent.

7. **Lines 299-327** (`it('Unit 11 (Phase 12): select projection contains no wornPublic field', ...)`): **DELETE** entire test. Per RESEARCH.md File 4: dead-weight negative assertion; column cannot regress.

8. **Line 363** (`_wornPublic = true,` parameter in `seedProfile` helper): **DELETE the parameter entirely**. Per RESEARCH.md §"Specific Ideas" + D-04: the underscore prefix already signals "unused"; remove from the helper signature. TS will guide deletion of every caller's positional arg.

   Caller sites at lines 448-451: each call site passes a positional value (`false` or `true`) as the last arg. Remove that argument from each call. Lines 448 (`seedProfile(alice, ...)`), 449 (`seedProfile(carol, ...)`), 450 (`seedProfile(bob, ..., false)` — drops the `false`), 451 (`seedProfile(...)`) — adjust each.

   **Semantics check:** If line 450's `seedProfile(bob, ..., false)` was modeling "bob's worn_public is false" — that semantics is dead with the column. Verify downstream assertions in this integration test don't depend on bob being worn-private; if they do, add `visibility: 'private'` to bob's wear-event seed instead. (Read the test logic carefully — this is the one place where deleting the parameter could change test semantics.)

After all 8 edits:
- `grep -E "wornPublic|worn_public" tests/data/getWearRailForViewer.test.ts` returns zero hits (this is part of Validation row {N}-05-03).
- `grep -n '_wornPublic' tests/data/getWearRailForViewer.test.ts` returns zero hits (Validation row {N}-05-02).
- Run the file:
```bash
npm test -- --run tests/data/getWearRailForViewer.test.ts
```
Surviving tests must pass. Net delta: -2 Phase-12 anchor tests; -1 helper parameter; -4 fixture properties; +0 new tests.

Then run the full suite to confirm no cascade:
```bash
npm test
```
Full suite must be green. Privacy regression-lock comparison: pass-set must be NARROWER or IDENTICAL vs `/tmp/phase24-privacy-baseline.txt`.
  </action>
  <verify>
    <automated>! grep -E "wornPublic|worn_public" tests/data/getWearRailForViewer.test.ts &amp;&amp; ! grep -n '_wornPublic' tests/data/getWearRailForViewer.test.ts &amp;&amp; npm test -- --run tests/data/getWearRailForViewer.test.ts &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "wornPublic|worn_public" tests/data/getWearRailForViewer.test.ts` returns zero hits
    - `grep '_wornPublic' tests/data/getWearRailForViewer.test.ts` returns zero hits
    - File no longer contains `'Unit 10 (Phase 12): where clause contains no reference to wornPublic'`
    - File no longer contains `'Unit 11 (Phase 12): select projection contains no wornPublic field'`
    - `npm test -- --run tests/data/getWearRailForViewer.test.ts` exits 0
    - `npm test` (full suite, including all earlier plans) exits 0
    - Privacy regression-lock holds: comparison against `/tmp/phase24-privacy-baseline.txt` shows no broadening-of-access regression
  </acceptance_criteria>
  <done>4 fixture properties deleted; helper parameter + caller args cleaned; 2 stale Phase-12 anchor tests deleted; full suite green; privacy regression-lock confirmed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture data shape -> production code paths | Fixtures seed test DBs only; production code paths are never reached by these tests. Risk surface is whether changed fixtures cause tests to ASSERT the wrong privacy behavior — a false-OK. |
| `wear_events.visibility` default value | `'public'` per `src/db/schema.ts:239`. Existing seeds without explicit `visibility` rely on this default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-FIXTURE-01 | Information Disclosure | Test-side fixture migration loses privacy semantics → false-OK on a real privacy regression | mitigate | Task 1 captures BASELINE pass/fail set BEFORE edits; Tasks 3 + 5 verify post-edit pass/fail set is NARROWER OR IDENTICAL (no broadening). Documented in RESEARCH.md threat T-24-FIXTURE + Pitfall 5. |
| T-24-FIXTURE-02 | Tampering | Helper signature change (`_wornPublic` removal in Task 5) silently changes test semantics where caller passes `false` | mitigate | Task 5 explicitly checks "if line 450's `seedProfile(bob, ..., false)` was modeling worn-private bob, that semantics is dead — verify downstream assertions; add `visibility: 'private'` if needed." TS errors on the positional-arg deletion guide every caller. |
| T-24-FIXTURE-03 | Information Disclosure | Stale top-of-file comments suggesting wornPublic is still active mislead future devs | accept | Tasks 2-5 update comments where present. Some `// removed in Phase 12` history-marker comments may be intentionally preserved as documentation; planner allows discretion. |

</threat_model>

<verification>

```bash
# Per-file grep guards (Validation map row {N}-05-03)
! grep -rE "wornPublic|worn_public" tests/ 2>/dev/null

# Helper parameter removal (Validation map row {N}-05-02)
! grep -n '_wornPublic' tests/data/getWearRailForViewer.test.ts

# Per-file test runs (Validation map row {N}-05-01)
npm test -- --run tests/integration/phase12-visibility-matrix.test.ts tests/integration/home-privacy.test.ts tests/data/getFeedForUser.test.ts tests/data/getWearRailForViewer.test.ts

# Full suite green
npm test

# Privacy regression-lock — diff post-rewrite vs baseline
npm test -- --run tests/integration/home-privacy.test.ts tests/integration/phase12-visibility-matrix.test.ts > /tmp/phase24-privacy-after.txt 2>&1
# Compare /tmp/phase24-privacy-baseline.txt vs /tmp/phase24-privacy-after.txt — post must be narrower or identical
```

</verification>

<success_criteria>

- [x] Baseline captured (Task 1) for regression comparison
- [x] All 4 wornPublic test files compile + pass post-rewrite
- [x] Zero `wornPublic` / `worn_public` hits anywhere under `tests/`
- [x] `_wornPublic` parameter removed from helper signature
- [x] Privacy regression-lock holds (no broadening of access)
- [x] Full suite green
- [x] Threat model documented (T-24-FIXTURE-01..03)

</success_criteria>

<output>
After completion, create `.planning/phases/24-notification-stub-cleanup-test-fixture-carryover/24-05-SUMMARY.md`
</output>
