---
phase: 36
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/0009_phase36_layer_c_variants.sql
  - drizzle/meta/_journal.json
autonomous: true
requirements: [CAT-17, CAT-14]
tags: [drizzle, migration, idempotent, journal]
threat_refs: [T-36-05]

must_haves:
  truths:
    - "drizzle/0009_phase36_layer_c_variants.sql exists with idempotent CREATE TABLE IF NOT EXISTS + ADD COLUMN IF NOT EXISTS + DO $$ FK guards (Phase 34/35 pattern)"
    - "drizzle/meta/_journal.json appended with idx=9 entry for tag 0009_phase36_layer_c_variants — required so drizzle-kit migrate picks the file up (STATE.md Phase 34 Plan 01 lesson)"
    - "Drizzle migration carries NO RLS, NO GRANT, NO DO $$ pre-flight, NO CHECK — those live exclusively in the Supabase migration"
    - "FK guards use DO $$ IF NOT EXISTS pg_constraint pattern verbatim from Phase 35's drizzle/0008_phase35_layer_b.sql"
  artifacts:
    - path: "drizzle/0009_phase36_layer_c_variants.sql"
      provides: "Idempotent Drizzle structural twin migration; locally re-runnable after `supabase db reset` + `npx drizzle-kit push`"
      contains: "CREATE TABLE IF NOT EXISTS \"watch_variants\""
    - path: "drizzle/meta/_journal.json"
      provides: "Journal entry idx=9 marking tag 0009_phase36_layer_c_variants as applicable"
      contains: "0009_phase36_layer_c_variants"
  key_links:
    - from: "drizzle/0009_phase36_layer_c_variants.sql"
      to: "drizzle/meta/_journal.json idx=9 entry"
      via: "drizzle-kit migrate matches tag in journal to filename without `.sql` suffix"
      pattern: "\"tag\": \"0009_phase36_layer_c_variants\""
---

<objective>
Write the Drizzle structural twin migration file `drizzle/0009_phase36_layer_c_variants.sql` and append the corresponding `idx=9` entry to `drizzle/meta/_journal.json`. Both edits must ship in the same task per STATE.md Phase 34 Plan 01 lesson — without the journal entry, `drizzle-kit migrate` silently skips the new SQL file.

Purpose: Provides the column-shape source of truth for `drizzle-kit migrate`. Idempotent (CREATE TABLE IF NOT EXISTS, ADD COLUMN IF NOT EXISTS, DO $$ IF NOT EXISTS pg_constraint FK guards) so it can re-run safely after Plan 02's Supabase migration has already applied the same DDL to local Docker.

Output: One new file `drizzle/0009_phase36_layer_c_variants.sql` + one modified `drizzle/meta/_journal.json` (appended idx=9 entry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-layer-c-variant-split-clean-slate-wipe-cat-14-not-null/36-CONTEXT.md
@.planning/phases/36-layer-c-variant-split-clean-slate-wipe-cat-14-not-null/36-RESEARCH.md
@.planning/phases/36-layer-c-variant-split-clean-slate-wipe-cat-14-not-null/36-PATTERNS.md
@drizzle/0008_phase35_layer_b.sql
@drizzle/meta/_journal.json

<interfaces>
<!-- Authoritative reference: drizzle/0008_phase35_layer_b.sql shows the exact idempotent shape. -->

Phase 35 Drizzle idempotent pattern (drizzle/0008_phase35_layer_b.sql lines 56–97):
```sql
CREATE TABLE IF NOT EXISTS "watch_lineage_edges" (
  "id" uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL,
  "predecessor_catalog_id" uuid NOT NULL,
  ...
);
--> statement-breakpoint
DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM pg_constraint
    WHERE conname = 'watch_lineage_edges_predecessor_catalog_id_fk'
      AND conrelid = 'watch_lineage_edges'::regclass
  ) THEN
    ALTER TABLE "watch_lineage_edges"
      ADD CONSTRAINT "watch_lineage_edges_predecessor_catalog_id_fk"
      FOREIGN KEY ("predecessor_catalog_id") REFERENCES "public"."watches_catalog"("id")
      ON DELETE restrict ON UPDATE no action;
  END IF;
END $$;
--> statement-breakpoint
CREATE INDEX IF NOT EXISTS "watch_lineage_edges_predecessor_idx" ON "watch_lineage_edges" USING btree ("predecessor_catalog_id");
```

Current `drizzle/meta/_journal.json` tail entry (idx=8 = Phase 35):
```json
    {
      "idx": 8,
      "version": "7",
      "when": 1778393232709,
      "tag": "0008_phase35_layer_b",
      "breakpoints": true
    }
  ]
}
```

The new idx=9 entry must be inserted as a sibling AFTER the existing idx=8 closing `}` and BEFORE the closing `]` of the entries array. Note the JSON syntax: a trailing `,` must be added after the existing idx=8 entry's `}` before the new entry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write drizzle/0009_phase36_layer_c_variants.sql AND append idx=9 to drizzle/meta/_journal.json</name>
  <files>drizzle/0009_phase36_layer_c_variants.sql, drizzle/meta/_journal.json</files>
  <read_first>
    - drizzle/0008_phase35_layer_b.sql (full — confirms idempotent pattern with `--> statement-breakpoint` markers, CREATE TABLE IF NOT EXISTS, DO $$ FK guards)
    - drizzle/0007_phase34_brands_families.sql (read for FK guard pattern on ADD COLUMN — if it exists)
    - drizzle/meta/_journal.json (full — confirm current tail entry is `idx: 8, tag: "0008_phase35_layer_b"` and there is no idx=9 already present)
    - .planning/phases/36-layer-c-variant-split-clean-slate-wipe-cat-14-not-null/36-RESEARCH.md (§Idempotent Drizzle migration lines 467–531; §Journal entry block lines 534–545)
    - .planning/phases/36-layer-c-variant-split-clean-slate-wipe-cat-14-not-null/36-PATTERNS.md (§drizzle/0009_phase36 analog lines 366–479)
    - Run `ls -1 drizzle/*.sql | tail -3` BEFORE writing to confirm the highest existing migration is still `drizzle/0008_phase35_layer_b.sql`
  </read_first>
  <action>
**Edit 1 — Write `drizzle/0009_phase36_layer_c_variants.sql` as a fresh Write.**

Filename non-negotiables:
- Exactly `0009_phase36_layer_c_variants.sql` (4-digit prefix `0009` — strictly the next integer after `0008`)
- Do NOT use `0010` or any other prefix

Full file contents (write verbatim):

```sql
-- Phase 36 — Layer C: watch_variants table + watches.variant_id (Drizzle-side).
-- Idempotent: this migration also runs AFTER supabase db push --linked has applied
--   supabase/migrations/20260511000000_phase36_layer_c_variants.sql (authoritative DDL).
-- Drizzle migration carries column shapes only. No RLS, no GRANT, no DO $$ pre-flight —
-- those live exclusively in the Supabase migration.
-- Per memory rule project_local_db_reset.md, local re-sync runs:
--   supabase db reset → drizzle-kit push → docker exec psql < supabase/migrations/...phase36...sql
-- so every CREATE / ALTER must be IF NOT EXISTS.

-- ----- watch_variants table -----
CREATE TABLE IF NOT EXISTS "watch_variants" (
  "id" uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL,
  "catalog_id" uuid NOT NULL,
  "name" text NOT NULL,
  "slug" text NOT NULL,
  "dial_color" text,
  "bezel" text,
  "bracelet_variant" text,
  "image_url" text,
  "created_at" timestamp with time zone DEFAULT now() NOT NULL,
  "updated_at" timestamp with time zone DEFAULT now() NOT NULL,
  CONSTRAINT "watch_variants_catalog_slug_unique" UNIQUE ("catalog_id", "slug")
);
--> statement-breakpoint
DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM pg_constraint
    WHERE conname = 'watch_variants_catalog_id_fk'
      AND conrelid = 'watch_variants'::regclass
  ) THEN
    ALTER TABLE "watch_variants"
      ADD CONSTRAINT "watch_variants_catalog_id_fk"
      FOREIGN KEY ("catalog_id") REFERENCES "public"."watches_catalog"("id")
      ON DELETE restrict ON UPDATE no action;
  END IF;
END $$;
--> statement-breakpoint
CREATE INDEX IF NOT EXISTS "watch_variants_catalog_id_idx" ON "watch_variants" USING btree ("catalog_id");
--> statement-breakpoint

-- ----- watches: ADD COLUMN variant_id (D-04) -----
ALTER TABLE "watches" ADD COLUMN IF NOT EXISTS "variant_id" uuid;
--> statement-breakpoint
DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM pg_constraint
    WHERE conname = 'watches_variant_id_fk'
      AND conrelid = 'watches'::regclass
  ) THEN
    ALTER TABLE "watches"
      ADD CONSTRAINT "watches_variant_id_fk"
      FOREIGN KEY ("variant_id") REFERENCES "public"."watch_variants"("id")
      ON DELETE set null ON UPDATE no action;
  END IF;
END $$;
--> statement-breakpoint
CREATE INDEX IF NOT EXISTS "watches_variant_id_idx" ON "watches" USING btree ("variant_id");
--> statement-breakpoint

-- ----- watches.catalog_id: CAT-14 NOT NULL flip (idempotent — no-op if already NOT NULL) -----
-- Per RESEARCH.md §Assumption A1, SET NOT NULL on an already-NOT-NULL column is a no-op in
-- Postgres. This statement only does work if drizzle-kit runs against a fresh local DB
-- where the Supabase Phase 36 migration has NOT yet been applied.
ALTER TABLE "watches" ALTER COLUMN "catalog_id" SET NOT NULL;
```

**Edit 2 — Append idx=9 entry to `drizzle/meta/_journal.json`.**

Use the Edit tool to modify the JSON file. Current state of the tail of the file:
```json
    {
      "idx": 8,
      "version": "7",
      "when": 1778393232709,
      "tag": "0008_phase35_layer_b",
      "breakpoints": true
    }
  ]
}
```

Replace with (adding a `,` after the idx=8 closing `}` and inserting a new idx=9 entry — use the current Unix epoch in milliseconds for `when`, obtained via `date +%s%3N` or `node -e "process.stdout.write(String(Date.now()))"`):

```json
    {
      "idx": 8,
      "version": "7",
      "when": 1778393232709,
      "tag": "0008_phase35_layer_b",
      "breakpoints": true
    },
    {
      "idx": 9,
      "version": "7",
      "when": <UNIX_MS_NOW>,
      "tag": "0009_phase36_layer_c_variants",
      "breakpoints": true
    }
  ]
}
```

To compute `<UNIX_MS_NOW>` deterministically before writing, run in the executor's shell:
```bash
node -e "process.stdout.write(String(Date.now()))"
```

Then substitute the numeric output (e.g., `1778500000000` — your actual value will differ) directly into the JSON. Do NOT use a JavaScript expression or template string — the value must be a plain integer literal.

**Critical pitfalls to avoid:**
- **Trailing comma trap** — JSON does NOT permit a trailing comma after the LAST entry in an array. The new idx=9 must be the LAST entry; idx=8 (now the second-to-last) gets a comma added; idx=9 closing `}` does NOT.
- **`idx` value** — must be exactly `9`, the next integer after the existing `8`. No insertion between adjacent integers (memory rule 2 mirror).
- **`tag` value** — must EXACTLY match the filename without the `.sql` suffix: `0009_phase36_layer_c_variants`. drizzle-kit matches on this tag.
- **`version`** — must be `"7"` to match the existing entries (a STRING, not an integer).
- **`breakpoints`** — must be `true` to match Phase 35's entry.

**Do NOT add to the Drizzle SQL file:**
- `BEGIN;` / `COMMIT;` envelope — drizzle-kit wraps internally
- `RLS POLICY` / `GRANT` / `DO $$ pre-flight` — those live exclusively in the Supabase migration
- `CREATE TYPE` — Phase 36 adds no pgEnums
- Hard `TRUNCATE` — Phase 35 inherited the wipe
  </action>
  <verify>
    <automated>test -f drizzle/0009_phase36_layer_c_variants.sql && node -e "const j=require('./drizzle/meta/_journal.json'); const e=j.entries.find(x=>x.idx===9); if(!e||e.tag!=='0009_phase36_layer_c_variants'||e.version!=='7'||e.breakpoints!==true){console.error('FAIL: journal entry shape', JSON.stringify(e));process.exit(1)}; console.log('PASS: journal idx=9 entry valid')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f drizzle/0009_phase36_layer_c_variants.sql` exits 0
    - `CREATE TABLE IF NOT EXISTS "watch_variants"` is present: `grep -c 'CREATE TABLE IF NOT EXISTS "watch_variants"' drizzle/0009_phase36_layer_c_variants.sql` returns `1`
    - `ALTER TABLE "watches" ADD COLUMN IF NOT EXISTS "variant_id"` is present: `grep -c 'ADD COLUMN IF NOT EXISTS "variant_id"' drizzle/0009_phase36_layer_c_variants.sql` returns `1`
    - FK guard `watch_variants_catalog_id_fk` is present: `grep -c 'watch_variants_catalog_id_fk' drizzle/0009_phase36_layer_c_variants.sql` returns at least `2` (one in `WHERE conname = ...` and one in `ADD CONSTRAINT ...`)
    - FK guard `watches_variant_id_fk` is present: `grep -c 'watches_variant_id_fk' drizzle/0009_phase36_layer_c_variants.sql` returns at least `2`
    - `ON DELETE restrict` appears for the variant→catalog FK: `grep -c 'ON DELETE restrict ON UPDATE no action' drizzle/0009_phase36_layer_c_variants.sql` returns at least `1`
    - `ON DELETE set null` appears for the watches→variant FK: `grep -c 'ON DELETE set null ON UPDATE no action' drizzle/0009_phase36_layer_c_variants.sql` returns at least `1`
    - `ALTER TABLE "watches" ALTER COLUMN "catalog_id" SET NOT NULL` is present: `grep -c 'ALTER COLUMN "catalog_id" SET NOT NULL' drizzle/0009_phase36_layer_c_variants.sql` returns `1`
    - No RLS / GRANT in this file: `grep -ciE "ROW LEVEL SECURITY|^GRANT|CREATE POLICY" drizzle/0009_phase36_layer_c_variants.sql` returns `0`
    - `--> statement-breakpoint` markers present between statements: `grep -c "^--> statement-breakpoint$" drizzle/0009_phase36_layer_c_variants.sql` returns at least `6`
    - Journal idx=9 entry parses cleanly and has correct shape: `node -e "const j=require('./drizzle/meta/_journal.json'); const e=j.entries.find(x=>x.idx===9); console.log(JSON.stringify(e))"` outputs an object with `tag: "0009_phase36_layer_c_variants"`, `version: "7"`, `breakpoints: true`, and a positive integer `when`
    - Journal is still valid JSON: `node -e "JSON.parse(require('fs').readFileSync('drizzle/meta/_journal.json','utf8'))"` exits 0 with no error
    - Journal has exactly 10 entries (was 9, +1): `node -e "console.log(require('./drizzle/meta/_journal.json').entries.length)"` outputs `10`
  </acceptance_criteria>
  <done>
    `drizzle/0009_phase36_layer_c_variants.sql` exists with idempotent shape (CREATE TABLE IF NOT EXISTS + ADD COLUMN IF NOT EXISTS + DO $$ FK guards); `drizzle/meta/_journal.json` has a new idx=9 entry with tag `0009_phase36_layer_c_variants`; JSON parses cleanly; no RLS/GRANT in the Drizzle file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drizzle migration → local DB | Applied via `npx drizzle-kit push` (local) and `drizzle-kit migrate` (prod); idempotency is the load-bearing property |
| `_journal.json` → drizzle-kit | drizzle-kit matches on `tag` field; missing entries cause silent skip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | T (Tampering) — Drizzle/Supabase shape drift | drizzle/0009_*.sql vs supabase/.../phase36*.sql | mitigate | FK clauses (`ON DELETE restrict` for variants→catalog, `ON DELETE set null` for watches→variant) declared verbatim per D-03/D-04; idempotent guards prevent re-apply errors; Plan 04 integration test asserts `pg_constraint.confdeltype` matches |
</threat_model>

<verification>
- File and journal updates committed together (single task — STATE.md Phase 34 Plan 01 lesson)
- `node -e "JSON.parse(require('fs').readFileSync('drizzle/meta/_journal.json','utf8'))"` exits 0 (JSON is valid)
- `wc -l drizzle/0009_phase36_layer_c_variants.sql` returns approximately 50–60 lines
</verification>

<success_criteria>
ROADMAP success #1 (Drizzle migration side): `watch_variants` table + `watches.variant_id` ADD COLUMN both ship in `drizzle/0009_phase36_layer_c_variants.sql`.

ROADMAP success #4 idempotency: `ALTER COLUMN "catalog_id" SET NOT NULL` ships in the Drizzle file as a no-op-on-already-set safety re-sync.

Journal entry idx=9 lands in `_journal.json` in the same commit (Phase 34 Plan 01 lesson — without it, drizzle-kit silently skips the new SQL).
</success_criteria>

<output>
After completion, create `.planning/phases/36-layer-c-variant-split-clean-slate-wipe-cat-14-not-null/36-03-SUMMARY.md` summarizing: drizzle file path + line count; journal idx=9 entry shape (just the JSON object); confirmation that `node -e "JSON.parse(...)"` succeeded.
</output>
