---
phase: 50
plan: 04
type: execute
wave: 4
depends_on: [50-01, 50-02, 50-03]
files_modified:
  - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "Section 8 Recommendation delivers a definitive primary verdict — keep-separate OR a specific merge variant — with rationale rooted in section 6 matrix scores and section 5 v7.0 lens evidence (RESEARCH Pitfall 5: no hedging)"
    - "Section 8 names the recommended variant by ID (A, B, C, D, or E) at the top of the section"
    - "Section 9 Ship-Now Eligibility uses the verbatim format from `49-SPIKE.md` section 9 (lines 429-463) — ROADMAP SC#4 quote block, **Verdict: YES | NO | NEEDS-DISCUSSION** line, Strongly favored / Cheap / Trigger blocks"
    - "Section 9 Trigger block references ARCH-02 as the next sequential requirement ID and Phase 50.1 as the implementation wave (mirroring Phase 49 → 49.1)"
    - "Final SPIKE.md has all 9 D-SKEL-02 sections in order: 1, 2, 3, 4, 5, 6, 7, 8, 9"
  artifacts:
    - path: ".planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md"
      provides: "Final 2 sections (8 Recommendation, 9 Ship-Now Eligibility) appended; spike now complete with all 9 D-SKEL-02 sections"
      contains: "## 8. Recommendation, ## 9. Ship-now Eligibility, **Verdict:"
      min_lines: 60
  key_links:
    - from: "50-SPIKE.md section 8 Recommendation"
      to: "section 6 Decision Matrix scores + section 5 v7.0 lens per-variant sketches"
      via: "evidence-cited verdict rooted in matrix"
      pattern: "matrix|score|Variant [A-E]"
    - from: "50-SPIKE.md section 9 Ship-Now Eligibility"
      to: "49-SPIKE.md section 9 verbatim format (Verdict line + Strongly favored / Cheap / Trigger blocks)"
      via: "format parity for the v5.2 mid-milestone requirement-add flow"
      pattern: "Verdict:|Strongly favored:|Cheap:|Trigger:"
---

<objective>
Append final two sections to `50-SPIKE.md` — section 8 Recommendation (definitive primary verdict + rationale) and section 9 Ship-Now Eligibility (verbatim format from `49-SPIKE.md` section 9). After this plan completes, the spike has all 9 D-SKEL-02 sections and is ready for `/gsd-verify-work`.

Purpose: Section 8 synthesizes section 6 matrix scores into one definitive call. Section 9 is format-locked so the v5.2 mid-milestone requirement-add flow (which worked for TAX-02 → Phase 49.1 → prod migration 2026-05-20) plugs in without friction if the spike verdicts YES.

Output: complete `50-SPIKE.md` with all 9 sections, definitive verdict, and a ship-now block that downstream `/gsd-phase --insert` can read directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-watch-detail-architecture-spike/50-CONTEXT.md
@.planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md
@.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
@.planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append section 8 Recommendation — definitive primary verdict + rationale rooted in section 6 matrix</name>
  <files>.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md</files>
  <read_first>
    - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md (FULL READ — this task's verdict synthesizes section 6 matrix scores from Plan 03; the executor must read sections 4 + 5 + 6 + 7 in full to choose the recommended variant)
    - .planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md (Common Pitfalls Pitfall 5 — hedging is forbidden; Open Questions Q4 — BUG-01 evidence used in section 8 if Variant B wins)
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md (lines 372-425 — Recommendation shape; Phase 49 committed to `remove-genre` definitively, with sub-recommendation `unify-archetype-surface` as fallback. Phase 50 should similarly commit to ONE primary variant. Sub-recommendations are optional but allowed if the matrix shows a clear secondary winner.)
  </read_first>
  <action>
APPEND `## 8. Recommendation` after section 7's closing `---` separator.

The section has the following structure:

**Section 8 opening — primary verdict line** — One line, bolded, at the top of the section, following this exact pattern:

`**Primary recommendation: Variant X — short name**` (e.g. "Variant B — URL canonicalization" or "Variant A — keep separate")

The executor SELECTS the variant by reading section 6 matrix scores from Plan 03. The selection rules per RESEARCH Pitfall 5:
- The highest-summed-score variant wins UNLESS the v7.0 lens (section 5) materially favors a different variant (per RESEARCH Specifics: "v7.0 lens is the differentiator" + CONTEXT D-VARIANTS-02 explicit listing of "v7.0 photo carousel fit" as a first-class criterion).
- If two variants tie within 1 point, the v7.0 lens score breaks the tie.
- If the matrix shows keep-separate (Variant A) winning, recommend it. Phase 49's spike committed to remove-genre because evidence supported it; Phase 50 must equally commit to whatever the matrix shows.

**Section 8.1 Rationale paragraph** — 2-4 paragraphs explaining the verdict with cited evidence:
1. **Why this variant** — Cite section 6 matrix scores by criterion; cite section 5 lens sub-points by variant. Reference section 4's variant-summary one-liners.
2. **Why not the other variants** — Brief one-sentence rationale per non-winning variant pointing at the section 6 score row that disqualified it (e.g., "Variant C scored lowest on entry-point disruption — 19 sites is the highest blast radius in the set" — RESEARCH section 7 cost table evidence).
3. **What this variant solves** — Concretely name the issues retired (e.g., if Variant B wins: "retires the in-route D-08 framing flip + the Phase 48 BUG-01 maintenance tax + the wishlist/sold-this edge cases"; if Variant A wins: "preserves zero-disruption status quo while the shared-component surface continues to converge UI"). Reference specific code by file:line.
4. **What this variant exposes for v6.0 and v7.0** — Cite the section 5 lens sketch for the recommended variant. The v6.0 / v7.0 implications are downstream consequences of the recommendation; surface them so the next discuss-phase has them ready.

**Section 8.2 Sub-recommendation (optional but allowed)** — If the matrix shows a clear secondary winner (e.g., Phase 49's `unify-archetype-surface` was Phase 49's sub-recommendation alongside primary `remove-genre`), include a `### Sub-recommendation` subsection with the same verdict line + rationale shape. If there is no clear secondary, omit this subsection entirely — do NOT force a sub-recommendation for symmetry's sake.

**Section 8 closing** — Add a `---` separator after section 8 to signal section 9 ship-now block follows.

VOICE: declarative, NO hedging ("could", "might", "depending on"), every claim cited to a prior section. Mirror `49-SPIKE.md` lines 372-405 voice and structure.
  </action>
  <verify>
    <automated>grep -cE '^## 8\. Recommendation' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 1) ? 0 : 1 }' && grep -E '^\*\*Primary recommendation: Variant [A-E]' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -cE 'matrix|score|Variant [A-E]' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 >= 5) ? 0 : 1 }' && grep -cE '^## ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 8) ? 0 : 1 }'</automated>
  </verify>
  <done>
    - Section 8 Recommendation appended after section 7
    - Primary verdict line bolded, names a single variant by ID (A, B, C, D, or E)
    - Rationale paragraphs cite section 6 scores + section 5 lens + section 4 summaries
    - No hedging language in scores or verdict
    - `---` separator follows section 8 to signal section 9 append point
    - SPIKE.md now has 8 of 9 sections; only section 9 remains
  </done>
</task>

<task type="auto">
  <name>Task 2: Append section 9 Ship-Now Eligibility — verbatim format from 49-SPIKE.md section 9 (Verdict + Strongly favored + Cheap + Trigger)</name>
  <files>.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md</files>
  <read_first>
    - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md (confirm section 8 is complete; this task appends section 9 immediately after section 8's `---` separator)
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md (lines 429-463 — Section Ship-Now Eligibility Check VERBATIM template; this is the format Plan 04 must mirror exactly per D-SKEL-02 mandate and RESEARCH Pitfall 4)
    - .planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md (Architecture Patterns / Ship-Now Section Format at lines 138-164 — duplicates the 49-SPIKE.md template in research voice for convenience)
    - .planning/REQUIREMENTS.md (line 18 — TAX-02's exact requirement-add language is the format template for ARCH-02's Trigger block)
    - .planning/ROADMAP.md (Phase 50 SC#4 — the exact gate language: "No consolidation or removal implementation is shipped in this phase unless the spike specifically flags it as cheap and strongly favored — in which case a new requirement is added mid-milestone")
  </read_first>
  <action>
APPEND `## 9. Ship-now Eligibility` after section 8's closing `---` separator. This section's format is LOCKED per D-SKEL-02 and RESEARCH Pitfall 4 — the v5.2 mid-milestone requirement-add flow (which closed Phase 49.1 on 2026-05-20) depends on format parity with `49-SPIKE.md` section 9.

The section has the following exact structure (transcribed from 49-SPIKE.md lines 429-463, with Phase 50 substitutions):

**Opening — gate quote (verbatim from ROADMAP Phase 50 SC#4):**

```
## 9. Ship-now Eligibility

ROADMAP SC#4 gate language (verbatim):

> "No consolidation or removal implementation is shipped in this phase unless the spike specifically flags it as cheap and strongly favored — in which case a new requirement is added mid-milestone"

---
```

**Primary recommendation eligibility block** — One subsection sized to match `49-SPIKE.md` lines 437-447:

```
### Primary recommendation eligibility (Variant X — short name)

**Verdict: YES | NO | NEEDS-DISCUSSION**

[ONE paragraph: strongly-favored justification, evidence cite back to section 6 matrix and section 5 lens. State plainly whether the matrix scores meet the "strongly favored" bar.]

**Strongly favored:** [Matrix scores cited criterion-by-criterion. Reference specific cells: "section 6 scores Variant X at N/N/N/N/N — winning on UX clarity, schema/URL stability, and v7.0 photo carousel fit." If verdict is NO, write "Variant X scored N/N/N/N/N — not winning by enough margin on N criteria; the bar 'strongly favored' is not cleared."]

**Cheap:** [File count + entry-point rewrites + migrations + DAL changes from section 7 cost table. Reference the watches_catalog NOT-wipeable carve-out (project_db_wipeable_2026_05_09, 2026-05-19 update) — confirm no variant in A-E requires catalog schema work, so the carve-out does not gate this verdict. If verdict is YES: state the file count plainly (e.g., "1-2 files for Variant B; 19 files for Variant C — only B clears the cheap bar"). If verdict is NO: write "Cost row in section 7 shows N files / N entry-point rewrites — does not clear the cheap bar."]

**Trigger:** Add new requirement **ARCH-02: <short name describing the merge variant> — <one-line action verb describing what ships>** to REQUIREMENTS.md and `/gsd-phase --insert` a Phase 50.1 implementation wave.
```

Substitutions per the verdict the executor selects:

- If verdict is YES: include the full Trigger line naming ARCH-02 and Phase 50.1.
- If verdict is NO: replace the Trigger paragraph with a single line: "Trigger: N/A — ARCH-01 closes as decision-only; no Phase 50.1 follows. Variant <X> remains a candidate for a future milestone if v6.0 or v7.0 changes the cost/benefit calculus."
- If verdict is NEEDS-DISCUSSION: the Trigger paragraph reads: "Trigger: NEEDS-DISCUSSION — return to `/gsd-discuss-phase 50` to resolve <ambiguity>; do NOT add ARCH-02 yet."

**Optional sub-recommendation eligibility block** — If section 8 included a Sub-recommendation subsection, mirror Phase 49's lines 451-461 shape here: a `### Sub-recommendation eligibility (<name>)` subsection with the same Verdict / Strongly favored / Cheap / Trigger structure. If section 8 did not include a sub-recommendation, omit this subsection entirely.

**Note on sequencing (only if both a primary AND sub-recommendation verdict are YES)** — Mirror `49-SPIKE.md` lines 462-463 verbatim shape: a closing note on whether the sub-recommendation should be implemented first as a polish fix, OR as the first task of the primary recommendation's implementation wave, OR as a separate Phase 50.1b. If only the primary verdict is YES, omit this note.

VOICE RULES (RESEARCH Pitfall 4 hard requirement):
- Keep the verbatim gate quote (the `>` blockquote with ROADMAP SC#4 text).
- Keep the bold `**Verdict:**` line as the sub-heading anchor — downstream tools grep for this string.
- Keep the `**Strongly favored:**`, `**Cheap:**`, `**Trigger:**` block labels exactly.
- Keep the `/gsd-phase --insert` invocation language verbatim in the Trigger block.
- Do not introduce new heading levels, new bullet formats, or alternate verdict labels.
  </action>
  <verify>
    <automated>grep -cE '^## 9\. Ship-now Eligibility' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 1) ? 0 : 1 }' && grep -E '^\*\*Verdict:' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -E '^\*\*Strongly favored:' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -E '^\*\*Cheap:' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -E '^\*\*Trigger:' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -E 'No consolidation or removal implementation is shipped' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -cE '^## ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 9) ? 0 : 1 }'</automated>
  </verify>
  <done>
    - Section 9 Ship-now Eligibility appended; SPIKE.md now has all 9 D-SKEL-02 sections
    - Verbatim ROADMAP SC#4 gate quote present in blockquote form
    - `**Verdict:**` line names YES, NO, or NEEDS-DISCUSSION exactly
    - `**Strongly favored:**` + `**Cheap:**` + `**Trigger:**` blocks present with the locked label format
    - If verdict is YES: Trigger names ARCH-02 + Phase 50.1 + `/gsd-phase --insert` verbatim
    - Sub-recommendation block included if and only if section 8 included a sub-recommendation
    - SPIKE.md final section count is exactly 9
  </done>
</task>

</tasks>

<threat_model>
- Decision-only spike; no executable code shipped in this phase.
- No new auth surface, no new data path, no new external input introduced.
- Section 9's verbatim format parity is the operational security guarantee — downstream `/gsd-phase --insert` parsing depends on grep-able anchors (`**Verdict:**`, `**Strongly favored:**`, `**Cheap:**`, `**Trigger:**`). Format drift breaks the mid-milestone requirement-add flow that just closed Phase 49.1 on 2026-05-20.
- Section 8's "Why this variant" rationale must NOT recommend a variant that weakens the two-layer privacy gate in `getWatchByIdForViewer` (RESEARCH Security Domain). All 5 variants A-E in this spike preserve the gate; the recommendation simply has to confirm this in passing.
- D-GUARD-01 enforcement: this plan writes ONLY to `.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md`. No code files modified.
</threat_model>

<verification>
After both tasks complete:

```bash
# Final: all 9 D-SKEL-02 sections present in order
grep -nE '^## [1-9]\.' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk -F: '{print $2}'
# Expected output (9 lines, in order):
#   ## 1. Domain
#   ## 2. Audience Matrix
#   ## 3. Route Reality Today
#   ## 4. Variants A-E (or similar)
#   ## 5. v7.0 Watch Photos Lens
#   ## 6. Decision Matrix
#   ## 7. Cost Estimate per Variant
#   ## 8. Recommendation
#   ## 9. Ship-now Eligibility (or "Ship-Now Eligibility Check")

# Section 9 format anchors present
grep -cE '^\*\*Verdict:|^\*\*Strongly favored:|^\*\*Cheap:|^\*\*Trigger:' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: >=4 (at minimum, one of each label)

# ROADMAP SC#4 verbatim quote present
grep -c 'No consolidation or removal implementation is shipped' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: >=1

# Section 8 primary recommendation names a variant
grep -E '^\*\*Primary recommendation: Variant [A-E]' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: 1 line

# D-GUARD-01 enforcement: only the SPIKE.md file modified in this plan's commits
git diff --name-only HEAD~1...HEAD | grep -v '^\.planning/'
# Expected: empty
```
</verification>

<success_criteria>
- Section 8 Recommendation names a single variant (A-E) as primary recommendation, with cited evidence and no hedging
- Section 9 Ship-now Eligibility matches `49-SPIKE.md` section 9 verbatim shape
- ROADMAP SC#4 gate language preserved verbatim in section 9 opening blockquote
- `**Verdict:**`, `**Strongly favored:**`, `**Cheap:**`, `**Trigger:**` labels appear exactly as in Phase 49
- Trigger block names ARCH-02 and Phase 50.1 if verdict is YES; falls back to N/A or NEEDS-DISCUSSION trigger language otherwise
- SPIKE.md has exactly 9 numbered sections; order matches D-SKEL-02 list
- No implementation files modified outside `.planning/phases/50-*/`
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/50-watch-detail-architecture-spike/50-04-SUMMARY.md` documenting: which variant won, the section 9 verdict (YES/NO/NEEDS-DISCUSSION), the section 9 Trigger language (ARCH-02 + Phase 50.1 if YES; alternate language otherwise), and any deviations from the `49-SPIKE.md` section 9 verbatim format with explicit justification. The summary is the input the next `/gsd-discuss-phase` would consume if the verdict is YES — write it accordingly.
</output>
