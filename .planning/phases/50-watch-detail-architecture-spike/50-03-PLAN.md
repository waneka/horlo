---
phase: 50
plan: 03
type: execute
wave: 3
depends_on: [50-01, 50-02]
files_modified:
  - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "§5 v7.0 Watch Photos Lens applies 4 sub-points × 5 variants = 20 distinct sketches per D-V7-LENS-01 — depth pitfall (RESEARCH Pitfall 3) avoided"
    - "§5 cites SEED-013 as the forcing function but does NOT pre-decide its open questions (per-person cap, opt-in/opt-out, ordering, storage bucket)"
    - "§5 carries the watches_catalog NOT-wipeable carve-out forward — any v7.0 photo schema cost note uses in-place ALTER + UPDATE language"
    - "§6 Decision Matrix scores all 5 variants (rows) on all 7 locked criteria (columns) from D-VARIANTS-02 — UX clarity / schema-URL stability / per-user data shape / v7.0 photo carousel fit / entry-point disruption / migration cost / irreversibility"
    - "§5 and §6 are spliced between §4 and §7 — final SPIKE.md ordering is 1, 2, 3, 4, 5, 6, 7 (with 8 and 9 to follow in Plan 04)"
    - "Section 9 of the final document is reserved for ship-now eligibility (Plan 04 owns it) — this plan does not write §8 or §9"
  artifacts:
    - path: ".planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md"
      provides: "Sections 5 (v7.0 Lens, 4×5=20 sketches) + 6 (Decision Matrix 5 variants × 7 criteria) spliced between §4 and §7"
      contains: "## 5. v7.0 Watch Photos Lens, ### A. ..., ### B. ..., ### C. ..., ### D. ..., ### E. ..., ## 6. Decision Matrix"
      min_lines: 250
  key_links:
    - from: "50-SPIKE.md §5 per-variant sketch"
      to: "SEED-013-v7.0-watch-photos.md scope items"
      via: "4 sub-points per variant: where carousel renders, data joins, writability axis, variant × viewer-state cell interaction"
      pattern: "carousel|wear.pic|writability|viewer-state"
    - from: "50-SPIKE.md §6 Decision Matrix"
      to: "D-VARIANTS-02 locked criteria + §4 variant summaries"
      via: "5×7 = 35 scored cells with rationale rooted in §4 + §5 evidence"
      pattern: "UX clarity|schema|URL|per-user|carousel|entry-point|migration|irreversibility"
---

<objective>
Splice sections 5 (v7.0 Watch Photos Lens) and 6 (Decision Matrix) BETWEEN §4 and §7 in `50-SPIKE.md`. §5 applies a deep per-variant v7.0 lens — 4 sub-points × 5 variants = 20 distinct sketches per D-V7-LENS-01. §6 renders the decision matrix scoring all 5 variants on all 7 locked criteria from D-VARIANTS-02. Plan 04 will append §8 (Recommendation) + §9 (Ship-Now Eligibility) after §7.

Purpose: §5 is the v7.0 forcing-function lens that differentiates Phase 50's spike from Phase 49's (where there was no v7.0-equivalent forcing function). §6 produces the actionable scoring that Plan 04's recommendation roots in. Depth-pitfall (RESEARCH Pitfall 3) is the single biggest risk in this plan — one-liner per variant fails the D-V7-LENS-01 mandate.

Output: `50-SPIKE.md` grown to 7 of the 9 mandated sections, in order 1, 2, 3, 4, 5, 6, 7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-watch-detail-architecture-spike/50-CONTEXT.md
@.planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md
@.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
@.planning/seeds/SEED-013-v7.0-watch-photos.md
@.planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Splice §5 v7.0 Watch Photos Lens — 4 sub-points × 5 variants (20 sketches) between §4 and §7</name>
  <files>.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md</files>
  <read_first>
    - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md (confirm structure is §1-4 then §7; this task inserts §5 above the §7 heading)
    - .planning/phases/50-watch-detail-architecture-spike/50-CONTEXT.md (D-V7-LENS-01 — the 4 sub-points are: where carousel renders, data joins, writability axis, variant × viewer-state cell interaction; D-SKEL-02 mandate that section is present)
    - .planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md (§v7.0 Watch Photos Lens — Evidence per Variant at lines 287-340 — this is the canonical evidence base for §5; transcribe its per-variant sketches into spike voice; §Common Pitfalls Pitfall 3 — depth violation warning; §Common Pitfalls Pitfall 6 — catalog-wipeable carve-out)
    - .planning/seeds/SEED-013-v7.0-watch-photos.md (the forcing function — scope items: multi-photo model replacing imageUrl, public wear pics surface on watch detail, wears-tab persistence, add-watch photo encouragement with per-person cap; also the open questions that the spike must NOT pre-decide)
    - src/db/schema.ts (read lines 130, 357 — confirm `imageUrl` exists on both `watches` and `watches_catalog` today; the multi-photo model is what v7.0 replaces)
  </read_first>
  <action>
Use the Edit tool to insert `## 5. v7.0 Watch Photos Lens` BEFORE the existing `## 7. Cost Estimate per Variant` heading. The §4 → §5 → §7 ordering keeps §5 and §7 separate per D-SKEL-02 (cost numbers stay comparable side-by-side; v7.0 sketches stay in their own section).

The section structure:

**§5 introductory paragraph** — Two short paragraphs:
1. SEED-013 (v7.0 Watch Photos) is the forcing function for this lens. Scope items the spike consumes: multi-photo model replacing `watches_catalog.imageUrl` (today `src/db/schema.ts:357` single image) and `watches.imageUrl` (today `src/db/schema.ts:130` single image); public wear-pic surfacing on watch detail (cross-user query, visibility-gated); wears-tab persistence for actual wear photo vs generic catalog image; add-watch photo encouragement with a per-person cap.
2. The spike does NOT pre-decide SEED-013's open questions (per-person cap, opt-in/opt-out wear-pic surfacing, carousel cover ordering, wears-tab persistence rules, v6.0 social interaction shape, storage bucket strategy). The lens applies the architectural decision the merge variant inherits, NOT the v7.0 product decisions.

**Five variant sketches** — one subsection per variant (`### A. Keep separate (status quo) — v7.0 lens`, `### B. URL canonicalization — v7.0 lens`, etc. — note the `— v7.0 lens` suffix to disambiguate from §4 subsection headings). Each subsection contains EXACTLY 4 sub-bullets in this order:

1. **Where the carousel renders** — which route file, Server vs Client component boundary.
2. **Data joins** — catalog photos + owner wear-pics (from `wear_events.photo_url` — cite the existing v3.0 Phase 15 pipeline) + other-owners' public wear-pics (cross-user query, public-visibility-gated, mirrors `getCollectorsForCatalog` shape).
3. **Writability axis** — who can add a photo on this surface (owner via `WatchDetail` island Server Actions vs catalog-spec writer vs nobody-just-display).
4. **Variant × Viewer-State cell interaction** — how the carousel composition changes per viewer-state (cross-reference §2 cells): owner sees own + others' public; non-owner-with-collection sees catalog + others' public; non-owner-empty-collection sees catalog + others' public; wishlist-holder sees same as non-owner; sold-this sees same as non-owner (no own wear-pics for sold watches in scope); anonymous-visitor unreachable.

Per-variant content (transcribe from RESEARCH §v7.0 Watch Photos Lens — Evidence per Variant lines 305-340):

- **§5.A Keep separate** — carousel rendered in shared `ReferenceIdentityCard.tsx` or a new sibling `PhotoCarousel.tsx`, in BOTH routes (`/catalog/[catalogId]/page.tsx` + `/watch/[id]/page.tsx`). Data joins fired twice (once per route). Writability: owner-only "add photo" affordance lives in `WatchDetail` island on `/watch/[id]`; catalog route stays read-only. Variant × viewer-state: every cell described twice (once per route). Cost note: maintaining two carousel surfaces creates drift risk — Variant A's v7.0 tax.

- **§5.B URL canonicalization** — carousel renders on `/watch/[id]` only (canonical). `/catalog/[catalogId]` renders carousel read-only for non-owners; owner viewers are redirected before render. Data joins same as A but only one route maintains the carousel-add UI. Writability: owner-only on `/watch/[id]` via `WatchDetail` island. Variant × viewer-state: owner → `/watch/[id]` with add affordance; non-owner viewers see catalog read-only carousel; wishlist/sold-this same as non-owner. Cost note: cheapest path to a single owner-facing carousel surface.

- **§5.C Unified `/w/[ref]`** — carousel renders in the unified `src/app/w/[ref]/page.tsx`, single component, single surface. Data joins: one server-side dispatch decides which DAL calls fire (`watches` row exists → owner data joins; only catalog → catalog + cross-user public). Writability: gated by `viewerCanEdit` from the same-user framing branch. Variant × viewer-state: cleanest single-cell composition. Anonymous-visitor cell (future) lands trivially since the route resolution already accepts either id type. Cost note: best v7.0 fit; highest entry-point rewrite cost (per §7).

- **§5.D Catalog absorbs watch** — carousel renders in `/catalog/[catalogId]/page.tsx` (rewritten). Data joins: catalog photos + (owner wear-pics when owner) + cross-user public wear-pics — single fetch path. Writability: owner-only via the conditionally-rendered `WatchDetail` island. Variant × viewer-state: branching happens at one route, one page, one carousel — cleaner than A/B. Cost note: similar to C for v7.0 fit but inherits catalog route's discovery-surface entry-point shape (search/Explore already point here, so entry-point rewrites trade discovery-side for ownership-side).

- **§5.E Watch absorbs catalog** — carousel renders in `/watch/[id]/page.tsx` (rewritten). Catalog-only mode renders read-only carousel. Data joins: same as D but route resolves on `watches.id` first, falls back to `watches_catalog.id`. Writability: owner-only via `WatchDetail` island (already gated by `viewerCanEdit`). Variant × viewer-state: catalog-only branch (non-owner with no per-user row) needs a "synthesize Watch from CatalogEntry" path — existing `catalogEntryToSimilarityInput` shim in `src/lib/verdict/shims.ts` is the precedent. Cost note: smaller entry-point blast radius (7) but UUID-dispatch fragility increases code-clarity cost.

**§5 closing paragraph — schema carve-out reminder** — Write exactly:

> v7.0's multi-photo schema replacement (`imageUrl` text → photos table/array) is OUT OF SCOPE for Phase 50 per D-GUARD-01. The v7.0 implementation phase will face the `watches_catalog` NOT-wipeable constraint (MEMORY `project_db_wipeable_2026_05_09`, 2026-05-19 update) — any photo schema migration must be in-place ALTER + UPDATE, not wipe-and-re-seed. The merge variant chosen here affects where the carousel UI lives; it does not pre-decide how the photo data is shaped.

Do NOT add a `---` separator after §5 — §6 follows directly. Task 2 of this plan handles the §6 splice; both tasks insert above the existing `## 7.` heading.
  </action>
  <verify>
    <automated>grep -cE '^## 5\. v7\.0 Watch Photos Lens' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 1) ? 0 : 1 }' && grep -cE '^### [A-E]\. .*v7\.0 lens' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 5) ? 0 : 1 }' && grep -cE 'carousel|wear.pic' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 >= 10) ? 0 : 1 }' && grep -E 'SEED-013|project_db_wipeable_2026_05_09|in-place ALTER' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -cE '^## ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 6) ? 0 : 1 }'</automated>
  </verify>
  <done>
    - §5 v7.0 Watch Photos Lens spliced between §4 and §7
    - 5 per-variant subsections present (### A. through ### E., each with `— v7.0 lens` suffix to disambiguate from §4)
    - Each variant has EXACTLY 4 sub-points (where carousel renders, data joins, writability axis, variant × viewer-state cell interaction)
    - SEED-013 named as forcing function; its open questions explicitly preserved (not pre-decided)
    - Closing paragraph cites D-GUARD-01 + `project_db_wipeable_2026_05_09` 2026-05-19 update + in-place ALTER + UPDATE language
    - SPIKE.md now has 6 ## sections (1, 2, 3, 4, 5, 7) — §6 still to come in Task 2
  </done>
</task>

<task type="auto">
  <name>Task 2: Splice §6 Decision Matrix — 5 variants × 7 locked criteria with scoring + rationale paragraph</name>
  <files>.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md</files>
  <read_first>
    - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md (confirm structure is §1-5 then §7; this task inserts §6 above the §7 heading)
    - .planning/phases/50-watch-detail-architecture-spike/50-CONTEXT.md (D-VARIANTS-02 — the 7 locked criteria, MEMORIZE this exact list: UX clarity, schema/URL stability, per-user data shape, v7.0 photo carousel fit, entry-point disruption, migration cost, irreversibility)
    - .planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md (§ Open Questions Q3 — recommends the Phase 49 hybrid format; § Architecture Patterns / Phase 49's 3-Plan Decomposition for matrix shape; § Variants A-E Evidence Base for per-cell rationale)
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md (lines 344-405 — §Decision Matrix shape; this is the canonical voice template. Note that Phase 49 used 5 options × 5 criteria; Phase 50 needs 5 variants × 7 criteria — 35 cells. Per RESEARCH Q3, planner discretion allows binary ✓/✗/? for binary criteria and numeric 1-5 for spectrum criteria)
  </read_first>
  <action>
Use the Edit tool to insert `## 6. Decision Matrix` BETWEEN §5 (the v7.0 lens closing paragraph) and the existing `## 7. Cost Estimate per Variant` heading. Final ordering after this task: §1, §2, §3, §4, §5, §6, §7 (with §8 and §9 still pending from Plan 04).

The section has three parts:

**§6.1 Criteria definitions (one-paragraph block, optional table)** — List the 7 locked criteria from D-VARIANTS-02 with a one-line gloss for each (so the matrix table below is interpretable):

1. **UX clarity** — Does the variant produce a coherent, single-meaning surface for each viewer-state? (Higher = clearer.)
2. **Schema/URL stability** — Does the variant preserve canonical URLs and avoid schema churn? (Higher = more stable.)
3. **Per-user data shape** — How cleanly does the per-user data layer compose with the catalog spec layer? (Higher = cleaner composition.)
4. **v7.0 photo carousel fit** — Does the variant give the v7.0 multi-photo carousel a single rendering surface? (Higher = better fit; rooted in §5 evidence.)
5. **Entry-point disruption** — How many of the 19 entry-point sites does the variant rewrite? (Higher = LESS disruption; inverted score per Phase 49 convention.)
6. **Migration cost** — Files touched + tests + DAL changes for the route migration itself (not the v7.0 photo schema). Lower cost = higher score. (Higher = cheaper.)
7. **Irreversibility** — Once shipped, how hard is the variant to revert? (Higher = MORE reversible / easier to back out.)

**§6.2 Scored matrix** — Render as a markdown table with rows = variants (A-E) and columns = the 7 criteria. Use the Phase 49 hybrid format per RESEARCH Q3 recommendation: numeric 1-5 for spectrum criteria (UX clarity, schema/URL stability, per-user data shape, v7.0 photo carousel fit) and a tighter scoring for the cost/disruption/reversibility columns (e.g., raw count for entry-point disruption inverted to 1-5, raw count for files touched inverted to 1-5, qualitative-1-5 for irreversibility). The planner's discretion is to pick scores grounded in §4 evidence (variant summaries) and §5 (v7.0 lens sketches). Cite §4 and §5 evidence rather than inventing.

Required table shape:

| Variant | UX clarity | Schema/URL stability | Per-user data shape | v7.0 photo carousel fit | Entry-point disruption (inverted: higher = less) | Migration cost (inverted: higher = cheaper) | Irreversibility (inverted: higher = easier to revert) |
|---------|------------|---------------------|---------------------|-------------------------|---------------------------------------------------|---------------------------------------------|--------------------------------------------------------|
| **A. Keep separate** | _score_ | _score_ | _score_ | _score_ | _score_ | _score_ | _score_ |
| **B. URL canonicalization** | _score_ | _score_ | _score_ | _score_ | _score_ | _score_ | _score_ |
| **C. Unified `/w/[ref]`** | _score_ | _score_ | _score_ | _score_ | _score_ | _score_ | _score_ |
| **D. Catalog absorbs watch** | _score_ | _score_ | _score_ | _score_ | _score_ | _score_ | _score_ |
| **E. Watch absorbs catalog** | _score_ | _score_ | _score_ | _score_ | _score_ | _score_ | _score_ |

(Executor: replace `_score_` with the chosen numeric/binary score per planner discretion. The plan does not lock specific scores — that is the synthesizing work this section does. Be definitive (RESEARCH Pitfall 5: no hedging). Each cell should be a clean number.)

**§6.3 Rationale paragraph** — After the table, write 1-2 paragraphs per variant (5 paragraphs total, one per variant row) explaining the scoring with explicit reference to §4 and §5 evidence by section. Example shape (use this as a template for each variant):

> **A. Keep separate** — Scores _high/low_ on X because [§3/§4/§5 cite]. Specifically [evidence from §4.A]. The v7.0 lens (§5.A) shows [carousel-fit rationale]. The cost row in §7 confirms [zero-disruption claim]. Irreversibility is the inverse — keep-separate is fully reversible because no work is done.

Repeat for B, C, D, E. Total §6 length target: 5 cells × 7 columns = 35 cells; 5 rationale paragraphs each averaging 4-6 sentences. This is the synthesizing section — every claim cites prior evidence sections.

Do NOT add a `---` separator after §6 — §7 follows directly. Plan 04 will append §8 and §9 after §7.
  </action>
  <verify>
    <automated>grep -cE '^## 6\. Decision Matrix' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 1) ? 0 : 1 }' && grep -E 'UX clarity.*Schema/URL stability.*Per-user data shape.*v7\.0 photo carousel fit.*Entry-point disruption.*Migration cost.*Irreversibility' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -cE '^\| \*\*[A-E]\. ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 >= 10) ? 0 : 1 }' && grep -cE '^## ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 7) ? 0 : 1 }'</automated>
  </verify>
  <done>
    - §6 Decision Matrix spliced between §5 and §7
    - All 7 D-VARIANTS-02 criteria appear as column headers (literal string match per the criterion names)
    - All 5 variants (A-E) appear as scored rows
    - Each variant has a rationale paragraph citing §4 or §5 evidence
    - Scoring is definitive (no "could", "might", "depending on" language in scores — RESEARCH Pitfall 5)
    - SPIKE.md now has 7 ## sections (1, 2, 3, 4, 5, 6, 7) — Plan 04 brings the final two to 9
  </done>
</task>

</tasks>

<threat_model>
- Decision-only spike; no executable code shipped in this phase.
- No new auth surface, no new data path, no new external input introduced.
- §5's writability-axis sub-point for each variant is the risk-modeling moment for future v7.0 implementation: the spike makes explicit which actor can add a photo on which surface — preventing a future implementation that grants write surface to a non-owner viewer.
- §5 closing paragraph carries the `watches_catalog` not-wipeable carve-out forward; this prevents a future Phase 50.1 cost estimate that ignores the 2026-05-19 update.
- §6's scoring system is the variant comparison's risk-model output — irreversibility column explicitly scores the back-out cost of each variant.
- D-GUARD-01 enforcement: this plan writes ONLY to `.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md`. No code files modified.
</threat_model>

<verification>
After both tasks complete:

```bash
# §5 + §6 spliced; total sections = 7 (1, 2, 3, 4, 5, 6, 7)
grep -cE '^## ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: 7

# §5 per-variant v7.0 lens has 5 subsections
grep -cE '^### [A-E]\. .*v7\.0 lens' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: 5

# §5 depth: carousel/wear-pic citations appear at least 10 times (4 sub-points × 5 variants)
grep -cE 'carousel|wear.pic|writability|viewer-state' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: >=20 (lower bound; actual will likely be higher with §2 + §5 hits)

# §6 matrix has 5 variant rows
grep -cE '^\| \*\*[A-E]\. ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: 10 (5 in §7 + 5 in §6)

# Ordering check: §5 appears before §6 appears before §7
grep -nE '^## (5\.|6\.|7\.)' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk -F: '{print $2}'
# Expected: 3 lines in order: "## 5. ...", "## 6. ...", "## 7. ..."

# D-GUARD-01 enforcement: only the SPIKE.md file modified
git diff --name-only HEAD~1...HEAD | grep -v '^\.planning/'
# Expected: empty
```
</verification>

<success_criteria>
- §5 v7.0 Watch Photos Lens delivers 4 sub-points × 5 variants = 20 distinct sketches
- §5 cites SEED-013 as forcing function; preserves its open questions explicitly
- §5 closing paragraph cites watches_catalog carve-out + in-place ALTER + UPDATE migration language
- §6 Decision Matrix scores all 5 variants × all 7 criteria (35 cells)
- §6 column headers match D-VARIANTS-02 verbatim
- §6 rationale paragraphs cite §4 + §5 evidence (no hedged scoring)
- SPIKE.md ordering after this plan: §1, §2, §3, §4, §5, §6, §7
- No implementation files modified outside `.planning/phases/50-*/`
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/50-watch-detail-architecture-spike/50-03-SUMMARY.md` documenting: §5 sub-point coverage (20 sketches confirmed), §6 scoring approach (hybrid numeric vs ✓/✗ choice rationale), which variant is emerging as the leader from the matrix scores (this is Plan 04's input), and any deviations from D-V7-LENS-01 / D-VARIANTS-02 evidence.
</output>
