---
phase: 14-nav-shell-explore-stub
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/explore/page.tsx
  - src/app/search/page.tsx
  - tests/app/explore.test.tsx
  - tests/app/search.test.tsx
autonomous: true
requirements: [NAV-11]
must_haves:
  truths:
    - "/explore renders a 'Discovery is coming.' placeholder page under the authenticated shell"
    - "/search renders a 'Search is coming.' placeholder page under the authenticated shell"
    - "Neither nav link 404s"
  artifacts:
    - path: "src/app/explore/page.tsx"
      provides: "Coming-soon stub page"
      contains: "Discovery is coming"
    - path: "src/app/search/page.tsx"
      provides: "Coming-soon stub page"
      contains: "Search is coming"
  key_links:
    - from: "src/components/layout/BottomNav.tsx (Plan 03)"
      to: "src/app/explore/page.tsx"
      via: "href='/explore'"
      pattern: "href=\"/explore\""
    - from: "src/components/layout/SlimTopNav.tsx (Plan 04)"
      to: "src/app/search/page.tsx"
      via: "href='/search'"
      pattern: "href=\"/search\""
---

<objective>
Ship minimal "coming soon" placeholder pages for `/explore` and `/search` so nav links from BottomNav (Plan 03) and SlimTopNav/DesktopTopNav (Plan 04) never 404. Per CONTEXT.md D-18, D-19, D-20.

Purpose: Phase 14 adds nav entry points to both routes; without these stubs the app would 404 in CI. Phase 16 rewrites `/search`; Discovery (`/explore`) is a future milestone beyond v3.0. Copy + icons per UI-SPEC §Copywriting Contract.

Output: Two new route files + two test files. Both pages are simple Server Components with no data dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-nav-shell-explore-stub/14-CONTEXT.md
@.planning/phases/14-nav-shell-explore-stub/14-UI-SPEC.md
@src/components/notifications/NotificationsEmptyState.tsx

<interfaces>
<!-- UI-SPEC Copywriting Contract (authoritative copy): -->
/explore heading: "Discovery is coming."
/explore body: "Explore will surface watches, collectors, and taste clusters curated to your collection. Check back soon."
/explore icon: Sparkles (lucide)

/search heading: "Search is coming."
/search body: "Find collectors by name, discover taste overlap, and follow people who wear what you love. Check back soon."
/search icon: Search (lucide)

<!-- Both render inside the authenticated shell (D-20) — the root layout wraps
     them in SlimTopNav/DesktopTopNav/BottomNav automatically; proxy.ts redirects
     unauth users to /login before reaching these pages. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create /explore stub page with test</name>
  <files>src/app/explore/page.tsx, tests/app/explore.test.tsx</files>
  <read_first>
    - .planning/phases/14-nav-shell-explore-stub/14-CONTEXT.md §Decisions D-18, D-20
    - .planning/phases/14-nav-shell-explore-stub/14-UI-SPEC.md §Copywriting Contract (exact heading + body + icon)
    - src/components/notifications/NotificationsEmptyState.tsx (visual density reference — mirror this pattern per D-18)
  </read_first>
  <behavior>
    - Test 1: Page component renders heading text "Discovery is coming."
    - Test 2: Page contains body text "Explore will surface watches, collectors, and taste clusters curated to your collection. Check back soon."
    - Test 3: Page renders a Sparkles icon (either by test-id, or by asserting an `<svg>` is present)
    - Test 4: Default export is a function (Server Component — rendered via direct invocation)
  </behavior>
  <action>
    Create `src/app/explore/page.tsx`:

    ```tsx
    import { Sparkles } from 'lucide-react'

    export default function ExplorePage() {
      return (
        <main className="mx-auto flex max-w-2xl flex-col items-center justify-center px-4 py-24 text-center">
          <div className="mb-4 flex size-12 items-center justify-center rounded-full bg-accent/10">
            <Sparkles className="size-6 text-accent" aria-hidden />
          </div>
          <h1 className="font-serif text-3xl md:text-4xl text-foreground">
            Discovery is coming.
          </h1>
          <p className="mt-3 max-w-md text-sm text-muted-foreground">
            Explore will surface watches, collectors, and taste clusters curated
            to your collection. Check back soon.
          </p>
        </main>
      )
    }
    ```

    Create `tests/app/explore.test.tsx`:

    ```tsx
    import { describe, it, expect } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import ExplorePage from '@/app/explore/page'

    describe('/explore stub (Phase 14 NAV-11 D-18)', () => {
      it('renders the "Discovery is coming." heading', () => {
        render(<ExplorePage />)
        expect(screen.getByRole('heading', { name: /discovery is coming\./i })).toBeInTheDocument()
      })
      it('renders the teaser copy verbatim', () => {
        render(<ExplorePage />)
        expect(
          screen.getByText(/Explore will surface watches, collectors, and taste clusters curated to your collection\. Check back soon\./i),
        ).toBeInTheDocument()
      })
      it('renders a Sparkles-iconish svg', () => {
        const { container } = render(<ExplorePage />)
        // lucide emits <svg> with class including "lucide" or specific icon class
        expect(container.querySelector('svg')).toBeInTheDocument()
      })
      it('default export is a function', () => {
        expect(typeof ExplorePage).toBe('function')
      })
    })
    ```
  </action>
  <verify>
    <automated>npm test -- --run tests/app/explore.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/explore/page.tsx` exists
    - `grep -c "Discovery is coming" src/app/explore/page.tsx` returns 1
    - `grep -c "Sparkles" src/app/explore/page.tsx` returns 2 (import + JSX)
    - `grep -c "Check back soon" src/app/explore/page.tsx` returns 1
    - File `tests/app/explore.test.tsx` exists
    - `npm test -- --run tests/app/explore.test.tsx` passes 4 tests
    - `npx tsc --noEmit` exits 0
    - `npm run build` still succeeds
  </acceptance_criteria>
  <done>/explore stub exists; renders coming-soon copy; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create /search stub page with test</name>
  <files>src/app/search/page.tsx, tests/app/search.test.tsx</files>
  <read_first>
    - .planning/phases/14-nav-shell-explore-stub/14-CONTEXT.md §Decisions D-19, D-20
    - .planning/phases/14-nav-shell-explore-stub/14-UI-SPEC.md §Copywriting Contract (search stub copy + icon)
    - src/app/explore/page.tsx (just created in Task 1 — mirror the visual shell)
  </read_first>
  <behavior>
    - Test 1: Page component renders heading text "Search is coming."
    - Test 2: Page contains body text "Find collectors by name, discover taste overlap, and follow people who wear what you love. Check back soon."
    - Test 3: Page renders a Search icon (svg present)
    - Test 4: Default export is a function
  </behavior>
  <action>
    Create `src/app/search/page.tsx`:

    ```tsx
    import { Search } from 'lucide-react'

    export default function SearchPage() {
      return (
        <main className="mx-auto flex max-w-2xl flex-col items-center justify-center px-4 py-24 text-center">
          <div className="mb-4 flex size-12 items-center justify-center rounded-full bg-accent/10">
            <Search className="size-6 text-accent" aria-hidden />
          </div>
          <h1 className="font-serif text-3xl md:text-4xl text-foreground">
            Search is coming.
          </h1>
          <p className="mt-3 max-w-md text-sm text-muted-foreground">
            Find collectors by name, discover taste overlap, and follow people
            who wear what you love. Check back soon.
          </p>
        </main>
      )
    }
    ```

    Create `tests/app/search.test.tsx` mirroring the /explore test with Search copy and icon.
  </action>
  <verify>
    <automated>npm test -- --run tests/app/search.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/search/page.tsx` exists
    - `grep -c "Search is coming" src/app/search/page.tsx` returns 1
    - `grep -c "import { Search }" src/app/search/page.tsx` returns 1
    - `grep -c "who wear what you love" src/app/search/page.tsx` returns 1
    - File `tests/app/search.test.tsx` exists and passes 4 tests: `npm test -- --run tests/app/search.test.tsx`
    - `npx tsc --noEmit` exits 0
    - `npm run build` still succeeds
  </acceptance_criteria>
  <done>/search stub exists; renders coming-soon copy; test green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → /explore, /search | Both routes behind proxy.ts auth redirect (D-20) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Information Disclosure | Stub pages rendering data from an unexpected source | accept | Pages render static text + icon only. No data fetches, no user-supplied input. Nothing to leak. |
| T-14-06-02 | Availability | Proxy exclusion mistake leaving these stubs public | mitigate | Neither `/explore` nor `/search` appears in PUBLIC_PATHS (Plan 01). Default proxy behavior redirects unauth to /login. |

</threat_model>

<verification>
- Unit tests: `npm test -- --run tests/app/explore.test.tsx tests/app/search.test.tsx`
- Build: `npm run build`
- Manual: Visit /explore and /search while logged in — see coming-soon page; visit while logged out — redirect to /login (proxy).
</verification>

<success_criteria>
- `/explore` renders "Discovery is coming." with Sparkles icon + teaser copy
- `/search` renders "Search is coming." with Search icon + teaser copy
- Both pages rendered inside the authenticated shell (top nav + bottom nav around them)
- No nav link 404s
</success_criteria>

<output>
Create `.planning/phases/14-nav-shell-explore-stub/14-06-SUMMARY.md` summarizing:
- New routes: /explore, /search
- Both are static Server Components; no data dependencies
- Copy and icons locked by UI-SPEC Copywriting Contract
- Phase 16 rewrites /search; Discovery feed deferred beyond v3.0
</output>
