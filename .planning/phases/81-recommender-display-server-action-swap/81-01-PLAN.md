---
phase: 81-recommender-display-server-action-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/data/watches.ts
  - src/data/catalog.ts
  - src/app/api/extract-watch/route.ts
  - src/app/actions/wishlist.ts
  - src/app/actions/watches.ts
autonomous: true
requirements:
  - DISP-01
  - DISP-02
tags:
  - phase-81
  - type-extension
  - dal-projection
  - return-type-widening
  - foundation
user_setup: []

must_haves:
  truths:
    - "Watch domain type exposes optional brandId?: string and familyId?: string near catalogId (D-81-02); test fixtures with Partial<Watch> continue to compile without change."
    - "getWatchesByUser and getWatchById project watches_catalog.brand_id and family_id through the existing LEFT JOIN; mapRowToWatch (or the row-mapper closure) converts LEFT-JOIN null → undefined (Pitfall 4 mitigation)."
    - "getCatalogById returns a shape with canonicalBrand: string and canonicalFamily: string derived from LEFT JOIN on brands.name and watch_families.name; all six existing callers still typecheck (extension is additive per D-81-01)."
    - "upsertCatalogFromUserInput and upsertCatalogFromExtractedUrl return { catalogId: string; brandName: string; familyName: string } | null (WAS string | null) — the CTE / follow-up SELECT resolves canonical names via subselects against brands and watch_families (D-81-01)."
    - "All 5 identified upsert-helper callsites (src/app/api/extract-watch/route.ts L226 + L367; src/app/actions/wishlist.ts L138; src/app/actions/watches.ts L164 + L830) destructure the new return shape and continue to compile; callsites that only need catalogId use `result?.catalogId ?? null`."
    - "npm run build exits 0 after the type widening; no `= ANY(${arr})` patterns introduced (grep armor holds)."
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Watch.brandId? and Watch.familyId? optional fields"
      contains: "brandId?"
    - path: "src/data/watches.ts"
      provides: "getWatchesByUser + getWatchById projected brandId/familyId"
      contains: "brandId:"
    - path: "src/data/catalog.ts"
      provides: "getCatalogById extended with canonicalBrand/canonicalFamily; upsert helpers return { catalogId, brandName, familyName }"
      contains: "canonicalBrand"
  key_links:
    - from: "src/data/watches.ts (mapRowToWatch / getWatchesByUser)"
      to: "src/lib/types.ts Watch.brandId"
      via: "LEFT JOIN watches_catalog on watches.catalogId; project brandId + familyId; convert null → undefined at the row-mapper boundary"
      pattern: "watchesCatalog\\.brandId|catalogBrandId"
    - from: "src/data/catalog.ts (upsertCatalogFromUserInput / upsertCatalogFromExtractedUrl / getCatalogById)"
      to: "brands.name + watch_families.name"
      via: "LEFT JOIN or SELECT subselect resolving brand_id → name, family_id → name"
      pattern: "brands.*name|watch_families.*name"
    - from: "src/app/actions/watches.ts L164 + L830, src/app/api/extract-watch/route.ts L226 + L367, src/app/actions/wishlist.ts L138"
      to: "extended upsert return shape"
      via: "destructure { catalogId } or read result?.catalogId ?? null"
      pattern: "catalogId.*\\?\\?|\\{ ?catalogId"
---

<objective>
Widen the shared Watch domain type with optional brandId?/familyId? FK fields; project those fields through the existing LEFT JOIN in getWatchesByUser / getWatchById; extend catalog DAL helpers so they return canonical brand/family names alongside catalogId. This plan is the load-bearing foundation for Plan 02 (recommender read path) and Plan 03 (Server Action canonical overwrite) — both consume the new type surface and the extended upsert-return shape.

Purpose: Phase 80 already flipped watches_catalog.brand_id and family_id to NOT NULL. Phase 81 harvests those FKs by exposing them on the Watch domain type + threading canonical names through the write path. Widening these interfaces up front (in isolation, before behavior changes) keeps subsequent plans focused on read/write logic instead of DAL plumbing.

Output: Compile-clean type widening; getCatalogById returns canonicalBrand/canonicalFamily; upsert helpers return { catalogId, brandName, familyName }; all 5 upsert callers updated to destructure the new shape; npm run build exits 0; existing test suites still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-recommender-display-server-action-swap/81-CONTEXT.md
@.planning/phases/81-recommender-display-server-action-swap/81-RESEARCH.md
@.planning/phases/81-recommender-display-server-action-swap/81-VALIDATION.md
@.planning/REQUIREMENTS.md

@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Key contracts extracted from source (verified 2026-07-12). Downstream plans consume these; Plan 01 delivers them. -->

Current Watch type (src/lib/types.ts L51-115) — extension target:
```
export interface Watch {
  id: string
  brand: string
  model: string
  reference?: string
  status: WatchStatus
  ...
  catalogId?: string | null   // L93 — Phase 17 FK
  catalogTaste?: CatalogTasteAttributes | null  // L99 — Phase 38 D-10 (LEFT JOIN)
  ...
}
```

Add near L93 (adjacent to catalogId):
- `brandId?: string`     // Phase 81 D-81-02 — resolved via watches.catalogId → watches_catalog.brand_id JOIN
- `familyId?: string`    // Phase 81 D-81-02 — resolved via watches.catalogId → watches_catalog.family_id JOIN

Current mapRowToWatch (src/data/watches.ts L17-60) — mapper receives raw watch row; the LEFT JOIN columns arrive alongside in the .select({ … }) shape. Extend the select projection AND the .map() destructure to propagate brandId/familyId with `?? undefined` per Pitfall 4.

Current getCatalogById (src/data/catalog.ts L268-272):
```
export async function getCatalogById(id: string): Promise<CatalogEntry | null>
```
Extend to return `CatalogEntryWithCanonical` (extends CatalogEntry) exposing:
- `canonicalBrand: string`  // brands.name via LEFT JOIN on brand_id
- `canonicalFamily: string` // watch_families.name via LEFT JOIN on family_id

Current upsert helpers (src/data/catalog.ts L139-170 + L184-258):
```
upsertCatalogFromUserInput(input): Promise<string | null>
upsertCatalogFromExtractedUrl(input): Promise<string | null>
```
Widen return to `Promise<{ catalogId: string; brandName: string; familyName: string } | null>` per D-81-01. Two implementation options are Claude's discretion (RESEARCH §Code Examples): (A) extend the CTE / execute() SQL with subselects `(SELECT name FROM brands WHERE id = brand_id) AS brand_name, (SELECT name FROM watch_families WHERE id = family_id) AS family_name` and RETURN both, or (B) run a small follow-up SELECT against brands + watch_families after the CTE returns catalogId. Recommend (A) — one round-trip, mirrors the existing execute(sql`WITH ins AS (...) SELECT ... FROM resolved`) shape.

Callsites requiring destructure updates (RESEARCH Pitfall 3 — verified callsite audit):
- `src/app/api/extract-watch/route.ts` L226 — URL branch, discards names → `const result = await upsertCatalogFromExtractedUrl(...); catalogId = result?.catalogId ?? null`
- `src/app/api/extract-watch/route.ts` L367 — structured branch, same unwrap pattern
- `src/app/actions/wishlist.ts` L138 — addToWishlistFromWearEvent, discards names → same unwrap
- `src/app/actions/watches.ts` L164 — addWatch user-input branch, Plan 03 will consume brandName/familyName; Plan 01 only widens the destructure to `{ catalogId, brandName, familyName }` and STORES catalogId, leaving cleanData.brand/model overwrite for Plan 03
- `src/app/actions/watches.ts` L830 — saveCatalogOnlyFromExtract (SEED-018 admin-only, no user watches write) → discards names, unwrap only
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen Watch type + propagate brandId/familyId through DAL projections + update Watch fixture consumers if any strict-key tests exist</name>
  <files>src/lib/types.ts, src/data/watches.ts, src/lib/__tests__/types-watch-shape.test.ts (only if needed for a specific new assertion; likely NOT needed since existing fixtures are Partial<Watch>)</files>
  <read_first>
    - src/lib/types.ts (current Watch shape L51-115 — position brandId/familyId next to catalogId at L93)
    - src/data/watches.ts (mapRowToWatch L17-60; getWatchesByUser L128-184; getWatchById L190-213; observe existing catalogImageUrl pattern for LEFT-JOIN nullable propagation)
    - src/db/schema.ts (watchesCatalog block ~L488-511 — confirm brandId + familyId column bindings; brands + watchFamilies definitions)
    - .planning/phases/81-recommender-display-server-action-swap/81-RESEARCH.md § Pattern 1 (Extend LEFT JOIN projection) + § Pitfall 4 (LEFT JOIN nullable propagation)
    - src/components/insights/CollectionFitCard.test.tsx (fixture-consumer pattern — verify Partial<Watch> tolerance)
  </read_first>
  <behavior>
    - Watch interface (src/lib/types.ts) gains `brandId?: string` and `familyId?: string` positioned near `catalogId` (L93 region).
    - `getWatchesByUser` `.select({...})` gains `catalogBrandId: watchesCatalog.brandId` + `catalogFamilyId: watchesCatalog.familyId`; the `.map(...)` destructure adds `brandId: catalogBrandId ?? undefined` + `familyId: catalogFamilyId ?? undefined` to the returned Watch (either inside mapRowToWatch if it can access the JOIN row shape, or in the callsite spread — mirror the existing `imageUrl: coverStoragePath ?? (catalogImageUrl ?? undefined)` idiom).
    - `getWatchById` mirror the same extension.
    - Legacy watches with catalogId=null yield brandId=undefined + familyId=undefined (Pitfall 4).
    - Existing test suites (tests/lib/recommendations.test.ts, src/data/__tests__/recommendations.test.ts, src/app/actions/__tests__/watches-recs-invalidation.test.ts, src/components/insights/CollectionFitCard.test.tsx, and any WishlistTabContent/CollectionTabContent tests) continue to pass without fixture edits — optional fields are structurally absorbed by Partial<Watch>-shaped factories.
  </behavior>
  <action>
    Add `brandId?: string` and `familyId?: string` to Watch (src/lib/types.ts) near the existing `catalogId?: string | null` at L93 with a `// Phase 81 D-81-02` comment naming the resolution path (watches.catalogId → watches_catalog.brand_id/family_id JOIN). In `src/data/watches.ts`, extend `getWatchesByUser`'s `.select({...})` block to project `catalogBrandId: watchesCatalog.brandId` and `catalogFamilyId: watchesCatalog.familyId` alongside the existing `catalogImageUrl: watchesCatalog.imageUrl`. Destructure `catalogBrandId, catalogFamilyId` in the `.map(...)` callback and add `brandId: catalogBrandId ?? undefined` + `familyId: catalogFamilyId ?? undefined` to the returned Watch (position adjacent to the `catalogTaste: ...` spread). Mirror the same projection + `?? undefined` propagation in `getWatchById`. Do NOT modify `mapRowToWatch` itself if it only sees the base `watches` row — Phase 81's JOIN columns live in the `.select` scope, not on `WatchRow`. Verify by grep after: `grep -n 'brandId\\|familyId' src/data/watches.ts | wc -l` returns ≥ 4 (2 select entries + 2 spread additions across the 2 functions). Do NOT add a `= ANY(...)` pattern or any other Drizzle template pattern in this task.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 | grep -E "Compiled|error" ; npm run test -- tests/lib/recommendations.test.ts src/components/insights/CollectionFitCard.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>src/lib/types.ts contains `brandId?: string` and `familyId?: string` in the Watch interface; src/data/watches.ts contains `watchesCatalog.brandId` and `watchesCatalog.familyId` in the select projections of both getWatchesByUser and getWatchById; both returned Watch shapes include `brandId: … ?? undefined` and `familyId: … ?? undefined`; `npm run build` exits 0; existing test files still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend getCatalogById with canonicalBrand/canonicalFamily; widen upsertCatalogFromUserInput + upsertCatalogFromExtractedUrl return shape; update all 5 callsites</name>
  <files>src/data/catalog.ts, src/app/api/extract-watch/route.ts, src/app/actions/wishlist.ts, src/app/actions/watches.ts</files>
  <read_first>
    - src/data/catalog.ts (upsertCatalogFromUserInput L139-170; upsertCatalogFromExtractedUrl L184-258; getCatalogById L268-272; CatalogEntry type definition; observe existing `db.execute<{ id: string }>(sql`WITH ins AS (…)`)` shape)
    - src/db/schema.ts (brands + watchFamilies definitions to confirm .name column bindings)
    - src/app/api/extract-watch/route.ts L200-260 (URL branch upsert callsite around L226) + L340-400 (structured branch upsert callsite around L367)
    - src/app/actions/wishlist.ts L125-165 (addToWishlistFromWearEvent, upsert callsite L138)
    - src/app/actions/watches.ts L155-180 (addWatch user-input branch, upsert callsite L164) + L820-855 (saveCatalogOnlyFromExtract, upsert callsite L830)
    - src/app/actions/verdict.ts L45-70 (getCatalogById caller — confirm additive-safe destructure)
    - src/app/watch/new/page.tsx L155-180 (getCatalogById caller — confirm additive-safe)
    - .planning/phases/81-recommender-display-server-action-swap/81-RESEARCH.md § Pitfall 3 (return-type extension breaks unwary destructure callers — 5-callsite audit) + § Code Examples §Extended getCatalogById + §Extended upsertCatalogFromUserInput (CTE variant)
  </read_first>
  <behavior>
    - `getCatalogById(id)` returns null OR an object exposing all existing CatalogEntry fields PLUS `canonicalBrand: string` (from LEFT JOIN on brands.name via brand_id) AND `canonicalFamily: string` (from LEFT JOIN on watch_families.name via family_id). For post-Phase-80 rows (NOT NULL FKs) both canonical strings are always populated; a defensive fallback to `catalog.brand`/`catalog.model` covers the theoretical LEFT-JOIN miss.
    - `upsertCatalogFromUserInput(input)` returns null OR `{ catalogId: string; brandName: string; familyName: string }` — brandName is `brands.name` for the resolved brand_id; familyName is `watch_families.name` for the resolved family_id.
    - `upsertCatalogFromExtractedUrl(input)` returns null OR the same shape.
    - 5 callsites updated to destructure the new shape or unwrap via `result?.catalogId ?? null`:
      - `src/app/api/extract-watch/route.ts` L226 URL branch: `const result = await catalogDAL.upsertCatalogFromExtractedUrl(...); catalogId = result?.catalogId ?? null` (throws away brandName/familyName — SEED-018 pattern).
      - `src/app/api/extract-watch/route.ts` L367 structured branch: same unwrap.
      - `src/app/actions/wishlist.ts` L138: same unwrap.
      - `src/app/actions/watches.ts` L164 (addWatch user-input branch): destructure `{ catalogId: catalogIdResult, brandName, familyName }` from the extended return but ONLY assign catalogId in this plan (leave cleanData.brand/model overwrite for Plan 03 to keep concerns separate — a comment marks the seam).
      - `src/app/actions/watches.ts` L830 (saveCatalogOnlyFromExtract, SEED-018 admin-only): same unwrap (no watches write).
    - All 6 existing getCatalogById callers (verdict.ts L54; watch/new/page.tsx L167; w/[ref]/page.tsx L322/L436/L516; watches.ts L143) continue to compile — extension is additive per D-81-01 (RESEARCH A7).
  </behavior>
  <action>
    In `src/data/catalog.ts`: (a) export a new type `CatalogEntryWithCanonical extends CatalogEntry` with `canonicalBrand: string` and `canonicalFamily: string` fields. Change `getCatalogById`'s return type to `Promise<CatalogEntryWithCanonical | null>`; rewrite its body to select `{ catalog: watchesCatalog, brandName: brands.name, familyName: watchFamilies.name }`, `.leftJoin(brands, eq(brands.id, watchesCatalog.brandId))`, `.leftJoin(watchFamilies, eq(watchFamilies.id, watchesCatalog.familyId))`, `.where(eq(watchesCatalog.id, id))`, `.limit(1)`; return `{ ...mapRowToCatalogEntry(catalog), canonicalBrand: brandName ?? catalog.brand, canonicalFamily: familyName ?? catalog.model }`. (b) Change `upsertCatalogFromUserInput` return type to `Promise<{ catalogId: string; brandName: string; familyName: string } | null>`; extend the existing `db.execute<{ id: string }>(sql`WITH ins AS (…) SELECT id FROM ins UNION ALL SELECT id FROM watches_catalog …`)` by widening the row shape to `{ id: string; brand_name: string; family_name: string }`, wrapping the current inner SELECT in a `resolved` CTE that projects `id, brand_id, family_id`, and finishing with `SELECT r.id, (SELECT name FROM brands WHERE id = r.brand_id) AS brand_name, (SELECT name FROM watch_families WHERE id = r.family_id) AS family_name FROM resolved r LIMIT 1`; return `{ catalogId: rows[0].id, brandName: rows[0].brand_name, familyName: rows[0].family_name }` on hit. (c) Apply the equivalent CTE-with-subselects extension to `upsertCatalogFromExtractedUrl` (it currently returns from a single INSERT … ON CONFLICT … DO UPDATE … RETURNING id; add `RETURNING id, brand_id, family_id` and wrap in a CTE that appends the two name subselects) — SAME return shape. Then update the 5 callsites verbatim per the behavior block; for the addWatch L164 callsite ALSO leave a `// Plan 03 — Phase 81 DISP-01: consume brandName/familyName here for cleanData.brand/model overwrite` comment marker (Plan 03 will replace this). Do NOT touch the addWatch catalogId branch (L141-157) — Plan 03 owns DISP-01 there. Do NOT introduce any `= ANY(${arr})` template pattern. Preserve existing fail-loud behavior in addWatch: if the extended result is null, throw exactly as today.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 | grep -E "Compiled|error" ; grep -c "= ANY(" src/data/catalog.ts src/app/api/extract-watch/route.ts src/app/actions/wishlist.ts src/app/actions/watches.ts ; grep -n "canonicalBrand\\|brandName" src/data/catalog.ts | head -20</automated>
  </verify>
  <done>`src/data/catalog.ts` exports `CatalogEntryWithCanonical` with `canonicalBrand` and `canonicalFamily`; `getCatalogById` returns that shape; `upsertCatalogFromUserInput` and `upsertCatalogFromExtractedUrl` return `{ catalogId, brandName, familyName } | null`; the 5 callsites destructure or unwrap the new shape and continue to compile; `grep -c "= ANY("` returns 0 across the 4 modified files; `npm run build` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | addWatch/editWatch entry via getCurrentUser gate — UNCHANGED by Plan 01 (return-shape widening only). |
| Server Action → DAL | catalogDAL exports (getCatalogById, upsertCatalogFrom*) — signature changes are ADDITIVE; existing auth boundary untouched. |
| DAL → Postgres | Drizzle parameterized binds — Plan 01 uses only `${col}` bindings and `sql.join`-free extensions to the existing CTE / execute shape. No sql.raw. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-P01-01 | Tampering | Extended CTE / subselect SQL in upsertCatalogFrom* | mitigate | Preserve existing Drizzle parameterized template shape; add `(SELECT name FROM brands WHERE id = r.brand_id)` etc as constant-column subselects — no user-controlled text lands in these subselects. Verified by inspection of the produced SQL string in the plan's local-first check. |
| T-81-P01-02 | Info Disclosure | getCatalogById extension leaks brand/family name to non-owner readers | accept | brands + watch_families are public-read tables (catalog RLS D-13 in schema); joining .name adds no PII. Documented in RESEARCH §Security. |
| T-81-P01-03 | Elevation | Return-type widening bypasses existing getCurrentUser gate | accept | Signature change is on the DAL boundary, not the Server-Action boundary; the auth gate at addWatch/editWatch entry is untouched. |
| T-81-P01-04 | Tampering | `= ANY(${arr})` pattern re-introduced accidentally during CTE edit | mitigate | Forward-armor grep in verify step returns 0; production incident 2026-06-23 (Drizzle SQL ANY) explicitly memorialized in CLAUDE.md Local-First Development section. |
| T-81-P01-05 | Denial of Service | Extended CTE with 2 subselects doubles per-call query cost | accept | Subselects hit brands.id and watch_families.id (both pk-indexed uuid columns); cost is ~2 index lookups per upsert. `getCatalogById` gains 2 LEFT JOINs on pk columns — same trivial cost. RESEARCH §Perf Baselines: no measurable regression expected. |
</threat_model>

<verification>
- `npm run build` exits 0 (authoritative gate per `[[baseline-not-green-build-is-gate]]`).
- `npm run test -- tests/lib/recommendations.test.ts src/components/insights/CollectionFitCard.test.tsx src/app/actions/__tests__/watches-recs-invalidation.test.ts` — all existing suites pass with the new optional Watch fields (fixtures are Partial<Watch> per RESEARCH Pitfall 8).
- `grep -c "= ANY(" src/data/catalog.ts src/app/api/extract-watch/route.ts src/app/actions/wishlist.ts src/app/actions/watches.ts src/data/watches.ts` returns 0 for every file.
- `grep -n "canonicalBrand" src/data/catalog.ts` returns at least 2 matches (type + return statement).
- `grep -n "brandName\\|familyName" src/data/catalog.ts | grep -v '^\\s*//'` returns at least 4 matches across the 3 helpers.
- The 5 callsite files each destructure or unwrap the new shape; `git diff` shows no orphaned uses of the old `string | null` unwrap for the two upsert helpers.
</verification>

<success_criteria>
- Watch type carries optional brandId? and familyId? (D-81-02).
- DAL projects those fields through LEFT JOIN in getWatchesByUser + getWatchById.
- getCatalogById exposes canonicalBrand + canonicalFamily.
- Both upsert helpers return { catalogId, brandName, familyName } | null.
- All 5 upsert-helper callsites compile against the new shape.
- No new `= ANY(${arr})` patterns introduced (forward armor holds).
- `npm run build` exits 0; existing test suites still pass.
- Plans 02 and 03 can consume the new type surface + upsert-return shape without further DAL work.
</success_criteria>

<output>
After completion, create `.planning/phases/81-recommender-display-server-action-swap/81-01-SUMMARY.md` documenting: (a) file diff shape (LOC before/after per file), (b) any deviations from the plan actions (Rule 1 auto-fixes), (c) `npm run build` timing + result, (d) the 5 callsite updates with a one-line "unwrap vs full destructure" note per site, (e) any grep-armor observations.
</output>