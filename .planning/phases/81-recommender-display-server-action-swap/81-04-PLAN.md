---
phase: 81-recommender-display-server-action-swap
plan: 04
type: execute
wave: 3
depends_on:
  - 81-02
  - 81-03
files_modified:
  - .planning/phases/81-recommender-display-server-action-swap/fixtures/drift-hamilton.sql
  - .planning/phases/81-recommender-display-server-action-swap/81-POST-DEPLOY.md
autonomous: false
requirements:
  - RECO-01
  - RECO-02
  - RECO-03
  - RECO-04
  - DISP-01
  - DISP-02
tags:
  - phase-81
  - local-first-verification
  - drift-fixture
  - prod-deploy
  - checkpoint

must_haves:
  truths:
    - "A committed drift-fixture SQL file at .planning/phases/81-.../fixtures/drift-hamilton.sql inserts ONE watches_catalog row with denorm `brand='Hamilton Watch'` on the canonical Hamilton brand_id + a matching Hamilton family_id (drift row) — reversible via a companion DELETE stanza inside the same file (D-81-04 fixture recipe adjusted for post-Phase-79 local state per RESEARCH Open Q #1). NOT a supabase migration."
    - "Local Supabase state verified before fixture apply: viewer@horlo.test owns a Hamilton watch (canonical brand='Hamilton', post-Phase-79 hydrated); at least one peer public collector owns another Hamilton catalog row (for the multi-brand-match assertion)."
    - "Four-step D-81-04 walkthrough executed on `npm run dev` against local Supabase and recorded in the plan SUMMARY: (i) viewer loads / — drift row NOT in rail (RECO-01); (ii) peer's Hamilton row surfaces with `Fans of Hamilton love this` — canonical, not `Hamilton Watch` (RECO-04); (iii) add watch with brand typed `Hamilton Watch` — DB persists `Hamilton` (DISP-01); (iv) edit that watch retyping `Hamilton Watch` — DB persists `Hamilton` (DISP-02)."
    - "Fixture cleanup runs after walkthrough — the drift catalog row is DELETEd so re-running the fixture is idempotent and local seed state returns to canonical."
    - "81-POST-DEPLOY.md captures the bundled Vercel push runbook: single commit range covering Plans 01-03 + fixture; post-push prod smoke walk (Tyler's account, load /, add watch typing drift brand, edit that watch) — mirroring D-81-04 walkthrough steps (i)-(iv) but on prod, using Tyler's real collection (no drift-fixture insert on prod — prod is canonical post-Phase-79)."
    - "Human-action checkpoint pauses execution until operator (Tyler) explicitly approves the bundled deploy sequence after the local walkthrough is green."
  artifacts:
    - path: ".planning/phases/81-recommender-display-server-action-swap/fixtures/drift-hamilton.sql"
      provides: "Reversible drift fixture for local testing — INSERT drift row + DELETE cleanup"
      contains: "Hamilton Watch"
      min_lines: 30
    - path: ".planning/phases/81-recommender-display-server-action-swap/81-POST-DEPLOY.md"
      provides: "Operator-facing bundled deploy runbook with prod smoke walkthrough"
      contains: "git push"
      min_lines: 60
  key_links:
    - from: "fixtures/drift-hamilton.sql"
      to: "brands.name='Hamilton' + watch_families (Hamilton family)"
      via: "INSERT watches_catalog with denorm brand='Hamilton Watch' on canonical brand_id + family_id"
      pattern: "INSERT INTO watches_catalog"
    - from: "npm run dev local walkthrough"
      to: "Plan 02 + Plan 03 runtime behavior"
      via: "sign in as viewer@horlo.test; load home; add + edit watch typing drift brand; SQL SELECT to confirm persisted canonical brand"
      pattern: "npm run dev|localhost:3000"
    - from: "81-POST-DEPLOY.md"
      to: "prod Vercel deployment"
      via: "single git push covering all Plan 01-03 commits + fixture folder (fixture file itself is dev-only artifact but committed for repeatable local test)"
      pattern: "git push"
---

<objective>
Author the local-first drift fixture (per D-81-04 recipe, adjusted for post-Phase-79 local seed state), execute the four-step walkthrough against `npm run dev` + local Supabase to prove Plans 02 + 03 landed correctly, then capture the bundled Vercel deploy runbook in operator-facing 81-POST-DEPLOY.md and pause for operator go-ahead.

Purpose: Phase 81 has a HIGH silent-bug risk (self-in-own-rail if exclusion key mismatches; drift brand persisted if overwrite path missed). Neither `npm run build` nor DAL-mock unit tests can catch this class — the JOIN-through-brand exclusion + Server-Action canonical overwrite are runtime behavior. `[[local-first-dev]]` is the mandatory gate. This plan is the load-bearing correctness proof between "unit tests green" and "prod ship."

Output: Committed drift-fixture SQL (reversible); documented four-step walkthrough outcome (all four RECO-01/02/03/04 + DISP-01/02 assertions green); 81-POST-DEPLOY.md with bundled Vercel push instructions + prod smoke walk; operator checkpoint gates the final `git push` to origin/main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-recommender-display-server-action-swap/81-CONTEXT.md
@.planning/phases/81-recommender-display-server-action-swap/81-RESEARCH.md
@.planning/phases/81-recommender-display-server-action-swap/81-VALIDATION.md
@.planning/phases/81-recommender-display-server-action-swap/81-01-SUMMARY.md
@.planning/phases/81-recommender-display-server-action-swap/81-02-SUMMARY.md
@.planning/phases/81-recommender-display-server-action-swap/81-03-SUMMARY.md

@.planning/phases/79-backfill-migration-display-hydration/79-POST-DEPLOY.md
@.planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md

@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author fixtures/drift-hamilton.sql + execute the 4-step D-81-04 local walkthrough against `npm run dev`</name>
  <files>.planning/phases/81-recommender-display-server-action-swap/fixtures/drift-hamilton.sql, .planning/phases/81-recommender-display-server-action-swap/81-04-SUMMARY.md (walkthrough outcomes captured here)</files>
  <read_first>
    - .planning/phases/81-recommender-display-server-action-swap/81-CONTEXT.md § D-81-04 (fixture recipe + walkthrough steps)
    - .planning/phases/81-recommender-display-server-action-swap/81-RESEARCH.md § Open Question #1 (local seed re-verification — DB is post-hydrated per planner check on 2026-07-12: viewer's Hamilton watches ARE canonical `brand='Hamilton'`; canonical Hamilton brand row exists as single row UUID `20969364-f3b1-4b1d-ab2f-e5d22e9ffabc`; the fixture only needs the NEW drift catalog row)
    - CLAUDE.md § Local-First Development (the gate)
    - supabase/seed.sql (understand seeded test users viewer@horlo.test / vintage-anna@horlo.test / modern-mike@horlo.test / dress-dan@horlo.test + password `password123` per project-level convention)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-05-SUMMARY.md (Phase 80 local walkthrough pattern for reference)
  </read_first>
  <acceptance_criteria>
    - `fixtures/drift-hamilton.sql` exists and contains BOTH an INSERT stanza (drift catalog row) AND a DELETE stanza (idempotent cleanup) under clearly-labeled `-- APPLY` and `-- REVERT` sections.
    - The INSERT uses `SELECT id FROM brands WHERE name = 'Hamilton'` and `SELECT id FROM watch_families WHERE brand_id = ... AND name = '<a real Hamilton family name in local DB>'` as subselects for FK resolution — NOT hardcoded UUIDs (portable across environments if the fixture is ever re-used; local prod UUIDs diverge per `[[catalog-id-divergence]]`).
    - The INSERT sets `brand='Hamilton Watch'` (denorm drift) with a distinct `model` string (e.g., `'DriftTest Chrono'`) to avoid collision with the natural-key unique constraint on any existing Hamilton catalog row.
    - Fixture applies cleanly via `docker exec supabase_db_horlo psql -U postgres -d postgres -f <fixture_path>` (or equivalent local psql invocation).
    - A pre-fixture peer-collector precondition SELECT is documented in a comment header: verify at least one public peer (e.g., vintage-anna) owns AT LEAST ONE Hamilton catalog row so the multi-brand `+100` match has a candidate to fire against; if not, the fixture header notes a `-- SEED-PREP` block that INSERTs a peer Hamilton watch (respecting foreign keys) OR simply flags "manual pre-check" if the seed data already covers it.
    - Walkthrough executed against `npm run dev` on localhost:3000 with local Supabase running (`.env.development.local` overrides per CLAUDE.md § Local-First Development):
      - **Step (i) RECO-01**: Sign in as viewer@horlo.test (password password123). Load home page. Inspect "From Collectors Like You" rail. Assert: the fixture drift row (denorm brand `Hamilton Watch`) does NOT appear in the rail. Screenshot / recorded observation in SUMMARY.
      - **Step (ii) RECO-04**: In the same rail, locate a Hamilton catalog row surfaced from a peer's collection (should be the multi-brand +100 boost's target). Assert: the rendered rationale reads `Fans of Hamilton love this` (canonical) — NOT `Fans of Hamilton Watch love this`. Screenshot / recorded observation.
      - **Step (iii) DISP-01**: Navigate to `/watch/new` (or the equivalent add-watch flow). Type brand string `Hamilton Watch`, model string `Test DISP-01`, status `owned`. Submit. After submit, query the DB: `SELECT id, brand, model FROM watches WHERE user_id = <viewer uuid> AND model = 'Test DISP-01'`. Assert: `brand = 'Hamilton'` (canonical) — NOT `Hamilton Watch`.
      - **Step (iv) DISP-02**: Navigate to that new watch's edit form. Retype brand as `Hamilton Watch`, submit. Re-query DB. Assert: `brand = 'Hamilton'` still.
    - Test cleanup: DELETE the drift catalog row (REVERT stanza) AND the two viewer test watches from steps (iii)/(iv). Documented in SUMMARY as "post-walk cleanup executed."
    - `81-04-SUMMARY.md` created capturing all four step outcomes + cleanup + local DB state pre/post.
  </acceptance_criteria>
  <action>
    Create the `fixtures/` subdirectory under the phase folder and write `drift-hamilton.sql`. The file's header (multiple SQL comment lines) documents purpose ("Phase 81 D-81-04 local-first drift fixture — INSERT one drift watches_catalog row with denorm brand='Hamilton Watch' on canonical Hamilton brand_id + a real Hamilton family_id; used to verify RECO-01 exclusion and RECO-04 canonical rationale rendering. Reversible via -- REVERT stanza. NOT a supabase migration."). Structure: (§1) header + preconditions comment; (§2) -- APPLY block with (a) `\\set canonical_hamilton_id (SELECT id FROM brands WHERE name = 'Hamilton')` OR an equivalent CTE-scoped resolution, (b) `INSERT INTO watches_catalog (id, brand, model, reference, source, brand_id, family_id, image_url, style_tags, design_traits, role_tags, complications, owners_count, wishlist_count) VALUES (gen_random_uuid(), 'Hamilton Watch', 'DriftTest Chrono', 'DR-81', 'admin_curated', (SELECT id FROM brands WHERE name = 'Hamilton'), (SELECT wf.id FROM watch_families wf JOIN brands b ON b.id = wf.brand_id WHERE b.name = 'Hamilton' LIMIT 1), 'https://example.com/hamilton-placeholder.jpg', ARRAY['sport']::text[], ARRAY[]::text[], ARRAY['field']::text[], ARRAY[]::text[], 0, 0) ON CONFLICT DO NOTHING;` — adjust column names to match current schema exactly (read from src/db/schema.ts if uncertain; note: post-Phase-80 brand_id + family_id are NOT NULL so both must resolve). (§3) -- REVERT block with `DELETE FROM watches_catalog WHERE brand = 'Hamilton Watch' AND model = 'DriftTest Chrono' AND reference = 'DR-81';`. Ensure both sections are clearly delineated with comment banners so the operator (or another Claude) can execute either half without confusion. Then start `npm run dev` (verify port 3000 available); apply fixture via `docker exec -i supabase_db_horlo psql -U postgres -d postgres < <fixture_path>` (piping only the APPLY section — extract via awk/sed if needed, or split the file physically). Execute the 4-step walkthrough per the acceptance criteria, recording each step's outcome. After step (iv), pipe the REVERT section to psql; DELETE the two test watches created in steps (iii)+(iv) via SQL. Author `81-04-SUMMARY.md` capturing: fixture file contents (or reference), pre/post DB state snippets, per-step observation + pass/fail marker, cleanup confirmation.
  </action>
  <verify>
    <automated>test -f .planning/phases/81-recommender-display-server-action-swap/fixtures/drift-hamilton.sql && wc -l .planning/phases/81-recommender-display-server-action-swap/fixtures/drift-hamilton.sql ; grep -c "APPLY\\|REVERT" .planning/phases/81-recommender-display-server-action-swap/fixtures/drift-hamilton.sql ; docker exec supabase_db_horlo psql -U postgres -d postgres -c "SELECT COUNT(*) FROM watches_catalog WHERE brand = 'Hamilton Watch' AND model = 'DriftTest Chrono';" 2>&1 | grep -E "^\\s+[0-9]+"</automated>
  </verify>
  <done>fixtures/drift-hamilton.sql exists with APPLY + REVERT stanzas (≥ 30 lines); 4-step walkthrough executed and outcomes captured in 81-04-SUMMARY.md; post-walk DB state clean (drift row DELETEd; test watches removed); all four assertions green (RECO-01 drift excluded from rail, RECO-04 canonical rationale, DISP-01 add persists canonical, DISP-02 edit persists canonical).</done>
</task>

<task type="auto">
  <name>Task 2: Author 81-POST-DEPLOY.md with bundled Vercel push runbook + prod smoke walkthrough</name>
  <files>.planning/phases/81-recommender-display-server-action-swap/81-POST-DEPLOY.md</files>
  <read_first>
    - .planning/phases/79-backfill-migration-display-hydration/79-POST-DEPLOY.md (Phase 79 POST-DEPLOY pattern for reference — sign-off SQL queries + operator instruction tone)
    - .planning/phases/80-not-null-constraint-flip-ingest-hardening/80-POST-DEPLOY.md (Phase 80 staged-deploy pattern for reference — plain-English tone, ordered steps)
    - .planning/phases/81-recommender-display-server-action-swap/81-CONTEXT.md § D-81-04 § "Deploy: bundled single Vercel push. Post-push prod smoke..."
    - .planning/phases/81-recommender-display-server-action-swap/81-04-SUMMARY.md (Task 1 output — local walk outcomes summarizable in the runbook's "why we're confident" preamble)
  </read_first>
  <acceptance_criteria>
    - `81-POST-DEPLOY.md` exists with frontmatter block (phase, plan, purpose, prod_target, deploy_type: bundled).
    - Runbook contains an ordered numbered checklist of deploy steps:
      - Step 1: Confirm local walkthrough (Task 1) is green — reference `81-04-SUMMARY.md`.
      - Step 2: Confirm all Plan 01 + Plan 02 + Plan 03 commits are on `main` and `git status` is clean (Phase 81 has NO migration to push — pure code deploy).
      - Step 3: `git push origin main` — triggers Vercel auto-deploy; single push covers all 3 code plans + the fixture folder.
      - Step 4: Wait for Vercel build to succeed (monitor at vercel.com dashboard; typical 2-4 min).
      - Step 5: Prod smoke walk (Tyler's account on horlo.app) — mirroring D-81-04 walkthrough on prod: (i) load `/`, verify Tyler's own owned watches are NOT in the "From Collectors Like You" rail (RECO-01 self-exclusion sanity check); (ii) inspect one Hamilton or drift-adjacent rec's rationale text — should read canonical brand (RECO-04); (iii) add a throwaway watch with typed drift brand (e.g., pick any brand Tyler doesn't own and add a "spelling variant" of the canonical); confirm DB via prod SQL editor or a follow-up `SELECT` that `watches.brand` persisted as canonical (DISP-01); (iv) edit that throwaway watch retyping drift; confirm persists canonical (DISP-02); DELETE the throwaway watch.
    - Runbook includes a "Rollback plan" section noting that Phase 81 is code-only — rollback = `git revert` of the merge range + Vercel redeploy; no DB migration to undo.
    - Runbook uses plain-English tone per Phase 80 D-80 discretion precedent — readable months later during incident response.
    - Sign-off section at bottom: operator (Tyler) initials + timestamp when prod walk complete.
  </acceptance_criteria>
  <action>
    Create `81-POST-DEPLOY.md` following the Phase 79 + Phase 80 POST-DEPLOY structural pattern. Frontmatter fields: `phase: 81-recommender-display-server-action-swap`, `plan: 04`, `deploy_type: bundled-code-only`, `has_migration: false`, `local_gate_ref: 81-04-SUMMARY.md`, `deployed_at: <TBD by operator>`. Body sections: (§Purpose) — one paragraph naming what Phase 81 changes (recommender read-path FK identity + Server-Action canonical overwrite) and why bundled deploy is safe (no schema change; all runtime behavior verified locally). (§Pre-deploy Checklist) — reference local walkthrough green + git status clean + no worktrees per project config. (§Deploy Steps) — 5 numbered steps per acceptance criteria including exact commands (`git push origin main`) and Vercel dashboard URL. (§Prod Smoke Walk) — 4 sub-steps mirroring D-81-04 with Tyler's account; include DB inspection instructions (Supabase prod SQL editor URL + example query `SELECT brand, model, catalog_id FROM watches WHERE user_id = <tyler_uuid> AND created_at > now() - interval '10 minutes'`). (§Rollback Plan) — plain-English one-paragraph description of `git revert` + redeploy path; note that catalog/watches data written during prod smoke walk (canonical overwrite fired) does NOT need rollback — canonical strings are the correct end state. (§Sign-off) — placeholder for `Tyler: _____ | Date: _____` line.
  </action>
  <verify>
    <automated>test -f .planning/phases/81-recommender-display-server-action-swap/81-POST-DEPLOY.md && wc -l .planning/phases/81-recommender-display-server-action-swap/81-POST-DEPLOY.md ; grep -c "git push\\|Rollback\\|Sign-off\\|Prod Smoke" .planning/phases/81-recommender-display-server-action-swap/81-POST-DEPLOY.md</automated>
  </verify>
  <done>81-POST-DEPLOY.md exists (≥ 60 lines) with frontmatter + 5 numbered deploy steps + prod smoke walkthrough mirroring D-81-04 + rollback plan + sign-off placeholder.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify checkpoint — operator approves bundled prod deploy after local walk green + POST-DEPLOY captured</name>
  <what-built>
    - Reversible drift fixture at fixtures/drift-hamilton.sql
    - Local walkthrough executed against `npm run dev` + local Supabase; all four D-81-04 assertions green (RECO-01/02/03/04 + DISP-01/02 verified via UI + SQL)
    - 81-POST-DEPLOY.md staged-deploy runbook ready for prod push
  </what-built>
  <how-to-verify>
    1. Review `81-04-SUMMARY.md` for the four-step walkthrough outcomes — every step must be marked pass; SQL inspection outputs must show canonical `brand='Hamilton'` persisted.
    2. Confirm local fixture cleanup ran (no lingering `Hamilton Watch`/`DriftTest Chrono` row in watches_catalog; no lingering test watches under viewer@horlo.test).
    3. Review `81-POST-DEPLOY.md` for the 5-step deploy checklist + prod smoke walk instructions.
    4. Run `git status` — verify all Phase 81 commits are on `main`; nothing uncommitted.
    5. Run `git log --oneline -20` — verify the Plan 01 + Plan 02 + Plan 03 + Plan 04 commits are present as a coherent range.
    6. When ready to push: reply `approved` (or `approved, pushing` + push output) OR `hold — <reason>` with any concerns; the executor will pause here regardless until you confirm.
  </how-to-verify>
  <resume-signal>Type "approved" once local walk is confirmed green and you have executed `git push origin main`, OR describe any issue that requires plan revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator hands → prod | Manual `git push origin main` gates the Vercel deploy; no auto-push. |
| Local fixture SQL → local DB | Runs against `docker exec supabase_db_horlo` — dev-only; the fixture SQL is never applied to prod. |
| Prod smoke walk → prod watches table | Tyler's real account writes a throwaway watch; must be DELETEd post-walk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-P04-01 | Info Disclosure | Drift fixture SQL accidentally applied to prod | mitigate | File lives under `.planning/phases/81-.../fixtures/` — NOT under `supabase/migrations/`; Supabase's migration runner never picks it up. Runbook explicitly notes "local-only fixture — NOT for prod." |
| T-81-P04-02 | Repudiation | Prod smoke walk leaves stale throwaway watch on Tyler's collection | mitigate | POST-DEPLOY runbook Step 5 sub-step includes "DELETE the throwaway watch" as an explicit checklist item; Tyler is the operator and owns cleanup. |
| T-81-P04-03 | Tampering | git push covers unrelated in-flight commits | mitigate | Pre-push checkpoint verifies `git status` clean + `git log --oneline` shows a coherent Phase 81 commit range only. |
| T-81-P04-04 | Elevation | Operator skips human-verify checkpoint | accept | The checkpoint is autonomous:false — execution literally pauses; the runbook's sign-off placeholder is a paper trail. |
| T-81-P04-05 | Denial of Service | Vercel build fails on push | accept | Rollback plan documented; `git revert` + redeploy path is standard. Phase 81 has no migration to undo. |
</threat_model>

<verification>
- Local walkthrough (Task 1) captured in 81-04-SUMMARY.md with per-step pass markers.
- fixtures/drift-hamilton.sql exists with APPLY + REVERT stanzas; APPLY uses SELECT-subquery FK resolution (not hardcoded UUIDs).
- 81-POST-DEPLOY.md exists ≥ 60 lines with 5-step checklist + prod smoke walk + rollback + sign-off.
- Task 3 checkpoint reached and operator resumed with "approved" — the prod deploy is EXPLICITLY OPERATOR-APPROVED.
- Post-deploy: prod smoke walk (documented in POST-DEPLOY.md sign-off section) confirms all 4 assertions green on horlo.app.
</verification>

<success_criteria>
- Local first-verification recipe executed and green (D-81-04 walk).
- Bundled Vercel deploy runbook captured in 81-POST-DEPLOY.md.
- Operator checkpoint gates the final push.
- Post-push prod smoke walk confirms Phase 81 correctness against real prod data.
- No lingering local or prod test data (fixture cleanup + throwaway watch DELETE both executed).
- Phase 81 marked complete in ROADMAP.md + STATE.md after operator sign-off.
</success_criteria>

<output>
Task 1 produces `81-04-SUMMARY.md` capturing walkthrough + cleanup. Task 2 produces `81-POST-DEPLOY.md`. Task 3 (checkpoint) records operator approval + deploy commit SHA in the POST-DEPLOY sign-off section after resume. Final plan SUMMARY reference the two artifacts + checkpoint outcome.
</output>