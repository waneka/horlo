---
phase: 81-recommender-display-server-action-swap
plan: 03
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - src/app/actions/watches.ts
  - src/app/actions/__tests__/watches-recs-invalidation.test.ts
autonomous: true
requirements:
  - DISP-01
  - DISP-02
tags:
  - phase-81
  - server-actions
  - canonical-overwrite
  - write-path
user_setup: []

must_haves:
  truths:
    - "addWatch catalogId branch (L141-157) reads catalogRow.canonicalBrand and catalogRow.canonicalFamily (WAS catalogRow.brand and catalogRow.model) — assigning canonical strings into cleanData.brand and cleanData.model. Reference remains catalogRow.reference ?? undefined per D-10 override (unchanged, per D-81-01 note). (DISP-01)"
    - "addWatch user-input branch (L158-177) destructures { catalogId, brandName, familyName } from the extended upsertCatalogFromUserInput return (Plan 01 output) and overwrites cleanData.brand = brandName, cleanData.model = familyName BEFORE createPayload is built at L181. Fail-loud on null result preserved. (DISP-01)"
    - "editWatch adds a canonical-overwrite path BEFORE the updateWatch call at L668 (and BEFORE db.transaction path at L639-662 for the owned→sold branch). Guarded on `priorRow.catalogId != null && (cleanData.brand !== undefined || cleanData.model !== undefined)`. Reads canonical strings via extended getCatalogById(priorRow.catalogId); overwrites both cleanData AND updatePayload for the brand/model fields when edited. Non-catalog-linked watches (priorRow.catalogId=null) skip overwrite — user's typed strings persist (D-81 Specifics § legacy). (DISP-02)"
    - "src/app/actions/__tests__/watches-recs-invalidation.test.ts extended with FOUR new DISP test cases: (i) addWatch catalogId branch — assert cleanData.brand persisted equals canonical string; (ii) addWatch user-input branch — assert cleanData.brand persisted equals resolver brandName; (iii) editWatch on catalog-linked watch — assert overwrite fires; (iv) editWatch on non-catalog-linked watch — assert overwrite is SKIPPED. Existing 4 Phase 75 cases still pass."
    - "Existing rec-invalidation contract preserved verbatim: updateTag(`viewer:${user.id}:recs`) fires on addWatch + editWatch; no revalidateTag call on the recs tag; cross-user revalidateTag('explore', 'max') + revalidateTag(`profile:...`, 'max') unchanged."
    - "npm run build exits 0; no `= ANY(${arr})` patterns introduced."
  artifacts:
    - path: "src/app/actions/watches.ts"
      provides: "addWatch DISP-01 canonical overwrite (both branches); editWatch DISP-02 canonical overwrite path"
      contains: "canonicalBrand"
    - path: "src/app/actions/__tests__/watches-recs-invalidation.test.ts"
      provides: "4 new DISP-01/02 unit cases folded into the existing suite"
      contains: "DISP-01|DISP-02"
  key_links:
    - from: "src/app/actions/watches.ts addWatch catalogId branch (L141-157)"
      to: "catalogRow.canonicalBrand and catalogRow.canonicalFamily"
      via: "extended catalogDAL.getCatalogById return shape from Plan 01"
      pattern: "canonicalBrand|canonicalFamily"
    - from: "src/app/actions/watches.ts addWatch user-input branch (L158-177)"
      to: "upsertResult.brandName and upsertResult.familyName"
      via: "extended catalogDAL.upsertCatalogFromUserInput return shape from Plan 01 (which currently only stores catalogId — this plan consumes brandName/familyName)"
      pattern: "brandName|familyName"
    - from: "src/app/actions/watches.ts editWatch (before L668)"
      to: "extended getCatalogById(priorRow.catalogId)"
      via: "conditional overwrite when priorRow.catalogId != null AND user edited brand or model"
      pattern: "priorRow\\.catalogId.*getCatalogById|canonicalBrand"
---

<objective>
Auto-overwrite `watches.brand` and `watches.model` from canonical `brands.name` / `watch_families.name` (resolved via catalogId → brand_id → brands.name JOIN) on every addWatch + editWatch write. Discards user-typed drift like `Hamilton Watch` in favor of canonical `Hamilton` — the display column never carries free-text drift on new writes after this plan ships.

Purpose: Closes DISP-01 (addWatch overwrite — both catalogId-supplied and user-input branches; the current addWatch line 153 reads catalogRow.brand which is the DENORM column, the exact source of drift) and DISP-02 (editWatch overwrite — new path before UPDATE). Both Server Actions preserve their existing ActionResult<Watch> return contract, cache-invalidation graph, and ownership gate. Depends on Plan 01's extended getCatalogById + upsertCatalogFromUserInput return shapes.

Output: addWatch persists cleanData.brand = canonical brand string in BOTH branches; editWatch persists canonical brand/model when the user edits either field on a catalog-linked watch; non-catalog-linked watches (priorRow.catalogId=null) bypass overwrite (existing user-typed strings persist); rec-invalidation unit tests extended with 4 DISP cases; npm run build exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-recommender-display-server-action-swap/81-CONTEXT.md
@.planning/phases/81-recommender-display-server-action-swap/81-RESEARCH.md
@.planning/phases/81-recommender-display-server-action-swap/81-VALIDATION.md
@.planning/phases/81-recommender-display-server-action-swap/81-01-SUMMARY.md

@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Contracts consumed from Plan 01 (foundation). -->

Consumed from Plan 01 (src/data/catalog.ts):
- `getCatalogById(id): Promise<CatalogEntryWithCanonical | null>` where `CatalogEntryWithCanonical` extends `CatalogEntry` with `canonicalBrand: string` + `canonicalFamily: string`.
- `upsertCatalogFromUserInput(input): Promise<{ catalogId: string; brandName: string; familyName: string } | null>`.

Consumed from src/app/actions/watches.ts (current shapes to modify):
- `addWatch(data): Promise<ActionResult<Watch>>` — L97+. Two identity-resolution branches:
  - **Branch A (catalogId-supplied)**: L141-157. Currently sets `cleanData.brand = catalogRow.brand` (denorm) at L153 — Phase 81 target: `= catalogRow.canonicalBrand`. Reference at L155 stays `catalogRow.reference ?? undefined` (D-10 override — unchanged per RESEARCH § Pattern 3 note).
  - **Branch B (user-input, resolver-driven)**: L158-177. Plan 01 already widened the destructure to `{ catalogId, brandName, familyName }` at L164 (per Plan 01 Task 2's comment marker); Plan 03 consumes brandName + familyName by assigning `cleanData.brand = brandName; cleanData.model = familyName` before `createPayload` builds at L181.
- `editWatch(watchId, data): Promise<ActionResult<Watch>>` — L553-704. `priorRow = await watchDAL.getWatchById(user.id, watchId)` at L595 gates ownership (unchanged); `updatePayload` is built from cleanData at L586; DISP-02 overwrite path inserts BEFORE the owned→sold transition detection at L611 (so both the transaction path L639-662 AND the non-transaction path L668 pick up the overwrite via `updatePayload`).

Import surface additions:
- Plan 03 uses `catalogDAL.getCatalogById` from `@/data/catalog` — already imported in the file (used in addWatch L143).

Cache-invalidation contract (unchanged):
- addWatch: `updateTag(\`viewer:${user.id}:recs\`)` L340 + `revalidateTag('explore', 'max')` L360 + `revalidateTag(\`profile:...\`, 'max')` L349 — all preserved.
- editWatch: `updateTag(\`viewer:${user.id}:recs\`)` L676 + `revalidateTag('explore', 'max')` L694 + `revalidateTag(\`profile:...\`, 'max')` L685 — all preserved.
- Phase 75 read-your-own-write semantic via `updateTag` (not deprecated single-arg `revalidateTag`) verified in existing test file.

Test surface (src/app/actions/__tests__/watches-recs-invalidation.test.ts):
- Already mocks `getCatalogById`, `upsertCatalogFromUserInput`, `updateTag`, `revalidateTag`, DAL writers.
- 4 existing Phase 75 cases (addWatch/moveWishlistToCollection/editWatch/removeWatch — each asserts updateTag(RECS_TAG) called + no revalidateTag on that tag).
- The `mkWatchRow` factory (L103-113) already carries brand/model/catalogId — Plan 03 extends the mock returns to include canonicalBrand/canonicalFamily on getCatalogById + brandName/familyName on upsertCatalogFromUserInput.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire DISP-01/02 canonical overwrite into addWatch (both branches) + editWatch, preserving fail-loud + ActionResult contract; extend watches-recs-invalidation.test.ts with 4 DISP cases</name>
  <files>src/app/actions/watches.ts, src/app/actions/__tests__/watches-recs-invalidation.test.ts</files>
  <read_first>
    - src/app/actions/watches.ts full addWatch (L97-370) + full editWatch (L553-704); note the addWatch catalogId branch at L141-157, user-input branch at L158-177, createPayload build at L181, and existing fail-loud pattern at L169-175. In editWatch note the priorRow fetch at L595, wishlist-sortOrder block at L600-609, owned→sold detection at L611-620, transaction path L639-662, non-transaction path L668, and updatePayload declaration at L586.
    - src/data/catalog.ts (Plan 01 output — confirm getCatalogById returns CatalogEntryWithCanonical + upsertCatalogFromUserInput returns { catalogId, brandName, familyName } | null)
    - src/app/actions/__tests__/watches-recs-invalidation.test.ts full file 228 lines (understand mock setup, VALID_UUID/VIEWER_ID/RECS_TAG constants, mkWatchRow helper, existing 4-case describe block)
    - .planning/phases/81-recommender-display-server-action-swap/81-RESEARCH.md § Pattern 3 (Server-Action canonical overwrite — both addWatch branches share one shape) + § editWatch canonical overwrite path (L595-668 insertion point) + § Pitfall 3 (return-type extension breaks unwary destructure)
    - .planning/phases/81-recommender-display-server-action-swap/81-CONTEXT.md § D-81-01 canonical-name source on writes + § Specifics § "The two-branch overwrite in addWatch shares a single JOIN helper" + § Specifics § "Legacy watches with catalogId = null bypass the overwrite"
  </read_first>
  <behavior>
    - `addWatch` catalogId branch (L141-157): reads `catalogRow.canonicalBrand` (WAS `catalogRow.brand`) and `catalogRow.canonicalFamily` (WAS `catalogRow.model`). Reference stays `catalogRow.reference ?? undefined` (unchanged).
    - `addWatch` user-input branch (L158-177): after the extended upsertResult destructure lands (per Plan 01's comment marker), sets `cleanData.brand = upsertResult.brandName` and `cleanData.model = upsertResult.familyName` BEFORE the createPayload build at L181. Fail-loud on `!upsertResult` preserved.
    - `editWatch` DISP-02 overwrite path (new, inserted after L609 wishlist-sortOrder block, BEFORE L611 owned→sold detection):
      - Guard: `if (priorRow.catalogId && (cleanData.brand !== undefined || cleanData.model !== undefined))`.
      - Fetch `const catalogRow = await catalogDAL.getCatalogById(priorRow.catalogId)`.
      - If catalogRow present: when `cleanData.brand !== undefined` → `cleanData.brand = catalogRow.canonicalBrand` AND `updatePayload = { ...updatePayload, brand: catalogRow.canonicalBrand }`; when `cleanData.model !== undefined` → same for canonicalFamily.
      - Non-catalog-linked watches (`priorRow.catalogId=null`): bypass — user's typed strings persist (DISP-01/02 don't apply to Phase 17 ON DELETE SET NULL legacy rows per D-81 Specifics).
      - Failure semantics: if getCatalogById returns null (defensive — shouldn't happen post-Phase-80 since FKs are NOT NULL), skip overwrite and let the user's typed strings persist (fail-safe over fail-loud — the request is on an existing watch with an existing catalogId, so a null return implies a race with catalog deletion which we tolerate silently).
    - `editWatch` transaction branch (L639-662 for owned→sold): picks up the overwrite naturally because it reads from `updatePayload` (L651).
    - Cache-invalidation contract preserved verbatim: `updateTag(\`viewer:${user.id}:recs\`)` at L340 (addWatch) + L676 (editWatch); `revalidateTag('explore', 'max')` unchanged; `revalidateTag(\`profile:${username}\`, 'max')` unchanged.
    - Test file extensions: 4 new cases inside the existing describe block:
      - Case 5: `addWatch catalogId branch — persists canonical brand/model from getCatalogById.canonicalBrand/canonicalFamily`. Mock `getCatalogById` to return `{ id, brand: 'Hamilton Watch' (denorm), model: 'Khaki Field' (denorm), reference: null, canonicalBrand: 'Hamilton', canonicalFamily: 'Khaki Field Mechanical', ... }`; call addWatch with `catalogId: VALID_CATALOG_UUID, brand: 'Anything', model: 'Whatever', status: 'owned'`; assert `createWatch` was called with a payload where `brand === 'Hamilton'` and `model === 'Khaki Field Mechanical'`.
      - Case 6: `addWatch user-input branch — persists canonical brand/model from upsertCatalogFromUserInput.brandName/familyName`. Mock `upsertCatalogFromUserInput` to return `{ catalogId: VALID_CATALOG_UUID, brandName: 'Hamilton', familyName: 'Khaki Field Mechanical' }`; call addWatch with `brand: 'Hamilton Watch', model: 'Khaki Field', status: 'owned'` (no catalogId); assert `createWatch` was called with `brand === 'Hamilton'` and `model === 'Khaki Field Mechanical'`.
      - Case 7: `editWatch on catalog-linked watch — canonical overwrite fires`. Mock `getWatchById` (priorRow) with `catalogId: VALID_CATALOG_UUID, brand: 'Hamilton', model: 'Old Model', status: 'owned'`; mock `getCatalogById` to return `{ ..., canonicalBrand: 'Hamilton', canonicalFamily: 'Khaki Field Mechanical' }`; call editWatch with `{ brand: 'Hamilton Watch', model: 'New Model' }`; assert `updateWatch` called with `brand === 'Hamilton'` and `model === 'Khaki Field Mechanical'`.
      - Case 8: `editWatch on non-catalog-linked watch — canonical overwrite SKIPPED`. Mock `getWatchById` (priorRow) with `catalogId: null`; call editWatch with `{ brand: 'Whatever Typed' }`; assert `getCatalogById` was NOT called; assert `updateWatch` called with `brand === 'Whatever Typed'` (user's typed string persists).
    - Existing 4 Phase 75 cases still pass (mock return shape updates absorb transparently since the assertions read updateTag/revalidateTag call args, not writer payloads).
  </behavior>
  <action>
    In `src/app/actions/watches.ts`: (a) In addWatch catalogId branch, change line 153 `cleanData.brand = catalogRow.brand` → `cleanData.brand = catalogRow.canonicalBrand` and line 154 `cleanData.model = catalogRow.model` → `cleanData.model = catalogRow.canonicalFamily`. Leave L155 reference assignment unchanged (D-10 override intent per RESEARCH § Pattern 3). Add a one-line JSDoc-style comment above the branch: `// DISP-01 Phase 81: read canonical strings from extended getCatalogById (D-81-01)`. (b) In addWatch user-input branch, Plan 01 left a comment marker `// Plan 03 — Phase 81 DISP-01: consume brandName/familyName here` between the upsertResult destructure and the `catalogId = catalogIdResult` (or renamed) assignment. Insert `cleanData.brand = upsertResult.brandName` and `cleanData.model = upsertResult.familyName` at that marker; delete the comment marker. Verify: `cleanData.brand` and `cleanData.model` are assigned BEFORE the L181 `let createPayload: typeof parsed.data = cleanData` line — cleanData spreads canonical values into createPayload. (c) In editWatch, insert the DISP-02 canonical overwrite block AFTER L609 (end of wishlist-sortOrder block) and BEFORE L611 (owned→sold detection). Wrap in the guarded-conditional per behavior spec. Use `catalogDAL.getCatalogById(priorRow.catalogId)`; update both cleanData AND updatePayload for the brand/model fields; skip on catalogRow-null (fail-safe). Add JSDoc comment: `// DISP-02 Phase 81: canonical overwrite before UPDATE (D-81-01) — fires only when priorRow has a catalogId AND user edited brand or model.` In `src/app/actions/__tests__/watches-recs-invalidation.test.ts`: (d) Extend the existing mock at the top for `getCatalogById` to include `canonicalBrand` and `canonicalFamily` in default returns. Extend `mkWatchRow` helper (L103-113) to accept overrides for canonicalBrand/canonicalFamily (with sane defaults matching brand/model). (e) Add the 4 new test cases (Case 5-8) inside the existing describe block. Each case: `vi.clearAllMocks()` in the beforeEach (already exists); mock the required DAL functions; call the Server Action; assert `createWatch` or `updateWatch` was called with the expected payload — inspect via `vi.mocked(createWatch).mock.calls[0][2]` for the third arg (the createPayload) or `vi.mocked(updateWatch).mock.calls[0][2]` for the updatePayload. Use `expect(...).toMatchObject({ brand: 'Hamilton', model: 'Khaki Field Mechanical' })` for readability. For Case 8 (non-catalog-linked skip), assert `expect(vi.mocked(getCatalogById)).not.toHaveBeenCalled()` AND `expect(vi.mocked(updateWatch).mock.calls[0][2]).toMatchObject({ brand: 'Whatever Typed' })`. Do NOT modify the existing 4 Phase 75 cases beyond potentially adjusting getCatalogById default mock return shape.
  </action>
  <verify>
    <automated>npm run test -- src/app/actions/__tests__/watches-recs-invalidation.test.ts 2>&1 | tail -30 ; grep -n "canonicalBrand\\|canonicalFamily" src/app/actions/watches.ts ; npm run build 2>&1 | tail -10 | grep -E "Compiled|error"</automated>
  </verify>
  <done>src/app/actions/watches.ts: addWatch catalogId branch reads canonicalBrand + canonicalFamily; user-input branch overwrites cleanData.brand/model with upsertResult.brandName/familyName; editWatch has the DISP-02 overwrite block before owned→sold detection. src/app/actions/__tests__/watches-recs-invalidation.test.ts has 8 total tests passing (4 Phase 75 + 4 Phase 81 DISP). `npm run build` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | addWatch/editWatch entry via getCurrentUser gate — UNCHANGED. Canonical overwrite happens on server-side data only. |
| Server Action → DAL | catalogDAL.getCatalogById + upsertCatalogFromUserInput — Plan 01 signature widening consumed here; ownership already verified via priorRow at editWatch L595. |
| DAL → Postgres | Not directly touched by Plan 03 — canonical strings come from catalog row projections written server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-P03-01 | Elevation | editWatch overwrite path bypasses ownership gate | mitigate | Overwrite executes AFTER `priorRow = getWatchById(user.id, watchId)` at L595 — ownership already verified. Canonical strings resolved via `priorRow.catalogId` (owner-scoped, never client-supplied). |
| T-81-P03-02 | Tampering | Client-supplied catalogId in addWatch bypasses canonical overwrite | accept | Existing L143 fail-fast on `getCatalogById(cleanData.catalogId)` null return unchanged; a spoofed catalogId either resolves to a real catalog row (canonical wins) or returns 400. |
| T-81-P03-03 | Repudiation | Non-catalog-linked watch's edit persists user-typed drift | accept | Documented in D-81 Specifics — Phase 17 ON DELETE SET NULL edge case. Post-Phase-80 all new catalog rows have FKs; legacy null-catalogId watches are the residual concern and are tolerated (user's typed strings persist). |
| T-81-P03-04 | Info Disclosure | canonicalBrand fetch leaks brand name to non-owner | accept | brands table is public-read (RLS D-13); the fetch is on the owner's own priorRow.catalogId — no cross-user surface. |
| T-81-P03-05 | Denial of Service | Additional getCatalogById call per edit adds latency | accept | Single pk-indexed SELECT with 2 LEFT JOINs on pk columns — trivial cost against the 4-5 DB calls editWatch already makes (priorRow fetch, transaction, revalidates). |
| T-81-P03-06 | Tampering | catalogRow-null defensive skip leaves stale drift on edit | accept | Defensive fail-safe over fail-loud on an already-verified priorRow — the null result implies a race with catalog deletion that the recommender handles via LEFT-JOIN degradation anyway. |
</threat_model>

<verification>
- `npm run build` exits 0.
- `npm run test -- src/app/actions/__tests__/watches-recs-invalidation.test.ts` — 8 tests pass (4 Phase 75 + 4 Phase 81 DISP-01/02).
- `grep -n "canonicalBrand" src/app/actions/watches.ts` returns ≥ 3 matches (addWatch catalogId branch + editWatch overwrite path).
- `grep -n "upsertResult.brandName\\|upsertResult.familyName" src/app/actions/watches.ts` returns ≥ 2 matches (user-input branch overwrite).
- No `= ANY(${arr})` patterns introduced in src/app/actions/watches.ts.
- Existing addWatch fail-loud contract (L173-175) intact — throws when upsert returns null.
- `grep -n "priorRow.catalogId" src/app/actions/watches.ts` returns ≥ 1 match in editWatch (the DISP-02 guard).
- Cache-invalidation grep audit unchanged: `grep -c "updateTag" src/app/actions/watches.ts` == same count as pre-Plan-03 (verified by git diff).
</verification>

<success_criteria>
- addWatch catalogId branch writes canonical brand/model from getCatalogById (DISP-01).
- addWatch user-input branch writes canonical brand/model from upsertCatalogFromUserInput (DISP-01).
- editWatch overwrites brand/model from canonical strings when user edited either field on a catalog-linked watch (DISP-02).
- Non-catalog-linked watches bypass overwrite (documented Phase 17 legacy edge case).
- Cache-invalidation graph unchanged; existing rec-invalidation tests still pass.
- 4 new DISP unit cases prove the overwrite behavior at Server-Action boundary.
- `npm run build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/81-recommender-display-server-action-swap/81-03-SUMMARY.md` documenting: (a) LOC diff for src/app/actions/watches.ts, (b) test pass counts before/after (4 → 8), (c) Rule-1 deviations if any, (d) confirmation that all four cache-invalidation lines (updateTag + 2 revalidateTag) exist unchanged in both addWatch + editWatch, (e) confirmation that the addWatch fail-loud path at L173-175 is preserved.
</output>