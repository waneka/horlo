---
phase: 57-comment-thread-ui-feed-extension-grid-counts
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/data/comments.ts
  - src/app/actions/comments.ts
  - src/data/activities.ts
  - src/lib/feedTypes.ts
  - src/components/home/ActivityRow.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CMNT-03, CMNT-08, FEED-06, FEED-07]

must_haves:
  truths:
    - "Per CMNT-03 (reconciled to operator decision 2026-05-22 + ROADMAP SC1): getCommentsForTarget returns comments newest-first (createdAt DESC), so the UI list renders newest at the top and optimistic inserts go at the TOP (CMNT-08)"
    - "Per FEED-06 / D-13: addCommentAction records a 'commented' activity ON CREATE ONLY (never on edit/delete), guarded by ownerId !== user.id, carrying targetType / targetOwnerId / watchStatus / wearEventId in metadata"
    - "Per FEED-07 / D-12 (LEAK GUARD): getFeedForUser does NOT surface a comment on a gated wishlist watch to a feed viewer who is not mutual-follow with the TARGET OWNER — the gate keys off the target owner, NOT the actor/commenter"
    - "deleteCommentAction revalidates the owner's profile:{username} tag (read-then-delete), so grid count badges refresh on delete (closes the Phase 55 Pitfall-6 gap)"
    - "REQUIREMENTS.md CMNT-03/CMNT-08 text is reconciled from the stale oldest-first/compose-below wording to newest-first/compose-above/optimistic-at-top"
    - "Per D-14: comment activities are ONE row per comment, EXEMPT from the feedAggregate ≥3-in-1hr collapse (mirroring watch_worn) — adding 'commented' to the ActivityType union requires NO change to feedAggregate.ts because its aggregatable check is a positive allowlist that omits 'commented'"
  artifacts:
    - path: "src/data/comments.ts"
      provides: "newest-first read (desc) + reconciled inline comment"
      contains: "desc"
    - path: "src/app/actions/comments.ts"
      provides: "logActivity('commented') on create + deleteCommentAction revalidateTag fix"
      contains: "logActivity"
    - path: "src/data/activities.ts"
      provides: "'commented' ActivityType + CommentedMetadata + logActivity overload + getFeedForUser D-12 gate + normalizeMetadata wearEventId passthrough"
      contains: "commented"
    - path: "src/lib/feedTypes.ts"
      provides: "ActivityType widened + RawFeedRow.metadata.wearEventId"
      contains: "commented"
    - path: "src/components/home/ActivityRow.tsx"
      provides: "commented verb + wear link branch"
      contains: "commented on"
  key_links:
    - from: "src/app/actions/comments.ts addCommentAction"
      to: "src/data/activities.ts logActivity"
      via: "logActivity(user.id, 'commented', watchId, metadata) guarded by ownerId !== user.id"
      pattern: "logActivity.*commented"
    - from: "src/data/activities.ts getFeedForUser WHERE"
      to: "follows table mutual-follow EXISTS subquery on metadata->>'targetOwnerId'"
      via: "commented branch keyed off target owner, not actor"
      pattern: "targetOwnerId"
    - from: "src/components/home/ActivityRow.tsx"
      to: "/wear/${row.metadata.wearEventId}"
      via: "wear link branch when watchId is null"
      pattern: "wearEventId"
---

<objective>
Land the entire backend correctness surface for comment activity: reconcile comment order to newest-first (CMNT-03), wire `addCommentAction` to record a `commented` feed activity on create (FEED-06), implement the per-viewer target-owner feed gate that prevents gated-wishlist-comment leakage (FEED-07 / D-12 — the single highest-risk leak in this phase), fix the `deleteCommentAction` revalidate gap, and extend the feed row to render the `commented` verb with a wear-link branch.

Purpose: Make the Wave 0 action + feed + comments-order tests GREEN, and close the two stale/missing correctness items (comment order, delete revalidate) plus the FEED-07 leak.
Output: 5 source files + REQUIREMENTS.md reconciliation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-RESEARCH.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-CONTEXT.md

<interfaces>
<!-- Verified live source. Use these directly; do not re-explore. -->

src/data/comments.ts:
- line 18: `import { and, asc, eq } from 'drizzle-orm'`  ← add `desc`, drop `asc` if unused
- lines 138-158: getCommentsForTarget — orderBy(asc(comments.createdAt)) at lines 150 & 156, with inline comment "(oldest first, CMNT-03)" near line 136-137

src/app/actions/comments.ts:
- line 51-184: addCommentAction. Watch SELECT at lines ~73-83 fetches { userId, brand, model } — must add imageUrl + status. ownerId !== user.id guard for notifications at lines 143-177.
- line 133: `revalidateTag(\`profile:${ownerProfile.username}\`, 'max')` — TWO-ARG form (copy exactly)
- line 186: editCommentAction — owner-lookup pattern at lines 208-234 (read comment, resolve owner from watchId OR wearEventId, revalidateTag). MIRROR this in deleteCommentAction.
- lines 243-269: deleteCommentAction — currently `await deleteComment(...)` then `return {success,data:{id}}` with NO revalidateTag.

src/data/activities.ts:
- line 21: `export type ActivityType = 'watch_added' | 'wishlist_added' | 'watch_worn'`
- lines 23-46: WatchAddedMetadata / WishlistAddedMetadata / WatchWornMetadata
- line 48: `export type ActivityMetadata = WatchAddedMetadata | WishlistAddedMetadata | WatchWornMetadata`
- lines 53-83: logActivity overloads (3) + impl body `db.insert(activities).values({ userId, type, watchId, metadata })`
- lines 107-195: getFeedForUser — innerJoin profileSettings on activities.userId (lines ~130-131); inner WHERE gate at lines 161-165 (actor-keyed)
- lines 197-211: normalizeMetadata — strips to { brand, model, imageUrl, visibility? }

src/lib/feedTypes.ts:
- line 12: `export type ActivityType = 'watch_added' | 'wishlist_added' | 'watch_worn'` (SECOND definition — keep in sync)
- lines 37-65: RawFeedRow; metadata object lines 47-52 = { brand, model, imageUrl, visibility? }

src/lib/feedAggregate.ts (verified — D-14 exemption is FREE):
- lines 21-23: `const aggregatable = head.type === 'watch_added' || head.type === 'wishlist_added'` — a POSITIVE ALLOWLIST, not an exhaustive switch. `watch_worn` is already exempt (not in the list). Adding `'commented'` to ActivityType requires NO edit here; the widened union does not break this file because there is no exhaustive match. DO NOT add 'commented' to the aggregatable allowlist.

src/components/home/ActivityRow.tsx:
- lines 11-15: `const VERBS: Record<RawFeedRow['type'], string> = { watch_added:'added', wishlist_added:'wishlisted', watch_worn:'wore' }`
- lines 49-58: watch-name link — `row.watchId ? <Link href={\`/watch/${row.watchId}\`}> : <span>`

src/db/schema.ts (verified):
- activities.type is `text('type').notNull()` — NOT an enum → NO SQL migration
- activities has `watchId` (nullable FK, ON DELETE SET NULL) but NO wearEventId column
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile comment order (newest-first) + REQUIREMENTS text</name>
  <files>src/data/comments.ts, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/data/comments.ts (full — focus lines 1-17 PRIVACY LAYER NOTE, 18 import, 60-86 gate, 138-158 getCommentsForTarget)
    - tests/data/getCommentsForTarget.test.ts (Wave 0 — the desc-order assertion this task turns GREEN)
    - .planning/REQUIREMENTS.md lines 23 (CMNT-03) and 28 (CMNT-08)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md (comments.ts modifications section, lines 421-449)
  </read_first>
  <behavior>
    - getCommentsForTarget(viewerId, {type:'watch', id}) returns rows ordered by createdAt DESC (newest-first).
    - getCommentsForTarget(viewerId, {type:'wear', id}) returns rows ordered by createdAt DESC.
    - Gated viewer (wishlist + non-mutual) still returns [] (unchanged — D-04).
  </behavior>
  <action>
    In `src/data/comments.ts`:
    - Change the import at line 18 from `{ and, asc, eq }` to `{ and, desc, eq }` (drop `asc` if it has no other consumer; verify with a grep first — if `asc` is used elsewhere keep it).
    - Change both `orderBy(asc(comments.createdAt))` calls (lines ~150 and ~156) to `orderBy(desc(comments.createdAt))`.
    - Update the stale inline comment near lines 136-137 from "(oldest first, CMNT-03)" to: "(newest first — CMNT-03 superseded by operator decision 2026-05-22, ROADMAP SC1)".

    In `.planning/REQUIREMENTS.md`:
    - Rewrite CMNT-03 (line 23) to: "Comments render as a flat, **newest-first** list showing the author avatar, linked username, body, and relative timestamp; the compose box sits **above** the list (operator decision 2026-05-22, ROADMAP SC1 — supersedes the original oldest-first/compose-below wording)."
    - Rewrite CMNT-08 (line 28) to: "A new comment appears optimistically at the **top** of the list (pending state) and reconciles on success / rolls back on failure (newest-first, ROADMAP SC4 — supersedes the original bottom wording)."
  </action>
  <verify>
    <automated>npm run test -- tests/data/getCommentsForTarget.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "desc(comments.createdAt)" src/data/comments.ts` returns 2 matches; `grep -n "asc(comments.createdAt)" src/data/comments.ts` returns 0.
    - No remaining "oldest first" substring in src/data/comments.ts (`grep -in "oldest first" src/data/comments.ts` returns 0).
    - REQUIREMENTS.md CMNT-03 contains "newest-first" and "above"; CMNT-08 contains "top". (`grep -n "newest-first" .planning/REQUIREMENTS.md` matches the CMNT-03 line.)
    - The Wave 0 CMNT-03 desc-order test PASSES.
  </acceptance_criteria>
  <done>getCommentsForTarget orders DESC; stale DAL comment + REQUIREMENTS CMNT-03/08 reconciled to newest-first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActivityType widen + CommentedMetadata + logActivity overload + getFeedForUser D-12 gate + ActivityRow verb</name>
  <files>src/data/activities.ts, src/lib/feedTypes.ts, src/components/home/ActivityRow.tsx</files>
  <read_first>
    - src/data/activities.ts (full — types lines 21-48, logActivity lines 53-83, getFeedForUser lines 107-195, normalizeMetadata 197-211)
    - src/lib/feedTypes.ts (full — ActivityType line 12, RawFeedRow lines 37-65)
    - src/lib/feedAggregate.ts (full — confirm the aggregatable allowlist lines 21-23 is positive, NOT exhaustive; this is why D-14 is free)
    - src/components/home/ActivityRow.tsx (full — VERBS lines 11-15, link branch lines 49-58)
    - tests/data/getFeedForUser.test.ts (Wave 0 FEED-07 cases this task turns GREEN)
    - src/data/follows.ts lines 77-95 (isMutualFollow self-join — the SQL shape the EXISTS subquery mirrors)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md (activities.ts + feedTypes.ts + ActivityRow.tsx modification sections, lines 561-763)
  </read_first>
  <behavior>
    - getFeedForUser does NOT return a 'commented' row where metadata.targetType='watch' AND metadata.watchStatus='wishlist' AND the viewer is NOT mutual-follow with metadata.targetOwnerId.
    - getFeedForUser DOES return a 'commented' row where metadata.watchStatus != 'wishlist' (non-wishlist watch comment surfaces normally, subject to the existing outer follow gate).
    - getFeedForUser DOES return a 'commented' row where metadata.targetType='wear' (wear comments always surface — no wishlist concept).
    - normalizeMetadata passes through metadata.wearEventId when present.
    - feedAggregate.ts is UNCHANGED: a run of ≥3 'commented' rows in 1hr is NOT collapsed (D-14 — comment rows stay one-per-comment, mirroring watch_worn).
  </behavior>
  <action>
    In `src/data/activities.ts`:
    - Widen `ActivityType` (line 21) to add `| 'commented'`.
    - Add `export type CommentedMetadata = { brand: string; model: string; imageUrl: string | null; targetType: 'watch' | 'wear'; targetOwnerId: string; watchStatus?: string; wearEventId?: string }`. Note: targetType uses `'wear'` NOT `'wear_event'`.
    - Add `CommentedMetadata` to the `ActivityMetadata` union (line 48).
    - Add a `logActivity` overload for `type: 'commented'` taking `(userId, 'commented', watchId: string | null, metadata: CommentedMetadata)` mirroring the 3 existing overloads (lines 53-70). The impl body (lines 71-83) is unchanged.
    - In `getFeedForUser`, extend the inner WHERE `sql\`(...)\`` (lines 161-165) by appending the D-12 `commented` branch. The branch (use the `${activities.metadata}->>'...'` JSON-path style already in the file, and `${viewerId}` for the bind):
      OR ( type = 'commented' AND (
        metadata->>'targetType' = 'wear'
        OR (metadata->>'targetType' = 'watch' AND metadata->>'watchStatus' != 'wishlist')
        OR (metadata->>'targetType' = 'watch' AND metadata->>'watchStatus' = 'wishlist'
            AND EXISTS ( SELECT 1 FROM follows f1 JOIN follows f2
                         ON f2.follower_id = f1.following_id AND f2.following_id = f1.follower_id
                         WHERE f1.follower_id = ${viewerId}
                           AND f1.following_id = (metadata->>'targetOwnerId')::uuid ))
      ))
      CRITICAL: the EXISTS self-join keys off `targetOwnerId` (the WATCH OWNER), NOT activities.userId (the commenter). The existing outer innerJoin on profileSettings/activities.userId stays as-is — it gates by the followed ACTOR; this new inner branch adds the target-owner visibility layer. Do NOT route the commented branch through the actor's profileSettings columns.
    - In `normalizeMetadata` (lines 197-211), add a pass-through: `...(typeof m.wearEventId === 'string' ? { wearEventId: m.wearEventId } : {})` to the returned object.
    - DO NOT touch `src/lib/feedAggregate.ts`. D-14 (comment rows exempt from collapse) is satisfied for free: the aggregatable check at feedAggregate.ts:21-23 is a positive allowlist (`watch_added || wishlist_added`) that already omits `watch_worn` and will omit `'commented'`. Adding `'commented'` to the ActivityType union does NOT produce a type error there (no exhaustive switch).

    In `src/lib/feedTypes.ts`:
    - Widen `ActivityType` (line 12) to add `| 'commented'` (KEEP IN SYNC with activities.ts — both files define it).
    - Add `wearEventId?: string` to `RawFeedRow.metadata` (lines 47-52).
    - (Optional dedup per RESEARCH Open Question 2 (RESOLVED): default is keep both definitions in sync within THIS task. Optionally have feedTypes import ActivityType from activities.ts ONLY if it introduces no import cycle; if a cycle exists, keep both definitions in sync instead. Document the choice in SUMMARY.)

    In `src/components/home/ActivityRow.tsx`:
    - Add `commented: 'commented on'` to the VERBS Record (lines 11-15). This is required: the Record is exhaustive over ActivityType, so widening the union without updating VERBS is a TypeScript error.
    - Extend the watch-name link (lines 49-58): keep `row.watchId → /watch/${row.watchId}`; add an else-if `row.metadata.wearEventId → /wear/${row.metadata.wearEventId}` (same Link className/aria as the watch branch); final fallback `<span>`. Use `/wear/` NOT `/wear_event/`.
  </action>
  <verify>
    <automated>npm run test -- tests/data/getFeedForUser.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The Wave 0 FEED-07 leak case PASSES (gated wishlist comment excluded for non-mutual viewer); the non-wishlist and wear cases PASS (surface normally).
    - The feed `commented` WHERE branch references `metadata->>'targetOwnerId'` and a `follows` self-join, and does NOT gate the commented branch on `profileSettings.wishlistPublic`/`collectionPublic` (`grep` confirms targetOwnerId appears in activities.ts).
    - `grep -c "'commented'" src/data/activities.ts` ≥ 1 and `grep -c "'commented'" src/lib/feedTypes.ts` ≥ 1 (both unions widened).
    - D-14 EXEMPTION VERIFIED (feedAggregate untouched): `grep -c "'commented'" src/lib/feedAggregate.ts` returns 0 (the comment type was NOT added to the aggregatable allowlist), AND `npx tsc --noEmit` confirms feedAggregate.ts still type-checks against the widened ActivityType union (proving the positive allowlist absorbs the new member without an exhaustive-match error). Together these prove widening ActivityType to 'commented' did NOT reach the aggregator.
    - ActivityRow VERBS contains `commented: 'commented on'` and a `/wear/${` link branch exists; no `'wear_event'` string appears in any edited file (`grep -rn "wear_event" src/data/activities.ts src/components/home/ActivityRow.tsx src/lib/feedTypes.ts` returns 0).
    - `npx tsc --noEmit` reports no new type errors (the exhaustive VERBS Record forced the verb addition).
  </acceptance_criteria>
  <done>ActivityType widened in both files; logActivity 'commented' overload added; getFeedForUser gates commented rows by target owner (FEED-07); ActivityRow renders the verb + wear link; feedAggregate.ts left untouched and confirmed exempt (D-14).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: addCommentAction += logActivity (FEED-06) + deleteCommentAction revalidate fix</name>
  <files>src/app/actions/comments.ts</files>
  <read_first>
    - src/app/actions/comments.ts (full — addCommentAction 51-184, watch SELECT ~73-83, notification guard 143-177, revalidateTag line 133, editCommentAction owner-lookup 208-234, deleteCommentAction 243-269)
    - src/data/activities.ts (the new logActivity 'commented' overload + CommentedMetadata from Task 2)
    - tests/actions/comments.test.ts (Wave 0 FEED-06 spy cases + CMNT-07 delete-revalidate case this task turns GREEN)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md (actions/comments.ts modifications, lines 478-557)
  </read_first>
  <behavior>
    - addCommentAction on a non-self watch comment calls logActivity(actorId, 'commented', watchId, metadata) once with metadata.targetType='watch', targetOwnerId=ownerId, watchStatus=watch.status, imageUrl set.
    - addCommentAction on a non-self WEAR comment calls logActivity(actorId, 'commented', null, metadata) with metadata.targetType='wear', wearEventId=target.id.
    - addCommentAction on a SELF comment (ownerId === user.id) does NOT call logActivity (mirrors NOTIF-12).
    - deleteCommentAction resolves the comment's owner (read-then-delete) and calls revalidateTag(`profile:${ownerUsername}`, 'max').
  </behavior>
  <action>
    In `src/app/actions/comments.ts` — addCommentAction:
    - Extend the watch SELECT (~lines 73-83) to also fetch `imageUrl: watches.imageUrl` and `status: watches.status` (keep userId, brand, model). This SELECT extension is the resolution of RESEARCH Open Question 3 (RESOLVED) — no new query, just extra columns.
    - Import `logActivity` from `@/data/activities`.
    - After `createComment` succeeds and after the existing notification/revalidate logic, add a logActivity call guarded by the SAME `ownerId !== user.id` condition used for notifications:
      logActivity(user.id, 'commented',
        target.type === 'watch' ? target.id : null,   // watchId null for wear (no wearEventId column)
        { brand: watchBrand, model: watchModel, imageUrl: imageUrl ?? null,
          targetType: target.type,                      // 'watch' | 'wear' — NOT 'wear_event'
          targetOwnerId: ownerId,
          watchStatus: target.type === 'watch' ? watch.status : undefined,
          wearEventId: target.type === 'wear' ? target.id : undefined })
    - Use whatever names the action already binds for brand/model/ownerId/watch.status (do not invent new SELECT — reuse the resolved row). For a WEAR target, resolve the wear's underlying watch brand/model/imageUrl from the wear→watch lookup the action already performs for notifications (mirror the existing notification metadata resolution; if the wear path resolves brand/model differently, reuse exactly that path).
    - Place the logActivity AFTER any `await`ed notification call but it may be awaited (mirror the NOTIF awaited pattern). Do NOT log on the gate-rejection path (the CommentGateError catch returns before this point).

    In `src/app/actions/comments.ts` — deleteCommentAction (lines 243-269):
    - Convert to read-then-delete, mirroring editCommentAction's owner-lookup (lines 208-234):
      1. SELECT the comment row (id, watchId, wearEventId) for `(commentId, user.id)` — reuse the DAL/query editCommentAction uses to load the comment, or read it before calling deleteComment.
      2. Call deleteComment(user.id, commentId) as today.
      3. Resolve owner: if comment.watchId → SELECT watches.userId → getProfileById → revalidateTag(`profile:${ownerProfile.username}`, 'max'); else if comment.wearEventId → mirror the wearEventId owner-resolution editCommentAction uses.
      4. Return `{ success: true, data: { id: commentId } }` unchanged.
    - Do NOT call logNotification or logActivity in delete (NOTIF-12 / D-13 INSERT-only — both stay out of edit/delete).
  </action>
  <verify>
    <automated>npm run test -- tests/actions/comments.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The Wave 0 FEED-06 logActivity-spy cases PASS: non-self watch comment → logActivity called with targetType='watch'; self comment → not called; wear comment → called with watchId=null + metadata.wearEventId + targetType='wear'.
    - The Wave 0 CMNT-07 delete-revalidate case PASSES: deleteCommentAction calls revalidateTag(`profile:${ownerUsername}`, 'max').
    - All pre-existing comments.test.ts cases (NOTIF-12, SEC-03, D-09 gate) still PASS — logActivity is guarded by ownerId !== user.id and never fires on the gate-rejection path.
    - `grep -c "logActivity" src/app/actions/comments.ts` ≥ 1 and it appears only inside addCommentAction (not in edit/delete).
    - `grep -c "revalidateTag" src/app/actions/comments.ts` ≥ 3 (add, edit, AND now delete).
    - No `'wear_event'` string in the edited action (`grep -n "wear_event" src/app/actions/comments.ts` returns 0).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>addCommentAction records a 'commented' activity on create (FEED-06, INSERT-only, gated to non-self); deleteCommentAction now revalidates the owner's profile tag (Pitfall-6 gap closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feed viewer → getFeedForUser | A 'commented' activity's actor (commenter) ≠ target owner; the existing actor-keyed gate does NOT protect the target owner's wishlist privacy |
| action caller → addCommentAction | Server Action re-verifies auth (Phase 55); this plan only adds activity logging, no new IDOR surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | getFeedForUser 'commented' row gate (FEED-07) | mitigate | Per-row EXISTS self-join on `follows` keyed off `metadata->>'targetOwnerId'` (the watch owner), NOT the actor; wishlist comments surface only to viewers mutual-follow with the target owner. Verified by the Wave 0 FEED-07 leak test. |
| T-57-04 | Information Disclosure | logActivity metadata over-capture | mitigate | Metadata carries only brand/model/imageUrl/targetType/targetOwnerId/watchStatus/wearEventId — no comment body text (D-11 verb-only), so the activity row cannot leak comment content even if the gate were bypassed |
| T-57-05 | Spoofing | commented activity actor | accept | actor is server-derived (`user.id` from getCurrentUser, Phase 55), not client-supplied; no new surface |
| T-57-06 | Tampering | activities.type plain text (no enum) | accept | type string is server-set in logActivity overload; not user-influenced; no SQL migration needed (verified schema.ts:284) |
</threat_model>

<verification>
- `npm run test` full suite green (Wave 0 action + feed + comments-order cases now PASS; no regressions).
- `npx tsc --noEmit` clean (ActivityType widened in both files; VERBS Record exhaustive).
- `grep -rn "wear_event" src/data/activities.ts src/app/actions/comments.ts src/lib/feedTypes.ts src/components/home/ActivityRow.tsx` returns 0 (landmine guard).
- `grep -in "oldest first" src/data/comments.ts` returns 0.
- `grep -c "'commented'" src/lib/feedAggregate.ts` returns 0 (D-14: comment type NOT added to the aggregatable allowlist — exemption preserved).
</verification>

<success_criteria>
- CMNT-03 newest-first reconciled in DAL + REQUIREMENTS; CMNT-08 text reconciled to optimistic-at-top.
- FEED-06: addCommentAction logs a 'commented' activity on create, gated to non-self, never on edit/delete.
- FEED-07: getFeedForUser gates 'commented' wishlist rows by the target owner (leak test GREEN).
- D-14: comment activities exempt from feedAggregate collapse (feedAggregate.ts untouched, exemption verified).
- deleteCommentAction revalidates the owner profile tag.
- ActivityRow renders the 'commented on' verb + wear-link branch.
</success_criteria>

<output>
After completion, create `.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-02-SUMMARY.md`. Record: (1) whether ActivityType was deduplicated or kept-in-sync across the two files and why; (2) the exact wear→watch metadata resolution path reused for wear-comment logActivity.
</output>
