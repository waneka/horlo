---
phase: 57-comment-thread-ui-feed-extension-grid-counts
plan: 05
type: execute
wave: 3
depends_on: [02, 04]
files_modified:
  - src/components/wear/WearCommentHost.tsx
  - src/components/wear/WearCard.tsx
  - src/app/wear/[wearEventId]/page.tsx
  - src/app/wears/[username]/page.tsx
  - src/components/wears/WearsLane.tsx
  - src/app/watch/[id]/page.tsx
  - src/components/watch/WatchDetail.tsx
autonomous: true
requirements: [CMNT-01, CMNT-02, CMNT-09, GATE-03]

must_haves:
  truths:
    - "Per CMNT-01: /watch/[id] renders a comment thread (compose + list) below WatchDetail as an uncached RSC sibling in Suspense; an authorized viewer can post/edit/delete there"
    - "Per CMNT-02: both wear hosts (/wear/[id] inline + /wears/[username] bottom-sheet) render the SAME shared comment thread via the filled WearCommentHost seam — single component, two container chromes"
    - "Per CMNT-09 / D-08: a MessageCircle + bare count appears next to the LikeButton in the watch-detail footer and the wear-detail footer, hidden at zero; the wears-lane comment trigger gains a count badge, hidden at zero"
    - "Per GATE-03 / D-03: each host resolves canComment, ownerFollowsViewer (isFollowing(ownerId, viewerId) direction), AND viewerIsFollowing server-side and passes them so CommentGateLocked can pick the correct state"
  artifacts:
    - path: "src/components/wear/WearCommentHost.tsx"
      provides: "both seams filled with <CommentList>; props extended with comment-thread + gate signals"
      contains: "CommentList"
    - path: "src/app/watch/[id]/page.tsx"
      provides: "CommentThread RSC sibling in Suspense + comment-count prop to WatchDetail"
      contains: "CommentThread"
    - path: "src/components/watch/WatchDetail.tsx"
      provides: "MessageCircle count in the footer action row (hidden at zero)"
      contains: "MessageCircle"
    - path: "src/app/wear/[wearEventId]/page.tsx"
      provides: "per-wear comment reads + gate signals passed to WearCard"
      contains: "getCommentsForTarget"
    - path: "src/app/wears/[username]/page.tsx"
      provides: "per-wear comment reads (Promise.all, not N+1 loop) added to WearSlide"
      contains: "getCommentsForTarget"
  key_links:
    - from: "src/app/watch/[id]/page.tsx"
      to: "src/components/comment/CommentThread.tsx"
      via: "RSC sibling below WatchDetail wrapped in Suspense(CommentThreadSkeleton)"
      pattern: "CommentThread"
    - from: "src/components/wear/WearCommentHost.tsx"
      to: "src/components/comment/CommentList.tsx"
      via: "seam fill in BOTH bottom-sheet and inline variants"
      pattern: "CommentList"
    - from: "src/app/wear/[wearEventId]/page.tsx"
      to: "isFollowing(ownerId, viewerId)"
      via: "ownerFollowsViewer direction owner→viewer (wear: always false; watch: only when gated)"
      pattern: "isFollowing"
---

<objective>
Wire the shared comment thread (Plan 04) into all three hosts: the watch-detail page (new RSC-sibling host), the wear-detail inline host, and the wears-lane bottom-sheet host (the latter two via filling the pre-marked WearCommentHost seams). Add the MessageCircle comment count to the watch + wear footer action rows and the wears-lane comment trigger (CMNT-09 / D-08), hidden at zero. Resolve the GATE-03 signals (canComment, ownerFollowsViewer, viewerIsFollowing) server-side per host.

Purpose: Make comments reachable and postable everywhere comments live, with one shared component and three container chromes — and the count visible per D-08.
Output: 7 files wired; comments live on /watch/[id], /wear/[id], /wears/[username].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-UI-SPEC.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-CONTEXT.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-04-SUMMARY.md

<interfaces>
<!-- Verified live source. Use these directly. -->

src/components/comment/ (from Plan 04 — read 57-04-SUMMARY for the final type homes):
- CommentThread (Server, uncached): props { viewerId, target, canComment, ownerFollowsViewer, ownerUserId, ownerUsername } + (verify in SUMMARY) viewerIsFollowing if it is a CommentThread prop rather than resolved internally.
- CommentList (client): props include initialComments (CommentWithAuthor[]), target, canComment, ownerFollowsViewer, ownerUserId, ownerUsername, viewerId, viewerAuthor, viewerIsFollowing.
- CommentThreadSkeleton.

src/components/wear/WearCommentHost.tsx (verified):
- Discriminated union props (lines 15-17): { variant:'bottom-sheet', wearEventId, open, onOpenChange } | { variant:'inline', wearEventId }.
- Seam markers: line 44 (bottom-sheet) and lines 60-61 (inline) — `{/* Phase 57: shared comment component renders here */}` + a "No comments yet." placeholder.
- It is a CLIENT component (cannot await) — server parent resolves comment data and passes via props.

src/components/wear/WearCard.tsx (verified):
- props (lines 16-33): includes viewerId, wearEventId, commentHostVariant:'bottom-sheet'|'inline', initialLiked, initialCount, permalinkUrl, showAddToWishlist, onCommentOpenChange.
- Engagement rows (lines 143-186): bottom-sheet row (lines 144-163) + inline row (lines 165-186) each have a `<MessageCircle className="size-5" ...>` comment-trigger button (left) and LikeButton (right). The inline trigger calls handleScrollToComments → `#wear-comments`.
- Comment host render (lines 188-198): passes only { variant, wearEventId, open, onOpenChange } today.

src/app/wear/[wearEventId]/page.tsx (verified):
- WearPhotoStreamed async server child resolves signedUrl + passes to WearCard (commentHostVariant="inline"). viewerId = user.id (auth-only). wear has userId + username.

src/app/wears/[username]/page.tsx (verified):
- Builds slides: WearSlide[] = wears.map(...) with per-slide initialLiked/initialCount from a `likeStates = Promise.all(wears.map(getLikesForTargetCached...))`. Passes slides + viewerId to <WearsLane> (commentHostVariant bottom-sheet). wears[i] has id, userId, username, brand, model.

src/components/wears/WearsLane.tsx (verified path): renders WearCard per slide (commentHostVariant="bottom-sheet"). WearSlide type defined near the page/lane.

src/components/watch/WatchDetail.tsx (verified):
- props (lines 33-54): watch, collection, preferences, lastWornDate, viewerCanEdit, verdict, viewerId?, initialLikeState?.
- footer action row (lines 153-163): `<div className="flex items-center gap-2 mt-3">` with LikeButton, gated on `viewerId !== undefined && initialLikeState !== undefined`.

src/app/watch/[id]/page.tsx (verified):
- line 40: `const likeState = await getLikesForTargetCached(user.id, { type:'watch', id })`.
- lines 110-118: RSC sibling pattern — <SameFamilyRail rows={...} /> <LineageRail rows={...} />. ownerProfile.username available (resolve alongside other data).

src/data/comments.ts: getCommentsForTarget, canViewerCommentOnTarget, CommentTarget.
src/data/follows.ts: isFollowing(followerId, followingId) — DIRECTION: ownerFollowsViewer = isFollowing(ownerId, viewerId); viewerIsFollowing = isFollowing(viewerId, ownerId).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill WearCommentHost seams + extend WearCard to thread comment props</name>
  <files>src/components/wear/WearCommentHost.tsx, src/components/wear/WearCard.tsx</files>
  <read_first>
    - src/components/wear/WearCommentHost.tsx (full — discriminated union props, both seam markers)
    - src/components/wear/WearCard.tsx (full — props 16-33, engagement rows 143-186, host render 188-198)
    - src/components/comment/CommentList.tsx + 57-04-SUMMARY.md (the exact CommentList prop names + CommentWithAuthor home)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md (WearCommentHost seam-fill section, lines 870-931)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-UI-SPEC.md (bottom-sheet over-photo CSS-chain assertions, lines 320-341, 422-433)
  </read_first>
  <action>
    In `src/components/wear/WearCommentHost.tsx`:
    - Extend BOTH variants of the discriminated union with the comment-thread + gate props: `initialComments: CommentWithAuthor[]; canComment: boolean; ownerFollowsViewer: boolean; viewerIsFollowing: boolean; ownerUserId: string; ownerUsername: string; viewerId: string | null`. Import CommentWithAuthor from the home Plan 04 chose (see 57-04-SUMMARY).
    - Replace the bottom-sheet seam (line 44 placeholder "No comments yet.") with `<CommentList initialComments={initialComments} target={{type:'wear', id:wearEventId}} canComment={canComment} ownerFollowsViewer={ownerFollowsViewer} viewerIsFollowing={viewerIsFollowing} ownerUserId={ownerUserId} ownerUsername={ownerUsername} viewerId={viewerId} viewerAuthor={viewerAuthor} />`. (If viewerAuthor must be threaded too, add it to the union — see 57-04-SUMMARY for whether CommentList requires viewerAuthor as a prop or CommentThread resolves it; for wear hosts there is no CommentThread server wrapper, so the SERVER PARENT (WearPhotoStreamed / wears page) must resolve initialComments AND viewerAuthor and pass both. Add viewerAuthor to the union.)
    - Replace the inline seam (lines 60-61) with the same `<CommentList ... />` (the `<section id="wear-comments">` wrapper stays so handleScrollToComments still works).
    - The `_wearEventId` eslint-disabled unused var is now used — remove the disable comment.
    - Bottom-sheet CSS chain (UI-SPEC assertions): the Sheet content must keep `bg-background` SOLID (NOT /80 opacity), `z-50`, `max-h-[60vh] overflow-y-auto`. Do NOT introduce opacity on the sheet background. The CommentList + CommentGateLocked are in-flow (no absolute/fixed) so they scroll within the sheet.

    In `src/components/wear/WearCard.tsx`:
    - Extend WearCardProps to add: `initialComments: CommentWithAuthor[]; canComment: boolean; ownerFollowsViewer: boolean; viewerIsFollowing: boolean; ownerUserId: string; ownerUsername: string; viewerAuthor: CommentAuthor | null; commentCount: number`.
    - Thread the comment-thread props into BOTH `<WearCommentHost ... />` renders (lines 188-198): add initialComments/canComment/ownerFollowsViewer/viewerIsFollowing/ownerUserId/ownerUsername/viewerId/viewerAuthor.
    - CMNT-09 count badge: in BOTH engagement rows, add a count next to the comment-trigger MessageCircle, hidden at zero. Pattern (mirror LikeButton's hidden-at-zero `♥ N`): wrap the trigger so it shows `<MessageCircle .../> {commentCount > 0 && <span className="text-sm tabular-nums text-muted-foreground">{commentCount}</span>}` with `gap-1`. Keep the existing aria-labels and the 44px touch target.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "CommentList" src/components/wear/WearCommentHost.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both WearCommentHost seams render `<CommentList ...>`; the "No comments yet." placeholder strings are gone (`grep -c "No comments yet" src/components/wear/WearCommentHost.tsx` returns 0).
    - `grep -c "use cache" src/components/wear/WearCommentHost.tsx` returns 0 (no caching introduced on the host).
    - WearCommentHost Sheet content keeps `bg-background` (solid) and `z-50` and `max-h-[60vh] overflow-y-auto`; no `bg-background/` opacity variant added (`grep -c "bg-background/" src/components/wear/WearCommentHost.tsx` returns 0).
    - Both WearCard engagement rows show `commentCount` next to the trigger only when `> 0` (`grep -c "commentCount > 0" src/components/wear/WearCard.tsx` ≥ 1).
    - No `'wear_event'` string introduced (`grep -c "wear_event" src/components/wear/WearCommentHost.tsx src/components/wear/WearCard.tsx` returns 0 — note 'wear_event_id' DB column does not appear in these UI files).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Both WearCommentHost seams filled with the shared CommentList; WearCard threads comment props + shows the CMNT-09 count badge.</done>
</task>

<task type="auto">
  <name>Task 2: Wear-detail + wears-lane page wiring (comment reads + gate signals + counts)</name>
  <files>src/app/wear/[wearEventId]/page.tsx, src/app/wears/[username]/page.tsx, src/components/wears/WearsLane.tsx</files>
  <read_first>
    - src/app/wear/[wearEventId]/page.tsx (full — WearPhotoStreamed resolution + WearCard usage)
    - src/app/wears/[username]/page.tsx (full — slides build, likeStates Promise.all, WearsLane usage)
    - src/components/wears/WearsLane.tsx (full — WearSlide type + WearCard render per slide)
    - src/data/comments.ts (getCommentsForTarget, canViewerCommentOnTarget) + src/data/profiles.ts getProfilesByIds (Plan 04)
    - 57-04-SUMMARY.md (CommentWithAuthor home + viewerAuthor sourcing)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md (wear/[wearEventId]/page.tsx section, lines 997-1031)
  </read_first>
  <action>
    Wear targets are NEVER wishlist-gated (GATE-01): `canComment` is always true and `ownerFollowsViewer`/`viewerIsFollowing` are both false. So the gate plumbing is trivial for wears but the props must still be passed.

    In `src/app/wear/[wearEventId]/page.tsx` (WearPhotoStreamed):
    - Add (uncached, alongside the existing signedUrl/likeState resolution): `const target = { type:'wear' as const, id: wearEventId }`; `const initialComments = await getCommentsForTarget(viewerId, target)` (returns the thread, newest-first per Plan 02); enrich authors via the same getProfilesByIds approach CommentThread uses — OR, since the wear host bypasses CommentThread, resolve authors HERE (batch getProfilesByIds over comment authorIds + viewerId) and build the CommentWithAuthor[] + viewerAuthor. `const canComment = true; const ownerFollowsViewer = false; const viewerIsFollowing = false`. `const commentCount = initialComments.length`.
    - Pass these to <WearCard ...>: initialComments, canComment, ownerFollowsViewer, viewerIsFollowing, ownerUserId={wear.userId}, ownerUsername={wear.username ?? ''}, viewerAuthor, commentCount.

    In `src/app/wears/[username]/page.tsx`:
    - Mirror the existing `likeStates = Promise.all(wears.map(getLikesForTargetCached...))` pattern for comments: `const commentLists = await Promise.all(wears.map((w) => getCommentsForTarget(viewerId, { type:'wear', id: w.id })))`. This is Promise.all over the (already-bounded ~active-wears) list — NOT an N+1 loop in render; it mirrors the accepted likes pattern. Batch-resolve author profiles for all comment authors across all slides in ONE getProfilesByIds call (collect all authorIds + viewerId), then build CommentWithAuthor[] per slide.
    - Extend each `WearSlide` (in the slides map) with: initialComments (enriched), canComment:true, ownerFollowsViewer:false, viewerIsFollowing:false, ownerUserId: w.userId, ownerUsername: w.username ?? '', viewerAuthor, commentCount: commentLists[i].length.

    In `src/components/wears/WearsLane.tsx`:
    - Extend the WearSlide type to carry the new fields; pass them through to each `<WearCard ...>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Wear-detail page resolves initialComments via getCommentsForTarget and passes commentCount + the gate props to WearCard.
    - Wears-lane page resolves comments via a single `Promise.all(wears.map(getCommentsForTarget...))` (NOT a sequential loop) and a single getProfilesByIds batch for authors (`grep -c "getCommentsForTarget" src/app/wears/[username]/page.tsx` ≥ 1; `grep -c "Promise.all" src/app/wears/[username]/page.tsx` ≥ 2 — likes + comments).
    - WearSlide type carries the comment-thread + count fields; WearsLane passes them to WearCard.
    - canComment/ownerFollowsViewer/viewerIsFollowing for wear targets are literally true/false/false (wears are ungated, GATE-01).
    - `npx tsc --noEmit` clean; `npm run test` green (no regressions).
  </acceptance_criteria>
  <done>Both wear hosts resolve comments + author enrichment + count server-side and pass them through WearCard.</done>
</task>

<task type="auto">
  <name>Task 3: Watch-detail host — CommentThread RSC sibling + footer comment count</name>
  <files>src/app/watch/[id]/page.tsx, src/components/watch/WatchDetail.tsx</files>
  <read_first>
    - src/app/watch/[id]/page.tsx (full — likeState resolution line 40, RSC sibling pattern lines 110-118, ownerProfile resolution)
    - src/components/watch/WatchDetail.tsx (full — props 33-54, footer action row 153-163)
    - src/components/comment/CommentThread.tsx + 57-04-SUMMARY.md (CommentThread prop list incl. whether viewerIsFollowing is a CommentThread prop)
    - src/data/comments.ts (canViewerCommentOnTarget) + src/data/follows.ts (isFollowing direction)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md (watch/[id]/page.tsx + WatchDetail sections, lines 935-993)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-UI-SPEC.md (footer count mirror, lines 232-250)
  </read_first>
  <action>
    In `src/app/watch/[id]/page.tsx`:
    - The watch page is auth-only (user = getCurrentUser()). Resolve the gate signals (uncached — alongside getLikesForTargetCached at line 40):
      `const target = { type:'watch' as const, id }`
      `const canComment = await canViewerCommentOnTarget(user.id, target)`
      `const ownerFollowsViewer = !canComment && watch.status === 'wishlist' ? await isFollowing(watch.userId, user.id) : false`  // owner→viewer direction
      `const viewerIsFollowing = !canComment && watch.status === 'wishlist' ? await isFollowing(user.id, watch.userId) : false`  // viewer→owner direction (CommentGateLocked State 1 vs 2)
      `const commentCount` — derive from the comment count. Prefer reusing the thread the CommentThread will load; since CommentThread loads its own list, resolve a lightweight count here for the footer: call getCommentsForTarget(user.id, target) once and use `.length`, then ALSO pass that list... OR (cleaner) let CommentThread own the list and resolve `commentCount` via the count branch of getCommentsForTarget here. Decision: call `getCommentsForTarget(user.id, target)` ONCE on the page, pass `.length` to WatchDetail as the footer count, and pass the SAME resolved list into CommentThread as an `initialComments` prop to avoid a double read. If CommentThread is built to fetch internally (Plan 04), instead pass `commentCount` only and let CommentThread fetch its own list (one extra uncached read is acceptable). Pick the single-read path if CommentThread accepts initialComments; document in SUMMARY.
    - Add `<CommentThread ... />` as an RSC sibling BELOW WatchDetail (mirror SameFamilyRail/LineageRail at lines 110-118), wrapped in `<Suspense fallback={<CommentThreadSkeleton />}>`: pass viewerId={user.id}, target, canComment, ownerFollowsViewer, viewerIsFollowing (if a prop), ownerUserId={watch.userId}, ownerUsername={ownerProfile.username}. Resolve ownerProfile.username alongside the other page data if not already available.
    - Pass `commentCount` (number) to <WatchDetail ... commentCount={...} />.

    In `src/components/watch/WatchDetail.tsx`:
    - Add `commentCount?: number` to WatchDetailProps.
    - In the footer action row (lines 153-163), add the comment count next to LikeButton, hidden at zero, mirroring D-08 / UI-SPEC line 237-243:
      `{(commentCount ?? 0) > 0 && (<span className="inline-flex items-center gap-1 text-sm tabular-nums text-muted-foreground px-2 min-h-[44px]"><MessageCircle className="size-5 text-muted-foreground" aria-hidden />{commentCount}</span>)}`
    - Import MessageCircle from lucide-react. No tap behavior on the watch detail count (the thread is already visible below — UI-SPEC line 246).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CommentThread" src/app/watch/[id]/page.tsx` ≥ 1 and it is wrapped in `<Suspense` with a CommentThreadSkeleton fallback.
    - ownerFollowsViewer uses `isFollowing(watch.userId, user.id)` (owner→viewer) and viewerIsFollowing uses `isFollowing(user.id, watch.userId)` (viewer→owner) — direction correct (`grep -n "isFollowing" src/app/watch/[id]/page.tsx` shows both directions).
    - WatchDetail footer shows MessageCircle + count only when `> 0` (`grep -c "commentCount" src/components/watch/WatchDetail.tsx` ≥ 1; `grep -c "MessageCircle" src/components/watch/WatchDetail.tsx` ≥ 1).
    - The CommentThread sibling is at the SERVER tree level (not imported inside the WatchDetail client island).
    - `npx tsc --noEmit` clean; `npm run test` green.
  </acceptance_criteria>
  <done>/watch/[id] renders the comment thread as an uncached RSC sibling in Suspense with correct two-direction gate signals; the footer shows the CMNT-09 count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer → /watch/[id] CommentThread | Gated wishlist watch must show CommentGateLocked, not the thread; canComment resolved server-side |
| anon viewer → /wear/[id] | Wear page is auth-only post-56A EN-6; getCurrentUser throws for anon (no anon thread leak) |
| viewer → wears-lane bottom sheet | Comment content renders over the photo; the solid sheet background + z-50 must not leak the photo through the text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-10 | Information Disclosure | gated thread on /watch/[id] | mitigate | canComment resolved server-side via canViewerCommentOnTarget; CommentThread renders [] + CommentGateLocked for gated viewers (Plan 04); no thread/count leaked |
| T-57-14 | Information Disclosure | over-photo bottom-sheet readability/leak | mitigate | Sheet content keeps `bg-background` SOLID + `z-50`; CSS-chain assertions in UI-SPEC enforced; `rm -rf .next` before judging any CSS fix (project_turbopack_next_cache_stale_css) |
| T-57-15 | Information Disclosure | wear thread to anon | mitigate | Wear pages are auth-only (56A EN-6); getCurrentUser throws for anon — no anon-reachable wear thread |
| T-57-13 | Tampering | stale gate between render and submit | accept | The action `code:'gate'` backstop (Plan 04 CommentList) handles a status flip; the page-level canComment is the first gate |
</threat_model>

<verification>
- `npm run test` full suite green; `npx tsc --noEmit` clean across all 7 files.
- `grep -rn "wear_event'" src/components/wear/ src/app/wear src/app/wears` returns 0 ('wear' discriminator landmine — note DB column 'wear_event_id' is allowed where it is a real column name).
- `rm -rf .next && npm run build` succeeds (CSS-chain + Cache Components build gate).
- Manual (prod, per VALIDATION + MEMORY feedback_mobile_ui_verify_on_prod): comments render/post/edit/delete in the wears-lane bottom sheet over the photo; count badges hidden at zero; GATE-03 two-state copy on a gated wishlist watch.
</verification>

<success_criteria>
- Comments are reachable and postable on /watch/[id], /wear/[id], and /wears/[username] via ONE shared component.
- CMNT-09 count appears next to LikeButton on watch + wear detail and as a wears-lane trigger badge, hidden at zero.
- GATE-03 signals (canComment, ownerFollowsViewer, viewerIsFollowing) resolved server-side with correct isFollowing directions.
- No 'use cache' on any comment host; over-photo sheet background solid.
</success_criteria>

<output>
After completion, create `.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-05-SUMMARY.md`. Record: (1) single-read vs double-read choice for the watch-detail footer count + CommentThread list; (2) where viewerAuthor is resolved for each wear host; (3) the verified bottom-sheet z-stack/background result after `rm -rf .next && npm run build`.
</output>
