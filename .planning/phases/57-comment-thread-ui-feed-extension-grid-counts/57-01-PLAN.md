---
phase: 57-comment-thread-ui-feed-extension-grid-counts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/actions/addCommentAction.test.ts
  - tests/actions/editCommentAction.test.ts
  - tests/actions/deleteCommentAction.test.ts
  - tests/data/getCommentsForTarget.test.ts
  - tests/data/getBatchedWatchCounts.test.ts
  - tests/data/getFeedForUser.test.ts
  - tests/data/comments.test.ts
autonomous: true
requirements: [CMNT-01, CMNT-02, CMNT-03, CMNT-04, CMNT-06, CMNT-07, CMNT-09, GATE-03, FEED-06, FEED-07, DISP-01]

must_haves:
  truths:
    - "Wave 0 test scaffolds exist and FAIL (RED) because the implementations they assert do not yet exist — per the Nyquist rule, every downstream behavior has an automated test waiting for it"
    - "FEED-07 leak guard (D-12) has a dedicated failing test: a commented-on-wishlist-watch activity must NOT appear in a non-mutual viewer's feed"
    - "DISP-01 leak guard (D-10) has a dedicated failing test: getBatchedWatchCounts returns commentCount:0 for a gated wishlist watch and does NOT issue per-watch (N+1) queries"
    - "CMNT-03 newest-first has a dedicated failing test asserting getCommentsForTarget orders by createdAt DESC"
  artifacts:
    - path: "tests/actions/addCommentAction.test.ts"
      provides: "CMNT-01/CMNT-02/FEED-06 assertions (logActivity spy) appended to existing file"
      contains: "logActivity"
    - path: "tests/data/getCommentsForTarget.test.ts"
      provides: "CMNT-03 desc-order + CMNT-04 + CMNT-09 count assertions"
      contains: "desc"
    - path: "tests/data/getBatchedWatchCounts.test.ts"
      provides: "DISP-01 no-N+1 + comment-count gate-leak assertions"
      contains: "commentCount"
    - path: "tests/data/getFeedForUser.test.ts"
      provides: "FEED-07 gated-wishlist-comment leak case appended to existing file"
      contains: "commented"
    - path: "tests/data/comments.test.ts"
      provides: "GATE-03 canViewerCommentOnTarget gate assertions"
      contains: "canViewerCommentOnTarget"
  key_links:
    - from: "tests/data/getFeedForUser.test.ts"
      to: "src/data/activities.ts getFeedForUser"
      via: "mock db.select chain returning a commented row with metadata.watchStatus='wishlist'"
      pattern: "commented"
    - from: "tests/data/getBatchedWatchCounts.test.ts"
      to: "src/data/reactions.ts getBatchedWatchCounts"
      via: "import and call with a gated wishlist watch"
      pattern: "getBatchedWatchCounts"
---

<objective>
Create the Wave 0 test scaffolds for Phase 57 so every downstream behavior has an automated test waiting for it (Nyquist rule). These tests are expected to FAIL (RED) on the current `main` because the implementations they assert do not yet exist — the backend plans (02, 03) and UI plans turn them GREEN.

Purpose: Lock the correctness contract BEFORE implementation, especially the two content-leak guards (FEED-07 / D-12 and DISP-01 / D-10) and the newest-first comment-order reconciliation (CMNT-03).
Output: 5 test files created/extended, RED until backend plans land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-RESEARCH.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-VALIDATION.md

<interfaces>
<!-- Contracts the tests assert against. Extracted from live source. Executor uses these directly. -->

From src/data/comments.ts (verified):
- `export type Comment = InferSelectModel<typeof comments>` (has: id, authorId, watchId, wearEventId, body, createdAt, editedAt, updatedAt)
- `export type CommentTarget = { type: 'watch' | 'wear'; id: string }`  ← discriminator is 'wear' NOT 'wear_event'
- `export class CommentGateError extends Error`
- `getCommentsForTarget(viewerId, target): Promise<Comment[]>` — line 138; CURRENTLY orderBy(asc(createdAt)) at lines 150 & 156 (STALE → will become desc)
- `canViewerCommentOnTarget(viewerId, target): Promise<boolean>` — line 60; wear → true; watch+owner → true; watch status!='wishlist' → true; wishlist → isMutualFollow

From src/app/actions/comments.ts (verified):
- `addCommentAction(data): Promise<ActionResult<Comment>>` — line 51; will gain a logActivity('commented', ...) call guarded by `ownerId !== user.id`
- `editCommentAction(data): Promise<ActionResult<Comment>>` — line 186
- `deleteCommentAction(data): Promise<ActionResult<{ id: string }>>` — line 243; will gain a revalidateTag fix (read-then-delete)

From src/data/activities.ts (verified):
- `ActivityType` (line 21) = 'watch_added' | 'wishlist_added' | 'watch_worn' (will gain 'commented')
- `getFeedForUser(viewerId, ...)` — line 107; inner WHERE gates by ACTOR profileSettings (lines 161-165); will gain a 'commented' branch keyed off TARGET OWNER (D-12)
- `logActivity` — overloaded (lines 53-83); will gain a 'commented' overload

From src/data/reactions.ts (verified):
- `getLikesForTarget(viewerId, target)` — line 29; single-target FILTER aggregate. NO batch fn exists yet.
- `getBatchedWatchCounts(viewerId, watchIds): Promise<Map<string, WatchCounts>>` — DOES NOT EXIST YET (Plan 03 creates it). `WatchCounts = { likeCount: number; commentCount: number }`.
</interfaces>

<existing_test_patterns>
<!-- Mock-db + spy patterns already in the repo. Mirror these exactly. -->
- `tests/actions/comments.test.ts` EXISTS — addCommentAction/editCommentAction/deleteCommentAction tests with vi.mock for the DAL. Line 47 imports the three actions; lines 81/175/236 are the three describe blocks. APPEND new cases; do NOT rewrite existing ones.
- `tests/data/getFeedForUser.test.ts` EXISTS (22KB) — mock `db.select` chain pattern for feed-gate assertions. APPEND the FEED-07 case.
- `tests/data/follows.test.ts`, `tests/actions/reactions.test.ts` — mock-db patterns for DAL/action unit tests.
</existing_test_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment-order + gate + grid-count DAL test scaffolds (RED)</name>
  <files>tests/data/getCommentsForTarget.test.ts, tests/data/comments.test.ts, tests/data/getBatchedWatchCounts.test.ts, tests/data/getFeedForUser.test.ts</files>
  <read_first>
    - tests/data/getFeedForUser.test.ts (the mock db.select chain pattern to mirror; APPEND the FEED-07 case here)
    - tests/data/follows.test.ts (mock-db DAL unit test pattern)
    - src/data/comments.ts lines 60-86 (canViewerCommentOnTarget gate logic) and 138-158 (getCommentsForTarget asc → will be desc)
    - src/data/activities.ts lines 107-195 (getFeedForUser WHERE clause to assert the gate against)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-VALIDATION.md (Wave 0 Requirements + Correctness-Critical Test Seams)
  </read_first>
  <action>
    Create three DAL test files + extend two existing ones. Use Vitest (`describe`/`it`/`expect`/`vi`) and the existing mock-db patterns. These MUST fail RED on current main because the asserted behavior is unimplemented.

    1. CREATE `tests/data/getCommentsForTarget.test.ts` (CMNT-03, CMNT-04, CMNT-09):
       - Test "CMNT-03: getCommentsForTarget orders by createdAt DESC (newest-first)": mock `db.select().from().where().orderBy()` and assert the orderBy receives a DESC ordering on `comments.createdAt` (assert via spying the `orderBy` arg or by mocking the drizzle `desc` call). Per the carried-forward operator decision 2026-05-22 + ROADMAP SC1, the DAL must order DESC. This test fails today because the source uses `asc` at lines 150/156.
       - Test "CMNT-09: getCommentsForTarget returns a list whose length is the comment count" (the detail-page count is derived from thread length): mock the query to return 3 rows, assert `result.length === 3`.
       - Test "gated viewer gets []": mock canViewerCommentOnTarget→false path (watch status 'wishlist', non-mutual), assert `getCommentsForTarget` returns `[]` (no count leak, D-04).

    2. CREATE `tests/data/comments.test.ts` (GATE-03 — `canViewerCommentOnTarget`):
       - Test "wear target always returns true (short-circuit, no DB call)".
       - Test "watch owner returns true regardless of status".
       - Test "non-wishlist watch returns true for any viewer".
       - Test "wishlist watch returns isMutualFollow(viewerId, ownerId) result" — mock isMutualFollow→false → false; →true → true.
       Mock `@/data/follows` isMutualFollow and the watches db.select. (Note: `tests/data/comments.test.ts` does NOT exist; `tests/actions/comments.test.ts` does — this is the DATA-layer file.)

    3. CREATE `tests/data/getBatchedWatchCounts.test.ts` (DISP-01, D-10 leak guard — HIGH priority):
       - Import `getBatchedWatchCounts` from `@/data/reactions` (Plan 03 will create it).
       - Test "DISP-01: returns commentCount:0 for a gated wishlist watch": mock a watch row with status='wishlist' owned by Y, viewer X NOT mutual-follow with Y; assert the returned `Map.get(watchId).commentCount === 0` while `likeCount` reflects the real like count (likes open, GATE-02).
       - Test "DISP-01: returns true comment count for a non-wishlist watch (open to all)".
       - Test "DISP-01: returns true comment count for the owner's own wishlist watch (viewer===owner)".
       - Test "DISP-01: NO N+1 — issues a constant number of queries regardless of watchIds length": call with a 50-item watchId list, assert `db.select` (or the db call spy) is invoked a constant, small number of times (≤5), NOT once per watch. Per RESEARCH Pattern 5: ≤5 queries total (watch rows, viewer→owners follows, owners→viewer follows, like counts, comment counts).

    4. EXTEND `tests/data/getFeedForUser.test.ts` (FEED-07, D-12 leak guard — HIGHEST priority):
       - Append a describe/it "FEED-07: a commented-on-wishlist-watch activity does NOT surface to a non-mutual feed viewer". Scenario: viewer A follows commenter B (outer gate passes); B commented on owner C's WISHLIST watch; A and C are NOT mutual-follow. Expected: A's feed does NOT contain that `commented` row. Mock `db.select` to return a `commented` row with `metadata.targetType='watch'`, `metadata.watchStatus='wishlist'`, `metadata.targetOwnerId=C`; assert the WHERE gate (which Plan 02 adds) excludes it. This test fails today because there is no `commented` branch.
       - Append "FEED-07: a commented-on-OWNED-watch activity DOES surface to a followed-actor's feed viewer" (non-wishlist surfaces normally).
       - Append "FEED-07: a wear-comment activity DOES surface (no wishlist concept on wears)".
  </action>
  <verify>
    <automated>npm run test -- tests/data/getCommentsForTarget.test.ts tests/data/comments.test.ts tests/data/getBatchedWatchCounts.test.ts tests/data/getFeedForUser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist and are syntactically valid TypeScript (vitest collects them without parse errors).
    - The new CMNT-03 / DISP-01 / FEED-07 leak-guard cases FAIL (RED) — confirming they assert behavior not yet implemented. Pre-existing getFeedForUser cases still PASS.
    - `tests/data/getBatchedWatchCounts.test.ts` imports `getBatchedWatchCounts` from `@/data/reactions` (will resolve once Plan 03 lands).
    - The N+1 assertion counts db invocations and asserts ≤5 (not proportional to watchIds.length).
  </acceptance_criteria>
  <done>Four DAL-layer test files created/extended; new leak-guard + order cases RED; pre-existing cases unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: Comment Server Action test scaffolds (RED)</name>
  <files>tests/actions/comments.test.ts</files>
  <read_first>
    - tests/actions/comments.test.ts (EXISTING — line 47 imports the three actions; describe blocks at 81/175/236; APPEND new cases, do NOT rewrite)
    - tests/actions/reactions.test.ts (action mock pattern incl. vi.mock of revalidateTag / logActivity)
    - src/app/actions/comments.ts lines 51-184 (addCommentAction; the ownerId !== user.id guard at 143-177) and 243-269 (deleteCommentAction — currently NO revalidateTag)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md (the src/app/actions/comments.ts modifications section, lines ~478-557)
  </read_first>
  <action>
    Append new cases to the EXISTING `tests/actions/comments.test.ts` (do NOT overwrite the file — preserve all current describe blocks). Add a `vi.mock('@/data/activities', ...)` to spy on `logActivity`. These assert the Plan 02 action changes and MUST fail RED today.

    Create two new action test files and extend the existing one:

    1. EXTEND `tests/actions/comments.test.ts` — within the existing `addCommentAction` describe:
       - "FEED-06: addCommentAction calls logActivity with type 'commented' on a non-self comment": spy on `logActivity`; comment on another user's watch; assert `logActivity` called once with `(actorId, 'commented', watchId, metadata)` where `metadata.targetType === 'watch'`, `metadata.targetOwnerId === <ownerId>`, `metadata.watchStatus` is present.
       - "FEED-06: addCommentAction does NOT call logActivity on a SELF comment" (ownerId === user.id guard, mirrors NOTIF-12).
       - "FEED-06: wear comment logs activity with watchId=null and metadata.wearEventId set, targetType==='wear'" (NOT 'wear_event').
       These fail today because addCommentAction does not yet import/call logActivity.

    2. EXTEND `tests/actions/comments.test.ts` — within the existing `deleteCommentAction` describe:
       - "CMNT-07: deleteCommentAction revalidates the owner's profile tag (read-then-delete)": spy on `revalidateTag` from `next/cache`; assert `deleteCommentAction` calls `revalidateTag(\`profile:${ownerUsername}\`, 'max')`. Fails today (Pitfall 6 — delete currently has no revalidateTag).
       - Preserve the existing "NOTIF-12: deleteCommentAction does NOT call logNotification" case.

    3. CREATE `tests/actions/addCommentAction.test.ts` ONLY IF a clean separate file is preferable — otherwise consolidate into the existing comments.test.ts (Claude's discretion; the VALIDATION map lists it separately but the existing file is the canonical home). If creating it, mirror the existing comments.test.ts mock setup. Keep CMNT-01/CMNT-02 happy-path (returns `ActionResult<Comment>` with id + createdAt) covered exactly once across the two files (no duplicate).

    Decision: APPEND to the existing `tests/actions/comments.test.ts` rather than creating parallel files, to avoid duplicate mock setup and keep CMNT-01/02/06/07/FEED-06 in one place. Record this in the SUMMARY so the VALIDATION map's separate-file rows are reconciled to the actual file.
  </action>
  <verify>
    <automated>npm run test -- tests/actions/comments.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/actions/comments.test.ts` retains all pre-existing cases (NOTIF-12, SEC-03, D-09 gate) AND gains the FEED-06 logActivity-spy cases + the CMNT-07 deleteCommentAction revalidateTag case.
    - The new FEED-06 and CMNT-07-revalidate cases FAIL (RED) on current main.
    - A `vi.mock('@/data/activities')` (or equivalent spy) is present so `logActivity` calls are observable.
    - No duplicate CMNT-01/CMNT-02 happy-path assertion across files.
  </acceptance_criteria>
  <done>Action-layer test scaffolds appended; FEED-06 logActivity spy + delete-revalidate cases RED; existing cases preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feed viewer → getFeedForUser | viewer requests a feed; gated wishlist-comment rows must not leak (FEED-07) |
| grid viewer → getBatchedWatchCounts | viewer loads a profile grid; comment counts on gated wishlist cards must not leak (DISP-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | getFeedForUser commented-row gate | mitigate | Wave 0 test asserts the FEED-07 leak case fails-closed; Plan 02 implements the D-12 target-owner gate that turns it GREEN |
| T-57-02 | Information Disclosure | getBatchedWatchCounts comment half | mitigate | Wave 0 test asserts commentCount:0 for gated wishlist watch; Plan 03 implements the D-10 gate that turns it GREEN |
| T-57-03 | Tampering | this plan creates no production code | accept | Test-only plan; no runtime attack surface introduced |
</threat_model>

<verification>
- `npm run test -- tests/data/ tests/actions/comments.test.ts` collects all scaffolds without parse errors.
- The leak-guard and order cases are RED (expected on current main).
- `git diff --stat` shows only test files touched.
</verification>

<success_criteria>
- All 5 Wave 0 test references from 57-VALIDATION.md exist (4 created/extended in Task 1, the action coverage in Task 2).
- FEED-07 (D-12) and DISP-01 (D-10) each have a dedicated failing leak-guard test.
- CMNT-03 newest-first (desc) has a dedicated failing test.
- No production source files modified.
</success_criteria>

<output>
After completion, create `.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-01-SUMMARY.md`. Note the decision to append to the existing `tests/actions/comments.test.ts` rather than create separate per-action files, so the VALIDATION map is reconciled.
</output>
