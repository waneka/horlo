---
phase: 57-comment-thread-ui-feed-extension-grid-counts
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/data/reactions.ts
autonomous: true
requirements: [DISP-01]

must_haves:
  truths:
    - "Per DISP-01: getBatchedWatchCounts(viewerId, watchIds) returns a Map of watchId → { likeCount, commentCount } sourced from a CONSTANT number of queries (no N+1) regardless of how many watchIds are passed"
    - "Per DISP-01 / D-10 (LEAK GUARD): commentCount is 0 for a wishlist watch when the viewer is NOT the owner and NOT mutual-follow with the owner; like counts stay open to all (GATE-02)"
    - "The cached wrapper getBatchedWatchCountsCached tags with profile:{username} (refreshed for free by the existing comment-action revalidateTag) AND viewer:{viewerId}:counts (comment counts are viewer-scoped because of the wishlist gate)"
  artifacts:
    - path: "src/data/reactions.ts"
      provides: "getBatchedWatchCounts + getBatchedWatchCountsCached + WatchCounts interface"
      exports: ["getBatchedWatchCounts", "getBatchedWatchCountsCached"]
      contains: "commentCount"
  key_links:
    - from: "src/data/reactions.ts getBatchedWatchCounts"
      to: "follows table batched mutual-follow set"
      via: "two inArray follows queries (viewer→owners, owners→viewer) intersected in JS — NOT isMutualFollow in a loop"
      pattern: "inArray"
    - from: "src/data/reactions.ts getBatchedWatchCounts"
      to: "comments table grouped count"
      via: "count(*) GROUP BY watchId restricted to allowed (non-gated) watchIds"
      pattern: "groupBy"
---

<objective>
Build the DISP-01 batched grid-count DAL: a single function returning like + comment counts per watch for a list of watchIds, using a constant number of queries (no N+1) and asymmetrically gating the comment half (likes open per GATE-02, comments mutual-follow-gated on wishlist watches per D-10). This is the second content-leak guard in the phase.

Purpose: Make the Wave 0 DISP-01 test GREEN — including the no-N+1 assertion and the comment-count-gate-leak assertion.
Output: `getBatchedWatchCounts` + `getBatchedWatchCountsCached` + `WatchCounts` in `src/data/reactions.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-RESEARCH.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md

<interfaces>
<!-- Verified live source. Use these directly. -->

src/data/reactions.ts (verified):
- line 1: `import 'server-only'`
- line 3: `import { cacheTag } from 'next/cache'`
- line 4: `import { and, eq, sql } from 'drizzle-orm'`  ← add `inArray`
- line 7: `import { watchLikes, wearLikes } from '@/db/schema'`  ← add `comments`, `watches`, `follows`
- lines 29-52: getLikesForTarget — FILTER aggregate single-target pattern (the count(*)::int + bool_or shape to mirror)
- lines 145-152: getLikesForTargetCached — `'use cache'` + `cacheTag(\`reactions:${target.type}:${target.id}\`, \`viewer:${viewerId}:reactions\`)` (the cache-wrapper pattern to mirror)

src/db/schema.ts (verified):
- watches (line 87): columns include `id`, `userId` (user_id), `status`
- watchLikes (line 319): `userId`, `watchId` (watch_id); index on watchId
- comments (table): `watchId` (watch_id, nullable), `wearEventId` (wear_event_id, nullable), createdAt — DB CHECK enforces watchId XOR wearEventId
- follows (line 239): `followerId` (follower_id), `followingId` (following_id)

src/data/follows.ts (verified):
- lines 77-95: isMutualFollow — single-pair bidirectional. DO NOT call in a loop (N+1). The batch uses two inArray queries + a JS set intersection instead.
- mergeListEntries (lines 165-184): the inArray + count(*) FILTER GROUP BY batch pattern to mirror.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getBatchedWatchCounts + cached wrapper (no N+1, D-10 gate)</name>
  <files>src/data/reactions.ts</files>
  <read_first>
    - src/data/reactions.ts (full — imports 1-7, getLikesForTarget 29-52, getLikesForTargetCached 145-152)
    - src/data/follows.ts lines 77-95 (isMutualFollow — what NOT to loop) and 165-184 (mergeListEntries inArray + FILTER GROUP BY batch pattern to mirror)
    - tests/data/getBatchedWatchCounts.test.ts (Wave 0 — the no-N+1 + gate-leak assertions this task turns GREEN)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-RESEARCH.md (Pattern 5, lines 340-381 and Deep-Dive #2, lines 445-520)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md (reactions.ts batched-query section, lines 1035-1136)
  </read_first>
  <behavior>
    - getBatchedWatchCounts(viewerId, [w1,w2,...]) returns Map<string, {likeCount, commentCount}> with an entry for every input watchId (watches with no rows get {likeCount:0, commentCount:0}).
    - For a non-wishlist watch: commentCount = real comment count for any viewer.
    - For the viewer's OWN wishlist watch (watch.userId === viewerId): commentCount = real comment count.
    - For another user's wishlist watch where viewer IS mutual-follow with the owner: commentCount = real comment count.
    - For another user's wishlist watch where viewer is NOT mutual-follow: commentCount = 0 (gated, D-10). likeCount stays the real count (likes open, GATE-02).
    - Query count is constant: ≤5 db calls regardless of watchIds.length (no per-watch query).
  </behavior>
  <action>
    In `src/data/reactions.ts`:
    - Extend imports: add `inArray` to the drizzle-orm import (line 4); add `comments`, `watches`, `follows` to the `@/db/schema` import (line 7).
    - Add `export interface WatchCounts { likeCount: number; commentCount: number }`.
    - Add `export async function getBatchedWatchCounts(viewerId: string, watchIds: string[]): Promise<Map<string, WatchCounts>>` implementing the constant-query batch from RESEARCH Pattern 5:
      1. Early return empty Map if `watchIds.length === 0`.
      2. Q1 — watch rows for the gate: `SELECT id, status, userId FROM watches WHERE id IN watchIds` (inArray).
      3. Compute `wishlistOwnerIds` = distinct userIds of watches where status==='wishlist' AND userId !== viewerId. If non-empty, run the two follows queries (else skip — that keeps the count at 3 for collections with no foreign wishlist watches):
         - Q2: `SELECT followingId FROM follows WHERE followerId = viewerId AND followingId IN wishlistOwnerIds` (viewer→owners)
         - Q3: `SELECT followerId FROM follows WHERE followingId = viewerId AND followerId IN wishlistOwnerIds` (owners→viewer)
         Build `mutualSet = intersection(viewerFollowsSet, ownersFollowSet)` in JS (mirror mergeListEntries set logic). DO NOT call isMutualFollow per watch.
      4. Compute `allowedWatchIds` = watchIds where (watch.userId === viewerId) OR (watch.status !== 'wishlist') OR (mutualSet.has(watch.userId)).
      5. Q4 — like counts (ALL watchIds, open): `SELECT watchId, count(*)::int FROM watchLikes WHERE watchId IN watchIds GROUP BY watchId` (mirror the FILTER/count(*) pattern from getLikesForTarget).
      6. Q5 — comment counts (ONLY allowedWatchIds): `SELECT watchId, count(*)::int FROM comments WHERE watchId IN allowedWatchIds AND watchId IS NOT NULL GROUP BY watchId`. Skip this query if allowedWatchIds is empty.
      7. Build the result Map: for every input watchId, set likeCount from Q4 (default 0) and commentCount from Q5 (default 0; watches not in allowedWatchIds are 0 by construction — the D-10 gate).
    - Add `export async function getBatchedWatchCountsCached(viewerId: string, watchIds: string[], profileUsername: string): Promise<Map<string, WatchCounts>>` mirroring getLikesForTargetCached:
      `'use cache'` then `cacheTag(\`profile:${profileUsername}\`, \`viewer:${viewerId}:counts\`)` then `return getBatchedWatchCounts(viewerId, watchIds)`. Include viewerId in the tag because comment counts are viewer-scoped (the wishlist gate). Resolve viewerId/username OUTSIDE this scope (request-time APIs are forbidden inside 'use cache').
  </action>
  <verify>
    <automated>npm run test -- tests/data/getBatchedWatchCounts.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Wave 0 DISP-01 tests PASS: commentCount===0 for the gated wishlist watch; real commentCount for non-wishlist + owner-own + mutual cases; likeCount always real.
    - The no-N+1 test PASSES: the db-call spy is invoked a constant ≤5 times for a 50-item watchId list (not ~50). For a list with NO foreign wishlist watches, the follows queries are skipped (≤3 calls).
    - `grep -c "isMutualFollow" src/data/reactions.ts` returns 0 inside getBatchedWatchCounts (no per-watch mutual-follow call); the batch uses `inArray` follows queries instead (`grep -c "inArray" src/data/reactions.ts` ≥ 2).
    - getBatchedWatchCountsCached has `'use cache'` and a cacheTag including both `profile:` and `viewer:${viewerId}:counts`.
    - Every input watchId has an entry in the returned Map (no missing keys).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Batched like+comment count DAL with constant query count and the D-10 comment-gate; cached wrapper tagged for the profile + viewer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grid viewer → getBatchedWatchCounts | viewer loads a profile collection/wishlist grid; comment counts on gated wishlist cards must not leak to non-mutual viewers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02 | Information Disclosure | getBatchedWatchCounts comment half (DISP-01 / D-10) | mitigate | Comment counts restricted to allowedWatchIds (owner OR non-wishlist OR mutual-follow); gated wishlist watches return commentCount:0. Likes stay open (GATE-02). Verified by the Wave 0 gate-leak test. |
| T-57-07 | Information Disclosure | per-viewer cache leak on comment counts | mitigate | getBatchedWatchCountsCached includes `viewer:${viewerId}:counts` in the cacheTag so viewer A's gated comment counts are not served to viewer B |
| T-57-08 | Denial of Service | N+1 on a 500-watch grid | mitigate | Constant ≤5 queries via inArray batch + JS set intersection (no isMutualFollow loop); verified by the no-N+1 test |
</threat_model>

<verification>
- `npm run test -- tests/data/getBatchedWatchCounts.test.ts` GREEN.
- `npm run test` full suite shows no regressions in reactions.test.ts.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- DISP-01 batched read exists with constant query count (no N+1).
- D-10 leak guard: gated wishlist watches return commentCount:0; likes open.
- Cached wrapper viewer-scoped for the comment-gate; profile-tagged for free invalidation.
</success_criteria>

<output>
After completion, create `.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-03-SUMMARY.md`. Record the exact final query count for (a) a collection with no foreign wishlist watches and (b) a collection with foreign wishlist watches, so the DISP-01 no-N+1 claim is documented.
</output>
