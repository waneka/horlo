---
phase: 57-comment-thread-ui-feed-extension-grid-counts
plan: 06
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/components/profile/ProfileWatchCard.tsx
  - src/components/profile/SortableProfileWatchCard.tsx
  - src/components/profile/CollectionTabContent.tsx
  - src/components/profile/WishlistTabContent.tsx
  - src/app/u/[username]/[tab]/page.tsx
autonomous: true
requirements: [DISP-01]

must_haves:
  truths:
    - "Per DISP-01 / D-09: each profile collection/wishlist grid card shows a '♥ N · 💬 M' line (icons + tabular numbers), each metric hidden at zero, and the WHOLE line removed when both are zero"
    - "Per DISP-01: the counts come from a SINGLE batched read (getBatchedWatchCountsCached) resolved once per grid in ProfileTabContent — NOT one query per card (no N+1)"
    - "Per DISP-01 / D-10 (LEAK GUARD inherited from Plan 03): the comment half is 0 for gated wishlist cards for non-mutual viewers (the query does the gating; the card display needs no per-card gate check); the like half stays open (GATE-02)"
    - "The grid read is tagged profile:{username} (invalidated for free by the comment actions' existing revalidateTag) and viewer:{viewerId}:counts (viewer-scoped comment gate)"
  artifacts:
    - path: "src/components/profile/ProfileWatchCard.tsx"
      provides: "♥ N · 💬 M count line in CardContent, hidden-at-zero per half, line removed when both zero"
      contains: "MessageCircle"
    - path: "src/app/u/[username]/[tab]/page.tsx"
      provides: "getBatchedWatchCountsCached call in ProfileTabContent + counts passed to tab content"
      contains: "getBatchedWatchCountsCached"
  key_links:
    - from: "src/app/u/[username]/[tab]/page.tsx ProfileTabContent"
      to: "src/data/reactions.ts getBatchedWatchCountsCached"
      via: "single call with (viewerId, ownerWatchIds, profile.username); serialized to a plain Record for the client tab components"
      pattern: "getBatchedWatchCountsCached"
    - from: "src/components/profile/ProfileWatchCard.tsx"
      to: "likeCount/commentCount props"
      via: "counts looked up by watch.id and passed per card from the tab content"
      pattern: "commentCount"
---

<objective>
Render the DISP-01 grid count line ('♥ N · 💬 M') on profile collection/wishlist cards, sourced from the single batched read built in Plan 03. Resolve the counts once per grid in `ProfileTabContent`, thread a counts lookup down through the tab content components to `ProfileWatchCard` / `SortableProfileWatchCard`. The query already enforces the D-10 comment-gate; the display just renders what it gets.

Purpose: Surface like + comment counts on the grid without N+1, with the comment half gated for non-mutual viewers on wishlist cards.
Output: 5 files; count line live on both grids.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-UI-SPEC.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-CONTEXT.md
@.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-03-SUMMARY.md

<interfaces>
<!-- Verified live source. Use these directly. -->

src/data/reactions.ts (from Plan 03):
- `getBatchedWatchCountsCached(viewerId: string, watchIds: string[], profileUsername: string): Promise<Map<string, WatchCounts>>`
- `WatchCounts = { likeCount: number; commentCount: number }` (commentCount already 0 for gated wishlist watches).

src/components/profile/ProfileWatchCard.tsx (verified):
- `ProfileWatchCardProps` (line 13): { watch: Watch; lastWornDate: string | null; showWishlistMeta?: boolean }
- CardContent block (lines 97-113): `<CardContent className="px-3 py-2 flex flex-col gap-1 flex-1">` — the count line lands at the bottom, after priceLine + showWishlistMeta.

src/components/profile/SortableProfileWatchCard.tsx (verified):
- `SortableProfileWatchCardProps` (lines 8-13): { id; watch; lastWornDate; showWishlistMeta } — renders <ProfileWatchCard ...> at lines 94-98.

src/components/profile/CollectionTabContent.tsx (verified):
- props: { watches: Watch[]; wearDates; isOwner; hasUrlExtract }. Renders `filtered.map(watch => <ProfileWatchCard key watch lastWornDate />)` (lines ~181-186).

src/components/profile/WishlistTabContent.tsx (verified):
- props: { watches: Watch[]; wearDates; isOwner?; username }. Non-owner branch maps <ProfileWatchCard ... showWishlistMeta /> (lines ~92-100). Owner branch (OwnerWishlistGrid) maps <SortableProfileWatchCard ...> (line ~260).

src/app/u/[username]/[tab]/page.tsx (verified):
- ProfileTabContent (async, inside Suspense, UNCACHED at the outer level): resolves viewerId (line ~170), `resolved.watches` as ownerWatches (line 184), `profile.username`. Renders CollectionTabContent / WishlistTabContent (lines ~282-319) for the collection/wishlist tabs.

src/lib/types.ts: Watch (has id, status, ...).
lucide-react: Heart, MessageCircle.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfileWatchCard + SortableProfileWatchCard count line (D-09)</name>
  <files>src/components/profile/ProfileWatchCard.tsx, src/components/profile/SortableProfileWatchCard.tsx</files>
  <read_first>
    - src/components/profile/ProfileWatchCard.tsx (full — props line 13, CardContent 97-113)
    - src/components/profile/SortableProfileWatchCard.tsx (full — props 8-13, ProfileWatchCard render 94-98)
    - src/components/shared/LikeButton.tsx (the hidden-at-zero count convention to mirror)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-UI-SPEC.md (ProfileWatchCard grid count line, lines 254-285, CSS-chain assertions 422-433)
    - .planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-PATTERNS.md (ProfileWatchCard + SortableProfileWatchCard sections, lines 782-866)
  </read_first>
  <action>
    In `src/components/profile/ProfileWatchCard.tsx`:
    - Add to ProfileWatchCardProps: `likeCount?: number; commentCount?: number`.
    - Import `Heart, MessageCircle` from lucide-react.
    - At the BOTTOM of `<CardContent>` (after priceLine + showWishlistMeta blocks), add the count line per UI-SPEC lines 259-277:
      `{((likeCount ?? 0) > 0 || (commentCount ?? 0) > 0) && (`
        `<p className="text-xs text-muted-foreground tabular-nums flex items-center gap-1">`
          `{(likeCount ?? 0) > 0 && (<><Heart className="size-3" aria-hidden />{likeCount}</>)}`
          `{(likeCount ?? 0) > 0 && (commentCount ?? 0) > 0 && (<span className="mx-1">·</span>)}`
          `{(commentCount ?? 0) > 0 && (<><MessageCircle className="size-3" aria-hidden />{commentCount}</>)}`
        `</p>`
      `)}`
    - The whole line is removed when both are zero (the outer guard); each half hidden when its own count is zero. Icons `size-3` (12px), text `text-xs tabular-nums` per UI-SPEC.

    In `src/components/profile/SortableProfileWatchCard.tsx`:
    - Add `likeCount?: number; commentCount?: number` to SortableProfileWatchCardProps; thread them into the `<ProfileWatchCard ... likeCount={likeCount} commentCount={commentCount} />` render (lines 94-98).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test -- src/components/profile/__tests__/ProfileWatchCard-priceLine.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ProfileWatchCard renders the count line ONLY when at least one count `> 0` (`grep -c "likeCount ?? 0) > 0 || (commentCount" src/components/profile/ProfileWatchCard.tsx` ≥ 1).
    - Each half is independently hidden at zero; the `·` separator only renders when BOTH `> 0`.
    - Icons are `size-3`; text is `text-xs tabular-nums text-muted-foreground` (matches UI-SPEC).
    - SortableProfileWatchCard threads both count props through to ProfileWatchCard.
    - `npx tsc --noEmit` clean; the existing ProfileWatchCard priceLine test still passes (props are optional, no breakage).
  </acceptance_criteria>
  <done>Grid count line on ProfileWatchCard + SortableProfileWatchCard with per-half hidden-at-zero and whole-line removal when both zero.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve batched counts in ProfileTabContent + thread through tab content</name>
  <files>src/app/u/[username]/[tab]/page.tsx, src/components/profile/CollectionTabContent.tsx, src/components/profile/WishlistTabContent.tsx</files>
  <read_first>
    - src/app/u/[username]/[tab]/page.tsx (full — ProfileTabContent async resolution; viewerId ~170, ownerWatches 184, profile.username, tab-content render 282-319)
    - src/components/profile/CollectionTabContent.tsx (props + ProfileWatchCard render)
    - src/components/profile/WishlistTabContent.tsx (props + both ProfileWatchCard / SortableProfileWatchCard render sites incl. OwnerWishlistGrid)
    - src/data/reactions.ts getBatchedWatchCountsCached (Plan 03) + 57-03-SUMMARY.md (query-count notes)
    - MEMORY: project_phase_plan_index_wave_collapse, project_router_cache_stale_instance (be careful editing the Cache-Components-sensitive [tab]/page.tsx — keep ProfileTabContent async/uncached; do NOT add 'use cache' to ProfileTabContent; the batched read brings its own 'use cache' wrapper)
  </read_first>
  <action>
    In `src/app/u/[username]/[tab]/page.tsx` (inside ProfileTabContent, for the collection/wishlist tabs only — Notes tab does not need counts):
    - After ownerWatches + viewerId + profile.username are resolved, compute the counts once:
      `const watchIds = ownerWatches.map((w) => w.id)`
      `const countsMap = viewerId !== null ? await getBatchedWatchCountsCached(viewerId, watchIds, profile.username) : new Map()`
      (For anon viewers — if this route is anon-reachable — pass an empty map or call with a sentinel; per Phase 51 Branch B `/u/*` is auth-gated, so viewerId should be non-null here. Confirm and document; if anon can reach, skip the comment half by passing empty.)
    - Map→Record for the client boundary (Maps don't serialize cleanly): `const counts: Record<string, { likeCount: number; commentCount: number }> = Object.fromEntries(countsMap)`.
    - Pass `counts={counts}` to <CollectionTabContent ...> and <WishlistTabContent ...>.
    - Do NOT add `'use cache'` to ProfileTabContent (Cache Components landmine — getBatchedWatchCountsCached carries its own wrapper; ProfileTabContent stays uncached async inside Suspense).

    In `src/components/profile/CollectionTabContent.tsx`:
    - Add `counts?: Record<string, { likeCount: number; commentCount: number }>` to props. At the ProfileWatchCard render (lines ~181-186), pass `likeCount={counts?.[watch.id]?.likeCount} commentCount={counts?.[watch.id]?.commentCount}`.

    In `src/components/profile/WishlistTabContent.tsx`:
    - Add the same `counts?` prop. Thread it into BOTH render sites: the non-owner ProfileWatchCard map (lines ~92-100) and the owner OwnerWishlistGrid → SortableProfileWatchCard (line ~260). OwnerWishlistGrid is a sub-component — pass `counts` down to it and look up per watch.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test && rm -rf .next && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `getBatchedWatchCountsCached` is called EXACTLY once per grid render in ProfileTabContent (`grep -c "getBatchedWatchCountsCached" src/app/u/[username]/[tab]/page.tsx` == 1) — not inside a map/loop.
    - The Map is converted to a plain Record before crossing into the client tab components (`grep -c "Object.fromEntries" src/app/u/[username]/[tab]/page.tsx` ≥ 1).
    - `'use cache'` is NOT added to ProfileTabContent (`grep -c "use cache" src/app/u/[username]/[tab]/page.tsx` is unchanged from before this plan — verify the count did not increase).
    - Both tab content components accept `counts?` and pass likeCount/commentCount per card by watch.id; the owner wishlist DnD grid threads counts through OwnerWishlistGrid.
    - `npx tsc --noEmit` clean; `npm run test` green; `rm -rf .next && npm run build` succeeds (Cache Components build gate — confirms no PPR regression on the profile route).
  </acceptance_criteria>
  <done>Batched counts resolved once per grid, serialized as a Record, threaded to every ProfileWatchCard/SortableProfileWatchCard render site; profile route still builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grid viewer → ProfileTabContent counts | Comment counts on gated wishlist cards must not leak to non-mutual viewers (the gate is in the Plan 03 query, surfaced here) |
| RSC → client tab content | The counts Record crosses the server→client boundary; it must carry no extra (un-gated) data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02 | Information Disclosure | grid comment-count leak (DISP-01 / D-10) | mitigate | The batched query returns commentCount:0 for gated wishlist cards (Plan 03); the card renders only what the query returns — no per-card gate bypass possible, and the Record carries only {likeCount, commentCount} |
| T-57-07 | Information Disclosure | per-viewer cache leak | mitigate | getBatchedWatchCountsCached cacheTag includes viewer:{viewerId}:counts (Plan 03), isolating viewer A's gated counts from viewer B |
| T-57-16 | Tampering | Cache Components PPR regression on /u/[username]/[tab] | mitigate | ProfileTabContent stays async/uncached inside Suspense; no 'use cache' added; `rm -rf .next && npm run build` gates the build (Phase 52 recurrence-4 prevention) |
</threat_model>

<verification>
- `npm run test` full suite green; `npx tsc --noEmit` clean.
- `rm -rf .next && npm run build` succeeds (no React #419 / PPR regression on the profile route).
- Manual (prod, per VALIDATION): grid cards show '♥ N · 💬 M' with each half hidden at zero; a non-mutual viewer on a wishlist watch sees the like count but NO comment count.
</verification>

<success_criteria>
- DISP-01 count line live on both collection + wishlist grids (owner DnD + non-owner read-only).
- Single batched read per grid (no N+1); comment half gated, like half open.
- Profile route still builds clean (Cache Components gate).
</success_criteria>

<output>
After completion, create `.planning/phases/57-comment-thread-ui-feed-extension-grid-counts/57-06-SUMMARY.md`. Record: (1) whether the /u/* route is auth-gated (Branch B) so viewerId is always non-null, or whether an anon empty-counts path was needed; (2) the Map→Record serialization decision; (3) confirmation that `rm -rf .next && npm run build` passed (PPR gate).
</output>
