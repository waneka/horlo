---
phase: 70
plan: 05
type: execute
wave: 3
depends_on:
  - 70-01
  - 70-02
  - 70-03
  - 70-04
files_modified:
  - src/components/watch/AddWatchFlow.tsx
  - src/components/watch/AddWatchFlow.test.tsx
autonomous: true
requirements:
  - DUPE-01 (UI part)
  - DUPE-02
  - DUPE-03 (UI part)
  - CLNP-05
  - CLNP-06
tags:
  - addwatchflow
  - phase-70
  - orchestrator
  - state-machine
  - dupe-01
  - dupe-02
  - dupe-03
  - clnp-06
  - photos-pending-gate
must_haves:
  truths:
    - "D-01 — AddWatchFlow consumes the FlowState union shipped by Plan 04 (search-idle | extracting-url | extraction-failed | confirming | form-prefill | manual-entry | photos-pending)"
    - "D-02 — Every transition in the JSDoc map (Plan 04) is implemented; the orchestrator owns dispatch + DupeBanner / ConfirmStep / SearchEntry are pure presenters"
    - "D-03 — initialState precedence (form-prefill > manual-entry > search-idle) preserved verbatim; ?manual=1 deep-link priority preserved"
    - "D-04 — initialReturnTo round-trip preserved in every commit branch (Phase 28 validateReturnTo gate stays at /watch/new/page.tsx; orchestrator threads initialReturnTo through every router.push)"
    - "D-05 — DUPE-01 routes via router.push(`/w/${encodeURIComponent(result.reference)}`) when result.viewerState==='owned' && result.reference (no findViewerWatchByCatalogId call needed on this branch — URL fully derivable from the picked result)"
    - "D-06 — null-reference owned-pick falls through to confirming with owned-banner (DupeBanner hides 'View existing' button when existingReference is null)"
    - "D-07 — DUPE-02 banner appears only in confirming when dupeContext.existingStatus==='owned'; handleStructuredSubmit + handleUrlBackup call findViewerWatchByCatalogId with ['owned','wishlist'] AFTER catalog upsert resolves (search-pick owned-branch SKIPS via DUPE-01 redirect)"
    - "D-08 — 'Add another copy' clears dupeContext; primary CTA stays addWatch(catalogId); no UNIQUE(userId,catalogId) constraint exists (RESEARCH §D-08 verified — two rows for same user/catalog is structurally permitted)"
    - "D-09 — no /w/[ref] edit (PROJECT.md milestone lock); DupeBanner 'View existing' navigates externally"
    - "D-11 — DupeBanner is sibling ABOVE ConfirmStep in confirming branch; ConfirmStep prop contract (Phase 68 D-03) LOCKED — no callback additions to ConfirmStep"
    - "D-12 — DupeBanner is the primary affordance when present; ConfirmStep CTA stays its own label (visual hierarchy via Button variant: primary for DupeBanner action, outline for 'Add another copy')"
    - "D-13 — post-DUPE-03 nav: defaultDestinationForStatus('owned', viewerUsername); toast 'Moved to collection' with View action; NO photos-pending step (existing row, not fresh add)"
    - "D-14 — extracting-url branch is INLINE (no PasteSection import); body {mode:'url', url} consumes Phase 66 mode-discriminated contract"
    - "D-15 — '← Back to search' affordance always rendered in extracting-url branch; setState({kind:'search-idle'})"
    - "D-16 — useUrlExtractCache(viewerUserId) reused identically (Phase 69 D-08 retrofit); verdict-compute branch REMOVED"
    - "D-17 — photos-pending gated on status==='owned'; wishlist+grail commits skip directly to defaultDestinationForStatus (v7.0→v8.0 UX evolution; manual-entry uses Plan 01 Task 3's WatchForm.onWatchCreated(watchId,dest,status) widened signature)"
    - "D-18 — full destination matrix implemented per CONTEXT.md (fresh add owned → photos-pending → /collection; fresh add wishlist/grail → /wishlist; DUPE-03 → /collection direct; DUPE-01 → /w/[ref] direct; form-prefill always owned via lockedStatus; manual-entry gated by D-17)"
    - "D-19 — CLNP-06 'Skip search — enter manually' link renders BELOW SearchEntry in search-idle branch; setState({kind:'manual-entry'}) with NO router.push (URL stays at /watch/new)"
    - "D-20 — manual-entry back affordance copy updated to '← Cancel — return to search'; setState({kind:'search-idle'})"
    - "D-21 — default confirming status: initialStatus ?? 'wishlist'; DUPE-02 owned-context overrides to 'owned'"
    - "D-22 — useLayoutEffect cleanup updated for new FlowState kinds; module-scope caches NOT cleared (CLNP-07 in Phase 69 handles cross-user reset via viewerUserId mismatch)"
    - "RESEARCH §1/§2 — Plan 01 Wave 0 patches (StructuredEntryPanel emit + findViewerWatchByCatalogId reference field + WatchForm.onWatchCreated status) consumed; no second-fetch round-trips"
    - "T-70-04 — dupeContext is re-verified server-side via findViewerWatchByCatalogId in structured-input + URL-backup paths (catalog-row-authoritative reads; not from client SearchEntry's viewerState flag for these paths)"
    - "Hard cutover — AddWatchFlow no longer imports PasteSection, VerdictStep, WishlistRationalePanel, RecentlyEvaluatedRail, useWatchSearchVerdictCache, getVerdictForCatalogWatch, or VerdictBundle (Phase 71 deletes the files + adds static guards; Phase 70 stops referencing them)"
    - "Sanity check (Claude's Discretion) — net LOC reduction expected (~250-300 lines of verdict-flow logic removed; ~100 LOC of new orchestrator handlers gained); if rewrite INCREASES LOC, scope creep likely"
  artifacts:
    - path: "src/components/watch/AddWatchFlow.tsx"
      provides: "Rewritten orchestrator mounting SearchEntry + ConfirmStep + DupeBanner + ExtractErrorCard + WatchForm + WatchPhotoStep per D-01 union"
      contains: "export function AddWatchFlow"
      min_lines: 400
    - path: "src/components/watch/AddWatchFlow.test.tsx"
      provides: "Phase 69 CLNP-07 four-cache test PRESERVED + new Phase 70 transition tests (DUPE-01 redirect, DUPE-02 banner, DUPE-03 commit, CLNP-06 link, photos-pending gate, URL-backup, manual-entry back-link)"
      contains: "describe.*Phase 70"
  key_links:
    - from: "src/components/watch/AddWatchFlow.tsx"
      to: "src/components/watch/SearchEntry.tsx"
      via: "mounted in search-idle branch; onPick/onSubmitStructured/onSwitchToUrl threaded with useCallback handlers"
      pattern: "<SearchEntry"
    - from: "src/components/watch/AddWatchFlow.tsx"
      to: "src/components/watch/ConfirmStep.tsx"
      via: "mounted in confirming branch; all 12 props per Phase 68 D-03 locked contract"
      pattern: "<ConfirmStep"
    - from: "src/components/watch/AddWatchFlow.tsx"
      to: "src/components/watch/DupeBanner.tsx (Plan 02)"
      via: "mounted ABOVE ConfirmStep in confirming branch when dupeContext is set"
      pattern: "<DupeBanner"
    - from: "src/components/watch/AddWatchFlow.tsx handleMoveToCollection"
      to: "src/app/actions/watches.ts moveWishlistToCollection (Plan 03)"
      via: "DupeBanner.onMoveToCollection → orchestrator handler → Server Action"
      pattern: "moveWishlistToCollection\\(state.dupeContext.existingWatchId"
    - from: "src/components/watch/AddWatchFlow.tsx handleSearchPick + handleStructuredSubmit + handleUrlBackup"
      to: "src/data/watches.ts findViewerWatchByCatalogId (Plan 01 Task 2 widened)"
      via: "called for DUPE-02/03 dupeContext resolution"
      pattern: "findViewerWatchByCatalogId.*\\['owned', 'wishlist'\\]"
    - from: "src/components/watch/AddWatchFlow.tsx"
      to: "src/components/watch/flowTypes.ts (Plan 04)"
      via: "type import FlowState + DupeContext"
      pattern: "import type \\{ FlowState"
---

<objective>
**The Phase 70 orchestrator rewrite.** Replace `src/components/watch/AddWatchFlow.tsx` end-to-end so the dormant Phase 66/67/68/69 primitives (SearchEntry, ConfirmStep, StructuredEntryPanel, ExtractErrorCard, WatchPhotoStep) come alive on `/watch/new` and ship the DUPE-01 / DUPE-02 / DUPE-03 / CLNP-06 user-observable behavior on top of the new FlowState union from Plan 04. This is the single largest task in Phase 70 — by design every other plan is the contracts this one consumes.

Purpose: Convert the legacy verdict-flow orchestrator into the v8.0 search-first orchestrator. After this plan lands, `/watch/new` renders SearchEntry as the primary path; clicking an owned search result redirects to `/w/[ref]` (no confirm screen); clicking a wishlist result confirms with the "Move to Collection" DupeBanner; "Add another copy" surfaces on owned-via-non-search paths; the URL-backup inline input replaces PasteSection; the CLNP-06 skip link transitions in-flow to manual-entry; the three-layer reset (Phase 29) handles back-nav cleanup without clobbering ?manual=1 deep-link state; photos-pending is gated on `status === 'owned'`.

Output: 1 file rewritten + 1 test file retrofitted (existing Phase 69 four-cache integration test PRESERVED; verdict-era tests REMOVED with explicit annotation in the SUMMARY; new Phase 70 transition tests added). Phase 71 follows with the static-guard cleanup + file deletions.

**This is the highest-LOC task in the phase; budget ~40-50% of an executor's context window.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@AGENTS.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-CONTEXT.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-RESEARCH.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-PATTERNS.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-UI-SPEC.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-VALIDATION.md

# Wave 0–1 SUMMARYs (read for the latest delta of contracts consumed):
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-01-SUMMARY.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-02-SUMMARY.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-03-SUMMARY.md
@.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-04-SUMMARY.md

# Current file being rewritten (read in full — 763 LOC):
@src/components/watch/AddWatchFlow.tsx
@src/components/watch/AddWatchFlow.test.tsx

# Surfaces being mounted (read prop contracts):
@src/components/watch/SearchEntry.tsx
@src/components/watch/ConfirmStep.tsx
@src/components/watch/StructuredEntryPanel.tsx
@src/components/watch/ExtractErrorCard.tsx
@src/components/watch/WatchPhotoStep.tsx
@src/components/watch/WatchForm.tsx
@src/components/watch/DupeBanner.tsx
@src/components/watch/flowTypes.ts
@src/components/watch/useUrlExtractCache.ts

# Server seams + page wire-up:
@src/app/actions/watches.ts
@src/data/watches.ts
@src/lib/watchFlow/destinations.ts
@src/app/watch/new/page.tsx
@src/lib/searchTypes.ts

<interfaces>
<!-- Locked contracts the orchestrator consumes. -->

FlowState union (Plan 04 — flowTypes.ts):
```typescript
export type FlowState =
  | { kind: 'search-idle' }
  | { kind: 'extracting-url'; url: string }
  | { kind: 'extraction-failed'; partial: ExtractedWatchData | null; reason: string; category: ExtractErrorCategory; mode: 'url' | 'structured' }
  | { kind: 'confirming'; catalogId: string | null; extracted: ExtractedWatchData; pickedResult: SearchCatalogWatchResult | null; dupeContext: DupeContext | null; pending: boolean }
  | { kind: 'form-prefill'; catalogId: string; extracted: ExtractedWatchData }
  | { kind: 'manual-entry'; partial?: ExtractedWatchData | null }
  | { kind: 'photos-pending'; watchId: string; destination: string }

export interface DupeContext { existingWatchId: string; existingStatus: 'owned' | 'wishlist'; existingReference: string | null }
```

SearchEntry props (Phase 69 D-03/D-11 — onSubmitStructured widened by Plan 01 Task 1):
```typescript
{ viewerUserId: string; catalogBrands: string[]; onPick: (result: SearchCatalogWatchResult) => void; onSubmitStructured: (result: ExtractedWatchData, catalogId: string | null) => void; onSwitchToUrl: () => void }
```

ConfirmStep props (Phase 68 D-03 LOCKED — orchestrator threads all of these):
- catalogImageUrl, extractedImageUrl, brand, model, reference, onReferenceChange, productionYear, onProductionYearChange, status, onStatusChange, price, onPriceChange, onPrimary, onEditDetails, onStartOver, pending, movement, caseSizeMm, dialColor

DupeBanner props (Plan 02):
```typescript
{ existingStatus: 'owned' | 'wishlist'; existingReference: string | null; onViewExisting: () => void; onMoveToCollection?: () => void; onAddAnotherCopy: () => void; pending?: boolean }
```

ExtractErrorCard mode prop (Phase 69 D-06):
```typescript
{ ..., mode?: 'url' | 'structured', retryAction, manualAction }
```

WatchPhotoStep props (Phase 61 PHOTO-09):
```typescript
{ watchId: string; userId: string; onDone: () => void; onSkip: () => void }
```

WatchForm.onWatchCreated (Plan 01 Task 3 widened):
```typescript
onWatchCreated?: (watchId: string, destination: string, status: WatchStatus) => void
```

findViewerWatchByCatalogId (Plan 01 Task 2 widened):
```typescript
findViewerWatchByCatalogId(userId, catalogId, statuses): Promise<{ id: string; status: 'owned' | 'wishlist'; reference: string | null } | null>
```

moveWishlistToCollection (Plan 03):
```typescript
moveWishlistToCollection(watchId: string, opts?: { pricePaid?: number; notes?: string }): Promise<ActionResult<Watch>>
```

addWatch (Phase 67 catalogId branch — consumed by handleConfirmPrimary):
```typescript
addWatch({...extracted, catalogId, status, pricePaid?, notes?}): Promise<ActionResult<Watch>>
```

Phase 28 helpers (src/lib/watchFlow/destinations.ts):
```typescript
defaultDestinationForStatus(status: WatchStatus, username: string | null): string
canonicalize(path: string, viewerUsername: string | null): string
validateReturnTo(value: unknown): string | null  // already gated at /watch/new/page.tsx
```

POST /api/extract-watch URL-backup body (Phase 66 D-08):
```typescript
{ mode: 'url', url: string }  // response: { data, catalogId?, catalogIdError?, error?, category?, mode: 'url' }
```

Hard-cutover REMOVED imports (Phase 71 deletes the files; Phase 70 stops referencing):
- `./PasteSection`, `./VerdictStep`, `./WishlistRationalePanel`, `./RecentlyEvaluatedRail`, `@/components/search/useWatchSearchVerdictCache`, `@/app/actions/verdict`, `@/lib/verdict/types`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite AddWatchFlow.tsx end-to-end per D-01..D-22</name>
  <files>src/components/watch/AddWatchFlow.tsx</files>
  <behavior>
    The rewrite must satisfy the contracts encoded by Plans 02–04's test suites PLUS render the correct branch tree per UI-SPEC §"Component Tree + Container Hierarchy" sections A–E. Specifically:
    - search-idle branch renders SearchEntry + CLNP-06 skip link in a `<div className="space-y-6">` container per UI-SPEC §C.
    - confirming branch renders DupeBanner (conditional on state.dupeContext) ABOVE ConfirmStep in a `<div className="space-y-6">` container per UI-SPEC §E.
    - extracting-url branch renders the inline URL input + "Find specs" button + "← Back to search" ghost link per UI-SPEC §B (NO PasteSection import).
    - manual-entry branch renders WatchForm with the new "← Cancel — return to search" back affordance per UI-SPEC §D.
    - form-prefill branch renders WatchForm with `lockedStatus="owned"` per existing pattern.
    - photos-pending branch renders WatchPhotoStep gated on `status === 'owned'` (handleConfirmPrimary applies the gate; manual-entry / form-prefill use the widened onWatchCreated to apply the gate).
    - extraction-failed branch renders ExtractErrorCard with the mode prop wired from state.mode.

    Handler behaviors:
    - handleSearchPick: owned + reference → router.push(`/w/${encodeURIComponent(result.reference)}`); owned + null reference → fall through to confirming with dupeContext from findViewerWatchByCatalogId; wishlist → confirming with dupeContext.existingStatus='wishlist'; null → confirming with dupeContext=null.
    - handleStructuredSubmit(extracted, catalogId): call findViewerWatchByCatalogId → setState confirming with dupeContext + pickedResult=null.
    - handleSwitchToUrl: setState({kind: 'extracting-url', url: ''}).
    - handleUrlBackup: cache check via useUrlExtractCache → if miss, POST /api/extract-watch with {mode:'url', url:trimmedUrl} → findViewerWatchByCatalogId on success → confirming; on failure → extraction-failed with mode='url'.
    - handleConfirmPrimary: addWatch({...state.extracted, catalogId: state.catalogId, status: confirmStatus, ...}) → on success: D-17 gate (status==='owned' → photos-pending; else → router.push(dest)); on failure: setState confirming with pending=false + toast.error.
    - handleConfirmEditDetails: setState({kind:'form-prefill', catalogId: state.catalogId, extracted: state.extracted}).
    - handleConfirmStartOver: setState({kind:'search-idle'}); setUrl('').
    - handleViewExisting: router.push(`/w/${encodeURIComponent(state.dupeContext.existingReference!)}`) when reference non-null (only called by DupeBanner when reference is non-null per Plan 02 conditional render).
    - handleMoveToCollection: setState confirming with pending=true → moveWishlistToCollection(state.dupeContext.existingWatchId, {pricePaid, notes}) → on success: toast.success + router.push(defaultDestinationForStatus('owned', viewerUsername)) per D-13; on failure: setState pending=false + toast.error.
    - handleAddAnotherCopy: setState({...confirming, dupeContext: null}) — clears banner, primary CTA stays addWatch(catalogId) per D-08.
    - handleSkipSearch (CLNP-06 link): setState({kind: 'manual-entry', partial: null}) with NO router.push per D-19.
    - handleWatchCreated(watchId, dest, status) — Plan 01 Task 3 widened signature; D-17 gate applied: status==='owned' → photos-pending; else → router.push(dest).
    - retryAction (extraction-failed): setState({kind:'search-idle'}); setUrl('').
    - manualAction (extraction-failed): router.push(`/watch/new?manual=1${initialReturnTo ? '&returnTo=' + encodeURIComponent(initialReturnTo) : ''}`).
  </behavior>
  <action>
    Full file rewrite. Read the current `src/components/watch/AddWatchFlow.tsx` (763 LOC) ONCE to extract the patterns being preserved (Activity-hide useLayoutEffect at 199-215, the extractedToPartialWatch helper at ~705-730, the WatchForm prop construction in form-prefill / manual-entry branches at ~605-645). Do NOT re-read the file for "one more thing" — use Grep with specific patterns instead.

    Then write the new file using PATTERNS.md "src/components/watch/AddWatchFlow.tsx (orchestrator component — full rewrite)" as the verbatim guide. PATTERNS.md provides:
    - The new imports block (removes 6 imports, adds 5)
    - The initialState ternary (third branch renamed `'idle'` → `'search-idle'`)
    - The state declarations (removes useWatchSearchVerdictCache + hasCollection; keeps useUrlExtractCache)
    - The useLayoutEffect cleanup with the 3 skip cases per D-22
    - The handleUrlBackup pattern (extracted from current handleExtract; verdict-compute branch REMOVED)
    - handleStartOver / retryAction / manualAction (preserved verbatim with idle→search-idle rename)
    - handleWatchCreated with D-17 gate (consumes Plan 01 Task 3's widened signature)
    - The manual-entry back affordance copy update (D-20)
    - The CLNP-06 skip link pattern (D-19)
    - The photos-pending render branch (reset to search-idle on Done/Skip)
    - The extractedToPartialWatch helper (no change — keep verbatim)

    Specific implementation requirements beyond what PATTERNS.md spells out:

    (1) **`handleSearchPick`** (NEW — wrap in `useCallback`, deps `[router, viewerUserId]`):
    ```
    if (result.viewerState === 'owned') {
      if (result.reference) {
        router.push(`/w/${encodeURIComponent(result.reference)}`)
        return
      }
      // D-06 null-reference fallback — fall through to confirming with owned banner
      console.warn('[Phase 70] dupeContext: owned existing → confirm-with-banner (null reference fallback)')
      const dupeRow = await findViewerWatchByCatalogId(viewerUserId, result.catalogId, ['owned', 'wishlist'])
      const dupeContext = dupeRow ? { existingWatchId: dupeRow.id, existingStatus: dupeRow.status, existingReference: dupeRow.reference } : null
      setState({ kind: 'confirming', catalogId: result.catalogId, extracted: searchResultToExtracted(result), pickedResult: result, dupeContext, pending: false })
      return
    }
    // wishlist or null:
    if (result.viewerState === 'wishlist') {
      console.warn('[Phase 70] dupeContext: wishlist existing → move-to-collection affordance')
    }
    const dupeRow = result.viewerState ? await findViewerWatchByCatalogId(viewerUserId, result.catalogId, ['owned', 'wishlist']) : null
    const dupeContext = dupeRow ? { existingWatchId: dupeRow.id, existingStatus: dupeRow.status, existingReference: dupeRow.reference } : null
    setState({ kind: 'confirming', catalogId: result.catalogId, extracted: searchResultToExtracted(result), pickedResult: result, dupeContext, pending: false })
    ```
    Note: `searchResultToExtracted(result)` is a small helper that maps `SearchCatalogWatchResult` (`{brand, model, reference, imageUrl, ...}`) into the partial `ExtractedWatchData` shape ConfirmStep expects. If the shapes are largely overlapping, this is a 5-10 line mapping; otherwise inline the field-by-field construction.

    (2) **`handleStructuredSubmit(extracted, catalogId)`** (consumes Plan 01 Task 1's widened emit):
    ```
    const dupeRow = catalogId ? await findViewerWatchByCatalogId(viewerUserId, catalogId, ['owned', 'wishlist']) : null
    const dupeContext = dupeRow ? { existingWatchId: dupeRow.id, existingStatus: dupeRow.status, existingReference: dupeRow.reference } : null
    if (dupeContext) console.warn(`[Phase 70] dupeContext: ${dupeContext.existingStatus} existing → ${dupeContext.existingStatus === 'owned' ? 'confirm-with-banner' : 'move-to-collection affordance'}`)
    setState({ kind: 'confirming', catalogId, extracted, pickedResult: null, dupeContext, pending: false })
    ```

    (3) **`handleConfirmPrimary`** (PRIMARY commit handler — see PATTERNS.md for the addWatch call shape):
    - Set state to confirming with pending=true (preserve catalogId/extracted/pickedResult/dupeContext).
    - Build the addWatch payload from state.extracted + state.catalogId + the ConfirmStep-controlled status/price/reference/year (these are local React state in AddWatchFlow — `const [confirmStatus, setConfirmStatus] = useState<WatchStatus>(initialStatus ?? 'wishlist')` per D-21; same for confirmPrice, confirmReference, confirmYear).
    - Call addWatch — on success: D-17 gate (status === 'owned' → setState photos-pending; else → setState search-idle + router.push(initialReturnTo ?? defaultDestinationForStatus(confirmStatus, viewerUsername))).
    - On failure: setState confirming with pending=false; toast.error(result.error).
    - DUPE-02 owned context: if dupeContext.existingStatus === 'owned', no special handling needed at commit time — addWatch with the same catalogId creates a second row (no UNIQUE constraint per RESEARCH §D-08).

    (4) **`handleMoveToCollection`** (DUPE-03 commit — consumes Plan 03):
    - Guard: state.kind === 'confirming' && state.dupeContext && state.dupeContext.existingStatus === 'wishlist'.
    - Set confirming.pending = true.
    - Call `moveWishlistToCollection(state.dupeContext.existingWatchId)` (opts omitted — wishlist watches typically have no pricePaid at the wishlist stage; if the user wants to set a price, they can do that on the watch detail page after moving).
    - On success: toast.success('Moved to collection', { action: { label: 'View', onClick: () => router.push(dest) } }) per D-13; router.push(initialReturnTo ?? defaultDestinationForStatus('owned', viewerUsername)); setState search-idle.
    - On failure: setState confirming with pending=false; toast.error(result.error).
    - No photos-pending step per D-13.

    (5) **`handleAddAnotherCopy`** (D-08):
    - Guard: state.kind === 'confirming'.
    - setState({...state, dupeContext: null, pending: false}) — clears dupeContext only; ConfirmStep stays mounted with same fields.

    (6) **`handleViewExisting`** (DupeBanner.onViewExisting — called only when dupeContext.existingReference is non-null per Plan 02's conditional render):
    - router.push(`/w/${encodeURIComponent(state.dupeContext.existingReference!)}`)
    - The `!` non-null assertion is safe because Plan 02's DupeBanner hides the "View existing" button when existingReference is null — this handler is unreachable in the null case.

    (7) **`handleSkipSearch`** (CLNP-06 link — D-19):
    - setState({ kind: 'manual-entry', partial: null })
    - NO router.push. URL stays at /watch/new.

    (8) **Imports — remove the legacy block + add the new block per PATTERNS.md.** Specifically:
    - REMOVE: `PasteSection`, `VerdictStep`, `WishlistRationalePanel`, `RecentlyEvaluatedRail`, `useWatchSearchVerdictCache`, `getVerdictForCatalogWatch`, `VerdictBundle`.
    - ADD: `SearchEntry`, `ConfirmStep`, `DupeBanner`, `findViewerWatchByCatalogId`, `moveWishlistToCollection`, `SearchCatalogWatchResult` (type-only), `DupeContext` (type-only).
    - KEEP: `WatchForm`, `WatchPhotoStep`, `ExtractErrorCard`, `useUrlExtractCache`, `addWatch`, `canonicalize`, `defaultDestinationForStatus`, `FlowState`, `RailEntry`, `ExtractedWatchData`, `Watch`, `MovementType`, `WatchStatus`, `useCallback` / `useEffect` / `useLayoutEffect` / `useRef` / `useState` from React, `useRouter` from `next/navigation`, `toast` from `sonner`, `Loader2` from `lucide-react`, `Button` + `Input` from `@/components/ui/`.

    (9) **JSX render branches — match UI-SPEC §C/B/E/D verbatim**:
    - search-idle: `<div className="space-y-6"><SearchEntry .../><button onClick={handleSkipSearch} className="...">Skip search — enter manually</button></div>`
    - extracting-url: per UI-SPEC §B — `<div className="space-y-4" aria-live="polite">` containing back-link button, Input, Find-specs Button with Loader2 swap.
    - confirming: per UI-SPEC §E — `<div className="space-y-6">` containing conditional DupeBanner + ConfirmStep (with all 12 LOCKED props threaded).
    - form-prefill: existing WatchForm pattern with `lockedStatus="owned"`, `onWatchCreated={handleWatchCreated}` (now 3-arg).
    - manual-entry: existing WatchForm pattern with the back-affordance label updated per D-20, `defaultStatus={initialStatus ?? undefined}`, `onWatchCreated={handleWatchCreated}`.
    - photos-pending: existing WatchPhotoStep pattern (Done/Skip transition to search-idle + router.push).
    - extraction-failed: ExtractErrorCard with `mode={state.mode}` wired (Phase 69 D-06).

    (10) **`useLayoutEffect` cleanup per D-22**:
    - Skip case 1: `s.kind === 'search-idle' && urlRef.current === '' && railRef.current.length === 0`
    - Skip case 2: `s.kind === 'form-prefill'`
    - Skip case 3 (NEW): `s.kind === 'manual-entry' && s.partial === null && initialManual === true`
    - Otherwise reset: `setState({kind: 'search-idle'}); setUrl(''); setRail([])`
    - DO NOT clear module-scope caches (Phase 69 CLNP-07 handles cross-user reset).

    (11) **Stable callbacks**: wrap handlers threaded as props to SearchEntry / ConfirmStep / DupeBanner / ExtractErrorCard / WatchForm / WatchPhotoStep with `useCallback` to prevent identity churn / effect loops downstream (Phase 25 T-25-04-04 pattern; PATTERNS.md "useCallback for handlers" shared pattern). At minimum: handleSearchPick, handleStructuredSubmit, handleSwitchToUrl, handleUrlBackup, handleConfirmPrimary, handleConfirmEditDetails, handleConfirmStartOver, handleViewExisting, handleMoveToCollection, handleAddAnotherCopy, handleSkipSearch, handleWatchCreated, retryAction, manualAction.

    (12) **AGENTS.md reminder**: Next.js 16 App Router. Before writing any code that touches `useRouter` / `router.push` / Server Action invocation patterns / fetch responses, verify against `node_modules/next/dist/docs/` if anything looks off. The existing AddWatchFlow.tsx is a known-working analog — the rewrite preserves the same `useRouter` + Server Action patterns.

    (13) **Guardrails — re-affirm per additional context**:
    - `font-semibold` NOT `font-medium` on plain text elements (the extracting-url branch and any new copy in AddWatchFlow follow this).
    - No console.warn / console.error usage beyond the 2 operator telemetry lines specified in (1) and (2) (and any existing error-path logging in handleUrlBackup that mirrors the current handleExtract).
    - No useTransition for moveWishlistToCollection (per CONTEXT Claude's Discretion).
    - No router.push to ?manual=1 from CLNP-06 link (D-19 LOCKED).

    Sanity check: after writing, verify LOC count. PATTERNS.md predicts net LOC reduction (~250-300 LOC of verdict-flow removed, ~100 LOC of new orchestrator gained). If the rewrite INCREASES total LOC, scope creep is likely — revisit handler structure to see if duplication can be consolidated (useCallback factories, shared dupeContext-resolver helper, etc.).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "AddWatchFlow|flowTypes|DupeBanner|moveWishlistToCollection" | head -20 && echo "---LOC---" && wc -l src/components/watch/AddWatchFlow.tsx</automated>
  </verify>
  <done>
    src/components/watch/AddWatchFlow.tsx compiles without TypeScript errors specific to this file. The grep on tsc output shows no errors from AddWatchFlow.tsx, flowTypes.ts, DupeBanner.tsx, or moveWishlistToCollection (any output from those grep targets MUST be a pre-existing baseline failure unrelated to Phase 70 per memory `project_baseline_not_green_build_is_gate`). LOC reports < 763 (net reduction confirmed per CONTEXT Claude's Discretion sanity check). The Task 2 test retrofit will close the verification loop.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retrofit AddWatchFlow.test.tsx — preserve Phase 69 four-cache test + remove verdict-era cases + add Phase 70 transitions</name>
  <files>src/components/watch/AddWatchFlow.test.tsx</files>
  <behavior>
    The retrofitted test file must contain exactly these test groups:
    - **KEEP** the `describe('Phase 69 — cache hygiene integration (CLNP-07)', ...)` block at lines ~699-786 verbatim — this is the four-cache integration test the additional context says MUST stay.
    - **REMOVE** every verdict-era test:
      - `describe('Phase 20.1 Plan 04 — AddWatchFlow state machine', ...)` at lines ~63-410 — all 7 cases reference VerdictBundleFull / getVerdictForCatalogWatch / verdict-ready FlowState; the entire union those tests target is gone.
      - `describe('Phase 20.1 gap-closure — Plan 06', ...)` at lines ~411-575 — all 3 cases reference verdict-flow.
      - `describe('Phase 20.1 gap-closure — Plan 08 ...', ...)` at lines ~576-698 — references the old idle-state back-link and ExtractErrorCard CTAs in the old union shape.
    - **ADD** a new `describe('Phase 70 — AddWatchFlow orchestrator state machine', ...)` block with the following test cases:
      - (T-70-01) DUPE-01 owned-pick with reference → router.push called with `/w/REF-001` and no confirm screen rendered.
      - (T-70-02) DUPE-01 owned-pick with null reference → fall-through to confirming with dupeContext.existingStatus='owned' and DupeBanner mounted (assert via mock-DupeBanner data-testid).
      - (T-70-03) DUPE-02 banner — structured-submit on owned existing → confirming with DupeBanner-owned mounted; clicking "Add another copy" clears dupeContext (DupeBanner unmounts; ConfirmStep stays).
      - (T-70-04) DUPE-03 wishlist pick → confirming with DupeBanner-wishlist mounted; clicking "Move to Collection" calls moveWishlistToCollection with the existing watchId; on success router.push to `/u/tester/collection`.
      - (T-70-05) CLNP-06 skip link → setState manual-entry; router.push NOT called (URL stays at /watch/new).
      - (T-70-06) URL-backup branch (handleUrlBackup) — onSwitchToUrl from SearchEntry → extracting-url branch renders inline Input + "Find specs" button; on Find-specs click, fetch called with {mode:'url', url:'...'}; on success → confirming.
      - (T-70-07) photos-pending gate (D-17) — confirming with status='wishlist' commit → router.push directly to /u/tester/wishlist (NO photos-pending step); confirming with status='owned' commit → photos-pending FlowState renders WatchPhotoStep.
      - (T-70-08) initialState precedence (D-03) — initialCatalogId+initialIntent='owned'+initialCatalogPrefill → form-prefill; only initialManual=true → manual-entry; neither → search-idle.

    Test infrastructure:
    - Mock SearchEntry, ConfirmStep, DupeBanner, StructuredEntryPanel, WatchPhotoStep, ExtractErrorCard, WatchForm via `vi.mock(...)` factories per PATTERNS.md "AddWatchFlow.test.tsx (retrofit)" section — pure-presenter children are replaced with `data-testid` divs exposing buttons that fire the callbacks for transition observation. This isolates orchestrator-level transition logic from child behavior (Phase 68/69 already tested the children).
    - Mock `@/app/actions/watches` with `{ addWatch: vi.fn(), moveWishlistToCollection: vi.fn() }`.
    - Mock `@/data/watches` with `findViewerWatchByCatalogId: vi.fn()` resolving to null by default; per-test mockResolvedValue for dupe cases.
    - Mock `next/navigation` with `useRouter: () => ({ push: vi.fn(), refresh: vi.fn(), back: vi.fn() })` — capture the push mock for assertion.
    - Mock `next/cache` revalidatePath/Tag (not consumed directly by orchestrator but kept for safety).
    - Mock `global.fetch` for the URL-backup test.

    Phase 69 four-cache test boundary:
    - It already uses the older mock shape; PRESERVE its imports + describe block verbatim. The orchestrator rewrite should keep the four cache hooks consumable from the same import paths (`./useUrlExtractCache`, `./useCatalogSearchCache`, `./useStructuredExtractCache`, `@/components/search/useWatchSearchVerdictCache`). The new orchestrator only consumes useUrlExtractCache directly; the other three are consumed by SearchEntry / StructuredEntryPanel — but the integration test asserts via the in-render reset chain, which still works because Phase 69 CLNP-07's `lastUserId` mismatch reset is module-level (not consumer-instance level).
  </behavior>
  <action>
    Retrofit `src/components/watch/AddWatchFlow.test.tsx` in three coordinated passes:

    (a) **DELETE the three verdict-era describe blocks** (lines ~63-698 in the current file). The cleanest approach is to physically remove every line from the start of `describe('Phase 20.1 Plan 04 — AddWatchFlow state machine', ...)` through the last `})` of `describe('Phase 20.1 gap-closure — Plan 08 ...', ...)` immediately before the Phase 69 cache hygiene describe block. Also delete the imports that ONLY those tests use: `VerdictBundleFull` from `@/lib/verdict/types`, `getVerdictForCatalogWatch` from `@/app/actions/verdict`, any verdict-related vi.mock factories at the top of the file. Audit the remaining imports — anything not consumed by either the new Phase 70 describe block OR the preserved Phase 69 cache hygiene block must go.

    (b) **PRESERVE** the `describe('Phase 69 — cache hygiene integration (CLNP-07)', ...)` block + its `beforeEach` + `afterEach` setup verbatim. Verify by post-edit grep that the four-cache reset assertion (`__resetCatalogSearchCacheForTests`, `__resetStructuredExtractCacheForTests`, `__resetUrlExtractCacheForTests`, `__resetVerdictCacheForTests`) is still present and the test still passes.

    (c) **ADD** the new `describe('Phase 70 — AddWatchFlow orchestrator state machine', ...)` block with the 8 cases per `<behavior>`. Use the PATTERNS.md "AddWatchFlow.test.tsx (retrofit)" mock factories verbatim:
    ```
    vi.mock('@/components/watch/SearchEntry', () => ({ SearchEntry: ({ onPick, onSubmitStructured, onSwitchToUrl }: ...) => ( ... data-testid + 5 buttons ... ) }))
    vi.mock('@/components/watch/ConfirmStep', () => ({ ConfirmStep: ({ onPrimary, onStartOver, onEditDetails, pending }: ...) => ( ... data-testid="confirm-step" + 3 buttons ... ) }))
    vi.mock('@/components/watch/DupeBanner', () => ({ DupeBanner: ({ onMoveToCollection, onAddAnotherCopy, existingStatus }: ...) => ( ... data-testid=`dupe-banner-${existingStatus}` + 2 buttons ... ) }))
    vi.mock('@/components/watch/WatchPhotoStep', () => ({ WatchPhotoStep: ({ onDone, onSkip }: ...) => ( ... data-testid="photos-pending" + 2 buttons ... ) }))
    vi.mock('@/components/watch/WatchForm', () => ({ WatchForm: ({ onWatchCreated }: ...) => ( ... data-testid="watch-form" + 1 button that fires onWatchCreated('w-id', '/u/tester/wishlist', 'wishlist') ... ) }))
    vi.mock('@/components/watch/ExtractErrorCard', () => ({ ExtractErrorCard: ({ retryAction, manualAction, mode }: ...) => ( ... data-testid={`extract-error-${mode}`} + 2 buttons ... ) }))
    vi.mock('@/app/actions/watches', () => ({ addWatch: vi.fn(), moveWishlistToCollection: vi.fn() }))
    vi.mock('@/data/watches', () => ({ findViewerWatchByCatalogId: vi.fn().mockResolvedValue(null) }))
    ```

    For each Phase 70 test case:
    - Render via the `renderFlow(overrides)` helper from PATTERNS.md (updated prop set per Plan 04's FlowState shape).
    - Trigger the transition via the appropriate mock-child button (`fireEvent.click(screen.getByText('Pick owned'))` etc.).
    - Assert the next branch's data-testid is present (`screen.getByTestId('confirm-step')` etc.) AND assert side-effects (router.push args, fetch call body, action call args).
    - For async transitions (handleUrlBackup, handleSearchPick with findViewerWatchByCatalogId): wrap in `await waitFor(() => ...)`.

    Plan 01 Task 1's widened onSubmitStructured signature: the SearchEntry mock's "Submit structured" button must fire `onSubmitStructured({brand:'Omega', model:'Speedmaster'}, 'cat-uuid')` (two args). Verify the mock factory passes two args; if the test was written before Wave 0 landed and uses one arg, update accordingly.

    Plan 01 Task 3's widened onWatchCreated signature: the WatchForm mock fires `onWatchCreated('w-id', '/u/tester/wishlist', 'wishlist')` (three args) so the D-17 gate test (T-70-07 wishlist branch) can verify the gate fires correctly.

    Cleanup gate:
    - After retrofit, run `grep -n "verdict-ready\|wishlist-rationale-open\|submitting-wishlist\|submitting-collection\|VerdictBundle\|getVerdictForCatalogWatch" src/components/watch/AddWatchFlow.test.tsx` — expect 0 matches. If matches remain, they reference dead code paths and must be deleted.
  </action>
  <verify>
    <automated>npx vitest run src/components/watch/AddWatchFlow.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>
    AddWatchFlow.test.tsx — all Phase 69 four-cache test cases STILL green + all 8 new Phase 70 transition test cases green. The verdict-era describe blocks are deleted; `grep -c "verdict-ready\|VerdictBundle\|getVerdictForCatalogWatch" src/components/watch/AddWatchFlow.test.tsx | head -1` reports 0. Phase 70 describe block exists: `grep -n "Phase 70 — AddWatchFlow orchestrator" src/components/watch/AddWatchFlow.test.tsx` matches.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-level build gate + SC#1-5 trace</name>
  <files>(no edits — verification only)</files>
  <action>
    This task is THE phase-level build gate per CONTEXT.md `project_baseline_not_green_build_is_gate` + VALIDATION.md sampling rate ("Per plan wave: Run npm run build").

    (a) Run `npm run build` and confirm exit 0. Any TypeScript error pointing to AddWatchFlow.tsx, flowTypes.ts, DupeBanner.tsx, moveWishlistToCollection, StructuredEntryPanel.tsx, SearchEntry.tsx, WatchForm.tsx, or findViewerWatchByCatalogId is a Phase 70 regression and must be fixed. Errors in unrelated files are pre-existing baseline noise per memory.

    (b) Run the full Phase 70 test set:
    - `npx vitest run src/components/watch/flowTypes.test.ts src/components/watch/DupeBanner.test.tsx src/components/watch/AddWatchFlow.test.tsx src/components/watch/StructuredEntryPanel.test.tsx src/components/watch/SearchEntry.test.tsx src/components/watch/WatchForm.lockedStatus.test.tsx src/app/actions/__tests__/moveWishlistToCollection.test.ts src/app/actions/__tests__/watches.test.ts tests/no-raw-palette.test.ts`
    - Every test in the set must be green (any pre-existing red is documented for the SUMMARY).

    (c) Phase-level SC#1-5 trace verification:
    - SC#1 — owned redirect: T-70-01 unit test in Task 2 PASS confirms; prod UAT per VALIDATION.md "Manual-Only Verifications" row 1.
    - SC#1 — wishlist DupeBanner + Move to Collection: T-70-04 unit test + Plan 03 unit tests PASS confirm; prod UAT row 3.
    - SC#2 — Add another copy: T-70-03 unit test PASS confirms; prod UAT row 2.
    - SC#3 — `?manual=1` priority: T-70-08 unit test PASS confirms; prod UAT row 5.
    - SC#4 — `?returnTo=` round-trip: T-70-08 + manual UAT row 6.
    - SC#5 — three-layer reset extension (no cache poisoning across sessions): Phase 69 four-cache test (preserved) + manual UAT row 7. D-22 useLayoutEffect cleanup verified via TypeScript exhaustiveness in Plan 04 + the new state-name guards in Task 1's cleanup function.

    (d) Hard-cutover assertion (forward-coordination for Phase 71):
    - `grep -nE "^import.*from.*('./PasteSection'|'./VerdictStep'|'./WishlistRationalePanel'|'./RecentlyEvaluatedRail'|useWatchSearchVerdictCache|@/app/actions/verdict|@/lib/verdict/types)" src/components/watch/AddWatchFlow.tsx | wc -l`
    - Expected: 0. Any hit means a removed-import slipped through; revisit Task 1.

    (e) Surface any prod-UAT items for the orchestrator-level VERIFICATION.md hook (see Output section).

    Per `feedback_ppr_cache_fill_no_longer_call_out` memory: do NOT bake soft-nav #419 or cache-fill checks into the build gate or UAT scripts. Per `feedback_mobile_ui_verify_on_prod` memory: bundle Phase 70 + Phase 71 push for a single prod UAT session.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && echo "---TARGETED TESTS---" && npx vitest run src/components/watch/flowTypes.test.ts src/components/watch/DupeBanner.test.tsx src/components/watch/AddWatchFlow.test.tsx src/app/actions/__tests__/moveWishlistToCollection.test.ts tests/no-raw-palette.test.ts 2>&1 | tail -15 && echo "---HARD CUTOVER---" && grep -nE "^import.*('./PasteSection'|'./VerdictStep'|'./WishlistRationalePanel'|'./RecentlyEvaluatedRail'|useWatchSearchVerdictCache|@/app/actions/verdict|@/lib/verdict/types)" src/components/watch/AddWatchFlow.tsx | wc -l</automated>
  </verify>
  <done>
    `npm run build` exits 0. All targeted Phase 70 tests green. Hard-cutover grep reports 0 (no legacy imports remain in AddWatchFlow.tsx). The Phase 69 four-cache integration test is still green. Plans 02–04's contracts are honored by the orchestrator (DupeBanner mounted with correct props, ConfirmStep mounted with all 12 LOCKED props, FlowState transitions match the D-02 map, moveWishlistToCollection called with the existing watchId on DUPE-03 commit). The phase is ready for `/gsd-verify-work` to run the SC#1-5 trace + manual prod UAT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (SearchEntry pick) → orchestrator handleSearchPick | The `result.viewerState` flag from SearchEntry is a client-side hint; the orchestrator re-verifies via server-side findViewerWatchByCatalogId for the structured + URL-backup paths (T-70-04 mitigation per additional context) |
| Client → /api/extract-watch (URL-backup) | URL is user-supplied; the API route's existing validation (Phase 25 / Phase 66) gates protocol allow-list (http/https only) and rejects empty bodies via Zod |
| Client → moveWishlistToCollection Server Action | watchId is client-supplied; Plan 03's auth-first gate + Zod uuid + DAL ownership check (T-70-01) is the IDOR mitigation |
| Client → router.push('/w/[ref]') | reference is server-authoritative (from SearchCatalogWatchResult.reference or DupeContext.existingReference which Plan 01 Task 2's DAL JOIN reads from watches_catalog) — never client-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Elevation of Privilege | DupeBanner.onMoveToCollection → moveWishlistToCollection IDOR | mitigate | Plan 03 owns the Server Action gate; this orchestrator passes the watchId from state.dupeContext.existingWatchId which was server-resolved via findViewerWatchByCatalogId(viewerUserId, ...) — already viewer-scoped at lookup time. |
| T-70-02 | Tampering | Double-click on DupeBanner buttons | mitigate | Orchestrator sets confirming.pending=true before await; ConfirmStep + DupeBanner both receive pending and disable buttons. Plan 03's idempotent already-owned branch is the server-side defense in depth. |
| T-70-03 | Tampering | Status whitelist bypass | mitigate | The DupeBanner only renders "Move to Collection" when dupeContext.existingStatus === 'wishlist' (Plan 02 conditional render); the orchestrator only constructs that state value when findViewerWatchByCatalogId returns status='wishlist' (server-authoritative). Plan 03's status whitelist is the defense in depth. |
| T-70-04 | Spoofing | Stale dupeContext from client-supplied SearchEntry viewerState | mitigate | For structured-input + URL-backup paths the orchestrator re-verifies via findViewerWatchByCatalogId server-side (NOT trusting the client viewerState). For search-pick paths the viewerState is informational — the actual /w/[ref] redirect uses result.reference (server-source from the catalog row via Phase 67 search action). |
| T-70-05 | Information Disclosure | console.warn telemetry surfaces dupeContext to browser console | accept | Operator visibility per CONTEXT Claude's Discretion. No PII — just the dupeContext.existingStatus discriminator. Can remove if noisy in prod (see Deferred Ideas — telemetry threshold). |
</threat_model>

<verification>
**Phase-level VERIFICATION.md hook (for /gsd-verify-work SC#1-5 trace):**

| Phase SC | Plan tracing | Automated verification |
|----------|--------------|------------------------|
| **SC#1** (owned-pick → /w/[ref]; wishlist-pick → DupeBanner + Move to Collection UPDATE) | Plan 05 Task 1 + Task 2 (T-70-01, T-70-02, T-70-04); Plan 03 (6-case unit suite) | `npx vitest run src/components/watch/AddWatchFlow.test.tsx src/app/actions/__tests__/moveWishlistToCollection.test.ts` |
| **SC#2** (Add another copy on confirm) | Plan 05 Task 2 (T-70-03); Plan 02 (DupeBanner.test.tsx case (b) wishlist context renders Add another copy) | `npx vitest run src/components/watch/AddWatchFlow.test.tsx src/components/watch/DupeBanner.test.tsx` |
| **SC#3** (`?manual=1` priority) | Plan 05 Task 1 (initialState ternary preserved per D-03); Task 2 (T-70-08) | `npx vitest run src/components/watch/AddWatchFlow.test.tsx -t "T-70-08"` |
| **SC#4** (`?returnTo=` round-trip) | Plan 05 Task 1 (every commit branch reads initialReturnTo per D-04); manual UAT row 6 of VALIDATION.md | `grep -nE "initialReturnTo\s*\?\?\s*defaultDestinationForStatus" src/components/watch/AddWatchFlow.tsx` (must match in every commit branch) |
| **SC#5** (three-layer reset extends to new caches) | Plan 04 (FlowState union shipped); Plan 05 Task 1 D-22 cleanup; Phase 69 four-cache test PRESERVED in Task 2 | `npx vitest run src/components/watch/AddWatchFlow.test.tsx -t "Phase 69"` |

**Manual UAT (per VALIDATION.md "Manual-Only Verifications"):**
8 prod UAT rows covering DUPE-01 redirect, DUPE-02 second-copy commit, DUPE-03 UPDATE-not-INSERT, CLNP-06 in-flow transition, ?manual=1 priority, ?returnTo= round-trip, three-layer reset cache hygiene, D-17 photos-pending gate. Per `feedback_mobile_ui_verify_on_prod`: bundle with Phase 71 push for single prod UAT session. Per `feedback_ppr_cache_fill_no_longer_call_out`: do NOT bake #419 / soft-nav into UAT.

**Forward-coordination signal for Phase 71:**
Hard cutover complete — `grep -E "PasteSection|VerdictStep|WishlistRationalePanel|RecentlyEvaluatedRail" src/components/watch/AddWatchFlow.tsx` reports 0 hits (excluding any incidental comment-prose mentions). Phase 71 can safely delete the source files + add the static guards.
</verification>

<success_criteria>
- src/components/watch/AddWatchFlow.tsx rewritten end-to-end per D-01..D-22.
- Net LOC reduction confirmed (PATTERNS.md predicts ~250-300 LOC removed of verdict-flow; ~100 LOC added of new orchestrator handlers; file should be smaller than current 763 LOC).
- src/components/watch/AddWatchFlow.test.tsx retrofitted: Phase 69 four-cache test PRESERVED + 8 new Phase 70 transition tests added + verdict-era tests REMOVED.
- `npm run build` exits 0 — the authoritative gate per memory.
- All Phase 70 targeted test files green (flowTypes.test.ts, DupeBanner.test.tsx, AddWatchFlow.test.tsx, moveWishlistToCollection.test.ts, StructuredEntryPanel.test.tsx, SearchEntry.test.tsx, WatchForm.lockedStatus.test.tsx).
- `tests/no-raw-palette.test.ts` green (no font-medium recurrence anywhere in Phase 70 surfaces).
- Hard-cutover verified: 0 imports of PasteSection / VerdictStep / WishlistRationalePanel / RecentlyEvaluatedRail / useWatchSearchVerdictCache / @/app/actions/verdict / @/lib/verdict/types in AddWatchFlow.tsx.
- All 22 D-NN locked decisions implemented (cite-trace in SUMMARY).
- Threat surface mitigated: T-70-01 (IDOR), T-70-02 (double-click), T-70-03 (status whitelist), T-70-04 (stale dupeContext).
- VERIFICATION.md SC#1-5 hook table populated above; `/gsd-verify-work` can trace plan-level coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-05-SUMMARY.md` summarizing:
- The new AddWatchFlow.tsx LOC count + the LOC delta vs the prior 763 LOC.
- Each of the 22 D-NN locked decisions and how the rewrite implements it (one bullet per D-NN with the file:line citation in the new code).
- The 8 new Phase 70 transition test cases + the explicit removal of the verdict-era describe blocks.
- The Phase 69 four-cache test preservation status (confirmed green).
- The hard-cutover grep result (0 legacy imports).
- The npm run build exit status and any pre-existing baseline noise encountered.
- The threat model mitigation status for T-70-01..T-70-05.
- The forward-coordination signal for Phase 71 (which files Phase 71 can now safely delete; which static guards Phase 71 will add to lock the absence).
- Any sanity-check observations (LOC trend, useCallback wrapping coverage, console.warn telemetry presence).
- The prod UAT readiness signal (8 manual rows from VALIDATION.md ready for the bundled Phase 70+71 prod push).
</output>
