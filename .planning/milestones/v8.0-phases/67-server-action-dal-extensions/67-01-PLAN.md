---
phase: 67
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/watches.ts
  - tests/data/findViewerWatchByCatalogId.test.ts
autonomous: true
requirements:
  - DUPE-01
  - DUPE-03
tags:
  - dal
  - drizzle
  - watches
  - dupe-detection

must_haves:
  truths:
    - "findViewerWatchByCatalogId accepts an optional third parameter `statuses?: ('owned' | 'wishlist')[]` defaulting to `['owned']` (D-06) — backward compatible for the one existing caller (/w/[ref]/page.tsx:439) which passes no statuses arg"
    - "findViewerWatchByCatalogId returns `{ id: string; status: 'owned' | 'wishlist' } | null` instead of `{ id: string } | null` (D-07) — `status` is the new field surfaced for Phase 70 DUPE-01 vs DUPE-03 branching"
    - "When BOTH an owned row AND a wishlist row exist for the same (userId, catalogId), the function returns the owned row (D-08 owned-wins precedence) — ORDER BY uses CASE status WHEN 'owned' THEN 0 WHEN 'wishlist' THEN 1 ELSE 2 END, then desc(createdAt) (D-05 carry-forward)"
    - "Default invocation `findViewerWatchByCatalogId(userId, catalogId)` returns owned-only results — BUG-01 contract preserved for the existing /w/[ref] page caller"
    - "tests/data/findViewerWatchByCatalogId.test.ts ships with 5 unit-test cases covering: (a) owned-only → owned returned; (b) wishlist-only with both statuses → wishlist returned; (c) both rows with both statuses → owned returned (D-08); (d) no rows → null; (e) default invocation → owned-only (BUG-01 backward compat) — all green via the chainable-Drizzle mock pattern from tests/data/searchCatalogWatches.test.ts"
  artifacts:
    - path: "src/data/watches.ts"
      provides: "Extended findViewerWatchByCatalogId DAL function with statuses parameter and widened return type"
      contains: "statuses: ('owned' | 'wishlist')[] = ['owned']"
    - path: "tests/data/findViewerWatchByCatalogId.test.ts"
      provides: "5-case unit test suite covering D-06/D-07/D-08 contract"
      contains: "describe('findViewerWatchByCatalogId"
  key_links:
    - from: "src/data/watches.ts:295 findViewerWatchByCatalogId"
      to: "src/app/w/[ref]/page.tsx:439 caller"
      via: "structural-superset return type (existing caller reads only .id)"
      pattern: "viewerOwnedRow\\.id"
---

<objective>
Extend `findViewerWatchByCatalogId` (`src/data/watches.ts:295-317`) in place per CONTEXT.md decisions D-06, D-07, D-08 so Phase 70 can branch DUPE-01 (owned → redirect to `/w/[ref]`) and DUPE-03 (wishlist → confirm screen with "Move to Collection" affordance) from a single DAL read without a second query.

**Purpose:** Phase 70's UI cannot tell whether a catalog row the viewer already has is `owned` or `wishlist` without surfacing `status` from this helper. Today the function is owned-only (BUG-01 fix) and returns `{ id } | null`. The widening (statuses param + status field) lets one DAL call serve both DUPE branches while keeping the one existing caller (`/w/[ref]/page.tsx:439`) untouched (it only reads `.id`).

**Output:** Modified `src/data/watches.ts` + new `tests/data/findViewerWatchByCatalogId.test.ts` covering the five contract cases from CONTEXT.md Claude's Discretion §"Unit test layering for the DAL helper".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/67-server-action-dal-extensions/67-CONTEXT.md
@.planning/phases/67-server-action-dal-extensions/67-RESEARCH.md
@.planning/phases/67-server-action-dal-extensions/67-PATTERNS.md
@.planning/phases/67-server-action-dal-extensions/67-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/data/watches.ts:1-7 (existing imports — verify `inArray` and `sql` are already imported):
```typescript
import { db } from '@/db'
import { watches, profileSettings, watchesCatalog, watchPhotos } from '@/db/schema'
import { eq, and, or, asc, desc, inArray, sql, type SQL } from 'drizzle-orm'
```
(`inArray`, `asc`, and `sql` are already present — no new imports needed.)

From src/data/watches.ts:295-317 (current function to extend):
```typescript
export async function findViewerWatchByCatalogId(
  userId: string,
  catalogId: string,
): Promise<{ id: string } | null> {
  const rows = await db
    .select({ id: watches.id })
    .from(watches)
    .where(and(
      eq(watches.userId, userId),
      eq(watches.catalogId, catalogId),
      eq(watches.status, 'owned'),  // BUG-01 fix
    ))
    .orderBy(desc(watches.createdAt))  // D-05
    .limit(1)
  if (rows.length === 0) return null
  return { id: rows[0].id }
}
```

From src/app/w/[ref]/page.tsx:439 (the ONE existing caller — confirm only `.id` is read):
```typescript
findViewerWatchByCatalogId(user.id, ref)
// ...later reads viewerOwnedRow.id only; widened return is structural superset → no caller update needed
```

From tests/data/searchCatalogWatches.test.ts (chainable-Drizzle mock pattern — copy verbatim):
```typescript
// Module-level vars:
let returnedRows: Array<{ id: string; status: string }> = []
let calls: Array<{ op: string; args: unknown[] }> = []

// Chain factory + db mock pattern at the top of the test file.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing test scaffold tests/data/findViewerWatchByCatalogId.test.ts (Wave 0)</name>
  <files>tests/data/findViewerWatchByCatalogId.test.ts</files>
  <read_first>
    - tests/data/searchCatalogWatches.test.ts (full file — copy chainable mock pattern + safeStringify helper verbatim from lines 16-92)
    - tests/data/getFollowedOwnersForCatalog.test.ts (simpler single-chain query reference)
    - src/data/watches.ts:295-317 (current function under test — captures the pre-extension contract)
    - .planning/phases/67-server-action-dal-extensions/67-PATTERNS.md §"tests/data/findViewerWatchByCatalogId.test.ts — new file" (canonical test structure with 5 cases)
    - .planning/phases/67-server-action-dal-extensions/67-CONTEXT.md "Claude's Discretion" §"Unit test layering for the DAL helper" (5 cases enumerated)
  </read_first>
  <behavior>
    - Test (a) statuses=['owned'], single owned row → result equals { id: 'w-1', status: 'owned' }
    - Test (b) statuses=['owned','wishlist'], single wishlist row → result equals { id: 'w-2', status: 'wishlist' }
    - Test (c) statuses=['owned','wishlist'], DB returns the owned row first (ORDER BY CASE) → result equals { id: 'w-owned', status: 'owned' }; this proves the CASE-DESC ordering by relying on the mocked row sequence
    - Test (d) no rows returned → result is null
    - Test (e) default invocation (no statuses arg) → DB call inspected: WHERE arg JSON does NOT contain the string 'wishlist' and DOES contain 'owned' (proves BUG-01 backward-compat default)
    - Mock infrastructure: single `db.select()` chainable returning a promise at `.limit()`; module-level `returnedRows` + `calls` arrays reset in `beforeEach`
  </behavior>
  <action>
    Create the new file `tests/data/findViewerWatchByCatalogId.test.ts`.
    - Import: `describe`, `it`, `expect`, `vi`, `beforeEach` from 'vitest'.
    - Module-level state: `let returnedRows: Array<{ id: string; status: string }> = []` and `let calls: Array<{ op: string; args: unknown[] }> = []`.
    - Factory `makeChain()`: returns an object whose `from`/`where`/`orderBy` methods push `{ op, args }` to `calls` and return `chain`; whose `limit` method pushes `{ op:'limit', args }` then returns `Promise.resolve(returnedRows)`. Type each method to accept `(...args: unknown[]) => unknown`.
    - `vi.mock('@/db', () => ({ db: { select: vi.fn(() => makeChain()) } }))` BEFORE the static import below (vitest hoists `vi.mock`).
    - Import `findViewerWatchByCatalogId` from `@/data/watches` AFTER the mock declaration.
    - Add a `safeStringify` helper (copy from tests/data/searchCatalogWatches.test.ts lines 83-92) for the WHERE-arg JSON assertion in case (e).
    - Constants: `const USER_ID = 'aaaaaaaa-bbbb-4ccc-8ddd-eeeeeeeeeeee'` and `const CATALOG_ID = '11111111-2222-4333-8444-555555555555'`.
    - `beforeEach(() => { calls = []; returnedRows = [] })`.
    - One `describe('findViewerWatchByCatalogId (D-06/D-07/D-08)', () => { ... })` block containing the five `it(...)` cases enumerated under `<behavior>`.
    - Cases (a)-(d) assert `expect(result).toEqual(...)` or `expect(result).toBeNull()`. Case (e) calls `findViewerWatchByCatalogId(USER_ID, CATALOG_ID)` with no third arg, then locates the `where` call in `calls` and runs `expect(safeStringify(whereCall.args)).not.toContain('wishlist')` AND `expect(safeStringify(whereCall.args)).toContain('owned')`.
    - The five test names must use the exact `(a) ...` / `(b) ...` / `(c) ...` / `(d) ...` / `(e) ...` prefix from `<behavior>` so reviewers can grep them.

    This task lands BEFORE the production change so cases (b), (c), (d), (e) initially fail (the current production function does not accept statuses arg → TS error; case (a) may pass partially). The failing-test gate proves the test scaffolding exercises the new contract.
  </action>
  <verify>
    <automated>npm test -- tests/data/findViewerWatchByCatalogId.test.ts --run 2>&1 | grep -E "(FAIL|PASS|✓|✗|Test Files|Tests)"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/data/findViewerWatchByCatalogId.test.ts` exists
    - Source contains the exact strings: `describe('findViewerWatchByCatalogId (D-06/D-07/D-08)'`, `vi.mock('@/db'`, `safeStringify`, `'aaaaaaaa-bbbb-4ccc-8ddd-eeeeeeeeeeee'`, `'11111111-2222-4333-8444-555555555555'`
    - Source contains five `it(` calls whose names begin with `(a)`, `(b)`, `(c)`, `(d)`, `(e)` respectively
    - Running `npm test -- tests/data/findViewerWatchByCatalogId.test.ts --run` exits non-zero (tests fail because production function does not yet accept statuses arg / return status) OR exits zero with at least 1 test pre-marked failing — Task 2 makes them all pass
  </acceptance_criteria>
  <done>The test file exists, contains all five cases with the agreed naming, and the suite is currently failing for reasons consistent with the production code not yet being extended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend findViewerWatchByCatalogId per D-06/D-07/D-08 in src/data/watches.ts</name>
  <files>src/data/watches.ts</files>
  <read_first>
    - src/data/watches.ts (lines 1-7 to confirm `inArray`, `asc`, `sql` are already imported; lines 295-317 for the function being extended; lines 280-285 to preserve the T-20-06-01 docblock context above the function)
    - src/app/w/[ref]/page.tsx:7 + :439 (the ONE existing caller — confirms `viewerOwnedRow.id` is the only field read; widened return is a structural superset → no caller change required)
    - .planning/phases/67-server-action-dal-extensions/67-PATTERNS.md §"src/data/watches.ts — extend findViewerWatchByCatalogId" (canonical extended-signature + query body)
    - .planning/phases/67-server-action-dal-extensions/67-RESEARCH.md "Pitfall 2" (SELECT projection must include `status` — runtime undefined trap)
    - .planning/phases/67-server-action-dal-extensions/67-RESEARCH.md "Open Question 1" (inArray with pgEnum status — pattern may need sql template fallback; the Drizzle docs claim inArray works with enum cols)
    - tests/data/findViewerWatchByCatalogId.test.ts (Task 1 output — proves green when this task lands)
  </read_first>
  <action>
    Modify `findViewerWatchByCatalogId` in-place per the canonical extended-signature shown in 67-PATTERNS.md §"src/data/watches.ts — extend findViewerWatchByCatalogId".

    Specific changes (target state):
    - Signature gains a third parameter `statuses: ('owned' | 'wishlist')[] = ['owned']` (D-06; default preserves BUG-01).
    - Return type becomes `Promise<{ id: string; status: 'owned' | 'wishlist' } | null>` (D-07).
    - `.select({ id: watches.id })` becomes `.select({ id: watches.id, status: watches.status })` (RESEARCH Pitfall 2 — projection must be widened or runtime returns `status: undefined`).
    - The `eq(watches.status, 'owned')` predicate becomes `inArray(watches.status, statuses)` (consume the new param).
    - The `.orderBy(desc(watches.createdAt))` becomes a two-clause ORDER BY: `asc(sql`CASE ${watches.status} WHEN 'owned' THEN 0 WHEN 'wishlist' THEN 1 ELSE 2 END`)` first, then `desc(watches.createdAt)` (D-08 owned-wins + D-05 carry-forward).
    - Return statement: `return { id: row.id, status: row.status as 'owned' | 'wishlist' }` (cast narrows the pgEnum row type to the parameter-constrained union).
    - DO NOT change the function name, the BUG-01 docblock at lines 287-294, or the T-20-06-01 reasoning comment.

    Do NOT add new imports — `inArray`, `asc`, `sql` are already imported on line 6.

    If `inArray(watches.status, statuses)` triggers a Drizzle type error against the pgEnum column (RESEARCH Open Q1 / assumption A2), fall back to: `sql`${watches.status} = ANY(${statuses}::watch_status[])`` — preserve the same semantic. Surface either choice in the summary.

    Audit existing call sites with `grep -rn "findViewerWatchByCatalogId" src/` BEFORE finishing: confirm the only caller at `src/app/w/[ref]/page.tsx:439` reads only `.id` (does not destructure `status`) — no caller change should be needed. If any caller does destructure `status` or otherwise expects the old shape, fix it inline and document in the task summary.
  </action>
  <verify>
    <automated>npm test -- tests/data/findViewerWatchByCatalogId.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "statuses: ('owned' | 'wishlist')\[\] = \['owned'\]" src/data/watches.ts` returns one match inside the `findViewerWatchByCatalogId` signature
    - `grep -n "status: watches.status" src/data/watches.ts` returns at least one match (the SELECT projection)
    - `grep -n "inArray(watches.status, statuses)" src/data/watches.ts` OR `grep -n "ANY(\${statuses}" src/data/watches.ts` returns one match
    - `grep -n "CASE \${watches.status} WHEN 'owned' THEN 0 WHEN 'wishlist' THEN 1 ELSE 2 END" src/data/watches.ts` returns one match
    - `grep -c "eq(watches.status, 'owned')" src/data/watches.ts` returns 0 (the old single-status predicate is gone from this function)
    - `npm test -- tests/data/findViewerWatchByCatalogId.test.ts --run` exits 0 with all 5 tests passing
    - `npx tsc --noEmit src/data/watches.ts 2>&1 | grep -v "test\." | grep -c "error TS" | grep -E "^0$"` — production file type-checks clean (the project baseline carries ~77 pre-existing test-file errors per project memory; the production file must add zero new ones)
    - `grep -rn "findViewerWatchByCatalogId" src/ | grep -v "src/data/watches.ts"` returns exactly one line (the `/w/[ref]/page.tsx:439` caller, untouched)
  </acceptance_criteria>
  <done>Extended function compiles and types check; all 5 unit tests in `tests/data/findViewerWatchByCatalogId.test.ts` pass; the one existing caller at `/w/[ref]/page.tsx:439` is untouched (structural-superset widening is non-breaking).</done>
</task>

</tasks>

<verification>
- Quick sample (per CONTEXT.md / 67-VALIDATION.md): `npm test -- tests/data/findViewerWatchByCatalogId.test.ts --run` exits 0 with 5/5 passing.
- Production type-check: the modified `src/data/watches.ts` adds zero new TypeScript errors (baseline ~77 pre-existing test-file errors per project memory — do not attribute to this phase).
- Caller audit: the one existing caller at `src/app/w/[ref]/page.tsx:439` continues to pass type checks without modification (structural-superset widening).
- Build gate: `npm run build` exits 0 (authoritative baseline gate per project memory).
</verification>

<success_criteria>
- `findViewerWatchByCatalogId(userId, catalogId, ['owned', 'wishlist'])` returns owned row when present, else wishlist row, else null — matches Phase 67 success criterion 3 from ROADMAP.md.
- `findViewerWatchByCatalogId(userId, catalogId)` (default invocation) returns owned-only result — BUG-01 contract preserved.
- DUPE-01 DAL part shipped: Phase 70 can read `result.id` to redirect to `/w/[ref]`.
- DUPE-03 DAL part shipped: Phase 70 can read `result.status === 'wishlist'` to branch into the confirm-screen + "Move to Collection" path.
- 5/5 unit tests in `tests/data/findViewerWatchByCatalogId.test.ts` green.
</success_criteria>

<output>
After completion, create `.planning/phases/67-server-action-dal-extensions/67-01-SUMMARY.md` covering: signature change, SELECT projection widening, CASE ORDER BY semantics, the `inArray` vs `sql\`...= ANY()\`` decision actually taken, the caller audit result (zero caller changes), and 5/5 test outcomes.
</output>
