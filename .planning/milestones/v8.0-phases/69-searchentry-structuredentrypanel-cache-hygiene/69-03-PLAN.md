---
phase: 69
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/watch/useUrlExtractCache.ts
  - src/components/watch/useUrlExtractCache.test.ts
  - src/components/search/useWatchSearchVerdictCache.ts
  - src/components/search/useWatchSearchVerdictCache.test.ts
  - src/components/watch/AddWatchFlow.tsx
  - src/components/search/WatchSearchRowsAccordion.tsx
  - src/components/search/SearchPageClient.tsx
autonomous: true
requirements: [CLNP-07]
tags: [retrofit, cache-hygiene, breaking-change-by-design, typescript-surface]

must_haves:
  truths:
    - "D-08: viewerUserId is added as a REQUIRED positional arg to BOTH pre-existing hooks — useUrlExtractCache(viewerUserId) and useWatchSearchVerdictCache(collectionRevision, viewerUserId); REQUIRED (not optional) so TypeScript surfaces every caller in the same compile"
    - "D-06: Both retrofit hooks add `let moduleUserId = ''` at module scope and the if (moduleUserId !== viewerUserId) reset block at the top of the hook body"
    - "D-06: useWatchSearchVerdictCache outer guard resets moduleRevision to 0 too — so the inner revision guard fires fresh for the new user"
    - "D-06: Both retrofit hooks add a stale-write guard `if (moduleUserId !== viewerUserId) return;` inside set() before moduleCache.set(...)"
    - "D-08 retrofit cascade: 3 existing caller sites updated — AddWatchFlow.tsx:~123 (useWatchSearchVerdictCache), AddWatchFlow.tsx:~127 (useUrlExtractCache), WatchSearchRowsAccordion.tsx:47 (useWatchSearchVerdictCache)"
    - "Phase 67 SearchPageClient.viewerId is already in scope on /search — Phase 69 threads it as a 3-layer prop drop: SearchPageClient → WatchesPanel (internal) → WatchSearchRowsAccordion as viewerUserId"
    - "AddWatchFlow.tsx props add catalogBrands: string[] alongside viewerUserId (typed prop only — NOT consumed by AddWatchFlow in Phase 69; Phase 70 consumes when wiring SearchEntry)"
    - "Both retrofit __reset___ForTests fns are updated to also reset moduleUserId — required by the Plan 06 integration test"
    - "WatchSearchRow.tsx is NOT modified (viewerState pill copy 'Owned'/'Wishlist' stays unchanged on /search; SearchEntry diverges intentionally per CONTEXT.md Discretion)"
    - "CLNP-07: All four module-scope caches now reset on viewerUserId switch — pre-existing useWatchSearchVerdictCache tech-debt leak is closed in this same change (success criterion #5)"
    - "Memory project_baseline_not_green_build_is_gate: npm run build exit 0 is the authoritative gate; if any unexpected TypeScript error appears in baseline files unrelated to this change, leave it"
    - "Memory proxy_router_cache_poisoning: NO getUser() inside any cache hook; prop-drill viewerUserId from server-supplied sources only (D-07)"
  artifacts:
    - path: "src/components/watch/useUrlExtractCache.ts"
      provides: "Retrofit: required viewerUserId arg + moduleUserId reset + stale-write guard"
      contains: "useUrlExtractCache(viewerUserId: string)"
    - path: "src/components/watch/useUrlExtractCache.test.ts"
      provides: "NEW test file (does not currently exist) — verifies user-switch invalidation"
      contains: "describe.*useUrlExtractCache"
    - path: "src/components/search/useWatchSearchVerdictCache.ts"
      provides: "Retrofit: required 2nd positional viewerUserId arg + outer user-switch guard before existing revision guard"
      contains: "useWatchSearchVerdictCache(\n  collectionRevision: number,\n  viewerUserId: string"
    - path: "src/components/search/useWatchSearchVerdictCache.test.ts"
      provides: "NEW test file (does not currently exist) — verifies user-switch + revision-change interactions"
      contains: "describe.*useWatchSearchVerdictCache"
    - path: "src/components/watch/AddWatchFlow.tsx"
      provides: "Cache call sites updated; AddWatchFlowProps gains catalogBrands: string[] (typed pass-through, Phase 70 consumer)"
      contains: "useWatchSearchVerdictCache(collectionRevision, viewerUserId)"
    - path: "src/components/search/WatchSearchRowsAccordion.tsx"
      provides: "Adds required viewerUserId: string prop; passes to useWatchSearchVerdictCache"
      contains: "viewerUserId: string"
    - path: "src/components/search/SearchPageClient.tsx"
      provides: "3-layer thread: SearchPageClient.viewerId → WatchesPanel.viewerId → WatchSearchRowsAccordion.viewerUserId"
      contains: "viewerId={viewerId}"
  key_links:
    - from: "src/components/watch/AddWatchFlow.tsx"
      to: "src/components/watch/useUrlExtractCache.ts + src/components/search/useWatchSearchVerdictCache.ts"
      via: "useUrlExtractCache(viewerUserId) + useWatchSearchVerdictCache(collectionRevision, viewerUserId)"
      pattern: "useUrlExtractCache\\(viewerUserId\\)"
    - from: "src/components/search/SearchPageClient.tsx"
      to: "src/components/search/WatchSearchRowsAccordion.tsx"
      via: "3-layer prop thread (viewerId)"
      pattern: "viewerUserId=\\{viewerId\\}"
---

<objective>
Retrofit the two pre-existing module-scope caches (`useUrlExtractCache` and `useWatchSearchVerdictCache`) to add a required `viewerUserId` positional arg + the D-06 user-switch reset. This is the **intentional TypeScript-breaking change** that surfaces all 3 existing caller sites in the same compile (D-08):

- `src/components/watch/AddWatchFlow.tsx:~123` (`useWatchSearchVerdictCache(collectionRevision)`)
- `src/components/watch/AddWatchFlow.tsx:~127` (`useUrlExtractCache()`)
- `src/components/search/WatchSearchRowsAccordion.tsx:47` (`useWatchSearchVerdictCache(collectionRevision)`)

The `WatchSearchRowsAccordion` retrofit requires a 3-layer prop thread through `SearchPageClient` → internal `WatchesPanel` → `WatchSearchRowsAccordion` because `WatchesPanel` does NOT currently accept `viewerId`. `SearchPageClient.viewerId` is already a prop (Phase 67 plumbing) — Phase 69 just routes it through.

Also: extend `AddWatchFlowProps` with `catalogBrands: string[]` (TYPED pass-through; AddWatchFlow does NOT consume in Phase 69 — Phase 70 owns mounting SearchEntry which is the consumer). This lets Plan 06 wire `/watch/new/page.tsx` without re-touching this file.

This plan closes the pre-existing `useWatchSearchVerdictCache` tech-debt leak in the same change (success criterion #5).

Parallel with Plans 01 and 02 (Wave 1; no file overlap).

Purpose: Close CLNP-07 retrofit half; lock the type signatures Wave 2 components consume.
Output: 7 file modifications (2 hook retrofits + 2 NEW colocated tests + 3 caller-site updates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-CONTEXT.md
@.planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-RESEARCH.md
@.planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-PATTERNS.md
@.planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-VALIDATION.md

<interfaces>
<!-- Current shapes — to be modified. Verbatim from PATTERNS.md self-extend sections. -->

From `src/components/watch/useUrlExtractCache.ts` (CURRENT):
```typescript
export function useUrlExtractCache() { ... }
```
TARGET (Phase 69):
```typescript
export function useUrlExtractCache(viewerUserId: string) { ... }
```

From `src/components/search/useWatchSearchVerdictCache.ts` (CURRENT):
```typescript
export function useWatchSearchVerdictCache(collectionRevision: number) {
  if (moduleRevision !== collectionRevision) {
    moduleCache = new Map()
    moduleRevision = collectionRevision
  }
  ...
}
```
TARGET (Phase 69):
```typescript
export function useWatchSearchVerdictCache(
  collectionRevision: number,
  viewerUserId: string,    // NEW
) {
  // Outer guard FIRST (user switch)
  if (moduleUserId !== viewerUserId) {
    moduleCache = new Map()
    moduleUserId = viewerUserId
    moduleRevision = 0       // reset so inner check fires fresh
  }
  // Inner guard (existing, unchanged semantics)
  if (moduleRevision !== collectionRevision) {
    moduleCache = new Map()
    moduleRevision = collectionRevision
  }
  ...
}
```

From `src/components/watch/AddWatchFlow.tsx`:
- `AddWatchFlowProps` lives at ~L52-85
- `viewerUserId: string` is already a typed prop (Phase 61 WR-03 lineage)
- Phase 69 ADDS: `catalogBrands: string[]` to the same interface
- Cache call sites at ~L123 and ~L127 (verify exact line during edit)

From `src/components/search/SearchPageClient.tsx`:
- `SearchPageClientProps.viewerId: string` is already a prop (Phase 67 plumbing — confirmed in RESEARCH.md)
- Internal `WatchesPanel` function (currently NO viewerId in its destructure or its type literal)
- `<WatchSearchRowsAccordion ...>` call site inside `WatchesPanel`

From `src/components/search/WatchSearchRowsAccordion.tsx`:
- Props at lines 31-43 currently: `results, q, collectionRevision, viewerUsername`
- `useWatchSearchVerdictCache(collectionRevision)` at line 47
- Phase 69 ADDS: `viewerUserId: string` to props; passes to the cache call
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retrofit useUrlExtractCache (add viewerUserId arg + reset block + create test file)</name>
  <files>src/components/watch/useUrlExtractCache.ts, src/components/watch/useUrlExtractCache.test.ts</files>
  <read_first>
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-CONTEXT.md (D-06 reset, D-07 prop-drill, D-08 required arg)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-RESEARCH.md (lines 360–392 — current file shape + after-retrofit shape)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-PATTERNS.md (§useUrlExtractCache MODIFY — explicit before/after diff)
    - src/components/watch/useUrlExtractCache.ts (current full file)
    - src/components/search/useWatchSearchVerdictCache.ts (lines 38-45 reset block + 51-54 stale-write guard — copy verbatim)
  </read_first>
  <behavior>
    - Test 1: set then get returns the cached entry for the same viewerUserId
    - Test 2: switching viewerUserId across renders clears the cache — get(url) returns undefined after rerender with new uid
    - Test 3: stale-write guard — old hook's set() after user-switch does NOT pollute the new user's cache
    - Test 4: __resetUrlExtractCacheForTests() now resets BOTH moduleCache AND moduleUserId
  </behavior>
  <action>
    Modify `src/components/watch/useUrlExtractCache.ts` IN PLACE per the PATTERNS.md before/after diff:

    1. Add `let moduleUserId = ''` at module scope (alongside the existing `let moduleCache`).

    2. Update `__resetUrlExtractCacheForTests()` body to ALSO reset `moduleUserId = ''`.

    3. Change the hook signature from `useUrlExtractCache()` to `useUrlExtractCache(viewerUserId: string)`. REQUIRED positional arg per D-08.

    4. Add the D-06 reset block at the top of the hook body (BEFORE the `return`):
       ```
       if (moduleUserId !== viewerUserId) {
         moduleCache = new Map()
         moduleUserId = viewerUserId
       }
       ```
       Annotate with the same in-render-mutation-safety comment as useWatchSearchVerdictCache.ts:38-45.

    5. Inside the returned `set` method, BEFORE `moduleCache.set(url, entry)`, add:
       ```
       if (moduleUserId !== viewerUserId) return
       ```
       This is the stale-write guard (mirrors useWatchSearchVerdictCache.ts:51-54).

    6. Update the existing JSDoc to mention the new viewerUserId-keyed reset behavior. The original JSDoc (existing) already explains the module-scope-survives-key-remount rationale; ADD a paragraph about the Phase 69 D-06 cross-user reset.

    Create NEW file `src/components/watch/useUrlExtractCache.test.ts` (verify it does NOT already exist before writing). Mirror the structure of Plan 02's `useCatalogSearchCache.test.ts`. Use `renderHook` + `__resetUrlExtractCacheForTests`. Implement all 4 tests in Behavior block above. Use a real ExtractCacheEntry fixture:
    ```
    const entry: ExtractCacheEntry = { catalogId: 'cat-abc', extracted: {/* ExtractedWatchData partial */}, catalogIdError: null }
    ```

    DO NOT touch the existing AddWatchFlow.tsx call site in this task — that's handled in Task 3 of this plan (so the TypeScript error window is short and intentional).
  </action>
  <verify>
    <automated>npm run test -- --run src/components/watch/useUrlExtractCache.test.ts</automated>
  </verify>
  <done>
    Hook signature is `useUrlExtractCache(viewerUserId: string)`. Module-level `moduleUserId` present. Reset block at top of hook body. Stale-write guard in `set()`. `__resetUrlExtractCacheForTests` resets both module vars. New test file passes 4 tests. TypeScript will currently fail at the existing AddWatchFlow.tsx caller — that's expected; Task 3 fixes it in the same plan.
  </done>
  <acceptance_criteria>
    - Source: `grep -c "^export function useUrlExtractCache(viewerUserId: string)" src/components/watch/useUrlExtractCache.ts` returns `1`
    - Source: `grep -c "let moduleUserId" src/components/watch/useUrlExtractCache.ts` returns `1`
    - Source: stale-write guard present — `grep -c "if (moduleUserId !== viewerUserId) return" src/components/watch/useUrlExtractCache.ts` returns `1`
    - Source: `__resetUrlExtractCacheForTests` resets moduleUserId — `awk '/__resetUrlExtractCacheForTests/,/^}/' src/components/watch/useUrlExtractCache.ts | grep -c "moduleUserId = ''"` returns `1`
    - File: `src/components/watch/useUrlExtractCache.test.ts` exists (was missing before this task)
    - Behavior: `npm run test -- --run src/components/watch/useUrlExtractCache.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retrofit useWatchSearchVerdictCache (add viewerUserId outer guard + create test file)</name>
  <files>src/components/search/useWatchSearchVerdictCache.ts, src/components/search/useWatchSearchVerdictCache.test.ts</files>
  <read_first>
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-CONTEXT.md (D-06, D-08 — closes pre-existing tech-debt leak per SC#5)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-RESEARCH.md (lines 324–358 — current vs retrofit shape with the outer-then-inner guard chain)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-PATTERNS.md (§useWatchSearchVerdictCache MODIFY — outer guard resets moduleRevision to 0 so inner fires fresh)
    - src/components/search/useWatchSearchVerdictCache.ts (current full file — lines 38-45 reset block, 33-36 __resetVerdictCacheForTests, 51-54 stale-write guard)
  </read_first>
  <behavior>
    - Test 1: set then get returns cached entry for same (collectionRevision, viewerUserId)
    - Test 2: switching viewerUserId across renders clears the cache AND resets moduleRevision (the inner revision guard fires fresh on next call)
    - Test 3: existing revision-change behavior is PRESERVED — switching collectionRevision (same user) clears the cache like before
    - Test 4: user-switch THEN revision-change in sequence — both guards fire correctly; cache stays empty for the new user
    - Test 5: __resetVerdictCacheForTests() resets ALL THREE module vars (moduleCache, moduleRevision, moduleUserId)
    - Test 6: stale-write guard — old hook's set() after user-switch does NOT pollute new user
  </behavior>
  <action>
    Modify `src/components/search/useWatchSearchVerdictCache.ts` IN PLACE per the PATTERNS.md before/after diff:

    1. Add `let moduleUserId = ''` at module scope (alongside `let moduleRevision = 0`).

    2. Update `__resetVerdictCacheForTests()` body to ALSO reset `moduleUserId = ''`.

    3. Change the hook signature from:
       ```
       export function useWatchSearchVerdictCache(collectionRevision: number)
       ```
       to:
       ```
       export function useWatchSearchVerdictCache(
         collectionRevision: number,
         viewerUserId: string,
       )
       ```
       REQUIRED positional arg per D-08.

    4. Add the OUTER user-switch guard at the top of the hook body, BEFORE the existing revision guard:
       ```
       if (moduleUserId !== viewerUserId) {
         moduleCache = new Map()
         moduleUserId = viewerUserId
         moduleRevision = 0    // reset so inner guard fires fresh for new user
       }
       ```
       The `moduleRevision = 0` reset is critical — it makes the inner `if (moduleRevision !== collectionRevision)` block also fire when the user switches, ensuring the cache stays clean even if the new user has a matching revision number by coincidence.

    5. Inside the existing `set()` method, BEFORE `moduleCache.set(...)`, add the stale-write guard:
       ```
       if (moduleUserId !== viewerUserId) return
       ```

    6. Update the existing JSDoc at the top of the file to add a paragraph about the Phase 69 D-06 cross-user reset; reference the success criterion that this closes the pre-existing tech-debt leak.

    Create NEW file `src/components/search/useWatchSearchVerdictCache.test.ts` (verify it does NOT already exist). Same `renderHook` pattern as the other cache tests. Import `useWatchSearchVerdictCache` + `__resetVerdictCacheForTests`. Implement all 6 tests in Behavior block above. Use realistic fixtures for the VerdictBundle type (look at the current `useWatchSearchVerdictCache.ts` for the exact `VerdictBundle` shape — copy a minimal fixture).

    DO NOT touch AddWatchFlow.tsx OR WatchSearchRowsAccordion.tsx OR SearchPageClient.tsx in this task — those are Task 3.
  </action>
  <verify>
    <automated>npm run test -- --run src/components/search/useWatchSearchVerdictCache.test.ts</automated>
  </verify>
  <done>
    Hook signature is `useWatchSearchVerdictCache(collectionRevision: number, viewerUserId: string)`. Outer user-switch guard PRECEDES inner revision guard. `moduleRevision = 0` reset present inside outer guard. Stale-write guard in `set()`. Test file passes 6 tests. TypeScript will currently fail at the 2 existing callers (AddWatchFlow.tsx + WatchSearchRowsAccordion.tsx) — expected; Task 3 closes the loop.
  </done>
  <acceptance_criteria>
    - Source: `grep -c "viewerUserId: string" src/components/search/useWatchSearchVerdictCache.ts` returns ≥ `1`
    - Source: `grep -c "let moduleUserId" src/components/search/useWatchSearchVerdictCache.ts` returns `1`
    - Source: outer guard precedes inner — order of `if (moduleUserId !==` line precedes `if (moduleRevision !==` line (verify with `grep -n` line numbers)
    - Source: outer guard contains `moduleRevision = 0` reset — `awk '/if \(moduleUserId !== viewerUserId\)/,/^  \}/' src/components/search/useWatchSearchVerdictCache.ts | grep -c "moduleRevision = 0"` returns `1`
    - Source: stale-write guard — `grep -c "if (moduleUserId !== viewerUserId) return" src/components/search/useWatchSearchVerdictCache.ts` returns `1`
    - File: `src/components/search/useWatchSearchVerdictCache.test.ts` exists
    - Behavior: `npm run test -- --run src/components/search/useWatchSearchVerdictCache.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update 3 caller sites + thread viewerUserId through SearchPageClient → WatchesPanel → WatchSearchRowsAccordion + add catalogBrands prop to AddWatchFlowProps</name>
  <files>src/components/watch/AddWatchFlow.tsx, src/components/search/WatchSearchRowsAccordion.tsx, src/components/search/SearchPageClient.tsx</files>
  <read_first>
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-CONTEXT.md (D-08 retrofit scope — the 3 caller sites + 3-layer SearchPageClient thread; Discretion §"/watch/new page-prop addition" — catalogBrands prop typed on AddWatchFlow even though Phase 70 consumes)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-RESEARCH.md (lines 434–446 retrofit call site map; lines 789–794 SearchPageClient 3-layer thread notes; lines 700–731 file modification list)
    - .planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-PATTERNS.md (§AddWatchFlow.tsx MODIFY — line 123 and line 127 cache call sites; §WatchSearchRowsAccordion.tsx MODIFY — line 31-43 props + line 47 call site; §SearchPageClient.tsx MODIFY — 3-layer thread at lines 317-348 WatchesPanel + 189-201 call site + 455-461 inner Accordion call)
    - src/components/watch/AddWatchFlow.tsx (lines 52-85 AddWatchFlowProps; lines 120-130 cache call site region)
    - src/components/search/WatchSearchRowsAccordion.tsx (lines 31-47 — current props + cache call)
    - src/components/search/SearchPageClient.tsx (lines 317-348 WatchesPanel definition; line 189-201 WatchesPanel call site; lines 455-461 WatchSearchRowsAccordion call inside WatchesPanel; line 31 SearchPageClientProps.viewerId)
  </read_first>
  <action>
    Update three files in this order (TypeScript errors will resolve incrementally; the order minimizes the time the build is red):

    **A) `src/components/watch/AddWatchFlow.tsx`** — two surgical changes inside the existing file:

    1. Extend `AddWatchFlowProps` (~L52-85): ADD `catalogBrands: string[]` as a required prop alongside the existing `viewerUserId: string`. JSDoc on the new prop: reference Phase 69 D-13, note that the SSR caller is `/watch/new/page.tsx` (Plan 06), and explicitly state "Phase 69 ships this typed only — Phase 70 mounts SearchEntry which consumes it as the parseSearchQuery catalogBrands arg." This makes the dormant-shipping intent explicit.

    2. Update both cache call sites:
       - Line ~123: `useWatchSearchVerdictCache(collectionRevision)` → `useWatchSearchVerdictCache(collectionRevision, viewerUserId)`
       - Line ~127: `useUrlExtractCache()` → `useUrlExtractCache(viewerUserId)`
       Verify the exact line numbers during edit; use `grep -n` first.

    Note: AddWatchFlow's INTERNAL render does NOT consume `catalogBrands` in this phase — it's a typed pass-through only. ESLint may warn about an unused destructure; suppress with a single-line `// eslint-disable-next-line` near the destructure or destructure it WITHOUT use (e.g. include in props destructure but leave reference in JSDoc only). Whichever approach matches the established Horlo style — check what AddWatchFlow does for any other Phase-70-bound typed-only props (likely nothing else fits this pattern; default to destructuring + no use, no lint suppression — TypeScript-typed-only props are fine in TSX).

    **B) `src/components/search/WatchSearchRowsAccordion.tsx`** — at lines 31-47:

    1. Extend the destructure + type literal at lines 31-43: ADD `viewerUserId: string` as a required prop. The prop type literal currently inlines all 4 fields — add `viewerUserId: string` as the 5th.

    2. Update line 47 cache call: `useWatchSearchVerdictCache(collectionRevision)` → `useWatchSearchVerdictCache(collectionRevision, viewerUserId)`.

    **C) `src/components/search/SearchPageClient.tsx`** — 3-layer thread:

    1. `WatchesPanel` function signature (~lines 317-348): currently does NOT destructure `viewerId` or declare it in the inline type literal. ADD `viewerId` to the destructure AND `viewerId: string` to the type literal. JSDoc: "Phase 69 D-08 — threaded to WatchSearchRowsAccordion.viewerUserId for the cache hygiene retrofit."

    2. `WatchesPanel` call site (~lines 189-201): currently passes `q`, `results`, ..., `onClearEra`. ADD `viewerId={viewerId}` to the JSX (viewerId is already in scope from SearchPageClientProps.viewerId at line 31).

    3. `WatchSearchRowsAccordion` call inside `WatchesPanel` (~lines 455-461): currently passes `results`, `q`, `collectionRevision`, `viewerUsername`. ADD `viewerUserId={viewerId}` — note the prop name asymmetry (WatchesPanel uses `viewerId`; Accordion uses `viewerUserId` per Task 2 — both are correct in their own context).

    After all three files are updated, the TypeScript surface should be GREEN. Run `npm run build` to verify.

    Counter-assertion guardrails:
    - DO NOT modify `WatchSearchRow.tsx` (the row component on /search). Its "Owned"/"Wishlist" badge copy stays unchanged — SearchEntry diverges in Plan 05 (uses "In collection"/"On wishlist" per UI-SPEC).
    - DO NOT add any `getUser()` or `createSupabaseBrowserClient()` call anywhere (proxy_router_cache_poisoning memory).
    - DO NOT change the existing `searchCatalogWatches` DAL or `searchWatchesAction` Server Action.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    `npm run build` exits 0. All 3 caller sites compile with the new required `viewerUserId` arg. `AddWatchFlow.tsx`'s `AddWatchFlowProps` declares `catalogBrands: string[]` (typed pass-through; not consumed in this phase). `SearchPageClient.tsx` threads `viewerId` through `WatchesPanel` to `WatchSearchRowsAccordion` as `viewerUserId`. `WatchSearchRow.tsx` is unchanged (counter-assertion).
  </done>
  <acceptance_criteria>
    - Source: `grep -c "useWatchSearchVerdictCache(collectionRevision, viewerUserId)" src/components/watch/AddWatchFlow.tsx` returns `1`
    - Source: `grep -c "useUrlExtractCache(viewerUserId)" src/components/watch/AddWatchFlow.tsx` returns `1`
    - Source: `grep -c "catalogBrands: string\\[\\]" src/components/watch/AddWatchFlow.tsx` returns ≥ `1` (the AddWatchFlowProps interface entry)
    - Source: `grep -c "useWatchSearchVerdictCache(collectionRevision, viewerUserId)" src/components/search/WatchSearchRowsAccordion.tsx` returns `1`
    - Source: `grep -c "viewerUserId: string" src/components/search/WatchSearchRowsAccordion.tsx` returns ≥ `1`
    - Source: `grep -c "viewerUserId={viewerId}" src/components/search/SearchPageClient.tsx` returns `1` (the Accordion call site)
    - Source: `grep -c "viewerId={viewerId}" src/components/search/SearchPageClient.tsx` returns ≥ `1` (the WatchesPanel call site)
    - Counter-assertion: `git diff --stat src/components/search/WatchSearchRow.tsx` returns empty (file unchanged in this plan)
    - Build: `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All 3 NEW test files (Plan 02 × 2 + Plan 03 × 2 = total 4 new cache tests + this plan's 2) pass after Task 1+2; existing `AddWatchFlow.test.tsx` may need `catalogBrands={[]}` added to existing renders — that lives in Plan 06 (do NOT touch in this plan)
- `npm run build` exits 0 after Task 3 completes
- `npm run test -- --run` does not surface NEW regressions (baseline-noise allowed per project_baseline_not_green_build_is_gate)
- Counter-assertion: `WatchSearchRow.tsx` unchanged
</verification>

<success_criteria>
- CLNP-07 retrofit half complete: both existing caches now have the user-switch reset
- The pre-existing useWatchSearchVerdictCache tech-debt leak is CLOSED (SC#5 verbatim)
- TypeScript signatures are LOCKED for the Wave 2 components (Plans 04/05 consume the retrofit caches directly through AddWatchFlow's prop surface in Phase 70)
- `AddWatchFlow.tsx` props can flow Plan 06's `catalogBrands` value without re-touching this file
- Build green; no test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/69-searchentry-structuredentrypanel-cache-hygiene/69-03-SUMMARY.md`
</output>
