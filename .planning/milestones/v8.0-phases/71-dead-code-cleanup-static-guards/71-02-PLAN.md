---
phase: 71
plan: 02
type: execute
wave: 2
depends_on:
  - "71-01"
files_modified:
  - src/components/watch/VerdictStep.tsx
  - src/components/watch/VerdictStep.test.tsx
  - src/components/watch/WishlistRationalePanel.tsx
  - src/components/watch/WishlistRationalePanel.test.tsx
  - src/components/watch/PasteSection.tsx
  - src/components/watch/PasteSection.test.tsx
  - src/components/watch/RecentlyEvaluatedRail.tsx
  - src/components/watch/RecentlyEvaluatedRail.test.tsx
  - src/components/watch/flowTypes.ts
  - src/components/watch/AddWatchFlow.tsx
autonomous: true
requirements:
  - CLNP-01
  - CLNP-04
tags:
  - dead-code-cleanup
  - file-deletes
  - flowTypes-prune
  - AddWatchFlow-sweep
  - state-residue-cleanup
must_haves:
  truths:
    - "Per D-01, RecentlyEvaluatedRail.tsx + .test.tsx are deleted OUTRIGHT (not retained for repurpose); RailEntry interface + PendingTarget type alias + the legacy `verdict: unknown | null` field are deleted from flowTypes.ts; the RailEntry type-only import + the `const [rail, setRail] = useState<RailEntry[]>([])` declaration + all `rail`/`setRail`/`railRef` references (10 verified call sites per RESEARCH §2) are deleted from AddWatchFlow.tsx — missing any one site breaks `npm run build` with `Cannot find name 'rail' | 'setRail' | 'railRef'` (RESEARCH Pitfall 2)."
    - "Per D-03, AddWatchFlow.tsx's top JSDoc (lines 35-36 + 52) is reworded forward-looking: the dead-component name list (`PasteSection / VerdictStep / WishlistRationalePanel / RecentlyEvaluatedRail`) and the `(no PasteSection)` parenthetical in D-14 prose are replaced with prose that names `tests/static/AddWatchFlow.no-verdict-step.test.ts` as the static-guard enforcement mechanism. Exact prose is Claude's Discretion (D-Discretion); target is 2-4 lines."
    - "Per D-04 + D-05, this plan depends on Plan 71-01 having shipped both static guards FIRST — Plan 71-01's CLNP-02 guard already asserts no VerdictStep/WishlistRationalePanel/PasteSection imports in AddWatchFlow.tsx, so if this plan's deletes accidentally leave a residual import the guard catches it in the same PR."
    - "Per D-05 + Memory project_baseline_not_green_build_is_gate.md, `npm run build` exit 0 is the authoritative gate; targeted vitest runs (tests/static/ + the 6-file add-flow suite) confirm no regression. Full `tsc --noEmit` and full `vitest run` carry pre-existing baseline noise and are NOT the gate (~77 pre-existing tsc errors in test files; ≥1 pre-existing vitest failure in CommentGateLocked font-medium per Phase 69 Plan 04 recurrence-2)."
    - "Per D-02 (forward-looking; the imports-only regex shipped in Plan 71-01 does not flag prose) and Memory feedback_decision_coverage_gate_citations.md, JSDoc prose referencing the dead component names by literal string CAN false-positive future grep-based gates; the reworded JSDoc must avoid the exact tokens `VerdictStep`, `WishlistRationalePanel`, `PasteSection`, `RecentlyEvaluatedRail` (paraphrase as `legacy verdict-flow components` or similar). This is recurrence-prevention for the Phase 69 Plan 04 + Phase 70 Plan 04 JSDoc-grep-collision pattern."
    - "Per CLNP-05 reconciliation note in 70-CONTEXT.md (Phase 70 D-01 final FlowState union shape), this plan does NOT touch the FlowState discriminated union beyond removing RailEntry/PendingTarget exports from flowTypes.ts; ROADMAP success criterion #4 (`FlowState obsolete variants removed`) is already satisfied by Phase 70 D-01 and the CLNP-05 reconciliation locks Phase 71 assertion against the final 7-variant union, NOT the ROADMAP draft enumeration. DupeContext interface stays untouched (Phase 70 active consumer)."
  artifacts:
    - path: "src/components/watch/VerdictStep.tsx"
      provides: "(absent after this plan — deleted)"
    - path: "src/components/watch/VerdictStep.test.tsx"
      provides: "(absent after this plan — deleted)"
    - path: "src/components/watch/WishlistRationalePanel.tsx"
      provides: "(absent after this plan — deleted)"
    - path: "src/components/watch/WishlistRationalePanel.test.tsx"
      provides: "(absent after this plan — deleted)"
    - path: "src/components/watch/PasteSection.tsx"
      provides: "(absent after this plan — deleted)"
    - path: "src/components/watch/PasteSection.test.tsx"
      provides: "(absent after this plan — deleted)"
    - path: "src/components/watch/RecentlyEvaluatedRail.tsx"
      provides: "(absent after this plan — deleted)"
    - path: "src/components/watch/RecentlyEvaluatedRail.test.tsx"
      provides: "(absent after this plan — deleted)"
    - path: "src/components/watch/flowTypes.ts"
      provides: "Pruned to only the FlowState discriminated union (Phase 70 D-01 7-variant shape) + DupeContext interface + their JSDoc — RailEntry, PendingTarget, and the Phase 71 forward-coordination JSDoc block (lines 66-93) deleted."
      contains: "export type FlowState"
      contains_also: "export interface DupeContext"
      forbids: "RailEntry"
      forbids_also: "PendingTarget"
      max_lines: 70
    - path: "src/components/watch/AddWatchFlow.tsx"
      provides: "Residue-swept: RailEntry removed from type-only import (line 24); JSDoc reworded forward-looking per D-03 (lines 35-36 + 52); useState + useRef + 8 setRail()/railRef references deleted at the 10 verified call sites (lines 103-104, 130, 133, 141-142, 146, 520, 579, 601, 786, 792)."
      forbids: "rail"
      forbids_also: "setRail"
      forbids_also2: "RailEntry"
      forbids_also3: "railRef"
  key_links:
    - from: "src/components/watch/AddWatchFlow.tsx (line 24, import type)"
      to: "src/components/watch/flowTypes.ts (export interface DupeContext + export type FlowState)"
      via: "import type { FlowState, DupeContext } from './flowTypes'"
      pattern: "import type \\{ FlowState, DupeContext \\}"
    - from: "src/components/watch/AddWatchFlow.tsx (reworded JSDoc)"
      to: "tests/static/AddWatchFlow.no-verdict-step.test.ts"
      via: "JSDoc prose names the static guard file path"
      pattern: "tests/static/AddWatchFlow.no-verdict-step"
    - from: "npm run build"
      to: "src/components/watch/AddWatchFlow.tsx + flowTypes.ts"
      via: "TypeScript compile (catches any missed rail/setRail/railRef sweep site)"
      pattern: "exit 0"
---

<objective>
Delete the 8 dead component+test files (~926 LOC), prune flowTypes.ts's RailEntry interface + PendingTarget alias + the Phase 71 forward-coordination JSDoc block, and sweep AddWatchFlow.tsx's RailEntry type-only import + the 10 `rail`/`setRail`/`railRef` call sites + reword the top JSDoc to forward-looking prose that names the Plan 71-01 static guard. Verify `npm run build` exits 0, both new static guards still pass, and the 6-file add-flow vitest suite stays green.

Purpose: Pure subtraction. Closes CLNP-01 (3 dead components removed + test files) and CLNP-04 (RecentlyEvaluatedRail disposition resolved + flowTypes residue swept). Zero user-visible behavior change; zero new code. The 12 deferred Phase 70 visual UAT items bundle with this plan's prod push per Memory feedback_mobile_ui_verify_on_prod.md.

Output: 8 file deletions; 2 file modifications (`flowTypes.ts` shrinks ~94 → ~65 LOC; `AddWatchFlow.tsx` shrinks by ~12 lines). `npm run build` exit 0 is the authoritative gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-dead-code-cleanup-static-guards/71-CONTEXT.md
@.planning/phases/71-dead-code-cleanup-static-guards/71-RESEARCH.md
@.planning/phases/71-dead-code-cleanup-static-guards/71-PATTERNS.md
@.planning/phases/71-dead-code-cleanup-static-guards/71-VALIDATION.md
@.planning/phases/71-dead-code-cleanup-static-guards/71-01-PLAN.md

# Files being pruned (must be in context — not the deleted ones)
@src/components/watch/flowTypes.ts
@src/components/watch/AddWatchFlow.tsx

# Static guards shipped by Plan 71-01 — enforce import-absence during this plan's deletes
@tests/static/AddWatchFlow.no-verdict-step.test.ts
@tests/static/AddWatchFlow.no-collection-fit-card.test.ts

<interfaces>
<!-- flowTypes.ts final shape after prune — only these two exports remain. -->
<!-- Everything ELSE in the file (RailEntry interface, PendingTarget alias, Phase 71 forward-coordination JSDoc block at lines 66-93) is deleted. -->

From src/components/watch/flowTypes.ts (after prune):
- `export type FlowState = | { kind: 'search-idle' } | { kind: 'extracting-url'; url: string } | { kind: 'extraction-failed'; partial: ExtractedWatchData | null; reason: string; category: ExtractErrorCategory; mode: 'url' | 'structured' } | { kind: 'confirming'; catalogId: string | null; extracted: ExtractedWatchData; pickedResult: SearchCatalogWatchResult | null; dupeContext: DupeContext | null; pending: boolean; photoBlob?: Blob | null } | { kind: 'form-prefill'; catalogId: string; extracted: ExtractedWatchData } | { kind: 'manual-entry'; partial?: ExtractedWatchData | null } | { kind: 'photos-pending'; watchId: string; destination: string }` — Phase 70 D-01 final 7-variant shape, untouched by Phase 71.
- `export interface DupeContext { existingWatchId: string; existingStatus: 'owned' | 'wishlist'; existingReference: string | null }` — Phase 70 D-06 active consumer, untouched.

From src/components/watch/AddWatchFlow.tsx (after sweep) — line 24 import:
- `import type { FlowState, DupeContext } from './flowTypes'` (RailEntry removed)

The 10 verified `rail`/`setRail`/`railRef` call sites to sweep (RESEARCH §2):
- Line 103: comment `// rail is preserved for Activity-hide cleanup safety; CLNP-04 deferred to Phase 71.`
- Line 104: `const [rail, setRail] = useState<RailEntry[]>([])`
- Line 130: `const railRef = useRef(rail)`
- Line 133: `railRef.current = rail`
- Line 141: comment referencing `railRef.current.length === 0` in skip-case-1
- Line 142: `if (s.kind === 'search-idle' && urlRef.current === '' && railRef.current.length === 0) return` — strip the `&& railRef.current.length === 0` predicate
- Line 146: `setRail([])` in useLayoutEffect cleanup
- Line 520: `setRail([])` in handleConfirmPrimary
- Line 579: `setRail([])` in handleMoveToCollection
- Line 601: `setRail([])` in handleWatchCreated
- Line 786: `setRail([])` in WatchPhotoStep onDone
- Line 792: `setRail([])` in WatchPhotoStep onSkip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 4 dead component+test pairs (8 files, ~926 LOC)</name>
  <files>src/components/watch/VerdictStep.tsx, src/components/watch/VerdictStep.test.tsx, src/components/watch/WishlistRationalePanel.tsx, src/components/watch/WishlistRationalePanel.test.tsx, src/components/watch/PasteSection.tsx, src/components/watch/PasteSection.test.tsx, src/components/watch/RecentlyEvaluatedRail.tsx, src/components/watch/RecentlyEvaluatedRail.test.tsx</files>
  <read_first>
    - .planning/phases/71-dead-code-cleanup-static-guards/71-CONTEXT.md §"Phase Boundary" (CLNP-01 + CLNP-04 deletion list)
    - .planning/phases/71-dead-code-cleanup-static-guards/71-RESEARCH.md §4 "Dead Component Files — All 8 Exist" (line counts) and §5 "Non-Test Consumer Audit — Zero Live Consumers" (safety verification)
    - .planning/phases/71-dead-code-cleanup-static-guards/71-RESEARCH.md §3 "flowTypes.test.ts — No RailEntry/PendingTarget Assertions" (confirms flowTypes.test.ts needs ZERO changes — Task 2's prune is the only flowTypes.ts touchpoint)
  </read_first>
  <action>
    `git rm` (or `rm`) the 8 files in two natural pairings — `.tsx` + `.test.tsx` together — per the executor's discretion (CONTEXT.md D-Discretion). Commit ordering within this task is the executor's call (single-commit-for-all-8 is acceptable; 4 paired commits is also acceptable — git-blame is preserved either way).

    The 8 file paths (verified to exist per RESEARCH §4):
    1. `src/components/watch/VerdictStep.tsx` (159 lines)
    2. `src/components/watch/VerdictStep.test.tsx` (196 lines)
    3. `src/components/watch/WishlistRationalePanel.tsx` (110 lines)
    4. `src/components/watch/WishlistRationalePanel.test.tsx` (129 lines)
    5. `src/components/watch/PasteSection.tsx` (83 lines)
    6. `src/components/watch/PasteSection.test.tsx` (92 lines)
    7. `src/components/watch/RecentlyEvaluatedRail.tsx` (64 lines)
    8. `src/components/watch/RecentlyEvaluatedRail.test.tsx` (93 lines)

    Total: 926 LOC. RESEARCH §5 verified zero non-test consumers; `grep -rn "from .*VerdictStep|WishlistRationalePanel|PasteSection|RecentlyEvaluatedRail" src/ tests/ | grep -v ".test."` returned empty output at research time. The only references in `AddWatchFlow.tsx` are JSDoc PROSE (not imports — Task 3 reworks those); the static guard from Plan 71-01 already asserts no imports.

    Do NOT delete any other files. Do NOT delete `flowTypes.test.ts` (it imports only `FlowState` and `DupeContext` per RESEARCH §3 — survives Phase 71 untouched). Do NOT delete `tests/static/CollectionFitCard.no-engine.test.ts` (it is Phase 20 D-04's own guard, not Phase 71 scope per CONTEXT.md "Not this phase").
  </action>
  <verify>
    <automated>test ! -e src/components/watch/VerdictStep.tsx && test ! -e src/components/watch/VerdictStep.test.tsx && test ! -e src/components/watch/WishlistRationalePanel.tsx && test ! -e src/components/watch/WishlistRationalePanel.test.tsx && test ! -e src/components/watch/PasteSection.tsx && test ! -e src/components/watch/PasteSection.test.tsx && test ! -e src/components/watch/RecentlyEvaluatedRail.tsx && test ! -e src/components/watch/RecentlyEvaluatedRail.test.tsx && echo "all 8 deleted"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 listed files are ABSENT after this task. `ls src/components/watch/VerdictStep.tsx src/components/watch/WishlistRationalePanel.tsx src/components/watch/PasteSection.tsx src/components/watch/RecentlyEvaluatedRail.tsx 2>&1 | grep -c "No such file"` returns 4 (each path errors).
    - All 4 `.test.tsx` counterparts also absent: same `ls` pattern returns 4 errors.
    - `git status` shows 8 deletions (D status) in the `src/components/watch/` directory.
    - `npm run build` will FAIL at this point with `Cannot find module './RecentlyEvaluatedRail'` IF RecentlyEvaluatedRail had a consumer — but RESEARCH §5 verified zero live consumers, so the only build-breaks come from Task 2 (RailEntry export still in flowTypes.ts after delete) and Task 3 (rail/setRail residue in AddWatchFlow.tsx). Build verification is deferred to Task 4 after all prunes land.
    - `tests/static/AddWatchFlow.no-verdict-step.test.ts` (Plan 71-01) still passes — vacuous-pass branch fires for paths that exist; deleted-file branches were never imports in AddWatchFlow.tsx so the guard is unaffected.
  </acceptance_criteria>
  <done>
    All 8 dead component + test files deleted. `git status` shows 8 deletions. Build will be temporarily red until Tasks 2-3 complete the prune (TypeScript still references RailEntry via flowTypes.ts exports + AddWatchFlow.tsx imports until those are pruned).
  </done>
</task>

<task type="auto">
  <name>Task 2: Prune flowTypes.ts — delete RailEntry, PendingTarget, and the Phase 71 forward-coordination JSDoc block</name>
  <files>src/components/watch/flowTypes.ts</files>
  <read_first>
    - src/components/watch/flowTypes.ts (current file — verify lines 66-93 contain exactly the JSDoc block + RailEntry interface + PendingTarget alias; lines 1-65 contain FlowState union + DupeContext + their JSDoc and must be preserved untouched)
    - .planning/phases/71-dead-code-cleanup-static-guards/71-PATTERNS.md §"src/components/watch/flowTypes.ts (prune lines 66-93)" for the verbatim line-by-line deletion table
    - .planning/phases/71-dead-code-cleanup-static-guards/71-RESEARCH.md §1 "flowTypes.ts — Exact Lines to Delete" for the verified delete range (lines 66-93)
    - .planning/phases/71-dead-code-cleanup-static-guards/71-RESEARCH.md §3 "flowTypes.test.ts — No RailEntry/PendingTarget Assertions" (confirms `flowTypes.test.ts` does NOT need updates)
  </read_first>
  <action>
    Edit `src/components/watch/flowTypes.ts`: delete lines 66-93 inclusive. This is the trailing tail of the file — everything from the blank line preceding the Phase 71 forward-coordination JSDoc (line 66) through the closing line of the `PendingTarget` type alias (line 93).

    What gets deleted (per PATTERNS.md §"src/components/watch/flowTypes.ts (prune lines 66-93)"):
    - Line 66: blank line
    - Lines 67-78: the Phase 71 forward-coordination JSDoc block (`/** Phase 71 forward-coordination — RailEntry + PendingTarget exports STAY in Phase 70 ... */`)
    - Lines 79-86: `export interface RailEntry { catalogId, brand, model, imageUrl, extracted, verdict: unknown | null }` (7 lines incl. closing brace + content lines)
    - Line 87: blank line
    - Lines 88-92: the `Pending state target for the legacy VerdictStep / WishlistRationalePanel pending-CTA disambiguation` JSDoc block
    - Line 93: `export type PendingTarget = 'wishlist' | 'collection' | 'skip' | null`

    What stays (lines 1-65):
    - Lines 1-3: type-only imports (ExtractedWatchData, ExtractErrorCategory, SearchCatalogWatchResult)
    - Lines 5-40: the 19-line transition map JSDoc (Phase 70 D-02)
    - Lines 41-48: `export type FlowState = ...` (7-variant union — Phase 70 D-01 final shape)
    - Lines 50-59: DupeContext JSDoc
    - Lines 60-64: `export interface DupeContext { existingWatchId, existingStatus, existingReference }`
    - Line 65: trailing blank (file's final newline)

    Optional (CONTEXT.md D-Discretion — implementer's call): add a single trailing comment marking the post-cleanup state, e.g. `// Phase 71 cleanup: file scope reduced to FlowState + DupeContext.` IF added, do NOT include literal forbidden tokens (no `RailEntry`, `PendingTarget`, `VerdictStep`, `WishlistRationalePanel`, `PasteSection`, `RecentlyEvaluatedRail`) — recurrence-prevention for the Phase 69/70 JSDoc-grep-collision pattern. Recommended: skip the trailing comment entirely; file's surviving JSDoc on FlowState + DupeContext already documents intent.

    Do NOT touch lines 1-65. Do NOT modify the FlowState union (Phase 70 D-01 is the LOCKED final shape). Do NOT modify DupeContext (Phase 70 D-06 active consumer in `confirming` branch). Do NOT touch `flowTypes.test.ts` — RESEARCH §3 confirmed it imports only FlowState + DupeContext.
  </action>
  <verify>
    <automated>npx vitest run src/components/watch/flowTypes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l src/components/watch/flowTypes.ts` reports approximately 65 lines (was 93; -28 lines; the optional trailing comment may shift this by ±1-3).
    - `grep -c "RailEntry" src/components/watch/flowTypes.ts` returns 0 (interface gone, no trailing JSDoc reference).
    - `grep -c "PendingTarget" src/components/watch/flowTypes.ts` returns 0.
    - `grep -c "Phase 71 forward-coordination" src/components/watch/flowTypes.ts` returns 0.
    - `grep -c "VerdictStep\|WishlistRationalePanel\|PasteSection\|RecentlyEvaluatedRail" src/components/watch/flowTypes.ts` returns 0 (paraphrase or omit if any prose remains — JSDoc-grep-collision prevention).
    - `grep -c "export type FlowState" src/components/watch/flowTypes.ts` returns 1 (preserved).
    - `grep -c "export interface DupeContext" src/components/watch/flowTypes.ts` returns 1 (preserved).
    - The FlowState union still contains all 7 kinds verbatim from Phase 70 D-01: `search-idle`, `extracting-url`, `extraction-failed`, `confirming`, `form-prefill`, `manual-entry`, `photos-pending`. (Spot-check via `grep "kind: '" src/components/watch/flowTypes.ts | wc -l` returns 7.)
    - `npx vitest run src/components/watch/flowTypes.test.ts` exits 0 — all 4 existing tests (FlowState kind enumeration, REMOVED_KINDS non-overlap, DupeContext literal type-check, extraction-failed.mode) still green per RESEARCH §3.
    - Build will STILL be temporarily red here because AddWatchFlow.tsx still imports `RailEntry` from this file — Task 3 fixes that.
  </acceptance_criteria>
  <done>
    `flowTypes.ts` pruned to ~65 lines containing only FlowState + DupeContext + their JSDoc. `flowTypes.test.ts` stays green untouched (no RailEntry/PendingTarget assertions to update). Build still red pending Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sweep AddWatchFlow.tsx — type-only import, 10 rail/setRail/railRef sites, JSDoc reword</name>
  <files>src/components/watch/AddWatchFlow.tsx</files>
  <read_first>
    - src/components/watch/AddWatchFlow.tsx (current file — verify the 10 verified RESEARCH §2 call sites are still at the cited line numbers; the file is 885 lines per RESEARCH so spot-check lines 24, 35-36, 52, 103-104, 130, 133, 141-142, 146 explicitly via Read with `offset`/`limit`)
    - .planning/phases/71-dead-code-cleanup-static-guards/71-PATTERNS.md §"src/components/watch/AddWatchFlow.tsx (10-site prune + JSDoc reword)" for the verbatim per-site diff table
    - .planning/phases/71-dead-code-cleanup-static-guards/71-RESEARCH.md §2 "AddWatchFlow.tsx — All Residue Locations" and Pitfall 2 (missing any site breaks the build)
    - tests/static/AddWatchFlow.no-verdict-step.test.ts (Plan 71-01 guard — confirms zero VerdictStep/WishlistRationalePanel/PasteSection imports must remain after this sweep)
  </read_first>
  <action>
    Make THREE coordinated edits to `src/components/watch/AddWatchFlow.tsx`:

    **Edit A — Line 24 type-only import (RESEARCH Pitfall 3):**
    Change `import type { FlowState, RailEntry, DupeContext } from './flowTypes'` to `import type { FlowState, DupeContext } from './flowTypes'`. Removing `RailEntry,` from the import list. If the import is not adjusted, the build fails with `Module './flowTypes' has no exported member 'RailEntry'` (Task 2 deleted it).

    **Edit B — JSDoc reword per D-03 (lines 35-36 + 52):**
    Replace the dead-component name list in the top JSDoc with a forward-looking statement that names `tests/static/AddWatchFlow.no-verdict-step.test.ts`. Exact prose is implementer's discretion (CONTEXT.md D-Discretion); the structural change per D-03 is: (1) remove `Hard-cutover: no PasteSection / VerdictStep / WishlistRationalePanel / RecentlyEvaluatedRail / useWatchSearchVerdictCache / verdict imports.` from lines 35-37, and (2) remove `(no PasteSection)` from line 52's D-14 description. Target prose tone (terse + forward-looking + names the enforcement mechanism + 2-4 lines): something like `AddWatchFlow is the search-first orchestrator (Phase 70). The legacy verdict-flow surface is deleted; tests/static/AddWatchFlow.no-verdict-step.test.ts prevents reintroduction.` For line 52: `D-14 extracting-url is INLINE; {mode:'url',url} body.` (drop the parenthetical).

    CRITICAL recurrence-prevention (Memory feedback_decision_coverage_gate_citations.md + Phase 69/70 JSDoc-grep-collision recurrence-3): the reworded JSDoc MUST avoid the literal tokens `VerdictStep`, `WishlistRationalePanel`, `PasteSection`, `RecentlyEvaluatedRail` in prose. Paraphrase as "legacy verdict-flow components" or similar. The PATH `tests/static/AddWatchFlow.no-verdict-step.test.ts` IS allowed — the hyphenated `no-verdict-step` does not match the regex `/from ['"](?:.*\/)?VerdictStep['"]/` from Plan 71-01's CLNP-02 guard.

    **Edit C — Sweep ALL 10 rail/setRail/railRef call sites (RESEARCH Pitfall 2 — CRITICAL):**
    Per RESEARCH §2 verified line numbers, delete these (line numbers will shift as edits happen; treat them as anchors at file load time, find by token after the first delete):
    - Line 103: comment `// rail is preserved for Activity-hide cleanup safety; CLNP-04 deferred to Phase 71.` — delete
    - Line 104: `const [rail, setRail] = useState<RailEntry[]>([])` — delete
    - Line 130: `const railRef = useRef(rail)` — delete
    - Line 133: `railRef.current = rail` — delete
    - Line 141: comment `// Skip case 1: nothing user-accumulated to reset (search-idle, no URL, no rail).` — reword to drop `no rail` reference, e.g. `// Skip case 1: nothing user-accumulated to reset (search-idle, no URL).`
    - Line 142: `if (s.kind === 'search-idle' && urlRef.current === '' && railRef.current.length === 0) return` — change to `if (s.kind === 'search-idle' && urlRef.current === '') return` (strip ` && railRef.current.length === 0`)
    - Line 146: `setRail([])` in useLayoutEffect cleanup branch — delete the whole statement line
    - Line 520: `setRail([])` in handleConfirmPrimary — delete
    - Line 579: `setRail([])` in handleMoveToCollection — delete
    - Line 601: `setRail([])` in handleWatchCreated non-owned branch — delete
    - Line 786: `setRail([])` in WatchPhotoStep onDone — delete
    - Line 792: `setRail([])` in WatchPhotoStep onSkip — delete

    That is 12 distinct edits across 10+ original line numbers (RESEARCH §2 lists 10 logical "sites" — some have a comment + statement pair like lines 103+104 and 141+142). Use Edit tool with unique-anchor matches; after the first delete, use literal token matching (grep for `rail`, `setRail`, `railRef`) rather than relying on line numbers since they shift.

    `stateRef` and `urlRef` stay (still consumed by skip-case-1 and the cleanup branch). The skip-case-1 condition `s.kind === 'search-idle' && urlRef.current === ''` remains semantically correct — "nothing user-accumulated to reset" reduces to "search-idle with no URL input" (RESEARCH §2 Skip-case-1 note).

    Final state: zero occurrences of `rail`, `setRail`, `railRef`, `RailEntry` anywhere in AddWatchFlow.tsx. Build must compile.
  </action>
  <verify>
    <automated>grep -cE "\\b(rail|setRail|railRef|RailEntry)\\b" src/components/watch/AddWatchFlow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\\b(rail|setRail|railRef|RailEntry)\\b" src/components/watch/AddWatchFlow.tsx` returns 0 — ZERO occurrences of any of the four forbidden tokens with word-boundary anchoring (does not falsely match `derailment` or similar — but no such words appear in the file).
    - `grep -c "import type { FlowState, DupeContext } from './flowTypes'" src/components/watch/AddWatchFlow.tsx` returns 1 (target import shape present, RailEntry removed).
    - `grep -c "import type { FlowState, RailEntry" src/components/watch/AddWatchFlow.tsx` returns 0 (old import shape gone).
    - `grep -c "tests/static/AddWatchFlow.no-verdict-step" src/components/watch/AddWatchFlow.tsx` returns at least 1 (JSDoc reworded to name the static guard file path per D-03).
    - `grep -cE "\\b(VerdictStep|WishlistRationalePanel|PasteSection|RecentlyEvaluatedRail)\\b" src/components/watch/AddWatchFlow.tsx` returns 0 — JSDoc reword scrubs every dead-component literal token (recurrence-prevention; hyphenated path `no-verdict-step` does NOT match the word-boundary regex because the `-` is a word boundary).
    - `grep -c "Hard-cutover: no" src/components/watch/AddWatchFlow.tsx` returns 0 (old prose gone).
    - The 6-file add-flow targeted vitest suite still passes: `npx vitest run src/components/watch/AddWatchFlow.test.tsx src/components/watch/SearchEntry.test.tsx src/components/watch/StructuredEntryPanel.test.tsx src/components/watch/ConfirmStep.test.tsx src/components/watch/DupeBanner.test.tsx src/components/watch/flowTypes.test.ts` exits 0.
    - `tests/static/AddWatchFlow.no-verdict-step.test.ts` (Plan 71-01 guard) still GREEN after the sweep — confirms no accidental re-import.
    - `tests/static/AddWatchFlow.no-collection-fit-card.test.ts` (Plan 71-01 guard) still GREEN — confirms no CollectionFitCard import was introduced during the sweep.
    - Build verification is in Task 4; this task's grep-based gates demonstrate completeness of the sweep BEFORE the build runs.
  </acceptance_criteria>
  <done>
    AddWatchFlow.tsx is fully swept: zero `rail`/`setRail`/`railRef`/`RailEntry` tokens; JSDoc reworded forward-looking with the static guard path named; the 6-file add-flow vitest suite passes; both Plan 71-01 static guards still GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify — npm run build exit 0 + static suite + full add-flow vitest</name>
  <files></files>
  <read_first>
    - Memory project_baseline_not_green_build_is_gate.md (build is THE gate; full tsc/vitest carry pre-existing noise)
    - Memory project_vitest_static_node_env.md (both new guards needed `// @vitest-environment node` — Plan 71-01 did that; this task confirms prebuild still passes after Plan 71-02 deletes)
    - .planning/phases/71-dead-code-cleanup-static-guards/71-VALIDATION.md §"Test Infrastructure" (full suite command authoritative)
  </read_first>
  <action>
    Run the three authoritative gates in order and confirm each exits 0:

    1. `npm run build` — Next.js production build (the authoritative gate per Memory). Confirms zero TypeScript errors triggered by Task 3's residue sweep. If RED with `Cannot find name 'rail' | 'setRail' | 'railRef'`, Task 3 missed a sweep site — re-grep AddWatchFlow.tsx, fix, re-run.
    2. `npx vitest run tests/static/` — runs the entire static guard directory (10+ files including the two new Phase 71 guards from Plan 71-01). Confirms no static-guard regression from this plan's deletes.
    3. `npx vitest run src/components/watch/AddWatchFlow.test.tsx src/components/watch/SearchEntry.test.tsx src/components/watch/StructuredEntryPanel.test.tsx src/components/watch/ConfirmStep.test.tsx src/components/watch/DupeBanner.test.tsx src/components/watch/flowTypes.test.ts` — the 6-file add-flow targeted suite. Confirms no functional regression in the live add-flow surface.

    Do NOT run `npx tsc --noEmit` as the gate — it carries ~77 pre-existing test-file errors. Do NOT run `npm test` (full `vitest run`) as the gate — it carries ≥1 pre-existing failure (CommentGateLocked font-medium per Phase 69 Plan 04 recurrence-2). Both are baseline noise unrelated to this phase.

    If any of the three gates fails, capture the failure mode in the SUMMARY and stop — do NOT push. Re-investigate via grep + targeted Read. Most likely failure modes:
    - Build red: missed `rail`/`setRail`/`railRef` site in AddWatchFlow.tsx → grep + fix
    - Static suite red: prebuild not yet wired (but Plan 71-01 already wired it, so this should not happen)
    - Add-flow suite red: an existing test asserted on rail state (RESEARCH §2 implied none do, but spot-check `npx vitest run src/components/watch/AddWatchFlow.test.tsx --reporter=verbose` output if red)
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/static/ && npx vitest run src/components/watch/AddWatchFlow.test.tsx src/components/watch/SearchEntry.test.tsx src/components/watch/StructuredEntryPanel.test.tsx src/components/watch/ConfirmStep.test.tsx src/components/watch/DupeBanner.test.tsx src/components/watch/flowTypes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 — Next.js production build successful, zero TypeScript errors. This is the authoritative gate per Memory project_baseline_not_green_build_is_gate.md.
    - `npx vitest run tests/static/` exits 0 — full static-guard directory green (both new Phase 71 guards + every existing one including CollectionFitCard.no-engine.test.ts, legacy-watch-routes.test.ts, ppr-dynamic-before-use-cache.test.ts, followed-owners-module-rsc.test.ts).
    - The 6-file add-flow targeted vitest suite exits 0 — AddWatchFlow.test.tsx, SearchEntry.test.tsx, StructuredEntryPanel.test.tsx, ConfirmStep.test.tsx, DupeBanner.test.tsx, flowTypes.test.ts all green.
    - `git status` shows: 8 deletions (Task 1) + 1 modification of flowTypes.ts (Task 2) + 1 modification of AddWatchFlow.tsx (Task 3). No other files touched.
    - `grep -rn "from .*VerdictStep\|from .*WishlistRationalePanel\|from .*PasteSection\|from .*RecentlyEvaluatedRail" src/ tests/` returns empty output (zero remaining imports of any deleted component).
    - `grep -rn "from .*flowTypes" src/ | grep -c "RailEntry\|PendingTarget"` returns 0 (no surviving imports of the deleted exports).
  </acceptance_criteria>
  <done>
    `npm run build` exit 0; static suite green (12+ test files); 6-file add-flow suite green. Phase 71 functionally complete. Ready for bundled prod push with Phase 70's 12 deferred visual UAT items per Memory feedback_mobile_ui_verify_on_prod.md.
  </done>
</task>

</tasks>

<verification>
After all four tasks land:

1. **Build gate** — `npm run build` exit 0. This is THE gate (Memory project_baseline_not_green_build_is_gate.md).
2. **Static suite** — `npx vitest run tests/static/` exit 0 (12+ files including both new Phase 71 guards from Plan 71-01).
3. **Add-flow targeted suite** — `npx vitest run src/components/watch/AddWatchFlow.test.tsx src/components/watch/SearchEntry.test.tsx src/components/watch/StructuredEntryPanel.test.tsx src/components/watch/ConfirmStep.test.tsx src/components/watch/DupeBanner.test.tsx src/components/watch/flowTypes.test.ts` exit 0.
4. **File-absence audit** — 8 dead component+test files gone; `flowTypes.ts` is ~65 lines (was 93); `AddWatchFlow.tsx` has zero `rail`/`setRail`/`railRef`/`RailEntry` tokens.
5. **Static-guard semantic verification** — `tests/static/AddWatchFlow.no-verdict-step.test.ts` (3 it() cases green) AND `tests/static/AddWatchFlow.no-collection-fit-card.test.ts` (8 it() cases green) both confirm post-cleanup the import-absence invariants hold.
6. **Forward-look** — `tests/static/AddWatchFlow.no-verdict-step.test.ts` path is named in AddWatchFlow.tsx's reworded JSDoc per D-03.

Do NOT use full `tsc --noEmit` or full `npm test` as gates — Memory project_baseline_not_green_build_is_gate.md documents pre-existing baseline noise in both (~77 tsc errors in test files; ≥1 vitest failure in CommentGateLocked font-medium). Build is the authoritative gate.

Phase 71 is pure subtraction with zero user-visible behavior change. Bundle with Phase 70's 12 deferred visual UAT items on the prod push per Memory feedback_mobile_ui_verify_on_prod.md.
</verification>

<success_criteria>
- [ ] 8 files deleted: VerdictStep.tsx + .test.tsx, WishlistRationalePanel.tsx + .test.tsx, PasteSection.tsx + .test.tsx, RecentlyEvaluatedRail.tsx + .test.tsx.
- [ ] `flowTypes.ts` is ~65 lines and contains only `FlowState` + `DupeContext` + their JSDoc; zero `RailEntry` or `PendingTarget` occurrences.
- [ ] `AddWatchFlow.tsx`: line 24 import is `import type { FlowState, DupeContext } from './flowTypes'`; zero `rail`/`setRail`/`railRef`/`RailEntry` tokens; JSDoc reworded to name `tests/static/AddWatchFlow.no-verdict-step.test.ts`; reworded JSDoc avoids literal `VerdictStep`/`WishlistRationalePanel`/`PasteSection`/`RecentlyEvaluatedRail` word tokens.
- [ ] `npm run build` exit 0.
- [ ] `npx vitest run tests/static/` exit 0 (all guards, including the 2 Phase 71 guards from Plan 71-01).
- [ ] 6-file add-flow targeted vitest suite exit 0.
- [ ] `flowTypes.test.ts` unchanged (RESEARCH §3 confirmed it only imports FlowState + DupeContext; needs zero updates).
- [ ] Plan 71-01's CLNP-02 + CLNP-03 guards still GREEN — confirms no accidental re-import during this plan's sweep.
</success_criteria>

<output>
After completion, create `.planning/phases/71-dead-code-cleanup-static-guards/71-02-SUMMARY.md` per the standard summary template. Include:
- 8 file deletions (paths + line counts; total ~926 LOC subtracted)
- `flowTypes.ts` final shape (line count + remaining exports)
- The 10+ verified `rail`/`setRail`/`railRef` call sites swept (RESEARCH §2 line numbers)
- The reworded AddWatchFlow.tsx JSDoc (verbatim) — for the next maintainer's reference
- `npm run build` + static suite + add-flow suite verification output (exit codes + counts)
- Forward-look: this is the last plan of Phase 71; Phase 71's prod push bundles with Phase 70's 12 deferred visual UAT items; STATE.md update + ROADMAP CLNP-01/02/03/04 checkbox flips happen at `/gsd-verify-phase 71`.
</output>
