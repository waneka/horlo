---
phase: 48
plan: 03
type: execute
wave: 2
depends_on: [48-02]
files_modified:
  - src/components/search/BrandChips.tsx
  - src/components/search/EraChips.tsx
  - src/components/search/GenreChips.tsx
  - src/components/search/ArchetypeChips.tsx
  - src/components/search/MovementChips.tsx
  - src/components/search/CaseSizeChips.tsx
  - src/components/search/StyleChips.tsx
  - src/components/search/SearchPageClient.tsx
autonomous: false
requirements: [BUG-02]
must_haves:
  truths:
    - "All 7 drawer chip components (Brand/Era/Genre/Archetype/Movement/CaseSize/Style) render via the shared <Chip> primitive — no per-component inline className strings"
    - "Both occurrences of the removable chip block in SearchPageClient.tsx (zero-results branch + results branch) render via <Chip variant='removable'>"
    - "Zero `text-accent-foreground` literal hits remain inside src/components/search/ (BUG-02 statically closed)"
    - "Zero `bg-accent/10` literal hits remain inside src/components/search/ (the tinted-pill pattern only lives inside the chip primitive)"
    - "All 7 drawer chip components retain their existing single-select / multi-select selection semantics (BrandChips toggles via slug-equality null, StyleChips toggles via array membership)"
    - "Dark-mode visual check at /search confirms all chip groups + removable inline chips show legible text on tinted/solid surfaces (manual UAT — jsdom cannot resolve oklch paint values)"
  artifacts:
    - path: "src/components/search/BrandChips.tsx"
      provides: "BrandChips component using <Chip variant='toggle' selected={...}>"
      contains: "import { Chip } from '@/components/ui/chip'"
    - path: "src/components/search/StyleChips.tsx"
      provides: "StyleChips multi-select component using <Chip variant='toggle' selected={selected.includes(tag)}>"
      contains: "import { Chip } from '@/components/ui/chip'"
    - path: "src/components/search/SearchPageClient.tsx"
      provides: "Removable inline chips replaced with <Chip variant='removable' removeLabel='Remove X filter'> at BOTH original sites (~lines 410-454 zero-results, ~lines 491-537 results)"
      contains: "<Chip variant=\"removable\""
  key_links:
    - from: "src/components/search/{Brand,Era,Genre,Archetype,Movement,CaseSize,Style}Chips.tsx"
      to: "src/components/ui/chip.tsx <Chip>"
      via: "import { Chip } from '@/components/ui/chip' + <Chip variant='toggle' selected={isSelected}>"
      pattern: "<Chip variant=\"toggle\""
    - from: "src/components/search/SearchPageClient.tsx (both chip blocks)"
      to: "src/components/ui/chip.tsx <Chip variant='removable'>"
      via: "import { Chip } + <Chip variant='removable' onClick={onClearX} removeLabel={`Remove ${label} filter`}>"
      pattern: "<Chip variant=\"removable\""
---

<objective>
Migrate all 8 chip surfaces — the 7 drawer chip components (Brand/Era/Genre/Archetype/Movement/CaseSize/Style) AND the two inline removable chip blocks in SearchPageClient.tsx (lines ~410-454 zero-results branch, lines ~491-537 results branch) — onto the shared `<Chip>` primitive shipped in Plan 02. Both the BUG-02 dark-mode fix (now embedded inside the primitive's `removable` variant per D-09) and the D-07/D-08 consolidation become observable in the running app after this plan.

Purpose: This is the wave where BUG-02 becomes user-visible-fixed and the chip consolidation refactor lands in production code. Per D-08, drawer and removable chips end up visually consistent (shape, padding, focus ring, transition); the deliberate variant-specific differences (background, weight, trailing X) remain — UI-SPEC.md §"Visual Consistency Between Variants" enumerates each. Per RESEARCH.md Pitfall 2, BOTH SearchPageClient chip blocks must be migrated; per RESEARCH.md Pitfall 4, dark-mode token contrast is verified only via manual UAT (jsdom cannot resolve oklch paint values).

Output: 8 component files refactored to use the primitive; zero `text-accent-foreground` and zero `bg-accent/10` literal hits inside `src/components/search/`; the dev server + manual dark-mode UAT confirms BUG-02 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/48-user-facing-bug-fixes/48-CONTEXT.md
@.planning/phases/48-user-facing-bug-fixes/48-RESEARCH.md
@.planning/phases/48-user-facing-bug-fixes/48-PATTERNS.md
@.planning/phases/48-user-facing-bug-fixes/48-UI-SPEC.md
@.planning/phases/48-user-facing-bug-fixes/48-VALIDATION.md
@.planning/phases/48-user-facing-bug-fixes/48-02-SUMMARY.md

@src/components/ui/chip.tsx
@src/components/search/BrandChips.tsx
@src/components/search/EraChips.tsx
@src/components/search/GenreChips.tsx
@src/components/search/ArchetypeChips.tsx
@src/components/search/MovementChips.tsx
@src/components/search/CaseSizeChips.tsx
@src/components/search/StyleChips.tsx
@src/components/search/SearchPageClient.tsx
@src/components/search/FilterDrawer.tsx

<interfaces>
<!-- The contract from Plan 02 — these are the props your call sites use. -->

import { Chip } from '@/components/ui/chip'

<Chip
  variant="toggle"
  selected={isSelected}            // boolean — caller computes (slug-equality or array-includes)
  aria-pressed={isSelected}        // standard <button> prop — passes through
  onClick={() => ...}              // toggle logic stays in the caller
>
  {labelText}
</Chip>

<Chip
  variant="removable"
  onClick={onClearFacet}           // single-action clear
  removeLabel={`Remove ${displayLabel} filter`}   // sr-only string
>
  {displayLabel}
</Chip>

Per-surface migration details:
- 6 single-select drawer chips (Brand, Era, Genre, Archetype, Movement, CaseSize) — `isSelected = selected === entry.slug` (or equivalent); toggle behavior: `onSelect(isSelected ? null : entry.slug)`. Existing semantics unchanged.
- 1 multi-select drawer chip (Style) — `isSelected = selected.includes(tag)`; toggle: `onSelect(isSelected ? selected.filter(s=>s!==tag) : [...selected, tag])`. Existing semantics unchanged. The primitive accepts `selected: boolean` — StyleChips computes the boolean per chip; multi-select array state lives in the parent.
- SearchPageClient inline removable chips: 4 facets (archetype, brand, era, genre) × 2 occurrences (zero-results block + results block) = 8 individual <Chip> instances. The `<X>` icon import from `lucide-react` (line 4 of SearchPageClient.tsx) can be REMOVED from SearchPageClient AFTER both blocks are migrated IFF no other `X` usage remains in the file — verify with `grep -n '\\bX\\b' src/components/search/SearchPageClient.tsx` AFTER refactor.

Manual UAT (per RESEARCH.md Pitfall 4 / UI-SPEC.md §"CSS Chain Assertion" / VALIDATION.md §"Manual-Only Verifications" / memory `feedback_ui_spec_css_chain_blind_spot`):
- jsdom cannot resolve `oklch(...)` CSS custom property values — automated contrast assertions are not possible.
- Dark-mode verification is REQUIRED before closing BUG-02 — run dev server, open /search, apply filters, toggle to .dark theme, visually confirm legible text on all chip groups + inline removable chips.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the 7 drawer chip components to <Chip variant="toggle"></name>
  <files>src/components/search/BrandChips.tsx, src/components/search/EraChips.tsx, src/components/search/GenreChips.tsx, src/components/search/ArchetypeChips.tsx, src/components/search/MovementChips.tsx, src/components/search/CaseSizeChips.tsx, src/components/search/StyleChips.tsx</files>
  <read_first>
    - src/components/ui/chip.tsx (the primitive — confirm exact prop shape from Plan 02)
    - src/components/search/BrandChips.tsx (the single-select template — pattern repeated 5 more times)
    - src/components/search/StyleChips.tsx (the multi-select divergence — note `selected.includes(tag)` computes the boolean)
    - src/components/search/EraChips.tsx (uses internal ERA_SIGNALS constant + ERA_DISPLAY_LABELS — vocab is local, not a prop)
    - .planning/phases/48-user-facing-bug-fixes/48-PATTERNS.md §"src/components/search/BrandChips.tsx", §"src/components/search/EraChips.tsx", §"src/components/search/StyleChips.tsx"
    - tests/components/search/ (any pre-existing tests on these chips — read first to confirm no contract breakage)
  </read_first>
  <behavior>
    - All 6 single-select drawer chip components (Brand, Era, Genre, Archetype, Movement, CaseSize) compile and render exactly the same selection behavior as before — clicking an unselected chip selects it; clicking a selected chip deselects it (sets `onSelect(null)`).
    - StyleChips continues to toggle individual tag membership in the `selected: string[]` array (clicking adds the tag if absent, removes it if present).
    - Existing per-component vocab handling is preserved:
      - BrandChips uses the `vocab: { slug, name }[]` prop.
      - EraChips uses the internal `ERA_SIGNALS` + `ERA_DISPLAY_LABELS` constants.
      - GenreChips, ArchetypeChips, MovementChips, CaseSizeChips each use their respective internal constant.
      - StyleChips uses the `vocab: string[]` prop with the `tag.charAt(0).toUpperCase() + tag.slice(1)` display transform.
    - The `aria-pressed={isSelected}` attribute remains on every chip (accessibility unchanged).
    - All 7 files retain their `'use client'` directive on line 1.
    - The `cn` import is REMOVED from each of the 7 files (no longer needed once the inline className strings are gone).
  </behavior>
  <action>
    For each of the 7 files (BrandChips.tsx, EraChips.tsx, GenreChips.tsx, ArchetypeChips.tsx, MovementChips.tsx, CaseSizeChips.tsx, StyleChips.tsx): (1) Replace `import { cn } from '@/lib/utils'` with `import { Chip } from '@/components/ui/chip'`. (2) Inside the `.map(...)` callback, replace the `<button type="button" aria-pressed={isSelected} className={cn(...)} onClick={...}>{label}</button>` block with `<Chip key={...} variant="toggle" selected={isSelected} aria-pressed={isSelected} onClick={...}>{label}</Chip>`. The `key` prop, `onClick` handler body, and label expression are copied verbatim from the existing code — selection-toggle logic stays in the parent. (3) Preserve all surrounding structure: the wrapping `<div className="flex flex-col gap-2">`, the `<p className="text-base font-semibold text-foreground">{groupLabel}</p>` header, the inner `<div className="flex flex-wrap gap-2" role="group" aria-label="...">`, and any per-component internal constants (ERA_SIGNALS, PRIMARY_ARCHETYPES, MOVEMENT_OPTIONS, CASE_SIZE_OPTIONS, ARCHETYPE_CONFIG, etc.) are UNCHANGED.

    For StyleChips specifically — the multi-select divergence: `isSelected = selected.includes(tag)` (existing line 17) computes a boolean, which is the value passed to `<Chip selected={isSelected}>`. The onClick toggle-array logic (existing lines 31-36, `selected.filter(s => s !== tag)` / `[...selected, tag]`) stays unchanged in StyleChips. The primitive remains stateless per D-07; StyleChips continues to own array membership.

    Do NOT touch FilterDrawer.tsx, FilterSheet.tsx, or any other file in this task. Do NOT change the chip primitive (Plan 02 owns that). Do NOT modify SearchPageClient.tsx in this task (Task 2 owns that).
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -L "import { Chip } from '@/components/ui/chip'" src/components/search/BrandChips.tsx src/components/search/EraChips.tsx src/components/search/GenreChips.tsx src/components/search/ArchetypeChips.tsx src/components/search/MovementChips.tsx src/components/search/CaseSizeChips.tsx src/components/search/StyleChips.tsx` returns empty (all 7 files contain the import).
    - `grep -l "import { cn } from '@/lib/utils'" src/components/search/BrandChips.tsx src/components/search/EraChips.tsx src/components/search/GenreChips.tsx src/components/search/ArchetypeChips.tsx src/components/search/MovementChips.tsx src/components/search/CaseSizeChips.tsx src/components/search/StyleChips.tsx` returns empty (cn no longer imported in any of the 7).
    - For each of the 7 files: `grep -c "<Chip variant=\"toggle\"" $FILE` returns at least 1.
    - For each of the 7 files: `grep -c "<button" $FILE` returns 0 (no inline button markup remains for the chips themselves).
    - `grep -c "text-accent-foreground" src/components/search/{Brand,Era,Genre,Archetype,Movement,CaseSize,Style}Chips.tsx` returns 0 total across all 7 files.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` exits 0 (no test regressions — chip primitive unit tests still green, catalog-page tests still green from Plan 01, any pre-existing chip component tests still green).
    - All 7 files still have `'use client'` on line 1: `grep -l "^'use client'" src/components/search/{Brand,Era,Genre,Archetype,Movement,CaseSize,Style}Chips.tsx` returns 7 paths.
  </acceptance_criteria>
  <done>All 7 drawer chip components route through `<Chip variant="toggle" selected={isSelected}>`; the `cn` import is gone from each; selection semantics are preserved (single-select null toggle + multi-select array membership); the test suite is green; TypeScript compiles. SearchPageClient.tsx is the only remaining surface — Task 2 handles it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate both inline removable chip blocks in SearchPageClient.tsx — closes BUG-02 in user-visible code</name>
  <files>src/components/search/SearchPageClient.tsx</files>
  <read_first>
    - src/components/ui/chip.tsx (the primitive's removable-variant API)
    - src/components/search/SearchPageClient.tsx (full file — both chip blocks at ~lines 408-454 and ~lines 491-537; lucide-react import at line 4)
    - .planning/phases/48-user-facing-bug-fixes/48-PATTERNS.md §"src/components/search/SearchPageClient.tsx" (the migration pattern; the X-icon import note)
    - .planning/phases/48-user-facing-bug-fixes/48-RESEARCH.md §"Pitfall 2: Forgetting the Duplicate Chip Block"
  </read_first>
  <behavior>
    - Both chip blocks (zero-results branch ~lines 408-454 AND results branch ~lines 491-537) render via `<Chip variant="removable">`.
    - Each of the 8 individual chip instances (4 facets × 2 blocks) preserves its existing behavior: same `onClick` handler (`onClearArchetype`, `onClearBrand`, `onClearEra`, `onClearGenre`), same display-label expression, same sr-only "Remove X filter" announcement.
    - The `X` icon and `<span className="sr-only">` move from the call site into the primitive's internal markup — SearchPageClient no longer renders either directly for these chips.
    - The `X` import from `lucide-react` (current line 4) is REMOVED from SearchPageClient.tsx iff no other usage of `X` remains in the file. If another usage remains, leave the import.
    - The component still compiles; all tests still pass; no behavioral regression (filter clearing still works, screen readers still announce removal).
  </behavior>
  <action>
    In `src/components/search/SearchPageClient.tsx`: (1) Add `import { Chip } from '@/components/ui/chip'` to the existing import block at the top of the file. (2) Locate BOTH chip blocks — the zero-results branch (~lines 408-454) and the results branch (~lines 491-537). For EACH of the 8 individual chip <button> instances (4 facets per block, archetype/brand/era/genre): replace the `<button type="button" onClick={onClearX} className="inline-flex items-center gap-1 rounded-full border border-accent bg-accent/10 px-3 py-1 text-sm font-semibold text-accent-foreground hover:bg-accent/20 transition-colors"><span>{label}</span><X className="size-3" aria-hidden /><span className="sr-only">Remove {label} filter</span></button>` block with `<Chip variant="removable" onClick={onClearX} removeLabel={\`Remove ${label} filter\`}>{label}</Chip>` — where `label` is the existing displayLabel expression (e.g., `archetypeConfig?.displayName ?? archetype`, the brand display name resolved from the vocab, etc.). The primitive renders the X icon + sr-only span internally. (3) After both blocks are migrated, run `grep -n '\\bX\\b' src/components/search/SearchPageClient.tsx` to confirm no other `X` usage remains; if 0 hits remain, remove `X` from the `import { Search, SlidersHorizontalIcon, X } from 'lucide-react'` line (line 4). If hits remain (e.g., `X` used as an icon elsewhere), leave the import. (4) Do NOT change any other code in SearchPageClient.tsx — no logic, no other JSX, no other imports.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && bash -c "test $(grep -c 'text-accent-foreground' src/components/search/SearchPageClient.tsx) -eq 0 && test $(grep -c 'bg-accent/10' src/components/search/SearchPageClient.tsx) -eq 0 && echo BUG-02-STATIC-PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<Chip variant=\"removable\"" src/components/search/SearchPageClient.tsx` returns at least 8 (4 facets × 2 blocks).
    - `grep -c "text-accent-foreground" src/components/search/SearchPageClient.tsx` returns 0 (the broken token is gone from the file).
    - `grep -c "bg-accent/10" src/components/search/SearchPageClient.tsx` returns 0 (the tinted-pill className now lives only in the primitive).
    - `grep -c "Remove .* filter" src/components/search/SearchPageClient.tsx` returns at least 8 (the sr-only label strings are now in the removeLabel prop strings — assertable via the `Remove ... filter` template literal pattern).
    - `grep -c "import { Chip } from '@/components/ui/chip'" src/components/search/SearchPageClient.tsx` returns 1.
    - After Task 1 + Task 2: `grep -rc "text-accent-foreground" src/components/search/ | grep -v ':0' | grep -v 'WatchSearchRowsAccordion.test.tsx'` returns no chip-component hits (the search subtree is statically free of the broken token).
    - `grep -rc "bg-accent/10" src/components/search/` returns 0 across all files (the tinted-pill pattern lives only in the chip primitive at src/components/ui/chip.tsx).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` exits 0 (full suite green).
  </acceptance_criteria>
  <done>Both inline removable chip blocks in SearchPageClient.tsx now use `<Chip variant="removable">`; BUG-02 is statically closed inside src/components/search/ (zero `text-accent-foreground` and zero `bg-accent/10` literal hits remaining); the X-from-lucide-react import is removed if no other usage remains; the test suite is green. Task 3 (manual dark-mode UAT) is the final gate — automated tests cannot resolve oklch paint values.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual dark-mode UAT — confirm BUG-02 is observably closed in the running app</name>
  <files>src/components/search/SearchPageClient.tsx, src/components/ui/chip.tsx</files>
  <read_first>
    - .planning/phases/48-user-facing-bug-fixes/48-UI-SPEC.md §"CSS Chain Assertion"
    - .planning/phases/48-user-facing-bug-fixes/48-VALIDATION.md §"Manual-Only Verifications"
    - .planning/phases/48-user-facing-bug-fixes/48-RESEARCH.md §"Pitfall 4: Dark/Light Token Assumptions"
  </read_first>
  <what-built>
    Plan 01 fixed BUG-01 (catalog ownership mislabel) with regression tests. Plan 02 created the shared Chip primitive with the BUG-02 token fix baked in and unit tests. Plan 03 Tasks 1-2 migrated all 8 chip surfaces (7 drawer chips + 2 SearchPageClient inline chip blocks) to the primitive. The static grep gates are green. The remaining unknown is dark-mode rendered contrast — jsdom cannot resolve `oklch(...)` CSS custom property paint values (per RESEARCH.md Pitfall 4 / UI-SPEC.md §"CSS Chain Assertion" / VALIDATION.md §"Manual-Only Verifications" / memory `feedback_ui_spec_css_chain_blind_spot`).
  </what-built>
  <how-to-verify>
    1. Clear the Next.js build cache to avoid stale CSS (`rm -rf .next` — per memory `project_turbopack_next_cache_stale_css`).
    2. Start the dev server: `npm run dev`.
    3. Open `http://localhost:3000/search` in a browser.
    4. Toggle the OS / app theme to dark mode (browser dev tools or app theme toggle, depending on how the project surfaces it — confirm `.dark` class is applied on `<html>` or `<body>` via inspector).
    5. Open the filter drawer ("Filters" button). For each of the 7 chip groups (Movement, Case Size, Style, Brand, Era, Genre, Archetype), confirm visually:
       a. Unselected chips show legible near-white text on a dark gray pill (the `bg-secondary` / `text-secondary-foreground` pair).
       b. Click a chip — the selected state shows near-black text on a solid golden accent pill (`bg-accent` / `text-accent-foreground` — UNCHANGED from before; this contrast is adequate per UI-SPEC.md §"CSS Chain Assertion" #2).
    6. Apply at least 2 filters (e.g., one brand + one era). Close the drawer. Two inline removable chips should now appear above the results list (results branch — UI-SPEC.md / SearchPageClient.tsx lines ~491-537). Confirm visually:
       a. The removable chips render with legible near-white text on a tinted accent pill (`bg-accent/10` + `text-foreground` — THE BUG-02 FIX). The label and the trailing X must both be clearly readable against the dark background.
       b. The contrast must be visibly comparable to UI-SPEC.md §"CSS Chain Assertion" #1's WCAG AAA target (effective surface ~L=0.17, text L=0.96).
       c. Hover a removable chip — background lightens slightly (`hover:bg-accent/20`); text remains legible.
       d. Click the X — the filter clears.
    7. Apply a filter that returns zero results (e.g., obscure brand × era combination) to land on the zero-results branch (~lines 410-454). Confirm the same removable chips above the empty-state message render with the same legibility as step 6 — this proves BOTH chip blocks were migrated (per RESEARCH.md Pitfall 2).
    8. Toggle back to light mode. Re-verify all chip groups + removable chips still render with adequate contrast (text-foreground is near-black in light mode on a near-white tinted surface — UI-SPEC.md §"Light Mode Verification" / RESEARCH.md §"Light Mode Verification"). No light-mode regression should be visible.
    9. Run `npm run lint` and `npx vitest run` one more time to confirm the full suite is still green after the dev-server interaction.
  </how-to-verify>
  <action>
    Operator runs the dev server and walks through the 9-step verification above. No source-code edits are made in this task — this task is a pure validation gate confirming that the rendered paint values in both light and dark modes match the UI-SPEC.md contract. If a regression is found, exit the checkpoint with a specific description (which chip group, which mode, which branch) so a follow-up fix can be planned. The `<files>` listed (SearchPageClient.tsx + chip.tsx) are the two implementation surfaces under visual inspection — they are NOT modified by this task.
  </action>
  <verify>
    <automated>npm run lint && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Steps 1-9 of the verification protocol all PASS by visual inspection in the operator's browser (both dark and light themes).
    - All 7 drawer chip groups (Movement, Case Size, Style, Brand, Era, Genre, Archetype) render legibly in dark mode — text contrast comparable to UI-SPEC.md §"CSS Chain Assertion" #2 and #3 (WCAG AA / AAA targets).
    - Inline removable chips render legibly in dark mode at BOTH branch locations (results branch AND zero-results branch) — text contrast comparable to UI-SPEC.md §"CSS Chain Assertion" #1 (WCAG AAA).
    - Light-mode visual check finds no regression vs the pre-fix appearance.
    - `npm run lint` exits 0.
    - `npx vitest run` exits 0.
    - Operator returns the resume-signal "approved" (or a specific failure description for follow-up).
  </acceptance_criteria>
  <done>BUG-02 is observably closed — dark-mode chip contrast is legible across all 8 surfaces; no light-mode regression; lint and tests green; operator-approved.</done>
  <resume-signal>Type "approved" if all 7 drawer chip groups + both removable chip block locations render with legible text in dark mode AND no light-mode regression is visible. Otherwise describe the specific chip group / branch / mode where the contrast is still inadequate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan is a UI consolidation refactor + token fix. No auth, no DB, no network surface area is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03-01 | Information disclosure | sr-only "Remove {label} filter" string in removable chips | accept | The label substring comes from server-supplied facet display names (archetype config, brand vocab, era display labels, genre display labels) — already rendered as visible text elsewhere in the page. No new disclosure surface. React escapes the substituted string automatically. |
| T-48-03-02 | Denial of service | Manual UAT bypass | accept | If a future change reintroduces `text-accent-foreground` inside a removable-style chip, the static grep gate in Task 2's `<verify>` block fails the CI run. The chip primitive's unit test (Plan 02 Task 1 Test 3) also fails. Two independent automated regression guards plus the static grep audit (`grep -rc "bg-accent/10" src/components/search/` returns 0) provide layered defense. Manual dark-mode UAT (Task 3) is the only gate that requires human judgment, and it is gated as blocking — `/gsd-execute-phase` will not proceed past it without operator approval. |

**Net assessment:** Per RESEARCH.md §"Security Domain", no security surface area changes in this phase. No high-severity threats. Block: no.
</threat_model>

<verification>
- `npx vitest run` exits 0 (full suite green — chip primitive unit tests + catalog-page tests + any pre-existing search-component tests)
- `npx tsc --noEmit` exits 0
- `grep -rc "text-accent-foreground" src/components/search/` returns 0 hits across all files in the search subtree
- `grep -rc "bg-accent/10" src/components/search/` returns 0 hits (the tinted-pill pattern lives only inside src/components/ui/chip.tsx now)
- All 7 drawer chip components import `Chip` from `@/components/ui/chip` and use `<Chip variant="toggle" selected={...}>`
- SearchPageClient.tsx contains at least 8 `<Chip variant="removable">` instances (4 facets × 2 blocks)
- Task 3 human-verify checkpoint returns "approved" — dark mode and light mode both visually legible across all chip surfaces
</verification>

<success_criteria>
After this plan: all 8 chip surfaces route through the shared `<Chip>` primitive; the BUG-02 dark-mode contrast failure is statically eliminated (zero `text-accent-foreground` hits in src/components/search/) AND user-visibly fixed (manual dark-mode UAT approved at /search); the D-07/D-08 chip consolidation refactor is shipped; D-09 holds (the BUG-02 fix lives in exactly one place — the primitive). ROADMAP success criterion #2 ("/search filter chip groups render with legible text contrast in dark mode — no black text on dark backgrounds") is observably true. The full v5.2 Phase 48 goal — both live production bugs resolved — is met after the Phase-48 audit gate.
</success_criteria>

<output>
After completion, create `.planning/phases/48-user-facing-bug-fixes/48-03-SUMMARY.md` summarizing the 7 drawer chip migrations, the 2-block SearchPageClient migration (with note on whether the `X` import was removable or retained), the green static gates (no `text-accent-foreground` / no `bg-accent/10` in src/components/search/), and the manual dark-mode UAT outcome.
</output>
