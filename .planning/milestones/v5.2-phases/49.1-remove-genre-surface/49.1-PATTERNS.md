# Phase 49.1: Remove Genre Surface - Pattern Map

**Mapped:** 2026-05-19
**Files analyzed:** 33 (modified) + 3 (deleted) + 2 (new migrations) + 2-3 (new tests, Wave 0) = ~40 total
**Analogs found:** 30 / 33 modified (3 pure deletions need no analog)

> **Subtractive phase.** Most files are modified or deleted, not created. Pattern assignments below focus on the *post-modification* shape — i.e., the existing pattern the modified file should converge to after the archetype removal. New file analogs are scoped to (a) the two SQL migrations and (b) Wave 0 test scaffolds.

---

## File Classification

### NEW files

| New File | Role | Data Flow | Closest Analog | Match Quality |
|----------|------|-----------|----------------|---------------|
| `drizzle/0012_phase49_1_drop_primary_archetype.sql` | migration | schema-DDL | `drizzle/0011_phase38_catalog_id_notnull.sql` | exact (same phase-driven idempotent DDL idiom; though 0011 uses DO block for SET NOT NULL — 0012 is simpler DROP COLUMN IF EXISTS) |
| `supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql` | migration | schema-DDL | `supabase/migrations/20260519000000_phase47_published_at.sql` | exact (same date-prefix ordering; same `public.<table>` qualification; ALTER TABLE … IF [NOT] EXISTS idiom) |
| `tests/integration/migration-drop-archetype.test.ts` (Wave 0 gap) | test | integration | `tests/integration/catalog-taste.test.ts` | exact (same `DATABASE_URL`-gated `maybe = describe.skip` idiom; same drizzle direct-db assertions on `watchesCatalog`) |
| `tests/app/explore-genres-404.test.ts` (Wave 0 gap, optional) | test | static | `tests/no-evaluate-route.test.ts` | role-match (filesystem-absence assertion; no live HTTP needed) |
| `tests/components/insights/CollectionFitCompareTable.test.tsx` (Wave 0, optional) | test | component | `tests/components/insights/ReferenceIdentityCard.test.tsx` | exact (component test, jsdom, same insights/ folder) |

### MODIFIED files — DAL / schema / migrations

| Modified File | Role | Data Flow | Closest Analog (post-modification shape) | Match Quality |
|--------|------|-----------|------------------------------------------|---------------|
| `src/db/schema.ts` (line 390 deletion) | schema | DDL-as-TS | `src/db/schema.ts:386-394` (siblings: `formality`, `eraSignal`, `designMotifs`) | exact (the surviving sibling columns ARE the post-modification shape) |
| `src/data/browse.ts` (rewire `getBrowseArchetypeCounts`; delete `getBrowseGenreCounts`) | DAL | request-response (cached) | `src/data/catalog.ts:533-544` `getTopStyleTags()` | **EXACT — the unnest(style_tags) precedent for D-EXPLORE-01** |
| `src/data/catalog.ts` (drop filter fields, tiebreaker, mapper field, upsert column) | DAL | CRUD | `src/data/catalog.ts:438-441` (era facet predicate — surviving pattern after archetype block removal) | exact (sibling predicate; same `isNotNull + eq` shape on a closed-vocab text column) |
| `src/lib/similarity.ts` (rebalance TASTE_SUB_WEIGHTS; remove archetype block) | engine | transform | `src/lib/similarity.ts:27-32` `WEIGHTS` Object.fromEntries idiom | **EXACT — the anti-magic-number rescale precedent for D-SIM-02** |

### MODIFIED files — Verdict engine

| Modified File | Role | Data Flow | Closest Analog | Match Quality |
|--------|------|-----------|----------------|---------------|
| `src/lib/verdict/templates.ts` (rewire 3 archetype templates to era) | engine | transform | `src/lib/verdict/templates.ts:95-104` `era-echo` template (era-axis predicate) | role-match (predicate signature identical; post-rewire templates discriminate on the same `profile.dominantEraSignal` field that era-echo references) |
| `src/lib/verdict/viewerTasteProfile.ts` (drop `dominantArchetype`) | engine | transform | `src/lib/verdict/viewerTasteProfile.ts:82-86` (`dominantEraSignal` survivor — same `mode<>()` call site) | exact (the surviving sibling field IS the post-modification shape) |
| `src/lib/verdict/types.ts` (drop `dominantArchetype` from interface) | type | type-system | `src/lib/verdict/types.ts:60` (`dominantEraSignal` survivor) | exact |
| `src/lib/verdict/fit-delta.ts` (drop archetype dimension; 6 → 5) | engine | transform | `src/lib/verdict/fit-delta.ts:36, 77, 108-109` (`eraSignal` survivor dimension — same null===null counts as match; same `displayEnum` phrase format) | exact |
| `src/lib/verdict/composer.ts` (drop `primaryArchetype` from CandidateTasteSnapshot construction; **add `eraSignal`** per Open Question #1) | engine | request-response | `src/lib/verdict/composer.ts:47-53` (existing snapshot construction; needs `eraSignal: catalogEntry.eraSignal` line added in same shape) | exact (extension of existing pattern) |

### MODIFIED files — LLM enricher

| Modified File | Role | Data Flow | Closest Analog | Match Quality |
|--------|------|-----------|----------------|---------------|
| `src/lib/taste/enricher.ts` (drop archetype from tool schema + required) | service | LLM round-trip | `src/lib/taste/enricher.ts:39, 51` (`era_signal` survivor in same tool schema) | exact |
| `src/lib/taste/prompt.ts` (drop the `primary_archetype` prompt line at :26) | service | LLM round-trip | adjacent `era_signal` / `design_motifs` prompt lines | exact |
| `src/lib/taste/vocab.ts` (drop `primary_archetype` from `TasteSchema`; drop archetype block from `validateAndCleanTaste`; **KEEP** `PRIMARY_ARCHETYPES` const) | service | validation | `src/lib/taste/vocab.ts:111-122` (`era_signal` block of `validateAndCleanTaste` — the surviving sibling pattern) | exact |

### MODIFIED files — UI components

| Modified File | Role | Data Flow | Closest Analog | Match Quality |
|--------|------|-----------|----------------|---------------|
| `src/components/insights/ReferenceIdentityCard.tsx` (drop `ARCHETYPE_LABELS`; drop archetype headline half) | component | render | `src/components/insights/ReferenceIdentityCard.tsx:29-33` `ERA_LABELS` survivor | exact (the era half of the headline IS the post-modification shape) |
| `src/components/insights/CollectionFitCompareTable.tsx` (delete Row 4; rebind tableLastClass) | component | render | `src/components/insights/CollectionFitCompareTable.tsx:168-177` (Row 5 Era — uses `thRowClass`/`tdClass`, becomes new mid-row) + `:178-183` (Row 6 Motifs — uses `thRowLastClass`/`tdLastClass`, becomes new last row) | exact |
| `src/components/search/SearchPageClient.tsx` (drop archetype editorial header, genre/archetype removable chips, ARCHETYPE_CONFIG import, URL params) | component | render | existing `brand` and `era` removable-chip blocks (survivors per UI-SPEC §C) | exact (the brand/era inline chip blocks ARE the post-modification shape — only archetype/genre blocks are deleted) |
| `src/components/search/FilterDrawer.tsx` (drop GenreChips + ArchetypeChips imports + props + renders + clear-all handlers) | component | render | existing `StyleChips`, `BrandChips`, `EraChips` rendering pattern in the same drawer | exact (5 surviving chip-group renders are the post-modification shape) |
| `src/components/explore/CollectorArchetypes.tsx` (repoint href `?archetype=` → `?style=`) | component | render | existing `Link href="/search?tab=watches&style=…"` patterns in StyleChips/StylePill consumers | exact (StyleChips already multi-selects on `style`) |
| `src/components/explore/BrowseModule.tsx` (remove `/explore/genres` tile at :43) | component | render | surrounding surviving tiles in the same array literal | exact (tile siblings define the post-modification shape) |

### MODIFIED files — types / server action / page

| Modified File | Role | Data Flow | Closest Analog | Match Quality |
|--------|------|-----------|----------------|---------------|
| `src/lib/types.ts` (drop `primaryArchetype` from `CatalogTasteAttributes` at :208 and :224; **KEEP** `PrimaryArchetype` union at :234-236) | type | type-system | `src/lib/types.ts:225` (`eraSignal` survivor in same interface) | exact |
| `src/app/actions/search.ts` (drop `.genre` and `.archetype` from Zod; drop from filters passthrough) | action | request-response | `src/app/actions/search.ts:34` (`era: z.enum(ERA_SIGNALS).optional()`) survivor; passthrough at :124 | exact |
| `src/app/catalog/[catalogId]/page.tsx` (drop `primaryArchetype: catalogEntry.primaryArchetype` at :94) | page | RSC projection | adjacent `eraSignal: catalogEntry.eraSignal` projection line | exact |

### MODIFIED files — tests

| Test File | Role | Action | Notes |
|-----------|------|--------|-------|
| `src/data/__tests__/browse.test.ts` | test | DELETE `getBrowseGenreCounts` describe; REPOINT `getBrowseArchetypeCounts` mock fixture | analog: surviving `getBrowseEraCounts` describe block in same file |
| `src/data/__tests__/catalog-facets.test.ts` | test | DELETE 3 archetype-facet describes (archetype filter, genre filter, archetype-wins precedence) at lines 98-148 | analog: surviving era-facet describes |
| `src/data/__tests__/watches-leftjoin.test.ts` | test | DELETE archetype assertions | — |
| `src/lib/verdict/composer.test.ts` | test | REPOINT archetype-template fixtures to era axis | analog: era-echo fixture |
| `src/lib/verdict/shims.test.ts` | test | DROP `primaryArchetype: 'dive'` at :42 from test fixtures | — |
| `src/lib/verdict/viewerTasteProfile.test.ts` | test | DROP `dominantArchetype` assertions; KEEP era-mode | — |
| `src/lib/taste/enricher.test.ts` | test | DROP archetype assertions | — |
| `src/lib/taste/vocab.test.ts` | test | DROP `validateAndCleanTaste()` archetype-path tests | — |
| `src/lib/archetype-config.test.ts` | test | KEEP ARCHETYPE_CONFIG assertions; DROP any column-bound assertions | — |
| `src/components/explore/__tests__/CollectorArchetypes.test.tsx` | test | REPOINT mocked `getBrowseArchetypeCounts` shape; update `href` assertion to `?style=` | — |
| `tests/components/search/DrawerChips.test.tsx` | test | DELETE GenreChips + ArchetypeChips describe blocks (whole-component deletions) | — |
| `tests/components/search/SearchPageClientChips.test.tsx` | test | DELETE Tests 1, 4, 5, 8 (archetype/genre references) | — |
| `tests/components/search/FilterDrawer.test.tsx` | test | REMOVE `vi.mock` lines 133-138; REMOVE archetype/genre props at lines 174-175 | — |
| `tests/components/insights/ReferenceIdentityCard.test.tsx` | test | UPDATE fixture: drop `primaryArchetype`; expect era-only headline | — |
| `tests/static/ReferenceIdentityCard.no-engine.test.ts` | test | UPDATE no-engine import-isolation assertion if changed | static-assertion test |
| `tests/unit/lib/verdict/fit-delta.test.ts` | test | DROP "Different archetype: Dive vs Dress" expectation at lines 51-54 | — |
| `tests/integration/catalog-taste.test.ts` | test | DROP `primaryArchetype` from VALID_TASTE fixture; drop `row.primaryArchetype` assertion | — |
| `tests/integration/catalog-taste-schema.test.ts` | test | UPDATE schema assertion to reflect dropped column | — |
| `tests/similarity.test.ts` | test | UPDATE TASTE_SUB_WEIGHTS expectations to new rescaled values; add sum-to-1.0 invariant assertion (D-SIM-04) | — |

### DELETED files (no analog needed)

| Deleted File | Reason |
|--------------|--------|
| `src/components/search/GenreChips.tsx` | Pure deletion (D-SCOPE per SC#2) |
| `src/components/search/ArchetypeChips.tsx` | Pure deletion (D-SCOPE per SC#2) |
| `src/app/explore/genres/page.tsx` | Pure deletion (route removed) |

---

## Pattern Assignments

### Migration: `drizzle/0012_phase49_1_drop_primary_archetype.sql` (NEW)

**Analog:** `drizzle/0011_phase38_catalog_id_notnull.sql` + `drizzle/0005_phase19_1_taste_columns.sql`

**Header-comment pattern** (from 0011 lines 1-6):

```sql
-- Phase 38 Plan 01 — watches.catalog_id NOT NULL (Drizzle type catch-up)
--
-- Phase 36 shipped the DB-level SET NOT NULL to production. This migration
-- brings the Drizzle schema in sync so drizzle-kit no longer diffs catalog_id
-- as nullable. Idempotent: safe to run when the constraint already exists.
--> statement-breakpoint
```

The new 0012 file follows the same comment style. RESEARCH.md §Code Examples gives the explicit body to use:

```sql
-- File: drizzle/migrations/0012_phase49_1_drop_primary_archetype.sql
-- Phase 49.1 — Remove genre surface. Drops the primary_archetype column from
-- watches_catalog. The watches_catalog_primary_archetype_check CHECK constraint
-- is dropped implicitly by Postgres along with the column.
--
-- Idempotent: re-running is safe (IF EXISTS).
-- Sequencing (D-MIG-02): runs LAST, after all reader code is removed AND deployed.

ALTER TABLE "watches_catalog" DROP COLUMN IF EXISTS "primary_archetype";
```

**Ordinal verification** (per RESEARCH Pitfall 7): `drizzle/meta/_journal.json` confirms last entry is `idx: 11, tag: 0011_phase38_catalog_id_notnull`. New file MUST be `0012`.

**Note:** `drizzle/0005_phase19_1_taste_columns.sql` line 4 is the original `ADD COLUMN "primary_archetype" text` — this is the *reverse* of what 0012 will do; keep that file as historical reference but do not modify it.

---

### Migration: `supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql` (NEW)

**Analog:** `supabase/migrations/20260519000000_phase47_published_at.sql`

**Header-comment pattern** (from 20260519000000 lines 1-2):

```sql
-- Phase 47 D-02: add published_at to curated_lists
-- Set on the first draft→published transition; drives rail freshness indicator and Hero rotation ordering.
ALTER TABLE public.curated_lists
  ADD COLUMN IF NOT EXISTS published_at timestamptz;
```

**Filename ordering** (RESEARCH Pitfall 7 + project memory `project_drizzle_supabase_db_mismatch` gotcha #1): latest existing supabase migration is `20260519000000_phase47_published_at.sql`; new file MUST be timestamped `>20260519000000`. The CONTEXT.md choice is `20260519010000_…`.

Body (from RESEARCH.md §Code Examples):

```sql
-- File: supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql
-- Phase 49.1 — Mirror of drizzle/migrations/0012_phase49_1_drop_primary_archetype.sql
-- for prod push via `supabase db push --linked`.
ALTER TABLE public.watches_catalog DROP COLUMN IF EXISTS primary_archetype;
```

The `public.<table>` qualification matches the analog. The `IF EXISTS` guard is idempotent against re-runs (gotcha #1).

---

### `src/data/browse.ts` — rewire `getBrowseArchetypeCounts`; delete `getBrowseGenreCounts`

**Analog (load-bearing):** `src/data/catalog.ts:533-544` `getTopStyleTags()` — verified in RESEARCH.md §"Pattern 2".

**`unnest(style_tags)` pattern to copy** (catalog.ts:533-544):

```typescript
export async function getTopStyleTags(limit = 8): Promise<string[]> {
  'use cache'
  cacheLife('hours')
  const rows = await db.execute(
    sql`SELECT tag, COUNT(*) AS freq
        FROM watches_catalog, unnest(style_tags) AS tag
        GROUP BY tag
        ORDER BY freq DESC
        LIMIT ${limit}`,
  )
  return (rows as unknown as Array<{ tag: string }>).map((r) => r.tag)
}
```

**`sql.join` parameter-binding pattern to copy** (from `src/data/catalog.ts:611` `updateCatalogTaste`, motifsSql block):

```typescript
const motifsSql =
  taste.designMotifs.length === 0
    ? sql`'{}'::text[]`
    : sql`ARRAY[${sql.join(taste.designMotifs.map((v) => sql`${v}`), sql`, `)}]::text[]`
```

**Resulting rewired function** (combines both patterns + the existing cache scope from browse.ts:44-56):

```typescript
import { PRIMARY_ARCHETYPES } from '@/lib/taste/vocab'

export async function getBrowseArchetypeCounts(): Promise<Array<{ archetype: string; count: number }>> {
  'use cache'
  cacheTag('explore', 'explore:browse')
  cacheLife('hours')
  const rows = await db.execute(
    sql`SELECT tag AS archetype, COUNT(*)::int AS count
        FROM watches_catalog, unnest(style_tags) AS tag
        WHERE tag = ANY(ARRAY[${sql.join(PRIMARY_ARCHETYPES.map((v) => sql`${v}`), sql`, `)}])
        GROUP BY tag
        ORDER BY count DESC`,
  )
  return rows as unknown as Array<{ archetype: string; count: number }>
}
```

**Function signature preservation** (browse.ts:44 — UNCHANGED): `Promise<Array<{ archetype: string; count: number }>>`. Cache scope tag (`'explore', 'explore:browse'`) and TTL (`'hours'`) preserved — no client of this function changes.

**Deletion target:** `getBrowseGenreCounts` (browse.ts:82-106) — pure delete; nothing analogous needed.

---

### `src/lib/similarity.ts` — `TASTE_SUB_WEIGHTS` rebalance (D-SIM-02)

**Analog (load-bearing):** `src/lib/similarity.ts:13-32` — `EXISTING_WEIGHTS_BASE` → `WEIGHTS` via `Object.fromEntries`, verified in RESEARCH.md §"Pattern 3".

**Existing precedent to copy** (similarity.ts:13-32):

```typescript
const EXISTING_WEIGHTS_BASE = {
  styleTags: 0.25,
  designTraits: 0.20,
  // … 8 entries
} as const

const TASTE_WEIGHT = 0.20
const EXISTING_SCALE = 1.0 - TASTE_WEIGHT  // 0.80

const WEIGHTS = {
  ...Object.fromEntries(
    Object.entries(EXISTING_WEIGHTS_BASE).map(([k, v]) => [k, v * EXISTING_SCALE]),
  ),
  taste: TASTE_WEIGHT,
} as { [K in keyof typeof EXISTING_WEIGHTS_BASE]: number } & { taste: number }
```

**Anti-magic-number invariant** (similarity.ts:10-12 forbids hardcoded rescaled values):

```typescript
// Phase 38 D-01/D-05 — preserve relative proportions while reserving 0.20 for taste.
// Anti-pattern (forbidden): hardcoding rescaled values as magic numbers.
// Verify: `grep -E "0\.20.*styleTags|styleTags.*0\.20" src/lib/similarity.ts` returns 0 matches.
```

**Resulting rebalanced block** (replaces similarity.ts:34-45):

```typescript
// Phase 38 D-03 (modified by Phase 49.1 D-SIM-01/02) — internal split of the 0.20
// taste budget. Anti-pattern (forbidden, lines 11-12): hardcoding rescaled values.
const TASTE_SUB_WEIGHTS_BASE = {
  numericTrioCosine: 0.40,
  eraMatch:          0.20,
  motifsJaccard:     0.20,
} as const  // sums to 0.80 (after removing 0.20 archetypeMatch)

const REMOVED_SUB_WEIGHT = 0.20
const RESCALE = 1 / (1 - REMOVED_SUB_WEIGHT)  // 1.25

const TASTE_SUB_WEIGHTS = Object.fromEntries(
  Object.entries(TASTE_SUB_WEIGHTS_BASE).map(([k, v]) => [k, v * RESCALE]),
) as { [K in keyof typeof TASTE_SUB_WEIGHTS_BASE]: number }
// → { numericTrioCosine: 0.50, eraMatch: 0.25, motifsJaccard: 0.25 }
```

**Categorical-match block deletion** (similarity.ts:124-127 — pure delete):

```typescript
// DELETE these lines:
if (t1.primaryArchetype !== null && t2.primaryArchetype !== null && t1.primaryArchetype === t2.primaryArchetype) {
  contrib += TASTE_SUB_WEIGHTS.archetypeMatch * 1.0
}
```

The surrounding `eraMatch` block at similarity.ts:130-132 is the surviving sibling — same null-safe categorical idiom.

---

### `src/lib/verdict/templates.ts` — pivot 3 archetype templates to era (D-VERDICT-01)

**Analog (load-bearing):** `templates.ts:95-104` `era-echo` reserved template — the surviving era-axis precedent.

**Existing era-axis template** (templates.ts:95-104):

```typescript
{
  id: 'era-echo',
  predicate: (_result, _profile, _c, _taste) => {
    // taste does not carry eraSignal in CandidateTasteSnapshot — derive via candidate's
    // Phase 19.1 catalog row upstream; if not threaded, predicate cannot fire.
    // Reserved slot — composer caller may inject candidateEraSignal in future.
    return null
  },
  template: 'Echoes the ${era} era of your collection.',
  rationaleTemplate: 'Echoes the ${era} era I keep coming back to.',
},
```

**Pre-modification archetype template to rewire** (templates.ts:16-26 `fills-a-hole`):

```typescript
{
  id: 'fills-a-hole',
  predicate: (result, profile, _candidate, taste) => {
    if (result.label !== 'taste-expansion' && result.label !== 'outlier') return null
    if (!taste.primaryArchetype) return null
    if (profile.dominantArchetype === taste.primaryArchetype) return null
    return { archetype: taste.primaryArchetype }
  },
  template: 'Fills a hole in your collection — your first ${archetype}.',
  rationaleTemplate: 'My first ${archetype} — fills a real hole in what I own.',
},
```

**Pivot pattern (D-VERDICT-01)** — read `profile.dominantEraSignal` and `(taste as CandidateTasteSnapshot).eraSignal` (requires composer.ts + types.ts extension per Open Question #1):

```typescript
{
  id: 'fills-a-hole',
  predicate: (result, profile, _candidate, taste) => {
    if (result.label !== 'taste-expansion' && result.label !== 'outlier') return null
    if (!taste.eraSignal) return null
    if (profile.dominantEraSignal === taste.eraSignal) return null
    return { era: taste.eraSignal }
  },
  template: 'Fills a hole in your collection — your first ${era} piece.',
  rationaleTemplate: 'My first ${era} piece — fills a real hole in what I own.',
},
```

The two other archetype-using templates (`collection-skews-contrast` at :38-48, `archetype-echo` at :85-93) follow the same pivot mechanic — see CONTEXT.md D-VERDICT-02 for the narrative-distinctness constraints from `era-echo`.

**The reserved `era-echo` template stays** as its own template (D-VERDICT-02). Implementation note from RESEARCH.md: the planner must also extend `CandidateTasteSnapshot` with `eraSignal: EraSignal | null` (types.ts:72-78) AND thread `catalogEntry.eraSignal` into composer.ts:47-53's snapshot construction. **This must land in the same wave** as the template rewire.

---

### `src/lib/verdict/viewerTasteProfile.ts` — drop `dominantArchetype` (D-VERDICT-03)

**Analog:** the surviving `dominantEraSignal` field at viewerTasteProfile.ts:82-86 — same `mode<>()` call site.

**Pre-modification block** (viewerTasteProfile.ts:77-86):

```typescript
return {
  // …
  dominantArchetype: mode<PrimaryArchetype>(           // ← REMOVE these 4 lines
    rows
      .map((r) => r.primaryArchetype as PrimaryArchetype | null)
      .filter((x): x is PrimaryArchetype => x !== null),
  ),
  dominantEraSignal: mode<EraSignal>(                  // ← KEEP unchanged
    rows
      .map((r) => r.eraSignal as EraSignal | null)
      .filter((x): x is EraSignal => x !== null),
  ),
  topDesignMotifs: topK(rows.flatMap((r) => r.designMotifs ?? []), 3),
}
```

**Companion changes:**
- `EMPTY_PROFILE` (line 15-22): drop `dominantArchetype: null` (line 19)
- SELECT projection (line 47): drop `primaryArchetype: watchesCatalog.primaryArchetype`
- Import (line 5): drop `PrimaryArchetype` if no other reference remains in this file (verify after edits)

---

### `src/lib/verdict/fit-delta.ts` — drop archetype dimension (D-SCOPE-01c)

**Analog:** the surviving `eraSignal` dimension at fit-delta.ts:36, 77, 108-109 — same null===null-counts-as-match logic; same `displayEnum` phrase format.

**Surviving era-dimension pattern to keep** (fit-delta.ts:36, 77, 108-109):

```typescript
// :36 — delta computation
const eraDelta = candidate.eraSignal === owned.eraSignal ? 0 : 1

// :77 — winner-list participation
if (eraDelta === 1) tryUpdate('eraSignal', eraDelta)

// :108-109 — phrase branch
case 'eraSignal':
  return `Different era: ${displayEnum(candidate.eraSignal)} vs ${displayEnum(owned.eraSignal)}`
```

**Archetype lines to DELETE** (fit-delta.ts:35, 51, 76, 105-106):

```typescript
// :35 — DELETE
const archetypeDelta = candidate.primaryArchetype === owned.primaryArchetype ? 0 : 1
// :51 — DELETE
archetypeDelta === 0 &&
// :76 — DELETE
if (archetypeDelta === 1) tryUpdate('primaryArchetype', archetypeDelta)
// :105-106 — DELETE
case 'primaryArchetype':
  return `Different archetype: ${displayEnum(candidate.primaryArchetype)} vs ${displayEnum(owned.primaryArchetype)}`
```

**Tie-break order doc comment** (:59-60) must also be updated: `formality > sportiness > heritageScore > eraSignal > designMotifs` (5 dimensions, not 6).

---

### `src/components/insights/ReferenceIdentityCard.tsx` — era-only headline (D-SCOPE-01a)

**Analog:** the surviving `ERA_LABELS` const at ReferenceIdentityCard.tsx:29-33 + the `hasHeadline` guard at :61.

**ARCHETYPE_LABELS to DELETE** (lines 35-46):

```typescript
const ARCHETYPE_LABELS: Record<NonNullable<CatalogTasteAttributes['primaryArchetype']>, string> = {
  dress: 'Dress',
  dive: 'Dive',
  // … 10 entries
}
```

**Headline JSX to simplify** (lines 73-77 — drop the archetype span + separator):

```tsx
// Pre-modification:
{hasHeadline && (
  <p className="text-base font-semibold text-foreground">
    {eraLabel && <span className="truncate">{eraLabel}</span>}
    {eraLabel && archetypeLabel && ' · '}
    {archetypeLabel && <span className="truncate">{archetypeLabel}</span>}
  </p>
)}

// Post-modification (per UI-SPEC §A):
{hasHeadline && (
  <p className="text-base font-semibold text-foreground">
    {eraLabel && <span className="truncate">{eraLabel}</span>}
  </p>
)}
```

**Guard simplification** (line 61):
- Pre: `const hasHeadline = Boolean(eraLabel || archetypeLabel)`
- Post: `const hasHeadline = Boolean(eraLabel)`

**D-39b-04 identical-rendering lock note:** the same change applies on both `/watch/{id}` and `/catalog/{id}` because both consume this component. The catalog page projection (page.tsx:94) must drop `primaryArchetype` in the same wave.

---

### `src/lib/types.ts` — drop `primaryArchetype` from `CatalogTasteAttributes`

**Analog:** the surviving `eraSignal` field at types.ts:225 (sibling in same interface).

**Two deletion sites:**
- Line 208: `primaryArchetype: PrimaryArchetype | null` (inside `CatalogEntry`-like interface)
- Line 224: `primaryArchetype: PrimaryArchetype | null` (inside `CatalogTasteAttributes`)

**KEEP** the `PrimaryArchetype` union at types.ts:234-236 — load-bearing for retained `ARCHETYPE_CONFIG` (D-EXPLORE-02) and the rewired `getBrowseArchetypeCounts` SQL filter. RESEARCH §Pitfall 5.

---

### `src/app/actions/search.ts` — drop `genre`/`archetype` from Zod (D-SCOPE-01f)

**Analog:** the surviving `era: z.enum(ERA_SIGNALS).optional()` field at line 34 and its passthrough at line 124.

**Pre-modification Zod block** (lines 24-38):

```typescript
const searchSchema = z
  .object({
    q: z.string().max(200),
    movement: z.enum(['auto', 'manual', 'quartz', 'spring_drive']).optional(),
    size: z.enum(['lt36', '36-39', '40-42', '43-45', '46plus']).optional(),
    style: z.string().max(500).optional(),
    brand:     z.string().max(100).optional(),
    era:       z.enum(ERA_SIGNALS).optional(),
    genre:     z.enum(PRIMARY_ARCHETYPES).optional(),    // ← DELETE
    archetype: z.enum(PRIMARY_ARCHETYPES).optional(),    // ← DELETE
  })
  .strict()
```

**Passthrough deletion** (lines 125-126):
```typescript
filters: {
  // …
  genre:     parsed.data.genre,        // ← DELETE
  archetype: parsed.data.archetype,    // ← DELETE
},
```

**Import:** verify whether `PRIMARY_ARCHETYPES` import (line 11) is still needed. Per RESEARCH §Open Question #2, the const may need to stay imported elsewhere (rewired `getBrowseArchetypeCounts` consumes it from `vocab.ts`), but at THIS file the import is no longer used and should be removed (`ERA_SIGNALS` stays).

**.strict() invariant preserved** — RESEARCH §Common Pitfalls notes that old `/search?archetype=X` URLs will now be REJECTED by `.strict()` at the action boundary. Acceptable per D-EXPLORE-03.

---

### `src/lib/taste/vocab.ts` + `enricher.ts` + `prompt.ts` — drop `primary_archetype` LLM writes

**Analog:** the surviving `era_signal` blocks in each file — sibling pattern identical to archetype.

**`vocab.ts:60` — TasteSchema deletion:**

```typescript
// DELETE: primary_archetype: z.string(),
// KEEP:   era_signal:        z.string(),  ← survivor analog
```

**`vocab.ts:98-109` — `validateAndCleanTaste()` archetype block deletion:**

```typescript
// DELETE these lines:
const archetype = PRIMARY_ARCHETYPE_SET.has(wire.primary_archetype)
  ? (wire.primary_archetype as PrimaryArchetype)
  : null
if (!archetype) {
  console.warn(JSON.stringify({
    event: 'taste_vocab_drift',
    catalog_id: context.catalogId,
    field: 'primary_archetype',
    value: wire.primary_archetype,
    timestamp: new Date().toISOString(),
  }))
}
```

**Survivor analog** (vocab.ts:111-122 — the `era` block — same shape):

```typescript
const era = ERA_SIGNAL_SET.has(wire.era_signal)
  ? (wire.era_signal as EraSignal)
  : null
if (!era) {
  console.warn(JSON.stringify({
    event: 'taste_vocab_drift',
    catalog_id: context.catalogId,
    field: 'era_signal',
    value: wire.era_signal,
    timestamp: new Date().toISOString(),
  }))
}
```

**`vocab.ts:128` — return-literal deletion:** drop `primaryArchetype: archetype,` line.

**`enricher.ts:38, 51` — TASTE_TOOL deletion** (RESEARCH §Pitfall 4):

```typescript
// :38 — DELETE this line from properties:
primary_archetype: { type: 'string', enum: [...PRIMARY_ARCHETYPES] },

// :51 — DELETE 'primary_archetype' from the required array:
required: ['formality', 'sportiness', 'heritage_score', /* 'primary_archetype', */ 'era_signal', 'design_motifs', 'confidence'],
```

**`prompt.ts:26` — DELETE the prompt line:** `primary_archetype (pick exactly one): ${PRIMARY_ARCHETYPES.join(', ')}`.

**KEEP `PRIMARY_ARCHETYPES` const** at vocab.ts:16-19 — load-bearing for `getBrowseArchetypeCounts` rewire and `PrimaryArchetype` union. Same with `PRIMARY_ARCHETYPE_SET` lookup at :44 — verify if any remaining consumer needs it post-removal; if not, can be deleted (it was only used by the deleted `validateAndCleanTaste` archetype block).

---

### Test analog: `tests/integration/migration-drop-archetype.test.ts` (NEW, Wave 0 gap)

**Analog:** `tests/integration/catalog-taste.test.ts:1-58`

**Pattern to copy** (catalog-taste.test.ts:1-22 — `DATABASE_URL`-gated integration test):

```typescript
import { describe, it, expect, afterAll } from 'vitest'
import { db } from '@/db'
import { watchesCatalog } from '@/db/schema'
import { eq, sql } from 'drizzle-orm'
import { updateCatalogTaste, applyUserUploadedPhoto } from '@/data/catalog'
import type { CatalogTasteAttributes } from '@/lib/types'

const maybe = process.env.DATABASE_URL ? describe : describe.skip

const TEST_BRAND = `_test_taste_${Date.now()}`

const VALID_TASTE: CatalogTasteAttributes = {
  formality: 0.7,
  sportiness: 0.2,
  heritageScore: 0.8,
  primaryArchetype: 'dress',   // ← In new test, this field is GONE
  eraSignal: 'modern',
  // …
}

afterAll(async () => {
  await db.execute(sql`DELETE FROM watches_catalog WHERE brand = ${TEST_BRAND}`)
})

maybe('updateCatalogTaste', () => {
  it('writes all 8 taste fields when confidence is NULL', async () => {
    // …
  })
})
```

**Wave 0 test should assert** (per RESEARCH §"Wave 0 Gaps"):
1. `primary_archetype` column is absent post-migration — query `information_schema.columns`.
2. `upsertCatalogFromExtractedUrl` succeeds without writing the column.
3. `getWatchesByUser` LEFT JOIN returns rows with `catalogTaste.formality`/`eraSignal`/etc. but no `primaryArchetype` field.

---

### Test analog: `src/data/__tests__/browse.test.ts` (MODIFIED — drop genre describe; update archetype mock)

**Analog (in-place):** the surviving `getBrowseEraCounts` describe block in the same file (acts as the post-modification model).

**Existing pattern** (browse.test.ts:15-37 — mock setup):

```typescript
vi.mock('@/db', () => ({
  db: { execute: vi.fn() },
}))

vi.mock('next/cache', () => ({
  cacheLife: vi.fn(),
  cacheTag: vi.fn(),
}))

import { db } from '@/db'
import {
  getBrowseArchetypeCounts,
  getBrowseEraCounts,
  getBrowseGenreCounts,   // ← DELETE this import after rewire
  getBrowseBrandCounts,
} from '@/data/browse'

beforeEach(() => {
  vi.clearAllMocks()
})
```

**Required updates:**
1. Delete the `getBrowseGenreCounts` import + the whole describe block (analogous to lines 39-56 but for genre).
2. The existing `getBrowseArchetypeCounts` describe block (lines 39-56+) keeps its return-shape assertions — only the underlying SQL is mocked differently. Since `db.execute` is mocked, the test doesn't see the new `unnest(style_tags)` SQL — but the mock fixture stays `[{ archetype: 'dive', count: 18 }, …]` because the function signature is preserved.
3. Optionally add an `it` asserting the rewired SQL is parameterized via `sql.join` (test inspecting `db.execute.mock.calls[0][0]`) — but this is fragile; recommend skipping.

---

## Shared Patterns (cross-cutting)

### Pattern A: `'use cache'` + `cacheTag` + `cacheLife` (DAL preservation)

**Source:** `src/data/browse.ts:44-56` (UNCHANGED on rewire)
**Apply to:** Rewired `getBrowseArchetypeCounts` only — cache scope is preserved verbatim. No invalidation work needed (RESEARCH §Pitfall 6).

```typescript
'use cache'
cacheTag('explore', 'explore:browse')
cacheLife('hours')
```

The function body changes; the directive block does not.

---

### Pattern B: Anti-magic-number weight rescale

**Source:** `src/lib/similarity.ts:13-32` (existing `WEIGHTS` literal)
**Apply to:** `TASTE_SUB_WEIGHTS` rebalance in similarity.ts (D-SIM-02).

The comment at similarity.ts:10-12 forbids hardcoded rescaled values. The rebalance uses `_BASE` constant + `Object.fromEntries(... × RESCALE)` — never inline `0.50 / 0.25 / 0.25`.

---

### Pattern C: Drizzle migration two-step (drizzle local + supabase prod)

**Source:** project memory `project_drizzle_supabase_db_mismatch` + the existing 11-drizzle + 41-supabase migration history
**Apply to:** Both NEW migration files.

| Side | Path | Ordinal | Body identical? |
|------|------|---------|-----------------|
| Drizzle (local only) | `drizzle/0012_phase49_1_drop_primary_archetype.sql` | sequential — must be `0012` | Yes |
| Supabase (prod) | `supabase/migrations/20260519010000_phase49_1_drop_primary_archetype.sql` | date-prefix `>20260519000000` | Yes — same DROP COLUMN IF EXISTS body, qualified `public.watches_catalog` |

**Sequencing invariant** (D-MIG-02): drizzle migration ships with code waves to keep dev synced; supabase prod migration runs LAST after all code is deployed.

---

### Pattern D: Sibling-survivor (era / eraSignal axis)

**Source:** every modified file with a sibling era field that survives the change.
**Apply to:** templates.ts (use era-echo as predicate analog), viewerTasteProfile.ts (dominantEraSignal survivor), fit-delta.ts (eraSignal dimension survivor), ReferenceIdentityCard.tsx (ERA_LABELS survivor), CompareTable (Row 5 Era survivor), vocab.ts (era_signal block in validateAndCleanTaste), enricher.ts (era_signal in TASTE_TOOL), prompt.ts (era_signal prompt line), Zod schema (era enum survivor), types.ts (eraSignal field survivor).

**Why load-bearing:** the era field has the exact same shape as the archetype field at every site. The post-modification shape is "the archetype block is gone, the era block stays — and looks like it always did." No new abstractions; pure deletion or pivot to era.

---

### Pattern E: `.strict()` Zod mass-assignment guard

**Source:** `src/app/actions/search.ts:38` (`.strict()` on `searchSchema`)
**Apply to:** Zod schema modification — dropping `genre`/`archetype` keys.

The `.strict()` invariant means unknown keys are rejected. Old URLs containing `?archetype=…` or `?genre=…` will error at the action boundary — acceptable per D-EXPLORE-03 + RESEARCH §V5 Input Validation row.

---

### Pattern F: Test mock setup (vitest + drizzle + next/cache)

**Source:** `src/data/__tests__/browse.test.ts:15-37`
**Apply to:** any new browse-DAL or migration test (Wave 0).

```typescript
vi.mock('@/db', () => ({ db: { execute: vi.fn() } }))
vi.mock('next/cache', () => ({ cacheLife: vi.fn(), cacheTag: vi.fn() }))
```

For integration tests against a live DB (`tests/integration/`): use the `DATABASE_URL`-gated `maybe = process.env.DATABASE_URL ? describe : describe.skip` idiom from `tests/integration/catalog-taste.test.ts:8`.

---

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| `src/components/search/GenreChips.tsx` | DELETED | — | Pure deletion; no post-modification shape |
| `src/components/search/ArchetypeChips.tsx` | DELETED | — | Pure deletion; no post-modification shape |
| `src/app/explore/genres/page.tsx` | DELETED | — | Pure deletion; route returns 404 |

These three deletions have no analog by design. The planner deletes the files; no replacement pattern is needed.

---

## Metadata

**Analog search scope:**
- `src/data/` (DAL), `src/lib/` (types + engine + verdict + taste), `src/components/` (UI), `src/app/` (RSC + actions)
- `drizzle/` + `supabase/migrations/` (migrations)
- `tests/` + `src/**/__tests__/` (tests)

**Files scanned:** ~50 (analog candidates), 20+ explicitly read for excerpt extraction

**Key load-bearing analogs (re-verified in this pass):**
- `src/data/catalog.ts:533-544` (getTopStyleTags) — D-EXPLORE-01 unnest precedent ✓
- `src/lib/similarity.ts:13-32` (WEIGHTS rescale) — D-SIM-02 anti-magic-number precedent ✓
- `drizzle/0011_phase38_catalog_id_notnull.sql` + `supabase/migrations/20260519000000_phase47_published_at.sql` — migration two-step precedent ✓
- `src/lib/verdict/viewerTasteProfile.ts:82-86` (dominantEraSignal survivor) — D-VERDICT-03 sibling precedent ✓
- `src/lib/verdict/templates.ts:95-104` (era-echo) — D-VERDICT-01 axis precedent ✓
- `src/lib/verdict/fit-delta.ts:36,77,108-109` (eraSignal dimension survivor) — D-SCOPE-01c precedent ✓
- `src/components/insights/ReferenceIdentityCard.tsx:29-33,61` (ERA_LABELS + hasHeadline survivor) — D-SCOPE-01a precedent ✓
- `src/lib/taste/vocab.ts:111-122` (era_signal validate block) — enricher path precedent ✓
- `src/app/actions/search.ts:34,124` (era Zod + passthrough survivor) — D-SCOPE-01f precedent ✓
- `tests/integration/catalog-taste.test.ts:1-22` (DATABASE_URL-gated integration) — Wave 0 test precedent ✓

**Pattern extraction date:** 2026-05-19
