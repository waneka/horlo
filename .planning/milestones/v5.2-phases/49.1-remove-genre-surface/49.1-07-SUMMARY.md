---
phase: 49.1-remove-genre-surface
plan: 07
subsystem: database
tags: [drizzle, postgres, schema-migration, watches_catalog, primary_archetype, genre-removal]

# Dependency graph
requires:
  - phase: 49.1-06
    provides: "All reader/writer code references to primary_archetype removed from src/data/* and src/lib/taste/*"
provides:
  - "src/db/schema.ts no longer declares the primary_archetype column on watchesCatalog (type-system clean)"
  - "drizzle/0012_phase49_1_drop_primary_archetype.sql: idempotent ALTER TABLE DROP COLUMN IF EXISTS migration"
  - "Source-of-truth artifact for the local dev DB column drop; ready for drizzle-kit push when user runs it in main repo"
affects: [49.1-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pattern C (RESEARCH §Pattern C): two-step local/prod migration trail — drizzle/0012_*.sql (local) mirrors a future supabase/migrations/YYYYMMDDHHMMSS_*.sql (prod) authored in Plan 08"
    - "Postgres implicit CHECK-constraint cleanup on DROP COLUMN (no explicit DROP CONSTRAINT statement needed)"

key-files:
  created:
    - "drizzle/0012_phase49_1_drop_primary_archetype.sql"
  modified:
    - "src/db/schema.ts"

key-decisions:
  - "Authored the SQL migration file but deferred drizzle-kit push execution — worktree has no .env.local/DATABASE_URL"
  - "Did NOT manually edit drizzle/meta/_journal.json (T-49.1-16: drizzle-kit owns this file; manual edits forbidden — journal updates when user runs push post-merge)"
  - "Kept the bare ALTER TABLE without a DO $$ existence guard — the IF EXISTS clause provides idempotency directly; the 0011 analog's DO $$ block guarded against a NOT NULL constraint check that does not apply to a column drop"

patterns-established:
  - "Migration file: header comment + statement-breakpoint + IF EXISTS clause for safe re-runs"
  - "drizzle-kit ordinal sequencing: 0012 follows 0011_phase38_catalog_id_notnull (sequential, not date-prefixed — distinct from supabase YYYYMMDDHHMMSS convention)"

requirements-completed: [TAX-02]

# Metrics
duration: 4min
completed: 2026-05-20
---

# Phase 49.1 Plan 07: Drop primary_archetype from watchesCatalog drizzle schema + author 0012 local migration Summary

**Drizzle schema and 0012 migration file authored for the local-side primary_archetype column drop; drizzle-kit push deferred to main repo (worktree lacks DATABASE_URL).**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-20T07:06:11Z
- **Completed:** 2026-05-20T07:10:26Z
- **Tasks:** 2
- **Files modified:** 1 (schema.ts)
- **Files created:** 1 (0012 migration)

## Accomplishments

- Removed the `primaryArchetype: text('primary_archetype')` column declaration from `src/db/schema.ts:390` inside the `watchesCatalog` pgTable. Sibling columns (`eraSignal`, `designMotifs`, `formality`, `sportiness`, `heritageScore`, `confidence`, `extractedFromPhoto`) preserved unchanged.
- Authored `drizzle/0012_phase49_1_drop_primary_archetype.sql` with an idempotent `ALTER TABLE "watches_catalog" DROP COLUMN IF EXISTS "primary_archetype"` body and a header comment matching the `0011_phase38_catalog_id_notnull.sql` analog.
- `drizzle-kit check` reports "Everything's fine" — migration structure is consistent with the existing schema and journal state.
- `npx tsc --noEmit` produces the same 70 pre-existing errors (no new errors introduced by the schema change; D-MIG-01 step 1 is type-system-clean).
- Confirmed the `watches_catalog_primary_archetype_check` CHECK constraint (added by `supabase/migrations/20260430000000_phase19_1_taste_constraints.sql`) will be dropped implicitly by Postgres along with the column — no explicit `DROP CONSTRAINT` statement needed (RESEARCH.md §Pitfall 7).

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove primary_archetype column declaration from src/db/schema.ts** — `e9b76ab` (refactor)
2. **Task 2: Author drizzle/0012_phase49_1_drop_primary_archetype.sql** — `ee08caf` (feat)

**Plan metadata:** (final docs commit added after SUMMARY.md write — see bottom of file)

## Files Created/Modified

- `src/db/schema.ts` — Deleted line 390 (`primaryArchetype: text('primary_archetype'),`); surrounding taste-attribute columns preserved.
- `drizzle/0012_phase49_1_drop_primary_archetype.sql` — Idempotent DROP COLUMN migration with header comment, statement-breakpoint separator, and `IF EXISTS` clause.

## Decisions Made

1. **Deferred `drizzle-kit push` execution to main repo.** The Claude Code worktree at `.claude/worktrees/agent-a8daa374e9294114d/` does not include `.env.local`, so `DATABASE_URL` is unavailable and `drizzle-kit push` cannot connect to the local Postgres dev DB. Per the executor brief: the schema.ts edit is the source of truth; the SQL migration file is the artifact; the actual push is a convenience the user runs in the main repo after merging this branch. Documented in commit message and below under **User Setup Required**.

2. **Did NOT manually edit `drizzle/meta/_journal.json`.** Per the plan's threat model entry T-49.1-16, drizzle-kit owns this file and manual edits are forbidden. The 0012 entry will be added automatically by `drizzle-kit push` when the user runs it in the main repo. `drizzle-kit check` already passes without the journal entry, confirming the SQL file alone is structurally consistent.

3. **Used a bare `ALTER TABLE ... DROP COLUMN IF EXISTS` without a `DO $$` existence guard.** The 0011 analog wrapped its statement in a `DO $$` block to guard a NOT NULL constraint check, which does not apply to a column drop. `IF EXISTS` alone provides full idempotency for the drop operation.

## Deviations from Plan

None — plan executed exactly as written, with the documented `drizzle-kit push` deferral that was pre-authorized in the executor brief (additional_context: *"If DATABASE_URL is not configured for the worktree: Document in SUMMARY.md that the local push could not be executed in this worktree environment and must be run by the user in the main repo after merge. Do NOT block the plan on this — the schema.ts change is the source of truth; the drizzle-kit push is a convenience that the user can run."*).

## Issues Encountered

- **Worktree DATABASE_URL absent.** Anticipated by the executor brief; resolved by deferring the push step and documenting the user action. Not a blocker.
- **Pre-existing TypeScript errors (70).** All unrelated to this plan's changes (errors in `LayoutProps`, `RailEntry` discriminated union, `PreferencesClient.debt01.test.tsx`, etc.). Confirmed via `npx tsc --noEmit` run on both pre- and post-edit states — error count is identical. Out of scope per `<deviation_rules>` SCOPE BOUNDARY.

## User Setup Required

To complete the local-DB side of D-MIG-01 after merging this branch to main, the user must run:

```bash
cd /Users/tylerwaneka/Documents/horlo
npx drizzle-kit push
# When prompted to confirm the destructive ALTER TABLE DROP COLUMN, accept (respond 'yes').
```

This will:
1. Read the updated `src/db/schema.ts` (no `primaryArchetype` declaration).
2. Diff against the local Postgres state at `127.0.0.1:54322` (supabase_db_horlo container).
3. Apply the `ALTER TABLE "watches_catalog" DROP COLUMN IF EXISTS "primary_archetype"` statement (the implicitly dropped CHECK constraint cleans up too).
4. Append the new entry to `drizzle/meta/_journal.json` with `idx: 12`.

Verify by running:
```bash
npx drizzle-kit check                                  # should report no pending diffs
psql "$DATABASE_URL" -c '\d watches_catalog'           # column list should NOT include primary_archetype
DATABASE_URL=... npm test tests/integration/migration-drop-archetype.test.ts
                                                       # Wave 0 integration test transitions from skipped/RED to GREEN
```

If the supabase docker container is not running, start it first: `supabase start`.

## Next Phase Readiness

- **Plan 49.1-08 (prod supabase migration):** Ready. All readers/writers of `primary_archetype` are gone from code (Plans 02-06), the type system no longer projects the column (this plan), and the local-side DDL artifact exists. Plan 08 can now author the prod-side `supabase/migrations/YYYYMMDDHHMMSS_phase49_1_drop_primary_archetype.sql` file and run `supabase db push --linked` against prod with the safety guarantee that no application code references the column anymore.
- **Wave 0 integration test `tests/integration/migration-drop-archetype.test.ts`:** Already wired to detect the post-drop contract. After the user runs `drizzle-kit push` locally, all three test assertions will pass (column-absent check, `getWatchesByUser` shape check, `updateCatalogTaste` no-`primaryArchetype`-key check).

## Self-Check: PASSED

- FOUND: `src/db/schema.ts` (modified — `primary_archetype` declaration absent)
- FOUND: `drizzle/0012_phase49_1_drop_primary_archetype.sql` (created — contains `DROP COLUMN IF EXISTS`)
- FOUND: commit `e9b76ab` (Task 1, refactor)
- FOUND: commit `ee08caf` (Task 2, feat)
- VERIFIED: `npx drizzle-kit check` reports "Everything's fine"
- VERIFIED: `grep -c 'primary_archetype\|primaryArchetype' src/db/schema.ts` returns 0
- VERIFIED: `grep -nE 'era_signal|design_motifs' src/db/schema.ts` returns sibling columns intact

---
*Phase: 49.1-remove-genre-surface*
*Completed: 2026-05-20*
