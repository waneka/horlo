---
phase: 49.1-remove-genre-surface
verified: 2026-05-20T06:45:00Z
status: human_needed
score: 7/7 must-haves verified (SC#1 partial — prod push user-gated as designed)
overrides_applied: 0
human_verification:
  - test: "supabase db push --linked against linked prod project"
    expected: "watches_catalog.primary_archetype is dropped from prod; supabase db diff reports no schema drift"
    why_human: "Plan 08 Task 2 is autonomous:false by design per D-MIG-02 sequencing invariant — must run AFTER prod Vercel deploy of reader-code removal; cannot be executed by the verifier"
  - test: "Run tests/integration/migration-drop-archetype.test.ts against prod DATABASE_URL after Task 2"
    expected: "3 cases GREEN (column absent + upsert succeeds + LEFT JOIN works)"
    why_human: "Requires prod DATABASE_URL credentials and prod-pointed test run; Plan 08 Task 3"
  - test: "Browser smoke: /explore loads with CollectorArchetypes rail deep-linking to ?style=X (not ?archetype=)"
    expected: "Rail renders chips for non-zero style_tags; clicking a chip navigates to /search?style=<tag>"
    why_human: "Visual UI rendering + click navigation; cannot be programmatically verified without running the app"
  - test: "Browser smoke: /watch/{id} and /catalog/{id} ReferenceIdentityCard renders identically with era-only headline"
    expected: "Both pages display the same card markup; no archetype text remains; D-39b-04 parity holds visually"
    why_human: "Visual UI parity + identical rendering lock requires human comparison of two pages"
  - test: "Browser smoke: /explore/genres returns 404 in prod after deploy"
    expected: "404 page rendered (Next.js default) — confirms route removal is live"
    why_human: "Live HTTP check against deployed environment; static filesystem test passes but live route confirmation needs a request to the deployed host"
---

# Phase 49.1: Remove Genre Surface — Verification Report

**Phase Goal:** The genre/archetype taxonomy surface is removed: `primary_archetype` column dropped from `watches_catalog`, `GenreChips` + `ArchetypeChips` + `/explore/genres` + `archetype-config.ts` deleted (NOTE: `archetype-config.ts` is RETAINED per D-EXPLORE-02 carve-out), catalog DAL and similarity engine updated. Single source of truth for the functional-category axis becomes `style_tags`.

**Verified:** 2026-05-20
**Status:** `human_needed` — 7/7 structural Success Criteria verified in code; the remaining acceptance gates are prod operations and visual UI smoke tests that REQUIRE human action (per Plan 08's documented `autonomous: false` gating).
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (mapped from ROADMAP SC#1–7)

| #   | Truth (Success Criterion) | Status     | Evidence       |
| --- | ------------------------- | ---------- | -------------- |
| SC#1a | `watches_catalog.primary_archetype` gone from `src/db/schema.ts` | ✓ VERIFIED | `grep -n "primary_archetype" src/db/schema.ts` returns 0 matches; exit 1 |
| SC#1b | Drizzle local migration `drizzle/0012_phase49_1_drop_primary_archetype.sql` exists | ✓ VERIFIED | File present, body: `ALTER TABLE "watches_catalog" DROP COLUMN IF EXISTS "primary_archetype";` |
| SC#1c | Supabase prod migration `supabase/migrations/20260520070000_phase49_1_drop_primary_archetype.sql` exists | ✓ VERIFIED | File present, mirrors drizzle migration with D-MIG-02 sequencing note |
| SC#1d | Supabase prod migration has been PUSHED to prod | ⏸ PARTIAL (deferred) | Plan 08 Task 2 is `autonomous: false` per D-MIG-02 — user-gated post-deploy operation. NOT a failure; intentional gating. Surfaces in `human_verification`. |
| SC#2a | `GenreChips`, `ArchetypeChips`, `/explore/genres/page.tsx` deleted | ✓ VERIFIED | `ls src/components/search/GenreChips.tsx` → ENOENT; `ls src/components/search/ArchetypeChips.tsx` → ENOENT; `ls src/app/explore/genres/` → ENOENT |
| SC#2b | `archetype-config.ts` RETAINED (D-EXPLORE-02 carve-out) | ✓ VERIFIED | `ls src/lib/archetype-config.ts` → present; consumed by `CollectorArchetypes.tsx:22` |
| SC#2c | `FilterDrawer.tsx` no longer imports/renders chip groups | ✓ VERIFIED | `grep "GenreChips\|ArchetypeChips\|archetype-config"` → 0 matches |
| SC#2d | `BrowseModule.tsx` no longer deep-links to `/explore/genres` | ✓ VERIFIED | `grep "GenreChips\|ArchetypeChips\|/explore/genres\|archetype-config"` → 0 matches |
| SC#3a | `CatalogSearchFilters` in `src/data/catalog.ts` no `genre`/`archetype` fields | ✓ VERIFIED | Interface at line 279 has movement/size/style/brand/era only; no genre/archetype fields |
| SC#3b | Archetype-wins tiebreaker removed | ✓ VERIFIED | catalog.ts:445-446 documents removal: "archetype-wins tiebreaker is gone with the predicate" |
| SC#3c | `getBrowseGenreCounts()` deleted from `src/data/browse.ts` | ✓ VERIFIED | `grep -rn "getBrowseGenreCounts"` returns 0 across `src/`; only `getBrowseArchetypeCounts` (rewired to style_tags) remains |
| SC#4a | `TASTE_SUB_WEIGHTS_BASE` + `RESCALE` algorithmic pattern (no magic numbers) | ✓ VERIFIED | `src/lib/similarity.ts:45-56` — `BASE = {numericTrioCosine: 0.40, eraMatch: 0.20, motifsJaccard: 0.20}` (sums to 0.80), `RESCALE = 1 / (1 - 0.20) = 1.25`, `TASTE_SUB_WEIGHTS = Object.fromEntries(× RESCALE)` |
| SC#4b | Archetype categorical-match block at line 125 removed | ✓ VERIFIED | `grep "primaryArchetype\|archetypeMatch" src/lib/similarity.ts` returns 0 matches |
| SC#4c | Sub-weights post-rescale sum to 1.00 | ✓ VERIFIED | 0.40 × 1.25 = 0.50; 0.20 × 1.25 = 0.25; 0.20 × 1.25 = 0.25; total 1.00 |
| SC#5a | `src/lib/taste/vocab.ts validateAndCleanTaste()` does not write `primary_archetype` | ✓ VERIFIED | Function (lines 84-124) returns `{formality, sportiness, heritageScore, eraSignal, designMotifs, confidence}` — no archetype field |
| SC#5b | `src/lib/taste/enricher.ts` TASTE_TOOL.input_schema has no `primary_archetype` | ✓ VERIFIED | `TASTE_TOOL.input_schema.properties` (lines 39-57) lists only formality, sportiness, heritage_score, era_signal, design_motifs, confidence |
| SC#5c | `src/lib/taste/prompt.ts` does not instruct on `primary_archetype` | ✓ VERIFIED | `grep "primary_archetype\|primaryArchetype"` returns only documentation comment confirming removal |
| SC#5d | `PRIMARY_ARCHETYPES` const RETAINED (Pitfall 5 — consumed by browse.ts) | ✓ VERIFIED | `src/lib/taste/vocab.ts:16-19` exports `PRIMARY_ARCHETYPES` const; documented at lines 46-47 as retained for `/explore` CollectorArchetypes rail |
| SC#6a | `/explore/genres` route deletion | ✓ VERIFIED | `tests/app/explore-genres-404.test.ts` GREEN (1/1 passing) |
| SC#6b | No internal links to `/explore/genres` in src/ | ✓ VERIFIED | `grep -rn "/explore/genres" src/` returns 0 matches |
| SC#7a | Existing tests still pass | ✓ VERIFIED | 10 phase-49.1-touched test files × 65 tests all GREEN |
| SC#7b | New tests cover simplified search filter surface AND rebalanced similarity weights | ✓ VERIFIED | `browse.test.ts` asserts `not.toMatch(/primary_archetype/)`; `vocab.test.ts` + `enricher.test.ts` cover dropped wire field; verdict tests cover era-axis pivot; CollectorArchetypes test asserts `?style=` deep-link |

**Score:** 7/7 ROADMAP Success Criteria verified in code. SC#1 has one component (prod push) that is intentionally user-gated and surfaces in `human_verification`.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/db/schema.ts` | No `primaryArchetype` column declaration | ✓ VERIFIED | Column declaration absent (grep 0 matches) |
| `drizzle/0012_phase49_1_drop_primary_archetype.sql` | DROP COLUMN IF EXISTS migration | ✓ VERIFIED | Exists with correct body + D-MIG-02 sequencing comment |
| `supabase/migrations/20260520070000_phase49_1_drop_primary_archetype.sql` | Mirror of drizzle migration | ✓ VERIFIED | Exists; filename ordinal > previous latest (gotcha #1) |
| `src/components/search/GenreChips.tsx` | DELETED | ✓ VERIFIED | ENOENT |
| `src/components/search/ArchetypeChips.tsx` | DELETED | ✓ VERIFIED | ENOENT |
| `src/app/explore/genres/page.tsx` | DELETED | ✓ VERIFIED | ENOENT (parent dir also absent) |
| `src/lib/archetype-config.ts` | RETAINED (D-EXPLORE-02) | ✓ VERIFIED | Present; consumed only by CollectorArchetypes (legitimate explore-rail usage) |
| `src/data/catalog.ts` | `CatalogSearchFilters` has no genre/archetype | ✓ VERIFIED | Interface at line 279 confirms |
| `src/data/browse.ts` | `getBrowseGenreCounts` deleted; `getBrowseArchetypeCounts` rewired to `style_tags` | ✓ VERIFIED | `getBrowseArchetypeCounts` SQL uses `unnest(style_tags)` (line 60-66); no `primary_archetype` SQL reference (test enforced) |
| `src/lib/similarity.ts` | RESCALE pattern + no archetypeMatch | ✓ VERIFIED | Lines 45-56 confirm pattern |
| `src/lib/taste/vocab.ts` | TasteSchema + validateAndCleanTaste both drop archetype field | ✓ VERIFIED | Wire schema has no primary_archetype; return type has no archetype |
| `src/lib/taste/enricher.ts` | TASTE_TOOL schema drops primary_archetype | ✓ VERIFIED | Properties + required arrays both clean |
| `src/lib/taste/prompt.ts` | No primary_archetype instruction line | ✓ VERIFIED | Documentation comment confirms removal; no live instruction |
| `tests/app/explore-genres-404.test.ts` | Filesystem-absence smoke test | ✓ VERIFIED | Test passes (1/1) |

---

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `CollectorArchetypes.tsx` | `getBrowseArchetypeCounts` | `import { getBrowseArchetypeCounts } from '@/data/browse'` | WIRED | Line 21; called at line 40 |
| `CollectorArchetypes.tsx` | `ARCHETYPE_CONFIG` (archetype-config.ts) | `import { ARCHETYPE_CONFIG } from '@/lib/archetype-config'` | WIRED | Line 22; drives display order (D-15) |
| `getBrowseArchetypeCounts` | `style_tags` column | `SELECT tag, COUNT(*) FROM unnest(style_tags) AS tag` | WIRED | browse.ts:60-66 |
| `/watch/{id}/page.tsx` | `ReferenceIdentityCard` | `<ReferenceIdentityCard taste={watch.catalogTaste} />` | WIRED + PARITY | line 92 |
| `/catalog/{id}/page.tsx` | `ReferenceIdentityCard` | `<ReferenceIdentityCard taste={catalogTaste} />` | WIRED + PARITY | line 215; D-39b-04 identical-rendering lock holds — both pass `taste` prop only, neither passes `primaryArchetype` |
| `searchCatalogWatches` filters | `CatalogSearchFilters` interface | filter consumption sites in WHERE-builder | WIRED | No `filters.genre` or `filters.archetype` lookups; only movement/size/style/brand/era |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `CollectorArchetypes` rail | `counts` | `getBrowseArchetypeCounts()` → SQL `unnest(style_tags)` | Yes (live SQL query on watches_catalog.style_tags) | ✓ FLOWING |
| `ReferenceIdentityCard` | `taste` prop | `watch.catalogTaste` (watch page) / `catalogTaste` literal (catalog page) | Yes (DB SELECT in DAL projection — primaryArchetype dropped per Pitfall 3) | ✓ FLOWING |
| Similarity engine (tasteSimilarityRaw01) | `t1, t2: CatalogTasteAttributes` | DAL projection from `catalog_taste` JSONB column | Yes; only 6 fields (no archetype) flow into the function | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| `/explore/genres` route file absent | `existsSync(src/app/explore/genres/page.tsx)` (via vitest) | false | ✓ PASS |
| All phase-touched tests | `npm test -- <10 files>` | 65/65 GREEN in 1.89s | ✓ PASS |
| ESLint phase-touched files | `npx eslint <16 files>` | 0 errors, 6 unused-var warnings (unrelated to genre surface removal) | ✓ PASS |
| Sub-weight invariant | algebraic: 0.40·1.25 + 0.20·1.25 + 0.20·1.25 | 1.00 | ✓ PASS |
| Live prod 404 on `/explore/genres` | curl against deployed host | n/a — needs prod deploy | ? SKIP (human verification) |

---

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| n/a | Phase 49.1 declares no `scripts/*/tests/probe-*.sh` artifacts; conventional probes do not apply (this is a Next.js code-level removal phase, not a migration/tooling phase requiring shell probes) | — | SKIP (no probes) |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| TAX-02 | All 8 plans (49.1-01 through 49.1-08) | Remove genre/archetype taxonomy surface | ✓ SATISFIED (code-side) | All 7 ROADMAP SC verified in code; SC#1d (prod push) deferred per D-MIG-02 — surfaces as human_verification |

No orphaned requirements.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |

No debt markers (`TBD/FIXME/XXX`) found in any phase-49.1-touched file. No stub returns, no hardcoded empty arrays, no placeholder comments.

Note: `.next/dev/types/validator.ts` references the deleted `src/app/explore/genres/page.js` — this is a stale Turbopack dev-cache artifact (`.next/` is gitignored). Per the user's `project_turbopack_next_cache_stale_css` memory pattern, the cache must be cleared (`rm -rf .next`) on the dev host. Not a source-code defect.

---

### Human Verification Required

1. **Prod push (Plan 08 Task 2)** — Run `git push origin main` → wait for Vercel deploy → then `supabase db push --linked`. The supabase migration file is staged and idempotent (`IF EXISTS`).
   - **Expected:** `supabase db diff` reports no schema drift after push.
   - **Why human:** `autonomous: false` per D-MIG-02 — cannot run before prod Vercel deploy of reader-code removal.

2. **Post-deploy migration test (Plan 08 Task 3)** — `DATABASE_URL=<prod> npm test -- tests/integration/migration-drop-archetype.test.ts`.
   - **Expected:** 3 cases GREEN (column absent + upsert succeeds + LEFT JOIN works).
   - **Why human:** Needs prod credentials.

3. **`/explore` rail visual smoke** — Open `/explore` post-deploy.
   - **Expected:** `CollectorArchetypes` chips render and deep-link to `/search?style=<tag>` (NOT `?archetype=`).
   - **Why human:** Visual rendering + click navigation.

4. **Watch + Catalog page parity smoke** — Open `/watch/{id}` and `/catalog/{id}` for the same reference.
   - **Expected:** `ReferenceIdentityCard` renders identically; no archetype text; era-only headline. D-39b-04 lock holds visually.
   - **Why human:** Side-by-side visual comparison.

5. **`/explore/genres` 404 in prod** — `curl -I https://<prod>/explore/genres`.
   - **Expected:** HTTP 404.
   - **Why human:** Live HTTP check on deployed host.

---

### Gaps Summary

**No structural gaps in the codebase.** All 7 ROADMAP Success Criteria are met at the code level. The phase has correctly:

- Dropped the `primaryArchetype` column from `src/db/schema.ts` and authored both drizzle + supabase migrations.
- Deleted `GenreChips`, `ArchetypeChips`, and `/explore/genres/page.tsx`.
- Retained `archetype-config.ts` and `PRIMARY_ARCHETYPES` const per the documented D-EXPLORE-02 / Pitfall 5 carve-outs (these power the `/explore` CollectorArchetypes rail which now reads from `style_tags`).
- Removed all `filters.genre` / `filters.archetype` lookups, archetype-wins tiebreaker, and `getBrowseGenreCounts()`.
- Replaced the magic-number archetype sub-weight with an algorithmic `RESCALE = 1 / (1 - REMOVED_SUB_WEIGHT)` pattern; remaining sub-weights sum to exactly 1.00.
- Cleaned the enricher chain (vocab.ts wire schema, enricher.ts tool schema, prompt.ts) so no LLM writer path emits `primary_archetype`.
- Wired the verdict engine to pivot from `primaryArchetype` to `eraSignal` (templates + composer + viewerTasteProfile + fit-delta).
- D-39b-04 identical-rendering lock holds: both `/watch/{id}` and `/catalog/{id}` render `ReferenceIdentityCard` with `taste={...}` only — neither passes a `primaryArchetype` prop.

**Goal-backward question:** "Can a developer assess a watch by archetype/genre?" — NO. The chips are deleted, `/explore/genres` is a 404, the similarity engine has no `archetypeMatch` block, the verdict engine pivoted off the field, and the LLM enricher does not write the field. "What's the functional-category axis?" — `style_tags` (sole source of truth).

**Why status is `human_needed`, not `passed`:** Five live-environment + visual-UI items legitimately require human action. The codebase side is complete; the human side is the prod operation gated by D-MIG-02.

---

_Verified: 2026-05-20T06:45:00Z_
_Verifier: Claude (gsd-verifier)_
