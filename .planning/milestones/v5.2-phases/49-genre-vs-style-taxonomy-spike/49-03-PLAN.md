---
phase: 49-genre-vs-style-taxonomy-spike
plan: 03
type: execute
wave: 3
depends_on: ["49-01", "49-02"]
files_modified:
  - .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md
autonomous: true
requirements: [TAX-01]
requirements_addressed: [TAX-01]

must_haves:
  truths:
    - "49-SPIKE.md `## Options` section enumerates all 5 options from D-05 §5 (consolidate, remove genre, remove style, unify archetype/genre surface, keep all as-is) with schema/UX/migration/irreversibility per option"
    - "49-SPIKE.md `## Decision Matrix` scores each option against the 5 D-05 criteria (UX clarity, schema simplicity, expressive power preserved, migration cost, irreversibility)"
    - "49-SPIKE.md `## Recommendation` states a single primary recommendation for genre↔style AND a sub-recommendation for genre↔archetype, labeled with one of: consolidate, remove-genre, remove-style, keep-both, unify-archetype-surface"
    - "49-SPIKE.md `## Cost Estimate per Option` lists files touched, drizzle + supabase migrations needed, data backfill required, test surface for each option"
    - "49-SPIKE.md `## Ship-Now Eligibility Check` outputs YES / NO / NEEDS-DISCUSSION for each recommendation against ROADMAP SC#4 (cheap AND strongly favored), naming the trigger gate"
  artifacts:
    - path: ".planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md"
      provides: "Sections 5-9 (Options, Decision Matrix, Recommendation, Cost Estimate, Ship-Now Eligibility) — completes the 9-section deliverable"
      contains: "## Options, ## Decision Matrix, ## Recommendation, ## Cost Estimate per Option, ## Ship-Now Eligibility Check"
  key_links:
    - from: "49-SPIKE.md §Recommendation"
      to: "49-SPIKE.md §Options + §Decision Matrix + §Live-Catalog Evidence"
      via: "the recommendation cites the matrix score AND the evidence numbers that drove it"
      pattern: "Recommendation|primary|sub-recommendation"
    - from: "49-SPIKE.md §Ship-Now Eligibility Check"
      to: "ROADMAP.md Phase 49 SC#4"
      via: "explicit YES/NO/NEEDS-DISCUSSION against the cheap-AND-strongly-favored gate"
      pattern: "cheap.*strongly favored|SC.4|requirement.*add"
---

<objective>
Synthesize Plans 01 (consumer map + overlap matrix) and 02 (live-catalog evidence) into sections 5-9 of 49-SPIKE.md: Options, Decision Matrix, Recommendation, Cost Estimate per Option, and Ship-Now Eligibility Check.

Purpose: This is the actual deliverable TAX-01 asks for — a written recommendation backed by code AND data evidence. Sections 1-4 are the facts; sections 5-9 are the call.

Output: 49-SPIKE.md fully populated end-to-end across all 9 D-05 sections. After this plan, the spike is done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-genre-vs-style-taxonomy-spike/49-CONTEXT.md
@.planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md
@.planning/phases/49-genre-vs-style-taxonomy-spike/49-01-SUMMARY.md
@.planning/phases/49-genre-vs-style-taxonomy-spike/49-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Options + Decision Matrix sections (§5 and §6)</name>
  <files>.planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md</files>
  <read_first>
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md (entire current file — sections 1-4 from Plans 01 + 02 are the substrate for the synthesis)
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-CONTEXT.md (D-05 §5 names the 5 options + §6 names the 5 criteria; §Code Insights §"Established Patterns" + §"Integration Points" inform the migration cost numbers; §Specifics notes "removing genre is cheap; removing style is not")
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-01-SUMMARY.md (Plan 01's note on any consumer surfaces beyond D-01 — these change the cost estimate)
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-02-SUMMARY.md (Plan 02's headline findings — the numeric basis for matrix scores)
    - .planning/REQUIREMENTS.md (TAX-01 + Out of Scope table; the 5 options must be evaluated against what TAX-01 actually asks)
  </read_first>
  <action>
    Replace the `_To be filled by Plan 02 / Plan 03._` placeholder under `## Options` (D-05 §5) with full content for all 5 options. For EACH option, write a 3-5 line block with these four sub-points labeled:

      - **Schema change:** what columns/tables/enums change on `watches`, `watches_catalog`, `user_preferences`
      - **UX change:** what chip surfaces / filter labels / display elements change
      - **Migration needed:** drizzle migration file + supabase migration script + data backfill (cite the prod-wipeable note from MEMORY where relevant)
      - **Irreversibility:** is this a one-way door (column drop, vocab change) or reversible (UI hide, feature flag)

    The 5 options from D-05 §5 (use these labels verbatim — they become anchors the Recommendation section references):
      A. `consolidate` — merge genre and style into one surface (e.g. unified `tags[]` array with `primary_*` highlighted)
      B. `remove-genre` — drop `primary_archetype` column + GenreChips + ArchetypeChips + filters.genre + filters.archetype
      C. `remove-style` — drop `style_tags` columns + StyleChips + FilterBar style filter + preferredStyles/dislikedStyles + WatchCard display + insights aggregation + WatchForm/AddWatchFlow write paths
      D. `unify-archetype-surface` — keep both fields, but remove either GenreChips OR ArchetypeChips (since they filter the same column with the same 10 values); pick which one to keep
      E. `keep-both` — status quo; document the divergence honestly so future readers don't re-litigate this

    Then replace the `_To be filled by Plan 02 / Plan 03._` placeholder under `## Decision Matrix` (D-05 §6) with a 5×5 markdown table — rows are the 5 options A-E, columns are the 5 criteria (UX clarity, schema simplicity, expressive power preserved, migration cost, irreversibility). Use either numeric scores (1-5) OR ✓/✗/~ — pick the format that reads clearest given the data on hand (D-05 §"Claude's Discretion"). Add a short paragraph after the table explaining how the criteria were scored — especially "migration cost" which depends on the divergence/null-coverage numbers from §4.

    Per D-08 HARD GUARDRAIL: still NO source code changes. Only 49-SPIKE.md is modified.
  </action>
  <verify>
    <automated>test -f .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "^## Options" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "^## Decision Matrix" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qE "consolidate" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qE "remove-genre|remove genre" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qE "remove-style|remove style" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qE "unify-archetype-surface|unify archetype" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qE "keep-both|keep both" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qiE "Schema change" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qiE "UX change" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qiE "Migration needed" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qiE "Irreversibility" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Options` section contains all 5 options (A-E) named verbatim: `consolidate`, `remove-genre`, `remove-style`, `unify-archetype-surface`, `keep-both`
    - Each option has all four sub-points labeled: Schema change, UX change, Migration needed, Irreversibility
    - `## Decision Matrix` section contains a 5×5 (or 5-row, 5-column) table scoring all 5 options against the D-05 §6 criteria: UX clarity, schema simplicity, expressive power preserved, migration cost, irreversibility
    - A short paragraph after the matrix explains the scoring rationale, citing the §4 Live-Catalog Evidence numbers (e.g. "remove-genre scores high on schema simplicity because the agreement rate of X% in §4-Query-1 confirms style largely restates archetype")
    - Plan 02's placeholder under `## Options` and `## Decision Matrix` is gone
    - Sections 1-4 (from Plans 01 + 02) remain present and unchanged
    - No source file outside `.planning/phases/49-genre-vs-style-taxonomy-spike/` is modified
  </acceptance_criteria>
  <done>Options (§5) and Decision Matrix (§6) sections fully populated; all 5 options labeled and scored; rationale cites §4 evidence; sections 7-9 placeholders remain for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write Recommendation + Cost Estimate + Ship-Now Eligibility Check (§7-9) and finalize</name>
  <files>.planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md</files>
  <read_first>
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md (current state — must include Task 1's §5 and §6 plus everything from Plans 01 + 02)
    - .planning/ROADMAP.md lines 185-194 (Phase 49 SC#4 — the cheap-AND-strongly-favored gate language must be quoted in §9)
    - .planning/REQUIREMENTS.md Out of Scope table row 1 ("Implementing a genre/style consolidation … only if the spike strongly favors it and it is cheap — then added as a new requirement mid-milestone") — quote in §9
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-CONTEXT.md D-08 (hard guardrail) and §"Specifics" §3 (the prod-wipeable note that affects cost estimates)
  </read_first>
  <action>
    Replace the `_To be filled by Plan 02 / Plan 03._` placeholder under `## Recommendation` (D-05 §7) with TWO labeled recommendations:

      - **Primary recommendation (genre↔style, TAX-01 direct answer):** must be one of the 5 option labels from §5: `consolidate` / `remove-genre` / `remove-style` / `unify-archetype-surface` / `keep-both`. Follow the label with a 2-4 paragraph rationale citing §3 (overlap matrix) and §4 (evidence numbers) and the §6 matrix scores. The rationale must answer ROADMAP SC#3 ("rationale strong enough to act on") — name the specific numbers/findings that swung the call.
      - **Sub-recommendation (genre↔archetype):** must be one of `unify-archetype-surface` / `keep-both` / `remove-genre` (or `remove-archetype` if §6 supports that). Same shape: label + 1-2 paragraph rationale.

    Replace the `_To be filled by Plan 02 / Plan 03._` placeholder under `## Cost Estimate per Option` (D-05 §8) with a per-option cost table — columns: `Option | Files touched (count + key paths) | Migrations (drizzle / supabase) | Data backfill | Test surface`. ALL 5 options get a row even though only the primary + sub-recommendation will be acted on (or not) — the table must show the cost differential so a reader can sanity-check the recommendation. Per D-05 §"Specifics" §3: distinguish "needs backfill" vs "wipe and re-enrich" — the latter is much cheaper for prod single-user. Cite specific file paths from §2 Consumer Map.

    Replace the `_To be filled by Plan 02 / Plan 03._` placeholder under `## Ship-Now Eligibility Check` (D-05 §9) with two evaluations:

      - **Primary recommendation eligibility:** quote ROADMAP SC#4's gate language verbatim ("No consolidation or removal implementation is shipped in this phase unless the spike specifically flags it as cheap and strongly favored"). Then output one of: `YES` (cheap AND strongly favored — proposes a new requirement for v5.2 mid-milestone via `/gsd-phase` add) / `NO` (not cheap, OR not strongly favored, OR both — defer to a future milestone) / `NEEDS-DISCUSSION` (close call — flag for human review). Provide 2-3 sentences justifying the verdict.
      - **Sub-recommendation eligibility:** same shape; if YES, name the new requirement string (e.g. "TAX-02: unify archetype surface — drop GenreChips, keep ArchetypeChips") so the user can directly add it.

    After all three sections are populated, scan the file ONE final time and confirm there are zero remaining `_To be filled by Plan 02 / Plan 03._` placeholders.

    Per D-08 HARD GUARDRAIL: NO source code changes. The only file touched is 49-SPIKE.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "^## Recommendation" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "^## Cost Estimate" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "^## Ship-Now Eligibility Check" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && ! grep -qE "_To be filled by Plan 02" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qiE "primary recommendation" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qiE "sub-recommendation|sub recommendation" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qE "YES|NO|NEEDS-DISCUSSION" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qiE "cheap.{0,5}(and|&).{0,5}strongly favored|strongly favored.{0,5}(and|&).{0,5}cheap" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && [ "$(grep -cE '^## ' .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md)" -ge 9 ]</automated>
  </verify>
  <acceptance_criteria>
    - `## Recommendation` section contains a labeled "Primary recommendation" line whose label is one of: `consolidate`, `remove-genre`, `remove-style`, `unify-archetype-surface`, `keep-both`
    - `## Recommendation` section contains a labeled "Sub-recommendation" line addressing genre↔archetype
    - Both recommendations include rationale paragraphs that cite specific §3 overlap findings AND §4 evidence numbers
    - `## Cost Estimate per Option` section contains a per-option table with at least 5 rows (one per option) and the columns Files touched, Migrations, Data backfill, Test surface
    - `## Ship-Now Eligibility Check` section quotes ROADMAP SC#4 language (cheap AND strongly favored)
    - Ship-Now section emits a YES / NO / NEEDS-DISCUSSION verdict for BOTH the primary recommendation AND the sub-recommendation
    - If either verdict is YES, the section proposes a concrete new requirement string for the mid-milestone add
    - Zero `_To be filled by Plan 02 / Plan 03._` placeholders remain in the file
    - All 9 D-05 sections are present (`## Domain`, `## Consumer Map`, `## Overlap & Divergence Matrix`, `## Live-Catalog Evidence`, `## Options`, `## Decision Matrix`, `## Recommendation`, `## Cost Estimate per Option`, `## Ship-Now Eligibility Check`) — verified by `grep -c '^## ' 49-SPIKE.md` ≥ 9
    - No source file outside `.planning/phases/49-genre-vs-style-taxonomy-spike/` is modified
  </acceptance_criteria>
  <done>49-SPIKE.md is complete: all 9 D-05 sections populated, primary + sub-recommendations stated with labeled options, cost estimates per option tabled, ship-now eligibility verdicts emitted with cited gate language. No source code touched.</done>
</task>

</tasks>

<threat_model>
**Phase scope:** documentation-only synthesis. This plan modifies one markdown file (`49-SPIKE.md`) and creates one SUMMARY.md.

**STRIDE assessment:**
- No source code, no schema, no migrations, no SQL execution (Plan 02 ran the queries; this plan only references them)
- No new attack surface, no endpoints, no auth flows, no RLS changes, no secrets
- The recommendation MAY propose a new requirement (e.g. "TAX-02: unify archetype surface") but the proposal is text in a doc — implementation requires a separate `/gsd-phase` add per D-08
- No PII, no user data exfiltration, no credentials

**Conclusion:** No HIGH or MEDIUM threats. No blockers under ASVS L1.
</threat_model>

<verification>
- `grep -c '^## ' 49-SPIKE.md` ≥ 9 (all D-05 sections present)
- `grep '_To be filled' 49-SPIKE.md` returns zero (no placeholders remain)
- `grep -E 'YES|NO|NEEDS-DISCUSSION' 49-SPIKE.md` matches in the Ship-Now section
- `grep -iE 'primary recommendation' 49-SPIKE.md` matches AND `grep -iE 'sub-recommendation' 49-SPIKE.md` matches
- `git status --porcelain | grep -v '\.planning/' | wc -l` returns 0 (no source files touched)
- A reader can answer all four ROADMAP SC#1-#4 questions by reading the doc end-to-end (manual review during execute)
</verification>

<success_criteria>
49-SPIKE.md is complete across all 9 D-05 sections. The Recommendation section emits a labeled primary recommendation (one of the 5 option labels) for TAX-01's genre↔style question, AND a labeled sub-recommendation for genre↔archetype. The Ship-Now Eligibility Check evaluates each recommendation against ROADMAP SC#4 with a YES/NO/NEEDS-DISCUSSION verdict. If YES, a concrete new-requirement string is named. No source code is modified. Phase 49 deliverable is shippable as a standalone reference for any future v5.2 mid-milestone consolidation work.
</success_criteria>

<output>
After completion, create `.planning/phases/49-genre-vs-style-taxonomy-spike/49-03-SUMMARY.md` noting:
- Final primary recommendation label and one-line rationale
- Final sub-recommendation label and one-line rationale
- Ship-Now verdict for each (YES / NO / NEEDS-DISCUSSION) and the gate that drove it
- If any verdict is YES, the proposed new-requirement string so the user can run `/gsd-phase` add directly
- Total sections in 49-SPIKE.md (should be 9)
</output>
