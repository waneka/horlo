---
phase: 49-genre-vs-style-taxonomy-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md
autonomous: true
requirements: [TAX-01]
requirements_addressed: [TAX-01]

must_haves:
  truths:
    - "49-SPIKE.md exists and opens with a Domain section restating what the spike decides (genre vs style + the genre↔archetype sub-question)"
    - "Every consumer named in 49-CONTEXT.md D-01 appears as a row in the Consumer Map table with file, line, field used, what it does, and UI label"
    - "Per-axis overlap and divergence is enumerated explicitly — genre↔style AND genre↔archetype both addressed"
  artifacts:
    - path: ".planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md"
      provides: "Sections 1 (Domain), 2 (Consumer Map), 3 (Overlap & Divergence Matrix) of the spike deliverable"
      contains: "## Domain, ## Consumer Map, ## Overlap & Divergence Matrix"
  key_links:
    - from: "49-SPIKE.md Consumer Map rows"
      to: "src/* file:line locations"
      via: "every row cites a real file path and line number from 49-CONTEXT.md §Canonical References"
      pattern: "src/.*\\.ts(x)?:\\d+"
    - from: "49-SPIKE.md Overlap & Divergence Matrix"
      to: "Consumer Map entries"
      via: "each overlap row points back to specific consumers from §2"
      pattern: "GenreChips|ArchetypeChips|StyleChips|primary_archetype|style_tags"
---

<objective>
Open the spike document `49-SPIKE.md` and write sections 1-3 (Domain, Consumer Map, Overlap & Divergence Matrix) by reading every consumer of `genre`, `archetype`, and `style` named in 49-CONTEXT.md D-01.

Purpose: Static-analysis grounding for the decision. Sections 1-3 establish the facts — what fields exist, where they are read/written, and where they overlap or diverge structurally — before live-catalog evidence (Plan 02) and synthesis (Plan 03) build on top.

Output: `49-SPIKE.md` populated with three labeled sections, all citations resolving to real file:line locations in the current tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-genre-vs-style-taxonomy-spike/49-CONTEXT.md
@.planning/phases/48-user-facing-bug-fixes/48-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 49-SPIKE.md + write Domain section + write Consumer Map covering all 9 surfaces</name>
  <files>.planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md</files>
  <read_first>
    Required reads — every file below MUST be opened (not just grepped) so the Consumer Map cites real, current line numbers:
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-CONTEXT.md (entire file — D-01 lists the 9 surfaces, §Canonical References gives line anchors)
    - .planning/REQUIREMENTS.md (TAX-01 + Out of Scope row 1)
    - .planning/ROADMAP.md lines 185-194 (Phase 49 goal + 4 success criteria)
    - src/db/schema.ts lines 81-167 (watches table — styleTags), 172-214 (userPreferences — preferredStyles/dislikedStyles), 334-405 (watches_catalog — primary_archetype + styleTags)
    - src/lib/taste/vocab.ts (full file — PRIMARY_ARCHETYPES const + validateAndCleanTaste)
    - src/lib/archetype-config.ts (identity copy used by ArchetypeChips)
    - src/components/search/GenreChips.tsx (entire file)
    - src/components/search/ArchetypeChips.tsx (entire file)
    - src/components/search/StyleChips.tsx (entire file)
    - src/components/search/FilterDrawer.tsx (hosts BOTH GenreChips and ArchetypeChips — confirm coexistence at lines ~11, ~86)
    - src/data/catalog.ts (CatalogSearchFilters interface lines ~283-286; archetype-wins-over-genre tiebreaker lines ~444-450)
    - src/data/browse.ts (getBrowseGenreCounts lines ~82-105)
    - src/app/explore/genres/page.tsx (entire file — index page consuming primary_archetype)
    - src/components/explore/BrowseModule.tsx (entire file — /explore Browse module)
    - src/components/filters/FilterBar.tsx (collection-page styleTags filter lines ~96-147)
    - src/components/preferences/PreferencesClient.tsx (preferredStyles/dislikedStyles toggle lines ~109-111)
    - src/components/watch/WatchCard.tsx (styleTags[0..2] display lines ~93-105)
    - src/components/profile/ProfileWatchCard.tsx (roleTags?.[0] ?? styleTags?.[0] fallback line ~39)
    - src/components/profile/InsightsTabContent.tsx (styleTags aggregation line ~168)
    - src/components/watch/WatchForm.tsx (styleTags assignment lines ~83, ~135, ~270)
    - src/components/watch/AddWatchFlow.tsx (styleTags propagation lines ~654, ~688)
    - src/lib/similarity.ts (styleTags weight lines ~14, ~170; preferredStyles/dislikedStyles adjustment lines ~222-228; primaryArchetype categorical match — locate by grep)
  </read_first>
  <action>
    Create `.planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md` and write sections 1-3 of the mandatory 9-section skeleton from 49-CONTEXT.md D-05.

    The file should open with a small YAML-or-prose header (phase/date/author) and then:

    Section 1 — `## Domain`: 2-4 short paragraphs restating what the spike decides. State the primary question (genre↔style: consolidate / remove genre / remove style / keep both) AND the sub-question (genre↔archetype: GenreChips and ArchetypeChips both filter `watches_catalog.primary_archetype`). Reference TAX-01 explicitly. State the hard guardrail from D-08 / ROADMAP SC#4: no implementation in this phase unless a path is cheap AND strongly favored — and even then, via a new requirement add, not direct execution.

    Section 2 — `## Consumer Map`: a markdown table with columns `File | Line | Field used | What it does | UI label`. ONE row per consumer. The table MUST include at minimum these 9 surfaces from D-01 (more rows fine; fewer is a failure):
      1. `/search` FilterDrawer hosting GenreChips
      2. `/search` FilterDrawer hosting ArchetypeChips
      3. `/search` FilterDrawer hosting StyleChips
      4. `searchCatalogWatches({ filters: { genre, archetype, style[] } })` in src/data/catalog.ts (archetype-wins tiebreaker called out)
      5. Similarity engine — styleTags weight (~0.20) AND primaryArchetype categorical match in src/lib/similarity.ts
      6. /explore Browse module (BrowseModule.tsx + browse.ts)
      7. /explore/genres index page (page.tsx + getBrowseGenreCounts)
      8. Watch cards displaying styleTags[0..2] — WatchCard.tsx AND ProfileWatchCard.tsx (one row each or one combined row with both file refs)
      9. /collection FilterBar styleTags multi-select
      10. Preferences UI — preferredStyles, dislikedStyles
      11. Profile insights — styleTags aggregation
      12. Add-watch / WatchForm + AddWatchFlow — styleTags write paths
    For each row, the "What it does" cell is a sentence (not a phrase): "Reads `primary_archetype` and renders a 10-button toggle group; selecting a chip pushes `?archetype=X` to the URL." The "UI label" cell is what the user sees ("Genre", "Style", "Archetype: Genre Crosser", "—" if not user-visible).

    Section 3 — `## Overlap & Divergence Matrix`: structured prose + a small table. Two top-level subsections:
      - `### genre ↔ style overlap` — for each axis they could overlap on (formality, sportiness, use-case), state whether both fields express it, only one, or neither. Cite specific catalog values where possible (e.g. "both have a `dive` value: archetype's enum AND style_tags often contains `dive`").
      - `### genre ↔ archetype redundancy` — call out that GenreChips and ArchetypeChips filter the SAME column (`primary_archetype`) with the SAME 10 values from `PRIMARY_ARCHETYPES`. Quote the archetype-wins-over-genre tiebreaker in src/data/catalog.ts:444-450. Note that the two chip surfaces are visually identical because both consume the unified `Chip` primitive (Phase 48 D-07/D-08).
    Then a compact table `Axis | genre says | style says | overlap?` listing 4-6 concrete axes (formality, sportiness, era affinity, dial complexity, use-case, design motif).

    Per D-04: this file lives at `.planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md` (NOT under `.planning/research/`).

    Reserve sections 4-9 as `## Live-Catalog Evidence` / `## Options` / `## Decision Matrix` / `## Recommendation` / `## Cost Estimate per Option` / `## Ship-Now Eligibility Check` headers with a single-line `_To be filled by Plan 02 / Plan 03._` placeholder under each, so the document is wellformed end-to-end after Plan 01 alone.

    Per D-08 HARD GUARDRAIL: do NOT touch any source file. The ONLY file this task creates is 49-SPIKE.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "^## Domain" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "^## Consumer Map" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "^## Overlap & Divergence Matrix" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && [ "$(grep -cE 'src/[a-zA-Z_/.-]+\.ts(x)?:[0-9]+' .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md)" -ge 9 ] \
      && grep -q "TAX-01" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "primary_archetype" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "style_tags" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -q "PRIMARY_ARCHETYPES" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md \
      && grep -qE "GenreChips.*ArchetypeChips|ArchetypeChips.*GenreChips" .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md</automated>
  </verify>
  <acceptance_criteria>
    - 49-SPIKE.md exists at .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md
    - Section `## Domain` is present and explicitly restates BOTH the genre↔style question (TAX-01) AND the genre↔archetype sub-question (D-02/D-03)
    - Section `## Domain` cites D-08 / ROADMAP SC#4 hard guardrail (no implementation this phase)
    - Section `## Consumer Map` is a markdown table with columns `File | Line | Field used | What it does | UI label`
    - Consumer Map contains at least 9 distinct rows; together they cover (a) FilterDrawer × Genre/Archetype/Style, (b) searchCatalogWatches DAL, (c) similarity engine (both styleTags weight AND primaryArchetype match), (d) /explore Browse module, (e) /explore/genres index, (f) watch card display (WatchCard or ProfileWatchCard), (g) /collection FilterBar, (h) Preferences preferred/dislikedStyles, (i) profile InsightsTabContent, (j) WatchForm/AddWatchFlow write paths
    - At least 9 rows cite a real `src/.../file.ts(x):<line>` location grep-matchable against the codebase
    - Section `## Overlap & Divergence Matrix` is present and has two subsections — one for genre↔style overlap, one for genre↔archetype redundancy — and includes an explicit reference to the archetype-wins-over-genre tiebreaker (src/data/catalog.ts:444-450 region)
    - Sections 4-9 exist as empty headers with `_To be filled by Plan 02 / Plan 03._` placeholders so the doc is wellformed end-to-end
    - No file outside `.planning/phases/49-genre-vs-style-taxonomy-spike/` is modified (D-08 hard guardrail)
  </acceptance_criteria>
  <done>49-SPIKE.md exists with sections 1-3 populated; Consumer Map covers all 9 D-01 surfaces with file:line citations; Overlap & Divergence Matrix names both the genre↔style and genre↔archetype redundancies; sections 4-9 placeholders are in place.</done>
</task>

</tasks>

<threat_model>
**Phase scope:** documentation-only. This plan creates ONE markdown file (`49-SPIKE.md`) under `.planning/`. No source code, no schema, no migrations, no endpoints, no RLS changes.

**STRIDE assessment:**
- No new attack surface — markdown file is not executable and is read by humans, not by the app
- No SQL executed in this plan (Plan 02 runs the read-only SELECT queries; this plan only embeds file references)
- No secrets introduced; no credentials, no API keys, no PII
- No new user input paths, no auth flows, no data exfiltration paths

**Conclusion:** No HIGH or MEDIUM threats. No blockers under ASVS L1.
</threat_model>

<verification>
- `grep -rE 'src/[a-zA-Z_/.-]+\.ts(x)?:[0-9]+' 49-SPIKE.md | wc -l` ≥ 9 (real citations exist)
- Every file path in the Consumer Map column resolves to a file in the current repo: `awk` extract column 1, `xargs ls` shows no missing files
- Section markers `## Domain`, `## Consumer Map`, `## Overlap & Divergence Matrix` all present in the file
- `git status --porcelain | grep -v '\.planning/' | wc -l` returns 0 (no source files touched)
</verification>

<success_criteria>
49-SPIKE.md is created with sections 1-3 fully populated and sections 4-9 stubbed. The Consumer Map contains at least 9 rows covering every D-01 surface with real file:line citations. The Overlap & Divergence Matrix explicitly addresses both the genre↔style overlap AND the genre↔archetype redundancy. No source code is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/49-genre-vs-style-taxonomy-spike/49-01-SUMMARY.md` noting:
- Sections written (1-3) and section structure of the deliverable
- Total consumer rows captured
- Any consumer surfaces discovered beyond the 9 in D-01 (and what they touch)
- Any callsites named in D-01 that turned out to be wrong/stale (so Plan 03 can correct course)
</output>
