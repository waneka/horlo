---
phase: 50
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "50-SPIKE.md exists at the locked path with frontmatter mirroring 49-SPIKE.md (phase, plan range, date, author, requirement)"
    - "Section 1 (Domain) restates what the spike decides — keep-separate vs merge for /catalog/[catalogId] and /watch/[id] — and names the hard guardrail (D-GUARD-01)"
    - "Section 2 (Audience Matrix) presents the viewer-state × ref-identity table per D-AUDIENCE-01, NOT the ROADMAP SC#1 labels — anonymous-visitor cell is flagged 'not reachable today (auth-gated)'"
    - "Section 3 (Route Reality Today) describes what each route does plus the entry-point map (12 /watch/[id] sites + 7 /catalog/[catalogId] sites verbatim from RESEARCH)"
    - "Every code claim cites file:line so future readers can grep-verify (src/app/catalog/[catalogId]/page.tsx:LINE, src/app/watch/[id]/page.tsx:LINE, src/data/watches.ts:193)"
    - "Phase 48 BUG-01 is named explicitly in §3 as the maintenance-tax evidence for the in-route framing flip"
  artifacts:
    - path: ".planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md"
      provides: "Frontmatter + sections 1-3 of the spike deliverable; scaffold for plans 02-04 to append against"
      contains: "## 1. Domain, ## 2. Audience Matrix, ## 3. Route Reality Today"
      min_lines: 120
  key_links:
    - from: "50-SPIKE.md §2 Audience Matrix rows"
      to: "5 viewer-state cells (owner, non-owner-with-collection, non-owner-empty-collection, wishlist-holder, sold-this) + anonymous-visitor cell flagged forward-compat"
      via: "viewer-state axis from D-AUDIENCE-01"
      pattern: "owner|non-owner|wishlist-holder|sold-this|anonymous"
    - from: "50-SPIKE.md §3 Route Reality Today entry-point map"
      to: "12 /watch/[id] entry-point files + 7 /catalog/[catalogId] entry-point files"
      via: "RESEARCH.md canonical entry-point list (lines 112-113)"
      pattern: "src/components/(search|explore|profile|home|watch|insights)/.*\\.tsx:\\d+"
---

<objective>
Open the spike document `.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md` and write sections 1-3: Domain (restate what the spike decides), Audience Matrix (viewer-state × ref-identity per D-AUDIENCE-01), Route Reality Today (what each route does + entry-point map).

Purpose: Establish the framing the rest of the spike sits on. Plan 02 (Variants A-E + Cost), Plan 03 (v7.0 Lens + Decision Matrix), and Plan 04 (Recommendation + Ship-Now) all reference §2's viewer-state cells and §3's entry-point counts. Get the framing right here and the downstream sections fall into place.

Output: `50-SPIKE.md` populated with frontmatter + three labeled sections (§1 Domain, §2 Audience Matrix, §3 Route Reality Today). Every code claim grep-verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-watch-detail-architecture-spike/50-CONTEXT.md
@.planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md
@.planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 50-SPIKE.md with frontmatter + write §1 Domain + §2 Audience Matrix (viewer-state × ref-identity)</name>
  <files>.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md</files>
  <read_first>
    - .planning/phases/50-watch-detail-architecture-spike/50-CONTEXT.md (D-AUDIENCE-01, D-SKEL-01/02, D-GUARD-01; the locked decisions this section implements)
    - .planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md (§ User Constraints / Locked Decisions for the audience-axis re-framing rationale; § Common Pitfalls Pitfall 1 for the framing-drift warning)
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md (lines 1-25 for frontmatter + §Domain shape and voice to mirror)
    - src/app/catalog/[catalogId]/page.tsx (read lines 1-120 + lines 282-308 — find `findViewerWatchByCatalogId` definition and the D-08 "You own this" framing flip site; cite both)
    - src/app/watch/[id]/page.tsx (read lines 1-80 — confirm `getWatchByIdForViewer` consumption and the `framing: isOwner ? 'same-user' : 'cross-user'` dispatch at line ~58)
    - src/data/watches.ts (read lines 180-235 — confirm `getWatchByIdForViewer` signature + two-layer privacy gate referenced by §2 cells)
  </read_first>
  <action>
Create `.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md` from scratch using the Write tool. Begin with frontmatter mirroring `49-SPIKE.md` lines 1-7 — fields: `phase: 50-watch-detail-architecture-spike`, `plan: 01 (§1-3); plans 02-04 complete the rest`, `date: 2026-05-20`, `author: executor-agent (claude-sonnet-4-6)`, `requirement: ARCH-01`.

Then write three sections in order:

**§1 Domain (heading `## 1. Domain`)** — restate that the spike answers ARCH-01: should `/catalog/[catalogId]` and `/watch/[id]` remain separate or be merged? State the five variant breadth (A keep-separate, B URL canonicalization, C unified `/w/[ref]`, D catalog-absorbs-watch, E watch-absorbs-catalog) per D-VARIANTS-01 as a forward pointer to §4. Name the hard guardrail (D-GUARD-01): no implementation in Phase 50; cheap+strongly-favored verdict triggers a `/gsd-phase --insert` Phase 50.1 wave, mirror of Phase 49 → 49.1. Cite ROADMAP SC#4 verbatim ("No consolidation or removal implementation is shipped in this phase unless...").

**§2 Audience Matrix (heading `## 2. Audience Matrix`)** — open with a one-paragraph re-framing note per D-AUDIENCE-01: the ROADMAP SC#1 audience labels ("owner / wishlist-holder / anonymous visitor / cross-user browse") mix two axes — viewer-state and ref-identity — and this section re-frames them into a 2D matrix per the locked decision. Then render a table with rows = viewer-state (`owner`, `non-owner-with-collection`, `non-owner-empty-collection`, `wishlist-holder`, `sold-this`, `anonymous-visitor`) and columns = ref-identity (`per-user (watches.id)`, `catalog (watches_catalog.id)`). Each cell describes: what UI shape the viewer sees today + which code path serves them (cite `getWatchByIdForViewer` at `src/data/watches.ts:193` for the per-user column same-user/cross-user branch; cite `findViewerWatchByCatalogId` at `src/app/catalog/[catalogId]/page.tsx:282` for the catalog column owner-detection; cite the D-08 self-via-cross-user framing-flip construction at `src/app/catalog/[catalogId]/page.tsx:107-115`). The `anonymous-visitor` row is a single merged cell labeled "not reachable today (auth-gated) — forward-compat placeholder for v6.0 social / v7.0 photo readers." After the table, write a one-paragraph observation per D-AUDIENCE-01: this corrects the implicit ROADMAP assumption that `/watch/[id]` is owner-only — it already supports cross-user via `getWatchByIdForViewer` (cite the framing dispatch in `src/app/watch/[id]/page.tsx` line ~58).

Use the same declarative, evidence-cited voice as `49-SPIKE.md` §Domain + §Consumer Map. Every code reference uses `src/path/to/file.tsx:LINE` format. Do NOT write §3 in this task — Task 2 owns it.
  </action>
  <verify>
    <automated>test -f .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -cE '^## (1\. Domain|2\. Audience Matrix)' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 2) ? 0 : 1 }' && grep -c 'D-AUDIENCE-01\|viewer-state\|ref-identity' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 >= 3) ? 0 : 1 }' && grep -E 'getWatchByIdForViewer|findViewerWatchByCatalogId' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -E 'anonymous-visitor.*not reachable|not reachable today' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md</automated>
  </verify>
  <done>
    - 50-SPIKE.md exists with frontmatter mirroring 49-SPIKE.md shape
    - §1 Domain restates the keep-vs-merge question, names all 5 variants A-E, cites D-GUARD-01 hard guardrail, quotes ROADMAP SC#4
    - §2 Audience Matrix uses viewer-state (rows) × ref-identity (columns), NOT ROADMAP SC#1 labels
    - anonymous-visitor cell is flagged "not reachable today (auth-gated)" — forward-compat
    - Code citations resolve to real file:line (getWatchByIdForViewer, findViewerWatchByCatalogId)
    - One-paragraph re-framing note explicitly corrects the ROADMAP "watch is owner-only" assumption
  </done>
</task>

<task type="auto">
  <name>Task 2: Write §3 Route Reality Today (what each route does + entry-point map)</name>
  <files>.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md</files>
  <read_first>
    - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md (current file state — confirm §1 + §2 are present from Task 1 before appending)
    - .planning/phases/50-watch-detail-architecture-spike/50-CONTEXT.md (lines 97-113 — canonical references + entry-point raw lists for /catalog and /watch routes)
    - .planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md (§Variants A-E Evidence Base / Variant A subsection for the file:line + entry-point counts; § Open Questions Q4 for the "Phase 48 BUG-01 cited in both §3 and §4" recommendation)
    - .planning/phases/48-user-facing-bug-fixes/48-CONTEXT.md (BUG-01 description — the wishlist mislabel that gave rise to ARCH-01; cite by name as evidence of the maintenance tax)
    - src/app/catalog/[catalogId]/page.tsx (confirm full line count = 308 server-only; D-08 framing flip lines 107-115; CatalogPageActions 3-CTA block; OtherOwnersRoster catalog-only by UI-SPEC)
    - src/app/watch/[id]/page.tsx (confirm 132 lines; getWatchByIdForViewer dispatch)
    - src/components/watch/WatchDetail.tsx (read lines 1-40 — confirm 'use client' declaration; viewerCanEdit prop; the owner-only Server Action surface)
  </read_first>
  <action>
APPEND `## 3. Route Reality Today` to the existing `50-SPIKE.md` (do NOT re-write §1 or §2; use Edit tool to append after the §2 section).

The section has three subsections:

**§3.1 What each route does today.** Two paragraphs:
1. `/catalog/[catalogId]` — server-only Next.js 16 App Router page at `src/app/catalog/[catalogId]/page.tsx` (308 lines). Renders catalog-spec data + `ReferenceIdentityCard`, `SameFamilyRail`, `LineageRail`, `CollectionFitCard` (shared), `OtherOwnersRoster` (catalog-only by UI-SPEC), and `CatalogPageActions` 3-CTA block. When viewer owns the catalog ref (detected via `findViewerWatchByCatalogId` at lines 282-308), the page flips to a `VerdictBundleSelfOwned` framing (D-08, lines 107-115) — the "You own this" inline callout introduced by Phase 20.1.
2. `/watch/[id]` — server page (132 lines) at `src/app/watch/[id]/page.tsx` + client island `src/components/watch/WatchDetail.tsx` (462 lines, `'use client'`). Uses `getWatchByIdForViewer` which serves BOTH same-user AND cross-user framings (line ~58: `framing: isOwner ? 'same-user' : 'cross-user'`). The client island gates the owner-only writable surface (edit, delete, markAsWorn, flag-deal Server Actions) on `viewerCanEdit`.

**§3.2 Entry-point map.** Two tables (one per route) sourced verbatim from CONTEXT.md "Entry-point map (raw)" / RESEARCH.md Variant C "Files touched" enumeration. Headers: File | Line | Link target.

- `/catalog/[catalogId]` entry points — 7 sites: `src/components/search/WatchSearchRow.tsx:31`, `src/components/explore/DiscoveryWatchCard.tsx:30`, `src/components/explore/PathCard.tsx:97,134,143`, `src/app/explore/lists/[id]/page.tsx:91,110`.
- `/watch/[id]` entry points — 12 sites: `src/components/watch/WatchCard.tsx:35`, `src/components/profile/ProfileWatchCard.tsx:59`, `src/components/home/RecommendationCard.tsx:22`, `src/components/home/ActivityRow.tsx:51`, `src/components/home/SleepingBeautyCard.tsx:33`, `src/components/home/MostWornThisMonthCard.tsx:21`, `src/components/home/WywtSlide.tsx:78`, `src/components/profile/StatsTabContent.tsx:62`, `src/components/profile/NoteRow.tsx:62`, `src/components/insights/CollectionFitCard.tsx:73`, `src/components/insights/SleepingBeautiesSection.tsx:44`, `src/components/insights/GoodDealsSection.tsx:48`.

Note after the tables: discovery surfaces (search, Explore) feed `/catalog/[catalogId]`; ownership surfaces (collection, profile, home recs) feed `/watch/[id]`. This split correlates with viewer-state in §2 but is not enforced by the routes themselves.

**§3.3 De-facto truncation and the BUG-01 maintenance tax.** Two short paragraphs:
1. The "truncated" render observation (user-raised, REQUIREMENTS line 48): `/catalog/[catalogId]` is by-design slimmer (cross-user spec-only view). The truncation is not a bug; it is the structural asymmetry that feeds ARCH-01.
2. Phase 48 BUG-01 evidence: the wishlist watch shown via `/catalog/[catalogId]` was mislabeled "You own this" because the D-08 framing flip didn't scope to `status='owned'`. The fix at `src/app/catalog/[catalogId]/page.tsx:296` (the `eq(watchesTable.status, 'owned')` clause inside `findViewerWatchByCatalogId`) is the maintenance tax of keeping the in-route framing flip. Any merge variant that retires that flip (Variant B, C, D, E) retires the bug class.

Add a section separator (`---`) after §3 to mark the end of Plan 01's territory and signal Plan 02's append point.
  </action>
  <verify>
    <automated>grep -cE '^## 3\. Route Reality Today' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 1) ? 0 : 1 }' && grep -c 'src/components/search/WatchSearchRow.tsx:31\|src/components/watch/WatchCard.tsx:35\|src/components/profile/ProfileWatchCard.tsx:59' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 >= 3) ? 0 : 1 }' && grep -E 'BUG-01|Phase 48' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -E 'findViewerWatchByCatalogId|status.*owned' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -cE '^## ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 >= 3) ? 0 : 1 }'</automated>
  </verify>
  <done>
    - §3 Route Reality Today exists with three subsections (what each route does, entry-point map, BUG-01 maintenance tax)
    - 12 /watch/[id] entry points + 7 /catalog/[catalogId] entry points enumerated by file:line
    - Phase 48 BUG-01 named explicitly as evidence of the in-route framing-flip maintenance tax
    - Truncation framed as by-design asymmetry per REQUIREMENTS line 48, NOT as a bug
    - Section separator `---` follows §3 to signal Plan 02's append point
  </done>
</task>

</tasks>

<threat_model>
- Decision-only spike; no executable code shipped in this phase.
- No new auth surface, no new data path, no new external input introduced.
- §3 cites `proxy.ts` only by absence — the spike does not propose any proxy change. The proxy.ts router-cache landmine is the Variant B subsection's concern, owned by Plan 02.
- §2's privacy-gate description names `getWatchByIdForViewer`'s two-layer RLS+WHERE pattern (CLAUDE.md "Personal first" constraint) as preserved evidence — no variant scored in this spike weakens it.
- D-GUARD-01 enforcement: this plan writes ONLY to `.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md`. No code files modified.
</threat_model>

<verification>
After both tasks complete:

```bash
# 50-SPIKE.md exists at locked path
test -f .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md

# Has frontmatter
head -10 .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | grep -E '^phase:|^plan:|^requirement: ARCH-01'

# Sections 1, 2, 3 all present
grep -cE '^## (1\. Domain|2\. Audience Matrix|3\. Route Reality Today)' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: 3

# Audience axis re-framing applied (NOT the ROADMAP labels)
grep -cE 'viewer-state.*ref-identity|D-AUDIENCE-01' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: >=2

# Entry-point counts match RESEARCH (12 + 7 = 19 unique file:line citations)
grep -cE 'src/components/(search|explore|profile|home|watch|insights)/.*\.tsx:[0-9]+' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: >=15 (some lines have multiple sites e.g. PathCard.tsx:97,134,143)

# D-GUARD-01 enforcement: only the SPIKE.md file modified in this plan's commit
git diff --name-only HEAD~1...HEAD | grep -v '^\.planning/'
# Expected: empty
```
</verification>

<success_criteria>
- `50-SPIKE.md` exists at `.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md`
- Frontmatter present (phase, plan range, date, author, requirement: ARCH-01)
- §1 Domain restates the keep-vs-merge question + names all 5 variants A-E + cites D-GUARD-01 + quotes ROADMAP SC#4
- §2 Audience Matrix uses viewer-state × ref-identity per D-AUDIENCE-01 (NOT ROADMAP SC#1 labels)
- anonymous-visitor cell flagged "not reachable today (auth-gated)" — forward-compat
- §3 enumerates the 12 /watch + 7 /catalog entry points with file:line citations
- Phase 48 BUG-01 named in §3 as maintenance-tax evidence
- Every code claim is grep-verifiable via file:line citation
- No implementation files modified outside `.planning/phases/50-*/`
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/50-watch-detail-architecture-spike/50-01-SUMMARY.md` documenting: sections written, file:line citations resolved, any deviations from the plan, and the section anchors Plan 02 should append against.
</output>
