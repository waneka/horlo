---
phase: 50
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "§4 Variants A-E describes all 5 variants per D-VARIANTS-01 — each with subsections for routing model, per-user data shape, entry-point disruption, brief summary"
    - "§4 Variant B subsection explicitly calls out the proxy.ts router-cache poisoning landmine and names `feedback_proxy_router_cache_poisoning` memory — canonicalization MUST happen at the page level via next/navigation redirect()"
    - "§4 Variant B subsection names Phase 48 BUG-01 as the maintenance-tax bug class that Variant B retires"
    - "§7 Cost Estimate per Variant renders as a side-by-side comparable table (5 rows × the cost dimensions from RESEARCH Per-Variant File Impact Summary) — files touched, entry-point rewrites, migrations, DAL changes, test surface"
    - "§7 carries forward the watches_catalog NOT-wipeable note for any variant that imagines schema work (per project_db_wipeable_2026_05_09)"
    - "Every per-variant file claim cites RESEARCH evidence by file:line, NOT free-form invented numbers"
  artifacts:
    - path: ".planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md"
      provides: "Sections 4 (Variants A-E) + 7 (Cost Estimate) appended after §3 — comparable variant evidence base for Plans 03/04"
      contains: "## 4. Variants A-E, ### A. Keep separate, ### B. URL canonicalization, ### C. Single unified route /w/[ref], ### D. Absorb /watch into /catalog, ### E. Absorb /catalog into /watch, ## 7. Cost Estimate per Variant"
      min_lines: 200
  key_links:
    - from: "50-SPIKE.md §4 Variant B subsection"
      to: "feedback_proxy_router_cache_poisoning memory + next/navigation redirect() at page level"
      via: "explicit landmine callout per RESEARCH Pitfall 2"
      pattern: "proxy\\.ts|router.cache|next/navigation.*redirect"
    - from: "50-SPIKE.md §7 Cost Estimate table"
      to: "RESEARCH Per-Variant File Impact Summary table (lines 277-285)"
      via: "verbatim row copy with planner-chosen voice"
      pattern: "Variant [A-E].*\\| [0-9]"
---

<objective>
Append sections 4 (Variants A-E) and 7 (Cost Estimate per Variant) to `50-SPIKE.md`. §4 describes each merge variant's routing model, per-user data shape, entry-point disruption, and one-paragraph summary; §7 renders a side-by-side cost table comparable across all 5 variants. Sections 5 (v7.0 Lens) and 6 (Decision Matrix) are owned by Plan 03, which depends on this plan's variant summaries.

Purpose: §4 is the substrate Plan 03's matrix scores on. §7 is intentionally separated from §4 per D-SKEL-02 so the cost numbers read side-by-side. The RESEARCH document provides the per-variant evidence base verbatim — this plan transcribes it into spike voice, NOT re-derives it.

Output: `50-SPIKE.md` grown to include §4 (5 variant subsections) and §7 (one comparison table). Plan 03 appends §5/§6 between them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-watch-detail-architecture-spike/50-CONTEXT.md
@.planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md
@.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
@.planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append §4 Variants A-E — five subsections (A keep-separate, B URL-canonical, C unified /w/[ref], D catalog-absorbs-watch, E watch-absorbs-catalog)</name>
  <files>.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md</files>
  <read_first>
    - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md (confirm §1-3 are present from Plan 01; Task 2 of Plan 01 ended with `---` separator — append after it)
    - .planning/phases/50-watch-detail-architecture-spike/50-CONTEXT.md (D-VARIANTS-01 variant definitions; D-VARIANTS-02 the 7 criteria forward-pointer to §6; the proxy.ts landmine note in `<canonical_refs>` MEMORY block)
    - .planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md (§Variants A-E Evidence Base — full read; this section's contents map directly to §4 subsections. Pay particular attention to the LANDMINE callout in Variant B and the Forcing-function notes in Variants D/E)
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md (lines 281-343 — §Options shape; this is the voice template for §4)
    - src/app/catalog/[catalogId]/page.tsx (re-confirm lines 282-308 findViewerWatchByCatalogId; lines 107-115 D-08 framing-flip site for Variant B's redirect target)
    - node_modules/next/dist/docs/02-app/03-deep-dive/03-redirecting.md OR equivalent (AGENTS.md mandate: confirm next/navigation `redirect()` signature is the server-side canonical-redirect API in Next.js 16 before claiming it; if doc path is different, grep `node_modules/next/dist/docs -name "*.md" | xargs grep -l "redirect"` to find the right reference)
  </read_first>
  <action>
APPEND `## 4. Variants A-E` heading after the `---` separator left by Plan 01 Task 2. Then five subsections — one per variant — each named exactly as below:

**§4.A `### A. Keep separate (status quo)`** — Routing model: two routes remain at `src/app/catalog/[catalogId]/page.tsx` (308 lines, server-only) and `src/app/watch/[id]/page.tsx` (132 lines server + 462-line client island). Per-user data shape: already converged via shared components (`ReferenceIdentityCard`, `SameFamilyRail`, `LineageRail`, `CollectionFitCard` render on both routes; `OtherOwnersRoster` catalog-only by UI-SPEC). Entry-point disruption: zero. Summary: pure no-op variant; UI-only convergence continues; the in-route framing flip (D-08) keeps its maintenance tax.

**§4.B `### B. URL canonicalization (/catalog → /watch when owned)`** — Routing model: when `findViewerWatchByCatalogId` (`src/app/catalog/[catalogId]/page.tsx:282-308`) detects ownership, the server page calls `redirect(\`/watch/${viewerOwnedRow.id}\`)` from `next/navigation` instead of constructing the inline `VerdictBundleSelfOwned` (current lines 107-115). Per-user data shape: unchanged. Entry-point disruption: zero direct rewrites — discovery surfaces still link to `/catalog/[catalogId]`, the route redirects when ownership is detected. **LANDMINE callout (verbatim, MANDATORY per RESEARCH Pitfall 2):** Variant B is ONLY safe if canonicalization happens at the page level (server component calling `next/navigation`'s `redirect()`). It is NOT safe at the `proxy.ts` layer — a 307 on an RSC prefetch poisons Next 16's Router Cache, producing 404s on soft-nav (the `/u/*` precedent recorded in MEMORY `feedback_proxy_router_cache_poisoning`). A sub-variant "canonicalize at proxy" is a structural landmine. Carry-forward signal: Phase 48 BUG-01 (status='owned' scope at line 296) is the maintenance tax for keeping the in-route framing flip; Variant B retires that tax by retiring the flip itself.

**§4.C `### C. Single unified route /w/[ref]`** — Routing model: new route `src/app/w/[ref]/page.tsx`; server-side resolution tries `getWatchByIdForViewer(user.id, ref)`, falls back to `getCatalogById(ref)`; both old URLs issue server redirects to canonical `/w/[ref]`. Per-user data shape: unified server page does framing dispatch (`same-user | cross-user | self-via-cross-user`) based on which resolver hit. Entry-point disruption: ALL 19 entry points rewrite (12 `/watch` + 7 `/catalog`). Bookmarks preserved via redirects from the old route files. Cite the entry-point file:line list from §3 with a forward pointer ("see §3.2 for the full list"). Summary: cleanest single-surface composition; highest rewrite blast radius; medium reversibility.

**§4.D `### D. Absorb /watch into /catalog`** — Routing model: `/catalog/[catalogId]/page.tsx` becomes the only watch-detail route. When viewer owns the catalog ref, the page layers in per-user data (currently in `/watch/[id]`) including the `WatchDetail` client island. Per-user URLs retire; `/watch/[id]` redirects to `/catalog/[catalogId]/${watch.catalogId}` (resolved via `watches.id → catalogId` lookup). Per-user data shape: calls `findViewerWatchByCatalogId` (already exists at lines 282-308) plus `getWatchByIdForViewer` to pull full `Watch` (per-user fields: strapType, notes, isFlaggedDeal, acquisitionDate, sortOrder, condition, boxPapers per `src/db/schema.ts:130,140,153`). Entry-point disruption: ALL 12 `/watch/[id]` entry points rewrite to `/catalog/[catalogId]/${watch.catalogId}` — feasible because `Watch.catalogId` is NOT NULL FK since Phase 38 D-06 (`src/db/schema.ts:146`). Forcing-function note (per RESEARCH): `OtherOwnersRoster` (catalog-only by UI-SPEC) needs a "do not show roster to owner" check — or the UI-SPEC policy changes to "owners see the roster too." Summary: clean owner-detection layering on one route; entry-point rewrite cost is 12 sites.

**§4.E `### E. Absorb /catalog into /watch`** — Routing model: `/watch/[id]/page.tsx` becomes the only watch-detail route. The route grows a "no per-user row yet" mode when the viewer hits an `id` that resolves only as a catalog id. `/catalog/[catalogId]` redirects to `/watch/[id]` via catalog-keyed lookup. Per-user data shape: `getWatchByIdForViewer` already cross-user-aware (returns `{ watch, isOwner }`); "no per-user row yet" mode falls back to `getCatalogById(catalogId)`. Forcing-function note (per RESEARCH): `watches.id` and `watches_catalog.id` are distinct UUID spaces — the route must accept either and dispatch. Existing `catalogEntryToSimilarityInput` shim in `src/lib/verdict/shims.ts` is the precedent for synthesizing a `Watch`-shaped object from a `CatalogEntry`. Entry-point disruption: 7 `/catalog/[catalogId]` entry points rewrite. `CatalogPageActions` (3-CTA block) becomes a conditional render on `/watch/[id]` when no per-user row exists. Summary: smaller blast radius than D (7 vs 12); UUID-dispatch fragility adds code-clarity cost.

Voice rules (mirror `49-SPIKE.md` §Options): declarative, evidence-cited, no hedging. Every variant ends with a one-sentence "Summary" line so §6 Decision Matrix can reference variants by their distinct character. Add a `---` separator after §4.E to signal Plan 03's append point for §5.
  </action>
  <verify>
    <automated>grep -cE '^### (A\. Keep separate|B\. URL canonicalization|C\. Single unified route|D\. Absorb /watch|E\. Absorb /catalog)' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 5) ? 0 : 1 }' && grep -E 'proxy\.ts|router.cache|feedback_proxy_router_cache_poisoning' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -E 'next/navigation' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -E 'BUG-01' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk 'END { exit (NR >= 2) ? 0 : 1 }' && grep -E 'OtherOwnersRoster' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -cE '^## 4\. Variants A' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 1) ? 0 : 1 }'</automated>
  </verify>
  <done>
    - §4 contains 5 subsections matching exact heading pattern (### A. through ### E.)
    - Variant B subsection cites `feedback_proxy_router_cache_poisoning` memory by name AND names `next/navigation`'s `redirect()` as the safe canonicalization API
    - Variant B subsection names Phase 48 BUG-01 as the bug class Variant B retires
    - Variant D subsection enumerates the per-user fields the catalog page would need to layer in (cites src/db/schema.ts line numbers)
    - Variant E subsection cites the existing `catalogEntryToSimilarityInput` shim as the synthesize-from-catalog precedent
    - Each variant ends with a one-sentence Summary line
    - `---` separator follows §4 to signal Plan 03 append point
  </done>
</task>

<task type="auto">
  <name>Task 2: Append §7 Cost Estimate per Variant — side-by-side comparable table covering files touched, entry-point rewrites, migrations, DAL changes, test surface</name>
  <files>.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md</files>
  <read_first>
    - .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md (confirm §4 is complete; this task appends §7 AFTER §4 — the slot for §5 and §6 between them is reserved for Plan 03)
    - .planning/phases/50-watch-detail-architecture-spike/50-RESEARCH.md (§Per-Variant File Impact Summary at lines 275-285 — this is the canonical evidence table; transcribe its row contents into spike voice. Also §Common Pitfalls Pitfall 6 — the catalog-wipeable carve-out note for the §7 prose)
    - .planning/phases/49-genre-vs-style-taxonomy-spike/49-SPIKE.md (lines 406-425 §Cost Estimate per Option — voice template; note the prose paragraph below the table covering migration sequence + carve-out impact)
  </read_first>
  <action>
APPEND `## 7. Cost Estimate per Variant` after §4's closing `---` separator. Do NOT write §5 or §6 — those slots belong to Plan 03; this section sits at §7 explicitly (per D-SKEL-02) and Plan 03 inserts §5 + §6 BETWEEN §4 and §7 by appending its content above the §7 heading.

(Implementation hint for the executor: the simplest sequencing is — Plan 02 appends `## 7. ...` immediately after §4's separator. Plan 03 then inserts §5 and §6 above the §7 heading using Edit. This works because §5 and §6 are still mandated D-SKEL-02 sections — Plan 03 owns their writing — and Plan 02 leaves a clean `## 7.` anchor for Plan 03 to splice against.)

The section has two parts:

**§7.1 Comparable cost table** — render exactly this row set (verbatim from RESEARCH §Per-Variant File Impact Summary, lines 277-284, with planner voice in the cells):

| Variant | Files touched | Entry-point rewrites | Migrations | DAL changes | Test surface |
|---------|---------------|----------------------|------------|-------------|--------------|
| **A. Keep separate** | 0 | 0 | 0 | 0 | None — no behavior change |
| **B. URL canonicalization** | 1-2 (`catalog/[catalogId]/page.tsx` + 1 test file) | 0 | 0 | 0 | Low — redirect test; assert 307 for owner viewers on `/catalog/[id]` |
| **C. Unified `/w/[ref]`** | 19+ (new route + 2 old routes + 17 entry-point files) | 19 | 0 | 0 (route-level dispatch) | High — full integration test for both old URLs → new URL + all 19 entry points |
| **D. Catalog absorbs watch** | 13-14 (`catalog/[catalogId]/page.tsx` rewrite + `watch/[id]/page.tsx` removal + 12 entry points) | 12 | 0 | 0 (reuses `findViewerWatchByCatalogId` + `getWatchByIdForViewer`) | Medium-high — owner-detection layering on catalog route; per-user data threading; `WatchDetail` island conditional |
| **E. Watch absorbs catalog** | 8-9 (`watch/[id]/page.tsx` rewrite + `catalog/[catalogId]/page.tsx` removal + 7 entry points + `OtherOwnersRoster` policy) | 7 | 0 | 0 (route-level dispatch on UUID space) | Medium — catalog-only mode test; UUID dispatch test; `OtherOwnersRoster` always-on vs always-off decision |

**§7.2 Backfill / migration impact paragraph** — write exactly:

> **None of A-E require schema changes to `watches` or `watches_catalog` in v5.2.** All merge variants are route + UI only. The catalog-wipeable carve-out (MEMORY `project_db_wipeable_2026_05_09`, updated 2026-05-19 — `watches_catalog` is NOT wipeable) only matters IF v7.0 photo schema lands as part of an implementation phase — out of scope for Phase 50 per D-GUARD-01. The v7.0 lens (§5) flags this consideration but does not pre-decide it.

(Plan 03 §5 then connects to the same carve-out from the photo-schema-cost angle; this prose in §7 establishes the carve-out reference for the spike up-front.)

Add a `---` separator after §7 to signal Plan 04's append point for §8.

After completing this task, the SPIKE.md structure should be:
- §1 Domain (Plan 01)
- §2 Audience Matrix (Plan 01)
- §3 Route Reality Today (Plan 01)
- §4 Variants A-E (Plan 02 — this task's predecessor)
- §7 Cost Estimate per Variant (Plan 02 — this task)

Plan 03 will splice §5 and §6 between §4 and §7. Plan 04 will append §8 + §9 after §7.
  </action>
  <verify>
    <automated>grep -cE '^## 7\. Cost Estimate per Variant' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 1) ? 0 : 1 }' && grep -cE '^\| \*\*[A-E]\. ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 == 5) ? 0 : 1 }' && grep -E 'project_db_wipeable_2026_05_09|2026-05-19|watches_catalog.*NOT wipeable' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -E 'D-GUARD-01' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md && grep -cE '^## ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md | awk '{ exit ($1 >= 5) ? 0 : 1 }'</automated>
  </verify>
  <done>
    - §7 Cost Estimate table renders 5 rows (one per variant) × 6 columns (Variant + 5 cost dimensions)
    - Row contents match RESEARCH §Per-Variant File Impact Summary
    - Carve-out paragraph cites `project_db_wipeable_2026_05_09` and the 2026-05-19 update date by name
    - D-GUARD-01 referenced as why v7.0 schema is out of scope
    - `---` separator follows §7 to signal Plan 04 append point
    - SPIKE.md now has 5 numbered ## sections (1, 2, 3, 4, 7) — Plan 03 will insert 5 + 6 to bring total to 7 of 9
  </done>
</task>

</tasks>

<threat_model>
- Decision-only spike; no executable code shipped in this phase.
- No new auth surface, no new data path, no new external input introduced.
- §4 Variant B's landmine callout is the security-adjacent risk-model contribution: the spike CALLS OUT that any Variant B implementation must canonicalize via next/navigation `redirect()` at the page level, NOT via `proxy.ts` (router-cache poisoning per MEMORY feedback_proxy_router_cache_poisoning). This is the spike's risk-modeling equivalent of a threat mitigation.
- Variant cost-scoring (§7 "migration cost" + "irreversibility" forwarded to §6) is the spike's risk-modeling equivalent.
- D-GUARD-01 enforcement: this plan writes ONLY to `.planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md`. No code files modified.
</threat_model>

<verification>
After both tasks complete:

```bash
# §4 has 5 variant subsections
grep -cE '^### [A-E]\.' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: 5

# §4 Variant B landmine called out
grep -E 'proxy\.ts.*router|feedback_proxy_router_cache_poisoning' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md

# §7 cost table has 5 variant rows
grep -cE '^\| \*\*[A-E]\.' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: 5

# Total ## sections so far: 5 (1, 2, 3, 4, 7) — Plan 03 brings to 7, Plan 04 brings to 9
grep -cE '^## ' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md
# Expected: 5

# Catalog-wipeable carve-out cited in cost prose
grep -E 'project_db_wipeable_2026_05_09|2026-05-19' .planning/phases/50-watch-detail-architecture-spike/50-SPIKE.md

# D-GUARD-01 enforcement: only the SPIKE.md file modified
git diff --name-only HEAD~1...HEAD | grep -v '^\.planning/'
# Expected: empty
```
</verification>

<success_criteria>
- §4 Variants A-E contains 5 named subsections; voice and depth match `49-SPIKE.md` §Options shape
- §4.B explicit proxy.ts router-cache landmine callout with memory citation
- §4.D forcing-function note for OtherOwnersRoster UI-SPEC policy
- §4.E forcing-function note for UUID-space dispatch
- §7 Cost Estimate table is comparable side-by-side across all 5 variants
- §7.2 prose cites the watches_catalog carve-out and D-GUARD-01 by name
- SPIKE.md ends with §7 followed by `---` separator
- No implementation files modified outside `.planning/phases/50-*/`
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/50-watch-detail-architecture-spike/50-02-SUMMARY.md` documenting: variant subsection structure, the landmine callout in §4.B (memory cited by name), the §7 table row count, any deviation from RESEARCH evidence, and the section anchors Plan 03 should splice §5/§6 against.
</output>
