---
phase: 50.1
slug: url-canonicalization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-20
---

# Phase 50.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `50.1-RESEARCH.md` §9 Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.x under jsdom |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run tests/app/catalog-page.test.ts` |
| **Full suite command** | `npx vitest run` |
| **Lint command (parallel gate)** | `npm run lint` |
| **Estimated runtime** | ~3–5 seconds (quick) / ~10–15 seconds (page tests) / ~30 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** `npx vitest run tests/app/catalog-page.test.ts && npm run lint`
- **After every plan wave:** `npx vitest run tests/app/`
- **Before `/gsd-verify-work`:** Full suite must be green — `npx vitest run && npm run lint && npm run build`
- **Max feedback latency:** 5 seconds (quick), 15 seconds (page suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 50.1-01-01 | 01 | 1 | ARCH-02 / SC#1 | — | Server page calls `redirect('/watch/${viewerOwnedRow.id}')` when viewer owns ref | unit | `npx vitest run tests/app/catalog-page.test.ts -t "ARCH-02.*owned.*redirect"` | ✅ existing file augmented | ⬜ pending |
| 50.1-01-02 | 01 | 1 | ARCH-02 / SC#2 | — | `findViewerWatchByCatalogId` preserves `status='owned'` filter (BUG-01 invariant); return shape simplified | unit | `npx vitest run tests/app/catalog-page.test.ts -t "predicate guard"` | ✅ existing WR-01 test | ⬜ pending |
| 50.1-01-03 | 01 | 1 | ARCH-02 / SC#3 (BUG-01) | — | Wishlist / sold / grail rows do NOT trigger redirect; cross-user verdict path still fires | unit | `npx vitest run tests/app/catalog-page.test.ts -t "BUG-01"` | ✅ 3 existing tests | ⬜ pending |
| 50.1-01-04 | 01 | 1 | ARCH-02 / SC#5 | — | `TODO: revisit for Variant C in v7.0` comment present at OtherOwnersRoster anchor | static (fs.readFileSync) | `npx vitest run tests/app/catalog-page.test.ts -t "SC#5.*TODO"` | ❌ Wave 0 — add assertion | ⬜ pending |
| 50.1-02-01 | 02 | 1 | ARCH-02 / SC#4 | — | `src/proxy.ts` contains no `/catalog/` redirect logic | static grep / vitest | `npx vitest run tests/proxy.test.ts -t "no catalog redirect"` OR `! grep -E "/catalog/" src/proxy.ts` | ❌ Wave 0 — add test | ⬜ pending |
| 50.1-03-01 | 03 | 2 | ARCH-02 / SC#7 | — | REQUIREMENTS.md reflects ARCH-01=Complete, ARCH-02 added In Progress → Complete | manual (phase-close) | `grep -E "^\| ARCH-02 \|.*Complete" .planning/REQUIREMENTS.md` | ✅ file exists, lines edited | ⬜ pending |
| (full file) | 01 | 1 | SC#6 (regression guard) | — | All 12 existing catalog-page tests stay green | unit (full file) | `npx vitest run tests/app/catalog-page.test.ts` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] **`tests/app/catalog-page.test.ts`** — augment existing file:
  - Extend `vi.mock('next/navigation', ...)` to mock `redirect` alongside `notFound` (use `vi.hoisted(() => vi.fn(...))` pattern from existing line 18)
  - Rewrite the two D-08 "You own this callout" tests at lines 175–203 → ARCH-02 redirect-assertion tests (assert `mockRedirect` called with correct URL; assert page promise rejects with `'NEXT_REDIRECT'`)
  - Add a `fs.readFileSync` static assertion for SC#5's TODO comment (mirrors `tests/app/insights-retirement.test.tsx:47-52` pattern)
- [ ] **`tests/proxy.test.ts`** (preferred) OR new `tests/proxy-no-catalog-redirect.test.ts` — add 3-line assertion: `const content = fs.readFileSync('src/proxy.ts', 'utf8'); expect(content).not.toContain('/catalog/')`
- [ ] **No framework install needed** — vitest 2.x already configured under jsdom
- [ ] **No new dev dependencies** — pure additions to existing test files

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| REQUIREMENTS.md traceability update | SC#7 | Markdown table edit — verified by phase-close `/gsd-verify-work`, not by unit test | Read `.planning/REQUIREMENTS.md`; confirm ARCH-01 row marked `[x] Complete`; confirm ARCH-02 row exists with status `[x] Complete` post-verification |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (SC#4 proxy guard, SC#5 TODO assertion)
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter after planner produces task list

**Approval:** pending
