---
id: 50.1-02
phase: 50.1
plan_num: 2
plan: 02
name: Proxy regression guard — assert src/proxy.ts has no /catalog/ redirect logic
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [ARCH-02]
files_modified:
  - tests/proxy.test.ts
threat_model_complete: true

must_haves:
  truths:
    - "An automated vitest assertion exists that fails CI if any future commit introduces `/catalog/` redirect logic into `src/proxy.ts` (SC#4 regression guard)"
    - "The assertion uses `fs.readFileSync('src/proxy.ts', 'utf8')` and checks that the substring `/catalog/` is absent from the proxy module (the canonical idiom from `tests/app/insights-retirement.test.tsx:47-52`)"
  artifacts:
    - path: "tests/proxy.test.ts"
      provides: "vitest regression guard for SC#4 — proves the catalog redirect is page-layer-only and prevents Router Cache poisoning regression"
      contains: "SC#4"
  key_links:
    - from: "tests/proxy.test.ts new SC#4 describe/it block"
      to: "src/proxy.ts content (read via fs.readFileSync)"
      via: "expect(content).not.toContain('/catalog/')"
      pattern: "not\\.toContain\\('/catalog/'\\)"
---

<objective>
Add an automated regression guard that fails the test suite if any future commit introduces `/catalog/` redirect logic into `src/proxy.ts`. The memory `feedback_proxy_router_cache_poisoning` documents that a `NextResponse.redirect` (any 3xx) issued from `proxy.ts` on an RSC prefetch poisons Next 16's in-memory Router Cache → 404 on soft-nav. Variant B is structurally safe ONLY at the page layer; the SC#4 deliverable is this regression test, NOT a code change to `src/proxy.ts` (which is already clean per RESEARCH §8 Risk 1, verified by grep).

Purpose: Prevent silent regression. The current `src/proxy.ts` (37 lines) contains exactly one `NextResponse.redirect` call — for the unauthenticated-non-public-non-profile login path. No `/catalog/` handling exists. This guard locks that invariant.

Output: Augmented `tests/proxy.test.ts` with a new SC#4 describe block containing a single static-grep assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50.1-url-canonicalization/50.1-RESEARCH.md
@.planning/phases/50.1-url-canonicalization/50.1-VALIDATION.md

# Files being modified — executor MUST read in full before editing
@tests/proxy.test.ts
@src/proxy.ts

# Canonical analog — the `fs.readFileSync` static-assertion idiom this plan replicates
@tests/app/insights-retirement.test.tsx

<interfaces>
<!-- The proxy module's current state — confirmed clean per RESEARCH §8 Risk 1. -->

From `src/proxy.ts` (37 lines, verified):
```typescript
import { NextResponse, type NextRequest } from 'next/server'
import { updateSession } from '@/lib/supabase/proxy'
import { isPublicPath, isProfilePath } from '@/lib/constants/public-paths'

export default async function proxy(request: NextRequest) {
  // ... auth-gate path; the ONLY NextResponse.redirect call is on the loginUrl path at line 22
  // NO /catalog/ handling exists. SC#4 is structurally satisfied; this plan adds the regression guard.
}
```

From `tests/app/insights-retirement.test.tsx:47-52` (canonical static-grep idiom):
```typescript
it('module does NOT declare `use cache` (Pitfall P-11 — redirect targets are per-request)', async () => {
  const fs = await import('node:fs')
  const content = fs.readFileSync('src/app/insights/page.tsx', 'utf8')
  expect(content).not.toContain("'use cache'")
  expect(content).not.toContain('"use cache"')
})
```

From `tests/proxy.test.ts` current structure (113 lines): 3 existing `describe` blocks (`AUTH-02`, `PUBLIC_PATHS parity (NAV-05 D-21)`, `profile route ungating`). The new SC#4 block appends as a 4th describe at the end of the file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <id>50.1-02-01</id>
  <name>Task 1: Append SC#4 describe block to tests/proxy.test.ts — fs.readFileSync regression guard</name>
  <files>tests/proxy.test.ts</files>
  <read_first>
    - `tests/proxy.test.ts` (READ IN FULL — 113 lines; you will APPEND a new describe block after the existing 3 describe blocks, before the trailing newline at EOF)
    - `src/proxy.ts` (READ IN FULL — 37 lines; verify the current state has no `/catalog/` token; the assertion you add must hold today)
    - `.planning/phases/50.1-url-canonicalization/50.1-RESEARCH.md` §8 Risk 1 (the Router Cache poisoning rationale and the live `/u/*` precedent; the grep command `grep -E "/catalog/|catalog/\[" src/proxy.ts` is the basis for this test's assertion)
    - `tests/app/insights-retirement.test.tsx` (lines 47-52 — the canonical `fs.readFileSync` + `expect(...).not.toContain(...)` idiom that this task replicates EXACTLY)
  </read_first>
  <action>
APPEND a new `describe` block to `tests/proxy.test.ts` (after the existing `describe('proxy.ts — profile route ungating (profile-page-404-top-nav)', ...)` block which ends at line 113). Do NOT modify any existing describe blocks; do NOT modify any existing tests. The file gets one new describe block containing one new `it()` test.

Concrete shape of the new block (replicate the structure of `tests/app/insights-retirement.test.tsx:47-52`):

  - Outer: `describe('proxy.ts — SC#4 ARCH-02 regression guard (no /catalog/ redirect logic)', () => { ... })`. Use this exact description string — the substring `SC#4` must appear so the validation map's grep can identify the test.
  - Inner `it()`: name it `'no catalog redirect — src/proxy.ts must not contain /catalog/ handling (feedback_proxy_router_cache_poisoning memory)'`. The description substring `no catalog redirect` MUST appear verbatim because the VALIDATION.md per-task verification map's vitest `-t` filter is `npx vitest run tests/proxy.test.ts -t "no catalog redirect"`.
  - Inside the `it()`: dynamic import `node:fs` (matches the analog at insights-retirement.test.tsx:48 — `const fs = await import('node:fs')`). Call `const content = fs.readFileSync('src/proxy.ts', 'utf8')`. Then assert `expect(content).not.toContain('/catalog/')`. This is the single load-bearing assertion. Add a one-line inline comment above the `expect` explaining the rationale: a `NextResponse.redirect` from proxy.ts on an RSC prefetch poisons Next 16's Router Cache → 404 on soft-nav; the canonical path for the ARCH-02 redirect is the page layer (`src/app/catalog/[catalogId]/page.tsx` per Plan 01); see `.planning/debug/profile-page-404-top-nav.md` for the live `/u/*` precedent.

Do NOT add additional assertions beyond the single `not.toContain('/catalog/')` check — the SC#4 invariant is exactly one substring absence. Do NOT add a second `it()` for variants like `not.toContain('catalog/[')` — the broader `/catalog/` substring covers all bracket/route-segment variants and adding more assertions creates noise.

Do NOT import any new modules at the top of the file (the dynamic `import('node:fs')` inside the test body is the canonical pattern — see analog). Do NOT change the existing `import` lines at the top of `tests/proxy.test.ts`.

The new describe block lives entirely below the existing code, separated by a blank line from the prior describe block.
  </action>
  <acceptance_criteria>
    - `grep -c "SC#4 ARCH-02 regression guard (no /catalog/ redirect logic)" tests/proxy.test.ts` returns `1`
    - `grep -c "no catalog redirect" tests/proxy.test.ts` returns at least `1` (the `it()` description)
    - `grep -c "fs.readFileSync('src/proxy.ts'" tests/proxy.test.ts` returns at least `1`
    - `grep -c "not.toContain('/catalog/')" tests/proxy.test.ts` returns `1`
    - `npx vitest run tests/proxy.test.ts -t "no catalog redirect"` exits 0 with exactly 1 test passing
    - `npx vitest run tests/proxy.test.ts` exits 0 (the existing 8 tests + the 1 new test all pass — verify the count does not drop)
    - Sanity check on the invariant being guarded: `grep -c "/catalog/" src/proxy.ts` returns `0` (the proxy file is clean today — this assertion is the floor the regression guard locks)
    - `npm run lint -- tests/proxy.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/proxy.test.ts && npm run lint -- tests/proxy.test.ts</automated>
  </verify>
  <done>
    `tests/proxy.test.ts` has a new appended describe block titled `proxy.ts — SC#4 ARCH-02 regression guard (no /catalog/ redirect logic)` containing exactly one `it('no catalog redirect — src/proxy.ts must not contain /catalog/ handling (feedback_proxy_router_cache_poisoning memory)', ...)` test. The test reads `src/proxy.ts` via `fs.readFileSync` and asserts the file does not contain the substring `/catalog/`. All existing proxy tests still pass; the new test passes. Lint is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a static-assertion test, not a runtime code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.1-03 (continued from Plan 01) | Denial of Service / Cache Poisoning | Future commit re-introduces `/catalog/` redirect in `src/proxy.ts` | mitigate | This plan IS the mitigation: a vitest assertion that fails CI if the `/catalog/` substring appears in `src/proxy.ts`. The assertion is a static-grep equivalent of the RESEARCH §8 Risk 1 sanity check (`grep -E "/catalog/" src/proxy.ts`). |

All threats: severity LOW or already-mitigated. No `[BLOCKING]` threats. `threat_model_complete: true`.
</threat_model>

<verification>
- The new test passes; the existing 8 proxy tests still pass (total: 9 tests in `tests/proxy.test.ts`).
- The substring `/catalog/` is verified absent from `src/proxy.ts` today (the floor this test locks).
- `npm run lint` exits 0.
</verification>

<success_criteria>
- SC#4 satisfied: an automated vitest assertion fails CI if any future commit introduces `/catalog/` redirect logic into `src/proxy.ts`. The assertion uses the canonical `fs.readFileSync` + `expect(...).not.toContain(...)` idiom from `tests/app/insights-retirement.test.tsx:47-52`. The current `src/proxy.ts` is clean (verified), so the test passes today; any future regression makes it fail.
</success_criteria>

<output>
After completion, create `.planning/phases/50.1-url-canonicalization/50.1-02-SUMMARY.md` documenting: (a) the new SC#4 describe block was appended to `tests/proxy.test.ts`, (b) the assertion replicates the `tests/app/insights-retirement.test.tsx:47-52` idiom, (c) the live `/u/*` precedent in `.planning/debug/profile-page-404-top-nav.md` is the structural rationale, (d) test passes today and locks the invariant for future commits.
</output>
