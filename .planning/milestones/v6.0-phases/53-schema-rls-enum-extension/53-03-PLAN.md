---
phase: 53-schema-rls-enum-extension
plan: 03
type: execute
wave: 3
depends_on: ["53-02"]
files_modified: []
autonomous: false
requirements: [SEC-01, SEC-06, LIKE-05, GATE-02, SEC-04]

must_haves:
  truths:
    - "Both Phase 53 migration files are applied to the PROD Supabase project via supabase db push --linked (D-12 — prod goes through supabase db push --linked, never drizzle-kit push)"
    - "The non-transactional enum migration applies to prod without an ADD-VALUE-inside-transaction error (D-09 — tested on local first per D-12)"
    - "notification_type enum reports 6 values on prod after the push"
    - "Both Phase 53 migrations appear as applied in supabase migration list --linked"
  artifacts:
    - path: "supabase/migrations/20260522000000_phase53_likes_comments_rls.sql"
      provides: "Applied to prod — recorded in supabase_migrations.schema_migrations"
    - path: "supabase/migrations/20260522000001_phase53_notification_enum.sql"
      provides: "Applied to prod (non-transactional) — enum count=6 on prod"
  key_links:
    - from: "Phase 53 migration files"
      to: "prod Supabase project (linked)"
      via: "supabase db push --linked"
      pattern: "supabase db push"
---

<objective>
Apply the two Phase 53 migrations to the PRODUCTION Supabase project, in order, via `supabase db push --linked` (the project's authoritative prod path — NOT `drizzle-kit push`), then confirm both migrations are recorded as applied and the prod enum carries 6 values. This is gated behind Plan 02's green local apply because the enum migration is non-transactional and the prod apply path differs from local — local must prove clean first (A2 risk).

Purpose: A schema layer that exists only on local is useless to the deployed app. Prod must carry the same tables, RLS, constraints, and enum values so Phases 54–58 (which deploy to prod) have the foundation they depend on.

Output: Prod Supabase carrying `watch_likes`, `wear_likes`, `comments`, the 6-value `notification_type` enum, and the two `profile_settings` opt-out columns — both migration files recorded in `supabase_migrations.schema_migrations`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/53-schema-rls-enum-extension/53-RESEARCH.md

<environment>
<!-- Verified at planning time. -->
- PROD apply path is `supabase db push --linked` — NEVER `drizzle-kit push` (project memory `project_drizzle_supabase_db_mismatch`: drizzle-kit push hit the wrong DB and broke prod in Phase 7).
- `supabase db push --linked` requires the project to be linked and may prompt for confirmation / database password — it is interactive and cannot be supplied non-interactively. Hence this plan is `autonomous: false`.
- Filename rule: both Phase 53 migrations have exactly-14-digit prefixes (`20260522000000`, `20260522000001`) — Supabase's CLI matches `^(\d{14})_name\.sql`. A non-14-digit prefix is silently skipped. (Already satisfied by Plan 01.)
- Phase 53 is purely ADDITIVE (no enum RENAME, no column type-change) — Rule 4 / pg_depend surgery does NOT apply. ADD VALUE keeps the type OID stable, so the existing `notifications_watch_overlap_dedup` partial index is unaffected.
- Local push already proven green in Plan 02 — this de-risks the non-transactional enum apply (A2).
</environment>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Task 1: Push both Phase 53 migrations to prod via supabase db push --linked</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260522000000_phase53_likes_comments_rls.sql
    - supabase/migrations/20260522000001_phase53_notification_enum.sql
    - .planning/phases/53-schema-rls-enum-extension/53-RESEARCH.md §"Manual-Only Verifications" (Prod enum apply row) and §"Assumptions Log" A2
    - docs/deploy-db-setup.md (the project's prod migration runbook — confirm the linked-project + push procedure)
  </read_first>
  <what-built>
    Plan 01 authored two migration files; Plan 02 applied both to the live local DB with all DO $$ assertions green and confirmed cascade + duplicate-like enforcement and enum count = 6. Both files are now proven clean on local. They are ready to push to prod.
  </what-built>
  <action>
    This is a checkpoint:human-action because `supabase db push --linked` needs linked-project credentials and interactive confirmation that cannot be supplied non-interactively. The executor stages and guides; the operator runs the push. Steps:

    1. Confirm the project is linked: `supabase projects list` (or `supabase link --project-ref <ref>` if not yet linked). Reference docs/deploy-db-setup.md for the project ref.
    2. Dry-run preview the pending migrations: `supabase db push --linked --dry-run` — confirm exactly the two new Phase 53 files (`20260522000000_phase53_likes_comments_rls.sql` and `20260522000001_phase53_notification_enum.sql`) are listed as pending and nothing unexpected (no stray, no skipped file).
    3. Apply: `supabase db push --linked`. The DDL file applies transactionally (its DO $$ assertions fire against prod — they MUST pass). The enum file applies non-transactionally (watch for any `ALTER TYPE ... ADD VALUE cannot run inside a transaction block` error — if it appears, the prod runner wrapped the bare file; STOP and report — do NOT patch around it).
    4. Confirm both migrations recorded: `supabase migration list --linked` — both `20260522000000` and `20260522000001` should show as applied (present in the remote column).
    5. Confirm the prod enum count via the Supabase SQL editor or a prod psql connection: `SELECT count(*) FROM pg_enum JOIN pg_type ON pg_enum.enumtypid = pg_type.oid WHERE pg_type.typname = 'notification_type';` → expect 6.

    Never invoke `drizzle-kit push` against prod (project memory Phase 7 incident — it hits the wrong DB).
  </action>
  <how-to-verify>
    Expected outcome: both migrations applied, no DO $$ exception, no transaction-block error on the enum file, prod enum count = 6, both files in `supabase migration list --linked`.
  </how-to-verify>
  <verify>
    Operator confirms: `supabase migration list --linked` shows both Phase 53 timestamps as applied AND the prod enum count query returns 6.
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` shows both `20260522000000_phase53_likes_comments_rls` and `20260522000001_phase53_notification_enum` as applied on remote.
    - The push produced no `Phase 53 failed --` DO $$ exception (DDL assertions passed against prod).
    - The push produced no `ALTER TYPE ... ADD VALUE cannot run inside a transaction block` error (non-transactional apply succeeded on prod).
    - Prod `SELECT count(*) ... WHERE typname = 'notification_type'` returns 6.
  </acceptance_criteria>
  <done>Both Phase 53 migrations are applied to prod and recorded in `supabase migration list --linked`; prod `notification_type` enum carries 6 values; no DO $$ exception and no transaction-block error occurred during the push.</done>
  <resume-signal>Type "applied" once both migrations show in `supabase migration list --linked` and prod enum count = 6; or paste the error output if either file failed to apply.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration files → prod Supabase | Authored SQL crosses into the deployed database; a bad apply affects real users. |
| linked-project credentials → human operator | The prod push requires credentials/confirmation only the operator can supply. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-11 | Tampering | enum ADD VALUE fails on the prod runner (transaction-block error) | mitigate | Local apply proven clean in Plan 02 (A2 de-risk); prod push uses the same bare non-transactional file; dry-run preview before apply; operator watches for the transaction-block error and stops if it appears. |
| T-53-12 | Information Disclosure | prod tables created without the anon REVOKE (false-OK if push partially applied) | mitigate | The DDL file is transactional and self-asserting — its DO $$ `has_table_privilege('anon', ...)` block fires against prod on apply; a missing REVOKE RAISEs and rolls back the whole DDL transaction. |
| T-53-13 | (Repudiation) Integrity | wrong-DB apply (drizzle-kit push hitting prod) | mitigate | Prod path is EXCLUSIVELY `supabase db push --linked`; drizzle-kit push is local-only and explicitly forbidden for prod (project memory Phase 7 incident). This plan never invokes drizzle-kit. |
| T-53-14 | Tampering | migration silently skipped due to malformed filename | mitigate | Both filenames are exactly 14-digit prefixed (Rule 1); `supabase db push --linked --dry-run` confirms both appear as pending before apply. |
</threat_model>

<verification>
- Both Phase 53 migrations recorded as applied on prod (`supabase migration list --linked`).
- No DO $$ exception and no transaction-block error during the push.
- Prod `notification_type` enum carries 6 values.
- Prod is now schema-aligned with local; the Phase 53 foundation is live for Phases 54–58.
</verification>

<success_criteria>
- Prod Supabase carries the full likes+comments schema layer, RLS, constraints, enum, and profile_settings columns.
- The non-transactional enum migration applied cleanly on prod (A2 risk closed).
- Phase 53 is complete: every requirement (SEC-01, SEC-04, SEC-06, LIKE-05, GATE-02) is enforced on both local and prod.
</success_criteria>

<output>
After completion, create `.planning/phases/53-schema-rls-enum-extension/53-03-SUMMARY.md`. Record the `supabase migration list --linked` output and the prod enum count.
</output>
