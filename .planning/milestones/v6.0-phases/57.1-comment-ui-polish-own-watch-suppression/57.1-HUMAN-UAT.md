---
status: complete
phase: 57.1-comment-ui-polish-own-watch-suppression
source: [57.1-VERIFICATION.md]
started: 2026-05-24T00:00:00Z
updated: 2026-05-24T18:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Desktop centering of wears-lane comment drawer (SC-1)
expected: On `/wears/[username]`, opening the comment drawer at 1024 / 1280 / 1440px shows the content centered at ~640px max-width, not stretched full-width. Mobile layout is unchanged.
result: pass

### 2. iPhone home indicator clearance in wears drawer (SC-4)
expected: On a real iPhone, the bottom of the comment drawer (compose box + last comment) sits clear of the home indicator bar (safe-area-inset-bottom padding).
result: pass
reported: "bottom padding is good - needs horizontal padding as well. content is running up against edges on both sides"
severity: minor
resolution: "Fixed (commit a008b3a) — added px-4 to the SC-1/SC-4 inner wrapper in WearCommentHost.tsx bottom-sheet branch; build green. SC-4 bottom clearance was already correct. Re-verify on next prod deploy."

### 3. Optimistic comment-count badge feel (SC-5)
expected: Posting a comment on the wears-lane or `/wear/[wearEventId]` immediately increments the count badge with NO page refresh; deleting a comment decrements it.
result: pass

### 4. /watch/[id] count badge updates after post (D-01)
expected: After posting a comment on a watch, the WatchDetail count badge updates within a server roundtrip (via router.refresh), without a manual page reload. (Known limitation: deleting a comment on `/watch/[id]` does NOT refresh the badge until a reload — best-effort, not a requirement.)
result: pass

### 5. Own-watch: no compose box, no gate copy (SC-6) — BEHAVIOR REVERSAL
expected: On YOUR OWN watch at `/watch/[id]`, there is NO compose box AND NO "Follow [username] to comment" gate copy — the comment list (if any) renders with nothing above it. The comment-count badge still shows. NOTE: Phase 57 UAT Test 1 said "owner always sees the compose box on their own watches" — SC-6 DELIBERATELY REVERSES this for watches only. Owner-sees-no-compose-box is the new correct behavior; do NOT flag it as a regression. Wears and all non-owner viewers are unchanged.
result: pass

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "The /wears/[username] comment drawer has adequate spacing — content does not run against the left/right screen edges on mobile"
  status: resolved
  reason: "User reported: bottom padding is good - needs horizontal padding as well. content is running up against edges on both sides"
  resolution: "Fixed in commit a008b3a — added px-4 to the SC-1/SC-4 inner wrapper in WearCommentHost.tsx bottom-sheet branch; build green. Awaiting prod re-verify on next deploy."
  severity: minor
  test: 2
  artifacts: ["src/components/wear/WearCommentHost.tsx"]
  missing: []
