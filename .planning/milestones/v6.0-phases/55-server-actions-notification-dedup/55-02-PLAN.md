---
phase: 55-server-actions-notification-dedup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260522000002_phase55_notif_like_dedup.sql
  - supabase/migrations/20260522000001_phase53_notification_enum.sql
  - src/lib/actionTypes.ts
autonomous: true
requirements: [NOTIF-14]

must_haves:
  truths:
    - "A new migration creates two partial UNIQUE dedup indexes on notifications: notifications_watch_like_dedup and notifications_wear_like_dedup (D-01)"
    - "The dedup indexes apply only to their like type via WHERE type = 'watch_like' / 'wear_like' and key on (payload->>'watch_id') / (payload->>'wear_event_id') respectively (NOTIF-14)"
    - "WR-03 is fixed in place: the Phase 53 enum migration asserts presence of the 4 Phase 53 enum values rather than an exact total of 6 (D-02), so supabase db reset replay survives a future 7th enum value"
    - "The dedup migration is applied to the live local DB and both indexes exist locally"
    - "ActionResult gains an optional code field on the failure branch so D-09 'gate' discriminant can be returned without breaking existing callers (D-09)"
  artifacts:
    - path: "supabase/migrations/20260522000002_phase55_notif_like_dedup.sql"
      provides: "Two partial UNIQUE dedup indexes (NOTIF-14)"
      contains: "notifications_watch_like_dedup"
    - path: "src/lib/actionTypes.ts"
      provides: "ActionResult with optional code field (D-09)"
      contains: "code?:"
  key_links:
    - from: "supabase/migrations/20260522000002_phase55_notif_like_dedup.sql"
      to: "notifications.payload->>'watch_id'"
      via: "CREATE UNIQUE INDEX expression"
      pattern: "payload->>'watch_id'"
    - from: "supabase/migrations/20260522000002_phase55_notif_like_dedup.sql"
      to: "notifications.payload->>'wear_event_id'"
      via: "CREATE UNIQUE INDEX expression"
      pattern: "payload->>'wear_event_id'"
---

<objective>
Author the Phase 55 schema change: one new migration adding the two partial UNIQUE dedup indexes
on `notifications` (D-01), fold the WR-03 enum-assertion fix into the Phase 53 migration in place (D-02),
apply the new migration to the live LOCAL database, and extend `ActionResult` with an optional `code`
field so plan 05's comment actions can return the D-09 `code:'gate'` discriminant.

Purpose: The dedup indexes are the DB-level enforcement of NOTIF-14 — without them, the logger's
`ON CONFLICT DO NOTHING` (plan 03) silently never dedups and rapid like/unlike churn spams the bell.
The WR-03 fix keeps `supabase db reset` replay correct now that a migration-touching phase has arrived.
Output: 1 new migration, 1 in-place migration edit, 1 actionTypes edit, and a verified local apply.

This plan does NOT push to prod — that is plan 06 (a blocking human-action checkpoint, D-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-server-actions-notification-dedup/55-CONTEXT.md
@.planning/phases/55-server-actions-notification-dedup/55-RESEARCH.md
@.planning/phases/55-server-actions-notification-dedup/55-PATTERNS.md

<interfaces>
<!-- Migration analogs (raw SQL is authoritative). Extracted from codebase. -->

Phase 11 dedup index pattern (the template to mirror) — supabase/migrations/20260423000002_phase11_notifications.sql:80-92:
```sql
CREATE UNIQUE INDEX IF NOT EXISTS notifications_watch_overlap_dedup
  ON notifications ( user_id, (payload->>'watch_brand_normalized'), (payload->>'watch_model_normalized'), ((created_at AT TIME ZONE 'UTC')::date) )
  WHERE type = 'watch_overlap';
```

WR-03 target (the broken assertion) — supabase/migrations/20260522000001_phase53_notification_enum.sql:29-41:
the `IF enum_count <> 6 THEN RAISE EXCEPTION ...` block. The ALTER TYPE ADD VALUE statements above it (lines 20-23) stay untouched.

Current ActionResult — src/lib/actionTypes.ts:5-7:
```typescript
export type ActionResult<T = void> =
  | { success: true; data: T }
  | { success: false; error: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the dedup-index migration + fold the WR-03 fix in place</name>
  <read_first>
    - supabase/migrations/20260423000002_phase11_notifications.sql (lines 80-92 — the notifications_watch_overlap_dedup partial UNIQUE index + ON CONFLICT precedent the new indexes mirror exactly)
    - supabase/migrations/20260522000001_phase53_notification_enum.sql (the full file — the ALTER TYPE ADD VALUE x4 stays; the IF enum_count <> 6 block at lines 29-41 is the WR-03 edit target)
    - .planning/phases/55-server-actions-notification-dedup/55-RESEARCH.md (§ WR-03 Migration Fix — the presence-based DO $$ assertion; § Migration: Two New Dedup Indexes)
    - .planning/phases/55-server-actions-notification-dedup/55-PATTERNS.md (§ supabase/migrations/...phase55... — index keys + WR-03 replacement; the file-header note that this migration CAN be transactional)
  </read_first>
  <action>
    Create supabase/migrations/20260522000002_phase55_notif_like_dedup.sql. Wrap it in BEGIN; ... COMMIT;
    (this migration is pure CREATE UNIQUE INDEX, no ALTER TYPE ADD VALUE, so it is transaction-safe — unlike the
    non-transactional Phase 53 enum file). Add two partial UNIQUE indexes mirroring the Phase 11 watch_overlap precedent:
      - CREATE UNIQUE INDEX IF NOT EXISTS notifications_watch_like_dedup ON notifications (user_id, actor_id, (payload->>'watch_id')) WHERE type = 'watch_like';
      - CREATE UNIQUE INDEX IF NOT EXISTS notifications_wear_like_dedup ON notifications (user_id, actor_id, (payload->>'wear_event_id')) WHERE type = 'wear_like';
    Use plain CREATE UNIQUE INDEX (not CONCURRENTLY) — CONCURRENTLY cannot run inside the BEGIN/COMMIT transaction and there are
    no existing like-notification rows to lock, so a blocking build is fine (Claude's Discretion D-02 leans this way; CONCURRENTLY would force dropping the transaction wrapper).
    Add a header comment block explaining: payload key watch_id (watch_like) vs wear_event_id (wear_like) — these MUST match
    the WatchLikePayload.watch_id / WearLikePayload.wear_event_id keys the logger sets (plan 03), or the partial index never fires.
    Then EDIT supabase/migrations/20260522000001_phase53_notification_enum.sql IN PLACE (D-02 — in-place is the locked mechanism
    per RESEARCH § WR-03 Fix Mechanism: the DO $$ block has no prod side-effect, it only runs during local supabase db reset replay):
    replace the `IF enum_count <> 6` exact-count assertion with the presence-of-4-values check (SELECT against pg_enum/pg_type joined,
    counting how many of 'watch_like','wear_like','watch_comment','wear_comment' are missing; RAISE EXCEPTION only if any are missing).
    Keep the ALTER TYPE ADD VALUE statements (lines 20-23) and the non-transactional shape of the Phase 53 file unchanged — do NOT
    add a BEGIN/COMMIT to the Phase 53 file. NAMING LANDMINE: the wear index keys on payload->>'wear_event_id' (column-style), never 'wear_id'/'wear_event'.
  </action>
  <verify>
    <automated>grep -q "notifications_watch_like_dedup" supabase/migrations/20260522000002_phase55_notif_like_dedup.sql &amp;&amp; grep -q "payload->>'wear_event_id'" supabase/migrations/20260522000002_phase55_notif_like_dedup.sql &amp;&amp; ! grep -q "enum_count <> 6" supabase/migrations/20260522000001_phase53_notification_enum.sql</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260522000002_phase55_notif_like_dedup.sql contains `CREATE UNIQUE INDEX` ... `notifications_watch_like_dedup` ... `(payload->>'watch_id')` ... `WHERE type = 'watch_like'`
    - Same file contains `notifications_wear_like_dedup` ... `(payload->>'wear_event_id')` ... `WHERE type = 'wear_like'`
    - The new migration includes both `user_id` and `actor_id` in each index key (per (recipient, actor, target) dedup pair, D-01)
    - supabase/migrations/20260522000001_phase53_notification_enum.sql NO LONGER contains the string `enum_count <> 6`
    - The Phase 53 file STILL contains all four `ALTER TYPE notification_type ADD VALUE IF NOT EXISTS` statements and has NO BEGIN/COMMIT wrapper
    - The Phase 53 file's replacement assertion references the 4 enum labels watch_like/wear_like/watch_comment/wear_comment and RAISEs only when one is missing
  </acceptance_criteria>
  <done>New dedup migration authored; WR-03 fixed in place in the Phase 53 migration; ALTER TYPE statements preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the dedup migration to the live local DB and verify both indexes exist</name>
  <read_first>
    - $HOME/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/project_drizzle_supabase_db_mismatch.md (local apply rules; drizzle-kit push is LOCAL ONLY; the new dedup migration is transactional unlike the Phase 53 enum migration)
    - $HOME/.claude/projects/-Users-tylerwaneka-Documents-horlo/memory/project_local_db_reset.md (the supabase db reset → drizzle push → selective supabase migration replay workflow if a full reset is used)
    - supabase/migrations/20260522000002_phase55_notif_like_dedup.sql (the file authored in Task 1)
  </read_first>
  <action>
    Apply the new migration to the running local Postgres. Because this migration is a raw-SQL CREATE UNIQUE INDEX (not represented
    in Drizzle), apply it via the supabase migration runner against local, or execute the SQL directly against the local DB
    (e.g. docker exec psql per the local-db-reset memory note) — whichever the project's established local-apply path is. If a full
    `supabase db reset` is used, follow the documented reset → drizzle push → selective supabase replay sequence so the WR-03'd
    Phase 53 assertion fires and passes. The two new indexes are the goal. After applying, verify with a catalog query that both
    notifications_watch_like_dedup and notifications_wear_like_dedup exist on the notifications table (e.g. SELECT indexname FROM pg_indexes
    WHERE tablename = 'notifications', or \d notifications). Capture the verification output in the SUMMARY.
    Do NOT run `supabase db push --linked` here — prod push is plan 06 (D-03 blocking checkpoint).
  </action>
  <verify>
    <automated>echo "Manual/CLI verify recorded in SUMMARY: both indexes present on local notifications table (pg_indexes query)."</automated>
  </verify>
  <acceptance_criteria>
    - A pg_indexes (or \d notifications) query against the LOCAL DB lists both notifications_watch_like_dedup and notifications_wear_like_dedup — output pasted into the SUMMARY
    - The WR-03'd Phase 53 enum assertion passed during the local apply/replay (no RAISE EXCEPTION)
    - `supabase db push --linked` was NOT run in this plan
  </acceptance_criteria>
  <done>Both dedup indexes exist on the local notifications table; WR-03 assertion replays cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Extend ActionResult with an optional code field (D-09)</name>
  <read_first>
    - src/lib/actionTypes.ts (the full current 7-line file)
    - .planning/phases/55-server-actions-notification-dedup/55-PATTERNS.md (§ src/lib/actionTypes.ts — the minimal least-disruptive extension; why code?:string over a tighter union)
    - .planning/phases/55-server-actions-notification-dedup/55-CONTEXT.md (D-09 — gate rejection as a discriminated error code)
  </read_first>
  <action>
    Edit src/lib/actionTypes.ts. Add an optional `code` field to the failure branch ONLY, preserving the success branch and the
    existing union identity for all current callers (least-disruptive choice per Claude's Discretion D-09):
    change `{ success: false; error: string }` to `{ success: false; error: string; code?: string }`.
    Add a one-line comment noting D-09: the optional code (e.g. 'gate') lets Phase 57 branch to the GATE-03 locked-state CTA without
    string-matching the error message. Do NOT change the success branch; do NOT make code required.
  </action>
  <verify>
    <automated>grep -q "code?:" src/lib/actionTypes.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -v "node_modules" | grep -i "actionTypes" ; echo "tsc actionTypes clean"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/actionTypes.ts failure branch reads `{ success: false; error: string; code?: string }`
    - The success branch `{ success: true; data: T }` is unchanged
    - `npx tsc --noEmit` reports no NEW type errors attributable to actionTypes.ts (existing callers unaffected because code is optional)
  </acceptance_criteria>
  <done>ActionResult failure branch carries optional code; no existing caller breaks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application write path → notifications table | The logger (plan 03) inserts rows on behalf of an actor. The dedup index is the DB-layer enforcement that a malicious or buggy caller cannot spam duplicate like-notification rows for the same (recipient, actor, target). |
| local migration replay → schema correctness | The WR-03 assertion is a self-check that runs during local replay; a brittle exact-count assertion is itself a denial-of-availability on the dev workflow (blocks db reset). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-SPAM | Denial of Service (inbox) | notifications dedup indexes | mitigate | Partial UNIQUE indexes (user_id, actor_id, payload target key) WHERE type = like-type make duplicate like-notification rows impossible at the DB layer; the logger's ON CONFLICT DO NOTHING (plan 03) hits them. Verified by NOTIF-14 churn test. |
| T-55-REPLAY | Denial of Service (dev availability) | Phase 53 enum assertion (WR-03) | mitigate | Replace exact-count `<> 6` with presence-of-4-values check so a future 7th/8th enum value never blocks supabase db reset replay (D-02). |
| T-55-KEYMISS | Tampering / silent failure | dedup index payload key | mitigate | Index keys on payload->>'watch_id' / payload->>'wear_event_id' matching the plan-03 payload interfaces exactly; a mismatch (the LANDMINE) would silently disable dedup. Acceptance criteria assert the exact key strings. |
</threat_model>

<verification>
- New migration file present with both named partial UNIQUE indexes keyed correctly per type
- Phase 53 migration no longer hard-codes the enum total; asserts presence of the 4 values
- Both indexes verified present on the LOCAL notifications table
- `src/lib/actionTypes.ts` extended; `npx tsc --noEmit` introduces no new errors
</verification>

<success_criteria>
- NOTIF-14 DB enforcement exists locally (two partial UNIQUE indexes)
- WR-03 fixed in place; local replay survives a hypothetical future enum value
- ActionResult ready for the D-09 gate discriminant
- No prod push performed (deferred to plan 06)
</success_criteria>

<output>
After completion, create `.planning/phases/55-server-actions-notification-dedup/55-02-SUMMARY.md`
</output>
