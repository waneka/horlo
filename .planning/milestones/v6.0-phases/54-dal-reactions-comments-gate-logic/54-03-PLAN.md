---
phase: 54-dal-reactions-comments-gate-logic
plan: 03
type: execute
wave: 2
depends_on: [54-02]
files_modified:
  - src/data/comments.ts
autonomous: true
requirements: [GATE-01, GATE-04, SEC-02]
must_haves:
  truths:
    - "canViewerCommentOnTarget is the single shared gate predicate driving create/read/UI (D-04)"
    - "canViewerCommentOnTarget returns true for any wear target without touching the DB"
    - "canViewerCommentOnTarget returns true for the watch owner regardless of status (GATE-04)"
    - "canViewerCommentOnTarget returns true for any authenticated viewer on owned/sold/grail watches (GATE-01 open path)"
    - "canViewerCommentOnTarget returns true on a wishlist watch only for a mutual follower; false otherwise"
    - "createComment throws a distinct typed CommentGateError when the gate is false (SEC-02, D-05); the DAL is the load-bearing privacy layer (D-01); succeeds and returns the new Comment otherwise"
    - "getCommentsForTarget returns [] (no content, no count) for a gated viewer on a wishlist watch (GATE-01 read-gate, D-06)"
    - "The comments DAL ships as pure TypeScript — no SQL migration and no change to the Phase 53 comments RLS (D-02)"
    - "src/data/comments.ts opens with a loud load-bearing-privacy-layer invariant doc comment documenting the RLS-bypass and the known supabase-js fail-closed landmine (D-03)"
  artifacts:
    - path: "src/data/comments.ts"
      provides: "comment gate predicate + create/read + CommentGateError + load-bearing invariant doc comment"
      contains: "export class CommentGateError"
  key_links:
    - from: "src/data/comments.ts (canViewerCommentOnTarget)"
      to: "src/data/follows.ts (isMutualFollow)"
      via: "import { isMutualFollow } and call for the wishlist branch"
      pattern: "isMutualFollow\\("
    - from: "src/data/comments.ts (createComment)"
      to: "CommentGateError"
      via: "if (!allowed) throw new CommentGateError()"
      pattern: "throw new CommentGateError"
---

<objective>
Wave 2 — implement the wishlist comment gate in a new `src/data/comments.ts`: the shared `canViewerCommentOnTarget` predicate (D-04, single source of truth for create/read/UI), `createComment` (throws `CommentGateError` on gate failure — SEC-02), `getCommentsForTarget` (returns `[]` for gated viewers — GATE-01 read-gate, D-06), the `CommentGateError` typed error class (D-05), and the load-bearing-DAL-gate invariant doc comment (D-03). Edit/delete CRUD is co-located here per CONTEXT.md "Claude's Discretion" (recommended) so Phase 55's Server Actions have the full DAL surface.

Purpose: This is the load-bearing privacy layer. Because Drizzle `db` bypasses RLS, `canViewerCommentOnTarget`/`createComment` is the SOLE enforced wishlist gate for authenticated users (D-01) — the TS analog of `assertOwner()`.
Output: `src/data/comments.ts` with the gate predicate, comment create/read (+ edit/delete), `CommentGateError`, and the invariant doc comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-dal-reactions-comments-gate-logic/54-CONTEXT.md
@.planning/phases/54-dal-reactions-comments-gate-logic/54-RESEARCH.md
@.planning/phases/54-dal-reactions-comments-gate-logic/54-PATTERNS.md

<interfaces>
<!-- Verified from the live codebase + Plan 02 output. Use directly. -->

From src/data/follows.ts (Plan 02 — Wave 1):
```typescript
export async function isMutualFollow(userA: string, userB: string): Promise<boolean>
```

From src/lib/auth.ts:6-11 (UnauthorizedError — shape to mirror for CommentGateError):
```typescript
export class UnauthorizedError extends Error {
  constructor(message = 'Not authenticated') { super(message); this.name = 'UnauthorizedError' }
}
```
The assertOwner CR-01 doc comment at src/lib/auth.ts:61-69 is the exact style to mirror for D-03.

From src/db/schema.ts (verified):
```typescript
// watches: userId (user_id) NOT NULL; status enum 'owned'|'wishlist'|'sold'|'grail'
// comments: authorId (author_id), watchId (watch_id, nullable), wearEventId (wear_event_id, nullable),
//           body (text, DB CHECK char_length<=500 AND non-blank), editedAt (edited_at, nullable),
//           createdAt, updatedAt. Chronological idx: comments_watch_id_created_at_idx / comments_wear_event_id_created_at_idx.
//           NO unique constraint (intentional).
```

Comment row type (derive in this file — no `Comment` alias exists in src/lib/types.ts yet):
```typescript
import type { InferSelectModel } from 'drizzle-orm'
export type Comment = InferSelectModel<typeof comments>
```

Target discriminator (shared shape, this phase): `{ type: 'watch' | 'wear'; id: string }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create comments.ts — CommentGateError, invariant doc comment, canViewerCommentOnTarget gate predicate</name>
  <files>src/data/comments.ts</files>
  <read_first>
    - src/lib/auth.ts:6-11 (UnauthorizedError shape) and 61-69 (assertOwner CR-01 load-bearing doc-comment style to mirror for D-03)
    - src/data/follows.ts (isFollowing row-existence idiom lines 54-68; `import 'server-only'` line 1; the new isMutualFollow from Plan 02)
    - src/db/schema.ts:353-380 (comments columns) + watches.userId/status
    - .planning/phases/54-dal-reactions-comments-gate-logic/54-CONTEXT.md (D-01..D-06, D-11 grandfather)
    - .planning/phases/54-dal-reactions-comments-gate-logic/54-RESEARCH.md (§Pattern 5 CommentGateError, §Pattern 6 doc comment full text, §Code Examples canViewerCommentOnTarget, §Pitfall 2 wear short-circuit)
    - .planning/phases/54-dal-reactions-comments-gate-logic/54-PATTERNS.md (§comments.ts — gate predicate + throw + doc comment)
  </read_first>
  <behavior>
    - canViewerCommentOnTarget(anyone, { type:'wear', id }) → true, and the db.select mock is NOT called (short-circuit, Pitfall 2)
    - canViewerCommentOnTarget(owner, { type:'watch', wishlistWatch }) → true (GATE-04 owner bypass, viewerId === watch.userId)
    - canViewerCommentOnTarget(stranger, { type:'watch', ownedWatch }) → true (GATE-01 non-wishlist open)
    - canViewerCommentOnTarget(mutual, { type:'watch', wishlistWatch }) → true (calls isMutualFollow → true)
    - canViewerCommentOnTarget(oneWay, { type:'watch', wishlistWatch }) → false (isMutualFollow → false)
    - canViewerCommentOnTarget(viewer, { type:'watch', missingId }) → false (watch not found, fail closed)
  </behavior>
  <action>
    First line: `import 'server-only'`. Immediately after, add the load-bearing invariant doc-comment block (D-03) mirroring src/lib/auth.ts:61-69 style — full approved text is in 54-RESEARCH.md §Pattern 6: state that the comments DAL runs through Drizzle `db` (bypasses RLS), that `canViewerCommentOnTarget`/`createComment` is the SOLE enforced wishlist gate, and the KNOWN LANDMINE (routing comment reads/writes through an RLS-respecting supabase-js client fails closed for ALL non-owners because the Phase 53 RLS SELECT subquery is intentionally non-functional — reference Phase 53 D-07 and Phase 54 D-02).

    Imports: `{ and, eq } from 'drizzle-orm'`, `type { InferSelectModel } from 'drizzle-orm'`, `db` from `@/db`, `{ comments, watches } from '@/db/schema'`, `{ isMutualFollow } from './follows'`. Export `type Comment = InferSelectModel<typeof comments>` and the shared `type CommentTarget = { type: 'watch' | 'wear'; id: string }`.

    Export `class CommentGateError extends Error` mirroring `UnauthorizedError` exactly (three-line constructor, `this.name = 'CommentGateError'`, default message `'Mutual follow required to comment on wishlist watches'`) — distinct class so Phase 55 catches by `instanceof` without string-matching (D-05).

    Export `canViewerCommentOnTarget(viewerId: string, target: CommentTarget): Promise<boolean>` exactly per D-04 + 54-RESEARCH.md §Code Examples: (1) `if (target.type === 'wear') return true` FIRST, before any DB query (Pitfall 2); (2) fetch the watch row via `db.select({ userId, status }).from(watches).where(eq(watches.id, target.id)).limit(1)`; (3) `if (!watch) return false` (fail closed); (4) `if (viewerId === watch.userId) return true` (GATE-04); (5) `if (watch.status !== 'wishlist') return true` (GATE-01 open); (6) `return isMutualFollow(viewerId, watch.userId)` (GATE-05, D-11 grandfather — keys off current status). This single predicate is the source of truth for create, read, and the Phase 57 UI.
  </action>
  <verify>
    <automated>npm run test -- src/data/__tests__/reactions-comments-gate.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/data/comments.ts first line is `import 'server-only'`
    - Contains 'export class CommentGateError' with `this.name = 'CommentGateError'`
    - Contains a doc comment with 'KNOWN LANDMINE' and the RLS-bypass invariant text (D-03)
    - Contains 'export async function canViewerCommentOnTarget' with `if (target.type === 'wear') return true` as the first statement
    - Owner-bypass, non-wishlist-open, wishlist+mutual, wishlist+non-mutual→false, and not-found→false unit branches pass in reactions-comments-gate.test.ts
    - canViewerCommentOnTarget calls isMutualFollow only on the wishlist branch
  </acceptance_criteria>
  <done>`CommentGateError`, the D-03 invariant doc comment, and the `canViewerCommentOnTarget` predicate exist; all gate-branch unit tests green; wear short-circuits before any DB call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add createComment / getCommentsForTarget (+ editComment / deleteComment) to comments.ts</name>
  <files>src/data/comments.ts</files>
  <read_first>
    - src/data/comments.ts (the file from Task 1 — gate predicate + CommentGateError already present)
    - src/data/follows.ts (followUser insert pattern lines 22-30; unfollowUser IDOR-safe delete lines 36-48 — template for deleteComment authorship scope)
    - src/lib/auth.ts:70-80 (assertOwner → throw pattern)
    - src/db/schema.ts:353-380 (comments columns + chronological indexes)
    - .planning/phases/54-dal-reactions-comments-gate-logic/54-RESEARCH.md (§Pattern 7 ActionResult/DAL-throws; §Code Examples; Open Question 1 edit/delete co-location recommendation)
    - .planning/phases/54-dal-reactions-comments-gate-logic/54-PATTERNS.md (§comments.ts createComment throw + getCommentsForTarget read+gate + IDOR-safe delete)
  </read_first>
  <behavior>
    - createComment as non-mutual-follower on a wishlist watch → throws CommentGateError (SEC-02)
    - createComment as owner on own wishlist watch → resolves, returns the inserted Comment (GATE-04)
    - createComment as stranger on an owned watch → resolves (non-wishlist open)
    - getCommentsForTarget for a gated viewer on a wishlist watch → [] (no content, no count — GATE-01 read-gate / D-06)
    - getCommentsForTarget for an allowed viewer → comments for that target, chronological order
    - deleteComment scopes WHERE by authorId (IDOR-safe; a non-author cannot delete)
  </behavior>
  <action>
    `createComment(input: { authorId: string; target: CommentTarget; body: string }): Promise<Comment>` — call `const allowed = await canViewerCommentOnTarget(input.authorId, input.target)`; `if (!allowed) throw new CommentGateError()` (SEC-02; mirrors assertOwner → throw). On allowed, `db.insert(comments).values({ authorId, watchId: target.type === 'watch' ? target.id : null, wearEventId: target.type === 'wear' ? target.id : null, body }).returning()` and return `rows[0]`. Do NOT re-fetch the watch row separately for the insert (Pitfall 3 — the gate already fetched it; one round-trip for the gate, one for the insert is acceptable, but do not add a third). Body length/blank validation is the DB CHECK + Phase 55 Zod's job — do not duplicate it here (SEC-03 is Phase 55).

    `getCommentsForTarget(viewerId: string, target: CommentTarget): Promise<Comment[]>` — call `canViewerCommentOnTarget(viewerId, target)`; `if (!allowed) return []` (D-06 — no content, no count leaked; the gated wishlist read returns an empty array, NOT a `{ comments, gated }` object). Otherwise `db.select().from(comments).where(eq(comments.watchId, target.id))` (or `wearEventId` for wear) ordered by `createdAt asc` (chronological — CMNT-03 is oldest-first; the `comments_*_created_at_idx` indexes back this). Return the rows as `Comment[]`.

    Co-locate edit/delete CRUD here (CONTEXT.md Claude's Discretion — recommended, since this is the DAL phase and Phase 55 needs the surface): `editComment(authorId: string, commentId: string, body: string): Promise<Comment>` — authorship-scoped update `db.update(comments).set({ body, editedAt: new Date(), updatedAt: new Date() }).where(and(eq(comments.id, commentId), eq(comments.authorId, authorId))).returning()` (CMNT-06 editedAt semantics); `deleteComment(authorId: string, commentId: string): Promise<void>` — `db.delete(comments).where(and(eq(comments.id, commentId), eq(comments.authorId, authorId)))` (IDOR-safe authorship scope, mirrors unfollowUser). These do NOT run the comment gate (authorship is the only check — an author editing their own comment is always allowed).
  </action>
  <verify>
    <automated>npm run test -- src/data/__tests__/reactions-comments-gate.test.ts 2>&1 | tail -10; echo "--- integration (localhost only) ---"; npm run test -- tests/integration/phase54-dal-gate.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - src/data/comments.ts contains 'export async function createComment', 'export async function getCommentsForTarget', 'export async function editComment', 'export async function deleteComment'
    - createComment contains 'throw new CommentGateError' guarded by `if (!allowed)`
    - getCommentsForTarget contains `if (!allowed) return []` and orders by createdAt ascending
    - deleteComment WHERE includes `eq(comments.authorId` (IDOR scope); editComment sets `editedAt`
    - On local Docker, tests/integration/phase54-dal-gate.test.ts is GREEN: SEC-02 (non-mutual + stranger throw CommentGateError), GATE-04 (owner createComment resolves), GATE-01 (gated read → [], owned-watch create resolves), mutual create resolves, GATE-05 (isMutualFollow one-way → false)
    - `npx tsc --noEmit` produces no new errors attributable to comments.ts
  </acceptance_criteria>
  <done>Full comment DAL surface present; SEC-02 gate throws for non-mutual callers on wishlist watches; GATE-04 owner always allowed; GATE-01 read-gate returns []; integration suite green on local Docker; edit/delete authorship-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated viewer → comment DAL | A non-mutual-follower may call `createComment`/`getCommentsForTarget` directly (DAL bypasses RLS). The DAL gate is the ONLY enforced layer for authenticated users (D-01). |
| Future supabase-js path → comments RLS | The Phase 53 `comments` RLS is intentionally fail-closed for non-owners. Documented landmine (D-03): all comment access MUST go through this DAL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SEC-02 | Tampering / Elevation of Privilege | createComment on wishlist watch | mitigate | `canViewerCommentOnTarget` gate + `throw new CommentGateError()` — verified by the SEC-02 direct-DAL integration test (non-mutual-follower bypassing RLS is rejected). |
| T-54-07 | Information disclosure | getCommentsForTarget on gated wishlist watch | mitigate | `if (!allowed) return []` — no comment content AND no count leaked to a non-mutual viewer (D-06); UI derives gate state from the boolean predicate, not the list shape. |
| T-54-08 | Tampering (IDOR) | editComment / deleteComment | mitigate | WHERE scoped by `authorId = caller` — a non-author cannot edit or delete another user's comment. |
| T-54-09 | Tampering | future dev routes comments through supabase-js | mitigate | D-03 load-bearing invariant doc comment warns that the RLS gate is a non-functional fail-closed backstop; all access must use this DAL. RLS still anon-blocks (Layer 1). |
| T-54-10 | Spoofing | client-supplied authorId | accept | DAL takes explicit `authorId`; Phase 55 Server Action resolves it from `getCurrentUser()` and rejects client-trusted ids (SEC-03). Out of scope for this phase. |
</threat_model>

<verification>
- Unit: `npm run test -- src/data/__tests__/reactions-comments-gate.test.ts` green for all `canViewerCommentOnTarget` branches.
- Integration (local Docker): `npm run test -- tests/integration/phase54-dal-gate.test.ts` green for SEC-02, GATE-01, GATE-04, GATE-05.
- Phase gate: full `npm run test` green before `/gsd-verify-work`.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
GATE-01, GATE-04, and SEC-02 satisfied: wishlist comments restricted to mutual followers (open on owned/sold/grail + all wears); owner always allowed; non-mutual direct-DAL caller rejected with `CommentGateError`; gated read returns `[]`. The single `canViewerCommentOnTarget` predicate is the source of truth for create, read, and the Phase 57 UI gate. The DAL-gate invariant is loudly documented.
</success_criteria>

<output>
After completion, create `.planning/phases/54-dal-reactions-comments-gate-logic/54-03-SUMMARY.md`
</output>
