---
phase: 56-like-ui
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/components/watch/WatchDetail.tsx
  - src/app/watch/[id]/page.tsx
autonomous: true
requirements: [LIKE-01, LIKE-03, LIKE-04]
tags: [likes, watch-detail, cache-components]

must_haves:
  truths:
    - "Every authenticated viewer of /watch/[id] sees a LikeButton under the brand/model title block, regardless of the watch's status (owned/sold/grail/wishlist) — LIKE-01, D-03, GATE-02"
    - "The LikeButton is separate from the owner-only Edit/Delete actions row and is interactive for the owner too (no self-hide branch) — D-09"
    - "Initial liked/count are server-hydrated via getLikesForTargetCached(user.id, {type:'watch', id}) so the Phase-55 action invalidations re-hydrate on next navigation — LIKE-01, LIKE-04"
  artifacts:
    - path: "src/components/watch/WatchDetail.tsx"
      provides: "LikeButton rendered under the title block; viewerId + initialLikeState props"
      contains: "LikeButton"
    - path: "src/app/watch/[id]/page.tsx"
      provides: "getLikesForTargetCached hydration read passed into WatchDetail"
      contains: "getLikesForTargetCached"
  key_links:
    - from: "src/app/watch/[id]/page.tsx"
      to: "getLikesForTargetCached"
      via: "await getLikesForTargetCached(user.id, { type: 'watch', id })"
      pattern: "getLikesForTargetCached"
    - from: "src/components/watch/WatchDetail.tsx"
      to: "src/components/shared/LikeButton"
      via: "import + render under title block"
      pattern: "LikeButton"
---

<objective>
Wire the watch detail page (D-03): render `LikeButton` inside the `'use client'` `WatchDetail` island, under the brand/model title block, visible to every authenticated viewer (LIKE-01, GATE-02) and separate from the owner-only actions. Hydrate initial like-state server-side in the page via `getLikesForTargetCached`.

Purpose: Delivers the watch half of the phase goal — any authenticated viewer can like/unlike any watch regardless of status, with the count next to the control.
Output: edited `WatchDetail.tsx` (new props + rendered button) and `watch/[id]/page.tsx` (hydration read + prop pass-through).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-like-ui/56-CONTEXT.md
@.planning/phases/56-like-ui/56-PATTERNS.md
@.planning/phases/56-like-ui/56-UI-SPEC.md

<interfaces>
<!-- Contracts produced by Plan 56-01 (Wave 1). Read directly. -->

From src/components/shared/LikeButton.tsx (created in 56-01):
```typescript
interface LikeButtonProps {
  viewerId: string | null
  target: { type: 'watch' | 'wear'; id: string }
  initialLiked: boolean
  initialCount: number
}
export function LikeButton(props: LikeButtonProps): JSX.Element
```

From src/data/reactions.ts (added in 56-01):
```typescript
export async function getLikesForTargetCached(viewerId: string, target: LikeTarget): Promise<{ count: number; viewerHasLiked: boolean }>
```

From src/app/watch/[id]/page.tsx (existing — auth-only route):
```typescript
// const user = await getCurrentUser()  → user.id is always a string (throws on anon; no sentinel needed)
// const [result, collection, preferences] = await Promise.all([...])
// const { watch, isOwner } = result   → watch.id, watch.status available
// <WatchDetail watch={watch} collection={...} preferences={...} lastWornDate={...} viewerCanEdit={isOwner} verdict={...} />
```

From src/components/watch/WatchDetail.tsx (existing — 'use client' island):
```typescript
// interface WatchDetailProps { watch; collection; preferences; lastWornDate?; viewerCanEdit?; verdict? }
// Title block: Badge(status) + h1(brand) + p(model) + optional p(ref) — closes ~L146
// Owner-only "Last worn" block begins ~L148; owner-only Edit/Delete row ~L185
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add viewerId + initialLikeState props to WatchDetail and render LikeButton under the title</name>
  <files>src/components/watch/WatchDetail.tsx</files>
  <read_first>
    - src/components/watch/WatchDetail.tsx (file being edited — WatchDetailProps interface ~L32-50; title block ~L134-146; the owner-only "Last worn" block at ~L148; lucide-react imports at L7)
    - src/components/shared/LikeButton.tsx (the component being rendered — props contract)
    - .planning/phases/56-like-ui/56-UI-SPEC.md (Watch page placement D-03: insertion point diagram + row styling `flex items-center gap-2 mt-3`)
    - .planning/phases/56-like-ui/56-PATTERNS.md (WatchDetail.tsx section — exact prop additions + insertion-point markup)
  </read_first>
  <action>
    Edit `WatchDetailProps` (interface ~L32-50): add `viewerId?: string | null` (comment: Phase 56 D-03 — null impossible since watch page is auth-only) and `initialLikeState?: { liked: boolean; count: number }` (comment: server-hydrated from getLikesForTargetCached). Add to the destructured signature at ~L71 (`export function WatchDetail({ ... , viewerId, initialLikeState })`).

    Add import: `import { LikeButton } from '@/components/shared/LikeButton'`. (Do NOT add a `Heart` import here — the icon lives inside LikeButton.)

    Insert the LikeButton row AFTER the title block's closing `</div>` (~L146, right after the `{watch.reference && …}` paragraph closes) and BEFORE the owner-only "Last worn line" block (~L148). Render only when both new props are defined:
    `{viewerId !== undefined && initialLikeState !== undefined && (<div className="flex items-center gap-2 mt-3"><LikeButton viewerId={viewerId ?? null} target={{ type: 'watch', id: watch.id }} initialLiked={initialLikeState.liked} initialCount={initialLikeState.count} /></div>)}`

    The row sits OUTSIDE / above the `viewerCanEdit`-gated blocks so it is visible to every viewer (D-09 — no self-hide; the owner sees and can use it). Use `type: 'watch'` — NEVER `'wear_event'`. Do not wrap it in any `viewerCanEdit` condition.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -q "WatchDetail.tsx" && echo "TSC-ERROR" || echo "tsc-clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { LikeButton } from '@/components/shared/LikeButton'" src/components/watch/WatchDetail.tsx` succeeds.
    - `grep -q "target={{ type: 'watch', id: watch.id }}" src/components/watch/WatchDetail.tsx` succeeds.
    - `grep -q "viewerId !== undefined && initialLikeState !== undefined" src/components/watch/WatchDetail.tsx` succeeds (conditional render).
    - The `<LikeButton` line is NOT inside a `viewerCanEdit` block: confirm the LikeButton render appears before the first `viewerCanEdit &&` block in file order (visible to all viewers, D-09).
    - `grep -c "wear_event" src/components/watch/WatchDetail.tsx` returns 0.
    - `npx tsc --noEmit` produces no error referencing `WatchDetail.tsx`.
  </acceptance_criteria>
  <done>WatchDetail accepts viewerId + initialLikeState and renders a LikeButton (watch target) under the title block, visible to all viewers and separate from owner-only actions.</done>
</task>

<task type="auto">
  <name>Task 2: Hydrate like-state in watch/[id]/page.tsx and pass to WatchDetail</name>
  <files>src/app/watch/[id]/page.tsx</files>
  <read_first>
    - src/app/watch/[id]/page.tsx (file being edited — getCurrentUser() at L24, Promise.all at L25-29, the `if (!result) notFound()` guard L31-33, the `<WatchDetail .../>` call L73-80)
    - src/data/reactions.ts (getLikesForTargetCached signature added in 56-01)
    - src/components/watch/WatchDetail.tsx (the new viewerId + initialLikeState props from Task 1)
    - .planning/phases/56-like-ui/56-PATTERNS.md (watch/[id]/page.tsx section — import addition, hydration placement, prop additions)
  </read_first>
  <action>
    Add import: `import { getLikesForTargetCached } from '@/data/reactions'`.

    After the `if (!result) notFound()` guard (L31-33) and the `const { watch, isOwner } = result` destructure (L35) — so `watch.id` is available and we know the page did not 404 — add: `const likeState = await getLikesForTargetCached(user.id, { type: 'watch', id })`. (`user.id` is always a string on this auth-only route; no `'__anon__'` sentinel needed here.) Place this read BEFORE the verdict computation block so it is not entangled with the conditional verdict logic; a plain sequential `await` is fine (it is a single cached aggregate read).

    Extend the `<WatchDetail .../>` call (L73-80) with two new props: `viewerId={user.id}` and `initialLikeState={{ liked: likeState.viewerHasLiked, count: likeState.count }}`. Leave all existing props (`watch`, `collection`, `preferences`, `lastWornDate`, `viewerCanEdit`, `verdict`) unchanged.

    Use `type: 'watch'` in the target — NEVER `'wear_event'`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -q "watch/\[id\]/page.tsx\|watch.*page.tsx" && echo "TSC-ERROR" || echo "tsc-clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { getLikesForTargetCached } from '@/data/reactions'" "src/app/watch/[id]/page.tsx"` succeeds.
    - `grep -q "getLikesForTargetCached(user.id, { type: 'watch', id })" "src/app/watch/[id]/page.tsx"` succeeds.
    - `grep -q "viewerId={user.id}" "src/app/watch/[id]/page.tsx"` and `grep -q "initialLikeState={{ liked: likeState.viewerHasLiked, count: likeState.count }}" "src/app/watch/[id]/page.tsx"` both succeed.
    - The `const likeState = await getLikesForTargetCached` line appears AFTER the `if (!result)` guard (so watch.id is non-null).
    - `grep -c "wear_event\|__anon__" "src/app/watch/[id]/page.tsx"` returns 0 (no sentinel on the auth-only route).
    - `npx tsc --noEmit` produces no error referencing this page.
  </acceptance_criteria>
  <done>watch/[id]/page.tsx hydrates {liked, count} via getLikesForTargetCached and passes viewerId + initialLikeState to WatchDetail; type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| watch/[id]/page.tsx (server, auth-resolved) → WatchDetail island | `user.id` is resolved by `getCurrentUser()` in the page (uncached scope) and passed down as a prop; the page throws for anon, so no anon path reaches WatchDetail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-05 | Information Disclosure | initial like-state hydration on /watch/[id] | accept | Cache isolation is owned by `getLikesForTargetCached` (SEC-05, modeled in Plan 56-01 T-56-01). This page passes the authenticated `user.id` as the viewer arg, so the per-viewer cache key is correct. No new surface. |
| T-56-06 | Elevation of Privilege | LikeButton visible to non-owners on owned/wishlist watches | accept | Intended per GATE-02 (likes open to all authenticated viewers, including wishlist). The button is deliberately outside the `viewerCanEdit` gate; the Phase 55 action enforces auth + idempotency server-side. No new surface — UI consumes a secured backend. |
</threat_model>

<verification>
- `npx tsc --noEmit` — no type errors in either touched file.
- `grep -rc "wear_event" src/components/watch/WatchDetail.tsx "src/app/watch/[id]/page.tsx"` — both 0.
- Manual (post-merge, browser): on `/watch/[id]` for an owned watch AND a wishlist watch, the heart renders under the title for any logged-in viewer; clicking toggles fill + count; the count is hidden when zero (LIKE-01, LIKE-04). The owner also sees and can use it (D-09).
</verification>

<success_criteria>
- A LikeButton appears under the brand/model title on /watch/[id] for every authenticated viewer regardless of status (LIKE-01, GATE-02, D-03).
- Initial liked/count are server-hydrated via the cached read; the button is separate from owner-only actions and interactive for the owner (D-09).
- No `'wear_event'` and no anon sentinel appear on this auth-only route.
</success_criteria>

<output>
After completion, create `.planning/phases/56-like-ui/56-02-SUMMARY.md`
</output>
