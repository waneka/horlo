---
phase: 56-like-ui
plan: 03
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/components/wear/WearDetailHero.tsx
  - src/components/wear/WearPhotoClient.tsx
  - src/components/wear/WearDetailMetadata.tsx
  - src/app/wear/[wearEventId]/page.tsx
autonomous: true
requirements: [LIKE-02, LIKE-03, LIKE-04]
tags: [likes, wear-detail, css-overlay, cache-components]

must_haves:
  truths:
    - "Any authenticated viewer can like/unlike a wear post from a footer action row at /wear/[wearEventId]; the count shows next to the heart and hides at zero — LIKE-02, LIKE-04, D-04"
    - "An anon viewer sees the heart + count; clicking bounces to /login?next=<pathname> via the LikeButton's null-viewer path — LIKE-02, D-10"
    - "Avatar + username + relative timestamp overlay the photo top-left; brand/model overlay bottom-left; the watch thumbnail is removed; the note renders as a caption below the photo (hidden when null) — D-05, D-06, D-07"
    - "All 4 photo-container callsites missing `relative` get it (WearPhotoClient ~L68 + ~L81, WearDetailHero ~L33 + ~L46); the L94 site already has it and is not double-applied — D-08"
    - "The overlay chain renders on the no-photo bg-muted fallback (text-foreground), not only on signed photos, and the old centered {brand} {model} fallback text is removed — D-08"
  artifacts:
    - path: "src/components/wear/WearDetailHero.tsx"
      provides: "relative photo container + photo overlays (no-photo fallback path)"
      contains: "absolute inset-x-0"
    - path: "src/components/wear/WearPhotoClient.tsx"
      provides: "relative on 2 fallback containers + photo overlays"
      contains: "absolute inset-x-0"
    - path: "src/components/wear/WearDetailMetadata.tsx"
      provides: "gutted to note-only caption"
      contains: "note"
    - path: "src/app/wear/[wearEventId]/page.tsx"
      provides: "anon-guarded like hydration + footer action row with LikeButton"
      contains: "getLikesForTargetCached"
  key_links:
    - from: "src/app/wear/[wearEventId]/page.tsx"
      to: "getLikesForTargetCached"
      via: "await getLikesForTargetCached(viewerId ?? '__anon__', { type: 'wear', id: wearEventId })"
      pattern: "getLikesForTargetCached"
    - from: "src/app/wear/[wearEventId]/page.tsx"
      to: "src/components/shared/LikeButton"
      via: "rendered in footer action row, target type 'wear'"
      pattern: "LikeButton"
    - from: "src/components/wear/WearPhotoClient.tsx"
      to: "photo overlay"
      via: "absolute inset-x-0 top-0/bottom-0 inside a relative container"
      pattern: "absolute inset-x-0"
---

<objective>
Execute the user-directed wear detail redesign (D-04..D-08) that hosts the like control: move collector identity (avatar + username + timestamp) to a top-left photo overlay and brand/model to a bottom-left overlay, remove the watch thumbnail, gut `WearDetailMetadata` to a note-only caption, add a footer action row (`[reserved comment slot] [LikeButton]`), and patch the 4 photo-container callsites missing `relative`. Hydrate like-state with the anon sentinel so the count shows for anon viewers (LIKE-02, LIKE-04).

Purpose: Delivers the wear half of the phase goal and gives the like control a coherent home. This plan owns the Phase-30-class CSS-chain risk (overlays on a positioned container, including the no-photo fallback).
Output: edited `WearDetailHero.tsx`, `WearPhotoClient.tsx`, `WearDetailMetadata.tsx`, and `wear/[wearEventId]/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-like-ui/56-CONTEXT.md
@.planning/phases/56-like-ui/56-RESEARCH.md
@.planning/phases/56-like-ui/56-PATTERNS.md
@.planning/phases/56-like-ui/56-UI-SPEC.md

<interfaces>
<!-- Contracts produced by Plan 56-01 (Wave 1) + existing wear data. Read directly. -->

From src/components/shared/LikeButton.tsx (created in 56-01):
```typescript
interface LikeButtonProps { viewerId: string | null; target: { type: 'watch' | 'wear'; id: string }; initialLiked: boolean; initialCount: number }
```

From src/data/reactions.ts (added in 56-01):
```typescript
export async function getLikesForTargetCached(viewerId: string, target: LikeTarget): Promise<{ count: number; viewerHasLiked: boolean }>
// viewerId must be a non-null string. For the anon wear path pass '__anon__'
// (SQL bool_or(userId='__anon__') resolves viewerHasLiked=false; count is still correct).
```

From src/data/wearEvents.ts getWearEventByIdForViewer return (existing — `wear.*`):
```typescript
{ username: string | null; displayName: string | null; avatarUrl: string | null;
  brand: string; model: string; note: string | null; createdAt: Date;
  photoUrl: string | null; watchImageUrl: string | null }
```

From src/components/profile/AvatarDisplay.tsx (existing):
```typescript
function AvatarDisplay(props: { avatarUrl: string|null; displayName: string|null; username: string; size?: 40|64|96; className?: string })
// NOTE: size only accepts 40 | 64 | 96 — use size={40} on the overlay (32 is NOT a valid value).
```

From src/lib/timeAgo.ts: `export function timeAgo(input: string | Date, now?: Date): string`

From src/app/wear/[wearEventId]/page.tsx (existing):
```typescript
// viewerId: string | null  (anon allowed — getCurrentUser in try/catch, UnauthorizedError swallowed)
// const wear = await getWearEventByIdForViewer(viewerId, wearEventId)  → has the fields above
// <article className="flex flex-col gap-4 pt-4">
//   <Suspense><WearPhotoStreamed photoUrl watchImageUrl brand model altText/></Suspense>
//   <WearDetailMetadata username displayName avatarUrl brand model watchImageUrl note createdAt/>
// </article>
// WearPhotoStreamed (server child) mints the signed URL and branches:
//   signedUrl → <WearPhotoClient signedUrl altText watchImageUrl brand model/>
//   else      → <WearDetailHero watchImageUrl brand model altText/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Photo overlays + 4 `relative` callsites + gut WearDetailMetadata</name>
  <files>src/components/wear/WearDetailHero.tsx, src/components/wear/WearPhotoClient.tsx, src/components/wear/WearDetailMetadata.tsx</files>
  <read_first>
    - src/components/wear/WearDetailHero.tsx (2 containers: watchImageUrl path L33, no-photo fallback L46 with centered {brand}{model} text L49-51 to remove)
    - src/components/wear/WearPhotoClient.tsx (3 containers: L68 failed+watchImageUrl, L81 failed+no-photo with centered text L84-86 to remove, L94 signed-URL path which ALREADY ENDS WITH `relative` — do NOT touch/duplicate)
    - src/components/wear/WearDetailMetadata.tsx (current 3-section layout: collector row L46-78, watch row L80-100, note L102-105 — only the note survives)
    - src/components/profile/AvatarDisplay.tsx (size prop accepts 40|64|96 only)
    - src/lib/timeAgo.ts (timeAgo signature)
    - .planning/phases/56-like-ui/56-UI-SPEC.md (Photo container CSS chain table + Top/Bottom overlay specs + No-photo fallback correctness + WearDetailMetadata redesign)
    - .planning/phases/56-like-ui/56-PATTERNS.md (WearDetailHero / WearPhotoClient / WearDetailMetadata sections — exact before/after class strings + overlay markup)
  </read_first>
  <action>
    Define a single shared overlay sub-component to avoid duplicating ~40 lines across the two photo surfaces. Create it co-located (export from `WearDetailHero.tsx`, import into `WearPhotoClient.tsx`) — call it `WearPhotoOverlays`. Props: `{ username: string | null; displayName: string | null; avatarUrl: string | null; createdAt: Date; brand: string; model: string; hasPhoto: boolean }`. It renders BOTH absolute overlay divs.

    Top overlay: `<div className="absolute inset-x-0 top-0 z-10 pointer-events-none" style={{ background: 'linear-gradient(to bottom, rgba(0,0,0,0.45) 0%, transparent 40%)' }}>` wrapping `<div className="flex items-center gap-2 p-3 pointer-events-auto">` containing `<AvatarDisplay avatarUrl={avatarUrl} displayName={displayName} username={username ?? '?'} size={40} />`, a `<Link href={\`/u/${username}\`} className={cn('text-sm font-semibold hover:opacity-80', textClass)}>{displayName ?? username}</Link>` (render the link only when `username` is non-null; otherwise a plain span "Unknown collector"), a `·` separator span and `{timeAgo(createdAt)}` span. Use `size={40}` (NOT 32 — invalid).

    Bottom overlay: `<div className="absolute inset-x-0 bottom-0 z-10 pointer-events-none" style={{ background: 'linear-gradient(to top, rgba(0,0,0,0.45) 0%, transparent 40%)' }}>` wrapping `<div className="flex flex-col p-3">` with brand `<span className={cn('text-sm font-semibold', textClass)}>{brand}</span>` and model `<span className={cn('text-sm', textClass)}>{model}</span>`.

    `textClass = hasPhoto ? 'text-white' : 'text-foreground'` (D-08 — `text-foreground` on the muted no-photo fallback; `text-white` over a photo scrim). Imports for this overlay: `Link` from `next/link`, `AvatarDisplay` from `@/components/profile/AvatarDisplay`, `timeAgo` from `@/lib/timeAgo`, `cn` from `@/lib/utils`.

    WearDetailHero edits — both containers gain overlay content and need new props (`username`, `displayName`, `avatarUrl`, `createdAt`; `brand`/`model` already accepted):
    - L33 (watchImageUrl path): change class to `"relative w-full aspect-[4/5] overflow-hidden bg-muted md:rounded-lg md:max-w-[600px] md:mx-auto"` and render `<WearPhotoOverlays ... hasPhoto={true} />` as the last child inside the div (after the `<img>`).
    - L46 (no-photo fallback): change class to start with `relative` (keep the rest), REMOVE the centered `<span>{brand} {model}</span>` (L49-51), and render `<WearPhotoOverlays ... hasPhoto={false} />` inside. Keep the `aria-label={\`No photo — ${brand} ${model}\`}`.

    WearPhotoClient edits — add the same new props (`username`, `displayName`, `avatarUrl`, `createdAt`) and import `WearPhotoOverlays` from `./WearDetailHero`:
    - L68 (failed + watchImageUrl): prepend `relative ` to the class; render `<WearPhotoOverlays ... hasPhoto={true} />` as last child.
    - L81 (failed + no-photo): prepend `relative `; REMOVE the centered `<span>{brand} {model}</span>` (L84-86); render `<WearPhotoOverlays ... hasPhoto={false} />` inside; keep the aria-label.
    - L94 (signed-URL happy path): ALREADY HAS `relative` (at the END of the class string) — do NOT add a second `relative`. Render `<WearPhotoOverlays ... hasPhoto={true} />` as last child (after the `<img>`), inside the existing `relative` container.

    WearDetailMetadata restructure: reduce to note-only. New signature `export function WearDetailMetadata({ note }: { note: string | null }): JSX.Element | null`. `if (!note) return null`. Return `<p className="text-sm text-foreground whitespace-pre-wrap px-4 pt-3 md:max-w-[600px] md:mx-auto">{note}</p>`. Remove the now-unused imports (`Link`, `Image`, `AvatarDisplay`, `getSafeImageUrl`, `timeAgo`) and the dropped props (`username`, `displayName`, `avatarUrl`, `brand`, `model`, `watchImageUrl`, `createdAt`).

    Use `'wear'` only — no `'wear_event'` anywhere.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -qE "WearDetailHero|WearPhotoClient|WearDetailMetadata" && echo "TSC-ERROR" || echo "tsc-clean"</automated>
  </verify>
  <acceptance_criteria>
    - All container class strings on the 4 patched callsites begin with `relative`: run `grep -nE "aspect-\[4/5\]" src/components/wear/WearDetailHero.tsx src/components/wear/WearPhotoClient.tsx` and confirm every matched container line includes the word `relative`. (WearDetailHero: 2 lines; WearPhotoClient: 3 lines, the L94 one already had it.)
    - `grep -c "relative.*aspect-\[4/5\]\|aspect-\[4/5\].*relative" src/components/wear/WearPhotoClient.tsx` returns 3 (no duplicate `relative` on L94).
    - The old centered fallback text is gone: `grep -c "text-sm font-semibold text-muted-foreground" src/components/wear/WearDetailHero.tsx src/components/wear/WearPhotoClient.tsx` — neither file still renders the centered `{brand} {model}` span inside the fallback (0 occurrences of that exact muted-fallback span class within the fallback divs).
    - `grep -q "absolute inset-x-0 top-0 z-10 pointer-events-none" src/components/wear/WearDetailHero.tsx` and `grep -q "absolute inset-x-0 bottom-0 z-10 pointer-events-none" src/components/wear/WearDetailHero.tsx` both succeed.
    - `grep -q "export function WearPhotoOverlays" src/components/wear/WearDetailHero.tsx` and `grep -q "import { WearPhotoOverlays } from './WearDetailHero'" src/components/wear/WearPhotoClient.tsx` both succeed.
    - `grep -q "hasPhoto ? 'text-white' : 'text-foreground'" src/components/wear/WearDetailHero.tsx` succeeds (D-08 fallback color swap).
    - WearDetailMetadata: `grep -q "export function WearDetailMetadata" src/components/wear/WearDetailMetadata.tsx` and the signature destructures only `{ note }` (confirm `grep -c "username\|avatarUrl\|brand\|watchImageUrl" src/components/wear/WearDetailMetadata.tsx` returns 0).
    - `size={40}` used for the overlay AvatarDisplay (not 32): `grep -c "size={32}" src/components/wear/WearDetailHero.tsx` returns 0.
    - `grep -rc "wear_event" src/components/wear/WearDetailHero.tsx src/components/wear/WearPhotoClient.tsx src/components/wear/WearDetailMetadata.tsx` — all 0.
    - `npx tsc --noEmit` produces no error in these three files.
  </acceptance_criteria>
  <done>All 4 fallback callsites are `relative`; top/bottom overlays render via a shared WearPhotoOverlays sub-component on photo and no-photo paths (with text-foreground on muted fallback); WearDetailMetadata is gutted to a note-only caption.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire wear page — anon-guarded hydration, overlay props threading, footer action row</name>
  <files>src/app/wear/[wearEventId]/page.tsx</files>
  <read_first>
    - src/app/wear/[wearEventId]/page.tsx (file being edited — viewerId try/catch L39-47, getWearEventByIdForViewer L49, article tree L56-78, WearPhotoStreamed server child L87-131 that branches WearPhotoClient vs WearDetailHero)
    - src/data/reactions.ts (getLikesForTargetCached signature from 56-01)
    - src/components/shared/LikeButton.tsx (LikeButton props from 56-01)
    - src/components/wear/WearDetailHero.tsx + WearPhotoClient.tsx (the new overlay props from Task 1 — both now require username/displayName/avatarUrl/createdAt)
    - src/components/wear/WearDetailMetadata.tsx (now takes only { note } after Task 1)
    - .planning/phases/56-like-ui/56-RESEARCH.md (Wear page hydration anon-guard + footer action row code examples)
    - .planning/phases/56-like-ui/56-UI-SPEC.md (Wear footer action row spec — exact classes + reserved left slot)
    - .planning/phases/56-like-ui/56-PATTERNS.md (wear/[wearEventId]/page.tsx section)
  </read_first>
  <action>
    Add imports: `import { getLikesForTargetCached } from '@/data/reactions'` and `import { LikeButton } from '@/components/shared/LikeButton'`.

    After `const wear = await getWearEventByIdForViewer(...)` and the `if (!wear) notFound()` guard (L49-50), define `const ANON_SENTINEL = '__anon__'` and `const likeState = await getLikesForTargetCached(viewerId ?? ANON_SENTINEL, { type: 'wear', id: wearEventId })`. (The `?? '__anon__'` is REQUIRED because `getLikesForTargetCached` expects a non-null string and `viewerId` is `string | null` for anon — see landmine. The count is still correct for anon; `viewerHasLiked` resolves false.)

    Thread the overlay content props through `WearPhotoStreamed` into both `WearPhotoClient` and `WearDetailHero`. `WearPhotoStreamed` already receives `brand`/`model`; ADD params `username`, `displayName`, `avatarUrl`, `createdAt` to its signature and pass `wear.username`, `wear.displayName`, `wear.avatarUrl`, `wear.createdAt` from the article callsite. Inside `WearPhotoStreamed`, forward all four to `<WearPhotoClient .../>` (signedUrl branch) and `<WearDetailHero .../>` (no-signed-url branch).

    Update `<WearDetailMetadata>` callsite to pass ONLY `note={wear.note}` (drop username/displayName/avatarUrl/brand/model/watchImageUrl/createdAt — they moved to the overlay).

    Add the footer action row as the LAST child of the `<article>` (after `<WearDetailMetadata>`, before `</article>`): `<div className="flex items-center px-4 py-3 border-t border-border md:max-w-[600px] md:mx-auto"><div className="flex-1 min-h-[44px]" aria-hidden /><LikeButton viewerId={viewerId} target={{ type: 'wear', id: wearEventId }} initialLiked={likeState.viewerHasLiked} initialCount={likeState.count} /></div>`. The `flex-1 min-h-[44px]` left slot is empty reserved space for Phase 57's comment input — NO placeholder text, NO border, NO visible content. `viewerId` (string|null) passes straight through to LikeButton, which handles the anon bounce.

    Leave the `<article className="flex flex-col gap-4 pt-4">` class unchanged. Use `type: 'wear'` — NEVER `'wear_event'`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -q "wear/\[wearEventId\]/page.tsx\|wearEventId.*page.tsx" && echo "TSC-ERROR" || echo "tsc-clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "getLikesForTargetCached(viewerId ?? ANON_SENTINEL, { type: 'wear', id: wearEventId })" "src/app/wear/[wearEventId]/page.tsx"` succeeds (anon sentinel applied).
    - `grep -q "const ANON_SENTINEL = '__anon__'" "src/app/wear/[wearEventId]/page.tsx"` succeeds.
    - `grep -q "target={{ type: 'wear', id: wearEventId }}" "src/app/wear/[wearEventId]/page.tsx"` and `grep -q "viewerId={viewerId}" "src/app/wear/[wearEventId]/page.tsx"` both succeed.
    - Footer row present: `grep -q "flex items-center px-4 py-3 border-t border-border" "src/app/wear/[wearEventId]/page.tsx"` and `grep -q 'className="flex-1 min-h-\[44px\]" aria-hidden' "src/app/wear/[wearEventId]/page.tsx"` both succeed.
    - `<WearDetailMetadata>` now passes only the note: `grep -A1 "<WearDetailMetadata" "src/app/wear/[wearEventId]/page.tsx"` shows `note={wear.note}` and NOT `username=`/`avatarUrl=`/`watchImageUrl=`.
    - `WearPhotoStreamed` forwards overlay props: `grep -c "username=\|displayName=\|avatarUrl=\|createdAt=" "src/app/wear/[wearEventId]/page.tsx"` returns >= 6 (passed to WearPhotoStreamed + forwarded to both child branches).
    - `grep -c "wear_event" "src/app/wear/[wearEventId]/page.tsx"` returns 0.
    - `npx tsc --noEmit` produces no error referencing this page.
  </acceptance_criteria>
  <done>The wear page hydrates like-state with the anon sentinel, threads overlay content to both photo branches, renders WearDetailMetadata as note-only, and renders the footer action row with LikeButton (wear target) — anon viewers see the count and bounce to /login on click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/auth viewer browser → wear page (anon-allowed) | `viewerId` is `string | null`; the page resolves auth in a try/catch (uncached) and passes `viewerId` to LikeButton and `viewerId ?? '__anon__'` to the cached read |
| Next.js cache layer → per-viewer wear like read | `getLikesForTargetCached` is keyed by the viewer arg; the `'__anon__'` sentinel forms its own shared anon entry (no per-user liked-state, correct) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-07 | Information Disclosure | anon wear like hydration via `'__anon__'` sentinel | accept | The `'__anon__'` sentinel can never equal a real UUID, so `bool_or(userId='__anon__')` is always false — anon never sees a per-user liked state. The shared `viewer:__anon__:reactions` cache entry holds only the public count, which is non-sensitive. Per-viewer isolation for real users is owned by SEC-05 (Plan 56-01 T-56-01). |
| T-56-08 | Tampering | LikeButton on anon wear page → action | accept | Anon clicks never reach `toggleLikeAction` — the LikeButton null-viewer branch routes to `/login?next=`. Authenticated submissions are re-validated server-side (Phase 55). No new surface. |
| T-56-09 | Information Disclosure | photo overlay rendering collector identity | accept | Overlay shows `username`/`displayName`/`avatarUrl` already returned by the visibility-gated `getWearEventByIdForViewer` DAL (the page 404s on denied wears). The redesign relocates already-authorized data; it exposes nothing new. |
</threat_model>

<verification>
- `npx tsc --noEmit` — no type errors across the 4 touched files.
- Structural (D-08, the Phase-30 regression guard): `grep -nE "aspect-\[4/5\]" src/components/wear/WearDetailHero.tsx src/components/wear/WearPhotoClient.tsx` — every photo-container line includes `relative`; WearPhotoClient L94 is not double-`relative`.
- `grep -rc "wear_event" src/components/wear/ "src/app/wear/[wearEventId]/page.tsx"` — all 0.
- Manual (post-merge, MANDATORY — clear `.next/` first per [[project_turbopack_next_cache_stale_css]]): `rm -rf .next && npm run dev`. (a) Visit a wear page WITH a signed photo: avatar+username+timestamp render top-left and brand/model bottom-left over scrims, photo at 4:5; footer heart at the right toggles + count hides at zero. (b) Visit a wear page on the no-photo `bg-muted` fallback: the SAME overlays render with `text-foreground` and the old centered `{brand} {model}` text is gone. (c) Logged out: heart + count visible, click bounces to `/login?next=`.
- Run `npm test` (full suite) at wave merge — confirms LikeButton unit suite still green after wiring.
</verification>

<success_criteria>
- A footer action row at /wear/[wearEventId] hosts a LikeButton (wear target) reachable by every viewer; count shows next to the heart and hides at zero (LIKE-02, LIKE-04, D-04).
- Anon viewers see the heart + count and bounce to /login on click (LIKE-02, D-10).
- Photo overlays (collector top-left, brand/model bottom-left) render on BOTH the signed-photo and no-photo fallback paths; thumbnail removed; note is a caption below the photo; all 4 `relative` callsites patched without double-applying L94 (D-05/06/07/08).
- No `'wear_event'` anywhere; anon sentinel correctly applied for the null-viewer hydration read.
</success_criteria>

<output>
After completion, create `.planning/phases/56-like-ui/56-03-SUMMARY.md`
</output>
