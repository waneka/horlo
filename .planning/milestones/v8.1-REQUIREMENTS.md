# Requirements Archive: v8.1 Add-Watch Polish

**Archived:** 2026-05-30
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: Horlo v8.1 Add-Watch Polish

**Defined:** 2026-05-29
**Core Value:** A collector can evaluate any watch against their collection and get a meaningful, preference-aware answer about whether it adds something or just duplicates what they already own.

**Milestone Goal:** Close the 6 prod-UAT-captured defects in the v8.0 search-first add-watch flow so the experience matches its design intent — search relevance works for multi-token queries, keyboard navigation works in the combobox, the owned-redirect resolves, the DupeBanner gate doesn't confuse users with stuck "Saving..." copy, and mobile inputs don't trigger iOS auto-zoom.

**Pattern:** Decimal polish milestone (matches v4.1, v5.1, v5.2 history). Pure subtraction-of-defects against shipped v8.0 code — no new features.

## v8.1 Requirements

Each defect from v8.0's prod UAT walk is captured as a user-centric requirement. The REQ-IDs reuse the defect identifiers from `.planning/milestones/v8.0-phases/{69,70}-*/...-UAT.md` for traceability.

### Search (3 requirements)

- [x] **SRCH-01**: User can search for a watch using brand + model tokens (e.g. "Brut Datejust", "Timex Weekender") and receive matching catalog results — multi-token queries return the same coverage as single-token brand queries when the catalog has the row
- [x] **SRCH-02**: User can navigate search results with the keyboard — Up/Down arrows move focus through result rows, Enter selects the focused row to fire `onPick` (advances flow per Phase 70 owned-redirect / confirm branching)
- [x] **SRCH-03**: User can click the "Not finding it? Add manually" footer row when search results are present and have the inline `StructuredEntryPanel` expand with the parsed query pre-seeded (same behavior as the no-match auto-mount path)

### Routing (1 requirement)

- [x] **ROUTE-01**: User can click an "In collection" search result and be redirected to the watch detail page `/w/[ref]` which renders successfully — no 404. Works for any owned watch regardless of which catalog row backs it (consistent with the v7.0 Phase 59 unified-route contract)

### DupeBanner UX (1 requirement)

- [x] **DUPE-04**: User sees a clear, non-ambiguous reason why the ConfirmStep primary CTA is disabled when DupeBanner is mounted — the CTA either hides entirely while the banner is active (verifier's "alternative fix" per 70-VERIFICATION.md WR-01), or differentiates "gated by banner" from "saving in progress" via distinct copy. The current "Saving..." copy on a stuck button does not satisfy this requirement.

### Mobile (1 requirement)

- [x] **MOB-01**: User can focus an input field on iOS Safari (mobile) without the page auto-zooming. Applies app-wide, not just to add-watch — the fix is a global CSS rule on `input, textarea, select` ensuring font-size ≥ 16px. Verify that intentional zoom (pinch-zoom) still works for users who need it; do NOT use `maximum-scale=1` on the viewport meta as a workaround (accessibility regression).

## Future Requirements (Deferred)

- **SEED-009 v9.0 Catalog Expansion** — still scheduled as the next major milestone after v8.1
- **SEED-005 v6.0 Market Value** — future, after v9.0 (needs SEED-007 pricing API spike first)
- **SEED-014 Cache Components canonical sweep** — broader codebase audit, not v8.1 scope
- **10 prior-milestone quick_tasks** (260413, 260421-srx, 260424, 260513, 260519 series) — remain in `.planning/quick/` as dormant backlog; most superseded by later phases. See STATE.md Deferred Items for full enumeration.

## Out of Scope

Explicitly excluded from v8.1 to prevent scope creep:

| Feature | Reason |
|---------|--------|
| New add-watch capabilities (beyond defect fixes) | v8.1 is strict polish — no new flows, no new states. v8.0 closed the milestone scope. |
| `/api/extract-watch` mode refactor | The structured + URL modes both work as designed. No need to revisit Phase 66. |
| Catalog row taste enrichment changes | Phase 67 / Phase 19.5 work stays as-is. Not in any defect. |
| Activity feed / overlap notification changes | DUPE-03 wishlist→collection fan-out works per UAT Test 3 pass. Not in scope. |
| Add-watch performance optimization | No defect captured against latency. Defer to a future polish if it becomes a real complaint. |
| Adjacency to `260421-rdb-fix-404-on-watch-detail-pages-for-watche` quick_task | Marked OBSOLETE at v8.1 kickoff — its target route (`/watch/[id]/page.tsx`) was deleted in v7.0 Phase 59. NOT folded into ROUTE-01 despite similar filename. |

## Requirement-to-Phase Traceability

| REQ-ID | Phase | Status |
|--------|-------|--------|
| SRCH-01 | Phase 72 | Complete |
| SRCH-02 | Phase 72 | Complete |
| SRCH-03 | Phase 72 | Complete |
| ROUTE-01 | Phase 73 | Complete |
| DUPE-04 | Phase 74 | Complete |
| MOB-01 | Phase 74 | Complete |

---

*Source defects: `.planning/milestones/v8.0-phases/69-searchentry-structuredentrypanel-cache-hygiene/69-UAT.md` (SRCH-02, SRCH-03) + `.planning/milestones/v8.0-phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-UAT.md` (SRCH-01, ROUTE-01, DUPE-04, MOB-01). Each UAT file has a `Gaps` section with the verbatim user report + hypothesis for the planner.*
