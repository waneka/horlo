# Milestone v4.1: Polish & Patch

**Status:** ✅ SHIPPED 2026-05-05
**Phases:** 27-31
**Total Plans:** 21
**Audit:** [`tech_debt`](v4.1-MILESTONE-AUDIT.md) — 12/12 requirements satisfied at code level; 1 NEW finding (DEBT-09) deferred to v4.2/v5.0

## Overview

Clear v4.0 carryover with bug fixes, UX tweaks, and small features so v5.0 (Discovery North Star) starts clean. No new domain. All items extend existing v4.0 patterns (WatchCard, useFormFeedback, UserMenu, UI tabs, WYWT capture pipeline, Phase 25/26 verification process, AddWatchFlow state machine).

5 phases over 2 days. 21 plans (5 + 5 + 6 + 2 + 3). 1 quick task (FORM-04 Gap 3 useUrlExtractCache). 1 post-ship hotfix (Phase 30 `h-full` on `<video>`). 71 source files changed, +5,044 / -190 lines (excluding `.planning/`). Tag: v4.1.

## Phases

### Phase 27: Watch Card & Collection Render Polish

**Goal:** Collection and wishlist owners can see and reorder their watches in a denser, price-aware grid that reflects the order they care about.
**Depends on:** v4.0 (WatchCard, Collection/Wishlist tabs, watches table schema)
**Requirements:** WISH-01, VIS-07, VIS-08
**Plans:** 5 plans across 4 waves

Plans:
- [x] 27-01: Wave 0 RED test scaffolds (7 new + 1 extend)
- [x] 27-02: Schema column + drizzle/supabase migrations + DAL helpers + addWatch/editWatch sort_order; `[BLOCKING]` local drizzle-kit push
- [x] 27-03: reorderWishlist Server Action (Zod .strict, owner-only, ActionResult)
- [x] 27-04: ProfileWatchCard status-driven price line + Image sizes attr; CollectionTabContent grid-cols-2
- [x] 27-05: Drag UX: install @dnd-kit/*, SortableProfileWatchCard with drop-indicator, WishlistTabContent DnD wiring + grid-cols-2; manual UAT

**Verification:** `passed` (4/4 must-haves verified). UAT approved 2026-05-04 across desktop, iOS Safari long-press 250ms, keyboard reorder, failure-path Sonner rollback, non-owner public view, click-through verification, drop-indicator both directions. Code review: 2 BLOCKERs (BR-01 set-completeness check, BR-02 revalidatePath dynamic segment) + 5 WARNINGs all fixed inline.

### Phase 28: Add-Watch Flow & Verdict Copy Polish

**Goal:** Adding a watch lands the user back where they started, gives them a clear next-step link, and the verdict copy reads coherently both as a verdict and as the wishlist-note auto-fill.
**Depends on:** Phase 27 (no hard dependency, but Phase 27 ships the price/grid surface UX-09's success toast links into); v4.0 Phases 20, 20.1, 25 (CollectionFitCard composer, Add-Watch Flow state machine, useFormFeedback hook)
**Requirements:** FIT-06, ADD-08, UX-09
**Plans:** 5 plans across 3 waves

Plans:
- [x] 28-01: FIT-06: verdict copy rewrite + speech-act split (25 literal copy strings; 6 DESCRIPTION_FOR_LABEL rewrites verb-led ≥6 words; 12 rationaleTemplates; RATIONALE_FOR_LABEL fallback table)
- [x] 28-02: UX-09 hook foundation: useFormFeedback successAction extension + suppress short-circuit (D-05/D-06)
- [x] 28-03: ADD-08 server foundation: /watch/new returnTo whitelist + shared destinations.ts module (regex byte-identical to auth-callback regex)
- [x] 28-04: UX-09 inline-commit wiring: /search + /catalog/[id] Wishlist commits switch to Sonner action-slot
- [x] 28-05: ADD-08 callsite append + UX-09 nav-on-commit: 8 entry-points + AddWatchFlow/WatchForm rewrites; D-15 router.refresh() removed from Wishlist commit

**Verification:** `passed` (4/4 must-haves + 22/22 D-decisions + 18/18 artifacts + 8/8 key links + 9/9 critical invariants verified). 85/85 phase-relevant tests pass. Security audit closed all 21 declared threats including open-redirect protection (`//evil.com` and self-loop attacks fall back to default).

### Phase 29: Nav & Profile Chrome Cleanup

**Goal:** The UserMenu, profile tab strip, and Add-Watch flow are tight and predictable — no duplicate Profile affordance, no surprise vertical scroll on the tab strip, and a fresh Add-Watch form on every entry instead of stale data from the prior session.
**Depends on:** v4.0 Phase 25 (avatar dual-affordance shipped Profile as primary path), Phase 20.1 (AddWatchFlow state machine)
**Requirements:** NAV-16, PROF-10, FORM-04
**Plans:** 6 plans across 3 waves + 1 Quick Task

Plans:
- [x] 29-01: FORM-04 Wave 0 RED test scaffolds (NEW AddWatchFlow.test.tsx + extend WatchForm.test.tsx)
- [x] 29-02: NAV-16: delete Profile DropdownMenuItem from UserMenu.tsx; preserve BOTH surrounding separators per UI-SPEC D-01 wording precision; rewrite UserMenu.test.tsx Test 3, delete Test 4
- [x] 29-03: PROF-10: append `overflow-y-hidden pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden` to ProfileTabs.tsx TabsList; assert all 4 utilities in ProfileTabs.test.tsx
- [x] 29-04: FORM-04 implementation: per-request `crypto.randomUUID()` nonce as `<AddWatchFlow key={flowKey}>` in /watch/new/page.tsx; `useLayoutEffect` cleanup-on-hide in AddWatchFlow.tsx (back-nav defense per RESEARCH Pitfall 4); explicit state reset in `handleWishlistConfirm` BEFORE `router.push(dest)` (D-14 defense-in-depth)
- [x] 29-05: FORM-04 Gap 1: migrate useWatchSearchVerdictCache.ts to module-scoped Map (smallest-blast-radius variant of Option A); revises Plan 04 hoisting decision (Option B literally cannot honor D-15 cache-survives-entry contract); adds remount-cache-hit regression test
- [x] 29-06: FORM-04 Gap 2: StrictMode-safe useLayoutEffect cleanup (ref-guarded skip cases for initial idle + form-prefill) so D-16 deep-link prefill survives Next.js 16 dev StrictMode mount/cleanup/mount; rename tests/setup.ts → tests/setup.tsx + global RTL render() StrictMode wrapper (test-infra gap that let Plan 04 regression slip through CI)
- [x] Quick Task FORM-04 Gap 3: `useUrlExtractCache` (module-scoped Map keyed on URL → `{catalogId, extracted, catalogIdError}`) so re-paste skips the `/api/extract-watch` round-trip — closes 29-UAT Test 8 user-observable bottleneck

**Verification:** `passed` (re-verified 2026-05-05 after 3 fix-chain ships). 3/3 must-haves verified at unit-test level; 10/10 UAT items passed in browser across three rounds (round 1: 8 pass + 2 issues; round 2: 1 closed via 29-05; round 3: 1 closed via 29-06; final issue closed via Quick Task Gap 3).

### Phase 30: WYWT Capture Alignment Fix

**Goal:** When a user aligns their wrist with the WYWT camera overlay, the saved photo crops the wrist where the overlay said it would be — not lower in the frame.
**Depends on:** v3.0 Phase 15 (CameraCaptureView, WristOverlaySvg, photo capture pipeline)
**Requirements:** WYWT-22
**Plans:** 2 plans across 2 waves + 1 post-ship hotfix

Plans:
- [x] 30-01: D-07 RED test scaffold: create tests/components/wywt/CameraCaptureView.test.tsx with 4 pure-math assertions on the not-yet-exported computeObjectCoverSourceRect helper
- [x] 30-02: Implement WYSIWYG fix in src/components/wywt/CameraCaptureView.tsx: wrapperRef + aspect-square wrapper class + computeObjectCoverSourceRect named export + extended readiness guard + 9-arg drawImage capture math
- [x] Post-ship hotfix `2dd7377`: Add `h-full` to `<video>` so object-cover engages on iPhone — initial ship had black bar in preview + wrist at bottom of saved JPEG; root cause was `<video>` keeping intrinsic 16:9 aspect inside the aspect-square wrapper, object-cover had nothing to crop, WYSIWYG capture math assumed a crop that never happened

**Verification:** `passed` (3/3 success criteria + iOS Safari UAT post-hotfix). Owner re-tested on iOS Safari post-deploy 2026-05-05: preview square, no black bar, wrist centered in saved photo. Locked files (`WristOverlaySvg.tsx`, `ComposeStep.tsx`, `src/lib/exif/strip.ts`) confirmed UNTOUCHED via `git diff` empty output.

### Phase 31: v4.0 Verification Backfill

**Goal:** Phase 23 and Phase 24 each have a phase-level VERIFICATION.md that goal-backward-audits the shipped code, closing the v4.0 verification asymmetry recorded in the milestone audit.
**Depends on:** v4.0 Phase 23 + Phase 24 (already shipped; this phase audits them retroactively)
**Requirements:** DEBT-07, DEBT-08
**Plans:** 3 plans across 2 waves

Plans:
- [x] 31-01: Write phase-level 23-VERIFICATION.md (Phase 23 goal-backward audit; FEAT-07 GAP framing; 5 UAT carryover; sub-plan citation for SET-09/11/12) — score `4/5 + 1 GAP`
- [x] 31-02: Write phase-level 24-VERIFICATION.md (Phase 24 goal-backward audit; 5/5 success criteria PASS; 7 REQ-IDs SATISFIED; 46 tests across 5 files) — score `5/5 PASS`
- [x] 31-03: Append `## Closure` section to v4.0-MILESTONE-AUDIT.md (cite both new VERIFICATION.md files; preserve audit body byte-equal per D-09/D-10) — verified zero deletions

**Verification:** `passed` (3/3 success criteria + 12/12 D-decisions + byte-equality invariant intact). Documentation-only phase; no production code touched. **Phase 31 surfaced new finding `DEBT-09`**: Phase 23 SUMMARY claimed `notesPublic` Zod field + `revalidatePath('/u/...')` shipped via commit `4d362ff`, but neither lives in `src/app/actions/watches.ts` on `main` — that commit is NOT an ancestor of HEAD. RED test scaffold `tests/actions/watches.notesPublic.test.ts` 0/4 PASS. Surfaced-as-debt; remediation deferred to v4.2 / v5.0 carryover.

---

## Milestone Summary

**Decimal phases:** None.

**Key Decisions:**

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Three-layer FORM-04 reset (key prop + useLayoutEffect cleanup + commit-time reset) | Each layer covers a navigation class JSDOM cannot replay (per-request mount, Activity-preservation back-nav, post-commit forward-nav); defense-in-depth so a single regression doesn't break the contract | ✓ Good — shipped Phase 29 Plan 04; survived 3 UAT rounds with 2 regressions caught by Plans 29-05 + 29-06 + Quick Task Gap 3 |
| Module-scoped Map for cross-remount caches (verdict + URL extract) | `useState`-backed caches die on `key={flowKey}` remount; module scope is the smallest-blast-radius variant of "survive remount" without re-architecting state ownership | ✓ Good — shipped Plan 29-05 (verdict cache) + Quick Task Gap 3 (URL extract cache); revisit on signOut invalidation as v5.0+ polish |
| Speech-act split: `rationalePhrasings` (1st-person) vs `contextualPhrasings` (verdict-to-user) | Same data feeding two surfaces with different speech acts produces incoherent copy; separating them at type level forces template authors to consider both contexts | ✓ Good — shipped Phase 28 (FIT-06); WishlistRationalePanel now reads `verdict.rationalePhrasings[0]` |
| `?returnTo=` regex byte-identical to auth-callback regex (proven by source-equality test) | Eliminates open-redirect attack surface drift between two validators that share the same threat model; one regex, one set of test cases, one place to update | ✓ Good — shipped Phase 28 (ADD-08); 15 unit tests including source-equality parity |
| StrictMode-safe useLayoutEffect cleanup with ref-guarded skip cases | StrictMode mount/cleanup/mount pattern in Next.js 16 dev cleared deep-link prefill state on first cleanup pass; ref-guarded skip cases preserve form-prefill across the cleanup cycle while still resetting on real unmount | ✓ Good — shipped Plan 29-06; global vitest StrictMode wrapper added so this regression class is caught in CI |
| Append-only audit edit pattern with byte-equality invariant | Audits are historical artifacts; rewriting them muddies provenance. Appending a `## Closure` section preserves the original audit byte-for-byte while linking to backfilled artifacts | ✓ Good — shipped Phase 31 Plan 03; verified by `git diff ... \| grep -E '^-[^-]' \| wc -l` returning 0 |
| Phase 30 hotfix: `h-full` on `<video>` for object-cover engagement | Initial ship had `<video class="block w-full object-cover">` — without h-full the element kept its intrinsic 16:9 aspect inside the aspect-square wrapper, object-cover had nothing to crop, and the WYSIWYG capture math assumed a crop that never happened | ✓ Good — shipped post-deploy as commit `2dd7377`; surfaces UI-SPEC CSS-chain blind spot in 6-pillar checker (saved as feedback memory `feedback_ui_spec_css_chain_blind_spot.md`) |

**Issues Resolved:**

- v4.0 verification asymmetry — Phase 23 and Phase 24 now have phase-level VERIFICATION.md (Phase 31 DEBT-07/08)
- Wishlist reordering shipped (WISH-01)
- Mobile collection/wishlist 2-column grid + price line on cards (VIS-07/08)
- Success-toast-with-link wired across all 4 commit sites (UX-09)
- ?returnTo= round-trip across 8 entry-points with open-redirect protection (ADD-08)
- "Unusual" verdict copy rewritten coherently across all 3 surfaces; speech-act-split source for wishlist auto-fill (FIT-06)
- UserMenu Profile row removed (NAV-16)
- Profile tab strip horizontal-only scroll (PROF-10)
- Add-Watch flow resets on every entry via three-layer defense (FORM-04)
- WYWT capture alignment fixed end-to-end (WYWT-22)

**Issues Deferred (Tech Debt):**

- **DEBT-09 (NEW, HIGH)** — Phase 23 SUMMARY claimed `notesPublic` Zod field + `revalidatePath('/u/{username}/{tab}')` shipped via commit `4d362ff`, but neither lives in `src/app/actions/watches.ts` on `main`; commit not in HEAD ancestry; RED test scaffold 0/4 PASS. Surfaced by Phase 31 audit; remediation deferred to v4.2 patch or v5.0 carryover.
- ROADMAP.md Phase 30 line stale `[ ]` checkbox (resolved at milestone close)
- REQUIREMENTS.md 7 unchecked-but-verified-complete boxes (resolved by archive)
- `useWatchSearchVerdictCache` not invalidated on signOut — theoretical cross-user leak when collectionRevision values coincidentally match (pre-existing post-29-05; v5.0+ polish)
- Cancel mid-flow doesn't honor `?returnTo=` — by Phase 28 spec (only mandated nav-on-commit); browser-back relies on Phase 29 Layer 2 cleanup
- Nyquist 4/5 partial (Phase 29 alone COMPLIANT) — same posture as v4.0; v5.0 Nyquist hardening sweep

**Technical Debt Incurred:**

- None v4.1-introduced beyond the documented design choices above. DEBT-09 is a v4.0-era regression discovered by v4.1's audit work.

---

_For current project status, see .planning/ROADMAP.md_
