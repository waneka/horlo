---
phase: 51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/profile-route-51.test.ts
  - scripts/verify-phase-51-prod.sh
  - scripts/assert-phase-51-build.mjs
autonomous: true
requirements: [REQ-51-01, REQ-51-02, REQ-51-03, REQ-51-04, REQ-51-05, REQ-51-06, REQ-51-07]

must_haves:
  truths:
    - "Three test artifacts exist in the repo at the canonical paths"
    - "tests/profile-route-51.test.ts initially FAILS on current main (regression contract — recurrence-4 gate)"
    - "scripts/verify-phase-51-prod.sh exits non-zero against current prod (current main reproduces the 0-byte bug)"
    - "scripts/assert-phase-51-build.mjs exits non-zero against the current build artifact (`/u/[username]/[tab]` is currently PPR-eligible)"
  artifacts:
    - path: "tests/profile-route-51.test.ts"
      provides: "Unit/contract specs for REQ-51-04, -05, -06 (structural locks)"
      contains: "describe('Phase 51 — profile route PPR opt-out', ...)"
    - path: "scripts/verify-phase-51-prod.sh"
      provides: "Post-deploy curl-based contract check for REQ-51-01, -02, -07"
      contains: "Next-Router-State-Tree"
    - path: "scripts/assert-phase-51-build.mjs"
      provides: "Local structural assertion that /u/[username]/[tab] is not classified PPR (REQ-51-03)"
      contains: "renderingMode"
  key_links:
    - from: "tests/profile-route-51.test.ts"
      to: "src/app/u/[username]/layout.tsx and src/app/u/[username]/profile-gate.tsx and src/app/u/[username]/profile-shell-resolver.tsx"
      via: "fs.readFileSync source inspection (no DOM render)"
      pattern: "readFileSync.*layout\\.tsx|profile-gate\\.tsx|profile-shell-resolver\\.tsx"
    - from: "scripts/verify-phase-51-prod.sh"
      to: "Vercel preview/prod URL"
      via: "curl -s with RSC + Next-Router-State-Tree headers"
      pattern: "curl.*Next-Router-State-Tree"
---

<objective>
Author the three Phase 51 test/verification artifacts. They MUST initially fail against current main — that proves the regression contract is real and protects against recurrence 4.

Purpose: Lock the invariants in CI/scripts before any production code changes. The test file replaces the F2-era `tests/profile-route-dynamic.test.ts` (deleted in this session); do NOT re-create it.

Output: Three new files at their canonical paths; one shell script is executable (`chmod +x`). Tests are committed in a known-failing state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-RESEARCH.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-PLAN.md

@vitest.config.ts
@src/app/u/[username]/layout.tsx
@src/app/u/[username]/profile-gate.tsx
@src/app/u/[username]/profile-shell-resolver.tsx

<interfaces>
<!-- Test framework: Vitest 3.x; `npm test` resolves to `vitest run`. -->
<!-- vitest.config.ts include pattern: tests/**/*.test.ts → tests/profile-route-51.test.ts will be picked up automatically. -->
<!-- Test environment: jsdom; `server-only` is aliased to a shim. Source files can be read via fs.readFileSync. -->

Existing test patterns to mirror (read tests/no-evaluate-route.test.ts as a precedent for source-grep tests):
- Use Node's `fs.readFileSync` + `path.resolve` to load the source file
- Use plain string `.includes(...)` and regex `.test(...)` for structural assertions
- Each `it()` asserts ONE structural invariant
- Use `expect.fail()` or `expect(false).toBe(true)` when the asserted invariant is violated
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/profile-route-51.test.ts</name>
  <files>tests/profile-route-51.test.ts</files>
  <action>
Create a Vitest spec file with `describe('Phase 51 — profile route PPR opt-out', ...)` containing three `it()` blocks, one per requirement REQ-51-04, REQ-51-05, REQ-51-06.

Implementation rules:
- Use `import { readFileSync } from 'node:fs'` + `import { resolve } from 'node:path'` at the top
- For each test, read the relevant source file via `readFileSync(resolve(process.cwd(), '<relpath>'), 'utf8')`
- DO NOT import or render the React components; this is a source-text-grep style test (mirrors existing `tests/no-evaluate-route.test.ts` pattern)

Test 1 — "layout does not Suspense-wrap ProfileGate (REQ-51-04)":
- Read `src/app/u/[username]/layout.tsx`
- Assert the source does NOT match the regex `/<Suspense[^>]*fallback={<ProfileShellSkeleton/s` (i.e. the layout-level Suspense wrapping the gate is gone)
- Assert the source does NOT import `ProfileGate` (layout no longer composes the gate after plan 51-03)
- Assert the source DOES contain `<main` (the layout still owns the chrome wrapper)

Test 2 — "ProfileGate accepts viewerId as a prop (REQ-51-05; Phase 39c Pitfall 1)":
- Read `src/app/u/[username]/profile-gate.tsx`
- Assert the function signature contains `viewerId` in the destructured props — match the regex `/export\s+async\s+function\s+ProfileGate\s*\(\s*\{[^}]*viewerId/s`
- Assert the source does NOT call `await getCurrentUser()` (the cookie read has moved to the page) — match string `.includes('getCurrentUser()')` should be false
- Assert the source does NOT import `getCurrentUser` from `@/lib/auth` — match regex `/from '@\/lib\/auth'/` should not appear alongside `getCurrentUser`

Test 3 — "ProfileShellResolver remains cached with the profile cacheTag (REQ-51-06; Phase 39c invariant)":
- Read `src/app/u/[username]/profile-shell-resolver.tsx`
- Assert the source contains the literal `'use cache'` directive (single-quoted) — use `source.includes("'use cache'")`
- Assert the source contains the cacheTag call with the username template literal — use straight substring match `source.includes("cacheTag(\`profile:${username}\`)")` (template literal must be escaped as a string with backslash-backtick around the inner backticks in JS source; ALTERNATIVELY use two separate substring checks: `source.includes("cacheTag(") && source.includes("profile:${username}")` — both are bulletproof against regex parsing ambiguity)
- Assert the source contains `cacheLife({ revalidate: 300 })` — `source.includes("cacheLife({ revalidate: 300 })")`

NOTE TO EXECUTOR: do NOT use a regex literal containing backticks for the cacheTag assertion. JavaScript regex literals with embedded backticks + `${...}` produce ambiguous parser results (`{username}` is read as a quantifier in lenient mode and as a syntax error in strict mode). Substring matches via `String.prototype.includes` are the safe equivalent.

All three tests are EXPECTED TO FAIL against current main (this is the regression contract). Plan 51-02 fixes Test 2 + 3; plan 51-03 fixes Test 1.
  </action>
  <verify>
    <automated>npx vitest run tests/profile-route-51.test.ts --reporter=verbose 2>&1 | grep -E "(✓|✗|FAIL|PASS)" | head -20</automated>
  </verify>
  <done>File `tests/profile-route-51.test.ts` exists; running `npm test -- tests/profile-route-51.test.ts` shows Test 1 and Test 2 FAILING (Test 3 passes — current resolver already meets the invariant). The two failures are the regression contract.</done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/verify-phase-51-prod.sh (post-deploy contract check)</name>
  <files>scripts/verify-phase-51-prod.sh</files>
  <action>
Create an executable bash script (`chmod +x` via separate file mode; the Write tool creates without exec bit, so the verify step below uses `bash`).

Script contract:
- Usage: `bash scripts/verify-phase-51-prod.sh <BASE_URL>` (e.g. `bash scripts/verify-phase-51-prod.sh https://www.horlo.app`)
- If `$1` is unset, print usage to stderr and exit 2
- Use `set -euo pipefail` at the top
- Variables:
  - `BASE_URL="$1"`
  - `TEST_USER="${PHASE51_TEST_USER:-twwaneka}"` (operator-configurable; default twwaneka)
  - `TEST_TAB="${PHASE51_TEST_TAB:-wishlist}"`
  - `STATE_TREE='%5B%22%22%2C%7B%22children%22%3A%5B%22u%22%2C%7B%22children%22%3A%5B%5B%22username%22%2C%22twwaneka%22%2C%22d%22%5D%2C%7B%22children%22%3A%5B%5B%22tab%22%2C%22collection%22%2C%22d%22%5D%2C%7B%22children%22%3A%5B%22__PAGE__%22%2C%7B%7D%5D%7D%5D%7D%5D%7D%5D%7D%2Cnull%2Cnull%2Ctrue%5D'`

Check 1 (REQ-51-01) — state-tree-aware RSC body is non-empty:
- `BYTES_STATE=$(curl -s "${BASE_URL}/u/${TEST_USER}/${TEST_TAB}?_rsc=verify-$(date +%s)" -H 'RSC: 1' -H "Next-Router-State-Tree: ${STATE_TREE}" | wc -c | tr -d ' ')`
- If `BYTES_STATE -lt 100`, print FAIL with the byte count, exit 1

Check 2 (REQ-51-02) — prefetch-headed RSC body either non-empty OR carries x-nextjs-postponed: 1:
- Issue `curl -sD - "${BASE_URL}/u/${TEST_USER}/${TEST_TAB}?_rsc=verify-$(date +%s)" -H 'RSC: 1' -H 'Next-Router-Prefetch: 1' -o /tmp/phase51-prefetch-body.txt`
- Capture headers + body, check `x-nextjs-postponed: 1` header OR `wc -c < /tmp/phase51-prefetch-body.txt -gt 100`
- If both fail, print FAIL, exit 1

Check 3 (REQ-51-07) — Branch B only; gated on `PHASE51_BRANCH_B=1` env var:
- If `PHASE51_BRANCH_B=1`, fetch `/u/${TEST_USER}/collection` (no RSC headers; simulating anon browser) with `--cookie-jar /dev/null --cookie /dev/null` to force anon
- Capture status code + `Cache-Control` header
- Assert status is 307 AND `Cache-Control` contains `no-store`
- If not, print FAIL, exit 1

Success path: print "OK Phase 51 prod contract verified: <BASE_URL>" and exit 0.

DO NOT hard-code the production hostname. Always require `$1`.
  </action>
  <verify>
    <automated>bash scripts/verify-phase-51-prod.sh 2>&1 | head -5; test -f scripts/verify-phase-51-prod.sh && echo "FILE_EXISTS"</automated>
  </verify>
  <done>File `scripts/verify-phase-51-prod.sh` exists; running without args prints usage and exits 2; the script is a single self-contained bash file with three checks gated as described.</done>
</task>

<task type="auto">
  <name>Task 3: Author scripts/assert-phase-51-build.mjs (local structural assertion)</name>
  <files>scripts/assert-phase-51-build.mjs</files>
  <action>
Create a Node ESM script that reads `.next/` build output and asserts `/u/[username]/[tab]` is NOT classified as `PARTIALLY_STATIC` (REQ-51-03).

Script contract:
- Usage: `node scripts/assert-phase-51-build.mjs` (no args)
- Requires `npm run build` to have run successfully first
- Uses `import { readFileSync, existsSync } from 'node:fs'` and `import { resolve } from 'node:path'`

Implementation:
1. Read `.next/app-build-manifest.json` if present (path may vary by Next 16; ALSO try `.next/prerender-manifest.json` and `.next/routes-manifest.json`)
2. For each manifest found, look for any entry keyed on `/u/[username]/[tab]` or matching the pattern `^/u/\[username\]/\[tab\]`
3. Inspect the entry — assertions:
   - `prerender-manifest.json` MUST NOT list `/u/[username]/[tab]` under `dynamicRoutes` with a `prerender: true` flag
   - OR if `app-build-manifest.json` has a per-route `renderingMode` field, it MUST NOT equal `'PARTIALLY_STATIC'` for this route
4. If neither file exists, print "SKIP: .next manifest not found; run `npm run build` first" and exit 2 (skip, not fail)
5. On assertion failure: print "FAIL: /u/[username]/[tab] is still PPR-eligible — Phase 51 structural fix incomplete" and `console.log` the offending manifest entry; exit 1
6. On assertion pass: print "OK: /u/[username]/[tab] is not PPR-classified in build output" and exit 0

The exact manifest shape in Next 16.2.3 should be discovered at execution time (`ls .next/*.json` after a build) — the script must be permissive about which manifest file is authoritative as long as ONE of them confirms the assertion. If neither manifest contains the route at all, that's a PASS (the route is fully dynamic). If multiple manifests disagree, the strictest reading wins (fail closed).

**Defense-in-depth (added per plan-checker WARNING 4):** in ADDITION to the manifest assertion, the script must also accept an optional `BUILD_LOG=<path>` env var pointing to a `npm run build` stdout capture. When set, the script greps the build log for the literal substring `◐ Partial Prerender /u/[username]/[tab]` and FAILS CLOSED if found (catches the case where the structural fix landed but a manifest format change hid it). Document the env var in the script header. When `BUILD_LOG` is unset, the script behaves as specified above (manifest-only check).

This script is EXPECTED TO FAIL against the current `.next/` build (the route is currently classified PARTIALLY_STATIC per the build output `◐ Partial Prerender /u/[username]/[tab]` cited in the debug log).
  </action>
  <verify>
    <automated>test -f scripts/assert-phase-51-build.mjs && node -c scripts/assert-phase-51-build.mjs && echo "SYNTAX_OK"</automated>
  </verify>
  <done>File `scripts/assert-phase-51-build.mjs` exists; `node -c` confirms valid syntax. If `.next/` exists from a prior build, running the script with current main code exits 1 (the regression contract). If no `.next/` exists, the script exits 2 (skip).</done>
</task>

</tasks>

<verification>
Run all three artifacts:

1. `npm test -- tests/profile-route-51.test.ts` → 1 or 2 FAILING specs (regression contract — Tests 1, 2 expected to fail against current main; Test 3 may pass since resolver already meets invariant)
2. `bash scripts/verify-phase-51-prod.sh` → exits 2 with usage (no args supplied)
3. `node -c scripts/assert-phase-51-build.mjs` → syntax-OK

The three failing/usage outputs ARE the gate. They prove the tests detect the current bug.
</verification>

<success_criteria>
- [ ] `tests/profile-route-51.test.ts` exists, runs under Vitest, contains 3 `it()` blocks
- [ ] At least 1 test FAILS on current main (regression contract)
- [ ] `scripts/verify-phase-51-prod.sh` exists, is bash-syntactically valid, exits 2 when run without args
- [ ] `scripts/assert-phase-51-build.mjs` exists, is ESM-syntactically valid, can be invoked via `node`
- [ ] No mention of `await connection()` or `prefetch={false}` or `dynamic = 'force-dynamic'` in any of the three new files (blocklist invariants)
</success_criteria>

<output>
After completion, create `.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-01-SUMMARY.md` per the standard summary template, capturing the failure output of `tests/profile-route-51.test.ts` against current main as evidence the regression contract is active.
</output>
