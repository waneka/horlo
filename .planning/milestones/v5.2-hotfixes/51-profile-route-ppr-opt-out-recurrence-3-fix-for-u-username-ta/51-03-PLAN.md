---
phase: 51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta
plan: 03
type: execute
wave: 2
depends_on: [51-02]
files_modified:
  - src/app/u/[username]/layout.tsx
  - src/app/u/[username]/[tab]/page.tsx
autonomous: true
requirements: [REQ-51-03, REQ-51-04]

must_haves:
  truths:
    - "layout.tsx is a pure static shell — no Suspense, no ProfileGate, no async children-resolution; only awaits params and wraps {children} in <main>"
    - "[tab]/page.tsx reads cookies (getCurrentUser) and composes <Suspense fallback={<ProfileShellSkeleton/>}><ProfileGate username viewerId>{tabContent}</ProfileGate></Suspense> around its tab-rendered children"
    - "[tab]/page.tsx passes viewerId as a prop to ProfileGate (matching new signature from plan 51-02)"
    - "ProfileShellResolver is invoked from inside the dynamic page render context (so its cached output is part of a dynamic response, not a static shell contribution)"
    - "TypeScript compilation passes again (layout's prior call to ProfileGate is removed; page is now the gate caller)"
    - "tests/profile-route-51.test.ts Test 1 (REQ-51-04 — layout has no Suspense around gate) PASSES"
    - "Phase 39c invariants preserved: viewerId outside cache (D-39c-03), cache-tag chain (D-39c-04/05), notFound() ordering (D-39c-Pitfall-5), locked-branch routing (D-39c-09)"
    - "`npm run build` succeeds AND `node scripts/assert-phase-51-build.mjs` PASSES (REQ-51-03)"
  artifacts:
    - path: "src/app/u/[username]/layout.tsx"
      provides: "Thin static chrome wrapper — fully prerenderable"
      contains: "<main className=\"mx-auto"
    - path: "src/app/u/[username]/[tab]/page.tsx"
      provides: "Page that reads cookies (runtime API) and owns the gate composition + page-level Suspense"
      contains: "<Suspense fallback={<ProfileShellSkeleton"
  key_links:
    - from: "src/app/u/[username]/[tab]/page.tsx"
      to: "src/app/u/[username]/profile-gate.tsx"
      via: "<ProfileGate username={username} viewerId={viewerId}>{tabContent}</ProfileGate>"
      pattern: "<ProfileGate\\s+username="
    - from: "src/app/u/[username]/[tab]/page.tsx"
      to: "src/app/u/[username]/profile-shell-skeleton.tsx"
      via: "<Suspense fallback={<ProfileShellSkeleton/>}>"
      pattern: "ProfileShellSkeleton"
---

<objective>
Land the F3-Composite structural change:
1. Collapse `src/app/u/[username]/layout.tsx` to a static chrome shell (remove Suspense, remove ProfileGate composition, drop the now-unused imports).
2. Move the gate composition + page-level Suspense fallback into `src/app/u/[username]/[tab]/page.tsx`. The page already calls `getCurrentUser()` (since recurrence-3 baseline); the new code wraps its returned tabContent in `<Suspense fallback={<ProfileShellSkeleton/>}><ProfileGate username={username} viewerId={viewerId}>{tabContent}</ProfileGate></Suspense>` so the gate (with its cached resolver) renders inside the dynamic page render context.

Purpose: This is the actual structural lever. After this change:
- The layout has zero async data sources and no Suspense boundary — it's a pure deterministic operation and is fully prerenderable as static (the route chrome is the entire static contribution).
- The page is the FIRST runtime-API consumer (it reads cookies via `getCurrentUser()`) which per the Next 16 Cache Components docs "Working with runtime APIs" pattern makes the page itself the dynamic stream target.
- The cached `ProfileShellResolver` is invoked from inside the dynamic page render — its output is part of a dynamic response, NOT a static shell contribution. The cache-tag invalidation chain (D-39c-04/05) remains effective; only the topology around the resolver changes.
- The Vercel build classifies the route as fully dynamic (no `renderingMode: 'PARTIALLY_STATIC'`) → no edge-cached static shell to serve → the 0-byte RSC bug is structurally impossible.

Output: Two files edited; build passes; both 51-01 tests pass; the build-output structural assertion passes.

**Phase 39c invariants — explicit re-verification after this plan:**
- D-39c-03 (viewerId out of cache): the page reads viewerId, passes it to the gate as a prop, and the gate passes only `{ username }` to the resolver. REINFORCED.
- D-39c-04/05 (cache-tag chain): resolver unchanged, `cacheTag('profile:${username}')` and `revalidatePath('/u/[username]', 'layout')` from Server Actions remain wired and effective.
- D-39c-Pitfall-5 (notFound() before post-suspending await): preserved inside the gate (call order in the gate body is unchanged).
- D-39c-09 (locked-branch routing): preserved — gate body still short-circuits to `<LockedProfileState/>` when `!isOwner && !settings.profilePublic`.
- D-39c-06 (loading.tsx skeleton): `src/app/u/[username]/loading.tsx` continues to render `ProfileTabContentSkeleton` for tab-segment navigations. The cold-load shell now comes from the page-owned `<Suspense fallback={<ProfileShellSkeleton/>}>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-CONTEXT.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-RESEARCH.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-PLAN.md
@.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-02-PLAN.md

@src/app/u/[username]/layout.tsx
@src/app/u/[username]/[tab]/page.tsx
@src/app/u/[username]/profile-gate.tsx
@src/app/u/[username]/profile-shell-skeleton.tsx
@src/app/u/[username]/loading.tsx

<interfaces>
<!-- Post-51-02 ProfileGate signature: -->
<!-- export async function ProfileGate({ username, viewerId, children }: { username: string; viewerId: string | null; children: React.ReactNode }) -->

<!-- Current [tab]/page.tsx already reads viewerId at lines 64-69 — that block is KEPT. -->
<!-- The page currently calls ProfileShellResolver directly at line 78 — we MUST AUDIT whether to keep that call or delegate fully to the gate. -->
<!-- Decision: KEEP the page's own ProfileShellResolver call (it's needed for the tab-specific renders that read `ownerWatches`, `ownerWearEvents`, etc. from the resolved object). The gate ALSO calls the resolver — within the same request the React `cache()` / Next `'use cache'` machinery dedups the two calls. The duplicate call is a no-op cost (and was already the topology in recurrence-3 baseline). -->

<!-- The cold-load shell is now page-owned: -->
<!-- <Suspense fallback={<ProfileShellSkeleton/>}> -->
<!--   <ProfileGate username={username} viewerId={viewerId}> -->
<!--     {tabContent} -->
<!--   </ProfileGate> -->
<!-- </Suspense> -->

<!-- Where `tabContent` is the JSX the current page body returns for the selected tab (CollectionTabContent, WishlistTabContent, etc.). -->

<!-- The page's existing notFound() guards and per-tab routing stay IDENTICAL — only the FINAL RETURN STATEMENTS for each tab are wrapped in the Suspense+Gate composition. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse layout.tsx to a pure static chrome shell</name>
  <files>src/app/u/[username]/layout.tsx</files>
  <behavior>
    - The file imports only the types needed (no `Suspense`, no `ProfileGate`, no `ProfileShellSkeleton`)
    - The component is `async` (still awaits `params`) but contains no other async work
    - The return value is `<main className="mx-auto max-w-5xl px-4 py-8 lg:px-8 lg:py-12">{children}</main>`
    - Test 1 (REQ-51-04) "layout does not Suspense-wrap ProfileGate" PASSES
    - TypeScript compiles successfully for this file in isolation
  </behavior>
  <action>
Replace the entire contents of `src/app/u/[username]/layout.tsx` with:

```tsx
// Phase 51 (F3-Composite): layout is a pure static chrome wrapper. The
// previous layout-level <Suspense fallback={<ProfileShellSkeleton/>}>
// + <ProfileGate> composition has been moved into [tab]/page.tsx so the
// page (which reads cookies via getCurrentUser) is the runtime-API
// consumer and the route's dynamic stream target. This eliminates Cache
// Components PPR qualification at the source — the layout has no async
// data, no Suspense boundary, and contributes only deterministic chrome
// to the static shell. See .planning/debug/profile-page-404-top-nav.md
// (recurrence 3) and Phase 51 RESEARCH.md for the full rationale.
export default async function ProfileLayout({
  children,
  params,
}: LayoutProps<'/u/[username]'>) {
  // params is awaited to preserve the typed param contract; the value is
  // not used in the layout itself — children (the tab page) consumes it
  // via its own `params` prop.
  await params
  return (
    <main className="mx-auto max-w-5xl px-4 py-8 lg:px-8 lg:py-12">
      {children}
    </main>
  )
}
```

DO NOT keep the `Suspense` import. DO NOT keep the `ProfileGate` or `ProfileShellSkeleton` imports.

The `username` destructure from `await params` is no longer needed (layout doesn't pass it down). Awaiting `params` (without destructuring) keeps the typed `LayoutProps<'/u/[username]'>` contract honored. If TypeScript complains about `params` being awaited-but-not-used, change to `void (await params)` or destructure and prefix with `_` (`const { username: _username } = await params`).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "(layout\\.tsx|ProfileGate)" | head -5; npm test -- tests/profile-route-51.test.ts -t "layout" --reporter=verbose 2>&1 | grep -E "(✓|✗)" | head -3</automated>
  </verify>
  <done>
    - `src/app/u/[username]/layout.tsx` contains 0 occurrences of `Suspense` and 0 occurrences of `ProfileGate`
    - File imports list contains 0 imports from `'react'` (Suspense is gone)
    - Test 1 (REQ-51-04) PASSES
    - `tsc --noEmit` succeeds for this file (the page-level fix in Task 2 also has to land before the whole build is clean — TypeScript may surface a transient error in `[tab]/page.tsx` until Task 2 completes; that's expected within a single plan)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move gate composition + page-owned Suspense into [tab]/page.tsx</name>
  <files>src/app/u/[username]/[tab]/page.tsx</files>
  <behavior>
    - The file imports `Suspense` from `'react'`, `ProfileGate` from `'../profile-gate'`, and `ProfileShellSkeleton` from `'../profile-shell-skeleton'`
    - The cookie-reading block (currently lines 64-69) is UNCHANGED — `getCurrentUser()` continues to resolve `viewerId` at the top of the page body
    - The ProfileShellResolver call at line 78 + the `if (!resolved.profile) notFound()` guard at line 79 are UNCHANGED (preserves Pitfall 5 ordering)
    - Each tab's return statement is wrapped in `<Suspense fallback={<ProfileShellSkeleton/>}><ProfileGate username={username} viewerId={viewerId}>...tabContent...</ProfileGate></Suspense>`
    - All seven tab branches (common-ground, insights, collection, wishlist, notes, worn, stats) AND the per-tab LockedTabCard early returns are wrapped consistently
    - All existing privacy gates, notFound() calls, LockedTabCard early returns, and tab-specific data fetches are UNCHANGED in their behavior — only the final JSX they return is wrapped
    - `npm run build` succeeds; `node scripts/assert-phase-51-build.mjs` PASSES (REQ-51-03)
    - The build output for `/u/[username]/[tab]` no longer shows `◐ Partial Prerender`
  </behavior>
  <action>
Edit `src/app/u/[username]/[tab]/page.tsx`. The change is structural but mechanical — wrap every JSX return statement in the page-owned Suspense+Gate.

**Step 1 — Add imports at the top of the file (after existing imports):**
```tsx
import { Suspense } from 'react'
import { ProfileGate } from '../profile-gate'
import { ProfileShellSkeleton } from '../profile-shell-skeleton'
```

**Step 2 — Define a local helper near the top of the page body (after the params destructure and viewerId resolution, before the resolver call):**
```tsx
// Phase 51 F3-Composite: wrap every per-tab JSX return in this composition
// so the page is the runtime-API consumer (getCurrentUser above) and the
// gate's cached resolver renders inside the dynamic page render context.
// The page is now the route's dynamic stream target; the layout collapsed
// to a pure static shell.
const wrapInGate = (tabContent: React.ReactNode) => (
  <Suspense fallback={<ProfileShellSkeleton />}>
    <ProfileGate username={username} viewerId={viewerId}>
      {tabContent}
    </ProfileGate>
  </Suspense>
)
```

This helper is defined AFTER `viewerId` is resolved (lines 64-69 in the current file) and AFTER `username` is destructured. It MUST be defined BEFORE any return statement — i.e. immediately after the `getCurrentUser()` try/catch and BEFORE the `ProfileShellResolver` call so it is in scope for every tab branch.

**Step 3 — Wrap every JSX return statement. There are EIGHT such sites in the current file. For each, change `return <X .../>` to `return wrapInGate(<X .../>)`. Apply to:**

1. Common-ground tab — line ~120: the "No shared watches yet" `<Card>` return → wrap
2. Common-ground tab — line ~150: the `<CommonGroundTabContent>` return → wrap
3. Insights tab — line ~164: `return <InsightsTabContent .../>` → wrap
4. Collection LockedTabCard — line ~170: `<LockedTabCard tab="collection" .../>` → wrap
5. Wishlist LockedTabCard — line ~183: `<LockedTabCard tab="wishlist" .../>` → wrap
6. Notes LockedTabCard — line ~206: `<LockedTabCard tab="notes" .../>` → wrap
7. Collection/wishlist/notes tab return — line ~236/246/263: three returns within the shared `if (tab === 'collection' || tab === 'wishlist' || tab === 'notes')` block → wrap each
8. Worn tab return — line ~292: `<WornTabContent .../>` → wrap
9. Stats LockedTabCard — line ~313: `<LockedTabCard tab="stats" .../>` → wrap
10. Stats final return — line ~346: `<StatsTabContent .../>` → wrap

(Total of 10 returns; the count discrepancy from "eight" above is fine — wrap ALL of them, including the three within the collection/wishlist/notes shared block.)

**Step 4 — Re-validate that the `notFound()` calls are NOT wrapped.** `notFound()` throws `NEXT_NOT_FOUND` and must propagate; it is NOT a return value. The two notFound() calls in the page body — line ~58 (invalid tab) and line ~79 (missing profile) — are bare statements, not returns, and stay unwrapped.

**Step 5 — Do NOT modify the cookie-reading block, the ProfileShellResolver call, any privacy gate logic, any data-fetching code, or any other behavior. The ONLY changes are the three new imports, the new helper definition, and the 10 return-wrapping edits.**

**Important — duplicate ProfileShellResolver call:** The page calls `ProfileShellResolver({ username })` at line 78 AND the gate (now invoked via the wrapper) calls it again. Within a single request, both calls hit the same `'use cache'` entry — the second is a sub-millisecond cache lookup, not a DB roundtrip. This duplication is intentional and was the topology in recurrence-3 baseline. DO NOT try to "optimize" it away in this plan — the page's call is needed for the tab-specific data extraction (`ownerWatches`, `ownerWearEvents`); the gate's call is needed because the gate composes the chrome (ProfileHeader, ProfileTabs, locked-branch, etc.). If a future plan wants to factor this, it's out of scope here.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30 ; node scripts/assert-phase-51-build.mjs 2>&1 | tail -5 ; npm test -- tests/profile-route-51.test.ts --reporter=verbose 2>&1 | grep -E "(✓|✗|FAIL|PASS)" | head -10</automated>
  </verify>
  <done>
    - `npm run build` exits 0
    - Build output for `/u/[username]/[tab]` no longer shows `◐ Partial Prerender` (it should show `λ` or equivalent dynamic marker)
    - `node scripts/assert-phase-51-build.mjs` exits 0 (REQ-51-03)
    - All three specs in `tests/profile-route-51.test.ts` PASS (REQ-51-04, REQ-51-05, REQ-51-06)
    - `npm test` full suite passes (no regressions)
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/profile-route-51.test.ts` → 3/3 PASS
- `npm test` (full suite) → all pass, no regressions
- `npm run build` → succeeds
- `node scripts/assert-phase-51-build.mjs` → exits 0
- `grep -c "Suspense" src/app/u/[username]/layout.tsx` → 0
- `grep -c "Suspense" src/app/u/[username]/[tab]/page.tsx` → ≥ 1
- `grep -c "wrapInGate" src/app/u/[username]/[tab]/page.tsx` → ≥ 10
- `grep -B 1 "notFound()" src/app/u/[username]/[tab]/page.tsx | grep -c "wrapInGate"` → 0 (the two notFound() calls at lines ~58 and ~79 must NOT be preceded by/wrapped in wrapInGate — they are bare statements that must propagate NEXT_NOT_FOUND, per Step 4)
</verification>

<success_criteria>
- [ ] Layout is a pure static chrome shell — verified by Test 1
- [ ] Page imports Suspense + ProfileGate + ProfileShellSkeleton and wraps every JSX return in the gate composition
- [ ] Page passes viewerId as a prop to ProfileGate (matching plan 51-02's signature)
- [ ] `npm run build` succeeds
- [ ] Build artifact assertion passes — route no longer PPR-classified (REQ-51-03)
- [ ] All 3 Phase 51 tests pass (REQ-51-04, -05, -06)
- [ ] No regressions in `npm test` full suite
- [ ] Phase 39c invariants D-39c-03, -04, -05, -09, Pitfall-5 all preserved
</success_criteria>

<output>
After completion, create `.planning/phases/51-profile-route-ppr-opt-out-recurrence-3-fix-for-u-username-ta/51-03-SUMMARY.md` per the standard summary template, including the `npm run build` output snippet showing `/u/[username]/[tab]` is no longer PPR-classified.

After this plan, if Branch A was selected: skip directly to plan 51-06 (preview deploy + prod verification). If Branch B was selected (default), proceed to plan 51-04.
</output>
