---
phase: 52
plan: 06
type: execute
wave: 2
depends_on: [03, 04, 05]
files_modified: []
autonomous: false
requirements: [REQ-52-02, REQ-52-05]

must_haves:
  truths:
    - "Every cross-route validator violation surfaced in 52-03-VALIDATOR-OUTPUT.md is addressed — either via canonical refactor (rare) or via `export const unstable_instant = false` opt-out (D-52-02 default)"
    - "`npm run build` exits 0 with no validator errors on ANY route (in-route + cross-route)"
    - "Each opt-out has an inline comment citing the surfaced error category and D-52-02 + SEED-014"
    - "If no cross-route findings surfaced in Wave 1, this plan executes a no-op confirmation step (build exits 0; SEED-014 records the empty finding set)"
  artifacts:
    - path: "(varies — one or more route files surfaced by 52-03-VALIDATOR-OUTPUT.md)"
      provides: "unstable_instant = false opt-out per surfaced route"
      contains: "unstable_instant = false"
  key_links:
    - from: "(each surfaced route file)"
      to: "Next 16 build/dev validator"
      via: "route-segment config export `unstable_instant = false`"
      pattern: "unstable_instant\\s*=\\s*false"
---

<objective>
Close the second half of REQ-52-02 (validator green on `npm run build`) by applying `unstable_instant = false` opt-outs to every cross-route violation surfaced by Wave 1's validator probe (Plan 03's `52-03-VALIDATOR-OUTPUT.md`). Per D-52-02, this is the documented opt-out pattern from instant.md — it tells the validator "this route legitimately can't be instant" without changing runtime behavior. After Plans 04 and 05 land, the in-route violations on `/u/[username]/[tab]` are resolved; this plan closes whatever else the validator surfaced.

Purpose: The `unstable_instant` validator runs at every shared layout boundary in the route (RESEARCH.md Pitfall 1). Adding it to `[tab]/page.tsx` triggers validation across the entire profile route subtree (`/u/[username]/followers`, `/u/[username]/following`, etc.) AND potentially across other PPR-classified routes (per the audit's original list of 30). Most of these are not Phase 52's scope to refactor — they get the opt-out + a SEED-014 entry for a future sweep phase.

This plan's scope is **validator-driven**. If Wave 1's `52-03-VALIDATOR-OUTPUT.md` has an empty cross-route findings table, this plan is a 5-minute no-op (run build, confirm green, record). If it has 5+ findings, this plan touches 5+ files. The shape is determined entirely by Wave 1's output.

Output: 0 to N files modified (each with a `unstable_instant = false` export); 0 to N inline comments citing D-52-02; `npm run build` exits 0 globally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-CONTEXT.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-RESEARCH.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-PATTERNS.md
@.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-03-VALIDATOR-OUTPUT.md

<interfaces>
unstable_instant opt-out shape (from instant.md "Disabling instant" + instant-navigation.md "Opting out"):

  export const unstable_instant = false

Single line. Goes at the top of the page or layout file (route segment config — Next 16 reads exports at module scope). Documents intent: "this route legitimately can't be instant" — no runtime behavior change.

The opt-out exempts the segment from the instant-nav contract requirement; it does NOT disable Cache Components for that segment.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Read 52-03-VALIDATOR-OUTPUT.md cross-route findings table; confirm scope</name>
  <action>Human-action checkpoint — Claude orchestrates context capture but the operator performs the action and responds via the resume-signal. The substantive work, verification, and resume protocol are documented in the &lt;what-needed&gt; and &lt;how-to-verify&gt; blocks below (intentionally kept in the checkpoint-template idiom for executor readability).</action>
  <verify>
    <automated>echo "checkpoint — awaiting operator response per resume-signal"</automated>
  </verify>
  <done>Operator responds via the resume-signal documented below; the orchestrator captures the response and proceeds to the next task.</done>
  <what-needed>
This plan executes against the cross-route findings table in `.planning/phases/52-.../52-03-VALIDATOR-OUTPUT.md` (created by Plan 03 Task 2). If the table is empty, this plan is mostly a no-op (verify + record). If non-empty, each row is a file to modify.

Operator: confirm the cross-route findings by running `npm run build` against the current state (Plans 04 + 05 have shipped). The build output is now the GROUND TRUTH for what cross-route violations remain — Wave 1's capture was the initial probe; Wave 2's intermediate state may have changed what surfaces (typically: in-route errors resolved by Plans 04+05, cross-route errors unchanged).
  </what-needed>
  <how-to-verify>
1. With Plans 04 and 05 shipped (verify via `git log --oneline -5`), clear the build cache: `rm -rf .next`
2. Run `npm run build 2>&1 | tee /tmp/phase-52-build-after-wave-2.log`
3. Grep the log for validator error markers:
   - `grep -n "instant\\|unstable_instant" /tmp/phase-52-build-after-wave-2.log`
   - `grep -n "Suspense\\|prerender\\|Push dynamic" /tmp/phase-52-build-after-wave-2.log`
4. Cross-reference with `52-03-VALIDATOR-OUTPUT.md` § Cross-route findings table. The current build's remaining errors are the scope of Plan 06.
5. If `npm run build` exits 0 with zero validator errors:
   - This plan's scope is "no-op + record" — skip Tasks 1+ and proceed to Task 2 (record empty finding set).
6. If the build still surfaces validator errors:
   - For each error, identify the route file (typically `src/app/<segment>/page.tsx` or `src/app/<segment>/layout.tsx`)
   - Each gets a Task 1-style application of `unstable_instant = false`
   - Reply to Claude with the file list as a comma-separated string

Operator: reply with one of:
  - `no-op` (build green; no cross-route opt-outs needed)
  - `apply-to: <file1>, <file2>, ...` (paths relative to repo root)
  - `unexpected: <description>` (validator error not solvable by opt-out — re-spawn planner)
  </how-to-verify>
  <resume-signal>Reply with `no-op` OR `apply-to: <comma-separated-file-list>` OR `unexpected: <description>`.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Apply `unstable_instant = false` opt-out to each surfaced route (skip if Task 0 resolved as no-op)</name>
  <files>(populated from Task 0 resolution — varies)</files>
  <read_first>
    - 52-03-VALIDATOR-OUTPUT.md § Cross-route findings — verbatim error transcripts per surfaced route
    - Each file in Task 0's resolved scope — read the file's current imports + existing route-segment config exports (some files may already have `revalidate`, `dynamic`, etc.; the new `unstable_instant = false` lives alongside them at module scope)
    - .planning/phases/52-.../52-RESEARCH.md § Pattern 4: `unstable_instant` Export (lines 315-346) — TypeScript interface for the export
    - .planning/phases/52-.../52-RESEARCH.md § Code Examples > Opt-Out Pattern (lines 515-520) — verbatim opt-out template
  </read_first>
  <action>
For EACH file in Task 0's resolved scope, apply this exact change pattern (skip if Task 0 resolved as `no-op`):

1. Add the line `export const unstable_instant = false` at module scope.
   - Placement: at the top of the file, after the import block, BEFORE any existing route-segment config exports (`revalidate`, `dynamic`, `fetchCache`, etc.)
   - If the file already has other route-segment config exports, group with them
   - If the file is a `layout.tsx` rather than a `page.tsx`, the same rule applies (Next 16 route-segment config exports work on both)

2. Add an inline comment immediately ABOVE the export, citing:
   - The Wave 1 validator error category that surfaced this route (copy verbatim from 52-03-VALIDATOR-OUTPUT.md)
   - D-52-02 + SEED-014 — references for "this route legitimately can't be instant; future sweep covers structural fix"
   - Phase 52 + the audit followup

   Comment template:
   ```
   // Phase 52 (D-52-02): this route surfaced an `unstable_instant` validator
   // error from /u/[username]/[tab]/page.tsx's instant-nav contract — see
   // .planning/phases/52-.../52-03-VALIDATOR-OUTPUT.md row {N}. Applied
   // canonical opt-out per instant.md "Disabling instant" section to keep
   // Phase 52 narrow. Structural fix (sync + Suspense + async chrome split)
   // deferred to SEED-014 (.planning/seeds/SEED-014-cache-components-
   // canonical-sweep.md) for the broader CC canonical-pattern sweep phase.
   ```

3. Do NOT modify any other aspect of the file. The opt-out is documentation of intent + a validator silencer; the runtime behavior is unchanged.

4. Add the file path to the running list that Plan 08 will record in SEED-014.

After this task: `npm run build` produces zero validator errors globally. The opt-out is the documented escape hatch from `instant.md`; using it is not a workaround, it's the docs-prescribed pattern.

If Task 0 resolved as `no-op`, this task is skipped entirely — proceed to Task 2.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tee /tmp/phase-52-build-plan-06.log | tail -20; grep -c "unstable_instant\|Suspense\|prerender" /tmp/phase-52-build-plan-06.log | xargs -I {} test {} -eq 0 || (echo "WARN: validator errors remain — recheck scope" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - For EACH file in Task 0 scope: source contains `export const unstable_instant = false` at module scope (verified via `grep -l "unstable_instant\s*=\s*false" <file>`)
    - For EACH file in Task 0 scope: an inline comment above the export cites D-52-02 + Phase 52 + 52-03-VALIDATOR-OUTPUT.md (verified via `grep -A 5 -B 5 "unstable_instant\s*=\s*false" <file>` includes "D-52-02" or "Phase 52")
    - Behavior: `npm run build` exits 0 (verified via the verify command's tee output)
    - Behavior: `npm run build` output contains NO occurrences of `instant`, `prerender`, `Suspense`, or `Push dynamic access down` in error-context lines (validator silent)
    - Behavior: existing route-segment configs (if any — `revalidate`, `dynamic`) in the modified files are preserved
    - Behavior: `npx tsc --noEmit` exits 0 — TypeScript accepts `false` for the export per the `InstantConfig` union type (RESEARCH.md Pattern 4 TypeScript interface)
  </acceptance_criteria>
  <done>
For each surfaced route: opt-out exported, comment added, build green. If Task 0 resolved as no-op, no source changes were made — Task 2 records the empty finding set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm build-time validator gate is green; record finding set for SEED-014</name>
  <files>.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-06-FINDINGS.md</files>
  <read_first>
    - 52-03-VALIDATOR-OUTPUT.md (Wave 1 capture)
    - The /tmp/phase-52-build-plan-06.log from Task 1's verify step (Wave 2 final state)
  </read_first>
  <action>
Create `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-06-FINDINGS.md` summarizing what Wave 2 actually surfaced and how it was resolved. Plan 08 will consume this when authoring SEED-014.

Structure:
```
# Phase 52 — Plan 06 Findings: Cross-Route Validator Closures

**Wave 1 probe captured:** see 52-03-VALIDATOR-OUTPUT.md (working hypothesis)
**Wave 2 closure (this plan):** the validator's actual state AFTER Plans 04+05 shipped

## Build state after Plans 04+05

Outcome: { all-green / N cross-route violations remaining }

## Surfaced cross-route violations (after Plans 04+05 shipped)

| # | Route file | Error category | Resolution | Comment cite |
|---|------------|----------------|------------|--------------|
| 1 | src/app/.../page.tsx | top-level await runtime API | unstable_instant = false | D-52-02 + SEED-014 |
... (one row per surfaced route — empty table if Wave 1 hypothesis was wrong and only the in-route errors surfaced)

## Validator state after Plan 06 closures

`npm run build` exits 0 — recurrence-5 prevention contract live.

## Hand-off to SEED-014 (consumed by Plan 08)

For the broader Cache Components canonical-pattern sweep phase, the following routes should receive the canonical refactor (sync layout + Suspense + async chrome split):

- (route 1) — surfaced category: ... — suggested fix shape: ...
- (route 2) — ...

If the table is empty, SEED-014 will record this as "no cross-route violations surfaced beyond the target route" — still a useful finding for the sweep phase.
```

If Task 0 resolved as `no-op`, the surfaced-violations table is empty and the hand-off section notes that. Plan 08's SEED-014 will reflect "no cross-route violations" as a finding — that's a valid sweep-phase input.
  </action>
  <verify>
    <automated>test -f .planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-06-FINDINGS.md && npm run build 2>&1 | tail -5 | grep -E "(Compiled|exited 0|error|warn)" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/52-.../52-06-FINDINGS.md` exists
    - File contains a "Surfaced cross-route violations" table (with rows or with an explicit empty-set note)
    - File contains a "Hand-off to SEED-014" section
    - Behavior: `npm run build` exits 0 (REQ-52-02 fully live)
    - File is committed to git alongside any source changes from Task 1
  </acceptance_criteria>
  <done>
The validator gate is green. The handoff document records what was found and what was resolved. SEED-014 (Plan 08) will reference this directly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time validator→CI exit code | `npm run build` exit code is the hard CI gate (D-52-03). No runtime data crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06-01 | Tampering | `unstable_instant = false` mass-applied to evade validator instead of fixing structural bugs | mitigate | Each opt-out requires an inline comment citing the surfaced error category + D-52-02 + SEED-014 (deferred-fix breadcrumb). Acceptance criterion enforces the comment presence. Reviewers can audit by `grep -A 5 unstable_instant`. |
| T-52-06-02 | Repudiation | Cross-route findings not recorded → future sweep phase has no input | mitigate | Task 2's `52-06-FINDINGS.md` is the canonical record + the hand-off for SEED-014 (Plan 08). The doc is committed to git. |
</threat_model>

<verification>
1. `npm run build` exits 0 globally
2. Every file in Task 0 scope contains `unstable_instant = false` + the cite comment
3. `.planning/phases/52-.../52-06-FINDINGS.md` exists and is committed
4. No regressions in vitest suite (`npm run test` exits 0)
</verification>

<success_criteria>
- [ ] Task 0 resolved (no-op OR specific file list confirmed)
- [ ] Each surfaced file has `unstable_instant = false` + cite comment
- [ ] `npm run build` exits 0 globally (REQ-52-02 fully live)
- [ ] `52-06-FINDINGS.md` records the resolved scope for Plan 08's SEED-014 hand-off
- [ ] No vitest regressions
</success_criteria>

<output>
After completion, create `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-06-SUMMARY.md` per the standard summary template. Include:
  - Final scope decided in Task 0 (no-op vs file list)
  - `npm run build` exit code + abbreviated output (last 10 lines)
  - Pointer to 52-06-FINDINGS.md for the SEED-014 hand-off

Update `.planning/phases/52-option-d-cache-components-canonical-pattern-fix-for-u-userna/52-VALIDATION.md` Per-Task Verification Map with rows:
| 52-06-01 | 06 | 2 | REQ-52-05 (cross-route opt-outs applied) | T-52-06-01 | Each surfaced route has documented unstable_instant=false + cite | unit (grep) | `grep -l "unstable_instant" <files>` returns the resolved file list | ✅ | ⬜ pending |
| 52-06-02 | 06 | 2 | REQ-52-02 (final — build-time validator green globally) | — | npm run build exit code | build | `npm run build && echo OK` | ✅ | ⬜ pending |
</output>
