---
phase: 82
plan: 02
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - src/components/watch/BrandPicker.tsx
  - src/components/watch/BrandPicker.test.tsx
  - src/components/watch/StructuredEntryPanel.tsx
  - src/components/watch/StructuredEntryPanel.test.tsx
  - src/components/watch/SearchEntry.tsx
autonomous: true
requirements: [UI-01, UI-02]
tags: [combobox, brand-picker, base-ui, controlled-open, srch-03-lesson]
truths:
  - "D-82-01: New src/components/watch/BrandPicker.tsx uses @base-ui/react/combobox controlled-open pattern (same as SearchEntry L207-232) with filteredItems + filter={null} + object value shape"
  - "D-82-03: UI-02 'Couldn't find' affordance is UX-only — no new API route; auto-create fires server-side inside /api/extract-watch on the subsequent Find specs click via Phase 80 resolveBrandId tier 3"
  - "D-82-04: Silent — no toast, no envelope field, no inline hint on auto-create commit"
  - "D-82-05: Affordance copy is verbatim 'Couldn't find that brand — add as \"{typed}\"'; button placed as SIBLING of Combobox.List inside Combobox.Popup (SRCH-03 lesson) — never inside the list"
  - "StructuredEntryPanel gains a brands: { id, name }[] prop threaded from AddWatchFlow (via SearchEntry per Plan 01 drill path)"
must_haves:
  truths:
    - "BrandPicker filters brands client-side by lowercased substring; selecting an item calls onChange with { id, name }"
    - "When filteredBrands.length === 0 && typed.trim().length > 0 && onCouldntFind is provided, a button labeled exactly 'Couldn\\'t find that brand — add as \"{typed}\"' renders as a sibling of Combobox.List inside Combobox.Popup"
    - "Clicking the affordance closes the popup AND calls onCouldntFind(typed) — both directions assertable per [[assert-disappearance-too]]"
    - "StructuredEntryPanel's Brand field is a <BrandPicker> (not a raw Input); POST body construction (brand: brand.trim()) is UNCHANGED"
  artifacts:
    - path: "src/components/watch/BrandPicker.tsx"
      provides: "Controlled-open combobox for brand selection with UI-02 affordance emit"
      min_lines: 80
      contains: "Combobox.Root"
    - path: "src/components/watch/BrandPicker.test.tsx"
      provides: "Unit tests for filter, selection, affordance mount+close (assert-disappearance-too)"
      min_lines: 60
      contains: "onCouldntFind"
    - path: "src/components/watch/StructuredEntryPanel.tsx"
      provides: "Brand field swapped from Input to BrandPicker; new brands prop accepted"
      contains: "<BrandPicker"
  key_links:
    - from: "src/components/watch/StructuredEntryPanel.tsx"
      to: "src/components/watch/BrandPicker.tsx"
      via: "component mount with brands + value + onChange + onCouldntFind"
      pattern: "<BrandPicker"
    - from: "src/components/watch/SearchEntry.tsx"
      to: "src/components/watch/StructuredEntryPanel.tsx"
      via: "prop drill: brands = brandsWithIds"
      pattern: "brandsWithIds"
    - from: "affordance click"
      to: "/api/extract-watch (later, on Find specs)"
      via: "typed value locked as picker state → panel POSTs → resolveBrandId tier 3"
      pattern: "onCouldntFind"
---

<objective>
Wave 2 — UI-01 + UI-02. Ship `src/components/watch/BrandPicker.tsx`, a dedicated ~120-line client component that mirrors `SearchEntry`'s `@base-ui/react/combobox` 1.3.0 controlled-open pattern for brand selection. Wire it into `StructuredEntryPanel` in place of the raw brand `<Input>` at L220-228 (adds a `brands: { id: string; name: string }[]` prop). Thread `brandsWithIds` through `SearchEntry` from `AddWatchFlow` (from Plan 01) to `StructuredEntryPanel`. Ship the BrandPicker test file with client-filter, selection, and the UI-02 affordance mount+close assertions per `[[assert-disappearance-too]]`. Extend `StructuredEntryPanel.test.tsx` to verify the brands prop threads to the BrandPicker mount.

Purpose: Give users a canonical brand-picker on the structured-entry path that (a) constrains typing to known canonical brands, (b) exposes the roadmap-language "Couldn't find that brand — add as '{typed}'" affordance when the filter finds no matches, and (c) leaves the existing `/api/extract-watch` upsert path untouched — the affordance is UX-only and the actual `needs_review=true` `brands` row auto-create happens inside Phase 80's frozen resolver when the user later clicks Find specs.

Output: One new component + one new test file + two modified files (StructuredEntryPanel + its test) + one modified file (SearchEntry — forwards the brandsWithIds prop through to StructuredEntryPanel's mount).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-add-watch-ui-operator-admin/82-CONTEXT.md
@.planning/phases/82-add-watch-ui-operator-admin/82-RESEARCH.md
@.planning/phases/82-add-watch-ui-operator-admin/82-PATTERNS.md
@.planning/phases/82-add-watch-ui-operator-admin/82-UI-SPEC.md
@.planning/phases/82-add-watch-ui-operator-admin/82-VALIDATION.md
@.planning/phases/82-01-SUMMARY.md
@src/components/watch/SearchEntry.tsx
@src/components/watch/StructuredEntryPanel.tsx
@src/components/watch/SearchEntry.test.tsx
@src/components/watch/StructuredEntryPanel.test.tsx

<interfaces>
<!-- Contracts BrandPicker exposes + consumes. -->

BrandPicker.tsx public shape (per D-82-01):
```typescript
interface BrandPickerProps {
  brands: { id: string; name: string }[]
  value: { id: string; name: string } | null
  onChange: (next: { id: string; name: string }) => void
  onCouldntFind?: (typed: string) => void  // optional — merge dialog (Plan 04) omits
}
export function BrandPicker(props: BrandPickerProps): JSX.Element
```

@base-ui/react/combobox 1.3.0 controlled-open pattern (verbatim from SearchEntry.tsx L207-232 per PATTERNS.md L47-68):
- `<Combobox.Root<Brand>` generic type on the item
- `inputValue={inputValue}` + `onInputValueChange={(val, details) => { if (details.reason !== 'input-change') return; setInputValue(val) }}` — SearchEntry L214-218 guard verbatim
- `filteredItems={filteredBrands}` + `filter={null}` — MUST be paired (RESEARCH § Anti-Patterns "filteredItems without filter={null}")
- `itemToStringLabel={(b) => b.name}` + `itemToStringValue={(b) => b.id}` + `isItemEqualToValue={(a,b) => a.id === b.id}`
- `open={open}` + `onOpenChange={setOpen}` controlled
- `onValueChange={(picked) => { if (picked) onChange(picked) }}`

UI-02 affordance placement (per D-82-05 + RESEARCH § Pitfall 1 — SRCH-03 lesson):
```jsx
<Combobox.Popup>
  {filteredBrands.length > 0 && (
    <Combobox.List>
      {filteredBrands.map(b => (<Combobox.Item ... />))}
    </Combobox.List>
  )}
  {/* SIBLING of List — MUST be outside Combobox.List per SRCH-03; L326-334 SearchEntry pattern */}
  {filteredBrands.length === 0 && inputValue.trim().length > 0 && onCouldntFind && (
    <button type="button" data-testid="brand-picker-couldnt-find" onClick={...}>
      Couldn&apos;t find that brand — add as &ldquo;{inputValue.trim()}&rdquo;
    </button>
  )}
</Combobox.Popup>
```

Affordance button className (verbatim from UI-SPEC L165):
`mt-1 flex w-full items-center gap-2 rounded-md px-3 py-3 text-sm text-muted-foreground hover:bg-muted hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring min-h-[44px]`

Combobox.Item highlight class (per UI-SPEC L83 + `[[accent-is-active-token]]`):
`group flex items-center gap-4 min-w-0 rounded-md pl-2 pr-3 py-2 cursor-default data-[highlighted]:bg-accent data-[highlighted]:text-accent-foreground`

StructuredEntryPanel new prop:
```typescript
interface StructuredEntryPanelProps {
  // ...existing...
  brands: { id: string; name: string }[]  // NEW — from Plan 01 drill; consumed by BrandPicker
}
```

The panel's outbound `brand` string (already sent to /api/extract-watch as `brand: brand.trim()`) is UNCHANGED. When picker `value.id != null` → brand string = value.name. When user activated the affordance → brand string = the locked typed value (from onCouldntFind). Either way, the POST body shape is identical.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BrandPicker.tsx + BrandPicker.test.tsx</name>
  <read_first>
    - src/components/watch/SearchEntry.tsx L200-340 (READ carefully — this is the CANONICAL pattern; extract Combobox.Root controlled-open shape, filteredItems + filter={null} pairing, Portal/Positioner/Popup shell, Item highlight class, footer-outside-List placement at L325-334)
    - src/components/watch/SearchEntry.test.tsx (test file structure, jsdom + userEvent + mocks pattern)
    - node_modules/@base-ui/react/combobox/root/ComboboxRoot.d.ts (API surface confirmation — filteredItems, filter, open, onOpenChange, onInputValueChange, value, onValueChange, itemToStringLabel, itemToStringValue, isItemEqualToValue)
    - .planning/phases/82-add-watch-ui-operator-admin/82-UI-SPEC.md § BrandPicker Interaction Contract (L138-176 — full a11y + copy + gate + testid + classes)
    - .planning/phases/82-add-watch-ui-operator-admin/82-RESEARCH.md § Pattern 1 (full BrandPicker code sketch L200-289) and § Pitfall 1 (SRCH-03 lesson — affordance placement)
    - .planning/phases/82-add-watch-ui-operator-admin/82-PATTERNS.md § BrandPicker.tsx (L35-107)
    - .planning/phases/82-add-watch-ui-operator-admin/82-CONTEXT.md D-82-01/03/04/05 (locked shape + affordance semantics + copy)
  </read_first>
  <behavior>
    - Test 1 (render + filter): Renders with `brands=[{id:'a',name:'Omega'},{id:'b',name:'Rolex'}]`, `value=null`; on focus + typing 'ome' the popup shows exactly ONE Combobox.Item with text 'Omega'; typing 'xyz' shows ZERO items AND the affordance button appears.
    - Test 2 (selection fires onChange): After typing 'ome', ArrowDown then Enter fires `onChange` with `{id:'a',name:'Omega'}`. Also assert popup closes after selection.
    - Test 3 (UI-02 affordance gate — empty input): `inputValue = ''` → no affordance button in DOM regardless of onCouldntFind being provided (gate: typed.trim().length > 0).
    - Test 4 (UI-02 affordance gate — no onCouldntFind prop): affordance omitted from JSX when `onCouldntFind` is undefined even with zero matches + non-empty typed (merge dialog use case per Plan 04).
    - Test 5 (UI-02 affordance click — assert-disappearance-too — the load-bearing test): with `onCouldntFind = vi.fn()` and typed value 'CustomWatchCo' matching zero brands: (a) the button `data-testid="brand-picker-couldnt-find"` renders with text containing `Couldn't find that brand — add as "CustomWatchCo"`; (b) clicking it fires `onCouldntFind` with argument `'CustomWatchCo'` (trimmed); (c) `queryByRole('listbox')` returns null AFTER the click (popup closes). BOTH the callback fire AND the popup close MUST be asserted per `[[assert-disappearance-too]]`.
    - Test 6 (input value trim in affordance emit): typed value `'  CustomCo  '` (with whitespace) → onCouldntFind called with `'CustomCo'` (trimmed).
    - Test 7 (grep armor — SRCH-03 lesson): `grep -c "Combobox.Empty" src/components/watch/BrandPicker.tsx` returns 0 (affordance MUST NOT use Combobox.Empty per RESEARCH § Pitfall 1).
    - Test 8 (grep armor — copy verbatim): `grep -c 'Couldn.t find that brand.*add as' src/components/watch/BrandPicker.tsx` returns exactly 1 (D-82-05 verbatim copy — including the em-dash between "brand" and "add").
  </behavior>
  <action>
    Create `src/components/watch/BrandPicker.tsx` following RESEARCH § Pattern 1 (L200-289) and PATTERNS.md § BrandPicker.tsx verbatim as a skeleton. Concrete implementation checklist:

    (1) `'use client'` first line. Imports: `import { useState, useMemo } from 'react'`; `import { Combobox } from '@base-ui/react/combobox'`; optionally `import { cn } from '@/lib/utils'` if you use it.

    (2) Define local `type Brand = { id: string; name: string }` (or inline it in props — pick one and be consistent).

    (3) Props interface exactly as in `<interfaces>` block above. `onCouldntFind` is OPTIONAL.

    (4) Component body:
       - `const [inputValue, setInputValue] = useState(value?.name ?? '')` — seeded from current value's name
       - `const [open, setOpen] = useState(false)` — controlled open state
       - `const filteredBrands = useMemo(() => brands.filter(b => b.name.toLowerCase().includes(inputValue.toLowerCase().trim())), [brands, inputValue])`

    (5) Return `<Combobox.Root<Brand>` with the exact prop pairing from the `<interfaces>` block. Copy the `onInputValueChange` guard verbatim from SearchEntry L214-218 (details.reason !== 'input-change' → early return). MUST pair `filteredItems={filteredBrands}` with `filter={null}` — RESEARCH § Anti-Patterns.

    (6) Inside: `<Combobox.Input aria-label="Search brands" placeholder="Search brands…" ... />` — the exact input className is discretionary but MUST render an accessible role="combobox" input.

    (7) `<Combobox.Portal>` → `<Combobox.Positioner sideOffset={4} align="start">` → `<Combobox.Popup className="z-50 w-[var(--anchor-width)] overflow-hidden rounded-md border bg-popover text-popover-foreground shadow-md">` — verbatim from SearchEntry L246-248 per PATTERNS.md L97-99.

    (8) Inside `<Combobox.Popup>`:
       (a) Conditional `{filteredBrands.length > 0 && (<Combobox.List>...map(b => <Combobox.Item key={b.id} value={b} className="{ITEM_HIGHLIGHT_CLASS}">{b.name}</Combobox.Item>)</Combobox.List>)}` — item class MUST use `data-[highlighted]:bg-accent data-[highlighted]:text-accent-foreground` (NOT bg-primary — `[[accent-is-active-token]]`).
       (b) SIBLING (NOT inside `<Combobox.List>`) — the UI-02 affordance button gated on `{filteredBrands.length === 0 && inputValue.trim().length > 0 && onCouldntFind && (<button type="button" data-testid="brand-picker-couldnt-find" onClick={() => { onCouldntFind(inputValue.trim()); setOpen(false); }} className="mt-1 flex w-full items-center gap-2 rounded-md px-3 py-3 text-sm text-muted-foreground hover:bg-muted hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring min-h-[44px]">Couldn&apos;t find that brand — add as &ldquo;{inputValue.trim()}&rdquo;</button>)}`

    (9) NO Combobox.Empty usage — SRCH-03 lesson. Grep armor asserts zero occurrences.

    (10) Copy in the affordance button MUST be verbatim `Couldn't find that brand — add as "{typed}"` (roadmap language per D-82-05). Use HTML entities `&apos;` for the apostrophe and `&ldquo;`/`&rdquo;` for the smart quotes around `{typed}` — grep check will match on `Couldn.t find that brand.*add as` which accepts either encoding.

    Create `src/components/watch/BrandPicker.test.tsx` with all 8 tests above. Use `@testing-library/react` + `userEvent` + `vitest`. Structure per PATTERNS.md § BrandPicker.test.tsx (L110-140) + SearchEntry.test.tsx analog. Prefer `userEvent` for the affordance-click test so that the click event fires with realistic pointer + keyboard sequences. For popup-close assertion after click, prefer `waitFor(() => expect(screen.queryByRole('listbox')).not.toBeInTheDocument())` — the popup close is not always synchronous.
  </action>
  <verify>
    <automated>test -f src/components/watch/BrandPicker.tsx && test -f src/components/watch/BrandPicker.test.tsx && grep -c "Combobox.Empty" src/components/watch/BrandPicker.tsx | awk '{ if ($1 == 0) exit 0; else exit 1 }' && grep -c 'Couldn.t find that brand.*add as' src/components/watch/BrandPicker.tsx | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c 'filter={null}' src/components/watch/BrandPicker.tsx | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "data-\\[highlighted\\]:bg-accent" src/components/watch/BrandPicker.tsx | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && npx vitest run src/components/watch/BrandPicker.test.tsx --reporter=basic 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `src/components/watch/BrandPicker.tsx` exists, exports `BrandPicker` with the props shape in `<interfaces>`
    - No `Combobox.Empty` usage anywhere in BrandPicker.tsx
    - The verbatim D-82-05 copy string appears in the file
    - `filter={null}` paired with `filteredItems=` in Combobox.Root
    - Highlighted item uses `data-[highlighted]:bg-accent data-[highlighted]:text-accent-foreground` (not bg-primary)
    - All 8 tests in `BrandPicker.test.tsx` pass under `npx vitest run src/components/watch/BrandPicker.test.tsx`
    - `npm run build` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire BrandPicker into StructuredEntryPanel + thread brands through SearchEntry</name>
  <read_first>
    - src/components/watch/StructuredEntryPanel.tsx (READ full; focus on L200-260 area with the Brand Input at L220-228 per PATTERNS.md L538-548 — replace it)
    - src/components/watch/StructuredEntryPanel.test.tsx (existing tests — extend, do not rewrite)
    - src/components/watch/SearchEntry.tsx L340-400 area (identify where StructuredEntryPanel is mounted inside SearchEntry's no-match path — per PATTERNS.md L580-581 the "L344 area"; the exact line may vary — trust what the file shows)
    - src/components/watch/BrandPicker.tsx (just-written — reference for prop shape)
    - .planning/phases/82-add-watch-ui-operator-admin/82-PATTERNS.md § StructuredEntryPanel.tsx (L534-551) + § AddWatchFlow.tsx (L556-582)
    - .planning/phases/82-add-watch-ui-operator-admin/82-CONTEXT.md D-82-03 + D-82-04 (silent auto-create on Find specs; picker locks typed value with value.id=null)
    - src/components/watch/AddWatchFlow.tsx (Plan 01 output — brandsWithIds prop now on SearchEntry mount; confirm)
  </read_first>
  <behavior>
    - Test 1 (SEP threads brands to BrandPicker): Mount StructuredEntryPanel with `brands={[{id:'a',name:'Omega'}]}` (mock all other required props) and mock BrandPicker via `vi.mock('@/components/watch/BrandPicker', () => ({ BrandPicker: (props) => <div data-testid="brand-picker-mock" data-brand-count={props.brands.length} /> }))`. Assert `data-testid="brand-picker-mock"` present with `data-brand-count="1"`.
    - Test 2 (SEP raw brand Input removed): Grep armor — `grep -c 'id="se-brand"' src/components/watch/StructuredEntryPanel.tsx` returns 0 (the raw Input at L220-228 is replaced).
    - Test 3 (SEP mounts BrandPicker): Grep — `grep -c '<BrandPicker' src/components/watch/StructuredEntryPanel.tsx` returns exactly 1.
    - Test 4 (POST body preserved): Existing StructuredEntryPanel.test.tsx tests that assert the POST body shape (`{ brand, model, reference, ... }` to /api/extract-watch on Find specs click) still pass — the outbound `brand: brand.trim()` value derives from BrandPicker's value.name (on selection) OR from the locked typed value (on onCouldntFind), but the string reaches the fetch body unchanged.
    - Test 5 (SearchEntry forwards brandsWithIds): Grep — `grep -c "brandsWithIds" src/components/watch/SearchEntry.tsx` returns ≥2 (prop-drill in and out of SearchEntry to the StructuredEntryPanel mount site).
    - Test 6 (UI-02 affordance wired): When BrandPicker fires `onCouldntFind('X')`, StructuredEntryPanel's brand-string state is set to 'X' AND `selectedBrand.id` (or however the panel tracks brand identity) is set to null. This is the "locked typed value" per D-82-03. Test with the real (unmocked) BrandPicker + a real userEvent.click on the affordance button.
  </behavior>
  <action>
    (1) In `src/components/watch/StructuredEntryPanel.tsx`:
       - Add a new prop `brands: { id: string; name: string }[]` to the panel's props interface (destructure from props).
       - Add local state to track the picker's value shape: `const [selectedBrand, setSelectedBrand] = useState<{ id: string; name: string } | null>(null)` (or reuse existing brand-string state if you can express both — planner discretion). The panel's outbound `brand` string is either `selectedBrand?.name` (selection) OR the last-typed value when the affordance fired (locked value; brand.id = null). One clean approach: keep a single `brand: string` state (existing) driven by the picker's selection OR the affordance's typed value; drop the raw setter tied to keystroke input.
       - REPLACE the raw brand `<Input id="se-brand" ... />` block (L220-228 per PATTERNS.md L540-548) with:
         `<BrandPicker brands={brands} value={selectedBrand} onChange={(next) => { setSelectedBrand(next); setBrand(next.name); }} onCouldntFind={(typed) => { setSelectedBrand(null); setBrand(typed); }} />`
         (adjust identifiers to whatever the existing panel state uses — the semantic is: onChange updates identity+string, onCouldntFind clears identity and locks the string).
       - Preserve the surrounding Label + form structure. `aria-required="true"` may remain on a parent wrapper if the existing panel exposes required semantics; the BrandPicker's Combobox.Input handles the input role.
       - Do NOT change the POST body construction — the outbound `brand: brand.trim()` line stays as-is. The picker constrains what CAN be typed; the string that reaches the route is unchanged in shape.
       - `disabled={isExtracting}` semantics from the original Input: BrandPicker doesn't currently accept a `disabled` prop per D-82-01 — planner discretion: (a) add `disabled?: boolean` to BrandPicker's props NOW and gate the Combobox.Input's disabled attribute on it (preferred — small addition), OR (b) leave the picker enabled during extraction (mildly awkward but not blocking). Recommend option (a): add `disabled?: boolean` to BrandPicker props and forward to `<Combobox.Input disabled={disabled}>`; add a test case for it if you take option (a).

    (2) In `src/components/watch/SearchEntry.tsx`:
       - Extend the SearchEntry props interface to accept `brandsWithIds: { id: string; name: string }[]`.
       - Destructure `brandsWithIds` from props.
       - Forward it to the `<StructuredEntryPanel ... />` mount site (around L344 per PATTERNS.md L581) as `brands={brandsWithIds}`.
       - If SearchEntry does NOT mount StructuredEntryPanel directly (i.e., the panel is mounted by AddWatchFlow instead), then this step is a no-op inside SearchEntry — verify against the actual code. Plan 01's task description ALREADY drilled brandsWithIds to whichever child actually mounts the panel; this task just wires the panel's new `brands` prop consumption.

    (3) In `src/components/watch/StructuredEntryPanel.test.tsx`:
       - Add ONE new test case: mount SEP with a mocked BrandPicker (per PATTERNS.md L120-127 pattern) and a non-empty `brands` array; assert the mock renders with the correct `data-brand-count`.
       - Do NOT rewrite existing tests. If existing tests fail because SEP's props shape widened, update the mock props they pass to include `brands={[]}` (minimal extension).

    (4) Verify the outbound POST body is not accidentally broken by grepping the current file: `grep -c "brand: brand.trim()" src/components/watch/StructuredEntryPanel.tsx` should still return the same count as before this task (>= 1 — the extract POST body construction).

    Per D-82-03: the panel does NOT need to know whether the brand was matched vs auto-created. The Find specs click POSTs the brand string to /api/extract-watch, which runs Phase 80's resolver (frozen contract), which either matches or auto-creates + sets `needs_review=true`. The affordance-locked value flows through the same code path as a picker-selected value — zero new backend surface.
  </action>
  <verify>
    <automated>grep -c 'id="se-brand"' src/components/watch/StructuredEntryPanel.tsx | awk '{ if ($1 == 0) exit 0; else exit 1 }' && grep -c "<BrandPicker" src/components/watch/StructuredEntryPanel.tsx | awk '{ if ($1 == 1) exit 0; else exit 1 }' && grep -c "brandsWithIds" src/components/watch/SearchEntry.tsx | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && npx vitest run src/components/watch/StructuredEntryPanel.test.tsx src/components/watch/BrandPicker.test.tsx --reporter=basic 2>&1 | tail -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - StructuredEntryPanel mounts `<BrandPicker>` in place of the raw `<Input id="se-brand">`
    - SearchEntry forwards `brandsWithIds` down to the StructuredEntryPanel `brands` prop
    - Existing StructuredEntryPanel tests still pass; a new test asserts the prop threading
    - BrandPicker.test.tsx all tests still pass
    - `npm run build` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client input → picker state | User types free text into Combobox.Input; picker selectively locks it as `value` |
| Client → /api/extract-watch | Panel POSTs brand string on Find specs; Phase 80 resolver contract enforces canonical or needs_review=true auto-create |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-P02-01 | Tampering | Client picker state | accept | Picker is client-side UX only; the security surface is /api/extract-watch (Phase 80 frozen, unchanged); no auth-relevant state lives on picker |
| T-82-P02-02 | Information Disclosure | Brand list rendered client-side | accept | Same as T-82-P01-01 — brand names are public catalog data (already shipped to catalog UI) |
| T-82-P02-03 | Cross-Site Scripting via typed brand string | inline in affordance copy | mitigate | React auto-escapes JSX text nodes; `{inputValue.trim()}` is a text child, not `dangerouslySetInnerHTML`; no injection surface |
| T-82-P02-04 | UX confusion (SRCH-03 recurrence) — click on affordance swallowed by listbox | Combobox.Popup layout | mitigate | Affordance rendered as SIBLING of Combobox.List per D-82-05; grep armor asserts `Combobox.Empty` count = 0; unit test asserts click fires callback AND popup closes |
</threat_model>

<verification>
- All BrandPicker unit tests pass under `npx vitest run src/components/watch/BrandPicker.test.tsx`
- Extended StructuredEntryPanel test passes; existing tests continue to pass
- `npm run build` exits 0
- Grep armor: no Combobox.Empty; verbatim D-82-05 copy present; filter={null}; bg-accent for highlighted (not bg-primary)
- Manual sanity: on `npm run dev`, open `/watch/new`, click "Not finding it? Add manually" to reach the structured panel, type an unknown brand → the "Couldn't find that brand — add as X" affordance appears; click it → popup closes and typed value locks (Plan 06 walkthrough validates end-to-end)
</verification>

<success_criteria>
- BrandPicker component exists with the D-82-01 shape and all a11y/copy/gate requirements from UI-SPEC § Interaction Contracts
- UI-02 affordance placement respects the SRCH-03 lesson (sibling of List, not inside)
- `[[assert-disappearance-too]]` satisfied — test asserts both callback fire AND popup close
- StructuredEntryPanel Brand field is a canonical picker, not a raw Input, on /watch/new
- Outbound POST to /api/extract-watch is unchanged in shape — Phase 80 resolver contract still frozen
</success_criteria>

<output>
After completion, create `.planning/phases/82-add-watch-ui-operator-admin/82-02-SUMMARY.md`
</output>
