---
phase: 78
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/static/phase78-schema-shape.test.ts
  - tests/integration/migrations/78-gin-index.test.ts
  - tests/integration/scripts/v8.4-brand-canonicalization.test.ts
  - tests/unit/scripts/v8.4-md-artifact-schema.test.ts
  - tests/unit/scripts/v8.4-seed021-golden.test.ts
  - tests/unit/scripts/v8.4-regenerate-merge.test.ts
  - tests/integration/scripts/v8.4-readonly.test.ts
autonomous: true
requirements: [CANON-03, CANON-04, MIG-01]
tags: [schema-migration, dry-run-script, wave-0-stubs]
must_haves:
  truths:
    - "Every test file required by 78-VALIDATION.md exists and is registered by vitest"
    - "Every test file uses `it.todo()` for unimplemented assertions per the Phase 77 Wave 0 RED stub convention (it.todo not it.skip)"
    - "The static schema-shape test file declares `// @vitest-environment node` on line 1 per `[[vitest-static-node-env]]`"
    - "Each DATABASE_URL-gated integration file uses the `process.env.DATABASE_URL ? describe : describe.skip` idiom (per `tests/integration/migration-drop-archetype.test.ts:26`)"
    - "Running `npm run test -- 78` returns >0 passing tests and 0 failing tests (todo callsites report as `↓ todo`, not failures), satisfying D-VALIDATION sampling rate"
  artifacts:
    - path: "tests/static/phase78-schema-shape.test.ts"
      provides: "static fs-walking guard for `src/db/schema.ts` containing aliases + needsReview"
      contains: "// @vitest-environment node"
    - path: "tests/integration/migrations/78-gin-index.test.ts"
      provides: "DATABASE_URL-gated psql introspection for pg_indexes GIN on watch_families(aliases)"
      contains: "process.env.DATABASE_URL"
    - path: "tests/integration/scripts/v8.4-brand-canonicalization.test.ts"
      provides: "End-to-end stub: runs script against local Supabase, asserts artifact written"
      contains: "process.env.DATABASE_URL"
    - path: "tests/unit/scripts/v8.4-md-artifact-schema.test.ts"
      provides: "Parses `.planning/v8.4-brand-merge-decisions.md`, asserts GFM table columns + row count"
    - path: "tests/unit/scripts/v8.4-seed021-golden.test.ts"
      provides: "Golden test: SEED-021 four cases (Hamilton/Hamilton Watch, Omega/OMEGA, Héron/Héron Watches, Brut Date/Brut Datejust) land in needs-review"
    - path: "tests/unit/scripts/v8.4-regenerate-merge.test.ts"
      provides: "Given prior .md with `merge:<uuid>` and `new` rows, `--regenerate` preserves them"
    - path: "tests/integration/scripts/v8.4-readonly.test.ts"
      provides: "Pre/post catalog COUNT(*) unchanged after running dry-run (D-78-05 enforcement)"
  key_links:
    - from: "tests/static/phase78-schema-shape.test.ts"
      to: "src/db/schema.ts"
      via: "readFileSync + grep for `aliases:` and `needsReview:`"
      pattern: "readFileSync.*schema.ts"
    - from: "tests/integration/migrations/78-gin-index.test.ts"
      to: "pg_indexes via postgres-lib SELECT"
      via: "SELECT indexname FROM pg_indexes WHERE indexname = 'watch_families_aliases_gin_idx'"
      pattern: "watch_families_aliases_gin_idx"
---

<objective>
Create Wave 0 RED test stubs per 78-VALIDATION.md so Plans 02 + 03 have failing-test scaffolds to drive against. Every stub uses `it.todo()` per the Phase 77 Wave 0 convention so vitest discovery returns a positive 1-passed signal per file without producing red failures before implementation.

Purpose: satisfy the Nyquist sampling-rate contract (no 3 consecutive implementation tasks without automated verify) and surface the SEED-021 golden + readonly invariant as explicit, named test files BEFORE the migration or script is written. Without these stubs, Plans 02/03 have no automation gate.

Output: 7 test files committed under tests/static/, tests/integration/migrations/, tests/integration/scripts/, tests/unit/scripts/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-CONTEXT.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-RESEARCH.md
@.planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md

<interfaces>
<!-- Patterns the Wave 0 stubs must mirror -->

From tests/static/legacy-watch-routes.test.ts (lines 1-40):
- Line 1 MUST be `// @vitest-environment node` (no leading whitespace, no JSDoc above it) — vitest reads this magic comment to switch envs; failure to do so makes `readdirSync`/`readFileSync` undefined on the Vercel prebuild build.
- Imports: `import { describe, it, expect } from 'vitest'` + `import { readFileSync, readdirSync, statSync, existsSync } from 'node:fs'` + `import { join } from 'node:path'`.

From tests/integration/migration-drop-archetype.test.ts (line 26):
- Idiom: `const maybe = process.env.DATABASE_URL ? describe : describe.skip` then `maybe('Phase NN — description', () => { ... })`.

From the v6.0 Phase 77 Wave 0 convention (STATE.md "Phase 77 Plan 01 outcomes — 2026-06-23"):
- First-line marker comment: `// Wave 0 RED stub — Phase 78 / 78-01-PLAN.md` (after the `// @vitest-environment node` line on static files).
- Each file has ≥1 sanity `it('Wave 0 stub registers in vitest discovery', () => expect(true).toBe(true))` plus `it.todo(...)` callsites for every behavior Plan 02/03 will eventually implement.
- `it.todo` NOT `it.skip` — vitest reports `↓ todo` (positive signal) vs `× failed` (negative signal).
</interfaces>

<read_first>
- tests/static/legacy-watch-routes.test.ts (lines 1-40) — line-1 env directive + fs import pattern
- tests/integration/migration-drop-archetype.test.ts (lines 1-50) — DATABASE_URL-gated `maybe` idiom
- tests/integration/phase34-rls.test.ts (lines 1-30) — stronger localhost-guarded variant of the maybe idiom (informational only — Plan 01 uses the simpler `maybe` from migration-drop-archetype)
- .planning/phases/78-schema-additions-operator-resolve-queue/78-CONTEXT.md — D-78-01..08 grammar so the unit stubs target the right artifact shape
- .planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md — per-task verification map (lines 38-49); list of Wave 0 stubs (lines 56-62)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static schema-shape stub + GIN-index integration stub</name>
  <files>tests/static/phase78-schema-shape.test.ts, tests/integration/migrations/78-gin-index.test.ts</files>
  <read_first>
    - tests/static/legacy-watch-routes.test.ts (lines 1-40)
    - tests/integration/migration-drop-archetype.test.ts (lines 1-50)
    - src/db/schema.ts (lines 518-555) — current brands + watchFamilies shape so the stub knows what to grep for
    - .planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md (lines 56-62) — Wave 0 stub list
  </read_first>
  <action>
    Create `tests/static/phase78-schema-shape.test.ts`. Line 1 = `// @vitest-environment node` (per `[[vitest-static-node-env]]`). Line 2 = `// Wave 0 RED stub — Phase 78 / 78-01-PLAN.md`. Import `describe, it, expect` from vitest; `readFileSync` from `node:fs`; `join` from `node:path`. Use `describe('Phase 78 — schema shape guard (static)', () => { ... })`. Include one sanity test that registers (`it('Wave 0 stub registers in vitest discovery', () => expect(true).toBe(true))`). Add `it.todo('src/db/schema.ts brands declares needsReview: boolean(...).notNull().default(false)')`, `it.todo('src/db/schema.ts watchFamilies declares aliases: text(...).array().notNull().default(...)')`, `it.todo('src/db/schema.ts watchFamilies declares needsReview: boolean(...).notNull().default(false)')`. Do NOT yet implement the grep — Plan 02 will green this.

    Create `tests/integration/migrations/78-gin-index.test.ts`. No `// @vitest-environment node` directive needed (no fs-walking). First line: `// Wave 0 RED stub — Phase 78 / 78-01-PLAN.md`. Use DATABASE_URL-gated idiom: `const maybe = process.env.DATABASE_URL ? describe : describe.skip` (per D-VALIDATION line 56). Inside `maybe('Phase 78 — watch_families_aliases_gin_idx exists', () => { ... })`, include one sanity passing test + `it.todo('watch_families_aliases_gin_idx exists in pg_indexes')` + `it.todo('brands.needs_review column exists with default false')` + `it.todo('watch_families.needs_review column exists with default false')` + `it.todo('existing rows backfill needs_review to false (count zero where IS NOT FALSE)')`.

    Create parent directory `tests/integration/migrations/` if it does not exist. Verify file paths in frontmatter match files created.
  </action>
  <verify>
    <automated>npm run test -- tests/static/phase78-schema-shape.test.ts tests/integration/migrations/78-gin-index.test.ts 2>&1 | grep -E "(passed|todo|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/static/phase78-schema-shape.test.ts` exists; line 1 is exactly `// @vitest-environment node`
    - File `tests/integration/migrations/78-gin-index.test.ts` exists; uses `process.env.DATABASE_URL ? describe : describe.skip` idiom
    - `npm run test -- tests/static/phase78-schema-shape.test.ts` exits 0 with at least 1 passed + ≥3 todo entries; zero failed
    - `npm run test -- tests/integration/migrations/78-gin-index.test.ts` either exits 0 with passed+todo entries (DATABASE_URL set) or with `describe.skip` skipped (DATABASE_URL unset); zero failed in either case
    - Neither file imports anything from Plans 02 or 03 source files (no premature dependency)
  </acceptance_criteria>
  <done>
    Both files exist, vitest discovers them, every assertion is `it.todo()` plus one sanity `it()`, no `failed` count.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dry-run script unit + integration stubs (5 files)</name>
  <files>tests/integration/scripts/v8.4-brand-canonicalization.test.ts, tests/unit/scripts/v8.4-md-artifact-schema.test.ts, tests/unit/scripts/v8.4-seed021-golden.test.ts, tests/unit/scripts/v8.4-regenerate-merge.test.ts, tests/integration/scripts/v8.4-readonly.test.ts</files>
  <read_first>
    - tests/integration/migration-drop-archetype.test.ts (lines 1-50) — DATABASE_URL-gated `maybe` pattern
    - tests/static/legacy-watch-routes.test.ts (lines 1-40) — fs-walking idiom (the unit stubs DON'T need this directive — they will not call readFileSync against gitignored paths; they parse output strings)
    - .planning/phases/78-schema-additions-operator-resolve-queue/78-CONTEXT.md (D-78-01 GFM columns, D-78-02 status grammar, D-78-03 candidates format, D-78-04 exact-only, D-78-05 read-only, D-78-07 refuse-to-overwrite + --regenerate merge-forward)
    - .planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md (lines 38-62) — task-to-test mapping
  </read_first>
  <action>
    Create parent directories `tests/integration/scripts/` and `tests/unit/scripts/` if they do not exist.

    `tests/integration/scripts/v8.4-brand-canonicalization.test.ts` — DATABASE_URL-gated. First line `// Wave 0 RED stub — Phase 78 / 78-01-PLAN.md`. Sanity `it()` + `it.todo('script connects via DATABASE_URL service-role and writes .planning/v8.4-brand-merge-decisions.md')` + `it.todo('artifact is non-empty after successful run')` + `it.todo('exit code 0 on first successful run')`.

    `tests/unit/scripts/v8.4-md-artifact-schema.test.ts` — no DB gate; pure parser test. First line marker. Sanity + `it.todo('output .md has GFM table header `| brand_raw | normalized | proposed_target_id | status | candidates / notes |` per D-78-01')` + `it.todo('row count equals distinct lower(trim(watches_catalog.brand)) values')` + `it.todo('every row has exactly 5 cells (no embedded pipes leaking)')`.

    `tests/unit/scripts/v8.4-seed021-golden.test.ts` — golden test, no DB gate (fixture-based). Sanity + `it.todo('Hamilton vs Hamilton Watch lands status=needs-review per D-78-04')` + `it.todo('Héron vs Héron Watches lands status=needs-review per D-78-04')` + `it.todo('Brut Date vs Brut Datejust lands status=needs-review per D-78-04')` + `it.todo('Omega/OMEGA both land status=auto-resolved with the SAME proposed_target_id per D-78-04 + B-78-01 (case-collapse to single canonical brands row)')` + `it.todo('candidates / notes cell carries top 3 fuzzy candidates with score format `name (0.XX)` per D-78-03')`. NOTE: The Omega/OMEGA case is the case-collapse / auto-resolve scenario (both normalize to `omega` and exact-match a single canonical brands row). The other three SEED-021 cases land in needs-review per D-78-04 because they carry distinct `name_normalized` values.

    `tests/unit/scripts/v8.4-regenerate-merge.test.ts` — no DB gate (writes temp files). Sanity + `it.todo('default run with existing .md exits non-zero per D-78-07')` + `it.todo('--regenerate preserves rows whose status is merge:<uuid> verbatim')` + `it.todo('--regenerate preserves rows whose status is `new` verbatim')` + `it.todo('--regenerate preserves rows whose status is `skip` verbatim')` + `it.todo('--regenerate overwrites rows whose status is `needs-review`')` + `it.todo('--regenerate appends fresh brand_raw rows not in prior .md at the bottom')` + `it.todo('--force overwrites unconditionally regardless of existing rows')`.

    `tests/integration/scripts/v8.4-readonly.test.ts` — DATABASE_URL-gated. Sanity + `it.todo('SELECT count(*) FROM brands before == after running dry-run (D-78-05)')` + `it.todo('SELECT count(*) FROM watch_families before == after')` + `it.todo('SELECT count(*) FROM watches_catalog before == after')` + `it.todo('SELECT max(updated_at) FROM brands before == after (no UPDATE fired)')`.

    All 5 files: `it.todo(...)` literals embed the decision IDs (D-78-XX) so the verifier can cite them.
  </action>
  <verify>
    <automated>npm run test -- tests/integration/scripts/v8.4-brand-canonicalization.test.ts tests/unit/scripts/v8.4-md-artifact-schema.test.ts tests/unit/scripts/v8.4-seed021-golden.test.ts tests/unit/scripts/v8.4-regenerate-merge.test.ts tests/integration/scripts/v8.4-readonly.test.ts 2>&1 | grep -E "(passed|todo|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist at the paths above
    - Each file's first non-blank line is the `// Wave 0 RED stub — Phase 78 / 78-01-PLAN.md` marker (or `// @vitest-environment node` followed by it for fs-walking variants — none of these 5 walk fs, so the marker is line 1)
    - Each DATABASE_URL-gated file uses the `process.env.DATABASE_URL ? describe : describe.skip` idiom
    - `npm run test -- tests/unit/scripts/v8.4-seed021-golden.test.ts` reports ≥4 `it.todo` entries explicitly naming ALL four SEED-021 cases (Hamilton, Omega, Héron, Brut Date) — three lands in `needs-review` (Hamilton/Héron/Brut Date), one lands in `auto-resolved` case-collapse (Omega/OMEGA per B-78-01)
    - Running all 5 files with `npm run test --` returns zero `failed`
    - Verify with `grep -l "D-78-04" tests/unit/scripts/v8.4-seed021-golden.test.ts` returns the file path (decision ID citation present)
    - Verify with `grep -l "D-78-07" tests/unit/scripts/v8.4-regenerate-merge.test.ts` returns the file path
    - Verify with `grep -l "D-78-05" tests/integration/scripts/v8.4-readonly.test.ts` returns the file path
  </acceptance_criteria>
  <done>
    All 5 stub files exist, vitest discovery shows 0 failed, decision IDs cited in stubs for downstream traceability.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip 78-VALIDATION.md wave_0_complete + verify full Wave 0 set with one command</name>
  <files>.planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md</files>
  <read_first>
    - .planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md (full file — line 6 is `wave_0_complete: false`)
  </read_first>
  <action>
    Edit `.planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md`. Change frontmatter line 6 from `wave_0_complete: false` to `wave_0_complete: true`. Change line 4 from `nyquist_compliant: false` to `nyquist_compliant: true` (we now have automated verify for every Phase 78 task per the matrix).

    In the "Validation Sign-Off" section (bottom of file), tick all 6 checkboxes from `[ ]` to `[x]`. Change `**Approval:** pending` to `**Approval:** Wave 0 stubs landed via 78-01-PLAN.md`.

    Confirm the test filename references on lines 41 + 56 already match `tests/static/phase78-schema-shape.test.ts` (W-78-02 fix). If any line still reads `tests/static/schema-78-columns.test.ts`, replace it with `tests/static/phase78-schema-shape.test.ts` — the canonical name used in Plans 01/02 and RESEARCH.md.

    Update the Per-Task Verification Map "File Exists" column entries for tasks 78-01-01 through 78-02-05: change `❌ W0` to `✅ W0` for the 7 Wave 0 rows.

    Do NOT change the `Status: ⬜ pending` column entries — they remain pending until Plans 02/03 green them.
  </action>
  <verify>
    <automated>grep -c "wave_0_complete: true" .planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "wave_0_complete: true" .planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md` returns 1
    - `grep -c "nyquist_compliant: true" .planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md` returns 1
    - `grep -c "Approval:.*pending" .planning/phases/78-schema-additions-operator-resolve-queue/78-VALIDATION.md` returns 0
    - Running `npm run test -- tests/static/phase78 tests/integration/migrations/78 tests/integration/scripts/v8.4 tests/unit/scripts/v8.4` exits 0 with 0 failed (todo entries OK; sanity tests pass)
  </acceptance_criteria>
  <done>
    `wave_0_complete: true`, `nyquist_compliant: true`, sign-off block ticked, all 7 stub files green-discovered by vitest.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→test-files | Test files are dev-time only; never shipped to prod. No runtime trust boundary crossed in Plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01-01 | Tampering | Wave 0 stub files | accept | Stubs use `it.todo()` — if a malicious edit replaces `todo` with a hard-coded `expect(true).toBe(true)`, downstream Plans 02/03 verifier will catch the missing real assertion via the `Status: ⬜ pending` column tracking in 78-VALIDATION.md (cross-doc reconciliation per VID-15-class guard) |
| T-78-01-02 | Information Disclosure | DATABASE_URL in test env | accept | `[[local-first-dev]]` enforces local-only DATABASE_URL for integration tests; `tests/integration/phase34-rls.test.ts:21` shows the prod-URL-refuses idiom (Plan 03 will adopt for the script's integration test if a prod-targeted stub is added) |
</threat_model>

<verification>
After all 3 tasks:
- 7 Wave 0 stub files exist at paths matching frontmatter
- `npm run test -- 78 v8.4` returns 0 failed; ≥7 sanity passed; ≥25 todo entries
- 78-VALIDATION.md frontmatter has `wave_0_complete: true` + `nyquist_compliant: true`
- No source files (under `src/`) modified by this plan
- No `supabase/migrations/` or `scripts/` files created by this plan
</verification>

<success_criteria>
- All 7 test files listed in `files_modified` exist
- Vitest discovery shows the stubs; zero failed; zero implementation in stub bodies (every assertion is `it.todo()` except sanity `it()` checks)
- 78-VALIDATION.md updated to reflect Wave 0 readiness
- Plan 02 + Plan 03 can begin with a green Wave 0 gate
</success_criteria>

<output>
After completion, create `.planning/phases/78-schema-additions-operator-resolve-queue/78-01-SUMMARY.md` per the standard summary template.
</output>
