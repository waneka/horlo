---
phase: 79
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - scripts/v8.4-brand-canonicalization.ts
  - tests/unit/scripts/v8.4-post-deploy-template.test.ts
  - tests/integration/scripts/v8.4-apply-atomic.test.ts
  - tests/integration/scripts/v8.4-apply-idempotent.test.ts
autonomous: true
requirements: [MIG-04, DISP-03]
tags: [migration, postgres, tsx-script, atomic-transaction, hydration, post-flight]

must_haves:
  truths:
    - "scripts/v8.4-brand-canonicalization.ts main() --apply --mode=both branch wraps applyBrandPath + applyFamilyPath + hydration + post-flight assertion in ONE sql.begin(async tx => ...) callback per D-79-03"
    - "Hydration (DISP-03, Step 4.6) is a single UPDATE FROM JOIN: UPDATE watches w SET brand = b.name, model = f.name FROM watches_catalog c JOIN brands b ON c.brand_id = b.id JOIN watch_families f ON c.family_id = f.id WHERE w.catalog_id = c.id — unconditional per D-79-08; touches brand + model columns only; preserves notes/serial/reference/all other columns"
    - "Post-flight assertion (MIG-04, Step 4.7) uses POSITIVE predicate IS DISTINCT FROM NULL per [[post-flight-assertion-predicate-divergence]]; throws Error inside sql.begin callback on mismatch → auto-rolls back; assertion phrasing differs structurally from any UPDATE WHERE in Steps 4.1-4.6"
    - "renderPostDeployMarkdown is a pure exported function generating the .planning/phases/79-backfill-migration-display-hydration/79-POST-DEPLOY.md content from {counts, postFlightQuery, postFlightResult, isLocal} per D-79-10"
    - "writePostDeployArtifact (non-exported wrapper) calls renderPostDeployMarkdown + mkdir + writeFile AFTER the sql.begin transaction commits (post-success only — never written if assertion rolls back)"
    - "Confirmation gate (D-79-02) fires BEFORE the sql.begin opens — confirmIfProd prints summary + reads yes from stdin on non-local URL; silent on local"
    - "Apply summary block printed AFTER successful commit reports: brandsCreated, catalogRowsResolvedBrand, familiesCreated, aliasesAppended, catalogRowsResolvedFamily, userWatchesHydrated"
    - "ALL Plan 01 integration stub it.todo callsites green: MIG-02 brand_id resolved, MIG-02 Hamilton merge, MIG-02 needs_review=false on new brands, MIG-03 family_id resolved, MIG-03 alias idempotent on re-apply, MIG-04 positive predicate + forced-fail rollback, DISP-03 hydration overwrites + preserves other columns, D-79-03 atomic rollback, D-79-10 POST-DEPLOY written"
    - "Plan 01 unit stub for renderPostDeployMarkdown (D-79-10) green; source-grep assertions (MIG-04 predicate divergence + D-79-08 unconditional hydration + D-79-09 needs_review=false) green"
    - "Idempotent integration: second --apply --mode=both run exits 0 with 'Already applied' per D-79-04; aliases array cardinality unchanged per D-79-06"
  artifacts:
    - path: scripts/v8.4-brand-canonicalization.ts
      provides: "Full --apply --mode=both branch with atomic 6-step transaction + post-flight assertion + auto-generated POST-DEPLOY artifact"
      exports: [renderPostDeployMarkdown]
    - path: tests/integration/scripts/v8.4-apply-atomic.test.ts
      provides: "Greened — all it.todo callsites for MIG-02/MIG-03/MIG-04/DISP-03 + D-79-03/D-79-09/D-79-10 converted to it() and pass against local DATABASE_URL"
    - path: tests/integration/scripts/v8.4-apply-idempotent.test.ts
      provides: "Greened — D-79-04 already-applied gate + D-79-06 alias cardinality stable on re-run"
    - path: tests/unit/scripts/v8.4-post-deploy-template.test.ts
      provides: "Greened — D-79-10 sections present + MIG-04 predicate divergence source-grep + D-79-08 unconditional hydration source-grep + D-79-09 needs_review=false source-grep"
  key_links:
    - from: "scripts/v8.4-brand-canonicalization.ts (main --apply --mode=both branch)"
      to: "sql.begin atomic transaction"
      via: "await sql.begin(async tx => { applyBrandPath; applyFamilyPath; hydration; postFlight })"
      pattern: "sql\\.begin\\(async\\s*\\(?tx\\)?\\s*=>"
    - from: "scripts/v8.4-brand-canonicalization.ts (Step 4.6 hydration)"
      to: "watches table brand + model columns"
      via: "UPDATE watches w SET brand = b.name, model = f.name FROM watches_catalog c JOIN brands b JOIN watch_families f WHERE w.catalog_id = c.id"
      pattern: "UPDATE watches w\\s+SET brand"
    - from: "scripts/v8.4-brand-canonicalization.ts (Step 4.7 post-flight)"
      to: "MIG-04 assertion"
      via: "SELECT count WHERE brand_id IS DISTINCT FROM NULL; throw new Error on mismatch"
      pattern: "IS DISTINCT FROM NULL"
    - from: "scripts/v8.4-brand-canonicalization.ts (writePostDeployArtifact)"
      to: ".planning/phases/79-backfill-migration-display-hydration/79-POST-DEPLOY.md"
      via: "renderPostDeployMarkdown + mkdir + writeFile (post-commit only)"
      pattern: "79-POST-DEPLOY\\.md"
---

<objective>
Wire the full --apply --mode=both atomic transaction: applyBrandPath (Plan 02) + applyFamilyPath (Plan 03) + hydration UPDATE FROM JOIN (DISP-03 / Step 4.6) + post-flight assertion with predicate divergence (MIG-04 / Step 4.7) — all inside ONE sql.begin callback per D-79-03. Add renderPostDeployMarkdown pure function + writePostDeployArtifact filesystem wrapper for D-79-10. Green ALL Plan 01 integration stubs against local DATABASE_URL.

Purpose: MIG-04 + DISP-03 — the highest-blast-radius write in v8.4. The atomic transaction is the rollback safety net; the post-flight assertion is the verification gate; the auto-generated POST-DEPLOY artifact is the audit trail. This is the plan that turns Phase 79 from "scripts that could run" into "scripts that have run against local DB with full assertions green."

Output: scripts/v8.4-brand-canonicalization.ts ~880 LOC (from Plan 03's ~700); 3 test files greened; one running end-to-end against local Supabase against the committed brand-merge-decisions.md fixture + a Plan 03-generated family-merge-decisions.md fixture (operator-edited inline by the test setup).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 79 context (decisions + research + patterns)
@.planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md
@.planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md
@.planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md
@.planning/phases/79-backfill-migration-display-hydration/79-03-SUMMARY.md

# The file Plan 04 extends + inputs the apply consumes
@scripts/v8.4-brand-canonicalization.ts
@.planning/v8.4-brand-merge-decisions.md

# Reference shape for POST-DEPLOY auto-generated artifact
@.planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md

# Schema reference
@src/db/schema.ts

# Stubs Plan 04 greens
@tests/unit/scripts/v8.4-post-deploy-template.test.ts
@tests/integration/scripts/v8.4-apply-atomic.test.ts
@tests/integration/scripts/v8.4-apply-idempotent.test.ts

# Phase 78 analog for integration test snapshot pattern
@tests/integration/scripts/v8.4-readonly.test.ts

<interfaces>
<!-- Plan 02 + Plan 03 exports — Plan 04 wires them together -->

From scripts/v8.4-brand-canonicalization.ts (Plans 02+03 shipped):
- export function isLocalDatabaseUrl(connStr: string): boolean
- export function parseDecisionsTable(content: string): BrandDecisionRow[]
- export function parseFamilyDecisionsTable(content: string): FamilyDecisionRow[]
- export function buildBrandMap(rows): BrandDecisionMap
- export function buildFamilyMap(rows, brandMap): FamilyDecisionMap
- export async function strictPreflightGate(sql, brandRows, familyRows, existingBrandIdsFn, existingFamilyIdsFn): Promise<void>
- async function idempotentReRunGate(sql): Promise<'already-applied' | 'proceed'>           // non-exported
- async function confirmIfProd(connStr, summary): Promise<void>                             // non-exported
- async function applyBrandPath(tx, brandMap, decisions): Promise<{ brandsCreated; catalogRowsResolvedBrand }>      // non-exported
- async function applyFamilyPath(tx, familyMap, decisions): Promise<{ familiesCreated; aliasesAppended; catalogRowsResolvedFamily }>   // non-exported

<!-- Phase 79 NEW exports landed by Plan 04 -->

NEW exported pure function:
- export interface ApplyCounts {
    brandsCreated: number;
    catalogRowsResolvedBrand: number;
    familiesCreated: number;
    aliasesAppended: number;
    catalogRowsResolvedFamily: number;
    userWatchesHydrated: number;
  }
- export interface PostDeployArgs {
    counts: ApplyCounts;
    postFlightQuery: string;
    postFlightResult: { total: number; resolvedBrand: number; resolvedFamily: number };
    isLocal: boolean;
    today?: string;  // injectable for unit testability
  }
- export function renderPostDeployMarkdown(args: PostDeployArgs): string

NEW non-exported helpers:
- async function applyHydration(tx): Promise<{ userWatchesHydrated: number }>           // Step 4.6 DISP-03
- async function postFlightAssertion(tx): Promise<{ total; resolvedBrand; resolvedFamily; postFlightQuery: string }>  // Step 4.7 MIG-04
- async function writePostDeployArtifact(args: PostDeployArgs): Promise<void>           // D-79-10 wrapper
- const POST_DEPLOY_PATH = path.join(process.cwd(), '.planning/phases/79-backfill-migration-display-hydration/79-POST-DEPLOY.md')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: applyHydration + postFlightAssertion + renderPostDeployMarkdown + writePostDeployArtifact + green post-deploy-template unit stub</name>

  <files>scripts/v8.4-brand-canonicalization.ts, tests/unit/scripts/v8.4-post-deploy-template.test.ts</files>

  <read_first>
- scripts/v8.4-brand-canonicalization.ts (current state after Plans 02+03 — applyBrandPath + applyFamilyPath shapes)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L513-540  (Pattern 5 — hydration UPDATE FROM JOIN with explicit JOINs)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L543-582  (Pattern 6 — post-flight assertion with predicate divergence)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L955-1088  (Code Examples — writePostDeployArtifact full inline template; 130 LOC)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L143-181  (hydration + post-flight pattern application)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L526-547  (POST-DEPLOY section structure; render pattern; Hamilton merge SQL block #2 to include verbatim)
- .planning/phases/78-schema-additions-operator-resolve-queue/78-POST-DEPLOY.md  (verbatim section structure to mirror)
- src/db/schema.ts L87-175  (watches table — Plan 04 hydration targets brand + model columns; must NOT touch other columns)
- src/db/schema.ts L431-510  (watches_catalog FKs the hydration JOINs through)
- .planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md L52-66  (D-79-08 + D-79-09 + D-79-10 verbatim)
- tests/unit/scripts/v8.4-post-deploy-template.test.ts  (Plan 01 RED stub — 8 it.todo callsites to green)
  </read_first>

  <behavior>
- renderPostDeployMarkdown is a PURE function (no FS, no DB, no Date.now if `today` is injected) — testable in vitest without environment mutation
- writePostDeployArtifact calls renderPostDeployMarkdown then mkdir + writeFile; runs AFTER the sql.begin transaction commits (post-success only)
- POST_DEPLOY artifact is written even when --apply targets local DB (per D-79-10 "auto-generated by the script after successful apply" — does not gate on isLocal). The artifact's `target` heading reports LOCAL vs PROD.
- applyHydration executes the Step 4.6 UPDATE FROM JOIN form per RESEARCH L518-527; result.count = userWatchesHydrated
- postFlightAssertion executes the Step 4.7 positive-predicate SELECT per RESEARCH L548-570; throws Error inside the sql.begin callback when resolvedBrand !== total or resolvedFamily !== total
  </behavior>

  <action>
In scripts/v8.4-brand-canonicalization.ts:

  (a) Define `const POST_DEPLOY_PATH = path.join(process.cwd(), '.planning/phases/79-backfill-migration-display-hydration/79-POST-DEPLOY.md')` near existing FAMILY_OUTPUT_FILE constant.

  (b) Add NEW exported `ApplyCounts` and `PostDeployArgs` interfaces per interfaces block.

  (c) Add NEW non-exported `async function applyHydration(tx): Promise<{ userWatchesHydrated: number }>` per PATTERNS L143-156:
     ```
     const result = await tx`
       UPDATE watches w
       SET brand = b.name,
           model = f.name
       FROM watches_catalog c
       JOIN brands b ON c.brand_id = b.id
       JOIN watch_families f ON c.family_id = f.id
       WHERE w.catalog_id = c.id
     `
     return { userWatchesHydrated: result.count }
     ```
     NO WHERE-clause filtering on watches.brand or watches.model text per D-79-08 — JOIN-only filter (the JOIN naturally skips watches.catalog_id IS NULL rows per Pitfall 5).

  (d) Add NEW non-exported `async function postFlightAssertion(tx): Promise<{ total: number; resolvedBrand: number; resolvedFamily: number; postFlightQuery: string }>` per PATTERNS L159-181:
     ```
     const postFlightQuery = `SELECT (SELECT count(*) FROM watches_catalog)::text AS total, (SELECT count(*) FROM watches_catalog WHERE brand_id IS DISTINCT FROM NULL)::text AS resolved_brand, (SELECT count(*) FROM watches_catalog WHERE family_id IS DISTINCT FROM NULL)::text AS resolved_family`
     const [pf] = await tx<{total: string; resolved_brand: string; resolved_family: string}[]>`
       SELECT
         (SELECT count(*) FROM watches_catalog)::text AS total,
         (SELECT count(*) FROM watches_catalog WHERE brand_id IS DISTINCT FROM NULL)::text AS resolved_brand,
         (SELECT count(*) FROM watches_catalog WHERE family_id IS DISTINCT FROM NULL)::text AS resolved_family
     `
     const total = Number(pf.total); const resolvedBrand = Number(pf.resolved_brand); const resolvedFamily = Number(pf.resolved_family)
     if (resolvedBrand !== total || resolvedFamily !== total) {
       throw new Error(`MIG-04 post-flight assertion FAILED: total=${total} resolved_brand=${resolvedBrand} resolved_family=${resolvedFamily}. Rolling back the entire transaction.`)
     }
     return { total, resolvedBrand, resolvedFamily, postFlightQuery }
     ```

  (e) Add NEW exported `function renderPostDeployMarkdown(args: PostDeployArgs): string` per RESEARCH L957-1088 (~130 LOC inline template):
     - Header: `# Phase 79 — ${isLocal ? 'LOCAL' : 'PROD'} Deployment Record`, Date (from args.today ?? new Date().toISOString().slice(0,10)), Operator placeholder, Status placeholder, Script invocation line.
     - Apply Summary GFM table (6 rows: brandsCreated, catalogRowsResolvedBrand, familiesCreated, aliasesAppended, catalogRowsResolvedFamily, userWatchesHydrated).
     - Post-Flight Assertion (MIG-04) section: SQL block (args.postFlightQuery) + result block (total / resolved_brand / resolved_family). Confirmation line `Both resolved counts equal total → zero unresolved rows`.
     - Operator Sign-Off Queries section: 6 SQL blocks per RESEARCH L1004-1052:
       1. zero NULL brand_id/family_id on catalog
       2. Hamilton merge collapsed correctly (brand_id = '20969364-f3b1-4b1d-ab2f-e5d22e9ffabc' verbatim — the canonical Hamilton uuid from the committed operator file)
       3. new brand row count matches summary (`created_at > now() - interval '1 hour'`)
       4. aliases appended via merge decisions (`cardinality(aliases) > 0`)
       5. hydration of a known user watch (Hamilton owners read 'Hamilton' canonical)
       6. natural-key UNIQUE constraint survived (per [[local-catalog-natural-key-drift]])
     - Sign-off checklist (3 items).
     - "What this push does NOT do (forward-armor against scope creep)" section listing Phase 80/81/82 deferred work (CANON-01/02, INGEST-01..04, RECO-01..04, DISP-01/02, UI-01..03, OPS-01/02).
     - Phase 79 Deliverables Summary GFM table (MIG-02/MIG-03/MIG-04/MIG-05/DISP-03 → ✅ with counts).
     - Next Phase pointer.

  (f) Add NEW non-exported `async function writePostDeployArtifact(args: PostDeployArgs): Promise<void>` per RESEARCH L959-967:
     ```
     const content = renderPostDeployMarkdown(args)
     await mkdir(path.dirname(POST_DEPLOY_PATH), { recursive: true })
     await writeFile(POST_DEPLOY_PATH, content, 'utf8')
     console.log(`[v8.4-brand-canon] wrote ${POST_DEPLOY_PATH}`)
     ```

In tests/unit/scripts/v8.4-post-deploy-template.test.ts:

  Replace all 8 it.todo callsites with it() assertions per Plan 01 spec:
  - D-79-10: section headings present (## Apply Summary, ## Post-Flight Assertion (MIG-04), ## Operator Sign-Off Queries, ## What this push does NOT do)
  - D-79-10: count substitution (assert `${args.counts.brandsCreated}` rendered)
  - D-79-10: post-flight SQL block present + result lines
  - D-79-10: Hamilton merge check #2 contains brand_id = '20969364-f3b1-4b1d-ab2f-e5d22e9ffabc' verbatim
  - D-79-10: forward-armor section lists Phase 80/81/82
  - MIG-04: source-grep — `grep -c 'IS DISTINCT FROM NULL' scripts/v8.4-brand-canonicalization.ts` ≥ 1 AND `grep -E 'IS NULL.*WHERE.*brand_id|brand_id.*IS NULL.*=.*0' scripts/v8.4-brand-canonicalization.ts | grep -v '^//\\|^\\s*//'` should NOT contain a post-flight-style assertion (verifies predicate divergence is structurally enforced)
  - D-79-08: source-grep — Plan 04's applyHydration UPDATE has no WHERE clause filtering on watches.brand or watches.model text (only `WHERE w.catalog_id = c.id`); use grep to confirm no `WHERE.*watches.*\\.\\(brand\\|model\\)` patterns in the apply path
  - D-79-09: source-grep — both `INSERT INTO brands` and `INSERT INTO watch_families` include `needs_review` set to `false` (regex matches `needs_review.*false` near both INSERT statements)

  Use `import { renderPostDeployMarkdown, type PostDeployArgs } from '../../../scripts/v8.4-brand-canonicalization'` and a fixture PostDeployArgs object for the template assertions. Source-grep tests use `readFileSync('scripts/v8.4-brand-canonicalization.ts', 'utf8')` + regex; `grep -c '^#' | grep -c needle` patterns explicitly filter out comment lines per `[[decision-coverage-gate-citations]]` per-comment hygiene.

Mitigates T-79-05 (post-flight predicate inheriting bug — predicate divergence enforced at source-grep level + at template level).
  </action>

  <verify>
    <automated>npx vitest run tests/unit/scripts/v8.4-post-deploy-template.test.ts && grep -c "IS DISTINCT FROM NULL" scripts/v8.4-brand-canonicalization.ts && grep -c "export function renderPostDeployMarkdown" scripts/v8.4-brand-canonicalization.ts</automated>
  </verify>

  <done>
    - applyHydration + postFlightAssertion + renderPostDeployMarkdown + writePostDeployArtifact defined.
    - tests/unit/scripts/v8.4-post-deploy-template.test.ts: all 8 it.todo + 1 sanity = 9 tests pass.
    - grep returns ≥1 for `IS DISTINCT FROM NULL` (positive predicate present).
    - grep returns 1 for `export function renderPostDeployMarkdown` (exported).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire main() --apply --mode=both branch with full atomic 6-step transaction + confirmIfProd + post-success POST-DEPLOY write</name>

  <files>scripts/v8.4-brand-canonicalization.ts</files>

  <read_first>
- scripts/v8.4-brand-canonicalization.ts (current state after Plans 02+03 + Task 1 — all helpers exist; main() dispatch only wires brand-only and family-dry-run)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L131-258  (system architecture diagram — Stage 1-5 ordering verbatim)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L678-826  (Code Examples — full applyPath() function combining all 6 steps; Plan 04 inlines this into main() with the helpers it has)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L628-674  (Pitfalls 1-8 — apply path forward armor)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L78-101  (sql.begin pattern + DO NOT REPLICATE anti-pattern)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L257-265  (Error handling pattern: NEVER process.exit inside sql.begin)
- .planning/phases/79-backfill-migration-display-hydration/79-CONTEXT.md L30-66  (D-79-01..10 verbatim apply-path requirements)
  </read_first>

  <action>
In scripts/v8.4-brand-canonicalization.ts, REPLACE the Plan 02 stub `--mode=families/both apply paths land in Plans 03 + 04` throw in the main() apply branch with the full implementation:

  ```
  if (args.apply) {
    if (args.mode !== 'both') {
      // Early validation already in main() head; redundant safety
      throw new Error('--apply requires --mode=both')
    }
    
    // STAGE 1: D-79-04 idempotent re-run gate (BEFORE strict gate)
    const gateStatus = await idempotentReRunGate(sql)
    if (gateStatus === 'already-applied') return  // sql.end fires in finally
    
    // STAGE 2: STRICTEST pre-flight gate (D-79-01) — brand + family
    if (!existsSync(OUTPUT_FILE)) throw new Error(`Missing ${OUTPUT_FILE}; run --mode=brands dry-run first.`)
    if (!existsSync(FAMILY_OUTPUT_FILE)) throw new Error(`Missing ${FAMILY_OUTPUT_FILE}; run --mode=families dry-run first.`)
    
    const brandContent = await readFile(OUTPUT_FILE, 'utf8')
    const familyContent = await readFile(FAMILY_OUTPUT_FILE, 'utf8')
    const brandRows = parseDecisionsTable(brandContent)
    const familyRows = parseFamilyDecisionsTable(familyContent)
    
    const existingBrandIdsFn = async (uuids: string[]) => {
      if (uuids.length === 0) return new Set<string>()
      const rows = await sql<{id: string}[]>`SELECT id FROM brands WHERE id IN ${sql(uuids)}`
      return new Set(rows.map(r => r.id))
    }
    const existingFamilyIdsFn = async (uuids: string[]) => {
      if (uuids.length === 0) return new Set<string>()
      const rows = await sql<{id: string}[]>`SELECT id FROM watch_families WHERE id IN ${sql(uuids)}`
      return new Set(rows.map(r => r.id))
    }
    await strictPreflightGate(sql, brandRows, familyRows, existingBrandIdsFn, existingFamilyIdsFn)
    
    // Build maps + summary
    const brandMap = buildBrandMap(brandRows)
    const familyMap = buildFamilyMap(familyRows, brandMap)
    
    // Compute summary for the confirm prompt (D-79-02): pre-counts of work to do
    const summary: ApplySummary = {
      brandsToCreate: [...brandMap.values()].filter(v => v.kind === 'new').length,
      catalogRowsToResolve: 0,  // post-transaction
      familiesToCreate: [...familyMap.values()].filter(v => v.kind === 'new').length,
      userWatchesToHydrate: 0,  // pre-compute via select count(*) from watches where catalog_id is not null
      aliasesToAppend: [...familyMap.values()].filter(v => v.kind === 'merge').length,
    }
    const [{count: hydrationCount}] = await sql<{count: string}[]>`SELECT count(*)::text AS count FROM watches WHERE catalog_id IS NOT NULL`
    summary.userWatchesToHydrate = Number(hydrationCount)
    const [{count: catalogCount}] = await sql<{count: string}[]>`SELECT count(*)::text AS count FROM watches_catalog`
    summary.catalogRowsToResolve = Number(catalogCount)
    
    // STAGE 3: D-79-02 confirm gate (silent local; interactive prod)
    await confirmIfProd(connStr, summary)
    
    // STAGE 4: ATOMIC TRANSACTION (D-79-03)
    let counts!: ApplyCounts
    let postFlight!: { total: number; resolvedBrand: number; resolvedFamily: number; postFlightQuery: string }
    
    await sql.begin(async (tx) => {
      // 4.1 + 4.2 (brand path)
      const brandRes = await applyBrandPath(tx, brandMap, { brandRows })
      // 4.3 + 4.4 + 4.5 (family path)
      const familyRes = await applyFamilyPath(tx, familyMap, { familyRows })
      // 4.6 (hydration — DISP-03)
      const hydRes = await applyHydration(tx)
      // 4.7 (post-flight — MIG-04). Throws inside callback → ROLLBACK.
      postFlight = await postFlightAssertion(tx)
      
      counts = {
        brandsCreated: brandRes.brandsCreated,
        catalogRowsResolvedBrand: brandRes.catalogRowsResolvedBrand,
        familiesCreated: familyRes.familiesCreated,
        aliasesAppended: familyRes.aliasesAppended,
        catalogRowsResolvedFamily: familyRes.catalogRowsResolvedFamily,
        userWatchesHydrated: hydRes.userWatchesHydrated,
      }
    })
    
    // STAGE 5: post-success artifact write (D-79-10)
    await writePostDeployArtifact({
      counts,
      postFlightQuery: postFlight.postFlightQuery,
      postFlightResult: { total: postFlight.total, resolvedBrand: postFlight.resolvedBrand, resolvedFamily: postFlight.resolvedFamily },
      isLocal: isLocalDatabaseUrl(connStr),
    })
    
    console.log(`[v8.4-brand-canon] APPLY COMPLETE.\n` +
      `  brandsCreated:               ${counts.brandsCreated}\n` +
      `  catalogRowsResolvedBrand:    ${counts.catalogRowsResolvedBrand}\n` +
      `  familiesCreated:             ${counts.familiesCreated}\n` +
      `  aliasesAppended:             ${counts.aliasesAppended}\n` +
      `  catalogRowsResolvedFamily:   ${counts.catalogRowsResolvedFamily}\n` +
      `  userWatchesHydrated:         ${counts.userWatchesHydrated}\n` +
      `  POST-DEPLOY artifact written: ${POST_DEPLOY_PATH}`)
    return
  }
  ```

DELETE the Plan 02 sole-brand-only standalone sql.begin block — the Plan 04 6-step transaction REPLACES it. Plan 02's applyBrandPath is now called from inside the unified sql.begin callback.

CRITICAL invariants per Pitfall 2:
- NEVER `process.exit` inside the `sql.begin(async tx => ...)` callback.
- ONLY `throw new Error(...)` for failures inside the callback.
- The outer `try { ... } finally { await sql.end() }` block handles process exit AFTER rollback (the outer .catch from L341-344 handles it).

Mitigates: T-79-01 (atomic transaction — partial state cannot persist; throw inside callback rolls back), T-79-05 (post-flight assertion uses positive predicate), T-79-04 (D-79-04 gate + per-step UPDATE predicates make re-run safe), T-79-02 (confirmIfProd fires before any write), T-79-03 (strict gate refuses on drift before transaction opens).
  </action>

  <verify>
    <automated>npx vitest run tests/unit/scripts/ && npm run build && grep -c "sql.begin" scripts/v8.4-brand-canonicalization.ts && test "$(grep -c 'process.exit' scripts/v8.4-brand-canonicalization.ts)" -le 6 && (test "$(grep -c '= ANY(' scripts/v8.4-brand-canonicalization.ts)" = "0" || (echo "FAIL: = ANY(arr) pattern present"; exit 1))</automated>
  </verify>

  <done>
    - main() --apply --mode=both branch fully implemented with 5 stages + single sql.begin containing 4.1-4.7.
    - All Plan 02 + 03 + Task 1 unit tests still green.
    - npm run build exits 0.
    - sql.begin count = 1 (single atomic transaction, NOT one per step).
    - process.exit count ≤ 6 (top-level main catch + confirmIfProd decline + parseArgs validation + DATABASE_URL missing + various error paths; NONE inside sql.begin callback).
    - Zero `= ANY(` patterns.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Green integration stubs (apply-atomic + apply-idempotent) against local DATABASE_URL</name>

  <files>tests/integration/scripts/v8.4-apply-atomic.test.ts, tests/integration/scripts/v8.4-apply-idempotent.test.ts</files>

  <read_first>
- tests/integration/scripts/v8.4-apply-atomic.test.ts (Plan 01 RED stub — all it.todo callsites)
- tests/integration/scripts/v8.4-apply-idempotent.test.ts (Plan 01 RED stub)
- tests/integration/scripts/v8.4-readonly.test.ts (Phase 78 snapshot pattern — beforeAll backup, afterAll restore + DB-reset)
- .planning/phases/79-backfill-migration-display-hydration/79-PATTERNS.md L416-507  (Phase 79 integration test deltas — snapshot capturing brand+family resolved counts; Hamilton hydration assertion)
- .planning/phases/79-backfill-migration-display-hydration/79-RESEARCH.md L1196-1208  (Phase Requirements → Test Map per assertion)
- .planning/v8.4-brand-merge-decisions.md  (committed operator fixture used by integration tests)
- supabase/seed.sql  (local seed has Hamilton + Hamilton Watch; confirm via grep before relying on assertions)
- CLAUDE.md § Local-First Development (the gate)
- Memory: [[local-db-reset]] for the multi-step recipe — beforeAll may need it for clean state between integration runs
  </read_first>

  <action>
In tests/integration/scripts/v8.4-apply-atomic.test.ts:

Replace all it.todo callsites with it() assertions per Plan 01 spec. Test setup:

  beforeAll (60s timeout):
  - connStr = process.env.DATABASE_URL!; sql = postgres(connStr, {max:1, prepare:false})
  - back up brand-merge-decisions.md + family-merge-decisions.md (if family file exists)
  - reset local DB to a known starting point: the test ASSUMES the committed v8.4-brand-merge-decisions.md is operator-edited (it is — 19 auto-resolved + 1 merge + 33 new). For family decisions, the test GENERATES a synthetic family file by spawning `tsx scripts/v8.4-brand-canonicalization.ts --mode=families --force` THEN edits all needs-review rows to `new` programmatically (the family file's purpose for this test is "all-new defaults" — no merge cases needed for the canonical MIG-02/MIG-03 happy path; Brut Date alias verification is unit-test-fixture covered per Pitfall 8).
  - Run the apply: `spawnSync('npx', ['tsx', 'scripts/v8.4-brand-canonicalization.ts', '--apply', '--mode=both'], {env: {...process.env}, encoding: 'utf8'})`
  - postSnapshot = await snapshot()

  Test assertions (per Plan 01 spec, 11 callsites):
  - MIG-02: post-snapshot resolved_brand_count === total_catalog_count → expect(postSnapshot.resolvedBrandCount).toBe(postSnapshot.totalCatalog)
  - MIG-02 + Hamilton: query `SELECT count(*) FROM watches_catalog WHERE brand_id = '20969364-f3b1-4b1d-ab2f-e5d22e9ffabc'`; expect >= preCount of `lower(trim(brand)) IN ('hamilton', 'hamilton watch')` rows
  - MIG-02 + D-79-09: `SELECT count(*) FROM brands WHERE needs_review = true AND created_at > now() - interval '1 hour'`; expect 0
  - MIG-03: resolved_family_count === total_catalog_count
  - MIG-03 + D-79-06 idempotent: run --apply a second time (or programmatically via Plan 02's invocation); for any family with merged-source aliases, assert `cardinality(aliases)` == pre-second-run value
  - MIG-04: stdout contains `IS DISTINCT FROM NULL` substring OR the source-grep covers this (acceptable to defer the runtime SQL check to the unit-grep done in Task 1; primary integration coverage is the rollback case below)
  - MIG-04 forced-fail rollback: this test inserts a deliberate post-Stage-4.6 mismatch by temporarily setting a watches_catalog row's brand_id to NULL via test setup (BEFORE spawn), then asserts the apply exit code is non-zero AND the snapshot's brand_id is still NULL (rollback preserved pre-state). Use a fresh sql client to verify post-spawn DB state.
  - DISP-03: `SELECT brand FROM watches WHERE lower(brand) LIKE 'hamilton%' LIMIT 5`; expect every row's brand reads 'Hamilton' (canonical) not 'Hamilton Watch'
  - DISP-03 preserves other columns: snapshot watches.notes / serial / reference / price_paid for a known user row BEFORE the apply; assert byte-identical AFTER
  - D-79-10: assert `existsSync(.planning/phases/79-backfill-migration-display-hydration/79-POST-DEPLOY.md)` AND contains '## Apply Summary' AND '## Post-Flight Assertion (MIG-04)' AND '## Operator Sign-Off Queries' AND '## What this push does NOT do' headings

  afterAll: restore decisions file backups; reset DB or note for operator follow-up that local DB has Phase 79 applied; sql.end.

In tests/integration/scripts/v8.4-apply-idempotent.test.ts:

Replace it.todo with it() per Plan 01 spec:
- D-79-04 first --apply: spawn; assert exit 0 (assumes test isn't reusing post-state from apply-atomic — beforeAll resets DB to pre-Plan-79 state)
- D-79-04 second --apply: spawn again; assert exit 0 AND stdout matches /Already applied — nothing to do\./
- D-79-06 alias cardinality: snapshot `SELECT name, cardinality(aliases) FROM watch_families WHERE cardinality(aliases) > 0`; spawn second --apply; re-snapshot; assert all cardinalities byte-identical

NOTE: integration tests are DATABASE_URL-gated; if DATABASE_URL unset, the maybe-describe.skip pattern from Phase 78 routes around them. Tests are skipped in CI without DB.
  </action>

  <verify>
    <automated>npx vitest run tests/integration/scripts/v8.4-apply-atomic.test.ts tests/integration/scripts/v8.4-apply-idempotent.test.ts</automated>
  </verify>

  <done>
    - tests/integration/scripts/v8.4-apply-atomic.test.ts: all 11 it.todo callsites converted to it() and (when DATABASE_URL set) pass.
    - tests/integration/scripts/v8.4-apply-idempotent.test.ts: all 3 it.todo callsites converted to it() and pass.
    - When DATABASE_URL UNSET, both suites are reported `↓ skipped` (sanity it remains passing).
    - When DATABASE_URL SET, the full apply runs end-to-end against local Supabase, all assertions green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-edited brand+family decisions → sql.begin transaction | tampering surface; mitigated by strict gate before transaction opens |
| script → Postgres (writes to brands/watch_families/watches_catalog/watches) | high-blast-radius privileged write; atomic transaction is the rollback safety net |
| post-success FS write → .planning/phases/79-.../79-POST-DEPLOY.md | low-risk artifact emission; mitigated by writing AFTER commit only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Integrity | main() --apply --mode=both branch (Task 2) | mitigate | Single sql.begin callback wraps Steps 4.1-4.7; any throw inside (network blip, invariant fail, FK violation, post-flight mismatch) triggers automatic ROLLBACK; pre-state preserved |
| T-79-02 | Tampering / Confused Deputy | confirmIfProd called before sql.begin (Task 2) | mitigate | Host-based detection + interactive `yes` prompt fires before the transaction opens; non-local URL requires explicit operator confirmation |
| T-79-03 | Tampering | strictPreflightGate called before sql.begin (Task 2) | mitigate | Gate refuses on any drift (needs-review, unknown token, merge:<uuid> missing, catalog row absent from decisions file) before any SQL write |
| T-79-04 | Integrity | applyBrandPath + applyFamilyPath UPDATEs (inherited from Plans 02+03) | mitigate | Per-step UPDATE predicates (`AND brand_id IS NULL OR <> resolved`; `AND NOT (aliases @> ARRAY[$src])`) make re-runs safe even if D-79-04 idempotent gate misfires |
| T-79-05 | Integrity | postFlightAssertion (Task 1) | mitigate | Positive predicate `IS DISTINCT FROM NULL` differs structurally from any UPDATE WHERE in Steps 4.1-4.6; verified by source-grep in unit test (Task 1) |
| T-79-06 | Information Disclosure | renderPostDeployMarkdown (Task 1) — UUIDs printed in operator sign-off queries | accept | Brand UUIDs are not sensitive in this project; required for operator audit trail; ASVS L1 acceptable risk per security_domain section |
| T-INJ-01 | Tampering (SQL injection) | applyHydration + postFlightAssertion (Task 1) | mitigate | Hydration UPDATE has zero operator-controlled inputs (it's a JOIN over schema-fixed columns); post-flight assertion has zero inputs |
| T-COMMIT-01 | Integrity | writePostDeployArtifact (Task 1) | mitigate | Called AFTER sql.begin resolves (commit complete); never invoked on rollback path because the throw exits before this line |
</threat_model>

<verification>
- All Plan 01-03 unit tests still green; all Plan 01 unit + integration stubs now green (DATABASE_URL-gated integration suites pass when DATABASE_URL set; skipped otherwise per maybe-describe convention).
- scripts/v8.4-brand-canonicalization.ts builds clean: `npm run build` exits 0.
- ZERO `= ANY(` patterns in the script per `[[drizzle-sql-any-array-pitfall]]` forward armor.
- ZERO `process.exit` calls INSIDE the sql.begin callback per Pitfall 2.
- Single `sql.begin` invocation in the apply path (NOT one per step).
- `IS DISTINCT FROM NULL` predicate present in post-flight assertion; NO `WHERE brand_id IS NULL = 0` style assertion (would inherit UPDATE WHERE bug per `[[post-flight-assertion-predicate-divergence]]`).
- 79-POST-DEPLOY.md auto-generated when apply succeeds — file path matches POST_DEPLOY_PATH constant.
- ROADMAP MIG-04 success criterion #3 verified at runtime: assertion uses positive predicate; assertion failure rolls back via the integration test forced-fail case.
- ROADMAP DISP-03 success criterion #4 verified: Hamilton-owners' watches.brand reads 'Hamilton' after apply (the canonical end-to-end test from CONTEXT specifics).
</verification>

<success_criteria>
- scripts/v8.4-brand-canonicalization.ts ~880 LOC (from Plan 03's ~700); main() apply branch wires 5 stages + atomic 6-step transaction.
- `npm run db:v8.4-brand-canon -- --apply --mode=both` against local DATABASE_URL executes successfully against the committed brand-merge-decisions.md (with a Plan 03-generated family-merge-decisions.md operator-edited to all-new).
- 79-POST-DEPLOY.md auto-generated with target=LOCAL, all 6 sign-off SQL queries present, Hamilton merge check verbatim.
- All Plan 01 stubs (4 unit + 2 integration) green; integration suites skip cleanly when DATABASE_URL unset.
- Plan 05 (local-first verification + prod push) inherits a fully-functional script; Plan 05 is gate execution only, not implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/79-backfill-migration-display-hydration/79-04-SUMMARY.md` recording:
- New exports added in Task 1 (function signatures verbatim)
- Atomic-transaction structure (5 stages + 7 steps inside sql.begin)
- Plan 01 stub green counts (all 6 stub files)
- Local apply smoke-test result if executed manually (counts; post-flight result)
- Confirmation: zero `= ANY(` patterns; zero `process.exit` inside sql.begin; single sql.begin in apply path
- Any deviations from spec per Rule 1 auto-fix policy
</output>
</content>
</invoke>