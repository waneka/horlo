---
phase: 77
plan: 08
type: execute
wave: 3
depends_on:
  - 77-01
  - 77-02
  - 77-03
  - 77-07
files_modified:
  - src/components/home/WywtTile.tsx
  - src/components/wears/WearsLane.tsx
  - src/app/wear/[wearEventId]/page.tsx
  - src/app/page.tsx
  - src/app/wears/[username]/page.tsx
  - tests/components/home/WywtTile.video.test.tsx
autonomous: true
requirements:
  - VID-13
  - VID-14
tags: [display, signed-urls, tile-overlay, ssr, wave-3-final]
must_haves:
  truths:
    - "Per VID-13 + D-13/D-14: WywtTile gains optional props mediaType + signedPosterUrl. When mediaType==='video' AND signedPosterUrl is non-null, the tile renders the poster as its image and overlays a VideoPlayBadge. When mediaType is absent or 'photo', the tile renders byte-identically to today (VID-15)."
    - "Per VID-14 + RESEARCH §Signed URL Minting: /wear/[wearEventId]/page.tsx WearPhotoStreamed mints BOTH signedVideoUrl AND signedPosterUrl via createSupabaseServerClient (admin client per T-77-03 + Phase 61 lesson) when wear.mediaType==='video'. The Promise.all minimizes round-trip latency. The existing photo signedUrl path is preserved for mediaType !== 'video' (VID-15)."
    - "Per VID-13 + RESEARCH §Same Pattern for WearsLane and WywtRail: src/app/page.tsx extends its existing tile signing loop to ALSO mint poster signed URLs for tiles where mediaType==='video' && posterPath !== null. The signed poster URL is threaded onto each video tile as signedPosterUrl before being passed to WywtRail and on to WywtTile. The existing photoUrl-signing loop is preserved verbatim (VID-15 invariant)."
    - "Per VID-14 + UI-SPEC §Surface 6: src/app/wears/[username]/page.tsx mints BOTH signedVideoUrl and signedPosterUrl for each slide whose underlying wear has mediaType==='video'. WearSlide's three new optional fields (mediaType, signedVideoUrl, signedPosterUrl) carry through to WearCard (Plan 07's discriminator branch then renders WearVideoClient)."
    - "Per VID-14 + D-07 + RESEARCH §Risk 8: NO video.onEnded listener is added anywhere in src/components/wears/WearsLane.tsx. The segmented progress indicator continues to advance only on embla 'select' (user swipe). Coupling lane advance to video loop completion is explicitly forbidden by D-07."
    - "Per RESEARCH §Risk 3 + durable memory project_ppr_dynamic_before_use_cache + feedback_ppr_cache_fill_no_longer_call_out: /wear/[wearEventId]/page.tsx does NOT add a new await connection() call. The page already opts out of the static shell via getCurrentUser() (request-time API). No new Suspense boundaries are introduced — we EXTEND WearPhotoStreamed in place."
    - "Per Plan 01 stub: tests/components/home/WywtTile.video.test.tsx (3 it.todo) is upgraded to passing assertions covering VideoPlayBadge presence/absence (VID-13) and signedPosterUrl image source for video tiles."
    - "Per durable memory project_baseline_not_green_build_is_gate: npm run build exits 0. Existing tests/components/wears/ and tests/components/home/ suites do not regress."
  artifacts:
    - path: src/components/home/WywtTile.tsx
      provides: "Video tile branch — poster signed URL as Image; VideoPlayBadge overlay"
      contains: "VideoPlayBadge"
    - path: src/components/wears/WearsLane.tsx
      provides: "WearSlide type extended with 3 video fields; flows through to WearCard via existing spread"
      contains: "signedVideoUrl"
    - path: src/app/wear/[wearEventId]/page.tsx
      provides: "WearPhotoStreamed mints both video + poster signed URLs for video wears; threads to WearCard"
      contains: "signedVideoUrl"
    - path: src/app/page.tsx
      provides: "Home rail Server Component mints poster signed URLs for each video tile"
      contains: "signedPosterUrl"
    - path: src/app/wears/[username]/page.tsx
      provides: "Stories lane Server Component mints both URLs per video slide"
      contains: "signedVideoUrl"
    - path: tests/components/home/WywtTile.video.test.tsx
      provides: "3 cases — VideoPlayBadge presence/absence + signedPosterUrl image source"
      contains: "VideoPlayBadge"
  key_links:
    - from: "src/app/wear/[wearEventId]/page.tsx (this plan)"
      to: "WearCard.mediaType + signedVideoUrl + signedPosterUrl (Plan 07)"
      via: "render-time prop pass"
      pattern: "signedVideoUrl"
    - from: "src/app/page.tsx (this plan)"
      to: "WywtTile.signedPosterUrl (this plan)"
      via: "tile data enrichment loop"
      pattern: "signedPosterUrl"
    - from: "src/app/wears/[username]/page.tsx (this plan)"
      to: "WearsLane slides → WearCard mediaType/signedVideoUrl/signedPosterUrl"
      via: "slide data + WearCard spread (Plan 07)"
      pattern: "signedVideoUrl"
    - from: "src/components/home/WywtTile.tsx (this plan)"
      to: "VideoPlayBadge (Plan 07)"
      via: "conditional render when mediaType === 'video'"
      pattern: "VideoPlayBadge"
---

<objective>
Wire video display end-to-end into the remaining 5 files: (1) WywtTile gains a video render branch with poster + VideoPlayBadge; (2) WearsLane WearSlide type is widened (additive, no render change, NO onEnded per D-07); (3) /wear/[wearEventId]/page.tsx WearPhotoStreamed mints both video + poster signed URLs; (4) /page.tsx home rail signs poster paths for video tiles; (5) /wears/[username]/page.tsx lane signs both URLs per video slide. Upgrade the final Wave 0 test stub.

Purpose: this is the final integration plan. After it ships, end-to-end video display works across home rail tile, detail page, and stories lane. Phase 77 is code-complete.

Output:
- src/components/home/WywtTile.tsx — video render branch
- src/components/wears/WearsLane.tsx — WearSlide widening
- src/app/wear/[wearEventId]/page.tsx — WearPhotoStreamed mints both URLs
- src/app/page.tsx — home rail signs poster paths
- src/app/wears/[username]/page.tsx — lane signs both URLs per video slide
- tests/components/home/WywtTile.video.test.tsx — upgraded
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-video-capture-display-ui/77-CONTEXT.md
@.planning/phases/77-video-capture-display-ui/77-RESEARCH.md
@.planning/phases/77-video-capture-display-ui/77-PATTERNS.md
@.planning/phases/77-video-capture-display-ui/77-UI-SPEC.md
@.planning/phases/77-video-capture-display-ui/77-VALIDATION.md

<interfaces>
<!-- Canonical contracts. Executor reads each before writing. -->

WywtTile additive changes (from 77-PATTERNS.md lines 970-1010):

```typescript
// New optional props (added to existing Props interface):
mediaType?: 'photo' | 'video'
signedPosterUrl?: string | null
```

```tsx
// Image render branch — extended (3-way ternary):
{mediaType === 'video' && signedPosterUrl ? (
  <Image src={signedPosterUrl} alt="" fill className="object-cover" unoptimized />
) : (tile.photoUrl ?? tile.imageUrl) ? (
  <Image src={tile.photoUrl ?? tile.imageUrl ?? ''} alt="" fill className="object-cover" unoptimized />
) : (
  <div className="absolute inset-0 bg-muted flex items-center justify-center">
    <WatchIcon className="text-muted-foreground" aria-hidden />
  </div>
)}
{/* Overlay AFTER image, BEFORE bottom gradient — video tiles only */}
{mediaType === 'video' && <VideoPlayBadge />}
```

Import: `import { VideoPlayBadge } from '@/components/wear/VideoPlayBadge'` (Plan 07 deliverable).

WearsLane WearSlide additive (from 77-PATTERNS.md lines 144-160):

```typescript
// Phase 77: video slide support (VID-14)
mediaType?: 'photo' | 'video'
signedVideoUrl?: string | null
signedPosterUrl?: string | null
```

NO render change in WearsLane; existing WearCard spread carries the new fields. NO `onEnded` listener added (D-07 guardrail per RESEARCH §Risk 8).

/wear/[wearEventId]/page.tsx extension (from 77-PATTERNS.md lines 1018-1056). Existing photo mint:

```typescript
let signedUrl: string | null = null
if (photoUrl) {
  const supabase = await createSupabaseServerClient()
  const { data } = await supabase.storage.from('wear-photos').createSignedUrl(photoUrl, 60 * 60)
  signedUrl = data?.signedUrl ?? null
}
```

Extended:

```typescript
let signedUrl: string | null = null
let signedVideoUrl: string | null = null
let signedPosterUrl: string | null = null
if (wear.mediaType === 'video') {
  const supabase = await createSupabaseServerClient()
  const [videoResult, posterResult] = await Promise.all([
    wear.mediaPath
      ? supabase.storage.from('wear-photos').createSignedUrl(wear.mediaPath, 60 * 60)
      : Promise.resolve({ data: null }),
    wear.posterPath
      ? supabase.storage.from('wear-photos').createSignedUrl(wear.posterPath, 60 * 60)
      : Promise.resolve({ data: null }),
  ])
  signedVideoUrl = videoResult.data?.signedUrl ?? null
  signedPosterUrl = posterResult.data?.signedUrl ?? null
} else if (photoUrl) {
  // Existing photo branch — unchanged
  const supabase = await createSupabaseServerClient()
  const { data } = await supabase.storage.from('wear-photos').createSignedUrl(photoUrl, 60 * 60)
  signedUrl = data?.signedUrl ?? null
}
```

WearCard invocation gains: `mediaType={wear.mediaType ?? undefined}`, `signedVideoUrl={signedVideoUrl}`, `signedPosterUrl={signedPosterUrl}`.

src/app/page.tsx (home rail) — existing signing loop (paraphrased from earlier grep at lines 33, 49, 56, 64):
- After Plan 03's WR-02 widening, `getWearRailForViewer` returns tiles with `mediaType` (NOT NULL default 'photo') and `posterPath` (string | null)
- Existing logic: filter tiles with `photoUrl !== null`, mint signed URLs via admin client, build `byId` Map, replace `t.photoUrl` with the signed URL
- Extension: in parallel, filter tiles with `mediaType === 'video' && posterPath !== null`, mint poster signed URLs, build `posterById` Map; pass `signedPosterUrl` onto the final WywtTile invocation alongside `mediaType`

src/app/wears/[username]/page.tsx — same pattern: for each slide whose wear has `mediaType === 'video'`, mint both URLs via admin client + Promise.all; pass `signedVideoUrl` + `signedPosterUrl` + `mediaType` onto the slide before the lane renders.

PPR / Cache-Components guidance per durable memory `project_ppr_dynamic_before_use_cache` + `feedback_ppr_cache_fill_no_longer_call_out`: the PPR / #419 family is RESOLVED infrastructure. Do NOT add new `await connection()` calls or new Suspense boundaries. /wear/[wearEventId]/page.tsx already opts out of static shell via `getCurrentUser()` (a request-time API). The Suspense child `WearPhotoStreamed` is the only mint site — extend it; don't add siblings.

Wave 0 stub (Plan 01 created):
- tests/components/home/WywtTile.video.test.tsx — 3 `it.todo` entries; preserves first-line marker
</interfaces>

@src/components/home/WywtTile.tsx
@src/components/home/WywtRail.tsx
@src/components/wears/WearsLane.tsx
@src/app/wear/[wearEventId]/page.tsx
@src/app/page.tsx
@src/app/wears/[username]/page.tsx
@src/components/wear/VideoPlayBadge.tsx
@src/components/wear/WearCard.tsx
@src/lib/wywtTypes.ts
@tests/components/home/WywtTile.video.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend WywtTile component + WearsLane WearSlide type + upgrade WywtTile.video test stub</name>
  <files>src/components/home/WywtTile.tsx, src/components/wears/WearsLane.tsx, tests/components/home/WywtTile.video.test.tsx</files>
  <read_first>
    - src/components/home/WywtTile.tsx (FULL file — locate Props interface, the (tile.photoUrl ?? tile.imageUrl) image ternary, the outer relative-overflow-hidden button wrapper, the bottom gradient overlay)
    - src/components/home/WywtRail.tsx (CONFIRM only — verify Props spread cleanly threads new fields from page.tsx through to WywtTile)
    - src/components/wear/VideoPlayBadge.tsx (Plan 07 deliverable — confirm zero-prop export)
    - src/components/wears/WearsLane.tsx (FULL file — locate WearSlide interface declaration; locate the WearCard invocation around line 398; confirm NO existing onEnded listener)
    - tests/components/home/WywtTile.video.test.tsx (Wave 0 stub — 3 it.todo from Plan 01)
    - 77-PATTERNS.md §WywtTile MODIFIED (lines 970-1010 — exact additive changes)
    - 77-PATTERNS.md §WearsLane.tsx (lines 139-167 — additive WearSlide fields + NO onEnded guardrail)
    - 77-UI-SPEC.md §"Surface 4: WywtTile (home rail tile) — video branch" (lines 457-468 — exact poster + badge positioning)
  </read_first>
  <behavior>
    - WywtTile Props interface gains two new OPTIONAL fields: `mediaType?: 'photo' | 'video'`, `signedPosterUrl?: string | null`. Comment: `// Phase 77: video tile support (VID-13)`
    - Import added: `import { VideoPlayBadge } from '@/components/wear/VideoPlayBadge'`
    - Image render branch becomes a 3-way ternary: `mediaType === 'video' && signedPosterUrl` → poster Image; else `(tile.photoUrl ?? tile.imageUrl)` → existing Image; else WatchIcon fallback
    - VideoPlayBadge overlay rendered when `mediaType === 'video'` — placed AFTER image, BEFORE bottom gradient (DOM order; absolute positioning lets it sit between)
    - Outer `relative overflow-hidden` button wrapper UNCHANGED (badge clips correctly via existing overflow-hidden)
    - WearsLane WearSlide interface gains 3 additive optional fields: `mediaType?: 'photo' | 'video'`, `signedVideoUrl?: string | null`, `signedPosterUrl?: string | null`. Comment: `// Phase 77: video slide support (VID-14)`
    - NO new imports in WearsLane.tsx; NO render-logic changes; NO onEnded listener (D-07 guardrail — grep gate)
    - Existing `<WearCard>` invocation in WearsLane already spreads slide fields (or uses explicit pass-throughs — the executor matches the existing convention); the 3 new fields flow through to WearCard's discriminator branch (Plan 07)
    - tests/components/home/WywtTile.video.test.tsx: 3 real `it(...)` cases — VideoPlayBadge present when mediaType='video', absent when mediaType='photo', absent when mediaType absent (VID-15 regression)
  </behavior>
  <action>
    Edit `src/components/home/WywtTile.tsx`:
    1. Add `import { VideoPlayBadge } from '@/components/wear/VideoPlayBadge'` after existing imports
    2. Locate Props interface (current lines 28-41 per PATTERNS). After the last field add comment `// Phase 77: video tile support (VID-13)` then the two new optional fields
    3. Add both new props to the destructured-props at the function signature with safe defaults (`signedPosterUrl = null`)
    4. Locate image-render branch (current lines 97-109 per PATTERNS). Replace the `(tile.photoUrl ?? tile.imageUrl)` ternary with the 3-way version from the interfaces excerpt: video poster branch first, photo branch unchanged, WatchIcon fallback unchanged
    5. After the image ternary closes (and BEFORE the existing bottom gradient overlay), add `{mediaType === 'video' && <VideoPlayBadge />}`. Per UI-SPEC §Surface 4 line 465: badge sits between image and bottom gradient text

    Edit `src/components/wears/WearsLane.tsx`:
    1. Locate WearSlide interface declaration (current lines 25-50 per PATTERNS)
    2. After last field add comment `// Phase 77: video slide support (VID-14)` then the 3 additive fields
    3. Locate the `<WearCard>` invocation (around line 398). If existing invocation uses `{...slide}` spread, NO additional change needed (fields flow through). If explicit prop pass-throughs, add `mediaType={slide.mediaType} signedVideoUrl={slide.signedVideoUrl} signedPosterUrl={slide.signedPosterUrl}` matching the existing convention
    4. Verify NO `onEnded` listener exists (per D-07 guardrail). Per durable memory `feedback_ppr_cache_fill_no_longer_call_out`: PPR concerns resolved; do NOT add `await connection()` or new Suspense boundaries

    Edit `tests/components/home/WywtTile.video.test.tsx`:
    1. Preserve `// Wave 0 RED stub` first-line marker; add `// Plan 08: upgraded from todo to assertions (VID-13)` below
    2. Imports: vitest + testing-library + WywtTile
    3. Define a minimal fixture helper `mkTile(overrides = {})` that returns required WywtTile props (the executor reads the post-edit WywtTile signature to extract exact shape)
    4. 3 real `it(...)` cases:
       - Case 1: `it('VID-13: VideoPlayBadge rendered when mediaType="video"', ...)` — render with mediaType='video' + signedPosterUrl set; assert a DOM node with `bg-black/50` class is present (VideoPlayBadge signature)
       - Case 2: `it('VID-13: VideoPlayBadge NOT rendered when mediaType="photo"', ...)` — render with mediaType='photo'; assert `.rounded-full.bg-black/50` is NOT in the DOM
       - Case 3: `it('VID-15: VideoPlayBadge NOT rendered when mediaType absent (regression)', ...)` — render without mediaType; assert NOT present; this is the VID-15 regression guard

    Per durable memory `feedback_test_assert_disappearance_too`: cases 2 + 3 assert ABSENCE — pair appearance and disappearance assertions.

    After all edits run `npm run build` (exit 0 gate) AND `npx vitest run tests/components/home/WywtTile.video.test.tsx` (3 passed). Per durable memory `project_baseline_not_green_build_is_gate`: build is the authoritative gate.
  </action>
  <verify>
    <automated>grep -c VideoPlayBadge /Users/tylerwaneka/Documents/horlo/src/components/home/WywtTile.tsx | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "signedPosterUrl" /Users/tylerwaneka/Documents/horlo/src/components/home/WywtTile.tsx | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "mediaType === 'video'" /Users/tylerwaneka/Documents/horlo/src/components/home/WywtTile.tsx | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "signedVideoUrl" /Users/tylerwaneka/Documents/horlo/src/components/wears/WearsLane.tsx | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "signedPosterUrl" /Users/tylerwaneka/Documents/horlo/src/components/wears/WearsLane.tsx | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c onEnded /Users/tylerwaneka/Documents/horlo/src/components/wears/WearsLane.tsx | grep -q "^0$" && grep -c "it(" /Users/tylerwaneka/Documents/horlo/tests/components/home/WywtTile.video.test.tsx | grep -q "^3$" && grep -c "it.todo" /Users/tylerwaneka/Documents/horlo/tests/components/home/WywtTile.video.test.tsx | grep -q "^0$" && grep -c "Wave 0 RED stub" /Users/tylerwaneka/Documents/horlo/tests/components/home/WywtTile.video.test.tsx | grep -q "^1$" && grep -c "Plan 08" /Users/tylerwaneka/Documents/horlo/tests/components/home/WywtTile.video.test.tsx | grep -q "^1$" && npx vitest run tests/components/home/WywtTile.video.test.tsx 2>&1 | tail -20 | grep -qE "3 passed|Tests\s+3 passed" && npm run build 2>&1 | grep -qE "Compiled successfully|✓ Compiled"</automated>
  </verify>
  <acceptance_criteria>
    - WywtTile.tsx: VideoPlayBadge referenced (>=2 — import + JSX); signedPosterUrl referenced (>=2 — prop + render); mediaType==='video' literal (>=2 — image branch + badge guard)
    - WearsLane.tsx: signedVideoUrl + signedPosterUrl present in WearSlide interface; ZERO onEnded references (D-07 guardrail)
    - WywtTile.video.test.tsx: 3 it(), 0 it.todo, both markers present
    - `npx vitest run tests/components/home/WywtTile.video.test.tsx` exits 0 with 3 passed
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    - WywtTile video branch wired with poster + badge
    - WearsLane WearSlide additively widened; no onEnded leaked
    - VID-13 covered by automated tests with VID-15 regression sentinel
    - Build green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend signed-URL minting in /wear/[wearEventId]/page.tsx, /page.tsx, and /wears/[username]/page.tsx</name>
  <files>src/app/wear/[wearEventId]/page.tsx, src/app/page.tsx, src/app/wears/[username]/page.tsx</files>
  <read_first>
    - src/app/wear/[wearEventId]/page.tsx (FULL file — locate WearPhotoStreamed; locate the existing photoUrl signed-URL mint block; locate the WearCard invocation; identify the Suspense structure to leave untouched per RESEARCH §Risk 3)
    - src/app/page.tsx (FULL file — locate getWearRailForViewer call; locate the tilesWithPhotos filter; locate the existing signing loop; locate the WywtRail invocation)
    - src/app/wears/[username]/page.tsx (FULL file — locate the wears query + slide mapping; identify per-slide signed-URL mint shape)
    - src/components/home/WywtTile.tsx (post-Task-1 form — confirm `mediaType` + `signedPosterUrl` props)
    - src/components/wear/WearCard.tsx (Plan 07 form — confirm new props mediaType/signedVideoUrl/signedPosterUrl)
    - 77-PATTERNS.md §`src/app/wear/[wearEventId]/page.tsx` MODIFIED (lines 1018-1071 — exact additive extension)
    - 77-RESEARCH.md §Signed URL Minting Pattern + §"Same Pattern for WearsLane and WywtRail" (lines 495-535 — admin client + Promise.all + 60-min TTL)
    - 77-RESEARCH.md §Risk 3 (the PPR / connection() note — do NOT add)
  </read_first>
  <behavior>
    - /wear/[wearEventId]/page.tsx:
      - In WearPhotoStreamed (the streamed Suspense child), add `let signedVideoUrl: string | null = null` and `let signedPosterUrl: string | null = null` alongside the existing `let signedUrl: string | null = null` declaration
      - Replace the existing `if (photoUrl) { ... }` mint block with the if/else version: `if (wear.mediaType === 'video') { ...mint both via Promise.all... } else if (photoUrl) { ...existing photo mint VERBATIM... }`
      - Both branches use `await createSupabaseServerClient()` (admin client per T-77-03)
      - WearCard invocation gains three new props: `mediaType={wear.mediaType ?? undefined}`, `signedVideoUrl={signedVideoUrl}`, `signedPosterUrl={signedPosterUrl}`
      - NO new Suspense boundaries; NO `await connection()` added (per RESEARCH §Risk 3 + durable PPR memory)
    - /page.tsx (home rail):
      - After `getWearRailForViewer(user.id)` returns tiles, identify video tiles via `t.mediaType === 'video' && t.posterPath !== null`; mint their poster signed URLs in parallel with the existing photo mint (use `Promise.all` to fan out both signing operations)
      - Build a `posterById: Map<string, string>` keyed by wearEventId; thread `signedPosterUrl: posterById.get(t.wearEventId) ?? null` onto each video tile alongside `mediaType: t.mediaType`
      - The existing photo signing loop is PRESERVED VERBATIM — same admin client invocation, same `byId` map shape, same `photoUrl` replacement
      - WywtRail invocation passes the new fields through (WywtRail itself just spreads tiles to WywtTile; the new prop fields flow without explicit edits if the existing code uses `<WywtTile tile={t} />` spread — confirm by reading WywtRail's prop shape)
    - /wears/[username]/page.tsx:
      - For each slide where the wear's `mediaType === 'video'`, mint both `signedVideoUrl` (from `mediaPath`) and `signedPosterUrl` (from `posterPath`) via `createSupabaseServerClient()` admin client + `Promise.all` — same pattern as /wear/[id]
      - For each slide where `mediaType !== 'video'`, preserve the existing per-slide signed-URL mint VERBATIM (VID-15 invariant — photo slides unchanged)
      - Pass `mediaType`, `signedVideoUrl`, `signedPosterUrl` onto the slide object before it's handed to `<WearsLane>`
    - All three files: use `createSupabaseServerClient()` (NOT the cookie/browser client) per T-77-03 + Phase 61 lesson
    - VID-15 invariant in all three files: photo paths preserved byte-for-byte; existing tests still pass
  </behavior>
  <action>
    Edit `src/app/wear/[wearEventId]/page.tsx`:
    1. Locate the WearPhotoStreamed function (current lines 126-230 per PATTERNS)
    2. Locate the `let signedUrl: string | null = null` declaration + the `if (photoUrl) { ... }` block (current lines 165-172). Replace with the extended block from the interfaces excerpt: declare `signedUrl` + `signedVideoUrl` + `signedPosterUrl`; branch on `wear.mediaType === 'video'` → Promise.all for both URLs; `else if (photoUrl)` → existing single-URL mint VERBATIM
    3. Locate the WearCard invocation (current lines 201-229 per PATTERNS). Add three new props: `mediaType={wear.mediaType ?? undefined}`, `signedVideoUrl={signedVideoUrl}`, `signedPosterUrl={signedPosterUrl}`
    4. Do NOT touch the outer Suspense structure. Do NOT add `await connection()`. Do NOT add a new streamed child — extend the existing one (per RESEARCH §Risk 3 + durable memory `project_ppr_dynamic_before_use_cache` + `feedback_ppr_cache_fill_no_longer_call_out`)

    Edit `src/app/page.tsx`:
    1. Locate the existing tilesWithPhotos filter + signing loop (current lines 42-64 per earlier grep)
    2. After the existing photo mint loop builds `byId`, add an analogous block that filters `tilesWithVideos = railData.tiles.filter(t => t.mediaType === 'video' && t.posterPath)`, mints poster signed URLs via admin client + `Promise.all`, builds `posterById: Map<string, string>`
    3. Modify the final tile-mapping step to thread BOTH the photo signed URL (existing behavior) AND the new `signedPosterUrl` (from posterById) AND `mediaType` onto each tile — adjust the spread/object-construction so each tile carries the new fields when video
    4. WywtRail invocation may stay unchanged if it spreads tiles to WywtTile (the new fields flow through). If it uses explicit prop pass-throughs, add `mediaType={t.mediaType} signedPosterUrl={...}` to match the existing convention
    5. Per `feedback_ppr_cache_fill_no_longer_call_out`: do NOT add `await connection()` or new Suspense boundaries

    Edit `src/app/wears/[username]/page.tsx`:
    1. Locate the wears query + slide-mapping loop
    2. For each slide whose underlying wear has `mediaType === 'video'`, mint both `signedVideoUrl` (from `mediaPath`) and `signedPosterUrl` (from `posterPath`) via `createSupabaseServerClient()` + `Promise.all` — same pattern as /wear/[id]
    3. For each slide whose wear has `mediaType !== 'video'`, PRESERVE the existing per-slide signedUrl mint VERBATIM (VID-15 invariant)
    4. Construct each slide object with `mediaType`, `signedVideoUrl`, `signedPosterUrl` fields populated (null for photo slides). These flow through to `<WearsLane slides={slides} />` and on through to `<WearCard {...slide} />`

    After all 3 edits, run `npm run build` (exit 0 gate). Per durable memory `project_baseline_not_green_build_is_gate`: build is authoritative. Per `feedback_mobile_ui_verify_on_prod`: iOS playsInline + autoplay-muted-loop behaviors verify on prod (manual UAT) — automated build-green + targeted vitest are the per-task gates.

    Per durable memory `project_baseline_not_green_build_is_gate`: also run `npx vitest run tests/components/wear/ tests/components/home/ tests/components/wears/` and confirm no NEW failures (existing tests stay green — VID-15 broader regression check).
  </action>
  <verify>
    <automated>grep -c "signedVideoUrl" /Users/tylerwaneka/Documents/horlo/src/app/wear/\[wearEventId\]/page.tsx | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "signedPosterUrl" /Users/tylerwaneka/Documents/horlo/src/app/wear/\[wearEventId\]/page.tsx | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "wear.mediaType === 'video'" /Users/tylerwaneka/Documents/horlo/src/app/wear/\[wearEventId\]/page.tsx | grep -q "^1$" && grep -c "createSupabaseServerClient" /Users/tylerwaneka/Documents/horlo/src/app/wear/\[wearEventId\]/page.tsx | awk '{print ($1 >= 2)}' | grep -q 1 && grep -c "await connection" /Users/tylerwaneka/Documents/horlo/src/app/wear/\[wearEventId\]/page.tsx | grep -q "^0$" && grep -c "signedPosterUrl" /Users/tylerwaneka/Documents/horlo/src/app/page.tsx | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "mediaType" /Users/tylerwaneka/Documents/horlo/src/app/page.tsx | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "createSignedUrl" /Users/tylerwaneka/Documents/horlo/src/app/page.tsx | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "signedVideoUrl" /Users/tylerwaneka/Documents/horlo/src/app/wears/\[username\]/page.tsx | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "signedPosterUrl" /Users/tylerwaneka/Documents/horlo/src/app/wears/\[username\]/page.tsx | awk '{print ($1 >= 1)}' | grep -q 1 && grep -c "mediaType === 'video'" /Users/tylerwaneka/Documents/horlo/src/app/wears/\[username\]/page.tsx | awk '{print ($1 >= 1)}' | grep -q 1 && npm run build 2>&1 | grep -qE "Compiled successfully|✓ Compiled"</automated>
  </verify>
  <acceptance_criteria>
    - /wear/[wearEventId]/page.tsx: contains signedVideoUrl (>=2), signedPosterUrl (>=2), `wear.mediaType === 'video'` (=1), createSupabaseServerClient (>=2 — preserved photo branch + new video branch), ZERO `await connection` calls (no new PPR opt-outs)
    - /page.tsx: contains signedPosterUrl + mediaType references; preserves existing createSignedUrl invocation
    - /wears/[username]/page.tsx: contains signedVideoUrl, signedPosterUrl, `mediaType === 'video'` discriminator
    - `npm run build` exits 0
    - Existing `tests/components/wear/`, `tests/components/home/`, `tests/components/wears/` suites continue to pass (VID-15 broader regression)
  </acceptance_criteria>
  <done>
    - Three Server Components mint signed URLs for video media via admin client + Promise.all
    - Photo paths preserved verbatim in all three files
    - No new Suspense boundaries; no await connection() additions
    - Build green; broader test suites do not regress
    - Phase 77 is now CODE-COMPLETE end-to-end
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAL row → Server Component signed-URL mint | Server fetches mediaType/mediaPath/posterPath; mints signed URLs server-side; passes opaque time-limited URLs to client |
| Server Component → WearCard / WywtTile | Streamed Suspense child renders client component with signed URLs as opaque props |
| WywtTile / WearVideoClient → browser `<video>` autoplay | iOS Safari escalates to fullscreen without playsInline — durable guardrail (Plan 07 + Plan 05 enforced) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03 | Spoofing / Information Disclosure (MEDIUM from planning_context — cookie client signing fails for non-owner covers) | createSupabaseServerClient admin client | mitigate | Per Phase 61 lesson + planning_context T-77-03: all three modified Server Components in this plan (`/wear/[wearEventId]/page.tsx`, `/page.tsx`, `/wears/[username]/page.tsx`) use `createSupabaseServerClient()` — the admin/server client — NOT the browser cookie client. Verify-block grep confirms presence (createSupabaseServerClient >= 2 in the detail page, present in the others). The admin client mints URLs without cookie-session dependency; non-owner covers sign correctly. |
| T-77-ppr-419 | Denial of Service / UX (LOW — adding new Suspense boundary re-introduces #419 family) | Suspense structure preservation | mitigate | Per RESEARCH §Risk 3 + durable memory `project_ppr_dynamic_before_use_cache` + `feedback_ppr_cache_fill_no_longer_call_out`: the PPR / #419 family is RESOLVED infrastructure. This plan EXTENDS WearPhotoStreamed in place; does NOT add new Suspense boundaries; does NOT add `await connection()` (page is already dynamic via getCurrentUser). Verify-block grep `await connection` returns 0 in /wear/[wearEventId]/page.tsx. |
| T-77-lane-onended | UX (LOW — coupling lane advance to video.onEnded breaks D-07) | WearsLane.tsx no-onEnded guardrail | mitigate | Per D-07 + RESEARCH §Risk 8: segmented progress indicator is driven by embla 'select' event only. NO video.onEnded listener added in WearsLane. Verify-block grep `onEnded` returns 0 in WearsLane.tsx. Video loops indefinitely until user swipes. |
| T-77-tile-poster-leak | Information Disclosure (LOW — passing raw posterPath storage path to client) | thread signedPosterUrl, not raw path | mitigate | The home rail signing loop signs `posterPath` on the server and passes only `signedPosterUrl` (60-minute opaque URL) to WywtTile. The raw `posterPath` Storage path is NEVER passed to the client. Same pattern as the existing photoUrl signing. |
| T-77-15-final | Tampering (HIGH — VID-15 — accidentally breaking photo flow during multi-file SSR edits) | additive-only edits + VERBATIM photo branch preservation | mitigate | Each of the three SSR files preserves the existing photo branch byte-for-byte. The video branch is added as an additional `if (wear.mediaType === 'video') { ... } else if (photoUrl) { ... }` chain. Existing tests (`tests/components/wear/`, `tests/components/home/`, `tests/components/wears/`) act as the regression net; Task 2 acceptance criterion requires no new failures. |
| T-77-non-owner-video | Information Disclosure (RESOLVED — visibility tier check upstream) | DAL access scoping | accept | Per Phase 76 schema, `wear_events.visibility` is the access gate; the DAL readers (Plan 03) preserve the existing visibility-based access scoping. The signed URL minting trusts the DAL's row return — if a row is returned, the viewer is authorized to see it. This is identical to existing photo behavior. |
</threat_model>

<verification>
- /wear/[wearEventId]/page.tsx: signedVideoUrl ≥2, signedPosterUrl ≥2, `wear.mediaType === 'video'` = 1, createSupabaseServerClient ≥2 (preserved photo + new video branches), ZERO `await connection`
- /page.tsx: signedPosterUrl ≥1, mediaType ≥1, createSignedUrl ≥1 (preserved + extended)
- /wears/[username]/page.tsx: signedVideoUrl ≥1, signedPosterUrl ≥1, `mediaType === 'video'` ≥1
- WywtTile.tsx: VideoPlayBadge ≥2, signedPosterUrl ≥2, `mediaType === 'video'` ≥2
- WearsLane.tsx: signedVideoUrl + signedPosterUrl present in WearSlide; ZERO `onEnded`
- WywtTile.video.test.tsx: 3 it() / 0 it.todo, both markers present
- `npm run build` exits 0
- `npx vitest run tests/components/home/WywtTile.video.test.tsx` exits 0 with 3 passed
- Broader test suites under tests/components/wear/, tests/components/home/, tests/components/wears/ do not regress
</verification>

<success_criteria>
- VID-13 satisfied: video tiles render poster + VideoPlayBadge overlay across rail surfaces (home + future tile surfaces)
- VID-14 satisfied: /wear/[id] mints both URLs and passes them through WearCard → WearVideoClient (Plan 07 deliverable) for autoplay-muted-loop playback; /wears/[username] does the same for stories slides
- VID-15 invariant: photo paths in all 3 SSR files preserved byte-for-byte; existing test suites continue to pass
- T-77-03 mitigated: admin client used for all signed-URL minting
- T-77-ppr-419 mitigated: no new Suspense boundaries; no `await connection()` added
- T-77-lane-onended mitigated: no `video.onEnded` listener in WearsLane
- Build green; new + existing tests pass
- Phase 77 CODE-COMPLETE: every requirement (VID-01..06, VID-13..15, WR-02) covered by code + automated test
</success_criteria>

<output>
After completion, create `.planning/phases/77-video-capture-display-ui/77-08-SUMMARY.md` per the GSD summary template. Include: (a) per-file diff summary (WywtTile, WearsLane, /wear/[id]/page, /page, /wears/[username]/page), (b) confirmation of signing-client choice (createSupabaseServerClient — admin) for all three SSR files, (c) confirmation of ZERO `await connection()` additions + ZERO new Suspense boundaries (PPR guardrail), (d) confirmation of ZERO `onEnded` in WearsLane (D-07 guardrail), (e) test results (3 new passing + existing suites green), (f) end-of-phase status: Phase 77 CODE-COMPLETE, ready for prod UAT walk per `feedback_mobile_ui_verify_on_prod` (iOS playsInline + autoplay-muted-loop behavior verifies on prod, not jsdom).
</output>
