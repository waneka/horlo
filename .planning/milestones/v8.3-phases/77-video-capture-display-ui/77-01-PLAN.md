---
phase: 77
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/spike-mr-capture
  - tests/hooks/useMediaCapability.test.ts
  - tests/unit/videoCapture.test.ts
  - tests/unit/posterExtraction.test.ts
  - tests/unit/mediaState.test.ts
  - tests/unit/dalMediaColumns.test.ts
  - tests/components/wywt/VideoCaptureView.test.tsx
  - tests/components/wywt/ComposeStep.video.test.tsx
  - tests/components/wywt/ComposeStep.submit.video.test.tsx
  - tests/components/wear/WearVideoClient.test.tsx
  - tests/components/wear/WearCard.video.test.tsx
  - tests/components/home/WywtTile.video.test.tsx
autonomous: true
requirements: []
tags: [foundation, cleanup, test-stubs, wave-0, security]
must_haves:
  truths:
    - "Per T-77-01 (HIGH): src/app/spike-mr-capture/ — currently an unauthenticated production route from Spike 001 — is fully removed from the repository. Vercel's next deploy publishes a 404 for /spike-mr-capture."
    - "Per 77-VALIDATION.md §Wave 0 Requirements: 11 RED test stub files exist on disk under tests/hooks/, tests/unit/, tests/components/wywt/, tests/components/wear/, tests/components/home/ — every Wave 1+ implementation task has at least one assertion to flip green (Nyquist)."
    - "Every stub uses `import { describe, it, expect } from 'vitest'` and at least one `it.todo(...)` or skipped placeholder — the file resolves through Vitest's test-discovery + parses + does not fail the suite at the file level (pre-condition for Wave 2 / Wave 3 to depend on these files)."
    - "Per durable memory `project_baseline_not_green_build_is_gate`: `npm run build` exits 0 after spike deletion and stub creation. The full vitest suite does not regress (already-green tests stay green; new stubs report as 0 failing + N todo)."
  artifacts:
    - path: tests/hooks/useMediaCapability.test.ts
      provides: "RED stub for useMediaCapability probe (VID-01, VID-04)"
      contains: "describe('useMediaCapability"
    - path: tests/unit/videoCapture.test.ts
      provides: "RED stub for MediaRecorder 3s timer + cancel guard (VID-02, VID-03)"
      contains: "describe('videoCapture"
    - path: tests/unit/posterExtraction.test.ts
      provides: "RED stub for canvas seek + toBlob (VID-05)"
      contains: "describe('extractPosterBlob"
    - path: tests/unit/mediaState.test.ts
      provides: "RED stub for discriminated MediaState union (VID-06)"
      contains: "describe('MediaState"
    - path: tests/unit/dalMediaColumns.test.ts
      provides: "RED stub for WR-02 DAL column inclusion across 5 readers"
      contains: "describe('DAL media columns"
    - path: tests/components/wywt/VideoCaptureView.test.tsx
      provides: "RED stub for VideoCaptureView render + discard + stream-as-prop guard"
      contains: "describe('VideoCaptureView"
    - path: tests/components/wywt/ComposeStep.video.test.tsx
      provides: "RED stub for 3-button chooser + capability hide (VID-01)"
      contains: "describe('ComposeStep video"
    - path: tests/components/wywt/ComposeStep.submit.video.test.tsx
      provides: "RED stub for submit pipeline integration (VID-01, VID-06)"
      contains: "describe('ComposeStep video submit"
    - path: tests/components/wear/WearVideoClient.test.tsx
      provides: "RED stub for autoplay attrs + error fallback (VID-14)"
      contains: "describe('WearVideoClient"
    - path: tests/components/wear/WearCard.video.test.tsx
      provides: "RED stub for video branch (VID-13) + VID-15 regression"
      contains: "describe('WearCard video"
    - path: tests/components/home/WywtTile.video.test.tsx
      provides: "RED stub for VideoPlayBadge overlay (VID-13)"
      contains: "describe('WywtTile video"
  key_links:
    - from: "Wave 1+ implementation tasks"
      to: "test stub files under tests/"
      via: "Each implementation flips its corresponding stub from todo → green by adding real assertions and the implementation code"
      pattern: "it\\.todo|it\\("
---

<objective>
Establish the Wave 0 foundation: (1) delete the spike test page src/app/spike-mr-capture/ — currently an unauthenticated production route — and (2) seed 11 RED Vitest stub files so every Wave 1+ implementation task has a concrete file to flip green per the Nyquist sampling contract in 77-VALIDATION.md.

Purpose: closes the highest-severity threat in Phase 77's threat model (T-77-01) BEFORE any other code change lands; populates the test scaffolding the orchestrator + per-task feedback loop depend on (per durable memory `project_phase_plan_index_wave_collapse` — sampling files must exist for downstream waves to verify against).

Output:
- src/app/spike-mr-capture/ directory removed
- 11 test stub files created under tests/hooks/, tests/unit/, tests/components/wywt/, tests/components/wear/, tests/components/home/
- 77-VALIDATION.md `wave_0_complete: true` flipped
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-video-capture-display-ui/77-CONTEXT.md
@.planning/phases/77-video-capture-display-ui/77-RESEARCH.md
@.planning/phases/77-video-capture-display-ui/77-PATTERNS.md
@.planning/phases/77-video-capture-display-ui/77-VALIDATION.md

<interfaces>
<!-- Existing test conventions (Vitest 3 + @testing-library/react + jsdom default) — copy these patterns into every new stub. -->

From tests/hooks/useViewedWears.test.ts:1-10 (canonical hook test file shape — copy verbatim, swap import path):
- `import { describe, it, expect, beforeEach, vi } from 'vitest'`
- `import { renderHook, act, waitFor } from '@testing-library/react'`
- `import { useViewedWears } from '@/hooks/useViewedWears'`
- `describe('useViewedWears — SSR-safe localStorage viewed-state hook', () => { beforeEach(() => { vi.restoreAllMocks() }) ... })`

From tests/actions/wearEventsVideo.test.ts (Phase 76 — canonical action-test shape; structurally similar to the new tests/components/wywt/ComposeStep.submit.video.test.tsx stub).

Existing test files confirmed present (do NOT modify):
- tests/components/wywt/CameraCaptureView.test.tsx (analog for VideoCaptureView stub)
- tests/components/wear/WearCard.test.tsx (analog for WearCard.video stub; existing tests must continue passing — VID-15 regression)
- tests/hooks/useViewedWears.test.ts (analog for useMediaCapability stub)
- tests/unit/buildWearVideoPath.test.ts (analog for unit-style stubs)
- tests/unit/wearRail.test.ts (analog for dalMediaColumns stub)

Spike route to delete (T-77-01):
- src/app/spike-mr-capture/ — entire directory; route was published unauthenticated during Spike 001 and Spike 001 README explicitly lists deletion as a cleanup step
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/app/spike-mr-capture/ (T-77-01 cleanup)</name>
  <files>src/app/spike-mr-capture</files>
  <read_first>
    - .planning/spikes/001-mr-ios-capture/README.md (confirm "delete after spike concludes" cleanup note)
    - 77-CONTEXT.md §specifics — "Spike test page exists at src/app/spike-mr-capture/ — Spike 001 cleanup note says delete after spike concludes."
    - 77-RESEARCH.md §Risk 4 — "Spike Test Page Deletion (Unauthenticated Prod Route): src/app/spike-mr-capture/ is currently live in production as an unauthenticated route. ... This must be the FIRST task in Wave 1"
  </read_first>
  <action>
    Use `git rm -r src/app/spike-mr-capture/` to remove the entire directory in a single tracked-deletion commit. No replacement file, no redirect — Vercel will publish 404 for /spike-mr-capture on next deploy. After deletion, run `npm run build` to confirm the route table compiles cleanly (no orphan import). Per durable memory `project_baseline_not_green_build_is_gate`: build is the authoritative gate. Do NOT create a redirect file; the spike was never user-facing — 404 is the correct end state. Do NOT modify any other src/app/ route. The deletion closes T-77-01 (HIGH) — every unauthenticated route surface that touches getUserMedia from an arbitrary origin is a privacy + permission-prompt-spam risk.
  </action>
  <verify>
    <automated>! test -d /Users/tylerwaneka/Documents/horlo/src/app/spike-mr-capture && npm run build 2>&1 | grep -qE "Compiled successfully|✓ Compiled"</automated>
  </verify>
  <acceptance_criteria>
    - `test -d src/app/spike-mr-capture` returns non-zero (directory absent)
    - `git status src/app/spike-mr-capture` shows the directory under "deleted" (tracked deletion staged)
    - `npm run build` exits 0 with no missing-module errors
    - No `redirect()` or replacement route file added
  </acceptance_criteria>
  <done>
    - Directory absent from working tree and staged in git as deletion
    - Build green
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed 11 RED Vitest stub files (Wave 0 scaffolding)</name>
  <files>tests/hooks/useMediaCapability.test.ts, tests/unit/videoCapture.test.ts, tests/unit/posterExtraction.test.ts, tests/unit/mediaState.test.ts, tests/unit/dalMediaColumns.test.ts, tests/components/wywt/VideoCaptureView.test.tsx, tests/components/wywt/ComposeStep.video.test.tsx, tests/components/wywt/ComposeStep.submit.video.test.tsx, tests/components/wear/WearVideoClient.test.tsx, tests/components/wear/WearCard.video.test.tsx, tests/components/home/WywtTile.video.test.tsx</files>
  <read_first>
    - tests/hooks/useViewedWears.test.ts (lines 1-10 — canonical hook-test imports + describe shape)
    - tests/components/wywt/CameraCaptureView.test.tsx (lines 1-16 — component-test imports including vi.stubGlobal pattern)
    - tests/unit/buildWearVideoPath.test.ts (canonical unit-test shape — no mocks, plain assertions)
    - 77-VALIDATION.md §"Wave 0 Requirements" (the 11-file list — exact paths)
    - 77-PATTERNS.md §"Wave 0 — Test Stubs" (per-file analog + key assertion patterns)
    - 77-RESEARCH.md §"Validation Architecture" (per-req test-mapping table — req-id → file → command)
  </read_first>
  <action>
    Create 11 new test files at exactly the paths listed in `<files>`. Each file is a minimal RED stub: imports from `vitest`, a single `describe(...)` block, and at least one `it.todo(...)` placeholder per requirement that file covers (e.g., useMediaCapability.test.ts has `it.todo('VID-01: returns supportsVideoCapture=false before probe runs')`, `it.todo('VID-01/VID-04: returns supportsVideoCapture=true + mp4 mimeType when mp4 supported')`, `it.todo('VID-04: falls back to webm when mp4 unsupported')`, `it.todo('VID-01: returns supportsVideoCapture=false when getUserMedia unavailable')`).

    Per-file required content:

    1. **tests/hooks/useMediaCapability.test.ts** — imports `describe, it, expect` from `vitest`; describe `'useMediaCapability — capability probe hook (VID-01, VID-04)'`; 4 `it.todo` entries matching the 4 cases in 77-PATTERNS.md §useMediaCapability.test.ts.

    2. **tests/unit/videoCapture.test.ts** — imports `describe, it, expect, vi` from `vitest`; describe `'videoCapture — MediaRecorder 3s timer + cancel guard (VID-02, VID-03)'`; 3 `it.todo` entries: VID-02 auto-stop at 3000ms, VID-03 cancel before 3000ms emits no blob, VID-02 chunks assembled inside onstop (iOS Pitfall 2).

    3. **tests/unit/posterExtraction.test.ts** — imports `describe, it, expect, vi` from `vitest`; describe `'extractPosterBlob — canvas seek + toBlob (VID-05)'`; 3 `it.todo` entries: VID-05 seeks to `duration * 0.75` before `toBlob`, VID-05 rejects when `toBlob` returns null, VID-05 revokes object URL on success.

    4. **tests/unit/mediaState.test.ts** — imports `describe, it, expect` from `vitest`; imports `type { MediaState } from '@/lib/wywtTypes'` — note this import will FAIL at the file level until Plan 02 lands; wrap the import in a top-level `try { ... } catch {}` IS NOT ALLOWED; instead, write the import as `// TODO Plan 02: import type { MediaState } from '@/lib/wywtTypes'` (commented out) and add a single `it.todo('VID-06: MediaState discriminated union has kind:none|photo|video — uncomment after Plan 02')`. After Plan 02 lands, that plan's task list includes uncommenting the import and replacing `it.todo` with the 3 cases from 77-PATTERNS.md §mediaState.test.ts.

    5. **tests/unit/dalMediaColumns.test.ts** — imports `describe, it, expect` from `vitest`; describe `'DAL media columns (WR-02)'`; 4 `it.todo` entries for the 4 affected readers: `getWearEventByIdForViewer`, `getWearEventsForViewer`, `getWearRailForViewer`, `getActiveWearsForUser` — each returns `mediaType`, `mediaPath`, `posterPath` (even if null).

    6. **tests/components/wywt/VideoCaptureView.test.tsx** — imports `describe, it, expect, vi` from `vitest`; describe `'VideoCaptureView — stream-as-prop video capture (VID-02, VID-03)'`; 4 `it.todo` entries: renders Cancel + Record 3s buttons before recording; does NOT call getUserMedia internally; discard returns to pre-recording state (VID-03); disabled prop disables Record button.

    7. **tests/components/wywt/ComposeStep.video.test.tsx** — imports `describe, it, expect, vi` from `vitest`; describe `'ComposeStep — 3-button chooser (VID-01, D-04)'`; 2 `it.todo` entries: renders 3 pre-capture buttons when supportsVideoCapture=true (VID-01); renders 2 buttons when supportsVideoCapture=false (D-04).

    8. **tests/components/wywt/ComposeStep.submit.video.test.tsx** — imports `describe, it, expect, vi` from `vitest`; describe `'ComposeStep — video submit pipeline (VID-01, VID-06)'`; 3 `it.todo` entries: submit calls logWearWithVideo (not logWearWithPhoto) when MediaState.kind='video'; upload ordering = video → poster → action; compensating .remove([videoPath]) fires when poster upload fails.

    9. **tests/components/wear/WearVideoClient.test.tsx** — imports `describe, it, expect, vi` from `vitest`; describe `'WearVideoClient — autoplay attrs + error fallback (VID-14)'`; 3 `it.todo` entries: VID-14 video element has autoplay+muted+loop+playsInline; VID-14 onError swaps to poster + "Video unavailable" label; VID-14 tap toggles .pause()/.play() (D-06).

    10. **tests/components/wear/WearCard.video.test.tsx** — imports `describe, it, expect, vi` from `vitest`; describe `'WearCard — video branch + VID-15 regression'`; 2 `it.todo` entries: VID-13 renders WearVideoClient (contains <video>) when mediaType='video'; VID-15 no <video> element when mediaType absent (photo regression).

    11. **tests/components/home/WywtTile.video.test.tsx** — imports `describe, it, expect, vi` from `vitest`; describe `'WywtTile — VideoPlayBadge overlay (VID-13)'`; 3 `it.todo` entries: VID-13 renders VideoPlayBadge when mediaType='video'; VID-13 no badge when mediaType='photo' or absent; VID-13 uses signedPosterUrl as image source for video tiles.

    Every file ends with a single newline. Each file uses `// Wave 0 RED stub — Phase 77 / 77-01-PLAN.md` as its first line (above imports) so future planner agents can locate the stub origin. Use `it.todo(...)` (not `it.skip` and not `it(...).skip()`) — `it.todo` is the Vitest 3 idiom for "this test has not been implemented yet" and reports as `↓ todo` in the output rather than as a failure.

    After all 11 files exist, run `npm run test 2>&1 | tail -20` and confirm the output shows `Test Files ... passed` with non-zero `todo` count and zero new `failed`. Per durable memory `project_baseline_not_green_build_is_gate`: also run `npm run build` and confirm exit 0 (none of the stubs imports from a path that doesn't yet exist — Plan 02 + later plans will create the implementation files; the `mediaState.test.ts` import is commented out to avoid a tsc red on Wave 0).

    After verification, update `.planning/phases/77-video-capture-display-ui/77-VALIDATION.md` frontmatter: flip `wave_0_complete: false` → `wave_0_complete: true`. Do NOT update `nyquist_compliant` here — that flips after every VID-* requirement has a real (non-todo) automated assertion.
  </action>
  <verify>
    <automated>test -f /Users/tylerwaneka/Documents/horlo/tests/hooks/useMediaCapability.test.ts && test -f /Users/tylerwaneka/Documents/horlo/tests/unit/videoCapture.test.ts && test -f /Users/tylerwaneka/Documents/horlo/tests/unit/posterExtraction.test.ts && test -f /Users/tylerwaneka/Documents/horlo/tests/unit/mediaState.test.ts && test -f /Users/tylerwaneka/Documents/horlo/tests/unit/dalMediaColumns.test.ts && test -f /Users/tylerwaneka/Documents/horlo/tests/components/wywt/VideoCaptureView.test.tsx && test -f /Users/tylerwaneka/Documents/horlo/tests/components/wywt/ComposeStep.video.test.tsx && test -f /Users/tylerwaneka/Documents/horlo/tests/components/wywt/ComposeStep.submit.video.test.tsx && test -f /Users/tylerwaneka/Documents/horlo/tests/components/wear/WearVideoClient.test.tsx && test -f /Users/tylerwaneka/Documents/horlo/tests/components/wear/WearCard.video.test.tsx && test -f /Users/tylerwaneka/Documents/horlo/tests/components/home/WywtTile.video.test.tsx && grep -lq "it.todo" /Users/tylerwaneka/Documents/horlo/tests/hooks/useMediaCapability.test.ts && grep -lq "Wave 0 RED stub" /Users/tylerwaneka/Documents/horlo/tests/components/wear/WearVideoClient.test.tsx && grep -q "wave_0_complete: true" /Users/tylerwaneka/Documents/horlo/.planning/phases/77-video-capture-display-ui/77-VALIDATION.md && npm run build 2>&1 | grep -qE "Compiled successfully|✓ Compiled"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 stub files exist at the paths listed in `<files>`
    - Each file contains `// Wave 0 RED stub — Phase 77` first-line marker
    - Each file contains at least one `it.todo(...)` entry
    - `tests/unit/mediaState.test.ts` has the `MediaState` import COMMENTED out (so Wave 0 doesn't depend on Plan 02)
    - `npm run build` exits 0
    - `npm run test` does not introduce any NEW failures (todo entries do not count as failures in Vitest 3)
    - `77-VALIDATION.md` frontmatter `wave_0_complete: true`
  </acceptance_criteria>
  <done>
    - 11 stubs present with the marker line + at least one `it.todo`
    - Build green; test suite shows no new failures
    - `wave_0_complete: true` in VALIDATION.md
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → `/spike-mr-capture` | Unauthenticated production route exposing getUserMedia + MediaRecorder UI — every public visitor can be prompted for camera permission |
| Test scaffolding → CI vitest runner | RED stubs must parse cleanly and not crash the test discovery pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Information Disclosure / Spoofing (HIGH from planning_context) | src/app/spike-mr-capture/ unauthenticated prod route | mitigate | `git rm -r src/app/spike-mr-capture/` in Task 1 — directory absent from working tree; Vercel publishes 404 on next deploy. File-existence assertion in Task 1 acceptance_criteria confirms. |
| T-77-test-collision | Spoofing (LOW — stub file shadowing real file) | tests/ stub paths | accept | All 11 stub paths are NEW files (verified by Wave 0 Requirements list in 77-VALIDATION.md); none overlap an existing test file. The risk of accidental overwrite is zero. |
| T-77-build-break | Denial of Service (LOW — typo in stub crashes vitest discovery) | RED stub syntax | mitigate | `npm run build` exit 0 + `npm run test` no-new-failures gates in Task 2 verify block catch any parse error before commit. `it.todo` is the canonical Vitest 3 todo-marker and does not crash discovery. |
</threat_model>

<verification>
- `! test -d src/app/spike-mr-capture` (spike directory absent)
- All 11 stub files present and contain `// Wave 0 RED stub` marker + at least one `it.todo`
- `tests/unit/mediaState.test.ts` has commented-out `MediaState` import (will be uncommented in Plan 02)
- `npm run build` exits 0
- `npm run test` shows the new `it.todo` entries as `↓ todo`, zero new `× failed`
- `.planning/phases/77-video-capture-display-ui/77-VALIDATION.md` frontmatter has `wave_0_complete: true`
</verification>

<success_criteria>
- T-77-01 mitigated: spike route deleted; Vercel next-deploy publishes 404
- Wave 0 scaffolding in place: every Wave 1+ implementation has a real file to flip green
- Build + test suite stay green; no regression to existing photo flow (VID-15 invariant)
</success_criteria>

<output>
After completion, create `.planning/phases/77-video-capture-display-ui/77-01-SUMMARY.md` per the GSD summary template. Include: (a) `git rm` commit hash for the spike deletion, (b) list of 11 stub file paths created, (c) Vitest `todo` count before vs after, (d) confirmation of `npm run build` exit 0.
</output>
