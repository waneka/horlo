---
phase: 60-multi-photo-schema-dal
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/data/watches.ts
  - src/data/wearEvents.ts
  - src/data/search.ts
  - src/app/actions/comments.ts
  - src/app/actions/wishlist.ts
  - src/app/actions/account.ts
  - src/app/actions/watches.ts
  - src/lib/types.ts
  - tests/integration/phase60-watch-photos.test.ts
autonomous: true
requirements: [PHOTO-04, PHOTO-07]
must_haves:
  truths:
    - "getWatchesByUser, getWatchById, and getWatchByIdForViewer resolve imageUrl from the lowest-sort_order watch_photos row (computed cover, no cached column; D-04)"
    - "Cover fallback chain is: owner upload[0] storage_path -> catalog imageUrl -> undefined (D-05); cover wins over catalog when both present (D-06)"
    - "Watch.imageUrl TS type field is unchanged; it is now populated by the computed cover - no grid/rail reader churns (D-08)"
    - "Every direct watches.image_url SELECT site (comments, wishlist, wearEvents, search) is repointed to the cover subquery so the dropped column does not break those queries at runtime"
    - "addWatchPhoto enforces a per-watch cap of MAX_PHOTOS_PER_WATCH=10, counting only watch_photos rows, and rejects the (cap+1)th insert with PhotoCapExceededError (D-12, D-13, D-14)"
    - "Public wear pics are NOT in watch_photos and do not count against the cap; the cap counts owner-upload watch_photos rows only — Phase 62 unions wear pics at read (D-02, D-14)"
    - "addWatchPhoto and bulkReorderPhotos confirm watches.user_id = userId before mutating (cross-tenant insert/reorder blocked; T-60-XTENANT)"
    - "bulkReorderPhotos does a full-rewrite CASE WHEN reorder with a set-completeness check (mirrors bulkReorderWishlist; D-03)"
    - "removeWatch purges watch-photos storage objects (DB-first: query watch_photos storage_paths then storage.remove) BEFORE deleteWatch (T-60-ORPHAN)"
    - "activities feed renderers reading metadata.imageUrl are confirmed display-only (read stored jsonb, not a watches join) - benign after the column drop"
  artifacts:
    - path: "src/data/watches.ts"
      provides: "Cover-resolving reads (3 paths), mapper repoint, PhotoCapExceededError, MAX_PHOTOS_PER_WATCH, addWatchPhoto, bulkReorderPhotos, deleteWatchPhoto"
      contains: "export async function addWatchPhoto"
    - path: "src/app/actions/account.ts"
      provides: "purgeWatchPhotos helper (DB-first storage purge); wipeCollection watch-photos sweep"
      contains: "purgeWatchPhotos"
    - path: "src/app/actions/watches.ts"
      provides: "removeWatch calls purgeWatchPhotos before deleteWatch"
      contains: "purgeWatchPhotos"
    - path: "src/lib/types.ts"
      provides: "WatchPhoto domain type"
      contains: "interface WatchPhoto"
  key_links:
    - from: "src/data/watches.ts getWatchesByUser"
      to: "watch_photos cover (lowest sort_order)"
      via: "correlated subquery in .select() projection"
      pattern: "watch_photos.*sort_order|coverStoragePath"
    - from: "src/data/watches.ts addWatchPhoto"
      to: "MAX_PHOTOS_PER_WATCH cap check"
      via: "count(*) before insert, throw PhotoCapExceededError"
      pattern: "PhotoCapExceededError"
    - from: "src/app/actions/watches.ts removeWatch"
      to: "purgeWatchPhotos before deleteWatch"
      via: "storage purge ordering"
      pattern: "purgeWatchPhotos"
---

<objective>
Extend the DAL so the cover thumbnail is computed at read across all three watch read paths (PHOTO-04), enforce the per-watch photo cap (PHOTO-07), add reorder + delete-photo + storage purge, and — critically — repoint EVERY query site that currently SELECTs `watches.image_url` directly so the dropped column (Plan 01) does not break them at runtime. The `Watch.imageUrl` TS contract is preserved (D-08); only the query sites change.

Purpose: Make multi-photo cover resolution observable at the data layer before any UI (SC2, D-06), enforce the cap in the DAL (the sole write path, D-13), and prevent the column-drop blast radius from breaking comments/wishlist/wearEvents/search queries.
Output: cover-resolving DAL + cap/reorder/delete photo functions + storage purge hook + repointed external readers + WatchPhoto type + green integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-multi-photo-schema-dal/60-CONTEXT.md
@.planning/phases/60-multi-photo-schema-dal/60-RESEARCH.md
@.planning/phases/60-multi-photo-schema-dal/60-PATTERNS.md
@.planning/phases/60-multi-photo-schema-dal/60-01-PLAN.md

<interfaces>
<!-- The DAL analogs and the exact set of direct-column read sites to repoint. -->
From src/data/watches.ts (the file being extended — read it; key analogs):
- mapRowToWatch(row) line ~17-59: line 45 `imageUrl: row.imageUrl ?? undefined` must be REMOVED (column gone); cover is resolved per-query and overridden after the spread.
- mapDomainToRow line ~93: `if ('imageUrl' in data) row.imageUrl = data.imageUrl ?? null` must be REMOVED (column not writable).
- getWatchesByUser lines 126-166: `.leftJoin(watchesCatalog, ...)` at 145 — add the cover subquery to `.select()`; catalog imageUrl is available from this existing leftJoin.
- getWatchById lines 172-178 (simple owner query, NO catalog join — add cover subquery AND a catalog leftJoin so the fallback chain works) and getWatchByIdForViewer lines 193-235 (privacy gate — add cover subquery + catalog leftJoin + override). Per RESEARCH Open Q3, update ALL THREE for consistency.
- OwnerMismatchError/SetMismatchError lines 390-410 (reuse these classes for reorder).
- bulkReorderWishlist lines 433-482 (the full CASE WHEN reorder + set-completeness analog — copy shape).
- deleteWatch lines 320-328 (ownership-scoped delete + throw analog).
- count(*)::int cast at line 445; `coalesce(max(...), -1)::int` at line 367 (next sort_order).
- imports line 5: `import { watches, profileSettings, watchesCatalog } from '@/db/schema'` — add `watchPhotos`.

DIRECT watches.image_url SELECT sites to REPOINT (these break at runtime once the column is dropped):
- src/app/actions/comments.ts:82 and :107 — `imageUrl: watches.imageUrl` (feed thumbnail metadata)
- src/app/actions/wishlist.ts:73 — `imageUrl: watches.imageUrl`
- src/data/wearEvents.ts:267, :344, :466, :518 — `watches.imageUrl` / `watchImageUrl: watches.imageUrl`
- src/data/search.ts — raw SQL `w.image_url` (WITH matched CTE ~line 243; jsonb_build_object `'image_url', m.image_url` ~line 279)
- src/data/search.ts:146 — `s.viewerWatch.imageUrl` reads the Watch domain type (populated by Task 1's cover override) — keeps working IF the upstream read resolves cover; verify, no change expected.

From src/app/actions/account.ts (purgeWearPhotos lines 22-51 analog; wipeCollection call-site lines 81-82).
From src/app/actions/watches.ts removeWatch lines 467-498 (call site to add purgeWatchPhotos before deleteWatch).

ActivityRow display-only confirmation (benign, discretion call #3):
- src/components/home/ActivityRow.tsx:33 `row.metadata.imageUrl` and AggregatedActivityRow.tsx:31 — read stored jsonb via getSafeImageUrl, NOT a watches join. Display-only. No change needed; assert benign in SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover resolution across all 3 read paths + mapper repoint + WatchPhoto type</name>
  <files>src/data/watches.ts, src/lib/types.ts</files>
  <read_first>
    - src/data/watches.ts (the file being modified — read mapRowToWatch lines 17-59, mapDomainToRow ~line 93, getWatchesByUser 126-166, getWatchById 172-178, getWatchByIdForViewer 193-235, imports line 5)
    - src/lib/types.ts (the Watch type — `imageUrl?: string` at ~line 76 STAYS unchanged per D-08; add the new WatchPhoto interface)
    - .planning/phases/60-multi-photo-schema-dal/60-PATTERNS.md (Extension 1 cover join lines 258-287; types.ts additions lines 488-509)
    - .planning/phases/60-multi-photo-schema-dal/60-RESEARCH.md (Pattern 3a cover join; Pitfall 5 mapper; Open Q1 return raw storagePath; Open Q3 update all 3 paths)
  </read_first>
  <behavior>
    - getWatchesByUser: a watch with a watch_photos row at sort_order=0 resolves imageUrl to that row's storage_path (cover wins over catalog)
    - getWatchesByUser: a watch with NO watch_photos but a non-null catalog imageUrl resolves imageUrl to the catalog value (fallback chain D-05)
    - getWatchesByUser: a watch with neither resolves imageUrl to undefined
    - getWatchById and getWatchByIdForViewer resolve imageUrl by the same cover->catalog->undefined chain
  </behavior>
  <action>
    Add `watchPhotos` to the `@/db/schema` import in src/data/watches.ts. REMOVE `imageUrl: row.imageUrl ?? undefined` from mapRowToWatch (the column no longer exists on the row) and REMOVE the `if ('imageUrl' in data) row.imageUrl = data.imageUrl ?? null` line from mapDomainToRow. In getWatchesByUser, add a correlated subquery to the `.select()` projection: a `sql<string | null>` selecting `wp.storage_path FROM watch_photos wp WHERE wp.watch_id = watches.id ORDER BY wp.sort_order ASC LIMIT 1` aliased `coverStoragePath`; also ensure `watchesCatalog.imageUrl` is in the catalog projection. In the rows.map callback, after `...mapRowToWatch(watch)`, override `imageUrl: row.coverStoragePath ?? (catalogImageUrl ?? undefined)`. Per RESEARCH Open Q1, return the RAW storagePath in the cover position (Phase 61 resolves signed URLs) — keep the DAL admin-client-free. Apply the SAME cover subquery + post-map override to getWatchById (add a catalog leftJoin for its fallback, since it currently has none) and getWatchByIdForViewer (add cover subquery + catalog leftJoin, override imageUrl on the returned watch). Add to src/lib/types.ts: `export interface WatchPhoto { id: string; watchId: string; storagePath: string; sortOrder: number; createdAt: string }`. Leave `Watch.imageUrl?: string` exactly as-is (D-08).
  </action>
  <verify>
    <automated>grep -q "coverStoragePath" src/data/watches.ts && grep -q "interface WatchPhoto" src/lib/types.ts && ! grep -q "imageUrl: row.imageUrl" src/data/watches.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/data/watches.ts imports `watchPhotos` from `@/db/schema`
    - src/data/watches.ts contains a `watch_photos` cover subquery ordering by `sort_order ASC LIMIT 1` in getWatchesByUser, getWatchById, and getWatchByIdForViewer
    - mapRowToWatch no longer references `row.imageUrl`; mapDomainToRow no longer writes `row.imageUrl`
    - src/lib/types.ts contains `export interface WatchPhoto` and still contains `imageUrl?: string` on `Watch` (D-08 unchanged)
    - With DATABASE_URL set, the SC2 cases in tests/integration/phase60-watch-photos.test.ts pass (cover wins over catalog; catalog fallback when no photos)
  </acceptance_criteria>
  <done>All 3 read paths resolve the computed cover with the cover->catalog->undefined fallback; mappers repointed; WatchPhoto type added; Watch.imageUrl contract preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: addWatchPhoto (cap + ownership), bulkReorderPhotos, deleteWatchPhoto, error class + constant</name>
  <files>src/data/watches.ts, tests/integration/phase60-watch-photos.test.ts</files>
  <read_first>
    - src/data/watches.ts (the file being modified — OwnerMismatchError/SetMismatchError 390-410, bulkReorderWishlist 433-482, deleteWatch 320-328, getMaxWishlistSortOrder 365-376, createWatch insert+returning shape)
    - tests/integration/phase60-watch-photos.test.ts (the Wave 0 stub from Plan 01 — wire SC3 + cross-tenant cases to the real DAL)
    - .planning/phases/60-multi-photo-schema-dal/60-PATTERNS.md (Extension 2 error class 289-311; Extension 3 addWatchPhoto 313-343; Extension 4 bulkReorderPhotos 345-369)
    - .planning/phases/60-multi-photo-schema-dal/60-RESEARCH.md (Pattern 3b addWatchPhoto; Pattern 3c bulkReorderPhotos; D-12/D-13/D-14 cap semantics)
  </read_first>
  <behavior>
    - addWatchPhoto inserts a row at the next sort_order (max+1, starting 0) for an owned watch and returns it
    - addWatchPhoto throws PhotoCapExceededError when the watch already has MAX_PHOTOS_PER_WATCH (10) rows
    - addWatchPhoto throws access-denied when watchId is not owned by userId (cross-tenant)
    - bulkReorderPhotos rewrites sort_order to 0..N-1 by orderedIds; throws SetMismatchError when orderedIds is not the complete set; throws access-denied for non-owned watch
    - deleteWatchPhoto removes a single photo row for an owned watch
  </behavior>
  <action>
    In src/data/watches.ts add `export const MAX_PHOTOS_PER_WATCH = 10` (D-12 — canonical location is the DAL enforcement site; tests import from here) and `export class PhotoCapExceededError extends Error` with `constructor(public cap: number)` setting message `Photo cap reached: a watch may have at most ${cap} photos` and `this.name = 'PhotoCapExceededError'`. Implement `addWatchPhoto(userId, watchId, storagePath): Promise<typeof watchPhotos.$inferSelect>`: (1) ownership check — select watches id WHERE `id=watchId AND user_id=userId` limit 1, throw `Error('Watch not found or access denied: ...')` if absent (T-60-XTENANT); (2) cap check — `count(*)::int` of watchPhotos for watchId, throw `PhotoCapExceededError(MAX_PHOTOS_PER_WATCH)` if `>= MAX_PHOTOS_PER_WATCH` (D-13; counts watch_photos rows only per D-14); (3) next sort_order = `coalesce(max(sortOrder), -1)::int + 1`; (4) insert and `.returning()`. Implement `bulkReorderPhotos(userId, watchId, orderedIds: string[]): Promise<void>` mirroring bulkReorderWishlist: early-return on empty; ownership check via watches; set-completeness check (`count(*)` of watchPhotos for watchId must equal orderedIds.length else SetMismatchError); CASE WHEN sql.join building `when ${watchPhotos.id} = ${id} then ${idx}::int4`; UPDATE watchPhotos SET sortOrder=caseExpr WHERE `eq(watchPhotos.watchId, watchId) AND inArray(watchPhotos.id, orderedIds)` `.returning({ id })`; throw OwnerMismatchError if updated.length !== orderedIds.length. Implement `deleteWatchPhoto(userId, watchId, photoId): Promise<void>`: ownership check via watches, then delete watchPhotos WHERE `id=photoId AND watch_id=watchId` `.returning()`, throw if none deleted.
    Then complete the SC3 + cross-tenant cases in tests/integration/phase60-watch-photos.test.ts (Plan 01 stub) against these real functions: insert 10 photos then expect PhotoCapExceededError on the 11th; expect addWatchPhoto with a foreign userId to throw access-denied.
  </action>
  <verify>
    <automated>grep -q "export const MAX_PHOTOS_PER_WATCH = 10" src/data/watches.ts && grep -q "export async function addWatchPhoto" src/data/watches.ts && grep -q "export async function bulkReorderPhotos" src/data/watches.ts && grep -q "PhotoCapExceededError" src/data/watches.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/data/watches.ts contains `export const MAX_PHOTOS_PER_WATCH = 10` and `export class PhotoCapExceededError`
    - addWatchPhoto does an ownership check (`watches.userId = userId AND watches.id = watchId`) before insert
    - addWatchPhoto cap check counts `watchPhotos` rows only (not any unioned source — D-14) and throws PhotoCapExceededError at `>= MAX_PHOTOS_PER_WATCH`
    - bulkReorderPhotos throws SetMismatchError on incomplete set and OwnerMismatchError on row-count mismatch
    - With DATABASE_URL set, `npx vitest run tests/integration/phase60-watch-photos.test.ts` passes the SC3 (cap) and cross-tenant cases
  </acceptance_criteria>
  <done>Cap-enforced addWatchPhoto, set-complete bulkReorderPhotos, and deleteWatchPhoto implemented with ownership checks; SC3 + cross-tenant integration cases green.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint all direct watches.image_url read sites + add purgeWatchPhotos storage hook</name>
  <files>src/app/actions/comments.ts, src/app/actions/wishlist.ts, src/data/wearEvents.ts, src/data/search.ts, src/app/actions/account.ts, src/app/actions/watches.ts</files>
  <read_first>
    - src/app/actions/comments.ts (lines 76-115 — both `imageUrl: watches.imageUrl` SELECT sites for watch + wear targets)
    - src/app/actions/wishlist.ts (lines 67-80 — `imageUrl: watches.imageUrl`)
    - src/data/wearEvents.ts (lines 258-270, 340-346, 462-470, 514-522 — four `watches.imageUrl` SELECT sites)
    - src/data/search.ts (lines 225-310 raw SQL `w.image_url`; line 146 `s.viewerWatch.imageUrl`)
    - src/app/actions/account.ts (purgeWearPhotos 22-51; wipeCollection 71-110)
    - src/app/actions/watches.ts (removeWatch 467-498)
    - .planning/phases/60-multi-photo-schema-dal/60-PATTERNS.md (purgeWatchPhotos DB-first approach lines 444-485)
    - .planning/phases/60-multi-photo-schema-dal/60-RESEARCH.md (Pattern 3d better purge approach lines 551-562; Pitfall 6)
  </read_first>
  <action>
    REPOINT every direct `watches.image_url` SELECT so the dropped column does not break the query at runtime. Fix pattern: replace `imageUrl: watches.imageUrl` (and `watchImageUrl: watches.imageUrl`) in each Drizzle `.select()` with a cover subquery `sql<string | null>` selecting `wp.storage_path FROM watch_photos wp WHERE wp.watch_id = watches.id ORDER BY wp.sort_order ASC LIMIT 1` — preserve the original alias key (`imageUrl` or `watchImageUrl`). Sites: src/app/actions/comments.ts (watch-target select ~line 82 AND wear-target parent-watch select ~line 107), src/app/actions/wishlist.ts (~line 73), src/data/wearEvents.ts (all four: ~267, ~344, ~466, ~518). In src/data/search.ts raw SQL: replace `w.image_url` in the `WITH matched` CTE select with a correlated subquery `(SELECT wp.storage_path FROM watch_photos wp WHERE wp.watch_id = w.id ORDER BY wp.sort_order ASC LIMIT 1) AS image_url`; the downstream `'image_url', m.image_url` in jsonb_build_object references the CTE alias and stays unchanged. For src/data/search.ts:146 `s.viewerWatch.imageUrl` — this reads the Watch domain type populated by Task 1's cover override, so it keeps working; confirm and leave as-is.
    Add `purgeWatchPhotos` to src/app/actions/account.ts (DB-first per RESEARCH Pattern 3d — query watchPhotos storagePaths for the watchId, then `supabase.storage.from('watch-photos').remove(paths)`; early-return when no photos; mirror purgeWearPhotos error-throw style). Add `import { watchPhotos } from '@/db/schema'` and `eq` if not present. Export it (so removeWatch can call it). Add a `watch-photos` sweep to wipeCollection alongside purgeWearPhotos using the same paginated `list(userId)` + `remove` loop over the `watch-photos` bucket.
    In src/app/actions/watches.ts removeWatch: create the session-scoped supabase client (`await createSupabaseServerClient()`) and call `await purgeWatchPhotos(supabase, watchId)` BEFORE `await watchDAL.deleteWatch(user.id, watchId)` (T-60-ORPHAN — purge storage before DB delete; cascade handles the watch_photos rows). Import purgeWatchPhotos + createSupabaseServerClient as needed.
    Confirm (discretion call #3): the activities feed renderers (src/components/home/ActivityRow.tsx, AggregatedActivityRow.tsx) read `metadata.imageUrl` from stored jsonb via getSafeImageUrl and do NOT join `watches` — display-only and benign after the column drop. Make NO code change there; record the confirmation in the SUMMARY.
  </action>
  <verify>
    <automated>! grep -nE "imageUrl: watches\.imageUrl|watchImageUrl: watches\.imageUrl" src/app/actions/comments.ts src/app/actions/wishlist.ts src/data/wearEvents.ts && ! grep -nE "\bw\.image_url\b" src/data/search.ts && grep -q "purgeWatchPhotos" src/app/actions/account.ts && grep -q "purgeWatchPhotos" src/app/actions/watches.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - No file in {comments.ts, wishlist.ts, wearEvents.ts} still contains `imageUrl: watches.imageUrl` or `watchImageUrl: watches.imageUrl`
    - src/data/search.ts no longer contains a bare `w.image_url` reference (replaced by the cover subquery alias)
    - src/app/actions/account.ts contains `purgeWatchPhotos` querying `watchPhotos` storagePaths then `from('watch-photos').remove`
    - src/app/actions/watches.ts removeWatch calls `purgeWatchPhotos` BEFORE `deleteWatch` (purge-before-delete ordering: `awk '/purgeWatchPhotos/{p=NR} /deleteWatch/{d=NR} END{exit !(p<d)}'` on watches.ts exits 0)
    - `npx tsc --noEmit` reports no new errors from these files (the column-drop blast radius is fully repointed)
  </acceptance_criteria>
  <done>All direct watches.image_url read sites repointed to the cover subquery; purgeWatchPhotos added and called before deleteWatch; activities renderers confirmed benign.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action / DAL caller -> watch_photos write | DAL is the sole write path; ownership confirmed before every mutation |
| removeWatch -> watch-photos storage objects | Orphan prevention requires purge-before-delete |
| direct Drizzle column reads -> dropped image_url column | Repoint prevents runtime query failure post-drop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-XTENANT | Spoofing | addWatchPhoto / bulkReorderPhotos / deleteWatchPhoto | mitigate | Ownership check `SELECT id FROM watches WHERE id=watchId AND user_id=userId` before any mutation; DAL is the sole write path (D-13) |
| T-60-CAP | Tampering | addWatchPhoto cap bypass | mitigate | `count(*)` of watch_photos rows before insert; rejects at MAX_PHOTOS_PER_WATCH; counts watch_photos only (D-14) |
| T-60-REORDER | Tampering | partial-set reorder leaving stale sort_order | mitigate | SetMismatchError set-completeness check + OwnerMismatchError row-count check (Phase 27 BR-01 analog) |
| T-60-ORPHAN | Denial of Service (storage growth) | removeWatch leaving orphaned storage objects | mitigate | purgeWatchPhotos (DB-first: query storagePaths, then storage.remove) runs BEFORE deleteWatch; cascade handles DB rows |
| T-60-DROPBREAK | Denial of Service (runtime query failure) | direct watches.image_url SELECT sites | mitigate | All direct-column SELECTs repointed to the cover subquery; tsc --noEmit confirms no dangling column refs |
</threat_model>

<verification>
- All task grep gates pass.
- `npx tsc --noEmit` reports no new errors (full column-drop blast radius repointed).
- With DATABASE_URL set, `npx vitest run tests/integration/phase60-watch-photos.test.ts` passes ALL cases (SC1/SC2/SC3/cross-tenant).
- removeWatch purges storage before deleting the watch.
</verification>

<success_criteria>
- Cover computed at read across all 3 paths with cover->catalog->undefined fallback (PHOTO-04 / SC2 / D-04 / D-05 / D-06).
- DAL enforces the 10-photo cap and rejects overflow (PHOTO-07 / SC3 / D-12 / D-13 / D-14).
- Watch.imageUrl TS contract preserved; no reader churn (D-08).
- All direct image_url query sites repointed; no runtime breakage from the drop.
- Storage orphan prevention via purge-before-delete.
</success_criteria>

<output>
After completion, create `.planning/phases/60-multi-photo-schema-dal/60-03-SUMMARY.md`.
</output>
