---
phase: 65-follow-scoped-owners-module
plan: 03
type: execute
wave: 2
depends_on:
  - 65-01
  - 65-02
files_modified:
  - src/app/w/[ref]/page.tsx
  - src/components/watch/WatchDetailHero.tsx
  - tests/static/watch-detail-ia-order.test.ts
autonomous: false
requirements:
  - FOLL-01
  - FOLL-02
  - FOLL-03
  - FOLL-04
tags:
  - integration
  - ppr-safe
  - hero
  - static-guard

user_setup: []

must_haves:
  truths:
    - "D-01 / FOLL-01: getFollowedOwnersForCatalog is invoked on ALL THREE render branches of src/app/w/[ref]/page.tsx — Branch 1 (perUserResult lines ~171), the Branch 2 owned sub-branch (viewerOwnedRow lines ~497-501), and the Branch 2 pure-catalog continuation (lines ~423-434)."
    - "D-01a / Pitfall 6: On Branch 1 only, the call is guarded by `watch.catalogId ? getFollowedOwnersForCatalog(...) : Promise.resolve({ owners: [], totalCount: 0 })` — the ternary mirrors the existing `sameFamily` / `lineage` / catalog-entry guards at page.tsx lines 312, 328-329. Branches 2 and the owned sub-branch use catalogId-as-ref directly (with the owned sub-branch using `ownedWatch.catalogId ? ... : Promise.resolve({...})` per the existing ownedSameFamily / ownedLineage pattern at lines 512-513)."
    - "FOLL-04 / D-09: Each branch's DAL call lives INSIDE its existing Promise.all block (not awaited serially), so the new fetch parallelizes with the existing collection / preferences / verdict / roster fetches — no new waterfall."
    - "D-10 / FOLL-01: WatchDetailHero renders <FollowedOwnersModule owners={followedOwners ?? []} totalCount={followedOwnersTotal ?? 0} /> as a sibling INSIDE the right column container (`<div className=\"space-y-6 min-w-0\">`), positioned AFTER the LikeButton + jump-to-comments block (current line ~298) and BEFORE the Last-worn line (current line ~301)."
    - "D-10 / D-11 B1 sibling-composition: WatchDetailHero.tsx adds `import type { FollowedOwner } from '@/data/follows'` (type-only). It does NOT import `getFollowedOwnersForCatalog` (the function); the page.tsx pre-resolves and passes data via props. The existing static guard 'WatchDetailHero.tsx has no import of CommentThread' is paralleled by a new assertion that WatchDetailHero does NOT import getFollowedOwnersForCatalog."
    - "WatchDetailHeroProps extension: two new optional props `followedOwners?: FollowedOwner[]` and `followedOwnersTotal?: number` are added to the interface (line ~48-102) AND destructured in the component signature (line ~104-122). Backward compat: both default to safe empty values at the render site via `?? []` / `?? 0`."
    - "PAGE-03 preservation / Pitfall 5 / project_ppr_dynamic_before_use_cache: This plan DOES NOT modify line 50 (`export const unstable_instant = false`), line 96 (`await connection()`), the outer-sync default export, or the local <Suspense fallback={<WatchPageSkeleton />}> shape. All new code lives INSIDE UnifiedWatchContent or its callees, below `await connection()`."
    - "Plan 03 threads the two new props into ALL THREE existing `<WatchDetailHero ...>` call sites: Branch 1 at lines ~337-355, the Branch 2 owned sub-branch at lines ~581-599. (Branch 2's pure-catalog continuation does NOT render <WatchDetailHero>; it renders the catalog hero shell inline at lines 684-714, so the module is rendered DIRECTLY in page.tsx on that branch as a sibling, NOT through the hero.)"
    - "D-03 / D-03a: The existing <OtherOwnersRoster> at page.tsx line 737 is UNTOUCHED — same position, same props. Branch 3 now has TWO rosters (hero-column 'From your circle' AND below-hero 'X collectors own this') by design."
    - "D-03b: On Branches 1 and 2, only the new FollowedOwnersModule is added — <OtherOwnersRoster> is NOT introduced where it does not currently render."
    - "Static guard extension: tests/static/watch-detail-ia-order.test.ts adds (a) an active-line assertion that WatchDetailHero.tsx does NOT import getFollowedOwnersForCatalog AND (b) an active-line assertion that each Promise.all block in page.tsx that previously contained getCollectorsForCatalog OR getWatchesByUser now also contains getFollowedOwnersForCatalog."
  artifacts:
    - path: "src/app/w/[ref]/page.tsx"
      provides: "Three pre-fetch sites for getFollowedOwnersForCatalog + three Hero call-site prop additions + a Branch-3 direct render of FollowedOwnersModule in the catalog hero shell"
      contains: "getFollowedOwnersForCatalog"
    - path: "src/components/watch/WatchDetailHero.tsx"
      provides: "Two new optional props on WatchDetailHeroProps + a render call of <FollowedOwnersModule/> as the next sibling after the LikeButton+jump row in the right column"
      contains: "FollowedOwnersModule"
    - path: "tests/static/watch-detail-ia-order.test.ts"
      provides: "Two new structural guards covering B1 sibling composition + Promise.all integration for the Phase 65 DAL"
      contains: "getFollowedOwnersForCatalog"
  key_links:
    - from: "src/app/w/[ref]/page.tsx"
      to: "src/data/follows.getFollowedOwnersForCatalog"
      via: "named import + Promise.all entries in 3 branches"
      pattern: "getFollowedOwnersForCatalog\\("
    - from: "src/components/watch/WatchDetailHero.tsx"
      to: "src/components/insights/FollowedOwnersModule"
      via: "named import + JSX render between LikeButton/jump block and Last-Worn line"
      pattern: "<FollowedOwnersModule"
    - from: "src/components/watch/WatchDetailHero.tsx"
      to: "src/data/follows (FollowedOwner type)"
      via: "import type — never the function"
      pattern: "import type \\{ FollowedOwner \\} from '@/data/follows'"
---

<objective>
Wire Plan 01's DAL and Plan 02's component into the live `/w/[ref]` route. This plan extends `src/app/w/[ref]/page.tsx` to pre-fetch `getFollowedOwnersForCatalog` inside each of the three branch-level `Promise.all` blocks (Branch 1 guarded by `watch.catalogId`, Branch 2 owned sub-branch guarded by `ownedWatch.catalogId`, Branch 2 pure-catalog using `ref` as catalogId), threads the resolved `{ owners, totalCount }` into the two existing `<WatchDetailHero>` call sites as new props, renders the module directly in the Branch 2 pure-catalog hero shell where there is no `<WatchDetailHero>`, extends `WatchDetailHero.tsx` with the two new optional props + the JSX render between the LikeButton/jump block and the Last-Worn line, and adds two structural assertions to `tests/static/watch-detail-ia-order.test.ts` that lock the B1 sibling-composition invariant (`WatchDetailHero` must not import the DAL function) and the parallel-Promise.all invariant.

Purpose: Phase 65 ships fully integrated and visible on prod when this plan completes. All four requirements (FOLL-01..04) are realized end-to-end, the Phase 51/52/61 PPR contract is preserved (no touching of `unstable_instant`, `await connection()`, or the outer Suspense shape — Pitfall 5 + project_ppr_dynamic_before_use_cache), and a prod human-verify checkpoint at the end confirms desktop placement + mobile single-column collapse + soft-nav PPR safety after Vercel cache fills.

Output: One commit-worth of integration edits + extended static guards + a checkpoint that closes the loop with prod verification (per feedback_mobile_ui_verify_on_prod — mobile/visual behavior verifies on prod, not locally).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-follow-scoped-owners-module/65-CONTEXT.md
@.planning/phases/65-follow-scoped-owners-module/65-RESEARCH.md
@.planning/phases/65-follow-scoped-owners-module/65-PATTERNS.md
@.planning/phases/65-follow-scoped-owners-module/65-UI-SPEC.md
@.planning/phases/65-follow-scoped-owners-module/65-01-SUMMARY.md
@.planning/phases/65-follow-scoped-owners-module/65-02-SUMMARY.md

<interfaces>
<!-- Resolved by Plan 01 + Plan 02; Plan 03 consumes -->

From src/data/follows.ts (Plan 01):
```typescript
export interface FollowedOwner {
  userId: string
  username: string
  displayName: string | null
  avatarUrl: string | null
}
export async function getFollowedOwnersForCatalog(
  catalogId: string,
  viewerId: string,
  opts: { limit?: number } = {},
): Promise<{ owners: FollowedOwner[]; totalCount: number }>
```

From src/components/insights/FollowedOwnersModule.tsx (Plan 02):
```typescript
import type { FollowedOwner } from '@/data/follows'
interface FollowedOwnersModuleProps {
  owners: FollowedOwner[]
  totalCount: number
}
export function FollowedOwnersModule(props: FollowedOwnersModuleProps): JSX.Element | null
```

From src/app/w/[ref]/page.tsx (CURRENT integration anchors that Plan 03 modifies):
- Line 50: `export const unstable_instant = false` — DO NOT TOUCH.
- Line 96: `await connection()` — DO NOT TOUCH.
- Lines 97-101: outer-sync default export with `<Suspense fallback={<WatchPageSkeleton/>}>` — DO NOT TOUCH.
- Branch 1 Promise.all at lines 171-174: `const [collection, preferences] = await Promise.all([...])` — EXTEND to a 3-tuple with the new DAL call, guarded by `watch.catalogId` ternary.
- Branch 1 hero call at lines 337-355: `<WatchDetailHero ...>` — APPEND two new props.
- Branch 2 (catalog) Promise.all at lines 423-434: `[catalogEntry, collection, preferences, viewerOwnedRow, viewerProfile, roster, sameFamily, lineage]` (8-tuple) — EXTEND to 9-tuple with `getFollowedOwnersForCatalog(ref, user.id, { limit: 5 })`.
- Branch 2 owned sub-branch (D-06 in-place owned) at lines 497-513: existing per-watch Promise.all chains — ADD a guarded DAL call alongside ownedSameFamily/ownedLineage following the same `ownedWatch.catalogId ? ... : Promise.resolve({...})` pattern.
- Branch 2 owned hero call at lines 581-599: `<WatchDetailHero ...>` — APPEND two new props.
- Branch 2 pure-catalog hero shell at lines 684-714: NO `<WatchDetailHero>` is rendered on this branch (catalog has no like button, no commentCount, etc.). The module is rendered DIRECTLY as a sibling in page.tsx around line 737 — visually beside / above the existing <OtherOwnersRoster>. (Decide placement: the new module is hero-right-column equivalent; on this branch, place it ABOVE the existing OtherOwnersRoster as another sibling block.)
- Existing untouched: <OtherOwnersRoster collectors={roster.collectors} totalCount={roster.totalCount} /> at line 737 (D-03a).

From src/components/watch/WatchDetailHero.tsx (CURRENT anchors that Plan 03 modifies):
- Line 1: `'use client'` — DO NOT TOUCH (the hero stays a client island; B1 sibling composition).
- Lines 48-102: `interface WatchDetailHeroProps` — APPEND `followedOwners?: FollowedOwner[]` and `followedOwnersTotal?: number` as new optional fields.
- Lines 104-122: function signature destructuring — APPEND the two new field names.
- Line 230: `<div className="space-y-6 min-w-0">` (right column container) — INSERT POINT for the new sibling.
- Lines 275-298: LikeButton + jump-to-comments block — the new <FollowedOwnersModule/> renders AFTER its closing `)}` on line 298.
- Line 300: `{/* Last worn line ... */}` — the new render sits BEFORE this block.

From tests/static/watch-detail-ia-order.test.ts (existing — Plan 03 EXTENDS):
- The `activeLineNumbers(lines, pattern)` helper at lines 26-42 ignores comments and imports and returns 1-indexed line numbers — REUSE for the new assertions.
- The "PAGE-03: WatchDetailHero does not import CommentThread" block at lines 74-90 is the template for the new "WatchDetailHero does not import getFollowedOwnersForCatalog" assertion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend WatchDetailHero with FollowedOwnersModule sibling + 2 new props</name>
  <files>src/components/watch/WatchDetailHero.tsx</files>
  <read_first>
    - src/components/watch/WatchDetailHero.tsx ENTIRE file (~360 lines — confirm the exact line numbers of: WatchDetailHeroProps interface end, function signature destructuring, right-column container `<div className="space-y-6 min-w-0">`, LikeButton + jump-to-comments block closing `)}` , the comment `{/* Last worn line ... */}`, and the existing `import type` style precedent for VerdictBundle on line 30)
    - src/components/insights/FollowedOwnersModule.tsx (Plan 02 — confirm exact named export, props interface, and that it is RSC-safe to import from a 'use client' file because it is referenced ONLY for JSX rendering; Next.js handles RSC-in-CC composition via the server-to-client boundary — but the hero is itself a client component, so the import is treated as a client-side reference to a server component, which Next.js renders on the server and serializes; this is the same pattern as importing the AvatarDisplay primitive, which is also RSC. The component import is SAFE.)
    - .planning/phases/65-follow-scoped-owners-module/65-CONTEXT.md (D-10, D-11)
    - .planning/phases/65-follow-scoped-owners-module/65-PATTERNS.md ("src/components/watch/WatchDetailHero.tsx" section — confirms props extension + insertion-point shape; lines 379-465)
    - .planning/phases/65-follow-scoped-owners-module/65-RESEARCH.md (Anti-Patterns + Code Examples 514-543 + Pattern 2)
  </read_first>
  <behavior>
    - On render with `followedOwners = []` AND `followedOwnersTotal = 0`: the JSX subtree for the new module emits `null` (delegated to FollowedOwnersModule's own hide-if-empty). The right column's `space-y-6` rhythm collapses cleanly (LikeButton/jump row → 24px gap → Last-Worn line).
    - On render with `followedOwners = [3 owners]` AND `followedOwnersTotal = 7`: the new module renders between the LikeButton/jump block and the Last-Worn block, showing the "From your circle" header + 3 chips + "and 4 more" caption.
    - On render with `followedOwners` / `followedOwnersTotal` UNDEFINED (backward compat — callers from before Plan 03 that have not been updated): the render uses `?? []` and `?? 0` defaults; the module returns null because owners.length === 0 is honored upstream by the component.
    - Owner-only blocks (Last-Worn, Flag-as-good-deal, Action buttons) continue to gate on `viewerCanEdit` exactly as today — the new module sits between the always-visible LikeButton+jump block and those owner-only blocks, so a non-owner viewer sees `title → spec strip → like+jump → "From your circle" → (nothing else)` and an owner viewer sees `title → spec strip → like+jump → "From your circle" → Last-Worn → Flag → Actions`.
  </behavior>
  <action>
    Modify `src/components/watch/WatchDetailHero.tsx` in exactly four places. DO NOT touch line 1 (`'use client'`), the existing prop names, or any existing JSX block.

    1. ADD imports (top of file, alongside existing imports):
       - `import { FollowedOwnersModule } from '@/components/insights/FollowedOwnersModule'`
       - `import type { FollowedOwner } from '@/data/follows'` (type-only — D-11; precedent: existing `import type { VerdictBundle } from '@/lib/verdict/types'` on line 30)

    2. EXTEND `interface WatchDetailHeroProps` (current lines 48-102) by appending — INSIDE the existing interface block, AFTER the `viewerIsFollowingForWears?: boolean` field on line 101, BEFORE the closing `}` on line 102 — two new optional fields with a JSDoc block per the project's existing convention. New fields:
       - `followedOwners?: FollowedOwner[]` with JSDoc: "Phase 65 FOLL-01..04. Pre-resolved by page.tsx via getFollowedOwnersForCatalog (or [] when watch.catalogId is null on Branch 1). Hide-if-empty is enforced inside <FollowedOwnersModule/>."
       - `followedOwnersTotal?: number` with JSDoc: "Phase 65 FOLL-04. count(DISTINCT) total from the DAL for the '+N more' caption."

    3. EXTEND the function destructuring (current lines 104-122) by appending the two new field names to the destructuring list — AFTER the existing `viewerIsFollowingForWears` on line 121. Adding two more parameters does not change any existing behavior.

    4. INSERT the new render INSIDE the right column container (current `<div className="space-y-6 min-w-0">` opening on line 230) at the position BETWEEN the LikeButton + jump-to-comments block's closing `)}` (current line 298) and the `{/* Last worn line ... */}` comment (current line 300). The new render is a single JSX element wrapped in a clarifying JSDoc-style comment:
       - Comment line: `{/* Phase 65 FOLL-01..04 — "From your circle". Hide-if-empty is inside the component (returns null on owners.length === 0). Sibling-composition: the DAL is server-side and pre-resolved by page.tsx; this client island only consumes the data via props (B1 invariant). */}`
       - Render: `<FollowedOwnersModule owners={followedOwners ?? []} totalCount={followedOwnersTotal ?? 0} />`

    DO NOT add any conditional gate on `viewerCanEdit` or `viewerId` for this render — the component's own hide-if-empty plus the page.tsx call-site guards (Branch 1 watch.catalogId ternary; auth gate at getCurrentUser) cover the empty / unauthorized cases.

    DO NOT add a `<Suspense>` wrapper around the new render (D-09 — only the page-level Promise.all matters; a Suspense inside the client island would corrupt the PPR boundary per RESEARCH Anti-Patterns).

    DO NOT change the spacing or position of any existing JSX block. The new render is a strict ADDITIVE sibling.

    The existing JSDoc on line 69 ("WatchDetailHero B1 invariant (PAGE-03): CommentThread is NOT referenced in this file") is a precedent for prose comments in this file. Do not introduce any text in your new comments that matches `/import.*getFollowedOwnersForCatalog/` (the new static guard in Task 3 will check active-code lines, but defensive: avoid the substring in JSDoc to keep the guard regression-safe — see STATE.md `WatchDetailTrailing comment reworded to avoid grep false-positive`).
  </action>
  <verify>
    <automated>npx vitest run tests/static/watch-detail-ia-order.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "^import { FollowedOwnersModule } from '@/components/insights/FollowedOwnersModule'" src/components/watch/WatchDetailHero.tsx` returns 1.
    - Source assertion: `grep -c "^import type { FollowedOwner } from '@/data/follows'" src/components/watch/WatchDetailHero.tsx` returns 1.
    - Source assertion: NO function-import of the DAL: `grep -c "import.*getFollowedOwnersForCatalog" src/components/watch/WatchDetailHero.tsx` returns 0.
    - Source assertion: `grep -c "followedOwners\\?:" src/components/watch/WatchDetailHero.tsx` returns 1 (the interface field).
    - Source assertion: `grep -c "followedOwnersTotal\\?:" src/components/watch/WatchDetailHero.tsx` returns 1 (the interface field).
    - Source assertion: the function destructuring contains both new field names: `grep -cE "^\s+followedOwners,?\s*$" src/components/watch/WatchDetailHero.tsx` ≥ 1 AND `grep -cE "^\s+followedOwnersTotal,?\s*$" src/components/watch/WatchDetailHero.tsx` ≥ 1.
    - Source assertion: `grep -c "<FollowedOwnersModule" src/components/watch/WatchDetailHero.tsx` returns 1.
    - Source assertion: the render line uses `?? []` and `?? 0` safe defaults: `grep -cE "owners=\\{followedOwners \\?\\? \\[\\]\\}" src/components/watch/WatchDetailHero.tsx` ≥ 1 AND `grep -cE "totalCount=\\{followedOwnersTotal \\?\\? 0\\}" src/components/watch/WatchDetailHero.tsx` ≥ 1.
    - Source assertion: the render sits BETWEEN the existing LikeButton block and the existing Last-Worn comment. Using the existing `activeLineNumbers` helper conceptually: the new `<FollowedOwnersModule` active-code line number is GREATER than the line of `<LikeButton` AND LESS than the line of `Last worn line`. Verify manually OR rely on Task 3's structural test once it lands.
    - Build: `npm run build` exits 0.
    - TypeScript: `npx tsc --noEmit` produces no NEW errors mentioning `WatchDetailHero.tsx`.
    - Pre-existing static guard `tests/static/watch-detail-ia-order.test.ts` still PASSES: `npx vitest run tests/static/watch-detail-ia-order.test.ts` returns 0 failures (the existing PAGE-01/02/03/04 + mobile-header assertions must not regress).
  </acceptance_criteria>
  <done>
    `src/components/watch/WatchDetailHero.tsx` adds the named-component import + the type-only DAL-type import, extends the props interface with two new optional fields, destructures them in the function signature, and renders `<FollowedOwnersModule>` between the LikeButton/jump block and the Last-Worn line with safe `?? []` / `?? 0` defaults. No other line of the file is touched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire 3 pre-fetch sites + 2 hero prop passes + 1 direct render into src/app/w/[ref]/page.tsx</name>
  <files>src/app/w/[ref]/page.tsx</files>
  <read_first>
    - src/app/w/[ref]/page.tsx lines 1-50 (confirm imports — add the new import alongside existing src/data/* imports), 96-102 (confirm `await connection()` + Suspense shape — DO NOT TOUCH), 143-174 (Branch 1 setup + Promise.all to EXTEND), 200-355 (Branch 1 hero call site at 337-355 to EXTEND with 2 props), 423-434 (Branch 2 Promise.all to EXTEND), 497-513 (Branch 2 owned sub-branch ownedSameFamily/ownedLineage pattern to MIRROR), 581-599 (Branch 2 owned hero call site to EXTEND with 2 props), 684-755 (Branch 2 pure-catalog inline shell — INSERT a direct <FollowedOwnersModule> render ABOVE the existing <OtherOwnersRoster> on line 737)
    - src/data/follows.ts (Plan 01 — confirm named export `getFollowedOwnersForCatalog`)
    - src/components/insights/FollowedOwnersModule.tsx (Plan 02 — confirm named export)
    - .planning/phases/65-follow-scoped-owners-module/65-CONTEXT.md (D-01, D-01a, D-03, D-03a, D-03b, D-09, D-10)
    - .planning/phases/65-follow-scoped-owners-module/65-RESEARCH.md (Pattern 2 lines 318-342 + Code Examples lines 496-512 + Pitfalls 5 + 6 — the page.tsx integration code samples; Pitfall 5 lists the EXACT three permitted changes: 1 import, 1 line per Promise.all, 2 props per hero call site — anything else is out of scope)
    - .planning/phases/65-follow-scoped-owners-module/65-PATTERNS.md ("src/app/w/[ref]/page.tsx" section — confirms the verbatim Branch 1/2/owned shape and the Branch 3 catalog-hero direct render)
    - MEMORY project_ppr_dynamic_before_use_cache (Phase 51/52/61 — `await connection()` ABOVE Suspense is PERMANENT; do NOT touch ordering of dynamic API + 'use cache' calls; the new DAL is NOT 'use cache' and is NOT a cookies()-reading client — safe to drop into the existing Promise.all blocks)
    - MEMORY project_baseline_not_green_build_is_gate (npm run build exit 0 is the authoritative gate; full tsc has pre-existing test-file errors)
  </read_first>
  <behavior>
    - Branch 1 (per-user watch): When `watch.catalogId` is non-null, the Promise.all on lines ~171-174 resolves a third tuple element with the DAL result; when null, the third element is `{ owners: [], totalCount: 0 }`. The `<WatchDetailHero>` call passes `followedOwners={followedOwners.owners}` and `followedOwnersTotal={followedOwners.totalCount}`.
    - Branch 2 (catalog, pure cross-user — NOT owned sub-branch): The 8-tuple Promise.all on lines ~423-434 becomes a 9-tuple with `getFollowedOwnersForCatalog(ref, user.id, { limit: 5 })` as the 9th element. On the pure-catalog return path (lines 684-755), the new `<FollowedOwnersModule owners={followedOwners.owners} totalCount={followedOwners.totalCount} />` renders as a sibling ABOVE the existing `<OtherOwnersRoster>` (line 737) — the hero-column-equivalent position on this branch.
    - Branch 2 owned sub-branch (lines 456+ — viewer already owns a watch matching this catalog): the DAL call is added alongside `ownedSameFamily` and `ownedLineage` at lines 512-513, gated by `ownedWatch.catalogId ? getFollowedOwnersForCatalog(...) : Promise.resolve({...})`. The `<WatchDetailHero>` call site at lines 581-599 passes the two new props.
    - PPR safety: `npm run build` exits 0; the pre-existing `tests/static/ppr-dynamic-before-use-cache.test.ts` continues to pass (the new DAL is NOT a `createSupabaseAdminClient` call and NOT a `'use cache'` helper, so the ordering invariant the existing guard checks is unaffected; the lookahead distance between `createSupabaseAdminClient` and `getLikesForTargetCached` may shift by a handful of lines but stays within MAX_LOOKAHEAD = 70).
    - Soft-nav: a soft-nav from a grid card to `/w/[ref]` continues to render without React #419 or 404 once cache fills on prod (verified by the human-verify checkpoint in Task 4).
    - Existing `<OtherOwnersRoster>` at line 737 is UNTOUCHED in props and position (D-03a).
  </behavior>
  <action>
    Modify `src/app/w/[ref]/page.tsx` in exactly six places. The total diff is small: 1 import, 3 Promise.all extensions, 2 `<WatchDetailHero>` prop additions, 1 direct `<FollowedOwnersModule>` render in the catalog inline-hero branch. DO NOT touch lines 50, 96, or the outer Suspense shape (Pitfall 5 + project_ppr_dynamic_before_use_cache).

    1. ADD imports near the existing src/data/* import block (around line 14 / line 30):
       - `import { getFollowedOwnersForCatalog } from '@/data/follows'` (named function — page.tsx is RSC, no boundary concern; the existing `import { isFollowing } from '@/data/follows'` on line 14 is the precedent).
       - `import { FollowedOwnersModule } from '@/components/insights/FollowedOwnersModule'` (named component — needed for the Branch 2 pure-catalog direct render).

    2. EXTEND Branch 1 Promise.all (current lines 171-174). Replace the 2-tuple destructure with a 3-tuple per RESEARCH Pattern 2:
       - Old shape: `const [collection, preferences] = await Promise.all([ getWatchesByUser(user.id), getPreferencesByUser(user.id) ])`
       - New shape: `const [collection, preferences, followedOwners] = await Promise.all([ getWatchesByUser(user.id), getPreferencesByUser(user.id), watch.catalogId ? getFollowedOwnersForCatalog(watch.catalogId, user.id, { limit: 5 }) : Promise.resolve({ owners: [], totalCount: 0 }) ])`
       - The ternary pattern is verbatim from page.tsx line 312 (`watch.catalogId ? getCatalogById(watch.catalogId) : Promise.resolve(null)`) — same shape, same null-handling convention (D-01a + Pitfall 6).

    3. EXTEND Branch 1 hero call (current lines 337-355). Append two new props AFTER the existing `viewerIsFollowingForWears={viewerIsFollowing}` prop on line 354, BEFORE the closing `/>` on line 355:
       - `followedOwners={followedOwners.owners}`
       - `followedOwnersTotal={followedOwners.totalCount}`

    4. EXTEND Branch 2 owned sub-branch (the in-place owned render starting around line 456). Add a new const at line ~512-513 alongside `ownedSameFamily` and `ownedLineage`:
       - `const ownedFollowedOwners = ownedWatch.catalogId ? await getFollowedOwnersForCatalog(ownedWatch.catalogId, user.id, { limit: 5 }) : { owners: [], totalCount: 0 }`
       - This mirrors the existing serial-await pattern on lines 512-513 (the owned sub-branch uses serial `await` rather than Promise.all because it does the resolution after the earlier per-user Promise.all). DO NOT batch into the earlier Promise.all on lines 482-485 unless trivially safe — the existing structure on this sub-branch deliberately separates concerns.
       - Append two new props to the `<WatchDetailHero>` call site at lines 581-599 (AFTER `viewerIsFollowingForWears={false}` on line 598, BEFORE the `/>` on line 599):
         * `followedOwners={ownedFollowedOwners.owners}`
         * `followedOwnersTotal={ownedFollowedOwners.totalCount}`

    5. EXTEND Branch 2 pure-catalog Promise.all (current lines 423-434). Add `getFollowedOwnersForCatalog(ref, user.id, { limit: 5 })` as the 9th tuple element AFTER `getCollectorsForCatalog(...)` on line 431, BEFORE `getSameFamilyForCatalog(ref)` on line 432. Rename the destructure on line 423 to add `followedOwnersForCatalog` (or any unambiguous name) as the 7th tuple element. NOTE: the existing 8-tuple becomes a 9-tuple — confirm the destructure adds the new name in the SAME position the new DAL call occupies in the Promise.all array (right after `roster`, before `sameFamily`).

    6. INSERT a new `<FollowedOwnersModule>` render in the Branch 2 pure-catalog inline hero shell. The current return JSX runs from lines 684-755. The existing `<OtherOwnersRoster collectors={roster.collectors} totalCount={roster.totalCount} />` is at line 737 (under the comment "Phase 64 D-13: OtherOwnersRoster MOVED UP — social proof near the verdict"). Insert the new module ABOVE that line, AFTER the empty-collection caption block (lines 725-732) and BEFORE the `<OtherOwnersRoster>` (line 737), with a clarifying comment:
       - Comment line: `{/* Phase 65 FOLL-01..04 — "From your circle" follow-scoped roster. Renders BEFORE the broad OtherOwnersRoster (D-03a — both coexist on Branch 3 by design). Hide-if-empty inside the component. */}`
       - Render line: `<FollowedOwnersModule owners={followedOwnersForCatalog.owners} totalCount={followedOwnersForCatalog.totalCount} />`
       - (Use the exact variable name chosen in step 5 for the Branch-2 destructure.)

    DO NOT touch the existing `<OtherOwnersRoster>` line — same props, same position (D-03a).

    DO NOT add the new module to the `CatalogPageActions` block or anywhere else on Branch 2.

    DO NOT add an `<OtherOwnersRoster>` to Branches 1 or the owned sub-branch (D-03b — out of scope; the broad roster does not currently render on those branches and Phase 65 does not introduce it).

    DO NOT alter the `connection()` line (96), the `unstable_instant` line (50), or the outer Suspense shape (97-101).

    DO NOT introduce a `<Suspense>` around the new pre-fetches (D-09 — pre-fetch in Promise.all is the chosen strategy; Suspense is the fallback only if profiling shows >100ms p95 impact).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "^import { getFollowedOwnersForCatalog } from '@/data/follows'" src/app/w/\[ref\]/page.tsx` returns 1.
    - Source assertion: `grep -c "^import { FollowedOwnersModule } from '@/components/insights/FollowedOwnersModule'" src/app/w/\[ref\]/page.tsx` returns 1.
    - Source assertion (3 DAL call sites): `grep -c "getFollowedOwnersForCatalog(" src/app/w/\[ref\]/page.tsx` returns AT LEAST 3 (one per branch — Branch 1 Promise.all entry, Branch 2 Promise.all entry, Branch 2 owned sub-branch serial await).
    - Source assertion (3 hero/direct render sites): combined count `grep -c "followedOwners={" src/app/w/\[ref\]/page.tsx` + `grep -c "<FollowedOwnersModule" src/app/w/\[ref\]/page.tsx` ≥ 3 (2 hero call sites × 1 prop named "followedOwners" each, + 1 direct render on Branch 2 pure-catalog).
    - Source assertion: the Branch 1 ternary guard appears verbatim — `grep -cE "watch\\.catalogId\\s*\\?\\s*getFollowedOwnersForCatalog" src/app/w/\[ref\]/page.tsx` ≥ 1.
    - Source assertion: the Branch 2 owned sub-branch ternary guard appears — `grep -cE "ownedWatch\\.catalogId\\s*\\?\\s*(await\\s*)?getFollowedOwnersForCatalog" src/app/w/\[ref\]/page.tsx` ≥ 1.
    - Source assertion: the existing OtherOwnersRoster line is UNCHANGED — `git diff src/app/w/\[ref\]/page.tsx | grep -E "^[-+].*OtherOwnersRoster"` shows no `-` lines for the OtherOwnersRoster call (only `+` lines if any context-line additions, which is acceptable).
    - Source assertion: `await connection()` line UNCHANGED — `git diff src/app/w/\[ref\]/page.tsx | grep -E "^[-+].*await connection\\(\\)"` shows no `-` lines.
    - Source assertion: `export const unstable_instant = false` line UNCHANGED — `git diff src/app/w/\[ref\]/page.tsx | grep -E "^[-+].*unstable_instant"` shows no `-` lines.
    - Build: `npm run build` exits 0 (project_baseline_not_green_build_is_gate — authoritative gate).
    - Static guard preservation: `npx vitest run tests/static/ppr-dynamic-before-use-cache.test.ts` passes 0 failures (the existing P61-BUG-01 lookahead is ≤ 70 lines; the new DAL adds a few lines per branch but stays well under the budget).
    - Static guard preservation: `npx vitest run tests/static/watch-detail-ia-order.test.ts` passes — Branch ordering invariants (Hero before CommentThread, CommentThread before Trailing, Trailing before Rails) are unaffected.
  </acceptance_criteria>
  <done>
    `src/app/w/[ref]/page.tsx` adds exactly 1 DAL named import + 1 component named import, extends 3 fetch sites (Branch 1 Promise.all, Branch 2 owned serial await, Branch 2 pure-catalog Promise.all), threads the two new props into the 2 `<WatchDetailHero>` call sites, renders `<FollowedOwnersModule>` directly above `<OtherOwnersRoster>` on Branch 2 pure-catalog, and does not touch lines 50, 96, the outer Suspense, or the existing OtherOwnersRoster render. `npm run build` exits 0 and the two existing static guards (ppr-dynamic-before-use-cache + watch-detail-ia-order) still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests/static/watch-detail-ia-order.test.ts with 2 new Phase 65 structural assertions</name>
  <files>tests/static/watch-detail-ia-order.test.ts</files>
  <read_first>
    - tests/static/watch-detail-ia-order.test.ts ENTIRE file (line 1: `// @vitest-environment node` pragma must be preserved; lines 26-42: `activeLineNumbers` helper to REUSE; lines 47-72: existing PAGE-01/02 describe; lines 74-90: existing "PAGE-03: WatchDetailHero does not import CommentThread" describe to MIRROR for the new "does not import getFollowedOwnersForCatalog" assertion; lines 109-161: mobile-header guards to LEAVE UNTOUCHED)
    - src/app/w/[ref]/page.tsx (Task 2 output — confirm `getFollowedOwnersForCatalog(` appears in active code at least 3 times)
    - src/components/watch/WatchDetailHero.tsx (Task 1 output — confirm `getFollowedOwnersForCatalog` does NOT appear in active code as a function call OR a function import; the type-only `FollowedOwner` import is fine)
    - .planning/phases/65-follow-scoped-owners-module/65-RESEARCH.md (Validation Architecture — confirms two structural guards are the right Wave-0 gate here)
    - MEMORY project_vitest_static_node_env (the `// @vitest-environment node` pragma must remain on line 1 — the existing test already has it; do NOT move it)
  </read_first>
  <behavior>
    - New `describe('FOLL-04 (Phase 65): page.tsx invokes getFollowedOwnersForCatalog in parallel with existing DAL fetches', ...)` containing: AT LEAST ONE active-code line matching `/getFollowedOwnersForCatalog\\(/` in `src/app/w/[ref]/page.tsx`; AND the count of active-code lines matching that pattern is ≥ 3 (one per branch).
    - New `describe('B1 sibling-composition (Phase 65): WatchDetailHero.tsx does not import getFollowedOwnersForCatalog', ...)` containing: ZERO active-code lines in `src/components/watch/WatchDetailHero.tsx` matching `/import.*getFollowedOwnersForCatalog/` (the active-code filter ignores the type-only `import type { FollowedOwner }` import because the pattern looks for the FUNCTION name specifically, not the TYPE name).
    - The existing PAGE-01/02/03/04 + mobile-header describes continue to pass (no regression).
  </behavior>
  <action>
    Extend `tests/static/watch-detail-ia-order.test.ts` by appending TWO new `describe` blocks at the END of the file (after the existing mobile-header describe on line 161). DO NOT modify the existing `// @vitest-environment node` pragma on line 1, the imports, the `activeLineNumbers` helper (lines 26-42), the file-path constants (lines 44-45), or any of the existing describes (PAGE-01/02/03/04, mobile-header).

    Reuse the existing `activeLineNumbers(lines, pattern)` helper and the existing `PAGE_TSX` / `HERO_TSX` path constants. The new describes follow the same shape as `PAGE-03: WatchDetailHero does not import CommentThread` (lines 74-90).

    New describe 1 — page.tsx parallel-fetch invariant:
    ```typescript
    describe('FOLL-04 (Phase 65): page.tsx invokes getFollowedOwnersForCatalog in active code', () => {
      const content = readFileSync(PAGE_TSX, 'utf8')
      const lines = content.split('\n')

      it('getFollowedOwnersForCatalog( appears in active code at least 3 times (one per branch)', () => {
        const callLines = activeLineNumbers(lines, /getFollowedOwnersForCatalog\(/)
        expect(callLines.length, `FOLL-01..04 wiring missing — expected ≥3 active-code calls (Branch 1 + Branch 2 owned + Branch 2 pure-catalog) but found ${callLines.length}`).toBeGreaterThanOrEqual(3)
      })
    })
    ```

    New describe 2 — B1 sibling-composition invariant on the client island:
    ```typescript
    describe('B1 sibling-composition (Phase 65): WatchDetailHero.tsx does not import the DAL function', () => {
      let content: string
      try {
        content = readFileSync(HERO_TSX, 'utf8')
      } catch {
        content = ''
      }

      it('WatchDetailHero.tsx has no import of getFollowedOwnersForCatalog', () => {
        if (content === '') {
          expect.fail('WatchDetailHero.tsx not found')
        }
        // The import-aware regex catches both `import { getFollowedOwnersForCatalog }` and `import { x, getFollowedOwnersForCatalog }`.
        // It does NOT match `import type { FollowedOwner }` (the type-only import is allowed per D-11).
        expect(content).not.toMatch(/import\s*\{[^}]*getFollowedOwnersForCatalog[^}]*\}\s*from/)
      })
    })
    ```

    DO NOT add any other assertion (the existing PAGE-01/02/03/04 + mobile-header coverage + the new two assertions are sufficient — over-asserting risks false-positives from prose). DO NOT change the file's `// @vitest-environment node` pragma or import set. DO NOT modify the existing `activeLineNumbers` helper.
  </action>
  <verify>
    <automated>npx vitest run tests/static/watch-detail-ia-order.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: line 1 of the test file is still exactly `// @vitest-environment node` (preserved). Verify with `head -1 tests/static/watch-detail-ia-order.test.ts`.
    - Source assertion: the file now contains TWO additional `describe(` blocks beyond the prior content. Count `grep -c "^describe(" tests/static/watch-detail-ia-order.test.ts` — should be ≥ 6 (was 4 before: PAGE-01/02, PAGE-03 CommentThread, PAGE-04, mobile-header).
    - Source assertion: `grep -c "FOLL-04 (Phase 65)" tests/static/watch-detail-ia-order.test.ts` returns 1 (the page.tsx parallel-fetch describe).
    - Source assertion: `grep -c "B1 sibling-composition (Phase 65)" tests/static/watch-detail-ia-order.test.ts` returns 1 (the hero non-import describe).
    - Source assertion: the page.tsx assertion uses `toBeGreaterThanOrEqual(3)` against the `getFollowedOwnersForCatalog(` call count — `grep -c "toBeGreaterThanOrEqual(3)" tests/static/watch-detail-ia-order.test.ts` ≥ 1.
    - Source assertion: the hero assertion uses the import-aware regex `/import\\s*\\{[^}]*getFollowedOwnersForCatalog[^}]*\\}\\s*from/` (NOT a fuzzy `/getFollowedOwnersForCatalog/` which would false-positive on a comment that mentions the function name): `grep -c "import\\\\s\\*\\\\{\\[\\^}\\]\\*getFollowedOwnersForCatalog" tests/static/watch-detail-ia-order.test.ts` ≥ 1.
    - Behavior assertion: `npx vitest run tests/static/watch-detail-ia-order.test.ts` reports the new tests PASSING (after Tasks 1 + 2 land) AND all pre-existing tests continue to pass.
    - Behavior assertion (regression check): briefly imagine the negative path — if Task 1 added `import { getFollowedOwnersForCatalog }` to the hero by accident, the B1 sibling-composition test FAILS with a clear message. The assertion is design-level; do NOT actually break the source for this check.
  </acceptance_criteria>
  <done>
    `tests/static/watch-detail-ia-order.test.ts` retains its `// @vitest-environment node` pragma, adds two new describe blocks (FOLL-04 parallel-fetch + B1 sibling-composition), uses the existing `activeLineNumbers` helper for the page.tsx call-count assertion and an import-aware regex for the hero non-import assertion, and passes all tests under `npx vitest run`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Prod human-verify checkpoint — desktop placement + mobile collapse + soft-nav PPR safety</name>
  <files>(no files modified — this task is a prod human-verify gate on already-committed code from Tasks 1-3)</files>
  <action>
    Push origin/main → Vercel deploy → wait ~2-3 minutes for PPR cache fill → execute the 10-step <how-to-verify> checklist below. The executor opens the build/test gate (npm run build exit 0 + all 3 static guards + DAL + component tests green) BEFORE pushing — Task 4 only runs against a confirmed-green local build (per project_baseline_not_green_build_is_gate memory and feedback_mobile_ui_verify_on_prod memory: mobile/visual + soft-nav PPR safety verifies on prod, not locally; bundle into one deploy and build-gate before push).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && echo "--- ready to push origin/main; manual checks below ---"</automated>
  </verify>
  <done>
    All 10 checks in <how-to-verify> pass on prod (Vercel deploy of the latest commit). User types "approved" via <resume-signal>.
  </done>
  <what-built>
    Phase 65 ships end-to-end: the new `getFollowedOwnersForCatalog` DAL (Plan 01), the new `FollowedOwnersModule` RSC (Plan 02), and the full integration into `src/app/w/[ref]/page.tsx` + `src/components/watch/WatchDetailHero.tsx` (Plan 03 Tasks 1-3). All static guards GREEN; `npm run build` exits 0; unit + DAL tests pass locally with env. The mobile/visual + soft-nav PPR safety behaviors only verify on prod (feedback_mobile_ui_verify_on_prod memory: empty local test DB skips e2e; project_ppr_dynamic_before_use_cache memory: "Build can't confirm; verify on prod AFTER cache fills").
  </what-built>
  <how-to-verify>
    1. **Push to origin/main and wait for Vercel deploy.** Confirm the latest commit is live on prod (Vercel dashboard or `curl -sI https://<prod-url>/` shows the new build).

    2. **Wait for cache fill.** Soft-nav React #419 regressions only surface AFTER PPR cache fills (Phase 51/52/61 lesson). Wait at least 2-3 minutes after the deploy completes before testing soft-nav behavior. A cold hard refresh ALWAYS works — false negative.

    3. **Desktop placement check (Branch 1 — your own watch):**
       - Visit `https://<prod-url>/w/<your-watch-id>` (one of your owned watches that has a `catalogId` set — most catalog-matched watches).
       - Expected: in the hero right column, you see `brand+model heading → ref + spec strip → LikeButton + jump-to-comments → "From your circle" module → Last worn line → Flag-as-good-deal → Action buttons`. The "From your circle" module shows the avatar + @username + (optional displayName) chips of collectors you follow who also own this catalog ref (if any exist). If you follow no one who owns this catalog ref, the module is ENTIRELY absent (no header, no placeholder — the LikeButton/jump row sits directly above the Last-Worn line with the 24px `space-y-6` gap collapsing cleanly).
       - Verify chip click: tap one chip → navigates to `/u/<their-username>/collection`.

    4. **Desktop placement check (Branch 1 — non-owner view via mutual-friend's watch URL OR a freshly-extracted URL watch with no catalogId):**
       - If you have access to a non-owned watch URL (e.g. someone else's watch detail link), open it. Expected: the right column shows `brand+model → spec strip → like+jump → "From your circle"` and NOTHING ELSE (no Last-Worn, no Flag, no Actions — those are owner-only).
       - If your test watch has `catalogId === null` (URL-extracted watches that never matched a catalog): the module is ENTIRELY absent. This is D-01a — null catalogId → no DOM (hide-if-empty via the page-level ternary + the component's own early-return).

    5. **Branch 2 (pure-catalog) check — both rosters coexist:**
       - Visit a `/w/<catalog-uuid>` URL where you do NOT own this catalog ref AND someone you do not follow does (the existing OtherOwnersRoster will show them).
       - Expected: TWO rosters render in distinct positions — the new "From your circle" module renders near the verdict/social-proof block (above the existing `<OtherOwnersRoster>` per Task 2 step 6), AND the existing "X collectors own this" broad roster renders unchanged in its current position. Both render on Branch 3 by design (D-03 / D-03a).

    6. **Mobile single-column collapse (the load-bearing visual check):**
       - On a mobile device or Chrome devtools mobile viewport (width 375-414px), visit any `/w/[ref]` URL with at least 1 followed owner.
       - Expected: the hero's `lg:grid-cols-[3fr_2fr]` collapses to a single column. The "From your circle" module stacks naturally below the right-column content (which itself sits below the photo block on mobile). Each chip's `min-h-[44px]` tap target is finger-friendly. Long usernames/displayNames truncate with `…` ellipsis without overflowing.
       - NO `lg:hidden` / `hidden lg:block` JSX duplication: the same DOM renders at every breakpoint (responsive behavior is entirely through the parent's `lg:grid-cols` collapse).

    7. **Overflow caption check:**
       - If you have a catalog ref with >5 followed owners (rare on a personal-collector app — may need to test by seeding follows in prod), expected: "and {N} more" plain text appears under the 5 chips. If <=5: no caption.

    8. **Soft-nav PPR safety (the #419 regression check):**
       - From the home page (or any other route), soft-nav (i.e., click a `<Link>`, do NOT hard-refresh) to a `/w/[ref]` page. Expected: page renders without React #419 / 404. Test BOTH a same-user owned watch and a cross-user catalog ref.
       - Repeat 2-3 times from different originating routes (home → /w/[ref], profile → /w/[ref], notifications → /w/[ref]).
       - If you see React #419 in the browser console OR a 404 on a URL that works on hard refresh, the PPR contract was broken — STOP and report (the integration violated Pitfall 5 / project_ppr_dynamic_before_use_cache).

    9. **Owner self-exclusion check:**
       - On a watch you own where you also follow at least 1 other collector who owns this same catalog ref, verify YOU do NOT appear in your own "From your circle" chip list (D-05a self-exclusion in the DAL). Other followed-owner chips appear.

    10. **Privacy gate check (optional — requires a private-profile follow):**
       - If you follow a user whose `profileSettings.profilePublic = false` OR `profileSettings.collectionPublic = false`, that user must NOT appear in the chip list even if they own this catalog ref (D-05 — follows do NOT override privacy gates).

    Report any failure with: (a) browser console screenshot if #419, (b) viewport width if mobile-specific, (c) the specific `/w/<ref>` URL that broke, (d) hard-refresh behavior (works vs broken) — needed to distinguish PPR soft-nav family from real bugs.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 10 checks pass on prod. Otherwise, describe the failed check + URL + console output + hard-refresh behavior; the executor will route to gap-closure planning.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer auth → page render | `getCurrentUser()` at line 153 throws `UnauthorizedError` for anon viewers (RESEARCH Assumption A2). All three branches inherit this gate. |
| RSC page → DAL | viewerId + catalogId pass to `getFollowedOwnersForCatalog` as bound parameters (already mitigated by Plan 01's DAL hardening). |
| RSC page → client hero | Resolved `{ owners, totalCount }` is serialized to the client as JSON (no functions, no closures — plain projection shape per Plan 01 / D-11). |
| client hero → RSC module | The hero passes props through to `<FollowedOwnersModule>`, which is itself RSC. Next.js handles the RSC-in-CC boundary by server-rendering the module's JSX and serializing the output. No new boundary surface. |
| PPR cache fill | The route is per-viewer dynamic (no `'use cache'` around the new DAL); `await connection()` opts out of the static shell. The new DAL adds NO new caching directives — viewer-scoped output remains correctly per-request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-11 | Information Disclosure (PPR cross-viewer cache leak) | new DAL inside the RSC page tree | mitigate | The DAL is NOT wrapped in `'use cache'` (verify by grep — Task 2 acceptance_criteria implicitly covers this since the diff only adds a function call + Promise.all entry + props, no caching directive). `await connection()` (line 96) + `unstable_instant = false` (line 50) remain intact. The resolved data is part of the per-request render and is NEVER part of a shared static shell. |
| T-65-12 | DoS / Availability (React #419 soft-nav regression) | route shape after integration | mitigate | Task 2 forbids touching lines 50 / 96 / outer Suspense; the pre-existing static guard `tests/static/ppr-dynamic-before-use-cache.test.ts` continues to enforce the `createSupabaseAdminClient` BEFORE `getLikesForTargetCached` ordering with MAX_LOOKAHEAD=70. The new DAL adds a handful of lines per branch — verify the lookahead stays within budget (acceptance_criteria includes the guard re-run). Task 4 prod human-verify catches any residual regression after cache fills. |
| T-65-13 | Information Disclosure (B1 invariant violation) | `WatchDetailHero` import surface | mitigate | Task 1 forbids importing the DAL function into the `'use client'` hero; Task 3 adds a static guard that fails if the function name appears in an `import { ... }` block in `WatchDetailHero.tsx`. Only the type `FollowedOwner` may be imported (and only via `import type`). The component import (`FollowedOwnersModule`) is RSC and is safe to render from a client island (standard Next.js RSC-in-CC pattern; mirrors AvatarDisplay). |
| T-65-14 | Tampering (Branch 1 null-catalogId crash) | Branch 1 Promise.all DAL call | mitigate | The `watch.catalogId ? getFollowedOwnersForCatalog(...) : Promise.resolve({ owners: [], totalCount: 0 })` ternary mirrors the existing sameFamily/lineage/catalog-entry null-handling at lines 312, 328-329. Acceptance criterion in Task 2 verifies the ternary's presence. The DAL signature is strict (`catalogId: string`, NOT `string | null`) so any future caller that drops the guard will fail at TypeScript compile time. |
| T-65-15 | Information Disclosure (existing OtherOwnersRoster regression on Branch 2) | Branch 2 pure-catalog hero shell | mitigate | Task 2 forbids touching the existing `<OtherOwnersRoster>` line; the new module renders ABOVE it as an additional sibling. D-03a — same position, same props. Acceptance criterion: `git diff` shows no `-` lines for the existing OtherOwnersRoster render. |
</threat_model>

<verification>
- All 4 FOLL requirements observable end-to-end:
  - FOLL-01 (hide-if-empty + Branch 1 null-catalog hide): Task 4 step 3 (no module when zero followed owners) + step 4 (null-catalogId → no module).
  - FOLL-02 (one-way direction): Task 4 step 9 (self-exclusion proves the join direction is correctly viewer-as-follower; reinforced by Plan 01 Tests 7 + 8).
  - FOLL-03 (chip semantics + collection click target): Task 4 step 3 (chip click navigates to /u/<username>/collection).
  - FOLL-04 (single query + privacy + non-blocking): Plan 01 DAL tests + Task 2 single-Promise.all integration + Task 4 step 10 (privacy-gate check).
- Build gate: `npm run build` exits 0 (Task 2 verify).
- Static guards: 3 files green —
  - `tests/static/watch-detail-ia-order.test.ts` (extended with 2 new describes — Task 3) passes.
  - `tests/static/ppr-dynamic-before-use-cache.test.ts` (pre-existing) still passes.
  - `tests/static/followed-owners-module-rsc.test.ts` (Plan 02) still passes.
- DAL integration tests: `npx vitest run tests/data/getFollowedOwnersForCatalog.test.ts` passes 8/8 with env loaded.
- Component tests: `npx vitest run tests/components/insights/FollowedOwnersModule.test.tsx` passes ≥10 it-cases.
- Existing tests untouched: `npx vitest run tests/components/insights/OtherOwnersRoster.test.tsx` continues to pass (D-03a regression check).
- Prod human-verify: Task 4 10-step checklist on Vercel after cache fill.
</verification>

<success_criteria>
- All 4 FOLL requirements (FOLL-01..04) are realized end-to-end on prod.
- `npm run build` exits 0; full vitest suite shows no NEW failures attributable to this phase.
- 3 static guards green; both DAL + component test suites pass; no existing test regresses.
- Branch 3 retains both rosters (new "From your circle" hero-column-equivalent + existing OtherOwnersRoster) per D-03a.
- Branches 1 + 2-owned do NOT have an OtherOwnersRoster (D-03b unchanged).
- `await connection()`, `unstable_instant = false`, and the outer Suspense shape are all preserved (PPR contract intact — no React #419 regression on soft-nav after cache fill).
- Prod human-verify checkpoint passes 10/10 checks.
</success_criteria>

<output>
After completion, create `.planning/phases/65-follow-scoped-owners-module/65-03-SUMMARY.md` documenting: (a) the per-file diff summary (page.tsx 6-place edit + WatchDetailHero.tsx 4-place edit + watch-detail-ia-order.test.ts 2-describe append), (b) `npm run build` exit status, (c) the static-guard pass record, (d) the human-verify checkpoint results (10 steps), (e) any deviations from the plan + their justification.
</output>
