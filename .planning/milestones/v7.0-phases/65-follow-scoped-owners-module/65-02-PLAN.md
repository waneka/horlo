---
phase: 65-follow-scoped-owners-module
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/insights/FollowedOwnersModule.tsx
  - tests/components/insights/FollowedOwnersModule.test.tsx
  - tests/static/followed-owners-module-rsc.test.ts
autonomous: true
requirements:
  - FOLL-01
  - FOLL-03
tags:
  - rsc
  - presentational
  - chip
  - a11y
  - static-guard

must_haves:
  truths:
    - "D-04 / D-04a / D-10: FollowedOwnersModule.tsx is a NEW pure-presentation RSC at src/components/insights/FollowedOwnersModule.tsx — no 'use client' directive, no hooks, no event handlers; renders a <section> with the literal aria-label 'People you follow who own this' and a visible <h3> header 'From your circle'."
    - "FOLL-01: Module returns null when owners.length === 0 — entirely absent from DOM (no header, no placeholder, no zero-state card). This same return-null path covers the Branch 1 null-catalogId case after Plan 03 wires it through."
    - "D-04 / FOLL-03: Each chip is a single tap target — absolute-inset <Link href=`/u/${owner.username}/collection`> with aria-label `${displayName ?? '@'+username}'s collection`, AvatarDisplay size={40} (the smallest legal value per AvatarDisplay.tsx:10), focus-visible:ring-2 focus-visible:ring-ring on the link, min-h-[44px] on the <li> for mobile tap-target."
    - "D-04 layout: Vertical chip stack as a semantic <ul> with one <li> per owner (NOT horizontal scroll like OtherOwnersRoster). Wrapper uses `flex items-center gap-3 min-h-[44px]` and the text column is `min-w-0` with `truncate` to ellipsis long usernames/displayNames at narrow widths."
    - "D-04b: At most 5 chips are rendered (consumer is expected to pass the limit-5 result from the DAL; the component does not slice — it trusts the prop). D-04c: When totalCount > owners.length, an overflow caption `<p class='text-xs text-muted-foreground'>and {totalCount - owners.length} more</p>` renders below the <ul>; otherwise the caption is absent."
    - "D-11: FollowedOwner type is imported as `import type { FollowedOwner } from '@/data/follows'` (type-only) — never the function. This preserves the option for the consuming hero to also use the type without dragging server-only code into a 'use client' boundary (Plan 03 dependency)."
    - "PAGE-03 preserve / RESEARCH Anti-Patterns: The new component file MUST NOT contain `'use client'` or `'use cache'` directives — a static guard tests/static/followed-owners-module-rsc.test.ts encodes this rule with `// @vitest-environment node` per project_vitest_static_node_env memory."
    - "Component tests cover: (a) returns null on empty owners (FOLL-01), (b) renders the literal header 'From your circle' (D-04a), (c) renders each chip with the correct href + aria-label (FOLL-03), (d) renders the '+N more' caption with the correct delta when totalCount > owners.length and omits it otherwise (D-04c)."
  artifacts:
    - path: "src/components/insights/FollowedOwnersModule.tsx"
      provides: "Pure-presentation RSC chip module — header + chip list + overflow caption + hide-if-empty"
      min_lines: 35
      contains: "export function FollowedOwnersModule"
    - path: "tests/components/insights/FollowedOwnersModule.test.tsx"
      provides: "Component unit tests for hide-if-empty, header copy, chip semantics, overflow caption"
      contains: "describe('FollowedOwnersModule'"
    - path: "tests/static/followed-owners-module-rsc.test.ts"
      provides: "Static guard: file contains no 'use client' / 'use cache' directives"
      contains: "// @vitest-environment node"
  key_links:
    - from: "src/components/insights/FollowedOwnersModule.tsx"
      to: "@/data/follows (FollowedOwner type only)"
      via: "import type"
      pattern: "import type \\{ FollowedOwner \\} from '@/data/follows'"
    - from: "src/components/insights/FollowedOwnersModule.tsx"
      to: "@/components/profile/AvatarDisplay"
      via: "import + size={40} usage"
      pattern: "AvatarDisplay[\\s\\S]*?size=\\{40\\}"
    - from: "src/components/insights/FollowedOwnersModule.tsx"
      to: "/u/{username}/collection routes"
      via: "next/link <Link href=`/u/${owner.username}/collection`>"
      pattern: "/u/\\$\\{[^}]+\\}/collection"
---

<objective>
Ship the new pure-presentation RSC `FollowedOwnersModule` at `src/components/insights/FollowedOwnersModule.tsx` with its component test suite and a static RSC guard. The module accepts `{ owners: FollowedOwner[]; totalCount: number }` as props (pre-resolved by the page-level RSC in Plan 03), returns `null` on empty input (FOLL-01 hide-if-empty), and otherwise renders the locked design contract: warmer "From your circle" header + vertical chip stack + "+N more" caption + absolute-inset <Link> click surface with `aria-label` per chip.

Purpose: Phase 65 FOLL-01 + FOLL-03 are realized in this plan. The component depends only on the FollowedOwner *type* from Plan 01 (not the function) and only on existing primitives (AvatarDisplay, next/link); it has zero runtime dependency on the DAL. This makes Plans 01 and 02 parallelizable in Wave 1 — the file boundaries do not collide.

Output: A self-contained RSC module + jsdom-rendered component tests + a node-environment static guard that locks the file as RSC (no `'use client'` / `'use cache'` directive). Plan 03 then wires the component into the WatchDetailHero right column with pre-resolved data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-follow-scoped-owners-module/65-CONTEXT.md
@.planning/phases/65-follow-scoped-owners-module/65-RESEARCH.md
@.planning/phases/65-follow-scoped-owners-module/65-PATTERNS.md
@.planning/phases/65-follow-scoped-owners-module/65-UI-SPEC.md

<interfaces>
<!-- Component contracts the executor must implement against. -->

From src/components/profile/AvatarDisplay.tsx (existing — DO NOT modify):
```typescript
interface AvatarDisplayProps {
  avatarUrl: string | null
  displayName: string | null
  username: string
  size?: 40 | 64 | 96   // literal union — 40 is the smallest legal value
  className?: string
}
export function AvatarDisplay(props: AvatarDisplayProps): JSX.Element
```

From src/components/insights/OtherOwnersRoster.tsx (the STRUCTURAL ANALOG — DO NOT modify, but mirror its primitives):
- Imports `Link` from `next/link`, `AvatarDisplay` from `@/components/profile/AvatarDisplay`, and `type { CatalogCollector }` from `@/data/discovery`.
- Returns `null` when `collectors.length === 0` (line 50).
- Each chip uses an absolute-inset `<Link href={\`/u/${c.username}/collection\`} aria-label={\`${name}'s collection\`} className="absolute inset-0 rounded-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring" />` over a `<div className="group relative ...">` wrapper (lines 65-73).
- Uses `AvatarDisplay size={40}` (line 79) per the literal-union constraint.

From src/data/follows.ts (Plan 01 dependency — TYPE ONLY):
```typescript
export interface FollowedOwner {
  userId: string
  username: string
  displayName: string | null
  avatarUrl: string | null
}
```

Plan 02 imports this as `import type { FollowedOwner } from '@/data/follows'` — NEVER the function. The DAL is server-only; only the type may cross the boundary.

Required JSX shape (verbatim from 65-UI-SPEC.md "Module Anatomy" section, lines 104-126):

```jsx
<section aria-label="People you follow who own this" className="space-y-2">
  <h3 className="text-sm font-medium text-foreground">From your circle</h3>
  <ul className="space-y-2">
    <li className="group relative flex items-center gap-3 min-h-[44px]">
      <Link href="/u/{username}/collection"
            aria-label="{displayName ?? '@'+username}'s collection"
            className="absolute inset-0 rounded-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring" />
      <AvatarDisplay avatarUrl={...} displayName={...} username={...} size={40} />
      <div className="min-w-0">
        <p className="text-sm font-medium text-foreground truncate">@{username}</p>
        {displayName && (
          <p className="text-xs text-muted-foreground truncate group-hover:text-foreground transition-colors">{displayName}</p>
        )}
      </div>
    </li>
    {/* ...repeat per owner... */}
  </ul>
  {totalCount > owners.length && (
    <p className="text-xs text-muted-foreground">and {totalCount - owners.length} more</p>
  )}
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FollowedOwnersModule.tsx (pure RSC, chip stack, hide-if-empty, overflow caption)</name>
  <files>src/components/insights/FollowedOwnersModule.tsx</files>
  <read_first>
    - src/components/insights/OtherOwnersRoster.tsx (ENTIRE file — the structural analog; reuse the import set + click-surface primitives + aria-label + AvatarDisplay size constraint; the divergence from this analog is documented in 65-PATTERNS.md "src/components/insights/FollowedOwnersModule.tsx" section)
    - src/components/profile/AvatarDisplay.tsx lines 1-12 (confirm the literal union `40 | 64 | 96` — Plan 02 uses size={40})
    - .planning/phases/65-follow-scoped-owners-module/65-UI-SPEC.md (Module Anatomy lines 104-126 — the verbatim JSX template; Component Layout & Composition; Copywriting Contract; Accessibility Contract; Interaction & State Contract — confirms no animations / no client state / no event handlers)
    - .planning/phases/65-follow-scoped-owners-module/65-RESEARCH.md (Pattern 3 lines 344-406 — the reference RSC skeleton; Anti-Patterns — confirms no 'use client' / no DAL function import)
    - .planning/phases/65-follow-scoped-owners-module/65-PATTERNS.md ("src/components/insights/FollowedOwnersModule.tsx" subsection — confirms exactly which class lists swap vs OtherOwnersRoster: outer wrapper from horizontal-scroll-flex to space-y-2 <ul>, chip wrapper from `flex flex-col items-center gap-2 w-16 shrink-0` to `flex items-center gap-3 min-h-[44px]`, and <div> → <li> semantic change)
    - .planning/phases/65-follow-scoped-owners-module/65-CONTEXT.md (D-04, D-04a, D-04b, D-04c, D-10, D-11)
  </read_first>
  <behavior>
    - Behavior 1 (FOLL-01 hide-if-empty): `<FollowedOwnersModule owners={[]} totalCount={0} />` renders nothing — `container.firstChild === null` after rendering with @testing-library/react.
    - Behavior 2 (FOLL-01 hide-if-empty with non-zero totalCount — defensive): `<FollowedOwnersModule owners={[]} totalCount={5} />` ALSO renders null (the visible chips are the contract; totalCount alone with no chips is meaningless to render). This matches OtherOwnersRoster's `collectors.length === 0` early-return.
    - Behavior 3 (D-04a header): With ≥1 owner, the literal text "From your circle" appears inside an `<h3>`; the wrapping <section> carries the literal aria-label "People you follow who own this" (the warmer header is the visible text; the literal phrasing is preserved in the SR tree per UI-SPEC Copywriting Contract).
    - Behavior 4 (FOLL-03 chip semantics): For one rendered owner `{ userId: 'u1', username: 'alice', displayName: 'Alice Wonder', avatarUrl: null }`, there is exactly one <Link> with `href="/u/alice/collection"` and `aria-label="Alice Wonder's collection"`. With `displayName: null` and `username: 'alice'` the aria-label is `"@alice's collection"`.
    - Behavior 5 (D-04 chip primary text): The visible primary text is `@${username}` (with the @ prefix); the secondary line shows `{displayName}` only when non-null.
    - Behavior 6 (D-04c overflow caption present): `<FollowedOwnersModule owners={[5 chips]} totalCount={12} />` renders the text "and 7 more" inside a <p>.
    - Behavior 7 (D-04c overflow caption absent): `<FollowedOwnersModule owners={[3 chips]} totalCount={3} />` does NOT render the caption.
    - Behavior 8 (D-04c overflow caption absent at exact match): `<FollowedOwnersModule owners={[5 chips]} totalCount={5} />` does NOT render the caption (the gate is `totalCount > owners.length`, strictly greater).
    - Behavior 9 (A11y): The module renders exactly one <ul> with one <li> per owner; each <li> has the `group` class and contains exactly one <Link> as the only focusable descendant.
  </behavior>
  <action>
    Create the new file `src/components/insights/FollowedOwnersModule.tsx`. It is a pure-presentation Server Component — NO `'use client'` directive, NO hooks, NO event handlers, NO `'use cache'` directive. The file imports only: `Link` from `next/link`, `AvatarDisplay` from `@/components/profile/AvatarDisplay`, and the type-only import `import type { FollowedOwner } from '@/data/follows'` (per D-11 and RESEARCH Anti-Patterns — NEVER the function from that module).

    Add a JSDoc header (mirroring OtherOwnersRoster.tsx lines 7-39 in tone) that cites: Phase 65 FOLL-01..04, D-04 (vertical-stack layout choice), D-04a (header copy "From your circle" vs literal SR aria-label), D-04c (plain-text overflow caption — no see-all link), D-10 (placement as a sibling in WatchDetailHero right column), D-11 (type-only import from follows.ts), and explicitly note "RSC — no 'use client' / no 'use cache'; the static guard at tests/static/followed-owners-module-rsc.test.ts enforces this."

    Component signature: `export function FollowedOwnersModule({ owners, totalCount }: FollowedOwnersModuleProps)`. Props interface above the function:
    ```typescript
    interface FollowedOwnersModuleProps {
      owners: FollowedOwner[]
      totalCount: number
    }
    ```

    Function body (in order):
    1. Hide-if-empty early return: `if (owners.length === 0) return null` (FOLL-01 — owners.length, NOT totalCount, is the gate; matches OtherOwnersRoster.tsx:50).
    2. Return a `<section className="space-y-2" aria-label="People you follow who own this">` containing:
       - An `<h3 className="text-sm font-medium text-foreground">From your circle</h3>` (D-04a visible header — literal "From your circle"; UI-SPEC Typography table — text-sm + font-medium + text-foreground).
       - A `<ul className="space-y-2">` containing one `<li>` per owner.
       - Conditionally a `<p className="text-xs text-muted-foreground">and {totalCount - owners.length} more</p>` ONLY when `totalCount > owners.length` (D-04c — strictly greater).
    3. Per `<li>`: `className="group relative flex items-center gap-3 min-h-[44px]"`, key={owner.userId}. Inside the <li>, render IN ORDER:
       - An absolute-inset `<Link>` with: `href={\`/u/${owner.username}/collection\`}`, `aria-label={\`${owner.displayName ?? '@' + owner.username}'s collection\`}`, `className="absolute inset-0 rounded-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"`. This is the click surface (FOLL-03 + D-02a; mirrors OtherOwnersRoster.tsx:69-73).
       - An `<AvatarDisplay avatarUrl={owner.avatarUrl} displayName={owner.displayName} username={owner.username} size={40} />` (size=40 per AvatarDisplay literal union + UI-SPEC D-04 + RESEARCH A4 — substitute 40 because 36 isn't legal).
       - A `<div className="min-w-0">` wrapping two `<p>` elements:
         * Primary: `<p className="text-sm font-medium text-foreground truncate">@{owner.username}</p>` (UI-SPEC chip primary).
         * Secondary (CONDITIONAL): rendered only when `owner.displayName` is non-null: `<p className="text-xs text-muted-foreground truncate group-hover:text-foreground transition-colors">{owner.displayName}</p>` (UI-SPEC Color + group-hover affordance + Assumption #2).

    DO NOT add icons (UI-SPEC Assumption #3 — no Users icon next to the header). DO NOT add animation, skeleton, hover background, or any state. DO NOT memoize. DO NOT slice the owners array — the consumer passes exactly the desired chip count (typically ≤5 from the DAL with limit=5).

    The file MUST start with the import block on lines 1-N (no `'use client'`), followed by the JSDoc + props interface + the named export.

    Reference the existing OtherOwnersRoster.tsx for whitespace/formatting conventions (4-space indent, single quotes, no trailing semicolons in JSX attributes, etc.).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "src/components/insights/FollowedOwnersModule\.tsx" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/components/insights/FollowedOwnersModule.tsx`.
    - Source assertion: the first 5 lines (excluding blank lines and comments) contain `import Link from 'next/link'`, `import { AvatarDisplay } from '@/components/profile/AvatarDisplay'`, and `import type { FollowedOwner } from '@/data/follows'`. None of those 5 lines contains `'use client'` or `'use cache'`.
    - Source assertion: `grep -c "^export function FollowedOwnersModule" src/components/insights/FollowedOwnersModule.tsx` returns 1.
    - Source assertion: `grep -c "if (owners.length === 0) return null" src/components/insights/FollowedOwnersModule.tsx` returns 1 (FOLL-01 hide-if-empty).
    - Source assertion: `grep -c "From your circle" src/components/insights/FollowedOwnersModule.tsx` returns 1 (D-04a header).
    - Source assertion: `grep -c "People you follow who own this" src/components/insights/FollowedOwnersModule.tsx` returns 1 (UI-SPEC aria-label for SR).
    - Source assertion: the file contains the literal `href={\`/u/${owner.username}/collection\`}` (or an equivalent backtick template; grep with `grep -F "/u/" src/components/insights/FollowedOwnersModule.tsx` returns at least 1 match showing the collection target).
    - Source assertion: the file contains `aria-label={` followed by a template referencing both `displayName` and `username` (e.g. `grep -E "aria-label=\\{.*displayName.*username|aria-label=\\{.*username.*displayName" src/components/insights/FollowedOwnersModule.tsx` returns ≥ 1).
    - Source assertion: `grep -c "size={40}" src/components/insights/FollowedOwnersModule.tsx` returns 1 (AvatarDisplay literal-union compliance).
    - Source assertion: `grep -c "focus-visible:ring-2 focus-visible:ring-ring" src/components/insights/FollowedOwnersModule.tsx` returns 1 (focus ring on the absolute-inset Link).
    - Source assertion: `grep -c "min-h-\\[44px\\]" src/components/insights/FollowedOwnersModule.tsx` returns 1 (mobile tap-target safeguard).
    - Source assertion: the overflow caption is gated by `totalCount > owners.length` (strictly greater, NOT `>=`): `grep -c "totalCount > owners.length" src/components/insights/FollowedOwnersModule.tsx` returns 1.
    - Source assertion: the file contains the literal "and " (with trailing space) and " more" substrings around a JSX expression: `grep -c "and {totalCount - owners.length} more" src/components/insights/FollowedOwnersModule.tsx` returns 1.
    - Source assertion: NO import of `getFollowedOwnersForCatalog` (function): `grep -c "import.*getFollowedOwnersForCatalog" src/components/insights/FollowedOwnersModule.tsx` returns 0.
    - Source assertion: NO 'use client' / 'use cache' anywhere in the file: `grep -cE "^'use (client|cache)'" src/components/insights/FollowedOwnersModule.tsx` returns 0.
    - TypeScript: `npx tsc --noEmit` produces no NEW errors mentioning `src/components/insights/FollowedOwnersModule.tsx`.
  </acceptance_criteria>
  <done>
    `src/components/insights/FollowedOwnersModule.tsx` exists as a pure RSC, imports only what's listed in <interfaces>, renders the locked JSX shape from UI-SPEC §Module Anatomy, gates the overflow caption with strict `>`, uses AvatarDisplay size=40 + focus-visible ring + min-h-[44px], returns null on empty owners, and ships with the FOLL-01 + D-04a + D-04c + FOLL-03 + D-11 contracts visible to grep.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/components/insights/FollowedOwnersModule.test.tsx (hide-if-empty, header, chip semantics, overflow caption)</name>
  <files>tests/components/insights/FollowedOwnersModule.test.tsx</files>
  <read_first>
    - tests/components/insights/OtherOwnersRoster.test.tsx (the structural template — uses `@testing-library/react`'s `render` + `getByText` + `container.firstChild === null` pattern; suite layout is a single `describe` with multiple `it`)
    - src/components/insights/FollowedOwnersModule.tsx (the component built in Task 1 — confirm prop shape and exported name match the test imports)
    - .planning/phases/65-follow-scoped-owners-module/65-UI-SPEC.md (Module Anatomy + Copywriting Contract — confirms aria-labels, href format, "and N more" copy)
  </read_first>
  <behavior>
    - "returns null when owners=[]": rendering `<FollowedOwnersModule owners={[]} totalCount={0} />` yields `container.firstChild === null`.
    - "returns null when owners=[] even with non-zero totalCount": rendering `<FollowedOwnersModule owners={[]} totalCount={42} />` yields `container.firstChild === null` (defensive — the chip list, not the count, is the visibility gate).
    - "renders the literal 'From your circle' header when owners.length >= 1": `getByText('From your circle')` succeeds; its tagName is 'H3' (or `closest('h3')` is non-null).
    - "applies the literal SR aria-label 'People you follow who own this' on the wrapping section": `getByRole('region', { name: 'People you follow who own this' })` resolves (a `<section>` with aria-label maps to role=region in jsdom/testing-library), OR `container.querySelector('section[aria-label="People you follow who own this"]')` is non-null.
    - "renders one <Link> per owner with href /u/{username}/collection and aria-label `${displayName ?? '@'+username}'s collection`": for `owners=[{ userId: 'u1', username: 'alice', displayName: 'Alice Wonder', avatarUrl: null }, { userId: 'u2', username: 'bob', displayName: null, avatarUrl: null }]`, the rendered DOM has an `<a href="/u/alice/collection" aria-label="Alice Wonder's collection">` AND an `<a href="/u/bob/collection" aria-label="@bob's collection">`.
    - "renders @username as the primary chip text": `getAllByText('@alice')` and `getAllByText('@bob')` each return at least one element.
    - "renders displayName only when present": `getByText('Alice Wonder')` succeeds; querying for any element with text content exactly 'null' or for bob's displayName fails (bob has no secondary line).
    - "renders 'and N more' caption when totalCount > owners.length": rendering 3 owners with totalCount=10 yields `getByText('and 7 more')`.
    - "omits the caption when totalCount === owners.length": rendering 5 owners with totalCount=5 — `queryByText(/and \d+ more/)` returns null.
    - "omits the caption when totalCount < owners.length (defensive)": rendering 5 owners with totalCount=3 — `queryByText(/and -?\d+ more/)` returns null (strict `>` gate).
  </behavior>
  <action>
    Create `tests/components/insights/FollowedOwnersModule.test.tsx` modeled on `tests/components/insights/OtherOwnersRoster.test.tsx`. Use `@testing-library/react` (already a project dependency — used by the OtherOwnersRoster test) and `vitest`.

    Imports:
    - `import { render } from '@testing-library/react'`
    - `import { describe, it, expect } from 'vitest'`
    - `import { FollowedOwnersModule } from '@/components/insights/FollowedOwnersModule'`
    - `import type { FollowedOwner } from '@/data/follows'`

    Helper functions (clone the shape of `makeCollector` / `makeCollectors` in the source test):
    ```typescript
    function makeOwner(n: number, opts: { displayName?: string | null } = {}): FollowedOwner {
      return {
        userId: `user-${n}`,
        username: `user${n}`,
        displayName: opts.displayName === undefined ? `User ${n}` : opts.displayName,
        avatarUrl: null,
      }
    }
    function makeOwners(count: number): FollowedOwner[] {
      return Array.from({ length: count }, (_, i) => makeOwner(i + 1))
    }
    ```

    Single `describe('FollowedOwnersModule', () => { ... })` containing the ~10 `it(...)` cases listed in `<behavior>`. Use:
    - `container.firstChild === null` for hide-if-empty (matches OtherOwnersRoster test line 65).
    - `getByText('From your circle')` for the header (and assert `.tagName.toLowerCase() === 'h3'` if straightforward; otherwise rely on the unique text + getByRole('heading', { level: 3, name: 'From your circle' })).
    - `container.querySelector('a[href="/u/alice/collection"]')` for the href assertion and `.getAttribute('aria-label')` for the aria-label.
    - `queryByText(/and \d+ more/)` or `queryByText('and 7 more')` for the caption — `queryBy*` for absent-assertions, `getBy*` for present-assertions.

    Specifically include the two CRITICAL Alice / Bob fixtures to validate the displayName fallback in the aria-label:
    - Alice: `{ userId: 'u1', username: 'alice', displayName: 'Alice Wonder', avatarUrl: null }` → expected aria-label `"Alice Wonder's collection"`.
    - Bob: `{ userId: 'u2', username: 'bob', displayName: null, avatarUrl: null }` → expected aria-label `"@bob's collection"`.

    DO NOT mock `next/link` — `@testing-library/react` handles it fine in jsdom (the OtherOwnersRoster test renders the same primitive without mocks). DO NOT mock `AvatarDisplay` — render against the real component.
  </action>
  <verify>
    <automated>npx vitest run tests/components/insights/FollowedOwnersModule.test.tsx --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/components/insights/FollowedOwnersModule.test.tsx`.
    - Source assertion: `grep -c "^import { render } from '@testing-library/react'" tests/components/insights/FollowedOwnersModule.test.tsx` returns 1.
    - Source assertion: `grep -c "from '@/components/insights/FollowedOwnersModule'" tests/components/insights/FollowedOwnersModule.test.tsx` returns 1.
    - Source assertion: `grep -c "import type { FollowedOwner } from '@/data/follows'" tests/components/insights/FollowedOwnersModule.test.tsx` returns 1.
    - Source assertion: `grep -cE "^\s+it\(" tests/components/insights/FollowedOwnersModule.test.tsx` ≥ 10 (covers all 10 behaviors).
    - Source assertion: the file references `"Alice Wonder's collection"` (Alice aria-label case) and `"@bob's collection"` (bob fallback case): `grep -c "Alice Wonder's collection\|@bob's collection" tests/components/insights/FollowedOwnersModule.test.tsx` ≥ 2.
    - Source assertion: the file references `'/u/alice/collection'` and `'/u/bob/collection'` (href assertions): `grep -c "/u/alice/collection\|/u/bob/collection" tests/components/insights/FollowedOwnersModule.test.tsx` ≥ 2.
    - Source assertion: the file references the literal "and 7 more" caption assertion: `grep -c "and 7 more" tests/components/insights/FollowedOwnersModule.test.tsx` ≥ 1.
    - Behavior assertion: `npx vitest run tests/components/insights/FollowedOwnersModule.test.tsx` reports all `it` cases passing (no failures, no skips). Expected count ≥ 10.
    - Behavior assertion: when Task 1's source file is intentionally broken (e.g. removing the `if (owners.length === 0) return null` line in a local experiment), the "returns null when owners=[]" test FAILS — the test must actually exercise the behavior, not be a no-op.
  </acceptance_criteria>
  <done>
    `tests/components/insights/FollowedOwnersModule.test.tsx` exists, covers ≥10 it-cases for FOLL-01 hide-if-empty + D-04a header + UI-SPEC aria-label + FOLL-03 chip semantics + D-04c overflow caption gating (both directions), and all tests pass under `npx vitest run`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author tests/static/followed-owners-module-rsc.test.ts (PAGE-03 RSC guard with @vitest-environment node)</name>
  <files>tests/static/followed-owners-module-rsc.test.ts</files>
  <read_first>
    - tests/static/comment-thread-no-client.test.ts (the EXACT structural template — `// @vitest-environment node` header, readFileSync of a target src file, `top.some((line) => line.trim() === "'use client'")` checked against the first 5 lines)
    - src/components/insights/FollowedOwnersModule.tsx (the file under test from Task 1 — confirm the directive lines are absent)
    - .planning/phases/65-follow-scoped-owners-module/65-RESEARCH.md (Anti-Patterns section + Validation Architecture Wave 0 Gaps — the optional static guard is recommended; here it is mandatory because PAGE-03 preservation is the load-bearing invariant)
    - MEMORY project_vitest_static_node_env (cited inline in the guard header — without `// @vitest-environment node` the static test passes locally under jsdom but fails Vercel prebuild)
  </read_first>
  <behavior>
    - Test 1: `src/components/insights/FollowedOwnersModule.tsx` does NOT contain the directive `'use client'` in its first 5 lines (the typical placement for the directive — if it appears elsewhere it has no effect anyway, but the project's other static guards check the top of the file).
    - Test 2: The same file does NOT contain the directive `'use cache'` in its first 5 lines.
    - Test 3: The file exports `FollowedOwnersModule` as a named function — the guard pattern `export function FollowedOwnersModule` appears at least once in the file (gives an actionable error message if the function gets renamed accidentally).
  </behavior>
  <action>
    Create `tests/static/followed-owners-module-rsc.test.ts` modeled VERBATIM on `tests/static/comment-thread-no-client.test.ts`. The file MUST begin with the `// @vitest-environment node` pragma on line 1 (project_vitest_static_node_env memory — without this, the test passes locally under jsdom but Vercel's prebuild fails because vite externalizes `node:fs` under jsdom and `readFileSync` becomes undefined). Cite this memory inline in the file header.

    File structure (mirror `tests/static/comment-thread-no-client.test.ts`):

    1. Line 1: `// @vitest-environment node`
    2. Lines 2-12: header JSDoc-style block explaining: (a) why the node environment is required (cite project_vitest_static_node_env + the Phase 59 prod deploy failure precedent), (b) what this guard enforces (Phase 65 FOLL-01..04 / PAGE-03 preservation — `FollowedOwnersModule.tsx` must be pure RSC because adding `'use client'` would corrupt the Phase 51/52/61 PPR boundary on `/w/[ref]` once Plan 03 wires the component into the hero), (c) the specific Pitfall being guarded (a future "client conversion" of the component to add hover state or click handlers).
    3. Standard imports: `import { describe, it, expect } from 'vitest'`, `import { readFileSync } from 'node:fs'`, `import { join } from 'node:path'`.
    4. `describe('PAGE-03 (Phase 65): FollowedOwnersModule.tsx is pure RSC', () => { ... })` containing three `it` blocks. The path constant: `const FILE = join('src', 'components', 'insights', 'FollowedOwnersModule.tsx')`. Read the file once at the top of the describe via `const content = readFileSync(FILE, 'utf8')`.
    5. Use the EXACT directive-detection pattern from `comment-thread-no-client.test.ts` lines 27-29 — extract the first 5 lines and assert no trimmed line equals the directive string. This is the load-bearing pattern: a fuzzy `content.includes("'use client'")` would false-positive on prose inside JSDoc comments (see project_react_419+404 family + the Phase 64 WatchDetailTrailing precedent in STATE.md — "comment reworded to avoid grep false-positive").

    Three it blocks (each with a clear failure message via `expect(...).toBe(false)` / `.toBeTruthy()`):
    - `it('does not contain "use client" directive in the first 5 lines', ...)`.
    - `it('does not contain "use cache" directive in the first 5 lines', ...)`.
    - `it('exports FollowedOwnersModule as a named function', () => { expect(content).toMatch(/export function FollowedOwnersModule/); })`.
  </action>
  <verify>
    <automated>npx vitest run tests/static/followed-owners-module-rsc.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/static/followed-owners-module-rsc.test.ts`.
    - Source assertion: line 1 is exactly `// @vitest-environment node` (no leading blank line, no shebang). Verify with `head -1 tests/static/followed-owners-module-rsc.test.ts` returning `// @vitest-environment node`.
    - Source assertion: the file references `project_vitest_static_node_env` in the header comments: `grep -c "project_vitest_static_node_env\|MEMORY" tests/static/followed-owners-module-rsc.test.ts` ≥ 1.
    - Source assertion: the file uses `readFileSync` (not a fuzzy `import.meta.glob` or anything else): `grep -c "readFileSync(FILE" tests/static/followed-owners-module-rsc.test.ts` ≥ 1.
    - Source assertion: the directive check uses the EXACT `line.trim() === "'use client'"` shape (NOT `content.includes(\"'use client'\")`): `grep -cE "trim\\(\\)\\s*===\\s*\"'use client'\"" tests/static/followed-owners-module-rsc.test.ts` ≥ 1.
    - Behavior assertion: `npx vitest run tests/static/followed-owners-module-rsc.test.ts` reports 3 passing tests, 0 failures.
    - Behavior assertion (negative — manual one-shot check): if a developer were to add `'use client'` as line 1 of `src/components/insights/FollowedOwnersModule.tsx`, this test FAILS with a clear message — the guard actually fires (do NOT permanently modify the source for this check; the assertion is design-level, not a CI gate).
    - Build assertion: `npm run build` completes without TypeScript errors mentioning this test file.
  </acceptance_criteria>
  <done>
    `tests/static/followed-owners-module-rsc.test.ts` exists with the `// @vitest-environment node` header (cited per project_vitest_static_node_env memory), reads `src/components/insights/FollowedOwnersModule.tsx` via `readFileSync`, asserts no `'use client'` / `'use cache'` directives in the first 5 lines using the exact `line.trim() === ...` shape, and asserts the named export. All 3 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props → render | `FollowedOwnersModule` receives `owners: FollowedOwner[]` from a trusted server-side caller (Plan 03's page.tsx). Untrusted values would have already been filtered by the DAL's privacy gates (Plan 01). |
| chip text → DOM | `owner.username` / `owner.displayName` flow into JSX text nodes and template-literal aria-labels. React auto-escapes both — no XSS vector (mirrors OtherOwnersRoster.tsx:36-39 hardened pattern). |
| chip href → SPA navigation | `\`/u/${owner.username}/collection\`` becomes a `<Link href>` value. Username is regex-validated at signup so it cannot contain `/` or quote characters that would break the URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-07 | XSS (Tampering / Information Disclosure) | username/displayName rendered in chip text + aria-label | mitigate | React auto-escapes JSX text nodes and template-literal interpolations in attribute values. No `dangerouslySetInnerHTML`. Username is regex-validated at signup. Mirrors the documented contract in OtherOwnersRoster.tsx:36-39. |
| T-65-08 | Tampering | A future developer adds `'use client'` to `FollowedOwnersModule.tsx` to support hover state | mitigate | Task 3 ships a `// @vitest-environment node` static guard at `tests/static/followed-owners-module-rsc.test.ts` that fails the test suite (and therefore the Vercel build via `npm run test` if it runs in CI) when the directive is added. The guard uses the strict `line.trim() === "'use client'"` shape proven not to false-positive on prose. |
| T-65-09 | Information Disclosure | Plan-02 component imports the DAL function (server-only) and inadvertently runs/bundles server code into a client boundary | mitigate | The component is RSC; it only `import type { FollowedOwner } from '@/data/follows'` (type-only — D-11). Source assertion in Task 1 acceptance_criteria verifies the function symbol `getFollowedOwnersForCatalog` is NOT imported. |
| T-65-10 | Privacy bypass via empty-list rendering | A developer changes the early-return gate from `owners.length === 0` to `totalCount === 0` | accept | Behavior 2 in Task 2 covers the "owners=[] with totalCount > 0 still hides" case — the test fails loudly if the gate is changed. Disposition `accept` because the test is the gate; no additional code-level mitigation needed. |
</threat_model>

<verification>
- Component source visible to grep: `grep -E "^export function FollowedOwnersModule|'use client'|'use cache'|size=\\{40\\}|focus-visible:ring-2 focus-visible:ring-ring|totalCount > owners.length|and \\{totalCount" src/components/insights/FollowedOwnersModule.tsx | head -10` shows the export, NO directives, and all locked design tokens.
- Component tests: `npx vitest run tests/components/insights/FollowedOwnersModule.test.tsx` → 0 failures, ≥10 passing.
- Static guard: `npx vitest run tests/static/followed-owners-module-rsc.test.ts` → 0 failures, 3 passing.
- Build: `npm run build` exits 0.
- TypeScript: `npx tsc --noEmit` adds no new errors mentioning `FollowedOwnersModule.tsx` or its test files.
</verification>

<success_criteria>
- `src/components/insights/FollowedOwnersModule.tsx` exists as a pure RSC implementing the UI-SPEC §Module Anatomy contract exactly (vertical chip stack, "From your circle" header, "and N more" overflow caption gated by `>`, absolute-inset Link with focus-visible ring, AvatarDisplay size=40, min-h-[44px], type-only import from follows.ts).
- Component test file (`tests/components/insights/FollowedOwnersModule.test.tsx`) passes ≥10 it-cases covering FOLL-01 + D-04a + D-04c + FOLL-03 contracts.
- Static guard (`tests/static/followed-owners-module-rsc.test.ts`) passes 3 it-cases with `// @vitest-environment node` header.
- No file under `src/` other than `FollowedOwnersModule.tsx` is touched by this plan.
- Plan 03 (Wave 2) can import the component without additional refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/65-follow-scoped-owners-module/65-02-SUMMARY.md` documenting the new component file path + JSX shape + the it-case roster from both test files, with a per-test pass record.
</output>
