---
phase: 59-unified-route-variant-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/watches.ts
  - tests/static/legacy-watch-routes.test.ts
  - tests/integration/phase59-unified-route.test.ts
  - package.json
autonomous: true
requirements: [ROUTE-01, ROUTE-02, ROUTE-03, ROUTE-04, ROUTE-05]
must_haves:
  truths:
    - "findViewerWatchByCatalogId is callable from @/data/watches (extracted from the page being deleted, per D-06)"
    - "The CI guard test exists and fails the build via a prebuild hook when a legacy /watch/${...} or /catalog/${...} link literal is present (ROUTE-03/D-11/D-12)"
    - "The guard allowlists /watch/new and does not false-flag /explore/lists/, /admin/lists/, /wear/ (D-13)"
    - "An integration test asserts the /w/[ref] resolution contract: per-user branch, catalog branch, owned-via-catalog D-06 branch, cross-user framing (ROUTE-01)"
  artifacts:
    - path: "src/data/watches.ts"
      provides: "findViewerWatchByCatalogId DAL function with the BUG-01 status='owned' fix preserved"
      contains: "findViewerWatchByCatalogId"
    - path: "tests/static/legacy-watch-routes.test.ts"
      provides: "ROUTE-03 build-failing source-scan guard + ROUTE-02 route-absence assertions"
      contains: "ROUTE-03"
    - path: "tests/integration/phase59-unified-route.test.ts"
      provides: "ROUTE-01 resolution-contract integration coverage (stubbed; asserts unified page resolution semantics)"
      contains: "phase59"
    - path: "package.json"
      provides: "prebuild hook that runs the guard before next build"
      contains: "prebuild"
  key_links:
    - from: "package.json prebuild script"
      to: "tests/static/legacy-watch-routes.test.ts"
      via: "vitest run"
      pattern: "prebuild.*vitest run tests/static/legacy-watch-routes"
    - from: "src/data/watches.ts findViewerWatchByCatalogId"
      to: "watches table"
      via: "drizzle select scoped by userId AND catalogId AND status='owned'"
      pattern: "eq\\(watches\\.status, 'owned'\\)"
---

<objective>
Lay the Wave 0 foundation for the route merge: extract the `findViewerWatchByCatalogId` DAL function (which currently lives inside the catalog page that Phase 59 deletes) into `src/data/watches.ts`, author the build-failing CI guard test plus the ROUTE-01 integration test scaffold, and wire the `prebuild` npm hook that makes the guard fail the Vercel build.

Purpose: The unified page (Plan 02) cannot import `findViewerWatchByCatalogId` from a page file that gets deleted, so the function must land in the DAL first (per D-06). The CI guard is the completeness guarantee for the hard cutover (D-11/D-12/D-13, ROUTE-03) — it must exist before the link re-point + deletions land in Plan 03 so the deterministic gate is proven, not assumed.

Output: `findViewerWatchByCatalogId` in the DAL; `tests/static/legacy-watch-routes.test.ts`; `tests/integration/phase59-unified-route.test.ts`; a `prebuild` script in `package.json`.

Scope note: The CI guard test is EXPECTED to be RED at the end of this plan — the 26 legacy literals still exist until Plan 03 re-points them. Do NOT attempt to make it green here. This plan delivers the gate machinery, not the migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-unified-route-variant-c/59-CONTEXT.md
@.planning/phases/59-unified-route-variant-c/59-RESEARCH.md
@.planning/phases/59-unified-route-variant-c/59-PATTERNS.md
@.planning/phases/59-unified-route-variant-c/59-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase 2026-05-25. Use directly — no exploration needed. -->

From src/data/watches.ts (existing — file header at lines 1-8; table imported as `watches`, NOT aliased):
```typescript
import 'server-only'
import { db } from '@/db'
import { watches, profileSettings, watchesCatalog } from '@/db/schema'
import { eq, and, or, asc, desc, inArray, sql, type SQL } from 'drizzle-orm'
import type { Watch, EraSignal } from '@/lib/types'

// existing signatures (do NOT modify):
export async function getWatchById(userId: string, watchId: string): Promise<Watch | null>
export async function getWatchByIdForViewer(viewerId: string, watchId: string):
  Promise<{ watch: Watch; isOwner: boolean; ownerUserId: string } | null>
```

Function to EXTRACT (source: src/app/catalog/[catalogId]/page.tsx:286-304 — uses `watches as watchesTable` THERE; must use plain `watches` in watches.ts):
```typescript
export async function findViewerWatchByCatalogId(
  userId: string,
  catalogId: string,
): Promise<{ id: string } | null>
// queries watches WHERE userId AND catalogId AND status='owned' (BUG-01 fix), .limit(1)
```

vitest config (tests/**/*.test.ts auto-discovered; `server-only` aliased to a shim so DAL is unit-testable under jsdom):
```
include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx', 'src/**/*.test.ts', 'src/**/*.test.tsx']
exclude: ['tests/e2e/**', ...]
```

Current package.json build script (line 7): `"build": "next build"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract findViewerWatchByCatalogId into the DAL</name>
  <read_first>
    - src/data/watches.ts (the destination file; read the header imports lines 1-8 and getWatchByIdForViewer ~193-235 for placement and the existing DAL pattern)
    - src/app/catalog/[catalogId]/page.tsx (lines ~286-304 — the function being moved, AND line ~112 to confirm the redirect that Plan 03 deletes; do not touch line 112 here)
    - .planning/phases/59-unified-route-variant-c/59-PATTERNS.md (section "src/data/watches.ts — add findViewerWatchByCatalogId" — the verbatim function body and the watchesTable→watches rename note)
  </read_first>
  <action>
    Add `findViewerWatchByCatalogId(userId: string, catalogId: string): Promise<{ id: string } | null>` to `src/data/watches.ts`, placed immediately after `getWatchByIdForViewer` (~line 235). Copy the function body from `src/app/catalog/[catalogId]/page.tsx:286-304` VERBATIM, with one mechanical change: the catalog page aliases the table as `watches as watchesTable`; in `watches.ts` the table is imported as `watches`, so use `watches` (not `watchesTable`). PRESERVE the BUG-01 fix line `eq(watches.status, 'owned')` exactly — this is the line that makes only truly-owned rows count (per D-06; without it, sold/wishlist rows would falsely trigger the owned render). The function depends only on `db`, `watches`, `and`, `eq` — all already imported in the DAL file; add NO new imports. Keep the existing JSDoc explaining the userId+catalogId scoping (IDOR mitigation). Do NOT remove the function from the catalog page in this task — Plan 03 deletes that whole file.
  </action>
  <verify>
    <automated>grep -n "findViewerWatchByCatalogId" src/data/watches.ts && grep -n "eq(watches.status, 'owned')" src/data/watches.ts</automated>
  </verify>
  <acceptance_criteria>
    - `findViewerWatchByCatalogId` is defined and exported in `src/data/watches.ts`
    - The function body in `src/data/watches.ts` contains `eq(watches.status, 'owned')` (BUG-01 fix preserved; D-06)
    - The function uses `watches` (not `watchesTable`) as the table identifier
    - No new imports added to the top of `src/data/watches.ts` (db, watches, and, eq already present)
    - `npx tsc --noEmit` reports no new errors attributable to watches.ts
  </acceptance_criteria>
  <done>findViewerWatchByCatalogId callable from @/data/watches with the owned-status filter intact; catalog page file untouched (deleted later in Plan 03).</done>
</task>

<task type="auto">
  <name>Task 2: Author the CI guard test + route-absence assertions</name>
  <read_first>
    - tests/no-evaluate-route.test.ts (the existsSync route-absence analog)
    - tests/static/CollectionFitCard.no-engine.test.ts (the readFileSync + regex source-scan analog)
    - tests/profile-route-51.test.ts (multi-file readFileSync + resolve(process.cwd()) analog)
    - .planning/phases/59-unified-route-variant-c/59-RESEARCH.md (sections "CI Guard Mechanism" and "CI Guard Test Skeleton" — FORBIDDEN/ALLOWLIST regex sets and the comment-line nuance)
    - .planning/phases/59-unified-route-variant-c/59-PATTERNS.md (section "tests/static/legacy-watch-routes.test.ts" — the three-analog combination)
  </read_first>
  <action>
    Create `tests/static/legacy-watch-routes.test.ts` with two `describe` blocks. Block A (ROUTE-02 route-absence, mirrors `tests/no-evaluate-route.test.ts`): three `existsSync` assertions that `src/app/watch/[id]/page.tsx`, `src/app/watch/[id]/edit/page.tsx`, and `src/app/catalog/[catalogId]/page.tsx` do NOT exist (each `.toBe(false)`). These will be RED until Plan 03 deletes the files — that is expected and correct. Block B (ROUTE-03 link-literal scan): implement `collectSourceFiles(dir)` recursing `src/` via `readdirSync`+`statSync` from `node:fs`, collecting `.ts`/`.tsx` files and excluding any path containing `.test.`. Define the FORBIDDEN regex array (template literal `` `/watch/${ `` for detail, `` `/watch/${...}/edit `` for edit, `` `/catalog/${ ``, `return \`/watch/${` for computed deep-links per D-12, and `router.push(\`/watch/${` for the NotesEmptyOwnerActions case) and the ALLOWLIST regex array (`/watch/new` per D-13, path-segment forms `/watch/[id]` and `/catalog/[catalogId]` in comments/docs, pure comment lines `^\s*//`, JSDoc `^\s*\*`). Per source file, iterate lines; skip allowlisted lines; assert no FORBIDDEN pattern matches, with a failure message embedding `file:line — label: lineContent`. Title the describe blocks with the literal strings `ROUTE-02` and `ROUTE-03` so the validation map can grep them. The guard must NOT false-flag `/explore/lists/`, `/admin/lists/`, `/wear/[id]` — those contain `lists/`/`wear/`, not `watch/`/`catalog/`, so the path regexes already exclude them; add a focused assertion-by-construction note in a comment.
  </action>
  <verify>
    <automated>test -f tests/static/legacy-watch-routes.test.ts && grep -q "ROUTE-03" tests/static/legacy-watch-routes.test.ts && grep -q "ROUTE-02" tests/static/legacy-watch-routes.test.ts && grep -q "watch/new" tests/static/legacy-watch-routes.test.ts && grep -q "return.*watch" tests/static/legacy-watch-routes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/static/legacy-watch-routes.test.ts` exists with describe blocks titled containing `ROUTE-02` and `ROUTE-03`
    - The FORBIDDEN set includes a pattern matching `return \`/watch/${` (D-12 computed deep-link) and a pattern for `/catalog/${` template literals
    - The ALLOWLIST set includes `/watch/new` (D-13) and pure-comment-line exclusions
    - `npm run test -- tests/static/legacy-watch-routes.test.ts` FAILS at this point (legacy literals still present — expected RED; the gate is proven to detect, not yet satisfied)
    - The guard does NOT report `/explore/lists/`, `/admin/lists/`, or `/wear/[id]` lines as violations (verifiable from the failure output naming only `/watch/` and `/catalog/` sites)
  </acceptance_criteria>
  <done>Guard test detects all legacy literals (RED now, by design) and route-absence assertions are wired; allowlist verified by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold the ROUTE-01 resolution integration test + wire prebuild hook</name>
  <read_first>
    - tests/integration/phase12-visibility-matrix.test.ts (the DAL privacy-gate integration analog — shows the integration test harness and how getWatchByIdForViewer is exercised across owner/non-owner/private cases)
    - src/data/watches.ts (getWatchByIdForViewer + getWatchById + the new findViewerWatchByCatalogId — the three resolvers the unified page composes)
    - src/data/catalog.ts (getCatalogById ~254 — the fallback resolver)
    - .planning/phases/59-unified-route-variant-c/59-VALIDATION.md (Per-Task Verification Map ROUTE-01 rows + Wave 0 Requirements — the four resolution behaviors to cover)
    - package.json (line 7 build script + line 11 test script)
  </read_first>
  <behavior>
    - Test: per-user branch — getWatchByIdForViewer(viewer, watchId) returns {watch, isOwner:true} for the owner → framing resolves same-user
    - Test: catalog branch — getWatchByIdForViewer returns null, getCatalogById(catalogId) returns a CatalogEntry → catalog resolution path engaged
    - Test: owned-via-catalog D-06 branch — on the catalog branch, findViewerWatchByCatalogId(viewer, catalogId) returns {id} → full owned view resolution (no redirect), framing same-user
    - Test: cross-user framing — getWatchByIdForViewer returns {isOwner:false} for a public watch viewed by a non-owner → framing cross-user, viewerCanEdit false
  </behavior>
  <action>
    Create `tests/integration/phase59-unified-route.test.ts` asserting the resolution CONTRACT the unified page (Plan 02) must satisfy. Mirror the harness style of `tests/integration/phase12-visibility-matrix.test.ts`. The test exercises the DAL composition the page performs: (1) per-user hit → isOwner true → same-user framing; (2) per-user miss + catalog hit → catalog branch engaged; (3) catalog branch + findViewerWatchByCatalogId hit → owned-in-place (assert it returns a non-null `{id}` for an owned row and null for a non-owned/sold row, exercising the D-06 + BUG-01 contract); (4) cross-user public watch → isOwner false. Where a live DB is unavailable (empty local test DB), guard the DB-backed cases with the same skip/guard pattern the existing integration tests use so the suite does not hard-fail locally; the assertions encode the contract regardless. Title the describe block with the literal `phase59` so the validation map can grep it. Then update `package.json`: add `"prebuild": "vitest run tests/static/legacy-watch-routes.test.ts"` as a script key (D-11 — npm lifecycle hook runs before `next build`, so the guard fails the Vercel build; per RESEARCH this is honored by Vercel's Node build pipeline). Leave the existing `"build": "next build"` unchanged. Note in the SUMMARY that the prebuild→build linkage will be PROVEN (not assumed) by the planted-literal build-fail step in Plan 03.
  </action>
  <verify>
    <automated>test -f tests/integration/phase59-unified-route.test.ts && grep -q "phase59" tests/integration/phase59-unified-route.test.ts && grep -q "\"prebuild\"" package.json && grep -q "vitest run tests/static/legacy-watch-routes.test.ts" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/phase59-unified-route.test.ts` exists, describe block contains `phase59`, and covers the four ROUTE-01 behaviors (per-user, catalog, owned-via-catalog D-06, cross-user)
    - `package.json` contains a `"prebuild"` script equal to `vitest run tests/static/legacy-watch-routes.test.ts`
    - `package.json` `"build"` script is still `next build` (unchanged)
    - `npm run test -- tests/integration/phase59-unified-route.test.ts` runs without a harness/import error (DB-backed cases may skip locally, mirroring existing integration tests)
  </acceptance_criteria>
  <done>ROUTE-01 contract test scaffolded; prebuild hook wired so the guard gates the build (proof deferred to Plan 03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer → DAL (findViewerWatchByCatalogId) | Untrusted `catalogId` + authenticated `userId` cross into a DB query; must never read another user's row |
| build pipeline → source files | The guard reads source content; a missed pattern lets a legacy link survive the cutover |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Information Disclosure | findViewerWatchByCatalogId | mitigate | Query scoped by BOTH `eq(watches.userId, userId)` AND `eq(watches.catalogId, catalogId)` — preserve the existing two-predicate WHERE so a viewer can never resolve another user's watches.id from a colliding catalogId (IDOR). |
| T-59-02 | Elevation of Privilege | findViewerWatchByCatalogId (BUG-01) | mitigate | Preserve `eq(watches.status, 'owned')` — without it a sold/wishlist row falsely triggers the owned-view-in-place render (D-06), exposing owner framing for a non-owned copy. |
| T-59-03 | Tampering | CI guard regex completeness | mitigate | FORBIDDEN set explicitly covers template literals, `/edit` forms, AND computed `return \`/watch/${` deep-links (D-12) so no un-migrated link survives the hard cutover undetected. |
</threat_model>

<verification>
- `npm run test -- tests/static/legacy-watch-routes.test.ts` runs (RED expected — legacy literals still present)
- `npm run test -- tests/integration/phase59-unified-route.test.ts` runs without harness error
- `grep -n "findViewerWatchByCatalogId" src/data/watches.ts` returns the new definition
- `grep -q "\"prebuild\"" package.json` succeeds
- `npx tsc --noEmit` reports no new type errors in src/data/watches.ts
</verification>

<success_criteria>
- findViewerWatchByCatalogId lives in the DAL with the BUG-01 owned-status filter and userId+catalogId scoping intact (D-06)
- The build-failing CI guard exists and currently DETECTS all legacy literals (RED by design until Plan 03)
- The ROUTE-01 resolution-contract integration test is scaffolded covering all four branches
- The prebuild hook is wired so the guard gates `next build`
</success_criteria>

<output>
After completion, create `.planning/phases/59-unified-route-variant-c/59-01-SUMMARY.md`
</output>
