---
phase: 64-detail-page-ia-redesign
plan: 05
type: execute
wave: 4
depends_on: [64-01, 64-02, 64-03, 64-04]
files_modified:
  - src/components/watch/WatchDetailHero.tsx
  - src/app/w/[ref]/page.tsx
  - tests/static/watch-detail-ia-order.test.ts
autonomous: false
gap_closure: true
requirements: [PAGE-01]

must_haves:
  truths:
    - "On a mobile viewport (<1024px) of /w/[ref], the brand+model identifier is the first visible element of the hero (above the carousel)."
    - "On a desktop viewport (≥1024px) of /w/[ref], the hero layout is byte-for-byte identical to today — brand+model lives in the right column, no duplicate heading is visible."
    - "Exactly one <h1> renders on the page at every breakpoint (page-level a11y rule)."
    - "Status badge ('owned'/'wishlist'/'grail') remains owner-only (gated by viewerCanEdit) on BOTH the mobile and the desktop renderings — cross-user viewers see no badge at any breakpoint."
    - "WatchPageSkeleton mirrors the new mobile order: brand+model skeleton row visible on mobile above the carousel placeholder; right-column brand+model skeleton visible only at lg+; carousel placeholder shape unchanged."
    - "ref line + SpecsSublabel stay inside the right column at all breakpoints — descriptive metadata sits below the photo on mobile and beside the photo on desktop, exactly as today on desktop."
    - "No CSS order-* utility is used to achieve the reorder (D-07 tab-order parity); the mobile hoist is JSX duplication with responsive visibility (lg:hidden / hidden lg:block) only."
    - "Branch 3 (catalog inline hero at page.tsx:~675) is unchanged."
    - "WatchDetailContextBlock continues to render full-width directly below the hero (page.tsx Branches 1 & 2) at the same DOM position as before this plan."
    - "Build exits 0; all 14 static guard files (≥427 tests) remain GREEN; no new lint warnings."
  artifacts:
    - path: "src/components/watch/WatchDetailHero.tsx"
      provides: "Mobile-only header block (lg:hidden) above the existing grid containing owner-only status Badge + <h1>{brand}</h1> + <p>{model}</p>; the existing right-column brand+model block wrapped in 'hidden lg:block' and its <h1> downgraded to <h2>; ref line + SpecsSublabel remain unconditionally visible in the right column."
      contains: "lg:hidden"
    - path: "src/app/w/[ref]/page.tsx"
      provides: "WatchPageSkeleton (lines 108-127) updated to add an lg:hidden brand+model skeleton row above the existing grid AND to wrap the existing right-column brand+model placeholders with 'hidden lg:block' — visual continuity through cache fill, no content-jump."
      contains: "lg:hidden"
    - path: "tests/static/watch-detail-ia-order.test.ts"
      provides: "New describe block 'PAGE-01 mobile: brand+model hoisted above the grid on mobile' asserting (a) WatchDetailHero contains an lg:hidden block whose content includes both watch.brand and watch.model identifiers, (b) the desktop right-column brand block carries 'hidden lg:block', (c) exactly one <h1> token is rendered in the source (no duplicate h1), (d) no order- utility appears in WatchDetailHero.tsx (D-07)."
      contains: "lg:hidden"
  key_links:
    - from: "src/components/watch/WatchDetailHero.tsx mobile-only block"
      to: "watch.brand + watch.model + viewerCanEdit-gated Badge"
      via: "JSX duplication with lg:hidden wrapper rendered ABOVE the existing `<div className='grid ...'>` at line 159"
      pattern: "lg:hidden"
    - from: "src/components/watch/WatchDetailHero.tsx desktop right-column title block"
      to: "the existing brand <h1>+model <p>+badge"
      via: "wrapped in 'hidden lg:block' so the desktop column hides them on mobile (no duplicate identifier paint)"
      pattern: "hidden lg:block"
    - from: "src/app/w/[ref]/page.tsx WatchPageSkeleton"
      to: "WatchDetailHero new mobile/desktop split"
      via: "mirrored lg:hidden brand+model skeleton row + hidden lg:block on the existing right-column brand placeholders"
      pattern: "lg:hidden"
    - from: "tests/static/watch-detail-ia-order.test.ts new describe block"
      to: "WatchDetailHero.tsx source"
      via: "filesystem readFileSync + activeLineNumbers regex on lg:hidden / hidden lg:block / <h1> / order-"
      pattern: "lg:hidden"
---

<objective>
Close the Phase 64 UAT Test 2 gap: on mobile, the brand+model identifier is currently below the fold on /w/[ref] because the hero grid collapses to a single track and DOM order renders the photo column (~470-580px of carousel + filmstrip) before the title block. Hoist the brand+model `<h1>` + model `<p>` above the carousel on mobile only, via JSX duplication with responsive visibility classes (NOT CSS order- — D-07 forbids it). Keep ref line + SpecsSublabel below the photo on mobile (descriptive metadata, intentional). Mirror the change in WatchPageSkeleton so the loading state matches and the post-cache-fill paint does not jump. Desktop (≥1024px) must remain byte-for-byte identical to today. Add a static guard to prevent regression.

Purpose: A watch detail page's identifier is the page's heading-of-record. Pushing it below the fold on the dominant viewport (mobile) is a hierarchy failure even though the UI-SPEC D-01 mandate of "mobile: single column" is structurally honored. This is a UX polish refinement scoped into Phase 64 by user choice during UAT.

Output: Updated WatchDetailHero.tsx (mobile header block + wrapped desktop title), updated WatchPageSkeleton in page.tsx (mirrored skeleton), and an extended watch-detail-ia-order static guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-detail-page-ia-redesign/64-HUMAN-UAT.md
@.planning/phases/64-detail-page-ia-redesign/64-UI-SPEC.md
@.planning/debug/mobile-title-above-fold.md
@.planning/phases/64-detail-page-ia-redesign/64-02-SUMMARY.md
@.planning/phases/64-detail-page-ia-redesign/64-04-SUMMARY.md
@src/components/watch/WatchDetailHero.tsx
@src/app/w/[ref]/page.tsx
@tests/static/watch-detail-ia-order.test.ts

<interfaces>
<!-- Existing exports/types the executor needs. Extracted from codebase to avoid scavenger-hunt. -->

From src/components/watch/WatchDetailHero.tsx (current shape — the file to edit):
- Default-exported function: `WatchDetailHero(props: WatchDetailHeroProps)`
- `viewerCanEdit?: boolean` (defaults false) — gates the status Badge on the existing right-column title block (lines 215-219)
- Watch fields used in the title block: `watch.brand`, `watch.model`, `watch.reference`, `watch.movement`, `watch.caseSizeMm`, `watch.dialColor`, `watch.status`
- Layout primitive: outer `<div className="grid gap-8 lg:grid-cols-[minmax(0,3fr)_minmax(0,2fr)]">` at line 159; left column = photo (line 167); right column = `<div className="space-y-6 min-w-0">` at line 209
- Existing right-column title block lives at lines 214-233 (the Badge + h1 + model p + ref p + SpecsSublabel sub-tree)
- Imports of interest already present: `Badge` from `@/components/ui/badge`, `SpecsSublabel` from `@/components/watch/SpecsSublabel`

From src/app/w/[ref]/page.tsx WatchPageSkeleton (lines 108-127):
- Server-renderable shell (no `'use client'`, no hooks, no dynamic APIs)
- Imports `Skeleton` (already imported above the function)
- Hero grid: `<div className="grid gap-8 lg:grid-cols-[minmax(0,3fr)_minmax(0,2fr)]">`
- Left placeholder: `<Skeleton className="aspect-square w-full rounded-lg" />`
- Right column placeholders inside `<div className="space-y-4 min-w-0">`:
  - `<Skeleton className="h-7 w-3/4" />` (brand)
  - `<Skeleton className="h-5 w-1/2" />` (model)
  - `<Skeleton className="h-4 w-1/3" />` (spec strip)
  - `<Skeleton className="h-40 w-full rounded-lg" />` (verdict — historical, can stay)
  - `<Skeleton className="h-9 w-24" />` (like)

From tests/static/watch-detail-ia-order.test.ts:
- Uses `// @vitest-environment node` (REQUIRED — MEMORY project_vitest_static_node_env: jsdom externalizes node:fs and breaks Vercel prebuild)
- Helper: `activeLineNumbers(lines, pattern)` — strips //, JSDoc `*`, imports, blank lines BEFORE matching (D-NN gate hygiene)
- Existing describes: 'PAGE-01/02: ...', 'PAGE-03: ... no CommentThread import', 'PAGE-04: ... includes WatchPhotoSection'
- Constants: `PAGE_TSX = src/app/w/[ref]/page.tsx`, `HERO_TSX = src/components/watch/WatchDetailHero.tsx`

Tailwind responsive utilities used:
- `lg:hidden` — visible <1024px (mobile/tablet), hidden ≥1024px
- `hidden lg:block` — hidden <1024px, visible ≥1024px
- D-07 ban: NO `order-first`, `order-last`, `order-1`, `lg:order-*` anywhere in WatchDetailHero.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist mobile brand+model header in WatchDetailHero + mirror in WatchPageSkeleton + extend static guard</name>
  <files>src/components/watch/WatchDetailHero.tsx, src/app/w/[ref]/page.tsx, tests/static/watch-detail-ia-order.test.ts</files>
  <action>
    Per UAT diagnosed gap (Test 2) and debug session `.planning/debug/mobile-title-above-fold.md`, implement Option A — a mobile-only JSX hoist of the brand+model identifier, with responsive-visibility classes only. NO CSS `order-*` (D-07). NO change to WatchPhotoSection. NO change to Branch 3 (catalog inline hero) in page.tsx. NO server-action / API / DB changes.

    Edits in three files, in this order:

    (A) src/components/watch/WatchDetailHero.tsx

    1. Above the existing `<div className="grid gap-8 lg:grid-cols-[minmax(0,3fr)_minmax(0,2fr)]">` at line 159 (i.e. immediately inside the function `return` and BEFORE the grid container), introduce a new top-level wrapper so the function returns a single fragment containing two siblings: the mobile-only header, then the existing grid. Use a React Fragment (`<>...</>`) or wrap both in a single outer `<div>` — Fragment is preferred to avoid altering the rendered DOM tree on desktop.

    2. Inside that Fragment, render a mobile-only header block ABOVE the grid:
       - Wrapper: `<div className="lg:hidden mb-6">` (the `mb-6` provides the vertical breathing room equivalent to the desktop right-column `space-y-6` — adjust to `mb-4` if visual cramping appears, but start with `mb-6`).
       - Inside, in this exact order:
         a. The status Badge — gated by `viewerCanEdit` exactly as on the desktop block: `{viewerCanEdit && (<Badge className="mb-2" variant="outline">{watch.status}</Badge>)}`. The Badge MUST remain owner-only at every breakpoint (commit 95385e9 fix preserved).
         b. The page heading: `<h1 className="font-serif text-3xl sm:text-4xl text-foreground">{watch.brand}</h1>`. This is the canonical `<h1>` for the page.
         c. The model line: `<p className="text-lg sm:text-xl text-muted-foreground">{watch.model}</p>`.
       - Do NOT include the ref line or `<SpecsSublabel/>` in the mobile-only block. Those remain in the right column at all breakpoints (descriptive metadata is intentional on the mobile sub-fold position).

    3. Wrap the EXISTING right-column title sub-tree at lines 214-233 in a `<div className="hidden lg:block">` block so it does not paint a duplicate heading on mobile. Concretely, the existing `<div>` at line 214 that contains the Badge + `<h1>` + model `<p>` + ref `<p>` + `<SpecsSublabel/>` must be split:
       - Outer wrapper changes from `<div>` to `<div className="hidden lg:block">` containing the Badge + `<h1>` + model `<p>` ONLY.
       - Move the ref line `<p>` + `<SpecsSublabel/>` into a sibling block directly below: `<div>` (no responsive class) so they continue to render at every breakpoint inside the right column.
       - Result on desktop (lg+): visually identical to today — Badge + h1 + model + ref + SpecsSublabel stack in the right column with `space-y-6` parent.
       - Result on mobile (<lg): only the ref line + SpecsSublabel render in the right column; the Badge + h1 + model live in the mobile-only header above the grid.

    4. Downgrade the heading in the desktop right-column block from `<h1>` to `<h2>` to preserve "one h1 per page". The mobile-only block now holds the sole `<h1>`. Keep typography classes identical (`font-serif text-3xl sm:text-4xl text-foreground`) — `<h2>` styled identically reads the same visually on desktop. Per UAT recommendation (option a in the gap fix direction): h1 in mobile block, h2 on desktop, so the page heading is above the fold where it matters most.

    5. Sanity rules — explicitly forbidden:
       - No `order-first`, `order-last`, `order-1`, `order-2`, `lg:order-*`, `flex-col-reverse`, or any CSS-order-changing utility (D-07; the existing static guard will fail if violated).
       - No change to the grid template `lg:grid-cols-[minmax(0,3fr)_minmax(0,2fr)]` (UAT Test 1 fix at 16c3700 must remain intact).
       - No change to `WatchPhotoSection` props or call site (`fill`, `signedPhotos`, etc.).
       - No change to the LikeButton block, owner-action block, last-worn block, flag-as-deal block — those stay in the right column with `space-y-6`.
       - No change to viewerCanEdit gating logic (preserve commit 95385e9).
       - Comments referencing CommentThread must keep using the wording "CommentThread is NOT referenced in this file" — the PAGE-03 guard is a full-content `import.*CommentThread` regex and will trip on any prose containing those two tokens with an `import` between them.

    (B) src/app/w/[ref]/page.tsx — WatchPageSkeleton ONLY (lines 108-127)

    1. Inside `WatchPageSkeleton`'s outer container `<div className="container mx-auto px-4 py-8 max-w-4xl space-y-8" aria-hidden>`, insert a NEW mobile-only header skeleton row immediately BEFORE the existing grid `<div className="grid gap-8 lg:grid-cols-[minmax(0,3fr)_minmax(0,2fr)]">`:
       - Wrapper: `<div className="lg:hidden space-y-2">`
       - Inside, two Skeleton rows mirroring the new mobile header content:
         a. `<Skeleton className="h-9 w-3/4" />` (brand — taller than the right-column skeleton to match `text-3xl sm:text-4xl`)
         b. `<Skeleton className="h-6 w-1/2" />` (model — to match `text-lg sm:text-xl`)
       - Optional badge placeholder is NOT included (owner-state is RSC-resolved; skeleton paints the pre-auth shape only).

    2. Wrap the EXISTING right-column brand+model skeleton rows in a `hidden lg:block` wrapper so they only paint on desktop, mirroring WatchDetailHero. Specifically:
       - Existing right column: `<div className="space-y-4 min-w-0">` at line 114 contains 5 Skeletons. Wrap the FIRST TWO (`h-7 w-3/4` brand + `h-5 w-1/2` model) in a `<div className="hidden lg:block space-y-2">` sub-block so they hide on mobile.
       - The remaining 3 Skeletons (`h-4 w-1/3` spec strip, `h-40 w-full rounded-lg` verdict, `h-9 w-24` like) stay at every breakpoint inside `space-y-4 min-w-0` — they correspond to content that is unconditionally rendered in the right column at every breakpoint (SpecsSublabel + reserved like row).
       - Net effect: on mobile the right column now shows ONLY spec strip + verdict-card + like; the brand+model paints in the new mobile-only header above; on desktop the right column shows all 5 placeholders exactly as today.

    3. Do NOT change the grid template (`grid gap-8 lg:grid-cols-[minmax(0,3fr)_minmax(0,2fr)]`), the comment skeleton, or the spec-cards skeleton (`h-32 w-full rounded-lg` and `h-40 w-full rounded-lg` below the grid).

    4. WatchPageSkeleton invariants (preserve — these are PPR-critical, see MEMORY project_ppr_dynamic_before_use_cache and Phase 51/52 history):
       - No `'use client'`.
       - No hooks, no dynamic API access (cookies / headers / params / connection()).
       - Pure JSX returning Skeletons.

    5. Do NOT touch any other code in page.tsx — only the WatchPageSkeleton function body. Branches 1 / 2 / 3 are untouched (they render WatchDetailHero, which carries the responsive split internally).

    (C) tests/static/watch-detail-ia-order.test.ts

    Append a NEW `describe` block at the END of the file, matching the file's existing pattern (uses `readFileSync(HERO_TSX, 'utf8')`, the `activeLineNumbers` helper, and the `// @vitest-environment node` already declared at the top of the file — do NOT add a second directive):

    Block name: `'PAGE-01 mobile: brand+model hoisted above the grid on mobile (gap closure 64-05)'`

    Tests inside (each `it(...)` is independent):

    1. `it('WatchDetailHero.tsx contains an lg:hidden mobile header block', () => {...})`
       - Read HERO_TSX. Use `expect(content).toMatch(/className="lg:hidden/)` to assert the mobile-only wrapper exists.
       - If file is empty / unreadable, `expect.fail('WatchDetailHero.tsx not found')` mirroring the existing block style.

    2. `it('WatchDetailHero.tsx contains a hidden lg:block wrapper around desktop title', () => {...})`
       - `expect(content).toMatch(/className="hidden lg:block/)`.

    3. `it('WatchDetailHero.tsx renders exactly one <h1>', () => {...})`
       - Split content into lines; use `activeLineNumbers(lines, /<h1[\s>]/)` to skip comments/JSDoc/imports.
       - `expect(activeLineNumbers(lines, /<h1[\s>]/).length).toBe(1)`.
       - This enforces the a11y rule that only the mobile-block heading is an h1; the desktop right-column heading was downgraded to h2.

    4. `it('WatchDetailHero.tsx contains no CSS order utilities (D-07)', () => {...})`
       - Read content. Use `activeLineNumbers(lines, /\border-(first|last|none|\d|lg:order)/)` to count active-code matches.
       - `expect(activeLineNumbers(lines, /\border-(first|last|none|\d)/).length).toBe(0)`.
       - This codifies the D-07 ban so a future contributor cannot quietly use `order-first`/`lg:order-*` to achieve the reorder.

    5. `it('WatchDetailHero.tsx mobile block contains both watch.brand and watch.model identifiers', () => {...})`
       - Read content. Find the `lg:hidden` wrapper span (use a regex like `/<div className="lg:hidden[^]*?<\/div>/` or string indexOf to extract the substring from the first `lg:hidden` opening up to a reasonable closing).
       - Within that substring, assert it contains BOTH `watch.brand` and `watch.model` token strings — proves the identifier is actually inside the mobile-only wrapper, not just declared.
       - If the regex match approach is too brittle for executor, the simpler form `expect(content).toMatch(/lg:hidden[\s\S]*?watch\.brand[\s\S]*?watch\.model/)` is acceptable.

    6. `it('WatchDetailHero.tsx mobile block status Badge is owner-only (viewerCanEdit gate preserved)', () => {...})`
       - `expect(content).toMatch(/lg:hidden[\s\S]*?viewerCanEdit\s*&&\s*\(\s*<Badge/)` — proves the mobile Badge stays gated like the desktop one. Preserves commit 95385e9 invariant at every breakpoint.

    Constraints on the new test block:
    - Use ONLY the patterns already in the file (readFileSync, activeLineNumbers, expect.fail-on-empty). Do not import new test utilities.
    - Do NOT add `// @vitest-environment node` again — it is already declared at the top of the file (one directive per file).
    - Do NOT touch the existing 3 describe blocks (PAGE-01/02, PAGE-03, PAGE-04) — append only.

    Deviation handling (executor: read carefully): if you discover the grid container is wrapped in a different way than indicated by the read (e.g. an additional outer div was added since the UAT diagnosis), preserve that wrapper and insert the mobile-only header as the FIRST child inside it; do not rewrite the surrounding tree. The mobile block ALWAYS goes before the grid container in DOM order.
  </action>
  <verify>
    <automated>
      cd /Users/tylerwaneka/Documents/horlo &amp;&amp; \
      npx vitest run tests/static/watch-detail-ia-order.test.ts --reporter=default &amp;&amp; \
      npx vitest run tests/static/ --reporter=default &amp;&amp; \
      npm run lint -- --quiet &amp;&amp; \
      npm run build
    </automated>
  </verify>
  <done>
    - `tests/static/watch-detail-ia-order.test.ts` passes with the new describe block (6 new it-cases, all green).
    - All other static guards (`tests/static/*.test.ts`) remain green — total still ≥427 tests.
    - `npm run lint --quiet` exits 0.
    - `npm run build` exits 0 (the authoritative gate per MEMORY project_baseline_not_green_build_is_gate).
    - `src/components/watch/WatchDetailHero.tsx` source: contains exactly one `<h1`, contains `lg:hidden`, contains `hidden lg:block`, contains no `order-(first|last|none|\d)` token in active code, both `watch.brand` and `watch.model` are inside the `lg:hidden` substring, the Badge inside the `lg:hidden` substring is gated by `viewerCanEdit`.
    - `src/app/w/[ref]/page.tsx` WatchPageSkeleton contains a `lg:hidden` Skeleton row above the grid AND a `hidden lg:block` wrapper around the brand+model right-column Skeletons; no other Skeleton code paths were modified; no new `'use client'`, no new hooks, no new dynamic-API calls in the file.
    - Branch 3 (catalog inline hero at ~page.tsx:675) byte-for-byte unchanged.
    - WatchDetailContextBlock import site / render position in Branches 1 &amp; 2 unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Prod human-verify mobile/desktop/cross-user/skeleton/catalog/soft-nav</name>
  <files>(no file edits — verification only)</files>
  <action>
    HOLD for human prod verification. Do NOT proceed past this task without explicit "approved" resume-signal from the user.

    Background context for the user: Task 1 shipped the mobile-only hoist of brand+model `<h1>` above the carousel on /w/[ref] (WatchDetailHero + WatchPageSkeleton mirror), with the desktop right-column title downgraded to `<h2>` and gated `hidden lg:block`; the static guard `tests/static/watch-detail-ia-order.test.ts` was extended to lock in the new structure.

    Per MEMORY feedback_mobile_ui_verify_on_prod, mobile/visual behavior is verified on prod (Vercel), not locally. Build must exit 0 before push (Task 1 already gated on this).

    Executor instructions:
    - Push current `main` to origin (`git push origin main`).
    - Wait for the Vercel deploy to go green.
    - Wait ~1-2 minutes after deploy for the PPR cache to fill (per MEMORY project_ppr_dynamic_before_use_cache — cold reads can falsely look broken).
    - Report to the user: "Pushed; Vercel green; cache filled. Please verify the 7 checks below and reply approved or describe issues."
    - Block.
  </action>
  <verify>
    User performs all 7 checks below and replies. The gap is closed ONLY if every check passes.

    1. **Pre-push gate (Claude already ran this in Task 1; user confirms):**
       - `npm run build` exited 0.
       - `npx vitest run tests/static/` shows all 14 files green, total ≥427 tests pass.

    2. **Push and wait for Vercel:**
       - `git push origin main` completed.
       - Vercel deploy is green (production target).
       - ~1-2 minutes elapsed after deploy for cache fill (per MEMORY project_ppr_dynamic_before_use_cache — verify AFTER cache fills, not on cold read).

    3. **Mobile prod verification (iPhone-class viewport ≤640px wide, e.g. iPhone 14):**
       - Visit any /w/[ref] page you own — e.g. `https://<your-vercel-host>/w/<a-watch-ref-id-you-own>`.
       - Expected above the fold: status Badge (owner-only) + brand `<h1>` + model `<p>`, THEN photo carousel below.
       - Tap into the title area: there should be no awkward focus jump (h1 is non-interactive; tab order: photo controls → page actions).
       - Scroll down: the existing ref line + SpecsSublabel (movement / case size / dial color) should appear below the photo + filmstrip inside the right column (mobile single-column collapsed).
       - Visit the same URL while logged out (or in incognito with another user's /w/[ref]) — confirm the status Badge does NOT appear at the top (owner-only gate preserved).

    4. **Desktop prod verification (≥1024px viewport):**
       - Visit the same /w/[ref] in a wide browser window.
       - Expected: byte-for-byte identical to today — Badge + heading + model + ref + SpecsSublabel in the right column; carousel in the left column. NO duplicate brand+model anywhere on the page.
       - Inspect element (or view source) and count `<h1>` tokens: should be exactly 1.
       - Cross-user view (open someone else's watch detail page): no status Badge anywhere, just the heading + model.

    5. **Loading skeleton check (mobile + desktop):**
       - On mobile: hard-reload a /w/[ref] page and observe the brief loading flash — a small brand+model skeleton row should paint ABOVE the square carousel placeholder. After hydration, the real h1+model should land in the same position (no content jump).
       - On desktop: hard-reload and confirm the brand+model skeleton is in the right column, beside the carousel placeholder (no jump after hydration).

    6. **Catalog branch sanity (Branch 3):**
       - Visit a catalog `/w/[ref]` you DO NOT own (a ref with no rows in your `watches` table). The inline side-by-side thumbnail+title hero should be visually unchanged from today on both mobile and desktop.

    7. **Soft-nav PPR sanity (per MEMORY project_phase_52_in_progress + project_ppr_dynamic_before_use_cache):**
       - Soft-navigate (in-app link click) into a /w/[ref] page from another route after the cache has filled. No React #419/404; page renders.
  </verify>
  <done>
    User replies "approved" — all 7 checks pass. UAT Test 2 flips from `issue` to `pass`. If any check fails, the gap is NOT closed; create a follow-up plan with the failing-check root cause.
  </done>
  <resume-signal>
    Reply with "approved" if all 7 checks pass, OR describe any issues. If any check fails, the gap is NOT closed and a follow-up plan is required.
  </resume-signal>
</task>

</tasks>

<verification>
- Build exits 0 (authoritative gate).
- All static guard files (`tests/static/*.test.ts`) pass; new describe block in `watch-detail-ia-order.test.ts` passes.
- Lint quiet exit 0.
- Prod human verification (mobile + desktop + cross-user + skeleton + catalog branch + soft-nav PPR) passes.
- Desktop renders byte-for-byte identical to pre-plan; mobile shows brand+model above the carousel; exactly one h1 per page.
- Status Badge remains owner-only at every breakpoint.
</verification>

<success_criteria>
- UAT Test 2 ("Mobile single-column collapse — brand+model reachable above the fold") flips from `issue` to `pass` on the next 64-HUMAN-UAT re-test.
- No regression on UAT Tests 1, 3-8 (re-verify a /w/[ref] you own on mobile + desktop + cross-user + catalog).
- Static guard `watch-detail-ia-order.test.ts` now enforces: lg:hidden mobile block exists, hidden lg:block desktop block exists, exactly one h1, no order- utilities (D-07), mobile block contains brand+model, mobile Badge gated by viewerCanEdit.
- Phase 64 advances to status `complete` (4 plans + 1 gap closure plan = 5; all UAT tests pass).
</success_criteria>

<output>
After completion, create `.planning/phases/64-detail-page-ia-redesign/64-05-SUMMARY.md` capturing:
- The mobile-hoist pattern (JSX duplication with `lg:hidden` / `hidden lg:block`, NOT CSS order- per D-07).
- Why `<h1>` lives in the mobile block (above the fold on the dominant viewport) and `<h2>` in the desktop right column (preserves one-h1-per-page).
- The WatchPageSkeleton mirror requirement (post-cache-fill paint must not jump — same `lg:hidden` / `hidden lg:block` split applied to the brand+model placeholders).
- Confirmation that Branch 3 (catalog) was untouched and WatchDetailContextBlock position was unchanged.
- Confirmation of prod human-verify pass (mobile + desktop + cross-user + skeleton + catalog + soft-nav).
- Lessons / reusable patterns (e.g., "Mobile-first identifier hoist via responsive-visibility class duplication is the GSD pattern for mobile DOM-order tweaks when D-07-style tab-order parity bans CSS order-").
</output>
