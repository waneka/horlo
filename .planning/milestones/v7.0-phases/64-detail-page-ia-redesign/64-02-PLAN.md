---
phase: 64-detail-page-ia-redesign
plan: 02
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - src/components/watch/WatchDetailHero.tsx
autonomous: true
requirements: [PAGE-01, PAGE-03, PAGE-04]

must_haves:
  truths:
    - "The hero is a single 'use client' island containing carousel, title, condensed spec strip, elevated verdict, like + jump-to-comments anchor, and owner-only actions"
    - "The carousel (WatchPhotoSection) is the primary visual: left column on desktop, top on mobile (PAGE-04)"
    - "The verdict (CollectionFitCard) is elevated into the hero right column, not at the bottom (D-09)"
    - "Owner-only actions stay gated behind viewerCanEdit (non-owners never see Mark-Worn/Edit/Delete/Flag-deal)"
    - "The hero NEVER imports CommentThread (B1 invariant, PAGE-03)"
    - "Any date rendered in the hero uses toLocaleDateString en-US with timeZone UTC (React #418)"
  artifacts:
    - path: "src/components/watch/WatchDetailHero.tsx"
      provides: "Hero client island for Branches 1 and 2-D06"
      contains: "'use client'"
  key_links:
    - from: "src/components/watch/WatchDetailHero.tsx"
      to: "src/components/comment/CommentThread.tsx (id=comments)"
      via: "anchor href #comments (display-only commentCount number prop, NOT a CommentThread import)"
      pattern: "href=\"#comments\""
    - from: "src/components/watch/WatchDetailHero.tsx"
      to: "src/components/insights/CollectionFitCard.tsx"
      via: "verdict prop rendered in hero right column"
      pattern: "CollectionFitCard"
    - from: "src/components/watch/WatchDetailHero.tsx"
      to: "src/components/watch/WatchPhotoSection.tsx"
      via: "carousel in hero left column"
      pattern: "WatchPhotoSection"
---

<objective>
Create the WatchDetailHero client island by extracting the hero portion of WatchDetail.tsx (lines 1-333) and applying the locked hero composition from the UI-SPEC: a 2-column desktop grid (lg:grid-cols-[3fr_2fr]) with WatchPhotoSection left and title → SpecsSublabel strip → elevated CollectionFitCard verdict → LikeButton + jump-to-comments anchor → owner actions right.

Purpose: This is the verdict-forward, photo-forward hero that PAGE-01/04 require. It is a clean extraction — RESEARCH confirms ALL interactive handlers (handleDelete, handleMarkAsWorn, handleFlagDealChange, useTransition, delete dialog state, router) live in the hero portion; nothing crosses the split boundary.
Output: src/components/watch/WatchDetailHero.tsx (new client island). The page.tsx wiring that consumes it is Plan 04; the trailing RSC is Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-detail-page-ia-redesign/64-CONTEXT.md
@.planning/phases/64-detail-page-ia-redesign/64-PATTERNS.md
@.planning/phases/64-detail-page-ia-redesign/64-UI-SPEC.md

<interfaces>
Analog source (extract hero subset): src/components/watch/WatchDetail.tsx lines 1-333.
  - Imports block: lines 1-34 (copy verbatim; ADD SpecsSublabel + ReferenceIdentityCard imports; do NOT add CommentThread).
  - Handlers: handleDelete (push /), handleMarkAsWorn (router.refresh), handleFlagDealChange (editWatch isFlaggedDeal) — all use useTransition + useRouter; delete uses useState isDeleteDialogOpen.
  - WatchPhotoSection block: lines 179-212.
  - Title/badge/ref block: lines 214-226.
  - Like + comment-count block: lines 228-245 (the MessageCircle count span at 237-243 becomes the jump anchor).
  - Last-worn line: lines 247-266 (uses formatDate — preserve timeZone UTC).
  - Flag-deal checkbox: lines 269-283.
  - Owner actions (Mark Worn / Edit / Delete dialog): lines 287-332.

NEW shared component (Plan 01): import { SpecsSublabel } from '@/components/watch/SpecsSublabel'.
Verdict component: import { CollectionFitCard } from '@/components/insights/CollectionFitCard' (pure renderer, takes a verdict prop).
Empty-state card: import { ReferenceIdentityCard } from '@/components/insights/ReferenceIdentityCard'.
Anchor target: CommentThread renders a section with id="comments" (Plan 01) — the hero references it via href only.

UI-SPEC locked decisions (read 64-UI-SPEC.md Hero Composition + Empty/Gap/Owner-actions sections):
  - Hero grid: grid gap-8 lg:grid-cols-[3fr_2fr]. Breakpoint lg (unchanged).
  - Right-column order: badge → brand h1 (font-serif text-3xl sm:text-4xl) → model (text-lg sm:text-xl) → reference → SpecsSublabel → CollectionFitCard verdict → LikeButton + jump anchor → last-worn (owner) → flag-deal (owner) → owner actions (owner).
  - D-10 empty verdict: verdict null + collection empty + catalogTaste.confidence >= 0.5 → ReferenceIdentityCard in the right column; else the caption "Add a few watches to see how this one fits your collection." (text-sm text-muted-foreground). The fresh-account 3-CTA block does NOT live here (Plan 04 keeps it at page bottom).
  - D-11 gap-fill: stays in WatchDetailTrailing (Plan 03), NOT the hero.
  - D-15 owner actions: stay in the hero right column.
  - Jump anchor: an a element with href #comments, classes inline-flex items-center gap-1 text-sm tabular-nums text-muted-foreground px-2 min-h-[44px] hover:text-foreground transition-colors; MessageCircle size-5 aria-hidden; aria-label "Jump to comments"; sr-only span "comments"; hidden when commentCount is 0.
  - Liked-state token: LikeButton uses text-destructive (no raw text-red palette) — reuse LikeButton as-is, do not restyle.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WatchDetailHero client island</name>
  <files>src/components/watch/WatchDetailHero.tsx</files>
  <behavior>
    - The hero file starts with the 'use client' directive on line 1.
    - It imports WatchPhotoSection, CollectionFitCard, LikeButton, SpecsSublabel, ReferenceIdentityCard, and the watch/wearEvents server actions — and does NOT import CommentThread (the watch-detail-ia-order static guard from Plan 01 asserts no /import.*CommentThread/ and presence of WatchPhotoSection; it turns GREEN here).
    - The root element is a grid with lg:grid-cols-[3fr_2fr].
    - CollectionFitCard renders inside the right column (verdict elevated, D-09).
    - The comment count renders as an a href="#comments" anchor, hidden when commentCount is 0, never importing comment data.
    - Owner UI (Mark Worn / Edit / Delete dialog / flag-deal / last-worn) renders only when viewerCanEdit is true.
    - formatDate uses toLocaleDateString en-US with timeZone UTC.
  </behavior>
  <read_first>
    - src/components/watch/WatchDetail.tsx (full — the source to extract lines 1-333; confirm handler bodies, prop interface, formatDate, the WatchPhotoSection block, and the owner-action gates)
    - .planning/phases/64-detail-page-ia-redesign/64-PATTERNS.md WatchDetailHero.tsx section (imports pattern, handler pattern, new 2-col grid layout pattern, delete dialog pattern, jump anchor pattern; the explicit "do NOT import CommentThread" note; the "props to REMOVE" note)
    - .planning/phases/64-detail-page-ia-redesign/64-UI-SPEC.md Hero Composition, Verdict Placement, Empty verdict state, Owner Actions Placement, Jump-to-Comments Anchor sections
    - src/components/insights/CollectionFitCard.tsx (confirm the verdict prop shape)
    - src/components/insights/ReferenceIdentityCard.tsx (confirm the taste prop shape for the D-10 empty state)
  </read_first>
  <action>
    Create src/components/watch/WatchDetailHero.tsx as a 'use client' island. Copy the imports/hooks/handlers from WatchDetail.tsx lines 1-333. ADD imports for SpecsSublabel (from @/components/watch/SpecsSublabel) and ReferenceIdentityCard. Do NOT import CommentThread (B1 invariant — its commentCount is a plain number prop).
    Define a WatchDetailHeroProps interface holding the hero-relevant subset of the current WatchDetailProps (watch, verdict, viewerCanEdit, viewerId, initialLikeState, commentCount, signedPhotos, userId, wearPics, ownerUserId, ownerUsername, viewerAuthor, canCommentOnWears, ownerFollowsViewerForWears, viewerIsFollowingForWears, lastWornDate; plus collection only if the D-10 empty-state branch needs collection.length and catalogTaste — pass collection so the empty-state condition can read collection.length). Drop gap-fill props (computeGapFill / gapFill belong to Plan 03 trailing).
    Render the 2-column grid grid gap-8 lg:grid-cols-[3fr_2fr]: LEFT column is the WatchPhotoSection block (copy from WatchDetail.tsx 179-212, including its non-photo fallback Image). RIGHT column (space-y-6 or space-y-4 per UI-SPEC) in this order: status Badge → brand h1 (font-serif text-3xl sm:text-4xl) → model (text-lg sm:text-xl) → reference (text-sm text-muted-foreground) → SpecsSublabel (movement / caseSizeMm ?? null / dialColor ?? null) → verdict block → like+jump row → last-worn (owner gate) → flag-deal (owner gate) → owner actions (owner gate).
    Verdict block (D-09 + D-10): if verdict is non-null render CollectionFitCard verdict={verdict}; else if collection.length === 0 and watch.catalogTaste with confidence >= 0.5 render ReferenceIdentityCard taste={watch.catalogTaste}; else render the caption paragraph "Add a few watches to see how this one fits your collection." with class text-sm text-muted-foreground.
    Like + jump row: a flex items-center gap-2 wrapper rendering LikeButton (same props as WatchDetail.tsx 228-245) followed by the jump anchor when commentCount > 0 — an a href="#comments" aria-label="Jump to comments" with the exact UI-SPEC classes, a MessageCircle size-5 aria-hidden icon, the count, and an sr-only span reading "comments".
    Copy the owner-action JSX (Mark Worn, Edit Link, Delete Dialog) verbatim from WatchDetail.tsx 287-332 keeping every viewerCanEdit gate and the isPending disabled states. Copy formatDate from WatchDetail.tsx 106-119 verbatim (keep timeZone UTC) for the last-worn line.
    Do NOT add CSS order tricks. Do NOT render spec cards, gap-fill, or notes (those are Plan 03). Leave WatchDetail.tsx in place for now (Plan 04 swaps the page.tsx usage; the old file may be retired later).
  </action>
  <verify>
    <automated>head -1 src/components/watch/WatchDetailHero.tsx | grep -q "use client" && grep -q "WatchPhotoSection" src/components/watch/WatchDetailHero.tsx && grep -q "CollectionFitCard" src/components/watch/WatchDetailHero.tsx && grep -q 'href="#comments"' src/components/watch/WatchDetailHero.tsx && grep -q "lg:grid-cols-\[3fr_2fr\]" src/components/watch/WatchDetailHero.tsx && grep -q "timeZone: 'UTC'" src/components/watch/WatchDetailHero.tsx && ! grep -qE "import.*CommentThread" src/components/watch/WatchDetailHero.tsx && npx vitest run tests/static/watch-detail-ia-order.test.ts --reporter=basic -t "WatchDetailHero"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/watch/WatchDetailHero.tsx line 1 is the 'use client' directive
    - File contains WatchPhotoSection (PAGE-04), CollectionFitCard (D-09 verdict elevation), and the exact string href="#comments" (D-06 anchor)
    - File contains lg:grid-cols-[3fr_2fr] (UI-SPEC hero grid)
    - File contains timeZone: 'UTC' (React #418 date safety)
    - File does NOT match the regex import.*CommentThread (B1 invariant, PAGE-03)
    - File contains the literal "Add a few watches to see how this one fits your collection." (D-10 caption)
    - Owner-action JSX is gated by viewerCanEdit (grep shows viewerCanEdit guarding Mark Worn / Edit / Delete)
    - The two WatchDetailHero-targeted assertions in tests/static/watch-detail-ia-order.test.ts (no-CommentThread-import + WatchPhotoSection-present) PASS
    - npm run build exits 0 (the new island compiles; it is unreferenced until Plan 04 — that is fine for build)
  </acceptance_criteria>
  <done>WatchDetailHero is a self-contained client island implementing the locked hero composition with the elevated verdict, the photo-forward left column, the jump anchor, owner gates, and #418-safe dates; the hero-targeted IA static assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-owner viewer → owner mutation UI | The hero renders Mark-Worn / Edit / Delete; these must never appear for a non-owner viewer |
| client island import graph → CommentThread privacy | Importing CommentThread into this 'use client' file would convert it to a client component and destroy the comment privacy guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-04 | Spoofing / Elevation of Privilege | WatchDetailHero owner actions | mitigate (preserve existing control) | thread viewerCanEdit into the hero and keep every Mark-Worn/Edit/Delete/Flag-deal block behind the existing viewerCanEdit gate (copied verbatim from WatchDetail.tsx 287-332); the underlying Server Actions (removeWatch, markAsWorn, editWatch) still double-verify ownership server-side (T-RDB-06) |
| T-64-05 | Information Disclosure | WatchDetailHero import graph | mitigate | do NOT import CommentThread; commentCount is a plain number prop; the Plan 01 IA guard asserts no import.*CommentThread in this file |
| T-64-06 | Information Disclosure | IDOR on photo signing | accept (preserve existing control) | the hero only consumes already-signed signedPhotos / wearPics props; no signing logic moves into the client island; admin-client + {userId}/ scoping unchanged |
</threat_model>

<verification>
- `npx vitest run tests/static/watch-detail-ia-order.test.ts --reporter=verbose` → the WatchDetailHero-targeted assertions (no CommentThread import, WatchPhotoSection present) are GREEN; the page.tsx order assertions remain RED until Plan 04.
- `npm run build` → exit 0.
- Responsive 2-col→1-col collapse, desktop hero visual balance, and the empty-verdict states are `human_needed` on prod (MEMORY feedback_mobile_ui_verify_on_prod) — bundled into the Plan 04 deploy.
</verification>

<success_criteria>
- WatchDetailHero island exists with photo-forward left column, elevated verdict + empty states right column, jump anchor, owner gates, #418-safe dates.
- B1 invariant holds (no CommentThread import); hero-targeted static assertions pass; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/64-detail-page-ia-redesign/64-02-SUMMARY.md`
</output>
