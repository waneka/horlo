---
phase: 74
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/watch/ConfirmStep.tsx
  - src/components/watch/AddWatchFlow.tsx
  - src/components/watch/AddWatchFlow.test.tsx
autonomous: true
requirements: [DUPE-04]
requirements_addressed: [DUPE-04]
nyquist_compliant: true
tags: [dupe-banner, confirm-step, gate, regression-test, disappearance-assertion]

must_haves:
  truths:
    - "D-01: when DupeBanner is mounted (state.dupeContext != null), the ConfirmStep primary CTA (Section 6 at ConfirmStep.tsx:311-325) is NOT rendered at all — no Saving... copy, no disabled stub, no placeholder spacer"
    - "D-02: ConfirmStep gains an additive optional prop bannerActive?: boolean (default false) per Phase 68 D-03 additive-extension contract; the gating decision is colocated with the gated <Button> via an early-return null inside Section 6's render block"
    - "D-02: AddWatchFlow.tsx:694 reverts pending={state.pending || state.dupeContext != null} to pending={state.pending} (Phase 68 D-03 pending-semantic purity restored) AND adds bannerActive={state.dupeContext != null} as a new prop on the same ConfirmStep call site"
    - "D-03: the ghost-row buttons (Edit details + Start over at ConfirmStep.tsx:289-308) stay rendered and clickable while bannerActive=true; price/reference/year inputs remain editable; only Section 6's primary <Button> is removed from the DOM"
    - "D-04 + D-10: the 3 existing Phase 70 gap-plan-08 WR-01 tests (Tests A/B/C at AddWatchFlow.test.tsx:749-807) pivot from toBeDisabled() assertions to disappearance assertions (queryByRole / queryByText … not.toBeInTheDocument()) PAIRED with banner-appears assertions on data-testid='dupe-banner-{owned|wishlist}' — triple-assertion pattern per feedback_test_assert_disappearance_too (recurrence-3) and project_baseline_not_green_build_is_gate baseline discipline"
    - "D-04 + D-10: new WR-01 Test D added to the same describe block — when bannerActive=true the ConfirmStep mock primary CTA is absent by construction (queryByText('Confirm primary').not.toBeInTheDocument()) AND addWatch was not called; closes DUPE-04 SC#1 with a pure-absence assertion"
    - "D-05: no visual / DOM-order change — no height-collapsing animation, no aria-live announcement, no placeholder; the banner itself is the announcement surface"
    - "Phase 68 D-03 ConfirmStep prop contract preserved — bannerActive? is additive only; no rename, no removal, no semantic-change of pending which now means real async work in progress only"
    - "Phase 70 D-11 DupeBanner sibling-above-ConfirmStep pattern UNCHANGED — banner still mounts at AddWatchFlow.tsx:660-672 as a sibling above the ConfirmStep block"
    - "Verification: npm run build exits 0 (project_baseline_not_green_build_is_gate — the authoritative gate; NOT tsc --noEmit which carries pre-existing test-file errors; NOT full vitest which has pre-existing CommentGateLocked font-medium failure) AND targeted vitest run AddWatchFlow.test.tsx is green (all 4 WR-01 tests + the existing 21+ other AddWatchFlow tests pass)"
    - "DUPE-04 prod UAT verification = bundled v8.1 deploy walk per D-15 + feedback_mobile_ui_verify_on_prod — local jsdom assertions guarantee structural absence; visual confirmation that the confirm screen feels clean under the banner is human_needed on horlo.app"
    - "font-semibold > font-medium guardrail (project_phase_68_complete recurrence-5) — NO new font-medium className is introduced in any modified file (DUPE-04 changes are not className rewrites but the discipline is universal)"
  artifacts:
    - path: "src/components/watch/ConfirmStep.tsx"
      provides: "Additive bannerActive?: boolean prop + Section 6 primary-CTA early-return null when bannerActive=true"
      contains: "bannerActive?: boolean"
      exports: ["ConfirmStep"]
      min_lines: 320
    - path: "src/components/watch/AddWatchFlow.tsx"
      provides: "Reverted pending semantic + new bannerActive prop wired into the confirming-branch ConfirmStep call site at line 694"
      contains: "bannerActive={state.dupeContext != null}"
    - path: "src/components/watch/AddWatchFlow.test.tsx"
      provides: "3 pivoted WR-01 tests (A/B/C — disappearance-paired) + 1 new WR-01 Test D (absence-by-construction) inside the existing Phase 70 gap-plan-08 describe block at lines 723-808"
      contains: "queryByText('Confirm primary')"
  key_links:
    - from: "src/components/watch/AddWatchFlow.tsx :line 694 ConfirmStep call site"
      to: "src/components/watch/ConfirmStep.tsx :ConfirmStepProps interface + Section 6 (lines 311-325)"
      via: "bannerActive={state.dupeContext != null} prop wired to gate the Section 6 early-return"
      pattern: "bannerActive=\\{state\\.dupeContext != null\\}"
    - from: "src/components/watch/AddWatchFlow.test.tsx :Phase 70 gap-plan-08 WR-01 describe (lines 723-808)"
      to: "src/components/watch/AddWatchFlow.test.tsx :DupeBanner mock testid (lines 210-228)"
      via: "data-testid='dupe-banner-{owned|wishlist}' presence-pair assertions"
      pattern: "queryByTestId\\('dupe-banner-(owned|wishlist)'\\)"
---

<objective>
Close DUPE-04: when the DupeBanner is mounted on the confirm screen, the ConfirmStep primary CTA currently renders `<Loader2 spinning/> Saving...` (gated via `pending={state.pending || state.dupeContext != null}` from Phase 70 gap plan 08 at AddWatchFlow.tsx:694, propagating to ConfirmStep.tsx:317-321). The "Saving..." copy on a non-saving button is what the user reported as broken on the v8.0 prod UAT walk (verbatim in 70-UAT.md Gaps).

Per CONTEXT.md D-01, the fix is **hide the primary CTA entirely** when `bannerActive=true` (NOT distinct copy, NOT a disabled stub). The DupeBanner IS the choice surface; the redundant gated CTA is noise. Per CONTEXT.md D-02 the prop surface uses an additive `bannerActive?: boolean` on ConfirmStep (Phase 68 D-03 prop contract allows additive extensions) wired from AddWatchFlow.tsx:694; the OR-combined `pending` reverts to original Phase 68 pending-semantic purity.

Per CONTEXT.md D-03 the ghost row (Edit details / Start over) stays enabled; price/reference/year inputs stay editable; only Section 6's primary `<Button>` is removed. Per CONTEXT.md D-05 no visual / DOM order change — early-return null only.

Per CONTEXT.md D-04 + D-10, the 3 existing Phase 70 gap-plan-08 WR-01 tests in AddWatchFlow.test.tsx (Tests A/B/C at lines 749-807) pivot from `toBeDisabled()` to disappearance assertions paired with banner-appears assertions on `data-testid='dupe-banner-{owned|wishlist}'` (triple-assertion per `feedback_test_assert_disappearance_too` recurrence-3 — the same discipline shipped in Phase 73 Plan 01). A new WR-01 Test D is added to the same describe block to close DUPE-04 SC#1 with a pure-absence assertion.

Output: 1-line addition to ConfirmStep props + ~5-line early-return inside Section 6 + 2-line edit at AddWatchFlow.tsx:694 + 3 pivoted tests + 1 new test, across 3 files.

Purpose: User sees a clean DupeBanner surface without a competing "Saving..." button that is not actually saving. ConfirmStep stays mounted (no flicker, no input re-mount, no radiogroup focus state loss per project_phase_68_complete WAI-ARIA pattern); only Section 6's primary CTA disappears.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-dupebanner-gate-refinement-mobile-polish/74-CONTEXT.md

<!-- Cross-phase locked contracts the executor MUST honor -->
@.planning/milestones/v8.0-phases/68-confirmstep-component/68-CONTEXT.md
@.planning/milestones/v8.0-phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-CONTEXT.md
@.planning/milestones/v8.0-phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-VERIFICATION.md
@.planning/milestones/v8.0-phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-UAT.md

<!-- Source files modified -->
@src/components/watch/ConfirmStep.tsx
@src/components/watch/AddWatchFlow.tsx
@src/components/watch/AddWatchFlow.test.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from current source. -->

From src/components/watch/ConfirmStep.tsx (props interface around lines 55-98):
- ConfirmStepProps has an optional `pending?: boolean` flag (default false via destructure at line 118 `pending = false`).
- Section 6 primary CTA is a single contiguous <Button> block at lines 311-325 inside the component return.
- `pending` currently double-duty gates: (a) `disabled={pending}` on the Button at line 314, and (b) selects between `<Loader2 spinning/> Saving...` (line 317-321) and `CTA_LABELS[status]` (line 323) for the label.
- After this plan: ConfirmStepProps gains optional `bannerActive?: boolean` (default false). Section 6 early-returns null when bannerActive=true. Pending reverts to original meaning (real async work).

From src/components/watch/AddWatchFlow.tsx (ConfirmStep call site around line 660-700):
- ConfirmStep is rendered inside the `state.kind === 'confirming'` branch as a sibling BELOW the DupeBanner sibling (Phase 70 D-11).
- Current line 694: `pending={state.pending || state.dupeContext != null}` (Phase 70 gap plan 08 WR-01 fix being reverted).
- After this plan: line 694 becomes `pending={state.pending}` + a new adjacent line `bannerActive={state.dupeContext != null}`.

From src/components/watch/AddWatchFlow.test.tsx (mocks around lines 195-230):
- ConfirmStep mock renders `<button onClick={onPrimary} disabled={pending}>Confirm primary</button>` (line 201-203). Mock currently keys off `pending` only.
- DupeBanner mock renders `<div data-testid={`dupe-banner-${existingStatus}`}>` (line 222) — the testid the disappearance-pair assertions in D-04 consume.
- The ConfirmStep mock must be updated to ALSO honor a new `bannerActive` prop — early-return null at the mock level when bannerActive=true — so the disappearance assertions actually observe the disappearance in jsdom.

From src/components/watch/AddWatchFlow.test.tsx (existing WR-01 describe at lines 723-807):
- Test A (line 749): wishlist dupeContext — asserts `expect(screen.getByText('Confirm primary')).toBeDisabled()`. Pivots to disappearance + banner-appears.
- Test B (line 765): owned dupeContext via structured-submit — asserts `toBeDisabled()` then attempts click and verifies `addWatch` not called. Pivots to disappearance + banner-appears + click-cannot-fire-because-button-absent.
- Test C (line 784): "Add another copy" round-trip — asserts disabled→enabled. Pivots to disappearance→appearance round-trip.
- New Test D (added): pure absence-by-construction — bannerActive=true means no Confirm primary in DOM, no addWatch call possible.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bannerActive prop to ConfirmStep + early-return null on Section 6</name>
  <read_first>
    - src/components/watch/ConfirmStep.tsx (full file — props interface at lines 53-98, destructure at line 102-122, Section 6 at lines 310-325)
    - .planning/phases/74-dupebanner-gate-refinement-mobile-polish/74-CONTEXT.md (D-01, D-02, D-03, D-05)
    - .planning/milestones/v8.0-phases/68-confirmstep-component/68-CONTEXT.md (D-03 LOCKED prop contract — additive extensions are explicitly allowed; renames/breakage forbidden)
  </read_first>
  <files>src/components/watch/ConfirmStep.tsx</files>
  <action>
    Apply two surgical edits to ConfirmStep.tsx per CONTEXT D-02 + D-01 + D-05:

    (1) ConfirmStepProps interface (currently lines 53-98). After the `pending?: boolean` line (currently line 92-93) — appending keeps related gate flags adjacent per CONTEXT Claude's-Discretion guidance — add a new optional boolean prop named `bannerActive` typed `boolean` with a JSDoc one-liner explaining it: when true, the Section 6 primary CTA is hidden because a DupeBanner is mounted as a sibling above ConfirmStep (Phase 70 D-11) and that banner is the user-action surface. Default false. DO NOT rename, remove, or reorder any existing field; this is an ADDITIVE extension per Phase 68 D-03 LOCKED contract.

    (2) Component destructure (currently around line 102-122). Add `bannerActive = false` to the destructure list alongside `pending = false`. Place adjacent to `pending` for readability.

    (3) Section 6 primary CTA block (currently lines 310-325 — the `{/* Section 6: Primary CTA */}` comment + the `<Button type="button" onClick={onPrimary} ...>` block). Wrap the entire Section 6 (comment + Button) in a conditional that returns nothing when `bannerActive` is true. Idiomatic JSX form: `{!bannerActive && ( <> {/* Section 6 ... */} <Button ...>...</Button> </> )}` (a fragment around the comment + Button). The CTA Button is THE ONLY element removed from the DOM. Per CONTEXT D-03 + D-05: Section 5 ghost row (Edit details / Start over) at lines 288-308 stays mounted and clickable; no placeholder, no spacer, no aria-live, no animation, no DOM-order change for any other section.

    Do NOT touch: Section 1 (cover photo), Section 2 (specHeadline), Section 3 (reference / year inputs), Section 4 (status radiogroup at lines with `groupRef` / `handleKeyDown` — preserving Phase 68 D-03 WAI-ARIA roving-tabindex), Section 5 ghost row, or the `pending` prop's existing wiring on the inputs/ghost-buttons (Section 3 input `disabled={pending}`, Section 5 ghost buttons `disabled={pending}` at lines 294/303). Per CONTEXT D-03, `pending` reverts to original Phase 68 semantics (real async work only) once Task 2 updates the AddWatchFlow call site.

    Do NOT introduce any `font-medium` className (project_phase_68_complete recurrence-5 guardrail). The CTA Button uses Button-primitive defaults; this task does not touch its className.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "bannerActive" src/components/watch/ConfirmStep.tsx | wc -l` returns at least 3 (interface declaration, destructure with default, conditional in Section 6)
    - `grep -c "pending?: boolean" src/components/watch/ConfirmStep.tsx` returns 1 (the existing optional prop, unchanged)
    - `grep -c "bannerActive?: boolean" src/components/watch/ConfirmStep.tsx` returns 1 (new additive prop)
    - `grep -c "bannerActive = false" src/components/watch/ConfirmStep.tsx` returns 1 (destructure default)
    - `grep -c "!bannerActive" src/components/watch/ConfirmStep.tsx` returns at least 1 (Section 6 gate)
    - Section 6 primary CTA `<Button>` block still exists in the file (`grep -c "Section 6" src/components/watch/ConfirmStep.tsx` returns 1) but is now wrapped in a `!bannerActive` conditional
    - `grep -c "font-medium" src/components/watch/ConfirmStep.tsx` returns 0 (font-semibold > font-medium guardrail; recurrence-5 — Button-primitive defaults are fine inside the primitive but no className overrides introduce font-medium)
    - `npm run build` exits 0 (project_baseline_not_green_build_is_gate — the authoritative gate)
  </acceptance_criteria>
  <done>
    ConfirmStep has a new additive `bannerActive?: boolean` prop, defaults to false, and when true the Section 6 primary CTA does not render. All other sections render unchanged. Build exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Revert pending semantic at AddWatchFlow.tsx:694 + wire new bannerActive prop</name>
  <read_first>
    - src/components/watch/AddWatchFlow.tsx (current ConfirmStep call site at lines 660-700, especially the prop block at lines 680-697)
    - .planning/phases/74-dupebanner-gate-refinement-mobile-polish/74-CONTEXT.md (D-02, D-03, D-04)
    - .planning/milestones/v8.0-phases/70-addwatchflow-state-machine-rewrite-dupe-wiring/70-CONTEXT.md (D-11 — DupeBanner sibling-above-ConfirmStep pattern UNCHANGED)
  </read_first>
  <files>src/components/watch/AddWatchFlow.tsx</files>
  <action>
    Surgical 2-line edit at the confirming-branch ConfirmStep call site (currently around lines 680-697 — find the `pending={state.pending || state.dupeContext != null}` line at line 694).

    (1) Revert the OR-combined pending: change `pending={state.pending || state.dupeContext != null}` back to `pending={state.pending}` (original Phase 68 D-03 pending-semantic purity — pending now means real async work in progress only, per CONTEXT D-02).

    (2) Add an adjacent new prop on the same ConfirmStep element: `bannerActive={state.dupeContext != null}`. Place it next to the `pending` prop for adjacency (Phase 68-D-03-style additive prop wiring). The full line block in the JSX should read (paraphrased, exact whitespace preserved): pending prop on one line followed by bannerActive prop on the next line, ordered to mirror the ConfirmStep destructure order from Task 1.

    (3) Update / replace the inline comment block currently at lines 689-693 (which explains the Phase 70 gap plan 08 WR-01 OR-combined gate). The new comment must explain: "When DupeBanner is mounted (state.dupeContext != null), bannerActive flips true and ConfirmStep hides its Section 6 primary CTA entirely per Phase 74 D-01 — the banner IS the user-action surface. Pending stays its original Phase 68 D-03 meaning (real async work) so ghost-row buttons still gate correctly during the moveWishlistToCollection await." Keep it concise; the comment is documentation, not a contract. Per CONTEXT and feedback_decision_coverage_gate_citations the comment is not the source of truth (the truths/must_haves block is).

    Do NOT touch: any other ConfirmStep prop (catalogImageUrl, extractedImageUrl, brand, model, reference, onReferenceChange, productionYear, onProductionYearChange, status, onStatusChange, price, onPriceChange, onPrimary, onEditDetails, onStartOver, movement, caseSizeMm, dialColor). Do NOT touch the DupeBanner sibling block at lines 660-672 — Phase 70 D-11 sibling pattern is LOCKED. Do NOT touch any handler function (handleConfirmPrimary, handleConfirmEditDetails, handleConfirmStartOver, handleSearchPick, handleStructuredSubmit, handleUrlBackup, handleAddAnotherCopy, handleViewExisting, handleMoveToCollection) — Phase 70 contracts intact.

    Verify no other call sites of ConfirmStep exist in the file (`grep -n "ConfirmStep" src/components/watch/AddWatchFlow.tsx` should show only the one confirming-branch call site at line ~673; the import at the top is the only other hit).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pending={state.pending || state.dupeContext" src/components/watch/AddWatchFlow.tsx` returns 0 (the OR-combined gate is removed)
    - `grep -c "pending={state.pending}" src/components/watch/AddWatchFlow.tsx` returns 1 (the reverted pure pending prop)
    - `grep -c "bannerActive={state.dupeContext != null}" src/components/watch/AddWatchFlow.tsx` returns 1 (the new gate signal)
    - The two new lines (`pending={state.pending}` and `bannerActive={state.dupeContext != null}`) are adjacent inside the same ConfirmStep JSX element block
    - The DupeBanner sibling block at lines 660-672 is UNCHANGED (the `state.dupeContext &&` conditional render is intact; no removal, no edit)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    AddWatchFlow.tsx wires the new bannerActive prop from `state.dupeContext != null` and reverts pending to its original semantic. DupeBanner sibling pattern unchanged. Build exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pivot 3 WR-01 tests to disappearance-paired assertions + add WR-01 Test D</name>
  <read_first>
    - src/components/watch/AddWatchFlow.test.tsx (ConfirmStep mock at lines 195-208, DupeBanner mock at lines 210-228, existing WR-01 describe at lines 723-808)
    - .planning/phases/74-dupebanner-gate-refinement-mobile-polish/74-CONTEXT.md (D-04, D-10, Claude's Discretion on WR-01 Test D phrasing)
    - .planning/phases/73-owned-redirect-route-fix/73-01-PLAN.md (recurrence-2 disappearance-pair example — for the triple-assertion form)
  </read_first>
  <files>src/components/watch/AddWatchFlow.test.tsx</files>
  <behavior>
    After Task 1 + Task 2 ship, the existing 4 WR-01 tests must encode the new contract:

    - WR-01 Test A (wishlist dupeContext) — banner-wishlist appears AND Confirm primary disappears AND attempting a click on the absent button cannot fire addWatch (which it can't by construction; attempt is included only for symmetry — covered explicitly in Test B and Test D).
    - WR-01 Test B (owned dupeContext via structured-submit) — banner-owned appears AND Confirm primary disappears AND a click attempt does not fire addWatch.
    - WR-01 Test C (Add another copy round-trip) — initial: banner-owned appears + Confirm primary absent. After "Add another copy" click: banner-owned unmounts + Confirm primary reappears + clicking the now-mounted Confirm primary fires addWatch.
    - NEW WR-01 Test D (DUPE-04 absence-by-construction) — Submit structured → bannerActive flips true → Confirm primary is not in the DOM (queryByText returns null) AND addWatch was NOT called. Pure-absence assertion; no click attempt needed (planner's discretion per CONTEXT). Distinct from Test B in that it does not even attempt to click the missing CTA — it just confirms the absence is the contract.

    The ConfirmStep mock at lines 195-208 must be updated to honor the new `bannerActive` prop: when bannerActive is true, the mock returns null for the Confirm primary button (or returns a wrapper div without the button) — mirrors the production component's Section 6 early-return null per Task 1. The mock's other buttons (Start over, Edit details) and the data-status attribute stay rendered (mirrors CONTEXT D-03 — ghost row stays mounted).
  </behavior>
  <action>
    Apply edits to AddWatchFlow.test.tsx in three blocks:

    (1) ConfirmStep mock at lines 195-208. Extend the destructured props to include `bannerActive` (typed `boolean | undefined` in the inline type-annotation literal). Inside the mock body, add an early conditional: when `bannerActive === true`, render the same outer `<div data-testid="confirm-step" data-status={status}>` but OMIT the `<button onClick={onPrimary}>Confirm primary</button>`. Keep Start over + Edit details ghost buttons rendered (matches CONTEXT D-03). Mirror the production component's behavior so jsdom can observe the disappearance.

    (2) Three existing WR-01 tests in the Phase 70 gap-plan-08 describe (currently lines 723-808):

    - Test A (currently line 749, `WR-01 — ConfirmStep primary CTA is disabled when wishlist dupeContext is set`): rename the it-string from `... is disabled ...` to `... is NOT rendered ...`. Replace `expect(screen.getByText('Confirm primary')).toBeDisabled()` with `expect(screen.queryByText('Confirm primary')).not.toBeInTheDocument()`. Keep the existing `expect(await screen.findByTestId('dupe-banner-wishlist')).toBeInTheDocument()` line — this is the banner-appears half of the disappearance-pair (triple-assertion form per feedback_test_assert_disappearance_too).

    - Test B (currently line 765, `... owned dupeContext is set (via structured-submit); clicking does NOT call addWatch`): replace `expect(screen.getByText('Confirm primary')).toBeDisabled()` with `expect(screen.queryByText('Confirm primary')).not.toBeInTheDocument()`. The subsequent `fireEvent.click(screen.getByText('Confirm primary'))` line at the current ~line 776 must be REMOVED (the button no longer exists; getByText would throw). Replace with a comment explaining that the absence-by-construction makes the click impossible, and KEEP the final `expect(vi.mocked(addWatch)).not.toHaveBeenCalled()` assertion (still meaningful — proves nothing else fired addWatch in this code path). Rename the it-string from `... is disabled ...` to `... is NOT rendered ...`. Keep the banner-appears `findByTestId('dupe-banner-owned')` line.

    - Test C (currently line 784, `clicking "Add another copy" clears dupeContext + re-enables ConfirmStep primary; subsequent click calls addWatch`): rename the it-string from `... re-enables ...` to `... re-mounts ...`. Initial assertion: change `expect(screen.getByText('Confirm primary')).toBeDisabled()` to `expect(screen.queryByText('Confirm primary')).not.toBeInTheDocument()` AND keep the banner-appears assertion. After-the-click assertion (currently `expect(screen.getByText('Confirm primary')).not.toBeDisabled()` at line 800): change to `expect(screen.getByText('Confirm primary')).toBeInTheDocument()` (the CTA is back in the DOM, and Enabled-ness is the default — the existing addWatch fire assertion at lines 803-806 proves it's clickable).

    (3) Add a NEW Test D inside the same describe block, after Test C. It-name suggestion: `WR-01 (DUPE-04) — bannerActive absence-by-construction: ConfirmStep Section 6 primary is not in the DOM and addWatch is never called`. Body: identical setup to Test B (mock findViewerWatchByCatalogIdAction with an owned dupeRow; renderFlow; fireEvent.click(screen.getByText('Submit structured'))). Then assert:
       - `expect(await screen.findByTestId('dupe-banner-owned')).toBeInTheDocument()` (banner appears — alternative-surface invariant)
       - `expect(screen.queryByText('Confirm primary')).not.toBeInTheDocument()` (CTA absent by construction per CONTEXT D-01)
       - `await new Promise((resolve) => setTimeout(resolve, 0))` (flush microtasks)
       - `expect(vi.mocked(addWatch)).not.toHaveBeenCalled()` (no firing — pure absence assertion)
       Per CONTEXT Claude's-Discretion: pure-absence form (not attempt-click-and-confirm-nothing) — both forms are acceptable; pure-absence is cleanest and Test B already covers the attempt-click form.

    Do NOT modify: the Phase 70 gap-plan-08 WR-02 describe block at line 810+ (out of DUPE-04 scope; ROUTE-01 / Phase 73 owns it). Do NOT modify the AddWatchFlow.test.tsx top-level mocks for `addWatch`, `findViewerWatchByCatalogIdAction`, `uploadCatalogSourcePhoto`, `createSupabaseBrowserClient`, `sonner` (toast) — those are infrastructure shared across the file's other 20+ tests. Do NOT modify the beforeEach setup of this describe (it correctly seeds mocks). Do NOT introduce any `font-medium` className (project_phase_68_complete recurrence-5).
  </action>
  <verify>
    <automated>npm test -- --run src/components/watch/AddWatchFlow.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "queryByText('Confirm primary')" src/components/watch/AddWatchFlow.test.tsx` returns at least 4 (one per Test A/B/C initial-assert + Test D)
    - `grep -c "toBeDisabled()" src/components/watch/AddWatchFlow.test.tsx` decreases by at least 3 versus the pre-edit count (the 3 WR-01 disable-assertions removed)
    - `grep -c "bannerActive" src/components/watch/AddWatchFlow.test.tsx` returns at least 1 (the mock destructure)
    - The string `WR-01 (DUPE-04) — bannerActive absence-by-construction` (or close phrasing per planner discretion) appears in the file as a new `it(...)` call inside the existing WR-01 describe block
    - `npx vitest run src/components/watch/AddWatchFlow.test.tsx` exits 0 (all WR-01 Tests A/B/C/D plus the existing other tests pass) — per project_baseline_not_green_build_is_gate, the targeted file run is the meaningful signal; full vitest has pre-existing CommentGateLocked baseline failure that is NOT this phase's
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    All 4 WR-01 tests in AddWatchFlow.test.tsx pass under the new disappearance-paired contract. Test D is new and asserts pure absence. The ConfirmStep mock honors bannerActive. Build exits 0 and the targeted vitest file run is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConfirmStep render → ARIA tree | Removing a button from DOM is observable by screen readers; could surprise users mid-interaction if the banner mounted while they were keyboard-Tabbing through the CTA |
| AddWatchFlow → ConfirmStep prop wire | New prop must default false for backward compat (other ConfirmStep consumers — none currently, but contract-level) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Information disclosure | ConfirmStep Section 6 | accept | The button hiding does not leak any new information — it removes a redundant gated surface; no PII, no data exposure. ARIA tree change is benign (banner is the alternative surface; user re-orients to it). |
| T-74-02 | Denial of service | ConfirmStep primary CTA | mitigate | An overly-aggressive gate could prevent legitimate submits. Test D + Test C round-trip together prove the gate releases correctly when dupeContext clears. Pending semantic revert (D-02) prevents accidental gating during real async work. |
| T-74-03 | Tampering | AddWatchFlow prop wire | mitigate | A future contributor could rewire bannerActive to a stale or wrong signal. Mitigation: the truths block ties bannerActive to `state.dupeContext != null` literally; Test D asserts the contract at the React tree level. |
| T-74-04 | Repudiation | DupeBanner sibling | accept | Phase 70 D-11 sibling pattern unchanged; user actions still funnel through the banner buttons which have their own action handlers (Phase 70 Plan 03/05). |

Severity assessment: none reach HIGH; this is a UI-rendering subtraction with no auth / data / network surface changes. Security gate: PASS for this phase scope.
</threat_model>

<verification>
**Automated checks (after all 3 tasks):**

1. `npm run build` exits 0 (the authoritative gate per project_baseline_not_green_build_is_gate)
2. `npx vitest run src/components/watch/AddWatchFlow.test.tsx` exits 0 (all WR-01 Tests A/B/C/D + the other ~21 AddWatchFlow tests pass)
3. `grep -c "bannerActive" src/components/watch/ConfirmStep.tsx` returns ≥ 3 (interface, destructure, conditional)
4. `grep -c "bannerActive={state.dupeContext != null}" src/components/watch/AddWatchFlow.tsx` returns 1
5. `grep -c "pending={state.pending || state.dupeContext" src/components/watch/AddWatchFlow.tsx` returns 0 (the OR-gate removed)
6. `grep -c "font-medium" src/components/watch/ConfirmStep.tsx src/components/watch/AddWatchFlow.tsx` returns 0 (font-medium guardrail recurrence-5)

**Prod UAT (bundled with Phases 72+73 deploy per D-15):**
- DUPE-04 manual walk on horlo.app: add a watch via search whose catalog row matches an existing owned/wishlist row → DupeBanner mounts → confirm ConfirmStep primary CTA is NOT visible under the banner; ghost row (Edit details / Start over) remains clickable; inputs remain editable; clicking "Add another copy" makes the primary CTA reappear and become functional. Classified `human_needed: true` per feedback_mobile_ui_verify_on_prod — local jsdom assertions cover the structural contract; visual confirmation that the surface feels clean is a runtime call.
- Do NOT layer #419 / cache-fill / PPR regression checks per feedback_ppr_cache_fill_no_longer_call_out.
</verification>

<success_criteria>
- ROADMAP Phase 74 success criterion #1 closed: when DupeBanner is mounted on the confirm screen, the ConfirmStep primary CTA is hidden entirely (D-01 chose the "hidden entirely" branch from SC#1's two options).
- ConfirmStep prop contract intact: Phase 68 D-03 LOCKED interface gains exactly one new optional field; no renames, no removals, no semantic changes to other fields.
- DupeBanner sibling pattern intact: Phase 70 D-11 LOCKED — the banner remains a sibling above ConfirmStep; only Section 6 of ConfirmStep changes.
- Disappearance-pair discipline carried forward (recurrence-3 of feedback_test_assert_disappearance_too).
- Build exits 0; targeted vitest file exits 0; font-medium guardrail (recurrence-5) holds.
</success_criteria>

<output>
After completion, create `.planning/phases/74-dupebanner-gate-refinement-mobile-polish/74-01-SUMMARY.md` summarizing the additive ConfirmStep prop, the AddWatchFlow call-site edit, the test pivots, and the build / vitest exit codes. Note any deferred items (visual prod UAT — handled by D-15 bundled deploy walk).
</output>
</content>
</invoke>