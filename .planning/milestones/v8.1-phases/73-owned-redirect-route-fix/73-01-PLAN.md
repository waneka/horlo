---
phase: 73
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/watch/AddWatchFlow.tsx
  - src/components/watch/AddWatchFlow.test.tsx
autonomous: true
requirements: [ROUTE-01]
requirements_addressed: [ROUTE-01]
nyquist_compliant: true
tags: [route, navigation, search-pick, regression-fix]

must_haves:
  truths:
    - "D-01: handleSearchPick owned branch pushes router.push('/w/${catalogId}') — UUID, not reference; passes /w/[ref] Branch 2 UUID guard at page.tsx:151"
    - "D-04: both owned branches (with-ref + null-ref) collapsed into a single early-return router.push; the D-06 owned-null-ref confirm-with-banner branch is dead-removed from search-pick path (still used elsewhere on structured-submit and URL-backup)"
    - "User can click an 'In collection' search result in the add-watch combobox and arrive at a working /w/[catalogId] watch detail page — no 404 — for any owned watch regardless of whether its catalog row has a non-empty reference"
    - "Owned-pick never enters the 'confirming' state (no ConfirmStep, no DupeBanner-owned mount on either Pick owned or Pick owned no-ref)"
    - "D-09: npm run build exits 0 AND vitest run AddWatchFlow.test.tsx is green; prod click-through bundles with Phase 74 deploy (human_needed)"
    - "Wishlist branch (handleSearchPick :194-227), structured-submit branch (handleStructuredSubmit), and URL-backup branch (handleUrlBackup) are UNTOUCHED — resolveDupeContext and toast.error imports remain consumed"
    - "D-02: src/app/w/[ref]/page.tsx UUID-only guard at line 151 is NOT loosened — Phase 59 D-04 invariant preserved; no edit to the route handler"
    - "D-03: no client-side findViewerWatchByCatalogIdAction round-trip is added to handleSearchPick — Branch 2's server-side lookup at page.tsx:439 already resolves ownership"
    - "D-05: encodeURIComponent is kept on the catalogId push for defense-in-depth (catalogId is a plain UUID — no reserved chars — but consistency with prior code holds)"
    - "D-07: no tests/static/ guard is added — the unit-test assertion (T-70-01 + T-70-02 push-target + disappearance) deterministically catches reference-based-push regression"
    - "D-08: no E2E test is added — project has no Playwright/Cypress harness; adding one for a 1-line fix is scope creep"
    - "D-10: no dev-server walkthrough is required — local DB lacks meaningful catalog/owned data; prod walk after deploy is the authoritative manual check"
  artifacts:
    - path: "src/components/watch/AddWatchFlow.tsx"
      provides: "Collapsed handleSearchPick owned branch — single early-return router.push('/w/${catalogId}')"
      contains: "router.push(`/w/${encodeURIComponent(result.catalogId)}`)"
    - path: "src/components/watch/AddWatchFlow.test.tsx"
      provides: "T-70-01 + T-70-02 push-target assertions updated to catalogId; T-70-02 description repurposed; WR-02 Test A deleted; WR-02 Test D push assertion updated; WR-01 Test B pivoted to structured-submit; T-70-02 dead findViewerWatchByCatalogIdAction.mockResolvedValueOnce removed"
      contains: "expect(pushSpy).toHaveBeenCalledWith('/w/cat-owned')"
  key_links:
    - from: "src/components/watch/AddWatchFlow.tsx :handleSearchPick"
      to: "src/app/w/[ref]/page.tsx :Branch 2 findViewerWatchByCatalogId :line 439 → D-06 in-place owned render :line 472"
      via: "router.push of catalogId UUID (passes UUID guard at page.tsx:151)"
      pattern: "router\\.push\\(`/w/\\$\\{encodeURIComponent\\(result\\.catalogId\\)\\}`\\)"
---

<objective>
Fix ROUTE-01: clicking an "In collection" search result in the add-watch combobox lands on a 404 because `handleSearchPick` pushes `result.reference` (a model number like "REF-001") into `/w/[ref]`, but the route's UUID-only guard at `src/app/w/[ref]/page.tsx:151` rejects non-UUIDs with `notFound()`.

Per CONTEXT.md D-01 + D-04, switch the slug source to `result.catalogId` (always a UUID, always present on `SearchCatalogWatchResult`) AND collapse both owned branches (with-ref redirect + null-ref confirm-with-banner) into a single early-return `router.push`. The dead null-ref branch's `resolveDupeContext` round-trip, `toast.error` path, `setConfirmStatus`/`setConfirmReference`/`setConfirmYear`/`setConfirmPrice` setup, and `setState({ kind: 'confirming', ... })` are removed from the search-pick owned path (still in use on structured-submit and URL-backup — UNTOUCHED).

Purpose: User can navigate from search → owned watch detail page (D-06 in-place owned view; verdict hidden on owned per `verdict_hidden_on_owned_watches` memory) without a 404. Cleanest possible caller-side fix; receiver route (`/w/[ref]`) is untouched (Phase 59 D-04 UUID-only invariant preserved per D-02).
Output: 1 production-source edit + 5 test-file edits (2 push-target swaps, 1 test repurpose, 1 test deletion, 1 test pivot) across 2 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-owned-redirect-route-fix/73-CONTEXT.md
@.planning/phases/73-owned-redirect-route-fix/73-RESEARCH.md
@.planning/phases/73-owned-redirect-route-fix/73-VALIDATION.md

@src/components/watch/AddWatchFlow.tsx
@src/components/watch/AddWatchFlow.test.tsx
@src/lib/searchTypes.ts

<interfaces>
<!-- Key contracts the executor must honor. Already exist in the codebase — no changes. -->

From src/lib/searchTypes.ts:
```typescript
export interface SearchCatalogWatchResult {
  catalogId: string             // line 36 — UUID, ALWAYS PRESENT (the new slug source)
  reference: string | null      // line 39 — model number, NULLABLE (no longer used for push)
  viewerState: 'owned' | 'wishlist' | null
  // ...other fields
}
```

From src/app/w/[ref]/page.tsx (UNTOUCHED — receiver is already correct):
```typescript
// line 151 — UUID regex guard (defense in depth; catalogId passes)
// line 439 — Branch 2: findViewerWatchByCatalogId(user.id, ref) → viewerOwnedRow
// line 472 — D-06 in-place owned render when viewerOwnedRow !== null
```

From src/components/watch/AddWatchFlow.tsx (CURRENT, before this phase):
```typescript
// Lines 152-156 (D-05 with-ref):   if (result.viewerState === 'owned' && result.reference) {
//                                    router.push(`/w/${encodeURIComponent(result.reference)}`)  // BUG
//                                    return
//                                  }
// Lines 158-192 (D-06 null-ref):   if (result.viewerState === 'owned') { /* confirm-with-banner */ }
// Lines 194-227 (wishlist):        if (result.viewerState === 'wishlist') { ... }   // UNTOUCHED
// Lines 228-246 (null viewerState):                                                   // UNTOUCHED
```

From src/components/watch/AddWatchFlow.test.tsx (CURRENT, before this phase):
```typescript
// Line 312  — beforeEach already calls vi.clearAllMocks() + sets pushSpy.mockClear()
// Line 319  — default findViewerWatchByCatalogIdAction resolver = { success: true, data: null }
// Lines 333-341  T-70-01 — Pick owned → pushSpy.toHaveBeenCalledWith('/w/REF-001')   // assertion swap
// Lines 343-354  T-70-02 — Pick owned no-ref → findByTestId('dupe-banner-owned')     // repurpose
//                  lines 345-348: vi.mocked(findViewerWatchByCatalogIdAction).mockResolvedValueOnce({...}) — DEAD; delete
// Lines 762-777  WR-01 Test B — Pick owned no-ref → DupeBanner-owned + disabled CTA + no addWatch on click   // pivot to Submit structured
// Lines 835-852  WR-02 Test A — owned-null-ref + resolver failure → toast.error                              // delete
// Lines 894-902  WR-02 Test D — owned-with-ref fast-path → pushSpy('/w/REF-001')     // assertion swap
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse handleSearchPick owned branches into a single catalogId early-return push</name>
  <files>src/components/watch/AddWatchFlow.tsx</files>
  <read_first>
    - src/components/watch/AddWatchFlow.tsx lines 1-260 (full handleSearchPick body)
    - .planning/phases/73-owned-redirect-route-fix/73-CONTEXT.md §decisions D-01..D-05
    - .planning/phases/73-owned-redirect-route-fix/73-RESEARCH.md §Common Pitfalls (especially Pitfall 1 — DO NOT touch imports)
  </read_first>
  <action>
    In `src/components/watch/AddWatchFlow.tsx`, replace the two owned branches (currently lines 152-192 — the D-05 with-ref branch AND the entire D-06 null-ref confirm-with-banner branch) inside `handleSearchPick` (defined at line 150) with a SINGLE collapsed branch. Implement per CONTEXT.md D-01 + D-04 + D-05:

    Replacement block (replaces lines 152-192 verbatim):
    - One `if (result.viewerState === 'owned') { ... return }` block
    - Body: `router.push(\`/w/${encodeURIComponent(result.catalogId)}\`)` (per D-01 + D-05; encodeURIComponent kept for defense-in-depth even though catalogId is a plain UUID)
    - Leading comment block must reference Phase 73 ROUTE-01, cite D-01 + D-04, and explain that `result.catalogId` is always a UUID (`SearchCatalogWatchResult` contract) so the `/w/[ref]` UUID guard at `page.tsx:151` passes → Branch 2 finds the viewer's row via `findViewerWatchByCatalogId` → in-place D-06 owned render at `page.tsx:472` (no client-side round-trip needed; D-03 rejects the round-trip)

    DO NOT touch:
    - The wishlist branch (currently lines 194-227) — preserve verbatim
    - The null-viewerState fallthrough (currently lines 228-246) — preserve verbatim
    - The `useCallback` deps array (currently `[router, initialStatus]`) — preserve verbatim
    - Any import statement (per RESEARCH Pitfall 1 — `resolveDupeContext`, `toast`, `findViewerWatchByCatalogIdAction`, `DupeContext` are all still consumed by the wishlist branch + structured-submit + URL-backup)
    - The `handleStructuredSubmit` function below (line 257+) — preserve verbatim (D-06 confirm-with-banner pattern remains in use there per CONTEXT.md D-04 final sentence)
    - The route handler `src/app/w/[ref]/page.tsx` — receiver is correct as-is (D-02)

    Commit message: `fix(73): collapse handleSearchPick owned branches to single catalogId redirect (ROUTE-01)`
  </action>
  <verify>
    <automated>
      # Hygiene check: ensure the bug literal is gone and the fix literal is present.
      # Filter `^//` comments out so prose containing the strings doesn't trip the counts (per project memory: comment-aware grep gates).
      RESULT_REF=$(grep -v '^\s*//' src/components/watch/AddWatchFlow.tsx | grep -v '^\s*\*' | grep -c "router.push(\`/w/\${encodeURIComponent(result.reference)}\`)")
      RESULT_CAT=$(grep -v '^\s*//' src/components/watch/AddWatchFlow.tsx | grep -v '^\s*\*' | grep -c "router.push(\`/w/\${encodeURIComponent(result.catalogId)}\`)")
      DEAD_SETCONFIRM=$(grep -v '^\s*//' src/components/watch/AddWatchFlow.tsx | grep -v '^\s*\*' | grep -A1 "viewerState === 'owned')" | grep -c "setConfirmStatus('owned')")
      test "$RESULT_REF" = "0" && test "$RESULT_CAT" = "1" && test "$DEAD_SETCONFIRM" = "0"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep `router.push(\`/w/${encodeURIComponent(result.reference)}\`)` in non-comment lines of AddWatchFlow.tsx → count 0 (bug literal gone)
    - grep `router.push(\`/w/${encodeURIComponent(result.catalogId)}\`)` in non-comment lines of AddWatchFlow.tsx → count 1 (fix literal present exactly once — the search-pick owned branch)
    - No `setConfirmStatus('owned')` line within the search-pick owned branch (the dead null-ref confirm-with-banner setup is removed)
    - `npx tsc --noEmit src/components/watch/AddWatchFlow.tsx` produces no NEW errors attributable to this file (baseline tsc has ~77 pre-existing errors per memory — only check that the count does not grow because of this edit; the real gate is `npm run build` in Task 3)
    - The wishlist branch (current lines 194-227), null-viewerState fallthrough (current lines 228-246), and handleStructuredSubmit (current line 257+) are byte-for-byte unchanged
    - Imports unchanged (resolveDupeContext, toast, findViewerWatchByCatalogIdAction, DupeContext all still imported because they remain in use on other branches)
  </acceptance_criteria>
  <done>
    Search-pick owned branch is a single early-return `router.push(\`/w/${encodeURIComponent(result.catalogId)}\`)`. Both `Pick owned` and `Pick owned no-ref` test buttons will now redirect with the catalog UUID; no transition into `confirming` state on either click. Wishlist + null-viewerState branches preserved verbatim. Imports untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update AddWatchFlow.test.tsx — T-70-01/T-70-02 assertion swaps + repurpose + WR-02 Test A delete + WR-02 Test D assertion swap + WR-01 Test B pivot</name>
  <files>src/components/watch/AddWatchFlow.test.tsx</files>
  <read_first>
    - src/components/watch/AddWatchFlow.test.tsx lines 300-410 (beforeEach + T-70-01 + T-70-02 + T-70-03 for pivot template)
    - src/components/watch/AddWatchFlow.test.tsx lines 745-810 (WR-01 Test B context: WR-01 Test A + B + C)
    - src/components/watch/AddWatchFlow.test.tsx lines 805-905 (WR-02 suite: A, B, C, D)
    - .planning/phases/73-owned-redirect-route-fix/73-RESEARCH.md §Open Questions (decisions on Test A delete + Test B pivot + dead mock cleanup)
    - .planning/phases/73-owned-redirect-route-fix/73-RESEARCH.md §Pitfall 5 (disappearance assertion requirement)
    - Memory `feedback_test_assert_disappearance_too` (assert both directions — mount AND dismiss; pair `getByX.toBeInTheDocument()` with `queryByY.not.toBeInTheDocument()`)
  </read_first>
  <action>
    Apply FIVE coordinated edits to `src/components/watch/AddWatchFlow.test.tsx`:

    EDIT A — T-70-01 assertion swap (current lines 333-341):
    - Update the `it(...)` description string from `'T-70-01 — owned-pick with non-null reference → router.push("/w/REF-001"); no confirm screen'` to `'T-70-01 — owned-pick with non-null reference → router.push("/w/cat-owned") (catalogId); no confirm screen, no DupeBanner'` (per RESEARCH Pitfall 5: disappearance assertion now also covers the banner)
    - Change `expect(pushSpy).toHaveBeenCalledWith('/w/REF-001')` to `expect(pushSpy).toHaveBeenCalledWith('/w/cat-owned')`
    - ADD a third assertion line after the existing `expect(screen.queryByTestId('confirm-step')).not.toBeInTheDocument()`: `expect(screen.queryByTestId('dupe-banner-owned')).not.toBeInTheDocument()` (per memory `feedback_test_assert_disappearance_too` — assert ConfirmStep AND DupeBanner BOTH stay unmounted to catch any future regression that re-introduces the confirming-state path on owned picks)
    - Note: the `Pick owned` scaffold button (test file lines 106-121) already fires `onPick` with `catalogId: 'cat-owned'` per RESEARCH (no scaffold change needed)

    EDIT B — T-70-02 repurpose (current lines 343-354):
    - Update the `it(...)` description string from `'T-70-02 — owned-pick with null reference → confirming + DupeBanner-owned mounted (D-06 fallback)'` to `'T-70-02 — owned-pick with null reference → router.push("/w/cat-owned-noref") (D-04 collapse); no confirm screen, no DupeBanner'`
    - DELETE the dead mock prime at current lines 345-348: `vi.mocked(findViewerWatchByCatalogIdAction).mockResolvedValueOnce({ success: true, data: { id: 'existing-owned-id', status: 'owned', reference: null }, })` — after the collapse, the resolver is never consulted on this path (per RESEARCH Pitfall 2 + Open Question 3)
    - Replace the body assertions verbatim with:
      ```
      renderFlow()
      fireEvent.click(screen.getByText('Pick owned no-ref'))
      await waitFor(() => {
        expect(pushSpy).toHaveBeenCalledWith('/w/cat-owned-noref')
      })
      expect(screen.queryByTestId('confirm-step')).not.toBeInTheDocument()
      expect(screen.queryByTestId('dupe-banner-owned')).not.toBeInTheDocument()
      ```
    - The triple assertion (push target + ConfirmStep absent + DupeBanner absent) per RESEARCH Pitfall 5 + memory `feedback_test_assert_disappearance_too`

    EDIT C — WR-02 Test A DELETE (current lines 835-852):
    - Delete the entire `it('WR-02 — search-pick owned (D-06 null-ref fallthrough) with resolver failure → toast.error + stay on search-idle', ...)` block in full (the `it(...)` opener, body, closing `})`)
    - Rationale: the owned-null-ref + resolver-failure code path ceases to exist after Task 1 (per RESEARCH Open Question 1). The wishlist version (WR-02 Test B at current lines 855-870) preserves the WR-02 toast.error invariant for the only remaining `viewerState`-pre-signal-with-resolver-call path
    - Leave WR-02 Test B (wishlist), Test C (null viewerState inverse), and Test D (owned-with-ref fast-path) intact aside from EDIT D below

    EDIT D — WR-02 Test D push assertion swap (current line 898):
    - Change `expect(pushSpy).toHaveBeenCalledWith('/w/REF-001')` to `expect(pushSpy).toHaveBeenCalledWith('/w/cat-owned')` inside the `it('WR-02 inverse — search-pick owned WITH reference fast-path redirects WITHOUT calling resolveDupeContext', ...)` block at current lines 894-902
    - The `expect(findViewerWatchByCatalogIdAction).not.toHaveBeenCalled()` assertion at current line 900 remains correct (Task 1's collapse still bypasses the resolver — even more so now)

    EDIT E — WR-01 Test B pivot to structured-submit (current lines 762-777):
    - The test's invariant is "owned dupeContext → ConfirmStep primary CTA is disabled; clicking does NOT call addWatch" — preserve this assertion
    - After Task 1, `Pick owned no-ref` no longer mounts the owned banner (the search-pick path collapses to a redirect). The ONLY remaining owned-banner producer is the structured-submit path (handleStructuredSubmit, AddWatchFlow.tsx :263-289)
    - Pivot per RESEARCH Open Question 2 + Pitfall 4 (use T-70-03 at current lines 357-373 as the structured-submit template). Replace the test body verbatim with:
      ```
      vi.mocked(findViewerWatchByCatalogIdAction).mockResolvedValueOnce({
        success: true,
        data: { id: 'existing-owned-id', status: 'owned', reference: 'REF-OWNED' },
      })
      renderFlow()
      fireEvent.click(screen.getByText('Submit structured'))
      expect(await screen.findByTestId('dupe-banner-owned')).toBeInTheDocument()
      expect(screen.getByText('Confirm primary')).toBeDisabled()

      // Attempt to click the disabled CTA — addWatch must not fire.
      fireEvent.click(screen.getByText('Confirm primary'))
      // Allow any pending microtasks to flush.
      await new Promise((resolve) => setTimeout(resolve, 0))
      expect(vi.mocked(addWatch)).not.toHaveBeenCalled()
      ```
    - Update the `it(...)` description from `'WR-01 — ConfirmStep primary CTA is disabled when owned dupeContext is set; clicking does NOT call addWatch'` to `'WR-01 — ConfirmStep primary CTA is disabled when owned dupeContext is set (via structured-submit); clicking does NOT call addWatch'`
    - The pivoted test's `mockResolvedValueOnce` resolver IS consumed (T-70-03 pattern proves this is required) — keep it

    Commit message: `test(73): update T-70-01/T-70-02 + WR-02 Test D push targets to catalogId; delete WR-02 Test A; pivot WR-01 Test B to structured-submit (ROUTE-01)`
  </action>
  <verify>
    <automated>npx vitest run src/components/watch/AddWatchFlow.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/watch/AddWatchFlow.test.tsx` exits 0 (all tests in the file green)
    - T-70-01 green; T-70-01 contains exactly `expect(pushSpy).toHaveBeenCalledWith('/w/cat-owned')` AND `expect(screen.queryByTestId('confirm-step')).not.toBeInTheDocument()` AND `expect(screen.queryByTestId('dupe-banner-owned')).not.toBeInTheDocument()`
    - T-70-02 green; T-70-02 contains exactly `expect(pushSpy).toHaveBeenCalledWith('/w/cat-owned-noref')` AND `expect(screen.queryByTestId('confirm-step')).not.toBeInTheDocument()` AND `expect(screen.queryByTestId('dupe-banner-owned')).not.toBeInTheDocument()`
    - T-70-02 NO LONGER contains the dead `vi.mocked(findViewerWatchByCatalogIdAction).mockResolvedValueOnce(...)` setup
    - `grep -c "WR-02 — search-pick owned (D-06 null-ref fallthrough)" src/components/watch/AddWatchFlow.test.tsx` returns 0 (Test A deleted)
    - WR-02 Test B (wishlist), Test C (null inverse), Test D (owned-with-ref) all green
    - WR-02 Test D push assertion is `expect(pushSpy).toHaveBeenCalledWith('/w/cat-owned')`; the `expect(findViewerWatchByCatalogIdAction).not.toHaveBeenCalled()` assertion stays
    - WR-01 Test A (wishlist), Test B (now structured-submit-mounted), Test C all green
    - WR-01 Test B description now contains "via structured-submit"; uses `getByText('Submit structured')` not `getByText('Pick owned no-ref')`; preserves the `expect(vi.mocked(addWatch)).not.toHaveBeenCalled()` assertion
    - Full file test count after edits: prior count − 1 (WR-02 Test A deleted)
    - No raw `/w/REF-001` push-target literal remains in the test file (`grep -c "'/w/REF-001'" src/components/watch/AddWatchFlow.test.tsx` returns 0)
  </acceptance_criteria>
  <done>
    All five edits applied; `npx vitest run src/components/watch/AddWatchFlow.test.tsx` is green; bug-literal push targets (`/w/REF-001`) are gone from the file; disappearance assertions in T-70-01 + T-70-02 guard against future regressions that re-introduce the confirming-state path on owned picks (per memory `feedback_test_assert_disappearance_too`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Run npm run build (milestone gate) + flag prod walk human_needed</name>
  <files>(no edits — gate verification + summary annotation only)</files>
  <read_first>
    - .planning/STATE.md §Key Decisions — confirm `npm run build` exit 0 is the authoritative gate
    - Memory `project_baseline_not_green_build_is_gate` — tsc/test baseline carries pre-existing failures unrelated to this phase
    - Memory `feedback_mobile_ui_verify_on_prod` — UI behavior verifies on prod, not locally
    - Memory `feedback_ppr_cache_fill_no_longer_call_out` — DO NOT bake soft-nav-#419 / cache-fill into UAT (resolved infrastructure)
  </read_first>
  <action>
    Run `npm run build` from the repo root. Per CONTEXT.md D-09 step 1 + project memory, this is the authoritative gate (NOT `tsc --noEmit` which has ~77 pre-existing test-file errors, and NOT `npm run test` which has at least one pre-existing failure unrelated to this phase).

    If build exits 0: phase code-side is COMPLETE.

    If build fails: capture the error, identify whether it is caused by THIS phase's edits (Tasks 1 + 2) or pre-existing baseline noise; only fix if attributable.

    Annotate the phase SUMMARY:
    - Note that prod click-through verification is HUMAN_NEEDED (per memory `feedback_mobile_ui_verify_on_prod` — local DB lacks meaningful catalog/owned data per CONTEXT.md D-10, so the only authoritative manual check is on prod after deploy)
    - Bundle the prod push with Phase 74's deploy if Phase 74 ships in the same session (per CONTEXT.md D-09 step 3); otherwise push standalone
    - Per memory `feedback_ppr_cache_fill_no_longer_call_out`, the prod walk does NOT need to include soft-nav-#419 or cache-fill checks — those are resolved infrastructure for the unified `/w/[ref]` route
    - The expected prod-walk script: open the add-watch popup → search for any owned watch → click the "In collection" result → confirm `/w/[catalogId]` renders the D-06 in-place owned view (hero present, Collection Fit verdict hidden per `verdict_hidden_on_owned_watches` memory, comment thread present); no 404
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - The build output contains no new errors attributable to `src/components/watch/AddWatchFlow.tsx` or `src/components/watch/AddWatchFlow.test.tsx`
    - Phase SUMMARY annotates: (a) prod click-through is human_needed; (b) bundling with Phase 74 deploy is preferred if same session; (c) the prod-walk script per CONTEXT.md D-09 step 3; (d) NO soft-nav-#419 / cache-fill checks needed (resolved infrastructure)
  </acceptance_criteria>
  <done>
    Build is green; phase code-side is complete and ready for prod deploy. Prod click-through flagged human_needed; verifier should not auto-flip ROUTE-01 to passed without the prod walk confirmation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /w/[ref] route handler | The push slug travels from the AddWatchFlow client component as a URL path segment into the server-rendered route handler. This boundary already exists and is unchanged in surface area. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | `/w/[ref]` slug from `router.push(\`/w/${encodeURIComponent(result.catalogId)}\`)` | mitigate (pre-existing controls) | (a) `encodeURIComponent` preserved per D-05 (defense-in-depth — catalogId is a plain UUID with no reserved chars, but the encode call mirrors prior pattern); (b) Server-side UUID regex guard at `src/app/w/[ref]/page.tsx:151` (Phase 59 D-04 invariant, UNTOUCHED per D-02) rejects any non-UUID slug with `notFound()`. No new attack surface — Phase 70's prior slug source `result.reference` (the model number, a free-form brand-supplied string) was actually LESS controlled than `result.catalogId` (a server-issued UUID per Phase 70 D-08 cache projection). This change REDUCES the threat surface. |
| T-73-02 | Information Disclosure | Catalog UUID enumeration via the search-pick redirect | accept | The route's Branch 2 lookup (`findViewerWatchByCatalogId` at page.tsx:439) is viewer-scoped: only the viewer's own row is returned. Enumerating catalog UUIDs reveals only the enumerator's own ownership status — no other-user data leak. Equivalent to the pre-Phase-73 risk via Branch 2 already in production from Phase 59. |
| T-73-03 | Spoofing | `result.catalogId` provenance (could the client forge a catalogId for another user's watch?) | accept (pre-existing posture) | The catalogId is sourced from the server-side search projection (`/api/extract-watch` → DAL); the client receives it via a trusted server response. Even if a malicious client crafted a fake catalogId and pushed `/w/<fake-uuid>`, the route's Branch 1 + Branch 2 lookups are viewer-scoped and would return null → `notFound()`. The viewer cannot observe any data they didn't already own. No new exploit path. |

**Summary — NO NEW ATTACK SURFACE.** This phase is a caller-side slug-source swap (model-number string → catalog UUID) on a path that was already in production with weaker provenance. The receiver route (`/w/[ref]`) is byte-for-byte unchanged. No new external input, no new auth boundary, no new data path. Per Phase 70 D-08 cache projection, `catalogId` is already trusted server-side. Block-on severity (high) is NOT triggered — disposition of all three threats is mitigate (pre-existing) or accept (pre-existing posture).
</threat_model>

<verification>
Phase-level verification (cumulative across all tasks):

1. **Source edit landed (Task 1):**
   - `grep -v '^\s*//' src/components/watch/AddWatchFlow.tsx | grep -v '^\s*\*' | grep -c "router.push(\`/w/\${encodeURIComponent(result.reference)}\`)"` returns 0
   - `grep -v '^\s*//' src/components/watch/AddWatchFlow.tsx | grep -v '^\s*\*' | grep -c "router.push(\`/w/\${encodeURIComponent(result.catalogId)}\`)"` returns 1
   - `grep -c "setConfirmStatus('owned')" src/components/watch/AddWatchFlow.tsx` — count drops by 1 (the dead null-ref branch contributed 1; the wishlist+structured-submit branches use `setConfirmStatus(initialStatus ?? 'wishlist')` and `setConfirmStatus('owned')` on the structured-submit path which remains)
   - Wishlist + null-viewerState fallthrough byte-for-byte unchanged (manual diff inspection)

2. **Test suite green (Task 2):**
   - `npx vitest run src/components/watch/AddWatchFlow.test.tsx` exits 0
   - `grep -c "'/w/REF-001'" src/components/watch/AddWatchFlow.test.tsx` returns 0 (bug-literal push target purged from tests)
   - `grep -c "'/w/cat-owned'" src/components/watch/AddWatchFlow.test.tsx` returns ≥ 2 (T-70-01 + WR-02 Test D)
   - `grep -c "'/w/cat-owned-noref'" src/components/watch/AddWatchFlow.test.tsx` returns 1 (T-70-02)
   - `grep -c "WR-02 — search-pick owned (D-06 null-ref fallthrough)" src/components/watch/AddWatchFlow.test.tsx` returns 0 (Test A deleted)
   - WR-01 Test B uses `Submit structured` (not `Pick owned no-ref`); preserves `expect(vi.mocked(addWatch)).not.toHaveBeenCalled()`

3. **Milestone gate (Task 3):**
   - `npm run build` exits 0

4. **Prod click-through (human_needed, deferred to Phase 74 deploy bundle):**
   - Verifier flags this requirement awaiting prod walk; ROUTE-01 promoted to `passed` only after operator confirms on prod
</verification>

<success_criteria>
Per ROADMAP.md §Phase 73 success criteria:

1. **Clicking an "In collection" result navigates to `/w/[ref]` and the page renders (not 404)** — VERIFIED automated by T-70-01 (`'/w/cat-owned'` passes Branch 2 UUID guard) + WR-02 Test D (`'/w/cat-owned'`); VERIFIED prod by human_needed walk
2. **Redirect works regardless of whether the catalog row's reference is non-empty** — VERIFIED automated by T-70-02 (`Pick owned no-ref` → `/w/cat-owned-noref` — the null-reference case no longer mounts the confirm-with-banner branch; it redirects identically)
3. **No change to redirect behavior for non-owned results (confirm screen still appears for new additions)** — VERIFIED automated by the un-edited wishlist branch + T-70-04 (DUPE-03 wishlist pick still mounts DupeBanner-wishlist) + WR-02 Test C (null viewerState still proceeds to confirming-without-banner) all staying green

`npm run build` exits 0 (Task 3 gate). Phase 70 D-04 sentence "The D-06 'confirm-with-banner' pattern is still in use on the structured-submit branch — leave that alone" honored (T-70-03 + WR-01 Test C + the pivoted WR-01 Test B all exercise the structured-submit owned-banner path successfully).
</success_criteria>

<output>
After completion, create `.planning/phases/73-owned-redirect-route-fix/73-01-SUMMARY.md` with:
- Tasks completed (1 production edit, 5 test-file edits, 1 build gate)
- Verification evidence (vitest output snippet showing T-70-01 + T-70-02 + WR-01 + WR-02 suite green; build exit status)
- Annotations:
  - Prod click-through is `human_needed` per memory `feedback_mobile_ui_verify_on_prod` (local DB lacks meaningful catalog/owned data per D-10)
  - Bundle prod deploy with Phase 74 if same session per CONTEXT.md D-09 step 3
  - DO NOT include soft-nav-#419 or cache-fill checks in the UAT for ROUTE-01 (resolved infrastructure per memory `feedback_ppr_cache_fill_no_longer_call_out`)
- Files modified: `src/components/watch/AddWatchFlow.tsx` + `src/components/watch/AddWatchFlow.test.tsx`
- Threat model disposition: no new attack surface (all 3 threats mitigated by pre-existing controls or pre-existing posture)
</output>
