---
phase: 72-search-composition-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/catalog.ts
  - src/data/__tests__/catalog-search-tokens.test.ts
autonomous: true
requirements:
  - SRCH-01
tags:
  - search
  - dal
  - drizzle
  - multi-token

must_haves:
  truths:
    - "Per D-01: searchCatalogForAddFlow returns the matching catalog row for multi-token queries 'Brut Datejust' and 'Timex Weekender' — without any catalog row edits in this phase"
    - "Per D-02: WHERE clause is AND-of-ORs over ILIKE — each token hits at least one of (brand_normalized, model_normalized, reference_normalized)"
    - "Per D-03: tokenization is qTrimmed.toLowerCase().split(/\\s+/).filter(Boolean) — no quote handling, no per-token min-length floor, no -exclusion"
    - "Per D-04: every per-token pattern is bound via Drizzle ilike() — never string-interpolated into SQL text (T-67-02-01 mitigation preserved)"
    - "Per D-11: catalog-search-tokens.test.ts asserts SRCH-01 at the DAL unit level using the @/db chain-mock pattern from catalog-facets.test.ts"
    - "Per D-12: no new test-runner config; jsdom default; no @vitest-environment node pragma (no fs walking)"
    - "exactRefOrderTier (reference-column exact-match bump) and the popularity / alpha tie-break ORDER BY chain are preserved bit-for-bit"
    - "queryNormalized lane (alphanumeric-stripped) is preserved for the reference-column branch — per-token version uses token.replace(/[^a-z0-9]+/g, '')"
    - "Token-order invariance: 'Datejust Brut' returns the same row as 'Brut Datejust' (AND is commutative)"
  artifacts:
    - path: "src/data/catalog.ts"
      provides: "searchCatalogForAddFlow with tokenized AND-of-ORs WHERE clause"
      contains: "and(...tokenClauses"
    - path: "src/data/__tests__/catalog-search-tokens.test.ts"
      provides: "SRCH-01 regression test — multi-token, single-token, order-invariance"
      contains: "searchCatalogForAddFlow"
  key_links:
    - from: "src/data/catalog.ts:searchCatalogForAddFlow"
      to: "watches_catalog table"
      via: "drizzle and(...tokenClauses) bound parameters"
      pattern: "and\\(\\.\\.\\.tokenClauses\\)"
    - from: "src/data/__tests__/catalog-search-tokens.test.ts"
      to: "src/data/catalog.ts:searchCatalogForAddFlow"
      via: "vi.mock('@/db') + import from '@/data/catalog'"
      pattern: "import.*searchCatalogForAddFlow.*from '@/data/catalog'"
---

<objective>
Close SRCH-01: rewrite `searchCatalogForAddFlow`'s WHERE clause from a single-substring OR across columns to an AND-of-ORs per whitespace-tokenized query. Add a Vitest unit test that locks the multi-token behavior, single-token regression, and token-order invariance.

Purpose: Multi-token brand+model queries like `"Brut Datejust"` and `"Timex Weekender"` currently return zero matches because no single column contains the full substring. After this plan, each token must match independently against brand_normalized OR model_normalized OR reference_normalized, with all token-clauses AND-composed.

Output:
- `src/data/catalog.ts` — `searchCatalogForAddFlow` returns matching rows for multi-token queries; T-67-02-01 mitigation preserved; existing exact-ref and popularity tier-rank unchanged.
- `src/data/__tests__/catalog-search-tokens.test.ts` — new file; SRCH-01 regression suite (multi-token + single-token + order-invariance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-search-composition-fixes/72-CONTEXT.md
@.planning/phases/72-search-composition-fixes/72-RESEARCH.md
@.planning/phases/72-search-composition-fixes/72-PATTERNS.md
@.planning/phases/72-search-composition-fixes/72-VALIDATION.md

@src/data/catalog.ts
@src/data/__tests__/catalog-facets.test.ts

<interfaces>
<!-- Existing Drizzle import set in src/data/catalog.ts:8 — all builders for the fix are already imported. -->

From src/data/catalog.ts:
- Imports: `and, arrayOverlaps, asc, between, desc, eq, ilike, inArray, isNotNull, or, sql` from 'drizzle-orm' (line 8) — no new imports required.
- Schema columns used: `watchesCatalog.brandNormalized`, `watchesCatalog.modelNormalized`, `watchesCatalog.referenceNormalized`.
- Constants: `SEARCH_ADD_FLOW_TRIM_MIN_LEN`, `SEARCH_ADD_FLOW_DEFAULT_LIMIT`, `SEARCH_ADD_FLOW_CANDIDATE_CAP`.
- Function signature (unchanged): `searchCatalogForAddFlow({ q, viewerId, limit }: { q: string; viewerId: string; limit: number }): Promise<SearchCatalogWatchResult[]>`.

From src/lib/searchTypes.ts:
- `SearchCatalogWatchResult` shape (unchanged): `{ catalogId, brand, model, reference, imageUrl, ownersCount, wishlistCount, viewerState: 'owned'|'wishlist'|null }`.

From src/data/__tests__/catalog-facets.test.ts (test analog):
- `vi.mock('@/db', () => ({ db: { select: vi.fn(...) } }))` hoist pattern.
- `makeCandidateChain()` / `makeStateChain()` two-chain mocks toggled by `selectCount` (searchCatalogForAddFlow does TWO `db.select()` calls — candidates then stateRows).
- `safeStringify(value)` circular-reference-safe serializer for WHERE-clause arg inspection.
- `beforeEach({ calls = []; candidateRows = []; stateRows = []; selectCount = 0 })`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SRCH-01 failing DAL regression test (Wave 0 — RED)</name>
  <files>src/data/__tests__/catalog-search-tokens.test.ts</files>
  <read_first>
    - src/data/__tests__/catalog-facets.test.ts (full file — copy the vi.mock('@/db') + makeCandidateChain/makeStateChain + safeStringify pattern verbatim)
    - src/data/catalog.ts:542-644 (current searchCatalogForAddFlow — note it does TWO db.select() calls in order: candidates THEN stateRows; the chain mock must serve both)
    - .planning/phases/72-search-composition-fixes/72-PATTERNS.md §"src/data/__tests__/catalog-search-tokens.test.ts — NEW file" (full skeleton)
    - .planning/phases/72-search-composition-fixes/72-CONTEXT.md §D-01, §D-11 (test shape: multi-token + single-token + order-invariance)
    - .planning/phases/72-search-composition-fixes/72-RESEARCH.md §"SRCH-01: DAL Multi-Token Pattern" §"Test Cases" (5 cases table)
  </read_first>
  <behavior>
    - Test 1 (single-token regression — must NOT break): `searchCatalogForAddFlow({ q: 'Brut', viewerId: VIEWER, limit: 10 })` with candidateRows seeded to a Brut row returns the mapped `SearchCatalogWatchResult` for that row.
    - Test 2 (multi-token primary fix — SRCH-01): seed candidateRows with a row whose brand='Brut' and model contains 'Datejust'; call with `q: 'Brut Datejust'`; assert the row is returned AND the captured `cand.where` arg list serializes to a JSON containing both `'brut'` and `'datejust'` (safeStringify inspection).
    - Test 3 (second multi-token primary fix — SRCH-01): seed with Timex/Weekender row; call with `q: 'Timex Weekender'`; assert returned + WHERE contains both tokens.
    - Test 4 (token-order invariance): call once with `q: 'Brut Datejust'`, capture cand.where serialization; call again with `q: 'Datejust Brut'`, capture; assert the SAME row is returned in both calls (the captured WHERE serializations may differ in token order — that's expected; assert RESULT equality, not WHERE-string equality).
    - Test 5 (defensive empty-token-list guard from Claude's Discretion in D-03): call with `q: '   '` (whitespace only, length ≥ SEARCH_ADD_FLOW_TRIM_MIN_LEN before trim but tokens.length === 0 after split+filter); assert returns `[]` and `cand.where` was NEVER called (early-return triggered). NOTE: if upstream `qTrimmed.length < SEARCH_ADD_FLOW_TRIM_MIN_LEN` already short-circuits, this test asserts the upstream guard suffices; the inner `if (tokens.length === 0) return []` is belt-and-suspenders defensive (Claude's discretion per D-03 + Pitfall 2).
  </behavior>
  <action>
    Create the file at `src/data/__tests__/catalog-search-tokens.test.ts` following the analog at `src/data/__tests__/catalog-facets.test.ts`. Hoist `vi.mock('@/db', () => ({ db: { select: vi.fn(...) } }))` at top of file; import `searchCatalogForAddFlow` from `@/data/catalog` AFTER the mock. Use the two-chain pattern (`makeCandidateChain`, `makeStateChain`, toggled by `selectCount`). Reset `calls`, `candidateRows`, `stateRows`, `selectCount` in `beforeEach`. Use `safeStringify` (copy verbatim from catalog-facets.test.ts) for WHERE-arg inspection. Define a `VIEWER` constant (`'aaaaaaaa-bbbb-4ccc-8ddd-eeeeeeeeeeee'`). Define seed rows BRUT_DATEJUST_ROW and TIMEX_WEEKENDER_ROW with the full candidate-row shape (id/brand/model/reference/imageUrl/ownersCount/wishlistCount). Implement the 5 tests above. THIS TEST IS EXPECTED TO FAIL against the current single-substring DAL (Tests 2/3/4 fail because the current WHERE returns 0 rows for multi-token queries — confirm RED before Task 2). Commit message: `test(72-01): add failing SRCH-01 multi-token DAL regression tests`.
  </action>
  <verify>
    <automated>npm run test -- --run src/data/__tests__/catalog-search-tokens.test.ts 2>&1 | tee /tmp/72-01-task1.log; grep -E "(FAIL|PASS|Tests)" /tmp/72-01-task1.log | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `src/data/__tests__/catalog-search-tokens.test.ts` exists.
    - File contains the literal string `vi.mock('@/db'`.
    - File contains the literal string `searchCatalogForAddFlow` (function under test imported).
    - File contains all four token strings as test inputs: `Brut`, `Brut Datejust`, `Timex Weekender`, `Datejust Brut`.
    - File contains the literal string `safeStringify` (WHERE-arg inspection helper present).
    - `npm run test -- --run src/data/__tests__/catalog-search-tokens.test.ts` runs to completion (no syntax/import errors). RED expected: at least Test 2 (`'Brut Datejust'`) is FAILING — captured in /tmp/72-01-task1.log. If ALL tests pass against current code, the test seeding is too permissive — re-tighten before Task 2.
    - `grep -c "describe\|it(" src/data/__tests__/catalog-search-tokens.test.ts` returns ≥ 6 (at least 1 describe + 5 it blocks).
  </acceptance_criteria>
  <done>
    New DAL test file exists with 5 tests. At least the 'Brut Datejust' multi-token test is RED against current catalog.ts (confirmed in /tmp/72-01-task1.log). No new imports added to test runner config; uses existing jsdom default.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite searchCatalogForAddFlow WHERE clause to tokenized AND-of-ORs (GREEN)</name>
  <files>src/data/catalog.ts</files>
  <read_first>
    - src/data/catalog.ts:530-644 (current docstring + searchCatalogForAddFlow body — the entire function)
    - src/data/catalog.ts:395-468 (searchCatalogWatches — the `and(...predicates)` spread analog already in this file)
    - src/data/catalog.ts:1-15 (import block — confirm `and`, `or`, `ilike`, `sql` already imported)
    - .planning/phases/72-search-composition-fixes/72-CONTEXT.md §D-02, §D-03, §D-04 (locked DAL primitive + tokenization + parameterization rules)
    - .planning/phases/72-search-composition-fixes/72-RESEARCH.md §"SRCH-01: DAL Multi-Token Pattern" §"Fix Pattern" (full code skeleton + Pitfall 1 token-normalization)
    - .planning/phases/72-search-composition-fixes/72-PATTERNS.md §"src/data/catalog.ts — searchCatalogForAddFlow SRCH-01 fix" (verbatim before/after snippets)
  </read_first>
  <action>
    In `src/data/catalog.ts:542-644`, modify `searchCatalogForAddFlow`. Replace the single-substring WHERE clause (lines 554-588 — `lowerQ`, `pattern`, and the `.where(or(...))` block) with the tokenized AND-of-ORs per D-02 + D-03 + D-04. Preserve verbatim: line 552 (early-return on qTrimmed.length < SEARCH_ADD_FLOW_TRIM_MIN_LEN), lines 556 (queryNormalized) and 562-565 (exactRefOrderTier), the `.orderBy(exactRefOrderTier, ...)` chain (lines 589-596), and the `.limit(SEARCH_ADD_FLOW_CANDIDATE_CAP)` (line 597). Implementation outline:
    (a) Add `const tokens = qTrimmed.toLowerCase().split(/\s+/).filter(Boolean)` after the early-return.
    (b) Add defensive `if (tokens.length === 0) return []` per D-03 + Pitfall 2 (belt-and-suspenders; upstream early-return already protects, but static analyzers can't see that).
    (c) Build `tokenClauses` as `tokens.map(token => or(ilike(brandNormalized, '%'+token+'%'), ilike(modelNormalized, '%'+token+'%'), refToken.length > 0 ? ilike(referenceNormalized, '%'+refToken+'%') : sql\`false\`))` where `refToken = token.replace(/[^a-z0-9]+/g, '')`. NEVER string-concat into SQL text — the `%${token}%` happens in TypeScript; Drizzle's `ilike(col, str)` binds the string as a parameter.
    (d) Replace `.where(or(...))` with `.where(and(...tokenClauses))`.
    (e) Update the docstring (lines 530-541) — keep the T-67-02-01 mitigation callout (D-04 reaffirms it); add a one-line note explaining the AND-of-ORs tokenization per D-02. The Pitfall 1 docstring (`queryNormalized` empty → `sql\`false\``) is now per-token; mirror in the comment.
    Planner's discretion (per D-03 + 72-CONTEXT.md §Claude's Discretion): name the array `tokenClauses` for readability; place the per-token `refToken` normalization inline (no helper extraction — single consumer).
    Commit message: `feat(72-01): SRCH-01 — tokenize searchCatalogForAddFlow WHERE clause (AND-of-ORs per D-02)`.
  </action>
  <verify>
    <automated>npm run test -- --run src/data/__tests__/catalog-search-tokens.test.ts 2>&1 | tee /tmp/72-01-task2.log; grep -E "(FAIL|PASS|Tests)" /tmp/72-01-task2.log | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "and(\\.\\.\\.tokenClauses)" src/data/catalog.ts` returns ≥ 1.
    - `grep -c "qTrimmed.toLowerCase().split(/\\\\s+/)" src/data/catalog.ts` returns ≥ 1.
    - `grep -c "if (tokens.length === 0) return \\[\\]" src/data/catalog.ts` returns ≥ 1.
    - Source preserves the existing exact-ref tier: `grep -c "exactRefOrderTier" src/data/catalog.ts` returns ≥ 2 (definition + ORDER BY usage).
    - Source preserves T-67-02-01 mention: `grep -c "T-67-02-01" src/data/catalog.ts` returns ≥ 1.
    - Source does NOT contain SQL string-interpolation patterns like ``sql`%${`` for the WHERE — verify with `grep -E "sql\\\`%\\\\\$\\{" src/data/catalog.ts | grep -v "^#"` returns 0 lines (the only ${} in `%${token}%` is TS template literal binding into ilike(), not raw sql template).
    - `npm run test -- --run src/data/__tests__/catalog-search-tokens.test.ts` ALL 5 tests PASS (GREEN) — `grep -E "Tests.*passed" /tmp/72-01-task2.log` confirms.
    - `npm run build` exit 0 (authoritative gate per `project_baseline_not_green_build_is_gate`).
  </acceptance_criteria>
  <done>
    SRCH-01 DAL test suite green (5/5). Build gate exit 0. WHERE clause is AND-of-ORs per token; T-67-02-01 parameterization mitigation preserved; exact-ref + popularity ORDER BY tier unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action (`searchCatalogForAddFlowAction`) | User-typed `q` string crosses from the browser to the Server Action, then to the DAL. |
| server action → DAL `searchCatalogForAddFlow` | Validated query string passes into Drizzle ilike() bind parameters. |
| DAL → Postgres | All token patterns bound as `$1`, `$2`, … placeholders; never inlined as SQL text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01-01 | T (Tampering / SQL injection) | `searchCatalogForAddFlow` WHERE construction | mitigate | Drizzle parameterized binds: each `pattern = \`%${token}%\`` is a TypeScript string passed to `ilike(column, pattern)` — Drizzle emits `$N` placeholders, never string-interpolated SQL. Per-D-04 lock + T-67-02-01 mitigation reaffirmed in docstring. AC: source-grep that `%${` does NOT appear inside a raw `sql\`\`` template for the WHERE block. |
| T-72-01-02 | I (Information disclosure) | DAL result mapping | accept | `viewerState` hydration uses `eq(watches.userId, viewerId)` already in place (line 614). No change. Each call is viewer-keyed; tokenization does not widen the candidate set beyond what the viewer can see. |
| T-72-01-03 | D (Denial of service via expensive query) | Multi-token AND-of-ORs WHERE | accept | Catalog scale per CONTEXT.md is <500 watches per user / O(thousands) catalog rows. ILIKE without index assists is acceptable at this scale; Postgres tsvector + GIN deferred to SEED-009 (per CONTEXT.md Deferred Ideas). No new attack surface vs. today's single-token query. |
</threat_model>

<verification>
## Phase-level checks (this plan)

- `npm run test -- --run src/data/__tests__/catalog-search-tokens.test.ts` — 5/5 tests pass GREEN.
- `npm run build` exit 0 — authoritative gate per `project_baseline_not_green_build_is_gate`.
- `grep -c "T-67-02-01" src/data/catalog.ts` ≥ 1 — security mitigation comment preserved.
- `grep -c "and(\\.\\.\\.tokenClauses)" src/data/catalog.ts` ≥ 1 — fix landed.

## Manual UAT (Phase 72 prod bundle — NOT this plan)

- After v8.1 deploy: open `/watch/new`, type `"Brut Datejust"` — confirm a row surfaces in the combobox. Repeat with `"Timex Weekender"`. Repeat with `"Datejust Brut"` (order-invariance). Single-token `"Brut"` still returns matches.
</verification>

<success_criteria>
- 5/5 SRCH-01 DAL regression tests PASS.
- `npm run build` exit 0.
- `searchCatalogForAddFlow` source contains the tokenized AND-of-ORs WHERE (`and(...tokenClauses)`).
- T-67-02-01 mitigation comment preserved in docstring.
- exactRefOrderTier + ORDER BY popularity tier chain unchanged.
- No new imports required (drizzle `and`, `or`, `ilike`, `sql` already at line 8).
- No catalog row edits made (D-01).
- No test-runner config changes (D-12).
</success_criteria>

<output>
After completion, create `.planning/phases/72-search-composition-fixes/72-01-SUMMARY.md` covering:
- WHERE-clause before/after one-liner.
- 5 test names + their final state (all green).
- Build gate evidence (exit code).
- Decisions cited: D-01, D-02, D-03, D-04, D-11, D-12.
- Forward signal for Plan 02: Plan 02 touches a DIFFERENT file (`SearchEntry.tsx`); same wave; no coupling.
</output>
