---
phase: 41-account-danger-zone-branded-auth-emails-parallel-track
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/account-wipe.test.ts
  - tests/integration/account-delete.test.ts
  - tests/components/WipeCollectionModal.test.tsx
  - tests/components/DeleteAccountModal.test.tsx
  - tests/static/email-templates.test.ts
  - package.json
  - package-lock.json
  - .gitignore
  - .env.example
autonomous: false
requirements: [SET-13, SET-14]
user_setup:
  - service: supabase
    why: "Delete Account uses a service-role client for auth.admin.deleteUser(); the exact env var name must be confirmed against the live Supabase dashboard (legacy `service_role` vs newer `secret` naming)."
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key (or Secret key under newer API key naming)"
    dashboard_config:
      - task: "Confirm the exact env var name and add the key to .env.local and Vercel project env"
        location: "Supabase Dashboard -> Project Settings -> API"

must_haves:
  truths:
    - "react-email + @react-email/components are installed as devDependencies"
    - "Five RED test files exist covering both phase requirements before any production code is written"
    - "The exact service-role env var name is confirmed by the operator and documented in .env.example"
  artifacts:
    - path: "tests/integration/account-wipe.test.ts"
      provides: "RED integration coverage for wipeCollection DB scope"
      contains: "wipeCollection"
    - path: "tests/integration/account-delete.test.ts"
      provides: "RED integration coverage for deleteAccount storage-before-DB ordering + public.users cascade"
      contains: "deleteAccount"
    - path: "tests/components/WipeCollectionModal.test.tsx"
      provides: "RED component coverage for type-to-confirm WIPE gate + step flow"
      contains: "WIPE"
    - path: "tests/components/DeleteAccountModal.test.tsx"
      provides: "RED component coverage for type-to-confirm DELETE gate + step flow"
      contains: "DELETE"
    - path: "tests/static/email-templates.test.ts"
      provides: "RED static coverage for exported email HTML properties"
      contains: "ConfirmationURL"
  key_links:
    - from: "package.json devDependencies"
      to: "react-email + @react-email/components"
      via: "npm install --save-dev"
      pattern: "react-email"
---

<objective>
Wave 0 scaffold for Phase 41. Install the Track B email tooling, create the five
RED test files that downstream plans (41-02, 41-03, 41-04) turn GREEN, and close
the one operator-input dependency: the exact name of the service-role env var.

Purpose: The Nyquist rule requires every downstream `<verify>` to reference an
existing test file. RESEARCH §Validation Architecture lists five MISSING test
files — this plan creates all five as RED scaffolds so 41-02/03/04 have a
deterministic GREEN target. The service-role env var name (RESEARCH Open
Question 1 / A1) is a hard blocker for Delete Account and must be resolved
before 41-02 runs.
Output: 5 RED test files, `react-email` + `@react-email/components` in
devDependencies, `emails/out/` gitignored, `.env.example` documents the
service-role key, operator has confirmed the env var name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-account-danger-zone-branded-auth-emails-parallel-track/41-RESEARCH.md
@.planning/phases/41-account-danger-zone-branded-auth-emails-parallel-track/41-VALIDATION.md

<interfaces>
<!-- Contracts the RED test scaffolds assert against. Extracted from codebase. -->

From src/lib/actionTypes.ts:
```typescript
export type ActionResult<T = void> =
  | { success: true; data: T }
  | { success: false; error: string }
```

From src/lib/auth.ts:
```typescript
export async function getCurrentUser(): Promise<{ id: string; email: string }>;
```

Server action contracts that 41-02 will create (assert these signatures in RED tests):
- `wipeCollection(): Promise<ActionResult<void>>` — exported from `src/app/actions/account.ts`
- `deleteAccount(): Promise<ActionResult<void>>` — exported from `src/app/actions/account.ts`

Modal component contracts that 41-03 will create:
- `WipeCollectionModal` — named export from `src/components/settings/WipeCollectionModal.tsx`; props include `open: boolean`, `onOpenChange: (next: boolean) => void`, `currentEmail: string`
- `DeleteAccountModal` — named export from `src/components/settings/DeleteAccountModal.tsx`; same props shape

Email export contract that 41-04 will produce:
- `emails/out/confirm-signup.html`, `emails/out/reset-password.html`, `emails/out/change-email.html` — static HTML files produced by `npx react-email export`
</interfaces>

<test_env_note>
vitest.config.ts does NOT auto-load .env.local (STATE.md Phase 36-04 lesson).
DB-touching integration tests must gate on env presence and skip cleanly when
absent: `const maybe = process.env.DATABASE_URL && process.env.SUPABASE_SERVICE_ROLE_KEY ? describe : describe.skip`.
To run them locally: `set -a; source .env.local; set +a; npx vitest run <file>`.
</test_env_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-email tooling and configure build exclusion</name>
  <files>package.json, package-lock.json, .gitignore</files>
  <read_first>
    - package.json (current dependency layout — confirm react-email is NOT already present)
    - .gitignore (current ignore rules — append emails/out/)
    - .planning/phases/41-account-danger-zone-branded-auth-emails-parallel-track/41-RESEARCH.md §Standard Stack, §Environment Availability
  </read_first>
  <action>
    Run `npm install --save-dev react-email@6.1.4 @react-email/components@1.0.12`
    (RESEARCH §Standard Stack: 6.1.4 is npm latest, 1.0.12 is the components
    package; both go in devDependencies — `emails/` is build-excluded and never
    imported by the Next.js app). Append `emails/out/` to `.gitignore` under the
    existing testing/build section — the exported HTML is regenerable via
    `npx react-email export` and the committed source of truth is the `.tsx`
    files (RESEARCH Open Question 2). Do NOT modify `tsconfig.json` in this task
    — the `"emails"` exclude entry is owned by plan 41-04 Task 1.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.devDependencies['react-email']||!p.devDependencies['@react-email/components']) process.exit(1)" && grep -q 'emails/out' .gitignore</automated>
  </verify>
  <done>
    - `package.json` `devDependencies` contains both `react-email` and `@react-email/components`
    - `react-email` does NOT appear in `dependencies` (only `devDependencies`)
    - `.gitignore` contains a line matching `emails/out`
    - `package-lock.json` is updated (npm install completed)
    - `npx react-email --help` exits 0 (CLI is resolvable)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create five RED test scaffolds for Track A and Track B</name>
  <files>tests/integration/account-wipe.test.ts, tests/integration/account-delete.test.ts, tests/components/WipeCollectionModal.test.tsx, tests/components/DeleteAccountModal.test.tsx, tests/static/email-templates.test.ts</files>
  <read_first>
    - .planning/phases/41-account-danger-zone-branded-auth-emails-parallel-track/41-RESEARCH.md §Validation Architecture (Phase Requirements -> Test Map, Wave 0 Gaps)
    - .planning/phases/41-account-danger-zone-branded-auth-emails-parallel-track/41-UI-SPEC.md §Copywriting Contract, §Modal Interaction Contract
    - tests/integration/phase36-rls.test.ts (existing DB-gated integration test pattern — env-gated describe.skip)
    - src/components/settings/PasswordReauthDialog.tsx (modal pattern the modal tests mirror)
    - src/lib/actionTypes.ts (ActionResult contract)
  </read_first>
  <action>
    Create five RED test files. Each MUST reference a not-yet-existing module so
    the suite is genuinely RED until the downstream plan ships:

    1. `tests/integration/account-wipe.test.ts` — `import { wipeCollection } from '@/app/actions/account'`.
       Env-gate: `const maybe = process.env.DATABASE_URL ? describe : describe.skip`.
       Assert: `wipeCollection` returns `{ success: true }` after deleting a
       seeded `watches` row + `wear_events` row for a test user, and that
       `profiles`/`follows` rows for that user SURVIVE the call (preserve-account
       contract from SET-13).
    2. `tests/integration/account-delete.test.ts` — `import { deleteAccount } from '@/app/actions/account'`.
       Env-gate on `DATABASE_URL && SUPABASE_SERVICE_ROLE_KEY`. Assert: storage
       purge call ordering precedes any DB delete (assert call order via a
       spy/mock), and a `public.users` row for the test user is gone after the
       call (RESEARCH Pitfall 1 — the explicit public.users delete).
    3. `tests/components/WipeCollectionModal.test.tsx` — `import { WipeCollectionModal } from '@/components/settings/WipeCollectionModal'`.
       The modal imports `wipeCollection` from `@/app/actions/account` (a
       `'use server'` module that is DB-backed and cannot run in jsdom), so this
       test file MUST begin with `vi.mock('@/app/actions/account', () => ({ wipeCollection: vi.fn() }))`
       — mock the server-action module so the component test stays deterministic
       and never invokes a real DB-backed action. Also stub
       `@/lib/supabase/client` (`createSupabaseBrowserClient`) so `signInWithPassword`
       is a controllable mock. Assert with @testing-library/react: step-1 warning
       text renders; clicking Continue advances to step 2; the execute button is
       `disabled` until the Confirm input value === `WIPE` AND the password field
       is non-empty (D-05).
    4. `tests/components/DeleteAccountModal.test.tsx` — `import { DeleteAccountModal } from '@/components/settings/DeleteAccountModal'`.
       This file MUST also begin with `vi.mock('@/app/actions/account', () => ({ deleteAccount: vi.fn() }))`
       (the modal imports the DB-backed `deleteAccount` server action — mock it so
       jsdom never reaches a real DB) and likewise stub `@/lib/supabase/client`.
       Same shape; keyword is `DELETE` (D-04); assert no success toast is
       configured (UI-SPEC line 195).
    5. `tests/static/email-templates.test.ts` — reads `emails/out/*.html` via
       `fs.readFileSync`. Gate the assertions behind an existence check:
       `const exported = fs.existsSync('emails/out/confirm-signup.html')`; use
       `(exported ? it : it.skip)`. Assert each exported HTML: contains the
       literal substring `{{ .ConfirmationURL }}`, contains no `oklch(`, contains
       the 600px container width, and contains exactly one `<a` CTA anchor.

    All five files must be syntactically valid and runnable (`npx vitest run`
    must collect them without a parse error) — they are RED via failing
    assertions or unresolved imports, not via syntax errors.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/account-wipe.test.ts tests/integration/account-delete.test.ts tests/components/WipeCollectionModal.test.tsx tests/components/DeleteAccountModal.test.tsx tests/static/email-templates.test.ts 2>&1 | grep -E 'Test Files|FAIL|skipped' | head -20</automated>
  </verify>
  <done>
    - All five test files exist at the paths listed in `files_modified`
    - `tests/integration/account-wipe.test.ts` contains the string `wipeCollection` and an env-gated `describe.skip` fallback
    - `tests/integration/account-delete.test.ts` contains `deleteAccount` and asserts storage-purge-before-DB ordering
    - `tests/components/WipeCollectionModal.test.tsx` begins with `vi.mock('@/app/actions/account', ...)` and asserts the execute button is `disabled` until input === `WIPE`
    - `tests/components/DeleteAccountModal.test.tsx` begins with `vi.mock('@/app/actions/account', ...)` and asserts the gate on keyword `DELETE`
    - `tests/static/email-templates.test.ts` greps for literal `{{ .ConfirmationURL }}` and absence of `oklch(`, with `it.skip` fallback when `emails/out/` is absent
    - `npx vitest run` collects all five files with no parse/collection error (RED is from assertions/imports, not syntax)
  </done>
</task>

<!--
  Task 3 (positional) is the human-action checkpoint below. The .env.example
  auto-task that follows is explicitly Task 4 — the checkpoint and the
  .env.example task now carry distinct, sequential labels.
-->
<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Tasks 1-2 installed react-email tooling and created the five RED test
    scaffolds. The Delete Account server action (plan 41-02) needs the Supabase
    service-role key, whose env var name cannot be determined from the codebase
    (RESEARCH Open Question 1 / A1 — legacy `service_role` vs newer `secret`
    naming). This is the one piece of information no tool can fetch. This is
    the Task 3 checkpoint; the .env.example documentation task that follows is
    Task 4.
  </what-built>
  <how-to-verify>
    1. Open the Supabase Dashboard for the Horlo project -> Project Settings -> API.
    2. Locate the service-role key (it may be labelled "service_role" under the
       legacy "Project API keys" panel, or "Secret keys" under the newer API
       key naming — copy whichever grants admin/service access).
    3. Add it to `.env.local` as `SUPABASE_SERVICE_ROLE_KEY=<value>`. If you
       prefer a different var name, note it — plan 41-02 will read whatever name
       you confirm here.
    4. Add the same key to the Vercel project environment variables (production
       Delete Account needs it server-side).
    5. Confirm: reply with the exact env var name you used (default expectation:
       `SUPABASE_SERVICE_ROLE_KEY`).
  </how-to-verify>
  <resume-signal>Reply with the confirmed env var name (e.g. "SUPABASE_SERVICE_ROLE_KEY") or describe any deviation.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Document the service-role env var in .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (current documented env vars)
    - .planning/phases/41-account-danger-zone-branded-auth-emails-parallel-track/41-RESEARCH.md §Runtime State Inventory (Secrets / env vars row)
  </read_first>
  <action>
    Append a documented entry for the service-role key to `.env.example`, using
    the exact var name confirmed in the preceding checkpoint (the Task 3
    human-action checkpoint; default `SUPABASE_SERVICE_ROLE_KEY`). Add a comment
    block explaining: required for the Delete Account server action
    (`auth.admin.deleteUser()`); server-only; bypasses RLS; sourced from
    Supabase Dashboard -> Project Settings -> API. The value MUST be a redacted
    placeholder (`eyJhbGci...` or `<service-role-key>`), never a real key.
  </action>
  <verify>
    <automated>grep -qE 'SUPABASE_SERVICE_ROLE_KEY|SUPABASE_SECRET_KEY' .env.example</automated>
  </verify>
  <done>
    - `.env.example` contains the confirmed service-role env var name with a redacted placeholder value
    - A comment above it explains it is server-only, used by Delete Account, and sourced from the Supabase dashboard
    - No real key value appears anywhere in `.env.example`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> .env.example | A documentation file — must never carry a real secret value |
| operator -> .env.local / Vercel env | The real service-role key is supplied out-of-band by the operator; never committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Information Disclosure | `.env.example` | mitigate | Task 4 acceptance criterion forbids any real key value; placeholder only. `.gitignore` already excludes `.env*` so `.env.local` (the real key) is never committed. |
| T-41-02 | Information Disclosure | RED test files | accept | The RED scaffolds reference env vars by name but contain no secret values; test runs gate on env presence and skip when absent. No exposure surface. |
</threat_model>

<verification>
- `npm test -- --run` collects all five new test files without parse errors
- `package.json` carries `react-email` + `@react-email/components` in devDependencies only
- `.gitignore` excludes `emails/out/`
- `.env.example` documents the service-role key (redacted)
- The operator has confirmed the service-role env var name
</verification>

<success_criteria>
- All five RED test files exist and are collected by vitest
- react-email tooling installed; build-exclusion groundwork (gitignore) in place
- Operator-input dependency (service-role env var name) resolved and documented
</success_criteria>

<output>
After completion, create `.planning/phases/41-account-danger-zone-branded-auth-emails-parallel-track/41-01-SUMMARY.md`
</output>
