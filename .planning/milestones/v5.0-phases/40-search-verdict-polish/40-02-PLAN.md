---
phase: 40
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/verdict/types.ts
  - src/lib/verdict/composer.ts
  - tests/static/composer-engine-alignment.test.ts
autonomous: true
requirements: [FIT-05]
tags: [verdict, composer, catalog-taste, types]
must_haves:
  truths:
    - "VerdictBundleFull exposes candidateCatalogTaste: CatalogTasteAttributes | null"
    - "computeVerdictBundle threads the candidate's CatalogEntry taste fields into the bundle with numeric coercion at the postgres-js boundary"
    - "When catalogEntry is null/undefined, candidateCatalogTaste is null (no synthesis)"
    - "All 3 call sites of computeVerdictBundle work without signature changes (catalogEntry already passed)"
  artifacts:
    - path: "src/lib/verdict/types.ts"
      provides: "VerdictBundleFull.candidateCatalogTaste field"
      contains: "candidateCatalogTaste"
    - path: "src/lib/verdict/composer.ts"
      provides: "Returns bundle with candidateCatalogTaste built from catalogEntry"
      contains: "candidateCatalogTaste"
  key_links:
    - from: "src/lib/verdict/composer.ts"
      to: "src/lib/types.ts CatalogTasteAttributes"
      via: "imported via @/lib/types"
      pattern: "CatalogTasteAttributes"
    - from: "src/lib/verdict/composer.ts"
      to: "Number() coercion"
      via: "postgres-js boundary"
      pattern: "Number\\(catalogEntry\\."
---

<objective>
Extend `VerdictBundleFull` to carry the candidate watch's `catalogTaste` and thread it through `computeVerdictBundle`. The RESEARCH.md "CRITICAL OPEN QUESTION" was resolved: `mostSimilar[0].watch.catalogTaste` is already present (Phase 38 LEFT JOIN), but the candidate's own taste is NOT — `catalogEntryToSimilarityInput` drops taste fields. This plan closes that gap so the FIT-05 compare table (40-06) can read both sides directly off the bundle.

Purpose: Deliver FIT-05 data contract. Without this, the FIT-05 component has no candidate-side taste data to compare against.

Output: Type-extended bundle, composer threading + numeric coercion, an extended composer-alignment test asserting the new field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-search-verdict-polish/40-CONTEXT.md
@.planning/phases/40-search-verdict-polish/40-RESEARCH.md
@.planning/phases/40-search-verdict-polish/40-PATTERNS.md

<interfaces>
From `src/lib/verdict/types.ts:22-35` (current `VerdictBundleFull` shape):
```
export interface VerdictBundleFull {
  framing: 'same-user' | 'cross-user'
  label: SimilarityLabel
  headlinePhrasing: string
  contextualPhrasings: string[]
  rationalePhrasings: string[]
  mostSimilar: VerdictMostSimilar[]
  roleOverlap: boolean
}
```
Add: `candidateCatalogTaste: CatalogTasteAttributes | null` (per D-15 module-absent-not-empty pattern — null when catalogEntry is null).

From `src/lib/types.ts:220-229` (current `CatalogTasteAttributes` interface):
```
export interface CatalogTasteAttributes {
  formality: number | null
  sportiness: number | null
  heritageScore: number | null
  primaryArchetype: PrimaryArchetype | null
  eraSignal: EraSignal | null
  designMotifs: string[]
  confidence: number | null
  extractedFromPhoto: boolean
}
```

From `src/lib/verdict/composer.ts:43-97` (current `computeVerdictBundle` function). The existing `candidateTaste` block at lines 47-53 already reads from `catalogEntry`:
```
const candidateTaste: CandidateTasteSnapshot = {
  primaryArchetype: catalogEntry?.primaryArchetype ?? null,
  heritageScore: catalogEntry?.heritageScore ?? null,
  formality: catalogEntry?.formality ?? null,
  sportiness: catalogEntry?.sportiness ?? null,
  confidence: catalogEntry?.confidence ?? null,
}
```
This is a 5-field internal helper for template predicates — NOT the full 8-field `CatalogTasteAttributes`. The new `candidateCatalogTaste` is a separate construction (full 8-field shape, with numeric coercion).

From `src/data/catalog.ts:60-89` `mapRowToCatalogEntry` already applies `Number()` coercion at lines 77, 78, 79, 83 — so `catalogEntry` reaching the composer ALREADY has numeric (not string) values for formality/sportiness/heritageScore/confidence. The numeric coercion in the composer is therefore IDEMPOTENT defense-in-depth, not load-bearing (`Number(0.72)` returns `0.72`). Per RESEARCH Pitfall 2 + Assumption A2, apply it anyway — costs nothing and protects against future regression of the DAL mapper.

Three call sites to verify (no changes needed — all 3 already pass `catalogEntry`):
- `src/app/watch/[id]/page.tsx:52` — passes `catalogEntry` from `getCatalogById(watch.catalogId)`
- `src/app/catalog/[catalogId]/page.tsx:117` — passes `catalogEntry`
- `src/app/actions/verdict.ts:63` — passes `catalogEntry`

From `tests/static/composer-engine-alignment.test.ts` (existing test file — extend with new assertion for `candidateCatalogTaste` field).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add candidateCatalogTaste field to VerdictBundleFull</name>
  <files>src/lib/verdict/types.ts</files>
  <read_first>
    - src/lib/verdict/types.ts (entire file — particularly lines 3 imports + lines 22-35 VerdictBundleFull)
    - src/lib/types.ts (lines 217-229 CatalogTasteAttributes interface for the type-only import)
    - .planning/phases/40-search-verdict-polish/40-PATTERNS.md § `src/lib/verdict/types.ts` — extend `VerdictBundleFull`
  </read_first>
  <action>Extend the type-only import at line 3 to include `CatalogTasteAttributes` alongside `Watch, SimilarityLabel, PrimaryArchetype, EraSignal`. Add a new field `candidateCatalogTaste: CatalogTasteAttributes | null` to `VerdictBundleFull` after `roleOverlap: boolean` at line 34. Add a JSDoc comment immediately above the new field: `/** Phase 40 FIT-05 D-14/D-15 — candidate's CAT-13 taste from catalogEntry; null when catalogEntry is null. Confidence-gate (>= 0.5) applied DOWNSTREAM in CollectionFitCard, not here. */`. Do not modify any other field; do not modify `VerdictBundleSelfOwned` (D-08 self-owned branch doesn't compute taste). Do not modify `CandidateTasteSnapshot` — it remains the internal 5-field shape used by template predicates.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "verdict/types\\.ts" | wc -l | xargs -I {} test {} -eq 0 && grep -c "candidateCatalogTaste" src/lib/verdict/types.ts</automated>
  </verify>
  <done>`grep -c "candidateCatalogTaste" src/lib/verdict/types.ts` returns 1. `grep -c "CatalogTasteAttributes" src/lib/verdict/types.ts` returns at least 2 (import + field type). tsc shows no new errors in `verdict/types.ts`. After this task, the project tsc baseline INCREASES temporarily because the 3 call sites construct `VerdictBundleFull` literals without `candidateCatalogTaste` — Task 2 closes that gap in `composer.ts` (the only producer of these literals). Commit: `feat(40-02): add candidateCatalogTaste field to VerdictBundleFull`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread candidateCatalogTaste through composer + extend composer-engine-alignment test</name>
  <files>src/lib/verdict/composer.ts, tests/static/composer-engine-alignment.test.ts</files>
  <read_first>
    - src/lib/verdict/composer.ts (entire file — particularly lines 1-9 imports, lines 43-97 computeVerdictBundle body)
    - src/lib/types.ts (CatalogTasteAttributes interface — 8 fields including extractedFromPhoto)
    - src/data/catalog.ts:60-89 mapRowToCatalogEntry (confirms catalogEntry fields arriving at composer already have Number() coercion applied — Pitfall 2 RESEARCH assumption A2)
    - tests/static/composer-engine-alignment.test.ts (existing test file — extend with new assertion)
    - .planning/phases/40-search-verdict-polish/40-PATTERNS.md § `src/lib/verdict/composer.ts` — thread `candidateCatalogTaste` into bundle
    - .planning/phases/40-search-verdict-polish/40-RESEARCH.md § Pattern 3 (full snippet) + § Pitfall 2 (numeric coercion)
  </read_first>
  <behavior>
    - Import `CatalogTasteAttributes` from `@/lib/types` (alongside the existing `Watch, UserPreferences, CatalogEntry` type-only imports at line 3).
    - Inside `computeVerdictBundle`, AFTER the existing `candidateTaste` snapshot block (line 53) and BEFORE the confidence-gating logic at line 55, construct:
      ```
      const candidateCatalogTaste: CatalogTasteAttributes | null = catalogEntry
        ? {
            formality: catalogEntry.formality !== null ? Number(catalogEntry.formality) : null,
            sportiness: catalogEntry.sportiness !== null ? Number(catalogEntry.sportiness) : null,
            heritageScore: catalogEntry.heritageScore !== null ? Number(catalogEntry.heritageScore) : null,
            primaryArchetype: catalogEntry.primaryArchetype,
            eraSignal: catalogEntry.eraSignal,
            designMotifs: catalogEntry.designMotifs ?? [],
            confidence: catalogEntry.confidence !== null ? Number(catalogEntry.confidence) : null,
            extractedFromPhoto: catalogEntry.extractedFromPhoto ?? false,
          }
        : null
      ```
    - Add `candidateCatalogTaste` to the returned object literal at lines 85-96 (immediately after `roleOverlap: result.roleOverlap`).
    - Extend `tests/static/composer-engine-alignment.test.ts` with a new `it(...)` block (or a new `describe` group) asserting that the composer source contains a `candidateCatalogTaste:` field in the return literal: `expect(readFileSync('src/lib/verdict/composer.ts','utf8')).toMatch(/candidateCatalogTaste:\s*catalogEntry/)`. This is a source-text guard ensuring the threading is wired — RESEARCH Q5 confirmed-plan acceptance criterion.
  </behavior>
  <action>Apply the named imports + the local `candidateCatalogTaste` construction + the return-object extension at the exact insertion sites described above. Apply numeric coercion to formality / sportiness / heritageScore / confidence per Pitfall 2 — idempotent on already-coerced values, defends against future DAL-mapper drift. Use `?? []` for `designMotifs` and `?? false` for `extractedFromPhoto` (Pitfall 6-style defensive nulling at the boundary even though the DAL mapper at catalog.ts:73 + line 84 already returns non-null defaults). Do NOT modify the existing `candidateTaste` 5-field snapshot — it remains the input to template predicates and is unchanged. Add the new test assertion to `tests/static/composer-engine-alignment.test.ts` following the file's existing describe/it structure.</action>
  <verify>
    <automated>npx vitest run tests/static/composer-engine-alignment.test.ts && npx tsc --noEmit 2>&1 | grep -E "verdict/(composer|types)\\.ts" | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>Extended `composer-engine-alignment.test.ts` GREEN including the new `candidateCatalogTaste:` source assertion. `grep -c "candidateCatalogTaste" src/lib/verdict/composer.ts` returns at least 2 (declaration + return-object field). `grep -c "Number(catalogEntry\\." src/lib/verdict/composer.ts` returns at least 4 (formality, sportiness, heritageScore, confidence). tsc shows no new errors in `verdict/composer.ts` AND the 3 call sites (`/watch/[id]/page.tsx`, `/catalog/[catalogId]/page.tsx`, `actions/verdict.ts`) — because they pass `catalogEntry` already and the composer constructs the new field internally. Project tsc baseline returns to pre-Task-1 count (27-28 baseline). Commit: `feat(40-02): thread candidateCatalogTaste through composer with numeric coercion`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres → composer | `catalogEntry` flows through `mapRowToCatalogEntry` (DAL) — already trusted/coerced; composer applies defense-in-depth `Number()` at the JS boundary. |
| Composer → RSC client serialization | `VerdictBundleFull` is shipped from Server Component to Client (CollectionFitCard) — every field must be JSON-serializable (no Date, Map, Set, undefined-as-property). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06 | Tampering | RSC serialization boundary | mitigate | `candidateCatalogTaste` is a plain object with primitive/array values; `extractedFromPhoto` defaults to `false` (no undefined). No Date / Map / Set introduced. Matches the Pitfall 3 contract from `src/lib/verdict/types.ts` JSDoc at line 12. |
| T-40-07 | Information Disclosure | candidate taste data | accept | Catalog taste fields are public catalog metadata (already exposed on `/catalog/{id}` pages). No PII; viewer can already read this data by visiting the catalog page directly. |
| T-40-08 | Spoofing | numeric coercion bypass | accept | The composer trusts `mapRowToCatalogEntry`'s output (already-coerced). The defense-in-depth `Number()` wrapper is idempotent and harmless on already-numeric values. |
</threat_model>

<verification>
```
npx vitest run tests/static/composer-engine-alignment.test.ts tests/static/CollectionFitCard.no-engine.test.ts
npx tsc --noEmit 2>&1 | grep -E "verdict/" | wc -l
```
Expected: composer-engine-alignment test GREEN with the new assertion. CollectionFitCard.no-engine still GREEN (this plan does not touch CollectionFitCard.tsx). tsc errors in `verdict/` files: 0.
</verification>

<success_criteria>
- `VerdictBundleFull.candidateCatalogTaste: CatalogTasteAttributes | null` field exists in `src/lib/verdict/types.ts`
- `computeVerdictBundle` returns the bundle with `candidateCatalogTaste` populated from `catalogEntry` (or null when `catalogEntry` is null)
- All 4 scalar fields use `Number()` coercion per Pitfall 2
- Extended `composer-engine-alignment.test.ts` GREEN
- No new tsc errors in any of: `verdict/types.ts`, `verdict/composer.ts`, `app/watch/[id]/page.tsx`, `app/catalog/[catalogId]/page.tsx`, `app/actions/verdict.ts`
- `tests/static/CollectionFitCard.no-engine.test.ts` stays green (unchanged)
</success_criteria>

<output>
Create `.planning/phases/40-search-verdict-polish/40-02-SUMMARY.md` documenting:
- Exact line numbers where the new field was added in `types.ts` and `composer.ts`
- Confirmation that the 3 call sites required NO modification (catalogEntry already passed)
- Numeric coercion choice (idempotent / defense-in-depth) per Pitfall 2 + Assumption A2
- The new source assertion added to `composer-engine-alignment.test.ts`
</output>