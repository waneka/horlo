---
phase: 40
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SRCH-16]
tags: [docs, paperwork, requirements]
must_haves:
  truths:
    - "REQUIREMENTS.md SRCH-16 entry no longer says 'numeric range slider' — replaced with 'chip group with 5 pre-defined size bands'"
    - "The contradiction between REQUIREMENTS.md SRCH-16 and ROADMAP §Phase 40 SC#1 is resolved in favor of D-05 chip group"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated SRCH-16 description aligned with Phase 40 D-05"
      contains: "chip group with 5 pre-defined size bands"
  key_links:
    - from: ".planning/REQUIREMENTS.md SRCH-16 entry"
      to: ".planning/ROADMAP.md §Phase 40 SC#1"
      via: "consistent specification of Case Size as chip group"
      pattern: "chip group"
---

<objective>
Resolve the documented contradiction between `.planning/REQUIREMENTS.md` SRCH-16 (says "numeric range slider") and `.planning/ROADMAP.md` Phase 40 SC#1 (says "chip group") by updating REQUIREMENTS.md to match the locked Phase 40 D-05 decision. This is a paperwork-only plan; no source code changes. It runs in Wave 1 in parallel with the engineering plans.

Purpose: Close the "required follow-up paperwork" item from `40-CONTEXT.md` line 220.

Output: One edit to `.planning/REQUIREMENTS.md` line 50 (SRCH-16 entry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/40-search-verdict-polish/40-CONTEXT.md

<interfaces>
From `.planning/REQUIREMENTS.md:50` (current text):
```
- [ ] **SRCH-16**: `/search` Watches tab gains three faceted filters: Movement Type (auto/manual/quartz/spring_drive — sourced from `watches_catalog.movement_type` enum), Case Size (numeric range slider — sourced from `case_size_mm`), Style (multi-select chip group — sourced from existing `style_tags`). Mobile UX: bottom-sheet filter pattern. Hard-blocked on CAT-16 Layer B `movement_type` enum — if Layer B slips, SRCH-16 defers to v5.x.
```

From `.planning/ROADMAP.md` line 440 (Phase 40 SC#1):
```
1. `/search` Watches tab has three faceted filters: Movement Type chip group (`auto` / `manual` / `quartz` / `spring_drive` from `watches_catalog.movement_type`), Case Size chip group (pre-defined size bands from `case_size_mm`), Style multi-select chip group (from `style_tags`) — all filters narrow results without a full page reload
```

From `.planning/phases/40-search-verdict-polish/40-CONTEXT.md` § D-05:
```
D-05 — Case Size = 5 chip bands. Exactly `<36` / `36-39` / `40-42` / `43-45` / `46+`. Each chip maps to a DAL `between()` predicate. ... This decision resolves the contradiction in favor of chip group — simpler, matches Movement / Style chip-group idiom, single component pattern. REQUIREMENTS.md must be updated to reflect this resolution.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md SRCH-16 to chip group</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 50 specifically — the SRCH-16 entry)
    - .planning/phases/40-search-verdict-polish/40-CONTEXT.md § D-05 (the resolving decision)
    - .planning/ROADMAP.md §Phase 40 SC#1 (the canonical chip-group framing)
  </read_first>
  <behavior>
    Replace the text `Case Size (numeric range slider — sourced from \`case_size_mm\`)` with `Case Size (chip group with 5 pre-defined size bands: \`<36\` / \`36-39\` / \`40-42\` / \`43-45\` / \`46+\` — sourced from \`case_size_mm\`)`.

    The rest of the SRCH-16 line is unchanged. No other lines in REQUIREMENTS.md are modified.

    The Phase 40 D-05 ID is referenced in an inline parenthetical at the end of the SRCH-16 description: append `Resolved per Phase 40 D-05.` to the end of the requirement description (after `... defers to v5.x.`).
  </behavior>
  <action>Edit `.planning/REQUIREMENTS.md` line 50 per the exact text replacement described in `<behavior>`. Use the literal `<` and `+` characters in the band list (these are markdown-safe; not HTML rendering — the surrounding backticks code-span them). Append the trailing sentence `Resolved per Phase 40 D-05.` to make the supersession traceable to the discuss-phase decision ID.</action>
  <verify>
    <automated>grep -c "chip group with 5 pre-defined size bands" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chip group with 5 pre-defined size bands" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "numeric range slider" .planning/REQUIREMENTS.md` returns 0 (old phrasing fully removed)
    - `grep -c "Resolved per Phase 40 D-05" .planning/REQUIREMENTS.md` returns 1
    - All other lines in REQUIREMENTS.md byte-identical to pre-plan state (verify with `git diff .planning/REQUIREMENTS.md | grep -E "^\\+|^-" | grep -v "^\\+\\+\\+\\|^---" | wc -l` returns at most 4 line-changes — one removal + one or two additions for the multi-clause band list).
  </acceptance_criteria>
  <done>REQUIREMENTS.md SRCH-16 description aligned with ROADMAP §Phase 40 SC#1 and Phase 40 D-05. Commit: `docs(40-07): update REQUIREMENTS.md SRCH-16 from range slider to chip group per Phase 40 D-05`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change; no code surface. |

## STRIDE Threat Register

No security-relevant changes. Plan is paperwork-only — REQUIREMENTS.md is a project documentation file. No threats apply.
</threat_model>

<verification>
```
grep -n "SRCH-16" .planning/REQUIREMENTS.md
git diff --stat .planning/REQUIREMENTS.md
```
Expected: SRCH-16 line matches the new wording; git diff shows exactly 1 file modified, ~4 lines changed.
</verification>

<success_criteria>
- `numeric range slider` no longer appears in REQUIREMENTS.md
- `chip group with 5 pre-defined size bands` appears once on the SRCH-16 line
- `Resolved per Phase 40 D-05.` traceability sentence appended
- No other lines in REQUIREMENTS.md modified
</success_criteria>

<output>
Create `.planning/phases/40-search-verdict-polish/40-07-SUMMARY.md` documenting:
- The exact line edit (before / after)
- Cross-reference to ROADMAP §Phase 40 SC#1 and 40-CONTEXT.md D-05
- Confirmation no other REQUIREMENTS.md lines touched
</output>
