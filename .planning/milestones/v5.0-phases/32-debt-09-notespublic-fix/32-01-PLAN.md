---
phase: 32-debt-09-notespublic-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/actions/watches.ts
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DEBT-09
decisions_addressed:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    # Phase goal (ROADMAP §"Phase 32") — success criteria #1–#5, with #4 normalized to test-contract wording per D-05
    - "tests/actions/watches.notesPublic.test.ts reaches 4/4 GREEN in CI (was 4/4 FAIL at v4.1 close)"
    - "insertWatchSchema in src/app/actions/watches.ts accepts notesPublic: z.boolean().optional() (auto-flows to updateWatchSchema via .partial())"
    - "Both addWatch and editWatch persist notesPublic to the database on every write (via existing mapDomainToRow path)"
    - "Both addWatch and editWatch call revalidatePath('/u/[username]', 'layout') after every successful write"
    - "Full test suite remains GREEN — no new test failures introduced"
    # Decision coverage (CONTEXT D-01 through D-06)
    - "D-01: revalidatePath signature is ('/u/[username]', 'layout') — NOT ('/u/[username]/[tab]', 'page'); test scaffold is the contract"
    - "D-02: revalidate fires unconditionally on every successful add/edit — no 'notesPublic' in parsed.data gate"
    - "D-03: insertion order is revalidatePath('/') then revalidatePath('/u/[username]', 'layout') then revalidateTag('explore', 'max')"
    - "D-04: removeWatch is intentionally NOT modified — out of scope"
    - "D-05: ROADMAP.md success criterion #4 wording is corrected inline this phase to match the test scaffold"
    - "D-06: No new tests authored — the 4 existing tests are the GREEN target as-is"
  artifacts:
    - path: "src/app/actions/watches.ts"
      provides: "Action-layer Zod schema + revalidate calls (the only broken segment)"
      contains: "notesPublic: z.boolean().optional()"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected success criterion #4 wording (line 126)"
      contains: "revalidatePath('/u/[username]', 'layout')"
  key_links:
    - from: "src/app/actions/watches.ts insertWatchSchema (lines 17-54)"
      to: "src/data/watches.ts mapDomainToRow (line 84)"
      via: "Zod safeParse pass-through"
      pattern: "notesPublic: z.boolean\\(\\).optional\\(\\)"
    - from: "src/app/actions/watches.ts addWatch (line 268, post-edit)"
      to: "/u/[username]/notes per-row NoteVisibilityPill"
      via: "revalidatePath layout-scoped invalidation cascade"
      pattern: "revalidatePath\\('/u/\\[username\\]', 'layout'\\)"
    - from: "src/app/actions/watches.ts editWatch (line 341, post-edit)"
      to: "/u/[username]/notes per-row NoteVisibilityPill"
      via: "revalidatePath layout-scoped invalidation cascade"
      pattern: "revalidatePath\\('/u/\\[username\\]', 'layout'\\)"
---

<objective>
Repair the Phase 23-era data-loss regression in `src/app/actions/watches.ts` by adding `notesPublic` to `insertWatchSchema` and inserting `revalidatePath('/u/[username]', 'layout')` after the existing `revalidatePath('/')` in both `addWatch` (line 267) and `editWatch` (line 340). Correct the ROADMAP.md success criterion #4 wording inline to match the test scaffold's literal assertion. Turn `tests/actions/watches.notesPublic.test.ts` from 4/4 RED to 4/4 GREEN while holding the full suite at zero net new failures.

Purpose: Closes DEBT-09. The WatchForm UI, DB column, domain type, and DAL persistence path are all already correct on `main` — only the action-layer schema and revalidate are missing. The fix is mechanical: 3 source edits + 1 doc edit, totaling ~4 lines of added code.

Output: 4/4 GREEN unit tests in `tests/actions/watches.notesPublic.test.ts`, full vitest suite GREEN, ROADMAP wording aligned with test contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-debt-09-notespublic-fix/32-CONTEXT.md
@.planning/phases/32-debt-09-notespublic-fix/32-RESEARCH.md
@.planning/phases/32-debt-09-notespublic-fix/32-PATTERNS.md
@.planning/phases/32-debt-09-notespublic-fix/32-VALIDATION.md
@src/app/actions/watches.ts
@src/app/actions/notes.ts
@tests/actions/watches.notesPublic.test.ts

<interfaces>
Verified line numbers on `main` 2026-05-06 (RESEARCH §"Verified Insertion Sites"; re-confirmed by planner via grep before writing this plan):

src/app/actions/watches.ts:
  3   import { revalidatePath, revalidateTag } from 'next/cache'   (already imported — no new imports needed)
  17  const insertWatchSchema = z.object({
  40  notes: z.string().optional(),                                  (insert notesPublic immediately AFTER)
  54  })                                                              (close of insertWatchSchema z.object body)
  57  const updateWatchSchema = insertWatchSchema.partial()           (auto-derives — no second schema edit needed)
  267 revalidatePath('/')        in addWatch    (insert NEW line immediately AFTER)
  277 revalidateTag('explore', 'max')   in addWatch
  340 revalidatePath('/')        in editWatch   (insert NEW line immediately AFTER)
  348 revalidateTag('explore', 'max')   in editWatch
  372 revalidatePath('/')        in removeWatch (D-04: DO NOT MODIFY)

Test contract (tests/actions/watches.notesPublic.test.ts):
  line 131: expect(revalidatePath).toHaveBeenCalledWith('/u/[username]', 'layout')   (editWatch test)
  line 158: expect(revalidatePath).toHaveBeenCalledWith('/u/[username]', 'layout')   (addWatch test)

Sibling pattern (notes.ts:53-58 WR-07 commentary, profile.ts:34, follows.ts:53 — all use this signature):
  revalidatePath('/u/[username]', 'layout')

Negative pattern (DO NOT COPY) — wishlist.ts:206:
  revalidatePath('/u/[username]/[tab]', 'page')   (PATTERNS.md §"Negative / Anti-Pattern")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add notesPublic to insertWatchSchema and insert revalidatePath layout calls in both addWatch and editWatch</name>
  <files>src/app/actions/watches.ts</files>
  <read_first>
    - src/app/actions/watches.ts (read in full — verify line numbers 40, 267, 277, 340, 348 still match RESEARCH §"Verified Insertion Sites" before editing; if any line has drifted because of a parallel commit, re-locate by grepping for `revalidatePath\|insertWatchSchema\|updateWatchSchema\|revalidateTag` and apply the diffs at the new line numbers — the relative ordering is what matters, not the absolute line numbers)
    - .planning/phases/32-debt-09-notespublic-fix/32-CONTEXT.md (D-01 through D-06 — all locked decisions; do not relitigate)
    - .planning/phases/32-debt-09-notespublic-fix/32-RESEARCH.md §"Implementation Mechanics" (verbatim diff hunks 1, 2, 3) and §"Test Mocking Surface Analysis" (confirms zero regression risk — no test asserts call counts on revalidatePath for the watches actions)
    - .planning/phases/32-debt-09-notespublic-fix/32-PATTERNS.md §"Pattern Assignments" (verbatim diff hunks) and §"Negative / Anti-Pattern" (`wishlist.ts:206` is NOT the pattern)
    - src/app/actions/notes.ts lines 50-60 (WR-07 commentary block — canonical sibling pattern showing the exact `revalidatePath('/u/[username]', 'layout')` call)
    - tests/actions/watches.notesPublic.test.ts lines 80-165 (the GREEN contract — read the literal assertion strings at lines 131 and 158 so the call signature in the source matches them character-for-character)
  </read_first>
  <action>
    Apply three edits to `src/app/actions/watches.ts`. Use the Edit tool. The file is small enough that three sequential Edit calls are fine; one combined edit per insertion point is also acceptable.

    **Edit 1 — Schema field insertion (between current lines 40 and 41 on `main`):**

    Find this exact block in `src/app/actions/watches.ts`:
    ```
      notes: z.string().optional(),
      imageUrl: z.string().optional(),
    ```

    Replace it with:
    ```
      notes: z.string().optional(),
      notesPublic: z.boolean().optional(),
      imageUrl: z.string().optional(),
    ```

    Why this exact placement: matches domain type ordering at `src/lib/types.ts:52-53` (`notes` then `notesPublic`). Per RESEARCH §"Diff Hunk 1", any line inside the `insertWatchSchema` z.object body works, but this placement reads best. Use `.optional()` (NOT `.default(true)`) — the DB column owns the default per CONTEXT `<code_context>` "Established Patterns" and Phase 23 D-13/D-16. `updateWatchSchema = insertWatchSchema.partial()` (line 57) auto-flows the new field to `editWatch`; no second schema edit is needed.

    **Edit 2 — addWatch revalidate insertion (between current lines 267 and 277 on `main`):**

    Find this exact block:
    ```
        revalidatePath('/')

        // Phase 18 DISC-05 / DISC-06 — fan out 'explore' tag so the global
    ```

    Replace it with:
    ```
        revalidatePath('/')
        revalidatePath('/u/[username]', 'layout')

        // Phase 18 DISC-05 / DISC-06 — fan out 'explore' tag so the global
    ```

    The new line is placed BEFORE the existing comment block that documents `revalidateTag('explore', 'max')`. Per CONTEXT D-03: order is `revalidatePath('/')` then `revalidatePath('/u/[username]', 'layout')` then `revalidateTag('explore', 'max')` — keeps path revalidates grouped before the tag fan-out. Per D-02: fire UNCONDITIONALLY — do NOT gate on `'notesPublic' in parsed.data`.

    **Edit 3 — editWatch revalidate insertion (between current lines 340 and 348 on `main`):**

    Find this exact block:
    ```
        const watch = await watchDAL.updateWatch(user.id, watchId, updatePayload)
        revalidatePath('/')

        // Phase 18 DISC-05 / DISC-06 — same fan-out as addWatch. editWatch can
    ```

    Replace it with:
    ```
        const watch = await watchDAL.updateWatch(user.id, watchId, updatePayload)
        revalidatePath('/')
        revalidatePath('/u/[username]', 'layout')

        // Phase 18 DISC-05 / DISC-06 — same fan-out as addWatch. editWatch can
    ```

    **Out-of-scope reminders (DO NOT do these):**
    - DO NOT modify `removeWatch` (line 372 area) — D-04 says it stays untouched; adding revalidate to deletion is a real-but-smaller bug captured as a Deferred Idea.
    - DO NOT modify any sibling action file (`notes.ts`, `profile.ts`, `follows.ts`, `wishlist.ts`) — they are reference patterns only.
    - DO NOT modify `src/db/schema.ts`, `src/lib/types.ts`, `src/data/watches.ts`, or `src/components/watch/WatchForm.tsx` — all are already correct on `main`.
    - DO NOT add new tests — D-06.

    The new `revalidatePath` call uses `'/u/[username]'` (the literal route template segment with brackets — NOT a resolved username) and `'layout'` (NOT `'page'`). This signature matches the test scaffold's assertion at lines 131 and 158 exactly, and matches 5 of 6 sibling actions on `main` (notes.ts ×2, profile.ts ×2, follows.ts ×2). The lone exception `wishlist.ts:206` uses `('/u/[username]/[tab]', 'page')` — that is a NEGATIVE pattern per PATTERNS.md §"Negative / Anti-Pattern"; DO NOT copy it.

    Optional commentary above the new call: a brief comment is acceptable but not required (per RESEARCH §"Optional explanatory comment"). Bare insertion matches `profile.ts`/`follows.ts`; commented insertion matches `notes.ts` WR-07 verbosity. Either passes the test contract. Recommendation: bare for minimal diff churn (matches CONTEXT D-03 "minimizes diff churn" framing).
  </action>
  <verify>
    <automated>grep -nE "notesPublic: z\.boolean\(\)\.optional\(\)" src/app/actions/watches.ts && test "$(grep -cE "revalidatePath\('/u/\[username\]', 'layout'\)" src/app/actions/watches.ts)" = "2" && test "$(grep -cE "revalidatePath\('/u/\[username\]/\[tab\]', 'page'\)" src/app/actions/watches.ts)" = "0" && npx vitest run tests/actions/watches.notesPublic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "notesPublic: z\.boolean\(\)\.optional\(\)" src/app/actions/watches.ts` returns exactly 1 hit on a line BETWEEN the `const insertWatchSchema = z.object({` line and its closing `})` (lines 17-55 post-edit, +1 from pre-edit's 17-54)
    - `grep -cE "revalidatePath\('/u/\[username\]', 'layout'\)" src/app/actions/watches.ts` returns exactly `2` (one in `addWatch`, one in `editWatch`)
    - `grep -cE "revalidatePath\('/u/\[username\]/\[tab\]', 'page'\)" src/app/actions/watches.ts` returns exactly `0` (the negative pattern from `wishlist.ts:206` was NOT copied)
    - `grep -cE "^    revalidatePath\('/'\)$" src/app/actions/watches.ts` returns exactly `3` (existing top-level `revalidatePath('/')` calls in addWatch/editWatch/removeWatch are all still present — no accidental deletions)
    - `npx vitest run tests/actions/watches.notesPublic.test.ts` exits 0 with output containing `Tests  4 passed (4)` (was `4 failed` on `main` per v4.1 audit — this is the RED→GREEN flip, success criterion #1)
    - `removeWatch` is unchanged — verified by reading lines around the post-edit `removeWatch` declaration: NO `revalidatePath('/u/[username]'` call appears between the `removeWatch` function header and its closing brace (verifies D-04)
  </acceptance_criteria>
  <done>
    `src/app/actions/watches.ts` has `notesPublic: z.boolean().optional()` inside `insertWatchSchema`, has `revalidatePath('/u/[username]', 'layout')` immediately after `revalidatePath('/')` in both `addWatch` and `editWatch`, and `removeWatch` is untouched. The 4 previously-RED tests in `tests/actions/watches.notesPublic.test.ts` are now 4/4 GREEN.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Correct ROADMAP.md success criterion #4 wording inline (D-05)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 118-128 (Phase 32 success criteria — confirm criterion #4 still has the wrong wording before editing; if it has already been corrected by another commit since this plan was authored, skip this task and note it in the SUMMARY)
    - .planning/phases/32-debt-09-notespublic-fix/32-CONTEXT.md §"ROADMAP correction" (D-05 — rationale: ROADMAP and the test cannot both be authoritative; the test is the contract per success criterion #1)
    - .planning/phases/32-debt-09-notespublic-fix/32-RESEARCH.md §"ROADMAP.md Edit Hunk" (verbatim diff)
    - .planning/phases/32-debt-09-notespublic-fix/32-PATTERNS.md §".planning/ROADMAP.md — Single-Line Wording Correction"
  </read_first>
  <action>
    Apply a single-line wording fix to `.planning/ROADMAP.md` at line 126.

    Find this exact block:
    ```
      3. Both Server Actions persist `notesPublic` to the database on every write
      4. Both Server Actions call `revalidatePath('/u/[username]/[tab]', 'page')` after every successful write
      5. No new test failures introduced; full test suite remains GREEN
    ```

    Replace it with:
    ```
      3. Both Server Actions persist `notesPublic` to the database on every write
      4. Both Server Actions call `revalidatePath('/u/[username]', 'layout')` after every successful write
      5. No new test failures introduced; full test suite remains GREEN
    ```

    This is a single-line edit (line 126 only). No surrounding doc structure changes. Per D-05: "ROADMAP and the test cannot both be authoritative; the test is the contract per success criterion #1. A doc fix that closes the contradiction belongs alongside the fix that satisfies it."

    DO NOT modify any other line in ROADMAP.md (other phase sections, traceability tables, milestone summaries, the requirement coverage table on line 296 — all are out of scope for this phase).
  </action>
  <verify>
    <automated>test "$(grep -cE "revalidatePath\('/u/\[username\]', 'layout'\)" .planning/ROADMAP.md)" = "1" && test "$(grep -cE "revalidatePath\('/u/\[username\]/\[tab\]', 'page'\)" .planning/ROADMAP.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "revalidatePath\('/u/\[username\]', 'layout'\)" .planning/ROADMAP.md` returns exactly `1` (the corrected line)
    - `grep -cE "revalidatePath\('/u/\[username\]/\[tab\]', 'page'\)" .planning/ROADMAP.md` returns exactly `0` (the old wording is gone — the contradiction with the test scaffold is closed)
    - `grep -nE "Both Server Actions call" .planning/ROADMAP.md` returns exactly 1 hit and the line content matches `4. Both Server Actions call \`revalidatePath('/u/[username]', 'layout')\` after every successful write`
    - `git diff .planning/ROADMAP.md` shows exactly 1 line changed (line 126), with no other line modifications
  </acceptance_criteria>
  <done>
    `.planning/ROADMAP.md` line 126 reads `4. Both Server Actions call \`revalidatePath('/u/[username]', 'layout')\` after every successful write`. ROADMAP success criterion #4 now matches the literal assertion in the test scaffold (`tests/actions/watches.notesPublic.test.ts:131,158`).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full-suite regression verification (success criterion #5)</name>
  <files></files>
  <read_first>
    - .planning/phases/32-debt-09-notespublic-fix/32-RESEARCH.md §"CLI Verification Commands" (the 7-step verification ladder)
    - .planning/phases/32-debt-09-notespublic-fix/32-RESEARCH.md §"Test Mocking Surface Analysis" (confirms no existing test asserts call counts on `revalidatePath` for the watches actions — if a regression appears, it is unexpected and should be investigated, not patched around)
    - .planning/phases/32-debt-09-notespublic-fix/32-VALIDATION.md §"Sampling Rate" (confirms `npm test` is the per-wave-merge command)
  </read_first>
  <action>
    Run the regression sweep to satisfy ROADMAP success criterion #5 ("No new test failures introduced; full test suite remains GREEN"). Execute the steps in order; STOP on the first failure and report it (do not proceed to later steps if an earlier one fails).

    1. Targeted RED→GREEN re-confirmation: `npx vitest run tests/actions/watches.notesPublic.test.ts`
       Expected: `Test Files  1 passed (1)` and `Tests  4 passed (4)`. (Was `Tests  4 failed` on `main` per v4.1 audit.)

    2. Adjacent regression check (the immediate neighbors that mock `next/cache` and import the watches actions): `npx vitest run tests/actions/watches.test.ts tests/actions/addwatch-catalog-resilience.test.ts`
       Expected: all previously-passing tests still pass. Per RESEARCH §"Test Mocking Surface Analysis", these files mock `revalidatePath` defensively but do NOT assert call counts on it for the watches actions, so adding the third `revalidatePath` call is invisible to their assertions.

    3. All action tests (broader sweep): `npx vitest run tests/actions/`
       Expected: all previously-passing tests still pass. Includes `wishlist.test.ts`, `preferences.test.ts`, `follows.test.ts`, `notifications.test.ts` — none import `addWatch`/`editWatch` so none are affected by the change.

    4. Full suite: `npm test`
       Expected: zero net new failures. Some integration tests skip without `DATABASE_URL` set (e.g., `tests/integration/phase17-addwatch-wiring.test.ts`, `tests/integration/add-watch-photo.test.ts`) — those skips are by design and are NOT failures.

    5. Type check: `npx tsc --noEmit`
       Expected: zero NEW errors. The pre-existing `LayoutProps` error in `src/app/u/[username]/layout.tsx:21` is carryover from v3.0 (per REQUIREMENTS.md "Future Requirements") and is acceptable. If `npx tsc --noEmit` reports ONLY that pre-existing error, the gate passes. If a new error appears in `src/app/actions/watches.ts` or any other file, that is a regression — investigate before proceeding.

    6. Lint: `npm run lint`
       Expected: zero errors. The new `revalidatePath('/u/[username]', 'layout')` line is structurally identical to existing calls in the same file and in 5 sibling actions; no new lint rule should fire.

    DO NOT modify code in this task — it is verification only. If any step fails, STOP, report the failure (file, test name, error message), and let the orchestrator decide whether to revise the plan or fix the regression in a follow-up task. Per the RESEARCH "Out-of-Scope Reminder": "If the executor encounters a test failure outside `tests/actions/watches.notesPublic.test.ts` after the edit, that is a regression signal — investigate before proceeding. The expected outcome is strictly additive: 4 newly-GREEN tests, all other test states unchanged."
  </action>
  <verify>
    <automated>npx vitest run tests/actions/watches.notesPublic.test.ts && npx vitest run tests/actions/ && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Step 1: `npx vitest run tests/actions/watches.notesPublic.test.ts` exits 0 with `Tests  4 passed (4)` (success criterion #1)
    - Step 2: `npx vitest run tests/actions/watches.test.ts tests/actions/addwatch-catalog-resilience.test.ts` exits 0 (the immediate neighbors stay GREEN)
    - Step 3: `npx vitest run tests/actions/` exits 0 (all action tests stay GREEN)
    - Step 4: `npm test` exits 0 (full suite GREEN — success criterion #5)
    - Step 5: `npx tsc --noEmit` produces ONLY the pre-existing `LayoutProps` error in `src/app/u/[username]/layout.tsx:21` (or zero errors if that carryover has been resolved by another phase since this plan was authored). NO new TS errors in `src/app/actions/watches.ts` or any other file.
    - Step 6: `npm run lint` exits 0 with no errors
  </acceptance_criteria>
  <done>
    All verification steps pass. ROADMAP success criteria #1-#5 are all satisfied: tests GREEN (4/4 in notesPublic test, full suite stable), schema accepts notesPublic, both actions persist + revalidate, no new test failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form -> Server Action | WatchForm submission crosses to addWatch / editWatch in src/app/actions/watches.ts; Zod validation at insertWatchSchema (line 17-54) is the gate. This phase does NOT introduce a new boundary — it restores a pre-existing field (notesPublic) to that gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | T (Tampering) | insertWatchSchema accepts notesPublic | mitigate | Field is typed z.boolean().optional() — Zod rejects non-boolean values. Test #4 in tests/actions/watches.notesPublic.test.ts (line 161) explicitly asserts the rejection path; this phase satisfies that test. |
| T-32-02 | I (Information disclosure) | revalidatePath('/u/[username]', 'layout') cascade | accept | The route /u/[username] is already a public profile surface; no PII is added by the cache invalidation. The pill UI already gates row visibility on existing notesPublic+follow logic in the page component (out of scope here). |
| T-32-03 | S (Spoofing) | Server Action auth context | accept | Both addWatch (line 65+) and editWatch (line 295+) already enforce supabase.auth.getUser() before the schema parse. This phase modifies neither auth gate. No regression risk because the new fields/calls run AFTER the existing auth check returns the user. |
| T-32-04 | E (Elevation of privilege) | notesPublic write authorization | accept | The DAL writes scoped to user.id (existing watchDAL.createWatch / updateWatch already filter by user_id) — a user cannot toggle another user's watch's notesPublic. RLS on watches table further enforces this. No new auth scope introduced. |
</threat_model>

<verification>
Phase-level verification (run after all 3 tasks complete):

1. The mechanical fix landed: `src/app/actions/watches.ts` contains `notesPublic: z.boolean().optional()` inside `insertWatchSchema` AND has exactly 2 `revalidatePath('/u/[username]', 'layout')` calls (one in `addWatch`, one in `editWatch`).
2. `removeWatch` is unchanged — D-04 honored.
3. ROADMAP.md line 126 reads `revalidatePath('/u/[username]', 'layout')` (D-05 honored).
4. The 4 previously-RED tests in `tests/actions/watches.notesPublic.test.ts` are now 4/4 GREEN.
5. The full vitest suite is GREEN — no net new failures.
6. `npx tsc --noEmit` produces only the pre-existing `LayoutProps` carryover (or zero errors).
7. No file outside `src/app/actions/watches.ts` and `.planning/ROADMAP.md` was modified — `git diff --stat` shows exactly 2 files changed.
8. No new test files were created — D-06 honored.

If any of these are false, the phase is NOT complete and the orchestrator must escalate.
</verification>

<success_criteria>
DEBT-09 closes when:
- [ ] `tests/actions/watches.notesPublic.test.ts` reports `Tests  4 passed (4)` (was `4 failed` at v4.1 close — ROADMAP criterion #1)
- [ ] `grep -cE "notesPublic: z\.boolean\(\)\.optional\(\)" src/app/actions/watches.ts` returns `1` (ROADMAP criterion #2)
- [ ] DAL persistence is intact: existing `mapDomainToRow` at `src/data/watches.ts:84` is unmodified and now receives `notesPublic` from the action layer (ROADMAP criterion #3 — implicitly verified by test #1 in the notesPublic suite)
- [ ] `grep -cE "revalidatePath\('/u/\[username\]', 'layout'\)" src/app/actions/watches.ts` returns `2` (ROADMAP criterion #4 — normalized to test-contract wording per D-05)
- [ ] `npm test` exits 0 (ROADMAP criterion #5)
- [ ] `grep -cE "revalidatePath\('/u/\[username\]/\[tab\]', 'page'\)" .planning/ROADMAP.md` returns `0` (D-05 — ROADMAP wording corrected inline)
- [ ] `git diff --stat` shows exactly 2 files changed: `src/app/actions/watches.ts` and `.planning/ROADMAP.md` (D-04 + D-06 — scope held)
</success_criteria>

<output>
After all 3 tasks complete and the verification gate passes, create `.planning/phases/32-debt-09-notespublic-fix/32-01-SUMMARY.md` documenting:

- Files modified (`src/app/actions/watches.ts` lines added, `.planning/ROADMAP.md` line 126 corrected)
- Test result: `tests/actions/watches.notesPublic.test.ts` 4/4 GREEN (was 4/4 RED on `main`)
- Full suite result: GREEN, no net new failures
- D-01 through D-06 all honored (one-line note per decision is sufficient)
- DEBT-09 status: CLOSED
- Optionally: note any pre-existing TS carryover errors observed during `npx tsc --noEmit` (the `LayoutProps` error in `src/app/u/[username]/layout.tsx:21` is the known carryover; flag if anything else surfaces)
</output>
