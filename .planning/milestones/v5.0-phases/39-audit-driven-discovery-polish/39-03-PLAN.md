---
phase: 39-audit-driven-discovery-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/insights/CollectionFitCard.tsx
autonomous: true
requirements:
  - DISC-11
audit_rows:
  - NSV-01
  - NSV-15
  - NSV-08
  - DISC-AUDIT-82
  - DISC-AUDIT-71
  - DISC-AUDIT-129
tags:
  - component
  - server-component
  - insights
  - link-wrap
  - audit-closure
must_haves:
  truths:
    - "User clicking a row in the mostSimilar list of CollectionFitCard navigates to /watch/{watchId} on both /watch/{id} and /catalog/{id} surfaces"
    - "Each <li> in the mostSimilar list is wrapped in a <Link href=`/watch/${watch.id}`> with hover affordance"
    - "Phase 20 D-04 import-boundary guard test (tests/static/CollectionFitCard.no-engine.test.ts) still passes — no engine imports added"
    - "NSV-08 audit row closed via grep evidence — both SleepingBeautiesSection and GoodDealsSection ALREADY wrap; no fabricated patch shipped"
    - "Inner <span flex items-center justify-between> preserves the existing row layout inside the new <Link block> wrapper"
  artifacts:
    - path: "src/components/insights/CollectionFitCard.tsx"
      provides: "Clickable mostSimilar rows that drill into /watch/{id} detail page"
      min_lines: 130
      contains: "block hover:bg-accent rounded-md p-1"
  key_links:
    - from: "src/components/insights/CollectionFitCard.tsx (mostSimilar list)"
      to: "/watch/{watch.id} route"
      via: "<Link href={`/watch/${watch.id}`}>"
      pattern: "<Link\\s+href=\\{`/watch/\\$\\{watch\\.id\\}`\\}"
    - from: "src/components/insights/CollectionFitCard.tsx (existing imports)"
      to: "next/link"
      via: "import Link from 'next/link' (line 1 — already present, no new import needed)"
      pattern: "^import Link from 'next/link'"
---

<objective>
Two parallel sub-items in one plan — both audit-driven, both single-file or single-grep:

1. **NSV-01 + NSV-15** (DISC-AUDIT-82 + DISC-AUDIT-71): Wrap each `<li>` in the `verdict.mostSimilar.map(...)` block of `src/components/insights/CollectionFitCard.tsx:69-78` with `<Link href={\`/watch/${watch.id}\`} className="block hover:bg-accent rounded-md p-1">`. Single component edit; same component renders on both `/watch/{id}` and `/catalog/{id}` so this fix closes both audit rows. Phase 20 D-04 import-boundary guard remains intact (`next/link` is already imported at line 1; no new imports).

2. **NSV-08 verify-before-patch** (DISC-AUDIT-129) per D-08: Run `grep -nE "<Link|<a " src/components/insights/SleepingBeautiesSection.tsx src/components/insights/GoodDealsSection.tsx` AT EXECUTION TIME (the 33-DISCOVERY-AUDIT snapshot is dated 2026-05-08; verification on 2026-05-12 by phase-researcher confirmed BOTH ALREADY wrap, but plans must NOT trust prior verification). If both wrap → close NSV-08 as "already shipped before Phase 39 began" with grep stdout captured in the SUMMARY. If drift has re-emerged → patch the missing section(s) using `SleepingBeautiesSection.tsx:43-51` as the verbatim shape.

Purpose: Close three Phase 33b Q3 dead-end audit rows. NSV-01 + NSV-15 are the cheapest cells in the sorted backlog; NSV-08 closes as documentation work per D-08's "Do NOT fabricate work to fill the plan" mandate.

Output: 1 file modification (CollectionFitCard.tsx) + 1 grep evidence block captured in SUMMARY for NSV-08. Zero new imports. Phase 20 static guard test continues to pass.

Audit rows addressed:
- NSV-01 (DISC-AUDIT-82) — Phase 33b leverage = high
- NSV-15 (DISC-AUDIT-71) — Phase 33b leverage = high
- NSV-08 (DISC-AUDIT-129) — Phase 33b leverage = high (likely already shipped; closure via grep evidence)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/39-audit-driven-discovery-polish/39-CONTEXT.md
@.planning/phases/39-audit-driven-discovery-polish/39-RESEARCH.md
@.planning/phases/39-audit-driven-discovery-polish/39-UI-SPEC.md
@.planning/phases/39-audit-driven-discovery-polish/39-PATTERNS.md

# File being modified — read current shape
@src/components/insights/CollectionFitCard.tsx

# Reference pattern (already shipped Link wrap shape)
@src/components/insights/SleepingBeautiesSection.tsx

# NSV-08 verification target
@src/components/insights/GoodDealsSection.tsx

# Import-boundary guard — must keep passing
@tests/static/CollectionFitCard.no-engine.test.ts

<interfaces>
<!-- Existing imports at CollectionFitCard.tsx:1-5 (verified at HEAD 2026-05-12). -->
<!-- Link IS already imported — no new imports needed for this plan. -->

```typescript
import Link from 'next/link'  // ← already present at line 1
import { AlertTriangle, Watch as WatchIcon, ArrowRight } from 'lucide-react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import type { VerdictBundle } from '@/lib/verdict/types'
```

<!-- Phase 20 D-04 deny-list (from tests/static/CollectionFitCard.no-engine.test.ts:12-40) -->
<!-- This edit must NOT add ANY import matching these patterns: -->
// - from '@/lib/similarity'
// - from '@/lib/verdict/composer'
// - from 'server-only'
// - from '@/lib/verdict/viewerTasteProfile'

<!-- next/link is NOT on the deny list — Link wrap is safe. -->

<!-- VerdictBundle.mostSimilar shape (consumed by CollectionFitCard.tsx:69-78) -->
// type VerdictBundleFull = {
//   ...
//   mostSimilar: Array<{
//     watch: { id: string; brand: string; model: string; ... }
//     score: number  // 0..1
//   }>
//   ...
// }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap CollectionFitCard mostSimilar &lt;li&gt; rows in &lt;Link&gt; (NSV-01 + NSV-15)</name>
  <files>src/components/insights/CollectionFitCard.tsx</files>
  <behavior>
    Before: lines 69-78 render a non-clickable `<li className="flex items-center justify-between">` with two `<span>` children (watch label + similarity %).
    After: each `<li>` contains a `<Link href={\`/watch/${watch.id}\`} className="block hover:bg-accent rounded-md p-1">` wrapping an inner `<span className="flex items-center justify-between">` that preserves the existing two-span layout. Click on any row navigates to `/watch/{watchId}`. Hover paints `bg-accent`.

    Test target: the Phase 20 import-boundary guard `tests/static/CollectionFitCard.no-engine.test.ts` MUST continue to pass (vacuously — no new imports added). No new unit test mandated by D-07.
  </behavior>
  <read_first>
    - src/components/insights/CollectionFitCard.tsx (full file — verify lines 69-78 current shape + verify Link is already at line 1 + verify no engine imports already present)
    - src/components/insights/SleepingBeautiesSection.tsx (canonical Link-wrap reference at lines 41-53 — note: that file uses `flex p-2 hover:bg-accent` whereas D-07 mandates `block p-1 hover:bg-accent`; the className differs intentionally to preserve a more compact card-internal padding)
    - tests/static/CollectionFitCard.no-engine.test.ts (verify deny-list at lines 12-40 — confirm `next/link` is NOT forbidden)
    - .planning/phases/39-audit-driven-discovery-polish/39-CONTEXT.md (D-07 — verbatim className lock)
    - .planning/phases/39-audit-driven-discovery-polish/39-UI-SPEC.md (§ NSV-01+15 — table row 100-104: Link target, className, hover affordance, ARIA)
    - .planning/phases/39-audit-driven-discovery-polish/39-PATTERNS.md (§ CollectionFitCard.tsx (MODIFY) — target shape lines 70-85)
  </read_first>
  <action>
**NSV-01 + NSV-15 / DISC-AUDIT-82 + DISC-AUDIT-71 / per D-07.**

Edit ONLY the `verdict.mostSimilar.map(...)` block at lines 69-78 of `src/components/insights/CollectionFitCard.tsx`.

### Current shape (verbatim from HEAD 2026-05-12)

```typescript
{verdict.mostSimilar.map(({ watch, score }) => (
  <li key={watch.id} className="flex items-center justify-between">
    <span className="truncate">
      {watch.brand} {watch.model}
    </span>
    <span className="text-muted-foreground/70">
      {Math.round(score * 100)}% similar
    </span>
  </li>
))}
```

### Target shape (replace with this verbatim)

```typescript
{verdict.mostSimilar.map(({ watch, score }) => (
  <li key={watch.id}>
    <Link
      href={`/watch/${watch.id}`}
      className="block hover:bg-accent rounded-md p-1"
    >
      <span className="flex items-center justify-between">
        <span className="truncate">{watch.brand} {watch.model}</span>
        <span className="text-muted-foreground/70">
          {Math.round(score * 100)}% similar
        </span>
      </span>
    </Link>
  </li>
))}
```

### Load-bearing details

1. **className verbatim per D-07:** `block hover:bg-accent rounded-md p-1`. NOT `flex p-2 hover:bg-accent` from SleepingBeautiesSection — D-07 mandates `block + p-1` so the row stays compact inside the card. The existing flex layout must move INTO an inner `<span className="flex items-center justify-between">` wrapper.

2. **Move `key={watch.id}` to the outer `<li>`:** React's reconciliation key stays on the `<li>`, not the `<Link>`. The `<li>` no longer needs its own className (the flex layout moved inside).

3. **Remove the className from the outer `<li>`:** the previous `flex items-center justify-between` was on the `<li>`; that className must NOT appear on the outer `<li>` anymore. The flex layout is now provided by the inner `<span className="flex items-center justify-between">` inside the `<Link>`.

4. **Inner `<span>` wrapper:** Wrap the existing two `<span>` children in `<span className="flex items-center justify-between">`. This preserves the visual contract (truncating watch name on left, similarity % on right) while letting the outer `<Link>` use `block + p-1`.

5. **No new imports:** `Link` is already imported at line 1. Do NOT add `import Link from 'next/link'` (duplicate import will fail tsc). Do NOT add any other import — Phase 20 D-04 deny-list compliance.

6. **No `'use client'` directive:** CollectionFitCard.tsx is a pure-renderer Server Component. The Link wrap stays server-renderable.

### Out-of-scope (DO NOT TOUCH)

- The verdict headline / contextual phrasings / role-overlap warning at lines 34-89 outside the mostSimilar block. UI-SPEC § Out-of-Scope Visual Concerns + D-07 explicit.
- The `YouOwnThisCallout` sub-component at lines 102-124.
- The `formatOwnedDate` helper at lines 126-137.
- `tests/static/CollectionFitCard.no-engine.test.ts` — do NOT modify; verify it passes after this edit (vacuously — no new imports).
  </action>
  <verify>
    <automated>npx vitest run tests/static/CollectionFitCard.no-engine.test.ts 2>&1 | tee /tmp/plan03-vitest.log; grep -q "Tests  3 passed\|Test Files  1 passed" /tmp/plan03-vitest.log && echo "PHASE20_GUARD_OK" || echo "PHASE20_GUARD_FAIL"</automated>
  </verify>
  <done>
- mostSimilar rows wrapped in `<Link href={\`/watch/${watch.id}\`}>`
- D-07 className `block hover:bg-accent rounded-md p-1` applied verbatim
- Inner `<span flex items-center justify-between>` preserves existing layout
- Phase 20 D-04 import-boundary guard test still passes
- No new imports added; existing `Link` import (line 1) is reused
  </done>
  <acceptance_criteria>
    - `grep -c "<Link" src/components/insights/CollectionFitCard.tsx` returns ≥ 2 (the new mostSimilar Link wrap PLUS the existing `<Link>` in YouOwnThisCallout at line 114; count was 1 before this edit)
    - `grep -c '<Link\s*$' src/components/insights/CollectionFitCard.tsx` returns ≥ 1 (the new multi-line Link element opening tag)
    - `grep -c 'href={`/watch/${watch.id}`}' src/components/insights/CollectionFitCard.tsx` returns 1 (mostSimilar Link target — note: this exact backtick template is the locked shape from D-07)
    - `grep -c "block hover:bg-accent rounded-md p-1" src/components/insights/CollectionFitCard.tsx` returns 1 (D-07 className verbatim — block + p-1, NOT flex + p-2)
    - `grep -c '<li key={watch\.id} className="flex items-center justify-between">' src/components/insights/CollectionFitCard.tsx` returns 0 (the OLD `<li>` className shape is removed — flex moved into the inner span)
    - `grep -c 'className="flex items-center justify-between"' src/components/insights/CollectionFitCard.tsx` returns ≥ 1 (the inner `<span>` wrapper preserves the layout)
    - `grep -c "from '@/lib/similarity'" src/components/insights/CollectionFitCard.tsx` returns 0 (Phase 20 D-04 deny-list — must stay 0)
    - `grep -c "from '@/lib/verdict/composer'" src/components/insights/CollectionFitCard.tsx` returns 0 (Phase 20 D-04 deny-list — must stay 0)
    - `grep -c "from 'server-only'" src/components/insights/CollectionFitCard.tsx` returns 0 (Phase 20 D-04 deny-list — must stay 0)
    - `grep -c "from '@/lib/verdict/viewerTasteProfile'" src/components/insights/CollectionFitCard.tsx` returns 0 (Phase 20 D-04 deny-list — must stay 0)
    - `grep -c "use client" src/components/insights/CollectionFitCard.tsx` returns 0 (no client directive — component stays server-renderable)
    - `grep -c "^import Link from 'next/link'" src/components/insights/CollectionFitCard.tsx` returns 1 (existing import preserved — NOT duplicated)
    - `npx vitest run tests/static/CollectionFitCard.no-engine.test.ts` exits 0 (Phase 20 D-04 guard still PASSES — vacuously, no new imports)
    - `npx tsc --noEmit 2>&1 | grep -c "src/components/insights/CollectionFitCard.tsx" | head -1` returns 0 (no NEW tsc errors — pre-existing 27-error baseline preserved)
    - `git diff --name-only HEAD` shows ONLY `src/components/insights/CollectionFitCard.tsx` modified by THIS task (NSV-08 task may add a SUMMARY file, but no other src/ changes)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: NSV-08 verify-before-patch protocol (D-08) — grep evidence, no fabricated patches</name>
  <files>.planning/phases/39-audit-driven-discovery-polish/39-03-SUMMARY.md</files>
  <read_first>
    - src/components/insights/SleepingBeautiesSection.tsx (visual verification of Link wrap at lines 43-51 at execution time — codebase may have drifted since 2026-05-12 research snapshot)
    - src/components/insights/GoodDealsSection.tsx (visual verification of Link wrap at lines 47-49 at execution time)
    - .planning/phases/39-audit-driven-discovery-polish/39-CONTEXT.md (D-08 verify-before-patch mandate — "Do NOT fabricate work to fill the plan")
    - .planning/phases/39-audit-driven-discovery-polish/39-RESEARCH.md (lines 372-383 — verified state at HEAD 2026-05-12 shows both wrap)
  </read_first>
  <action>
**NSV-08 / DISC-AUDIT-129 / per D-08 verify-before-patch protocol.**

DO NOT write any code in this task. The work is verification + documentation.

### Step 1 — Run the D-08 grep at execution time

Execute exactly this command (verbatim — D-08 lock):

```bash
grep -nE "<Link|<a " src/components/insights/SleepingBeautiesSection.tsx src/components/insights/GoodDealsSection.tsx
```

Capture stdout in full. Expected output (per HEAD 2026-05-12 — verified by phase-researcher AND by phase-planner):

```
src/components/insights/SleepingBeautiesSection.tsx:43:                <Link
src/components/insights/SleepingBeautiesSection.tsx:44:                  href={`/watch/${watch.id}`}
src/components/insights/SleepingBeautiesSection.tsx:45:                  className="flex items-center justify-between rounded-md p-2 hover:bg-accent"
src/components/insights/GoodDealsSection.tsx:47:                <Link
src/components/insights/GoodDealsSection.tsx:48:                  href={`/watch/${w.id}`}
src/components/insights/GoodDealsSection.tsx:49:                  className="flex items-center gap-3 rounded-md p-2 hover:bg-accent"
```

### Step 2 — Decision tree

- **Case A — Both files contain `<Link` matches (expected):** Close NSV-08 as **"already shipped before Phase 39 began"**. Capture the grep output verbatim in the SUMMARY (Step 3). Write NO code changes. The audit row closure IS the work.

- **Case B — Only ONE file contains `<Link`:** Patch the missing file using `SleepingBeautiesSection.tsx:41-53` as the verbatim shape reference. Add Task 3 to this plan dynamically (executor SHOULD ask for plan amendment if this case occurs — drift between research and execution is unexpected).

- **Case C — Neither file contains `<Link`:** Patch BOTH files using the same shape reference. Ask for plan amendment (this is the unexpected case — Phase 33b 2026-05-08 snapshot showed neither wrapped; 2026-05-12 research showed both wrap; if 2026-05-13+ shows neither, something rolled back).

### Step 3 — Write/append the SUMMARY evidence block

In `.planning/phases/39-audit-driven-discovery-polish/39-03-SUMMARY.md` (the same SUMMARY file Task 1 will populate), add a dedicated `## NSV-08 / DISC-AUDIT-129 — Verify-Before-Patch Evidence` section containing:

- The exact `grep -nE "<Link|<a "` command run
- The full stdout output (paste verbatim — do NOT paraphrase)
- The decision tree branch hit (A / B / C)
- For Case A: explicit closure statement — "NSV-08 / DISC-AUDIT-129 closed as 'already shipped before Phase 39 began'. Zero code changes per D-08 ('Do NOT fabricate work to fill the plan'). Phase 33b status row for NSV-08 should be updated from `partial` → `ship`."
- The two-file file paths confirmed
- The timestamp the grep was run (capture from `date -u +"%Y-%m-%dT%H:%M:%SZ"`)

### Anti-patterns to avoid (D-08 explicit)

- Do NOT write a "patch" task that adds duplicate `<Link>` wrapping. The wraps ALREADY exist.
- Do NOT skip the grep and assume the 2026-05-12 research is still valid. D-08 mandates re-verification at execution time.
- Do NOT close NSV-08 as "shipped in Phase 39 plan 03" — the wraps shipped BEFORE Phase 39 began (sibling phase work; codebase drift). The SUMMARY closure note must use the verbatim phrasing "already shipped before Phase 39 began".
- Do NOT write `Phase 39 added NSV-08 Link wraps to SleepingBeautiesSection / GoodDealsSection` in the SUMMARY. False attribution.
  </action>
  <verify>
    <automated>grep -nE "<Link|<a " src/components/insights/SleepingBeautiesSection.tsx src/components/insights/GoodDealsSection.tsx | tee /tmp/nsv08-grep.log; (grep -q "SleepingBeautiesSection.tsx.*<Link" /tmp/nsv08-grep.log && grep -q "GoodDealsSection.tsx.*<Link" /tmp/nsv08-grep.log) && echo "NSV08_CASE_A_BOTH_WRAP" || echo "NSV08_DRIFT_DETECTED_NEED_AMENDMENT"</automated>
  </verify>
  <done>
- D-08 grep executed at execution time; stdout captured
- Decision tree branch identified (expected: Case A — both wrap)
- SUMMARY contains `## NSV-08 / DISC-AUDIT-129` section with verbatim grep stdout
- NSV-08 explicitly closed as "already shipped before Phase 39 began" (Case A) OR plan amendment requested (Cases B/C)
- Zero code modifications to SleepingBeautiesSection.tsx or GoodDealsSection.tsx
  </done>
  <acceptance_criteria>
    - The bash verify command echoes `NSV08_CASE_A_BOTH_WRAP` (both files contain `<Link` matches at HEAD)
    - `grep -c "NSV-08" .planning/phases/39-audit-driven-discovery-polish/39-03-SUMMARY.md` returns ≥ 2 (audit row referenced in section header + closure statement)
    - `grep -c "DISC-AUDIT-129" .planning/phases/39-audit-driven-discovery-polish/39-03-SUMMARY.md` returns ≥ 1 (DISC-AUDIT row id referenced)
    - `grep -c "already shipped before Phase 39 began" .planning/phases/39-audit-driven-discovery-polish/39-03-SUMMARY.md` returns 1 (verbatim closure phrasing per Case A)
    - `grep -c "SleepingBeautiesSection.tsx" .planning/phases/39-audit-driven-discovery-polish/39-03-SUMMARY.md` returns ≥ 1 (file path captured in evidence)
    - `grep -c "GoodDealsSection.tsx" .planning/phases/39-audit-driven-discovery-polish/39-03-SUMMARY.md` returns ≥ 1 (file path captured in evidence)
    - `git diff --name-only HEAD src/components/insights/SleepingBeautiesSection.tsx src/components/insights/GoodDealsSection.tsx` returns empty output (neither file was modified — D-08 anti-fabrication compliance)
    - `grep -cE "^src/components/insights/(SleepingBeauties|GoodDeals)Section\.tsx:[0-9]+:.*<Link" .planning/phases/39-audit-driven-discovery-polish/39-03-SUMMARY.md` returns ≥ 2 (the grep stdout was pasted verbatim, including the file:line:content shape — proves the evidence was not paraphrased)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer → mostSimilar row click | A click on a verdict-derived watch row crosses to `/watch/{watchId}`. The `watch.id` is derived from server-rendered verdict payload (already authenticated and authorized via the upstream getWatchesByUser DAL). No new trust boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-NSV01-01 | Tampering / open redirect | `<Link href={\`/watch/${watch.id}\`}>` | accept | `watch.id` is a server-controlled string from the VerdictBundle (`watch.id` originates from `getWatchesByUser` DAL which authenticates by `user_id`). No user-controlled input crosses into the href. Next.js Link validates the href shape to a same-origin path. No open-redirect risk. |
| T-39-NSV01-02 | Information Disclosure / Phase 20 boundary leak | CollectionFitCard.tsx import surface | mitigate | This task adds ZERO new imports — `Link` is already at line 1. Phase 20 D-04 deny-list compliance is vacuous. Verified by `tests/static/CollectionFitCard.no-engine.test.ts` continuing to pass after the edit. |
| T-39-NSV08-01 | Repudiation / false-attribution closure | NSV-08 audit row | mitigate | Task 2 captures the D-08 grep stdout verbatim in the SUMMARY. The closure phrase "already shipped before Phase 39 began" prevents Phase 39 from being credited with code shipped in sibling phases. Phase 33b row update from `partial` → `ship` is grounded in the captured grep evidence, not in Phase 39 commits. |
</threat_model>

<verification>
- CollectionFitCard.tsx mostSimilar rows wrapped in `<Link>` with the D-07 verbatim className
- Phase 20 D-04 static guard test continues to pass
- No new imports added to CollectionFitCard.tsx (existing `Link` import at line 1 reused)
- NSV-08 grep evidence captured in SUMMARY with verbatim stdout
- SleepingBeautiesSection.tsx and GoodDealsSection.tsx UNMODIFIED (D-08 anti-fabrication)
- All three audit rows (NSV-01, NSV-15, NSV-08) referenced in plan frontmatter `audit_rows`
- tsc baseline preserved (27-error pre-existing baseline unchanged)
</verification>

<success_criteria>
- NSV-01 (DISC-AUDIT-82) closed: clicking a mostSimilar row on `/watch/{id}` navigates to `/watch/{rowWatchId}`
- NSV-15 (DISC-AUDIT-71) closed: clicking a mostSimilar row on `/catalog/{id}` navigates to `/watch/{rowWatchId}` (same component, same fix)
- NSV-08 (DISC-AUDIT-129) closed: grep evidence in SUMMARY confirms both insights sections already wrap; no fabricated patches shipped
- ROADMAP Phase 39 success criterion #1 satisfied: plan cites NSV-01 + NSV-15 + NSV-08 + DISC-AUDIT-82 + DISC-AUDIT-71 + DISC-AUDIT-129 in audit_rows frontmatter
- ROADMAP Phase 39 success criterion #2 satisfied: every `<li>` in CollectionFitCard mostSimilar wraps in `<Link href={\`/watch/${watch.id}\`}>`; Phase 20 import-boundary guard still passes
- ROADMAP Phase 39 success criterion #3 satisfied: NSV-08 explicitly marked "already shipped" with grep evidence in plan SUMMARY (no fabricated patches)
</success_criteria>

<output>
After completion, create `.planning/phases/39-audit-driven-discovery-polish/39-03-SUMMARY.md` containing:

1. **NSV-01 + NSV-15 / DISC-AUDIT-82 + DISC-AUDIT-71 — CollectionFitCard mostSimilar Link wrap**
   - File modified (src/components/insights/CollectionFitCard.tsx) with line-count delta
   - Grep evidence: paste output of `grep -nE "<Link|className=\"block hover" src/components/insights/CollectionFitCard.tsx`
   - Phase 20 D-04 static guard test result: paste `npx vitest run tests/static/CollectionFitCard.no-engine.test.ts` summary line
   - tsc baseline check: confirm 27-error pre-existing baseline unchanged
   - Phase 33b row status update note: NSV-01 + NSV-15 → `ship`

2. **NSV-08 / DISC-AUDIT-129 — Verify-Before-Patch Evidence**
   - Exact command run: `grep -nE "<Link|<a " src/components/insights/SleepingBeautiesSection.tsx src/components/insights/GoodDealsSection.tsx`
   - Verbatim stdout pasted in a code block
   - Decision tree branch hit (expected: Case A — both wrap)
   - Closure statement: "NSV-08 / DISC-AUDIT-129 closed as 'already shipped before Phase 39 began'. Zero code changes per D-08."
   - Phase 33b row status update note: NSV-08 → `ship`
   - Execution timestamp (UTC ISO 8601)
</output>
