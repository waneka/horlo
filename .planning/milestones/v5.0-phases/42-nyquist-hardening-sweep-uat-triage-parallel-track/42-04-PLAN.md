---
phase: 42-nyquist-hardening-sweep-uat-triage-parallel-track
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PRE-TRIAGE.md
  - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-HUMAN-UAT.md
autonomous: true
requirements: [DEBT-11]
must_haves:
  truths:
    - "All ~33 deferred UAT items across Phases 18/20/20.1/22/23 are classified with cited evidence"
    - "SUPERSEDED items cite the specific superseding phase/plan; DEFERRED items carry an explicit reason"
    - "Surviving CLOSED-candidate items appear as numbered entries in a blocking 42-HUMAN-UAT.md checklist"
    - "D-01: Claude pre-triages all ~33 UAT items with cited evidence; SUPERSEDED/DEFERRED items close on evidence alone, survivors become the CLOSED-candidate set"
    - "D-03: ambiguous items (unprovable supersession but testable) default to CLOSED-candidate — err toward running, expect a larger-than-minimal batch"
    - "D-04: the 5 stale Phase 20.1 debug entries are already resolved (no move task needed); 42-PRE-TRIAGE.md records this explicitly"
  artifacts:
    - path: ".planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PRE-TRIAGE.md"
      provides: "Claude's evidence-cited pre-triage of all 33 UAT items"
      contains: "CLOSED-candidate"
    - path: ".planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-HUMAN-UAT.md"
      provides: "Blocking human-UAT checklist for the CLOSED-candidate set"
      contains: "result: [pending]"
  key_links:
    - from: "42-PRE-TRIAGE.md CLOSED-candidate rows"
      to: "42-HUMAN-UAT.md numbered tests"
      via: "one checklist entry per surviving item"
      pattern: "### [0-9]"
---

<objective>
Pre-triage all ~33 deferred human UAT items from `v4.0-MILESTONE-AUDIT.md` (Phases 18/20/20.1/22/23) into CLOSED-candidate / SUPERSEDED / DEFERRED with cited evidence (D-01), then build the blocking `42-HUMAN-UAT.md` checklist from the surviving CLOSED-candidate set (D-02, patterned on `41-HUMAN-UAT.md`).

Purpose: D-01 says SUPERSEDED/DEFERRED items close on evidence alone — only genuine survivors need a live run. D-03 says ambiguous items default to CLOSED-candidate (err toward running). This plan does the autonomous evidence work and produces the checklist the user will run in Plan 05.
Output: `42-PRE-TRIAGE.md` (evidence-cited classification) and `42-HUMAN-UAT.md` (skeleton checklist with `result: [pending]`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-CONTEXT.md
@.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md
@.planning/milestones/v4.0-MILESTONE-AUDIT.md
@.planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md
@.planning/phases/41-account-danger-zone-branded-auth-emails-parallel-track/41-HUMAN-UAT.md

<interfaces>
The ~33 items (counts from 42-RESEARCH.md §UAT Source Material, sourced from v4.0-MILESTONE-AUDIT.md `items:` blocks):
- Phase 18 (/explore Discovery Surface): 9 items — sparse-network hero render, See-all surfaces, mobile BottomNav, Follow→/explore RYO, Add-watch→/explore SWR fan-out.
- Phase 20 (Collection Fit + Verdict Copy): 5 items — CollectionFitCard visual rhythm, D-08 self-via-cross-user callout, accordion inline preview, Discovery click-through to /catalog/[catalogId], FIT-02 phrasing on real data.
- Phase 20.1 (Add-Watch Flow Rethink): 8 items — visual smoke, wishlist commit smoke, skip+rail smoke, manual entry inline link, extraction failure recovery, /search inline 3 CTAs, /catalog cross-user 3 CTAs, deep-link /watch/new?catalogId smoke. Audit explicitly notes "most likely partially overtaken by gap-closure plans 06/07/08".
- Phase 22 (Settings Restructure + Account): 6 items — email change end-to-end, password fresh-session, password stale-session re-auth, /settings vertical-tabs visual, /preferences redirect, email-change banner persistence.
- Phase 23 (Settings Sections + Schema-Field UI): 5 items — preferences persistence, similarity preference re-read, theme cross-surface sync, notesPublic cross-page revalidation, chronometer Certification row. Full test steps in 23-VERIFICATION.md `human_verification:` frontmatter array.

D-04 is ALREADY DONE: the 5 stale Phase 20.1 debug entries are already in `.planning/debug/resolved/` (verified). Do NOT create a task to move them — the triage table will simply note D-04 as already-resolved (handled in Plan 05).

Evidence sources for SUPERSEDED classification:
- Gap-closure plans 20.1-06/07/08 SUMMARYs (closed multiple original 20.1 findings).
- Phase 39 + 39b changes (audit-driven discovery polish — many surfaces reshaped).
- Phase 32 resolved the DEBT-09 notesPublic regression (relevant to the Phase 23 notesPublic item).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-triage all ~33 UAT items with cited evidence</name>
  <files>.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PRE-TRIAGE.md</files>
  <read_first>
    - .planning/milestones/v4.0-MILESTONE-AUDIT.md (the authoritative `items:` blocks for Phases 18/20/20.1/22/23)
    - .planning/milestones/v4.0-phases/23-settings-sections-schema-field-ui/23-VERIFICATION.md (`human_verification:` array — detailed test steps for Phase 23's 5 items)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-RESEARCH.md (§UAT Source Material — pre-triage signals; §D-04 Already Resolved)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-CONTEXT.md (D-01/D-02/D-03 — triage process rules)
  </read_first>
  <action>
    Create `42-PRE-TRIAGE.md`. Enumerate every UAT item from the `v4.0-MILESTONE-AUDIT.md` `items:` blocks for Phases 18/20/20.1/22/23 — one row per item. Use a Markdown table with columns: `# | Original Phase | Item Description | Disposition | Evidence`.
    Classify each item per D-01:
    - SUPERSEDED: the item's surface was overtaken by later work. Cite the SPECIFIC superseding artifact — gap-closure plan `20.1-06`/`07`/`08`, a Phase 39/39b plan, Phase 32 (notesPublic regression fix), or a named quick task. To assert supersession, read the cited SUMMARY and confirm the surface match; do not guess.
    - DEFERRED: the item genuinely cannot be closed now (e.g. live-network or sparse-network states not reliably reproducible). Provide an explicit reason and a v5.x carry-forward note. Use DEFERRED sparingly — D-03 says ambiguous items default to CLOSED-candidate.
    - CLOSED-candidate: the item genuinely survived and needs a real run. This is the default for any item where supersession cannot be proven and the behavior IS testable (even if expensive — D-03 says err toward running, expect a larger-than-minimal batch).
    For each Phase 23 item, pull the precise expected behavior from the 23-VERIFICATION.md `human_verification:` array so the CLOSED-candidate entries have exact expected text.
    Add a top-of-file summary line: total items, count CLOSED-candidate / SUPERSEDED / DEFERRED. Add a note that D-04 (5 stale debug entries) is already resolved — they are in `.planning/debug/resolved/` and are NOT UAT items, so they are out of this table (the final closure table in Plan 05 records D-04 as already-resolved).
    Every row MUST have a non-empty Evidence cell — no blank dispositions.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PRE-TRIAGE.md','utf8');const rows=s.split('\n').filter(l=>/^\|\s*\d+\s*\|/.test(l));if(rows.length<30)throw new Error('expected ~33 item rows, found '+rows.length);for(const r of rows){const cells=r.split('|').map(c=>c.trim());if(!/^(CLOSED-candidate|SUPERSEDED|DEFERRED)$/.test(cells[4]||''))throw new Error('bad disposition in row: '+r);if(!(cells[5]||'').length)throw new Error('empty evidence in row: '+r);}console.log('pre-triage OK: '+rows.length+' rows')"</automated>
  </verify>
  <acceptance_criteria>
    - `42-PRE-TRIAGE.md` exists with a table of ~33 numbered item rows covering Phases 18/20/20.1/22/23.
    - Every row's Disposition is exactly one of `CLOSED-candidate`, `SUPERSEDED`, `DEFERRED`.
    - Every row has a non-empty Evidence cell; SUPERSEDED rows cite a specific superseding plan/phase, DEFERRED rows give an explicit reason.
    - The file has a top summary line with total + per-disposition counts and a note that D-04 is already resolved.
    - No item is left unclassified.
  </acceptance_criteria>
  <done>All 33 UAT items classified with cited evidence; the CLOSED-candidate set is identified.</done>
</task>

<task type="auto">
  <name>Task 2: Build the blocking 42-HUMAN-UAT.md checklist from CLOSED-candidates</name>
  <files>.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PRE-TRIAGE.md (Task 1 output — the CLOSED-candidate rows)
    - .planning/phases/41-account-danger-zone-branded-auth-emails-parallel-track/41-HUMAN-UAT.md (exact shape to reuse per D-02)
    - .planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-PATTERNS.md (§42-HUMAN-UAT.md — frontmatter adaptation)
  </read_first>
  <action>
    Create `42-HUMAN-UAT.md` following the exact `41-HUMAN-UAT.md` shape.
    Frontmatter: `status: partial`, `phase: 42-nyquist-hardening-sweep-uat-triage-parallel-track`, `source: [42-PRE-TRIAGE.md — CLOSED-candidate items]`, `started:` and `updated:` ISO timestamps.
    Body sections: `## Current Test` (`[awaiting human testing]`), `## Tests`, `## Summary`, `## Gaps`.
    Under `## Tests`, one `### N. <short title>` entry per CLOSED-candidate row from `42-PRE-TRIAGE.md`. Each entry has:
    - `expected:` — the precise expected behavior copied from the original UAT item (for Phase 23 items use the exact `human_verification:` expected text from 23-VERIFICATION.md). Include any setup steps the user needs (URL, preconditions).
    - `original phase:` — note which v4.0 phase the item came from so the user has context.
    - `result: [pending]`.
    `## Summary`: `total: <N CLOSED-candidates>`, `passed: 0`, `issues: 0`, `pending: <N>`, `skipped: 0`, `blocked: 0`.
    Do NOT include SUPERSEDED or DEFERRED items in this checklist — they close on evidence in Plan 05's closure table, not via a live run.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-HUMAN-UAT.md','utf8');if(!/status:\s*partial/.test(s))throw new Error('frontmatter status must be partial');const tests=(s.match(/^###\s+\d+\./gm)||[]).length;if(tests<1)throw new Error('no numbered test entries');const pend=(s.match(/result:\s*\[pending\]/g)||[]).length;if(pend!==tests)throw new Error('every test must have result: [pending]; tests='+tests+' pending='+pend);const m=s.match(/total:\s*(\d+)/);if(!m||Number(m[1])!==tests)throw new Error('Summary total must equal test count');console.log('HUMAN-UAT OK: '+tests+' CLOSED-candidate tests')"</automated>
  </verify>
  <acceptance_criteria>
    - `42-HUMAN-UAT.md` exists with frontmatter `status: partial` and `source` referencing `42-PRE-TRIAGE.md`.
    - One `### N.` test entry per CLOSED-candidate item from `42-PRE-TRIAGE.md`; each has `expected:`, `original phase:`, and `result: [pending]`.
    - `## Summary` `total:` equals the number of `### N.` entries; `pending:` equals `total:`; `passed: 0`.
    - No SUPERSEDED or DEFERRED items appear as checklist entries.
  </acceptance_criteria>
  <done>Blocking human-UAT checklist built; ready for the user to run in Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — planning artifacts only) | This plan writes Markdown triage and checklist files. No code, no API, no data handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Repudiation | Triage dispositions in 42-PRE-TRIAGE.md | accept | Every SUPERSEDED/DEFERRED disposition carries cited evidence (specific plan/phase or explicit reason); the verify gate rejects empty evidence cells. Documentation-only; no runtime threat surface. |

This plan produces triage documentation only. No production threat surface; documented honestly per the security-enforcement note for this phase.
</threat_model>

<verification>
- `42-PRE-TRIAGE.md` classifies all ~33 items with non-empty evidence.
- `42-HUMAN-UAT.md` has one entry per CLOSED-candidate, all `result: [pending]`, `Summary` counts consistent.
</verification>

<success_criteria>
- DEBT-11 pre-triage (D-01) complete with cited evidence.
- The blocking checklist (D-02) is ready; SUPERSEDED/DEFERRED items excluded from the live run.
- D-04 noted as already-resolved (no move task created).
</success_criteria>

<output>
After completion, create `.planning/phases/42-nyquist-hardening-sweep-uat-triage-parallel-track/42-04-SUMMARY.md`
</output>
