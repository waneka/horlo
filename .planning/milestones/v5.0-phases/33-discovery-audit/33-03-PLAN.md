---
phase: 33-discovery-audit
plan: 03
type: execute
wave: 3
depends_on: [33-02]
files_modified:
  - .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
autonomous: true
requirements: [DISC-10]
tags: [audit, documentation, discovery, gate-annotation]

must_haves:
  truths:
    - "Every Click-Path Audit row has viewer_state ∈ {owner-populated, fresh-account, N/A} — zero TBD remain"
    - "Rows affected by gates G-1..G-20 from RESEARCH.md carry the correct viewer_state per gate condition"
    - "Rows whose runtime gate produces TWO observable states (e.g., G-3 catalog framing, G-4 verdict suppression, G-8 LockedTabCard) are SPLIT into two rows — one per viewer_state — sharing element/target but with different evidence + viewer_state"
    - "Implements decisions: D-03 (every row carries owner-populated / fresh-account / N/A in the viewer_state column — Pass B finalizes the assignments started in Pass A based on which viewer state actually renders the affordance), D-04 (gate-driven viewport divergence is captured during gate annotation — when a runtime gate's outcome differs at desktop vs mobile, the row is split per viewport in addition to viewer_state, applying the D-04 viewport row-splitting heuristic)"
    - "Zero files modified outside .planning/phases/33-discovery-audit/"
  artifacts:
    - path: ".planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md"
      provides: "Click-Path Audit table with viewer_state column finalized; row-splits applied for runtime-gate-divergent rows; viewport column unchanged from Wave 1; ready for Wave 3 browser pass"
      contains: "owner-populated"
  key_links:
    - from: "Audit rows annotated with viewer_state=owner-populated"
      to: "RESEARCH.md G-1..G-20 owner-populated branch conditions"
      via: "gate annotation"
      pattern: "owner-populated"
    - from: "Audit rows annotated with viewer_state=fresh-account"
      to: "RESEARCH.md G-1..G-20 fresh-account branch conditions"
      via: "gate annotation"
      pattern: "fresh-account"
---

<objective>
Pass B — runtime-gate annotation (D-01 follow-up). Walk the 20 runtime gates G-1..G-20 cataloged in RESEARCH.md §"Conditional Rendering Map" and update the Click-Path Audit table so every row's `viewer_state` column carries a final value (owner-populated / fresh-account / N/A) instead of TBD. For gates that produce TWO observable rendered states (e.g., G-3 catalog "you own this" vs cross-user framing, G-4 verdict suppression vs verdict shown, G-8 LockedTabCard vs unlocked content), SPLIT the affected rows into two rows sharing element/target but with different viewer_state values.

Purpose: Pass A's source-grep can't fully see runtime gates — the source confirms a gate EXISTS but the audit needs to know what RENDERS in each viewer state. Pass B closes that gap WITHOUT touching the browser yet (Wave 3 Plan 04). This pass is pure source-reading: read the gate file:line, determine which viewer states render, update the audit rows.

Output: Every Click-Path Audit row has viewer_state ∈ {owner-populated, fresh-account, N/A}. Row count grows by ~10–30 from gate-induced splits (RESEARCH.md A1: 130–210 final rows after viewer_state and viewport splits). Zero application-code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-discovery-audit/33-CONTEXT.md
@.planning/phases/33-discovery-audit/33-RESEARCH.md
@.planning/phases/33-discovery-audit/33-PATTERNS.md
@.planning/phases/33-discovery-audit/33-VALIDATION.md
@.planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
@.planning/phases/33-discovery-audit/33-02-SUMMARY.md

<interfaces>
<!-- Locked schema (D-09, D-10, D-11) — unchanged from Plan 02. -->

<!-- Runtime Gate Map — RESEARCH.md §Conditional Rendering Map lines 224–250. Each gate maps to (a) which audit rows it affects, (b) what viewer_state(s) the gate produces. -->

<!-- High-stakes gates (RESEARCH.md "10 highest-stakes"): G-1, G-3, G-4, G-6, G-7, G-8, G-12, G-13, G-15, G-19. These are the gates Wave 3 Plan 04 walks in the browser. -->

<!-- Gate → viewer_state assignment table (apply to audit rows whose evidence file:line falls under each gate's component subtree): -->
<!--   G-1 (/explore ExploreHero):                  rows whose element refers to ExploreHero CTAs → split: one row viewer_state=fresh-account (gate fires, ExploreHero renders), one row viewer_state=owner-populated (gate suppressed, ExploreHero absent — this row tag stays Live since absence-by-design is correct). -->
<!--   G-2 (/catalog UUID 404):                     N/A — error state, not an affordance. Skip. -->
<!--   G-3 (/catalog "you own this" framing):       rows on /catalog/{catalogId} where element refers to CatalogPageActions CTAs → split into owner-populated-cross-user (CTAs render) + owner-populated-self (CTAs absent — but D-10 viewer_state enum has only 3 values; collapse "owner-populated-self" into owner-populated and note in evidence). Final: owner-populated. -->
<!--   G-4 (/catalog verdict suppression):          CatalogPageActions + verdict rows → split: owner-populated (verdict + CTAs render) + fresh-account (verdict suppressed, no CTAs — affordance absence; the row stays as-is with viewer_state=fresh-account and tag=Live for "no affordance, by design" OR Missing if Rdio principle expects something there). -->
<!--   G-5 (/watch notFound 404):                   N/A — error state. -->
<!--   G-6 (/watch verdict suppression):            same as G-4 — split CollectionFitCard rows on /watch/{id}. -->
<!--   G-7 (/watch same-user vs cross-user):        Edit/Delete affordance rows → owner-populated only. Brand pill / "Same Watch by Other Collectors" rail rows → owner-populated AND fresh-account (gate-independent rendering). -->
<!--   G-8 (/u/{user}/collection LockedTabCard):    LockedTabCard "Connect" CTA row → fresh-account (only fresh-account viewing private profile sees the locked card; the lock IS the affordance per RESEARCH.md Pitfall 2). ProfileWatchCard rows → owner-populated (only owner sees their own collection populated). Add a separate Live row for the LockedTabCard rendering as a fresh-account affordance. -->
<!--   G-9 (/u/{user}/wishlist LockedTabCard):      same pattern as G-8. -->
<!--   G-10 (/u/{user}/notes LockedTabCard WR-01):  same pattern as G-8. -->
<!--   G-11 (/u/{user}/stats LockedTabCard):        same pattern as G-8. -->
<!--   G-12 (/u/{user}/common-ground 404 gate):     watch-card affordance rows → fresh-account renders 404 (not Live; Wave 3 confirms). Owner-populated viewing a follower with overlap → Live. Split: one row owner-populated (Live, affordance works), one row fresh-account (Dead candidate — 404 instead of content; OR Missing if Rdio expects a fallback CTA). -->
<!--   G-13 (/u/{user}/insights owner-only):        all rows → owner-populated. -->
<!--   G-14 (/u/{user} LockedProfileState):         affects ALL profile-tab rows when non-owner views private profile. Per D-07 each tab is its own block; capture LockedProfileState as a single row on EACH affected tab block (or note in evidence "see LockedTabCard rows G-8/G-9/G-10/G-11"). Recommend: do NOT add 7 redundant LockedProfileState rows; instead note in each tab's existing LockedTabCard row that "LockedProfileState pre-empts at layout level when profilePublic=false". Decide based on table cleanliness — if it inflates beyond the 250 ceiling, consolidate. -->
<!--   G-15 (/u/{user}/notes per-row notesPublic):  NoteRow affordance rows → split: owner-populated (sees all rows), fresh-account (sees only notesPublic !== false rows). The per-row visibility filter doesn't change the AFFORDANCE — same NoteRow click affordance — but Wave 3 should confirm that fresh-account viewing a profile with mixed public/private notes sees only public ones. Single row owner-populated; add a separate row noting "viewer_state: fresh-account renders subset" if needed for clarity. -->
<!--   G-16 (/u/{user}/worn per-row wornPublic):    same pattern as G-15. -->
<!--   G-17 (proxy.ts auth gate):                   N/A — D-03 explicitly skips anon walk; not audited. -->
<!--   G-18 (Header / BottomNav null on PUBLIC_PATHS): N/A — Open Question Q2 from RESEARCH.md (recommendation: skip; PUBLIC_PATHS are out of scope per D-05). -->
<!--   G-19 (ProfileTabs showCommonGround / Insights tab visibility): ProfileTabs link rows → split: owner-populated sees Insights link (G-13 makes it visible to owner only), fresh-account sees Common Ground link only when overlap.hasAny. The link rows on the ProfileTabs source-pass row need to capture which viewer state sees each tab link. -->
<!--   G-20 (CollectionTabContent AddWatchCard env gate): AddWatchCard CTA row → owner-populated (env-dependent; production has ANTHROPIC_API_KEY per CLAUDE.md tech stack); evidence note: "env-dependent; production has key per CLAUDE.md". Single row owner-populated. -->

<!-- Decision rule for "should I split a row?" -->
<!--   SPLIT when: gate produces visibly different rendered output across owner-populated and fresh-account that affects the affordance set (presence/absence of element, different target route). -->
<!--   COLLAPSE when: gate produces identical rendering with viewer-state-dependent data inside (e.g., CollectorsLikeYou shows the SAME row layout to both owner and fresh-account; the data is empty for fresh-account but the affordance EXISTS in both states). Use viewer_state=owner-populated when the affordance is INTENDED for the populated case; use N/A when truly viewer-independent (search input, header logo). -->

<!-- TBD elimination protocol: every row currently tagged TBD MUST get a final value. If a row's gate analysis is genuinely indeterminate, DEFAULT to owner-populated (the populated case is the audit's primary lens per CONTEXT.md "owner is product owner") and add a TODO note in evidence: "viewer_state defaulted owner-populated; Wave 3 browser pass to confirm". -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk gates G-1..G-13; update viewer_state column on /, /explore, /search, /catalog, /watch surface rows; perform row-splits for G-3/G-4/G-6/G-7/G-8/G-12 divergent renderings</name>
  <files>
    .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
  </files>
  <read_first>
    - .planning/phases/33-discovery-audit/33-RESEARCH.md (lines 224–250: gate map G-1..G-13; lines 251–267: viewport divergence catalog V-1..V-8 — relevant where a viewport-split row also crosses a viewer-state gate)
    - .planning/phases/33-discovery-audit/33-CONTEXT.md (D-03 owner + fresh-account states; D-11 Live/Dead/Redundant/Missing definitions to apply when a split row reveals an absence)
    - .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md (CURRENT STATE — read to find every row with viewer_state=TBD that lives on `/`, `/explore`, `/search`, `/catalog/{catalogId}`, `/watch/{id}` surfaces)
    - src/app/explore/page.tsx (line ~38 — G-1 ExploreHero gate condition)
    - src/app/catalog/[catalogId]/page.tsx (lines ~46, ~70, ~79–110 — G-2/G-3/G-4 gate conditions)
    - src/app/watch/[id]/page.tsx (lines ~25, ~41, ~52 — G-5/G-6/G-7 gate conditions)
    - src/app/u/[username]/[tab]/page.tsx (lines ~107, ~116, ~135, ~234 — G-8/G-9/G-10/G-11 LockedTabCard gates; line ~87 G-12 common-ground 404; lines ~100–103 G-13 insights owner-only)
    - src/proxy.ts (lines 11–15 — G-17 auth gate; for D-03 skip rationale)
  </read_first>
  <action>
    For every row currently in 33-DISCOVERY-AUDIT.md with `viewer_state: TBD` whose `surface` column is one of `/`, `/explore`, `/search`, `/catalog/{catalogId}`, or `/watch/{id}`, determine the correct viewer_state by walking the relevant gate(s):

    1. Read the file:line cited in the row's `evidence` cell.
    2. Walk up to the nearest gate condition in the rendering chain (e.g., ExploreHero rendering is gated at `src/app/explore/page.tsx:38`).
    3. Apply the gate map from `<interfaces>` to assign a final viewer_state.
    4. If the gate produces TWO observable rendered states (G-3, G-4, G-6, G-7, G-8, G-12), SPLIT the row into two rows — same element + target, different viewer_state, slightly different evidence (`evidence: src/path:LINE — owner-populated branch` vs `evidence: src/path:LINE — fresh-account branch (verdict suppressed; CTAs absent)`). Increment row IDs sequentially for the new rows.
    5. If the gate produces ONE observable state (G-13 owner-only insights, G-1 ExploreHero fresh-account-only — note that "absent in the other state" is correct behavior, not a row), assign the single viewer_state and leave a single row.
    6. For rows whose affordance is genuinely viewer-independent (e.g., header logo, search input — the affordance EXISTS for any viewer that has reached the surface), assign `viewer_state: N/A`.

    Apply specific gate-driven splits:

    - **G-1 (/explore ExploreHero)**: any row whose element refers to ExploreHero CTAs → assign `viewer_state: fresh-account` (single row; ExploreHero is absent in owner-populated state, so the affordance only exists for fresh-account viewers).
    - **G-3 (/catalog ownership framing)**: CatalogPageActions CTAs (Wishlist / Collection / Skip) → 1 row `viewer_state: owner-populated` with evidence noting "cross-user catalog ref; owner viewing own catalog ref hides these CTAs". Do NOT add a "self-catalog" row — the absence of CTAs in self-catalog state is design-correct, captured implicitly.
    - **G-4 (/catalog verdict suppression)**: CollectionFitCard verdict rows + CatalogPageActions rows on `/catalog/{catalogId}` → SPLIT each row into 2: one `viewer_state: owner-populated` (verdict + CTAs render; tag=Live) + one `viewer_state: fresh-account` (verdict + CTAs suppressed; tag=Missing IF the SEED-004 Rdio principle is violated by the absence — e.g., "fresh-account viewer lands on /catalog/{id} with no affordance to walk to other watches in the same family", evidence cites Rdio quote per D-12; OR tag=Live if the empty state is acceptable).
    - **G-6 (/watch verdict suppression)**: same pattern as G-4.
    - **G-7 (/watch same-user vs cross-user)**: Edit + Delete affordance rows → 1 row `viewer_state: owner-populated` (the only state these affordances render). Brand pill / "Same Watch by Other Collectors" rows → `viewer_state: N/A` (renders for any viewer).
    - **G-12 (/u/{user}/common-ground 404)**: SPLIT the watch-card affordance row into 2: `viewer_state: owner-populated` (Live, tab works) + `viewer_state: fresh-account` (Dead candidate — 404 page instead of content; evidence: "src/app/u/[username]/[tab]/page.tsx:87 — gate-fail returns 404; Wave 3 to confirm browser experience"; tag stays Dead PROVISIONAL until Wave 3, OR Missing if Rdio principle expects a "explore other follows" fallback CTA on the 404 page).
    - **G-13 (/u/{user}/insights owner-only)**: insights tab rows → `viewer_state: owner-populated` (single state; non-owner gets 404 by design — not a row).

    Row-split mechanic: for each row that splits, INSERT the new row IMMEDIATELY AFTER the original (preserves visual locality), assign the next sequential DISC-AUDIT-NN id, copy element + target + viewport, modify evidence to disambiguate the two states, set the new viewer_state.

    Re-number all subsequent rows that come AFTER the inserted rows so the sequence remains flat-sequential with no gaps. (Use a script if helpful: `awk` to renumber DISC-AUDIT-NN ids in column 1.)

    After updating, run `bash .planning/phases/33-discovery-audit/checks/quick.sh` — must exit 0.
  </action>
  <verify>
    <automated>bash .planning/phases/33-discovery-audit/checks/quick.sh && [ "$(awk -F'\\|' '/^\| DISC-AUDIT-/ { vs=$8; gsub(/^ | $/, "", vs); if (vs == "TBD") c++ } END { print c+0 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md)" -lt "$(awk -F'\\|' '/^\| DISC-AUDIT-/ { c++ } END { print c+0 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - On the 5 surfaces handled by this task (`/`, `/explore`, `/search`, `/catalog/{catalogId}`, `/watch/{id}`): zero rows still have `viewer_state: TBD`. Verify: `awk -F'\\|' '/^\| DISC-AUDIT-/ { surface=$3; vs=$8; gsub(/^ | $/, "", surface); gsub(/^ | $/, "", vs); if ((surface == "/" || surface == "/explore" || surface == "/search" || surface == "/catalog/{catalogId}" || surface == "/watch/{id}") && vs == "TBD") print $2 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - At least 2 row-splits for G-4 visible: rows on `/catalog/{catalogId}` whose element references "verdict" or "CatalogPageActions" appear with BOTH `viewer_state: owner-populated` AND `viewer_state: fresh-account`. Verify: `awk -F'\\|' '/^\| DISC-AUDIT-/ { surface=$3; element=$4; vs=$8; gsub(/^ | $/, "", surface); gsub(/^ | $/, "", element); gsub(/^ | $/, "", vs); if (surface == "/catalog/{catalogId}" && (element ~ /[Vv]erdict/ || element ~ /CatalogPageActions/) && vs == "owner-populated") c1++; if (surface == "/catalog/{catalogId}" && (element ~ /[Vv]erdict/ || element ~ /CatalogPageActions/) && vs == "fresh-account") c2++ } END { exit !(c1 >= 1 && c2 >= 1) }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` exits 0
    - At least 1 G-12 split row on `/u/{user}/common-ground` exists if Plan 02 produced any common-ground rows: optional check (depends on Plan 02 row inventory)
    - G-1 ExploreHero rows assigned `viewer_state: fresh-account`: `awk -F'\\|' '/^\| DISC-AUDIT-/ { surface=$3; element=$4; vs=$8; gsub(/^ | $/, "", surface); gsub(/^ | $/, "", element); gsub(/^ | $/, "", vs); if (surface == "/explore" && element ~ /ExploreHero/ && vs != "fresh-account") print $2 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - G-7 Edit/Delete on `/watch/{id}` assigned `viewer_state: owner-populated`: `awk -F'\\|' '/^\| DISC-AUDIT-/ { surface=$3; element=$4; vs=$8; gsub(/^ | $/, "", surface); gsub(/^ | $/, "", element); gsub(/^ | $/, "", vs); if (surface == "/watch/{id}" && (element ~ /Edit/ || element ~ /Delete/) && vs != "owner-populated") print $2 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - Row IDs remain flat sequential after splits: `grep -oE '^\| DISC-AUDIT-[0-9]+' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md | sed 's/| DISC-AUDIT-//' | sort -n | uniq -d` returns no lines AND `IDS=$(grep -oE '^\| DISC-AUDIT-[0-9]+' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md | sed 's/| DISC-AUDIT-//' | sort -n); FIRST=$(echo "$IDS" | head -1); LAST=$(echo "$IDS" | tail -1); COUNT=$(echo "$IDS" | wc -l); test "$COUNT" -eq "$((LAST - FIRST + 1))"` exits 0 (no gaps)
    - `bash .planning/phases/33-discovery-audit/checks/quick.sh` exits 0
    - `git status --porcelain | grep -v '^?? \.planning/phases/33-discovery-audit/' | grep -v '^ M \.planning/phases/33-discovery-audit/'` returns no lines
  </acceptance_criteria>
  <done>All TBD viewer_state values on `/`, `/explore`, `/search`, `/catalog/{catalogId}`, `/watch/{id}` rows are replaced; G-3/G-4/G-6/G-7/G-12 row-splits applied; row IDs renumbered flat-sequential. quick.sh exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Walk gates G-8..G-20; update viewer_state on Header + 7 profile-tab rows; finalize all remaining TBD values</name>
  <files>
    .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
  </files>
  <read_first>
    - .planning/phases/33-discovery-audit/33-RESEARCH.md (lines 234–250: gates G-8..G-20)
    - .planning/phases/33-discovery-audit/33-CONTEXT.md (D-08 Header single-block; D-07 each profile tab is its own block; D-11 LockedTabCard is Live, NOT Dead, per RESEARCH.md Pitfall 2)
    - .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md (CURRENT STATE — read to find every remaining `viewer_state: TBD` row on Header + profile-tab surfaces)
    - src/app/u/[username]/[tab]/page.tsx (lines 107, 116, 135, 234 LockedTabCard branches; line 87 common-ground gate; line 73 G-20 AddWatchCard env gate; lines 100–103 G-13 insights owner-only)
    - src/app/u/[username]/layout.tsx (line ~47 — G-14 LockedProfileState branch)
    - src/components/profile/ProfileTabs.tsx (lines 54–55 — G-19 showCommonGround + isOwner conditional tab visibility)
    - src/components/layout/Header.tsx + SlimTopNav.tsx + DesktopTopNav.tsx + UserMenu.tsx + BottomNav.tsx + NavWearButton.tsx (Header surface rows for G-18 PUBLIC_PATHS recommendation: skip; for D-08 single-row policy)
    - src/lib/constants/public-paths.ts (G-18 reference for skip rationale)
  </read_first>
  <action>
    For every row currently in 33-DISCOVERY-AUDIT.md with `viewer_state: TBD` whose `surface` column is one of `Header`, `/u/{user}/collection`, `/u/{user}/wishlist`, `/u/{user}/worn`, `/u/{user}/notes`, `/u/{user}/stats`, `/u/{user}/common-ground`, or `/u/{user}/insights`, determine the correct viewer_state by walking the relevant gate(s) per the gate map in `<interfaces>`.

    Apply specific gate-driven assignments:

    - **Header rows (D-08 single-block, G-18 skip)**: most Header affordances render for any authenticated viewer → `viewer_state: N/A`. UserMenu trigger rows → `viewer_state: N/A` (renders whenever a user exists). NotificationBell → `viewer_state: N/A` (data inside is per-viewer; affordance is universal). Auth-page-hidden behavior (G-18) is per-route, not per-viewer-state — note in evidence "renders NULL on PUBLIC_PATHS per BottomNav.tsx:104" but assign N/A.
    - **G-8 (/u/{user}/collection)**:
      - LockedTabCard "Connect" / "Sign in to view" CTA rows → `viewer_state: fresh-account` (the lock UI ONLY renders for non-owner viewing private collection; per Pitfall 2, tag stays Live — the lock IS the designed affordance).
      - ProfileWatchCard rows + AddWatchCard CTA → `viewer_state: owner-populated` (only owner sees their own collection populated AND has add-CTA visibility; G-20 env gate notes "env-dependent" in evidence).
      - If a row genuinely renders for both owner and fresh-account (e.g., a public-collection viewer can see the same affordances as the owner minus owner-only CTAs), SPLIT into 2 rows.
    - **G-9 (/u/{user}/wishlist)**: same pattern as G-8. The WR-07 flagship Dead row from Plan 02 stays `viewer_state: TBD` → reassign to `owner-populated` (drag-reorder is owner-only). Update evidence to note: "owner-populated drag-reorder; src/app/actions/wishlist.ts:206 silent no-op; Wave 3 browser pass confirms visible regression".
    - **G-10 (/u/{user}/notes)**: NoteVisibilityPill (DEBT-09) → `owner-populated` (`viewerCanEdit={isOwner}`). NoteRow click → 1 row `viewer_state: N/A` (renders for owner and any viewer who can see the note per WR-01 collection-public gate; the per-row notesPublic filter affects what's RENDERED, not what AFFORDANCE exists). LockedTabCard → `fresh-account` per G-8 pattern.
    - **G-11 (/u/{user}/stats)**: stats are read-only mostly. StatsCard "View all" affordances → `viewer_state: N/A` (renders for any viewer with access). LockedTabCard → `fresh-account`.
    - **G-12 (/u/{user}/common-ground)**: SPLIT pattern from Task 1; if Task 1 didn't split a common-ground row, do it here. Owner-populated viewing follower with overlap → Live. Fresh-account viewing owner → Dead candidate (404) OR Missing if Rdio expects a fallback (cite Rdio quote in evidence).
    - **G-13 (/u/{user}/insights)**: all rows → `viewer_state: owner-populated` (G-13 owner-only).
    - **G-14 (LockedProfileState)**: do NOT add 7 rows for LockedProfileState across each tab. Recommendation: capture ONE row on each LockedTabCard row's evidence noting "LockedProfileState pre-empts at layout.tsx:47 when profilePublic=false". This is per RESEARCH.md G-14 collapse-to-existing rationale.
    - **G-15 (notes per-row notesPublic)**: NoteRow rows already assigned N/A above; if Wave 3 browser pass observes that fresh-account sees a SUBSET, append "viewer_state: fresh-account renders subset per `notesPublic !== false`" to evidence. No additional row.
    - **G-16 (worn per-row wornPublic)**: same pattern as G-15.
    - **G-19 (ProfileTabs visibility)**: ProfileTabs link rows for "Common Ground" → `viewer_state: TBD` should be split: `owner-populated` (if owner has overlap with self — collapse, Common Ground tab itself is hidden for owner viewing self per Phase 25 D-02; revisit) + `fresh-account` (visible only when overlap.hasAny; capture as 1 row `viewer_state: fresh-account` with evidence noting "ProfileTabs.tsx:54-55 — visible only when showCommonGround"). ProfileTabs link for "Insights" → `viewer_state: owner-populated` (G-13 ProfileTabs OMITS the link entirely for non-owners — two-layer privacy).
    - **G-20 (AddWatchCard env gate)**: AddWatchCard CTA rows → `viewer_state: owner-populated`; evidence note: "env-dependent on ANTHROPIC_API_KEY; production has key per CLAUDE.md tech stack — confirm in Wave 3".

    For ANY remaining `viewer_state: TBD` rows after applying the gate map: default to `owner-populated` (per `<interfaces>` rule: owner is the audit's primary lens) and append a TODO note in evidence: "viewer_state defaulted owner-populated per gate-indeterminate; Wave 3 browser pass to confirm".

    After updating, re-number row IDs if any new rows were added.

    Run `bash .planning/phases/33-discovery-audit/checks/quick.sh` — must exit 0.

    Run a TBD-zero check:
    ```bash
    awk -F'\\|' '/^\| DISC-AUDIT-/ { vs=$8; gsub(/^ | $/, "", vs); if (vs == "TBD") c++ } END { print c+0 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
    ```
    Expected output: `0`.
  </action>
  <verify>
    <automated>bash .planning/phases/33-discovery-audit/checks/quick.sh && [ "$(awk -F'\\|' '/^\| DISC-AUDIT-/ { vs=$8; gsub(/^ | $/, "", vs); if (vs == "TBD") c++ } END { print c+0 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - ZERO rows have `viewer_state: TBD` after Task 2 completes: `awk -F'\\|' '/^\| DISC-AUDIT-/ { vs=$8; gsub(/^ | $/, "", vs); if (vs == "TBD") c++ } END { print c+0 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns 0
    - Every viewer_state value is one of {owner-populated, fresh-account, N/A}: `awk -F'\\|' '/^\| DISC-AUDIT-/ { vs=$8; gsub(/^ | $/, "", vs); if (vs != "owner-populated" && vs != "fresh-account" && vs != "N/A") print $2 ":" vs }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - Header rows assigned N/A (D-08 single-block + universal-affordance): `awk -F'\\|' '/^\| DISC-AUDIT-/ { surface=$3; vs=$8; gsub(/^ | $/, "", surface); gsub(/^ | $/, "", vs); if (surface == "Header" && vs != "N/A") print $2 ":" vs }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines (allow exception only if a Header affordance is genuinely owner-only — document in evidence)
    - WR-07 wishlist.ts:206 row has `viewer_state: owner-populated`: `awk -F'\\|' '/^\| DISC-AUDIT-/ { ev=$7; vs=$8; gsub(/^ | $/, "", ev); gsub(/^ | $/, "", vs); if (ev ~ /wishlist\.ts:206/ && vs != "owner-populated") print $2 ":" vs }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - Insights tab rows assigned owner-populated (G-13): `awk -F'\\|' '/^\| DISC-AUDIT-/ { surface=$3; vs=$8; gsub(/^ | $/, "", surface); gsub(/^ | $/, "", vs); if (surface == "/u/{user}/insights" && vs != "owner-populated") print $2 ":" vs }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` returns no lines
    - LockedTabCard rows present on at least 3 profile-tab blocks (G-8, G-9, G-10, G-11), each tagged Live with viewer_state=fresh-account (per RESEARCH.md Pitfall 2): `awk -F'\\|' '/^\| DISC-AUDIT-/ { surface=$3; element=$4; tag=$6; vs=$8; gsub(/^ | $/, "", surface); gsub(/^ | $/, "", element); gsub(/^ | $/, "", tag); gsub(/^ | $/, "", vs); if (element ~ /LockedTabCard|Locked.*Card|Connect|Sign in/ && tag == "Live" && vs == "fresh-account") seen[surface]++ } END { c=0; for (s in seen) c++; exit !(c >= 3) }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md` exits 0
    - Total row count between 130 and 250 (RESEARCH.md A1 estimate 130–210; allow ≤ 250 for safety): `[ "$(grep -c '^| DISC-AUDIT-' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md)" -ge 130 ] && [ "$(grep -c '^| DISC-AUDIT-' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md)" -le 250 ]`
    - Row IDs flat sequential, no gaps, no duplicates: same checks as Task 1
    - `bash .planning/phases/33-discovery-audit/checks/quick.sh` exits 0
    - `git status --porcelain | grep -v '^?? \.planning/phases/33-discovery-audit/' | grep -v '^ M \.planning/phases/33-discovery-audit/'` returns no lines
  </acceptance_criteria>
  <done>All viewer_state values are final ({owner-populated, fresh-account, N/A}); zero TBD remain; LockedTabCard rows correctly tagged Live with fresh-account viewer_state per Pitfall 2; insights rows owner-populated; total rows in 130–250 range. quick.sh exits 0.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Zero TBD remaining
awk -F'\\|' '/^\| DISC-AUDIT-/ { vs=$8; gsub(/^ | $/, "", vs); if (vs == "TBD") c++ } END { print c+0 }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md
# Expect: 0

# All viewer_state values are valid
awk -F'\\|' '/^\| DISC-AUDIT-/ { vs=$8; gsub(/^ | $/, "", vs); print vs }' .planning/phases/33-discovery-audit/33-DISCOVERY-AUDIT.md | sort -u
# Expect: owner-populated, fresh-account, N/A (no other values)

# quick.sh green
bash .planning/phases/33-discovery-audit/checks/quick.sh

# full.sh still red ONLY because verdicts pending (Wave 3 fixes)
bash .planning/phases/33-discovery-audit/checks/full.sh; echo "exit=$?"
# Expect: rule 5 fail "expected 4 Verdict lines; found 4 — verdict not in {YES,NO,DEFERRED}: PENDING" — confirms Wave 3 is the remaining gate

# Zero changes outside the phase directory
git status --porcelain | grep -v '^?? \.planning/phases/33-discovery-audit/' | grep -v '^ M \.planning/phases/33-discovery-audit/' | wc -l | tr -d ' '
# Expect: 0
```
</verification>

<success_criteria>
- Zero rows with `viewer_state: TBD`
- Total rows in 130–250 range (RESEARCH.md A1 estimate)
- LockedTabCard rows tagged Live with viewer_state=fresh-account per Pitfall 2
- WR-07 wishlist.ts:206 row carries viewer_state=owner-populated
- Row IDs remain flat sequential after splits
- Zero changes outside `.planning/phases/33-discovery-audit/`
- quick.sh exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/33-discovery-audit/33-03-SUMMARY.md` describing:
- Total rows finalized (with viewer_state breakdown: owner-populated count, fresh-account count, N/A count)
- Number of row-splits added by gate annotation (track G-3/G-4/G-6/G-12/G-19 splits)
- Any gates G-1..G-20 where the audit author judged a different mapping than RESEARCH.md suggested (with rationale)
- Wave 3 hand-off: Plan 04 walks ~25–30 high-stakes rows in browser (owner + fresh-account, desktop + mobile) and authors the 4 decision verdicts
</output>
