# Horlo

## What This Is

A taste-aware watch collection intelligence system for personal collectors. Users manage their collection and wishlist, understand how their watches relate to each other, and make more intentional buying decisions. The core insight engine evaluates any watch against the user's collection and preferences to produce a semantic label (Core Fit, Role Duplicate, Hard Mismatch, etc.) rather than a raw score.

Shipped as a cloud-backed, authenticated web app at [horlo.app](https://horlo.app) — data persists across devices and browsers via Supabase Postgres.

## Core Value

A collector can evaluate any watch against their collection and get a meaningful, preference-aware answer about whether it adds something or just duplicates what they already own.

## Current State

**Shipped:** v5.1 Explore Page Redesign (2026-05-19)

5 phases (43-47), 27 plans, 269 commits over 3 days. 32/32 v5.1 requirements shipped. Built `/explore` as a top-level evergreen, taste-driven discovery surface — a five-module "rabbit hole" distinct from Home's daily check-in. A polish pass migrated the `/search` filter sheet to a swipe-dismissable `FilterDrawer`, shipped device avatar upload into a new `avatars` Storage bucket, and updated the Claude model ID to `claude-sonnet-4-6`; a catalog-enrichment pass ran LLM/vision backfill across all ~100 `watches_catalog` rows with a downgrade guard and human-reviewed factual fields; a new in-app admin CMS (5 RLS-gated tables, owner-gated `/admin/*` routes with `assertOwner()` in every Server Action) lets the owner author curated lists and collection paths; and `/explore` itself shipped as a five-module shell — Hero, Collector Archetypes, Curated Lists Rail, Where Collections Go, Browse the Catalog — with the old Phase 18 Explore surface retired. Two end-of-milestone follow-ups (FU-01 `/search` facet menu, FU-02 `/explore/brands` smooth scroll) were closed as quick tasks before close.

**Known deferred:** DEBT-12 (prod `drizzle.__drizzle_migrations` journal repair) plus the v5.0-carryover list (Phase 39c UAT Issue 2; 31 v3.0 + Phase 35/41 human-verification UAT items; smaller carryover) — see STATE.md `## Deferred Items`.

See `.planning/MILESTONES.md` and `.planning/milestones/v5.1-ROADMAP.md` for full detail. The milestone closed without a formal `/gsd-audit-milestone` (as v5.0 was); the pre-close artifact audit's 23 open items were all cosmetic flags or expected backlog, acknowledged as non-blocking.

## Current Milestone: v5.2 Polish + Taxonomy

**Goal:** Clear two live prod bugs and run two taxonomy/architecture spikes to de-risk the v6.0/v7.0 work ahead.

**Target features:**
- Fix the wishlist watch mislabeled "you own this" on the `/catalog/[catalogId]` route (wishlist ≠ owned; truncated render)
- Fix dark-mode filter-chip legibility on `/search` (black text, unreadable)
- Spike: genre vs. style taxonomy overlap — audit consumers, recommend consolidate / remove / keep
- Spike: two watch-detail views (`/catalog/[catalogId]` vs `/watch/[id]`) — keep separate or merge (decision only)

Scoped from SEED-011. Small, fast milestone. Spikes are decision-only — a consolidation/merge ships in v5.2 only if a spike strongly favors it and it is cheap. Phase numbering continues from 48.

**Trajectory** (reshaped 2026-05-19 from a post-v5.1 bug/feature triage): v5.2 Polish + Taxonomy (SEED-011) → v6.0 Social Interaction (SEED-012 — scoped likes/comments) → v7.0 Watch Photos (SEED-013 — multi-photo carousel + wear-pic surfacing) → v8.0 Add-Watch Redesign (SEED-010 — search-first add flow). **Catalog Expansion (SEED-009) is unscheduled** — the catalog-growth strategy is being rethought. **Market Value (SEED-005)** sits after v8.0 and still needs the SEED-007 pricing spike. Phase numbering continues from 48 (no reset).

**Standing context:**

- **No paywall** — SEED-006 resolved 2026-05-06 → Horlo ships fully free; no Subscription tab, no Stripe, no entitlements scaffolding. See `.planning/research/PREMIUM-MAP.md`.
- **Recommender (SEED-002) is future paid-candidate but built free** — Layer C Reference granularity + Layer D `divestments` schema landed in v5.0 as prep; the recommender itself is a later milestone.
- **Catalog growth is being rethought** — v5.0 shipped the hierarchy + a 100-watch bootstrap; v5.1 enriched those 100 rows. The breadth-expansion plan (old SEED-009) is unscheduled pending a strategy rethink; downstream features assuming catalog depth (e.g. the v8.0 search-first add flow) carry that as an open dependency.
- **Home and Explore stay distinct** — Phase 33b Q1 verdict. Home = daily recency-driven check-in; Explore = evergreen taste-driven rabbit hole. No personalization on Explore.
- **Social stays scoped, not a feed** — v6.0 adds likes/comments but explicitly avoids an Instagram-style attention machine; wishlist comments are mutual-follow gated.


## Requirements

### Validated

- ✓ Watch CRUD — add, edit, delete; status transitions (owned → sold, wishlist → owned) — existing
- ✓ Collection grid — card layout with image, brand/model, key specs — existing
- ✓ Status toggle — owned / wishlist / sold / grail views — existing
- ✓ Filter bar — status, style tags, role tags, dial color filters — existing
- ✓ Tagging system — style, design traits, and role tags per watch — existing
- ✓ User preferences — styles, design traits, complications, dial colors, overlap tolerance — existing
- ✓ Similarity engine — 3-layer scoring (objective similarity → role overlap → preference adjustment) with semantic labels — existing
- ✓ Collection balance analysis — distribution of style, role, dial color across owned watches — existing
- ✓ URL import — 3-stage extraction pipeline (structured data → HTML selectors → LLM fallback) — existing
- ✓ Wear tracking — "mark as worn today" stores lastWornDate on each watch — existing
- ✓ UI polish — spacing, typography, color system, dark/light/system theme toggle — v1.0 (VIS-01/02/03)
- ✓ Better data display — richer watch cards, improved detail view, days-since-worn, balance charts — v1.0 (VIS-04/05/06)
- ✓ Mobile / responsive — usable on phone and tablet — v1.0 (VIS-02)
- ✓ Dark mode — full dark/light mode toggle with system preference detection — v1.0 (VIS-01)
- ✓ SSRF hardening — IP pinning, redirect validation, protocol allowlist — v1.0 (SEC-01)
- ✓ Image security — `next/image` with `remotePatterns` domain allowlist — v1.0 (SEC-02)
- ✓ Fix complicationExceptions — exceptions applied in similarity scoring — v1.0 (FEAT-01)
- ✓ Fix collectionGoal — balanced/specialist/variety influences thresholds and insights — v1.0 (FEAT-02)
- ✓ Wishlist intelligence — deal flags, target price, gap-fill scores, actionable wishlist — v1.0 (FEAT-03/04/05)
- ✓ Wear tracking insights — "haven't worn in X days", Sleeping Beauties panel — v1.0 (VIS-05)
- ✓ UUID watch IDs — `crypto.randomUUID()` replaces Date.now() IDs — v1.0 (FEAT-06)
- ✓ Authentication — Supabase Auth email/password, proxy.ts enforcement, double-verified in DAL + Server Actions — v1.0 (AUTH-01/02/03/04)
- ✓ Cloud persistence — Supabase Postgres via Drizzle ORM, server-only DAL, Server Actions — v1.0 (DATA-01/02/03/04/05)
- ✓ Similarity engine props — receives collection + preferences as props, not Zustand reads — v1.0 (DATA-05)
- ✓ Prod deploy runbook — verified `docs/deploy-db-setup.md` with 6 footgun fixes — v1.0 (OPS-01)
- ✓ Phase completion checklist — `docs/phase-completion-checklist.md` covers drizzle↔supabase parity, prod DB push, prod code push — v4.0
- ✓ Test foundation — Vitest + React Testing Library + MSW configured — v1.0 (TEST-01)
- ✓ Similarity tests — all six SimilarityLabel outputs covered including preference-aware paths — v1.0 (TEST-02)
- ✓ Extractor pipeline tests — structured, HTML, merge logic with fixture HTML — v1.0 (TEST-03)
- ✓ Follow / unfollow (social graph) — v2.0 Phase 9 (FOLL-01, FOLL-02)
- ✓ Follower counts — v2.0 Phase 9 (FOLL-03)
- ✓ Follower / following list routes — v2.0 Phase 9 (FOLL-04)
- ✓ Read-only public collection at `/u/[username]` with per-tab privacy gating — v2.0 Phase 9 (PROF-08)
- ✓ Common Ground taste overlap (server-computed; result-only to client) — v2.0 Phase 9 (PROF-09)
- ✓ 5-section Network Home — WYWT rail + Network Activity feed + Collectors Like You + Personal Insights + Suggested Collectors — v2.0 Phase 10 (FEED-01..05, WYWT-03, DISC-02, DISC-04)
- ✓ Nav `+ Wear` button wired to shared WatchPickerDialog — v2.0 Phase 10 (WYWT-03)
- ✓ Cache Components enabled (Next.js 16 `cacheComponents: true`) with canonical Suspense+inline-theme-script layout pattern — v2.0 Phase 10
- ✓ Schema + storage foundation — `wear_visibility` enum (public/followers/private), `wear_events.photo_url`+`note`+`visibility` columns with `worn_public` backfill, `notifications` table + `notification_type` enum with recipient-only RLS + partial UNIQUE dedup, `pg_trgm` + GIN trigram indexes on `profiles.username`/`bio`, `wear-photos` private Supabase Storage bucket with three-tier SELECT RLS + folder-enforcement INSERT/UPDATE/DELETE, SECURITY DEFINER helpers with revoked PUBLIC/anon EXECUTE, and DEBT-02 RLS audit on users/watches/user_preferences — v3.0 Phase 11 (WYWT-09, WYWT-11, WYWT-13, WYWT-14, NOTIF-01, SRCH-08, DEBT-02)
- ✓ Visibility ripple in DAL — `getWearEventsForViewer` (renamed from `getPublicWearEventsForViewer`) with three-tier predicate (owner bypass + per-row follow check + public/followers visibility OR), `getWearRailForViewer` leftJoin(follows) with per-row visibility, feed hot path uses `metadata->>'visibility' IN ('public','followers')` gate, wishlist `addToWishlistFromWearEvent` gates on `wear_events.visibility` + follows with Letterboxd-style uniform "not found" error, Worn History settings toggle retired, `worn_public` column dropped from schema + local + prod DB — v3.0 Phase 12 (WYWT-10, WYWT-11)
- ✓ Phase 5 code-review follow-ups — `PreferencesClient` surfaces save failures via `role="alert"` inline banner with `aria-live="polite"` "Saving…" indicator (MR-01); dead `UnauthorizedError` imports removed from `src/app/actions/watches.ts` and `src/app/actions/preferences.ts` (MR-02); MR-03 closed paperwork-only with in-tree note `999.1-MR-03-CLOSURE.md` citing Phase 6 RLS migration + Phase 11 DEBT-02 audit migration as evidence (no new SQL) — v3.0 Phase 999.1 (MR-01, MR-02, MR-03)
- ✓ Notifications foundation — fire-and-forget `logNotification` logger with D-18 opt-out + D-24 self-guard + D-27 internal try/catch; 6-function notifications DAL with explicit-viewerId two-layer defense (D-25); `markAllNotificationsRead` + `markNotificationRead` + `markNotificationsSeen` Server Actions using Next 16 `updateTag()` (not `revalidateTag`) for read-your-own-writes correctness; pure-UI NotificationRow (Client Component with `useOptimistic` + `useTransition` + `useRouter` for D-08 per-row optimistic read flip), NotificationsInbox (Today/Yesterday/Earlier grouping + NOTIF-08 watch_overlap display-time collapse), NotificationsEmptyState; cached NotificationBell Server Component (`'use cache'` + `cacheTag('notifications', 'viewer:${viewerId}')` + `cacheLife({revalidate:30})`); `/notifications` page with client-mount `MarkNotificationsSeenOnMount` (avoids render-time revalidation per Next 16 Cache Components); Settings Notifications section with `notifyOnFollow` + `notifyOnWatchOverlap` opt-out toggles; `followUser` + `addWatch` Server Actions await `logNotification` and invalidate the recipient's `viewer:` tag on write; dedup end-to-end contract verified via partial UNIQUE index integration test — v3.0 Phase 13 (NOTIF-02..10)
- ✓ Production nav shell — Mobile `BottomNav` (5-item sticky with elevated Wear cradle), `SlimTopNav` (<768px) / `DesktopTopNav` (≥768px) split, `MobileNav` hamburger deleted, `/explore` + `/search` stubs close nav-link 404s, `/insights` retired to owner-only profile tab with two-layer P-08 privacy defense, `UserMenu` consolidates Profile/Settings/Theme/Sign out via `InlineThemeSegmented`, shared `PUBLIC_PATHS` constant + `isPublicPath` predicate unifies proxy + nav auth gate, IBM Plex Sans + `viewport-fit=cover` metadata, DEBT-01 regression-locked — v3.0 Phase 14 (NAV-01..12, DEBT-01)
- ✓ WYWT photo post flow — Two-step `WywtPostDialog` (reuses `WatchPickerDialog` for step 1, `ComposeStep` for photo+note+visibility); `CameraCaptureView` with `WristOverlaySvg` static guide overlay; `PhotoUploader` with HEIC→JPEG via `heic2any` lazy-loaded in Web Worker; canvas-reencoded JPEG ≤1080px with EXIF strip; client-direct upload to `wear-photos` bucket; `logWearWithPhoto` Server Action with orphan-cleanup on 23505 + non-23505; `/wear/[wearEventId]` route with three-tier gate + uniform 404 + per-request signed URL; Sonner `<ThemedToaster />` bound to custom `ThemeProvider` outside Suspense — v3.0 Phase 15 (WYWT-01..08, WYWT-12, WYWT-15..19)
- ✓ /explore Discovery Surface — Server-Component shell with hero gate (`followingCount < 3 && wearEventsCount < 1` AND predicate, Promise.all outside cache per D-06), 3 cached rails (Popular Collectors per-viewer `cacheLife({revalidate:300})`, Trending Watches global 5min, Gaining Traction global 24h with D-12 three-window logic 0/1-6/7); 4 server-only DAL readers in `src/data/discovery.ts` + `src/data/wearEvents.ts` with two-layer privacy on Popular Collectors (innerJoin profile_settings + profile_public=true + RLS), `::int` casts on every COUNT (Pitfall 2), `::date` casts on snapshot_date arithmetic (Pitfall 3), `notInArray` length-guarded (Pitfall 6); See-all routes at `/explore/collectors` (50 rows + cap footer + empty state) and `/explore/watches` (two stacked sections Trending+Gaining at limit:50 each, responsive grid 2/3/4 cols); Server Action invalidation matrix — RYO via `updateTag('explore:popular-collectors:viewer:${user.id}')` on followUser/unfollowUser, SWR via `revalidateTag('explore', 'max')` on addWatch/editWatch/removeWatch (planner over-delivered patching all three watches mutations, not just addWatch); BottomNav v4.0 reshape to 5-slot Home/Search/Wear/Explore/Profile (Plus/Add slot dropped, Profile permanent, Notifications stays in TopNav bell — supersedes original DISC-08 + NAV-14 wording); 87 component tests + 43 Server Action tests + 7 integration tests green; 0 critical / 4 warning / 6 info from code review — v4.0 Phase 18 (DISC-03, DISC-04, DISC-05, DISC-06, DISC-07, DISC-08)
- ✓ People search — `/search` 4-tab page (All / Watches / People / Collections) with People populated and the other three tabs showing "coming soon" cards (no query firing); `searchProfiles` DAL using `pg_trgm` ILIKE on `username` + bio search gated at `q.length>=3` with two-layer privacy (`profile_public = true` + viewer self-exclusion); pre-LIMIT 50 in SQL then JS sort `(overlap DESC, username ASC)` with batched `inArray(follows.followingId, topIds)` for `isFollowing` (Pitfall C-4 anti-N+1); `searchPeopleAction` Server Action with Zod `.strict().max(200)` schema (mass-assignment guard) + auth gate; `useSearchState` hook (250 ms debounce + AbortController + URL sync + 2-char client minimum) wired to a 4-tab `SearchPageClient`; XSS-safe `HighlightedText` (regex-escape + React text children, no `dangerouslySetInnerHTML`); `PeopleSearchRow` with avatar + highlighted username/bio + overlap pill + inline `FollowButton`; persistent `DesktopTopNav` search input restyled (D-24 muted-fill + leading lucide Search icon) and `HeaderNav` deleted (D-23) — Profile/Settings now reachable only via the UserMenu dropdown shipped in Phase 14; Pitfall C-1 (pg_trgm GIN index reachability) verified via forced-plan EXPLAIN ANALYZE evidence in `16-VERIFICATION.md`; UAT approved 2026-04-25 — v3.0 Phase 16 (SRCH-01..07)
- ✓ Custom SMTP via Resend on `mail.horlo.app` — DKIM + SPF + DMARC (`p=none`) published at Cloudflare via Resend × Cloudflare auto-configure (Plan 21-01) + manual DMARC; Supabase Auth SMTP wired to `smtp.resend.com:465` with `Horlo <noreply@mail.horlo.app>` sender; D-07 round-trip gate passed (Gmail Inbox + From verified + reset link round-trips through prod); Confirm email + Secure email change + Secure password change toggles flipped ON in prod after gate; signup-form (`src/app/signup/signup-form.tsx`) branches on `data.session === null` for the no-session path with neutral T-21-04 user-enumeration-safe copy (D-10); Supabase email templates (Confirm signup, Reset Password, Change Email) updated to route through `/auth/callback?token_hash={{ .TokenHash }}&type=...&next=...` — the canonical Next.js + `@supabase/ssr` PKCE pattern that horlo's existing `/auth/callback` route handler was designed for; backout-plan section appended to `docs/deploy-db-setup.md` with **Footgun T-21-PREVIEWMAIL** (preview deploys share prod sender reputation) and **Footgun T-21-WWWALLOWLIST** (Vercel apex→www canonicalization requires BOTH allowlist entries) — v4.0 Phase 21 (SMTP-01..SMTP-05; SMTP-06 deferred pending staging Supabase project per CONTEXT D-01)
- ✓ Catalog Foundation — canonical `watches_catalog` table laid silently underneath per-user `watches` with public-read RLS + service-role-only writes, surrogate UUID PK + NULLS-NOT-DISTINCT natural-key UNIQUE on normalized `(brand, model, reference)` trio, `pg_trgm` GIN indexes on brand+model, nullable `watches.catalog_id` FK with `ON DELETE SET NULL`, idempotent batched backfill (`npm run db:backfill-catalog`), two upsert helpers (`upsertCatalogFromUserInput` ON CONFLICT DO NOTHING + `upsertCatalogFromExtractedUrl` ON CONFLICT DO UPDATE COALESCE), fire-and-forget wiring into `addWatch` + `/api/extract-watch`, daily SECDEF pg_cron count refresh at 03:00 UTC + manual `npm run db:refresh-counts`, `watches_catalog_daily_snapshots` for Gaining Traction. `analyzeSimilarity()` byte-locked (silent infrastructure) — v4.0 Phase 17 (CAT-01..12)
- ✓ Catalog Taste Enrichment — 8 LLM-derived taste columns on `watches_catalog` (formality, sportiness, heritage_score, primary_archetype, era_signal, design_motifs, confidence, extracted_from_photo) via Anthropic claude-sonnet-4-6 strict tool-use (`tool_choice: { type: 'tool', name: 'record_taste_attributes' }`); `catalog-source-photos` Supabase bucket with three-tier RLS; `src/lib/taste/` service module (vocab + types + prompt + enricher); fire-and-forget enrichment on both manual entry + URL extract; `updateCatalogTaste` first-write-wins DAL helper (D-13); WatchForm strips Style/Role/Design pickers and adds optional Reference Photo Card on `mode='create'`; `scripts/backfill-taste.ts` + `scripts/reenrich-taste.ts` operator tooling. `extractWithLlm()` body byte-identical (D-07 lock) — v4.0 Phase 19.1
- ✓ /explore Discovery Surface — Server Component shell with sparse-network welcome hero (gated `followingCount < 3 && wearEventsCount < 1`); 3 cached rails (Popular Collectors per-viewer 5min, Trending Watches global 5min, Gaining Traction global 24h with D-12 three-window logic 0/1-6/7); See-all routes at `/explore/collectors` (50-row cap) and `/explore/watches` (stacked Trending+Gaining at limit:50 each, responsive grid 2/3/4 cols); Server Action invalidation matrix — `updateTag` for RYO on followUser/unfollowUser, `revalidateTag('explore', 'max')` for SWR fan-out on addWatch/editWatch/removeWatch; BottomNav v4.0 reshape to 5 slots Home/Search/Wear/Explore/Profile (Notifications stays in TopNav bell, Profile permanent — supersedes original DISC-08/NAV-14 wording) — v4.0 Phase 18 + 25 (DISC-03..08, NAV-13..15)
- ✓ /search Watches + Collections — Watches tab populated with catalog matches via `searchCatalogWatches` DAL (anti-N+1 single `inArray` viewer-state batch keyed by viewerId, SRCH-10); Collections tab via `searchCollections` DAL with two-layer-privacy AND-locked (BOTH `profile_public` AND `collection_public` + viewer self-exclusion, SRCH-12); All-tab union capped at 5 each via three independent sub-effects each owning its own AbortController (SRCH-13/14 — safe rapid-tab-switch, no result leak); XSS-safe `<HighlightedText>` reused across all surfaces — v4.0 Phase 19 (SRCH-09..15)
- ✓ Collection Fit + Verdict Copy — Pure-renderer `<CollectionFitCard>` (no engine imports — static guard `tests/static/CollectionFitCard.no-engine.test.ts`) replaces deleted `<SimilarityBadge>` across `/watch/[id]`, `/search` row inline-expand accordion, and new `/catalog/[catalogId]` route; 12-template composer (4 roadmap-mandated + 8 supporting) with confidence gate at 0.5/0.7 thresholds reads Phase 19.1 taste attributes via `viewerTasteProfile` Drizzle aggregate (confidence ≥ 0.5 SQL filter, INNER JOIN); `getVerdictForCatalogWatch` Server Action with `useWatchSearchVerdictCache` collectionRevision-keyed cache; `/evaluate` route eliminated — `tests/no-evaluate-route.test.ts` static guard prevents reintroduction; `analyzeSimilarity` byte-locked (D-09) — v4.0 Phase 20 (FIT-01..04)
- ✓ Add-Watch Flow Rethink + Verdict-as-Step — URL paste → verdict preview → 3-button status decision (wishlist / owned / skip) as one coherent gesture; `<AddWatchFlow>` 9-state machine orchestrating `<PasteSection>` / `<VerdictStep>` / `<WishlistRationalePanel>` / `<RecentlyEvaluatedRail>` / `<WatchForm lockedStatus>`; "Skip" path covers lightweight evaluate-only use case; manual entry preserved as secondary affordance via inline link; extraction failure preserves partial data with `<ExtractErrorCard>` + Continue-manually CTA; `/search?tab=watches` inline 3-CTA accordion (Wishlist/Collection/Hide) and `/catalog/[catalogId]` cross-user 3-CTA Client Component both deep-link to `/watch/new?catalogId=X&intent=owned` short-circuiting to form-prefill; gap-closure plans 06/07/08 closed all 5 UAT debug entries inline — v4.0 Phase 20.1 (ADD-01..07)
- ✓ Settings Restructure + Account Section — `@base-ui/react` vertical-tabs shell at `/settings` with 6 sections in canonical SaaS order (Account / Profile / Preferences / Privacy / Notifications / Appearance), hash-driven via `window.history.pushState` (NOT `router.push`); email change with pending banner (T-22-S4 — UI never displays new email as current pre-confirmation); password change with `isSessionStale(lastSignInAt)` 24h re-auth dialog chaining `signInWithPassword` → `updateUser` (D-08 Option C with 401 catch as defense-in-depth); `/auth/confirm/route.ts` extended to switch on `type` (5 EmailOtpType values) with redirect map (post-`email_change` → `/settings#account?status=email_changed`); `/preferences` server-side redirects to `/settings#preferences` — v4.0 Phase 22 (SET-01..06)
- ✓ Settings Sections + Schema-Field UI — `<CollectionGoalCard>` (Brand Loyalist option added) + `<OverlapToleranceCard>` lifted to top of Preferences tab as dedicated Cards; `<PreferencesClient>` accepts `embedded` prop suppressing page chrome; `<AppearanceSection>` mounts `<InlineThemeSegmented>` (Server-renders-Client child per Next.js 16 — UserMenu retains its segmented control, both surfaces sync via `horlo-theme` cookie); `<WatchForm>` exposes `isChronometer` Checkbox + `notesPublic` Public/Private pill below Notes textarea (matches `<NoteVisibilityPill>` chip shape); `<WatchDetail>` renders only-if-true Certification row with text-foreground Check icon (Anti-Pattern 9 honored); `addWatch`/`editWatch` Zod accepts `notesPublic` and revalidates `'/u/[username]', 'layout'` for cross-page sync (D-19) — v4.0 Phase 23 (SET-07..12, FEAT-07..08)
- ✓ Notification Stub Cleanup + Test Fixture & Carryover — Pre-flight zero-row whitelist assertion script + `notification_type` enum rename+recreate (with T-24-PARTIDX partial-index surgery for enum-bound dependents) eliminated `price_drop` + `trending_collector` dead code across src/, tests/, scripts/, seed/; 4 `wornPublic` test fixture files rewritten to `wear_visibility` enum per D-04; three v1.0-carryover test suites finally landed: `watchStore` filter reducer (TEST-04 with Zustand v5 replace-mode reset), `/api/extract-watch` route integration past auth gate (TEST-05 — 9 tests covering happy path + URL validation + SsrfError + generic error + catalog-null), `WatchForm`/`FilterBar`/`WatchCard` component tests (TEST-06 — PointerEvent polyfill lifted to setup.ts) — v4.0 Phase 24 (DEBT-03..06, TEST-04..06)
- ✓ Profile Nav Prominence + Empty States + Form Polish — DesktopTopNav + SlimTopNav avatar dual-affordance (avatar Link → `/u/{username}/collection`, chevron → UserMenu dropdown) with avatarUrl threaded from existing `getProfileById`; SlimTopNav drops Settings cog (consolidated into UserMenu); 4 empty-state CTAs across collection/wishlist/worn/notes (Collection has API-key-aware "Add manually" fallback when ANTHROPIC_API_KEY unset); 5-category URL-extract error taxonomy with locked D-15 copy + D-14 lucide icons (`host-403`, `structured-data-missing`, `LLM-timeout`, `quota-exceeded`, `generic-network`); shared `useFormFeedback` hook + `<FormStatusBanner>` primitive standardize hybrid Sonner toast + `aria-live="polite"` banner across 7 forms (PreferencesClient, OverlapToleranceCard, CollectionGoalCard, WatchForm add+edit, ProfileEditForm, EmailChangeForm, PasswordReauthDialog); `<MarkAllReadButton>` uses `useFormStatus` for pending state; profile edit fires success toast on save (UX-08); UAT approved on prod commit 7132ac0 — v4.0 Phase 25 (NAV-13..15, UX-01..08)
- ✓ WYWT Auto-Nav — After successful WYWT post submission, dialog closes and user is auto-navigated to `/wear/{wearEventId}` (`router.push` fires after BOTH upload + `logWearWithPhoto` resolve, inside `useTransition` callback); `/wear/[wearEventId]` page wraps photo render in `<Suspense fallback={<PhotoSkeleton />}>` covering the 200–800ms storage-CDN propagation window; `<WearPhotoClient>` retries onError up to 3× at ~300ms with cache-buster query string + setTimeout cleanup via `useRef`; PhotoSkeleton mobile rounding gap closed inline (commit 4212a08 added `rounded-none` before `md:rounded-lg` so `tailwind-merge` resolves the shadcn base `rounded-md` correctly) — v4.0 Phase 26 (WYWT-20..21)
- ✓ Wishlist reorder + 2-col mobile grid + price-aware cards — `watches.sort_order` + composite index, `bulkReorderWishlist` DAL with three-layer owner enforcement (Zod `.strict` + session-userId + DAL WHERE+count+set-completeness), `reorderWishlist` Server Action with `revalidatePath('/u/[username]/[tab]', 'page')` dynamic-segment match, @dnd-kit DnD wiring (mouse 150ms / touch 250ms / keyboard sensors, symmetric drop-indicator), status-driven price line on ProfileWatchCard, `grid-cols-2` mobile grid on both Collection + Wishlist (owner + non-owner branches) — v4.1 Phase 27 (WISH-01, VIS-07, VIS-08)
- ✓ Add-Watch flow returns user to entry point + success toast with profile-tab CTA + verdict copy coherent across surfaces — Sonner action-slot toast across 4 commit sites with literal "View" → `/u/{viewerUsername}/{matching-tab}` (D-05/D-06 suppress when destination matches via `canonicalize`); `?returnTo=` validated round-trip across 8 entry-points (server-side validator at `/watch/new` chokepoint reuses auth-callback regex byte-identically + self-loop guard); 25 literal copy strings rewritten verb-led; speech-act-split verdict bundle (verb-led `rationalePhrasings` 1st-person for wishlist auto-fill, `contextualPhrasings` for verdict display); `WishlistRationalePanel` reads `verdict.rationalePhrasings[0]`; D-15 `router.refresh()` removed from AddWatchFlow Wishlist commit, retained at /search and /catalog inline-commit sites — v4.1 Phase 28 (UX-09, FIT-06, ADD-08)
- ✓ Nav & Profile chrome cleanup — UserMenu Profile DropdownMenuItem removed (avatar Link is sole profile entry per Phase 25 dual-affordance, both surrounding separators preserved per UI-SPEC D-01 wording precision); ProfileTabs horizontal-only scroll (`overflow-y-hidden pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden`; `tabs.tsx` primitive UNCHANGED); Add-Watch flow three-layer reset (Layer 1: per-request `crypto.randomUUID()` nonce on `<AddWatchFlow key={flowKey}>`; Layer 2: `useLayoutEffect` cleanup-on-hide with StrictMode-safe ref-guarded skip cases for initial idle + form-prefill; Layer 3: explicit reset BEFORE `router.push(dest)` in handleWishlistConfirm); module-scope migration of `useWatchSearchVerdictCache` + new `useUrlExtractCache` (both keyed on URL → `{catalogId, extracted, catalogIdError}`) so verdict + extract bundles survive remount; global vitest `<StrictMode>` wrapper added so this regression class is caught in CI; UAT 10/10 across three rounds — v4.1 Phase 29 (NAV-16, PROF-10, FORM-04)
- ✓ WYWT capture WYSIWYG alignment — `CameraCaptureView.tsx` wrapperRef + aspect-square wrapper + `computeObjectCoverSourceRect` named export (4/4 pure-math fixtures GREEN at delta=(0,0)) + extended readiness guard + 9-arg `ctx.drawImage` capture math; post-ship hotfix `2dd7377` added `h-full` to `<video>` so object-cover engages on iPhone (initial ship had black bar in preview + wrist at bottom of saved JPEG because `<video>` kept intrinsic 16:9 aspect inside aspect-square wrapper); `WristOverlaySvg.tsx` + `ComposeStep.tsx` + `src/lib/exif/strip.ts` UNTOUCHED; iOS Safari UAT confirmed 2026-05-05 — v4.1 Phase 30 (WYWT-22)
- ✓ v4.0 verification asymmetry resolved — Phase 23 + Phase 24 each have phase-level VERIFICATION.md backfilled to `.planning/milestones/v4.0-phases/`; v4.0-MILESTONE-AUDIT.md `## Closure` section appended with byte-equality invariant intact (`git diff ... | grep -E '^-[^-]' | wc -l` returns 0; 17 insertions, 0 deletions); bidirectional `closes_audit_items:` cross-references between audit and backfilled VERIFICATION.md files — v4.1 Phase 31 (DEBT-07, DEBT-08)
- ✓ DEBT-09 `notesPublic` regression fixed — `addWatch`/`editWatch` Zod schemas accept `notesPublic`, persist it on every write, and call `revalidatePath('/u/[username]', 'layout')`; the 4 RED scaffold tests flipped GREEN — v5.0 Phase 32 (DEBT-09)
- ✓ Discovery audit — falsifiable 136-row click-path audit across 13 surfaces (Phase 33) + 42-cell Rdio drift-vector matrix with 4 product verdicts (combine home+explore=NO, lineage browse=DEFERRED, dead-end closure=YES, CAT-13 framing=discovery) gating all downstream polish (Phase 33b) — v5.0 (DISC-10, DISC-12)
- ✓ Catalog 5-level hierarchy (Layers A–D, schema + DAL) — `brands` + `watch_families` with public-read RLS (A); `watch_lineage_edges` junction with BEFORE-INSERT cycle trigger + recursive-CTE `CYCLE` guards, structured `movement_type` enum, era/material columns (B); `watch_variants` + clean-slate catalog wipe-and-relink + `watches.catalog_id` NOT NULL flip (C); 7 provenance columns + `divestments` table (D) — v5.0 Phases 34-37 (CAT-15, CAT-16, CAT-17, CAT-14, CAT-18)
- ✓ CAT-13 engine rewire — `analyzeSimilarity()` reads catalog taste columns as an additive 9th scoring dimension gated on `confidence >= 0.5`; 19/19 static guards green — v5.0 Phase 38 (CAT-13)
- ✓ Audit-driven discovery polish — closed the entire Phase 33b Q3 high-leverage dead-end backlog (NSV-01/02/06/08/12/14/15/16/18/20): mostSimilar Link wraps, common-ground walk-back fallback, fresh-account ReferenceIdentityCard, 8-row Collector Profile sub-cluster, catalog other-owners roster with two-layer privacy, inline Same-family + Lineage rails; backed by a 100-watch / 32-family / 52-edge prod catalog bootstrap — v5.0 Phases 39 + 39b (DISC-11)
- ✓ Profile layout Next 16 conformance — `/u/[username]` refactored to Cache Components + Suspense (ProfileGate Server Component + cached ProfileShellResolver replacing 8 uncached top-level fetches), achieving Partial Prerender and fixing the Router-Cache-poisoning 404 surfaced during 39b UAT — v5.0 Phase 39c
- ✓ Search & verdict polish — three faceted filters (movement / case-size band / style) on the `/search` Watches tab with a mobile bottom-sheet; pairwise "Compare with what you own" drill-down in CollectionFitCard — v5.0 Phase 40 (SRCH-16, FIT-05)
- ✓ Account Danger Zone + branded auth emails — Wipe Collection / Delete Account with type-to-confirm + password re-auth two-step modals; three branded react-email Supabase Auth templates installed in the dashboard — v5.0 Phase 41 (SET-13, SET-14)
- ✓ Test & validation hardening — Nyquist hardening sweep across v3.0+ phases + ~33 deferred human-UAT items triaged — v5.0 Phase 42 (DEBT-10, DEBT-11)
- ✓ Polish pass — `/search` filter sheet migrated to a swipe/backdrop-dismissable `FilterDrawer`; wishlist cards drop wear UI; equal-height watch cards + add-CTA above the grid; device avatar upload (circular crop) into a new `avatars` Supabase Storage bucket; deprecated Claude model ID updated to `claude-sonnet-4-6` — v5.1 Phase 43 (PLSH-01..07)
- ✓ Catalog enrichment — enrichment script hardened with two-turn `web_search` + rate-limit retry/backoff + per-`catalog_id` logging; confidence-threshold + photo-existence downgrade guard in `updateCatalogTaste`; human-reviewed factual propose/apply scripts; full ~100-row production enrichment run with verified archetype coverage — v5.1 Phase 44 (ENRH-01..06)
- ✓ In-app admin CMS — 5-table migration (`curated_lists`, `curated_list_items`, `collection_paths`, `collection_path_nodes`, `cms_settings`) with two-layer-defended RLS; owner-gated `/admin/lists` + `/admin/paths` routes with `assertOwner()` in every Server Action; full list/path CRUD with markdown editor, cover uploader, search-as-you-type watch picker; hero pin; FK-RESTRICT delete guard; 6 seed collection paths authored — v5.1 Phase 45 (CMS-01..10)
- ✓ Explore shell + Browse + Archetypes — new `/explore` five-module responsive shell (self-hiding empty modules); Browse the Catalog with brand/era/genre indices + A–Z brand nav; Collector Archetypes chip rail deep-linking into archetype-prefiltered `/search`; `/search` Watches tab extended with brand/era/genre/archetype facets; old Phase 18 Explore surface retired — v5.1 Phase 46 (EXPL-01..05)
- ✓ Editorial half of /explore — Curated Lists Rail + `/explore/lists` see-all + per-list detail pages; quality-gated Hero with manual pin override + weekly rotation + immediate `revalidateTag('explore:hero')` propagation; Where Collections Go module + `PathCard` (numbered vertical stack at 360px) + `/explore/paths` — v5.1 Phase 47 (EXPL-06..09)

### Active

- [ ] **DEBT-12** — repair prod's `drizzle.__drizzle_migrations` journal: it holds only 1 row (`idx=0`) while local `_journal.json` has many. All migrations since shipped via `supabase db push --linked` only; their journal rows were never recorded on prod. Write a one-shot `scripts/repair-drizzle-journal.ts` that SHA256s each `drizzle/00NN.sql` and INSERTs `(hash, created_at)` rows idempotently (`ON CONFLICT DO NOTHING`); verify post-repair `npx drizzle-kit migrate` is a clean no-op. Never mapped to a v5.0 phase — opportunistic; fold into the next prod-deploy phase that needs `drizzle-kit migrate` to run normally.
- [ ] WristOverlaySvg geometry redesign — user owns; canonical 10:10 + arm spacing fix (v3.0 deferred UAT, NOT v4.1 WYWT-22 which only fixed alignment math)
- [ ] 31 v3.0 deferred human-verification UAT items (iOS device tests, multi-session flows, FOUC checks, prod browser smoke tests) — see `.planning/milestones/v3.0-MILESTONE-AUDIT.md`
- [ ] Phase 35 + Phase 41 human-verification items — operator-approved at v5.0 close (not deferred): Phase 35 lineage cycle-trigger smoke + G6 backfill counts are prod-DB follow-ups once the catalog grows; Phase 41 cross-client email rendering + Supabase dashboard install + DKIM/SMTP checks. See `35-HUMAN-UAT.md` / `41-HUMAN-UAT.md`.
- [ ] Pre-existing `LayoutProps` TS error in `src/app/u/[username]/layout.tsx:21` — carried from v3.0 (note: layout heavily refactored in Phase 39c — re-confirm this error still exists)
- [ ] REQUIREMENTS.md DISC-08 / NAV-14 wording drift — historical: REQUIREMENTS.md said "Notifications" slot but Phase 18 D-03/D-04 amended to "Profile"; archived as carried-forward note in v4.0 archive
- [ ] Phase 999.1 directory still in `.planning/phases/` (v3.0 archival miss) — should move to `milestones/v3.0-phases/`. Run `/gsd-cleanup` to archive v5.0 phase directories at the same time.
- [ ] WatchForm.tsx CardDescription / photoError unused imports — flagged in plan summaries; not introduced by v4.0
- [ ] Pre-existing test-suite baseline (~48-51 failures): `tests/no-raw-palette.test.ts`, `tests/app/explore.test.tsx`, preferences DEBT-01, WYWT post dialog, DB-required integration tests gated-skip without env — pre-existing on `main`; v5.0 plans tracked net regression delta against this baseline and introduced zero net new failures
- [ ] SMTP-06 staging-prod sender split — pending staging Supabase project per Phase 21 CONTEXT D-01
- [ ] DEBT (Phase 17 local DB reset hygiene): Phase 17 Supabase migrations missing from local DB resets — `watches_catalog_natural_key` UNIQUE constraint, `refresh_watches_catalog_counts()` SECDEF function, and pg_cron schedule are not applied by `drizzle-kit push`. Per memory `project_local_db_reset.md` — extend the post-`supabase db reset` script with `docker exec psql` apply for these migrations.
- [ ] `useWatchSearchVerdictCache` (module-scoped Map post v4.1 29-05) not cleared on signOut — theoretical cross-user verdict leak when collectionRevision values coincidentally match. Pre-existing post-29-05 risk; v5.x polish.
- [ ] Cancel mid-flow does not honor `?returnTo=` — by Phase 28 spec (only mandated nav-on-commit); browser-back relies on Phase 29 Layer 2 useLayoutEffect cleanup. Documented design choice; revisit if user feedback warrants.
- [ ] Phase 39c UAT Issue 2 — `removeWatch` Server Action leaves stale state in the home "from collectors like you" rail AND `/watch/[id]` user-status projection still shows "owned" after server-side reload; Plan 39c-05's `revalidateTag('profile:${ownerUsername}', 'max')` doesn't reach the explore/discovery rail. Tracked in `39c-UAT.md` Issue 2 — small fix phase or debug session when ready.

### Out of Scope

- Social / community features — partial: v2.0 Phase 7-10 shipped public profiles, follow graph, Common Ground overlap, and the 5-section Network Home (activity feed, WYWT, recommendations, insights, suggested collectors)
- Automated price tracking / market integrations / deal alerts — future milestone, significant infrastructure
- AI recommendation engine (best gap filler, most versatile addition) — future milestone after auth/data layer is solid
- Collection visualization map (2D dressy↔sporty × affordable↔expensive plot) — future milestone
- Sharing / export to others — future milestone once multi-user is established

## Context

**Current state (post v5.1):** Next.js 16 App Router with `cacheComponents: true`, React 19, TypeScript 5 strict, Supabase Auth + Postgres with RLS enabled project-wide (DEBT-02 audited), Drizzle ORM, Tailwind CSS 4 with IBM Plex Sans, Shadcn/base-ui + Sonner, `react-markdown` for CMS copy rendering. ~46,900 LOC TypeScript in `src/`. The catalog is a 5-level Brand/Family/Reference/Variant/Individual hierarchy (`brands`, `watch_families`, `watch_lineage_edges`, `watch_variants` + provenance columns and the `divestments` table), now LLM/vision-enriched across all ~100 rows; `analyzeSimilarity()` reads catalog taste as a 9th scoring dimension. A new in-app admin CMS (5 RLS-gated tables, owner-gated `/admin/*` routes) backs the editorial content on `/explore`, which shipped in v5.1 as a five-module discovery surface (Hero, Collector Archetypes, Curated Lists Rail, Where Collections Go, Browse the Catalog). Test suite carries a known ~48-51-failure pre-existing baseline tracked in Active. Deployed at horlo.app on Vercel; production navigation frame, three-tier wear privacy, notifications, photo-first WYWT flow, people search, the audit-driven discovery surfaces, and the new `/explore` are all live.

**Architecture:** Server Components by default, Zustand demoted to filter-only ephemeral state (31 lines), server-only DAL for all data access, Server Actions for all mutations, proxy.ts enforces auth at the edge, double-verified in every DAL function and Server Action. Two-layer privacy (RLS at DB + DAL WHERE clauses) protects follower/followee reads — extended in v5.0 to a load-bearing second layer (`collectionPublic`) on cross-user catalog reads. Root layout uses inline theme script + Suspense around dynamic children; `/u/[username]` was refactored in Phase 39c to a thin Suspense shell over a cached `ProfileShellResolver` + viewer-dependent `ProfileGate`, yielding Partial Prerender. `watches_catalog` is the canonical catalog with public-read RLS + service-role writes.

**Production:** Supabase project `wdntzsckjaoqodsyscns`, Vercel deployment at `horlo.app`/`www.horlo.app`. Email confirmation ON (via Resend SMTP at `mail.horlo.app`, flipped 2026-04-30 in Phase 21). Drizzle migrations tracked; production RLS policies (including `activities_select_own_or_followed`) pushed via `supabase db push --linked --include-all`. Deploy runbook: `docs/deploy-db-setup.md`.

## Constraints

- **Tech stack**: Next.js 16 App Router — continue with existing framework, no rewrites
- **Data model**: Watch and UserPreferences types are established — extend, don't break existing structure
- **Personal first**: Single-user experience and data isolation must remain correct even after multi-user auth is added
- **Performance**: Target <500 watches per user; no need for complex pagination or infinite scroll in MVP

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Local-first with localStorage | Zero backend complexity for MVP, fast iteration | ✓ Completed migration — localStorage replaced by Supabase Postgres in v1.0 |
| URL import with LLM extraction | Product vision said "no external APIs" but the feature was built and adds real value | ✓ Good — SSRF hardened in Phase 1, auth-gated in Phase 4 |
| Semantic similarity labels over raw scores | Scores are confusing; collector language (Core Fit, Role Duplicate) is more actionable | ✓ Good |
| Zustand for state | Simple, no boilerplate, works well with localStorage persist | ✓ Resolved — demoted to filter-only (31 lines) in Phase 5; DB is source of truth |
| App Router only, no pages/ | Clean separation, server components where possible | ✓ Good |
| Supabase Auth + Postgres + Drizzle | Integrated auth+DB, free tier sufficient for personal use, Drizzle for type-safe queries | ✓ Good — deployed and verified in prod |
| DAL + Server Actions (not API routes) | Colocation of data access with auth verification, better type safety | ✓ Good — single POST route remains for extract-watch only |
| proxy.ts + double-verified auth | Defense in depth: proxy.ts for redirects, DAL/SA for data access | ✓ Good — tested via Phase 4 UAT |
| Email confirmation OFF | Personal-MVP posture; free-tier SMTP limited to 2/hour | ✓ Resolved — flipped ON 2026-04-30 in Phase 21 via custom Resend SMTP on `mail.horlo.app` (DKIM+SPF+DMARC verified; D-07 round-trip gate passed). Backout in `docs/deploy-db-setup.md` "Phase 21 — Custom SMTP via Resend Backout". |
| Session-mode pooler for migrations | Direct-connect host is IPv6-only, unreachable on most home ISPs | ✓ Good — documented in runbook |
| Two-layer privacy (RLS + DAL WHERE) | Single-layer is fragile; if one breaks, data leaks. Enforce at both layers so either breaking alone is still caught | ✓ Good — shipped in v2.0 Phase 6-10; integration tests in `tests/integration/home-privacy.test.ts` exercise the combined guarantee |
| No Supabase Realtime in v2.0 | Free tier limit of 200 concurrent WebSockets; server-rendered + `router.refresh()` is sufficient at MVP scale | ✓ Good — v2.0 shipped without it; revisit if user scale grows |
| Per-user independent watch entries (no canonical watch table) | Canonical normalization adds huge product complexity; per-user entries ship faster and let the UI/DAL compose views | ✓ Good — shipped in v2.0; revisit in a future "data strategy" phase if social features need cross-user watch identity |
| Cache Components (`cacheComponents: true`) with inline theme script | Next.js 16 `cacheComponents` forbids `cookies()` in layout body; canonical shadcn/next-themes inline `<script>` in `<head>` is the zero-FOUC escape hatch | ✓ Good — shipped in Phase 10; Suspense wraps Header + main |
| Rule-based recommendations (no LLM) in v2.0 | Keep the home page rendereable without API keys; 5 priority-ordered rationale templates produce deterministic, testable output | ✓ Good — shipped; revisit if product needs richer personalization |
| Viewer-aware DAL for cross-user reads | Different functions for owner-only (`getWatchById`) vs viewer-aware (`getWatchByIdForViewer`) keeps edit/delete paths strictly owner-scoped while allowing public views of public profiles | ✓ Good — pattern established in quick-260421-rdb; mirror for any future cross-user read |
| Three-tier wear visibility (public/followers/private) per row instead of `worn_public` boolean | Per-row visibility lets users mix public + followers-only + private wears on the same profile; tab-level boolean was too coarse | ✓ Good — shipped in v3.0 Phase 11 (schema + backfill) + Phase 12 (DAL ripple + column drop). Two-layer privacy preserved (RLS + DAL WHERE) |
| Default wear visibility: Public | Override researcher recommendation; explicitly chosen by user. Default-to-Public + clear UI copy is the safety net against confirmation-dialog friction | ✓ Good — shipped in v3.0 Phase 15 |
| Client-direct wear photo upload pipeline | Browser → Supabase Storage with user's session; Server Action validates storage key + inserts row. Avoids doubling bandwidth and Next.js 4MB body limit | ✓ Good — shipped in v3.0 Phase 15; orphan-cleanup on row-insert failure (23505 + non-23505) |
| Sonner Toaster bound to custom `ThemeProvider` (NOT next-themes) | Horlo's existing ThemeProvider is the single source of truth; binding to next-themes would fork theme state | ✓ Good — `ThemedToaster` Client Component shipped in v3.0 Phase 15 |
| `getWornTodayIdsForUser` preflight in WYWT picker + 23505 server catch | Two-layer defense against duplicate-day wears: client disables already-worn watches, server enforces UNIQUE constraint | ✓ Good — shipped in v3.0 Phase 15 |
| `revalidateTag(viewer:${id}, 'max')` for cross-user notification fan-out vs `updateTag` for self-reads | Read-your-own-writes via `updateTag()` (immediate); SWR semantics via `revalidateTag('max')` for OTHER users (recipient sees dot within 30s TTL) | ✓ Good — shipped in v3.0 Phase 13 |
| Fire-and-forget notification logger | Notification insert failure must never block follow / addWatch primary commit; same pattern as `logActivity` | ✓ Good — `logNotification` with internal try/catch + opt-out + self-guard shipped in v3.0 Phase 13 |
| Cradle-style elevated Wear CTA in mobile bottom nav (no SVG cutout) | Stacking-context simplicity > true notch cutout at v3.0 scale; visual elevation via translate + shadow achieves Figma intent | ✓ Good — shipped in v3.0 Phase 14 |
| Shared `PUBLIC_PATHS` constant for proxy + 3 nav surfaces | Single source of truth eliminates drift between auth gate and nav rendering | ✓ Good — shipped in v3.0 Phase 14 |
| pg_trgm GIN indexes for people search | Cost model picks Seq Scan at <127 rows but Bitmap Index Scan available at production scale (forced-plan EXPLAIN ANALYZE proves indexes are reachable) | ✓ Good — shipped in v3.0 Phase 11 + 16; production trajectory verified |

### v4.0 Phase 17 — Catalog Foundation

| Decision | Rationale | Trade-off |
|----------|-----------|-----------|
| Canonical `watches_catalog` table reverses v2.0 "no canonical watch table" decision | v4.0 needs cross-user identity for /search Watches, /explore Trending, /search Collections, /evaluate?catalogId= deep-link | Adds a second source of truth; CAT-11 splits SPEC fields (catalog) vs OWNERSHIP fields (per-user `watches`) at display-JOIN time |
| `watches.catalog_id` is NULLABLE INDEFINITELY in v4.0 — never `SET NOT NULL` | Backfill safety; URL-extract failures or orphaned data should leave the user's collection intact | Future v5+ phase (CAT-14) will SET NOT NULL after 100% backfill verified across two consecutive deploys |
| `watches_catalog` deliberate departure from two-layer privacy: public-read RLS, service-role-write ONLY | The catalog is silent infrastructure; per-user privacy is enforced on `watches` rows, not on catalog identity | Asymmetry is intentional and documented; new pattern in this codebase. v5+ admin tooling will be the only other writer. |
| `analyzeSimilarity()` is NOT modified in v4.0 — catalog is silent infrastructure | Avoids regression risk in the core value prop; current scoring works against per-user `watches` data which is unchanged | Future v5+ phase (CAT-13) migrates similarity engine to JOIN catalog at read time; tag taxonomy audit phase must land first |
| pg_cron daily refresh at 03:00 UTC + manual `npm run db:refresh-counts` for local dev | Off-peak refresh; local Docker doesn't ship pg_cron | Counts may be up to 24h stale (acceptable for "trending" / "gaining traction" surfaces) |
| source CHECK constraint NOT pgEnum — avoids ALTER TYPE ADD/DROP VALUE pain | Phase 24 ENUM cleanup proved rename-and-recreate is the costly path | Evolution = ALTER TABLE DROP/ADD CONSTRAINT (cheaper); literal union maintained manually in `src/lib/types.ts` |

### v4.0 Phases 18–26 — Discovery, Verdict, Settings, Polish

| Decision | Rationale | Trade-off |
|----------|-----------|-----------|
| Phase 19.1 inserted mid-milestone for LLM-derived taste enrichment | Phase 20 verdict copy quality (FIT-02 contextual phrasings) needs structured taste signals beyond hand-picked tags; computing once at write time keeps reads cheap | Adds 8 columns + Anthropic dependency on every catalog write; first-write-wins semantics (D-13) means re-enrichment requires explicit `--force` script flag |
| `analyzeSimilarity()` byte-locked across Phase 17, 19.1, AND Phase 20 | Verdict copy reframe must not regress the core scoring engine; `tests/static/CollectionFitCard.no-engine.test.ts` + git diff guards enforce | Engine rewire to read catalog taste at JOIN time deferred to v5.0+ (CAT-13); two sources of taste truth temporarily |
| `/evaluate` route eliminated; URL-paste capability moved into Add-Watch Flow as verdict-as-step | One coherent gesture (paste → verdict → 3-button decide) reads better than a separate evaluation surface; "skip" path covers the lightweight evaluate-only use case | `tests/no-evaluate-route.test.ts` static guard prevents reintroduction; deep-link from `/search?tab=watches` Evaluate CTA repointed to inline-expand accordion |
| BottomNav reshape to 5 slots Home/Search/Wear/Explore/Profile (Phase 18 D-03/D-04) | Notifications stays in TopNav bell (universal social-app convention); Profile permanent in BottomNav (muscle-memory) | Supersedes original DISC-08 + NAV-14 REQUIREMENTS.md wording — implementation matches amendment, doc string still references old slot |
| DKIM/SPF/DMARC verification BEFORE flipping Confirm-email toggle (D-07 ordering gate) | Catches MX-record / domain-config errors before users hit them; D-07 required Invite + real Gmail signup to land Inbox (not Spam) | Required full DNS lead-time + manual round-trip; surfaced two latent auth bugs (forgot-password redirectTo silent-drop, apex/www allowlist mismatch) that needed fixing in-flight |
| All three Supabase Auth email templates standardized on `/auth/callback?token_hash={{ .TokenHash }}&type=...&next=...` | Canonical Next.js + `@supabase/ssr` PKCE pattern; preempts SET-04 in Phase 22 (Email change template needs the same shape) | Phase 21 over-delivered relative to original plan; email-template realignment was unscoped scope |
| Hash-driven Settings tabs via `window.history.pushState`, NOT `router.push` | Tab switching must not re-run the page Server Component loader; URL fragment must be shareable + back-button friendly | Required custom `parseHash` + `hashchange` listener + Strict-Mode ref guard in `<StatusToastHandler>`; CR-01 fix during code review removed initially-incorrect `useSearchParams` reliance |
| Email-change UI never displays new email as current pre-confirmation (T-22-S4) | If new-email confirmation lands but old-email confirmation fails, displaying new email as current would leave the account in an undefined state | UI is "stickier" — the disabled input always shows currentEmail; pending banner copy "Confirmation sent to BOTH old@ AND new@" carries the visual change instead |
| 24h staleness threshold for password change re-auth (Phase 22 D-08 Option C) | OWASP recommends re-auth for sensitive ops on stale sessions; 24h balances security vs UX friction | `signInWithPassword` → `updateUser` chain in dialog; 401 catch reopens dialog as defense-in-depth |
| Notification enum cleanup via rename+recreate (Phase 24 DEBT-04) | Postgres has no `ALTER TYPE … DROP VALUE`; rename+recreate is the only path | Costly migration with T-24-PARTIDX partial-index surgery for enum-bound dependents; documented in deploy runbook as canonical pattern for future enum cleanups |
| Hybrid form-feedback pattern: Sonner toast + inline `aria-live="polite"` banner | Toast handles transient cue; banner handles accessibility + persistence; both required for screen-reader users + visual users | Adds `useFormFeedback` hook + `<FormStatusBanner>` primitive; rolled out across 7 forms with consistent successMessage / dialogMode props |
| 5-category URL-extract error taxonomy with locked copy | Generic "extraction failed" is unactionable; 5 categories (host-403, structured-data-missing, LLM-timeout, quota-exceeded, generic-network) each get specific recovery CTAs | Adds dispatch + CONTRACT_BY_CATEGORY map; tests parameterized per category (15 tests in ExtractErrorCard.test.tsx) |
| 5-slot BottomNav with Profile (NOT in TopNav alone) | Profile graduates from "buried in dropdown" to first-class affordance — but BottomNav slot 5 (Phase 18 inheritance) AND TopNav avatar dual-affordance (Phase 25) both ship | Two profile entrances on mobile; intentional redundancy for muscle-memory + glanceability |

### v5.0 — Discovery North Star

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Audit-first ordering: Phase 33 (read-only click-path audit) before any schema/polish work | Every downstream phase should cite specific audit row IDs, not vibes; falsifiable Pass/Fail criteria written before the audit ran | ✓ Good — 136-row click-path table is the immutable research substrate; all polish phases cite DISC-AUDIT-NN / NSV-NN ids |
| Phase 33 → 33b split (engineering audit vs product north-star audit) | The 4 D-17 decisions (combine home+explore, lineage priority, dead-end priority, CAT-13 framing) are product judgments against the SEED-004 Rdio principle, not answerable from click-path data alone | ✓ Good — mirrors a clean separation; 33b authored 4 verdicts that gated Phases 34/35/38/39 |
| Clean-slate catalog wipe (Phase 36) instead of in-place Variant dedup | Single-user owner + small DB + willingness to wipe makes `DELETE` + reseed safe; the clean slate is what makes the CAT-14 `catalog_id NOT NULL` flip feasible inside the milestone | ✓ Good — wipe-and-relink shipped; `is_nullable=NO` live in prod; no production-migration-safety burden |
| Self-idempotent Drizzle migrations (`CREATE TABLE IF NOT EXISTS` + `DO`-block FK guards) + journal entry in the same commit as the SQL file | Survives `supabase db push --linked` and `drizzle-kit migrate` running in either order; a missing `_journal.json` idx silently skips the migration in prod | ✓ Good — pattern held across Phases 34-37; the prod-journal drift it exposed is tracked as DEBT-12 |
| CAT-13: catalog taste as an **additive** 9th scoring dimension gated on `confidence >= 0.5` | Additive (not replacement) keeps the proven 8-dimension engine intact; the confidence gate falls back to per-user tag data when catalog taste is absent or low-confidence | ✓ Good — 19/19 static guards green; static guards written and passing BEFORE `similarity.ts` changed |
| DISC-09 dropped from v5.0 and promoted to a dedicated v5.1 milestone | The user inlined a comprehensive 5-module `/explore` redesign mid-Phase-39 discuss — far larger than the original single-slot Editorial Featured Collection framing | ✓ Good — captured as SEED-008; v5.0 ships `/explore` unchanged from Phase 18 |
| Phase 39c inserted: refactor `/u/[username]` to Cache Components + Suspense conformance | The `prefetch={false}` diagnostic from Phase 39b masked a Router-Cache-poisoning 404; the proper fix is a thin Suspense shell + cached resolver + viewer-dependent gate | ⚠️ Revisit — fixed the 404 + Partial Prerender, but post-close UAT found a stale-rail bug (39c UAT Issue 2, tracked in Active); 39c VERIFICATION.md is stale vs the post-recovery codebase |
| Bootstrap (one-shot SQL transaction) ≠ Curation (iterative `seed-lineage.ts`) | When prod catalog is empty, a one-shot bootstrap is correct; ongoing additions go through the idempotent curation script once UUIDs are known | ✓ Good — Phase 39b-01 bootstrapped 100 watches / 32 families / 52 edges; `seed-lineage.ts` ships as forward infrastructure |
| Milestone closed without `/gsd-audit-milestone`; verification + UAT gaps operator-approved | All 14 phases disk-complete (100%); the 4 verification gaps were stale (38 re-verified PASSED, 40 fixed post-verification) or human-only (35/41); operator accepted rather than deferred | — Pending — acceptable at single-user scale; a formal audit would have added confidence on cross-phase integration |

### v5.1 — Explore Page Redesign

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| In-app admin CMS over a third-party CMS (Sanity/Contentlayer) | No external dependency; fits the single-user / free / personal-first stance; `/admin/*` routes reuse the existing auth + Server Action stack | ✓ Good — 5-table CMS + owner-gated routes shipped in Phase 45 |
| `assertOwner()` at the start of every CMS Server Action, not just the layout guard | Server Actions are HTTP-callable endpoints that bypass layout gates; the route guard alone is insufficient | ✓ Good — two independent enforcement layers; verified in Phase 45 human-UAT |
| Two-layer RLS for published CMS content — `USING (status = 'published')` + explicit `WHERE status = 'published'` in every public-read DAL function | Single-layer is fragile; either layer breaking alone is still caught — same posture as the v2.0 follower-privacy pattern | ✓ Good — shipped Phase 45; no draft leaks |
| Catalog *enrichment* (v5.1) before *expansion* (v5.2) | Enriching the existing ~100 rows makes Browse + Archetypes return real data immediately; expanding breadth first would multiply the enrichment surface | ✓ Good — Phase 44 enriched all 100 rows; v5.2 SEED-009 handles breadth |
| LLM writes taste columns only; factual specs (movement, case size) stay human-reviewed | LLM hallucination on factual fields would corrupt catalog data used by `/search` filters | ✓ Good — Phase 44 propose/apply scripts keep factual edits human-gated |
| `revalidateTag('explore:hero', 'max')` in all four Hero write paths, never `revalidatePath` | Manual pin / publish / unpublish must propagate immediately under Next 16 `'use cache'`; `revalidatePath` does not invalidate tag-scoped caches | ✓ Good — shipped Phase 47; immediate Hero propagation verified |
| Hero data shape accepts both featured-list and featured-collector formats; only featured-list wired in v5.1 | Forward-compatible without building the unused path; featured-collector is a future-requirement | ✓ Good — EXPL-08 shipped list-only; shape is extensible |
| Closed end-of-milestone follow-ups (FU-01/FU-02) as quick tasks before close, not a v5.2 carry | Both were small, scoped, and operator-raised at Phase 46 UAT; closing them keeps v5.1 self-contained | ✓ Good — both shipped, deployed, operator-verified 2026-05-19 |
| Closed v5.1 without `/gsd-audit-milestone` | All 5 phases disk-complete + verified; verification debt cleared; the pre-close artifact audit's 23 items were all cosmetic or backlog | — Pending — acceptable at single-user scale; mirrors the v5.0 close decision |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

<details>
<summary>Previous Last-updated entries</summary>

*2026-05-01 — Phase 23 (Settings Sections + Schema-Field UI) complete. SET-07/08/09/10/11/12 + FEAT-07/08 satisfied: collectionGoal + overlapTolerance lifted to top of Preferences tab as dedicated Cards (Brand Loyalist option added with locked em-dash copy); PreferencesClient gains embedded prop suppressing page chrome inside Settings tab; AppearanceSection mounts InlineThemeSegmented in SettingsSection title="Theme" (Server-renders-Client child per Next.js 16; UserMenu retains its segmented control — both sync via horlo-theme cookie); WatchForm exposes isChronometer Checkbox and notesPublic Public/Private pill below Notes textarea; WatchDetail renders only-if-true Certification row with text-foreground Check icon (Anti-Pattern 9 honored); addWatch/editWatch Zod accepts notesPublic and revalidates /u/[username] layout for cross-page sync (D-19); SET-09/SET-11/SET-12 verified-no-change via Phase 22 D-01/D-15; D-20 cleanup grep ZERO orphans. Zero schema changes, zero new DAL functions; 6 plans in 2 worktree-parallelized waves; 5 manual UAT items approved. Up next: Phase 24 (Notification Stub Cleanup + Test Fixture & Carryover).*</details>

---
*Last updated: 2026-05-19 — v5.2 Polish + Taxonomy milestone started (SEED-011). Goal: fix the wishlist "you own this" mislabel on `/catalog/[catalogId]` + dark-mode `/search` filter-chip legibility; run two spikes (genre vs. style overlap, two-views merge decision). Small, fast milestone — spikes decision-only. Phase numbering continues from 48. Up next: research → requirements → roadmap.*

*Previous: 2026-05-19 after v5.1 milestone — Explore Page Redesign shipped. 5 phases (43-47), 27 plans, 269 commits over 3 days. 32/32 v5.1 requirements shipped. Built `/explore` as a five-module evergreen taste-driven discovery surface: a UI polish pass (swipe-dismissable filter drawer, device avatar upload, Claude model ID bump), an LLM/vision catalog-enrichment pass across all ~100 rows, a new in-app admin CMS (5 RLS-gated tables, owner-gated `/admin/*`), the five-module `/explore` shell with Browse + Collector Archetypes, and the editorial half (Curated Lists Rail + quality-gated Hero + Where Collections Go). Two end-of-milestone follow-ups (FU-01/FU-02) closed as quick tasks. Milestone closed without `/gsd-audit-milestone`; 23 pre-close artifact-audit items were all cosmetic/backlog and acknowledged. Up next: `/gsd-new-milestone` for v5.2 Catalog Expansion (SEED-009).*

*Previous: 2026-05-16 after v5.0 milestone — Discovery North Star shipped. 14 phases (32-42, incl. inserted 33b + 39c), 64 plans, 97 tasks, 433 commits over 11 days. 16/16 in-scope requirements shipped; DEBT-12 (prod drizzle journal repair) carried to v5.x as opportunistic housekeeping. Rdio-style click-driven discovery became the organizing principle: 136-row click-path audit + 42-cell drift-vector matrix drove the polish backlog; the catalog was rebuilt as a 5-level Brand/Family/Reference/Variant/Individual hierarchy (Layers A-D); `analyzeSimilarity()` now reads catalog taste as a 9th dimension (CAT-13); the Phase 33b high-leverage dead-end backlog was fully closed (Phases 39/39b); `/u/[username]` was refactored to Cache Components + Suspense (Phase 39c); Danger Zone + branded auth emails shipped (Phase 41); Nyquist sweep + ~33 deferred UAT items triaged (Phase 42). Milestone closed without `/gsd-audit-milestone`; 4 verification + 2 human-UAT gaps operator-approved. Up next: `/gsd-new-milestone` for v5.1 Explore Page Redesign (SEED-008).*

*Previous: 2026-05-12 — Phase 39 discuss-phase reframe. DISC-09 (single-slot Editorial Featured Collection) DROPPED from Phase 39 mid-discussion after the user inlined a comprehensive 5-module `/explore` page redesign spec — captured at `.planning/seeds/SEED-008-v5.1-explore-redesign.md` and promoted to a new v5.1 milestone. Phase 39 split into Phase 39 (cheap-tier dead-end patches) + Phase 39b (heavier UX). Mirrors Phase 33 → 33b pattern. Framing solidified: Home = daily check-in (recency-driven); Explore = rabbit hole (evergreen, taste-driven).*

*Previous: 2026-05-06 — v5.0 Discovery North Star milestone started. Goal: Rdio click-driven discovery as organizing principle + full catalog hierarchy (SEED-001 all 4 layers, schema-only) earning Reference granularity for the future recommender + v4.x carryover (DEBT-09, Nyquist sweep, deferred UAT triage). Audit-first per SEED-004 — Phase 32 is the discovery audit (read-only decisions doc); SEED-001 schema waves come after. Clean-slate DB wipe enabled by single-user state; CAT-14 `SET NOT NULL` lands alongside. No paywall (SEED-006 resolved 2026-05-06 — `.planning/research/PREMIUM-MAP.md`). Phase numbering continues from 32. Up next: research → requirements → roadmap.*

*Previous: 2026-05-05 after v4.1 milestone — Polish & Patch shipped. 5 phases (27-31), 21 plans + 1 quick task + 1 post-ship hotfix, 152 commits over 2 days. 12/12 v4.1 requirements satisfied at code level. Phase 31 audit surfaced DEBT-09 (HIGH severity Phase 23-era regression: `notesPublic` Zod field + `revalidatePath('/u/...')` absent from `src/app/actions/watches.ts` on `main`; commit `4d362ff` not in HEAD ancestry) — deferred to v5.0 carryover. Audit status: `tech_debt`.*

*Previous: 2026-05-05 after Phase 29 — Nav & Profile Chrome Cleanup (6 plans across 3 waves + 1 quick task) shipped: NAV-16 + PROF-10 + FORM-04 satisfied. UserMenu Profile row removed; Profile tab strip locked to horizontal scroll only; Add-Watch flow resets on every entry via per-request `crypto.randomUUID()` nonce + ref-guarded `useLayoutEffect` cleanup, with verdict + URL-extract caches hoisted to module scope so they survive remount. StrictMode-safe deep-link prefill from `/search`. Global vitest `<StrictMode>` wrapper added so this regression class is caught in CI. UAT 10/10 passed across three rounds. v4.1 progress: 4/5 phases complete (27 + 28 + 29 + 30); only Phase 31 (v4.0 Verification Backfill — DEBT-07/DEBT-08) remains.*

*Previous: 2026-05-05 after Phase 28 — Add-Watch Flow & Verdict Copy Polish (5 plans, 3 waves) shipped: FIT-06 + ADD-08 + UX-09 satisfied. Sonner action-slot toasts across 4 commit sites with destination links, `?returnTo=` validated round-trip across 8 entry-points (open-redirect-safe), 25 locked copy strings + speech-act-split verdict bundle, WishlistRationalePanel auto-fill switched to `rationalePhrasings[0]`. UAT 12/12 passed; security 21/21 closed. Surfaced DEBT-09 (Phase 17 supabase migrations missing locally) during UAT — backlog'd. v4.1 progress: 2/5 phases complete (27 + 28).*

*Previous: 2026-04-28 — Phase 19 (/search Watches + Collections) complete. 7/7 SRCH requirements satisfied; 116 unit + RTL tests green + 2 live-DB integration tests green; 8 advisory findings in 19-REVIEW.md (3 warning + 5 info, all sub-Critical and non-blocking).*

*Previous: 2026-04-27 — Phase 17 (Catalog Foundation) complete. `watches_catalog` + `watches_catalog_daily_snapshots` tables shipped with public-read RLS, generated normalization columns, pg_trgm GIN indexes, and a named NULLS NOT DISTINCT unique constraint. DAL upserters wired into `addWatch` + `/api/extract-watch` (fire-and-forget; catalog failure cannot block the user). Idempotent batched backfill via `npm run db:backfill-catalog`. Daily refresh: SECDEF function (anon=f, authed=f, service=t) scheduled by pg_cron at 03:00 UTC in prod, mirrored locally by `npm run db:refresh-counts`. 12/12 CAT-NN requirements; 56/56 integration tests green; 0 critical / 4 warning / 4 info from code review. `analyzeSimilarity()` byte-identical (silent infrastructure honored). Up next: Phase 18 /explore Discovery Surface.*

*Previous: 2026-04-26 — v4.0 Discovery & Polish milestone started. Goal: finish v3.0-era stubs (/explore, /search Watches, /search Collections, Settings expansion), expose schema-driven knobs (collectionGoal, overlapTolerance, notesPublic, isChronometer), surface the similarity engine as a first-class "Evaluate this watch" flow, raise profile prominence, ship empty-state CTAs + WYWT auto-nav + form polish, and lay the canonical `watches_catalog` table that unblocks future cross-user features. Carryover: custom SMTP + email confirmation ON, test fixture cleanup, TEST-04/05/06. Cleanup: remove price_drop + trending_collector notification stub dead code. Up next: research → requirements → roadmap.*

*Previous: 2026-04-27 after v3.0 milestone — Production Nav & Daily Wear Loop shipped. 7 phases (11, 12, 13, 14, 15, 16, 999.1), 37 plans, 56 tasks, 178 commits over 5 days; 51/51 v3.0 requirements satisfied at code level. Schema + storage foundation, three-tier wear privacy ripple, notifications system, production navigation frame (mobile BottomNav + SlimTopNav + DesktopTopNav with shared PUBLIC_PATHS gate), photo-first WYWT post flow with `/wear/[id]` detail route, and people search with pg_trgm-backed live debounced results all live. Audit status: `tech_debt` — 31 deferred human-verification UAT items + ~30 advisory items, none blocking. See `.planning/milestones/v3.0-MILESTONE-AUDIT.md`.*

*Previous: 2026-04-24 — Phase 14 (Nav Shell + Explore Stub) complete. Production navigation frame shipped: mobile BottomNav (5-item sticky bar, 80px tall, shrink-0 Wear cradle with 12px natural overflow above the bar, all 5 labels share a bottom baseline); desktop Header delegator split into SlimTopNav (<768px) / DesktopTopNav (≥768px); legacy `MobileNav.tsx` deleted (NAV-12); `/explore` + `/search` stubs close nav-link 404s; `/insights` retired to owner-only profile tab with two-layer P-08 privacy defense; profile dropdown consolidated via `InlineThemeSegmented` (D-17) with base-ui dismissal guard on the theme buttons; Settings routes to `/preferences` as sole v3.0 entry point (D-12); shared `PUBLIC_PATHS` constant + `isPublicPath` predicate unify proxy + nav auth gate (NAV-05 D-21/D-22); IBM Plex Sans + `viewport-fit=cover` metadata added. DEBT-01 regression-locked. 5 human UAT items resolved. 2489 tests pass, 0 regressions. Code review: 0 critical, 3 warnings, 5 info (non-blocking, tracked in 14-REVIEW.md). Up next: Phase 15 (WYWT Photo Post Flow).*
