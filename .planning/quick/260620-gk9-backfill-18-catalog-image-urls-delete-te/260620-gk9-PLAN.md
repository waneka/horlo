---
quick_task: 260620-gk9-backfill-18-catalog-image-urls-delete-te
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/{NEW_TIMESTAMP}_quick_260620_gk9_catalog_image_backfill.sql
  - scripts/seed-data/catalog-missing-images.md
autonomous: false
requirements:
  - QUICK-260620-GK9-01  # Backfill 18 missing watches_catalog.image_url values (idempotent, natural-key keyed)
  - QUICK-260620-GK9-02  # Delete rogue brand='test' model='test' reference=NULL row (guarded)
  - QUICK-260620-GK9-03  # Verify 0 rows missing image_url remain in prod

must_haves:
  truths:
    - "All 18 listed (brand, model, reference) triples in prod watches_catalog have a non-empty image_url after migration"
    - "The brand='test' model='test' reference=NULL row no longer exists in prod watches_catalog"
    - "Re-running `npm run catalog:missing-images` against prod prints '0 of 193 catalog rows missing image_url'"
    - "Migration is idempotent: re-applying the file does not error and does not overwrite any image_url the user later changes by hand"
    - "Migration is keyed by (brand, model, reference) natural triple — NEVER by id (per project_catalog-id-divergence)"
    - "Migration was applied to PROD only via `supabase db push --linked` — never via drizzle-kit, never to local"
  artifacts:
    - path: "supabase/migrations/{NEW_TIMESTAMP}_quick_260620_gk9_catalog_image_backfill.sql"
      provides: "18 guarded UPDATE statements + 1 guarded DELETE + DO $$ pre/post assertions"
      contains: "UPDATE public.watches_catalog SET image_url"
  key_links:
    - from: "supabase/migrations/{NEW_TIMESTAMP}_quick_260620_gk9_catalog_image_backfill.sql"
      to: "prod public.watches_catalog rows"
      via: "supabase db push --linked"
      pattern: "WHERE brand = .* AND model = .* AND (reference = .* OR reference IS NULL)"
---

<objective>
Backfill 18 missing `image_url` values on prod `public.watches_catalog`, and delete the
rogue `brand='test' model='test' reference=NULL` row that has 0 owners, 0 wishlist, and 0
user photos. Source of truth for the 18 URLs is the markdown table at
`/Users/tylerwaneka/Documents/horlo/scripts/seed-data/catalog-missing-images.md` rows 12-30
(skip row 27 — that's the test/test DELETE target). Apply path is a single Supabase
migration pushed to prod only.

Purpose: Eliminates 19 placeholder-image rows in the catalog so every explore / search /
detail view renders a real image. Closes a long-standing UX paper cut where 9.8% of catalog
rows showed the watch-icon fallback.

Output:
- One new file: `supabase/migrations/{NEW_TIMESTAMP}_quick_260620_gk9_catalog_image_backfill.sql`
- Updated `scripts/seed-data/catalog-missing-images.md` (regenerated to 0 rows after apply)
- Zero source-code changes — pure data mutation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tylerwaneka/Documents/horlo/CLAUDE.md
@/Users/tylerwaneka/Documents/horlo/AGENTS.md
@/Users/tylerwaneka/Documents/horlo/.planning/STATE.md
@/Users/tylerwaneka/Documents/horlo/scripts/seed-data/catalog-missing-images.md
@/Users/tylerwaneka/Documents/horlo/scripts/list-catalog-missing-images.ts
@/Users/tylerwaneka/Documents/horlo/supabase/migrations/20260526120000_phase61_cleanup_url_watch_photos.sql
@/Users/tylerwaneka/Documents/horlo/supabase/migrations/20260620001739_seed_explore_editorial.sql

<interfaces>
<!-- watches_catalog columns relevant to this task (from src/db/schema.ts lines 433-503). -->
<!-- Executor: key by (brand, model, reference) natural triple — NEVER by id (project_catalog-id-divergence). -->

watches_catalog columns of interest:
  - id                uuid PK (DO NOT USE as a key — diverges local vs prod)
  - brand             text NOT NULL
  - model             text NOT NULL
  - reference         text NULL
  - image_url         text NULL                 ← target column
  - owners_count      integer NOT NULL default 0
  - wishlist_count    integer NOT NULL default 0

UNIQUE constraint on prod: watches_catalog_natural_key on (brand_normalized, model_normalized, reference_normalized) NULLS NOT DISTINCT.
Generated columns: brand_normalized = lower(trim(brand)), model_normalized = lower(trim(model)),
reference_normalized = CASE WHEN reference IS NULL THEN NULL ELSE regexp_replace(lower(trim(reference)), '[^a-z0-9]+', '', 'g') END.

We match plainly on (brand, model, reference) text — values in catalog-missing-images.md
were emitted by the read-only inventory script directly from those exact columns, so
plain equality is correct. Use `reference IS NULL` for the test row (no value in column).

Cross-reference for the DELETE guard — these schema relationships ensure the test row is orphan:
  - watches.catalog_id → watches_catalog.id (ON DELETE SET NULL)
  - watch_photos.watch_id → watches.id (ON DELETE CASCADE)
A safe DELETE requires: owners_count=0, wishlist_count=0, and NOT EXISTS (any watches row pointing at this catalog row).
</interfaces>

<reference_data>
<!-- 18 backfill rows + 1 delete row, copied verbatim from catalog-missing-images.md lines 12-30. -->
<!-- Row 27 (test/test) is the DELETE target — not in the UPDATE set. -->

UPDATE targets (18 rows — fill image_url WHERE current image_url IS NULL or empty):
 1. A. Lange & Söhne | 1815                              | 233.026
 2. A. Lange & Söhne | Odysseus                          | 363.179
 3. A. Lange & Söhne | Saxonia Thin                      | 211.027
 4. Baume & Mercier  | Riviera                           | 10728
 5. Blancpain        | Fifty Fathoms                     | 5015-1130-71S
 6. Certina          | DS Action GMT Powermatic 80       | C032.929.11.051.00
 7. Christopher Ward | The Twelve 40mm                   | C12-40A-S00K0-S00B0-K
 8. Jaeger-LeCoultre | Master Ultra Thin                 | Q1342520
 9. Jaeger-LeCoultre | Master Ultra Thin Small Seconds   | Q1218420
10. Jaeger-LeCoultre | Polaris Date                      | Q9068681
11. Omega            | Seamaster 300                     | 234.30.41.21.03.001
12. Orient           | Bambino Version 7                 | FAC0000DD0
13. Rolex            | Datejust 41                       | 126334
14. Rolex            | Explorer 40                       | 224270
15. Sinn             | 856 UTC                           | 856.010
16. Tudor            | Black Bay 58 GMT                  | 7939G1A0NRU
17. Vacheron Constantin | Overseas                       | 4500V/110A-B128
18. Zenith           | Defy Skyline                      | 03.9300.3620/01.I001

DELETE target (row 27):
  brand='test' AND model='test' AND reference IS NULL

URLs are in catalog-missing-images.md column 6 of the corresponding markdown row. Read them
directly from that file when writing the migration — do not retype (transcription risk on
the long Rolex / Tudor / Vacheron / Christopher Ward / Blancpain CDN URLs). No URL contains
a single quote (inspected — apostrophe-free); if any future row does, double it (`''`) per
SQL string-literal escaping.
</reference_data>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the prod migration file</name>
  <files>supabase/migrations/{NEW_TIMESTAMP}_quick_260620_gk9_catalog_image_backfill.sql</files>
  <action>
First, resolve {NEW_TIMESTAMP}: run `date -u +%Y%m%d%H%M%S` in Bash and use the printed
value (must be > 20260620001739 so the file sorts AFTER the latest migration
`20260620001739_seed_explore_editorial.sql` per project_drizzle_supabase_db_mismatch
filename-ordering gotcha #1). Confirm by `ls -t supabase/migrations/ | head -3`.

Create the file at the absolute path
`/Users/tylerwaneka/Documents/horlo/supabase/migrations/{NEW_TIMESTAMP}_quick_260620_gk9_catalog_image_backfill.sql`
using the Write tool.

Migration structure (follow the `20260526120000_phase61_cleanup_url_watch_photos.sql`
shape — leading comment block explaining intent + memory references, BEGIN/COMMIT wrap,
DO $$ assertions for pre/post checks):

1. Leading comment block (>=15 lines) explaining:
   - Quick task ID `260620-gk9`
   - PROD-ONLY application (skip local; local has known catalog-id divergence and dropped
     natural-key constraints per project_catalog-id-divergence + project_local_catalog_natural_key_drift)
   - Source: scripts/seed-data/catalog-missing-images.md, generated by
     `npm run catalog:missing-images`
   - Apply via `supabase db push --linked` (NOT drizzle-kit) per
     project_drizzle_supabase_db_mismatch
   - Idempotency strategy: each UPDATE has `AND (image_url IS NULL OR btrim(image_url) = '')`
     so re-applying the file later does NOT clobber any URL the operator changes by hand
   - Note that watches_catalog is non-wipeable per project_db_wipeable_2026_05_09 (this is
     why we use ALTER+UPDATE, not a reseed)

2. `BEGIN;`

3. Pre-flight assertion (DO $$ block) — confirms all 18 (brand, model, reference) triples
   exist in prod catalog. Hardcode the count via `WHERE (brand, model, reference) IN (...)`
   with all 18 triples listed (use `(brand, model, reference)` row-constructor; for the
   18-row IN-list, NULL is fine because all 18 have a non-null reference). Assert count = 18,
   raise EXCEPTION if not. Format:
       DO $$
       DECLARE found int;
       BEGIN
         SELECT count(*) INTO found FROM public.watches_catalog
          WHERE (brand, model, reference) IN (
            ('A. Lange & Söhne','1815','233.026'),
            ('A. Lange & Söhne','Odysseus','363.179'),
            ... -- all 18
          );
         IF found <> 18 THEN
           RAISE EXCEPTION 'pre-flight: expected 18 matching catalog rows, found %', found;
         END IF;
       END $$;

4. Eighteen UPDATE statements, one per row. Pattern:
       UPDATE public.watches_catalog
          SET image_url = '<URL from catalog-missing-images.md>',
              updated_at = now()
        WHERE brand = '<brand>'
          AND model = '<model>'
          AND reference = '<reference>'
          AND (image_url IS NULL OR btrim(image_url) = '');
   Read each URL VERBATIM from `scripts/seed-data/catalog-missing-images.md` column 6.
   Watch for special characters:
   - Apostrophes in brand `A. Lange & Söhne` (ö is fine, the file is UTF-8; & is fine)
   - The Rolex URLs contain commas inside CDN transform params — fine inside single quotes
   - No URL in the source file contains an apostrophe (verified)
   - All 18 references are non-NULL — `reference = 'X'` form is correct for all
   Note: the `updated_at = now()` makes the row updated_at column reflect the backfill,
   matching the schema's `updated_at` Phase 17 trigger semantics.

5. DELETE for the test/test row — guarded with defense in depth:
       DELETE FROM public.watches_catalog
        WHERE brand = 'test'
          AND model = 'test'
          AND reference IS NULL
          AND coalesce(owners_count, 0) = 0
          AND coalesce(wishlist_count, 0) = 0
          AND NOT EXISTS (
            SELECT 1 FROM public.watches w WHERE w.catalog_id = watches_catalog.id
          );

6. Post-flight assertion (DO $$ block) — confirms post-state:
       DO $$
       DECLARE still_missing int;
       DECLARE test_row int;
       BEGIN
         SELECT count(*) INTO still_missing FROM public.watches_catalog
          WHERE (brand, model, reference) IN (... all 18 ...)
            AND (image_url IS NULL OR btrim(image_url) = '');
         IF still_missing > 0 THEN
           RAISE EXCEPTION 'post-flight: % of the 18 targeted rows still missing image_url', still_missing;
         END IF;

         SELECT count(*) INTO test_row FROM public.watches_catalog
          WHERE brand = 'test' AND model = 'test' AND reference IS NULL;
         IF test_row <> 0 THEN
           RAISE EXCEPTION 'post-flight: test/test row was not deleted (count=%)', test_row;
         END IF;
       END $$;

7. `COMMIT;`

IMPORTANT — local apply prohibition. Do NOT run:
  - `supabase db reset`
  - `npx drizzle-kit push`
  - `psql` against the local Supabase container
on this migration. Local watches_catalog has known id divergence + may be missing the
natural-key constraint (project_local_catalog_natural_key_drift). The user explicitly chose
prod-only. File creation only in this task; prod apply happens in Task 2.
  </action>
  <verify>
    <automated>
test -f /Users/tylerwaneka/Documents/horlo/supabase/migrations/*_quick_260620_gk9_catalog_image_backfill.sql && grep -c "UPDATE public.watches_catalog" /Users/tylerwaneka/Documents/horlo/supabase/migrations/*_quick_260620_gk9_catalog_image_backfill.sql | grep -v '^#' | awk -F: '{print $NF}' | grep -qx 18 && grep -q "DELETE FROM public.watches_catalog" /Users/tylerwaneka/Documents/horlo/supabase/migrations/*_quick_260620_gk9_catalog_image_backfill.sql && grep -q "RAISE EXCEPTION 'pre-flight" /Users/tylerwaneka/Documents/horlo/supabase/migrations/*_quick_260620_gk9_catalog_image_backfill.sql && grep -q "RAISE EXCEPTION 'post-flight" /Users/tylerwaneka/Documents/horlo/supabase/migrations/*_quick_260620_gk9_catalog_image_backfill.sql && echo OK
    </automated>
  </verify>
  <done>
Migration file exists with exactly 18 `UPDATE public.watches_catalog` statements, one
`DELETE FROM public.watches_catalog` statement, and both pre-flight and post-flight
`RAISE EXCEPTION` assertions inside `DO $$` blocks. Filename timestamp sorts AFTER
`20260620001739_seed_explore_editorial.sql`. File NOT applied anywhere yet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Push migration to prod + re-verify with read-only inventory</name>
  <what-built>
The migration file from Task 1. The operator now applies it to PROD ONLY (skip local entirely
per user decision and per project_local_catalog_natural_key_drift — local catalog
keys/constraints don't match prod), then re-runs the read-only inventory to confirm 0
missing-image rows remain.
  </what-built>
  <how-to-verify>
Run these commands from `/Users/tylerwaneka/Documents/horlo` and confirm each output.

1. **(Operator) Confirm the migration looks right before pushing:**
   ```
   cat supabase/migrations/$(ls -t supabase/migrations/ | head -1)
   ```
   Spot-check: 18 UPDATEs, 1 DELETE, pre/post DO $$ blocks, filename `*_quick_260620_gk9_*`.

2. **Push to prod (NOT local):**
   ```
   supabase db push --linked
   ```
   Expected: CLI prompts for confirmation, applies the one new migration, prints
   `Finished supabase db push.` Any RAISE EXCEPTION from the DO $$ assertions causes
   transaction rollback — investigate and report back BEFORE proceeding. Common failure
   modes:
   - Pre-flight `expected 18 matching catalog rows, found N` (N<18) → a (brand, model,
     reference) triple in the migration doesn't match prod. Likely cause: hidden trailing
     space or smart-quote in the source markdown. Re-export with
     `npm run catalog:missing-images` and compare.
   - Pre-flight extension-schema / enum gotchas — unlikely for this pure-DML migration but
     keep project_drizzle_supabase_db_mismatch's 4 gotchas in mind.

3. **Re-run the read-only inventory against prod:**
   ```
   npm run catalog:missing-images
   ```
   Expected console output:
   ```
   [missing-images] 0 of 193 catalog rows missing image_url (0.0%)
   ```
   (193 = 194 prior total − 1 deleted test/test row. If you see 194 = 0 then the DELETE
   silently no-op'd; report back.)

4. **Confirm the regenerated markdown file shows 0 data rows:**
   ```
   cat scripts/seed-data/catalog-missing-images.md
   ```
   Expected: the header table is present but with NO data rows below the
   `| ----- | ----- | ... |` separator. Only the front-matter and column headers remain.

5. **Sanity check on prod via direct SQL (optional but recommended):**
   ```
   PROD_DATABASE_URL=postgresql://... psql "$PROD_DATABASE_URL" \
     -c "SELECT count(*) FROM public.watches_catalog WHERE image_url IS NULL OR btrim(image_url)='';"
   ```
   Expected: `0`. And:
   ```
   PROD_DATABASE_URL=postgresql://... psql "$PROD_DATABASE_URL" \
     -c "SELECT count(*) FROM public.watches_catalog WHERE brand='test' AND model='test';"
   ```
   Expected: `0`.

6. **Visual smoke check (optional, 30s):** Pick one of the backfilled watches (e.g. open the
   explore page filtered to Rolex or browse to the Tudor Black Bay 58 GMT detail page on
   prod) and confirm a real image renders instead of the watch-icon placeholder.

If all six checks pass, the migration succeeded. Stage the migration file + the
regenerated `scripts/seed-data/catalog-missing-images.md` and proceed to commit.
  </how-to-verify>
  <resume-signal>
Type "approved" once `npm run catalog:missing-images` reports 0 missing rows on prod, OR
describe any issues encountered (failed assertion, mismatched count, etc.) for triage.
  </resume-signal>
</task>

</tasks>

<verification>
1. New migration file exists under `supabase/migrations/` with `*_quick_260620_gk9_*` in the
   filename and a timestamp later than `20260620001739`.
2. Migration contains exactly 18 `UPDATE public.watches_catalog` statements, 1
   `DELETE FROM public.watches_catalog`, and pre/post `DO $$ ... RAISE EXCEPTION` guards.
3. `supabase db push --linked` applied the migration to prod without error.
4. `npm run catalog:missing-images` reports `0 of 193 catalog rows missing image_url (0.0%)`.
5. Regenerated `scripts/seed-data/catalog-missing-images.md` has zero data rows.
6. The migration was NOT applied to the local Supabase container (no `drizzle-kit push`,
   no `supabase db reset`, no local psql apply).
</verification>

<success_criteria>
- 18 prod catalog rows have a real `image_url` (the URLs listed in
  catalog-missing-images.md rows 12-30, excluding row 27).
- The rogue `brand='test' model='test'` row is gone from prod.
- Prod inventory script reports 0 of 193 rows missing an image.
- The migration is idempotent — re-applying it to prod is a no-op (every UPDATE clause's
  `AND (image_url IS NULL OR btrim(image_url) = '')` predicate matches zero rows on retry;
  DELETE matches zero rows; pre-flight still finds 18 catalog rows by triple; post-flight
  still passes).
- No source code changes. No local DB mutation. No drizzle-kit involvement.
</success_criteria>

<output>
After completion, create
`/Users/tylerwaneka/Documents/horlo/.planning/quick/260620-gk9-backfill-18-catalog-image-urls-delete-te/260620-gk9-01-SUMMARY.md`
with:
- What shipped (1 migration, 18 backfills + 1 delete)
- Apply path (prod-only via `supabase db push --linked`)
- Final inventory count (0 of 193)
- Any deviations encountered during prod push
- Commit SHA(s)
</output>
