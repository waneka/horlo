---
phase: 260530-e55-srch-03-followup
plan: 01
subsystem: ui
tags: [react, zustand, base-ui, combobox, vitest, tdd]

requires:
  - phase: 72-search-composition-fixes
    provides: "SRCH-03 structural fix (footer outside listbox) + SRCH-03b panel-mount assertion"

provides:
  - "Composite footer onClick handler that closes popup AND mounts StructuredEntryPanel"
  - "SRCH-03b extended with listbox-unmount regression assertion"

affects: [v8.1-prod-push, 72-HUMAN-UAT]

tech-stack:
  added: []
  patterns:
    - "Composite controlled-combobox close: setShowPanel + setIsPopupOpen in one arrow body"

key-files:
  created: []
  modified:
    - src/components/watch/SearchEntry.tsx
    - src/components/watch/SearchEntry.test.tsx

key-decisions:
  - "D-09 refined: Phase 72 D-09 said keep onClick verbatim; root cause was incomplete diagnosis. This quick task keeps setShowPanel(true) as the first statement and adds setIsPopupOpen(false) as an additive second statement."
  - "WR-02 stale-comment cleanup in SRCH-03b test prose deferred (out of scope per plan constraints)"

requirements-completed:
  - SRCH-03-followup

duration: 5min
completed: 2026-05-30
---

# Quick Task 260530-e55: SRCH-03 Followup Popup-Stay-Open Fix Summary

**Composite footer onClick closes base-ui combobox popup and mounts StructuredEntryPanel atomically, with regression assertion added to SRCH-03b.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-30T10:13:00Z
- **Completed:** 2026-05-30T10:18:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added listbox-unmount assertion to SRCH-03b: `expect(screen.queryByRole('listbox')).not.toBeInTheDocument()` after footer click
- Replaced `onClick={() => setShowPanel(true)}` with `onClick={() => { setShowPanel(true); setIsPopupOpen(false); }}` at SearchEntry.tsx:329
- RED->GREEN transition confirmed: SRCH-03b fails on old handler, passes on composite handler; all 24 SearchEntry tests green
- Build gate passed: `npm run build` exits 0

## Task Commits

1. **Task 1: Add listbox-unmount assertion to SRCH-03b (RED)** — `6070c5cc` (test)
2. **Task 2: Composite footer handler — close popup on panel mount (GREEN + build gate)** — `17d5bc0f` (fix)

## Files Created/Modified

- `src/components/watch/SearchEntry.tsx` — Footer button onClick changed from single `setShowPanel(true)` to composite `{ setShowPanel(true); setIsPopupOpen(false); }` (line 329)
- `src/components/watch/SearchEntry.test.tsx` — SRCH-03b extended with `expect(screen.queryByRole('listbox')).not.toBeInTheDocument()` after the panel-mount assertion (after line 871)

## Change Before/After

**Before (SearchEntry.tsx:329):**
```tsx
onClick={() => setShowPanel(true)}
```

**After:**
```tsx
onClick={() => { setShowPanel(true); setIsPopupOpen(false); }}
```

**Test addition (SearchEntry.test.tsx, after line 871):**
```tsx
expect(screen.queryByRole('listbox')).not.toBeInTheDocument()
```

## RED -> GREEN Log Paths

- `/tmp/quick-srch03-followup-red.log` — SRCH-03b FAIL on `queryByRole('listbox')` assertion; 23 other tests pass
- `/tmp/quick-srch03-followup-green.log` — 24/24 tests pass after composite handler
- `/tmp/quick-srch03-followup-build.log` — `npm run build` exit 0

## Decisions Made

**D-09 refinement:** Phase 72 D-09 recorded "preserve onClick verbatim" based on incomplete prod-bug diagnosis at the time of 72-HUMAN-UAT.md. This quick task refines D-09: the `setShowPanel(true)` panel-mount mechanism is preserved AS THE FIRST STATEMENT of the composite handler; `setIsPopupOpen(false)` is purely additive and closes the z-stacked popup so users can see the freshly-mounted StructuredEntryPanel.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

**font-medium guardrail note:** `grep -v '^//' src/components/watch/SearchEntry.tsx | grep -c "font-medium"` returns 2 (not 0 as plan success criteria states). Both are pre-existing JSX block comments (`{/* ... */}`) explaining the guardrail — present in the baseline before this change. The plan's `grep -v '^//'` only strips `//`-style comments, not JSX block comments. No `font-medium` was introduced by this change (confirmed via `git diff`). The guardrail intent (no raw `font-medium` className in production code) is intact.

## User Setup Required

None.

## Deferred Items

- **WR-02:** Stale comment prose cleanup in SRCH-03b ("jsdom-tolerant (click works either way in jsdom)") deferred out of scope per plan constraints. Code-review hygiene task.

## Next Steps

- Prod verification: footer click -> popup closes -> StructuredEntryPanel visible inline with pre-seeded brand/model. Bundles into v8.1 prod push per `feedback_mobile_ui_verify_on_prod`.
- Phase 72 HUMAN-UAT SC#3 can flip from `issue` to `pass` after prod push confirms.

---
*Phase: 260530-e55-srch-03-followup*
*Completed: 2026-05-30*
