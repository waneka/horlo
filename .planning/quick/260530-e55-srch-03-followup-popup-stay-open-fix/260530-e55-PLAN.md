---
phase: 260530-e55-srch-03-followup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/watch/SearchEntry.test.tsx
  - src/components/watch/SearchEntry.tsx
autonomous: true
requirements:
  - SRCH-03-followup
must_haves:
  truths:
    - "After clicking the 'Not finding it? Add manually' footer button, the combobox listbox is removed from the DOM (popup closes)"
    - "After clicking the footer button, the StructuredEntryPanel mounts inline below the search input with pre-seeded brand/model from the current query"
    - "The popup-close behavior is asserted by a regression test that fails on the current code and passes after the fix"
  artifacts:
    - path: "src/components/watch/SearchEntry.tsx"
      provides: "Composite footer onClick handler that mounts the panel AND closes the popup"
      contains: "setShowPanel(true)"
    - path: "src/components/watch/SearchEntry.test.tsx"
      provides: "SRCH-03b extended with listbox-unmount assertion"
      contains: "queryByRole('listbox')"
  key_links:
    - from: "src/components/watch/SearchEntry.tsx footer button onClick"
      to: "isPopupOpen state setter (setIsPopupOpen)"
      via: "composite arrow body { setShowPanel(true); setIsPopupOpen(false); }"
      pattern: "setIsPopupOpen\\(false\\)"
    - from: "src/components/watch/SearchEntry.test.tsx SRCH-03b"
      to: "post-click listbox absence assertion"
      via: "expect(screen.queryByRole('listbox')).not.toBeInTheDocument()"
      pattern: "queryByRole\\('listbox'\\)\\)\\.not\\.toBeInTheDocument"
---

<objective>
Close the SRCH-03 follow-up defect captured in Phase 72 verification: the footer "Not finding it? Add manually" click pre-seeds the inline StructuredEntryPanel correctly, but does NOT close the combobox popup. The popup z-stacks over the freshly-mounted panel, so prod users perceive the click as a no-op.

Purpose: Restore the user-visible footer-click affordance to its intended behavior (popup collapses, panel appears) so the Phase 72 SC#3 contract holds end-to-end on prod, not just structurally in jsdom.

Output: One-line composite handler in `SearchEntry.tsx` + one assertion line added to the existing SRCH-03b test in `SearchEntry.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./AGENTS.md
@.planning/phases/72-search-composition-fixes/72-CONTEXT.md
@.planning/phases/72-search-composition-fixes/72-HUMAN-UAT.md
@src/components/watch/SearchEntry.tsx
@src/components/watch/SearchEntry.test.tsx

<interfaces>
<!-- Key composition surface from SearchEntry.tsx the executor must use as-is. Extracted from current code. -->

State setters in scope at the footer JSX (SearchEntry.tsx:103, 109):
```
const [showPanel, setShowPanel] = useState(false)        // line 103
const [isPopupOpen, setIsPopupOpen] = useState(false)    // line 109
```

Current footer button (SearchEntry.tsx:326-334) — the line being changed is the onClick:
```
{!isLoading && results.length > 0 && (
  <button
    type="button"
    onClick={() => setShowPanel(true)}      // line 329 — REPLACE the arrow body
    className="mt-1 flex w-full items-center gap-2 rounded-md px-2 py-3 text-sm text-muted-foreground hover:bg-muted hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring min-h-[44px]"
  >
    Not finding it? Add manually
  </button>
)}
```

Existing SRCH-03b test (SearchEntry.test.tsx:850-872) — the post-click assertion at line 871 stays; one new assertion is appended after it:
```
fireEvent.click(footer)                                          // line 869
expect(screen.getByTestId('structured-panel-mock')).toBeInTheDocument()   // line 871
// NEW: assert popup closed
```

Existing structural guard SRCH-03a (line 827) already asserts listbox presence BEFORE the footer click — confirms our new "absent AFTER click" assertion is meaningful (the listbox is verified mounted in the same render).
</interfaces>

<phase_72_context_recap>
- D-09 ("Keep `onClick={() => setShowPanel(true)}` exactly as it is") was written from the prod-bug-incomplete diagnosis. This plan REFINES D-09: the panel-mount mechanism is preserved verbatim AS THE FIRST STATEMENT of the composite handler; the popup-close is additive (`setIsPopupOpen(false)`).
- font-semibold guardrail (`project_phase_68_complete` recurrence): no `font-medium` may be introduced anywhere. This change is JSX-handler-only; no className touched.
- `npm run build` (exit 0) is the gate (`project_baseline_not_green_build_is_gate`). Pre-existing `tsc --noEmit` errors and the `CommentGateLocked font-medium` vitest failure are baseline noise — do NOT attempt to fix them.
- No worktrees (`workflow.use_worktrees=false` permanent).
- SearchEntry is a pure presenter; no new props are added (Phase 68 + 69 precedent + Phase 72 D-07 single-change discipline).
</phase_72_context_recap>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add listbox-unmount assertion to SRCH-03b (RED)</name>
  <files>src/components/watch/SearchEntry.test.tsx</files>
  <behavior>
    - After `fireEvent.click(footer)` in SRCH-03b (currently line 869), a new line asserts `expect(screen.queryByRole('listbox')).not.toBeInTheDocument()` before the test body ends.
    - SRCH-03a (structural sibling guard) is NOT modified. Its `expect(screen.getByRole('listbox')).toBeInTheDocument()` at line 842 stays — it asserts the pre-click state.
    - SRCH-03b's existing `expect(screen.getByTestId('structured-panel-mock')).toBeInTheDocument()` at line 871 stays unchanged.
    - Run the SearchEntry test file: the new assertion turns RED on the current production handler (`onClick={() => setShowPanel(true)}`) because the popup stays open after the click; `isPopupOpen` remains true; listbox stays in the DOM. SRCH-03a and the panel-mount assertion still pass.
  </behavior>
  <action>
    Edit `src/components/watch/SearchEntry.test.tsx` in the SRCH-03b test (the `it('(SRCH-03b)...'` block starting at line 850). After the existing `expect(screen.getByTestId('structured-panel-mock')).toBeInTheDocument()` line (currently line 871), append a single new assertion line:

      expect(screen.queryByRole('listbox')).not.toBeInTheDocument()

    No other lines move. No new `it()` block. No new describe. No new imports — `screen`, `queryByRole`, and `expect` are already in scope (used throughout the file). Do NOT clean up the stale "SRCH-03: jsdom-tolerant (click works either way in jsdom)" comment in SRCH-03b — code-review WR-02 hygiene cleanup is OUT OF SCOPE per the planning constraints. After saving, run the targeted vitest run; the new assertion MUST fail (popup did not close), proving the test catches the prod defect.
  </action>
  <verify>
    <automated>npm run test -- --run src/components/watch/SearchEntry.test.tsx 2>&1 | tee /tmp/quick-srch03-followup-red.log | tail -60</automated>
  </verify>
  <done>
    Targeted vitest run of `SearchEntry.test.tsx` reports SRCH-03b as FAILED with the listbox-still-in-DOM message at the new assertion line; SRCH-03a passes; all other tests in the file maintain their pre-change status (the RED is scoped to SRCH-03b only). Log file `/tmp/quick-srch03-followup-red.log` contains the failure for hand-off to Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Composite footer handler — close popup on panel mount (GREEN + build gate)</name>
  <files>src/components/watch/SearchEntry.tsx</files>
  <behavior>
    - Footer button onClick now executes both state mutations on the same tick: panel mount AND popup close.
    - `showPanel` still becomes `true` (panel mounts via the existing `showStructuredPanel = showPanel || forceClose` derivation at line 120 — unchanged).
    - `isPopupOpen` becomes `false` directly. The existing `useEffect` at lines 139-147 that derives `isPopupOpen` from `forceClose / debouncedQuery / isLoading / results.length` does NOT immediately reopen the popup because the user is no longer typing — the effect's dependencies haven't changed at the click moment and it will not fire again until the next render triggered by a typed character. (The user's expected next action is to interact with the now-visible StructuredEntryPanel, not the search input.)
    - The SRCH-03b RED assertion from Task 1 now turns GREEN: post-click, `role="listbox"` is no longer in the DOM (base-ui unmounts the popup when controlled `open` flips to false).
    - No other behavior changes: no new state, no new props, no className changes, no JSX restructuring. Single-line surgical fix to the arrow body.
  </behavior>
  <action>
    Edit `src/components/watch/SearchEntry.tsx` at the footer button (around lines 326-334). Replace the single line

      onClick={() => setShowPanel(true)}

    with

      onClick={() => { setShowPanel(true); setIsPopupOpen(false); }}

    Do NOT modify the className (font-semibold guardrail recurrence — `project_phase_68_complete`). Do NOT add a comment line — the change is self-documenting and the file already has extensive D-09 / SRCH-03 context comments at 320-325 which remain accurate as a placement note. Do NOT touch the `useEffect` block at 139-147 (the controlled-popup derivation is correct — it just was not being driven by the footer click before this fix). After saving:

    1. Re-run the same targeted vitest as Task 1: `npm run test -- --run src/components/watch/SearchEntry.test.tsx 2>&1 | tee /tmp/quick-srch03-followup-green.log | tail -60`. The previously-RED SRCH-03b MUST now report PASS; the full SearchEntry suite (including SRCH-03a) MUST stay green.
    2. Run the build gate: `npm run build 2>&1 | tee /tmp/quick-srch03-followup-build.log | tail -20`. Exit 0 required.
    3. Confirm no font-medium leaked: `grep -c "font-medium" src/components/watch/SearchEntry.tsx` MUST stay at its pre-change value (0 in SearchEntry.tsx for the production component code path — the recurring guardrail offender is unrelated test fixtures, NOT this file).
  </action>
  <verify>
    <automated>npm run test -- --run src/components/watch/SearchEntry.test.tsx 2>&1 | tee /tmp/quick-srch03-followup-green.log | tail -60 && npm run build 2>&1 | tee /tmp/quick-srch03-followup-build.log | tail -20 && grep -v '^//' src/components/watch/SearchEntry.tsx | grep -c "font-medium"</automated>
  </verify>
  <done>
    1. Targeted vitest reports SRCH-03b PASS (popup unmounts after footer click + panel mounts); SRCH-03a stays PASS; no other test in `SearchEntry.test.tsx` regresses. 2. `npm run build` exits 0 (build gate cleared). 3. `grep -v '^//' src/components/watch/SearchEntry.tsx | grep -c "font-medium"` returns 0 (font-semibold guardrail intact, no recurrence). Three log files (`/tmp/quick-srch03-followup-red.log`, `/tmp/quick-srch03-followup-green.log`, `/tmp/quick-srch03-followup-build.log`) document the RED→GREEN→build-clean transition.
  </done>
</task>

</tasks>

<verification>
- `npm run test -- --run src/components/watch/SearchEntry.test.tsx` — full file passes; SRCH-03a + SRCH-03b both green; no other tests regress.
- `npm run build` exits 0.
- Hand-eye scan of `src/components/watch/SearchEntry.tsx:329` confirms the composite handler is in place.
- Prod verification (footer click → popup closes → StructuredEntryPanel visible inline with pre-seeded brand/model) bundles into the v8.1 prod push per `feedback_mobile_ui_verify_on_prod` — not required to close this quick task locally.
</verification>

<success_criteria>
- Phase 72 SRCH-03 contract holds end-to-end: clicking the footer button BOTH mounts the inline panel AND closes the combobox popup.
- Regression test `SRCH-03b` now asserts both halves of the contract (panel mount + listbox absence); future structural-only refactors cannot silently regress the popup-close behavior.
- No collateral changes: no new props on SearchEntry, no className touched, no font-medium introduction, no test infrastructure changes (jsdom default + real timers, identical to existing SRCH-03 describe block).
- Build gate green; Phase 72 verification can flip from `gaps_found` to `passed` after prod confirms the bundled v8.1 push.
</success_criteria>

<output>
After completion, create `.planning/quick/260530-e55-srch-03-followup-popup-stay-open-fix/260530-e55-SUMMARY.md` capturing:
- The one-line composite-handler change at SearchEntry.tsx:329 (with the before/after).
- The one-line assertion addition at SearchEntry.test.tsx (post-line-871).
- The RED→GREEN transition log paths (`/tmp/quick-srch03-followup-{red,green,build}.log`).
- D-09 refinement note: D-09 said "keep onClick exactly as it is"; this quick task refines D-09 to "preserve the panel-mount mechanism + add the popup close" — D-09 was based on the prod-bug-incomplete diagnosis surfaced in 72-HUMAN-UAT.md.
- Hand-off note: prod verification bundles into the v8.1 prod push; expected behavior on prod = footer click visibly collapses popup AND surfaces the inline StructuredEntryPanel.
- Defer-list confirmation: WR-02 stale-comment cleanup on existing SRCH-03 test prose stays deferred (out of scope for this quick task).
</output>
