---
phase: quick-260912-jo6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/components/watch/WatchDetailTrailing.isChronometer.test.tsx
  - tests/components/watch/WatchDetail.isChronometer.test.tsx
  - src/components/watch/WatchDetail.tsx
  - tests/no-raw-img.test.ts
  - tests/static/no-legacy-watch-detail.test.ts
  - src/app/w/[ref]/page.tsx
  - src/components/notifications/MarkNotificationsSeenOnMount.tsx
  - src/components/watch/WatchPhotoSection.tsx
  - src/data/watches.ts
autonomous: true
requirements: [WR-01]

must_haves:
  truths:
    - "WR-01 (83-REVIEW): src/components/watch/WatchDetail.tsx no longer exists, so no future plan can edit the unrendered island instead of the live WatchDetailHero/WatchDetailTrailing"
    - "FEAT-08 / D-11 Certification-row coverage survives: the four isChronometer assertions (true renders row + 'Chronometer'; false/undefined/null hide it) now run against WatchDetailTrailing, the component /w/[ref] actually renders"
    - "The no-raw-img guard protects the rendered image path (WatchDetailHero.tsx + WatchPhotoSection.tsx) instead of a deleted file"
    - "A tiny static guard in tests/static/ fails the prebuild if src/components/watch/WatchDetail.tsx is re-introduced"
    - "Comments that describe the live island name WatchDetailHero (not WatchDetail); zero runtime behavior change"
    - "npm run build exits 0"
  artifacts:
    - path: "tests/components/watch/WatchDetailTrailing.isChronometer.test.tsx"
      provides: "Certification row tests against the live spec card"
      contains: "WatchDetailTrailing"
    - path: "tests/static/no-legacy-watch-detail.test.ts"
      provides: "Re-introduction guard for the deleted legacy island"
      contains: "@vitest-environment node"
    - path: "tests/no-raw-img.test.ts"
      provides: "Raw <img guard over live watch-detail image renderers"
      contains: "WatchDetailHero.tsx"
  key_links:
    - from: "tests/components/watch/WatchDetailTrailing.isChronometer.test.tsx"
      to: "src/components/watch/WatchDetailTrailing.tsx"
      via: "import { WatchDetailTrailing } from '@/components/watch/WatchDetailTrailing'"
      pattern: "from '@/components/watch/WatchDetailTrailing'"
    - from: "src/app/w/[ref]/page.tsx"
      to: "WatchDetailHero + WatchDetailTrailing"
      via: "existing imports (lines 21, 23) — unchanged"
      pattern: "components/watch/WatchDetail(Hero|Trailing)"
---

<objective>
Delete the dead legacy `src/components/watch/WatchDetail.tsx` (unrendered since Phase 64 D-02/D-09 — `/w/[ref]/page.tsx` renders `WatchDetailHero` + `WatchDetailTrailing`) and migrate every piece of test/guard coverage that still points at it onto the live components. Addresses 83-REVIEW WR-01: the dead island caused Plan 83-03 to ship an edit to nowhere.

Purpose: Remove a trap file; keep FEAT-08 Certification coverage and the no-raw-img guard protecting what users actually see.
Output: One deleted component, one deleted + one new component test, updated no-raw-img guard, one tiny static guard, comment-only fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/83-polish-sweep/83-REVIEW.md
@src/components/watch/WatchDetailTrailing.tsx
@tests/components/watch/WatchDetail.isChronometer.test.tsx
@tests/no-raw-img.test.ts

<planner_findings>
Verified by reading code (do not re-investigate):

1. **Certification row lives in `WatchDetailTrailing.tsx`** (lines ~123-135, inside the Specifications `<dl>`, `watch.isChronometer === true` guard, renders `<dt>Certification</dt>` + `<span>Chronometer</span>`). NOT in WatchDetailHero.
2. **`WatchDetailTrailing` is a pure synchronous RSC** — no `'use client'`, no hooks, no `next/navigation`, no Server Action imports. Imports only lucide-react, `@/components/ui/badge`, `@/components/ui/card`, `@/lib/constants`, `@/lib/gapFill`, `@/lib/wear`, and types. It renders directly under jsdom with `@testing-library/react` `render()`; **no `vi.mock` calls are needed**. Props: `watch: Watch`, `collection: Watch[]`, `preferences: UserPreferences`, `lastWornDate?: string | null`, `verdict?: VerdictBundle | null`.
3. **`WatchDetail.tsx` exports exactly one symbol:** `export function WatchDetail` (line 130). `WatchDetailProps` is not exported. Nothing in `src/`, `tests/`, or `scripts/` imports it except `tests/components/watch/WatchDetail.isChronometer.test.tsx`. Nothing to relocate.
4. **Live image renderers on the detail route:** `WatchDetailHero.tsx` (`import Image from 'next/image'` line 6, `<Image` ~line 229 fallback) and `WatchPhotoSection.tsx` (`import Image from 'next/image'` line 52, multiple `<Image`). Neither contains `<img `.
5. **Prebuild runs `vitest run tests/static/`** — any new file under `tests/static/` using `node:fs` MUST start with `// @vitest-environment node` (MEMORY project_vitest_static_node_env; mirror header of `tests/static/legacy-watch-routes.test.ts`). `tests/no-raw-img.test.ts` is at `tests/` root (not prebuild) — leave its environment as-is.
6. **Stale comment references (comment-only):**
   - `src/app/w/[ref]/page.tsx:66` — "the 'use client' WatchDetail island" → describes live island.
   - `src/app/w/[ref]/page.tsx:303` — "WatchDetail footer badge (CMNT-09)" → the badge renders in WatchDetailHero (`commentCount` prop, Hero lines ~301-309).
   - `src/app/w/[ref]/page.tsx:337` — "siblings of <WatchDetail/>" → live island.
   - `src/app/w/[ref]/page.tsx:343` — "WatchDetailHero replaces monolithic WatchDetail island" — historically accurate; LEAVE AS-IS.
   - `src/components/notifications/MarkNotificationsSeenOnMount.tsx:29` — "WatchDetail:87/96" cites router.refresh() sites; live equivalents are `WatchDetailHero` `handleMarkAsWorn` / `handleFlagDealChange`.
   - `src/components/watch/WatchPhotoSection.tsx:5, 8, 494` — "for WatchDetail", "single <Image> block in WatchDetail (lines 128-143)", "same as current WatchDetail fallback".
   - `src/data/watches.ts:754` — "passing signedPhotos to WatchDetail/WatchPhotoSection" (page.tsx:356 passes `signedPhotos` to WatchDetailHero).
   - `tests/components/watch/WatchDetailHero.removeCopy.test.tsx:4` — historical Phase 83 narrative; LEAVE AS-IS.
</planner_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate isChronometer Certification tests to WatchDetailTrailing; delete old test</name>
  <files>tests/components/watch/WatchDetailTrailing.isChronometer.test.tsx, tests/components/watch/WatchDetail.isChronometer.test.tsx</files>
  <read_first>tests/components/watch/WatchDetail.isChronometer.test.tsx, src/components/watch/WatchDetailTrailing.tsx (lines 40-70 props + 120-136 Certification row)</read_first>
  <action>
Create `tests/components/watch/WatchDetailTrailing.isChronometer.test.tsx` (FEAT-08 / D-11 coverage, per WR-01 migration):
- Header comment: 3-5 lines stating this migrates the Phase 23 FEAT-08/D-11 Certification-row coverage from the deleted legacy `WatchDetail.tsx` to `WatchDetailTrailing`, which is the component `/w/[ref]` renders (quick-260912-jo6). Do not copy the old "Wave 0 RED scaffold / MUST FAIL today" prose — it is obsolete.
- Imports: `describe, it, expect` from `vitest`; `render, screen` from `@testing-library/react`; `import type { Watch, UserPreferences } from '@/lib/types'`; `import { WatchDetailTrailing } from '@/components/watch/WatchDetailTrailing'`. No `vi.mock` blocks (Trailing is a pure sync RSC with no navigation/Server Action/CollectionFitCard deps — finding 2).
- Copy `baseWatch` and `basePreferences` fixtures verbatim from the old test (`status: 'owned'` keeps `computeGapFill` off).
- describe title: `'<WatchDetailTrailing> — Certification row (FEAT-08 / D-11)'`.
- Port all four cases with IDENTICAL assertions, rendering `<WatchDetailTrailing watch={...} collection={[]} preferences={basePreferences} verdict={null} />`:
  1. `isChronometer: true` → `getByText('Certification')` and `getByText('Chronometer')` in document.
  2. `isChronometer: false` → `queryByText('Certification')` and `queryByText('Chronometer')` null.
  3. `isChronometer` undefined (baseWatch) → `queryByText('Certification')` null.
  4. `isChronometer: null as unknown as boolean` (keep the legacy-row comment) → `queryByText('Certification')` null.

Then delete `tests/components/watch/WatchDetail.isChronometer.test.tsx` with `git rm`.
  </action>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && npx vitest run tests/components/watch/WatchDetailTrailing.isChronometer.test.tsx && test ! -e tests/components/watch/WatchDetail.isChronometer.test.tsx</automated>
  </verify>
  <done>New test file passes 4/4 against WatchDetailTrailing; old WatchDetail.isChronometer.test.tsx is removed; no test imports '@/components/watch/WatchDetail'.</done>
</task>

<task type="auto">
  <name>Task 2: Delete WatchDetail.tsx, repoint no-raw-img guard to live renderers, add re-introduction guard</name>
  <files>src/components/watch/WatchDetail.tsx, tests/no-raw-img.test.ts, tests/static/no-legacy-watch-detail.test.ts</files>
  <read_first>tests/no-raw-img.test.ts, tests/static/legacy-watch-routes.test.ts (first 10 lines — node-env header pattern only)</read_first>
  <action>
1. Pre-delete safety grep: confirm no importer remains — `grep -rnE "components/watch/WatchDetail['\"]" src tests scripts` must return nothing (finding 3). Then `git rm src/components/watch/WatchDetail.tsx` (WR-01).

2. Update `tests/no-raw-img.test.ts` so the guard protects the rendered path rather than just dropping the entry (finding 4):
   - In both `it` blocks replace `'src/components/watch/WatchDetail.tsx'` with two entries: `'src/components/watch/WatchDetailHero.tsx'` and `'src/components/watch/WatchPhotoSection.tsx'` (keep `WatchCard.tsx`).
   - Rename test titles: `'WatchCard, WatchDetailHero, and WatchPhotoSection have no raw <img'` and `'next/image Image component is imported in WatchCard, WatchDetailHero, and WatchPhotoSection'`.
   - Keep assertion regexes unchanged. Do not change the file's environment.

3. Create `tests/static/no-legacy-watch-detail.test.ts` — keep it tiny (~20 lines):
   - Line 1 MUST be `// @vitest-environment node` followed by a 1-2 line comment citing MEMORY project_vitest_static_node_env (prebuild runs tests/static/).
   - Short comment: WR-01 / quick-260912-jo6 — legacy `WatchDetail.tsx` was unrendered since Phase 64 and caused Plan 83-03 to edit a dead file; edit `WatchDetailHero.tsx` / `WatchDetailTrailing.tsx` instead.
   - Imports `existsSync` from `node:fs`, `join` from `node:path`, `describe/it/expect` from `vitest`.
   - One `it`: `expect(existsSync(join(process.cwd(), 'src/components/watch/WatchDetail.tsx'))).toBe(false)`. No source-text scanning (tsc already fails any import of a missing module; avoids comment-prose grep collisions).
  </action>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && test ! -e src/components/watch/WatchDetail.tsx && ! grep -rnE "components/watch/WatchDetail['\"]" src tests scripts && npx vitest run tests/no-raw-img.test.ts tests/static/no-legacy-watch-detail.test.ts tests/components/watch/WatchDetailHero.removeCopy.test.tsx</automated>
  </verify>
  <done>WatchDetail.tsx deleted; no-raw-img guard covers WatchCard + WatchDetailHero + WatchPhotoSection and passes; new static guard passes under node env; WatchDetailHero.removeCopy tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Fix stale comment references to the live island; run build gate</name>
  <files>src/app/w/[ref]/page.tsx, src/components/notifications/MarkNotificationsSeenOnMount.tsx, src/components/watch/WatchPhotoSection.tsx, src/data/watches.ts</files>
  <read_first>Each target line only via Read with offset/limit (page.tsx 60-70, 300-306, 334-346; MarkNotificationsSeenOnMount.tsx 22-32; WatchPhotoSection.tsx 1-12 and 490-497; watches.ts 748-756)</read_first>
  <action>
Comment-only edits (no code token changes; finding 6):
- `src/app/w/[ref]/page.tsx:66`: "the 'use client' WatchDetail island" → "the 'use client' WatchDetailHero island".
- `src/app/w/[ref]/page.tsx:303`: "WatchDetail footer badge" → "WatchDetailHero footer badge".
- `src/app/w/[ref]/page.tsx:337`: "siblings of <WatchDetail/>" → "siblings of <WatchDetailHero/>".
- Leave page.tsx:343 ("replaces monolithic WatchDetail island") unchanged — historically accurate.
- `src/components/notifications/MarkNotificationsSeenOnMount.tsx:29`: replace "WatchDetail:87/96" with "WatchDetailHero handleMarkAsWorn/handleFlagDealChange" (named sites instead of line numbers, which drift). Re-wrap the JSDoc lines if needed; keep ` * ` prefix.
- `src/components/watch/WatchPhotoSection.tsx:5`: "for WatchDetail." → "for WatchDetailHero."
- `src/components/watch/WatchPhotoSection.tsx:8`: "single <Image> block in WatchDetail (lines 128-143)" → "single <Image> block in the legacy WatchDetail (deleted quick-260912-jo6)" — keeps history without a dead line-number pointer.
- `src/components/watch/WatchPhotoSection.tsx:494`: "same as current WatchDetail fallback" → "same as WatchDetailHero fallback".
- `src/data/watches.ts:754`: "WatchDetail/WatchPhotoSection" → "WatchDetailHero/WatchPhotoSection".

Then run `npm run build` (authoritative gate; prebuild also runs tests/static/). Do NOT run full `tsc --noEmit` or full `vitest` as a gate — ~54 pre-existing unrelated failures (MEMORY project_baseline_not_green_build_is_gate). No local `npm run dev` walk is required: this is pure dead-code deletion + test migration + comment edits with zero runtime behavior change (CLAUDE.md Local-First exception class — no DAL/SA/RSC/UI behavior touched). Note this in the SUMMARY.

Build requires `.env.local` — run in the main working tree, not a worktree (MEMORY feedback_execute_phase_no_worktree_when_db).
  </action>
  <verify>
    <automated>cd /Users/tylerwaneka/Documents/horlo && [ "$(grep -rn 'WatchDetail\b' src | grep -v 'WatchDetailHero\|WatchDetailTrailing' | grep -v 'monolithic WatchDetail island' | grep -v 'legacy WatchDetail' | wc -l | tr -d ' ')" = "0" ] && git diff --stat -- src/app src/components/notifications src/components/watch/WatchPhotoSection.tsx src/data && npm run build</automated>
  </verify>
  <done>Only the two intentional historical mentions of bare "WatchDetail" remain in src/ (page.tsx:343, WatchPhotoSection.tsx:8); diffs in the four files are comment-only; `npm run build` exits 0 (including prebuild tests/static/).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | No runtime code path, input handling, auth, or data access changes — deletion of an unimported component plus tests and comments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-jo6-01 | Tampering | Regression: future edit lands in a re-created dead WatchDetail.tsx | mitigate | tests/static/no-legacy-watch-detail.test.ts fails prebuild if the file reappears |
| T-jo6-02 | Denial of Service | Build breaks from a missed importer of the deleted file | mitigate | Pre-delete grep in Task 2 + `npm run build` gate in Task 3 |
</threat_model>

<verification>
- `test ! -e src/components/watch/WatchDetail.tsx`
- `npx vitest run tests/components/watch/WatchDetailTrailing.isChronometer.test.tsx tests/no-raw-img.test.ts tests/static/no-legacy-watch-detail.test.ts tests/components/watch/WatchDetailHero.removeCopy.test.tsx` all pass
- `npm run build` exits 0
- No raw palette classes or `font-medium` introduced (no className changes in this plan)
</verification>

<success_criteria>
- Legacy WatchDetail.tsx and its only test consumer are gone
- FEAT-08/D-11 Certification assertions run against WatchDetailTrailing (4/4 pass)
- no-raw-img guard covers WatchDetailHero.tsx + WatchPhotoSection.tsx
- Static re-introduction guard exists with node env header and passes in prebuild
- Stale comments point at WatchDetailHero; build green
</success_criteria>

<output>
Create `.planning/quick/260912-jo6-delete-dead-legacy-src-components-watch-/260912-jo6-SUMMARY.md` when done
</output>
