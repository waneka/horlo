---
phase: quick
plan: 260413-qp3
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QP3-UI-PRICE-PROMINENCE-FILTER-COLLAPSE]
files_modified:
  - src/components/watch/WatchForm.tsx
  - src/components/watch/WatchCard.tsx
  - src/components/filters/FilterBar.tsx
  - src/store/watchStore.ts
  - tests/smoke.test.ts
must_haves:
  truths:
    - "Add/edit watch form shows Market/Retail + Paid/Target inside Basic Information (no separate Pricing card)."
    - "Form label for the pricePaid/targetPrice slot reads 'Paid' when status==='owned' and 'Target' otherwise."
    - "Unowned watch cards on the collection page display the marketPrice when it is set."
    - "Collection filter bar has a min/max market-price range control wired through getFilteredWatches()."
    - "Style, Role, Dial Color, and Price filter groups are collapsible and collapsed by default; Status is always open."
    - "All existing Vitest tests pass after every commit."
  artifacts:
    - path: "src/components/watch/WatchForm.tsx"
      provides: "Price inputs relocated into Basic Info with conditional labels"
    - path: "src/components/watch/WatchCard.tsx"
      provides: "Market price display for unowned watches"
    - path: "src/components/filters/FilterBar.tsx"
      provides: "Collapsible filter sections + price range control"
    - path: "src/store/watchStore.ts"
      provides: "priceRange filter state and matching getFilteredWatches logic"
  key_links:
    - from: "src/components/filters/FilterBar.tsx"
      to: "src/store/watchStore.ts"
      via: "setFilter('priceRange', ...)"
      pattern: "setFilter\\('priceRange'"
    - from: "src/store/watchStore.ts"
      to: "getFilteredWatches"
      via: "marketPrice min/max predicate"
      pattern: "marketPrice"
---

<objective>
Six small UI tweaks on the collection page + new/edit watch form:
1. Move price fields into the Basic Information card.
2. Conditionally label the pricePaid/targetPrice slot as "Paid" when owned and "Target" otherwise; marketPrice always labeled "Market/Retail".
3. Show marketPrice on unowned watch cards.
4. Add a min/max market-price range filter wired through the Zustand store.
5. Make filter groups collapsible (collapsed by default), except Status which stays open.
6. Any small type/store/test updates required to support the above.

Purpose: tighten the collection/form UX for the upcoming visual milestone without schema changes.
Output: six atomic commits on main, test suite green after each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/PROJECT.md
@src/lib/types.ts
@src/store/watchStore.ts
@src/components/watch/WatchForm.tsx
@src/components/watch/WatchCard.tsx
@src/components/filters/FilterBar.tsx
@src/components/filters/StatusToggle.tsx
@src/app/page.tsx

<interfaces>
Confirmed from src/lib/types.ts:

```typescript
export type WatchStatus = 'owned' | 'wishlist' | 'sold' | 'grail'

export interface Watch {
  // ...
  status: WatchStatus
  pricePaid?: number    // maps to the "Paid" label when owned
  targetPrice?: number  // maps to the "Target" label when NOT owned
  marketPrice?: number  // maps to the "Market/Retail" label always
  // ...
}
```

Confirmed from src/store/watchStore.ts:

```typescript
export interface WatchFilters {
  status: 'all' | WatchStatus
  styleTags: string[]
  roleTags: string[]
  dialColors: string[]
}
// getFilteredWatches(): Watch[] applies all filters client-side.
```

No slider primitive exists in src/components/ui (checked: badge, button, card, chart, checkbox, dialog, dropdown-menu, input, label, popover, select, sheet, tabs, textarea). Use native dual `<input type="range">` — do NOT add a new dependency.

Test runner: Vitest. Run `npm test` (or the project's configured script) to verify green after each commit. Tests live in `tests/`.
</interfaces>
</context>

<scope_boundary>
OUT OF SCOPE:
- No changes to the Watch type fields. `pricePaid` / `targetPrice` / `marketPrice` remain as-is.
- No changes to WatchDetail.tsx, insights views, or server actions.
- No new UI dependencies (no shadcn slider install, no @radix-ui/react-slider).
- No persistence of filter collapse state — local `useState` per section is sufficient.
- No changes to StatusToggle behavior. Status filter is NOT wrapped in a collapsible.
- No change to the existing "deal" badge logic on WatchCard.
- No refactor of unrelated form sections (Specifications, Complications, Style, Role, Design, Notes).
- Do not touch the filter state for mobile Sheet drawer beyond what FilterBar changes naturally propagate.
</scope_boundary>

<dependencies>
Sequencing constraints between the six tasks:

- Task 1 (move price into Basic Info) and Task 2 (conditional labels) touch the same file (WatchForm.tsx) and the same JSX region. Do Task 1 first, then Task 2 on top of it. Could be combined but keeping them separate preserves commit atomicity per the task description.
- Task 4 (price range filter wiring) MUST land before Task 5 (collapsible filter sections) if Task 5 is to include the new Price section in its collapse treatment. Order: Task 4 → Task 5.
- Task 3 (card market-price display) is independent of everything else.
- Task 6 (type/store/test cleanups) is reactive: only do what earlier tasks actually require. The store update in Task 4 likely absorbs all store-level changes; Task 6 becomes a "run the test suite + any residual tweaks" pass, and may be a no-op commit only if something truly needs fixing. If nothing needs fixing, Task 6 is skipped and we end with 5 commits — this is acceptable.

Final ordering:
1 → 2 → 3 (independent, can run anywhere) → 4 → 5 → 6 (contingent)
</dependencies>

<tasks>

<task type="auto">
  <name>Task 1: Move price fields into Basic Information card</name>
  <files>src/components/watch/WatchForm.tsx</files>
  <action>
  In `WatchForm.tsx`:
  1. Delete the entire "Pricing" `<Card>` block (the IIFE wrapping `showTargetPrice` lines ~547-618). Keep its three `<Input>` elements and their `setFormData` handlers — they move into Basic Information.
  2. In the Basic Information `<Card>` (the one titled "Basic Information"), add three new price input fields after the Image URL field (which currently has `sm:col-span-2`). Place them as the last three grid children inside the same `CardContent` grid (`grid gap-6 sm:grid-cols-2`).
  3. The three fields in order: Market Price (`marketPrice`), Paid/Target slot (`pricePaid` for now — Task 2 will make it conditional), Target Price (`targetPrice`). For THIS task, keep the existing behavior: render `pricePaid` always, render `targetPrice` only when `formData.status === 'wishlist' || formData.status === 'grail'`. Labels stay as "Price Paid", "Target Price", "Market Price" — label changes come in Task 2.
  4. Ensure the `sm:col-span-2` behavior on Image URL still looks right — remove the `sm:col-span-2` from Image URL if it would create an awkward row with the new price fields; make Image URL a normal grid item.
  5. Do not touch any other card or form section.

  Verify the form still compiles, lints, and renders in both create and edit mode (no runtime errors when toggling status).
  </action>
  <verify>
    <automated>npm run lint && npm test -- --run</automated>
  </verify>
  <done>
  - The "Pricing" Card is gone from WatchForm.tsx.
  - Market, Price Paid, and (conditionally) Target Price inputs live inside the Basic Information Card's grid.
  - `formData.pricePaid`, `formData.targetPrice`, `formData.marketPrice` still update correctly via existing setState handlers.
  - `npm run lint` passes. Vitest run passes.
  </done>
  <commit>feat(watch-form): move price fields into Basic Information card</commit>
</task>

<task type="auto">
  <name>Task 2: Conditional Paid/Target label based on ownership status</name>
  <files>src/components/watch/WatchForm.tsx</files>
  <action>
  In `WatchForm.tsx` (after Task 1):
  1. Compute `const isOwned = formData.status === 'owned'` near the top of the render.
  2. Relabel the `marketPrice` input's `<Label>` text from "Market Price" to "Market/Retail".
  3. Collapse the two inputs `pricePaid` and `targetPrice` into a single conditional field:
     - When `isOwned === true`: render ONE input bound to `formData.pricePaid` with label "Paid" and id/htmlFor `pricePaid`.
     - When `isOwned === false` (wishlist, grail, sold): render ONE input bound to `formData.targetPrice` with label "Target" and id/htmlFor `targetPrice`.
     - The underlying Watch field differs (pricePaid vs targetPrice) but the UI shows exactly ONE slot.
  4. Do NOT show both fields simultaneously. The old "wishlist/grail shows target AND paid" behavior is replaced by this strict owned-vs-unowned fork.
  5. Keep the existing `setFormData` handlers; just wire each label-branch to the right field.
  6. Sold status is unowned — confirm it falls into the `!isOwned` branch (label "Target"). This is intentional per task description ("unowned (wishlist / interested / any non-owned status)").
  7. When status changes from owned → unowned, do NOT forcibly clear `pricePaid`; leave existing data intact so re-editing is non-destructive. When status changes from unowned → owned, do NOT clear `targetPrice` either. Just show the appropriate field.
  8. Remove the now-unused `showTargetPrice` calculation if any remnant exists.

  After this task, Basic Information contains: Brand, Model, Reference, Status, Image URL, Market/Retail, and (Paid | Target).
  </action>
  <verify>
    <automated>npm run lint && npm test -- --run</automated>
  </verify>
  <done>
  - Setting Status = owned in the form shows a single field labeled "Paid" bound to `pricePaid`.
  - Setting Status = wishlist / grail / sold shows a single field labeled "Target" bound to `targetPrice`.
  - Market/Retail field is always visible.
  - No duplicate or dead pricePaid/targetPrice inputs.
  - Lint + Vitest green.
  </done>
  <commit>feat(watch-form): conditional Paid/Target label based on ownership</commit>
</task>

<task type="auto">
  <name>Task 3: Display market/retail price on unowned watch cards</name>
  <files>src/components/watch/WatchCard.tsx</files>
  <action>
  In `WatchCard.tsx`:
  1. Add a helper at the top of the component: `const isOwned = watch.status === 'owned'`.
  2. In the `<CardContent>` section, after the style tags block, add a conditional price line for unowned watches:
     ```tsx
     {!isOwned && watch.marketPrice != null && (
       <p className="mt-2 text-sm font-medium text-card-foreground">
         ${watch.marketPrice.toLocaleString()}
       </p>
     )}
     ```
     Use the existing typography tokens; no new Tailwind classes beyond what already appears in the file.
  3. Do NOT touch the owned watch rendering path — owned cards continue rendering as before. If owned cards currently show no price, leave them alone; if they do show one somewhere we didn't find, do not modify it.
  4. Keep the existing "Deal" badge logic (`autoDeal`, `isFlaggedDeal`) unchanged.
  5. The price is rendered as plain text, not a Badge, to visually distinguish price from tags.

  Make sure the card still renders when `marketPrice` is undefined (it should simply not show the price line).
  </action>
  <verify>
    <automated>npm run lint && npm test -- --run</automated>
  </verify>
  <done>
  - Unowned watch cards with a `marketPrice` show a formatted USD price line under the style tags.
  - Unowned watch cards without `marketPrice` render unchanged (no empty `$` line).
  - Owned watch cards render exactly as they did before this task.
  - Lint + Vitest green.
  </done>
  <commit>feat(watch-card): show market price on unowned cards</commit>
</task>

<task type="auto">
  <name>Task 4: Add market-price range filter to store and FilterBar</name>
  <files>src/store/watchStore.ts, src/components/filters/FilterBar.tsx</files>
  <action>
  Store changes (`src/store/watchStore.ts`):
  1. Extend `WatchFilters`:
     ```typescript
     export interface WatchFilters {
       status: 'all' | WatchStatus
       styleTags: string[]
       roleTags: string[]
       dialColors: string[]
       priceRange: { min: number | null; max: number | null }
     }
     ```
  2. Extend `defaultFilters` with `priceRange: { min: null, max: null }`.
  3. In `getFilteredWatches`, after the dial colors predicate, add a market-price predicate:
     ```typescript
     const { min, max } = filters.priceRange
     if (min != null || max != null) {
       if (watch.marketPrice == null) return false
       if (min != null && watch.marketPrice < min) return false
       if (max != null && watch.marketPrice > max) return false
     }
     ```
     Rationale: when the user sets any range bound, watches with no `marketPrice` are excluded (otherwise "filter by price" would silently keep priceless wishlist items).
  4. `setFilter` already accepts any key of `WatchFilters`, so `setFilter('priceRange', { min, max })` works without signature changes.
  5. `resetFilters` uses `defaultFilters`, so it automatically resets the range — no code change needed beyond (2).

  FilterBar changes (`src/components/filters/FilterBar.tsx`):
  6. Destructure `priceRange` from `filters`.
  7. Add a new filter section titled "Price" below Dial Color and above "Clear all filters":
     - Two number inputs OR two `<input type="range">` sliders for min and max. Per the task: "slider filter (min/max)". Use TWO native `<input type="range">` elements stacked, backed by local display state + debounced/immediate commit to the store via `setFilter('priceRange', { min, max })`.
     - Define sane bounds as constants at the top of the file: `const PRICE_MIN = 0; const PRICE_MAX = 100000; const PRICE_STEP = 100;`.
     - Show the current numeric range to the right of the label, e.g. `"$0 – $100,000"` formatted with `toLocaleString()`.
     - If `min === PRICE_MIN` AND `max === PRICE_MAX` (or either is null), the store treats it as unbounded — pass `null` for a bound when it sits at its extreme so unbounded searches don't accidentally filter out cheap or expensive watches.
     - A small "Reset" text button next to the range resets `priceRange` to `{ min: null, max: null }` via `setFilter`.
  8. Update `hasActiveFilters` to include `filters.priceRange.min != null || filters.priceRange.max != null`.
  9. Do NOT add a new dependency. Use `<input type="range">` with Tailwind styling. Two separate range inputs are acceptable — no need for a true dual-handle slider.

  Implementation detail for the range UI: keep local `useState` for the draft values mirrored from the store so drag is responsive, and commit to the store on `onPointerUp`/`onChange`. A simple `onChange → setFilter` approach is also acceptable for MVP.
  </action>
  <verify>
    <automated>npm run lint && npm test -- --run</automated>
  </verify>
  <done>
  - `WatchFilters` has `priceRange: { min: number | null; max: number | null }`.
  - `defaultFilters.priceRange = { min: null, max: null }`.
  - `getFilteredWatches` excludes watches outside `[min, max]` when either bound is set; unbounded = unchanged results.
  - FilterBar renders a Price section with two range inputs and a readable label showing the current $ bounds.
  - Adjusting the range updates `filters.priceRange` via `setFilter` and changes the grid live.
  - `hasActiveFilters` becomes true when any range bound is set.
  - Lint + Vitest green.
  </done>
  <commit>feat(filters): add market-price range filter to store and FilterBar</commit>
</task>

<task type="auto">
  <name>Task 5: Collapsible filter categories (collapsed by default, except Status)</name>
  <files>src/components/filters/FilterBar.tsx</files>
  <action>
  In `FilterBar.tsx`:
  1. Note that Status is NOT rendered by `FilterBar` — it lives in `StatusToggle` on the page header. So "exclude Status" is automatically satisfied because Status is not inside FilterBar to begin with. Verify this is still true after reading the file. Do NOT add collapsing to `StatusToggle.tsx`.
  2. Extract the inline `<div><h4>...</h4><div>...</div></div>` pattern into a small local component inside this file:
     ```tsx
     function CollapsibleSection({
       title,
       defaultOpen = false,
       children,
     }: {
       title: string
       defaultOpen?: boolean
       children: React.ReactNode
     }) {
       const [open, setOpen] = useState(defaultOpen)
       return (
         <div>
           <button
             type="button"
             onClick={() => setOpen((v) => !v)}
             className="flex w-full items-center justify-between text-sm font-semibold text-foreground mb-2"
             aria-expanded={open}
           >
             <span>{title}</span>
             <ChevronDown
               className={cn(
                 'h-4 w-4 transition-transform',
                 open ? 'rotate-180' : 'rotate-0'
               )}
               aria-hidden
             />
           </button>
           {open && <div>{children}</div>}
         </div>
       )
     }
     ```
     Import `ChevronDown` from `lucide-react` (already used elsewhere in the project, no new dep).
  3. Add `'use client'` is already present at the top — no change.
  4. Wrap each existing filter group in `<CollapsibleSection title="Style">...</CollapsibleSection>`, `<CollapsibleSection title="Role">...</CollapsibleSection>`, `<CollapsibleSection title="Dial Color">...</CollapsibleSection>`, and the new `<CollapsibleSection title="Price">...</CollapsibleSection>` from Task 4.
  5. Default all sections to `defaultOpen={false}`.
  6. Clear All Filters button stays outside collapsibles, at the bottom, rendered only when `hasActiveFilters`.
  7. Do not persist collapsed state. Local `useState` per section is explicitly fine.

  Accessibility: the button uses `aria-expanded`; the chevron uses `aria-hidden`. No popover, no animation library beyond Tailwind class transitions.
  </action>
  <verify>
    <automated>npm run lint && npm test -- --run</automated>
  </verify>
  <done>
  - FilterBar renders Style, Role, Dial Color, and Price as collapsed-by-default toggles.
  - Clicking a section header toggles visibility of its contents.
  - StatusToggle behavior is unchanged (Status is not in FilterBar).
  - Desktop sidebar and mobile Sheet both work (they both render `<FilterBar />`).
  - Lint + Vitest green.
  </done>
  <commit>feat(filters): collapsible filter sections, collapsed by default</commit>
</task>

<task type="auto">
  <name>Task 6: Residual type/store/test fixups + final green test run</name>
  <files>src/store/watchStore.ts, tests/smoke.test.ts</files>
  <action>
  Final sweep:
  1. Run `npm run lint` and `npm test -- --run`. If anything fails from Tasks 1–5, fix it here. Likely suspects:
     - A test that deep-checks the shape of `defaultFilters` and breaks on the new `priceRange` key. Update the test to expect the new key.
     - A type error in `setFilter<K extends keyof WatchFilters>` consumer that needs adjusting now that `priceRange` is a structured object.
     - An unused import (the old Pricing section imports) left behind in `WatchForm.tsx`.
  2. Grep the repo for any other consumer of `WatchFilters` or `getFilteredWatches` that needs updating (`grep -rn "WatchFilters\\|priceRange" src tests`).
  3. If NO fixes are required after Tasks 1–5, Task 6 becomes a no-op: do NOT create an empty commit. Skip the commit and stop.
  4. If fixes ARE required, commit them as a single atomic commit.

  Do not add new tests in this plan — the task description says "small type/store/test updates required to support the above", not a test-expansion exercise. Only touch tests that are failing due to earlier tasks.
  </action>
  <verify>
    <automated>npm run lint && npm test -- --run</automated>
  </verify>
  <done>
  - Full `npm run lint` passes.
  - Full `npm test -- --run` passes.
  - No dangling unused imports in WatchForm.tsx.
  - If any fixup commit was made, it is scoped and explained. If not, task is correctly skipped.
  </done>
  <commit>chore(qp3): fixups to keep tests green after price + filter refactor</commit>
</task>

</tasks>

<verification>
After all tasks:
- `npm run lint` exits 0.
- `npm test -- --run` exits 0.
- Manual smoke (user-run, not automated): open `/watch/new`, toggle status between owned and wishlist, confirm the labels swap Paid ↔ Target; confirm Market/Retail is always visible; confirm the form no longer has a separate Pricing card. Open `/`, confirm unowned cards show a price line when `marketPrice` is set; confirm filter sections start collapsed and expand on click; confirm the price range slider narrows the grid.
</verification>

<success_criteria>
- [ ] WatchForm has no separate Pricing card; prices live in Basic Information.
- [ ] Paid/Target label follows `status === 'owned'`.
- [ ] Unowned WatchCards show `marketPrice` when set.
- [ ] Store has `priceRange` filter and `getFilteredWatches` honors it.
- [ ] FilterBar has Price section and all four sections are collapsible/collapsed-by-default.
- [ ] 5 or 6 atomic commits land, lint + Vitest green after each.
</success_criteria>

<output>
No SUMMARY.md required — this is GSD quick mode. The executor should report back with the list of commit SHAs + short log of what landed.
</output>
