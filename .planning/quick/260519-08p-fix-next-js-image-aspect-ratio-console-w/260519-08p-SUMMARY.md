---
phase: quick-260519-08p
plan: 01
subsystem: search-ui, watch-ui
tags: [image, next-image, aspect-ratio, fill-pattern, console-warning]
dependency_graph:
  requires: []
  provides: [clean-dev-console, correct-image-layout]
  affects: [search-results-page, recently-evaluated-rail]
tech_stack:
  added: []
  patterns: [next-image-fill, relative-positioned-container]
key_files:
  modified:
    - src/components/search/WatchSearchRow.tsx
    - src/components/search/CollectionSearchRow.tsx
    - src/components/watch/RecentlyEvaluatedRail.tsx
decisions:
  - "Use fill + relative parent pattern (matches existing WatchCard/ProfileWatchCard/WatchDetail) rather than h-auto/w-auto which would break the fixed-size circle crop"
metrics:
  duration: ~10 minutes
  completed: 2026-05-19T07:15:41Z
  tasks_completed: 2
  files_modified: 3
---

# Phase quick-260519-08p Plan 01: Fix Next.js Image Aspect-Ratio Console Warning — Summary

**One-liner:** Converted three search/rail watch thumbnail `<Image>` usages from explicit `width`/`height` + CSS-resize to the `fill` + relative-parent pattern, eliminating the Next.js aspect-ratio console warning.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Convert WatchSearchRow + CollectionSearchRow to fill pattern | 8da0962 | src/components/search/WatchSearchRow.tsx, src/components/search/CollectionSearchRow.tsx |
| 2 | Convert RecentlyEvaluatedRail thumbnail to fill pattern, verify build | 7a23238 | src/components/watch/RecentlyEvaluatedRail.tsx |

## What Changed

### WatchSearchRow.tsx (8da0962)
- Thumbnail wrapper `div` class: added `relative` (was `size-10 md:size-12 rounded-full ...`)
- `<Image>`: replaced `width={48} height={48}` with `fill` — removed conflicting explicit dimensions

### CollectionSearchRow.tsx (8da0962)
- Matched-watch thumbnail `div` class: added `relative`
- `<Image>`: replaced `width={48} height={48}` with `fill` — removed conflicting explicit dimensions
- Inline `marginLeft` style retained (coexists with `relative`)

### RecentlyEvaluatedRail.tsx (7a23238)
- Thumbnail wrapper `span` class: added `relative`
- `<Image>`: replaced `width={20} height={20}` with `fill`, removed `w-full h-full` from className (redundant with fill)

## Verification

- TypeScript: no new errors in any of the three changed source files (pre-existing test file errors in `RecentlyEvaluatedRail.test.tsx` confirmed pre-existing before this change)
- `npm run build`: passes successfully
- Pattern consistency: all six watch image components now use the same `fill` + relative-parent pattern

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None — purely presentational markup change, no new network endpoints, auth paths, or schema changes.

## Self-Check: PASSED

- [x] `src/components/search/WatchSearchRow.tsx` modified — verified `fill` present, no `width`/`height` props
- [x] `src/components/search/CollectionSearchRow.tsx` modified — verified `fill` present, no `width`/`height` props
- [x] `src/components/watch/RecentlyEvaluatedRail.tsx` modified — verified `fill` present, no `width`/`height` or `w-full h-full`
- [x] Commit 8da0962 exists
- [x] Commit 7a23238 exists
- [x] `npm run build` passed
