---
phase: quick-260513-m31
plan: 01
subsystem: discovery-ui-polish
tags: [quick-task, ux-fix, supersession, phase-39b-uat]
requires:
  - "src/components/insights/OtherOwnersRoster.tsx (existed — Phase 39b-04)"
  - "src/components/profile/WornCalendar.tsx (existed — Phase 39b-03)"
  - "src/data/discovery.ts CatalogCollector interface (existed — Phase 39b-04)"
provides:
  - "OtherOwnersRoster: unconditional count label for totalCount >= 1 with singular/plural copy"
  - "WornCalendar: unconditional interactivity on all day cells (event-bearing OR empty, in-month OR adjacent-month)"
affects:
  - "/catalog/[catalogId] — count label now visible at totalCount=1..5 (previously hidden)"
  - "/u/[username]/stats WornCalendar — empty days now clickable via mouse + keyboard; empty-state caption user-reachable"
tech-stack:
  added: []
  patterns:
    - "Supersession via inline comment + JSDoc citation (260513-m31 references D-39b-09 reversal)"
    - "Test-only optional prop preserved alongside unconditional source path (W1 fix from 39b-03 stays intact)"
key-files:
  created:
    - "tests/components/insights/OtherOwnersRoster.test.tsx (4 assertions)"
  modified:
    - "src/components/insights/OtherOwnersRoster.tsx"
    - "src/components/profile/WornCalendar.tsx"
    - "tests/components/profile/WornCalendar.test.tsx"
decisions:
  - "Quick task 260513-m31 supersedes D-39b-09 'totalCount > 5 → suppress' rule per Phase 39b UAT test 7 product feedback — count label is useful at any non-zero count"
  - "Quick task 260513-m31 supersedes the dayEvents.length > 0 interactivity gate on WornCalendar per Phase 39b UAT test 5 — adjacent-month cells intentionally kept clickable for consistency since formatDateLabel handles arbitrary YYYY-MM-DD"
  - "W1 fix initialSelectedDate prop preserved — the existing test 3 (uses the prop) and the new test 4 (no prop, clicks empty cell) both exercise the empty-day caption path"
metrics:
  duration: ~8 min
  completed: 2026-05-13
  commits: 2
  tasks: 2
  files_changed: 4
  tests_added: 5
---

# Quick Task 260513-m31: Fix OtherOwnersRoster count label + WornCalendar empty-day clickability

**One-liner:** Reverses two Phase 39b UAT findings — D-39b-09 count-label gate (now renders for any totalCount >= 1 with singular/plural copy) and WornCalendar's `dayEvents.length > 0` interactivity gate (all day cells now clickable; empty-state caption user-reachable).

## Note for the user

This patch supersedes **D-39b-09** (shipped in Phase 39b-04, commit `bc557bb`) per your UAT test 7 product feedback that the count label is useful at any non-zero collector count. It also closes UAT test 5 by making empty WornCalendar days reachable via mouse + keyboard.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Reverse OtherOwnersRoster count-label gate + add singular/plural copy + new test file | `c3218a1` | `src/components/insights/OtherOwnersRoster.tsx`, `tests/components/insights/OtherOwnersRoster.test.tsx` (new) |
| 2 | Make all WornCalendar day cells interactive + extend tests | `5060d05` | `src/components/profile/WornCalendar.tsx`, `tests/components/profile/WornCalendar.test.tsx` |

## Acceptance Criteria Verification

| # | Criterion | Result |
|---|-----------|--------|
| 1 | `grep -v '^[[:space:]]*\*' src/components/insights/OtherOwnersRoster.tsx \| grep -v '^[[:space:]]*//' \| grep -cE 'totalCount > 5'` → 0 (gate removed from render path) | PASSED (0) |
| 2 | `grep -nE '1 collector owns this' src/components/insights/OtherOwnersRoster.tsx` → at least 1 match in JSX | PASSED (line 58) |
| 3 | `grep -cE 'const interactive' src/components/profile/WornCalendar.tsx` → 0 | PASSED (0) |
| 4 | `grep -cE 'interactive \?' src/components/profile/WornCalendar.tsx` (excluding comments) → 0 | PASSED (0) |
| 5 | `grep -cE 'role="button"' src/components/profile/WornCalendar.tsx` → >=1 | PASSED (1) |
| 6 | `npx vitest run tests/components/insights/OtherOwnersRoster.test.tsx tests/components/profile/WornCalendar.test.tsx` | PASSED (8/8: 4 + 4) |
| 7 | `npm run build` | PASSED (Compiled successfully in 5.1s; 27/27 static pages) |
| 8 | Both files reference `260513-m31` inline | PASSED (OtherOwnersRoster.tsx lines 20, 54; WornCalendar.tsx line 179) |

## Test Counts

- `tests/components/insights/OtherOwnersRoster.test.tsx` — 4 new tests (singular at totalCount=1, plural at totalCount=3 proving the >5 gate is gone, regression check at totalCount=10, hide-if-empty at collectors=[]).
- `tests/components/profile/WornCalendar.test.tsx` — 3 existing + 1 new (empty-day click without `initialSelectedDate` prop) = 4 total.
- Combined run: **8/8 green**.

## TDD Gate Compliance

Each task followed the RED → GREEN cycle:

- **Task 1 RED:** wrote 4 assertions; `npx vitest run` reported 2 failed | 2 passed (the totalCount=1 singular and totalCount=3 plural assertions failed under the existing >5 gate). Confirmed test was actually testing the gate before any source edit.
- **Task 1 GREEN:** removed the `totalCount > 5 &&` wrapper, replaced with unconditional `<p>` using `totalCount === 1 ? '1 collector owns this' : `${totalCount} collectors own this``; updated JSDoc to cite 260513-m31 supersession; ran tests → 4/4.
- **Task 2 RED:** added the empty-day-click test (no `initialSelectedDate`); `npx vitest run` reported 1 failed | 3 passed (the new test failed at `getByLabelText('View wear events for 2026-05-12')` because empty cells had no aria-label).
- **Task 2 GREEN:** deleted `const interactive` and the four `interactive ? ... : undefined` ternaries; made `role="button"`, `tabIndex={0}`, `onClick`, `onKeyDown`, `aria-label`, and the `cursor-pointer hover:bg-muted/60` class unconditional; added inline comment referencing 260513-m31 + UAT test 5 + adjacent-month rationale. Ran tests → 4/4.

Commit message convention follows the constraint exactly (`fix(quick-260513-m31)...` and `feat(quick-260513-m31)...`) rather than the TDD-canonical `test(...)` + `feat(...)` split because the quick-task workflow commits each task atomically.

## Deviations from Plan

None — plan executed exactly as written. No Rule 1/2/3 auto-fixes triggered. No new untracked files left behind. No pre-existing files outside the four-file allowlist touched.

## Known Stubs

None.

## Self-Check: PASSED

- `src/components/insights/OtherOwnersRoster.tsx` modified — FOUND
- `src/components/profile/WornCalendar.tsx` modified — FOUND
- `tests/components/insights/OtherOwnersRoster.test.tsx` created — FOUND
- `tests/components/profile/WornCalendar.test.tsx` extended (4 it() blocks) — FOUND
- Commit `c3218a1` (Task 1) — FOUND in git log
- Commit `5060d05` (Task 2) — FOUND in git log
- 8/8 tests pass — VERIFIED
- `npm run build` exit 0 — VERIFIED
