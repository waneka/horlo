// Wave 0 RED stub — Phase 77 / 77-01-PLAN.md
//
// Stub for ComposeStep — 3-button chooser (VID-01, D-04).
// Filled in during Wave 1+ implementation per 77-PATTERNS.md §ComposeStep.video.test.tsx.

import { describe, it, expect, vi } from 'vitest'

describe('ComposeStep video — 3-button chooser (VID-01, D-04)', () => {
  it.todo('VID-01: renders 3 pre-capture buttons (photo / record / upload) when supportsVideoCapture=true')
  it.todo('D-04: renders 2 pre-capture buttons (photo / upload only) when supportsVideoCapture=false — Record-3s is hidden, never disabled')

  it('stub: vitest module discovery + import resolution', () => {
    expect(typeof vi.fn).toBe('function')
  })
})
