// Wave 0 RED stub — Phase 77 / 77-01-PLAN.md
//
// Stub for WywtTile — VideoPlayBadge overlay (VID-13).
// Filled in during Wave 1+ implementation per 77-PATTERNS.md §WywtTile.video.test.tsx.

import { describe, it, expect, vi } from 'vitest'

describe('WywtTile video — VideoPlayBadge overlay (VID-13)', () => {
  it.todo('VID-13: renders the VideoPlayBadge overlay when mediaType === "video"')
  it.todo('VID-13: renders NO VideoPlayBadge when mediaType is "photo" or null/undefined')
  it.todo('VID-13: uses signedPosterUrl as the image src for video tiles (static poster + play-icon; no in-feed autoplay)')

  it('stub: vitest module discovery + import resolution', () => {
    expect(typeof vi.fn).toBe('function')
  })
})
